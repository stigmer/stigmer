/**
 * SPIKE VARIANT V2 (throwaway — not a shipped tour, excluded from the gate
 * and typecheck by living outside demos/tours/). Same timeline as the pilot;
 * only the framing differs (see index.tsx).
 */
export { createAgentTourSteps } from "../../tours/create-agent-tour/steps";
