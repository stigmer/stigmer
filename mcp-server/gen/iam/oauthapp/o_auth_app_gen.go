// Code generated by stigmer-codegen --target=mcp. DO NOT EDIT.

package oauthapp

import (
	"github.com/stigmer/stigmer/mcp-server/internal/convert"
	"github.com/stigmer/stigmer/mcp-server/proto/ai/stigmer/commons/apiresource"
	oauthappv1 "github.com/stigmer/stigmer/mcp-server/proto/ai/stigmer/iam/oauthapp/v1"
)

// OAuthAppSpec defines a registered OAuth application with an external vendor.
//
//	@internal
//	Represents Stigmer's (or a platform builder's) OAuth app registration
//	with an external vendor like Slack, Salesforce, or Figma.
//
//	Created by org admins or platform operators. Referenced by McpServer
//	resources that need vendor OAuth authentication via McpServerVendorOAuth.
//
//	Analogous to IdentityProvider (inbound auth trust), OAuthApp represents
//	outbound auth — how Stigmer authenticates with external services on
//	behalf of users.
//
//	The redirect URI is intentionally omitted: the platform derives its
//	OAuth callback URL from server configuration, not from per-app settings.
//	When registering the OAuth app with a vendor, the admin uses the
//	callback URL documented in Stigmer's deployment settings.
//
//	Security:
//	- client_secret is encrypted at rest and redacted in logs.
//	- The resource should have visibility_private to prevent secret leakage.
//	- OAuthApp does not support public visibility.
//
//	Example YAML:
//	  apiVersion: iam.stigmer.ai/v1
//	  kind: OAuthApp
//	  metadata:
//	    name: Slack OAuth
//	    slug: slack-oauth
//	    org: acme
//	  spec:
//	    provider: "Slack"
//	    client_id: "1234567890.abcdef"
//	    client_secret: "xoxs-..."
//	    authorization_url: "https://slack.com/oauth/v2/authorize"
//	    token_url: "https://slack.com/api/oauth.v2.access"
//	    scopes: ["channels:read", "chat:write"]
//	    scope_parameter_name: "user_scope"
type OAuthAppInput struct {
	// Human-readable name of the resource.
	Name string `json:"name" jsonschema:"Human-readable name of the resource."`
	// URL-friendly identifier (lowercase alphanumeric with hyphens). Auto-generated from name if omitted.
	Slug string `json:"slug,omitempty" jsonschema:"URL-friendly identifier (lowercase alphanumeric with hyphens). Auto-generated from name if omitted."`
	// Organization that owns this resource (e.g. acme).
	Org string `json:"org" jsonschema:"Organization that owns this resource (e.g. acme)."`
	// Resource visibility: PRIVATE or PUBLIC. Omit to leave unchanged on updates.
	Visibility string `json:"visibility,omitempty" jsonschema:"Resource visibility: PRIVATE or PUBLIC. Omit to leave unchanged on updates."`
	// Key-value labels for organization and filtering.
	Labels map[string]string `json:"labels,omitempty" jsonschema:"Key-value labels for organization and filtering."`
	// Tags for categorization and discovery.
	Tags []string `json:"tags,omitempty" jsonschema:"Tags for categorization and discovery."`

	// Human-readable vendor name for UI display and logging. Examples: "Slack", "Salesforce", "Figma"
	Provider string `json:"provider,omitempty" jsonschema:"Human-readable vendor name for UI display and logging. Examples: 'Slack', 'Salesforce', 'Figma'"`
	// OAuth client identifier registered with the vendor.
	ClientId string `json:"client_id,omitempty" jsonschema:"OAuth client identifier registered with the vendor."`
	// OAuth client secret registered with the vendor. Encrypted at rest, redacted in logs.
	ClientSecret string `json:"client_secret,omitempty" jsonschema:"OAuth client secret registered with the vendor. Encrypted at rest, redacted in logs."`
	// Vendor's OAuth authorization endpoint. The browser redirects here to start the authorization code flow. Example: "https://slack.com/oauth/v2/authorize"
	AuthorizationUrl string `json:"authorization_url,omitempty" jsonschema:"Vendor's OAuth authorization endpoint. The browser redirects here to start the authorization code flow. Example: 'https://slack.com/oauth/v2/authorize'"`
	// Vendor's OAuth token endpoint. The backend exchanges the authorization code for tokens here. Example: "https://slack.com/api/oauth.v2.access"
	TokenUrl string `json:"token_url,omitempty" jsonschema:"Vendor's OAuth token endpoint. The backend exchanges the authorization code for tokens here. Example: 'https://slack.com/api/oauth.v2.access'"`
	// OAuth scopes to request during the authorization flow. Scope values are vendor-specific. Examples: ["channels:read", "chat:write"] (Slack), ["read_api", "api"] (GitLab)
	Scopes []string `json:"scopes,omitempty" jsonschema:"OAuth scopes to request during the authorization flow. Scope values are vendor-specific. Examples: ['channels:read', 'chat:write'] (Slack), ['read_api', 'api'] (GitLab)"`
	// Optional OIDC UserInfo endpoint for fetching user profile data. When set, Stigmer calls this endpoint after token acquisition to retrieve the user's display name and avatar for the connected account. Omit for vendors that do not support a standard userinfo endpoint.
	UserinfoUrl string `json:"userinfo_url,omitempty" jsonschema:"Optional OIDC UserInfo endpoint for fetching user profile data. When set, Stigmer calls this endpoint after token acquisition to retrieve the user's display name and avatar for the connected account. Omit for vendors that do not support a standard userinfo endpoint."`
	// Name of the query parameter used to send scopes in the authorization URL. Most OAuth providers use the standard "scope" parameter. Some vendors use a non-standard name — for example, Slack's V2 OAuth API requires user token scopes to be sent as "user_scope" instead of "scope". When empty, defaults to "scope" (standard OAuth 2.0 behavior).
	ScopeParameterName string `json:"scope_parameter_name,omitempty" jsonschema:"Name of the query parameter used to send scopes in the authorization URL. Most OAuth providers use the standard 'scope' parameter. Some vendors use a non-standard name — for example, Slack's V2 OAuth API requires user token scopes to be sent as 'user_scope' instead of 'scope'. When empty, defaults to 'scope' (standard OAuth 2.0 behavior)."`
	// Vendor marketplace/app-review approval status for this OAuth app. Many vendors (Slack, Figma, Salesforce, etc.) require an approval process before a third-party OAuth app can be used publicly. This field tracks where Stigmer's registration stands with the vendor. UNSPECIFIED is treated as approved for backwards compatibility — only apps explicitly marked PENDING are gated in the UI.
	VendorApprovalStatus string `json:"vendor_approval_status,omitempty" jsonschema:"Vendor marketplace/app-review approval status for this OAuth app. Many vendors (Slack, Figma, Salesforce, etc.) require an approval process before a third-party OAuth app can be used publicly. This field tracks where Stigmer's registration stands with the vendor. UNSPECIFIED is treated as approved for backwards compatibility — only apps explicitly marked PENDING are gated in the UI. Allowed values: VENDOR_APPROVAL_STATUS_PENDING, VENDOR_APPROVAL_STATUS_APPROVED, VENDOR_APPROVAL_STATUS_REJECTED."`
	// Documentation URL explaining how users can bring their own OAuth app credentials or personal access tokens for this vendor while the platform's OAuth app is pending approval. Shown in the frontend as a help link when vendor_approval_status is PENDING. Empty means no documentation link is displayed.
	VendorApprovalDocsUrl string `json:"vendor_approval_docs_url,omitempty" jsonschema:"Documentation URL explaining how users can bring their own OAuth app credentials or personal access tokens for this vendor while the platform's OAuth app is pending approval. Shown in the frontend as a help link when vendor_approval_status is PENDING. Empty means no documentation link is displayed."`
}

// ToProto converts the flat MCP input into a fully-formed OAuthApp proto message.
func (input *OAuthAppInput) ToProto() (*oauthappv1.OAuthApp, error) {
	slug := input.Slug
	if slug == "" {
		slug = convert.GenerateSlug(input.Name)
	}

	spec, err := input.specToProto()
	if err != nil {
		return nil, err
	}

	return &oauthappv1.OAuthApp{
		ApiVersion: "iam.stigmer.ai/v1",
		Kind:       "OAuthApp",
		Metadata: &apiresource.ApiResourceMetadata{
			Name:       input.Name,
			Slug:       slug,
			Org:        input.Org,
			Visibility: convert.VisibilityFromString(input.Visibility),
			Labels:     input.Labels,
			Tags:       input.Tags,
		},
		Spec: spec,
	}, nil
}

func (input *OAuthAppInput) specToProto() (*oauthappv1.OAuthAppSpec, error) {
	spec := &oauthappv1.OAuthAppSpec{}

	spec.Provider = input.Provider
	spec.ClientId = input.ClientId
	spec.ClientSecret = input.ClientSecret
	spec.AuthorizationUrl = input.AuthorizationUrl
	spec.TokenUrl = input.TokenUrl
	spec.Scopes = input.Scopes
	spec.UserinfoUrl = input.UserinfoUrl
	spec.ScopeParameterName = input.ScopeParameterName
	spec.VendorApprovalStatus = oauthappv1.VendorApprovalStatus(oauthappv1.VendorApprovalStatus_value[input.VendorApprovalStatus])
	spec.VendorApprovalDocsUrl = input.VendorApprovalDocsUrl
	return spec, nil
}
