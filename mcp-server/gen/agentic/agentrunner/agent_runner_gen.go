// Code generated by stigmer-codegen --target=mcp. DO NOT EDIT.

package agentrunner

import (
	"github.com/stigmer/stigmer/mcp-server/internal/convert"
	agentrunnerv1 "github.com/stigmer/stigmer/mcp-server/proto/ai/stigmer/agentic/agentrunner/v1"
	"github.com/stigmer/stigmer/mcp-server/proto/ai/stigmer/commons/apiresource"
)

// AgentRunnerSpec defines the user-declared desired state of an agent runner.
//
//	AgentRunner follows the Kubernetes Node pattern: the spec is intentionally
//	thin because the runner is infrastructure that registers itself. The user's
//	intent when creating a runner is minimal — "I want a runner with this name."
//	All operational state (phase, queue, machine info, capacity) is reported by
//	the runner process via heartbeat and lives in AgentRunnerStatus.
//
//	For system-managed (ephemeral) runners created by the platform for cloud
//	executions, the spec is typically empty — the platform fills in metadata
//	labels (stigmer.ai/system-managed: "true") and the runner self-reports
//	everything else.
type AgentRunnerInput struct {
	// Human-readable name of the resource.
	Name string `json:"name" jsonschema:"Human-readable name of the resource."`
	// URL-friendly identifier (lowercase alphanumeric with hyphens). Auto-generated from name if omitted.
	Slug string `json:"slug,omitempty" jsonschema:"URL-friendly identifier (lowercase alphanumeric with hyphens). Auto-generated from name if omitted."`
	// Organization that owns this resource (e.g. acme).
	Org string `json:"org" jsonschema:"Organization that owns this resource (e.g. acme)."`
	// Resource visibility: PRIVATE or PUBLIC. Omit to leave unchanged on updates.
	Visibility string `json:"visibility,omitempty" jsonschema:"Resource visibility: PRIVATE or PUBLIC. Omit to leave unchanged on updates."`
	// Key-value labels for organization and filtering.
	Labels map[string]string `json:"labels,omitempty" jsonschema:"Key-value labels for organization and filtering."`
	// Tags for categorization and discovery.
	Tags []string `json:"tags,omitempty" jsonschema:"Tags for categorization and discovery."`

	// Human-readable purpose of this runner. Example: "Development runner on my MacBook", "CI/CD build server"
	Description string `json:"description,omitempty" jsonschema:"Human-readable purpose of this runner. Example: 'Development runner on my MacBook', 'CI/CD build server'"`
}

// ToProto converts the flat MCP input into a fully-formed AgentRunner proto message.
func (input *AgentRunnerInput) ToProto() (*agentrunnerv1.AgentRunner, error) {
	slug := input.Slug
	if slug == "" {
		slug = convert.GenerateSlug(input.Name)
	}

	spec, err := input.specToProto()
	if err != nil {
		return nil, err
	}

	return &agentrunnerv1.AgentRunner{
		ApiVersion: "agentic.stigmer.ai/v1",
		Kind:       "AgentRunner",
		Metadata: &apiresource.ApiResourceMetadata{
			Name:       input.Name,
			Slug:       slug,
			Org:        input.Org,
			Visibility: convert.VisibilityFromString(input.Visibility),
			Labels:     input.Labels,
			Tags:       input.Tags,
		},
		Spec: spec,
	}, nil
}

func (input *AgentRunnerInput) specToProto() (*agentrunnerv1.AgentRunnerSpec, error) {
	spec := &agentrunnerv1.AgentRunnerSpec{}

	spec.Description = input.Description
	return spec, nil
}
