// Code generated by stigmer-codegen --target=mcp. DO NOT EDIT.
// Generated: 2026-03-23T11:55:31+05:30

package skill

import (
	skillv1 "github.com/stigmer/stigmer/apis/stubs/go/ai/stigmer/agentic/skill/v1"
	"github.com/stigmer/stigmer/apis/stubs/go/ai/stigmer/commons/apiresource"
	"github.com/stigmer/stigmer/mcp-server/internal/convert"
)

// SkillSpec defines the stored desired state of a skill.
//
//	All fields are extracted from the skill artifact by the backend.
//	This follows the Kubernetes spec pattern - contains user's desired state.
//
//	Note: Source/provenance information is NOT stored here - it's in SkillStatus
//	as GitProvenance (observed state). See synth.proto for SDK input structure.
type SkillInput struct {
	// Human-readable name of the resource.
	Name string `json:"name" jsonschema:"Human-readable name of the resource."`
	// URL-friendly identifier (lowercase alphanumeric with hyphens). Auto-generated from name if omitted.
	Slug string `json:"slug,omitempty" jsonschema:"URL-friendly identifier (lowercase alphanumeric with hyphens). Auto-generated from name if omitted."`
	// Organization that owns this resource (e.g. acme).
	Org string `json:"org" jsonschema:"Organization that owns this resource (e.g. acme)."`
	// Resource visibility: PRIVATE (default) or PUBLIC.
	Visibility string `json:"visibility,omitempty" jsonschema:"Resource visibility: PRIVATE (default) or PUBLIC."`
	// Key-value labels for organization and filtering.
	Labels map[string]string `json:"labels,omitempty" jsonschema:"Key-value labels for organization and filtering."`
	// Tags for categorization and discovery.
	Tags []string `json:"tags,omitempty" jsonschema:"Tags for categorization and discovery."`

	// SKILL.md content extracted from the artifact. Contains the skill's interface definition, documentation, usage examples, and tool descriptions. This content is injected into agent prompts.
	SkillMd string `json:"skill_md,omitempty" jsonschema:"SKILL.md content extracted from the artifact. Contains the skill's interface definition, documentation, usage examples, and tool descriptions. This content is injected into agent prompts."`
	// Optional user-provided tag for this skill version. Tags are mutable pointers that can be updated to reference new versions. Examples: "stable", "v1.0", "beta", "latest" If not provided, the version will only be accessible via its immutable hash.
	Tag string `json:"tag,omitempty" jsonschema:"Optional user-provided tag for this skill version. Tags are mutable pointers that can be updated to reference new versions. Examples: 'stable', 'v1.0', 'beta', 'latest' If not provided, the version will only be accessible via its immutable hash."`
	// Human-readable description extracted from SKILL.md YAML frontmatter. Provides a concise summary of what this skill does for marketplace display and system prompt injection into AI agents. Should be 1-2 sentences, ideally under 100 tokens for prompt efficiency. Example: "Extracts text and tables from PDF files using OCR when needed"
	Description string `json:"description,omitempty" jsonschema:"Human-readable description extracted from SKILL.md YAML frontmatter. Provides a concise summary of what this skill does for marketplace display and system prompt injection into AI agents. Should be 1-2 sentences, ideally under 100 tokens for prompt efficiency. Example: 'Extracts text and tables from PDF files using OCR when needed'"`
}

// ToProto converts the flat MCP input into a fully-formed Skill proto message.
func (input *SkillInput) ToProto() (*skillv1.Skill, error) {
	slug := input.Slug
	if slug == "" {
		slug = convert.GenerateSlug(input.Name)
	}

	spec, err := input.specToProto()
	if err != nil {
		return nil, err
	}

	return &skillv1.Skill{
		ApiVersion: "agentic.stigmer.ai/v1",
		Kind:       "Skill",
		Metadata: &apiresource.ApiResourceMetadata{
			Name:       input.Name,
			Slug:       slug,
			Org:        input.Org,
			Visibility: convert.VisibilityFromString(input.Visibility),
			Labels:     input.Labels,
			Tags:       input.Tags,
		},
		Spec: spec,
	}, nil
}

func (input *SkillInput) specToProto() (*skillv1.SkillSpec, error) {
	spec := &skillv1.SkillSpec{}

	spec.SkillMd = input.SkillMd
	spec.Tag = input.Tag
	spec.Description = input.Description
	return spec, nil
}
