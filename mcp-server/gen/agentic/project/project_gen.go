// Code generated by stigmer-codegen --target=mcp. DO NOT EDIT.
// Generated: 2026-02-25T16:37:21+05:30

package project

import (
	"encoding/json"
	"fmt"
	projectv1 "github.com/stigmer/stigmer/apis/stubs/go/ai/stigmer/agentic/project/v1"
	"github.com/stigmer/stigmer/apis/stubs/go/ai/stigmer/commons/apiresource"
	"github.com/stigmer/stigmer/mcp-server/gen/agentic/agent"
	"github.com/stigmer/stigmer/mcp-server/gen/agentic/mcpserver"
	"github.com/stigmer/stigmer/mcp-server/gen/agentic/skill"
	"github.com/stigmer/stigmer/mcp-server/gen/agentic/workflow"
	"github.com/stigmer/stigmer/mcp-server/internal/convert"
	"google.golang.org/protobuf/encoding/protojson"
	"google.golang.org/protobuf/proto"
	structpb "google.golang.org/protobuf/types/known/structpb"
)

// ProjectSpec defines the configuration for a Stigmer project.
//
//	Projects use SDK synthesis - all resources are defined in code.
//
//	The SDK:
//	1. Reads the entry_point file
//	2. Executes it to generate a resource manifest
//	3. Writes manifest for CLI to apply
//
//	Note: No YAML glob patterns. Use Atomic Track (`stigmer <resource> apply`)
//	for individual YAML files. Project Track is SDK-only.
//
//	The Project is an aggregate root that contains all managed resources.
//	On apply, the backend reconciles resources to match this specification:
//	- Resources in spec are created or updated
//	- Resources removed from spec are deleted (orphan pruning)
//	- Resource dependencies are resolved in order (MCP Servers → Agents → Workflows)
type ProjectInput struct {
	// Human-readable name of the resource.
	Name string `json:"name" jsonschema:"required,description=Human-readable name of the resource."`
	// URL-friendly identifier (lowercase alphanumeric with hyphens). Auto-generated from name if omitted.
	Slug string `json:"slug,omitempty" jsonschema:"description=URL-friendly identifier (lowercase alphanumeric with hyphens). Auto-generated from name if omitted."`
	// Organization that owns this resource (e.g. acme).
	Org string `json:"org" jsonschema:"required,description=Organization that owns this resource (e.g. acme)."`
	// Resource visibility: PRIVATE (default) or PUBLIC.
	Visibility string `json:"visibility,omitempty" jsonschema:"description=Resource visibility: PRIVATE (default) or PUBLIC."`
	// Key-value labels for organization and filtering.
	Labels map[string]string `json:"labels,omitempty" jsonschema:"description=Key-value labels for organization and filtering."`
	// Tags for categorization and discovery.
	Tags []string `json:"tags,omitempty" jsonschema:"description=Tags for categorization and discovery."`

	// SDK runtime for resource synthesis. Determines how the entry_point is executed to generate the manifest. Required - must be a valid enum value (not UNSPECIFIED).
	Runtime string `json:"runtime" jsonschema:"required,enum=go|python|node,description=SDK runtime for resource synthesis. Determines how the entry_point is executed to generate the manifest. Required - must be a valid enum value (not UNSPECIFIED)."`
	// Entry point file for SDK synthesis. This file is executed to generate the resource manifest. Optional - defaults based on runtime: - go: main.go - python: main.py - node: index.ts Cross-field validation (runtime + entry_point extension consistency) is handled by the CLI validator.
	EntryPoint string `json:"entry_point,omitempty" jsonschema:"description=Entry point file for SDK synthesis. This file is executed to generate the resource manifest. Optional - defaults based on runtime: - go: main.go - python: main.py - node: index.ts Cross-field validation (runtime + entry_point extension consistency) is handled by the CLI validator."`
	// Human-readable description of the project.
	Description string `json:"description,omitempty" jsonschema:"description=Human-readable description of the project."`
	// Agents managed by this project. Each Agent must have unique metadata.name within the project. Agents may reference MCP Servers (via mcp_server_usages) and Skills (via skill_refs) - these dependencies are resolved during reconciliation. On apply: - Agents present here are created or updated - Agents removed from this list are deleted (orphan pruning) - Agent IDs are assigned by the backend and populated in the response
	Agents []agent.AgentInput `json:"agents,omitempty" jsonschema:"description=Agents managed by this project. Each Agent must have unique metadata.name within the project. Agents may reference MCP Servers (via mcp_server_usages) and Skills (via skill_refs) - these dependencies are resolved during reconciliation. On apply: - Agents present here are created or updated - Agents removed from this list are deleted (orphan pruning) - Agent IDs are assigned by the backend and populated in the response"`
	// Workflows managed by this project. Each Workflow must have unique metadata.name within the project. Workflows define multi-step orchestrations using the Zigflow DSL. On apply: - Workflows present here are created or updated - Workflows removed from this list are deleted (orphan pruning) - Workflow IDs are assigned by the backend and populated in the response
	Workflows []workflow.WorkflowInput `json:"workflows,omitempty" jsonschema:"description=Workflows managed by this project. Each Workflow must have unique metadata.name within the project. Workflows define multi-step orchestrations using the Zigflow DSL. On apply: - Workflows present here are created or updated - Workflows removed from this list are deleted (orphan pruning) - Workflow IDs are assigned by the backend and populated in the response"`
	// MCP Servers managed by this project. Each McpServer must have unique metadata.name within the project. MCP Servers are processed first during reconciliation since Agents may depend on them via mcp_server_usages. On apply: - MCP Servers present here are created or updated - MCP Servers removed from this list are deleted (orphan pruning) - Server IDs are assigned by the backend and populated in the response
	McpServers []mcpserver.McpServerInput `json:"mcp_servers,omitempty" jsonschema:"description=MCP Servers managed by this project. Each McpServer must have unique metadata.name within the project. MCP Servers are processed first during reconciliation since Agents may depend on them via mcp_server_usages. On apply: - MCP Servers present here are created or updated - MCP Servers removed from this list are deleted (orphan pruning) - Server IDs are assigned by the backend and populated in the response"`
	// Skills managed by this project. Each Skill must have unique metadata.name within the project. Skills are special: they require code to be pushed separately before project apply. The CLI handles this flow: 1. CLI pushes skill code → SkillCommandController.Push() → returns Skill with ID 2. CLI adds returned Skill to project.spec.skills 3. CLI calls ProjectCommandController.Apply() 4. Backend validates skill references exist On apply: - Skills present here are validated (code must already be pushed) - Skills removed from this list are deleted (orphan pruning)
	Skills []skill.SkillInput `json:"skills,omitempty" jsonschema:"description=Skills managed by this project. Each Skill must have unique metadata.name within the project. Skills are special: they require code to be pushed separately before project apply. The CLI handles this flow: 1. CLI pushes skill code → SkillCommandController.Push() → returns Skill with ID 2. CLI adds returned Skill to project.spec.skills 3. CLI calls ProjectCommandController.Apply() 4. Backend validates skill references exist On apply: - Skills present here are validated (code must already be pushed) - Skills removed from this list are deleted (orphan pruning)"`
}

func protoToStruct(msg proto.Message) (*structpb.Struct, error) {
	data, err := protojson.Marshal(msg)
	if err != nil {
		return nil, fmt.Errorf("protojson marshal: %w", err)
	}
	var m map[string]any
	if err := json.Unmarshal(data, &m); err != nil {
		return nil, fmt.Errorf("json unmarshal: %w", err)
	}
	return structpb.NewStruct(m)
}

// ToProto converts the flat MCP input into a fully-formed Project proto message.
func (input *ProjectInput) ToProto() (*projectv1.Project, error) {
	slug := input.Slug
	if slug == "" {
		slug = convert.GenerateSlug(input.Name)
	}

	spec, err := input.specToProto()
	if err != nil {
		return nil, err
	}

	return &projectv1.Project{
		ApiVersion: "agentic.stigmer.ai/v1",
		Kind:       "Project",
		Metadata: &apiresource.ApiResourceMetadata{
			Name:       input.Name,
			Slug:       slug,
			Org:        input.Org,
			Visibility: convert.VisibilityFromString(input.Visibility),
			Labels:     input.Labels,
			Tags:       input.Tags,
		},
		Spec: spec,
	}, nil
}

func (input *ProjectInput) specToProto() (*projectv1.ProjectSpec, error) {
	spec := &projectv1.ProjectSpec{}

	spec.Runtime = projectv1.ProjectRuntime(projectv1.ProjectRuntime_value[input.Runtime])
	spec.EntryPoint = input.EntryPoint
	spec.Description = input.Description
	for _, item := range input.Agents {
		v, err := item.ToProto()
		if err != nil {
			return nil, err
		}
		spec.Agents = append(spec.Agents, v)
	}
	for _, item := range input.Workflows {
		v, err := item.ToProto()
		if err != nil {
			return nil, err
		}
		spec.Workflows = append(spec.Workflows, v)
	}
	for _, item := range input.McpServers {
		v, err := item.ToProto()
		if err != nil {
			return nil, err
		}
		spec.McpServers = append(spec.McpServers, v)
	}
	for _, item := range input.Skills {
		v, err := item.ToProto()
		if err != nil {
			return nil, err
		}
		spec.Skills = append(spec.Skills, v)
	}
	return spec, nil
}
