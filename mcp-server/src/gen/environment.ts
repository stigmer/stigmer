// Code generated by stigmer-codegen --target=mcp-ts. DO NOT EDIT.
//
// Flattened apply-input zod schema + toProto bridge for the Environment resource.
// Source proto package: ai.stigmer.agentic.environment.v1

import { generateSlug, visibilityFromString } from "./apply-runtime.js";
import { create } from "@bufbuild/protobuf";
import { EnvironmentSchema, type Environment } from "@stigmer/protos/ai/stigmer/agentic/environment/v1/api_pb";
import { EnvironmentSpecSchema, EnvironmentValueSchema } from "@stigmer/protos/ai/stigmer/agentic/environment/v1/spec_pb";
import { ApiResourceMetadataSchema } from "@stigmer/protos/ai/stigmer/commons/apiresource/metadata_pb";
import { z } from "zod";

/** EnvironmentSpec defines the configurable properties of an environment. */
export const EnvironmentInputShape = {
  name: z.string().describe("Human-readable name of the resource."),
  slug: z.string().optional().describe("URL-friendly identifier (lowercase alphanumeric with hyphens). Auto-generated from name if omitted."),
  org: z.string().describe("Organization that owns this resource (e.g. acme)."),
  visibility: z.string().optional().describe("Resource visibility: PRIVATE or PUBLIC. Omit to leave unchanged on updates."),
  labels: z.record(z.string()).optional().describe("Key-value labels for organization and filtering."),
  tags: z.array(z.string()).optional().describe("Tags for categorization and discovery."),
  description: z.string().optional().describe("Human-readable description for UI and listing display."),
  data: z.record(z.lazy(() => EnvironmentValueInputSchema)).optional().describe("Key-value pairs containing configuration and secrets. Each value includes a flag indicating whether it is a secret."),
} as const;

export const EnvironmentInputSchema = z.object(EnvironmentInputShape);
export type EnvironmentInput = z.infer<typeof EnvironmentInputSchema>;

const EnvironmentValueInputSchema = z.object({
  value: z.string().optional().describe("The configuration or secret string."),
  is_secret: z.boolean().optional().describe("Whether this value should be treated as a secret."),
  description: z.string().optional().describe("Human-readable description of what this value is used for."),
});
type EnvironmentValueInput = z.infer<typeof EnvironmentValueInputSchema>;


/** Build the fully-formed Environment proto from the flat MCP apply input. */
export function environmentInputToProto(input: EnvironmentInput): Environment {
  const slug = input.slug && input.slug.length > 0 ? input.slug : generateSlug(input.name);
  const spec = create(EnvironmentSpecSchema);
  if (input.description !== undefined) spec.description = input.description;
  if (input.data !== undefined) {
    for (const [k, v] of Object.entries(input.data)) spec.data[k] = environmentValueInputToProto(v);
  }
  return Object.assign(create(EnvironmentSchema), {
    apiVersion: "agentic.stigmer.ai/v1",
    kind: "Environment",
    metadata: Object.assign(create(ApiResourceMetadataSchema), {
      name: input.name,
      slug,
      org: input.org,
      ...(input.visibility !== undefined && { visibility: visibilityFromString(input.visibility) }),
      ...(input.labels !== undefined && { labels: input.labels }),
      ...(input.tags !== undefined && { tags: input.tags }),
    }),
    spec,
  }) as Environment;
}

function environmentValueInputToProto(input: EnvironmentValueInput) {
  const result = create(EnvironmentValueSchema);
  if (input.value !== undefined) result.value = input.value;
  if (input.is_secret !== undefined) result.isSecret = input.is_secret;
  if (input.description !== undefined) result.description = input.description;
  return result;
}

