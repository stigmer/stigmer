// Code generated by protoc-gen-go-grpc. DO NOT EDIT.
// versions:
// - protoc-gen-go-grpc v1.5.1
// - protoc             (unknown)
// source: ai/stigmer/agentic/agentchannel/v1/message_command.proto

package agentchannelv1

import (
	context "context"
	grpc "google.golang.org/grpc"
	codes "google.golang.org/grpc/codes"
	status "google.golang.org/grpc/status"
)

// This is a compile-time assertion to ensure that this generated file
// is compatible with the grpc package it is being compiled against.
// Requires gRPC-Go v1.64.0 or later.
const _ = grpc.SupportPackageIsVersion9

const (
	ChannelMessageCommandController_SendMessage_FullMethodName = "/ai.stigmer.agentic.agentchannel.v1.ChannelMessageCommandController/sendMessage"
)

// ChannelMessageCommandControllerClient is the client API for ChannelMessageCommandController service.
//
// For semantics around ctx use and closing/ending streaming RPCs, please refer to https://pkg.go.dev/google.golang.org/grpc/?tab=doc#ClientConn.NewStream.
//
// ChannelMessageCommandController handles business-initiated outbound
// messages on agent channels.
//
// @internal
// proactive-messaging DD-002 D2: the runtime messaging surface beside
// the AgentChannel resource controllers — the
// DatastoreRecordCommandController-beside-DatastoreCommandController
// split, so resource CRUD and runtime traffic never mix. Dual-audience
// by token-class dispatch (the RecordReach shape): the agent's
// send_channel_message tool calls with a session-scoped sandbox token;
// direct principals (console, CLI, SDK) call with their own identity.
type ChannelMessageCommandControllerClient interface {
	// Send a business-initiated message to a recipient on an agent channel.
	//
	// The message is durably recorded and attempted once inline; transient
	// failures are retried in the background. The outcome reports the
	// truth of the inline attempt.
	//
	// @internal
	// Authorization in-handler (DD-002 D2/D6/D9): token-class dispatch,
	// fail closed; agent-anchored chain (never session-sender-anchored —
	// the recipient is an argument). Error contract (DD-002 D4): unknown
	// or foreign token class, no serving channel, proactive messaging not
	// enabled, or channel/org mismatch → PERMISSION_DENIED with no policy
	// detail leaked; malformed input or ambiguous channel/language →
	// INVALID_ARGUMENT with the candidates listed; channel not installed →
	// FAILED_PRECONDITION; rate caps, recipient policy, and provider
	// refusals → outcome=refused; transient provider failures →
	// outcome=queued. Cloud-first runtime: the OSS edition returns
	// FAILED_PRECONDITION (decision 001 D-g posture, the initiateInstall
	// precedent).
	SendMessage(ctx context.Context, in *SendChannelMessageInput, opts ...grpc.CallOption) (*SendChannelMessageOutput, error)
}

type channelMessageCommandControllerClient struct {
	cc grpc.ClientConnInterface
}

func NewChannelMessageCommandControllerClient(cc grpc.ClientConnInterface) ChannelMessageCommandControllerClient {
	return &channelMessageCommandControllerClient{cc}
}

func (c *channelMessageCommandControllerClient) SendMessage(ctx context.Context, in *SendChannelMessageInput, opts ...grpc.CallOption) (*SendChannelMessageOutput, error) {
	cOpts := append([]grpc.CallOption{grpc.StaticMethod()}, opts...)
	out := new(SendChannelMessageOutput)
	err := c.cc.Invoke(ctx, ChannelMessageCommandController_SendMessage_FullMethodName, in, out, cOpts...)
	if err != nil {
		return nil, err
	}
	return out, nil
}

// ChannelMessageCommandControllerServer is the server API for ChannelMessageCommandController service.
// All implementations should embed UnimplementedChannelMessageCommandControllerServer
// for forward compatibility.
//
// ChannelMessageCommandController handles business-initiated outbound
// messages on agent channels.
//
// @internal
// proactive-messaging DD-002 D2: the runtime messaging surface beside
// the AgentChannel resource controllers — the
// DatastoreRecordCommandController-beside-DatastoreCommandController
// split, so resource CRUD and runtime traffic never mix. Dual-audience
// by token-class dispatch (the RecordReach shape): the agent's
// send_channel_message tool calls with a session-scoped sandbox token;
// direct principals (console, CLI, SDK) call with their own identity.
type ChannelMessageCommandControllerServer interface {
	// Send a business-initiated message to a recipient on an agent channel.
	//
	// The message is durably recorded and attempted once inline; transient
	// failures are retried in the background. The outcome reports the
	// truth of the inline attempt.
	//
	// @internal
	// Authorization in-handler (DD-002 D2/D6/D9): token-class dispatch,
	// fail closed; agent-anchored chain (never session-sender-anchored —
	// the recipient is an argument). Error contract (DD-002 D4): unknown
	// or foreign token class, no serving channel, proactive messaging not
	// enabled, or channel/org mismatch → PERMISSION_DENIED with no policy
	// detail leaked; malformed input or ambiguous channel/language →
	// INVALID_ARGUMENT with the candidates listed; channel not installed →
	// FAILED_PRECONDITION; rate caps, recipient policy, and provider
	// refusals → outcome=refused; transient provider failures →
	// outcome=queued. Cloud-first runtime: the OSS edition returns
	// FAILED_PRECONDITION (decision 001 D-g posture, the initiateInstall
	// precedent).
	SendMessage(context.Context, *SendChannelMessageInput) (*SendChannelMessageOutput, error)
}

// UnimplementedChannelMessageCommandControllerServer should be embedded to have
// forward compatible implementations.
//
// NOTE: this should be embedded by value instead of pointer to avoid a nil
// pointer dereference when methods are called.
type UnimplementedChannelMessageCommandControllerServer struct{}

func (UnimplementedChannelMessageCommandControllerServer) SendMessage(context.Context, *SendChannelMessageInput) (*SendChannelMessageOutput, error) {
	return nil, status.Errorf(codes.Unimplemented, "method SendMessage not implemented")
}
func (UnimplementedChannelMessageCommandControllerServer) testEmbeddedByValue() {}

// UnsafeChannelMessageCommandControllerServer may be embedded to opt out of forward compatibility for this service.
// Use of this interface is not recommended, as added methods to ChannelMessageCommandControllerServer will
// result in compilation errors.
type UnsafeChannelMessageCommandControllerServer interface {
	mustEmbedUnimplementedChannelMessageCommandControllerServer()
}

func RegisterChannelMessageCommandControllerServer(s grpc.ServiceRegistrar, srv ChannelMessageCommandControllerServer) {
	// If the following call pancis, it indicates UnimplementedChannelMessageCommandControllerServer was
	// embedded by pointer and is nil.  This will cause panics if an
	// unimplemented method is ever invoked, so we test this at initialization
	// time to prevent it from happening at runtime later due to I/O.
	if t, ok := srv.(interface{ testEmbeddedByValue() }); ok {
		t.testEmbeddedByValue()
	}
	s.RegisterService(&ChannelMessageCommandController_ServiceDesc, srv)
}

func _ChannelMessageCommandController_SendMessage_Handler(srv interface{}, ctx context.Context, dec func(interface{}) error, interceptor grpc.UnaryServerInterceptor) (interface{}, error) {
	in := new(SendChannelMessageInput)
	if err := dec(in); err != nil {
		return nil, err
	}
	if interceptor == nil {
		return srv.(ChannelMessageCommandControllerServer).SendMessage(ctx, in)
	}
	info := &grpc.UnaryServerInfo{
		Server:     srv,
		FullMethod: ChannelMessageCommandController_SendMessage_FullMethodName,
	}
	handler := func(ctx context.Context, req interface{}) (interface{}, error) {
		return srv.(ChannelMessageCommandControllerServer).SendMessage(ctx, req.(*SendChannelMessageInput))
	}
	return interceptor(ctx, in, info, handler)
}

// ChannelMessageCommandController_ServiceDesc is the grpc.ServiceDesc for ChannelMessageCommandController service.
// It's only intended for direct use with grpc.RegisterService,
// and not to be introspected or modified (even as a copy)
var ChannelMessageCommandController_ServiceDesc = grpc.ServiceDesc{
	ServiceName: "ai.stigmer.agentic.agentchannel.v1.ChannelMessageCommandController",
	HandlerType: (*ChannelMessageCommandControllerServer)(nil),
	Methods: []grpc.MethodDesc{
		{
			MethodName: "sendMessage",
			Handler:    _ChannelMessageCommandController_SendMessage_Handler,
		},
	},
	Streams:  []grpc.StreamDesc{},
	Metadata: "ai/stigmer/agentic/agentchannel/v1/message_command.proto",
}
