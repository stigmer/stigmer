# Generated by the gRPC Python protocol compiler plugin. DO NOT EDIT!
"""Client and server classes corresponding to protobuf-defined services."""
import grpc

from ai.stigmer.iam.platformclient.v1 import token_pb2 as ai_dot_stigmer_dot_iam_dot_platformclient_dot_v1_dot_token__pb2


class PlatformClientTokenControllerStub(object):
    """PlatformClientTokenController provides token-minting operations for
    platform builders embedding Stigmer into their products.

    This service is distinct from the CRUD controllers — it does not manage
    a resource lifecycle. Instead, it issues Stigmer-signed JWTs on behalf
    of platform builder users, authenticated via PlatformClient credentials
    (client_id + client_secret).

    The minted JWT is signed by Stigmer's own key pair (not Auth0). The auth
    chain validates these tokens via a dedicated PlatformClientTokenAuthenticationProvider
    that checks the Stigmer-issued signature and resolves the identity account.
    """

    def __init__(self, channel):
        """Constructor.

        Args:
            channel: A grpc.Channel.
        """
        self.mintUserToken = channel.unary_unary(
                '/ai.stigmer.iam.platformclient.v1.PlatformClientTokenController/mintUserToken',
                request_serializer=ai_dot_stigmer_dot_iam_dot_platformclient_dot_v1_dot_token__pb2.MintUserTokenRequest.SerializeToString,
                response_deserializer=ai_dot_stigmer_dot_iam_dot_platformclient_dot_v1_dot_token__pb2.MintUserTokenResponse.FromString,
                _registered_method=True)


class PlatformClientTokenControllerServicer(object):
    """PlatformClientTokenController provides token-minting operations for
    platform builders embedding Stigmer into their products.

    This service is distinct from the CRUD controllers — it does not manage
    a resource lifecycle. Instead, it issues Stigmer-signed JWTs on behalf
    of platform builder users, authenticated via PlatformClient credentials
    (client_id + client_secret).

    The minted JWT is signed by Stigmer's own key pair (not Auth0). The auth
    chain validates these tokens via a dedicated PlatformClientTokenAuthenticationProvider
    that checks the Stigmer-issued signature and resolves the identity account.
    """

    def mintUserToken(self, request, context):
        """Mint a user-scoped JWT for browser-based access to Stigmer resources.

        The platform builder's backend calls this RPC with its PlatformClient
        credentials (client_id + client_secret) and the end user's identity.
        Stigmer validates the credentials, optionally JIT-provisions the user's
        identity account, and returns a Stigmer-signed JWT.

        The returned JWT can be used by the React SDK (via StigmerProvider's
        getAccessToken callback) to authenticate API calls from the browser.

        Authentication flow:
        1. Validate client_id + client_secret against stored hash
        2. Resolve or JIT-provision the identity account for user_id
        3. If auto_grant_on_org is enabled, grant the configured role
        4. Sign a JWT with Stigmer's private key containing the user's identity

        Error scenarios:
        - UNAUTHENTICATED: Invalid client_id or client_secret
        - NOT_FOUND: user_id does not exist and auto_provision_accounts is false
        - FAILED_PRECONDITION: PlatformClient secret has expired
        - PERMISSION_DENIED: Origin not in allowed_origins (when configured)

        @internal
        This RPC is public — no Bearer token is required. The caller authenticates
        by providing client_id + client_secret in the request body. The handler
        validates these credentials as business logic, not via the auth interceptor.
        """
        context.set_code(grpc.StatusCode.UNIMPLEMENTED)
        context.set_details('Method not implemented!')
        raise NotImplementedError('Method not implemented!')


def add_PlatformClientTokenControllerServicer_to_server(servicer, server):
    rpc_method_handlers = {
            'mintUserToken': grpc.unary_unary_rpc_method_handler(
                    servicer.mintUserToken,
                    request_deserializer=ai_dot_stigmer_dot_iam_dot_platformclient_dot_v1_dot_token__pb2.MintUserTokenRequest.FromString,
                    response_serializer=ai_dot_stigmer_dot_iam_dot_platformclient_dot_v1_dot_token__pb2.MintUserTokenResponse.SerializeToString,
            ),
    }
    generic_handler = grpc.method_handlers_generic_handler(
            'ai.stigmer.iam.platformclient.v1.PlatformClientTokenController', rpc_method_handlers)
    server.add_generic_rpc_handlers((generic_handler,))
    server.add_registered_method_handlers('ai.stigmer.iam.platformclient.v1.PlatformClientTokenController', rpc_method_handlers)


 # This class is part of an EXPERIMENTAL API.
class PlatformClientTokenController(object):
    """PlatformClientTokenController provides token-minting operations for
    platform builders embedding Stigmer into their products.

    This service is distinct from the CRUD controllers — it does not manage
    a resource lifecycle. Instead, it issues Stigmer-signed JWTs on behalf
    of platform builder users, authenticated via PlatformClient credentials
    (client_id + client_secret).

    The minted JWT is signed by Stigmer's own key pair (not Auth0). The auth
    chain validates these tokens via a dedicated PlatformClientTokenAuthenticationProvider
    that checks the Stigmer-issued signature and resolves the identity account.
    """

    @staticmethod
    def mintUserToken(request,
            target,
            options=(),
            channel_credentials=None,
            call_credentials=None,
            insecure=False,
            compression=None,
            wait_for_ready=None,
            timeout=None,
            metadata=None):
        return grpc.experimental.unary_unary(
            request,
            target,
            '/ai.stigmer.iam.platformclient.v1.PlatformClientTokenController/mintUserToken',
            ai_dot_stigmer_dot_iam_dot_platformclient_dot_v1_dot_token__pb2.MintUserTokenRequest.SerializeToString,
            ai_dot_stigmer_dot_iam_dot_platformclient_dot_v1_dot_token__pb2.MintUserTokenResponse.FromString,
            options,
            channel_credentials,
            insecure,
            call_credentials,
            compression,
            wait_for_ready,
            timeout,
            metadata,
            _registered_method=True)
