// Generated by the protocol buffer compiler.  DO NOT EDIT!
// NO CHECKED-IN PROTOBUF GENCODE
// source: ai/stigmer/agentic/agentexecution/v1/invocation.proto
// Protobuf Java Version: 4.34.0

package ai.stigmer.agentic.agentexecution.v1;

/**
 * <pre>
 * AgentInvocation is what a caller may ask an agent to do.
 *
 * It is the owner-settable subset of an agent run — the interactive
 * composer's vocabulary (agent, message, harness, workspace,
 * environments, run bounds) minus what an unattended surface makes
 * structurally impossible. Surfaces that trigger agents on someone's
 * behalf embed this message instead of re-deriving the shape:
 * schedules embed it whole; the workflow agent_call task adopts it at
 * the TYPE level (embedding RunConfig and documenting a field-by-field
 * correspondence) because its task config is a kind+Struct authoring
 * DSL whose field names are the YAML keys — see
 * workflow/v1/tasks/agent_call.proto; channels embed RunConfig.
 *
 * &#64;internal
 * Project DD-018 (whatsapp-proactive-messaging). An ALLOWLIST by
 * construction: platform-owned execution fields (approval_mode,
 * execution_target, callback_token, harness_state_id,
 * activity_task_queue, auto_approve_all, org/labels/subject) cannot
 * appear here, so embedding surfaces never need a blocklist validator
 * — the rejected alternative was embedding AgentExecutionSpec /
 * SessionSpec and validating the illegal fields away. Exclusions,
 * each with its reason (DD-018 D-1): runtime_env (plaintext secrets
 * in manifests leak; environments are the vehicle), attachments
 * (one-shot uploads don't recur), mcp_server_usages/skill_refs (the
 * agent blueprint owns its tool set), interaction_mode (Plan mode's
 * deliverable is a plan for a human who isn't there), cursor_mode
 * (runner-owned constant). Surface-specific constraints live in the
 * embedding surface's handlers, never here — e.g. schedules reject
 * local_path workspace sources and enforce agent_ref.org ==
 * metadata.org.
 * </pre>
 *
 * Protobuf type {@code ai.stigmer.agentic.agentexecution.v1.AgentInvocation}
 */
@com.google.protobuf.Generated
public final class AgentInvocation extends
    com.google.protobuf.GeneratedMessage implements
    // @@protoc_insertion_point(message_implements:ai.stigmer.agentic.agentexecution.v1.AgentInvocation)
    AgentInvocationOrBuilder {
private static final long serialVersionUID = 0L;
  static {
    com.google.protobuf.RuntimeVersion.validateProtobufGencodeVersion(
      com.google.protobuf.RuntimeVersion.RuntimeDomain.PUBLIC,
      /* major= */ 4,
      /* minor= */ 34,
      /* patch= */ 0,
      /* suffix= */ "",
      "AgentInvocation");
  }
  // Use AgentInvocation.newBuilder() to construct.
  private AgentInvocation(com.google.protobuf.GeneratedMessage.Builder<?> builder) {
    super(builder);
  }
  private AgentInvocation() {
    message_ = "";
    harness_ = 0;
    workspaceEntries_ = java.util.Collections.emptyList();
    environmentRefs_ = java.util.Collections.emptyList();
  }

  public static final com.google.protobuf.Descriptors.Descriptor
      getDescriptor() {
    return ai.stigmer.agentic.agentexecution.v1.InvocationProto.internal_static_ai_stigmer_agentic_agentexecution_v1_AgentInvocation_descriptor;
  }

  @java.lang.Override
  public com.google.protobuf.Descriptors.Descriptor getDescriptorForType() {
    return ai.stigmer.agentic.agentexecution.v1.InvocationProto.internal_static_ai_stigmer_agentic_agentexecution_v1_AgentInvocation_descriptor;
  }

  @java.lang.Override
  protected com.google.protobuf.GeneratedMessage.FieldAccessorTable
      internalGetFieldAccessorTable() {
    return ai.stigmer.agentic.agentexecution.v1.InvocationProto.internal_static_ai_stigmer_agentic_agentexecution_v1_AgentInvocation_fieldAccessorTable
        .ensureFieldAccessorsInitialized(
            ai.stigmer.agentic.agentexecution.v1.AgentInvocation.class, ai.stigmer.agentic.agentexecution.v1.AgentInvocation.Builder.class);
  }

  private int bitField0_;
  public static final int AGENT_REF_FIELD_NUMBER = 1;
  private ai.stigmer.commons.apiresource.ApiResourceReference agentRef_;
  /**
   * <pre>
   * Reference to the agent to run.
   *
   * &#64;internal
   * Embedding surfaces own the referential invariants: the schedule
   * requires agent_ref.org == metadata.org (the owning org is the
   * billing org for every run) and can_edit on this agent at create
   * (DD-009 C-6). Deletion of the agent does not cascade (house
   * convention): a dangling reference surfaces at run time as a
   * failed start, never as a silent stall.
   * </pre>
   *
   * <code>.ai.stigmer.commons.apiresource.ApiResourceReference agent_ref = 1 [json_name = "agentRef", (.buf.validate.field) = { ... }</code>
   * @return Whether the agentRef field is set.
   */
  @java.lang.Override
  public boolean hasAgentRef() {
    return ((bitField0_ & 0x00000001) != 0);
  }
  /**
   * <pre>
   * Reference to the agent to run.
   *
   * &#64;internal
   * Embedding surfaces own the referential invariants: the schedule
   * requires agent_ref.org == metadata.org (the owning org is the
   * billing org for every run) and can_edit on this agent at create
   * (DD-009 C-6). Deletion of the agent does not cascade (house
   * convention): a dangling reference surfaces at run time as a
   * failed start, never as a silent stall.
   * </pre>
   *
   * <code>.ai.stigmer.commons.apiresource.ApiResourceReference agent_ref = 1 [json_name = "agentRef", (.buf.validate.field) = { ... }</code>
   * @return The agentRef.
   */
  @java.lang.Override
  public ai.stigmer.commons.apiresource.ApiResourceReference getAgentRef() {
    return agentRef_ == null ? ai.stigmer.commons.apiresource.ApiResourceReference.getDefaultInstance() : agentRef_;
  }
  /**
   * <pre>
   * Reference to the agent to run.
   *
   * &#64;internal
   * Embedding surfaces own the referential invariants: the schedule
   * requires agent_ref.org == metadata.org (the owning org is the
   * billing org for every run) and can_edit on this agent at create
   * (DD-009 C-6). Deletion of the agent does not cascade (house
   * convention): a dangling reference surfaces at run time as a
   * failed start, never as a silent stall.
   * </pre>
   *
   * <code>.ai.stigmer.commons.apiresource.ApiResourceReference agent_ref = 1 [json_name = "agentRef", (.buf.validate.field) = { ... }</code>
   */
  @java.lang.Override
  public ai.stigmer.commons.apiresource.ApiResourceReferenceOrBuilder getAgentRefOrBuilder() {
    return agentRef_ == null ? ai.stigmer.commons.apiresource.ApiResourceReference.getDefaultInstance() : agentRef_;
  }

  public static final int MESSAGE_FIELD_NUMBER = 2;
  @SuppressWarnings("serial")
  private volatile java.lang.Object message_ = "";
  /**
   * <pre>
   * Prompt the run starts from.
   *
   * &#64;internal
   * DD-008 D5: the runner injects no current date into any prompt, so
   * unattended surfaces compose this message plus a fire-context line.
   * The bound applies to the stored prompt, not the composed message.
   * </pre>
   *
   * <code>string message = 2 [json_name = "message", (.buf.validate.field) = { ... }</code>
   * @return The message.
   */
  @java.lang.Override
  public java.lang.String getMessage() {
    java.lang.Object ref = message_;
    if (ref instanceof java.lang.String) {
      return (java.lang.String) ref;
    } else {
      com.google.protobuf.ByteString bs = 
          (com.google.protobuf.ByteString) ref;
      java.lang.String s = bs.toStringUtf8();
      message_ = s;
      return s;
    }
  }
  /**
   * <pre>
   * Prompt the run starts from.
   *
   * &#64;internal
   * DD-008 D5: the runner injects no current date into any prompt, so
   * unattended surfaces compose this message plus a fire-context line.
   * The bound applies to the stored prompt, not the composed message.
   * </pre>
   *
   * <code>string message = 2 [json_name = "message", (.buf.validate.field) = { ... }</code>
   * @return The bytes for message.
   */
  @java.lang.Override
  public com.google.protobuf.ByteString
      getMessageBytes() {
    java.lang.Object ref = message_;
    if (ref instanceof java.lang.String) {
      com.google.protobuf.ByteString b = 
          com.google.protobuf.ByteString.copyFromUtf8(
              (java.lang.String) ref);
      message_ = b;
      return b;
    } else {
      return (com.google.protobuf.ByteString) ref;
    }
  }

  public static final int HARNESS_FIELD_NUMBER = 3;
  private int harness_ = 0;
  /**
   * <pre>
   * Execution engine for the run's session. Unspecified inherits the
   * embedding surface's platform default.
   *
   * &#64;internal
   * DD-018 D-1: graduates from surface platform config (e.g.
   * stigmer.schedules.session-defaults.harness) to the invocation —
   * the config demotes from "the only source" to "the default".
   * Verified before deciding: unattended approval semantics are one
   * shared contract across both harnesses (approval-policy.ts), so
   * owner harness choice changes the engine, never the safety
   * posture. cursor_mode stays runner-owned and is deliberately
   * absent.
   * </pre>
   *
   * <code>.ai.stigmer.agentic.session.v1.Harness harness = 3 [json_name = "harness"];</code>
   * @return The enum numeric value on the wire for harness.
   */
  @java.lang.Override public int getHarnessValue() {
    return harness_;
  }
  /**
   * <pre>
   * Execution engine for the run's session. Unspecified inherits the
   * embedding surface's platform default.
   *
   * &#64;internal
   * DD-018 D-1: graduates from surface platform config (e.g.
   * stigmer.schedules.session-defaults.harness) to the invocation —
   * the config demotes from "the only source" to "the default".
   * Verified before deciding: unattended approval semantics are one
   * shared contract across both harnesses (approval-policy.ts), so
   * owner harness choice changes the engine, never the safety
   * posture. cursor_mode stays runner-owned and is deliberately
   * absent.
   * </pre>
   *
   * <code>.ai.stigmer.agentic.session.v1.Harness harness = 3 [json_name = "harness"];</code>
   * @return The harness.
   */
  @java.lang.Override public ai.stigmer.agentic.session.v1.Harness getHarness() {
    ai.stigmer.agentic.session.v1.Harness result = ai.stigmer.agentic.session.v1.Harness.forNumber(harness_);
    return result == null ? ai.stigmer.agentic.session.v1.Harness.UNRECOGNIZED : result;
  }

  public static final int WORKSPACE_ENTRIES_FIELD_NUMBER = 4;
  @SuppressWarnings("serial")
  private java.util.List<ai.stigmer.agentic.session.v1.WorkspaceEntry> workspaceEntries_;
  /**
   * <pre>
   * Workspace the run's session operates on. Empty means no workspace.
   *
   * &#64;internal
   * Maps onto SessionSpec.workspace_entries of the session each run
   * creates. Unattended surfaces constrain sources in their handlers:
   * schedules accept git_repo only (no client is connected at fire
   * time to serve a local_path). Credentials (DD-018 D-4): the
   * provisioner resolves GITHUB_TOKEN from the merged environment;
   * for surfaces with no interactive caller the one supported
   * contract is an org-visibility Environment holding GITHUB_TOKEN
   * bound via environment_refs — the personal-environment fallback
   * resolves as-caller and is structurally closed to synthetic
   * accounts. Public repos need no token.
   * </pre>
   *
   * <code>repeated .ai.stigmer.agentic.session.v1.WorkspaceEntry workspace_entries = 4 [json_name = "workspaceEntries"];</code>
   */
  @java.lang.Override
  public java.util.List<ai.stigmer.agentic.session.v1.WorkspaceEntry> getWorkspaceEntriesList() {
    return workspaceEntries_;
  }
  /**
   * <pre>
   * Workspace the run's session operates on. Empty means no workspace.
   *
   * &#64;internal
   * Maps onto SessionSpec.workspace_entries of the session each run
   * creates. Unattended surfaces constrain sources in their handlers:
   * schedules accept git_repo only (no client is connected at fire
   * time to serve a local_path). Credentials (DD-018 D-4): the
   * provisioner resolves GITHUB_TOKEN from the merged environment;
   * for surfaces with no interactive caller the one supported
   * contract is an org-visibility Environment holding GITHUB_TOKEN
   * bound via environment_refs — the personal-environment fallback
   * resolves as-caller and is structurally closed to synthetic
   * accounts. Public repos need no token.
   * </pre>
   *
   * <code>repeated .ai.stigmer.agentic.session.v1.WorkspaceEntry workspace_entries = 4 [json_name = "workspaceEntries"];</code>
   */
  @java.lang.Override
  public java.util.List<? extends ai.stigmer.agentic.session.v1.WorkspaceEntryOrBuilder> 
      getWorkspaceEntriesOrBuilderList() {
    return workspaceEntries_;
  }
  /**
   * <pre>
   * Workspace the run's session operates on. Empty means no workspace.
   *
   * &#64;internal
   * Maps onto SessionSpec.workspace_entries of the session each run
   * creates. Unattended surfaces constrain sources in their handlers:
   * schedules accept git_repo only (no client is connected at fire
   * time to serve a local_path). Credentials (DD-018 D-4): the
   * provisioner resolves GITHUB_TOKEN from the merged environment;
   * for surfaces with no interactive caller the one supported
   * contract is an org-visibility Environment holding GITHUB_TOKEN
   * bound via environment_refs — the personal-environment fallback
   * resolves as-caller and is structurally closed to synthetic
   * accounts. Public repos need no token.
   * </pre>
   *
   * <code>repeated .ai.stigmer.agentic.session.v1.WorkspaceEntry workspace_entries = 4 [json_name = "workspaceEntries"];</code>
   */
  @java.lang.Override
  public int getWorkspaceEntriesCount() {
    return workspaceEntries_.size();
  }
  /**
   * <pre>
   * Workspace the run's session operates on. Empty means no workspace.
   *
   * &#64;internal
   * Maps onto SessionSpec.workspace_entries of the session each run
   * creates. Unattended surfaces constrain sources in their handlers:
   * schedules accept git_repo only (no client is connected at fire
   * time to serve a local_path). Credentials (DD-018 D-4): the
   * provisioner resolves GITHUB_TOKEN from the merged environment;
   * for surfaces with no interactive caller the one supported
   * contract is an org-visibility Environment holding GITHUB_TOKEN
   * bound via environment_refs — the personal-environment fallback
   * resolves as-caller and is structurally closed to synthetic
   * accounts. Public repos need no token.
   * </pre>
   *
   * <code>repeated .ai.stigmer.agentic.session.v1.WorkspaceEntry workspace_entries = 4 [json_name = "workspaceEntries"];</code>
   */
  @java.lang.Override
  public ai.stigmer.agentic.session.v1.WorkspaceEntry getWorkspaceEntries(int index) {
    return workspaceEntries_.get(index);
  }
  /**
   * <pre>
   * Workspace the run's session operates on. Empty means no workspace.
   *
   * &#64;internal
   * Maps onto SessionSpec.workspace_entries of the session each run
   * creates. Unattended surfaces constrain sources in their handlers:
   * schedules accept git_repo only (no client is connected at fire
   * time to serve a local_path). Credentials (DD-018 D-4): the
   * provisioner resolves GITHUB_TOKEN from the merged environment;
   * for surfaces with no interactive caller the one supported
   * contract is an org-visibility Environment holding GITHUB_TOKEN
   * bound via environment_refs — the personal-environment fallback
   * resolves as-caller and is structurally closed to synthetic
   * accounts. Public repos need no token.
   * </pre>
   *
   * <code>repeated .ai.stigmer.agentic.session.v1.WorkspaceEntry workspace_entries = 4 [json_name = "workspaceEntries"];</code>
   */
  @java.lang.Override
  public ai.stigmer.agentic.session.v1.WorkspaceEntryOrBuilder getWorkspaceEntriesOrBuilder(
      int index) {
    return workspaceEntries_.get(index);
  }

  public static final int ENVIRONMENT_REFS_FIELD_NUMBER = 5;
  @SuppressWarnings("serial")
  private java.util.List<ai.stigmer.commons.apiresource.ApiResourceReference> environmentRefs_;
  /**
   * <pre>
   * References to Environment resources whose values are provided to
   * the runs this invocation creates.
   *
   * This is how a tool-using agent becomes runnable unattended: bind
   * an org-shared environment holding the needed credentials (for
   * example an MCP server's shared secret), and the runs receive its
   * values at runtime. The agent and its default instance stay
   * untouched.
   *
   * &#64;internal
   * The AgentShare/AgentChannel/Schedule environment_refs lineage
   * (project DD-017 D-2). Resolution stays with the embedding
   * surface's pipeline (CreateExecutionContextStep branch, claim-
   * driven, LOWEST merge priority — instance refs and runtime_env
   * override on key conflicts). No write-time existence or visibility
   * check: enforcement lives solely at runtime resolution, which
   * fails closed.
   * </pre>
   *
   * <code>repeated .ai.stigmer.commons.apiresource.ApiResourceReference environment_refs = 5 [json_name = "environmentRefs", (.buf.validate.field) = { ... }</code>
   */
  @java.lang.Override
  public java.util.List<ai.stigmer.commons.apiresource.ApiResourceReference> getEnvironmentRefsList() {
    return environmentRefs_;
  }
  /**
   * <pre>
   * References to Environment resources whose values are provided to
   * the runs this invocation creates.
   *
   * This is how a tool-using agent becomes runnable unattended: bind
   * an org-shared environment holding the needed credentials (for
   * example an MCP server's shared secret), and the runs receive its
   * values at runtime. The agent and its default instance stay
   * untouched.
   *
   * &#64;internal
   * The AgentShare/AgentChannel/Schedule environment_refs lineage
   * (project DD-017 D-2). Resolution stays with the embedding
   * surface's pipeline (CreateExecutionContextStep branch, claim-
   * driven, LOWEST merge priority — instance refs and runtime_env
   * override on key conflicts). No write-time existence or visibility
   * check: enforcement lives solely at runtime resolution, which
   * fails closed.
   * </pre>
   *
   * <code>repeated .ai.stigmer.commons.apiresource.ApiResourceReference environment_refs = 5 [json_name = "environmentRefs", (.buf.validate.field) = { ... }</code>
   */
  @java.lang.Override
  public java.util.List<? extends ai.stigmer.commons.apiresource.ApiResourceReferenceOrBuilder> 
      getEnvironmentRefsOrBuilderList() {
    return environmentRefs_;
  }
  /**
   * <pre>
   * References to Environment resources whose values are provided to
   * the runs this invocation creates.
   *
   * This is how a tool-using agent becomes runnable unattended: bind
   * an org-shared environment holding the needed credentials (for
   * example an MCP server's shared secret), and the runs receive its
   * values at runtime. The agent and its default instance stay
   * untouched.
   *
   * &#64;internal
   * The AgentShare/AgentChannel/Schedule environment_refs lineage
   * (project DD-017 D-2). Resolution stays with the embedding
   * surface's pipeline (CreateExecutionContextStep branch, claim-
   * driven, LOWEST merge priority — instance refs and runtime_env
   * override on key conflicts). No write-time existence or visibility
   * check: enforcement lives solely at runtime resolution, which
   * fails closed.
   * </pre>
   *
   * <code>repeated .ai.stigmer.commons.apiresource.ApiResourceReference environment_refs = 5 [json_name = "environmentRefs", (.buf.validate.field) = { ... }</code>
   */
  @java.lang.Override
  public int getEnvironmentRefsCount() {
    return environmentRefs_.size();
  }
  /**
   * <pre>
   * References to Environment resources whose values are provided to
   * the runs this invocation creates.
   *
   * This is how a tool-using agent becomes runnable unattended: bind
   * an org-shared environment holding the needed credentials (for
   * example an MCP server's shared secret), and the runs receive its
   * values at runtime. The agent and its default instance stay
   * untouched.
   *
   * &#64;internal
   * The AgentShare/AgentChannel/Schedule environment_refs lineage
   * (project DD-017 D-2). Resolution stays with the embedding
   * surface's pipeline (CreateExecutionContextStep branch, claim-
   * driven, LOWEST merge priority — instance refs and runtime_env
   * override on key conflicts). No write-time existence or visibility
   * check: enforcement lives solely at runtime resolution, which
   * fails closed.
   * </pre>
   *
   * <code>repeated .ai.stigmer.commons.apiresource.ApiResourceReference environment_refs = 5 [json_name = "environmentRefs", (.buf.validate.field) = { ... }</code>
   */
  @java.lang.Override
  public ai.stigmer.commons.apiresource.ApiResourceReference getEnvironmentRefs(int index) {
    return environmentRefs_.get(index);
  }
  /**
   * <pre>
   * References to Environment resources whose values are provided to
   * the runs this invocation creates.
   *
   * This is how a tool-using agent becomes runnable unattended: bind
   * an org-shared environment holding the needed credentials (for
   * example an MCP server's shared secret), and the runs receive its
   * values at runtime. The agent and its default instance stay
   * untouched.
   *
   * &#64;internal
   * The AgentShare/AgentChannel/Schedule environment_refs lineage
   * (project DD-017 D-2). Resolution stays with the embedding
   * surface's pipeline (CreateExecutionContextStep branch, claim-
   * driven, LOWEST merge priority — instance refs and runtime_env
   * override on key conflicts). No write-time existence or visibility
   * check: enforcement lives solely at runtime resolution, which
   * fails closed.
   * </pre>
   *
   * <code>repeated .ai.stigmer.commons.apiresource.ApiResourceReference environment_refs = 5 [json_name = "environmentRefs", (.buf.validate.field) = { ... }</code>
   */
  @java.lang.Override
  public ai.stigmer.commons.apiresource.ApiResourceReferenceOrBuilder getEnvironmentRefsOrBuilder(
      int index) {
    return environmentRefs_.get(index);
  }

  public static final int RUN_CONFIG_FIELD_NUMBER = 6;
  private ai.stigmer.agentic.agentexecution.v1.RunConfig runConfig_;
  /**
   * <pre>
   * Per-invocation model choice and run bounds. Unset fields inherit
   * the embedding surface's platform execution profile.
   * </pre>
   *
   * <code>.ai.stigmer.agentic.agentexecution.v1.RunConfig run_config = 6 [json_name = "runConfig"];</code>
   * @return Whether the runConfig field is set.
   */
  @java.lang.Override
  public boolean hasRunConfig() {
    return ((bitField0_ & 0x00000002) != 0);
  }
  /**
   * <pre>
   * Per-invocation model choice and run bounds. Unset fields inherit
   * the embedding surface's platform execution profile.
   * </pre>
   *
   * <code>.ai.stigmer.agentic.agentexecution.v1.RunConfig run_config = 6 [json_name = "runConfig"];</code>
   * @return The runConfig.
   */
  @java.lang.Override
  public ai.stigmer.agentic.agentexecution.v1.RunConfig getRunConfig() {
    return runConfig_ == null ? ai.stigmer.agentic.agentexecution.v1.RunConfig.getDefaultInstance() : runConfig_;
  }
  /**
   * <pre>
   * Per-invocation model choice and run bounds. Unset fields inherit
   * the embedding surface's platform execution profile.
   * </pre>
   *
   * <code>.ai.stigmer.agentic.agentexecution.v1.RunConfig run_config = 6 [json_name = "runConfig"];</code>
   */
  @java.lang.Override
  public ai.stigmer.agentic.agentexecution.v1.RunConfigOrBuilder getRunConfigOrBuilder() {
    return runConfig_ == null ? ai.stigmer.agentic.agentexecution.v1.RunConfig.getDefaultInstance() : runConfig_;
  }

  private byte memoizedIsInitialized = -1;
  @java.lang.Override
  public final boolean isInitialized() {
    byte isInitialized = memoizedIsInitialized;
    if (isInitialized == 1) return true;
    if (isInitialized == 0) return false;

    memoizedIsInitialized = 1;
    return true;
  }

  @java.lang.Override
  public void writeTo(com.google.protobuf.CodedOutputStream output)
                      throws java.io.IOException {
    if (((bitField0_ & 0x00000001) != 0)) {
      output.writeMessage(1, getAgentRef());
    }
    if (!com.google.protobuf.GeneratedMessage.isStringEmpty(message_)) {
      com.google.protobuf.GeneratedMessage.writeString(output, 2, message_);
    }
    if (harness_ != ai.stigmer.agentic.session.v1.Harness.HARNESS_UNSPECIFIED.getNumber()) {
      output.writeEnum(3, harness_);
    }
    for (int i = 0; i < workspaceEntries_.size(); i++) {
      output.writeMessage(4, workspaceEntries_.get(i));
    }
    for (int i = 0; i < environmentRefs_.size(); i++) {
      output.writeMessage(5, environmentRefs_.get(i));
    }
    if (((bitField0_ & 0x00000002) != 0)) {
      output.writeMessage(6, getRunConfig());
    }
    getUnknownFields().writeTo(output);
  }

  @java.lang.Override
  public int getSerializedSize() {
    int size = memoizedSize;
    if (size != -1) return size;

    size = 0;
    if (((bitField0_ & 0x00000001) != 0)) {
      size += com.google.protobuf.CodedOutputStream
        .computeMessageSize(1, getAgentRef());
    }
    if (!com.google.protobuf.GeneratedMessage.isStringEmpty(message_)) {
      size += com.google.protobuf.GeneratedMessage.computeStringSize(2, message_);
    }
    if (harness_ != ai.stigmer.agentic.session.v1.Harness.HARNESS_UNSPECIFIED.getNumber()) {
      size += com.google.protobuf.CodedOutputStream
        .computeEnumSize(3, harness_);
    }

        {
          final int count = workspaceEntries_.size();
          for (int i = 0; i < count; i++) {
            size += com.google.protobuf.CodedOutputStream
              .computeMessageSizeNoTag(workspaceEntries_.get(i));
          }
          size += 1 * count;
        }

        {
          final int count = environmentRefs_.size();
          for (int i = 0; i < count; i++) {
            size += com.google.protobuf.CodedOutputStream
              .computeMessageSizeNoTag(environmentRefs_.get(i));
          }
          size += 1 * count;
        }
    if (((bitField0_ & 0x00000002) != 0)) {
      size += com.google.protobuf.CodedOutputStream
        .computeMessageSize(6, getRunConfig());
    }
    size += getUnknownFields().getSerializedSize();
    memoizedSize = size;
    return size;
  }

  @java.lang.Override
  public boolean equals(final java.lang.Object obj) {
    if (obj == this) {
     return true;
    }
    if (!(obj instanceof ai.stigmer.agentic.agentexecution.v1.AgentInvocation)) {
      return super.equals(obj);
    }
    ai.stigmer.agentic.agentexecution.v1.AgentInvocation other = (ai.stigmer.agentic.agentexecution.v1.AgentInvocation) obj;

    if (hasAgentRef() != other.hasAgentRef()) return false;
    if (hasAgentRef()) {
      if (!getAgentRef()
          .equals(other.getAgentRef())) return false;
    }
    if (!getMessage()
        .equals(other.getMessage())) return false;
    if (harness_ != other.harness_) return false;
    if (!getWorkspaceEntriesList()
        .equals(other.getWorkspaceEntriesList())) return false;
    if (!getEnvironmentRefsList()
        .equals(other.getEnvironmentRefsList())) return false;
    if (hasRunConfig() != other.hasRunConfig()) return false;
    if (hasRunConfig()) {
      if (!getRunConfig()
          .equals(other.getRunConfig())) return false;
    }
    if (!getUnknownFields().equals(other.getUnknownFields())) return false;
    return true;
  }

  @java.lang.Override
  public int hashCode() {
    if (memoizedHashCode != 0) {
      return memoizedHashCode;
    }
    int hash = 41;
    hash = (19 * hash) + getDescriptor().hashCode();
    if (hasAgentRef()) {
      hash = (37 * hash) + AGENT_REF_FIELD_NUMBER;
      hash = (53 * hash) + getAgentRef().hashCode();
    }
    hash = (37 * hash) + MESSAGE_FIELD_NUMBER;
    hash = (53 * hash) + getMessage().hashCode();
    hash = (37 * hash) + HARNESS_FIELD_NUMBER;
    hash = (53 * hash) + harness_;
    if (getWorkspaceEntriesCount() > 0) {
      hash = (37 * hash) + WORKSPACE_ENTRIES_FIELD_NUMBER;
      hash = (53 * hash) + getWorkspaceEntriesList().hashCode();
    }
    if (getEnvironmentRefsCount() > 0) {
      hash = (37 * hash) + ENVIRONMENT_REFS_FIELD_NUMBER;
      hash = (53 * hash) + getEnvironmentRefsList().hashCode();
    }
    if (hasRunConfig()) {
      hash = (37 * hash) + RUN_CONFIG_FIELD_NUMBER;
      hash = (53 * hash) + getRunConfig().hashCode();
    }
    hash = (29 * hash) + getUnknownFields().hashCode();
    memoizedHashCode = hash;
    return hash;
  }

  public static ai.stigmer.agentic.agentexecution.v1.AgentInvocation parseFrom(
      java.nio.ByteBuffer data)
      throws com.google.protobuf.InvalidProtocolBufferException {
    return PARSER.parseFrom(data);
  }
  public static ai.stigmer.agentic.agentexecution.v1.AgentInvocation parseFrom(
      java.nio.ByteBuffer data,
      com.google.protobuf.ExtensionRegistryLite extensionRegistry)
      throws com.google.protobuf.InvalidProtocolBufferException {
    return PARSER.parseFrom(data, extensionRegistry);
  }
  public static ai.stigmer.agentic.agentexecution.v1.AgentInvocation parseFrom(
      com.google.protobuf.ByteString data)
      throws com.google.protobuf.InvalidProtocolBufferException {
    return PARSER.parseFrom(data);
  }
  public static ai.stigmer.agentic.agentexecution.v1.AgentInvocation parseFrom(
      com.google.protobuf.ByteString data,
      com.google.protobuf.ExtensionRegistryLite extensionRegistry)
      throws com.google.protobuf.InvalidProtocolBufferException {
    return PARSER.parseFrom(data, extensionRegistry);
  }
  public static ai.stigmer.agentic.agentexecution.v1.AgentInvocation parseFrom(byte[] data)
      throws com.google.protobuf.InvalidProtocolBufferException {
    return PARSER.parseFrom(data);
  }
  public static ai.stigmer.agentic.agentexecution.v1.AgentInvocation parseFrom(
      byte[] data,
      com.google.protobuf.ExtensionRegistryLite extensionRegistry)
      throws com.google.protobuf.InvalidProtocolBufferException {
    return PARSER.parseFrom(data, extensionRegistry);
  }
  public static ai.stigmer.agentic.agentexecution.v1.AgentInvocation parseFrom(java.io.InputStream input)
      throws java.io.IOException {
    return com.google.protobuf.GeneratedMessage
        .parseWithIOException(PARSER, input);
  }
  public static ai.stigmer.agentic.agentexecution.v1.AgentInvocation parseFrom(
      java.io.InputStream input,
      com.google.protobuf.ExtensionRegistryLite extensionRegistry)
      throws java.io.IOException {
    return com.google.protobuf.GeneratedMessage
        .parseWithIOException(PARSER, input, extensionRegistry);
  }

  public static ai.stigmer.agentic.agentexecution.v1.AgentInvocation parseDelimitedFrom(java.io.InputStream input)
      throws java.io.IOException {
    return com.google.protobuf.GeneratedMessage
        .parseDelimitedWithIOException(PARSER, input);
  }

  public static ai.stigmer.agentic.agentexecution.v1.AgentInvocation parseDelimitedFrom(
      java.io.InputStream input,
      com.google.protobuf.ExtensionRegistryLite extensionRegistry)
      throws java.io.IOException {
    return com.google.protobuf.GeneratedMessage
        .parseDelimitedWithIOException(PARSER, input, extensionRegistry);
  }
  public static ai.stigmer.agentic.agentexecution.v1.AgentInvocation parseFrom(
      com.google.protobuf.CodedInputStream input)
      throws java.io.IOException {
    return com.google.protobuf.GeneratedMessage
        .parseWithIOException(PARSER, input);
  }
  public static ai.stigmer.agentic.agentexecution.v1.AgentInvocation parseFrom(
      com.google.protobuf.CodedInputStream input,
      com.google.protobuf.ExtensionRegistryLite extensionRegistry)
      throws java.io.IOException {
    return com.google.protobuf.GeneratedMessage
        .parseWithIOException(PARSER, input, extensionRegistry);
  }

  @java.lang.Override
  public Builder newBuilderForType() { return newBuilder(); }
  public static Builder newBuilder() {
    return DEFAULT_INSTANCE.toBuilder();
  }
  public static Builder newBuilder(ai.stigmer.agentic.agentexecution.v1.AgentInvocation prototype) {
    return DEFAULT_INSTANCE.toBuilder().mergeFrom(prototype);
  }
  @java.lang.Override
  public Builder toBuilder() {
    return this == DEFAULT_INSTANCE
        ? new Builder() : new Builder().mergeFrom(this);
  }

  @java.lang.Override
  protected Builder newBuilderForType(
      com.google.protobuf.GeneratedMessage.BuilderParent parent) {
    Builder builder = new Builder(parent);
    return builder;
  }
  /**
   * <pre>
   * AgentInvocation is what a caller may ask an agent to do.
   *
   * It is the owner-settable subset of an agent run — the interactive
   * composer's vocabulary (agent, message, harness, workspace,
   * environments, run bounds) minus what an unattended surface makes
   * structurally impossible. Surfaces that trigger agents on someone's
   * behalf embed this message instead of re-deriving the shape:
   * schedules embed it whole; the workflow agent_call task adopts it at
   * the TYPE level (embedding RunConfig and documenting a field-by-field
   * correspondence) because its task config is a kind+Struct authoring
   * DSL whose field names are the YAML keys — see
   * workflow/v1/tasks/agent_call.proto; channels embed RunConfig.
   *
   * &#64;internal
   * Project DD-018 (whatsapp-proactive-messaging). An ALLOWLIST by
   * construction: platform-owned execution fields (approval_mode,
   * execution_target, callback_token, harness_state_id,
   * activity_task_queue, auto_approve_all, org/labels/subject) cannot
   * appear here, so embedding surfaces never need a blocklist validator
   * — the rejected alternative was embedding AgentExecutionSpec /
   * SessionSpec and validating the illegal fields away. Exclusions,
   * each with its reason (DD-018 D-1): runtime_env (plaintext secrets
   * in manifests leak; environments are the vehicle), attachments
   * (one-shot uploads don't recur), mcp_server_usages/skill_refs (the
   * agent blueprint owns its tool set), interaction_mode (Plan mode's
   * deliverable is a plan for a human who isn't there), cursor_mode
   * (runner-owned constant). Surface-specific constraints live in the
   * embedding surface's handlers, never here — e.g. schedules reject
   * local_path workspace sources and enforce agent_ref.org ==
   * metadata.org.
   * </pre>
   *
   * Protobuf type {@code ai.stigmer.agentic.agentexecution.v1.AgentInvocation}
   */
  public static final class Builder extends
      com.google.protobuf.GeneratedMessage.Builder<Builder> implements
      // @@protoc_insertion_point(builder_implements:ai.stigmer.agentic.agentexecution.v1.AgentInvocation)
      ai.stigmer.agentic.agentexecution.v1.AgentInvocationOrBuilder {
    public static final com.google.protobuf.Descriptors.Descriptor
        getDescriptor() {
      return ai.stigmer.agentic.agentexecution.v1.InvocationProto.internal_static_ai_stigmer_agentic_agentexecution_v1_AgentInvocation_descriptor;
    }

    @java.lang.Override
    protected com.google.protobuf.GeneratedMessage.FieldAccessorTable
        internalGetFieldAccessorTable() {
      return ai.stigmer.agentic.agentexecution.v1.InvocationProto.internal_static_ai_stigmer_agentic_agentexecution_v1_AgentInvocation_fieldAccessorTable
          .ensureFieldAccessorsInitialized(
              ai.stigmer.agentic.agentexecution.v1.AgentInvocation.class, ai.stigmer.agentic.agentexecution.v1.AgentInvocation.Builder.class);
    }

    // Construct using ai.stigmer.agentic.agentexecution.v1.AgentInvocation.newBuilder()
    private Builder() {
      maybeForceBuilderInitialization();
    }

    private Builder(
        com.google.protobuf.GeneratedMessage.BuilderParent parent) {
      super(parent);
      maybeForceBuilderInitialization();
    }
    private void maybeForceBuilderInitialization() {
      if (com.google.protobuf.GeneratedMessage
              .alwaysUseFieldBuilders) {
        internalGetAgentRefFieldBuilder();
        internalGetWorkspaceEntriesFieldBuilder();
        internalGetEnvironmentRefsFieldBuilder();
        internalGetRunConfigFieldBuilder();
      }
    }
    @java.lang.Override
    public Builder clear() {
      super.clear();
      bitField0_ = 0;
      agentRef_ = null;
      if (agentRefBuilder_ != null) {
        agentRefBuilder_.dispose();
        agentRefBuilder_ = null;
      }
      message_ = "";
      harness_ = 0;
      if (workspaceEntriesBuilder_ == null) {
        workspaceEntries_ = java.util.Collections.emptyList();
      } else {
        workspaceEntries_ = null;
        workspaceEntriesBuilder_.clear();
      }
      bitField0_ = (bitField0_ & ~0x00000008);
      if (environmentRefsBuilder_ == null) {
        environmentRefs_ = java.util.Collections.emptyList();
      } else {
        environmentRefs_ = null;
        environmentRefsBuilder_.clear();
      }
      bitField0_ = (bitField0_ & ~0x00000010);
      runConfig_ = null;
      if (runConfigBuilder_ != null) {
        runConfigBuilder_.dispose();
        runConfigBuilder_ = null;
      }
      return this;
    }

    @java.lang.Override
    public com.google.protobuf.Descriptors.Descriptor
        getDescriptorForType() {
      return ai.stigmer.agentic.agentexecution.v1.InvocationProto.internal_static_ai_stigmer_agentic_agentexecution_v1_AgentInvocation_descriptor;
    }

    @java.lang.Override
    public ai.stigmer.agentic.agentexecution.v1.AgentInvocation getDefaultInstanceForType() {
      return ai.stigmer.agentic.agentexecution.v1.AgentInvocation.getDefaultInstance();
    }

    @java.lang.Override
    public ai.stigmer.agentic.agentexecution.v1.AgentInvocation build() {
      ai.stigmer.agentic.agentexecution.v1.AgentInvocation result = buildPartial();
      if (!result.isInitialized()) {
        throw newUninitializedMessageException(result);
      }
      return result;
    }

    @java.lang.Override
    public ai.stigmer.agentic.agentexecution.v1.AgentInvocation buildPartial() {
      ai.stigmer.agentic.agentexecution.v1.AgentInvocation result = new ai.stigmer.agentic.agentexecution.v1.AgentInvocation(this);
      buildPartialRepeatedFields(result);
      if (bitField0_ != 0) { buildPartial0(result); }
      onBuilt();
      return result;
    }

    private void buildPartialRepeatedFields(ai.stigmer.agentic.agentexecution.v1.AgentInvocation result) {
      if (workspaceEntriesBuilder_ == null) {
        if (((bitField0_ & 0x00000008) != 0)) {
          workspaceEntries_ = java.util.Collections.unmodifiableList(workspaceEntries_);
          bitField0_ = (bitField0_ & ~0x00000008);
        }
        result.workspaceEntries_ = workspaceEntries_;
      } else {
        result.workspaceEntries_ = workspaceEntriesBuilder_.build();
      }
      if (environmentRefsBuilder_ == null) {
        if (((bitField0_ & 0x00000010) != 0)) {
          environmentRefs_ = java.util.Collections.unmodifiableList(environmentRefs_);
          bitField0_ = (bitField0_ & ~0x00000010);
        }
        result.environmentRefs_ = environmentRefs_;
      } else {
        result.environmentRefs_ = environmentRefsBuilder_.build();
      }
    }

    private void buildPartial0(ai.stigmer.agentic.agentexecution.v1.AgentInvocation result) {
      int from_bitField0_ = bitField0_;
      int to_bitField0_ = 0;
      if (((from_bitField0_ & 0x00000001) != 0)) {
        result.agentRef_ = agentRefBuilder_ == null
            ? agentRef_
            : agentRefBuilder_.build();
        to_bitField0_ |= 0x00000001;
      }
      if (((from_bitField0_ & 0x00000002) != 0)) {
        result.message_ = message_;
      }
      if (((from_bitField0_ & 0x00000004) != 0)) {
        result.harness_ = harness_;
      }
      if (((from_bitField0_ & 0x00000020) != 0)) {
        result.runConfig_ = runConfigBuilder_ == null
            ? runConfig_
            : runConfigBuilder_.build();
        to_bitField0_ |= 0x00000002;
      }
      result.bitField0_ |= to_bitField0_;
    }

    @java.lang.Override
    public Builder mergeFrom(com.google.protobuf.Message other) {
      if (other instanceof ai.stigmer.agentic.agentexecution.v1.AgentInvocation) {
        return mergeFrom((ai.stigmer.agentic.agentexecution.v1.AgentInvocation)other);
      } else {
        super.mergeFrom(other);
        return this;
      }
    }

    public Builder mergeFrom(ai.stigmer.agentic.agentexecution.v1.AgentInvocation other) {
      if (other == ai.stigmer.agentic.agentexecution.v1.AgentInvocation.getDefaultInstance()) return this;
      if (other.hasAgentRef()) {
        mergeAgentRef(other.getAgentRef());
      }
      if (!other.getMessage().isEmpty()) {
        message_ = other.message_;
        bitField0_ |= 0x00000002;
        onChanged();
      }
      if (other.harness_ != 0) {
        setHarnessValue(other.getHarnessValue());
      }
      if (workspaceEntriesBuilder_ == null) {
        if (!other.workspaceEntries_.isEmpty()) {
          if (workspaceEntries_.isEmpty()) {
            workspaceEntries_ = other.workspaceEntries_;
            bitField0_ = (bitField0_ & ~0x00000008);
          } else {
            ensureWorkspaceEntriesIsMutable();
            workspaceEntries_.addAll(other.workspaceEntries_);
          }
          onChanged();
        }
      } else {
        if (!other.workspaceEntries_.isEmpty()) {
          if (workspaceEntriesBuilder_.isEmpty()) {
            workspaceEntriesBuilder_.dispose();
            workspaceEntriesBuilder_ = null;
            workspaceEntries_ = other.workspaceEntries_;
            bitField0_ = (bitField0_ & ~0x00000008);
            workspaceEntriesBuilder_ = 
              com.google.protobuf.GeneratedMessage.alwaysUseFieldBuilders ?
                 internalGetWorkspaceEntriesFieldBuilder() : null;
          } else {
            workspaceEntriesBuilder_.addAllMessages(other.workspaceEntries_);
          }
        }
      }
      if (environmentRefsBuilder_ == null) {
        if (!other.environmentRefs_.isEmpty()) {
          if (environmentRefs_.isEmpty()) {
            environmentRefs_ = other.environmentRefs_;
            bitField0_ = (bitField0_ & ~0x00000010);
          } else {
            ensureEnvironmentRefsIsMutable();
            environmentRefs_.addAll(other.environmentRefs_);
          }
          onChanged();
        }
      } else {
        if (!other.environmentRefs_.isEmpty()) {
          if (environmentRefsBuilder_.isEmpty()) {
            environmentRefsBuilder_.dispose();
            environmentRefsBuilder_ = null;
            environmentRefs_ = other.environmentRefs_;
            bitField0_ = (bitField0_ & ~0x00000010);
            environmentRefsBuilder_ = 
              com.google.protobuf.GeneratedMessage.alwaysUseFieldBuilders ?
                 internalGetEnvironmentRefsFieldBuilder() : null;
          } else {
            environmentRefsBuilder_.addAllMessages(other.environmentRefs_);
          }
        }
      }
      if (other.hasRunConfig()) {
        mergeRunConfig(other.getRunConfig());
      }
      this.mergeUnknownFields(other.getUnknownFields());
      onChanged();
      return this;
    }

    @java.lang.Override
    public final boolean isInitialized() {
      return true;
    }

    @java.lang.Override
    public Builder mergeFrom(
        com.google.protobuf.CodedInputStream input,
        com.google.protobuf.ExtensionRegistryLite extensionRegistry)
        throws java.io.IOException {
      if (extensionRegistry == null) {
        throw new java.lang.NullPointerException();
      }
      try {
        boolean done = false;
        while (!done) {
          int tag = input.readTag();
          switch (tag) {
            case 0:
              done = true;
              break;
            case 10: {
              input.readMessage(
                  internalGetAgentRefFieldBuilder().getBuilder(),
                  extensionRegistry);
              bitField0_ |= 0x00000001;
              break;
            } // case 10
            case 18: {
              message_ = input.readStringRequireUtf8();
              bitField0_ |= 0x00000002;
              break;
            } // case 18
            case 24: {
              harness_ = input.readEnum();
              bitField0_ |= 0x00000004;
              break;
            } // case 24
            case 34: {
              ai.stigmer.agentic.session.v1.WorkspaceEntry m =
                  input.readMessage(
                      ai.stigmer.agentic.session.v1.WorkspaceEntry.parser(),
                      extensionRegistry);
              if (workspaceEntriesBuilder_ == null) {
                ensureWorkspaceEntriesIsMutable();
                workspaceEntries_.add(m);
              } else {
                workspaceEntriesBuilder_.addMessage(m);
              }
              break;
            } // case 34
            case 42: {
              ai.stigmer.commons.apiresource.ApiResourceReference m =
                  input.readMessage(
                      ai.stigmer.commons.apiresource.ApiResourceReference.parser(),
                      extensionRegistry);
              if (environmentRefsBuilder_ == null) {
                ensureEnvironmentRefsIsMutable();
                environmentRefs_.add(m);
              } else {
                environmentRefsBuilder_.addMessage(m);
              }
              break;
            } // case 42
            case 50: {
              input.readMessage(
                  internalGetRunConfigFieldBuilder().getBuilder(),
                  extensionRegistry);
              bitField0_ |= 0x00000020;
              break;
            } // case 50
            default: {
              if (!super.parseUnknownField(input, extensionRegistry, tag)) {
                done = true; // was an endgroup tag
              }
              break;
            } // default:
          } // switch (tag)
        } // while (!done)
      } catch (com.google.protobuf.InvalidProtocolBufferException e) {
        throw e.unwrapIOException();
      } finally {
        onChanged();
      } // finally
      return this;
    }
    private int bitField0_;

    private ai.stigmer.commons.apiresource.ApiResourceReference agentRef_;
    private com.google.protobuf.SingleFieldBuilder<
        ai.stigmer.commons.apiresource.ApiResourceReference, ai.stigmer.commons.apiresource.ApiResourceReference.Builder, ai.stigmer.commons.apiresource.ApiResourceReferenceOrBuilder> agentRefBuilder_;
    /**
     * <pre>
     * Reference to the agent to run.
     *
     * &#64;internal
     * Embedding surfaces own the referential invariants: the schedule
     * requires agent_ref.org == metadata.org (the owning org is the
     * billing org for every run) and can_edit on this agent at create
     * (DD-009 C-6). Deletion of the agent does not cascade (house
     * convention): a dangling reference surfaces at run time as a
     * failed start, never as a silent stall.
     * </pre>
     *
     * <code>.ai.stigmer.commons.apiresource.ApiResourceReference agent_ref = 1 [json_name = "agentRef", (.buf.validate.field) = { ... }</code>
     * @return Whether the agentRef field is set.
     */
    public boolean hasAgentRef() {
      return ((bitField0_ & 0x00000001) != 0);
    }
    /**
     * <pre>
     * Reference to the agent to run.
     *
     * &#64;internal
     * Embedding surfaces own the referential invariants: the schedule
     * requires agent_ref.org == metadata.org (the owning org is the
     * billing org for every run) and can_edit on this agent at create
     * (DD-009 C-6). Deletion of the agent does not cascade (house
     * convention): a dangling reference surfaces at run time as a
     * failed start, never as a silent stall.
     * </pre>
     *
     * <code>.ai.stigmer.commons.apiresource.ApiResourceReference agent_ref = 1 [json_name = "agentRef", (.buf.validate.field) = { ... }</code>
     * @return The agentRef.
     */
    public ai.stigmer.commons.apiresource.ApiResourceReference getAgentRef() {
      if (agentRefBuilder_ == null) {
        return agentRef_ == null ? ai.stigmer.commons.apiresource.ApiResourceReference.getDefaultInstance() : agentRef_;
      } else {
        return agentRefBuilder_.getMessage();
      }
    }
    /**
     * <pre>
     * Reference to the agent to run.
     *
     * &#64;internal
     * Embedding surfaces own the referential invariants: the schedule
     * requires agent_ref.org == metadata.org (the owning org is the
     * billing org for every run) and can_edit on this agent at create
     * (DD-009 C-6). Deletion of the agent does not cascade (house
     * convention): a dangling reference surfaces at run time as a
     * failed start, never as a silent stall.
     * </pre>
     *
     * <code>.ai.stigmer.commons.apiresource.ApiResourceReference agent_ref = 1 [json_name = "agentRef", (.buf.validate.field) = { ... }</code>
     */
    public Builder setAgentRef(ai.stigmer.commons.apiresource.ApiResourceReference value) {
      if (agentRefBuilder_ == null) {
        if (value == null) {
          throw new NullPointerException();
        }
        agentRef_ = value;
      } else {
        agentRefBuilder_.setMessage(value);
      }
      bitField0_ |= 0x00000001;
      onChanged();
      return this;
    }
    /**
     * <pre>
     * Reference to the agent to run.
     *
     * &#64;internal
     * Embedding surfaces own the referential invariants: the schedule
     * requires agent_ref.org == metadata.org (the owning org is the
     * billing org for every run) and can_edit on this agent at create
     * (DD-009 C-6). Deletion of the agent does not cascade (house
     * convention): a dangling reference surfaces at run time as a
     * failed start, never as a silent stall.
     * </pre>
     *
     * <code>.ai.stigmer.commons.apiresource.ApiResourceReference agent_ref = 1 [json_name = "agentRef", (.buf.validate.field) = { ... }</code>
     */
    public Builder setAgentRef(
        ai.stigmer.commons.apiresource.ApiResourceReference.Builder builderForValue) {
      if (agentRefBuilder_ == null) {
        agentRef_ = builderForValue.build();
      } else {
        agentRefBuilder_.setMessage(builderForValue.build());
      }
      bitField0_ |= 0x00000001;
      onChanged();
      return this;
    }
    /**
     * <pre>
     * Reference to the agent to run.
     *
     * &#64;internal
     * Embedding surfaces own the referential invariants: the schedule
     * requires agent_ref.org == metadata.org (the owning org is the
     * billing org for every run) and can_edit on this agent at create
     * (DD-009 C-6). Deletion of the agent does not cascade (house
     * convention): a dangling reference surfaces at run time as a
     * failed start, never as a silent stall.
     * </pre>
     *
     * <code>.ai.stigmer.commons.apiresource.ApiResourceReference agent_ref = 1 [json_name = "agentRef", (.buf.validate.field) = { ... }</code>
     */
    public Builder mergeAgentRef(ai.stigmer.commons.apiresource.ApiResourceReference value) {
      if (agentRefBuilder_ == null) {
        if (((bitField0_ & 0x00000001) != 0) &&
          agentRef_ != null &&
          agentRef_ != ai.stigmer.commons.apiresource.ApiResourceReference.getDefaultInstance()) {
          getAgentRefBuilder().mergeFrom(value);
        } else {
          agentRef_ = value;
        }
      } else {
        agentRefBuilder_.mergeFrom(value);
      }
      if (agentRef_ != null) {
        bitField0_ |= 0x00000001;
        onChanged();
      }
      return this;
    }
    /**
     * <pre>
     * Reference to the agent to run.
     *
     * &#64;internal
     * Embedding surfaces own the referential invariants: the schedule
     * requires agent_ref.org == metadata.org (the owning org is the
     * billing org for every run) and can_edit on this agent at create
     * (DD-009 C-6). Deletion of the agent does not cascade (house
     * convention): a dangling reference surfaces at run time as a
     * failed start, never as a silent stall.
     * </pre>
     *
     * <code>.ai.stigmer.commons.apiresource.ApiResourceReference agent_ref = 1 [json_name = "agentRef", (.buf.validate.field) = { ... }</code>
     */
    public Builder clearAgentRef() {
      bitField0_ = (bitField0_ & ~0x00000001);
      agentRef_ = null;
      if (agentRefBuilder_ != null) {
        agentRefBuilder_.dispose();
        agentRefBuilder_ = null;
      }
      onChanged();
      return this;
    }
    /**
     * <pre>
     * Reference to the agent to run.
     *
     * &#64;internal
     * Embedding surfaces own the referential invariants: the schedule
     * requires agent_ref.org == metadata.org (the owning org is the
     * billing org for every run) and can_edit on this agent at create
     * (DD-009 C-6). Deletion of the agent does not cascade (house
     * convention): a dangling reference surfaces at run time as a
     * failed start, never as a silent stall.
     * </pre>
     *
     * <code>.ai.stigmer.commons.apiresource.ApiResourceReference agent_ref = 1 [json_name = "agentRef", (.buf.validate.field) = { ... }</code>
     */
    public ai.stigmer.commons.apiresource.ApiResourceReference.Builder getAgentRefBuilder() {
      bitField0_ |= 0x00000001;
      onChanged();
      return internalGetAgentRefFieldBuilder().getBuilder();
    }
    /**
     * <pre>
     * Reference to the agent to run.
     *
     * &#64;internal
     * Embedding surfaces own the referential invariants: the schedule
     * requires agent_ref.org == metadata.org (the owning org is the
     * billing org for every run) and can_edit on this agent at create
     * (DD-009 C-6). Deletion of the agent does not cascade (house
     * convention): a dangling reference surfaces at run time as a
     * failed start, never as a silent stall.
     * </pre>
     *
     * <code>.ai.stigmer.commons.apiresource.ApiResourceReference agent_ref = 1 [json_name = "agentRef", (.buf.validate.field) = { ... }</code>
     */
    public ai.stigmer.commons.apiresource.ApiResourceReferenceOrBuilder getAgentRefOrBuilder() {
      if (agentRefBuilder_ != null) {
        return agentRefBuilder_.getMessageOrBuilder();
      } else {
        return agentRef_ == null ?
            ai.stigmer.commons.apiresource.ApiResourceReference.getDefaultInstance() : agentRef_;
      }
    }
    /**
     * <pre>
     * Reference to the agent to run.
     *
     * &#64;internal
     * Embedding surfaces own the referential invariants: the schedule
     * requires agent_ref.org == metadata.org (the owning org is the
     * billing org for every run) and can_edit on this agent at create
     * (DD-009 C-6). Deletion of the agent does not cascade (house
     * convention): a dangling reference surfaces at run time as a
     * failed start, never as a silent stall.
     * </pre>
     *
     * <code>.ai.stigmer.commons.apiresource.ApiResourceReference agent_ref = 1 [json_name = "agentRef", (.buf.validate.field) = { ... }</code>
     */
    private com.google.protobuf.SingleFieldBuilder<
        ai.stigmer.commons.apiresource.ApiResourceReference, ai.stigmer.commons.apiresource.ApiResourceReference.Builder, ai.stigmer.commons.apiresource.ApiResourceReferenceOrBuilder> 
        internalGetAgentRefFieldBuilder() {
      if (agentRefBuilder_ == null) {
        agentRefBuilder_ = new com.google.protobuf.SingleFieldBuilder<
            ai.stigmer.commons.apiresource.ApiResourceReference, ai.stigmer.commons.apiresource.ApiResourceReference.Builder, ai.stigmer.commons.apiresource.ApiResourceReferenceOrBuilder>(
                getAgentRef(),
                getParentForChildren(),
                isClean());
        agentRef_ = null;
      }
      return agentRefBuilder_;
    }

    private java.lang.Object message_ = "";
    /**
     * <pre>
     * Prompt the run starts from.
     *
     * &#64;internal
     * DD-008 D5: the runner injects no current date into any prompt, so
     * unattended surfaces compose this message plus a fire-context line.
     * The bound applies to the stored prompt, not the composed message.
     * </pre>
     *
     * <code>string message = 2 [json_name = "message", (.buf.validate.field) = { ... }</code>
     * @return The message.
     */
    public java.lang.String getMessage() {
      java.lang.Object ref = message_;
      if (!(ref instanceof java.lang.String)) {
        com.google.protobuf.ByteString bs =
            (com.google.protobuf.ByteString) ref;
        java.lang.String s = bs.toStringUtf8();
        message_ = s;
        return s;
      } else {
        return (java.lang.String) ref;
      }
    }
    /**
     * <pre>
     * Prompt the run starts from.
     *
     * &#64;internal
     * DD-008 D5: the runner injects no current date into any prompt, so
     * unattended surfaces compose this message plus a fire-context line.
     * The bound applies to the stored prompt, not the composed message.
     * </pre>
     *
     * <code>string message = 2 [json_name = "message", (.buf.validate.field) = { ... }</code>
     * @return The bytes for message.
     */
    public com.google.protobuf.ByteString
        getMessageBytes() {
      java.lang.Object ref = message_;
      if (ref instanceof String) {
        com.google.protobuf.ByteString b = 
            com.google.protobuf.ByteString.copyFromUtf8(
                (java.lang.String) ref);
        message_ = b;
        return b;
      } else {
        return (com.google.protobuf.ByteString) ref;
      }
    }
    /**
     * <pre>
     * Prompt the run starts from.
     *
     * &#64;internal
     * DD-008 D5: the runner injects no current date into any prompt, so
     * unattended surfaces compose this message plus a fire-context line.
     * The bound applies to the stored prompt, not the composed message.
     * </pre>
     *
     * <code>string message = 2 [json_name = "message", (.buf.validate.field) = { ... }</code>
     * @param value The message to set.
     * @return This builder for chaining.
     */
    public Builder setMessage(
        java.lang.String value) {
      if (value == null) { throw new NullPointerException(); }
      message_ = value;
      bitField0_ |= 0x00000002;
      onChanged();
      return this;
    }
    /**
     * <pre>
     * Prompt the run starts from.
     *
     * &#64;internal
     * DD-008 D5: the runner injects no current date into any prompt, so
     * unattended surfaces compose this message plus a fire-context line.
     * The bound applies to the stored prompt, not the composed message.
     * </pre>
     *
     * <code>string message = 2 [json_name = "message", (.buf.validate.field) = { ... }</code>
     * @return This builder for chaining.
     */
    public Builder clearMessage() {
      message_ = getDefaultInstance().getMessage();
      bitField0_ = (bitField0_ & ~0x00000002);
      onChanged();
      return this;
    }
    /**
     * <pre>
     * Prompt the run starts from.
     *
     * &#64;internal
     * DD-008 D5: the runner injects no current date into any prompt, so
     * unattended surfaces compose this message plus a fire-context line.
     * The bound applies to the stored prompt, not the composed message.
     * </pre>
     *
     * <code>string message = 2 [json_name = "message", (.buf.validate.field) = { ... }</code>
     * @param value The bytes for message to set.
     * @return This builder for chaining.
     */
    public Builder setMessageBytes(
        com.google.protobuf.ByteString value) {
      if (value == null) { throw new NullPointerException(); }
      checkByteStringIsUtf8(value);
      message_ = value;
      bitField0_ |= 0x00000002;
      onChanged();
      return this;
    }

    private int harness_ = 0;
    /**
     * <pre>
     * Execution engine for the run's session. Unspecified inherits the
     * embedding surface's platform default.
     *
     * &#64;internal
     * DD-018 D-1: graduates from surface platform config (e.g.
     * stigmer.schedules.session-defaults.harness) to the invocation —
     * the config demotes from "the only source" to "the default".
     * Verified before deciding: unattended approval semantics are one
     * shared contract across both harnesses (approval-policy.ts), so
     * owner harness choice changes the engine, never the safety
     * posture. cursor_mode stays runner-owned and is deliberately
     * absent.
     * </pre>
     *
     * <code>.ai.stigmer.agentic.session.v1.Harness harness = 3 [json_name = "harness"];</code>
     * @return The enum numeric value on the wire for harness.
     */
    @java.lang.Override public int getHarnessValue() {
      return harness_;
    }
    /**
     * <pre>
     * Execution engine for the run's session. Unspecified inherits the
     * embedding surface's platform default.
     *
     * &#64;internal
     * DD-018 D-1: graduates from surface platform config (e.g.
     * stigmer.schedules.session-defaults.harness) to the invocation —
     * the config demotes from "the only source" to "the default".
     * Verified before deciding: unattended approval semantics are one
     * shared contract across both harnesses (approval-policy.ts), so
     * owner harness choice changes the engine, never the safety
     * posture. cursor_mode stays runner-owned and is deliberately
     * absent.
     * </pre>
     *
     * <code>.ai.stigmer.agentic.session.v1.Harness harness = 3 [json_name = "harness"];</code>
     * @param value The enum numeric value on the wire for harness to set.
     * @throws IllegalArgumentException if UNRECOGNIZED is provided.
     * @return This builder for chaining.
     */
    public Builder setHarnessValue(int value) {
      harness_ = value;
      bitField0_ |= 0x00000004;
      onChanged();
      return this;
    }
    /**
     * <pre>
     * Execution engine for the run's session. Unspecified inherits the
     * embedding surface's platform default.
     *
     * &#64;internal
     * DD-018 D-1: graduates from surface platform config (e.g.
     * stigmer.schedules.session-defaults.harness) to the invocation —
     * the config demotes from "the only source" to "the default".
     * Verified before deciding: unattended approval semantics are one
     * shared contract across both harnesses (approval-policy.ts), so
     * owner harness choice changes the engine, never the safety
     * posture. cursor_mode stays runner-owned and is deliberately
     * absent.
     * </pre>
     *
     * <code>.ai.stigmer.agentic.session.v1.Harness harness = 3 [json_name = "harness"];</code>
     * @return The harness.
     */
    @java.lang.Override
    public ai.stigmer.agentic.session.v1.Harness getHarness() {
      ai.stigmer.agentic.session.v1.Harness result = ai.stigmer.agentic.session.v1.Harness.forNumber(harness_);
      return result == null ? ai.stigmer.agentic.session.v1.Harness.UNRECOGNIZED : result;
    }
    /**
     * <pre>
     * Execution engine for the run's session. Unspecified inherits the
     * embedding surface's platform default.
     *
     * &#64;internal
     * DD-018 D-1: graduates from surface platform config (e.g.
     * stigmer.schedules.session-defaults.harness) to the invocation —
     * the config demotes from "the only source" to "the default".
     * Verified before deciding: unattended approval semantics are one
     * shared contract across both harnesses (approval-policy.ts), so
     * owner harness choice changes the engine, never the safety
     * posture. cursor_mode stays runner-owned and is deliberately
     * absent.
     * </pre>
     *
     * <code>.ai.stigmer.agentic.session.v1.Harness harness = 3 [json_name = "harness"];</code>
     * @param value The harness to set.
     * @return This builder for chaining.
     */
    public Builder setHarness(ai.stigmer.agentic.session.v1.Harness value) {
      if (value == null) { throw new NullPointerException(); }
      bitField0_ |= 0x00000004;
      harness_ = value.getNumber();
      onChanged();
      return this;
    }
    /**
     * <pre>
     * Execution engine for the run's session. Unspecified inherits the
     * embedding surface's platform default.
     *
     * &#64;internal
     * DD-018 D-1: graduates from surface platform config (e.g.
     * stigmer.schedules.session-defaults.harness) to the invocation —
     * the config demotes from "the only source" to "the default".
     * Verified before deciding: unattended approval semantics are one
     * shared contract across both harnesses (approval-policy.ts), so
     * owner harness choice changes the engine, never the safety
     * posture. cursor_mode stays runner-owned and is deliberately
     * absent.
     * </pre>
     *
     * <code>.ai.stigmer.agentic.session.v1.Harness harness = 3 [json_name = "harness"];</code>
     * @return This builder for chaining.
     */
    public Builder clearHarness() {
      bitField0_ = (bitField0_ & ~0x00000004);
      harness_ = 0;
      onChanged();
      return this;
    }

    private java.util.List<ai.stigmer.agentic.session.v1.WorkspaceEntry> workspaceEntries_ =
      java.util.Collections.emptyList();
    private void ensureWorkspaceEntriesIsMutable() {
      if (!((bitField0_ & 0x00000008) != 0)) {
        workspaceEntries_ = new java.util.ArrayList<ai.stigmer.agentic.session.v1.WorkspaceEntry>(workspaceEntries_);
        bitField0_ |= 0x00000008;
       }
    }

    private com.google.protobuf.RepeatedFieldBuilder<
        ai.stigmer.agentic.session.v1.WorkspaceEntry, ai.stigmer.agentic.session.v1.WorkspaceEntry.Builder, ai.stigmer.agentic.session.v1.WorkspaceEntryOrBuilder> workspaceEntriesBuilder_;

    /**
     * <pre>
     * Workspace the run's session operates on. Empty means no workspace.
     *
     * &#64;internal
     * Maps onto SessionSpec.workspace_entries of the session each run
     * creates. Unattended surfaces constrain sources in their handlers:
     * schedules accept git_repo only (no client is connected at fire
     * time to serve a local_path). Credentials (DD-018 D-4): the
     * provisioner resolves GITHUB_TOKEN from the merged environment;
     * for surfaces with no interactive caller the one supported
     * contract is an org-visibility Environment holding GITHUB_TOKEN
     * bound via environment_refs — the personal-environment fallback
     * resolves as-caller and is structurally closed to synthetic
     * accounts. Public repos need no token.
     * </pre>
     *
     * <code>repeated .ai.stigmer.agentic.session.v1.WorkspaceEntry workspace_entries = 4 [json_name = "workspaceEntries"];</code>
     */
    public java.util.List<ai.stigmer.agentic.session.v1.WorkspaceEntry> getWorkspaceEntriesList() {
      if (workspaceEntriesBuilder_ == null) {
        return java.util.Collections.unmodifiableList(workspaceEntries_);
      } else {
        return workspaceEntriesBuilder_.getMessageList();
      }
    }
    /**
     * <pre>
     * Workspace the run's session operates on. Empty means no workspace.
     *
     * &#64;internal
     * Maps onto SessionSpec.workspace_entries of the session each run
     * creates. Unattended surfaces constrain sources in their handlers:
     * schedules accept git_repo only (no client is connected at fire
     * time to serve a local_path). Credentials (DD-018 D-4): the
     * provisioner resolves GITHUB_TOKEN from the merged environment;
     * for surfaces with no interactive caller the one supported
     * contract is an org-visibility Environment holding GITHUB_TOKEN
     * bound via environment_refs — the personal-environment fallback
     * resolves as-caller and is structurally closed to synthetic
     * accounts. Public repos need no token.
     * </pre>
     *
     * <code>repeated .ai.stigmer.agentic.session.v1.WorkspaceEntry workspace_entries = 4 [json_name = "workspaceEntries"];</code>
     */
    public int getWorkspaceEntriesCount() {
      if (workspaceEntriesBuilder_ == null) {
        return workspaceEntries_.size();
      } else {
        return workspaceEntriesBuilder_.getCount();
      }
    }
    /**
     * <pre>
     * Workspace the run's session operates on. Empty means no workspace.
     *
     * &#64;internal
     * Maps onto SessionSpec.workspace_entries of the session each run
     * creates. Unattended surfaces constrain sources in their handlers:
     * schedules accept git_repo only (no client is connected at fire
     * time to serve a local_path). Credentials (DD-018 D-4): the
     * provisioner resolves GITHUB_TOKEN from the merged environment;
     * for surfaces with no interactive caller the one supported
     * contract is an org-visibility Environment holding GITHUB_TOKEN
     * bound via environment_refs — the personal-environment fallback
     * resolves as-caller and is structurally closed to synthetic
     * accounts. Public repos need no token.
     * </pre>
     *
     * <code>repeated .ai.stigmer.agentic.session.v1.WorkspaceEntry workspace_entries = 4 [json_name = "workspaceEntries"];</code>
     */
    public ai.stigmer.agentic.session.v1.WorkspaceEntry getWorkspaceEntries(int index) {
      if (workspaceEntriesBuilder_ == null) {
        return workspaceEntries_.get(index);
      } else {
        return workspaceEntriesBuilder_.getMessage(index);
      }
    }
    /**
     * <pre>
     * Workspace the run's session operates on. Empty means no workspace.
     *
     * &#64;internal
     * Maps onto SessionSpec.workspace_entries of the session each run
     * creates. Unattended surfaces constrain sources in their handlers:
     * schedules accept git_repo only (no client is connected at fire
     * time to serve a local_path). Credentials (DD-018 D-4): the
     * provisioner resolves GITHUB_TOKEN from the merged environment;
     * for surfaces with no interactive caller the one supported
     * contract is an org-visibility Environment holding GITHUB_TOKEN
     * bound via environment_refs — the personal-environment fallback
     * resolves as-caller and is structurally closed to synthetic
     * accounts. Public repos need no token.
     * </pre>
     *
     * <code>repeated .ai.stigmer.agentic.session.v1.WorkspaceEntry workspace_entries = 4 [json_name = "workspaceEntries"];</code>
     */
    public Builder setWorkspaceEntries(
        int index, ai.stigmer.agentic.session.v1.WorkspaceEntry value) {
      if (workspaceEntriesBuilder_ == null) {
        if (value == null) {
          throw new NullPointerException();
        }
        ensureWorkspaceEntriesIsMutable();
        workspaceEntries_.set(index, value);
        onChanged();
      } else {
        workspaceEntriesBuilder_.setMessage(index, value);
      }
      return this;
    }
    /**
     * <pre>
     * Workspace the run's session operates on. Empty means no workspace.
     *
     * &#64;internal
     * Maps onto SessionSpec.workspace_entries of the session each run
     * creates. Unattended surfaces constrain sources in their handlers:
     * schedules accept git_repo only (no client is connected at fire
     * time to serve a local_path). Credentials (DD-018 D-4): the
     * provisioner resolves GITHUB_TOKEN from the merged environment;
     * for surfaces with no interactive caller the one supported
     * contract is an org-visibility Environment holding GITHUB_TOKEN
     * bound via environment_refs — the personal-environment fallback
     * resolves as-caller and is structurally closed to synthetic
     * accounts. Public repos need no token.
     * </pre>
     *
     * <code>repeated .ai.stigmer.agentic.session.v1.WorkspaceEntry workspace_entries = 4 [json_name = "workspaceEntries"];</code>
     */
    public Builder setWorkspaceEntries(
        int index, ai.stigmer.agentic.session.v1.WorkspaceEntry.Builder builderForValue) {
      if (workspaceEntriesBuilder_ == null) {
        ensureWorkspaceEntriesIsMutable();
        workspaceEntries_.set(index, builderForValue.build());
        onChanged();
      } else {
        workspaceEntriesBuilder_.setMessage(index, builderForValue.build());
      }
      return this;
    }
    /**
     * <pre>
     * Workspace the run's session operates on. Empty means no workspace.
     *
     * &#64;internal
     * Maps onto SessionSpec.workspace_entries of the session each run
     * creates. Unattended surfaces constrain sources in their handlers:
     * schedules accept git_repo only (no client is connected at fire
     * time to serve a local_path). Credentials (DD-018 D-4): the
     * provisioner resolves GITHUB_TOKEN from the merged environment;
     * for surfaces with no interactive caller the one supported
     * contract is an org-visibility Environment holding GITHUB_TOKEN
     * bound via environment_refs — the personal-environment fallback
     * resolves as-caller and is structurally closed to synthetic
     * accounts. Public repos need no token.
     * </pre>
     *
     * <code>repeated .ai.stigmer.agentic.session.v1.WorkspaceEntry workspace_entries = 4 [json_name = "workspaceEntries"];</code>
     */
    public Builder addWorkspaceEntries(ai.stigmer.agentic.session.v1.WorkspaceEntry value) {
      if (workspaceEntriesBuilder_ == null) {
        if (value == null) {
          throw new NullPointerException();
        }
        ensureWorkspaceEntriesIsMutable();
        workspaceEntries_.add(value);
        onChanged();
      } else {
        workspaceEntriesBuilder_.addMessage(value);
      }
      return this;
    }
    /**
     * <pre>
     * Workspace the run's session operates on. Empty means no workspace.
     *
     * &#64;internal
     * Maps onto SessionSpec.workspace_entries of the session each run
     * creates. Unattended surfaces constrain sources in their handlers:
     * schedules accept git_repo only (no client is connected at fire
     * time to serve a local_path). Credentials (DD-018 D-4): the
     * provisioner resolves GITHUB_TOKEN from the merged environment;
     * for surfaces with no interactive caller the one supported
     * contract is an org-visibility Environment holding GITHUB_TOKEN
     * bound via environment_refs — the personal-environment fallback
     * resolves as-caller and is structurally closed to synthetic
     * accounts. Public repos need no token.
     * </pre>
     *
     * <code>repeated .ai.stigmer.agentic.session.v1.WorkspaceEntry workspace_entries = 4 [json_name = "workspaceEntries"];</code>
     */
    public Builder addWorkspaceEntries(
        int index, ai.stigmer.agentic.session.v1.WorkspaceEntry value) {
      if (workspaceEntriesBuilder_ == null) {
        if (value == null) {
          throw new NullPointerException();
        }
        ensureWorkspaceEntriesIsMutable();
        workspaceEntries_.add(index, value);
        onChanged();
      } else {
        workspaceEntriesBuilder_.addMessage(index, value);
      }
      return this;
    }
    /**
     * <pre>
     * Workspace the run's session operates on. Empty means no workspace.
     *
     * &#64;internal
     * Maps onto SessionSpec.workspace_entries of the session each run
     * creates. Unattended surfaces constrain sources in their handlers:
     * schedules accept git_repo only (no client is connected at fire
     * time to serve a local_path). Credentials (DD-018 D-4): the
     * provisioner resolves GITHUB_TOKEN from the merged environment;
     * for surfaces with no interactive caller the one supported
     * contract is an org-visibility Environment holding GITHUB_TOKEN
     * bound via environment_refs — the personal-environment fallback
     * resolves as-caller and is structurally closed to synthetic
     * accounts. Public repos need no token.
     * </pre>
     *
     * <code>repeated .ai.stigmer.agentic.session.v1.WorkspaceEntry workspace_entries = 4 [json_name = "workspaceEntries"];</code>
     */
    public Builder addWorkspaceEntries(
        ai.stigmer.agentic.session.v1.WorkspaceEntry.Builder builderForValue) {
      if (workspaceEntriesBuilder_ == null) {
        ensureWorkspaceEntriesIsMutable();
        workspaceEntries_.add(builderForValue.build());
        onChanged();
      } else {
        workspaceEntriesBuilder_.addMessage(builderForValue.build());
      }
      return this;
    }
    /**
     * <pre>
     * Workspace the run's session operates on. Empty means no workspace.
     *
     * &#64;internal
     * Maps onto SessionSpec.workspace_entries of the session each run
     * creates. Unattended surfaces constrain sources in their handlers:
     * schedules accept git_repo only (no client is connected at fire
     * time to serve a local_path). Credentials (DD-018 D-4): the
     * provisioner resolves GITHUB_TOKEN from the merged environment;
     * for surfaces with no interactive caller the one supported
     * contract is an org-visibility Environment holding GITHUB_TOKEN
     * bound via environment_refs — the personal-environment fallback
     * resolves as-caller and is structurally closed to synthetic
     * accounts. Public repos need no token.
     * </pre>
     *
     * <code>repeated .ai.stigmer.agentic.session.v1.WorkspaceEntry workspace_entries = 4 [json_name = "workspaceEntries"];</code>
     */
    public Builder addWorkspaceEntries(
        int index, ai.stigmer.agentic.session.v1.WorkspaceEntry.Builder builderForValue) {
      if (workspaceEntriesBuilder_ == null) {
        ensureWorkspaceEntriesIsMutable();
        workspaceEntries_.add(index, builderForValue.build());
        onChanged();
      } else {
        workspaceEntriesBuilder_.addMessage(index, builderForValue.build());
      }
      return this;
    }
    /**
     * <pre>
     * Workspace the run's session operates on. Empty means no workspace.
     *
     * &#64;internal
     * Maps onto SessionSpec.workspace_entries of the session each run
     * creates. Unattended surfaces constrain sources in their handlers:
     * schedules accept git_repo only (no client is connected at fire
     * time to serve a local_path). Credentials (DD-018 D-4): the
     * provisioner resolves GITHUB_TOKEN from the merged environment;
     * for surfaces with no interactive caller the one supported
     * contract is an org-visibility Environment holding GITHUB_TOKEN
     * bound via environment_refs — the personal-environment fallback
     * resolves as-caller and is structurally closed to synthetic
     * accounts. Public repos need no token.
     * </pre>
     *
     * <code>repeated .ai.stigmer.agentic.session.v1.WorkspaceEntry workspace_entries = 4 [json_name = "workspaceEntries"];</code>
     */
    public Builder addAllWorkspaceEntries(
        java.lang.Iterable<? extends ai.stigmer.agentic.session.v1.WorkspaceEntry> values) {
      if (workspaceEntriesBuilder_ == null) {
        ensureWorkspaceEntriesIsMutable();
        com.google.protobuf.AbstractMessageLite.Builder.addAll(
            values, workspaceEntries_);
        onChanged();
      } else {
        workspaceEntriesBuilder_.addAllMessages(values);
      }
      return this;
    }
    /**
     * <pre>
     * Workspace the run's session operates on. Empty means no workspace.
     *
     * &#64;internal
     * Maps onto SessionSpec.workspace_entries of the session each run
     * creates. Unattended surfaces constrain sources in their handlers:
     * schedules accept git_repo only (no client is connected at fire
     * time to serve a local_path). Credentials (DD-018 D-4): the
     * provisioner resolves GITHUB_TOKEN from the merged environment;
     * for surfaces with no interactive caller the one supported
     * contract is an org-visibility Environment holding GITHUB_TOKEN
     * bound via environment_refs — the personal-environment fallback
     * resolves as-caller and is structurally closed to synthetic
     * accounts. Public repos need no token.
     * </pre>
     *
     * <code>repeated .ai.stigmer.agentic.session.v1.WorkspaceEntry workspace_entries = 4 [json_name = "workspaceEntries"];</code>
     */
    public Builder clearWorkspaceEntries() {
      if (workspaceEntriesBuilder_ == null) {
        workspaceEntries_ = java.util.Collections.emptyList();
        bitField0_ = (bitField0_ & ~0x00000008);
        onChanged();
      } else {
        workspaceEntriesBuilder_.clear();
      }
      return this;
    }
    /**
     * <pre>
     * Workspace the run's session operates on. Empty means no workspace.
     *
     * &#64;internal
     * Maps onto SessionSpec.workspace_entries of the session each run
     * creates. Unattended surfaces constrain sources in their handlers:
     * schedules accept git_repo only (no client is connected at fire
     * time to serve a local_path). Credentials (DD-018 D-4): the
     * provisioner resolves GITHUB_TOKEN from the merged environment;
     * for surfaces with no interactive caller the one supported
     * contract is an org-visibility Environment holding GITHUB_TOKEN
     * bound via environment_refs — the personal-environment fallback
     * resolves as-caller and is structurally closed to synthetic
     * accounts. Public repos need no token.
     * </pre>
     *
     * <code>repeated .ai.stigmer.agentic.session.v1.WorkspaceEntry workspace_entries = 4 [json_name = "workspaceEntries"];</code>
     */
    public Builder removeWorkspaceEntries(int index) {
      if (workspaceEntriesBuilder_ == null) {
        ensureWorkspaceEntriesIsMutable();
        workspaceEntries_.remove(index);
        onChanged();
      } else {
        workspaceEntriesBuilder_.remove(index);
      }
      return this;
    }
    /**
     * <pre>
     * Workspace the run's session operates on. Empty means no workspace.
     *
     * &#64;internal
     * Maps onto SessionSpec.workspace_entries of the session each run
     * creates. Unattended surfaces constrain sources in their handlers:
     * schedules accept git_repo only (no client is connected at fire
     * time to serve a local_path). Credentials (DD-018 D-4): the
     * provisioner resolves GITHUB_TOKEN from the merged environment;
     * for surfaces with no interactive caller the one supported
     * contract is an org-visibility Environment holding GITHUB_TOKEN
     * bound via environment_refs — the personal-environment fallback
     * resolves as-caller and is structurally closed to synthetic
     * accounts. Public repos need no token.
     * </pre>
     *
     * <code>repeated .ai.stigmer.agentic.session.v1.WorkspaceEntry workspace_entries = 4 [json_name = "workspaceEntries"];</code>
     */
    public ai.stigmer.agentic.session.v1.WorkspaceEntry.Builder getWorkspaceEntriesBuilder(
        int index) {
      return internalGetWorkspaceEntriesFieldBuilder().getBuilder(index);
    }
    /**
     * <pre>
     * Workspace the run's session operates on. Empty means no workspace.
     *
     * &#64;internal
     * Maps onto SessionSpec.workspace_entries of the session each run
     * creates. Unattended surfaces constrain sources in their handlers:
     * schedules accept git_repo only (no client is connected at fire
     * time to serve a local_path). Credentials (DD-018 D-4): the
     * provisioner resolves GITHUB_TOKEN from the merged environment;
     * for surfaces with no interactive caller the one supported
     * contract is an org-visibility Environment holding GITHUB_TOKEN
     * bound via environment_refs — the personal-environment fallback
     * resolves as-caller and is structurally closed to synthetic
     * accounts. Public repos need no token.
     * </pre>
     *
     * <code>repeated .ai.stigmer.agentic.session.v1.WorkspaceEntry workspace_entries = 4 [json_name = "workspaceEntries"];</code>
     */
    public ai.stigmer.agentic.session.v1.WorkspaceEntryOrBuilder getWorkspaceEntriesOrBuilder(
        int index) {
      if (workspaceEntriesBuilder_ == null) {
        return workspaceEntries_.get(index);  } else {
        return workspaceEntriesBuilder_.getMessageOrBuilder(index);
      }
    }
    /**
     * <pre>
     * Workspace the run's session operates on. Empty means no workspace.
     *
     * &#64;internal
     * Maps onto SessionSpec.workspace_entries of the session each run
     * creates. Unattended surfaces constrain sources in their handlers:
     * schedules accept git_repo only (no client is connected at fire
     * time to serve a local_path). Credentials (DD-018 D-4): the
     * provisioner resolves GITHUB_TOKEN from the merged environment;
     * for surfaces with no interactive caller the one supported
     * contract is an org-visibility Environment holding GITHUB_TOKEN
     * bound via environment_refs — the personal-environment fallback
     * resolves as-caller and is structurally closed to synthetic
     * accounts. Public repos need no token.
     * </pre>
     *
     * <code>repeated .ai.stigmer.agentic.session.v1.WorkspaceEntry workspace_entries = 4 [json_name = "workspaceEntries"];</code>
     */
    public java.util.List<? extends ai.stigmer.agentic.session.v1.WorkspaceEntryOrBuilder> 
         getWorkspaceEntriesOrBuilderList() {
      if (workspaceEntriesBuilder_ != null) {
        return workspaceEntriesBuilder_.getMessageOrBuilderList();
      } else {
        return java.util.Collections.unmodifiableList(workspaceEntries_);
      }
    }
    /**
     * <pre>
     * Workspace the run's session operates on. Empty means no workspace.
     *
     * &#64;internal
     * Maps onto SessionSpec.workspace_entries of the session each run
     * creates. Unattended surfaces constrain sources in their handlers:
     * schedules accept git_repo only (no client is connected at fire
     * time to serve a local_path). Credentials (DD-018 D-4): the
     * provisioner resolves GITHUB_TOKEN from the merged environment;
     * for surfaces with no interactive caller the one supported
     * contract is an org-visibility Environment holding GITHUB_TOKEN
     * bound via environment_refs — the personal-environment fallback
     * resolves as-caller and is structurally closed to synthetic
     * accounts. Public repos need no token.
     * </pre>
     *
     * <code>repeated .ai.stigmer.agentic.session.v1.WorkspaceEntry workspace_entries = 4 [json_name = "workspaceEntries"];</code>
     */
    public ai.stigmer.agentic.session.v1.WorkspaceEntry.Builder addWorkspaceEntriesBuilder() {
      return internalGetWorkspaceEntriesFieldBuilder().addBuilder(
          ai.stigmer.agentic.session.v1.WorkspaceEntry.getDefaultInstance());
    }
    /**
     * <pre>
     * Workspace the run's session operates on. Empty means no workspace.
     *
     * &#64;internal
     * Maps onto SessionSpec.workspace_entries of the session each run
     * creates. Unattended surfaces constrain sources in their handlers:
     * schedules accept git_repo only (no client is connected at fire
     * time to serve a local_path). Credentials (DD-018 D-4): the
     * provisioner resolves GITHUB_TOKEN from the merged environment;
     * for surfaces with no interactive caller the one supported
     * contract is an org-visibility Environment holding GITHUB_TOKEN
     * bound via environment_refs — the personal-environment fallback
     * resolves as-caller and is structurally closed to synthetic
     * accounts. Public repos need no token.
     * </pre>
     *
     * <code>repeated .ai.stigmer.agentic.session.v1.WorkspaceEntry workspace_entries = 4 [json_name = "workspaceEntries"];</code>
     */
    public ai.stigmer.agentic.session.v1.WorkspaceEntry.Builder addWorkspaceEntriesBuilder(
        int index) {
      return internalGetWorkspaceEntriesFieldBuilder().addBuilder(
          index, ai.stigmer.agentic.session.v1.WorkspaceEntry.getDefaultInstance());
    }
    /**
     * <pre>
     * Workspace the run's session operates on. Empty means no workspace.
     *
     * &#64;internal
     * Maps onto SessionSpec.workspace_entries of the session each run
     * creates. Unattended surfaces constrain sources in their handlers:
     * schedules accept git_repo only (no client is connected at fire
     * time to serve a local_path). Credentials (DD-018 D-4): the
     * provisioner resolves GITHUB_TOKEN from the merged environment;
     * for surfaces with no interactive caller the one supported
     * contract is an org-visibility Environment holding GITHUB_TOKEN
     * bound via environment_refs — the personal-environment fallback
     * resolves as-caller and is structurally closed to synthetic
     * accounts. Public repos need no token.
     * </pre>
     *
     * <code>repeated .ai.stigmer.agentic.session.v1.WorkspaceEntry workspace_entries = 4 [json_name = "workspaceEntries"];</code>
     */
    public java.util.List<ai.stigmer.agentic.session.v1.WorkspaceEntry.Builder> 
         getWorkspaceEntriesBuilderList() {
      return internalGetWorkspaceEntriesFieldBuilder().getBuilderList();
    }
    private com.google.protobuf.RepeatedFieldBuilder<
        ai.stigmer.agentic.session.v1.WorkspaceEntry, ai.stigmer.agentic.session.v1.WorkspaceEntry.Builder, ai.stigmer.agentic.session.v1.WorkspaceEntryOrBuilder> 
        internalGetWorkspaceEntriesFieldBuilder() {
      if (workspaceEntriesBuilder_ == null) {
        workspaceEntriesBuilder_ = new com.google.protobuf.RepeatedFieldBuilder<
            ai.stigmer.agentic.session.v1.WorkspaceEntry, ai.stigmer.agentic.session.v1.WorkspaceEntry.Builder, ai.stigmer.agentic.session.v1.WorkspaceEntryOrBuilder>(
                workspaceEntries_,
                ((bitField0_ & 0x00000008) != 0),
                getParentForChildren(),
                isClean());
        workspaceEntries_ = null;
      }
      return workspaceEntriesBuilder_;
    }

    private java.util.List<ai.stigmer.commons.apiresource.ApiResourceReference> environmentRefs_ =
      java.util.Collections.emptyList();
    private void ensureEnvironmentRefsIsMutable() {
      if (!((bitField0_ & 0x00000010) != 0)) {
        environmentRefs_ = new java.util.ArrayList<ai.stigmer.commons.apiresource.ApiResourceReference>(environmentRefs_);
        bitField0_ |= 0x00000010;
       }
    }

    private com.google.protobuf.RepeatedFieldBuilder<
        ai.stigmer.commons.apiresource.ApiResourceReference, ai.stigmer.commons.apiresource.ApiResourceReference.Builder, ai.stigmer.commons.apiresource.ApiResourceReferenceOrBuilder> environmentRefsBuilder_;

    /**
     * <pre>
     * References to Environment resources whose values are provided to
     * the runs this invocation creates.
     *
     * This is how a tool-using agent becomes runnable unattended: bind
     * an org-shared environment holding the needed credentials (for
     * example an MCP server's shared secret), and the runs receive its
     * values at runtime. The agent and its default instance stay
     * untouched.
     *
     * &#64;internal
     * The AgentShare/AgentChannel/Schedule environment_refs lineage
     * (project DD-017 D-2). Resolution stays with the embedding
     * surface's pipeline (CreateExecutionContextStep branch, claim-
     * driven, LOWEST merge priority — instance refs and runtime_env
     * override on key conflicts). No write-time existence or visibility
     * check: enforcement lives solely at runtime resolution, which
     * fails closed.
     * </pre>
     *
     * <code>repeated .ai.stigmer.commons.apiresource.ApiResourceReference environment_refs = 5 [json_name = "environmentRefs", (.buf.validate.field) = { ... }</code>
     */
    public java.util.List<ai.stigmer.commons.apiresource.ApiResourceReference> getEnvironmentRefsList() {
      if (environmentRefsBuilder_ == null) {
        return java.util.Collections.unmodifiableList(environmentRefs_);
      } else {
        return environmentRefsBuilder_.getMessageList();
      }
    }
    /**
     * <pre>
     * References to Environment resources whose values are provided to
     * the runs this invocation creates.
     *
     * This is how a tool-using agent becomes runnable unattended: bind
     * an org-shared environment holding the needed credentials (for
     * example an MCP server's shared secret), and the runs receive its
     * values at runtime. The agent and its default instance stay
     * untouched.
     *
     * &#64;internal
     * The AgentShare/AgentChannel/Schedule environment_refs lineage
     * (project DD-017 D-2). Resolution stays with the embedding
     * surface's pipeline (CreateExecutionContextStep branch, claim-
     * driven, LOWEST merge priority — instance refs and runtime_env
     * override on key conflicts). No write-time existence or visibility
     * check: enforcement lives solely at runtime resolution, which
     * fails closed.
     * </pre>
     *
     * <code>repeated .ai.stigmer.commons.apiresource.ApiResourceReference environment_refs = 5 [json_name = "environmentRefs", (.buf.validate.field) = { ... }</code>
     */
    public int getEnvironmentRefsCount() {
      if (environmentRefsBuilder_ == null) {
        return environmentRefs_.size();
      } else {
        return environmentRefsBuilder_.getCount();
      }
    }
    /**
     * <pre>
     * References to Environment resources whose values are provided to
     * the runs this invocation creates.
     *
     * This is how a tool-using agent becomes runnable unattended: bind
     * an org-shared environment holding the needed credentials (for
     * example an MCP server's shared secret), and the runs receive its
     * values at runtime. The agent and its default instance stay
     * untouched.
     *
     * &#64;internal
     * The AgentShare/AgentChannel/Schedule environment_refs lineage
     * (project DD-017 D-2). Resolution stays with the embedding
     * surface's pipeline (CreateExecutionContextStep branch, claim-
     * driven, LOWEST merge priority — instance refs and runtime_env
     * override on key conflicts). No write-time existence or visibility
     * check: enforcement lives solely at runtime resolution, which
     * fails closed.
     * </pre>
     *
     * <code>repeated .ai.stigmer.commons.apiresource.ApiResourceReference environment_refs = 5 [json_name = "environmentRefs", (.buf.validate.field) = { ... }</code>
     */
    public ai.stigmer.commons.apiresource.ApiResourceReference getEnvironmentRefs(int index) {
      if (environmentRefsBuilder_ == null) {
        return environmentRefs_.get(index);
      } else {
        return environmentRefsBuilder_.getMessage(index);
      }
    }
    /**
     * <pre>
     * References to Environment resources whose values are provided to
     * the runs this invocation creates.
     *
     * This is how a tool-using agent becomes runnable unattended: bind
     * an org-shared environment holding the needed credentials (for
     * example an MCP server's shared secret), and the runs receive its
     * values at runtime. The agent and its default instance stay
     * untouched.
     *
     * &#64;internal
     * The AgentShare/AgentChannel/Schedule environment_refs lineage
     * (project DD-017 D-2). Resolution stays with the embedding
     * surface's pipeline (CreateExecutionContextStep branch, claim-
     * driven, LOWEST merge priority — instance refs and runtime_env
     * override on key conflicts). No write-time existence or visibility
     * check: enforcement lives solely at runtime resolution, which
     * fails closed.
     * </pre>
     *
     * <code>repeated .ai.stigmer.commons.apiresource.ApiResourceReference environment_refs = 5 [json_name = "environmentRefs", (.buf.validate.field) = { ... }</code>
     */
    public Builder setEnvironmentRefs(
        int index, ai.stigmer.commons.apiresource.ApiResourceReference value) {
      if (environmentRefsBuilder_ == null) {
        if (value == null) {
          throw new NullPointerException();
        }
        ensureEnvironmentRefsIsMutable();
        environmentRefs_.set(index, value);
        onChanged();
      } else {
        environmentRefsBuilder_.setMessage(index, value);
      }
      return this;
    }
    /**
     * <pre>
     * References to Environment resources whose values are provided to
     * the runs this invocation creates.
     *
     * This is how a tool-using agent becomes runnable unattended: bind
     * an org-shared environment holding the needed credentials (for
     * example an MCP server's shared secret), and the runs receive its
     * values at runtime. The agent and its default instance stay
     * untouched.
     *
     * &#64;internal
     * The AgentShare/AgentChannel/Schedule environment_refs lineage
     * (project DD-017 D-2). Resolution stays with the embedding
     * surface's pipeline (CreateExecutionContextStep branch, claim-
     * driven, LOWEST merge priority — instance refs and runtime_env
     * override on key conflicts). No write-time existence or visibility
     * check: enforcement lives solely at runtime resolution, which
     * fails closed.
     * </pre>
     *
     * <code>repeated .ai.stigmer.commons.apiresource.ApiResourceReference environment_refs = 5 [json_name = "environmentRefs", (.buf.validate.field) = { ... }</code>
     */
    public Builder setEnvironmentRefs(
        int index, ai.stigmer.commons.apiresource.ApiResourceReference.Builder builderForValue) {
      if (environmentRefsBuilder_ == null) {
        ensureEnvironmentRefsIsMutable();
        environmentRefs_.set(index, builderForValue.build());
        onChanged();
      } else {
        environmentRefsBuilder_.setMessage(index, builderForValue.build());
      }
      return this;
    }
    /**
     * <pre>
     * References to Environment resources whose values are provided to
     * the runs this invocation creates.
     *
     * This is how a tool-using agent becomes runnable unattended: bind
     * an org-shared environment holding the needed credentials (for
     * example an MCP server's shared secret), and the runs receive its
     * values at runtime. The agent and its default instance stay
     * untouched.
     *
     * &#64;internal
     * The AgentShare/AgentChannel/Schedule environment_refs lineage
     * (project DD-017 D-2). Resolution stays with the embedding
     * surface's pipeline (CreateExecutionContextStep branch, claim-
     * driven, LOWEST merge priority — instance refs and runtime_env
     * override on key conflicts). No write-time existence or visibility
     * check: enforcement lives solely at runtime resolution, which
     * fails closed.
     * </pre>
     *
     * <code>repeated .ai.stigmer.commons.apiresource.ApiResourceReference environment_refs = 5 [json_name = "environmentRefs", (.buf.validate.field) = { ... }</code>
     */
    public Builder addEnvironmentRefs(ai.stigmer.commons.apiresource.ApiResourceReference value) {
      if (environmentRefsBuilder_ == null) {
        if (value == null) {
          throw new NullPointerException();
        }
        ensureEnvironmentRefsIsMutable();
        environmentRefs_.add(value);
        onChanged();
      } else {
        environmentRefsBuilder_.addMessage(value);
      }
      return this;
    }
    /**
     * <pre>
     * References to Environment resources whose values are provided to
     * the runs this invocation creates.
     *
     * This is how a tool-using agent becomes runnable unattended: bind
     * an org-shared environment holding the needed credentials (for
     * example an MCP server's shared secret), and the runs receive its
     * values at runtime. The agent and its default instance stay
     * untouched.
     *
     * &#64;internal
     * The AgentShare/AgentChannel/Schedule environment_refs lineage
     * (project DD-017 D-2). Resolution stays with the embedding
     * surface's pipeline (CreateExecutionContextStep branch, claim-
     * driven, LOWEST merge priority — instance refs and runtime_env
     * override on key conflicts). No write-time existence or visibility
     * check: enforcement lives solely at runtime resolution, which
     * fails closed.
     * </pre>
     *
     * <code>repeated .ai.stigmer.commons.apiresource.ApiResourceReference environment_refs = 5 [json_name = "environmentRefs", (.buf.validate.field) = { ... }</code>
     */
    public Builder addEnvironmentRefs(
        int index, ai.stigmer.commons.apiresource.ApiResourceReference value) {
      if (environmentRefsBuilder_ == null) {
        if (value == null) {
          throw new NullPointerException();
        }
        ensureEnvironmentRefsIsMutable();
        environmentRefs_.add(index, value);
        onChanged();
      } else {
        environmentRefsBuilder_.addMessage(index, value);
      }
      return this;
    }
    /**
     * <pre>
     * References to Environment resources whose values are provided to
     * the runs this invocation creates.
     *
     * This is how a tool-using agent becomes runnable unattended: bind
     * an org-shared environment holding the needed credentials (for
     * example an MCP server's shared secret), and the runs receive its
     * values at runtime. The agent and its default instance stay
     * untouched.
     *
     * &#64;internal
     * The AgentShare/AgentChannel/Schedule environment_refs lineage
     * (project DD-017 D-2). Resolution stays with the embedding
     * surface's pipeline (CreateExecutionContextStep branch, claim-
     * driven, LOWEST merge priority — instance refs and runtime_env
     * override on key conflicts). No write-time existence or visibility
     * check: enforcement lives solely at runtime resolution, which
     * fails closed.
     * </pre>
     *
     * <code>repeated .ai.stigmer.commons.apiresource.ApiResourceReference environment_refs = 5 [json_name = "environmentRefs", (.buf.validate.field) = { ... }</code>
     */
    public Builder addEnvironmentRefs(
        ai.stigmer.commons.apiresource.ApiResourceReference.Builder builderForValue) {
      if (environmentRefsBuilder_ == null) {
        ensureEnvironmentRefsIsMutable();
        environmentRefs_.add(builderForValue.build());
        onChanged();
      } else {
        environmentRefsBuilder_.addMessage(builderForValue.build());
      }
      return this;
    }
    /**
     * <pre>
     * References to Environment resources whose values are provided to
     * the runs this invocation creates.
     *
     * This is how a tool-using agent becomes runnable unattended: bind
     * an org-shared environment holding the needed credentials (for
     * example an MCP server's shared secret), and the runs receive its
     * values at runtime. The agent and its default instance stay
     * untouched.
     *
     * &#64;internal
     * The AgentShare/AgentChannel/Schedule environment_refs lineage
     * (project DD-017 D-2). Resolution stays with the embedding
     * surface's pipeline (CreateExecutionContextStep branch, claim-
     * driven, LOWEST merge priority — instance refs and runtime_env
     * override on key conflicts). No write-time existence or visibility
     * check: enforcement lives solely at runtime resolution, which
     * fails closed.
     * </pre>
     *
     * <code>repeated .ai.stigmer.commons.apiresource.ApiResourceReference environment_refs = 5 [json_name = "environmentRefs", (.buf.validate.field) = { ... }</code>
     */
    public Builder addEnvironmentRefs(
        int index, ai.stigmer.commons.apiresource.ApiResourceReference.Builder builderForValue) {
      if (environmentRefsBuilder_ == null) {
        ensureEnvironmentRefsIsMutable();
        environmentRefs_.add(index, builderForValue.build());
        onChanged();
      } else {
        environmentRefsBuilder_.addMessage(index, builderForValue.build());
      }
      return this;
    }
    /**
     * <pre>
     * References to Environment resources whose values are provided to
     * the runs this invocation creates.
     *
     * This is how a tool-using agent becomes runnable unattended: bind
     * an org-shared environment holding the needed credentials (for
     * example an MCP server's shared secret), and the runs receive its
     * values at runtime. The agent and its default instance stay
     * untouched.
     *
     * &#64;internal
     * The AgentShare/AgentChannel/Schedule environment_refs lineage
     * (project DD-017 D-2). Resolution stays with the embedding
     * surface's pipeline (CreateExecutionContextStep branch, claim-
     * driven, LOWEST merge priority — instance refs and runtime_env
     * override on key conflicts). No write-time existence or visibility
     * check: enforcement lives solely at runtime resolution, which
     * fails closed.
     * </pre>
     *
     * <code>repeated .ai.stigmer.commons.apiresource.ApiResourceReference environment_refs = 5 [json_name = "environmentRefs", (.buf.validate.field) = { ... }</code>
     */
    public Builder addAllEnvironmentRefs(
        java.lang.Iterable<? extends ai.stigmer.commons.apiresource.ApiResourceReference> values) {
      if (environmentRefsBuilder_ == null) {
        ensureEnvironmentRefsIsMutable();
        com.google.protobuf.AbstractMessageLite.Builder.addAll(
            values, environmentRefs_);
        onChanged();
      } else {
        environmentRefsBuilder_.addAllMessages(values);
      }
      return this;
    }
    /**
     * <pre>
     * References to Environment resources whose values are provided to
     * the runs this invocation creates.
     *
     * This is how a tool-using agent becomes runnable unattended: bind
     * an org-shared environment holding the needed credentials (for
     * example an MCP server's shared secret), and the runs receive its
     * values at runtime. The agent and its default instance stay
     * untouched.
     *
     * &#64;internal
     * The AgentShare/AgentChannel/Schedule environment_refs lineage
     * (project DD-017 D-2). Resolution stays with the embedding
     * surface's pipeline (CreateExecutionContextStep branch, claim-
     * driven, LOWEST merge priority — instance refs and runtime_env
     * override on key conflicts). No write-time existence or visibility
     * check: enforcement lives solely at runtime resolution, which
     * fails closed.
     * </pre>
     *
     * <code>repeated .ai.stigmer.commons.apiresource.ApiResourceReference environment_refs = 5 [json_name = "environmentRefs", (.buf.validate.field) = { ... }</code>
     */
    public Builder clearEnvironmentRefs() {
      if (environmentRefsBuilder_ == null) {
        environmentRefs_ = java.util.Collections.emptyList();
        bitField0_ = (bitField0_ & ~0x00000010);
        onChanged();
      } else {
        environmentRefsBuilder_.clear();
      }
      return this;
    }
    /**
     * <pre>
     * References to Environment resources whose values are provided to
     * the runs this invocation creates.
     *
     * This is how a tool-using agent becomes runnable unattended: bind
     * an org-shared environment holding the needed credentials (for
     * example an MCP server's shared secret), and the runs receive its
     * values at runtime. The agent and its default instance stay
     * untouched.
     *
     * &#64;internal
     * The AgentShare/AgentChannel/Schedule environment_refs lineage
     * (project DD-017 D-2). Resolution stays with the embedding
     * surface's pipeline (CreateExecutionContextStep branch, claim-
     * driven, LOWEST merge priority — instance refs and runtime_env
     * override on key conflicts). No write-time existence or visibility
     * check: enforcement lives solely at runtime resolution, which
     * fails closed.
     * </pre>
     *
     * <code>repeated .ai.stigmer.commons.apiresource.ApiResourceReference environment_refs = 5 [json_name = "environmentRefs", (.buf.validate.field) = { ... }</code>
     */
    public Builder removeEnvironmentRefs(int index) {
      if (environmentRefsBuilder_ == null) {
        ensureEnvironmentRefsIsMutable();
        environmentRefs_.remove(index);
        onChanged();
      } else {
        environmentRefsBuilder_.remove(index);
      }
      return this;
    }
    /**
     * <pre>
     * References to Environment resources whose values are provided to
     * the runs this invocation creates.
     *
     * This is how a tool-using agent becomes runnable unattended: bind
     * an org-shared environment holding the needed credentials (for
     * example an MCP server's shared secret), and the runs receive its
     * values at runtime. The agent and its default instance stay
     * untouched.
     *
     * &#64;internal
     * The AgentShare/AgentChannel/Schedule environment_refs lineage
     * (project DD-017 D-2). Resolution stays with the embedding
     * surface's pipeline (CreateExecutionContextStep branch, claim-
     * driven, LOWEST merge priority — instance refs and runtime_env
     * override on key conflicts). No write-time existence or visibility
     * check: enforcement lives solely at runtime resolution, which
     * fails closed.
     * </pre>
     *
     * <code>repeated .ai.stigmer.commons.apiresource.ApiResourceReference environment_refs = 5 [json_name = "environmentRefs", (.buf.validate.field) = { ... }</code>
     */
    public ai.stigmer.commons.apiresource.ApiResourceReference.Builder getEnvironmentRefsBuilder(
        int index) {
      return internalGetEnvironmentRefsFieldBuilder().getBuilder(index);
    }
    /**
     * <pre>
     * References to Environment resources whose values are provided to
     * the runs this invocation creates.
     *
     * This is how a tool-using agent becomes runnable unattended: bind
     * an org-shared environment holding the needed credentials (for
     * example an MCP server's shared secret), and the runs receive its
     * values at runtime. The agent and its default instance stay
     * untouched.
     *
     * &#64;internal
     * The AgentShare/AgentChannel/Schedule environment_refs lineage
     * (project DD-017 D-2). Resolution stays with the embedding
     * surface's pipeline (CreateExecutionContextStep branch, claim-
     * driven, LOWEST merge priority — instance refs and runtime_env
     * override on key conflicts). No write-time existence or visibility
     * check: enforcement lives solely at runtime resolution, which
     * fails closed.
     * </pre>
     *
     * <code>repeated .ai.stigmer.commons.apiresource.ApiResourceReference environment_refs = 5 [json_name = "environmentRefs", (.buf.validate.field) = { ... }</code>
     */
    public ai.stigmer.commons.apiresource.ApiResourceReferenceOrBuilder getEnvironmentRefsOrBuilder(
        int index) {
      if (environmentRefsBuilder_ == null) {
        return environmentRefs_.get(index);  } else {
        return environmentRefsBuilder_.getMessageOrBuilder(index);
      }
    }
    /**
     * <pre>
     * References to Environment resources whose values are provided to
     * the runs this invocation creates.
     *
     * This is how a tool-using agent becomes runnable unattended: bind
     * an org-shared environment holding the needed credentials (for
     * example an MCP server's shared secret), and the runs receive its
     * values at runtime. The agent and its default instance stay
     * untouched.
     *
     * &#64;internal
     * The AgentShare/AgentChannel/Schedule environment_refs lineage
     * (project DD-017 D-2). Resolution stays with the embedding
     * surface's pipeline (CreateExecutionContextStep branch, claim-
     * driven, LOWEST merge priority — instance refs and runtime_env
     * override on key conflicts). No write-time existence or visibility
     * check: enforcement lives solely at runtime resolution, which
     * fails closed.
     * </pre>
     *
     * <code>repeated .ai.stigmer.commons.apiresource.ApiResourceReference environment_refs = 5 [json_name = "environmentRefs", (.buf.validate.field) = { ... }</code>
     */
    public java.util.List<? extends ai.stigmer.commons.apiresource.ApiResourceReferenceOrBuilder> 
         getEnvironmentRefsOrBuilderList() {
      if (environmentRefsBuilder_ != null) {
        return environmentRefsBuilder_.getMessageOrBuilderList();
      } else {
        return java.util.Collections.unmodifiableList(environmentRefs_);
      }
    }
    /**
     * <pre>
     * References to Environment resources whose values are provided to
     * the runs this invocation creates.
     *
     * This is how a tool-using agent becomes runnable unattended: bind
     * an org-shared environment holding the needed credentials (for
     * example an MCP server's shared secret), and the runs receive its
     * values at runtime. The agent and its default instance stay
     * untouched.
     *
     * &#64;internal
     * The AgentShare/AgentChannel/Schedule environment_refs lineage
     * (project DD-017 D-2). Resolution stays with the embedding
     * surface's pipeline (CreateExecutionContextStep branch, claim-
     * driven, LOWEST merge priority — instance refs and runtime_env
     * override on key conflicts). No write-time existence or visibility
     * check: enforcement lives solely at runtime resolution, which
     * fails closed.
     * </pre>
     *
     * <code>repeated .ai.stigmer.commons.apiresource.ApiResourceReference environment_refs = 5 [json_name = "environmentRefs", (.buf.validate.field) = { ... }</code>
     */
    public ai.stigmer.commons.apiresource.ApiResourceReference.Builder addEnvironmentRefsBuilder() {
      return internalGetEnvironmentRefsFieldBuilder().addBuilder(
          ai.stigmer.commons.apiresource.ApiResourceReference.getDefaultInstance());
    }
    /**
     * <pre>
     * References to Environment resources whose values are provided to
     * the runs this invocation creates.
     *
     * This is how a tool-using agent becomes runnable unattended: bind
     * an org-shared environment holding the needed credentials (for
     * example an MCP server's shared secret), and the runs receive its
     * values at runtime. The agent and its default instance stay
     * untouched.
     *
     * &#64;internal
     * The AgentShare/AgentChannel/Schedule environment_refs lineage
     * (project DD-017 D-2). Resolution stays with the embedding
     * surface's pipeline (CreateExecutionContextStep branch, claim-
     * driven, LOWEST merge priority — instance refs and runtime_env
     * override on key conflicts). No write-time existence or visibility
     * check: enforcement lives solely at runtime resolution, which
     * fails closed.
     * </pre>
     *
     * <code>repeated .ai.stigmer.commons.apiresource.ApiResourceReference environment_refs = 5 [json_name = "environmentRefs", (.buf.validate.field) = { ... }</code>
     */
    public ai.stigmer.commons.apiresource.ApiResourceReference.Builder addEnvironmentRefsBuilder(
        int index) {
      return internalGetEnvironmentRefsFieldBuilder().addBuilder(
          index, ai.stigmer.commons.apiresource.ApiResourceReference.getDefaultInstance());
    }
    /**
     * <pre>
     * References to Environment resources whose values are provided to
     * the runs this invocation creates.
     *
     * This is how a tool-using agent becomes runnable unattended: bind
     * an org-shared environment holding the needed credentials (for
     * example an MCP server's shared secret), and the runs receive its
     * values at runtime. The agent and its default instance stay
     * untouched.
     *
     * &#64;internal
     * The AgentShare/AgentChannel/Schedule environment_refs lineage
     * (project DD-017 D-2). Resolution stays with the embedding
     * surface's pipeline (CreateExecutionContextStep branch, claim-
     * driven, LOWEST merge priority — instance refs and runtime_env
     * override on key conflicts). No write-time existence or visibility
     * check: enforcement lives solely at runtime resolution, which
     * fails closed.
     * </pre>
     *
     * <code>repeated .ai.stigmer.commons.apiresource.ApiResourceReference environment_refs = 5 [json_name = "environmentRefs", (.buf.validate.field) = { ... }</code>
     */
    public java.util.List<ai.stigmer.commons.apiresource.ApiResourceReference.Builder> 
         getEnvironmentRefsBuilderList() {
      return internalGetEnvironmentRefsFieldBuilder().getBuilderList();
    }
    private com.google.protobuf.RepeatedFieldBuilder<
        ai.stigmer.commons.apiresource.ApiResourceReference, ai.stigmer.commons.apiresource.ApiResourceReference.Builder, ai.stigmer.commons.apiresource.ApiResourceReferenceOrBuilder> 
        internalGetEnvironmentRefsFieldBuilder() {
      if (environmentRefsBuilder_ == null) {
        environmentRefsBuilder_ = new com.google.protobuf.RepeatedFieldBuilder<
            ai.stigmer.commons.apiresource.ApiResourceReference, ai.stigmer.commons.apiresource.ApiResourceReference.Builder, ai.stigmer.commons.apiresource.ApiResourceReferenceOrBuilder>(
                environmentRefs_,
                ((bitField0_ & 0x00000010) != 0),
                getParentForChildren(),
                isClean());
        environmentRefs_ = null;
      }
      return environmentRefsBuilder_;
    }

    private ai.stigmer.agentic.agentexecution.v1.RunConfig runConfig_;
    private com.google.protobuf.SingleFieldBuilder<
        ai.stigmer.agentic.agentexecution.v1.RunConfig, ai.stigmer.agentic.agentexecution.v1.RunConfig.Builder, ai.stigmer.agentic.agentexecution.v1.RunConfigOrBuilder> runConfigBuilder_;
    /**
     * <pre>
     * Per-invocation model choice and run bounds. Unset fields inherit
     * the embedding surface's platform execution profile.
     * </pre>
     *
     * <code>.ai.stigmer.agentic.agentexecution.v1.RunConfig run_config = 6 [json_name = "runConfig"];</code>
     * @return Whether the runConfig field is set.
     */
    public boolean hasRunConfig() {
      return ((bitField0_ & 0x00000020) != 0);
    }
    /**
     * <pre>
     * Per-invocation model choice and run bounds. Unset fields inherit
     * the embedding surface's platform execution profile.
     * </pre>
     *
     * <code>.ai.stigmer.agentic.agentexecution.v1.RunConfig run_config = 6 [json_name = "runConfig"];</code>
     * @return The runConfig.
     */
    public ai.stigmer.agentic.agentexecution.v1.RunConfig getRunConfig() {
      if (runConfigBuilder_ == null) {
        return runConfig_ == null ? ai.stigmer.agentic.agentexecution.v1.RunConfig.getDefaultInstance() : runConfig_;
      } else {
        return runConfigBuilder_.getMessage();
      }
    }
    /**
     * <pre>
     * Per-invocation model choice and run bounds. Unset fields inherit
     * the embedding surface's platform execution profile.
     * </pre>
     *
     * <code>.ai.stigmer.agentic.agentexecution.v1.RunConfig run_config = 6 [json_name = "runConfig"];</code>
     */
    public Builder setRunConfig(ai.stigmer.agentic.agentexecution.v1.RunConfig value) {
      if (runConfigBuilder_ == null) {
        if (value == null) {
          throw new NullPointerException();
        }
        runConfig_ = value;
      } else {
        runConfigBuilder_.setMessage(value);
      }
      bitField0_ |= 0x00000020;
      onChanged();
      return this;
    }
    /**
     * <pre>
     * Per-invocation model choice and run bounds. Unset fields inherit
     * the embedding surface's platform execution profile.
     * </pre>
     *
     * <code>.ai.stigmer.agentic.agentexecution.v1.RunConfig run_config = 6 [json_name = "runConfig"];</code>
     */
    public Builder setRunConfig(
        ai.stigmer.agentic.agentexecution.v1.RunConfig.Builder builderForValue) {
      if (runConfigBuilder_ == null) {
        runConfig_ = builderForValue.build();
      } else {
        runConfigBuilder_.setMessage(builderForValue.build());
      }
      bitField0_ |= 0x00000020;
      onChanged();
      return this;
    }
    /**
     * <pre>
     * Per-invocation model choice and run bounds. Unset fields inherit
     * the embedding surface's platform execution profile.
     * </pre>
     *
     * <code>.ai.stigmer.agentic.agentexecution.v1.RunConfig run_config = 6 [json_name = "runConfig"];</code>
     */
    public Builder mergeRunConfig(ai.stigmer.agentic.agentexecution.v1.RunConfig value) {
      if (runConfigBuilder_ == null) {
        if (((bitField0_ & 0x00000020) != 0) &&
          runConfig_ != null &&
          runConfig_ != ai.stigmer.agentic.agentexecution.v1.RunConfig.getDefaultInstance()) {
          getRunConfigBuilder().mergeFrom(value);
        } else {
          runConfig_ = value;
        }
      } else {
        runConfigBuilder_.mergeFrom(value);
      }
      if (runConfig_ != null) {
        bitField0_ |= 0x00000020;
        onChanged();
      }
      return this;
    }
    /**
     * <pre>
     * Per-invocation model choice and run bounds. Unset fields inherit
     * the embedding surface's platform execution profile.
     * </pre>
     *
     * <code>.ai.stigmer.agentic.agentexecution.v1.RunConfig run_config = 6 [json_name = "runConfig"];</code>
     */
    public Builder clearRunConfig() {
      bitField0_ = (bitField0_ & ~0x00000020);
      runConfig_ = null;
      if (runConfigBuilder_ != null) {
        runConfigBuilder_.dispose();
        runConfigBuilder_ = null;
      }
      onChanged();
      return this;
    }
    /**
     * <pre>
     * Per-invocation model choice and run bounds. Unset fields inherit
     * the embedding surface's platform execution profile.
     * </pre>
     *
     * <code>.ai.stigmer.agentic.agentexecution.v1.RunConfig run_config = 6 [json_name = "runConfig"];</code>
     */
    public ai.stigmer.agentic.agentexecution.v1.RunConfig.Builder getRunConfigBuilder() {
      bitField0_ |= 0x00000020;
      onChanged();
      return internalGetRunConfigFieldBuilder().getBuilder();
    }
    /**
     * <pre>
     * Per-invocation model choice and run bounds. Unset fields inherit
     * the embedding surface's platform execution profile.
     * </pre>
     *
     * <code>.ai.stigmer.agentic.agentexecution.v1.RunConfig run_config = 6 [json_name = "runConfig"];</code>
     */
    public ai.stigmer.agentic.agentexecution.v1.RunConfigOrBuilder getRunConfigOrBuilder() {
      if (runConfigBuilder_ != null) {
        return runConfigBuilder_.getMessageOrBuilder();
      } else {
        return runConfig_ == null ?
            ai.stigmer.agentic.agentexecution.v1.RunConfig.getDefaultInstance() : runConfig_;
      }
    }
    /**
     * <pre>
     * Per-invocation model choice and run bounds. Unset fields inherit
     * the embedding surface's platform execution profile.
     * </pre>
     *
     * <code>.ai.stigmer.agentic.agentexecution.v1.RunConfig run_config = 6 [json_name = "runConfig"];</code>
     */
    private com.google.protobuf.SingleFieldBuilder<
        ai.stigmer.agentic.agentexecution.v1.RunConfig, ai.stigmer.agentic.agentexecution.v1.RunConfig.Builder, ai.stigmer.agentic.agentexecution.v1.RunConfigOrBuilder> 
        internalGetRunConfigFieldBuilder() {
      if (runConfigBuilder_ == null) {
        runConfigBuilder_ = new com.google.protobuf.SingleFieldBuilder<
            ai.stigmer.agentic.agentexecution.v1.RunConfig, ai.stigmer.agentic.agentexecution.v1.RunConfig.Builder, ai.stigmer.agentic.agentexecution.v1.RunConfigOrBuilder>(
                getRunConfig(),
                getParentForChildren(),
                isClean());
        runConfig_ = null;
      }
      return runConfigBuilder_;
    }

    // @@protoc_insertion_point(builder_scope:ai.stigmer.agentic.agentexecution.v1.AgentInvocation)
  }

  // @@protoc_insertion_point(class_scope:ai.stigmer.agentic.agentexecution.v1.AgentInvocation)
  private static final ai.stigmer.agentic.agentexecution.v1.AgentInvocation DEFAULT_INSTANCE;
  static {
    DEFAULT_INSTANCE = new ai.stigmer.agentic.agentexecution.v1.AgentInvocation();
  }

  public static ai.stigmer.agentic.agentexecution.v1.AgentInvocation getDefaultInstance() {
    return DEFAULT_INSTANCE;
  }

  private static final com.google.protobuf.Parser<AgentInvocation>
      PARSER = new com.google.protobuf.AbstractParser<AgentInvocation>() {
    @java.lang.Override
    public AgentInvocation parsePartialFrom(
        com.google.protobuf.CodedInputStream input,
        com.google.protobuf.ExtensionRegistryLite extensionRegistry)
        throws com.google.protobuf.InvalidProtocolBufferException {
      Builder builder = newBuilder();
      try {
        builder.mergeFrom(input, extensionRegistry);
      } catch (com.google.protobuf.InvalidProtocolBufferException e) {
        throw e.setUnfinishedMessage(builder.buildPartial());
      } catch (com.google.protobuf.UninitializedMessageException e) {
        throw e.asInvalidProtocolBufferException().setUnfinishedMessage(builder.buildPartial());
      } catch (java.io.IOException e) {
        throw new com.google.protobuf.InvalidProtocolBufferException(e)
            .setUnfinishedMessage(builder.buildPartial());
      }
      return builder.buildPartial();
    }
  };

  public static com.google.protobuf.Parser<AgentInvocation> parser() {
    return PARSER;
  }

  @java.lang.Override
  public com.google.protobuf.Parser<AgentInvocation> getParserForType() {
    return PARSER;
  }

  @java.lang.Override
  public ai.stigmer.agentic.agentexecution.v1.AgentInvocation getDefaultInstanceForType() {
    return DEFAULT_INSTANCE;
  }

}

