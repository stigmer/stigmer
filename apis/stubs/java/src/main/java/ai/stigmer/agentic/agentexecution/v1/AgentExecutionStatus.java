// Generated by the protocol buffer compiler.  DO NOT EDIT!
// NO CHECKED-IN PROTOBUF GENCODE
// source: ai/stigmer/agentic/agentexecution/v1/api.proto
// Protobuf Java Version: 4.34.0

package ai.stigmer.agentic.agentexecution.v1;

/**
 * <pre>
 * AgentExecutionStatus contains all execution results and state.
 * Everything populated during or after execution goes here, not in spec.
 * </pre>
 *
 * Protobuf type {@code ai.stigmer.agentic.agentexecution.v1.AgentExecutionStatus}
 */
@com.google.protobuf.Generated
public final class AgentExecutionStatus extends
    com.google.protobuf.GeneratedMessage implements
    // @@protoc_insertion_point(message_implements:ai.stigmer.agentic.agentexecution.v1.AgentExecutionStatus)
    AgentExecutionStatusOrBuilder {
private static final long serialVersionUID = 0L;
  static {
    com.google.protobuf.RuntimeVersion.validateProtobufGencodeVersion(
      com.google.protobuf.RuntimeVersion.RuntimeDomain.PUBLIC,
      /* major= */ 4,
      /* minor= */ 34,
      /* patch= */ 0,
      /* suffix= */ "",
      "AgentExecutionStatus");
  }
  // Use AgentExecutionStatus.newBuilder() to construct.
  private AgentExecutionStatus(com.google.protobuf.GeneratedMessage.Builder<?> builder) {
    super(builder);
  }
  private AgentExecutionStatus() {
    messages_ = java.util.Collections.emptyList();
    phase_ = 0;
    subAgentExecutions_ = java.util.Collections.emptyList();
    error_ = "";
    startedAt_ = "";
    completedAt_ = "";
    callbackToken_ = com.google.protobuf.ByteString.EMPTY;
    pendingApprovals_ = java.util.Collections.emptyList();
    artifacts_ = java.util.Collections.emptyList();
    workspaceWriteBacks_ = java.util.Collections.emptyList();
    fileChangeSets_ = java.util.Collections.emptyList();
  }

  public static final com.google.protobuf.Descriptors.Descriptor
      getDescriptor() {
    return ai.stigmer.agentic.agentexecution.v1.ApiProto.internal_static_ai_stigmer_agentic_agentexecution_v1_AgentExecutionStatus_descriptor;
  }

  @java.lang.Override
  public com.google.protobuf.Descriptors.Descriptor getDescriptorForType() {
    return ai.stigmer.agentic.agentexecution.v1.ApiProto.internal_static_ai_stigmer_agentic_agentexecution_v1_AgentExecutionStatus_descriptor;
  }

  @SuppressWarnings({"rawtypes"})
  @java.lang.Override
  protected com.google.protobuf.MapFieldReflectionAccessor internalGetMapFieldReflection(
      int number) {
    switch (number) {
      case 9:
        return internalGetTodos();
      default:
        throw new RuntimeException(
            "Invalid map field number: " + number);
    }
  }
  @java.lang.Override
  protected com.google.protobuf.GeneratedMessage.FieldAccessorTable
      internalGetFieldAccessorTable() {
    return ai.stigmer.agentic.agentexecution.v1.ApiProto.internal_static_ai_stigmer_agentic_agentexecution_v1_AgentExecutionStatus_fieldAccessorTable
        .ensureFieldAccessorsInitialized(
            ai.stigmer.agentic.agentexecution.v1.AgentExecutionStatus.class, ai.stigmer.agentic.agentexecution.v1.AgentExecutionStatus.Builder.class);
  }

  private int bitField0_;
  public static final int AUDIT_FIELD_NUMBER = 99;
  private ai.stigmer.commons.apiresource.ApiResourceAudit audit_;
  /**
   * <pre>
   * Standard audit information (created_at, updated_at, created_by, etc.)
   * </pre>
   *
   * <code>.ai.stigmer.commons.apiresource.ApiResourceAudit audit = 99 [json_name = "audit"];</code>
   * @return Whether the audit field is set.
   */
  @java.lang.Override
  public boolean hasAudit() {
    return ((bitField0_ & 0x00000001) != 0);
  }
  /**
   * <pre>
   * Standard audit information (created_at, updated_at, created_by, etc.)
   * </pre>
   *
   * <code>.ai.stigmer.commons.apiresource.ApiResourceAudit audit = 99 [json_name = "audit"];</code>
   * @return The audit.
   */
  @java.lang.Override
  public ai.stigmer.commons.apiresource.ApiResourceAudit getAudit() {
    return audit_ == null ? ai.stigmer.commons.apiresource.ApiResourceAudit.getDefaultInstance() : audit_;
  }
  /**
   * <pre>
   * Standard audit information (created_at, updated_at, created_by, etc.)
   * </pre>
   *
   * <code>.ai.stigmer.commons.apiresource.ApiResourceAudit audit = 99 [json_name = "audit"];</code>
   */
  @java.lang.Override
  public ai.stigmer.commons.apiresource.ApiResourceAuditOrBuilder getAuditOrBuilder() {
    return audit_ == null ? ai.stigmer.commons.apiresource.ApiResourceAudit.getDefaultInstance() : audit_;
  }

  public static final int MESSAGES_FIELD_NUMBER = 1;
  @SuppressWarnings("serial")
  private java.util.List<ai.stigmer.agentic.agentexecution.v1.AgentMessage> messages_;
  /**
   * <pre>
   * Sequential stream of execution events: AI messages, tool calls, and system notifications.
   * Ordered chronologically to reconstruct the complete execution flow.
   * Each message captures what happened during execution (agent responses, tool invocations, progress updates).
   * </pre>
   *
   * <code>repeated .ai.stigmer.agentic.agentexecution.v1.AgentMessage messages = 1 [json_name = "messages"];</code>
   */
  @java.lang.Override
  public java.util.List<ai.stigmer.agentic.agentexecution.v1.AgentMessage> getMessagesList() {
    return messages_;
  }
  /**
   * <pre>
   * Sequential stream of execution events: AI messages, tool calls, and system notifications.
   * Ordered chronologically to reconstruct the complete execution flow.
   * Each message captures what happened during execution (agent responses, tool invocations, progress updates).
   * </pre>
   *
   * <code>repeated .ai.stigmer.agentic.agentexecution.v1.AgentMessage messages = 1 [json_name = "messages"];</code>
   */
  @java.lang.Override
  public java.util.List<? extends ai.stigmer.agentic.agentexecution.v1.AgentMessageOrBuilder> 
      getMessagesOrBuilderList() {
    return messages_;
  }
  /**
   * <pre>
   * Sequential stream of execution events: AI messages, tool calls, and system notifications.
   * Ordered chronologically to reconstruct the complete execution flow.
   * Each message captures what happened during execution (agent responses, tool invocations, progress updates).
   * </pre>
   *
   * <code>repeated .ai.stigmer.agentic.agentexecution.v1.AgentMessage messages = 1 [json_name = "messages"];</code>
   */
  @java.lang.Override
  public int getMessagesCount() {
    return messages_.size();
  }
  /**
   * <pre>
   * Sequential stream of execution events: AI messages, tool calls, and system notifications.
   * Ordered chronologically to reconstruct the complete execution flow.
   * Each message captures what happened during execution (agent responses, tool invocations, progress updates).
   * </pre>
   *
   * <code>repeated .ai.stigmer.agentic.agentexecution.v1.AgentMessage messages = 1 [json_name = "messages"];</code>
   */
  @java.lang.Override
  public ai.stigmer.agentic.agentexecution.v1.AgentMessage getMessages(int index) {
    return messages_.get(index);
  }
  /**
   * <pre>
   * Sequential stream of execution events: AI messages, tool calls, and system notifications.
   * Ordered chronologically to reconstruct the complete execution flow.
   * Each message captures what happened during execution (agent responses, tool invocations, progress updates).
   * </pre>
   *
   * <code>repeated .ai.stigmer.agentic.agentexecution.v1.AgentMessage messages = 1 [json_name = "messages"];</code>
   */
  @java.lang.Override
  public ai.stigmer.agentic.agentexecution.v1.AgentMessageOrBuilder getMessagesOrBuilder(
      int index) {
    return messages_.get(index);
  }

  public static final int PHASE_FIELD_NUMBER = 2;
  private int phase_ = 0;
  /**
   * <pre>
   * Current execution lifecycle phase.
   * Tracks the execution state from creation (PENDING) through active processing (IN_PROGRESS)
   * to terminal states (COMPLETED/FAILED/CANCELLED).
   * </pre>
   *
   * <code>.ai.stigmer.agentic.agentexecution.v1.ExecutionPhase phase = 2 [json_name = "phase", (.buf.validate.field) = { ... }</code>
   * @return The enum numeric value on the wire for phase.
   */
  @java.lang.Override public int getPhaseValue() {
    return phase_;
  }
  /**
   * <pre>
   * Current execution lifecycle phase.
   * Tracks the execution state from creation (PENDING) through active processing (IN_PROGRESS)
   * to terminal states (COMPLETED/FAILED/CANCELLED).
   * </pre>
   *
   * <code>.ai.stigmer.agentic.agentexecution.v1.ExecutionPhase phase = 2 [json_name = "phase", (.buf.validate.field) = { ... }</code>
   * @return The phase.
   */
  @java.lang.Override public ai.stigmer.agentic.agentexecution.v1.ExecutionPhase getPhase() {
    ai.stigmer.agentic.agentexecution.v1.ExecutionPhase result = ai.stigmer.agentic.agentexecution.v1.ExecutionPhase.forNumber(phase_);
    return result == null ? ai.stigmer.agentic.agentexecution.v1.ExecutionPhase.UNRECOGNIZED : result;
  }

  public static final int SUB_AGENT_EXECUTIONS_FIELD_NUMBER = 4;
  @SuppressWarnings("serial")
  private java.util.List<ai.stigmer.agentic.agentexecution.v1.SubAgentExecution> subAgentExecutions_;
  /**
   * <pre>
   * Sub-agent executions invoked during this execution.
   * Tracks when the main agent delegates work to specialized sub-agents.
   * Ordered chronologically by invocation time.
   * </pre>
   *
   * <code>repeated .ai.stigmer.agentic.agentexecution.v1.SubAgentExecution sub_agent_executions = 4 [json_name = "subAgentExecutions"];</code>
   */
  @java.lang.Override
  public java.util.List<ai.stigmer.agentic.agentexecution.v1.SubAgentExecution> getSubAgentExecutionsList() {
    return subAgentExecutions_;
  }
  /**
   * <pre>
   * Sub-agent executions invoked during this execution.
   * Tracks when the main agent delegates work to specialized sub-agents.
   * Ordered chronologically by invocation time.
   * </pre>
   *
   * <code>repeated .ai.stigmer.agentic.agentexecution.v1.SubAgentExecution sub_agent_executions = 4 [json_name = "subAgentExecutions"];</code>
   */
  @java.lang.Override
  public java.util.List<? extends ai.stigmer.agentic.agentexecution.v1.SubAgentExecutionOrBuilder> 
      getSubAgentExecutionsOrBuilderList() {
    return subAgentExecutions_;
  }
  /**
   * <pre>
   * Sub-agent executions invoked during this execution.
   * Tracks when the main agent delegates work to specialized sub-agents.
   * Ordered chronologically by invocation time.
   * </pre>
   *
   * <code>repeated .ai.stigmer.agentic.agentexecution.v1.SubAgentExecution sub_agent_executions = 4 [json_name = "subAgentExecutions"];</code>
   */
  @java.lang.Override
  public int getSubAgentExecutionsCount() {
    return subAgentExecutions_.size();
  }
  /**
   * <pre>
   * Sub-agent executions invoked during this execution.
   * Tracks when the main agent delegates work to specialized sub-agents.
   * Ordered chronologically by invocation time.
   * </pre>
   *
   * <code>repeated .ai.stigmer.agentic.agentexecution.v1.SubAgentExecution sub_agent_executions = 4 [json_name = "subAgentExecutions"];</code>
   */
  @java.lang.Override
  public ai.stigmer.agentic.agentexecution.v1.SubAgentExecution getSubAgentExecutions(int index) {
    return subAgentExecutions_.get(index);
  }
  /**
   * <pre>
   * Sub-agent executions invoked during this execution.
   * Tracks when the main agent delegates work to specialized sub-agents.
   * Ordered chronologically by invocation time.
   * </pre>
   *
   * <code>repeated .ai.stigmer.agentic.agentexecution.v1.SubAgentExecution sub_agent_executions = 4 [json_name = "subAgentExecutions"];</code>
   */
  @java.lang.Override
  public ai.stigmer.agentic.agentexecution.v1.SubAgentExecutionOrBuilder getSubAgentExecutionsOrBuilder(
      int index) {
    return subAgentExecutions_.get(index);
  }

  public static final int ERROR_FIELD_NUMBER = 6;
  @SuppressWarnings("serial")
  private volatile java.lang.Object error_ = "";
  /**
   * <pre>
   * Error message if execution failed.
   * Only populated when phase == EXECUTION_FAILED.
   * </pre>
   *
   * <code>string error = 6 [json_name = "error"];</code>
   * @return The error.
   */
  @java.lang.Override
  public java.lang.String getError() {
    java.lang.Object ref = error_;
    if (ref instanceof java.lang.String) {
      return (java.lang.String) ref;
    } else {
      com.google.protobuf.ByteString bs = 
          (com.google.protobuf.ByteString) ref;
      java.lang.String s = bs.toStringUtf8();
      error_ = s;
      return s;
    }
  }
  /**
   * <pre>
   * Error message if execution failed.
   * Only populated when phase == EXECUTION_FAILED.
   * </pre>
   *
   * <code>string error = 6 [json_name = "error"];</code>
   * @return The bytes for error.
   */
  @java.lang.Override
  public com.google.protobuf.ByteString
      getErrorBytes() {
    java.lang.Object ref = error_;
    if (ref instanceof java.lang.String) {
      com.google.protobuf.ByteString b = 
          com.google.protobuf.ByteString.copyFromUtf8(
              (java.lang.String) ref);
      error_ = b;
      return b;
    } else {
      return (com.google.protobuf.ByteString) ref;
    }
  }

  public static final int STARTED_AT_FIELD_NUMBER = 7;
  @SuppressWarnings("serial")
  private volatile java.lang.Object startedAt_ = "";
  /**
   * <pre>
   * ISO 8601 timestamp when execution started.
   * Example: "2025-01-10T10:30:00Z"
   * </pre>
   *
   * <code>string started_at = 7 [json_name = "startedAt"];</code>
   * @return The startedAt.
   */
  @java.lang.Override
  public java.lang.String getStartedAt() {
    java.lang.Object ref = startedAt_;
    if (ref instanceof java.lang.String) {
      return (java.lang.String) ref;
    } else {
      com.google.protobuf.ByteString bs = 
          (com.google.protobuf.ByteString) ref;
      java.lang.String s = bs.toStringUtf8();
      startedAt_ = s;
      return s;
    }
  }
  /**
   * <pre>
   * ISO 8601 timestamp when execution started.
   * Example: "2025-01-10T10:30:00Z"
   * </pre>
   *
   * <code>string started_at = 7 [json_name = "startedAt"];</code>
   * @return The bytes for startedAt.
   */
  @java.lang.Override
  public com.google.protobuf.ByteString
      getStartedAtBytes() {
    java.lang.Object ref = startedAt_;
    if (ref instanceof java.lang.String) {
      com.google.protobuf.ByteString b = 
          com.google.protobuf.ByteString.copyFromUtf8(
              (java.lang.String) ref);
      startedAt_ = b;
      return b;
    } else {
      return (com.google.protobuf.ByteString) ref;
    }
  }

  public static final int COMPLETED_AT_FIELD_NUMBER = 8;
  @SuppressWarnings("serial")
  private volatile java.lang.Object completedAt_ = "";
  /**
   * <pre>
   * ISO 8601 timestamp when execution completed, failed, or was cancelled.
   * Only populated for terminal states.
   * </pre>
   *
   * <code>string completed_at = 8 [json_name = "completedAt"];</code>
   * @return The completedAt.
   */
  @java.lang.Override
  public java.lang.String getCompletedAt() {
    java.lang.Object ref = completedAt_;
    if (ref instanceof java.lang.String) {
      return (java.lang.String) ref;
    } else {
      com.google.protobuf.ByteString bs = 
          (com.google.protobuf.ByteString) ref;
      java.lang.String s = bs.toStringUtf8();
      completedAt_ = s;
      return s;
    }
  }
  /**
   * <pre>
   * ISO 8601 timestamp when execution completed, failed, or was cancelled.
   * Only populated for terminal states.
   * </pre>
   *
   * <code>string completed_at = 8 [json_name = "completedAt"];</code>
   * @return The bytes for completedAt.
   */
  @java.lang.Override
  public com.google.protobuf.ByteString
      getCompletedAtBytes() {
    java.lang.Object ref = completedAt_;
    if (ref instanceof java.lang.String) {
      com.google.protobuf.ByteString b = 
          com.google.protobuf.ByteString.copyFromUtf8(
              (java.lang.String) ref);
      completedAt_ = b;
      return b;
    } else {
      return (com.google.protobuf.ByteString) ref;
    }
  }

  public static final int TODOS_FIELD_NUMBER = 9;
  private static final class TodosDefaultEntryHolder {
    static final com.google.protobuf.MapEntry<
        java.lang.String, ai.stigmer.agentic.agentexecution.v1.TodoItem> defaultEntry =
            com.google.protobuf.MapEntry
            .<java.lang.String, ai.stigmer.agentic.agentexecution.v1.TodoItem>newDefaultInstance(
                ai.stigmer.agentic.agentexecution.v1.ApiProto.internal_static_ai_stigmer_agentic_agentexecution_v1_AgentExecutionStatus_TodosEntry_descriptor, 
                com.google.protobuf.WireFormat.FieldType.STRING,
                "",
                com.google.protobuf.WireFormat.FieldType.MESSAGE,
                ai.stigmer.agentic.agentexecution.v1.TodoItem.getDefaultInstance());
  }
  @SuppressWarnings("serial")
  private com.google.protobuf.MapField<
      java.lang.String, ai.stigmer.agentic.agentexecution.v1.TodoItem> todos_;
  private com.google.protobuf.MapField<java.lang.String, ai.stigmer.agentic.agentexecution.v1.TodoItem>
  internalGetTodos() {
    if (todos_ == null) {
      return com.google.protobuf.MapField.emptyMapField(
          TodosDefaultEntryHolder.defaultEntry);
    }
    return todos_;
  }
  public int getTodosCount() {
    return internalGetTodos().getMap().size();
  }
  /**
   * <pre>
   * Todo list tracking multi-step tasks and progress for this execution.
   * Updated via write_todos tool (if TodoListMiddleware is enabled).
   * Each execution maintains its own snapshot of todos at execution time.
   * Key: todo item ID, Value: todo item details
   * </pre>
   *
   * <code>map&lt;string, .ai.stigmer.agentic.agentexecution.v1.TodoItem&gt; todos = 9 [json_name = "todos"];</code>
   */
  @java.lang.Override
  public boolean containsTodos(
      java.lang.String key) {
    if (key == null) { throw new NullPointerException("map key"); }
    return internalGetTodos().getMap().containsKey(key);
  }
  /**
   * Use {@link #getTodosMap()} instead.
   */
  @java.lang.Override
  @java.lang.Deprecated
  public java.util.Map<java.lang.String, ai.stigmer.agentic.agentexecution.v1.TodoItem> getTodos() {
    return getTodosMap();
  }
  /**
   * <pre>
   * Todo list tracking multi-step tasks and progress for this execution.
   * Updated via write_todos tool (if TodoListMiddleware is enabled).
   * Each execution maintains its own snapshot of todos at execution time.
   * Key: todo item ID, Value: todo item details
   * </pre>
   *
   * <code>map&lt;string, .ai.stigmer.agentic.agentexecution.v1.TodoItem&gt; todos = 9 [json_name = "todos"];</code>
   */
  @java.lang.Override
  public java.util.Map<java.lang.String, ai.stigmer.agentic.agentexecution.v1.TodoItem> getTodosMap() {
    return internalGetTodos().getMap();
  }
  /**
   * <pre>
   * Todo list tracking multi-step tasks and progress for this execution.
   * Updated via write_todos tool (if TodoListMiddleware is enabled).
   * Each execution maintains its own snapshot of todos at execution time.
   * Key: todo item ID, Value: todo item details
   * </pre>
   *
   * <code>map&lt;string, .ai.stigmer.agentic.agentexecution.v1.TodoItem&gt; todos = 9 [json_name = "todos"];</code>
   */
  @java.lang.Override
  public /* nullable */
ai.stigmer.agentic.agentexecution.v1.TodoItem getTodosOrDefault(
      java.lang.String key,
      /* nullable */
ai.stigmer.agentic.agentexecution.v1.TodoItem defaultValue) {
    if (key == null) { throw new NullPointerException("map key"); }
    java.util.Map<java.lang.String, ai.stigmer.agentic.agentexecution.v1.TodoItem> map =
        internalGetTodos().getMap();
    return map.containsKey(key) ? map.get(key) : defaultValue;
  }
  /**
   * <pre>
   * Todo list tracking multi-step tasks and progress for this execution.
   * Updated via write_todos tool (if TodoListMiddleware is enabled).
   * Each execution maintains its own snapshot of todos at execution time.
   * Key: todo item ID, Value: todo item details
   * </pre>
   *
   * <code>map&lt;string, .ai.stigmer.agentic.agentexecution.v1.TodoItem&gt; todos = 9 [json_name = "todos"];</code>
   */
  @java.lang.Override
  public ai.stigmer.agentic.agentexecution.v1.TodoItem getTodosOrThrow(
      java.lang.String key) {
    if (key == null) { throw new NullPointerException("map key"); }
    java.util.Map<java.lang.String, ai.stigmer.agentic.agentexecution.v1.TodoItem> map =
        internalGetTodos().getMap();
    if (!map.containsKey(key)) {
      throw new java.lang.IllegalArgumentException();
    }
    return map.get(key);
  }

  public static final int CALLBACK_TOKEN_FIELD_NUMBER = 10;
  private com.google.protobuf.ByteString callbackToken_ = com.google.protobuf.ByteString.EMPTY;
  /**
   * <pre>
   * Callback token for notifying a parent workflow when this execution completes.
   *
   * Present when this execution was triggered by a workflow. Empty when the
   * execution runs independently (chat, API call).
   *
   * &#64;internal
   *
   * When this AgentExecution is triggered from a Temporal activity, the
   * calling activity passes its task token here. The runner completes
   * the external activity when the execution finishes.
   *
   * Token Handshake Pattern:
   * 1. External activity extracts token and stores in AgentExecution.status
   * 2. Activity returns activity.ErrResultPending (paused, thread freed)
   * 3. Agent executes (seconds to minutes)
   * 4. On completion: the runner completes the external activity via token
   *
   * Why in Status (not Spec):
   * - System-generated by Temporal, not user-provided
   * - Runtime state, not configuration
   * - Ephemeral, only relevant during execution
   *
   * References:
   * - WorkflowExecution.spec.callback_token (field 7) - Same pattern
   * - ADR: docs/adr/20260122-async-agent-execution-temporal-token-handshake.md
   * </pre>
   *
   * <code>bytes callback_token = 10 [json_name = "callbackToken"];</code>
   * @return The callbackToken.
   */
  @java.lang.Override
  public com.google.protobuf.ByteString getCallbackToken() {
    return callbackToken_;
  }

  public static final int PENDING_APPROVALS_FIELD_NUMBER = 16;
  @SuppressWarnings("serial")
  private java.util.List<ai.stigmer.agentic.agentexecution.v1.PendingApproval> pendingApprovals_;
  /**
   * <pre>
   * Active pending approval requests for this execution.
   *
   * Contains one entry per tool call that requires user approval before
   * it can proceed. Sub-agent approvals are included with from_sub_agent
   * set to true. Empty when no approvals are pending.
   *
   * &#64;internal
   *
   * Computed server-side by the Go/Java UpdateStatus handlers on every write.
   * The handler scans messages[].tool_calls and
   * sub_agent_executions[].messages[].tool_calls, collecting entries where
   * status == WAITING_APPROVAL &amp;&amp; requires_approval == true, and projects
   * them into PendingApproval entries.
   *
   * Because this field is recomputed (not merged), it is always consistent
   * with the authoritative tool call state embedded in messages. No lifecycle
   * state machine, no merge logic, no forward-only enforcement needed.
   *
   * Lifecycle:
   * 1. The runner sets ToolCall.status = WAITING_APPROVAL on the message
   * 2. UpdateStatus handler recomputes this field, entry appears
   * 3. Phase changes to EXECUTION_WAITING_FOR_APPROVAL
   * 4. User submits decisions via SubmitApproval RPC (one per tool call)
   * 5. Agent resumes, ToolCall.status advances, next recompute drops the entry
   * </pre>
   *
   * <code>repeated .ai.stigmer.agentic.agentexecution.v1.PendingApproval pending_approvals = 16 [json_name = "pendingApprovals"];</code>
   */
  @java.lang.Override
  public java.util.List<ai.stigmer.agentic.agentexecution.v1.PendingApproval> getPendingApprovalsList() {
    return pendingApprovals_;
  }
  /**
   * <pre>
   * Active pending approval requests for this execution.
   *
   * Contains one entry per tool call that requires user approval before
   * it can proceed. Sub-agent approvals are included with from_sub_agent
   * set to true. Empty when no approvals are pending.
   *
   * &#64;internal
   *
   * Computed server-side by the Go/Java UpdateStatus handlers on every write.
   * The handler scans messages[].tool_calls and
   * sub_agent_executions[].messages[].tool_calls, collecting entries where
   * status == WAITING_APPROVAL &amp;&amp; requires_approval == true, and projects
   * them into PendingApproval entries.
   *
   * Because this field is recomputed (not merged), it is always consistent
   * with the authoritative tool call state embedded in messages. No lifecycle
   * state machine, no merge logic, no forward-only enforcement needed.
   *
   * Lifecycle:
   * 1. The runner sets ToolCall.status = WAITING_APPROVAL on the message
   * 2. UpdateStatus handler recomputes this field, entry appears
   * 3. Phase changes to EXECUTION_WAITING_FOR_APPROVAL
   * 4. User submits decisions via SubmitApproval RPC (one per tool call)
   * 5. Agent resumes, ToolCall.status advances, next recompute drops the entry
   * </pre>
   *
   * <code>repeated .ai.stigmer.agentic.agentexecution.v1.PendingApproval pending_approvals = 16 [json_name = "pendingApprovals"];</code>
   */
  @java.lang.Override
  public java.util.List<? extends ai.stigmer.agentic.agentexecution.v1.PendingApprovalOrBuilder> 
      getPendingApprovalsOrBuilderList() {
    return pendingApprovals_;
  }
  /**
   * <pre>
   * Active pending approval requests for this execution.
   *
   * Contains one entry per tool call that requires user approval before
   * it can proceed. Sub-agent approvals are included with from_sub_agent
   * set to true. Empty when no approvals are pending.
   *
   * &#64;internal
   *
   * Computed server-side by the Go/Java UpdateStatus handlers on every write.
   * The handler scans messages[].tool_calls and
   * sub_agent_executions[].messages[].tool_calls, collecting entries where
   * status == WAITING_APPROVAL &amp;&amp; requires_approval == true, and projects
   * them into PendingApproval entries.
   *
   * Because this field is recomputed (not merged), it is always consistent
   * with the authoritative tool call state embedded in messages. No lifecycle
   * state machine, no merge logic, no forward-only enforcement needed.
   *
   * Lifecycle:
   * 1. The runner sets ToolCall.status = WAITING_APPROVAL on the message
   * 2. UpdateStatus handler recomputes this field, entry appears
   * 3. Phase changes to EXECUTION_WAITING_FOR_APPROVAL
   * 4. User submits decisions via SubmitApproval RPC (one per tool call)
   * 5. Agent resumes, ToolCall.status advances, next recompute drops the entry
   * </pre>
   *
   * <code>repeated .ai.stigmer.agentic.agentexecution.v1.PendingApproval pending_approvals = 16 [json_name = "pendingApprovals"];</code>
   */
  @java.lang.Override
  public int getPendingApprovalsCount() {
    return pendingApprovals_.size();
  }
  /**
   * <pre>
   * Active pending approval requests for this execution.
   *
   * Contains one entry per tool call that requires user approval before
   * it can proceed. Sub-agent approvals are included with from_sub_agent
   * set to true. Empty when no approvals are pending.
   *
   * &#64;internal
   *
   * Computed server-side by the Go/Java UpdateStatus handlers on every write.
   * The handler scans messages[].tool_calls and
   * sub_agent_executions[].messages[].tool_calls, collecting entries where
   * status == WAITING_APPROVAL &amp;&amp; requires_approval == true, and projects
   * them into PendingApproval entries.
   *
   * Because this field is recomputed (not merged), it is always consistent
   * with the authoritative tool call state embedded in messages. No lifecycle
   * state machine, no merge logic, no forward-only enforcement needed.
   *
   * Lifecycle:
   * 1. The runner sets ToolCall.status = WAITING_APPROVAL on the message
   * 2. UpdateStatus handler recomputes this field, entry appears
   * 3. Phase changes to EXECUTION_WAITING_FOR_APPROVAL
   * 4. User submits decisions via SubmitApproval RPC (one per tool call)
   * 5. Agent resumes, ToolCall.status advances, next recompute drops the entry
   * </pre>
   *
   * <code>repeated .ai.stigmer.agentic.agentexecution.v1.PendingApproval pending_approvals = 16 [json_name = "pendingApprovals"];</code>
   */
  @java.lang.Override
  public ai.stigmer.agentic.agentexecution.v1.PendingApproval getPendingApprovals(int index) {
    return pendingApprovals_.get(index);
  }
  /**
   * <pre>
   * Active pending approval requests for this execution.
   *
   * Contains one entry per tool call that requires user approval before
   * it can proceed. Sub-agent approvals are included with from_sub_agent
   * set to true. Empty when no approvals are pending.
   *
   * &#64;internal
   *
   * Computed server-side by the Go/Java UpdateStatus handlers on every write.
   * The handler scans messages[].tool_calls and
   * sub_agent_executions[].messages[].tool_calls, collecting entries where
   * status == WAITING_APPROVAL &amp;&amp; requires_approval == true, and projects
   * them into PendingApproval entries.
   *
   * Because this field is recomputed (not merged), it is always consistent
   * with the authoritative tool call state embedded in messages. No lifecycle
   * state machine, no merge logic, no forward-only enforcement needed.
   *
   * Lifecycle:
   * 1. The runner sets ToolCall.status = WAITING_APPROVAL on the message
   * 2. UpdateStatus handler recomputes this field, entry appears
   * 3. Phase changes to EXECUTION_WAITING_FOR_APPROVAL
   * 4. User submits decisions via SubmitApproval RPC (one per tool call)
   * 5. Agent resumes, ToolCall.status advances, next recompute drops the entry
   * </pre>
   *
   * <code>repeated .ai.stigmer.agentic.agentexecution.v1.PendingApproval pending_approvals = 16 [json_name = "pendingApprovals"];</code>
   */
  @java.lang.Override
  public ai.stigmer.agentic.agentexecution.v1.PendingApprovalOrBuilder getPendingApprovalsOrBuilder(
      int index) {
    return pendingApprovals_.get(index);
  }

  public static final int APPROVAL_EVENT_STREAM_FIELD_NUMBER = 22;
  private ai.stigmer.agentic.agentexecution.v1.ApprovalEventStream approvalEventStream_;
  /**
   * <pre>
   * Server-authored, append-only record of every approval request and decision.
   *
   * &#64;internal
   *
   * This is the persisted form of the append-only stream that is the target
   * single source of truth for approvals (see ApprovalEventStream in
   * approval.proto). It is NOT yet read by any surface: pending_approvals is
   * still projected from the authoritative message scan, and the UI/SDK read
   * that. The stream coexists as a parallel, independently-authored record so
   * that (1) the message-scan vs event-stream projection parity check becomes a
   * real cross-writer regression guard rather than a tautology, and (2) the
   * approval audit trail — who decided, when, with what comment — has a home,
   * since the flat ToolCall.approval_action / approval_decided_at fields cannot
   * capture a decision comment at all.
   *
   * Field ownership (one writer per event type):
   * - REQUESTED events are authored by the UpdateStatus handlers when a tool
   * call enters WAITING_APPROVAL (seeded once from the message scan for
   * executions that predate this field).
   * - Decision events (APPROVED / REJECTED / SKIPPED) are authored by the
   * SubmitApproval handler, carrying decided_by and the user's comment.
   *
   * Every append is keyed by the deterministic ApprovalEvent.event_id, so the
   * stream is idempotent under retries and the rich decision event (authored in
   * the same operation that records the decision on the message scan) can never
   * be duplicated or overwritten by a coarse re-derivation.
   *
   * Because it is server-only, the runner never sends it; it is preserved
   * across UpdateStatus writes automatically (the merge starts from the loaded
   * execution and only replaces runner-owned fields).
   * </pre>
   *
   * <code>.ai.stigmer.agentic.agentexecution.v1.ApprovalEventStream approval_event_stream = 22 [json_name = "approvalEventStream"];</code>
   * @return Whether the approvalEventStream field is set.
   */
  @java.lang.Override
  public boolean hasApprovalEventStream() {
    return ((bitField0_ & 0x00000002) != 0);
  }
  /**
   * <pre>
   * Server-authored, append-only record of every approval request and decision.
   *
   * &#64;internal
   *
   * This is the persisted form of the append-only stream that is the target
   * single source of truth for approvals (see ApprovalEventStream in
   * approval.proto). It is NOT yet read by any surface: pending_approvals is
   * still projected from the authoritative message scan, and the UI/SDK read
   * that. The stream coexists as a parallel, independently-authored record so
   * that (1) the message-scan vs event-stream projection parity check becomes a
   * real cross-writer regression guard rather than a tautology, and (2) the
   * approval audit trail — who decided, when, with what comment — has a home,
   * since the flat ToolCall.approval_action / approval_decided_at fields cannot
   * capture a decision comment at all.
   *
   * Field ownership (one writer per event type):
   * - REQUESTED events are authored by the UpdateStatus handlers when a tool
   * call enters WAITING_APPROVAL (seeded once from the message scan for
   * executions that predate this field).
   * - Decision events (APPROVED / REJECTED / SKIPPED) are authored by the
   * SubmitApproval handler, carrying decided_by and the user's comment.
   *
   * Every append is keyed by the deterministic ApprovalEvent.event_id, so the
   * stream is idempotent under retries and the rich decision event (authored in
   * the same operation that records the decision on the message scan) can never
   * be duplicated or overwritten by a coarse re-derivation.
   *
   * Because it is server-only, the runner never sends it; it is preserved
   * across UpdateStatus writes automatically (the merge starts from the loaded
   * execution and only replaces runner-owned fields).
   * </pre>
   *
   * <code>.ai.stigmer.agentic.agentexecution.v1.ApprovalEventStream approval_event_stream = 22 [json_name = "approvalEventStream"];</code>
   * @return The approvalEventStream.
   */
  @java.lang.Override
  public ai.stigmer.agentic.agentexecution.v1.ApprovalEventStream getApprovalEventStream() {
    return approvalEventStream_ == null ? ai.stigmer.agentic.agentexecution.v1.ApprovalEventStream.getDefaultInstance() : approvalEventStream_;
  }
  /**
   * <pre>
   * Server-authored, append-only record of every approval request and decision.
   *
   * &#64;internal
   *
   * This is the persisted form of the append-only stream that is the target
   * single source of truth for approvals (see ApprovalEventStream in
   * approval.proto). It is NOT yet read by any surface: pending_approvals is
   * still projected from the authoritative message scan, and the UI/SDK read
   * that. The stream coexists as a parallel, independently-authored record so
   * that (1) the message-scan vs event-stream projection parity check becomes a
   * real cross-writer regression guard rather than a tautology, and (2) the
   * approval audit trail — who decided, when, with what comment — has a home,
   * since the flat ToolCall.approval_action / approval_decided_at fields cannot
   * capture a decision comment at all.
   *
   * Field ownership (one writer per event type):
   * - REQUESTED events are authored by the UpdateStatus handlers when a tool
   * call enters WAITING_APPROVAL (seeded once from the message scan for
   * executions that predate this field).
   * - Decision events (APPROVED / REJECTED / SKIPPED) are authored by the
   * SubmitApproval handler, carrying decided_by and the user's comment.
   *
   * Every append is keyed by the deterministic ApprovalEvent.event_id, so the
   * stream is idempotent under retries and the rich decision event (authored in
   * the same operation that records the decision on the message scan) can never
   * be duplicated or overwritten by a coarse re-derivation.
   *
   * Because it is server-only, the runner never sends it; it is preserved
   * across UpdateStatus writes automatically (the merge starts from the loaded
   * execution and only replaces runner-owned fields).
   * </pre>
   *
   * <code>.ai.stigmer.agentic.agentexecution.v1.ApprovalEventStream approval_event_stream = 22 [json_name = "approvalEventStream"];</code>
   */
  @java.lang.Override
  public ai.stigmer.agentic.agentexecution.v1.ApprovalEventStreamOrBuilder getApprovalEventStreamOrBuilder() {
    return approvalEventStream_ == null ? ai.stigmer.agentic.agentexecution.v1.ApprovalEventStream.getDefaultInstance() : approvalEventStream_;
  }

  public static final int CONTEXT_INFO_FIELD_NUMBER = 14;
  private ai.stigmer.agentic.agentexecution.v1.ContextInfo contextInfo_;
  /**
   * <pre>
   * Context window utilization and summarization tracking.
   *
   * Provides visibility into how the agent is using its context window and
   * any summarization events that occurred during execution.
   *
   * Populated when context management is active (not disabled via config).
   * Updated progressively during streaming as token counts change.
   *
   * Use cases:
   * - **Monitoring**: Track context utilization across executions
   * - **Debugging**: Understand why an execution hit context limits
   * - **Cost optimization**: Identify executions with excessive summarization
   * - **UX**: Show users their context window health
   *
   * &#64;since Phase 3 (Context Summarization Architecture)
   * </pre>
   *
   * <code>.ai.stigmer.agentic.agentexecution.v1.ContextInfo context_info = 14 [json_name = "contextInfo"];</code>
   * @return Whether the contextInfo field is set.
   */
  @java.lang.Override
  public boolean hasContextInfo() {
    return ((bitField0_ & 0x00000004) != 0);
  }
  /**
   * <pre>
   * Context window utilization and summarization tracking.
   *
   * Provides visibility into how the agent is using its context window and
   * any summarization events that occurred during execution.
   *
   * Populated when context management is active (not disabled via config).
   * Updated progressively during streaming as token counts change.
   *
   * Use cases:
   * - **Monitoring**: Track context utilization across executions
   * - **Debugging**: Understand why an execution hit context limits
   * - **Cost optimization**: Identify executions with excessive summarization
   * - **UX**: Show users their context window health
   *
   * &#64;since Phase 3 (Context Summarization Architecture)
   * </pre>
   *
   * <code>.ai.stigmer.agentic.agentexecution.v1.ContextInfo context_info = 14 [json_name = "contextInfo"];</code>
   * @return The contextInfo.
   */
  @java.lang.Override
  public ai.stigmer.agentic.agentexecution.v1.ContextInfo getContextInfo() {
    return contextInfo_ == null ? ai.stigmer.agentic.agentexecution.v1.ContextInfo.getDefaultInstance() : contextInfo_;
  }
  /**
   * <pre>
   * Context window utilization and summarization tracking.
   *
   * Provides visibility into how the agent is using its context window and
   * any summarization events that occurred during execution.
   *
   * Populated when context management is active (not disabled via config).
   * Updated progressively during streaming as token counts change.
   *
   * Use cases:
   * - **Monitoring**: Track context utilization across executions
   * - **Debugging**: Understand why an execution hit context limits
   * - **Cost optimization**: Identify executions with excessive summarization
   * - **UX**: Show users their context window health
   *
   * &#64;since Phase 3 (Context Summarization Architecture)
   * </pre>
   *
   * <code>.ai.stigmer.agentic.agentexecution.v1.ContextInfo context_info = 14 [json_name = "contextInfo"];</code>
   */
  @java.lang.Override
  public ai.stigmer.agentic.agentexecution.v1.ContextInfoOrBuilder getContextInfoOrBuilder() {
    return contextInfo_ == null ? ai.stigmer.agentic.agentexecution.v1.ContextInfo.getDefaultInstance() : contextInfo_;
  }

  public static final int ARTIFACTS_FIELD_NUMBER = 15;
  @SuppressWarnings("serial")
  private java.util.List<ai.stigmer.agentic.agentexecution.v1.ExecutionArtifact> artifacts_;
  /**
   * <pre>
   * Files/directories published by the agent for download.
   *
   * When an agent creates artifacts (e.g., generated code, reports, skills),
   * it can call the publish_artifact tool to make them downloadable.
   * Each artifact includes a pre-signed URL for direct download.
   *
   * Artifacts are stored in R2 and URLs expire after a configured period.
   * Use the execution download endpoint to refresh expired URLs.
   *
   * Ordered by creation time (oldest first).
   *
   * &#64;since Artifact Lifecycle (Attachments &amp; Artifacts)
   * </pre>
   *
   * <code>repeated .ai.stigmer.agentic.agentexecution.v1.ExecutionArtifact artifacts = 15 [json_name = "artifacts"];</code>
   */
  @java.lang.Override
  public java.util.List<ai.stigmer.agentic.agentexecution.v1.ExecutionArtifact> getArtifactsList() {
    return artifacts_;
  }
  /**
   * <pre>
   * Files/directories published by the agent for download.
   *
   * When an agent creates artifacts (e.g., generated code, reports, skills),
   * it can call the publish_artifact tool to make them downloadable.
   * Each artifact includes a pre-signed URL for direct download.
   *
   * Artifacts are stored in R2 and URLs expire after a configured period.
   * Use the execution download endpoint to refresh expired URLs.
   *
   * Ordered by creation time (oldest first).
   *
   * &#64;since Artifact Lifecycle (Attachments &amp; Artifacts)
   * </pre>
   *
   * <code>repeated .ai.stigmer.agentic.agentexecution.v1.ExecutionArtifact artifacts = 15 [json_name = "artifacts"];</code>
   */
  @java.lang.Override
  public java.util.List<? extends ai.stigmer.agentic.agentexecution.v1.ExecutionArtifactOrBuilder> 
      getArtifactsOrBuilderList() {
    return artifacts_;
  }
  /**
   * <pre>
   * Files/directories published by the agent for download.
   *
   * When an agent creates artifacts (e.g., generated code, reports, skills),
   * it can call the publish_artifact tool to make them downloadable.
   * Each artifact includes a pre-signed URL for direct download.
   *
   * Artifacts are stored in R2 and URLs expire after a configured period.
   * Use the execution download endpoint to refresh expired URLs.
   *
   * Ordered by creation time (oldest first).
   *
   * &#64;since Artifact Lifecycle (Attachments &amp; Artifacts)
   * </pre>
   *
   * <code>repeated .ai.stigmer.agentic.agentexecution.v1.ExecutionArtifact artifacts = 15 [json_name = "artifacts"];</code>
   */
  @java.lang.Override
  public int getArtifactsCount() {
    return artifacts_.size();
  }
  /**
   * <pre>
   * Files/directories published by the agent for download.
   *
   * When an agent creates artifacts (e.g., generated code, reports, skills),
   * it can call the publish_artifact tool to make them downloadable.
   * Each artifact includes a pre-signed URL for direct download.
   *
   * Artifacts are stored in R2 and URLs expire after a configured period.
   * Use the execution download endpoint to refresh expired URLs.
   *
   * Ordered by creation time (oldest first).
   *
   * &#64;since Artifact Lifecycle (Attachments &amp; Artifacts)
   * </pre>
   *
   * <code>repeated .ai.stigmer.agentic.agentexecution.v1.ExecutionArtifact artifacts = 15 [json_name = "artifacts"];</code>
   */
  @java.lang.Override
  public ai.stigmer.agentic.agentexecution.v1.ExecutionArtifact getArtifacts(int index) {
    return artifacts_.get(index);
  }
  /**
   * <pre>
   * Files/directories published by the agent for download.
   *
   * When an agent creates artifacts (e.g., generated code, reports, skills),
   * it can call the publish_artifact tool to make them downloadable.
   * Each artifact includes a pre-signed URL for direct download.
   *
   * Artifacts are stored in R2 and URLs expire after a configured period.
   * Use the execution download endpoint to refresh expired URLs.
   *
   * Ordered by creation time (oldest first).
   *
   * &#64;since Artifact Lifecycle (Attachments &amp; Artifacts)
   * </pre>
   *
   * <code>repeated .ai.stigmer.agentic.agentexecution.v1.ExecutionArtifact artifacts = 15 [json_name = "artifacts"];</code>
   */
  @java.lang.Override
  public ai.stigmer.agentic.agentexecution.v1.ExecutionArtifactOrBuilder getArtifactsOrBuilder(
      int index) {
    return artifacts_.get(index);
  }

  public static final int WORKSPACE_WRITE_BACKS_FIELD_NUMBER = 17;
  @SuppressWarnings("serial")
  private java.util.List<ai.stigmer.agentic.agentexecution.v1.WorkspaceWriteBack> workspaceWriteBacks_;
  /**
   * <pre>
   * Git write-back outcomes for workspace entries that had changes.
   *
   * One entry per git-backed workspace entry where the agent modified
   * files and write_back_mode was enabled on the GitRepoSource. Omitted
   * for workspace entries with no changes, non-git sources, or disabled
   * write-back mode.
   *
   * Populated during post-execution processing. Each entry tracks the
   * write-back lifecycle (committed -&gt; pushed -&gt; PR created) and carries
   * the PR URL for UI display.
   *
   * &#64;since Platform-Owned Git Write-Back
   * </pre>
   *
   * <code>repeated .ai.stigmer.agentic.agentexecution.v1.WorkspaceWriteBack workspace_write_backs = 17 [json_name = "workspaceWriteBacks"];</code>
   */
  @java.lang.Override
  public java.util.List<ai.stigmer.agentic.agentexecution.v1.WorkspaceWriteBack> getWorkspaceWriteBacksList() {
    return workspaceWriteBacks_;
  }
  /**
   * <pre>
   * Git write-back outcomes for workspace entries that had changes.
   *
   * One entry per git-backed workspace entry where the agent modified
   * files and write_back_mode was enabled on the GitRepoSource. Omitted
   * for workspace entries with no changes, non-git sources, or disabled
   * write-back mode.
   *
   * Populated during post-execution processing. Each entry tracks the
   * write-back lifecycle (committed -&gt; pushed -&gt; PR created) and carries
   * the PR URL for UI display.
   *
   * &#64;since Platform-Owned Git Write-Back
   * </pre>
   *
   * <code>repeated .ai.stigmer.agentic.agentexecution.v1.WorkspaceWriteBack workspace_write_backs = 17 [json_name = "workspaceWriteBacks"];</code>
   */
  @java.lang.Override
  public java.util.List<? extends ai.stigmer.agentic.agentexecution.v1.WorkspaceWriteBackOrBuilder> 
      getWorkspaceWriteBacksOrBuilderList() {
    return workspaceWriteBacks_;
  }
  /**
   * <pre>
   * Git write-back outcomes for workspace entries that had changes.
   *
   * One entry per git-backed workspace entry where the agent modified
   * files and write_back_mode was enabled on the GitRepoSource. Omitted
   * for workspace entries with no changes, non-git sources, or disabled
   * write-back mode.
   *
   * Populated during post-execution processing. Each entry tracks the
   * write-back lifecycle (committed -&gt; pushed -&gt; PR created) and carries
   * the PR URL for UI display.
   *
   * &#64;since Platform-Owned Git Write-Back
   * </pre>
   *
   * <code>repeated .ai.stigmer.agentic.agentexecution.v1.WorkspaceWriteBack workspace_write_backs = 17 [json_name = "workspaceWriteBacks"];</code>
   */
  @java.lang.Override
  public int getWorkspaceWriteBacksCount() {
    return workspaceWriteBacks_.size();
  }
  /**
   * <pre>
   * Git write-back outcomes for workspace entries that had changes.
   *
   * One entry per git-backed workspace entry where the agent modified
   * files and write_back_mode was enabled on the GitRepoSource. Omitted
   * for workspace entries with no changes, non-git sources, or disabled
   * write-back mode.
   *
   * Populated during post-execution processing. Each entry tracks the
   * write-back lifecycle (committed -&gt; pushed -&gt; PR created) and carries
   * the PR URL for UI display.
   *
   * &#64;since Platform-Owned Git Write-Back
   * </pre>
   *
   * <code>repeated .ai.stigmer.agentic.agentexecution.v1.WorkspaceWriteBack workspace_write_backs = 17 [json_name = "workspaceWriteBacks"];</code>
   */
  @java.lang.Override
  public ai.stigmer.agentic.agentexecution.v1.WorkspaceWriteBack getWorkspaceWriteBacks(int index) {
    return workspaceWriteBacks_.get(index);
  }
  /**
   * <pre>
   * Git write-back outcomes for workspace entries that had changes.
   *
   * One entry per git-backed workspace entry where the agent modified
   * files and write_back_mode was enabled on the GitRepoSource. Omitted
   * for workspace entries with no changes, non-git sources, or disabled
   * write-back mode.
   *
   * Populated during post-execution processing. Each entry tracks the
   * write-back lifecycle (committed -&gt; pushed -&gt; PR created) and carries
   * the PR URL for UI display.
   *
   * &#64;since Platform-Owned Git Write-Back
   * </pre>
   *
   * <code>repeated .ai.stigmer.agentic.agentexecution.v1.WorkspaceWriteBack workspace_write_backs = 17 [json_name = "workspaceWriteBacks"];</code>
   */
  @java.lang.Override
  public ai.stigmer.agentic.agentexecution.v1.WorkspaceWriteBackOrBuilder getWorkspaceWriteBacksOrBuilder(
      int index) {
    return workspaceWriteBacks_.get(index);
  }

  public static final int SETUP_PROGRESS_FIELD_NUMBER = 18;
  private ai.stigmer.agentic.agentexecution.v1.SetupProgress setupProgress_;
  /**
   * <pre>
   * Real-time setup progress reported by the runner during EXECUTION_PENDING.
   * Populated progressively as the worker completes setup steps (sandbox init,
   * workspace provisioning, skill loading, MCP server connection, etc.).
   * Only meaningful while phase == EXECUTION_PENDING. Cleared by the server
   * when phase transitions away from PENDING.
   * </pre>
   *
   * <code>.ai.stigmer.agentic.agentexecution.v1.SetupProgress setup_progress = 18 [json_name = "setupProgress"];</code>
   * @return Whether the setupProgress field is set.
   */
  @java.lang.Override
  public boolean hasSetupProgress() {
    return ((bitField0_ & 0x00000008) != 0);
  }
  /**
   * <pre>
   * Real-time setup progress reported by the runner during EXECUTION_PENDING.
   * Populated progressively as the worker completes setup steps (sandbox init,
   * workspace provisioning, skill loading, MCP server connection, etc.).
   * Only meaningful while phase == EXECUTION_PENDING. Cleared by the server
   * when phase transitions away from PENDING.
   * </pre>
   *
   * <code>.ai.stigmer.agentic.agentexecution.v1.SetupProgress setup_progress = 18 [json_name = "setupProgress"];</code>
   * @return The setupProgress.
   */
  @java.lang.Override
  public ai.stigmer.agentic.agentexecution.v1.SetupProgress getSetupProgress() {
    return setupProgress_ == null ? ai.stigmer.agentic.agentexecution.v1.SetupProgress.getDefaultInstance() : setupProgress_;
  }
  /**
   * <pre>
   * Real-time setup progress reported by the runner during EXECUTION_PENDING.
   * Populated progressively as the worker completes setup steps (sandbox init,
   * workspace provisioning, skill loading, MCP server connection, etc.).
   * Only meaningful while phase == EXECUTION_PENDING. Cleared by the server
   * when phase transitions away from PENDING.
   * </pre>
   *
   * <code>.ai.stigmer.agentic.agentexecution.v1.SetupProgress setup_progress = 18 [json_name = "setupProgress"];</code>
   */
  @java.lang.Override
  public ai.stigmer.agentic.agentexecution.v1.SetupProgressOrBuilder getSetupProgressOrBuilder() {
    return setupProgress_ == null ? ai.stigmer.agentic.agentexecution.v1.SetupProgress.getDefaultInstance() : setupProgress_;
  }

  public static final int STREAMING_USAGE_FIELD_NUMBER = 20;
  private ai.stigmer.agentic.agentexecution.v1.StreamingUsageSummary streamingUsage_;
  /**
   * <pre>
   * Streaming usage summary, updated progressively during execution.
   * </pre>
   *
   * <code>.ai.stigmer.agentic.agentexecution.v1.StreamingUsageSummary streaming_usage = 20 [json_name = "streamingUsage"];</code>
   * @return Whether the streamingUsage field is set.
   */
  @java.lang.Override
  public boolean hasStreamingUsage() {
    return ((bitField0_ & 0x00000010) != 0);
  }
  /**
   * <pre>
   * Streaming usage summary, updated progressively during execution.
   * </pre>
   *
   * <code>.ai.stigmer.agentic.agentexecution.v1.StreamingUsageSummary streaming_usage = 20 [json_name = "streamingUsage"];</code>
   * @return The streamingUsage.
   */
  @java.lang.Override
  public ai.stigmer.agentic.agentexecution.v1.StreamingUsageSummary getStreamingUsage() {
    return streamingUsage_ == null ? ai.stigmer.agentic.agentexecution.v1.StreamingUsageSummary.getDefaultInstance() : streamingUsage_;
  }
  /**
   * <pre>
   * Streaming usage summary, updated progressively during execution.
   * </pre>
   *
   * <code>.ai.stigmer.agentic.agentexecution.v1.StreamingUsageSummary streaming_usage = 20 [json_name = "streamingUsage"];</code>
   */
  @java.lang.Override
  public ai.stigmer.agentic.agentexecution.v1.StreamingUsageSummaryOrBuilder getStreamingUsageOrBuilder() {
    return streamingUsage_ == null ? ai.stigmer.agentic.agentexecution.v1.StreamingUsageSummary.getDefaultInstance() : streamingUsage_;
  }

  public static final int STRUCTURED_OUTPUT_FIELD_NUMBER = 21;
  private com.google.protobuf.Struct structuredOutput_;
  /**
   * <pre>
   * Structured output extracted from the agent's final response.
   *
   * Populated when ExecutionConfig.structured_output_schema was set and the
   * runner successfully extracted schema-conforming data. Only meaningful
   * when phase is EXECUTION_COMPLETED.
   *
   * For native harness: populated from LangGraph graphState.structuredResponse
   * (ToolStrategy enforcement). For Cursor harness: populated from 3-tier
   * extraction (JSON.parse → fence extraction → extraction LLM fallback).
   *
   * Consumers: frontend architect/refine hooks read this instead of parsing
   * YAML from messages. Workflow callback path reads structured_output from
   * the Temporal activity result (separate channel, same data).
   * </pre>
   *
   * <code>.google.protobuf.Struct structured_output = 21 [json_name = "structuredOutput"];</code>
   * @return Whether the structuredOutput field is set.
   */
  @java.lang.Override
  public boolean hasStructuredOutput() {
    return ((bitField0_ & 0x00000020) != 0);
  }
  /**
   * <pre>
   * Structured output extracted from the agent's final response.
   *
   * Populated when ExecutionConfig.structured_output_schema was set and the
   * runner successfully extracted schema-conforming data. Only meaningful
   * when phase is EXECUTION_COMPLETED.
   *
   * For native harness: populated from LangGraph graphState.structuredResponse
   * (ToolStrategy enforcement). For Cursor harness: populated from 3-tier
   * extraction (JSON.parse → fence extraction → extraction LLM fallback).
   *
   * Consumers: frontend architect/refine hooks read this instead of parsing
   * YAML from messages. Workflow callback path reads structured_output from
   * the Temporal activity result (separate channel, same data).
   * </pre>
   *
   * <code>.google.protobuf.Struct structured_output = 21 [json_name = "structuredOutput"];</code>
   * @return The structuredOutput.
   */
  @java.lang.Override
  public com.google.protobuf.Struct getStructuredOutput() {
    return structuredOutput_ == null ? com.google.protobuf.Struct.getDefaultInstance() : structuredOutput_;
  }
  /**
   * <pre>
   * Structured output extracted from the agent's final response.
   *
   * Populated when ExecutionConfig.structured_output_schema was set and the
   * runner successfully extracted schema-conforming data. Only meaningful
   * when phase is EXECUTION_COMPLETED.
   *
   * For native harness: populated from LangGraph graphState.structuredResponse
   * (ToolStrategy enforcement). For Cursor harness: populated from 3-tier
   * extraction (JSON.parse → fence extraction → extraction LLM fallback).
   *
   * Consumers: frontend architect/refine hooks read this instead of parsing
   * YAML from messages. Workflow callback path reads structured_output from
   * the Temporal activity result (separate channel, same data).
   * </pre>
   *
   * <code>.google.protobuf.Struct structured_output = 21 [json_name = "structuredOutput"];</code>
   */
  @java.lang.Override
  public com.google.protobuf.StructOrBuilder getStructuredOutputOrBuilder() {
    return structuredOutput_ == null ? com.google.protobuf.Struct.getDefaultInstance() : structuredOutput_;
  }

  public static final int FILE_CHANGE_SETS_FIELD_NUMBER = 23;
  @SuppressWarnings("serial")
  private java.util.List<ai.stigmer.agentic.agentexecution.v1.FileChangeSet> fileChangeSets_;
  /**
   * <pre>
   * Server-computed projection of file change sets awaiting or completing
   * review for this execution.
   *
   * &#64;internal
   *
   * Recomputed on every status write from file_review_event_stream (never
   * merged), so it is always consistent with the authoritative ledger — exactly
   * as pending_approvals is recomputed from the message scan / approval stream.
   * Empty until the runner's capture activity authors events (Phase 2). A
   * terminal execution projects no actionable review. See FileChangeSet.
   *
   * Field 23: appended after approval_event_stream (22), the prior maximum.
   * </pre>
   *
   * <code>repeated .ai.stigmer.agentic.agentexecution.v1.FileChangeSet file_change_sets = 23 [json_name = "fileChangeSets"];</code>
   */
  @java.lang.Override
  public java.util.List<ai.stigmer.agentic.agentexecution.v1.FileChangeSet> getFileChangeSetsList() {
    return fileChangeSets_;
  }
  /**
   * <pre>
   * Server-computed projection of file change sets awaiting or completing
   * review for this execution.
   *
   * &#64;internal
   *
   * Recomputed on every status write from file_review_event_stream (never
   * merged), so it is always consistent with the authoritative ledger — exactly
   * as pending_approvals is recomputed from the message scan / approval stream.
   * Empty until the runner's capture activity authors events (Phase 2). A
   * terminal execution projects no actionable review. See FileChangeSet.
   *
   * Field 23: appended after approval_event_stream (22), the prior maximum.
   * </pre>
   *
   * <code>repeated .ai.stigmer.agentic.agentexecution.v1.FileChangeSet file_change_sets = 23 [json_name = "fileChangeSets"];</code>
   */
  @java.lang.Override
  public java.util.List<? extends ai.stigmer.agentic.agentexecution.v1.FileChangeSetOrBuilder> 
      getFileChangeSetsOrBuilderList() {
    return fileChangeSets_;
  }
  /**
   * <pre>
   * Server-computed projection of file change sets awaiting or completing
   * review for this execution.
   *
   * &#64;internal
   *
   * Recomputed on every status write from file_review_event_stream (never
   * merged), so it is always consistent with the authoritative ledger — exactly
   * as pending_approvals is recomputed from the message scan / approval stream.
   * Empty until the runner's capture activity authors events (Phase 2). A
   * terminal execution projects no actionable review. See FileChangeSet.
   *
   * Field 23: appended after approval_event_stream (22), the prior maximum.
   * </pre>
   *
   * <code>repeated .ai.stigmer.agentic.agentexecution.v1.FileChangeSet file_change_sets = 23 [json_name = "fileChangeSets"];</code>
   */
  @java.lang.Override
  public int getFileChangeSetsCount() {
    return fileChangeSets_.size();
  }
  /**
   * <pre>
   * Server-computed projection of file change sets awaiting or completing
   * review for this execution.
   *
   * &#64;internal
   *
   * Recomputed on every status write from file_review_event_stream (never
   * merged), so it is always consistent with the authoritative ledger — exactly
   * as pending_approvals is recomputed from the message scan / approval stream.
   * Empty until the runner's capture activity authors events (Phase 2). A
   * terminal execution projects no actionable review. See FileChangeSet.
   *
   * Field 23: appended after approval_event_stream (22), the prior maximum.
   * </pre>
   *
   * <code>repeated .ai.stigmer.agentic.agentexecution.v1.FileChangeSet file_change_sets = 23 [json_name = "fileChangeSets"];</code>
   */
  @java.lang.Override
  public ai.stigmer.agentic.agentexecution.v1.FileChangeSet getFileChangeSets(int index) {
    return fileChangeSets_.get(index);
  }
  /**
   * <pre>
   * Server-computed projection of file change sets awaiting or completing
   * review for this execution.
   *
   * &#64;internal
   *
   * Recomputed on every status write from file_review_event_stream (never
   * merged), so it is always consistent with the authoritative ledger — exactly
   * as pending_approvals is recomputed from the message scan / approval stream.
   * Empty until the runner's capture activity authors events (Phase 2). A
   * terminal execution projects no actionable review. See FileChangeSet.
   *
   * Field 23: appended after approval_event_stream (22), the prior maximum.
   * </pre>
   *
   * <code>repeated .ai.stigmer.agentic.agentexecution.v1.FileChangeSet file_change_sets = 23 [json_name = "fileChangeSets"];</code>
   */
  @java.lang.Override
  public ai.stigmer.agentic.agentexecution.v1.FileChangeSetOrBuilder getFileChangeSetsOrBuilder(
      int index) {
    return fileChangeSets_.get(index);
  }

  public static final int FILE_REVIEW_EVENT_STREAM_FIELD_NUMBER = 24;
  private ai.stigmer.agentic.agentexecution.v1.FileReviewEventStream fileReviewEventStream_;
  /**
   * <pre>
   * Server-owned, append-only record of every file-review event (baseline /
   * candidate capture, file decisions, reconcile, failures).
   *
   * &#64;internal
   *
   * The persisted source of truth that file_change_sets is projected from — a
   * sibling of approval_event_stream for the file-review lifecycle (the
   * ApprovalEvent oneof is closed, so file events cannot ride it). Appends are
   * keyed by the deterministic FileReviewEvent.event_id, so the stream is
   * idempotent under retries.
   *
   * Write contract (one writer per event type): the runner CONTRIBUTES its
   * capture/reconcile events (BASELINE_CAPTURED / CANDIDATE_CAPTURED /
   * RECONCILED / FAILED) by carrying them here on UpdateStatus; the server folds
   * them into the stored stream append-only, by event_id, on the freshly-loaded
   * execution under the write lock. The runner can never replace an existing
   * event and never authors FILE_DECIDED (the server drops a runner-sent
   * decision); FILE_DECIDED is authored solely by SubmitFileDecision. The stored
   * stream is otherwise preserved in place across writes. See
   * FileReviewEventStream.
   *
   * Field 24: appended after file_change_sets (23), the prior maximum.
   * </pre>
   *
   * <code>.ai.stigmer.agentic.agentexecution.v1.FileReviewEventStream file_review_event_stream = 24 [json_name = "fileReviewEventStream"];</code>
   * @return Whether the fileReviewEventStream field is set.
   */
  @java.lang.Override
  public boolean hasFileReviewEventStream() {
    return ((bitField0_ & 0x00000040) != 0);
  }
  /**
   * <pre>
   * Server-owned, append-only record of every file-review event (baseline /
   * candidate capture, file decisions, reconcile, failures).
   *
   * &#64;internal
   *
   * The persisted source of truth that file_change_sets is projected from — a
   * sibling of approval_event_stream for the file-review lifecycle (the
   * ApprovalEvent oneof is closed, so file events cannot ride it). Appends are
   * keyed by the deterministic FileReviewEvent.event_id, so the stream is
   * idempotent under retries.
   *
   * Write contract (one writer per event type): the runner CONTRIBUTES its
   * capture/reconcile events (BASELINE_CAPTURED / CANDIDATE_CAPTURED /
   * RECONCILED / FAILED) by carrying them here on UpdateStatus; the server folds
   * them into the stored stream append-only, by event_id, on the freshly-loaded
   * execution under the write lock. The runner can never replace an existing
   * event and never authors FILE_DECIDED (the server drops a runner-sent
   * decision); FILE_DECIDED is authored solely by SubmitFileDecision. The stored
   * stream is otherwise preserved in place across writes. See
   * FileReviewEventStream.
   *
   * Field 24: appended after file_change_sets (23), the prior maximum.
   * </pre>
   *
   * <code>.ai.stigmer.agentic.agentexecution.v1.FileReviewEventStream file_review_event_stream = 24 [json_name = "fileReviewEventStream"];</code>
   * @return The fileReviewEventStream.
   */
  @java.lang.Override
  public ai.stigmer.agentic.agentexecution.v1.FileReviewEventStream getFileReviewEventStream() {
    return fileReviewEventStream_ == null ? ai.stigmer.agentic.agentexecution.v1.FileReviewEventStream.getDefaultInstance() : fileReviewEventStream_;
  }
  /**
   * <pre>
   * Server-owned, append-only record of every file-review event (baseline /
   * candidate capture, file decisions, reconcile, failures).
   *
   * &#64;internal
   *
   * The persisted source of truth that file_change_sets is projected from — a
   * sibling of approval_event_stream for the file-review lifecycle (the
   * ApprovalEvent oneof is closed, so file events cannot ride it). Appends are
   * keyed by the deterministic FileReviewEvent.event_id, so the stream is
   * idempotent under retries.
   *
   * Write contract (one writer per event type): the runner CONTRIBUTES its
   * capture/reconcile events (BASELINE_CAPTURED / CANDIDATE_CAPTURED /
   * RECONCILED / FAILED) by carrying them here on UpdateStatus; the server folds
   * them into the stored stream append-only, by event_id, on the freshly-loaded
   * execution under the write lock. The runner can never replace an existing
   * event and never authors FILE_DECIDED (the server drops a runner-sent
   * decision); FILE_DECIDED is authored solely by SubmitFileDecision. The stored
   * stream is otherwise preserved in place across writes. See
   * FileReviewEventStream.
   *
   * Field 24: appended after file_change_sets (23), the prior maximum.
   * </pre>
   *
   * <code>.ai.stigmer.agentic.agentexecution.v1.FileReviewEventStream file_review_event_stream = 24 [json_name = "fileReviewEventStream"];</code>
   */
  @java.lang.Override
  public ai.stigmer.agentic.agentexecution.v1.FileReviewEventStreamOrBuilder getFileReviewEventStreamOrBuilder() {
    return fileReviewEventStream_ == null ? ai.stigmer.agentic.agentexecution.v1.FileReviewEventStream.getDefaultInstance() : fileReviewEventStream_;
  }

  public static final int FILE_CHANGE_PROGRESS_FIELD_NUMBER = 25;
  private ai.stigmer.agentic.agentexecution.v1.FileChangeProgress fileChangeProgress_;
  /**
   * <pre>
   * Transient, non-authoritative snapshot of the workspace delta accumulating
   * during the current turn — the live "N files changed so far" surface, shown
   * before any review is possible.
   *
   * &#64;internal
   *
   * NOT part of the file-review ledger. This is a runner-owned, latest-snapshot
   * display field in the family of setup_progress (18) / streaming_usage (20):
   * the runner overwrites it on each mid-run persist and the server clears it once
   * its change set leaves CAPTURING, exactly as setup_progress is cleared when the
   * phase leaves PENDING. It carries paths + kinds + line counts only (no bodies,
   * no digests) and is never decidable — the turn-boundary CANDIDATE_CAPTURED in
   * file_change_sets remains the single authoritative, reviewable diff. See
   * FileChangeProgress.
   *
   * Field 25: appended after file_review_event_stream (24), the prior maximum.
   * </pre>
   *
   * <code>.ai.stigmer.agentic.agentexecution.v1.FileChangeProgress file_change_progress = 25 [json_name = "fileChangeProgress"];</code>
   * @return Whether the fileChangeProgress field is set.
   */
  @java.lang.Override
  public boolean hasFileChangeProgress() {
    return ((bitField0_ & 0x00000080) != 0);
  }
  /**
   * <pre>
   * Transient, non-authoritative snapshot of the workspace delta accumulating
   * during the current turn — the live "N files changed so far" surface, shown
   * before any review is possible.
   *
   * &#64;internal
   *
   * NOT part of the file-review ledger. This is a runner-owned, latest-snapshot
   * display field in the family of setup_progress (18) / streaming_usage (20):
   * the runner overwrites it on each mid-run persist and the server clears it once
   * its change set leaves CAPTURING, exactly as setup_progress is cleared when the
   * phase leaves PENDING. It carries paths + kinds + line counts only (no bodies,
   * no digests) and is never decidable — the turn-boundary CANDIDATE_CAPTURED in
   * file_change_sets remains the single authoritative, reviewable diff. See
   * FileChangeProgress.
   *
   * Field 25: appended after file_review_event_stream (24), the prior maximum.
   * </pre>
   *
   * <code>.ai.stigmer.agentic.agentexecution.v1.FileChangeProgress file_change_progress = 25 [json_name = "fileChangeProgress"];</code>
   * @return The fileChangeProgress.
   */
  @java.lang.Override
  public ai.stigmer.agentic.agentexecution.v1.FileChangeProgress getFileChangeProgress() {
    return fileChangeProgress_ == null ? ai.stigmer.agentic.agentexecution.v1.FileChangeProgress.getDefaultInstance() : fileChangeProgress_;
  }
  /**
   * <pre>
   * Transient, non-authoritative snapshot of the workspace delta accumulating
   * during the current turn — the live "N files changed so far" surface, shown
   * before any review is possible.
   *
   * &#64;internal
   *
   * NOT part of the file-review ledger. This is a runner-owned, latest-snapshot
   * display field in the family of setup_progress (18) / streaming_usage (20):
   * the runner overwrites it on each mid-run persist and the server clears it once
   * its change set leaves CAPTURING, exactly as setup_progress is cleared when the
   * phase leaves PENDING. It carries paths + kinds + line counts only (no bodies,
   * no digests) and is never decidable — the turn-boundary CANDIDATE_CAPTURED in
   * file_change_sets remains the single authoritative, reviewable diff. See
   * FileChangeProgress.
   *
   * Field 25: appended after file_review_event_stream (24), the prior maximum.
   * </pre>
   *
   * <code>.ai.stigmer.agentic.agentexecution.v1.FileChangeProgress file_change_progress = 25 [json_name = "fileChangeProgress"];</code>
   */
  @java.lang.Override
  public ai.stigmer.agentic.agentexecution.v1.FileChangeProgressOrBuilder getFileChangeProgressOrBuilder() {
    return fileChangeProgress_ == null ? ai.stigmer.agentic.agentexecution.v1.FileChangeProgress.getDefaultInstance() : fileChangeProgress_;
  }

  public static final int RECALLED_MEMORIES_REPORT_FIELD_NUMBER = 26;
  private ai.stigmer.agentic.agentexecution.v1.RecalledMemoriesReport recalledMemoriesReport_;
  /**
   * <pre>
   * Report of which recalled memories were injected into this execution's
   * prompt.
   *
   * Absent, or present with selection_active=false, means wholesale: every
   * fact in the spec.recalled_memories snapshot was injected.
   *
   * &#64;internal
   *
   * Runner-owned, single writer (the streaming_usage / context_info
   * posture): written at most once per execution, at prompt build, through
   * the runner's status lane. The server merges it presence-guarded and
   * never writes it — the create pipeline stamps only the spec snapshot
   * (DD-008 D5, stigmer/stigmer#293 Phase 3a).
   *
   * The audit contract: spec.recalled_memories is the server-composed
   * CANDIDATE set (everything the caller's consent state made recallable);
   * what-the-model-saw = this report joined to that snapshot. Absent
   * report = wholesale is true by construction — for every execution that
   * predates this field and for every wholesale path since (selection
   * below threshold, no embeddings-capable provider, embed/rank failure).
   *
   * Field 26: appended after file_change_progress (25), the prior maximum.
   * </pre>
   *
   * <code>.ai.stigmer.agentic.agentexecution.v1.RecalledMemoriesReport recalled_memories_report = 26 [json_name = "recalledMemoriesReport"];</code>
   * @return Whether the recalledMemoriesReport field is set.
   */
  @java.lang.Override
  public boolean hasRecalledMemoriesReport() {
    return ((bitField0_ & 0x00000100) != 0);
  }
  /**
   * <pre>
   * Report of which recalled memories were injected into this execution's
   * prompt.
   *
   * Absent, or present with selection_active=false, means wholesale: every
   * fact in the spec.recalled_memories snapshot was injected.
   *
   * &#64;internal
   *
   * Runner-owned, single writer (the streaming_usage / context_info
   * posture): written at most once per execution, at prompt build, through
   * the runner's status lane. The server merges it presence-guarded and
   * never writes it — the create pipeline stamps only the spec snapshot
   * (DD-008 D5, stigmer/stigmer#293 Phase 3a).
   *
   * The audit contract: spec.recalled_memories is the server-composed
   * CANDIDATE set (everything the caller's consent state made recallable);
   * what-the-model-saw = this report joined to that snapshot. Absent
   * report = wholesale is true by construction — for every execution that
   * predates this field and for every wholesale path since (selection
   * below threshold, no embeddings-capable provider, embed/rank failure).
   *
   * Field 26: appended after file_change_progress (25), the prior maximum.
   * </pre>
   *
   * <code>.ai.stigmer.agentic.agentexecution.v1.RecalledMemoriesReport recalled_memories_report = 26 [json_name = "recalledMemoriesReport"];</code>
   * @return The recalledMemoriesReport.
   */
  @java.lang.Override
  public ai.stigmer.agentic.agentexecution.v1.RecalledMemoriesReport getRecalledMemoriesReport() {
    return recalledMemoriesReport_ == null ? ai.stigmer.agentic.agentexecution.v1.RecalledMemoriesReport.getDefaultInstance() : recalledMemoriesReport_;
  }
  /**
   * <pre>
   * Report of which recalled memories were injected into this execution's
   * prompt.
   *
   * Absent, or present with selection_active=false, means wholesale: every
   * fact in the spec.recalled_memories snapshot was injected.
   *
   * &#64;internal
   *
   * Runner-owned, single writer (the streaming_usage / context_info
   * posture): written at most once per execution, at prompt build, through
   * the runner's status lane. The server merges it presence-guarded and
   * never writes it — the create pipeline stamps only the spec snapshot
   * (DD-008 D5, stigmer/stigmer#293 Phase 3a).
   *
   * The audit contract: spec.recalled_memories is the server-composed
   * CANDIDATE set (everything the caller's consent state made recallable);
   * what-the-model-saw = this report joined to that snapshot. Absent
   * report = wholesale is true by construction — for every execution that
   * predates this field and for every wholesale path since (selection
   * below threshold, no embeddings-capable provider, embed/rank failure).
   *
   * Field 26: appended after file_change_progress (25), the prior maximum.
   * </pre>
   *
   * <code>.ai.stigmer.agentic.agentexecution.v1.RecalledMemoriesReport recalled_memories_report = 26 [json_name = "recalledMemoriesReport"];</code>
   */
  @java.lang.Override
  public ai.stigmer.agentic.agentexecution.v1.RecalledMemoriesReportOrBuilder getRecalledMemoriesReportOrBuilder() {
    return recalledMemoriesReport_ == null ? ai.stigmer.agentic.agentexecution.v1.RecalledMemoriesReport.getDefaultInstance() : recalledMemoriesReport_;
  }

  private byte memoizedIsInitialized = -1;
  @java.lang.Override
  public final boolean isInitialized() {
    byte isInitialized = memoizedIsInitialized;
    if (isInitialized == 1) return true;
    if (isInitialized == 0) return false;

    memoizedIsInitialized = 1;
    return true;
  }

  @java.lang.Override
  public void writeTo(com.google.protobuf.CodedOutputStream output)
                      throws java.io.IOException {
    for (int i = 0; i < messages_.size(); i++) {
      output.writeMessage(1, messages_.get(i));
    }
    if (phase_ != ai.stigmer.agentic.agentexecution.v1.ExecutionPhase.EXECUTION_PHASE_UNSPECIFIED.getNumber()) {
      output.writeEnum(2, phase_);
    }
    for (int i = 0; i < subAgentExecutions_.size(); i++) {
      output.writeMessage(4, subAgentExecutions_.get(i));
    }
    if (!com.google.protobuf.GeneratedMessage.isStringEmpty(error_)) {
      com.google.protobuf.GeneratedMessage.writeString(output, 6, error_);
    }
    if (!com.google.protobuf.GeneratedMessage.isStringEmpty(startedAt_)) {
      com.google.protobuf.GeneratedMessage.writeString(output, 7, startedAt_);
    }
    if (!com.google.protobuf.GeneratedMessage.isStringEmpty(completedAt_)) {
      com.google.protobuf.GeneratedMessage.writeString(output, 8, completedAt_);
    }
    com.google.protobuf.GeneratedMessage
      .serializeStringMapTo(
        output,
        internalGetTodos(),
        TodosDefaultEntryHolder.defaultEntry,
        9);
    if (!callbackToken_.isEmpty()) {
      output.writeBytes(10, callbackToken_);
    }
    if (((bitField0_ & 0x00000004) != 0)) {
      output.writeMessage(14, getContextInfo());
    }
    for (int i = 0; i < artifacts_.size(); i++) {
      output.writeMessage(15, artifacts_.get(i));
    }
    for (int i = 0; i < pendingApprovals_.size(); i++) {
      output.writeMessage(16, pendingApprovals_.get(i));
    }
    for (int i = 0; i < workspaceWriteBacks_.size(); i++) {
      output.writeMessage(17, workspaceWriteBacks_.get(i));
    }
    if (((bitField0_ & 0x00000008) != 0)) {
      output.writeMessage(18, getSetupProgress());
    }
    if (((bitField0_ & 0x00000010) != 0)) {
      output.writeMessage(20, getStreamingUsage());
    }
    if (((bitField0_ & 0x00000020) != 0)) {
      output.writeMessage(21, getStructuredOutput());
    }
    if (((bitField0_ & 0x00000002) != 0)) {
      output.writeMessage(22, getApprovalEventStream());
    }
    for (int i = 0; i < fileChangeSets_.size(); i++) {
      output.writeMessage(23, fileChangeSets_.get(i));
    }
    if (((bitField0_ & 0x00000040) != 0)) {
      output.writeMessage(24, getFileReviewEventStream());
    }
    if (((bitField0_ & 0x00000080) != 0)) {
      output.writeMessage(25, getFileChangeProgress());
    }
    if (((bitField0_ & 0x00000100) != 0)) {
      output.writeMessage(26, getRecalledMemoriesReport());
    }
    if (((bitField0_ & 0x00000001) != 0)) {
      output.writeMessage(99, getAudit());
    }
    getUnknownFields().writeTo(output);
  }

  @java.lang.Override
  public int getSerializedSize() {
    int size = memoizedSize;
    if (size != -1) return size;

    size = 0;

        {
          final int count = messages_.size();
          for (int i = 0; i < count; i++) {
            size += com.google.protobuf.CodedOutputStream
              .computeMessageSizeNoTag(messages_.get(i));
          }
          size += 1 * count;
        }
    if (phase_ != ai.stigmer.agentic.agentexecution.v1.ExecutionPhase.EXECUTION_PHASE_UNSPECIFIED.getNumber()) {
      size += com.google.protobuf.CodedOutputStream
        .computeEnumSize(2, phase_);
    }

        {
          final int count = subAgentExecutions_.size();
          for (int i = 0; i < count; i++) {
            size += com.google.protobuf.CodedOutputStream
              .computeMessageSizeNoTag(subAgentExecutions_.get(i));
          }
          size += 1 * count;
        }
    if (!com.google.protobuf.GeneratedMessage.isStringEmpty(error_)) {
      size += com.google.protobuf.GeneratedMessage.computeStringSize(6, error_);
    }
    if (!com.google.protobuf.GeneratedMessage.isStringEmpty(startedAt_)) {
      size += com.google.protobuf.GeneratedMessage.computeStringSize(7, startedAt_);
    }
    if (!com.google.protobuf.GeneratedMessage.isStringEmpty(completedAt_)) {
      size += com.google.protobuf.GeneratedMessage.computeStringSize(8, completedAt_);
    }
    for (java.util.Map.Entry<java.lang.String, ai.stigmer.agentic.agentexecution.v1.TodoItem> entry
         : internalGetTodos().getMap().entrySet()) {
      com.google.protobuf.MapEntry<java.lang.String, ai.stigmer.agentic.agentexecution.v1.TodoItem>
      todos__ = TodosDefaultEntryHolder.defaultEntry.newBuilderForType()
          .setKey(entry.getKey())
          .setValue(entry.getValue())
          .buildPartial();
      size += com.google.protobuf.CodedOutputStream
          .computeMessageSize(9, todos__);
    }
    if (!callbackToken_.isEmpty()) {
      size += com.google.protobuf.CodedOutputStream
        .computeBytesSize(10, callbackToken_);
    }
    if (((bitField0_ & 0x00000004) != 0)) {
      size += com.google.protobuf.CodedOutputStream
        .computeMessageSize(14, getContextInfo());
    }

        {
          final int count = artifacts_.size();
          for (int i = 0; i < count; i++) {
            size += com.google.protobuf.CodedOutputStream
              .computeMessageSizeNoTag(artifacts_.get(i));
          }
          size += 1 * count;
        }

        {
          final int count = pendingApprovals_.size();
          for (int i = 0; i < count; i++) {
            size += com.google.protobuf.CodedOutputStream
              .computeMessageSizeNoTag(pendingApprovals_.get(i));
          }
          size += 2 * count;
        }

        {
          final int count = workspaceWriteBacks_.size();
          for (int i = 0; i < count; i++) {
            size += com.google.protobuf.CodedOutputStream
              .computeMessageSizeNoTag(workspaceWriteBacks_.get(i));
          }
          size += 2 * count;
        }
    if (((bitField0_ & 0x00000008) != 0)) {
      size += com.google.protobuf.CodedOutputStream
        .computeMessageSize(18, getSetupProgress());
    }
    if (((bitField0_ & 0x00000010) != 0)) {
      size += com.google.protobuf.CodedOutputStream
        .computeMessageSize(20, getStreamingUsage());
    }
    if (((bitField0_ & 0x00000020) != 0)) {
      size += com.google.protobuf.CodedOutputStream
        .computeMessageSize(21, getStructuredOutput());
    }
    if (((bitField0_ & 0x00000002) != 0)) {
      size += com.google.protobuf.CodedOutputStream
        .computeMessageSize(22, getApprovalEventStream());
    }

        {
          final int count = fileChangeSets_.size();
          for (int i = 0; i < count; i++) {
            size += com.google.protobuf.CodedOutputStream
              .computeMessageSizeNoTag(fileChangeSets_.get(i));
          }
          size += 2 * count;
        }
    if (((bitField0_ & 0x00000040) != 0)) {
      size += com.google.protobuf.CodedOutputStream
        .computeMessageSize(24, getFileReviewEventStream());
    }
    if (((bitField0_ & 0x00000080) != 0)) {
      size += com.google.protobuf.CodedOutputStream
        .computeMessageSize(25, getFileChangeProgress());
    }
    if (((bitField0_ & 0x00000100) != 0)) {
      size += com.google.protobuf.CodedOutputStream
        .computeMessageSize(26, getRecalledMemoriesReport());
    }
    if (((bitField0_ & 0x00000001) != 0)) {
      size += com.google.protobuf.CodedOutputStream
        .computeMessageSize(99, getAudit());
    }
    size += getUnknownFields().getSerializedSize();
    memoizedSize = size;
    return size;
  }

  @java.lang.Override
  public boolean equals(final java.lang.Object obj) {
    if (obj == this) {
     return true;
    }
    if (!(obj instanceof ai.stigmer.agentic.agentexecution.v1.AgentExecutionStatus)) {
      return super.equals(obj);
    }
    ai.stigmer.agentic.agentexecution.v1.AgentExecutionStatus other = (ai.stigmer.agentic.agentexecution.v1.AgentExecutionStatus) obj;

    if (hasAudit() != other.hasAudit()) return false;
    if (hasAudit()) {
      if (!getAudit()
          .equals(other.getAudit())) return false;
    }
    if (!getMessagesList()
        .equals(other.getMessagesList())) return false;
    if (phase_ != other.phase_) return false;
    if (!getSubAgentExecutionsList()
        .equals(other.getSubAgentExecutionsList())) return false;
    if (!getError()
        .equals(other.getError())) return false;
    if (!getStartedAt()
        .equals(other.getStartedAt())) return false;
    if (!getCompletedAt()
        .equals(other.getCompletedAt())) return false;
    if (!internalGetTodos().equals(
        other.internalGetTodos())) return false;
    if (!getCallbackToken()
        .equals(other.getCallbackToken())) return false;
    if (!getPendingApprovalsList()
        .equals(other.getPendingApprovalsList())) return false;
    if (hasApprovalEventStream() != other.hasApprovalEventStream()) return false;
    if (hasApprovalEventStream()) {
      if (!getApprovalEventStream()
          .equals(other.getApprovalEventStream())) return false;
    }
    if (hasContextInfo() != other.hasContextInfo()) return false;
    if (hasContextInfo()) {
      if (!getContextInfo()
          .equals(other.getContextInfo())) return false;
    }
    if (!getArtifactsList()
        .equals(other.getArtifactsList())) return false;
    if (!getWorkspaceWriteBacksList()
        .equals(other.getWorkspaceWriteBacksList())) return false;
    if (hasSetupProgress() != other.hasSetupProgress()) return false;
    if (hasSetupProgress()) {
      if (!getSetupProgress()
          .equals(other.getSetupProgress())) return false;
    }
    if (hasStreamingUsage() != other.hasStreamingUsage()) return false;
    if (hasStreamingUsage()) {
      if (!getStreamingUsage()
          .equals(other.getStreamingUsage())) return false;
    }
    if (hasStructuredOutput() != other.hasStructuredOutput()) return false;
    if (hasStructuredOutput()) {
      if (!getStructuredOutput()
          .equals(other.getStructuredOutput())) return false;
    }
    if (!getFileChangeSetsList()
        .equals(other.getFileChangeSetsList())) return false;
    if (hasFileReviewEventStream() != other.hasFileReviewEventStream()) return false;
    if (hasFileReviewEventStream()) {
      if (!getFileReviewEventStream()
          .equals(other.getFileReviewEventStream())) return false;
    }
    if (hasFileChangeProgress() != other.hasFileChangeProgress()) return false;
    if (hasFileChangeProgress()) {
      if (!getFileChangeProgress()
          .equals(other.getFileChangeProgress())) return false;
    }
    if (hasRecalledMemoriesReport() != other.hasRecalledMemoriesReport()) return false;
    if (hasRecalledMemoriesReport()) {
      if (!getRecalledMemoriesReport()
          .equals(other.getRecalledMemoriesReport())) return false;
    }
    if (!getUnknownFields().equals(other.getUnknownFields())) return false;
    return true;
  }

  @java.lang.Override
  public int hashCode() {
    if (memoizedHashCode != 0) {
      return memoizedHashCode;
    }
    int hash = 41;
    hash = (19 * hash) + getDescriptor().hashCode();
    if (hasAudit()) {
      hash = (37 * hash) + AUDIT_FIELD_NUMBER;
      hash = (53 * hash) + getAudit().hashCode();
    }
    if (getMessagesCount() > 0) {
      hash = (37 * hash) + MESSAGES_FIELD_NUMBER;
      hash = (53 * hash) + getMessagesList().hashCode();
    }
    hash = (37 * hash) + PHASE_FIELD_NUMBER;
    hash = (53 * hash) + phase_;
    if (getSubAgentExecutionsCount() > 0) {
      hash = (37 * hash) + SUB_AGENT_EXECUTIONS_FIELD_NUMBER;
      hash = (53 * hash) + getSubAgentExecutionsList().hashCode();
    }
    hash = (37 * hash) + ERROR_FIELD_NUMBER;
    hash = (53 * hash) + getError().hashCode();
    hash = (37 * hash) + STARTED_AT_FIELD_NUMBER;
    hash = (53 * hash) + getStartedAt().hashCode();
    hash = (37 * hash) + COMPLETED_AT_FIELD_NUMBER;
    hash = (53 * hash) + getCompletedAt().hashCode();
    if (!internalGetTodos().getMap().isEmpty()) {
      hash = (37 * hash) + TODOS_FIELD_NUMBER;
      hash = (53 * hash) + internalGetTodos().hashCode();
    }
    hash = (37 * hash) + CALLBACK_TOKEN_FIELD_NUMBER;
    hash = (53 * hash) + getCallbackToken().hashCode();
    if (getPendingApprovalsCount() > 0) {
      hash = (37 * hash) + PENDING_APPROVALS_FIELD_NUMBER;
      hash = (53 * hash) + getPendingApprovalsList().hashCode();
    }
    if (hasApprovalEventStream()) {
      hash = (37 * hash) + APPROVAL_EVENT_STREAM_FIELD_NUMBER;
      hash = (53 * hash) + getApprovalEventStream().hashCode();
    }
    if (hasContextInfo()) {
      hash = (37 * hash) + CONTEXT_INFO_FIELD_NUMBER;
      hash = (53 * hash) + getContextInfo().hashCode();
    }
    if (getArtifactsCount() > 0) {
      hash = (37 * hash) + ARTIFACTS_FIELD_NUMBER;
      hash = (53 * hash) + getArtifactsList().hashCode();
    }
    if (getWorkspaceWriteBacksCount() > 0) {
      hash = (37 * hash) + WORKSPACE_WRITE_BACKS_FIELD_NUMBER;
      hash = (53 * hash) + getWorkspaceWriteBacksList().hashCode();
    }
    if (hasSetupProgress()) {
      hash = (37 * hash) + SETUP_PROGRESS_FIELD_NUMBER;
      hash = (53 * hash) + getSetupProgress().hashCode();
    }
    if (hasStreamingUsage()) {
      hash = (37 * hash) + STREAMING_USAGE_FIELD_NUMBER;
      hash = (53 * hash) + getStreamingUsage().hashCode();
    }
    if (hasStructuredOutput()) {
      hash = (37 * hash) + STRUCTURED_OUTPUT_FIELD_NUMBER;
      hash = (53 * hash) + getStructuredOutput().hashCode();
    }
    if (getFileChangeSetsCount() > 0) {
      hash = (37 * hash) + FILE_CHANGE_SETS_FIELD_NUMBER;
      hash = (53 * hash) + getFileChangeSetsList().hashCode();
    }
    if (hasFileReviewEventStream()) {
      hash = (37 * hash) + FILE_REVIEW_EVENT_STREAM_FIELD_NUMBER;
      hash = (53 * hash) + getFileReviewEventStream().hashCode();
    }
    if (hasFileChangeProgress()) {
      hash = (37 * hash) + FILE_CHANGE_PROGRESS_FIELD_NUMBER;
      hash = (53 * hash) + getFileChangeProgress().hashCode();
    }
    if (hasRecalledMemoriesReport()) {
      hash = (37 * hash) + RECALLED_MEMORIES_REPORT_FIELD_NUMBER;
      hash = (53 * hash) + getRecalledMemoriesReport().hashCode();
    }
    hash = (29 * hash) + getUnknownFields().hashCode();
    memoizedHashCode = hash;
    return hash;
  }

  public static ai.stigmer.agentic.agentexecution.v1.AgentExecutionStatus parseFrom(
      java.nio.ByteBuffer data)
      throws com.google.protobuf.InvalidProtocolBufferException {
    return PARSER.parseFrom(data);
  }
  public static ai.stigmer.agentic.agentexecution.v1.AgentExecutionStatus parseFrom(
      java.nio.ByteBuffer data,
      com.google.protobuf.ExtensionRegistryLite extensionRegistry)
      throws com.google.protobuf.InvalidProtocolBufferException {
    return PARSER.parseFrom(data, extensionRegistry);
  }
  public static ai.stigmer.agentic.agentexecution.v1.AgentExecutionStatus parseFrom(
      com.google.protobuf.ByteString data)
      throws com.google.protobuf.InvalidProtocolBufferException {
    return PARSER.parseFrom(data);
  }
  public static ai.stigmer.agentic.agentexecution.v1.AgentExecutionStatus parseFrom(
      com.google.protobuf.ByteString data,
      com.google.protobuf.ExtensionRegistryLite extensionRegistry)
      throws com.google.protobuf.InvalidProtocolBufferException {
    return PARSER.parseFrom(data, extensionRegistry);
  }
  public static ai.stigmer.agentic.agentexecution.v1.AgentExecutionStatus parseFrom(byte[] data)
      throws com.google.protobuf.InvalidProtocolBufferException {
    return PARSER.parseFrom(data);
  }
  public static ai.stigmer.agentic.agentexecution.v1.AgentExecutionStatus parseFrom(
      byte[] data,
      com.google.protobuf.ExtensionRegistryLite extensionRegistry)
      throws com.google.protobuf.InvalidProtocolBufferException {
    return PARSER.parseFrom(data, extensionRegistry);
  }
  public static ai.stigmer.agentic.agentexecution.v1.AgentExecutionStatus parseFrom(java.io.InputStream input)
      throws java.io.IOException {
    return com.google.protobuf.GeneratedMessage
        .parseWithIOException(PARSER, input);
  }
  public static ai.stigmer.agentic.agentexecution.v1.AgentExecutionStatus parseFrom(
      java.io.InputStream input,
      com.google.protobuf.ExtensionRegistryLite extensionRegistry)
      throws java.io.IOException {
    return com.google.protobuf.GeneratedMessage
        .parseWithIOException(PARSER, input, extensionRegistry);
  }

  public static ai.stigmer.agentic.agentexecution.v1.AgentExecutionStatus parseDelimitedFrom(java.io.InputStream input)
      throws java.io.IOException {
    return com.google.protobuf.GeneratedMessage
        .parseDelimitedWithIOException(PARSER, input);
  }

  public static ai.stigmer.agentic.agentexecution.v1.AgentExecutionStatus parseDelimitedFrom(
      java.io.InputStream input,
      com.google.protobuf.ExtensionRegistryLite extensionRegistry)
      throws java.io.IOException {
    return com.google.protobuf.GeneratedMessage
        .parseDelimitedWithIOException(PARSER, input, extensionRegistry);
  }
  public static ai.stigmer.agentic.agentexecution.v1.AgentExecutionStatus parseFrom(
      com.google.protobuf.CodedInputStream input)
      throws java.io.IOException {
    return com.google.protobuf.GeneratedMessage
        .parseWithIOException(PARSER, input);
  }
  public static ai.stigmer.agentic.agentexecution.v1.AgentExecutionStatus parseFrom(
      com.google.protobuf.CodedInputStream input,
      com.google.protobuf.ExtensionRegistryLite extensionRegistry)
      throws java.io.IOException {
    return com.google.protobuf.GeneratedMessage
        .parseWithIOException(PARSER, input, extensionRegistry);
  }

  @java.lang.Override
  public Builder newBuilderForType() { return newBuilder(); }
  public static Builder newBuilder() {
    return DEFAULT_INSTANCE.toBuilder();
  }
  public static Builder newBuilder(ai.stigmer.agentic.agentexecution.v1.AgentExecutionStatus prototype) {
    return DEFAULT_INSTANCE.toBuilder().mergeFrom(prototype);
  }
  @java.lang.Override
  public Builder toBuilder() {
    return this == DEFAULT_INSTANCE
        ? new Builder() : new Builder().mergeFrom(this);
  }

  @java.lang.Override
  protected Builder newBuilderForType(
      com.google.protobuf.GeneratedMessage.BuilderParent parent) {
    Builder builder = new Builder(parent);
    return builder;
  }
  /**
   * <pre>
   * AgentExecutionStatus contains all execution results and state.
   * Everything populated during or after execution goes here, not in spec.
   * </pre>
   *
   * Protobuf type {@code ai.stigmer.agentic.agentexecution.v1.AgentExecutionStatus}
   */
  public static final class Builder extends
      com.google.protobuf.GeneratedMessage.Builder<Builder> implements
      // @@protoc_insertion_point(builder_implements:ai.stigmer.agentic.agentexecution.v1.AgentExecutionStatus)
      ai.stigmer.agentic.agentexecution.v1.AgentExecutionStatusOrBuilder {
    public static final com.google.protobuf.Descriptors.Descriptor
        getDescriptor() {
      return ai.stigmer.agentic.agentexecution.v1.ApiProto.internal_static_ai_stigmer_agentic_agentexecution_v1_AgentExecutionStatus_descriptor;
    }

    @SuppressWarnings({"rawtypes"})
    protected com.google.protobuf.MapFieldReflectionAccessor internalGetMapFieldReflection(
        int number) {
      switch (number) {
        case 9:
          return internalGetTodos();
        default:
          throw new RuntimeException(
              "Invalid map field number: " + number);
      }
    }
    @SuppressWarnings({"rawtypes"})
    protected com.google.protobuf.MapFieldReflectionAccessor internalGetMutableMapFieldReflection(
        int number) {
      switch (number) {
        case 9:
          return internalGetMutableTodos();
        default:
          throw new RuntimeException(
              "Invalid map field number: " + number);
      }
    }
    @java.lang.Override
    protected com.google.protobuf.GeneratedMessage.FieldAccessorTable
        internalGetFieldAccessorTable() {
      return ai.stigmer.agentic.agentexecution.v1.ApiProto.internal_static_ai_stigmer_agentic_agentexecution_v1_AgentExecutionStatus_fieldAccessorTable
          .ensureFieldAccessorsInitialized(
              ai.stigmer.agentic.agentexecution.v1.AgentExecutionStatus.class, ai.stigmer.agentic.agentexecution.v1.AgentExecutionStatus.Builder.class);
    }

    // Construct using ai.stigmer.agentic.agentexecution.v1.AgentExecutionStatus.newBuilder()
    private Builder() {
      maybeForceBuilderInitialization();
    }

    private Builder(
        com.google.protobuf.GeneratedMessage.BuilderParent parent) {
      super(parent);
      maybeForceBuilderInitialization();
    }
    private void maybeForceBuilderInitialization() {
      if (com.google.protobuf.GeneratedMessage
              .alwaysUseFieldBuilders) {
        internalGetAuditFieldBuilder();
        internalGetMessagesFieldBuilder();
        internalGetSubAgentExecutionsFieldBuilder();
        internalGetPendingApprovalsFieldBuilder();
        internalGetApprovalEventStreamFieldBuilder();
        internalGetContextInfoFieldBuilder();
        internalGetArtifactsFieldBuilder();
        internalGetWorkspaceWriteBacksFieldBuilder();
        internalGetSetupProgressFieldBuilder();
        internalGetStreamingUsageFieldBuilder();
        internalGetStructuredOutputFieldBuilder();
        internalGetFileChangeSetsFieldBuilder();
        internalGetFileReviewEventStreamFieldBuilder();
        internalGetFileChangeProgressFieldBuilder();
        internalGetRecalledMemoriesReportFieldBuilder();
      }
    }
    @java.lang.Override
    public Builder clear() {
      super.clear();
      bitField0_ = 0;
      audit_ = null;
      if (auditBuilder_ != null) {
        auditBuilder_.dispose();
        auditBuilder_ = null;
      }
      if (messagesBuilder_ == null) {
        messages_ = java.util.Collections.emptyList();
      } else {
        messages_ = null;
        messagesBuilder_.clear();
      }
      bitField0_ = (bitField0_ & ~0x00000002);
      phase_ = 0;
      if (subAgentExecutionsBuilder_ == null) {
        subAgentExecutions_ = java.util.Collections.emptyList();
      } else {
        subAgentExecutions_ = null;
        subAgentExecutionsBuilder_.clear();
      }
      bitField0_ = (bitField0_ & ~0x00000008);
      error_ = "";
      startedAt_ = "";
      completedAt_ = "";
      internalGetMutableTodos().clear();
      callbackToken_ = com.google.protobuf.ByteString.EMPTY;
      if (pendingApprovalsBuilder_ == null) {
        pendingApprovals_ = java.util.Collections.emptyList();
      } else {
        pendingApprovals_ = null;
        pendingApprovalsBuilder_.clear();
      }
      bitField0_ = (bitField0_ & ~0x00000200);
      approvalEventStream_ = null;
      if (approvalEventStreamBuilder_ != null) {
        approvalEventStreamBuilder_.dispose();
        approvalEventStreamBuilder_ = null;
      }
      contextInfo_ = null;
      if (contextInfoBuilder_ != null) {
        contextInfoBuilder_.dispose();
        contextInfoBuilder_ = null;
      }
      if (artifactsBuilder_ == null) {
        artifacts_ = java.util.Collections.emptyList();
      } else {
        artifacts_ = null;
        artifactsBuilder_.clear();
      }
      bitField0_ = (bitField0_ & ~0x00001000);
      if (workspaceWriteBacksBuilder_ == null) {
        workspaceWriteBacks_ = java.util.Collections.emptyList();
      } else {
        workspaceWriteBacks_ = null;
        workspaceWriteBacksBuilder_.clear();
      }
      bitField0_ = (bitField0_ & ~0x00002000);
      setupProgress_ = null;
      if (setupProgressBuilder_ != null) {
        setupProgressBuilder_.dispose();
        setupProgressBuilder_ = null;
      }
      streamingUsage_ = null;
      if (streamingUsageBuilder_ != null) {
        streamingUsageBuilder_.dispose();
        streamingUsageBuilder_ = null;
      }
      structuredOutput_ = null;
      if (structuredOutputBuilder_ != null) {
        structuredOutputBuilder_.dispose();
        structuredOutputBuilder_ = null;
      }
      if (fileChangeSetsBuilder_ == null) {
        fileChangeSets_ = java.util.Collections.emptyList();
      } else {
        fileChangeSets_ = null;
        fileChangeSetsBuilder_.clear();
      }
      bitField0_ = (bitField0_ & ~0x00020000);
      fileReviewEventStream_ = null;
      if (fileReviewEventStreamBuilder_ != null) {
        fileReviewEventStreamBuilder_.dispose();
        fileReviewEventStreamBuilder_ = null;
      }
      fileChangeProgress_ = null;
      if (fileChangeProgressBuilder_ != null) {
        fileChangeProgressBuilder_.dispose();
        fileChangeProgressBuilder_ = null;
      }
      recalledMemoriesReport_ = null;
      if (recalledMemoriesReportBuilder_ != null) {
        recalledMemoriesReportBuilder_.dispose();
        recalledMemoriesReportBuilder_ = null;
      }
      return this;
    }

    @java.lang.Override
    public com.google.protobuf.Descriptors.Descriptor
        getDescriptorForType() {
      return ai.stigmer.agentic.agentexecution.v1.ApiProto.internal_static_ai_stigmer_agentic_agentexecution_v1_AgentExecutionStatus_descriptor;
    }

    @java.lang.Override
    public ai.stigmer.agentic.agentexecution.v1.AgentExecutionStatus getDefaultInstanceForType() {
      return ai.stigmer.agentic.agentexecution.v1.AgentExecutionStatus.getDefaultInstance();
    }

    @java.lang.Override
    public ai.stigmer.agentic.agentexecution.v1.AgentExecutionStatus build() {
      ai.stigmer.agentic.agentexecution.v1.AgentExecutionStatus result = buildPartial();
      if (!result.isInitialized()) {
        throw newUninitializedMessageException(result);
      }
      return result;
    }

    @java.lang.Override
    public ai.stigmer.agentic.agentexecution.v1.AgentExecutionStatus buildPartial() {
      ai.stigmer.agentic.agentexecution.v1.AgentExecutionStatus result = new ai.stigmer.agentic.agentexecution.v1.AgentExecutionStatus(this);
      buildPartialRepeatedFields(result);
      if (bitField0_ != 0) { buildPartial0(result); }
      onBuilt();
      return result;
    }

    private void buildPartialRepeatedFields(ai.stigmer.agentic.agentexecution.v1.AgentExecutionStatus result) {
      if (messagesBuilder_ == null) {
        if (((bitField0_ & 0x00000002) != 0)) {
          messages_ = java.util.Collections.unmodifiableList(messages_);
          bitField0_ = (bitField0_ & ~0x00000002);
        }
        result.messages_ = messages_;
      } else {
        result.messages_ = messagesBuilder_.build();
      }
      if (subAgentExecutionsBuilder_ == null) {
        if (((bitField0_ & 0x00000008) != 0)) {
          subAgentExecutions_ = java.util.Collections.unmodifiableList(subAgentExecutions_);
          bitField0_ = (bitField0_ & ~0x00000008);
        }
        result.subAgentExecutions_ = subAgentExecutions_;
      } else {
        result.subAgentExecutions_ = subAgentExecutionsBuilder_.build();
      }
      if (pendingApprovalsBuilder_ == null) {
        if (((bitField0_ & 0x00000200) != 0)) {
          pendingApprovals_ = java.util.Collections.unmodifiableList(pendingApprovals_);
          bitField0_ = (bitField0_ & ~0x00000200);
        }
        result.pendingApprovals_ = pendingApprovals_;
      } else {
        result.pendingApprovals_ = pendingApprovalsBuilder_.build();
      }
      if (artifactsBuilder_ == null) {
        if (((bitField0_ & 0x00001000) != 0)) {
          artifacts_ = java.util.Collections.unmodifiableList(artifacts_);
          bitField0_ = (bitField0_ & ~0x00001000);
        }
        result.artifacts_ = artifacts_;
      } else {
        result.artifacts_ = artifactsBuilder_.build();
      }
      if (workspaceWriteBacksBuilder_ == null) {
        if (((bitField0_ & 0x00002000) != 0)) {
          workspaceWriteBacks_ = java.util.Collections.unmodifiableList(workspaceWriteBacks_);
          bitField0_ = (bitField0_ & ~0x00002000);
        }
        result.workspaceWriteBacks_ = workspaceWriteBacks_;
      } else {
        result.workspaceWriteBacks_ = workspaceWriteBacksBuilder_.build();
      }
      if (fileChangeSetsBuilder_ == null) {
        if (((bitField0_ & 0x00020000) != 0)) {
          fileChangeSets_ = java.util.Collections.unmodifiableList(fileChangeSets_);
          bitField0_ = (bitField0_ & ~0x00020000);
        }
        result.fileChangeSets_ = fileChangeSets_;
      } else {
        result.fileChangeSets_ = fileChangeSetsBuilder_.build();
      }
    }

    private void buildPartial0(ai.stigmer.agentic.agentexecution.v1.AgentExecutionStatus result) {
      int from_bitField0_ = bitField0_;
      int to_bitField0_ = 0;
      if (((from_bitField0_ & 0x00000001) != 0)) {
        result.audit_ = auditBuilder_ == null
            ? audit_
            : auditBuilder_.build();
        to_bitField0_ |= 0x00000001;
      }
      if (((from_bitField0_ & 0x00000004) != 0)) {
        result.phase_ = phase_;
      }
      if (((from_bitField0_ & 0x00000010) != 0)) {
        result.error_ = error_;
      }
      if (((from_bitField0_ & 0x00000020) != 0)) {
        result.startedAt_ = startedAt_;
      }
      if (((from_bitField0_ & 0x00000040) != 0)) {
        result.completedAt_ = completedAt_;
      }
      if (((from_bitField0_ & 0x00000080) != 0)) {
        result.todos_ = internalGetTodos().build(TodosDefaultEntryHolder.defaultEntry);
      }
      if (((from_bitField0_ & 0x00000100) != 0)) {
        result.callbackToken_ = callbackToken_;
      }
      if (((from_bitField0_ & 0x00000400) != 0)) {
        result.approvalEventStream_ = approvalEventStreamBuilder_ == null
            ? approvalEventStream_
            : approvalEventStreamBuilder_.build();
        to_bitField0_ |= 0x00000002;
      }
      if (((from_bitField0_ & 0x00000800) != 0)) {
        result.contextInfo_ = contextInfoBuilder_ == null
            ? contextInfo_
            : contextInfoBuilder_.build();
        to_bitField0_ |= 0x00000004;
      }
      if (((from_bitField0_ & 0x00004000) != 0)) {
        result.setupProgress_ = setupProgressBuilder_ == null
            ? setupProgress_
            : setupProgressBuilder_.build();
        to_bitField0_ |= 0x00000008;
      }
      if (((from_bitField0_ & 0x00008000) != 0)) {
        result.streamingUsage_ = streamingUsageBuilder_ == null
            ? streamingUsage_
            : streamingUsageBuilder_.build();
        to_bitField0_ |= 0x00000010;
      }
      if (((from_bitField0_ & 0x00010000) != 0)) {
        result.structuredOutput_ = structuredOutputBuilder_ == null
            ? structuredOutput_
            : structuredOutputBuilder_.build();
        to_bitField0_ |= 0x00000020;
      }
      if (((from_bitField0_ & 0x00040000) != 0)) {
        result.fileReviewEventStream_ = fileReviewEventStreamBuilder_ == null
            ? fileReviewEventStream_
            : fileReviewEventStreamBuilder_.build();
        to_bitField0_ |= 0x00000040;
      }
      if (((from_bitField0_ & 0x00080000) != 0)) {
        result.fileChangeProgress_ = fileChangeProgressBuilder_ == null
            ? fileChangeProgress_
            : fileChangeProgressBuilder_.build();
        to_bitField0_ |= 0x00000080;
      }
      if (((from_bitField0_ & 0x00100000) != 0)) {
        result.recalledMemoriesReport_ = recalledMemoriesReportBuilder_ == null
            ? recalledMemoriesReport_
            : recalledMemoriesReportBuilder_.build();
        to_bitField0_ |= 0x00000100;
      }
      result.bitField0_ |= to_bitField0_;
    }

    @java.lang.Override
    public Builder mergeFrom(com.google.protobuf.Message other) {
      if (other instanceof ai.stigmer.agentic.agentexecution.v1.AgentExecutionStatus) {
        return mergeFrom((ai.stigmer.agentic.agentexecution.v1.AgentExecutionStatus)other);
      } else {
        super.mergeFrom(other);
        return this;
      }
    }

    public Builder mergeFrom(ai.stigmer.agentic.agentexecution.v1.AgentExecutionStatus other) {
      if (other == ai.stigmer.agentic.agentexecution.v1.AgentExecutionStatus.getDefaultInstance()) return this;
      if (other.hasAudit()) {
        mergeAudit(other.getAudit());
      }
      if (messagesBuilder_ == null) {
        if (!other.messages_.isEmpty()) {
          if (messages_.isEmpty()) {
            messages_ = other.messages_;
            bitField0_ = (bitField0_ & ~0x00000002);
          } else {
            ensureMessagesIsMutable();
            messages_.addAll(other.messages_);
          }
          onChanged();
        }
      } else {
        if (!other.messages_.isEmpty()) {
          if (messagesBuilder_.isEmpty()) {
            messagesBuilder_.dispose();
            messagesBuilder_ = null;
            messages_ = other.messages_;
            bitField0_ = (bitField0_ & ~0x00000002);
            messagesBuilder_ = 
              com.google.protobuf.GeneratedMessage.alwaysUseFieldBuilders ?
                 internalGetMessagesFieldBuilder() : null;
          } else {
            messagesBuilder_.addAllMessages(other.messages_);
          }
        }
      }
      if (other.phase_ != 0) {
        setPhaseValue(other.getPhaseValue());
      }
      if (subAgentExecutionsBuilder_ == null) {
        if (!other.subAgentExecutions_.isEmpty()) {
          if (subAgentExecutions_.isEmpty()) {
            subAgentExecutions_ = other.subAgentExecutions_;
            bitField0_ = (bitField0_ & ~0x00000008);
          } else {
            ensureSubAgentExecutionsIsMutable();
            subAgentExecutions_.addAll(other.subAgentExecutions_);
          }
          onChanged();
        }
      } else {
        if (!other.subAgentExecutions_.isEmpty()) {
          if (subAgentExecutionsBuilder_.isEmpty()) {
            subAgentExecutionsBuilder_.dispose();
            subAgentExecutionsBuilder_ = null;
            subAgentExecutions_ = other.subAgentExecutions_;
            bitField0_ = (bitField0_ & ~0x00000008);
            subAgentExecutionsBuilder_ = 
              com.google.protobuf.GeneratedMessage.alwaysUseFieldBuilders ?
                 internalGetSubAgentExecutionsFieldBuilder() : null;
          } else {
            subAgentExecutionsBuilder_.addAllMessages(other.subAgentExecutions_);
          }
        }
      }
      if (!other.getError().isEmpty()) {
        error_ = other.error_;
        bitField0_ |= 0x00000010;
        onChanged();
      }
      if (!other.getStartedAt().isEmpty()) {
        startedAt_ = other.startedAt_;
        bitField0_ |= 0x00000020;
        onChanged();
      }
      if (!other.getCompletedAt().isEmpty()) {
        completedAt_ = other.completedAt_;
        bitField0_ |= 0x00000040;
        onChanged();
      }
      internalGetMutableTodos().mergeFrom(
          other.internalGetTodos());
      bitField0_ |= 0x00000080;
      if (!other.getCallbackToken().isEmpty()) {
        setCallbackToken(other.getCallbackToken());
      }
      if (pendingApprovalsBuilder_ == null) {
        if (!other.pendingApprovals_.isEmpty()) {
          if (pendingApprovals_.isEmpty()) {
            pendingApprovals_ = other.pendingApprovals_;
            bitField0_ = (bitField0_ & ~0x00000200);
          } else {
            ensurePendingApprovalsIsMutable();
            pendingApprovals_.addAll(other.pendingApprovals_);
          }
          onChanged();
        }
      } else {
        if (!other.pendingApprovals_.isEmpty()) {
          if (pendingApprovalsBuilder_.isEmpty()) {
            pendingApprovalsBuilder_.dispose();
            pendingApprovalsBuilder_ = null;
            pendingApprovals_ = other.pendingApprovals_;
            bitField0_ = (bitField0_ & ~0x00000200);
            pendingApprovalsBuilder_ = 
              com.google.protobuf.GeneratedMessage.alwaysUseFieldBuilders ?
                 internalGetPendingApprovalsFieldBuilder() : null;
          } else {
            pendingApprovalsBuilder_.addAllMessages(other.pendingApprovals_);
          }
        }
      }
      if (other.hasApprovalEventStream()) {
        mergeApprovalEventStream(other.getApprovalEventStream());
      }
      if (other.hasContextInfo()) {
        mergeContextInfo(other.getContextInfo());
      }
      if (artifactsBuilder_ == null) {
        if (!other.artifacts_.isEmpty()) {
          if (artifacts_.isEmpty()) {
            artifacts_ = other.artifacts_;
            bitField0_ = (bitField0_ & ~0x00001000);
          } else {
            ensureArtifactsIsMutable();
            artifacts_.addAll(other.artifacts_);
          }
          onChanged();
        }
      } else {
        if (!other.artifacts_.isEmpty()) {
          if (artifactsBuilder_.isEmpty()) {
            artifactsBuilder_.dispose();
            artifactsBuilder_ = null;
            artifacts_ = other.artifacts_;
            bitField0_ = (bitField0_ & ~0x00001000);
            artifactsBuilder_ = 
              com.google.protobuf.GeneratedMessage.alwaysUseFieldBuilders ?
                 internalGetArtifactsFieldBuilder() : null;
          } else {
            artifactsBuilder_.addAllMessages(other.artifacts_);
          }
        }
      }
      if (workspaceWriteBacksBuilder_ == null) {
        if (!other.workspaceWriteBacks_.isEmpty()) {
          if (workspaceWriteBacks_.isEmpty()) {
            workspaceWriteBacks_ = other.workspaceWriteBacks_;
            bitField0_ = (bitField0_ & ~0x00002000);
          } else {
            ensureWorkspaceWriteBacksIsMutable();
            workspaceWriteBacks_.addAll(other.workspaceWriteBacks_);
          }
          onChanged();
        }
      } else {
        if (!other.workspaceWriteBacks_.isEmpty()) {
          if (workspaceWriteBacksBuilder_.isEmpty()) {
            workspaceWriteBacksBuilder_.dispose();
            workspaceWriteBacksBuilder_ = null;
            workspaceWriteBacks_ = other.workspaceWriteBacks_;
            bitField0_ = (bitField0_ & ~0x00002000);
            workspaceWriteBacksBuilder_ = 
              com.google.protobuf.GeneratedMessage.alwaysUseFieldBuilders ?
                 internalGetWorkspaceWriteBacksFieldBuilder() : null;
          } else {
            workspaceWriteBacksBuilder_.addAllMessages(other.workspaceWriteBacks_);
          }
        }
      }
      if (other.hasSetupProgress()) {
        mergeSetupProgress(other.getSetupProgress());
      }
      if (other.hasStreamingUsage()) {
        mergeStreamingUsage(other.getStreamingUsage());
      }
      if (other.hasStructuredOutput()) {
        mergeStructuredOutput(other.getStructuredOutput());
      }
      if (fileChangeSetsBuilder_ == null) {
        if (!other.fileChangeSets_.isEmpty()) {
          if (fileChangeSets_.isEmpty()) {
            fileChangeSets_ = other.fileChangeSets_;
            bitField0_ = (bitField0_ & ~0x00020000);
          } else {
            ensureFileChangeSetsIsMutable();
            fileChangeSets_.addAll(other.fileChangeSets_);
          }
          onChanged();
        }
      } else {
        if (!other.fileChangeSets_.isEmpty()) {
          if (fileChangeSetsBuilder_.isEmpty()) {
            fileChangeSetsBuilder_.dispose();
            fileChangeSetsBuilder_ = null;
            fileChangeSets_ = other.fileChangeSets_;
            bitField0_ = (bitField0_ & ~0x00020000);
            fileChangeSetsBuilder_ = 
              com.google.protobuf.GeneratedMessage.alwaysUseFieldBuilders ?
                 internalGetFileChangeSetsFieldBuilder() : null;
          } else {
            fileChangeSetsBuilder_.addAllMessages(other.fileChangeSets_);
          }
        }
      }
      if (other.hasFileReviewEventStream()) {
        mergeFileReviewEventStream(other.getFileReviewEventStream());
      }
      if (other.hasFileChangeProgress()) {
        mergeFileChangeProgress(other.getFileChangeProgress());
      }
      if (other.hasRecalledMemoriesReport()) {
        mergeRecalledMemoriesReport(other.getRecalledMemoriesReport());
      }
      this.mergeUnknownFields(other.getUnknownFields());
      onChanged();
      return this;
    }

    @java.lang.Override
    public final boolean isInitialized() {
      return true;
    }

    @java.lang.Override
    public Builder mergeFrom(
        com.google.protobuf.CodedInputStream input,
        com.google.protobuf.ExtensionRegistryLite extensionRegistry)
        throws java.io.IOException {
      if (extensionRegistry == null) {
        throw new java.lang.NullPointerException();
      }
      try {
        boolean done = false;
        while (!done) {
          int tag = input.readTag();
          switch (tag) {
            case 0:
              done = true;
              break;
            case 10: {
              ai.stigmer.agentic.agentexecution.v1.AgentMessage m =
                  input.readMessage(
                      ai.stigmer.agentic.agentexecution.v1.AgentMessage.parser(),
                      extensionRegistry);
              if (messagesBuilder_ == null) {
                ensureMessagesIsMutable();
                messages_.add(m);
              } else {
                messagesBuilder_.addMessage(m);
              }
              break;
            } // case 10
            case 16: {
              phase_ = input.readEnum();
              bitField0_ |= 0x00000004;
              break;
            } // case 16
            case 34: {
              ai.stigmer.agentic.agentexecution.v1.SubAgentExecution m =
                  input.readMessage(
                      ai.stigmer.agentic.agentexecution.v1.SubAgentExecution.parser(),
                      extensionRegistry);
              if (subAgentExecutionsBuilder_ == null) {
                ensureSubAgentExecutionsIsMutable();
                subAgentExecutions_.add(m);
              } else {
                subAgentExecutionsBuilder_.addMessage(m);
              }
              break;
            } // case 34
            case 50: {
              error_ = input.readStringRequireUtf8();
              bitField0_ |= 0x00000010;
              break;
            } // case 50
            case 58: {
              startedAt_ = input.readStringRequireUtf8();
              bitField0_ |= 0x00000020;
              break;
            } // case 58
            case 66: {
              completedAt_ = input.readStringRequireUtf8();
              bitField0_ |= 0x00000040;
              break;
            } // case 66
            case 74: {
              com.google.protobuf.MapEntry<java.lang.String, ai.stigmer.agentic.agentexecution.v1.TodoItem>
              todos__ = input.readMessage(
                  TodosDefaultEntryHolder.defaultEntry.getParserForType(), extensionRegistry);
              internalGetMutableTodos().ensureBuilderMap().put(
                  todos__.getKey(), todos__.getValue());
              bitField0_ |= 0x00000080;
              break;
            } // case 74
            case 82: {
              callbackToken_ = input.readBytes();
              bitField0_ |= 0x00000100;
              break;
            } // case 82
            case 114: {
              input.readMessage(
                  internalGetContextInfoFieldBuilder().getBuilder(),
                  extensionRegistry);
              bitField0_ |= 0x00000800;
              break;
            } // case 114
            case 122: {
              ai.stigmer.agentic.agentexecution.v1.ExecutionArtifact m =
                  input.readMessage(
                      ai.stigmer.agentic.agentexecution.v1.ExecutionArtifact.parser(),
                      extensionRegistry);
              if (artifactsBuilder_ == null) {
                ensureArtifactsIsMutable();
                artifacts_.add(m);
              } else {
                artifactsBuilder_.addMessage(m);
              }
              break;
            } // case 122
            case 130: {
              ai.stigmer.agentic.agentexecution.v1.PendingApproval m =
                  input.readMessage(
                      ai.stigmer.agentic.agentexecution.v1.PendingApproval.parser(),
                      extensionRegistry);
              if (pendingApprovalsBuilder_ == null) {
                ensurePendingApprovalsIsMutable();
                pendingApprovals_.add(m);
              } else {
                pendingApprovalsBuilder_.addMessage(m);
              }
              break;
            } // case 130
            case 138: {
              ai.stigmer.agentic.agentexecution.v1.WorkspaceWriteBack m =
                  input.readMessage(
                      ai.stigmer.agentic.agentexecution.v1.WorkspaceWriteBack.parser(),
                      extensionRegistry);
              if (workspaceWriteBacksBuilder_ == null) {
                ensureWorkspaceWriteBacksIsMutable();
                workspaceWriteBacks_.add(m);
              } else {
                workspaceWriteBacksBuilder_.addMessage(m);
              }
              break;
            } // case 138
            case 146: {
              input.readMessage(
                  internalGetSetupProgressFieldBuilder().getBuilder(),
                  extensionRegistry);
              bitField0_ |= 0x00004000;
              break;
            } // case 146
            case 162: {
              input.readMessage(
                  internalGetStreamingUsageFieldBuilder().getBuilder(),
                  extensionRegistry);
              bitField0_ |= 0x00008000;
              break;
            } // case 162
            case 170: {
              input.readMessage(
                  internalGetStructuredOutputFieldBuilder().getBuilder(),
                  extensionRegistry);
              bitField0_ |= 0x00010000;
              break;
            } // case 170
            case 178: {
              input.readMessage(
                  internalGetApprovalEventStreamFieldBuilder().getBuilder(),
                  extensionRegistry);
              bitField0_ |= 0x00000400;
              break;
            } // case 178
            case 186: {
              ai.stigmer.agentic.agentexecution.v1.FileChangeSet m =
                  input.readMessage(
                      ai.stigmer.agentic.agentexecution.v1.FileChangeSet.parser(),
                      extensionRegistry);
              if (fileChangeSetsBuilder_ == null) {
                ensureFileChangeSetsIsMutable();
                fileChangeSets_.add(m);
              } else {
                fileChangeSetsBuilder_.addMessage(m);
              }
              break;
            } // case 186
            case 194: {
              input.readMessage(
                  internalGetFileReviewEventStreamFieldBuilder().getBuilder(),
                  extensionRegistry);
              bitField0_ |= 0x00040000;
              break;
            } // case 194
            case 202: {
              input.readMessage(
                  internalGetFileChangeProgressFieldBuilder().getBuilder(),
                  extensionRegistry);
              bitField0_ |= 0x00080000;
              break;
            } // case 202
            case 210: {
              input.readMessage(
                  internalGetRecalledMemoriesReportFieldBuilder().getBuilder(),
                  extensionRegistry);
              bitField0_ |= 0x00100000;
              break;
            } // case 210
            case 794: {
              input.readMessage(
                  internalGetAuditFieldBuilder().getBuilder(),
                  extensionRegistry);
              bitField0_ |= 0x00000001;
              break;
            } // case 794
            default: {
              if (!super.parseUnknownField(input, extensionRegistry, tag)) {
                done = true; // was an endgroup tag
              }
              break;
            } // default:
          } // switch (tag)
        } // while (!done)
      } catch (com.google.protobuf.InvalidProtocolBufferException e) {
        throw e.unwrapIOException();
      } finally {
        onChanged();
      } // finally
      return this;
    }
    private int bitField0_;

    private ai.stigmer.commons.apiresource.ApiResourceAudit audit_;
    private com.google.protobuf.SingleFieldBuilder<
        ai.stigmer.commons.apiresource.ApiResourceAudit, ai.stigmer.commons.apiresource.ApiResourceAudit.Builder, ai.stigmer.commons.apiresource.ApiResourceAuditOrBuilder> auditBuilder_;
    /**
     * <pre>
     * Standard audit information (created_at, updated_at, created_by, etc.)
     * </pre>
     *
     * <code>.ai.stigmer.commons.apiresource.ApiResourceAudit audit = 99 [json_name = "audit"];</code>
     * @return Whether the audit field is set.
     */
    public boolean hasAudit() {
      return ((bitField0_ & 0x00000001) != 0);
    }
    /**
     * <pre>
     * Standard audit information (created_at, updated_at, created_by, etc.)
     * </pre>
     *
     * <code>.ai.stigmer.commons.apiresource.ApiResourceAudit audit = 99 [json_name = "audit"];</code>
     * @return The audit.
     */
    public ai.stigmer.commons.apiresource.ApiResourceAudit getAudit() {
      if (auditBuilder_ == null) {
        return audit_ == null ? ai.stigmer.commons.apiresource.ApiResourceAudit.getDefaultInstance() : audit_;
      } else {
        return auditBuilder_.getMessage();
      }
    }
    /**
     * <pre>
     * Standard audit information (created_at, updated_at, created_by, etc.)
     * </pre>
     *
     * <code>.ai.stigmer.commons.apiresource.ApiResourceAudit audit = 99 [json_name = "audit"];</code>
     */
    public Builder setAudit(ai.stigmer.commons.apiresource.ApiResourceAudit value) {
      if (auditBuilder_ == null) {
        if (value == null) {
          throw new NullPointerException();
        }
        audit_ = value;
      } else {
        auditBuilder_.setMessage(value);
      }
      bitField0_ |= 0x00000001;
      onChanged();
      return this;
    }
    /**
     * <pre>
     * Standard audit information (created_at, updated_at, created_by, etc.)
     * </pre>
     *
     * <code>.ai.stigmer.commons.apiresource.ApiResourceAudit audit = 99 [json_name = "audit"];</code>
     */
    public Builder setAudit(
        ai.stigmer.commons.apiresource.ApiResourceAudit.Builder builderForValue) {
      if (auditBuilder_ == null) {
        audit_ = builderForValue.build();
      } else {
        auditBuilder_.setMessage(builderForValue.build());
      }
      bitField0_ |= 0x00000001;
      onChanged();
      return this;
    }
    /**
     * <pre>
     * Standard audit information (created_at, updated_at, created_by, etc.)
     * </pre>
     *
     * <code>.ai.stigmer.commons.apiresource.ApiResourceAudit audit = 99 [json_name = "audit"];</code>
     */
    public Builder mergeAudit(ai.stigmer.commons.apiresource.ApiResourceAudit value) {
      if (auditBuilder_ == null) {
        if (((bitField0_ & 0x00000001) != 0) &&
          audit_ != null &&
          audit_ != ai.stigmer.commons.apiresource.ApiResourceAudit.getDefaultInstance()) {
          getAuditBuilder().mergeFrom(value);
        } else {
          audit_ = value;
        }
      } else {
        auditBuilder_.mergeFrom(value);
      }
      if (audit_ != null) {
        bitField0_ |= 0x00000001;
        onChanged();
      }
      return this;
    }
    /**
     * <pre>
     * Standard audit information (created_at, updated_at, created_by, etc.)
     * </pre>
     *
     * <code>.ai.stigmer.commons.apiresource.ApiResourceAudit audit = 99 [json_name = "audit"];</code>
     */
    public Builder clearAudit() {
      bitField0_ = (bitField0_ & ~0x00000001);
      audit_ = null;
      if (auditBuilder_ != null) {
        auditBuilder_.dispose();
        auditBuilder_ = null;
      }
      onChanged();
      return this;
    }
    /**
     * <pre>
     * Standard audit information (created_at, updated_at, created_by, etc.)
     * </pre>
     *
     * <code>.ai.stigmer.commons.apiresource.ApiResourceAudit audit = 99 [json_name = "audit"];</code>
     */
    public ai.stigmer.commons.apiresource.ApiResourceAudit.Builder getAuditBuilder() {
      bitField0_ |= 0x00000001;
      onChanged();
      return internalGetAuditFieldBuilder().getBuilder();
    }
    /**
     * <pre>
     * Standard audit information (created_at, updated_at, created_by, etc.)
     * </pre>
     *
     * <code>.ai.stigmer.commons.apiresource.ApiResourceAudit audit = 99 [json_name = "audit"];</code>
     */
    public ai.stigmer.commons.apiresource.ApiResourceAuditOrBuilder getAuditOrBuilder() {
      if (auditBuilder_ != null) {
        return auditBuilder_.getMessageOrBuilder();
      } else {
        return audit_ == null ?
            ai.stigmer.commons.apiresource.ApiResourceAudit.getDefaultInstance() : audit_;
      }
    }
    /**
     * <pre>
     * Standard audit information (created_at, updated_at, created_by, etc.)
     * </pre>
     *
     * <code>.ai.stigmer.commons.apiresource.ApiResourceAudit audit = 99 [json_name = "audit"];</code>
     */
    private com.google.protobuf.SingleFieldBuilder<
        ai.stigmer.commons.apiresource.ApiResourceAudit, ai.stigmer.commons.apiresource.ApiResourceAudit.Builder, ai.stigmer.commons.apiresource.ApiResourceAuditOrBuilder> 
        internalGetAuditFieldBuilder() {
      if (auditBuilder_ == null) {
        auditBuilder_ = new com.google.protobuf.SingleFieldBuilder<
            ai.stigmer.commons.apiresource.ApiResourceAudit, ai.stigmer.commons.apiresource.ApiResourceAudit.Builder, ai.stigmer.commons.apiresource.ApiResourceAuditOrBuilder>(
                getAudit(),
                getParentForChildren(),
                isClean());
        audit_ = null;
      }
      return auditBuilder_;
    }

    private java.util.List<ai.stigmer.agentic.agentexecution.v1.AgentMessage> messages_ =
      java.util.Collections.emptyList();
    private void ensureMessagesIsMutable() {
      if (!((bitField0_ & 0x00000002) != 0)) {
        messages_ = new java.util.ArrayList<ai.stigmer.agentic.agentexecution.v1.AgentMessage>(messages_);
        bitField0_ |= 0x00000002;
       }
    }

    private com.google.protobuf.RepeatedFieldBuilder<
        ai.stigmer.agentic.agentexecution.v1.AgentMessage, ai.stigmer.agentic.agentexecution.v1.AgentMessage.Builder, ai.stigmer.agentic.agentexecution.v1.AgentMessageOrBuilder> messagesBuilder_;

    /**
     * <pre>
     * Sequential stream of execution events: AI messages, tool calls, and system notifications.
     * Ordered chronologically to reconstruct the complete execution flow.
     * Each message captures what happened during execution (agent responses, tool invocations, progress updates).
     * </pre>
     *
     * <code>repeated .ai.stigmer.agentic.agentexecution.v1.AgentMessage messages = 1 [json_name = "messages"];</code>
     */
    public java.util.List<ai.stigmer.agentic.agentexecution.v1.AgentMessage> getMessagesList() {
      if (messagesBuilder_ == null) {
        return java.util.Collections.unmodifiableList(messages_);
      } else {
        return messagesBuilder_.getMessageList();
      }
    }
    /**
     * <pre>
     * Sequential stream of execution events: AI messages, tool calls, and system notifications.
     * Ordered chronologically to reconstruct the complete execution flow.
     * Each message captures what happened during execution (agent responses, tool invocations, progress updates).
     * </pre>
     *
     * <code>repeated .ai.stigmer.agentic.agentexecution.v1.AgentMessage messages = 1 [json_name = "messages"];</code>
     */
    public int getMessagesCount() {
      if (messagesBuilder_ == null) {
        return messages_.size();
      } else {
        return messagesBuilder_.getCount();
      }
    }
    /**
     * <pre>
     * Sequential stream of execution events: AI messages, tool calls, and system notifications.
     * Ordered chronologically to reconstruct the complete execution flow.
     * Each message captures what happened during execution (agent responses, tool invocations, progress updates).
     * </pre>
     *
     * <code>repeated .ai.stigmer.agentic.agentexecution.v1.AgentMessage messages = 1 [json_name = "messages"];</code>
     */
    public ai.stigmer.agentic.agentexecution.v1.AgentMessage getMessages(int index) {
      if (messagesBuilder_ == null) {
        return messages_.get(index);
      } else {
        return messagesBuilder_.getMessage(index);
      }
    }
    /**
     * <pre>
     * Sequential stream of execution events: AI messages, tool calls, and system notifications.
     * Ordered chronologically to reconstruct the complete execution flow.
     * Each message captures what happened during execution (agent responses, tool invocations, progress updates).
     * </pre>
     *
     * <code>repeated .ai.stigmer.agentic.agentexecution.v1.AgentMessage messages = 1 [json_name = "messages"];</code>
     */
    public Builder setMessages(
        int index, ai.stigmer.agentic.agentexecution.v1.AgentMessage value) {
      if (messagesBuilder_ == null) {
        if (value == null) {
          throw new NullPointerException();
        }
        ensureMessagesIsMutable();
        messages_.set(index, value);
        onChanged();
      } else {
        messagesBuilder_.setMessage(index, value);
      }
      return this;
    }
    /**
     * <pre>
     * Sequential stream of execution events: AI messages, tool calls, and system notifications.
     * Ordered chronologically to reconstruct the complete execution flow.
     * Each message captures what happened during execution (agent responses, tool invocations, progress updates).
     * </pre>
     *
     * <code>repeated .ai.stigmer.agentic.agentexecution.v1.AgentMessage messages = 1 [json_name = "messages"];</code>
     */
    public Builder setMessages(
        int index, ai.stigmer.agentic.agentexecution.v1.AgentMessage.Builder builderForValue) {
      if (messagesBuilder_ == null) {
        ensureMessagesIsMutable();
        messages_.set(index, builderForValue.build());
        onChanged();
      } else {
        messagesBuilder_.setMessage(index, builderForValue.build());
      }
      return this;
    }
    /**
     * <pre>
     * Sequential stream of execution events: AI messages, tool calls, and system notifications.
     * Ordered chronologically to reconstruct the complete execution flow.
     * Each message captures what happened during execution (agent responses, tool invocations, progress updates).
     * </pre>
     *
     * <code>repeated .ai.stigmer.agentic.agentexecution.v1.AgentMessage messages = 1 [json_name = "messages"];</code>
     */
    public Builder addMessages(ai.stigmer.agentic.agentexecution.v1.AgentMessage value) {
      if (messagesBuilder_ == null) {
        if (value == null) {
          throw new NullPointerException();
        }
        ensureMessagesIsMutable();
        messages_.add(value);
        onChanged();
      } else {
        messagesBuilder_.addMessage(value);
      }
      return this;
    }
    /**
     * <pre>
     * Sequential stream of execution events: AI messages, tool calls, and system notifications.
     * Ordered chronologically to reconstruct the complete execution flow.
     * Each message captures what happened during execution (agent responses, tool invocations, progress updates).
     * </pre>
     *
     * <code>repeated .ai.stigmer.agentic.agentexecution.v1.AgentMessage messages = 1 [json_name = "messages"];</code>
     */
    public Builder addMessages(
        int index, ai.stigmer.agentic.agentexecution.v1.AgentMessage value) {
      if (messagesBuilder_ == null) {
        if (value == null) {
          throw new NullPointerException();
        }
        ensureMessagesIsMutable();
        messages_.add(index, value);
        onChanged();
      } else {
        messagesBuilder_.addMessage(index, value);
      }
      return this;
    }
    /**
     * <pre>
     * Sequential stream of execution events: AI messages, tool calls, and system notifications.
     * Ordered chronologically to reconstruct the complete execution flow.
     * Each message captures what happened during execution (agent responses, tool invocations, progress updates).
     * </pre>
     *
     * <code>repeated .ai.stigmer.agentic.agentexecution.v1.AgentMessage messages = 1 [json_name = "messages"];</code>
     */
    public Builder addMessages(
        ai.stigmer.agentic.agentexecution.v1.AgentMessage.Builder builderForValue) {
      if (messagesBuilder_ == null) {
        ensureMessagesIsMutable();
        messages_.add(builderForValue.build());
        onChanged();
      } else {
        messagesBuilder_.addMessage(builderForValue.build());
      }
      return this;
    }
    /**
     * <pre>
     * Sequential stream of execution events: AI messages, tool calls, and system notifications.
     * Ordered chronologically to reconstruct the complete execution flow.
     * Each message captures what happened during execution (agent responses, tool invocations, progress updates).
     * </pre>
     *
     * <code>repeated .ai.stigmer.agentic.agentexecution.v1.AgentMessage messages = 1 [json_name = "messages"];</code>
     */
    public Builder addMessages(
        int index, ai.stigmer.agentic.agentexecution.v1.AgentMessage.Builder builderForValue) {
      if (messagesBuilder_ == null) {
        ensureMessagesIsMutable();
        messages_.add(index, builderForValue.build());
        onChanged();
      } else {
        messagesBuilder_.addMessage(index, builderForValue.build());
      }
      return this;
    }
    /**
     * <pre>
     * Sequential stream of execution events: AI messages, tool calls, and system notifications.
     * Ordered chronologically to reconstruct the complete execution flow.
     * Each message captures what happened during execution (agent responses, tool invocations, progress updates).
     * </pre>
     *
     * <code>repeated .ai.stigmer.agentic.agentexecution.v1.AgentMessage messages = 1 [json_name = "messages"];</code>
     */
    public Builder addAllMessages(
        java.lang.Iterable<? extends ai.stigmer.agentic.agentexecution.v1.AgentMessage> values) {
      if (messagesBuilder_ == null) {
        ensureMessagesIsMutable();
        com.google.protobuf.AbstractMessageLite.Builder.addAll(
            values, messages_);
        onChanged();
      } else {
        messagesBuilder_.addAllMessages(values);
      }
      return this;
    }
    /**
     * <pre>
     * Sequential stream of execution events: AI messages, tool calls, and system notifications.
     * Ordered chronologically to reconstruct the complete execution flow.
     * Each message captures what happened during execution (agent responses, tool invocations, progress updates).
     * </pre>
     *
     * <code>repeated .ai.stigmer.agentic.agentexecution.v1.AgentMessage messages = 1 [json_name = "messages"];</code>
     */
    public Builder clearMessages() {
      if (messagesBuilder_ == null) {
        messages_ = java.util.Collections.emptyList();
        bitField0_ = (bitField0_ & ~0x00000002);
        onChanged();
      } else {
        messagesBuilder_.clear();
      }
      return this;
    }
    /**
     * <pre>
     * Sequential stream of execution events: AI messages, tool calls, and system notifications.
     * Ordered chronologically to reconstruct the complete execution flow.
     * Each message captures what happened during execution (agent responses, tool invocations, progress updates).
     * </pre>
     *
     * <code>repeated .ai.stigmer.agentic.agentexecution.v1.AgentMessage messages = 1 [json_name = "messages"];</code>
     */
    public Builder removeMessages(int index) {
      if (messagesBuilder_ == null) {
        ensureMessagesIsMutable();
        messages_.remove(index);
        onChanged();
      } else {
        messagesBuilder_.remove(index);
      }
      return this;
    }
    /**
     * <pre>
     * Sequential stream of execution events: AI messages, tool calls, and system notifications.
     * Ordered chronologically to reconstruct the complete execution flow.
     * Each message captures what happened during execution (agent responses, tool invocations, progress updates).
     * </pre>
     *
     * <code>repeated .ai.stigmer.agentic.agentexecution.v1.AgentMessage messages = 1 [json_name = "messages"];</code>
     */
    public ai.stigmer.agentic.agentexecution.v1.AgentMessage.Builder getMessagesBuilder(
        int index) {
      return internalGetMessagesFieldBuilder().getBuilder(index);
    }
    /**
     * <pre>
     * Sequential stream of execution events: AI messages, tool calls, and system notifications.
     * Ordered chronologically to reconstruct the complete execution flow.
     * Each message captures what happened during execution (agent responses, tool invocations, progress updates).
     * </pre>
     *
     * <code>repeated .ai.stigmer.agentic.agentexecution.v1.AgentMessage messages = 1 [json_name = "messages"];</code>
     */
    public ai.stigmer.agentic.agentexecution.v1.AgentMessageOrBuilder getMessagesOrBuilder(
        int index) {
      if (messagesBuilder_ == null) {
        return messages_.get(index);  } else {
        return messagesBuilder_.getMessageOrBuilder(index);
      }
    }
    /**
     * <pre>
     * Sequential stream of execution events: AI messages, tool calls, and system notifications.
     * Ordered chronologically to reconstruct the complete execution flow.
     * Each message captures what happened during execution (agent responses, tool invocations, progress updates).
     * </pre>
     *
     * <code>repeated .ai.stigmer.agentic.agentexecution.v1.AgentMessage messages = 1 [json_name = "messages"];</code>
     */
    public java.util.List<? extends ai.stigmer.agentic.agentexecution.v1.AgentMessageOrBuilder> 
         getMessagesOrBuilderList() {
      if (messagesBuilder_ != null) {
        return messagesBuilder_.getMessageOrBuilderList();
      } else {
        return java.util.Collections.unmodifiableList(messages_);
      }
    }
    /**
     * <pre>
     * Sequential stream of execution events: AI messages, tool calls, and system notifications.
     * Ordered chronologically to reconstruct the complete execution flow.
     * Each message captures what happened during execution (agent responses, tool invocations, progress updates).
     * </pre>
     *
     * <code>repeated .ai.stigmer.agentic.agentexecution.v1.AgentMessage messages = 1 [json_name = "messages"];</code>
     */
    public ai.stigmer.agentic.agentexecution.v1.AgentMessage.Builder addMessagesBuilder() {
      return internalGetMessagesFieldBuilder().addBuilder(
          ai.stigmer.agentic.agentexecution.v1.AgentMessage.getDefaultInstance());
    }
    /**
     * <pre>
     * Sequential stream of execution events: AI messages, tool calls, and system notifications.
     * Ordered chronologically to reconstruct the complete execution flow.
     * Each message captures what happened during execution (agent responses, tool invocations, progress updates).
     * </pre>
     *
     * <code>repeated .ai.stigmer.agentic.agentexecution.v1.AgentMessage messages = 1 [json_name = "messages"];</code>
     */
    public ai.stigmer.agentic.agentexecution.v1.AgentMessage.Builder addMessagesBuilder(
        int index) {
      return internalGetMessagesFieldBuilder().addBuilder(
          index, ai.stigmer.agentic.agentexecution.v1.AgentMessage.getDefaultInstance());
    }
    /**
     * <pre>
     * Sequential stream of execution events: AI messages, tool calls, and system notifications.
     * Ordered chronologically to reconstruct the complete execution flow.
     * Each message captures what happened during execution (agent responses, tool invocations, progress updates).
     * </pre>
     *
     * <code>repeated .ai.stigmer.agentic.agentexecution.v1.AgentMessage messages = 1 [json_name = "messages"];</code>
     */
    public java.util.List<ai.stigmer.agentic.agentexecution.v1.AgentMessage.Builder> 
         getMessagesBuilderList() {
      return internalGetMessagesFieldBuilder().getBuilderList();
    }
    private com.google.protobuf.RepeatedFieldBuilder<
        ai.stigmer.agentic.agentexecution.v1.AgentMessage, ai.stigmer.agentic.agentexecution.v1.AgentMessage.Builder, ai.stigmer.agentic.agentexecution.v1.AgentMessageOrBuilder> 
        internalGetMessagesFieldBuilder() {
      if (messagesBuilder_ == null) {
        messagesBuilder_ = new com.google.protobuf.RepeatedFieldBuilder<
            ai.stigmer.agentic.agentexecution.v1.AgentMessage, ai.stigmer.agentic.agentexecution.v1.AgentMessage.Builder, ai.stigmer.agentic.agentexecution.v1.AgentMessageOrBuilder>(
                messages_,
                ((bitField0_ & 0x00000002) != 0),
                getParentForChildren(),
                isClean());
        messages_ = null;
      }
      return messagesBuilder_;
    }

    private int phase_ = 0;
    /**
     * <pre>
     * Current execution lifecycle phase.
     * Tracks the execution state from creation (PENDING) through active processing (IN_PROGRESS)
     * to terminal states (COMPLETED/FAILED/CANCELLED).
     * </pre>
     *
     * <code>.ai.stigmer.agentic.agentexecution.v1.ExecutionPhase phase = 2 [json_name = "phase", (.buf.validate.field) = { ... }</code>
     * @return The enum numeric value on the wire for phase.
     */
    @java.lang.Override public int getPhaseValue() {
      return phase_;
    }
    /**
     * <pre>
     * Current execution lifecycle phase.
     * Tracks the execution state from creation (PENDING) through active processing (IN_PROGRESS)
     * to terminal states (COMPLETED/FAILED/CANCELLED).
     * </pre>
     *
     * <code>.ai.stigmer.agentic.agentexecution.v1.ExecutionPhase phase = 2 [json_name = "phase", (.buf.validate.field) = { ... }</code>
     * @param value The enum numeric value on the wire for phase to set.
     * @throws IllegalArgumentException if UNRECOGNIZED is provided.
     * @return This builder for chaining.
     */
    public Builder setPhaseValue(int value) {
      phase_ = value;
      bitField0_ |= 0x00000004;
      onChanged();
      return this;
    }
    /**
     * <pre>
     * Current execution lifecycle phase.
     * Tracks the execution state from creation (PENDING) through active processing (IN_PROGRESS)
     * to terminal states (COMPLETED/FAILED/CANCELLED).
     * </pre>
     *
     * <code>.ai.stigmer.agentic.agentexecution.v1.ExecutionPhase phase = 2 [json_name = "phase", (.buf.validate.field) = { ... }</code>
     * @return The phase.
     */
    @java.lang.Override
    public ai.stigmer.agentic.agentexecution.v1.ExecutionPhase getPhase() {
      ai.stigmer.agentic.agentexecution.v1.ExecutionPhase result = ai.stigmer.agentic.agentexecution.v1.ExecutionPhase.forNumber(phase_);
      return result == null ? ai.stigmer.agentic.agentexecution.v1.ExecutionPhase.UNRECOGNIZED : result;
    }
    /**
     * <pre>
     * Current execution lifecycle phase.
     * Tracks the execution state from creation (PENDING) through active processing (IN_PROGRESS)
     * to terminal states (COMPLETED/FAILED/CANCELLED).
     * </pre>
     *
     * <code>.ai.stigmer.agentic.agentexecution.v1.ExecutionPhase phase = 2 [json_name = "phase", (.buf.validate.field) = { ... }</code>
     * @param value The phase to set.
     * @return This builder for chaining.
     */
    public Builder setPhase(ai.stigmer.agentic.agentexecution.v1.ExecutionPhase value) {
      if (value == null) { throw new NullPointerException(); }
      bitField0_ |= 0x00000004;
      phase_ = value.getNumber();
      onChanged();
      return this;
    }
    /**
     * <pre>
     * Current execution lifecycle phase.
     * Tracks the execution state from creation (PENDING) through active processing (IN_PROGRESS)
     * to terminal states (COMPLETED/FAILED/CANCELLED).
     * </pre>
     *
     * <code>.ai.stigmer.agentic.agentexecution.v1.ExecutionPhase phase = 2 [json_name = "phase", (.buf.validate.field) = { ... }</code>
     * @return This builder for chaining.
     */
    public Builder clearPhase() {
      bitField0_ = (bitField0_ & ~0x00000004);
      phase_ = 0;
      onChanged();
      return this;
    }

    private java.util.List<ai.stigmer.agentic.agentexecution.v1.SubAgentExecution> subAgentExecutions_ =
      java.util.Collections.emptyList();
    private void ensureSubAgentExecutionsIsMutable() {
      if (!((bitField0_ & 0x00000008) != 0)) {
        subAgentExecutions_ = new java.util.ArrayList<ai.stigmer.agentic.agentexecution.v1.SubAgentExecution>(subAgentExecutions_);
        bitField0_ |= 0x00000008;
       }
    }

    private com.google.protobuf.RepeatedFieldBuilder<
        ai.stigmer.agentic.agentexecution.v1.SubAgentExecution, ai.stigmer.agentic.agentexecution.v1.SubAgentExecution.Builder, ai.stigmer.agentic.agentexecution.v1.SubAgentExecutionOrBuilder> subAgentExecutionsBuilder_;

    /**
     * <pre>
     * Sub-agent executions invoked during this execution.
     * Tracks when the main agent delegates work to specialized sub-agents.
     * Ordered chronologically by invocation time.
     * </pre>
     *
     * <code>repeated .ai.stigmer.agentic.agentexecution.v1.SubAgentExecution sub_agent_executions = 4 [json_name = "subAgentExecutions"];</code>
     */
    public java.util.List<ai.stigmer.agentic.agentexecution.v1.SubAgentExecution> getSubAgentExecutionsList() {
      if (subAgentExecutionsBuilder_ == null) {
        return java.util.Collections.unmodifiableList(subAgentExecutions_);
      } else {
        return subAgentExecutionsBuilder_.getMessageList();
      }
    }
    /**
     * <pre>
     * Sub-agent executions invoked during this execution.
     * Tracks when the main agent delegates work to specialized sub-agents.
     * Ordered chronologically by invocation time.
     * </pre>
     *
     * <code>repeated .ai.stigmer.agentic.agentexecution.v1.SubAgentExecution sub_agent_executions = 4 [json_name = "subAgentExecutions"];</code>
     */
    public int getSubAgentExecutionsCount() {
      if (subAgentExecutionsBuilder_ == null) {
        return subAgentExecutions_.size();
      } else {
        return subAgentExecutionsBuilder_.getCount();
      }
    }
    /**
     * <pre>
     * Sub-agent executions invoked during this execution.
     * Tracks when the main agent delegates work to specialized sub-agents.
     * Ordered chronologically by invocation time.
     * </pre>
     *
     * <code>repeated .ai.stigmer.agentic.agentexecution.v1.SubAgentExecution sub_agent_executions = 4 [json_name = "subAgentExecutions"];</code>
     */
    public ai.stigmer.agentic.agentexecution.v1.SubAgentExecution getSubAgentExecutions(int index) {
      if (subAgentExecutionsBuilder_ == null) {
        return subAgentExecutions_.get(index);
      } else {
        return subAgentExecutionsBuilder_.getMessage(index);
      }
    }
    /**
     * <pre>
     * Sub-agent executions invoked during this execution.
     * Tracks when the main agent delegates work to specialized sub-agents.
     * Ordered chronologically by invocation time.
     * </pre>
     *
     * <code>repeated .ai.stigmer.agentic.agentexecution.v1.SubAgentExecution sub_agent_executions = 4 [json_name = "subAgentExecutions"];</code>
     */
    public Builder setSubAgentExecutions(
        int index, ai.stigmer.agentic.agentexecution.v1.SubAgentExecution value) {
      if (subAgentExecutionsBuilder_ == null) {
        if (value == null) {
          throw new NullPointerException();
        }
        ensureSubAgentExecutionsIsMutable();
        subAgentExecutions_.set(index, value);
        onChanged();
      } else {
        subAgentExecutionsBuilder_.setMessage(index, value);
      }
      return this;
    }
    /**
     * <pre>
     * Sub-agent executions invoked during this execution.
     * Tracks when the main agent delegates work to specialized sub-agents.
     * Ordered chronologically by invocation time.
     * </pre>
     *
     * <code>repeated .ai.stigmer.agentic.agentexecution.v1.SubAgentExecution sub_agent_executions = 4 [json_name = "subAgentExecutions"];</code>
     */
    public Builder setSubAgentExecutions(
        int index, ai.stigmer.agentic.agentexecution.v1.SubAgentExecution.Builder builderForValue) {
      if (subAgentExecutionsBuilder_ == null) {
        ensureSubAgentExecutionsIsMutable();
        subAgentExecutions_.set(index, builderForValue.build());
        onChanged();
      } else {
        subAgentExecutionsBuilder_.setMessage(index, builderForValue.build());
      }
      return this;
    }
    /**
     * <pre>
     * Sub-agent executions invoked during this execution.
     * Tracks when the main agent delegates work to specialized sub-agents.
     * Ordered chronologically by invocation time.
     * </pre>
     *
     * <code>repeated .ai.stigmer.agentic.agentexecution.v1.SubAgentExecution sub_agent_executions = 4 [json_name = "subAgentExecutions"];</code>
     */
    public Builder addSubAgentExecutions(ai.stigmer.agentic.agentexecution.v1.SubAgentExecution value) {
      if (subAgentExecutionsBuilder_ == null) {
        if (value == null) {
          throw new NullPointerException();
        }
        ensureSubAgentExecutionsIsMutable();
        subAgentExecutions_.add(value);
        onChanged();
      } else {
        subAgentExecutionsBuilder_.addMessage(value);
      }
      return this;
    }
    /**
     * <pre>
     * Sub-agent executions invoked during this execution.
     * Tracks when the main agent delegates work to specialized sub-agents.
     * Ordered chronologically by invocation time.
     * </pre>
     *
     * <code>repeated .ai.stigmer.agentic.agentexecution.v1.SubAgentExecution sub_agent_executions = 4 [json_name = "subAgentExecutions"];</code>
     */
    public Builder addSubAgentExecutions(
        int index, ai.stigmer.agentic.agentexecution.v1.SubAgentExecution value) {
      if (subAgentExecutionsBuilder_ == null) {
        if (value == null) {
          throw new NullPointerException();
        }
        ensureSubAgentExecutionsIsMutable();
        subAgentExecutions_.add(index, value);
        onChanged();
      } else {
        subAgentExecutionsBuilder_.addMessage(index, value);
      }
      return this;
    }
    /**
     * <pre>
     * Sub-agent executions invoked during this execution.
     * Tracks when the main agent delegates work to specialized sub-agents.
     * Ordered chronologically by invocation time.
     * </pre>
     *
     * <code>repeated .ai.stigmer.agentic.agentexecution.v1.SubAgentExecution sub_agent_executions = 4 [json_name = "subAgentExecutions"];</code>
     */
    public Builder addSubAgentExecutions(
        ai.stigmer.agentic.agentexecution.v1.SubAgentExecution.Builder builderForValue) {
      if (subAgentExecutionsBuilder_ == null) {
        ensureSubAgentExecutionsIsMutable();
        subAgentExecutions_.add(builderForValue.build());
        onChanged();
      } else {
        subAgentExecutionsBuilder_.addMessage(builderForValue.build());
      }
      return this;
    }
    /**
     * <pre>
     * Sub-agent executions invoked during this execution.
     * Tracks when the main agent delegates work to specialized sub-agents.
     * Ordered chronologically by invocation time.
     * </pre>
     *
     * <code>repeated .ai.stigmer.agentic.agentexecution.v1.SubAgentExecution sub_agent_executions = 4 [json_name = "subAgentExecutions"];</code>
     */
    public Builder addSubAgentExecutions(
        int index, ai.stigmer.agentic.agentexecution.v1.SubAgentExecution.Builder builderForValue) {
      if (subAgentExecutionsBuilder_ == null) {
        ensureSubAgentExecutionsIsMutable();
        subAgentExecutions_.add(index, builderForValue.build());
        onChanged();
      } else {
        subAgentExecutionsBuilder_.addMessage(index, builderForValue.build());
      }
      return this;
    }
    /**
     * <pre>
     * Sub-agent executions invoked during this execution.
     * Tracks when the main agent delegates work to specialized sub-agents.
     * Ordered chronologically by invocation time.
     * </pre>
     *
     * <code>repeated .ai.stigmer.agentic.agentexecution.v1.SubAgentExecution sub_agent_executions = 4 [json_name = "subAgentExecutions"];</code>
     */
    public Builder addAllSubAgentExecutions(
        java.lang.Iterable<? extends ai.stigmer.agentic.agentexecution.v1.SubAgentExecution> values) {
      if (subAgentExecutionsBuilder_ == null) {
        ensureSubAgentExecutionsIsMutable();
        com.google.protobuf.AbstractMessageLite.Builder.addAll(
            values, subAgentExecutions_);
        onChanged();
      } else {
        subAgentExecutionsBuilder_.addAllMessages(values);
      }
      return this;
    }
    /**
     * <pre>
     * Sub-agent executions invoked during this execution.
     * Tracks when the main agent delegates work to specialized sub-agents.
     * Ordered chronologically by invocation time.
     * </pre>
     *
     * <code>repeated .ai.stigmer.agentic.agentexecution.v1.SubAgentExecution sub_agent_executions = 4 [json_name = "subAgentExecutions"];</code>
     */
    public Builder clearSubAgentExecutions() {
      if (subAgentExecutionsBuilder_ == null) {
        subAgentExecutions_ = java.util.Collections.emptyList();
        bitField0_ = (bitField0_ & ~0x00000008);
        onChanged();
      } else {
        subAgentExecutionsBuilder_.clear();
      }
      return this;
    }
    /**
     * <pre>
     * Sub-agent executions invoked during this execution.
     * Tracks when the main agent delegates work to specialized sub-agents.
     * Ordered chronologically by invocation time.
     * </pre>
     *
     * <code>repeated .ai.stigmer.agentic.agentexecution.v1.SubAgentExecution sub_agent_executions = 4 [json_name = "subAgentExecutions"];</code>
     */
    public Builder removeSubAgentExecutions(int index) {
      if (subAgentExecutionsBuilder_ == null) {
        ensureSubAgentExecutionsIsMutable();
        subAgentExecutions_.remove(index);
        onChanged();
      } else {
        subAgentExecutionsBuilder_.remove(index);
      }
      return this;
    }
    /**
     * <pre>
     * Sub-agent executions invoked during this execution.
     * Tracks when the main agent delegates work to specialized sub-agents.
     * Ordered chronologically by invocation time.
     * </pre>
     *
     * <code>repeated .ai.stigmer.agentic.agentexecution.v1.SubAgentExecution sub_agent_executions = 4 [json_name = "subAgentExecutions"];</code>
     */
    public ai.stigmer.agentic.agentexecution.v1.SubAgentExecution.Builder getSubAgentExecutionsBuilder(
        int index) {
      return internalGetSubAgentExecutionsFieldBuilder().getBuilder(index);
    }
    /**
     * <pre>
     * Sub-agent executions invoked during this execution.
     * Tracks when the main agent delegates work to specialized sub-agents.
     * Ordered chronologically by invocation time.
     * </pre>
     *
     * <code>repeated .ai.stigmer.agentic.agentexecution.v1.SubAgentExecution sub_agent_executions = 4 [json_name = "subAgentExecutions"];</code>
     */
    public ai.stigmer.agentic.agentexecution.v1.SubAgentExecutionOrBuilder getSubAgentExecutionsOrBuilder(
        int index) {
      if (subAgentExecutionsBuilder_ == null) {
        return subAgentExecutions_.get(index);  } else {
        return subAgentExecutionsBuilder_.getMessageOrBuilder(index);
      }
    }
    /**
     * <pre>
     * Sub-agent executions invoked during this execution.
     * Tracks when the main agent delegates work to specialized sub-agents.
     * Ordered chronologically by invocation time.
     * </pre>
     *
     * <code>repeated .ai.stigmer.agentic.agentexecution.v1.SubAgentExecution sub_agent_executions = 4 [json_name = "subAgentExecutions"];</code>
     */
    public java.util.List<? extends ai.stigmer.agentic.agentexecution.v1.SubAgentExecutionOrBuilder> 
         getSubAgentExecutionsOrBuilderList() {
      if (subAgentExecutionsBuilder_ != null) {
        return subAgentExecutionsBuilder_.getMessageOrBuilderList();
      } else {
        return java.util.Collections.unmodifiableList(subAgentExecutions_);
      }
    }
    /**
     * <pre>
     * Sub-agent executions invoked during this execution.
     * Tracks when the main agent delegates work to specialized sub-agents.
     * Ordered chronologically by invocation time.
     * </pre>
     *
     * <code>repeated .ai.stigmer.agentic.agentexecution.v1.SubAgentExecution sub_agent_executions = 4 [json_name = "subAgentExecutions"];</code>
     */
    public ai.stigmer.agentic.agentexecution.v1.SubAgentExecution.Builder addSubAgentExecutionsBuilder() {
      return internalGetSubAgentExecutionsFieldBuilder().addBuilder(
          ai.stigmer.agentic.agentexecution.v1.SubAgentExecution.getDefaultInstance());
    }
    /**
     * <pre>
     * Sub-agent executions invoked during this execution.
     * Tracks when the main agent delegates work to specialized sub-agents.
     * Ordered chronologically by invocation time.
     * </pre>
     *
     * <code>repeated .ai.stigmer.agentic.agentexecution.v1.SubAgentExecution sub_agent_executions = 4 [json_name = "subAgentExecutions"];</code>
     */
    public ai.stigmer.agentic.agentexecution.v1.SubAgentExecution.Builder addSubAgentExecutionsBuilder(
        int index) {
      return internalGetSubAgentExecutionsFieldBuilder().addBuilder(
          index, ai.stigmer.agentic.agentexecution.v1.SubAgentExecution.getDefaultInstance());
    }
    /**
     * <pre>
     * Sub-agent executions invoked during this execution.
     * Tracks when the main agent delegates work to specialized sub-agents.
     * Ordered chronologically by invocation time.
     * </pre>
     *
     * <code>repeated .ai.stigmer.agentic.agentexecution.v1.SubAgentExecution sub_agent_executions = 4 [json_name = "subAgentExecutions"];</code>
     */
    public java.util.List<ai.stigmer.agentic.agentexecution.v1.SubAgentExecution.Builder> 
         getSubAgentExecutionsBuilderList() {
      return internalGetSubAgentExecutionsFieldBuilder().getBuilderList();
    }
    private com.google.protobuf.RepeatedFieldBuilder<
        ai.stigmer.agentic.agentexecution.v1.SubAgentExecution, ai.stigmer.agentic.agentexecution.v1.SubAgentExecution.Builder, ai.stigmer.agentic.agentexecution.v1.SubAgentExecutionOrBuilder> 
        internalGetSubAgentExecutionsFieldBuilder() {
      if (subAgentExecutionsBuilder_ == null) {
        subAgentExecutionsBuilder_ = new com.google.protobuf.RepeatedFieldBuilder<
            ai.stigmer.agentic.agentexecution.v1.SubAgentExecution, ai.stigmer.agentic.agentexecution.v1.SubAgentExecution.Builder, ai.stigmer.agentic.agentexecution.v1.SubAgentExecutionOrBuilder>(
                subAgentExecutions_,
                ((bitField0_ & 0x00000008) != 0),
                getParentForChildren(),
                isClean());
        subAgentExecutions_ = null;
      }
      return subAgentExecutionsBuilder_;
    }

    private java.lang.Object error_ = "";
    /**
     * <pre>
     * Error message if execution failed.
     * Only populated when phase == EXECUTION_FAILED.
     * </pre>
     *
     * <code>string error = 6 [json_name = "error"];</code>
     * @return The error.
     */
    public java.lang.String getError() {
      java.lang.Object ref = error_;
      if (!(ref instanceof java.lang.String)) {
        com.google.protobuf.ByteString bs =
            (com.google.protobuf.ByteString) ref;
        java.lang.String s = bs.toStringUtf8();
        error_ = s;
        return s;
      } else {
        return (java.lang.String) ref;
      }
    }
    /**
     * <pre>
     * Error message if execution failed.
     * Only populated when phase == EXECUTION_FAILED.
     * </pre>
     *
     * <code>string error = 6 [json_name = "error"];</code>
     * @return The bytes for error.
     */
    public com.google.protobuf.ByteString
        getErrorBytes() {
      java.lang.Object ref = error_;
      if (ref instanceof String) {
        com.google.protobuf.ByteString b = 
            com.google.protobuf.ByteString.copyFromUtf8(
                (java.lang.String) ref);
        error_ = b;
        return b;
      } else {
        return (com.google.protobuf.ByteString) ref;
      }
    }
    /**
     * <pre>
     * Error message if execution failed.
     * Only populated when phase == EXECUTION_FAILED.
     * </pre>
     *
     * <code>string error = 6 [json_name = "error"];</code>
     * @param value The error to set.
     * @return This builder for chaining.
     */
    public Builder setError(
        java.lang.String value) {
      if (value == null) { throw new NullPointerException(); }
      error_ = value;
      bitField0_ |= 0x00000010;
      onChanged();
      return this;
    }
    /**
     * <pre>
     * Error message if execution failed.
     * Only populated when phase == EXECUTION_FAILED.
     * </pre>
     *
     * <code>string error = 6 [json_name = "error"];</code>
     * @return This builder for chaining.
     */
    public Builder clearError() {
      error_ = getDefaultInstance().getError();
      bitField0_ = (bitField0_ & ~0x00000010);
      onChanged();
      return this;
    }
    /**
     * <pre>
     * Error message if execution failed.
     * Only populated when phase == EXECUTION_FAILED.
     * </pre>
     *
     * <code>string error = 6 [json_name = "error"];</code>
     * @param value The bytes for error to set.
     * @return This builder for chaining.
     */
    public Builder setErrorBytes(
        com.google.protobuf.ByteString value) {
      if (value == null) { throw new NullPointerException(); }
      checkByteStringIsUtf8(value);
      error_ = value;
      bitField0_ |= 0x00000010;
      onChanged();
      return this;
    }

    private java.lang.Object startedAt_ = "";
    /**
     * <pre>
     * ISO 8601 timestamp when execution started.
     * Example: "2025-01-10T10:30:00Z"
     * </pre>
     *
     * <code>string started_at = 7 [json_name = "startedAt"];</code>
     * @return The startedAt.
     */
    public java.lang.String getStartedAt() {
      java.lang.Object ref = startedAt_;
      if (!(ref instanceof java.lang.String)) {
        com.google.protobuf.ByteString bs =
            (com.google.protobuf.ByteString) ref;
        java.lang.String s = bs.toStringUtf8();
        startedAt_ = s;
        return s;
      } else {
        return (java.lang.String) ref;
      }
    }
    /**
     * <pre>
     * ISO 8601 timestamp when execution started.
     * Example: "2025-01-10T10:30:00Z"
     * </pre>
     *
     * <code>string started_at = 7 [json_name = "startedAt"];</code>
     * @return The bytes for startedAt.
     */
    public com.google.protobuf.ByteString
        getStartedAtBytes() {
      java.lang.Object ref = startedAt_;
      if (ref instanceof String) {
        com.google.protobuf.ByteString b = 
            com.google.protobuf.ByteString.copyFromUtf8(
                (java.lang.String) ref);
        startedAt_ = b;
        return b;
      } else {
        return (com.google.protobuf.ByteString) ref;
      }
    }
    /**
     * <pre>
     * ISO 8601 timestamp when execution started.
     * Example: "2025-01-10T10:30:00Z"
     * </pre>
     *
     * <code>string started_at = 7 [json_name = "startedAt"];</code>
     * @param value The startedAt to set.
     * @return This builder for chaining.
     */
    public Builder setStartedAt(
        java.lang.String value) {
      if (value == null) { throw new NullPointerException(); }
      startedAt_ = value;
      bitField0_ |= 0x00000020;
      onChanged();
      return this;
    }
    /**
     * <pre>
     * ISO 8601 timestamp when execution started.
     * Example: "2025-01-10T10:30:00Z"
     * </pre>
     *
     * <code>string started_at = 7 [json_name = "startedAt"];</code>
     * @return This builder for chaining.
     */
    public Builder clearStartedAt() {
      startedAt_ = getDefaultInstance().getStartedAt();
      bitField0_ = (bitField0_ & ~0x00000020);
      onChanged();
      return this;
    }
    /**
     * <pre>
     * ISO 8601 timestamp when execution started.
     * Example: "2025-01-10T10:30:00Z"
     * </pre>
     *
     * <code>string started_at = 7 [json_name = "startedAt"];</code>
     * @param value The bytes for startedAt to set.
     * @return This builder for chaining.
     */
    public Builder setStartedAtBytes(
        com.google.protobuf.ByteString value) {
      if (value == null) { throw new NullPointerException(); }
      checkByteStringIsUtf8(value);
      startedAt_ = value;
      bitField0_ |= 0x00000020;
      onChanged();
      return this;
    }

    private java.lang.Object completedAt_ = "";
    /**
     * <pre>
     * ISO 8601 timestamp when execution completed, failed, or was cancelled.
     * Only populated for terminal states.
     * </pre>
     *
     * <code>string completed_at = 8 [json_name = "completedAt"];</code>
     * @return The completedAt.
     */
    public java.lang.String getCompletedAt() {
      java.lang.Object ref = completedAt_;
      if (!(ref instanceof java.lang.String)) {
        com.google.protobuf.ByteString bs =
            (com.google.protobuf.ByteString) ref;
        java.lang.String s = bs.toStringUtf8();
        completedAt_ = s;
        return s;
      } else {
        return (java.lang.String) ref;
      }
    }
    /**
     * <pre>
     * ISO 8601 timestamp when execution completed, failed, or was cancelled.
     * Only populated for terminal states.
     * </pre>
     *
     * <code>string completed_at = 8 [json_name = "completedAt"];</code>
     * @return The bytes for completedAt.
     */
    public com.google.protobuf.ByteString
        getCompletedAtBytes() {
      java.lang.Object ref = completedAt_;
      if (ref instanceof String) {
        com.google.protobuf.ByteString b = 
            com.google.protobuf.ByteString.copyFromUtf8(
                (java.lang.String) ref);
        completedAt_ = b;
        return b;
      } else {
        return (com.google.protobuf.ByteString) ref;
      }
    }
    /**
     * <pre>
     * ISO 8601 timestamp when execution completed, failed, or was cancelled.
     * Only populated for terminal states.
     * </pre>
     *
     * <code>string completed_at = 8 [json_name = "completedAt"];</code>
     * @param value The completedAt to set.
     * @return This builder for chaining.
     */
    public Builder setCompletedAt(
        java.lang.String value) {
      if (value == null) { throw new NullPointerException(); }
      completedAt_ = value;
      bitField0_ |= 0x00000040;
      onChanged();
      return this;
    }
    /**
     * <pre>
     * ISO 8601 timestamp when execution completed, failed, or was cancelled.
     * Only populated for terminal states.
     * </pre>
     *
     * <code>string completed_at = 8 [json_name = "completedAt"];</code>
     * @return This builder for chaining.
     */
    public Builder clearCompletedAt() {
      completedAt_ = getDefaultInstance().getCompletedAt();
      bitField0_ = (bitField0_ & ~0x00000040);
      onChanged();
      return this;
    }
    /**
     * <pre>
     * ISO 8601 timestamp when execution completed, failed, or was cancelled.
     * Only populated for terminal states.
     * </pre>
     *
     * <code>string completed_at = 8 [json_name = "completedAt"];</code>
     * @param value The bytes for completedAt to set.
     * @return This builder for chaining.
     */
    public Builder setCompletedAtBytes(
        com.google.protobuf.ByteString value) {
      if (value == null) { throw new NullPointerException(); }
      checkByteStringIsUtf8(value);
      completedAt_ = value;
      bitField0_ |= 0x00000040;
      onChanged();
      return this;
    }

    private static final class TodosConverter implements com.google.protobuf.MapFieldBuilder.Converter<java.lang.String, ai.stigmer.agentic.agentexecution.v1.TodoItemOrBuilder, ai.stigmer.agentic.agentexecution.v1.TodoItem> {
      @java.lang.Override
      public ai.stigmer.agentic.agentexecution.v1.TodoItem build(ai.stigmer.agentic.agentexecution.v1.TodoItemOrBuilder val) {
        if (val instanceof ai.stigmer.agentic.agentexecution.v1.TodoItem) { return (ai.stigmer.agentic.agentexecution.v1.TodoItem) val; }
        return ((ai.stigmer.agentic.agentexecution.v1.TodoItem.Builder) val).build();
      }

      @java.lang.Override
      public com.google.protobuf.MapEntry<java.lang.String, ai.stigmer.agentic.agentexecution.v1.TodoItem> defaultEntry() {
        return TodosDefaultEntryHolder.defaultEntry;
      }
    };
    private static final TodosConverter todosConverter = new TodosConverter();

    private com.google.protobuf.MapFieldBuilder<
        java.lang.String, ai.stigmer.agentic.agentexecution.v1.TodoItemOrBuilder, ai.stigmer.agentic.agentexecution.v1.TodoItem, ai.stigmer.agentic.agentexecution.v1.TodoItem.Builder> todos_;
    private com.google.protobuf.MapFieldBuilder<java.lang.String, ai.stigmer.agentic.agentexecution.v1.TodoItemOrBuilder, ai.stigmer.agentic.agentexecution.v1.TodoItem, ai.stigmer.agentic.agentexecution.v1.TodoItem.Builder>
        internalGetTodos() {
      if (todos_ == null) {
        return new com.google.protobuf.MapFieldBuilder<>(todosConverter);
      }
      return todos_;
    }
    private com.google.protobuf.MapFieldBuilder<java.lang.String, ai.stigmer.agentic.agentexecution.v1.TodoItemOrBuilder, ai.stigmer.agentic.agentexecution.v1.TodoItem, ai.stigmer.agentic.agentexecution.v1.TodoItem.Builder>
        internalGetMutableTodos() {
      if (todos_ == null) {
        todos_ = new com.google.protobuf.MapFieldBuilder<>(todosConverter);
      }
      bitField0_ |= 0x00000080;
      onChanged();
      return todos_;
    }
    public int getTodosCount() {
      return internalGetTodos().ensureBuilderMap().size();
    }
    /**
     * <pre>
     * Todo list tracking multi-step tasks and progress for this execution.
     * Updated via write_todos tool (if TodoListMiddleware is enabled).
     * Each execution maintains its own snapshot of todos at execution time.
     * Key: todo item ID, Value: todo item details
     * </pre>
     *
     * <code>map&lt;string, .ai.stigmer.agentic.agentexecution.v1.TodoItem&gt; todos = 9 [json_name = "todos"];</code>
     */
    @java.lang.Override
    public boolean containsTodos(
        java.lang.String key) {
      if (key == null) { throw new NullPointerException("map key"); }
      return internalGetTodos().ensureBuilderMap().containsKey(key);
    }
    /**
     * Use {@link #getTodosMap()} instead.
     */
    @java.lang.Override
    @java.lang.Deprecated
    public java.util.Map<java.lang.String, ai.stigmer.agentic.agentexecution.v1.TodoItem> getTodos() {
      return getTodosMap();
    }
    /**
     * <pre>
     * Todo list tracking multi-step tasks and progress for this execution.
     * Updated via write_todos tool (if TodoListMiddleware is enabled).
     * Each execution maintains its own snapshot of todos at execution time.
     * Key: todo item ID, Value: todo item details
     * </pre>
     *
     * <code>map&lt;string, .ai.stigmer.agentic.agentexecution.v1.TodoItem&gt; todos = 9 [json_name = "todos"];</code>
     */
    @java.lang.Override
    public java.util.Map<java.lang.String, ai.stigmer.agentic.agentexecution.v1.TodoItem> getTodosMap() {
      return internalGetTodos().getImmutableMap();
    }
    /**
     * <pre>
     * Todo list tracking multi-step tasks and progress for this execution.
     * Updated via write_todos tool (if TodoListMiddleware is enabled).
     * Each execution maintains its own snapshot of todos at execution time.
     * Key: todo item ID, Value: todo item details
     * </pre>
     *
     * <code>map&lt;string, .ai.stigmer.agentic.agentexecution.v1.TodoItem&gt; todos = 9 [json_name = "todos"];</code>
     */
    @java.lang.Override
    public /* nullable */
ai.stigmer.agentic.agentexecution.v1.TodoItem getTodosOrDefault(
        java.lang.String key,
        /* nullable */
ai.stigmer.agentic.agentexecution.v1.TodoItem defaultValue) {
      if (key == null) { throw new NullPointerException("map key"); }
      java.util.Map<java.lang.String, ai.stigmer.agentic.agentexecution.v1.TodoItemOrBuilder> map = internalGetMutableTodos().ensureBuilderMap();
      return map.containsKey(key) ? todosConverter.build(map.get(key)) : defaultValue;
    }
    /**
     * <pre>
     * Todo list tracking multi-step tasks and progress for this execution.
     * Updated via write_todos tool (if TodoListMiddleware is enabled).
     * Each execution maintains its own snapshot of todos at execution time.
     * Key: todo item ID, Value: todo item details
     * </pre>
     *
     * <code>map&lt;string, .ai.stigmer.agentic.agentexecution.v1.TodoItem&gt; todos = 9 [json_name = "todos"];</code>
     */
    @java.lang.Override
    public ai.stigmer.agentic.agentexecution.v1.TodoItem getTodosOrThrow(
        java.lang.String key) {
      if (key == null) { throw new NullPointerException("map key"); }
      java.util.Map<java.lang.String, ai.stigmer.agentic.agentexecution.v1.TodoItemOrBuilder> map = internalGetMutableTodos().ensureBuilderMap();
      if (!map.containsKey(key)) {
        throw new java.lang.IllegalArgumentException();
      }
      return todosConverter.build(map.get(key));
    }
    public Builder clearTodos() {
      bitField0_ = (bitField0_ & ~0x00000080);
      internalGetMutableTodos().clear();
      return this;
    }
    /**
     * <pre>
     * Todo list tracking multi-step tasks and progress for this execution.
     * Updated via write_todos tool (if TodoListMiddleware is enabled).
     * Each execution maintains its own snapshot of todos at execution time.
     * Key: todo item ID, Value: todo item details
     * </pre>
     *
     * <code>map&lt;string, .ai.stigmer.agentic.agentexecution.v1.TodoItem&gt; todos = 9 [json_name = "todos"];</code>
     */
    public Builder removeTodos(
        java.lang.String key) {
      if (key == null) { throw new NullPointerException("map key"); }
      internalGetMutableTodos().ensureBuilderMap()
          .remove(key);
      return this;
    }
    /**
     * Use alternate mutation accessors instead.
     */
    @java.lang.Deprecated
    public java.util.Map<java.lang.String, ai.stigmer.agentic.agentexecution.v1.TodoItem>
        getMutableTodos() {
      bitField0_ |= 0x00000080;
      return internalGetMutableTodos().ensureMessageMap();
    }
    /**
     * <pre>
     * Todo list tracking multi-step tasks and progress for this execution.
     * Updated via write_todos tool (if TodoListMiddleware is enabled).
     * Each execution maintains its own snapshot of todos at execution time.
     * Key: todo item ID, Value: todo item details
     * </pre>
     *
     * <code>map&lt;string, .ai.stigmer.agentic.agentexecution.v1.TodoItem&gt; todos = 9 [json_name = "todos"];</code>
     */
    public Builder putTodos(
        java.lang.String key,
        ai.stigmer.agentic.agentexecution.v1.TodoItem value) {
      if (key == null) { throw new NullPointerException("map key"); }
      if (value == null) { throw new NullPointerException("map value"); }
      internalGetMutableTodos().ensureBuilderMap()
          .put(key, value);
      bitField0_ |= 0x00000080;
      return this;
    }
    /**
     * <pre>
     * Todo list tracking multi-step tasks and progress for this execution.
     * Updated via write_todos tool (if TodoListMiddleware is enabled).
     * Each execution maintains its own snapshot of todos at execution time.
     * Key: todo item ID, Value: todo item details
     * </pre>
     *
     * <code>map&lt;string, .ai.stigmer.agentic.agentexecution.v1.TodoItem&gt; todos = 9 [json_name = "todos"];</code>
     */
    public Builder putAllTodos(
        java.util.Map<java.lang.String, ai.stigmer.agentic.agentexecution.v1.TodoItem> values) {
      for (java.util.Map.Entry<java.lang.String, ai.stigmer.agentic.agentexecution.v1.TodoItem> e : values.entrySet()) {
        if (e.getKey() == null || e.getValue() == null) {
          throw new NullPointerException();
        }
      }
      internalGetMutableTodos().ensureBuilderMap()
          .putAll(values);
      bitField0_ |= 0x00000080;
      return this;
    }
    /**
     * <pre>
     * Todo list tracking multi-step tasks and progress for this execution.
     * Updated via write_todos tool (if TodoListMiddleware is enabled).
     * Each execution maintains its own snapshot of todos at execution time.
     * Key: todo item ID, Value: todo item details
     * </pre>
     *
     * <code>map&lt;string, .ai.stigmer.agentic.agentexecution.v1.TodoItem&gt; todos = 9 [json_name = "todos"];</code>
     */
    public ai.stigmer.agentic.agentexecution.v1.TodoItem.Builder putTodosBuilderIfAbsent(
        java.lang.String key) {
      java.util.Map<java.lang.String, ai.stigmer.agentic.agentexecution.v1.TodoItemOrBuilder> builderMap = internalGetMutableTodos().ensureBuilderMap();
      ai.stigmer.agentic.agentexecution.v1.TodoItemOrBuilder entry = builderMap.get(key);
      if (entry == null) {
        entry = ai.stigmer.agentic.agentexecution.v1.TodoItem.newBuilder();
        builderMap.put(key, entry);
      }
      if (entry instanceof ai.stigmer.agentic.agentexecution.v1.TodoItem) {
        entry = ((ai.stigmer.agentic.agentexecution.v1.TodoItem) entry).toBuilder();
        builderMap.put(key, entry);
      }
      return (ai.stigmer.agentic.agentexecution.v1.TodoItem.Builder) entry;
    }

    private com.google.protobuf.ByteString callbackToken_ = com.google.protobuf.ByteString.EMPTY;
    /**
     * <pre>
     * Callback token for notifying a parent workflow when this execution completes.
     *
     * Present when this execution was triggered by a workflow. Empty when the
     * execution runs independently (chat, API call).
     *
     * &#64;internal
     *
     * When this AgentExecution is triggered from a Temporal activity, the
     * calling activity passes its task token here. The runner completes
     * the external activity when the execution finishes.
     *
     * Token Handshake Pattern:
     * 1. External activity extracts token and stores in AgentExecution.status
     * 2. Activity returns activity.ErrResultPending (paused, thread freed)
     * 3. Agent executes (seconds to minutes)
     * 4. On completion: the runner completes the external activity via token
     *
     * Why in Status (not Spec):
     * - System-generated by Temporal, not user-provided
     * - Runtime state, not configuration
     * - Ephemeral, only relevant during execution
     *
     * References:
     * - WorkflowExecution.spec.callback_token (field 7) - Same pattern
     * - ADR: docs/adr/20260122-async-agent-execution-temporal-token-handshake.md
     * </pre>
     *
     * <code>bytes callback_token = 10 [json_name = "callbackToken"];</code>
     * @return The callbackToken.
     */
    @java.lang.Override
    public com.google.protobuf.ByteString getCallbackToken() {
      return callbackToken_;
    }
    /**
     * <pre>
     * Callback token for notifying a parent workflow when this execution completes.
     *
     * Present when this execution was triggered by a workflow. Empty when the
     * execution runs independently (chat, API call).
     *
     * &#64;internal
     *
     * When this AgentExecution is triggered from a Temporal activity, the
     * calling activity passes its task token here. The runner completes
     * the external activity when the execution finishes.
     *
     * Token Handshake Pattern:
     * 1. External activity extracts token and stores in AgentExecution.status
     * 2. Activity returns activity.ErrResultPending (paused, thread freed)
     * 3. Agent executes (seconds to minutes)
     * 4. On completion: the runner completes the external activity via token
     *
     * Why in Status (not Spec):
     * - System-generated by Temporal, not user-provided
     * - Runtime state, not configuration
     * - Ephemeral, only relevant during execution
     *
     * References:
     * - WorkflowExecution.spec.callback_token (field 7) - Same pattern
     * - ADR: docs/adr/20260122-async-agent-execution-temporal-token-handshake.md
     * </pre>
     *
     * <code>bytes callback_token = 10 [json_name = "callbackToken"];</code>
     * @param value The callbackToken to set.
     * @return This builder for chaining.
     */
    public Builder setCallbackToken(com.google.protobuf.ByteString value) {
      if (value == null) { throw new NullPointerException(); }
      callbackToken_ = value;
      bitField0_ |= 0x00000100;
      onChanged();
      return this;
    }
    /**
     * <pre>
     * Callback token for notifying a parent workflow when this execution completes.
     *
     * Present when this execution was triggered by a workflow. Empty when the
     * execution runs independently (chat, API call).
     *
     * &#64;internal
     *
     * When this AgentExecution is triggered from a Temporal activity, the
     * calling activity passes its task token here. The runner completes
     * the external activity when the execution finishes.
     *
     * Token Handshake Pattern:
     * 1. External activity extracts token and stores in AgentExecution.status
     * 2. Activity returns activity.ErrResultPending (paused, thread freed)
     * 3. Agent executes (seconds to minutes)
     * 4. On completion: the runner completes the external activity via token
     *
     * Why in Status (not Spec):
     * - System-generated by Temporal, not user-provided
     * - Runtime state, not configuration
     * - Ephemeral, only relevant during execution
     *
     * References:
     * - WorkflowExecution.spec.callback_token (field 7) - Same pattern
     * - ADR: docs/adr/20260122-async-agent-execution-temporal-token-handshake.md
     * </pre>
     *
     * <code>bytes callback_token = 10 [json_name = "callbackToken"];</code>
     * @return This builder for chaining.
     */
    public Builder clearCallbackToken() {
      bitField0_ = (bitField0_ & ~0x00000100);
      callbackToken_ = getDefaultInstance().getCallbackToken();
      onChanged();
      return this;
    }

    private java.util.List<ai.stigmer.agentic.agentexecution.v1.PendingApproval> pendingApprovals_ =
      java.util.Collections.emptyList();
    private void ensurePendingApprovalsIsMutable() {
      if (!((bitField0_ & 0x00000200) != 0)) {
        pendingApprovals_ = new java.util.ArrayList<ai.stigmer.agentic.agentexecution.v1.PendingApproval>(pendingApprovals_);
        bitField0_ |= 0x00000200;
       }
    }

    private com.google.protobuf.RepeatedFieldBuilder<
        ai.stigmer.agentic.agentexecution.v1.PendingApproval, ai.stigmer.agentic.agentexecution.v1.PendingApproval.Builder, ai.stigmer.agentic.agentexecution.v1.PendingApprovalOrBuilder> pendingApprovalsBuilder_;

    /**
     * <pre>
     * Active pending approval requests for this execution.
     *
     * Contains one entry per tool call that requires user approval before
     * it can proceed. Sub-agent approvals are included with from_sub_agent
     * set to true. Empty when no approvals are pending.
     *
     * &#64;internal
     *
     * Computed server-side by the Go/Java UpdateStatus handlers on every write.
     * The handler scans messages[].tool_calls and
     * sub_agent_executions[].messages[].tool_calls, collecting entries where
     * status == WAITING_APPROVAL &amp;&amp; requires_approval == true, and projects
     * them into PendingApproval entries.
     *
     * Because this field is recomputed (not merged), it is always consistent
     * with the authoritative tool call state embedded in messages. No lifecycle
     * state machine, no merge logic, no forward-only enforcement needed.
     *
     * Lifecycle:
     * 1. The runner sets ToolCall.status = WAITING_APPROVAL on the message
     * 2. UpdateStatus handler recomputes this field, entry appears
     * 3. Phase changes to EXECUTION_WAITING_FOR_APPROVAL
     * 4. User submits decisions via SubmitApproval RPC (one per tool call)
     * 5. Agent resumes, ToolCall.status advances, next recompute drops the entry
     * </pre>
     *
     * <code>repeated .ai.stigmer.agentic.agentexecution.v1.PendingApproval pending_approvals = 16 [json_name = "pendingApprovals"];</code>
     */
    public java.util.List<ai.stigmer.agentic.agentexecution.v1.PendingApproval> getPendingApprovalsList() {
      if (pendingApprovalsBuilder_ == null) {
        return java.util.Collections.unmodifiableList(pendingApprovals_);
      } else {
        return pendingApprovalsBuilder_.getMessageList();
      }
    }
    /**
     * <pre>
     * Active pending approval requests for this execution.
     *
     * Contains one entry per tool call that requires user approval before
     * it can proceed. Sub-agent approvals are included with from_sub_agent
     * set to true. Empty when no approvals are pending.
     *
     * &#64;internal
     *
     * Computed server-side by the Go/Java UpdateStatus handlers on every write.
     * The handler scans messages[].tool_calls and
     * sub_agent_executions[].messages[].tool_calls, collecting entries where
     * status == WAITING_APPROVAL &amp;&amp; requires_approval == true, and projects
     * them into PendingApproval entries.
     *
     * Because this field is recomputed (not merged), it is always consistent
     * with the authoritative tool call state embedded in messages. No lifecycle
     * state machine, no merge logic, no forward-only enforcement needed.
     *
     * Lifecycle:
     * 1. The runner sets ToolCall.status = WAITING_APPROVAL on the message
     * 2. UpdateStatus handler recomputes this field, entry appears
     * 3. Phase changes to EXECUTION_WAITING_FOR_APPROVAL
     * 4. User submits decisions via SubmitApproval RPC (one per tool call)
     * 5. Agent resumes, ToolCall.status advances, next recompute drops the entry
     * </pre>
     *
     * <code>repeated .ai.stigmer.agentic.agentexecution.v1.PendingApproval pending_approvals = 16 [json_name = "pendingApprovals"];</code>
     */
    public int getPendingApprovalsCount() {
      if (pendingApprovalsBuilder_ == null) {
        return pendingApprovals_.size();
      } else {
        return pendingApprovalsBuilder_.getCount();
      }
    }
    /**
     * <pre>
     * Active pending approval requests for this execution.
     *
     * Contains one entry per tool call that requires user approval before
     * it can proceed. Sub-agent approvals are included with from_sub_agent
     * set to true. Empty when no approvals are pending.
     *
     * &#64;internal
     *
     * Computed server-side by the Go/Java UpdateStatus handlers on every write.
     * The handler scans messages[].tool_calls and
     * sub_agent_executions[].messages[].tool_calls, collecting entries where
     * status == WAITING_APPROVAL &amp;&amp; requires_approval == true, and projects
     * them into PendingApproval entries.
     *
     * Because this field is recomputed (not merged), it is always consistent
     * with the authoritative tool call state embedded in messages. No lifecycle
     * state machine, no merge logic, no forward-only enforcement needed.
     *
     * Lifecycle:
     * 1. The runner sets ToolCall.status = WAITING_APPROVAL on the message
     * 2. UpdateStatus handler recomputes this field, entry appears
     * 3. Phase changes to EXECUTION_WAITING_FOR_APPROVAL
     * 4. User submits decisions via SubmitApproval RPC (one per tool call)
     * 5. Agent resumes, ToolCall.status advances, next recompute drops the entry
     * </pre>
     *
     * <code>repeated .ai.stigmer.agentic.agentexecution.v1.PendingApproval pending_approvals = 16 [json_name = "pendingApprovals"];</code>
     */
    public ai.stigmer.agentic.agentexecution.v1.PendingApproval getPendingApprovals(int index) {
      if (pendingApprovalsBuilder_ == null) {
        return pendingApprovals_.get(index);
      } else {
        return pendingApprovalsBuilder_.getMessage(index);
      }
    }
    /**
     * <pre>
     * Active pending approval requests for this execution.
     *
     * Contains one entry per tool call that requires user approval before
     * it can proceed. Sub-agent approvals are included with from_sub_agent
     * set to true. Empty when no approvals are pending.
     *
     * &#64;internal
     *
     * Computed server-side by the Go/Java UpdateStatus handlers on every write.
     * The handler scans messages[].tool_calls and
     * sub_agent_executions[].messages[].tool_calls, collecting entries where
     * status == WAITING_APPROVAL &amp;&amp; requires_approval == true, and projects
     * them into PendingApproval entries.
     *
     * Because this field is recomputed (not merged), it is always consistent
     * with the authoritative tool call state embedded in messages. No lifecycle
     * state machine, no merge logic, no forward-only enforcement needed.
     *
     * Lifecycle:
     * 1. The runner sets ToolCall.status = WAITING_APPROVAL on the message
     * 2. UpdateStatus handler recomputes this field, entry appears
     * 3. Phase changes to EXECUTION_WAITING_FOR_APPROVAL
     * 4. User submits decisions via SubmitApproval RPC (one per tool call)
     * 5. Agent resumes, ToolCall.status advances, next recompute drops the entry
     * </pre>
     *
     * <code>repeated .ai.stigmer.agentic.agentexecution.v1.PendingApproval pending_approvals = 16 [json_name = "pendingApprovals"];</code>
     */
    public Builder setPendingApprovals(
        int index, ai.stigmer.agentic.agentexecution.v1.PendingApproval value) {
      if (pendingApprovalsBuilder_ == null) {
        if (value == null) {
          throw new NullPointerException();
        }
        ensurePendingApprovalsIsMutable();
        pendingApprovals_.set(index, value);
        onChanged();
      } else {
        pendingApprovalsBuilder_.setMessage(index, value);
      }
      return this;
    }
    /**
     * <pre>
     * Active pending approval requests for this execution.
     *
     * Contains one entry per tool call that requires user approval before
     * it can proceed. Sub-agent approvals are included with from_sub_agent
     * set to true. Empty when no approvals are pending.
     *
     * &#64;internal
     *
     * Computed server-side by the Go/Java UpdateStatus handlers on every write.
     * The handler scans messages[].tool_calls and
     * sub_agent_executions[].messages[].tool_calls, collecting entries where
     * status == WAITING_APPROVAL &amp;&amp; requires_approval == true, and projects
     * them into PendingApproval entries.
     *
     * Because this field is recomputed (not merged), it is always consistent
     * with the authoritative tool call state embedded in messages. No lifecycle
     * state machine, no merge logic, no forward-only enforcement needed.
     *
     * Lifecycle:
     * 1. The runner sets ToolCall.status = WAITING_APPROVAL on the message
     * 2. UpdateStatus handler recomputes this field, entry appears
     * 3. Phase changes to EXECUTION_WAITING_FOR_APPROVAL
     * 4. User submits decisions via SubmitApproval RPC (one per tool call)
     * 5. Agent resumes, ToolCall.status advances, next recompute drops the entry
     * </pre>
     *
     * <code>repeated .ai.stigmer.agentic.agentexecution.v1.PendingApproval pending_approvals = 16 [json_name = "pendingApprovals"];</code>
     */
    public Builder setPendingApprovals(
        int index, ai.stigmer.agentic.agentexecution.v1.PendingApproval.Builder builderForValue) {
      if (pendingApprovalsBuilder_ == null) {
        ensurePendingApprovalsIsMutable();
        pendingApprovals_.set(index, builderForValue.build());
        onChanged();
      } else {
        pendingApprovalsBuilder_.setMessage(index, builderForValue.build());
      }
      return this;
    }
    /**
     * <pre>
     * Active pending approval requests for this execution.
     *
     * Contains one entry per tool call that requires user approval before
     * it can proceed. Sub-agent approvals are included with from_sub_agent
     * set to true. Empty when no approvals are pending.
     *
     * &#64;internal
     *
     * Computed server-side by the Go/Java UpdateStatus handlers on every write.
     * The handler scans messages[].tool_calls and
     * sub_agent_executions[].messages[].tool_calls, collecting entries where
     * status == WAITING_APPROVAL &amp;&amp; requires_approval == true, and projects
     * them into PendingApproval entries.
     *
     * Because this field is recomputed (not merged), it is always consistent
     * with the authoritative tool call state embedded in messages. No lifecycle
     * state machine, no merge logic, no forward-only enforcement needed.
     *
     * Lifecycle:
     * 1. The runner sets ToolCall.status = WAITING_APPROVAL on the message
     * 2. UpdateStatus handler recomputes this field, entry appears
     * 3. Phase changes to EXECUTION_WAITING_FOR_APPROVAL
     * 4. User submits decisions via SubmitApproval RPC (one per tool call)
     * 5. Agent resumes, ToolCall.status advances, next recompute drops the entry
     * </pre>
     *
     * <code>repeated .ai.stigmer.agentic.agentexecution.v1.PendingApproval pending_approvals = 16 [json_name = "pendingApprovals"];</code>
     */
    public Builder addPendingApprovals(ai.stigmer.agentic.agentexecution.v1.PendingApproval value) {
      if (pendingApprovalsBuilder_ == null) {
        if (value == null) {
          throw new NullPointerException();
        }
        ensurePendingApprovalsIsMutable();
        pendingApprovals_.add(value);
        onChanged();
      } else {
        pendingApprovalsBuilder_.addMessage(value);
      }
      return this;
    }
    /**
     * <pre>
     * Active pending approval requests for this execution.
     *
     * Contains one entry per tool call that requires user approval before
     * it can proceed. Sub-agent approvals are included with from_sub_agent
     * set to true. Empty when no approvals are pending.
     *
     * &#64;internal
     *
     * Computed server-side by the Go/Java UpdateStatus handlers on every write.
     * The handler scans messages[].tool_calls and
     * sub_agent_executions[].messages[].tool_calls, collecting entries where
     * status == WAITING_APPROVAL &amp;&amp; requires_approval == true, and projects
     * them into PendingApproval entries.
     *
     * Because this field is recomputed (not merged), it is always consistent
     * with the authoritative tool call state embedded in messages. No lifecycle
     * state machine, no merge logic, no forward-only enforcement needed.
     *
     * Lifecycle:
     * 1. The runner sets ToolCall.status = WAITING_APPROVAL on the message
     * 2. UpdateStatus handler recomputes this field, entry appears
     * 3. Phase changes to EXECUTION_WAITING_FOR_APPROVAL
     * 4. User submits decisions via SubmitApproval RPC (one per tool call)
     * 5. Agent resumes, ToolCall.status advances, next recompute drops the entry
     * </pre>
     *
     * <code>repeated .ai.stigmer.agentic.agentexecution.v1.PendingApproval pending_approvals = 16 [json_name = "pendingApprovals"];</code>
     */
    public Builder addPendingApprovals(
        int index, ai.stigmer.agentic.agentexecution.v1.PendingApproval value) {
      if (pendingApprovalsBuilder_ == null) {
        if (value == null) {
          throw new NullPointerException();
        }
        ensurePendingApprovalsIsMutable();
        pendingApprovals_.add(index, value);
        onChanged();
      } else {
        pendingApprovalsBuilder_.addMessage(index, value);
      }
      return this;
    }
    /**
     * <pre>
     * Active pending approval requests for this execution.
     *
     * Contains one entry per tool call that requires user approval before
     * it can proceed. Sub-agent approvals are included with from_sub_agent
     * set to true. Empty when no approvals are pending.
     *
     * &#64;internal
     *
     * Computed server-side by the Go/Java UpdateStatus handlers on every write.
     * The handler scans messages[].tool_calls and
     * sub_agent_executions[].messages[].tool_calls, collecting entries where
     * status == WAITING_APPROVAL &amp;&amp; requires_approval == true, and projects
     * them into PendingApproval entries.
     *
     * Because this field is recomputed (not merged), it is always consistent
     * with the authoritative tool call state embedded in messages. No lifecycle
     * state machine, no merge logic, no forward-only enforcement needed.
     *
     * Lifecycle:
     * 1. The runner sets ToolCall.status = WAITING_APPROVAL on the message
     * 2. UpdateStatus handler recomputes this field, entry appears
     * 3. Phase changes to EXECUTION_WAITING_FOR_APPROVAL
     * 4. User submits decisions via SubmitApproval RPC (one per tool call)
     * 5. Agent resumes, ToolCall.status advances, next recompute drops the entry
     * </pre>
     *
     * <code>repeated .ai.stigmer.agentic.agentexecution.v1.PendingApproval pending_approvals = 16 [json_name = "pendingApprovals"];</code>
     */
    public Builder addPendingApprovals(
        ai.stigmer.agentic.agentexecution.v1.PendingApproval.Builder builderForValue) {
      if (pendingApprovalsBuilder_ == null) {
        ensurePendingApprovalsIsMutable();
        pendingApprovals_.add(builderForValue.build());
        onChanged();
      } else {
        pendingApprovalsBuilder_.addMessage(builderForValue.build());
      }
      return this;
    }
    /**
     * <pre>
     * Active pending approval requests for this execution.
     *
     * Contains one entry per tool call that requires user approval before
     * it can proceed. Sub-agent approvals are included with from_sub_agent
     * set to true. Empty when no approvals are pending.
     *
     * &#64;internal
     *
     * Computed server-side by the Go/Java UpdateStatus handlers on every write.
     * The handler scans messages[].tool_calls and
     * sub_agent_executions[].messages[].tool_calls, collecting entries where
     * status == WAITING_APPROVAL &amp;&amp; requires_approval == true, and projects
     * them into PendingApproval entries.
     *
     * Because this field is recomputed (not merged), it is always consistent
     * with the authoritative tool call state embedded in messages. No lifecycle
     * state machine, no merge logic, no forward-only enforcement needed.
     *
     * Lifecycle:
     * 1. The runner sets ToolCall.status = WAITING_APPROVAL on the message
     * 2. UpdateStatus handler recomputes this field, entry appears
     * 3. Phase changes to EXECUTION_WAITING_FOR_APPROVAL
     * 4. User submits decisions via SubmitApproval RPC (one per tool call)
     * 5. Agent resumes, ToolCall.status advances, next recompute drops the entry
     * </pre>
     *
     * <code>repeated .ai.stigmer.agentic.agentexecution.v1.PendingApproval pending_approvals = 16 [json_name = "pendingApprovals"];</code>
     */
    public Builder addPendingApprovals(
        int index, ai.stigmer.agentic.agentexecution.v1.PendingApproval.Builder builderForValue) {
      if (pendingApprovalsBuilder_ == null) {
        ensurePendingApprovalsIsMutable();
        pendingApprovals_.add(index, builderForValue.build());
        onChanged();
      } else {
        pendingApprovalsBuilder_.addMessage(index, builderForValue.build());
      }
      return this;
    }
    /**
     * <pre>
     * Active pending approval requests for this execution.
     *
     * Contains one entry per tool call that requires user approval before
     * it can proceed. Sub-agent approvals are included with from_sub_agent
     * set to true. Empty when no approvals are pending.
     *
     * &#64;internal
     *
     * Computed server-side by the Go/Java UpdateStatus handlers on every write.
     * The handler scans messages[].tool_calls and
     * sub_agent_executions[].messages[].tool_calls, collecting entries where
     * status == WAITING_APPROVAL &amp;&amp; requires_approval == true, and projects
     * them into PendingApproval entries.
     *
     * Because this field is recomputed (not merged), it is always consistent
     * with the authoritative tool call state embedded in messages. No lifecycle
     * state machine, no merge logic, no forward-only enforcement needed.
     *
     * Lifecycle:
     * 1. The runner sets ToolCall.status = WAITING_APPROVAL on the message
     * 2. UpdateStatus handler recomputes this field, entry appears
     * 3. Phase changes to EXECUTION_WAITING_FOR_APPROVAL
     * 4. User submits decisions via SubmitApproval RPC (one per tool call)
     * 5. Agent resumes, ToolCall.status advances, next recompute drops the entry
     * </pre>
     *
     * <code>repeated .ai.stigmer.agentic.agentexecution.v1.PendingApproval pending_approvals = 16 [json_name = "pendingApprovals"];</code>
     */
    public Builder addAllPendingApprovals(
        java.lang.Iterable<? extends ai.stigmer.agentic.agentexecution.v1.PendingApproval> values) {
      if (pendingApprovalsBuilder_ == null) {
        ensurePendingApprovalsIsMutable();
        com.google.protobuf.AbstractMessageLite.Builder.addAll(
            values, pendingApprovals_);
        onChanged();
      } else {
        pendingApprovalsBuilder_.addAllMessages(values);
      }
      return this;
    }
    /**
     * <pre>
     * Active pending approval requests for this execution.
     *
     * Contains one entry per tool call that requires user approval before
     * it can proceed. Sub-agent approvals are included with from_sub_agent
     * set to true. Empty when no approvals are pending.
     *
     * &#64;internal
     *
     * Computed server-side by the Go/Java UpdateStatus handlers on every write.
     * The handler scans messages[].tool_calls and
     * sub_agent_executions[].messages[].tool_calls, collecting entries where
     * status == WAITING_APPROVAL &amp;&amp; requires_approval == true, and projects
     * them into PendingApproval entries.
     *
     * Because this field is recomputed (not merged), it is always consistent
     * with the authoritative tool call state embedded in messages. No lifecycle
     * state machine, no merge logic, no forward-only enforcement needed.
     *
     * Lifecycle:
     * 1. The runner sets ToolCall.status = WAITING_APPROVAL on the message
     * 2. UpdateStatus handler recomputes this field, entry appears
     * 3. Phase changes to EXECUTION_WAITING_FOR_APPROVAL
     * 4. User submits decisions via SubmitApproval RPC (one per tool call)
     * 5. Agent resumes, ToolCall.status advances, next recompute drops the entry
     * </pre>
     *
     * <code>repeated .ai.stigmer.agentic.agentexecution.v1.PendingApproval pending_approvals = 16 [json_name = "pendingApprovals"];</code>
     */
    public Builder clearPendingApprovals() {
      if (pendingApprovalsBuilder_ == null) {
        pendingApprovals_ = java.util.Collections.emptyList();
        bitField0_ = (bitField0_ & ~0x00000200);
        onChanged();
      } else {
        pendingApprovalsBuilder_.clear();
      }
      return this;
    }
    /**
     * <pre>
     * Active pending approval requests for this execution.
     *
     * Contains one entry per tool call that requires user approval before
     * it can proceed. Sub-agent approvals are included with from_sub_agent
     * set to true. Empty when no approvals are pending.
     *
     * &#64;internal
     *
     * Computed server-side by the Go/Java UpdateStatus handlers on every write.
     * The handler scans messages[].tool_calls and
     * sub_agent_executions[].messages[].tool_calls, collecting entries where
     * status == WAITING_APPROVAL &amp;&amp; requires_approval == true, and projects
     * them into PendingApproval entries.
     *
     * Because this field is recomputed (not merged), it is always consistent
     * with the authoritative tool call state embedded in messages. No lifecycle
     * state machine, no merge logic, no forward-only enforcement needed.
     *
     * Lifecycle:
     * 1. The runner sets ToolCall.status = WAITING_APPROVAL on the message
     * 2. UpdateStatus handler recomputes this field, entry appears
     * 3. Phase changes to EXECUTION_WAITING_FOR_APPROVAL
     * 4. User submits decisions via SubmitApproval RPC (one per tool call)
     * 5. Agent resumes, ToolCall.status advances, next recompute drops the entry
     * </pre>
     *
     * <code>repeated .ai.stigmer.agentic.agentexecution.v1.PendingApproval pending_approvals = 16 [json_name = "pendingApprovals"];</code>
     */
    public Builder removePendingApprovals(int index) {
      if (pendingApprovalsBuilder_ == null) {
        ensurePendingApprovalsIsMutable();
        pendingApprovals_.remove(index);
        onChanged();
      } else {
        pendingApprovalsBuilder_.remove(index);
      }
      return this;
    }
    /**
     * <pre>
     * Active pending approval requests for this execution.
     *
     * Contains one entry per tool call that requires user approval before
     * it can proceed. Sub-agent approvals are included with from_sub_agent
     * set to true. Empty when no approvals are pending.
     *
     * &#64;internal
     *
     * Computed server-side by the Go/Java UpdateStatus handlers on every write.
     * The handler scans messages[].tool_calls and
     * sub_agent_executions[].messages[].tool_calls, collecting entries where
     * status == WAITING_APPROVAL &amp;&amp; requires_approval == true, and projects
     * them into PendingApproval entries.
     *
     * Because this field is recomputed (not merged), it is always consistent
     * with the authoritative tool call state embedded in messages. No lifecycle
     * state machine, no merge logic, no forward-only enforcement needed.
     *
     * Lifecycle:
     * 1. The runner sets ToolCall.status = WAITING_APPROVAL on the message
     * 2. UpdateStatus handler recomputes this field, entry appears
     * 3. Phase changes to EXECUTION_WAITING_FOR_APPROVAL
     * 4. User submits decisions via SubmitApproval RPC (one per tool call)
     * 5. Agent resumes, ToolCall.status advances, next recompute drops the entry
     * </pre>
     *
     * <code>repeated .ai.stigmer.agentic.agentexecution.v1.PendingApproval pending_approvals = 16 [json_name = "pendingApprovals"];</code>
     */
    public ai.stigmer.agentic.agentexecution.v1.PendingApproval.Builder getPendingApprovalsBuilder(
        int index) {
      return internalGetPendingApprovalsFieldBuilder().getBuilder(index);
    }
    /**
     * <pre>
     * Active pending approval requests for this execution.
     *
     * Contains one entry per tool call that requires user approval before
     * it can proceed. Sub-agent approvals are included with from_sub_agent
     * set to true. Empty when no approvals are pending.
     *
     * &#64;internal
     *
     * Computed server-side by the Go/Java UpdateStatus handlers on every write.
     * The handler scans messages[].tool_calls and
     * sub_agent_executions[].messages[].tool_calls, collecting entries where
     * status == WAITING_APPROVAL &amp;&amp; requires_approval == true, and projects
     * them into PendingApproval entries.
     *
     * Because this field is recomputed (not merged), it is always consistent
     * with the authoritative tool call state embedded in messages. No lifecycle
     * state machine, no merge logic, no forward-only enforcement needed.
     *
     * Lifecycle:
     * 1. The runner sets ToolCall.status = WAITING_APPROVAL on the message
     * 2. UpdateStatus handler recomputes this field, entry appears
     * 3. Phase changes to EXECUTION_WAITING_FOR_APPROVAL
     * 4. User submits decisions via SubmitApproval RPC (one per tool call)
     * 5. Agent resumes, ToolCall.status advances, next recompute drops the entry
     * </pre>
     *
     * <code>repeated .ai.stigmer.agentic.agentexecution.v1.PendingApproval pending_approvals = 16 [json_name = "pendingApprovals"];</code>
     */
    public ai.stigmer.agentic.agentexecution.v1.PendingApprovalOrBuilder getPendingApprovalsOrBuilder(
        int index) {
      if (pendingApprovalsBuilder_ == null) {
        return pendingApprovals_.get(index);  } else {
        return pendingApprovalsBuilder_.getMessageOrBuilder(index);
      }
    }
    /**
     * <pre>
     * Active pending approval requests for this execution.
     *
     * Contains one entry per tool call that requires user approval before
     * it can proceed. Sub-agent approvals are included with from_sub_agent
     * set to true. Empty when no approvals are pending.
     *
     * &#64;internal
     *
     * Computed server-side by the Go/Java UpdateStatus handlers on every write.
     * The handler scans messages[].tool_calls and
     * sub_agent_executions[].messages[].tool_calls, collecting entries where
     * status == WAITING_APPROVAL &amp;&amp; requires_approval == true, and projects
     * them into PendingApproval entries.
     *
     * Because this field is recomputed (not merged), it is always consistent
     * with the authoritative tool call state embedded in messages. No lifecycle
     * state machine, no merge logic, no forward-only enforcement needed.
     *
     * Lifecycle:
     * 1. The runner sets ToolCall.status = WAITING_APPROVAL on the message
     * 2. UpdateStatus handler recomputes this field, entry appears
     * 3. Phase changes to EXECUTION_WAITING_FOR_APPROVAL
     * 4. User submits decisions via SubmitApproval RPC (one per tool call)
     * 5. Agent resumes, ToolCall.status advances, next recompute drops the entry
     * </pre>
     *
     * <code>repeated .ai.stigmer.agentic.agentexecution.v1.PendingApproval pending_approvals = 16 [json_name = "pendingApprovals"];</code>
     */
    public java.util.List<? extends ai.stigmer.agentic.agentexecution.v1.PendingApprovalOrBuilder> 
         getPendingApprovalsOrBuilderList() {
      if (pendingApprovalsBuilder_ != null) {
        return pendingApprovalsBuilder_.getMessageOrBuilderList();
      } else {
        return java.util.Collections.unmodifiableList(pendingApprovals_);
      }
    }
    /**
     * <pre>
     * Active pending approval requests for this execution.
     *
     * Contains one entry per tool call that requires user approval before
     * it can proceed. Sub-agent approvals are included with from_sub_agent
     * set to true. Empty when no approvals are pending.
     *
     * &#64;internal
     *
     * Computed server-side by the Go/Java UpdateStatus handlers on every write.
     * The handler scans messages[].tool_calls and
     * sub_agent_executions[].messages[].tool_calls, collecting entries where
     * status == WAITING_APPROVAL &amp;&amp; requires_approval == true, and projects
     * them into PendingApproval entries.
     *
     * Because this field is recomputed (not merged), it is always consistent
     * with the authoritative tool call state embedded in messages. No lifecycle
     * state machine, no merge logic, no forward-only enforcement needed.
     *
     * Lifecycle:
     * 1. The runner sets ToolCall.status = WAITING_APPROVAL on the message
     * 2. UpdateStatus handler recomputes this field, entry appears
     * 3. Phase changes to EXECUTION_WAITING_FOR_APPROVAL
     * 4. User submits decisions via SubmitApproval RPC (one per tool call)
     * 5. Agent resumes, ToolCall.status advances, next recompute drops the entry
     * </pre>
     *
     * <code>repeated .ai.stigmer.agentic.agentexecution.v1.PendingApproval pending_approvals = 16 [json_name = "pendingApprovals"];</code>
     */
    public ai.stigmer.agentic.agentexecution.v1.PendingApproval.Builder addPendingApprovalsBuilder() {
      return internalGetPendingApprovalsFieldBuilder().addBuilder(
          ai.stigmer.agentic.agentexecution.v1.PendingApproval.getDefaultInstance());
    }
    /**
     * <pre>
     * Active pending approval requests for this execution.
     *
     * Contains one entry per tool call that requires user approval before
     * it can proceed. Sub-agent approvals are included with from_sub_agent
     * set to true. Empty when no approvals are pending.
     *
     * &#64;internal
     *
     * Computed server-side by the Go/Java UpdateStatus handlers on every write.
     * The handler scans messages[].tool_calls and
     * sub_agent_executions[].messages[].tool_calls, collecting entries where
     * status == WAITING_APPROVAL &amp;&amp; requires_approval == true, and projects
     * them into PendingApproval entries.
     *
     * Because this field is recomputed (not merged), it is always consistent
     * with the authoritative tool call state embedded in messages. No lifecycle
     * state machine, no merge logic, no forward-only enforcement needed.
     *
     * Lifecycle:
     * 1. The runner sets ToolCall.status = WAITING_APPROVAL on the message
     * 2. UpdateStatus handler recomputes this field, entry appears
     * 3. Phase changes to EXECUTION_WAITING_FOR_APPROVAL
     * 4. User submits decisions via SubmitApproval RPC (one per tool call)
     * 5. Agent resumes, ToolCall.status advances, next recompute drops the entry
     * </pre>
     *
     * <code>repeated .ai.stigmer.agentic.agentexecution.v1.PendingApproval pending_approvals = 16 [json_name = "pendingApprovals"];</code>
     */
    public ai.stigmer.agentic.agentexecution.v1.PendingApproval.Builder addPendingApprovalsBuilder(
        int index) {
      return internalGetPendingApprovalsFieldBuilder().addBuilder(
          index, ai.stigmer.agentic.agentexecution.v1.PendingApproval.getDefaultInstance());
    }
    /**
     * <pre>
     * Active pending approval requests for this execution.
     *
     * Contains one entry per tool call that requires user approval before
     * it can proceed. Sub-agent approvals are included with from_sub_agent
     * set to true. Empty when no approvals are pending.
     *
     * &#64;internal
     *
     * Computed server-side by the Go/Java UpdateStatus handlers on every write.
     * The handler scans messages[].tool_calls and
     * sub_agent_executions[].messages[].tool_calls, collecting entries where
     * status == WAITING_APPROVAL &amp;&amp; requires_approval == true, and projects
     * them into PendingApproval entries.
     *
     * Because this field is recomputed (not merged), it is always consistent
     * with the authoritative tool call state embedded in messages. No lifecycle
     * state machine, no merge logic, no forward-only enforcement needed.
     *
     * Lifecycle:
     * 1. The runner sets ToolCall.status = WAITING_APPROVAL on the message
     * 2. UpdateStatus handler recomputes this field, entry appears
     * 3. Phase changes to EXECUTION_WAITING_FOR_APPROVAL
     * 4. User submits decisions via SubmitApproval RPC (one per tool call)
     * 5. Agent resumes, ToolCall.status advances, next recompute drops the entry
     * </pre>
     *
     * <code>repeated .ai.stigmer.agentic.agentexecution.v1.PendingApproval pending_approvals = 16 [json_name = "pendingApprovals"];</code>
     */
    public java.util.List<ai.stigmer.agentic.agentexecution.v1.PendingApproval.Builder> 
         getPendingApprovalsBuilderList() {
      return internalGetPendingApprovalsFieldBuilder().getBuilderList();
    }
    private com.google.protobuf.RepeatedFieldBuilder<
        ai.stigmer.agentic.agentexecution.v1.PendingApproval, ai.stigmer.agentic.agentexecution.v1.PendingApproval.Builder, ai.stigmer.agentic.agentexecution.v1.PendingApprovalOrBuilder> 
        internalGetPendingApprovalsFieldBuilder() {
      if (pendingApprovalsBuilder_ == null) {
        pendingApprovalsBuilder_ = new com.google.protobuf.RepeatedFieldBuilder<
            ai.stigmer.agentic.agentexecution.v1.PendingApproval, ai.stigmer.agentic.agentexecution.v1.PendingApproval.Builder, ai.stigmer.agentic.agentexecution.v1.PendingApprovalOrBuilder>(
                pendingApprovals_,
                ((bitField0_ & 0x00000200) != 0),
                getParentForChildren(),
                isClean());
        pendingApprovals_ = null;
      }
      return pendingApprovalsBuilder_;
    }

    private ai.stigmer.agentic.agentexecution.v1.ApprovalEventStream approvalEventStream_;
    private com.google.protobuf.SingleFieldBuilder<
        ai.stigmer.agentic.agentexecution.v1.ApprovalEventStream, ai.stigmer.agentic.agentexecution.v1.ApprovalEventStream.Builder, ai.stigmer.agentic.agentexecution.v1.ApprovalEventStreamOrBuilder> approvalEventStreamBuilder_;
    /**
     * <pre>
     * Server-authored, append-only record of every approval request and decision.
     *
     * &#64;internal
     *
     * This is the persisted form of the append-only stream that is the target
     * single source of truth for approvals (see ApprovalEventStream in
     * approval.proto). It is NOT yet read by any surface: pending_approvals is
     * still projected from the authoritative message scan, and the UI/SDK read
     * that. The stream coexists as a parallel, independently-authored record so
     * that (1) the message-scan vs event-stream projection parity check becomes a
     * real cross-writer regression guard rather than a tautology, and (2) the
     * approval audit trail — who decided, when, with what comment — has a home,
     * since the flat ToolCall.approval_action / approval_decided_at fields cannot
     * capture a decision comment at all.
     *
     * Field ownership (one writer per event type):
     * - REQUESTED events are authored by the UpdateStatus handlers when a tool
     * call enters WAITING_APPROVAL (seeded once from the message scan for
     * executions that predate this field).
     * - Decision events (APPROVED / REJECTED / SKIPPED) are authored by the
     * SubmitApproval handler, carrying decided_by and the user's comment.
     *
     * Every append is keyed by the deterministic ApprovalEvent.event_id, so the
     * stream is idempotent under retries and the rich decision event (authored in
     * the same operation that records the decision on the message scan) can never
     * be duplicated or overwritten by a coarse re-derivation.
     *
     * Because it is server-only, the runner never sends it; it is preserved
     * across UpdateStatus writes automatically (the merge starts from the loaded
     * execution and only replaces runner-owned fields).
     * </pre>
     *
     * <code>.ai.stigmer.agentic.agentexecution.v1.ApprovalEventStream approval_event_stream = 22 [json_name = "approvalEventStream"];</code>
     * @return Whether the approvalEventStream field is set.
     */
    public boolean hasApprovalEventStream() {
      return ((bitField0_ & 0x00000400) != 0);
    }
    /**
     * <pre>
     * Server-authored, append-only record of every approval request and decision.
     *
     * &#64;internal
     *
     * This is the persisted form of the append-only stream that is the target
     * single source of truth for approvals (see ApprovalEventStream in
     * approval.proto). It is NOT yet read by any surface: pending_approvals is
     * still projected from the authoritative message scan, and the UI/SDK read
     * that. The stream coexists as a parallel, independently-authored record so
     * that (1) the message-scan vs event-stream projection parity check becomes a
     * real cross-writer regression guard rather than a tautology, and (2) the
     * approval audit trail — who decided, when, with what comment — has a home,
     * since the flat ToolCall.approval_action / approval_decided_at fields cannot
     * capture a decision comment at all.
     *
     * Field ownership (one writer per event type):
     * - REQUESTED events are authored by the UpdateStatus handlers when a tool
     * call enters WAITING_APPROVAL (seeded once from the message scan for
     * executions that predate this field).
     * - Decision events (APPROVED / REJECTED / SKIPPED) are authored by the
     * SubmitApproval handler, carrying decided_by and the user's comment.
     *
     * Every append is keyed by the deterministic ApprovalEvent.event_id, so the
     * stream is idempotent under retries and the rich decision event (authored in
     * the same operation that records the decision on the message scan) can never
     * be duplicated or overwritten by a coarse re-derivation.
     *
     * Because it is server-only, the runner never sends it; it is preserved
     * across UpdateStatus writes automatically (the merge starts from the loaded
     * execution and only replaces runner-owned fields).
     * </pre>
     *
     * <code>.ai.stigmer.agentic.agentexecution.v1.ApprovalEventStream approval_event_stream = 22 [json_name = "approvalEventStream"];</code>
     * @return The approvalEventStream.
     */
    public ai.stigmer.agentic.agentexecution.v1.ApprovalEventStream getApprovalEventStream() {
      if (approvalEventStreamBuilder_ == null) {
        return approvalEventStream_ == null ? ai.stigmer.agentic.agentexecution.v1.ApprovalEventStream.getDefaultInstance() : approvalEventStream_;
      } else {
        return approvalEventStreamBuilder_.getMessage();
      }
    }
    /**
     * <pre>
     * Server-authored, append-only record of every approval request and decision.
     *
     * &#64;internal
     *
     * This is the persisted form of the append-only stream that is the target
     * single source of truth for approvals (see ApprovalEventStream in
     * approval.proto). It is NOT yet read by any surface: pending_approvals is
     * still projected from the authoritative message scan, and the UI/SDK read
     * that. The stream coexists as a parallel, independently-authored record so
     * that (1) the message-scan vs event-stream projection parity check becomes a
     * real cross-writer regression guard rather than a tautology, and (2) the
     * approval audit trail — who decided, when, with what comment — has a home,
     * since the flat ToolCall.approval_action / approval_decided_at fields cannot
     * capture a decision comment at all.
     *
     * Field ownership (one writer per event type):
     * - REQUESTED events are authored by the UpdateStatus handlers when a tool
     * call enters WAITING_APPROVAL (seeded once from the message scan for
     * executions that predate this field).
     * - Decision events (APPROVED / REJECTED / SKIPPED) are authored by the
     * SubmitApproval handler, carrying decided_by and the user's comment.
     *
     * Every append is keyed by the deterministic ApprovalEvent.event_id, so the
     * stream is idempotent under retries and the rich decision event (authored in
     * the same operation that records the decision on the message scan) can never
     * be duplicated or overwritten by a coarse re-derivation.
     *
     * Because it is server-only, the runner never sends it; it is preserved
     * across UpdateStatus writes automatically (the merge starts from the loaded
     * execution and only replaces runner-owned fields).
     * </pre>
     *
     * <code>.ai.stigmer.agentic.agentexecution.v1.ApprovalEventStream approval_event_stream = 22 [json_name = "approvalEventStream"];</code>
     */
    public Builder setApprovalEventStream(ai.stigmer.agentic.agentexecution.v1.ApprovalEventStream value) {
      if (approvalEventStreamBuilder_ == null) {
        if (value == null) {
          throw new NullPointerException();
        }
        approvalEventStream_ = value;
      } else {
        approvalEventStreamBuilder_.setMessage(value);
      }
      bitField0_ |= 0x00000400;
      onChanged();
      return this;
    }
    /**
     * <pre>
     * Server-authored, append-only record of every approval request and decision.
     *
     * &#64;internal
     *
     * This is the persisted form of the append-only stream that is the target
     * single source of truth for approvals (see ApprovalEventStream in
     * approval.proto). It is NOT yet read by any surface: pending_approvals is
     * still projected from the authoritative message scan, and the UI/SDK read
     * that. The stream coexists as a parallel, independently-authored record so
     * that (1) the message-scan vs event-stream projection parity check becomes a
     * real cross-writer regression guard rather than a tautology, and (2) the
     * approval audit trail — who decided, when, with what comment — has a home,
     * since the flat ToolCall.approval_action / approval_decided_at fields cannot
     * capture a decision comment at all.
     *
     * Field ownership (one writer per event type):
     * - REQUESTED events are authored by the UpdateStatus handlers when a tool
     * call enters WAITING_APPROVAL (seeded once from the message scan for
     * executions that predate this field).
     * - Decision events (APPROVED / REJECTED / SKIPPED) are authored by the
     * SubmitApproval handler, carrying decided_by and the user's comment.
     *
     * Every append is keyed by the deterministic ApprovalEvent.event_id, so the
     * stream is idempotent under retries and the rich decision event (authored in
     * the same operation that records the decision on the message scan) can never
     * be duplicated or overwritten by a coarse re-derivation.
     *
     * Because it is server-only, the runner never sends it; it is preserved
     * across UpdateStatus writes automatically (the merge starts from the loaded
     * execution and only replaces runner-owned fields).
     * </pre>
     *
     * <code>.ai.stigmer.agentic.agentexecution.v1.ApprovalEventStream approval_event_stream = 22 [json_name = "approvalEventStream"];</code>
     */
    public Builder setApprovalEventStream(
        ai.stigmer.agentic.agentexecution.v1.ApprovalEventStream.Builder builderForValue) {
      if (approvalEventStreamBuilder_ == null) {
        approvalEventStream_ = builderForValue.build();
      } else {
        approvalEventStreamBuilder_.setMessage(builderForValue.build());
      }
      bitField0_ |= 0x00000400;
      onChanged();
      return this;
    }
    /**
     * <pre>
     * Server-authored, append-only record of every approval request and decision.
     *
     * &#64;internal
     *
     * This is the persisted form of the append-only stream that is the target
     * single source of truth for approvals (see ApprovalEventStream in
     * approval.proto). It is NOT yet read by any surface: pending_approvals is
     * still projected from the authoritative message scan, and the UI/SDK read
     * that. The stream coexists as a parallel, independently-authored record so
     * that (1) the message-scan vs event-stream projection parity check becomes a
     * real cross-writer regression guard rather than a tautology, and (2) the
     * approval audit trail — who decided, when, with what comment — has a home,
     * since the flat ToolCall.approval_action / approval_decided_at fields cannot
     * capture a decision comment at all.
     *
     * Field ownership (one writer per event type):
     * - REQUESTED events are authored by the UpdateStatus handlers when a tool
     * call enters WAITING_APPROVAL (seeded once from the message scan for
     * executions that predate this field).
     * - Decision events (APPROVED / REJECTED / SKIPPED) are authored by the
     * SubmitApproval handler, carrying decided_by and the user's comment.
     *
     * Every append is keyed by the deterministic ApprovalEvent.event_id, so the
     * stream is idempotent under retries and the rich decision event (authored in
     * the same operation that records the decision on the message scan) can never
     * be duplicated or overwritten by a coarse re-derivation.
     *
     * Because it is server-only, the runner never sends it; it is preserved
     * across UpdateStatus writes automatically (the merge starts from the loaded
     * execution and only replaces runner-owned fields).
     * </pre>
     *
     * <code>.ai.stigmer.agentic.agentexecution.v1.ApprovalEventStream approval_event_stream = 22 [json_name = "approvalEventStream"];</code>
     */
    public Builder mergeApprovalEventStream(ai.stigmer.agentic.agentexecution.v1.ApprovalEventStream value) {
      if (approvalEventStreamBuilder_ == null) {
        if (((bitField0_ & 0x00000400) != 0) &&
          approvalEventStream_ != null &&
          approvalEventStream_ != ai.stigmer.agentic.agentexecution.v1.ApprovalEventStream.getDefaultInstance()) {
          getApprovalEventStreamBuilder().mergeFrom(value);
        } else {
          approvalEventStream_ = value;
        }
      } else {
        approvalEventStreamBuilder_.mergeFrom(value);
      }
      if (approvalEventStream_ != null) {
        bitField0_ |= 0x00000400;
        onChanged();
      }
      return this;
    }
    /**
     * <pre>
     * Server-authored, append-only record of every approval request and decision.
     *
     * &#64;internal
     *
     * This is the persisted form of the append-only stream that is the target
     * single source of truth for approvals (see ApprovalEventStream in
     * approval.proto). It is NOT yet read by any surface: pending_approvals is
     * still projected from the authoritative message scan, and the UI/SDK read
     * that. The stream coexists as a parallel, independently-authored record so
     * that (1) the message-scan vs event-stream projection parity check becomes a
     * real cross-writer regression guard rather than a tautology, and (2) the
     * approval audit trail — who decided, when, with what comment — has a home,
     * since the flat ToolCall.approval_action / approval_decided_at fields cannot
     * capture a decision comment at all.
     *
     * Field ownership (one writer per event type):
     * - REQUESTED events are authored by the UpdateStatus handlers when a tool
     * call enters WAITING_APPROVAL (seeded once from the message scan for
     * executions that predate this field).
     * - Decision events (APPROVED / REJECTED / SKIPPED) are authored by the
     * SubmitApproval handler, carrying decided_by and the user's comment.
     *
     * Every append is keyed by the deterministic ApprovalEvent.event_id, so the
     * stream is idempotent under retries and the rich decision event (authored in
     * the same operation that records the decision on the message scan) can never
     * be duplicated or overwritten by a coarse re-derivation.
     *
     * Because it is server-only, the runner never sends it; it is preserved
     * across UpdateStatus writes automatically (the merge starts from the loaded
     * execution and only replaces runner-owned fields).
     * </pre>
     *
     * <code>.ai.stigmer.agentic.agentexecution.v1.ApprovalEventStream approval_event_stream = 22 [json_name = "approvalEventStream"];</code>
     */
    public Builder clearApprovalEventStream() {
      bitField0_ = (bitField0_ & ~0x00000400);
      approvalEventStream_ = null;
      if (approvalEventStreamBuilder_ != null) {
        approvalEventStreamBuilder_.dispose();
        approvalEventStreamBuilder_ = null;
      }
      onChanged();
      return this;
    }
    /**
     * <pre>
     * Server-authored, append-only record of every approval request and decision.
     *
     * &#64;internal
     *
     * This is the persisted form of the append-only stream that is the target
     * single source of truth for approvals (see ApprovalEventStream in
     * approval.proto). It is NOT yet read by any surface: pending_approvals is
     * still projected from the authoritative message scan, and the UI/SDK read
     * that. The stream coexists as a parallel, independently-authored record so
     * that (1) the message-scan vs event-stream projection parity check becomes a
     * real cross-writer regression guard rather than a tautology, and (2) the
     * approval audit trail — who decided, when, with what comment — has a home,
     * since the flat ToolCall.approval_action / approval_decided_at fields cannot
     * capture a decision comment at all.
     *
     * Field ownership (one writer per event type):
     * - REQUESTED events are authored by the UpdateStatus handlers when a tool
     * call enters WAITING_APPROVAL (seeded once from the message scan for
     * executions that predate this field).
     * - Decision events (APPROVED / REJECTED / SKIPPED) are authored by the
     * SubmitApproval handler, carrying decided_by and the user's comment.
     *
     * Every append is keyed by the deterministic ApprovalEvent.event_id, so the
     * stream is idempotent under retries and the rich decision event (authored in
     * the same operation that records the decision on the message scan) can never
     * be duplicated or overwritten by a coarse re-derivation.
     *
     * Because it is server-only, the runner never sends it; it is preserved
     * across UpdateStatus writes automatically (the merge starts from the loaded
     * execution and only replaces runner-owned fields).
     * </pre>
     *
     * <code>.ai.stigmer.agentic.agentexecution.v1.ApprovalEventStream approval_event_stream = 22 [json_name = "approvalEventStream"];</code>
     */
    public ai.stigmer.agentic.agentexecution.v1.ApprovalEventStream.Builder getApprovalEventStreamBuilder() {
      bitField0_ |= 0x00000400;
      onChanged();
      return internalGetApprovalEventStreamFieldBuilder().getBuilder();
    }
    /**
     * <pre>
     * Server-authored, append-only record of every approval request and decision.
     *
     * &#64;internal
     *
     * This is the persisted form of the append-only stream that is the target
     * single source of truth for approvals (see ApprovalEventStream in
     * approval.proto). It is NOT yet read by any surface: pending_approvals is
     * still projected from the authoritative message scan, and the UI/SDK read
     * that. The stream coexists as a parallel, independently-authored record so
     * that (1) the message-scan vs event-stream projection parity check becomes a
     * real cross-writer regression guard rather than a tautology, and (2) the
     * approval audit trail — who decided, when, with what comment — has a home,
     * since the flat ToolCall.approval_action / approval_decided_at fields cannot
     * capture a decision comment at all.
     *
     * Field ownership (one writer per event type):
     * - REQUESTED events are authored by the UpdateStatus handlers when a tool
     * call enters WAITING_APPROVAL (seeded once from the message scan for
     * executions that predate this field).
     * - Decision events (APPROVED / REJECTED / SKIPPED) are authored by the
     * SubmitApproval handler, carrying decided_by and the user's comment.
     *
     * Every append is keyed by the deterministic ApprovalEvent.event_id, so the
     * stream is idempotent under retries and the rich decision event (authored in
     * the same operation that records the decision on the message scan) can never
     * be duplicated or overwritten by a coarse re-derivation.
     *
     * Because it is server-only, the runner never sends it; it is preserved
     * across UpdateStatus writes automatically (the merge starts from the loaded
     * execution and only replaces runner-owned fields).
     * </pre>
     *
     * <code>.ai.stigmer.agentic.agentexecution.v1.ApprovalEventStream approval_event_stream = 22 [json_name = "approvalEventStream"];</code>
     */
    public ai.stigmer.agentic.agentexecution.v1.ApprovalEventStreamOrBuilder getApprovalEventStreamOrBuilder() {
      if (approvalEventStreamBuilder_ != null) {
        return approvalEventStreamBuilder_.getMessageOrBuilder();
      } else {
        return approvalEventStream_ == null ?
            ai.stigmer.agentic.agentexecution.v1.ApprovalEventStream.getDefaultInstance() : approvalEventStream_;
      }
    }
    /**
     * <pre>
     * Server-authored, append-only record of every approval request and decision.
     *
     * &#64;internal
     *
     * This is the persisted form of the append-only stream that is the target
     * single source of truth for approvals (see ApprovalEventStream in
     * approval.proto). It is NOT yet read by any surface: pending_approvals is
     * still projected from the authoritative message scan, and the UI/SDK read
     * that. The stream coexists as a parallel, independently-authored record so
     * that (1) the message-scan vs event-stream projection parity check becomes a
     * real cross-writer regression guard rather than a tautology, and (2) the
     * approval audit trail — who decided, when, with what comment — has a home,
     * since the flat ToolCall.approval_action / approval_decided_at fields cannot
     * capture a decision comment at all.
     *
     * Field ownership (one writer per event type):
     * - REQUESTED events are authored by the UpdateStatus handlers when a tool
     * call enters WAITING_APPROVAL (seeded once from the message scan for
     * executions that predate this field).
     * - Decision events (APPROVED / REJECTED / SKIPPED) are authored by the
     * SubmitApproval handler, carrying decided_by and the user's comment.
     *
     * Every append is keyed by the deterministic ApprovalEvent.event_id, so the
     * stream is idempotent under retries and the rich decision event (authored in
     * the same operation that records the decision on the message scan) can never
     * be duplicated or overwritten by a coarse re-derivation.
     *
     * Because it is server-only, the runner never sends it; it is preserved
     * across UpdateStatus writes automatically (the merge starts from the loaded
     * execution and only replaces runner-owned fields).
     * </pre>
     *
     * <code>.ai.stigmer.agentic.agentexecution.v1.ApprovalEventStream approval_event_stream = 22 [json_name = "approvalEventStream"];</code>
     */
    private com.google.protobuf.SingleFieldBuilder<
        ai.stigmer.agentic.agentexecution.v1.ApprovalEventStream, ai.stigmer.agentic.agentexecution.v1.ApprovalEventStream.Builder, ai.stigmer.agentic.agentexecution.v1.ApprovalEventStreamOrBuilder> 
        internalGetApprovalEventStreamFieldBuilder() {
      if (approvalEventStreamBuilder_ == null) {
        approvalEventStreamBuilder_ = new com.google.protobuf.SingleFieldBuilder<
            ai.stigmer.agentic.agentexecution.v1.ApprovalEventStream, ai.stigmer.agentic.agentexecution.v1.ApprovalEventStream.Builder, ai.stigmer.agentic.agentexecution.v1.ApprovalEventStreamOrBuilder>(
                getApprovalEventStream(),
                getParentForChildren(),
                isClean());
        approvalEventStream_ = null;
      }
      return approvalEventStreamBuilder_;
    }

    private ai.stigmer.agentic.agentexecution.v1.ContextInfo contextInfo_;
    private com.google.protobuf.SingleFieldBuilder<
        ai.stigmer.agentic.agentexecution.v1.ContextInfo, ai.stigmer.agentic.agentexecution.v1.ContextInfo.Builder, ai.stigmer.agentic.agentexecution.v1.ContextInfoOrBuilder> contextInfoBuilder_;
    /**
     * <pre>
     * Context window utilization and summarization tracking.
     *
     * Provides visibility into how the agent is using its context window and
     * any summarization events that occurred during execution.
     *
     * Populated when context management is active (not disabled via config).
     * Updated progressively during streaming as token counts change.
     *
     * Use cases:
     * - **Monitoring**: Track context utilization across executions
     * - **Debugging**: Understand why an execution hit context limits
     * - **Cost optimization**: Identify executions with excessive summarization
     * - **UX**: Show users their context window health
     *
     * &#64;since Phase 3 (Context Summarization Architecture)
     * </pre>
     *
     * <code>.ai.stigmer.agentic.agentexecution.v1.ContextInfo context_info = 14 [json_name = "contextInfo"];</code>
     * @return Whether the contextInfo field is set.
     */
    public boolean hasContextInfo() {
      return ((bitField0_ & 0x00000800) != 0);
    }
    /**
     * <pre>
     * Context window utilization and summarization tracking.
     *
     * Provides visibility into how the agent is using its context window and
     * any summarization events that occurred during execution.
     *
     * Populated when context management is active (not disabled via config).
     * Updated progressively during streaming as token counts change.
     *
     * Use cases:
     * - **Monitoring**: Track context utilization across executions
     * - **Debugging**: Understand why an execution hit context limits
     * - **Cost optimization**: Identify executions with excessive summarization
     * - **UX**: Show users their context window health
     *
     * &#64;since Phase 3 (Context Summarization Architecture)
     * </pre>
     *
     * <code>.ai.stigmer.agentic.agentexecution.v1.ContextInfo context_info = 14 [json_name = "contextInfo"];</code>
     * @return The contextInfo.
     */
    public ai.stigmer.agentic.agentexecution.v1.ContextInfo getContextInfo() {
      if (contextInfoBuilder_ == null) {
        return contextInfo_ == null ? ai.stigmer.agentic.agentexecution.v1.ContextInfo.getDefaultInstance() : contextInfo_;
      } else {
        return contextInfoBuilder_.getMessage();
      }
    }
    /**
     * <pre>
     * Context window utilization and summarization tracking.
     *
     * Provides visibility into how the agent is using its context window and
     * any summarization events that occurred during execution.
     *
     * Populated when context management is active (not disabled via config).
     * Updated progressively during streaming as token counts change.
     *
     * Use cases:
     * - **Monitoring**: Track context utilization across executions
     * - **Debugging**: Understand why an execution hit context limits
     * - **Cost optimization**: Identify executions with excessive summarization
     * - **UX**: Show users their context window health
     *
     * &#64;since Phase 3 (Context Summarization Architecture)
     * </pre>
     *
     * <code>.ai.stigmer.agentic.agentexecution.v1.ContextInfo context_info = 14 [json_name = "contextInfo"];</code>
     */
    public Builder setContextInfo(ai.stigmer.agentic.agentexecution.v1.ContextInfo value) {
      if (contextInfoBuilder_ == null) {
        if (value == null) {
          throw new NullPointerException();
        }
        contextInfo_ = value;
      } else {
        contextInfoBuilder_.setMessage(value);
      }
      bitField0_ |= 0x00000800;
      onChanged();
      return this;
    }
    /**
     * <pre>
     * Context window utilization and summarization tracking.
     *
     * Provides visibility into how the agent is using its context window and
     * any summarization events that occurred during execution.
     *
     * Populated when context management is active (not disabled via config).
     * Updated progressively during streaming as token counts change.
     *
     * Use cases:
     * - **Monitoring**: Track context utilization across executions
     * - **Debugging**: Understand why an execution hit context limits
     * - **Cost optimization**: Identify executions with excessive summarization
     * - **UX**: Show users their context window health
     *
     * &#64;since Phase 3 (Context Summarization Architecture)
     * </pre>
     *
     * <code>.ai.stigmer.agentic.agentexecution.v1.ContextInfo context_info = 14 [json_name = "contextInfo"];</code>
     */
    public Builder setContextInfo(
        ai.stigmer.agentic.agentexecution.v1.ContextInfo.Builder builderForValue) {
      if (contextInfoBuilder_ == null) {
        contextInfo_ = builderForValue.build();
      } else {
        contextInfoBuilder_.setMessage(builderForValue.build());
      }
      bitField0_ |= 0x00000800;
      onChanged();
      return this;
    }
    /**
     * <pre>
     * Context window utilization and summarization tracking.
     *
     * Provides visibility into how the agent is using its context window and
     * any summarization events that occurred during execution.
     *
     * Populated when context management is active (not disabled via config).
     * Updated progressively during streaming as token counts change.
     *
     * Use cases:
     * - **Monitoring**: Track context utilization across executions
     * - **Debugging**: Understand why an execution hit context limits
     * - **Cost optimization**: Identify executions with excessive summarization
     * - **UX**: Show users their context window health
     *
     * &#64;since Phase 3 (Context Summarization Architecture)
     * </pre>
     *
     * <code>.ai.stigmer.agentic.agentexecution.v1.ContextInfo context_info = 14 [json_name = "contextInfo"];</code>
     */
    public Builder mergeContextInfo(ai.stigmer.agentic.agentexecution.v1.ContextInfo value) {
      if (contextInfoBuilder_ == null) {
        if (((bitField0_ & 0x00000800) != 0) &&
          contextInfo_ != null &&
          contextInfo_ != ai.stigmer.agentic.agentexecution.v1.ContextInfo.getDefaultInstance()) {
          getContextInfoBuilder().mergeFrom(value);
        } else {
          contextInfo_ = value;
        }
      } else {
        contextInfoBuilder_.mergeFrom(value);
      }
      if (contextInfo_ != null) {
        bitField0_ |= 0x00000800;
        onChanged();
      }
      return this;
    }
    /**
     * <pre>
     * Context window utilization and summarization tracking.
     *
     * Provides visibility into how the agent is using its context window and
     * any summarization events that occurred during execution.
     *
     * Populated when context management is active (not disabled via config).
     * Updated progressively during streaming as token counts change.
     *
     * Use cases:
     * - **Monitoring**: Track context utilization across executions
     * - **Debugging**: Understand why an execution hit context limits
     * - **Cost optimization**: Identify executions with excessive summarization
     * - **UX**: Show users their context window health
     *
     * &#64;since Phase 3 (Context Summarization Architecture)
     * </pre>
     *
     * <code>.ai.stigmer.agentic.agentexecution.v1.ContextInfo context_info = 14 [json_name = "contextInfo"];</code>
     */
    public Builder clearContextInfo() {
      bitField0_ = (bitField0_ & ~0x00000800);
      contextInfo_ = null;
      if (contextInfoBuilder_ != null) {
        contextInfoBuilder_.dispose();
        contextInfoBuilder_ = null;
      }
      onChanged();
      return this;
    }
    /**
     * <pre>
     * Context window utilization and summarization tracking.
     *
     * Provides visibility into how the agent is using its context window and
     * any summarization events that occurred during execution.
     *
     * Populated when context management is active (not disabled via config).
     * Updated progressively during streaming as token counts change.
     *
     * Use cases:
     * - **Monitoring**: Track context utilization across executions
     * - **Debugging**: Understand why an execution hit context limits
     * - **Cost optimization**: Identify executions with excessive summarization
     * - **UX**: Show users their context window health
     *
     * &#64;since Phase 3 (Context Summarization Architecture)
     * </pre>
     *
     * <code>.ai.stigmer.agentic.agentexecution.v1.ContextInfo context_info = 14 [json_name = "contextInfo"];</code>
     */
    public ai.stigmer.agentic.agentexecution.v1.ContextInfo.Builder getContextInfoBuilder() {
      bitField0_ |= 0x00000800;
      onChanged();
      return internalGetContextInfoFieldBuilder().getBuilder();
    }
    /**
     * <pre>
     * Context window utilization and summarization tracking.
     *
     * Provides visibility into how the agent is using its context window and
     * any summarization events that occurred during execution.
     *
     * Populated when context management is active (not disabled via config).
     * Updated progressively during streaming as token counts change.
     *
     * Use cases:
     * - **Monitoring**: Track context utilization across executions
     * - **Debugging**: Understand why an execution hit context limits
     * - **Cost optimization**: Identify executions with excessive summarization
     * - **UX**: Show users their context window health
     *
     * &#64;since Phase 3 (Context Summarization Architecture)
     * </pre>
     *
     * <code>.ai.stigmer.agentic.agentexecution.v1.ContextInfo context_info = 14 [json_name = "contextInfo"];</code>
     */
    public ai.stigmer.agentic.agentexecution.v1.ContextInfoOrBuilder getContextInfoOrBuilder() {
      if (contextInfoBuilder_ != null) {
        return contextInfoBuilder_.getMessageOrBuilder();
      } else {
        return contextInfo_ == null ?
            ai.stigmer.agentic.agentexecution.v1.ContextInfo.getDefaultInstance() : contextInfo_;
      }
    }
    /**
     * <pre>
     * Context window utilization and summarization tracking.
     *
     * Provides visibility into how the agent is using its context window and
     * any summarization events that occurred during execution.
     *
     * Populated when context management is active (not disabled via config).
     * Updated progressively during streaming as token counts change.
     *
     * Use cases:
     * - **Monitoring**: Track context utilization across executions
     * - **Debugging**: Understand why an execution hit context limits
     * - **Cost optimization**: Identify executions with excessive summarization
     * - **UX**: Show users their context window health
     *
     * &#64;since Phase 3 (Context Summarization Architecture)
     * </pre>
     *
     * <code>.ai.stigmer.agentic.agentexecution.v1.ContextInfo context_info = 14 [json_name = "contextInfo"];</code>
     */
    private com.google.protobuf.SingleFieldBuilder<
        ai.stigmer.agentic.agentexecution.v1.ContextInfo, ai.stigmer.agentic.agentexecution.v1.ContextInfo.Builder, ai.stigmer.agentic.agentexecution.v1.ContextInfoOrBuilder> 
        internalGetContextInfoFieldBuilder() {
      if (contextInfoBuilder_ == null) {
        contextInfoBuilder_ = new com.google.protobuf.SingleFieldBuilder<
            ai.stigmer.agentic.agentexecution.v1.ContextInfo, ai.stigmer.agentic.agentexecution.v1.ContextInfo.Builder, ai.stigmer.agentic.agentexecution.v1.ContextInfoOrBuilder>(
                getContextInfo(),
                getParentForChildren(),
                isClean());
        contextInfo_ = null;
      }
      return contextInfoBuilder_;
    }

    private java.util.List<ai.stigmer.agentic.agentexecution.v1.ExecutionArtifact> artifacts_ =
      java.util.Collections.emptyList();
    private void ensureArtifactsIsMutable() {
      if (!((bitField0_ & 0x00001000) != 0)) {
        artifacts_ = new java.util.ArrayList<ai.stigmer.agentic.agentexecution.v1.ExecutionArtifact>(artifacts_);
        bitField0_ |= 0x00001000;
       }
    }

    private com.google.protobuf.RepeatedFieldBuilder<
        ai.stigmer.agentic.agentexecution.v1.ExecutionArtifact, ai.stigmer.agentic.agentexecution.v1.ExecutionArtifact.Builder, ai.stigmer.agentic.agentexecution.v1.ExecutionArtifactOrBuilder> artifactsBuilder_;

    /**
     * <pre>
     * Files/directories published by the agent for download.
     *
     * When an agent creates artifacts (e.g., generated code, reports, skills),
     * it can call the publish_artifact tool to make them downloadable.
     * Each artifact includes a pre-signed URL for direct download.
     *
     * Artifacts are stored in R2 and URLs expire after a configured period.
     * Use the execution download endpoint to refresh expired URLs.
     *
     * Ordered by creation time (oldest first).
     *
     * &#64;since Artifact Lifecycle (Attachments &amp; Artifacts)
     * </pre>
     *
     * <code>repeated .ai.stigmer.agentic.agentexecution.v1.ExecutionArtifact artifacts = 15 [json_name = "artifacts"];</code>
     */
    public java.util.List<ai.stigmer.agentic.agentexecution.v1.ExecutionArtifact> getArtifactsList() {
      if (artifactsBuilder_ == null) {
        return java.util.Collections.unmodifiableList(artifacts_);
      } else {
        return artifactsBuilder_.getMessageList();
      }
    }
    /**
     * <pre>
     * Files/directories published by the agent for download.
     *
     * When an agent creates artifacts (e.g., generated code, reports, skills),
     * it can call the publish_artifact tool to make them downloadable.
     * Each artifact includes a pre-signed URL for direct download.
     *
     * Artifacts are stored in R2 and URLs expire after a configured period.
     * Use the execution download endpoint to refresh expired URLs.
     *
     * Ordered by creation time (oldest first).
     *
     * &#64;since Artifact Lifecycle (Attachments &amp; Artifacts)
     * </pre>
     *
     * <code>repeated .ai.stigmer.agentic.agentexecution.v1.ExecutionArtifact artifacts = 15 [json_name = "artifacts"];</code>
     */
    public int getArtifactsCount() {
      if (artifactsBuilder_ == null) {
        return artifacts_.size();
      } else {
        return artifactsBuilder_.getCount();
      }
    }
    /**
     * <pre>
     * Files/directories published by the agent for download.
     *
     * When an agent creates artifacts (e.g., generated code, reports, skills),
     * it can call the publish_artifact tool to make them downloadable.
     * Each artifact includes a pre-signed URL for direct download.
     *
     * Artifacts are stored in R2 and URLs expire after a configured period.
     * Use the execution download endpoint to refresh expired URLs.
     *
     * Ordered by creation time (oldest first).
     *
     * &#64;since Artifact Lifecycle (Attachments &amp; Artifacts)
     * </pre>
     *
     * <code>repeated .ai.stigmer.agentic.agentexecution.v1.ExecutionArtifact artifacts = 15 [json_name = "artifacts"];</code>
     */
    public ai.stigmer.agentic.agentexecution.v1.ExecutionArtifact getArtifacts(int index) {
      if (artifactsBuilder_ == null) {
        return artifacts_.get(index);
      } else {
        return artifactsBuilder_.getMessage(index);
      }
    }
    /**
     * <pre>
     * Files/directories published by the agent for download.
     *
     * When an agent creates artifacts (e.g., generated code, reports, skills),
     * it can call the publish_artifact tool to make them downloadable.
     * Each artifact includes a pre-signed URL for direct download.
     *
     * Artifacts are stored in R2 and URLs expire after a configured period.
     * Use the execution download endpoint to refresh expired URLs.
     *
     * Ordered by creation time (oldest first).
     *
     * &#64;since Artifact Lifecycle (Attachments &amp; Artifacts)
     * </pre>
     *
     * <code>repeated .ai.stigmer.agentic.agentexecution.v1.ExecutionArtifact artifacts = 15 [json_name = "artifacts"];</code>
     */
    public Builder setArtifacts(
        int index, ai.stigmer.agentic.agentexecution.v1.ExecutionArtifact value) {
      if (artifactsBuilder_ == null) {
        if (value == null) {
          throw new NullPointerException();
        }
        ensureArtifactsIsMutable();
        artifacts_.set(index, value);
        onChanged();
      } else {
        artifactsBuilder_.setMessage(index, value);
      }
      return this;
    }
    /**
     * <pre>
     * Files/directories published by the agent for download.
     *
     * When an agent creates artifacts (e.g., generated code, reports, skills),
     * it can call the publish_artifact tool to make them downloadable.
     * Each artifact includes a pre-signed URL for direct download.
     *
     * Artifacts are stored in R2 and URLs expire after a configured period.
     * Use the execution download endpoint to refresh expired URLs.
     *
     * Ordered by creation time (oldest first).
     *
     * &#64;since Artifact Lifecycle (Attachments &amp; Artifacts)
     * </pre>
     *
     * <code>repeated .ai.stigmer.agentic.agentexecution.v1.ExecutionArtifact artifacts = 15 [json_name = "artifacts"];</code>
     */
    public Builder setArtifacts(
        int index, ai.stigmer.agentic.agentexecution.v1.ExecutionArtifact.Builder builderForValue) {
      if (artifactsBuilder_ == null) {
        ensureArtifactsIsMutable();
        artifacts_.set(index, builderForValue.build());
        onChanged();
      } else {
        artifactsBuilder_.setMessage(index, builderForValue.build());
      }
      return this;
    }
    /**
     * <pre>
     * Files/directories published by the agent for download.
     *
     * When an agent creates artifacts (e.g., generated code, reports, skills),
     * it can call the publish_artifact tool to make them downloadable.
     * Each artifact includes a pre-signed URL for direct download.
     *
     * Artifacts are stored in R2 and URLs expire after a configured period.
     * Use the execution download endpoint to refresh expired URLs.
     *
     * Ordered by creation time (oldest first).
     *
     * &#64;since Artifact Lifecycle (Attachments &amp; Artifacts)
     * </pre>
     *
     * <code>repeated .ai.stigmer.agentic.agentexecution.v1.ExecutionArtifact artifacts = 15 [json_name = "artifacts"];</code>
     */
    public Builder addArtifacts(ai.stigmer.agentic.agentexecution.v1.ExecutionArtifact value) {
      if (artifactsBuilder_ == null) {
        if (value == null) {
          throw new NullPointerException();
        }
        ensureArtifactsIsMutable();
        artifacts_.add(value);
        onChanged();
      } else {
        artifactsBuilder_.addMessage(value);
      }
      return this;
    }
    /**
     * <pre>
     * Files/directories published by the agent for download.
     *
     * When an agent creates artifacts (e.g., generated code, reports, skills),
     * it can call the publish_artifact tool to make them downloadable.
     * Each artifact includes a pre-signed URL for direct download.
     *
     * Artifacts are stored in R2 and URLs expire after a configured period.
     * Use the execution download endpoint to refresh expired URLs.
     *
     * Ordered by creation time (oldest first).
     *
     * &#64;since Artifact Lifecycle (Attachments &amp; Artifacts)
     * </pre>
     *
     * <code>repeated .ai.stigmer.agentic.agentexecution.v1.ExecutionArtifact artifacts = 15 [json_name = "artifacts"];</code>
     */
    public Builder addArtifacts(
        int index, ai.stigmer.agentic.agentexecution.v1.ExecutionArtifact value) {
      if (artifactsBuilder_ == null) {
        if (value == null) {
          throw new NullPointerException();
        }
        ensureArtifactsIsMutable();
        artifacts_.add(index, value);
        onChanged();
      } else {
        artifactsBuilder_.addMessage(index, value);
      }
      return this;
    }
    /**
     * <pre>
     * Files/directories published by the agent for download.
     *
     * When an agent creates artifacts (e.g., generated code, reports, skills),
     * it can call the publish_artifact tool to make them downloadable.
     * Each artifact includes a pre-signed URL for direct download.
     *
     * Artifacts are stored in R2 and URLs expire after a configured period.
     * Use the execution download endpoint to refresh expired URLs.
     *
     * Ordered by creation time (oldest first).
     *
     * &#64;since Artifact Lifecycle (Attachments &amp; Artifacts)
     * </pre>
     *
     * <code>repeated .ai.stigmer.agentic.agentexecution.v1.ExecutionArtifact artifacts = 15 [json_name = "artifacts"];</code>
     */
    public Builder addArtifacts(
        ai.stigmer.agentic.agentexecution.v1.ExecutionArtifact.Builder builderForValue) {
      if (artifactsBuilder_ == null) {
        ensureArtifactsIsMutable();
        artifacts_.add(builderForValue.build());
        onChanged();
      } else {
        artifactsBuilder_.addMessage(builderForValue.build());
      }
      return this;
    }
    /**
     * <pre>
     * Files/directories published by the agent for download.
     *
     * When an agent creates artifacts (e.g., generated code, reports, skills),
     * it can call the publish_artifact tool to make them downloadable.
     * Each artifact includes a pre-signed URL for direct download.
     *
     * Artifacts are stored in R2 and URLs expire after a configured period.
     * Use the execution download endpoint to refresh expired URLs.
     *
     * Ordered by creation time (oldest first).
     *
     * &#64;since Artifact Lifecycle (Attachments &amp; Artifacts)
     * </pre>
     *
     * <code>repeated .ai.stigmer.agentic.agentexecution.v1.ExecutionArtifact artifacts = 15 [json_name = "artifacts"];</code>
     */
    public Builder addArtifacts(
        int index, ai.stigmer.agentic.agentexecution.v1.ExecutionArtifact.Builder builderForValue) {
      if (artifactsBuilder_ == null) {
        ensureArtifactsIsMutable();
        artifacts_.add(index, builderForValue.build());
        onChanged();
      } else {
        artifactsBuilder_.addMessage(index, builderForValue.build());
      }
      return this;
    }
    /**
     * <pre>
     * Files/directories published by the agent for download.
     *
     * When an agent creates artifacts (e.g., generated code, reports, skills),
     * it can call the publish_artifact tool to make them downloadable.
     * Each artifact includes a pre-signed URL for direct download.
     *
     * Artifacts are stored in R2 and URLs expire after a configured period.
     * Use the execution download endpoint to refresh expired URLs.
     *
     * Ordered by creation time (oldest first).
     *
     * &#64;since Artifact Lifecycle (Attachments &amp; Artifacts)
     * </pre>
     *
     * <code>repeated .ai.stigmer.agentic.agentexecution.v1.ExecutionArtifact artifacts = 15 [json_name = "artifacts"];</code>
     */
    public Builder addAllArtifacts(
        java.lang.Iterable<? extends ai.stigmer.agentic.agentexecution.v1.ExecutionArtifact> values) {
      if (artifactsBuilder_ == null) {
        ensureArtifactsIsMutable();
        com.google.protobuf.AbstractMessageLite.Builder.addAll(
            values, artifacts_);
        onChanged();
      } else {
        artifactsBuilder_.addAllMessages(values);
      }
      return this;
    }
    /**
     * <pre>
     * Files/directories published by the agent for download.
     *
     * When an agent creates artifacts (e.g., generated code, reports, skills),
     * it can call the publish_artifact tool to make them downloadable.
     * Each artifact includes a pre-signed URL for direct download.
     *
     * Artifacts are stored in R2 and URLs expire after a configured period.
     * Use the execution download endpoint to refresh expired URLs.
     *
     * Ordered by creation time (oldest first).
     *
     * &#64;since Artifact Lifecycle (Attachments &amp; Artifacts)
     * </pre>
     *
     * <code>repeated .ai.stigmer.agentic.agentexecution.v1.ExecutionArtifact artifacts = 15 [json_name = "artifacts"];</code>
     */
    public Builder clearArtifacts() {
      if (artifactsBuilder_ == null) {
        artifacts_ = java.util.Collections.emptyList();
        bitField0_ = (bitField0_ & ~0x00001000);
        onChanged();
      } else {
        artifactsBuilder_.clear();
      }
      return this;
    }
    /**
     * <pre>
     * Files/directories published by the agent for download.
     *
     * When an agent creates artifacts (e.g., generated code, reports, skills),
     * it can call the publish_artifact tool to make them downloadable.
     * Each artifact includes a pre-signed URL for direct download.
     *
     * Artifacts are stored in R2 and URLs expire after a configured period.
     * Use the execution download endpoint to refresh expired URLs.
     *
     * Ordered by creation time (oldest first).
     *
     * &#64;since Artifact Lifecycle (Attachments &amp; Artifacts)
     * </pre>
     *
     * <code>repeated .ai.stigmer.agentic.agentexecution.v1.ExecutionArtifact artifacts = 15 [json_name = "artifacts"];</code>
     */
    public Builder removeArtifacts(int index) {
      if (artifactsBuilder_ == null) {
        ensureArtifactsIsMutable();
        artifacts_.remove(index);
        onChanged();
      } else {
        artifactsBuilder_.remove(index);
      }
      return this;
    }
    /**
     * <pre>
     * Files/directories published by the agent for download.
     *
     * When an agent creates artifacts (e.g., generated code, reports, skills),
     * it can call the publish_artifact tool to make them downloadable.
     * Each artifact includes a pre-signed URL for direct download.
     *
     * Artifacts are stored in R2 and URLs expire after a configured period.
     * Use the execution download endpoint to refresh expired URLs.
     *
     * Ordered by creation time (oldest first).
     *
     * &#64;since Artifact Lifecycle (Attachments &amp; Artifacts)
     * </pre>
     *
     * <code>repeated .ai.stigmer.agentic.agentexecution.v1.ExecutionArtifact artifacts = 15 [json_name = "artifacts"];</code>
     */
    public ai.stigmer.agentic.agentexecution.v1.ExecutionArtifact.Builder getArtifactsBuilder(
        int index) {
      return internalGetArtifactsFieldBuilder().getBuilder(index);
    }
    /**
     * <pre>
     * Files/directories published by the agent for download.
     *
     * When an agent creates artifacts (e.g., generated code, reports, skills),
     * it can call the publish_artifact tool to make them downloadable.
     * Each artifact includes a pre-signed URL for direct download.
     *
     * Artifacts are stored in R2 and URLs expire after a configured period.
     * Use the execution download endpoint to refresh expired URLs.
     *
     * Ordered by creation time (oldest first).
     *
     * &#64;since Artifact Lifecycle (Attachments &amp; Artifacts)
     * </pre>
     *
     * <code>repeated .ai.stigmer.agentic.agentexecution.v1.ExecutionArtifact artifacts = 15 [json_name = "artifacts"];</code>
     */
    public ai.stigmer.agentic.agentexecution.v1.ExecutionArtifactOrBuilder getArtifactsOrBuilder(
        int index) {
      if (artifactsBuilder_ == null) {
        return artifacts_.get(index);  } else {
        return artifactsBuilder_.getMessageOrBuilder(index);
      }
    }
    /**
     * <pre>
     * Files/directories published by the agent for download.
     *
     * When an agent creates artifacts (e.g., generated code, reports, skills),
     * it can call the publish_artifact tool to make them downloadable.
     * Each artifact includes a pre-signed URL for direct download.
     *
     * Artifacts are stored in R2 and URLs expire after a configured period.
     * Use the execution download endpoint to refresh expired URLs.
     *
     * Ordered by creation time (oldest first).
     *
     * &#64;since Artifact Lifecycle (Attachments &amp; Artifacts)
     * </pre>
     *
     * <code>repeated .ai.stigmer.agentic.agentexecution.v1.ExecutionArtifact artifacts = 15 [json_name = "artifacts"];</code>
     */
    public java.util.List<? extends ai.stigmer.agentic.agentexecution.v1.ExecutionArtifactOrBuilder> 
         getArtifactsOrBuilderList() {
      if (artifactsBuilder_ != null) {
        return artifactsBuilder_.getMessageOrBuilderList();
      } else {
        return java.util.Collections.unmodifiableList(artifacts_);
      }
    }
    /**
     * <pre>
     * Files/directories published by the agent for download.
     *
     * When an agent creates artifacts (e.g., generated code, reports, skills),
     * it can call the publish_artifact tool to make them downloadable.
     * Each artifact includes a pre-signed URL for direct download.
     *
     * Artifacts are stored in R2 and URLs expire after a configured period.
     * Use the execution download endpoint to refresh expired URLs.
     *
     * Ordered by creation time (oldest first).
     *
     * &#64;since Artifact Lifecycle (Attachments &amp; Artifacts)
     * </pre>
     *
     * <code>repeated .ai.stigmer.agentic.agentexecution.v1.ExecutionArtifact artifacts = 15 [json_name = "artifacts"];</code>
     */
    public ai.stigmer.agentic.agentexecution.v1.ExecutionArtifact.Builder addArtifactsBuilder() {
      return internalGetArtifactsFieldBuilder().addBuilder(
          ai.stigmer.agentic.agentexecution.v1.ExecutionArtifact.getDefaultInstance());
    }
    /**
     * <pre>
     * Files/directories published by the agent for download.
     *
     * When an agent creates artifacts (e.g., generated code, reports, skills),
     * it can call the publish_artifact tool to make them downloadable.
     * Each artifact includes a pre-signed URL for direct download.
     *
     * Artifacts are stored in R2 and URLs expire after a configured period.
     * Use the execution download endpoint to refresh expired URLs.
     *
     * Ordered by creation time (oldest first).
     *
     * &#64;since Artifact Lifecycle (Attachments &amp; Artifacts)
     * </pre>
     *
     * <code>repeated .ai.stigmer.agentic.agentexecution.v1.ExecutionArtifact artifacts = 15 [json_name = "artifacts"];</code>
     */
    public ai.stigmer.agentic.agentexecution.v1.ExecutionArtifact.Builder addArtifactsBuilder(
        int index) {
      return internalGetArtifactsFieldBuilder().addBuilder(
          index, ai.stigmer.agentic.agentexecution.v1.ExecutionArtifact.getDefaultInstance());
    }
    /**
     * <pre>
     * Files/directories published by the agent for download.
     *
     * When an agent creates artifacts (e.g., generated code, reports, skills),
     * it can call the publish_artifact tool to make them downloadable.
     * Each artifact includes a pre-signed URL for direct download.
     *
     * Artifacts are stored in R2 and URLs expire after a configured period.
     * Use the execution download endpoint to refresh expired URLs.
     *
     * Ordered by creation time (oldest first).
     *
     * &#64;since Artifact Lifecycle (Attachments &amp; Artifacts)
     * </pre>
     *
     * <code>repeated .ai.stigmer.agentic.agentexecution.v1.ExecutionArtifact artifacts = 15 [json_name = "artifacts"];</code>
     */
    public java.util.List<ai.stigmer.agentic.agentexecution.v1.ExecutionArtifact.Builder> 
         getArtifactsBuilderList() {
      return internalGetArtifactsFieldBuilder().getBuilderList();
    }
    private com.google.protobuf.RepeatedFieldBuilder<
        ai.stigmer.agentic.agentexecution.v1.ExecutionArtifact, ai.stigmer.agentic.agentexecution.v1.ExecutionArtifact.Builder, ai.stigmer.agentic.agentexecution.v1.ExecutionArtifactOrBuilder> 
        internalGetArtifactsFieldBuilder() {
      if (artifactsBuilder_ == null) {
        artifactsBuilder_ = new com.google.protobuf.RepeatedFieldBuilder<
            ai.stigmer.agentic.agentexecution.v1.ExecutionArtifact, ai.stigmer.agentic.agentexecution.v1.ExecutionArtifact.Builder, ai.stigmer.agentic.agentexecution.v1.ExecutionArtifactOrBuilder>(
                artifacts_,
                ((bitField0_ & 0x00001000) != 0),
                getParentForChildren(),
                isClean());
        artifacts_ = null;
      }
      return artifactsBuilder_;
    }

    private java.util.List<ai.stigmer.agentic.agentexecution.v1.WorkspaceWriteBack> workspaceWriteBacks_ =
      java.util.Collections.emptyList();
    private void ensureWorkspaceWriteBacksIsMutable() {
      if (!((bitField0_ & 0x00002000) != 0)) {
        workspaceWriteBacks_ = new java.util.ArrayList<ai.stigmer.agentic.agentexecution.v1.WorkspaceWriteBack>(workspaceWriteBacks_);
        bitField0_ |= 0x00002000;
       }
    }

    private com.google.protobuf.RepeatedFieldBuilder<
        ai.stigmer.agentic.agentexecution.v1.WorkspaceWriteBack, ai.stigmer.agentic.agentexecution.v1.WorkspaceWriteBack.Builder, ai.stigmer.agentic.agentexecution.v1.WorkspaceWriteBackOrBuilder> workspaceWriteBacksBuilder_;

    /**
     * <pre>
     * Git write-back outcomes for workspace entries that had changes.
     *
     * One entry per git-backed workspace entry where the agent modified
     * files and write_back_mode was enabled on the GitRepoSource. Omitted
     * for workspace entries with no changes, non-git sources, or disabled
     * write-back mode.
     *
     * Populated during post-execution processing. Each entry tracks the
     * write-back lifecycle (committed -&gt; pushed -&gt; PR created) and carries
     * the PR URL for UI display.
     *
     * &#64;since Platform-Owned Git Write-Back
     * </pre>
     *
     * <code>repeated .ai.stigmer.agentic.agentexecution.v1.WorkspaceWriteBack workspace_write_backs = 17 [json_name = "workspaceWriteBacks"];</code>
     */
    public java.util.List<ai.stigmer.agentic.agentexecution.v1.WorkspaceWriteBack> getWorkspaceWriteBacksList() {
      if (workspaceWriteBacksBuilder_ == null) {
        return java.util.Collections.unmodifiableList(workspaceWriteBacks_);
      } else {
        return workspaceWriteBacksBuilder_.getMessageList();
      }
    }
    /**
     * <pre>
     * Git write-back outcomes for workspace entries that had changes.
     *
     * One entry per git-backed workspace entry where the agent modified
     * files and write_back_mode was enabled on the GitRepoSource. Omitted
     * for workspace entries with no changes, non-git sources, or disabled
     * write-back mode.
     *
     * Populated during post-execution processing. Each entry tracks the
     * write-back lifecycle (committed -&gt; pushed -&gt; PR created) and carries
     * the PR URL for UI display.
     *
     * &#64;since Platform-Owned Git Write-Back
     * </pre>
     *
     * <code>repeated .ai.stigmer.agentic.agentexecution.v1.WorkspaceWriteBack workspace_write_backs = 17 [json_name = "workspaceWriteBacks"];</code>
     */
    public int getWorkspaceWriteBacksCount() {
      if (workspaceWriteBacksBuilder_ == null) {
        return workspaceWriteBacks_.size();
      } else {
        return workspaceWriteBacksBuilder_.getCount();
      }
    }
    /**
     * <pre>
     * Git write-back outcomes for workspace entries that had changes.
     *
     * One entry per git-backed workspace entry where the agent modified
     * files and write_back_mode was enabled on the GitRepoSource. Omitted
     * for workspace entries with no changes, non-git sources, or disabled
     * write-back mode.
     *
     * Populated during post-execution processing. Each entry tracks the
     * write-back lifecycle (committed -&gt; pushed -&gt; PR created) and carries
     * the PR URL for UI display.
     *
     * &#64;since Platform-Owned Git Write-Back
     * </pre>
     *
     * <code>repeated .ai.stigmer.agentic.agentexecution.v1.WorkspaceWriteBack workspace_write_backs = 17 [json_name = "workspaceWriteBacks"];</code>
     */
    public ai.stigmer.agentic.agentexecution.v1.WorkspaceWriteBack getWorkspaceWriteBacks(int index) {
      if (workspaceWriteBacksBuilder_ == null) {
        return workspaceWriteBacks_.get(index);
      } else {
        return workspaceWriteBacksBuilder_.getMessage(index);
      }
    }
    /**
     * <pre>
     * Git write-back outcomes for workspace entries that had changes.
     *
     * One entry per git-backed workspace entry where the agent modified
     * files and write_back_mode was enabled on the GitRepoSource. Omitted
     * for workspace entries with no changes, non-git sources, or disabled
     * write-back mode.
     *
     * Populated during post-execution processing. Each entry tracks the
     * write-back lifecycle (committed -&gt; pushed -&gt; PR created) and carries
     * the PR URL for UI display.
     *
     * &#64;since Platform-Owned Git Write-Back
     * </pre>
     *
     * <code>repeated .ai.stigmer.agentic.agentexecution.v1.WorkspaceWriteBack workspace_write_backs = 17 [json_name = "workspaceWriteBacks"];</code>
     */
    public Builder setWorkspaceWriteBacks(
        int index, ai.stigmer.agentic.agentexecution.v1.WorkspaceWriteBack value) {
      if (workspaceWriteBacksBuilder_ == null) {
        if (value == null) {
          throw new NullPointerException();
        }
        ensureWorkspaceWriteBacksIsMutable();
        workspaceWriteBacks_.set(index, value);
        onChanged();
      } else {
        workspaceWriteBacksBuilder_.setMessage(index, value);
      }
      return this;
    }
    /**
     * <pre>
     * Git write-back outcomes for workspace entries that had changes.
     *
     * One entry per git-backed workspace entry where the agent modified
     * files and write_back_mode was enabled on the GitRepoSource. Omitted
     * for workspace entries with no changes, non-git sources, or disabled
     * write-back mode.
     *
     * Populated during post-execution processing. Each entry tracks the
     * write-back lifecycle (committed -&gt; pushed -&gt; PR created) and carries
     * the PR URL for UI display.
     *
     * &#64;since Platform-Owned Git Write-Back
     * </pre>
     *
     * <code>repeated .ai.stigmer.agentic.agentexecution.v1.WorkspaceWriteBack workspace_write_backs = 17 [json_name = "workspaceWriteBacks"];</code>
     */
    public Builder setWorkspaceWriteBacks(
        int index, ai.stigmer.agentic.agentexecution.v1.WorkspaceWriteBack.Builder builderForValue) {
      if (workspaceWriteBacksBuilder_ == null) {
        ensureWorkspaceWriteBacksIsMutable();
        workspaceWriteBacks_.set(index, builderForValue.build());
        onChanged();
      } else {
        workspaceWriteBacksBuilder_.setMessage(index, builderForValue.build());
      }
      return this;
    }
    /**
     * <pre>
     * Git write-back outcomes for workspace entries that had changes.
     *
     * One entry per git-backed workspace entry where the agent modified
     * files and write_back_mode was enabled on the GitRepoSource. Omitted
     * for workspace entries with no changes, non-git sources, or disabled
     * write-back mode.
     *
     * Populated during post-execution processing. Each entry tracks the
     * write-back lifecycle (committed -&gt; pushed -&gt; PR created) and carries
     * the PR URL for UI display.
     *
     * &#64;since Platform-Owned Git Write-Back
     * </pre>
     *
     * <code>repeated .ai.stigmer.agentic.agentexecution.v1.WorkspaceWriteBack workspace_write_backs = 17 [json_name = "workspaceWriteBacks"];</code>
     */
    public Builder addWorkspaceWriteBacks(ai.stigmer.agentic.agentexecution.v1.WorkspaceWriteBack value) {
      if (workspaceWriteBacksBuilder_ == null) {
        if (value == null) {
          throw new NullPointerException();
        }
        ensureWorkspaceWriteBacksIsMutable();
        workspaceWriteBacks_.add(value);
        onChanged();
      } else {
        workspaceWriteBacksBuilder_.addMessage(value);
      }
      return this;
    }
    /**
     * <pre>
     * Git write-back outcomes for workspace entries that had changes.
     *
     * One entry per git-backed workspace entry where the agent modified
     * files and write_back_mode was enabled on the GitRepoSource. Omitted
     * for workspace entries with no changes, non-git sources, or disabled
     * write-back mode.
     *
     * Populated during post-execution processing. Each entry tracks the
     * write-back lifecycle (committed -&gt; pushed -&gt; PR created) and carries
     * the PR URL for UI display.
     *
     * &#64;since Platform-Owned Git Write-Back
     * </pre>
     *
     * <code>repeated .ai.stigmer.agentic.agentexecution.v1.WorkspaceWriteBack workspace_write_backs = 17 [json_name = "workspaceWriteBacks"];</code>
     */
    public Builder addWorkspaceWriteBacks(
        int index, ai.stigmer.agentic.agentexecution.v1.WorkspaceWriteBack value) {
      if (workspaceWriteBacksBuilder_ == null) {
        if (value == null) {
          throw new NullPointerException();
        }
        ensureWorkspaceWriteBacksIsMutable();
        workspaceWriteBacks_.add(index, value);
        onChanged();
      } else {
        workspaceWriteBacksBuilder_.addMessage(index, value);
      }
      return this;
    }
    /**
     * <pre>
     * Git write-back outcomes for workspace entries that had changes.
     *
     * One entry per git-backed workspace entry where the agent modified
     * files and write_back_mode was enabled on the GitRepoSource. Omitted
     * for workspace entries with no changes, non-git sources, or disabled
     * write-back mode.
     *
     * Populated during post-execution processing. Each entry tracks the
     * write-back lifecycle (committed -&gt; pushed -&gt; PR created) and carries
     * the PR URL for UI display.
     *
     * &#64;since Platform-Owned Git Write-Back
     * </pre>
     *
     * <code>repeated .ai.stigmer.agentic.agentexecution.v1.WorkspaceWriteBack workspace_write_backs = 17 [json_name = "workspaceWriteBacks"];</code>
     */
    public Builder addWorkspaceWriteBacks(
        ai.stigmer.agentic.agentexecution.v1.WorkspaceWriteBack.Builder builderForValue) {
      if (workspaceWriteBacksBuilder_ == null) {
        ensureWorkspaceWriteBacksIsMutable();
        workspaceWriteBacks_.add(builderForValue.build());
        onChanged();
      } else {
        workspaceWriteBacksBuilder_.addMessage(builderForValue.build());
      }
      return this;
    }
    /**
     * <pre>
     * Git write-back outcomes for workspace entries that had changes.
     *
     * One entry per git-backed workspace entry where the agent modified
     * files and write_back_mode was enabled on the GitRepoSource. Omitted
     * for workspace entries with no changes, non-git sources, or disabled
     * write-back mode.
     *
     * Populated during post-execution processing. Each entry tracks the
     * write-back lifecycle (committed -&gt; pushed -&gt; PR created) and carries
     * the PR URL for UI display.
     *
     * &#64;since Platform-Owned Git Write-Back
     * </pre>
     *
     * <code>repeated .ai.stigmer.agentic.agentexecution.v1.WorkspaceWriteBack workspace_write_backs = 17 [json_name = "workspaceWriteBacks"];</code>
     */
    public Builder addWorkspaceWriteBacks(
        int index, ai.stigmer.agentic.agentexecution.v1.WorkspaceWriteBack.Builder builderForValue) {
      if (workspaceWriteBacksBuilder_ == null) {
        ensureWorkspaceWriteBacksIsMutable();
        workspaceWriteBacks_.add(index, builderForValue.build());
        onChanged();
      } else {
        workspaceWriteBacksBuilder_.addMessage(index, builderForValue.build());
      }
      return this;
    }
    /**
     * <pre>
     * Git write-back outcomes for workspace entries that had changes.
     *
     * One entry per git-backed workspace entry where the agent modified
     * files and write_back_mode was enabled on the GitRepoSource. Omitted
     * for workspace entries with no changes, non-git sources, or disabled
     * write-back mode.
     *
     * Populated during post-execution processing. Each entry tracks the
     * write-back lifecycle (committed -&gt; pushed -&gt; PR created) and carries
     * the PR URL for UI display.
     *
     * &#64;since Platform-Owned Git Write-Back
     * </pre>
     *
     * <code>repeated .ai.stigmer.agentic.agentexecution.v1.WorkspaceWriteBack workspace_write_backs = 17 [json_name = "workspaceWriteBacks"];</code>
     */
    public Builder addAllWorkspaceWriteBacks(
        java.lang.Iterable<? extends ai.stigmer.agentic.agentexecution.v1.WorkspaceWriteBack> values) {
      if (workspaceWriteBacksBuilder_ == null) {
        ensureWorkspaceWriteBacksIsMutable();
        com.google.protobuf.AbstractMessageLite.Builder.addAll(
            values, workspaceWriteBacks_);
        onChanged();
      } else {
        workspaceWriteBacksBuilder_.addAllMessages(values);
      }
      return this;
    }
    /**
     * <pre>
     * Git write-back outcomes for workspace entries that had changes.
     *
     * One entry per git-backed workspace entry where the agent modified
     * files and write_back_mode was enabled on the GitRepoSource. Omitted
     * for workspace entries with no changes, non-git sources, or disabled
     * write-back mode.
     *
     * Populated during post-execution processing. Each entry tracks the
     * write-back lifecycle (committed -&gt; pushed -&gt; PR created) and carries
     * the PR URL for UI display.
     *
     * &#64;since Platform-Owned Git Write-Back
     * </pre>
     *
     * <code>repeated .ai.stigmer.agentic.agentexecution.v1.WorkspaceWriteBack workspace_write_backs = 17 [json_name = "workspaceWriteBacks"];</code>
     */
    public Builder clearWorkspaceWriteBacks() {
      if (workspaceWriteBacksBuilder_ == null) {
        workspaceWriteBacks_ = java.util.Collections.emptyList();
        bitField0_ = (bitField0_ & ~0x00002000);
        onChanged();
      } else {
        workspaceWriteBacksBuilder_.clear();
      }
      return this;
    }
    /**
     * <pre>
     * Git write-back outcomes for workspace entries that had changes.
     *
     * One entry per git-backed workspace entry where the agent modified
     * files and write_back_mode was enabled on the GitRepoSource. Omitted
     * for workspace entries with no changes, non-git sources, or disabled
     * write-back mode.
     *
     * Populated during post-execution processing. Each entry tracks the
     * write-back lifecycle (committed -&gt; pushed -&gt; PR created) and carries
     * the PR URL for UI display.
     *
     * &#64;since Platform-Owned Git Write-Back
     * </pre>
     *
     * <code>repeated .ai.stigmer.agentic.agentexecution.v1.WorkspaceWriteBack workspace_write_backs = 17 [json_name = "workspaceWriteBacks"];</code>
     */
    public Builder removeWorkspaceWriteBacks(int index) {
      if (workspaceWriteBacksBuilder_ == null) {
        ensureWorkspaceWriteBacksIsMutable();
        workspaceWriteBacks_.remove(index);
        onChanged();
      } else {
        workspaceWriteBacksBuilder_.remove(index);
      }
      return this;
    }
    /**
     * <pre>
     * Git write-back outcomes for workspace entries that had changes.
     *
     * One entry per git-backed workspace entry where the agent modified
     * files and write_back_mode was enabled on the GitRepoSource. Omitted
     * for workspace entries with no changes, non-git sources, or disabled
     * write-back mode.
     *
     * Populated during post-execution processing. Each entry tracks the
     * write-back lifecycle (committed -&gt; pushed -&gt; PR created) and carries
     * the PR URL for UI display.
     *
     * &#64;since Platform-Owned Git Write-Back
     * </pre>
     *
     * <code>repeated .ai.stigmer.agentic.agentexecution.v1.WorkspaceWriteBack workspace_write_backs = 17 [json_name = "workspaceWriteBacks"];</code>
     */
    public ai.stigmer.agentic.agentexecution.v1.WorkspaceWriteBack.Builder getWorkspaceWriteBacksBuilder(
        int index) {
      return internalGetWorkspaceWriteBacksFieldBuilder().getBuilder(index);
    }
    /**
     * <pre>
     * Git write-back outcomes for workspace entries that had changes.
     *
     * One entry per git-backed workspace entry where the agent modified
     * files and write_back_mode was enabled on the GitRepoSource. Omitted
     * for workspace entries with no changes, non-git sources, or disabled
     * write-back mode.
     *
     * Populated during post-execution processing. Each entry tracks the
     * write-back lifecycle (committed -&gt; pushed -&gt; PR created) and carries
     * the PR URL for UI display.
     *
     * &#64;since Platform-Owned Git Write-Back
     * </pre>
     *
     * <code>repeated .ai.stigmer.agentic.agentexecution.v1.WorkspaceWriteBack workspace_write_backs = 17 [json_name = "workspaceWriteBacks"];</code>
     */
    public ai.stigmer.agentic.agentexecution.v1.WorkspaceWriteBackOrBuilder getWorkspaceWriteBacksOrBuilder(
        int index) {
      if (workspaceWriteBacksBuilder_ == null) {
        return workspaceWriteBacks_.get(index);  } else {
        return workspaceWriteBacksBuilder_.getMessageOrBuilder(index);
      }
    }
    /**
     * <pre>
     * Git write-back outcomes for workspace entries that had changes.
     *
     * One entry per git-backed workspace entry where the agent modified
     * files and write_back_mode was enabled on the GitRepoSource. Omitted
     * for workspace entries with no changes, non-git sources, or disabled
     * write-back mode.
     *
     * Populated during post-execution processing. Each entry tracks the
     * write-back lifecycle (committed -&gt; pushed -&gt; PR created) and carries
     * the PR URL for UI display.
     *
     * &#64;since Platform-Owned Git Write-Back
     * </pre>
     *
     * <code>repeated .ai.stigmer.agentic.agentexecution.v1.WorkspaceWriteBack workspace_write_backs = 17 [json_name = "workspaceWriteBacks"];</code>
     */
    public java.util.List<? extends ai.stigmer.agentic.agentexecution.v1.WorkspaceWriteBackOrBuilder> 
         getWorkspaceWriteBacksOrBuilderList() {
      if (workspaceWriteBacksBuilder_ != null) {
        return workspaceWriteBacksBuilder_.getMessageOrBuilderList();
      } else {
        return java.util.Collections.unmodifiableList(workspaceWriteBacks_);
      }
    }
    /**
     * <pre>
     * Git write-back outcomes for workspace entries that had changes.
     *
     * One entry per git-backed workspace entry where the agent modified
     * files and write_back_mode was enabled on the GitRepoSource. Omitted
     * for workspace entries with no changes, non-git sources, or disabled
     * write-back mode.
     *
     * Populated during post-execution processing. Each entry tracks the
     * write-back lifecycle (committed -&gt; pushed -&gt; PR created) and carries
     * the PR URL for UI display.
     *
     * &#64;since Platform-Owned Git Write-Back
     * </pre>
     *
     * <code>repeated .ai.stigmer.agentic.agentexecution.v1.WorkspaceWriteBack workspace_write_backs = 17 [json_name = "workspaceWriteBacks"];</code>
     */
    public ai.stigmer.agentic.agentexecution.v1.WorkspaceWriteBack.Builder addWorkspaceWriteBacksBuilder() {
      return internalGetWorkspaceWriteBacksFieldBuilder().addBuilder(
          ai.stigmer.agentic.agentexecution.v1.WorkspaceWriteBack.getDefaultInstance());
    }
    /**
     * <pre>
     * Git write-back outcomes for workspace entries that had changes.
     *
     * One entry per git-backed workspace entry where the agent modified
     * files and write_back_mode was enabled on the GitRepoSource. Omitted
     * for workspace entries with no changes, non-git sources, or disabled
     * write-back mode.
     *
     * Populated during post-execution processing. Each entry tracks the
     * write-back lifecycle (committed -&gt; pushed -&gt; PR created) and carries
     * the PR URL for UI display.
     *
     * &#64;since Platform-Owned Git Write-Back
     * </pre>
     *
     * <code>repeated .ai.stigmer.agentic.agentexecution.v1.WorkspaceWriteBack workspace_write_backs = 17 [json_name = "workspaceWriteBacks"];</code>
     */
    public ai.stigmer.agentic.agentexecution.v1.WorkspaceWriteBack.Builder addWorkspaceWriteBacksBuilder(
        int index) {
      return internalGetWorkspaceWriteBacksFieldBuilder().addBuilder(
          index, ai.stigmer.agentic.agentexecution.v1.WorkspaceWriteBack.getDefaultInstance());
    }
    /**
     * <pre>
     * Git write-back outcomes for workspace entries that had changes.
     *
     * One entry per git-backed workspace entry where the agent modified
     * files and write_back_mode was enabled on the GitRepoSource. Omitted
     * for workspace entries with no changes, non-git sources, or disabled
     * write-back mode.
     *
     * Populated during post-execution processing. Each entry tracks the
     * write-back lifecycle (committed -&gt; pushed -&gt; PR created) and carries
     * the PR URL for UI display.
     *
     * &#64;since Platform-Owned Git Write-Back
     * </pre>
     *
     * <code>repeated .ai.stigmer.agentic.agentexecution.v1.WorkspaceWriteBack workspace_write_backs = 17 [json_name = "workspaceWriteBacks"];</code>
     */
    public java.util.List<ai.stigmer.agentic.agentexecution.v1.WorkspaceWriteBack.Builder> 
         getWorkspaceWriteBacksBuilderList() {
      return internalGetWorkspaceWriteBacksFieldBuilder().getBuilderList();
    }
    private com.google.protobuf.RepeatedFieldBuilder<
        ai.stigmer.agentic.agentexecution.v1.WorkspaceWriteBack, ai.stigmer.agentic.agentexecution.v1.WorkspaceWriteBack.Builder, ai.stigmer.agentic.agentexecution.v1.WorkspaceWriteBackOrBuilder> 
        internalGetWorkspaceWriteBacksFieldBuilder() {
      if (workspaceWriteBacksBuilder_ == null) {
        workspaceWriteBacksBuilder_ = new com.google.protobuf.RepeatedFieldBuilder<
            ai.stigmer.agentic.agentexecution.v1.WorkspaceWriteBack, ai.stigmer.agentic.agentexecution.v1.WorkspaceWriteBack.Builder, ai.stigmer.agentic.agentexecution.v1.WorkspaceWriteBackOrBuilder>(
                workspaceWriteBacks_,
                ((bitField0_ & 0x00002000) != 0),
                getParentForChildren(),
                isClean());
        workspaceWriteBacks_ = null;
      }
      return workspaceWriteBacksBuilder_;
    }

    private ai.stigmer.agentic.agentexecution.v1.SetupProgress setupProgress_;
    private com.google.protobuf.SingleFieldBuilder<
        ai.stigmer.agentic.agentexecution.v1.SetupProgress, ai.stigmer.agentic.agentexecution.v1.SetupProgress.Builder, ai.stigmer.agentic.agentexecution.v1.SetupProgressOrBuilder> setupProgressBuilder_;
    /**
     * <pre>
     * Real-time setup progress reported by the runner during EXECUTION_PENDING.
     * Populated progressively as the worker completes setup steps (sandbox init,
     * workspace provisioning, skill loading, MCP server connection, etc.).
     * Only meaningful while phase == EXECUTION_PENDING. Cleared by the server
     * when phase transitions away from PENDING.
     * </pre>
     *
     * <code>.ai.stigmer.agentic.agentexecution.v1.SetupProgress setup_progress = 18 [json_name = "setupProgress"];</code>
     * @return Whether the setupProgress field is set.
     */
    public boolean hasSetupProgress() {
      return ((bitField0_ & 0x00004000) != 0);
    }
    /**
     * <pre>
     * Real-time setup progress reported by the runner during EXECUTION_PENDING.
     * Populated progressively as the worker completes setup steps (sandbox init,
     * workspace provisioning, skill loading, MCP server connection, etc.).
     * Only meaningful while phase == EXECUTION_PENDING. Cleared by the server
     * when phase transitions away from PENDING.
     * </pre>
     *
     * <code>.ai.stigmer.agentic.agentexecution.v1.SetupProgress setup_progress = 18 [json_name = "setupProgress"];</code>
     * @return The setupProgress.
     */
    public ai.stigmer.agentic.agentexecution.v1.SetupProgress getSetupProgress() {
      if (setupProgressBuilder_ == null) {
        return setupProgress_ == null ? ai.stigmer.agentic.agentexecution.v1.SetupProgress.getDefaultInstance() : setupProgress_;
      } else {
        return setupProgressBuilder_.getMessage();
      }
    }
    /**
     * <pre>
     * Real-time setup progress reported by the runner during EXECUTION_PENDING.
     * Populated progressively as the worker completes setup steps (sandbox init,
     * workspace provisioning, skill loading, MCP server connection, etc.).
     * Only meaningful while phase == EXECUTION_PENDING. Cleared by the server
     * when phase transitions away from PENDING.
     * </pre>
     *
     * <code>.ai.stigmer.agentic.agentexecution.v1.SetupProgress setup_progress = 18 [json_name = "setupProgress"];</code>
     */
    public Builder setSetupProgress(ai.stigmer.agentic.agentexecution.v1.SetupProgress value) {
      if (setupProgressBuilder_ == null) {
        if (value == null) {
          throw new NullPointerException();
        }
        setupProgress_ = value;
      } else {
        setupProgressBuilder_.setMessage(value);
      }
      bitField0_ |= 0x00004000;
      onChanged();
      return this;
    }
    /**
     * <pre>
     * Real-time setup progress reported by the runner during EXECUTION_PENDING.
     * Populated progressively as the worker completes setup steps (sandbox init,
     * workspace provisioning, skill loading, MCP server connection, etc.).
     * Only meaningful while phase == EXECUTION_PENDING. Cleared by the server
     * when phase transitions away from PENDING.
     * </pre>
     *
     * <code>.ai.stigmer.agentic.agentexecution.v1.SetupProgress setup_progress = 18 [json_name = "setupProgress"];</code>
     */
    public Builder setSetupProgress(
        ai.stigmer.agentic.agentexecution.v1.SetupProgress.Builder builderForValue) {
      if (setupProgressBuilder_ == null) {
        setupProgress_ = builderForValue.build();
      } else {
        setupProgressBuilder_.setMessage(builderForValue.build());
      }
      bitField0_ |= 0x00004000;
      onChanged();
      return this;
    }
    /**
     * <pre>
     * Real-time setup progress reported by the runner during EXECUTION_PENDING.
     * Populated progressively as the worker completes setup steps (sandbox init,
     * workspace provisioning, skill loading, MCP server connection, etc.).
     * Only meaningful while phase == EXECUTION_PENDING. Cleared by the server
     * when phase transitions away from PENDING.
     * </pre>
     *
     * <code>.ai.stigmer.agentic.agentexecution.v1.SetupProgress setup_progress = 18 [json_name = "setupProgress"];</code>
     */
    public Builder mergeSetupProgress(ai.stigmer.agentic.agentexecution.v1.SetupProgress value) {
      if (setupProgressBuilder_ == null) {
        if (((bitField0_ & 0x00004000) != 0) &&
          setupProgress_ != null &&
          setupProgress_ != ai.stigmer.agentic.agentexecution.v1.SetupProgress.getDefaultInstance()) {
          getSetupProgressBuilder().mergeFrom(value);
        } else {
          setupProgress_ = value;
        }
      } else {
        setupProgressBuilder_.mergeFrom(value);
      }
      if (setupProgress_ != null) {
        bitField0_ |= 0x00004000;
        onChanged();
      }
      return this;
    }
    /**
     * <pre>
     * Real-time setup progress reported by the runner during EXECUTION_PENDING.
     * Populated progressively as the worker completes setup steps (sandbox init,
     * workspace provisioning, skill loading, MCP server connection, etc.).
     * Only meaningful while phase == EXECUTION_PENDING. Cleared by the server
     * when phase transitions away from PENDING.
     * </pre>
     *
     * <code>.ai.stigmer.agentic.agentexecution.v1.SetupProgress setup_progress = 18 [json_name = "setupProgress"];</code>
     */
    public Builder clearSetupProgress() {
      bitField0_ = (bitField0_ & ~0x00004000);
      setupProgress_ = null;
      if (setupProgressBuilder_ != null) {
        setupProgressBuilder_.dispose();
        setupProgressBuilder_ = null;
      }
      onChanged();
      return this;
    }
    /**
     * <pre>
     * Real-time setup progress reported by the runner during EXECUTION_PENDING.
     * Populated progressively as the worker completes setup steps (sandbox init,
     * workspace provisioning, skill loading, MCP server connection, etc.).
     * Only meaningful while phase == EXECUTION_PENDING. Cleared by the server
     * when phase transitions away from PENDING.
     * </pre>
     *
     * <code>.ai.stigmer.agentic.agentexecution.v1.SetupProgress setup_progress = 18 [json_name = "setupProgress"];</code>
     */
    public ai.stigmer.agentic.agentexecution.v1.SetupProgress.Builder getSetupProgressBuilder() {
      bitField0_ |= 0x00004000;
      onChanged();
      return internalGetSetupProgressFieldBuilder().getBuilder();
    }
    /**
     * <pre>
     * Real-time setup progress reported by the runner during EXECUTION_PENDING.
     * Populated progressively as the worker completes setup steps (sandbox init,
     * workspace provisioning, skill loading, MCP server connection, etc.).
     * Only meaningful while phase == EXECUTION_PENDING. Cleared by the server
     * when phase transitions away from PENDING.
     * </pre>
     *
     * <code>.ai.stigmer.agentic.agentexecution.v1.SetupProgress setup_progress = 18 [json_name = "setupProgress"];</code>
     */
    public ai.stigmer.agentic.agentexecution.v1.SetupProgressOrBuilder getSetupProgressOrBuilder() {
      if (setupProgressBuilder_ != null) {
        return setupProgressBuilder_.getMessageOrBuilder();
      } else {
        return setupProgress_ == null ?
            ai.stigmer.agentic.agentexecution.v1.SetupProgress.getDefaultInstance() : setupProgress_;
      }
    }
    /**
     * <pre>
     * Real-time setup progress reported by the runner during EXECUTION_PENDING.
     * Populated progressively as the worker completes setup steps (sandbox init,
     * workspace provisioning, skill loading, MCP server connection, etc.).
     * Only meaningful while phase == EXECUTION_PENDING. Cleared by the server
     * when phase transitions away from PENDING.
     * </pre>
     *
     * <code>.ai.stigmer.agentic.agentexecution.v1.SetupProgress setup_progress = 18 [json_name = "setupProgress"];</code>
     */
    private com.google.protobuf.SingleFieldBuilder<
        ai.stigmer.agentic.agentexecution.v1.SetupProgress, ai.stigmer.agentic.agentexecution.v1.SetupProgress.Builder, ai.stigmer.agentic.agentexecution.v1.SetupProgressOrBuilder> 
        internalGetSetupProgressFieldBuilder() {
      if (setupProgressBuilder_ == null) {
        setupProgressBuilder_ = new com.google.protobuf.SingleFieldBuilder<
            ai.stigmer.agentic.agentexecution.v1.SetupProgress, ai.stigmer.agentic.agentexecution.v1.SetupProgress.Builder, ai.stigmer.agentic.agentexecution.v1.SetupProgressOrBuilder>(
                getSetupProgress(),
                getParentForChildren(),
                isClean());
        setupProgress_ = null;
      }
      return setupProgressBuilder_;
    }

    private ai.stigmer.agentic.agentexecution.v1.StreamingUsageSummary streamingUsage_;
    private com.google.protobuf.SingleFieldBuilder<
        ai.stigmer.agentic.agentexecution.v1.StreamingUsageSummary, ai.stigmer.agentic.agentexecution.v1.StreamingUsageSummary.Builder, ai.stigmer.agentic.agentexecution.v1.StreamingUsageSummaryOrBuilder> streamingUsageBuilder_;
    /**
     * <pre>
     * Streaming usage summary, updated progressively during execution.
     * </pre>
     *
     * <code>.ai.stigmer.agentic.agentexecution.v1.StreamingUsageSummary streaming_usage = 20 [json_name = "streamingUsage"];</code>
     * @return Whether the streamingUsage field is set.
     */
    public boolean hasStreamingUsage() {
      return ((bitField0_ & 0x00008000) != 0);
    }
    /**
     * <pre>
     * Streaming usage summary, updated progressively during execution.
     * </pre>
     *
     * <code>.ai.stigmer.agentic.agentexecution.v1.StreamingUsageSummary streaming_usage = 20 [json_name = "streamingUsage"];</code>
     * @return The streamingUsage.
     */
    public ai.stigmer.agentic.agentexecution.v1.StreamingUsageSummary getStreamingUsage() {
      if (streamingUsageBuilder_ == null) {
        return streamingUsage_ == null ? ai.stigmer.agentic.agentexecution.v1.StreamingUsageSummary.getDefaultInstance() : streamingUsage_;
      } else {
        return streamingUsageBuilder_.getMessage();
      }
    }
    /**
     * <pre>
     * Streaming usage summary, updated progressively during execution.
     * </pre>
     *
     * <code>.ai.stigmer.agentic.agentexecution.v1.StreamingUsageSummary streaming_usage = 20 [json_name = "streamingUsage"];</code>
     */
    public Builder setStreamingUsage(ai.stigmer.agentic.agentexecution.v1.StreamingUsageSummary value) {
      if (streamingUsageBuilder_ == null) {
        if (value == null) {
          throw new NullPointerException();
        }
        streamingUsage_ = value;
      } else {
        streamingUsageBuilder_.setMessage(value);
      }
      bitField0_ |= 0x00008000;
      onChanged();
      return this;
    }
    /**
     * <pre>
     * Streaming usage summary, updated progressively during execution.
     * </pre>
     *
     * <code>.ai.stigmer.agentic.agentexecution.v1.StreamingUsageSummary streaming_usage = 20 [json_name = "streamingUsage"];</code>
     */
    public Builder setStreamingUsage(
        ai.stigmer.agentic.agentexecution.v1.StreamingUsageSummary.Builder builderForValue) {
      if (streamingUsageBuilder_ == null) {
        streamingUsage_ = builderForValue.build();
      } else {
        streamingUsageBuilder_.setMessage(builderForValue.build());
      }
      bitField0_ |= 0x00008000;
      onChanged();
      return this;
    }
    /**
     * <pre>
     * Streaming usage summary, updated progressively during execution.
     * </pre>
     *
     * <code>.ai.stigmer.agentic.agentexecution.v1.StreamingUsageSummary streaming_usage = 20 [json_name = "streamingUsage"];</code>
     */
    public Builder mergeStreamingUsage(ai.stigmer.agentic.agentexecution.v1.StreamingUsageSummary value) {
      if (streamingUsageBuilder_ == null) {
        if (((bitField0_ & 0x00008000) != 0) &&
          streamingUsage_ != null &&
          streamingUsage_ != ai.stigmer.agentic.agentexecution.v1.StreamingUsageSummary.getDefaultInstance()) {
          getStreamingUsageBuilder().mergeFrom(value);
        } else {
          streamingUsage_ = value;
        }
      } else {
        streamingUsageBuilder_.mergeFrom(value);
      }
      if (streamingUsage_ != null) {
        bitField0_ |= 0x00008000;
        onChanged();
      }
      return this;
    }
    /**
     * <pre>
     * Streaming usage summary, updated progressively during execution.
     * </pre>
     *
     * <code>.ai.stigmer.agentic.agentexecution.v1.StreamingUsageSummary streaming_usage = 20 [json_name = "streamingUsage"];</code>
     */
    public Builder clearStreamingUsage() {
      bitField0_ = (bitField0_ & ~0x00008000);
      streamingUsage_ = null;
      if (streamingUsageBuilder_ != null) {
        streamingUsageBuilder_.dispose();
        streamingUsageBuilder_ = null;
      }
      onChanged();
      return this;
    }
    /**
     * <pre>
     * Streaming usage summary, updated progressively during execution.
     * </pre>
     *
     * <code>.ai.stigmer.agentic.agentexecution.v1.StreamingUsageSummary streaming_usage = 20 [json_name = "streamingUsage"];</code>
     */
    public ai.stigmer.agentic.agentexecution.v1.StreamingUsageSummary.Builder getStreamingUsageBuilder() {
      bitField0_ |= 0x00008000;
      onChanged();
      return internalGetStreamingUsageFieldBuilder().getBuilder();
    }
    /**
     * <pre>
     * Streaming usage summary, updated progressively during execution.
     * </pre>
     *
     * <code>.ai.stigmer.agentic.agentexecution.v1.StreamingUsageSummary streaming_usage = 20 [json_name = "streamingUsage"];</code>
     */
    public ai.stigmer.agentic.agentexecution.v1.StreamingUsageSummaryOrBuilder getStreamingUsageOrBuilder() {
      if (streamingUsageBuilder_ != null) {
        return streamingUsageBuilder_.getMessageOrBuilder();
      } else {
        return streamingUsage_ == null ?
            ai.stigmer.agentic.agentexecution.v1.StreamingUsageSummary.getDefaultInstance() : streamingUsage_;
      }
    }
    /**
     * <pre>
     * Streaming usage summary, updated progressively during execution.
     * </pre>
     *
     * <code>.ai.stigmer.agentic.agentexecution.v1.StreamingUsageSummary streaming_usage = 20 [json_name = "streamingUsage"];</code>
     */
    private com.google.protobuf.SingleFieldBuilder<
        ai.stigmer.agentic.agentexecution.v1.StreamingUsageSummary, ai.stigmer.agentic.agentexecution.v1.StreamingUsageSummary.Builder, ai.stigmer.agentic.agentexecution.v1.StreamingUsageSummaryOrBuilder> 
        internalGetStreamingUsageFieldBuilder() {
      if (streamingUsageBuilder_ == null) {
        streamingUsageBuilder_ = new com.google.protobuf.SingleFieldBuilder<
            ai.stigmer.agentic.agentexecution.v1.StreamingUsageSummary, ai.stigmer.agentic.agentexecution.v1.StreamingUsageSummary.Builder, ai.stigmer.agentic.agentexecution.v1.StreamingUsageSummaryOrBuilder>(
                getStreamingUsage(),
                getParentForChildren(),
                isClean());
        streamingUsage_ = null;
      }
      return streamingUsageBuilder_;
    }

    private com.google.protobuf.Struct structuredOutput_;
    private com.google.protobuf.SingleFieldBuilder<
        com.google.protobuf.Struct, com.google.protobuf.Struct.Builder, com.google.protobuf.StructOrBuilder> structuredOutputBuilder_;
    /**
     * <pre>
     * Structured output extracted from the agent's final response.
     *
     * Populated when ExecutionConfig.structured_output_schema was set and the
     * runner successfully extracted schema-conforming data. Only meaningful
     * when phase is EXECUTION_COMPLETED.
     *
     * For native harness: populated from LangGraph graphState.structuredResponse
     * (ToolStrategy enforcement). For Cursor harness: populated from 3-tier
     * extraction (JSON.parse → fence extraction → extraction LLM fallback).
     *
     * Consumers: frontend architect/refine hooks read this instead of parsing
     * YAML from messages. Workflow callback path reads structured_output from
     * the Temporal activity result (separate channel, same data).
     * </pre>
     *
     * <code>.google.protobuf.Struct structured_output = 21 [json_name = "structuredOutput"];</code>
     * @return Whether the structuredOutput field is set.
     */
    public boolean hasStructuredOutput() {
      return ((bitField0_ & 0x00010000) != 0);
    }
    /**
     * <pre>
     * Structured output extracted from the agent's final response.
     *
     * Populated when ExecutionConfig.structured_output_schema was set and the
     * runner successfully extracted schema-conforming data. Only meaningful
     * when phase is EXECUTION_COMPLETED.
     *
     * For native harness: populated from LangGraph graphState.structuredResponse
     * (ToolStrategy enforcement). For Cursor harness: populated from 3-tier
     * extraction (JSON.parse → fence extraction → extraction LLM fallback).
     *
     * Consumers: frontend architect/refine hooks read this instead of parsing
     * YAML from messages. Workflow callback path reads structured_output from
     * the Temporal activity result (separate channel, same data).
     * </pre>
     *
     * <code>.google.protobuf.Struct structured_output = 21 [json_name = "structuredOutput"];</code>
     * @return The structuredOutput.
     */
    public com.google.protobuf.Struct getStructuredOutput() {
      if (structuredOutputBuilder_ == null) {
        return structuredOutput_ == null ? com.google.protobuf.Struct.getDefaultInstance() : structuredOutput_;
      } else {
        return structuredOutputBuilder_.getMessage();
      }
    }
    /**
     * <pre>
     * Structured output extracted from the agent's final response.
     *
     * Populated when ExecutionConfig.structured_output_schema was set and the
     * runner successfully extracted schema-conforming data. Only meaningful
     * when phase is EXECUTION_COMPLETED.
     *
     * For native harness: populated from LangGraph graphState.structuredResponse
     * (ToolStrategy enforcement). For Cursor harness: populated from 3-tier
     * extraction (JSON.parse → fence extraction → extraction LLM fallback).
     *
     * Consumers: frontend architect/refine hooks read this instead of parsing
     * YAML from messages. Workflow callback path reads structured_output from
     * the Temporal activity result (separate channel, same data).
     * </pre>
     *
     * <code>.google.protobuf.Struct structured_output = 21 [json_name = "structuredOutput"];</code>
     */
    public Builder setStructuredOutput(com.google.protobuf.Struct value) {
      if (structuredOutputBuilder_ == null) {
        if (value == null) {
          throw new NullPointerException();
        }
        structuredOutput_ = value;
      } else {
        structuredOutputBuilder_.setMessage(value);
      }
      bitField0_ |= 0x00010000;
      onChanged();
      return this;
    }
    /**
     * <pre>
     * Structured output extracted from the agent's final response.
     *
     * Populated when ExecutionConfig.structured_output_schema was set and the
     * runner successfully extracted schema-conforming data. Only meaningful
     * when phase is EXECUTION_COMPLETED.
     *
     * For native harness: populated from LangGraph graphState.structuredResponse
     * (ToolStrategy enforcement). For Cursor harness: populated from 3-tier
     * extraction (JSON.parse → fence extraction → extraction LLM fallback).
     *
     * Consumers: frontend architect/refine hooks read this instead of parsing
     * YAML from messages. Workflow callback path reads structured_output from
     * the Temporal activity result (separate channel, same data).
     * </pre>
     *
     * <code>.google.protobuf.Struct structured_output = 21 [json_name = "structuredOutput"];</code>
     */
    public Builder setStructuredOutput(
        com.google.protobuf.Struct.Builder builderForValue) {
      if (structuredOutputBuilder_ == null) {
        structuredOutput_ = builderForValue.build();
      } else {
        structuredOutputBuilder_.setMessage(builderForValue.build());
      }
      bitField0_ |= 0x00010000;
      onChanged();
      return this;
    }
    /**
     * <pre>
     * Structured output extracted from the agent's final response.
     *
     * Populated when ExecutionConfig.structured_output_schema was set and the
     * runner successfully extracted schema-conforming data. Only meaningful
     * when phase is EXECUTION_COMPLETED.
     *
     * For native harness: populated from LangGraph graphState.structuredResponse
     * (ToolStrategy enforcement). For Cursor harness: populated from 3-tier
     * extraction (JSON.parse → fence extraction → extraction LLM fallback).
     *
     * Consumers: frontend architect/refine hooks read this instead of parsing
     * YAML from messages. Workflow callback path reads structured_output from
     * the Temporal activity result (separate channel, same data).
     * </pre>
     *
     * <code>.google.protobuf.Struct structured_output = 21 [json_name = "structuredOutput"];</code>
     */
    public Builder mergeStructuredOutput(com.google.protobuf.Struct value) {
      if (structuredOutputBuilder_ == null) {
        if (((bitField0_ & 0x00010000) != 0) &&
          structuredOutput_ != null &&
          structuredOutput_ != com.google.protobuf.Struct.getDefaultInstance()) {
          getStructuredOutputBuilder().mergeFrom(value);
        } else {
          structuredOutput_ = value;
        }
      } else {
        structuredOutputBuilder_.mergeFrom(value);
      }
      if (structuredOutput_ != null) {
        bitField0_ |= 0x00010000;
        onChanged();
      }
      return this;
    }
    /**
     * <pre>
     * Structured output extracted from the agent's final response.
     *
     * Populated when ExecutionConfig.structured_output_schema was set and the
     * runner successfully extracted schema-conforming data. Only meaningful
     * when phase is EXECUTION_COMPLETED.
     *
     * For native harness: populated from LangGraph graphState.structuredResponse
     * (ToolStrategy enforcement). For Cursor harness: populated from 3-tier
     * extraction (JSON.parse → fence extraction → extraction LLM fallback).
     *
     * Consumers: frontend architect/refine hooks read this instead of parsing
     * YAML from messages. Workflow callback path reads structured_output from
     * the Temporal activity result (separate channel, same data).
     * </pre>
     *
     * <code>.google.protobuf.Struct structured_output = 21 [json_name = "structuredOutput"];</code>
     */
    public Builder clearStructuredOutput() {
      bitField0_ = (bitField0_ & ~0x00010000);
      structuredOutput_ = null;
      if (structuredOutputBuilder_ != null) {
        structuredOutputBuilder_.dispose();
        structuredOutputBuilder_ = null;
      }
      onChanged();
      return this;
    }
    /**
     * <pre>
     * Structured output extracted from the agent's final response.
     *
     * Populated when ExecutionConfig.structured_output_schema was set and the
     * runner successfully extracted schema-conforming data. Only meaningful
     * when phase is EXECUTION_COMPLETED.
     *
     * For native harness: populated from LangGraph graphState.structuredResponse
     * (ToolStrategy enforcement). For Cursor harness: populated from 3-tier
     * extraction (JSON.parse → fence extraction → extraction LLM fallback).
     *
     * Consumers: frontend architect/refine hooks read this instead of parsing
     * YAML from messages. Workflow callback path reads structured_output from
     * the Temporal activity result (separate channel, same data).
     * </pre>
     *
     * <code>.google.protobuf.Struct structured_output = 21 [json_name = "structuredOutput"];</code>
     */
    public com.google.protobuf.Struct.Builder getStructuredOutputBuilder() {
      bitField0_ |= 0x00010000;
      onChanged();
      return internalGetStructuredOutputFieldBuilder().getBuilder();
    }
    /**
     * <pre>
     * Structured output extracted from the agent's final response.
     *
     * Populated when ExecutionConfig.structured_output_schema was set and the
     * runner successfully extracted schema-conforming data. Only meaningful
     * when phase is EXECUTION_COMPLETED.
     *
     * For native harness: populated from LangGraph graphState.structuredResponse
     * (ToolStrategy enforcement). For Cursor harness: populated from 3-tier
     * extraction (JSON.parse → fence extraction → extraction LLM fallback).
     *
     * Consumers: frontend architect/refine hooks read this instead of parsing
     * YAML from messages. Workflow callback path reads structured_output from
     * the Temporal activity result (separate channel, same data).
     * </pre>
     *
     * <code>.google.protobuf.Struct structured_output = 21 [json_name = "structuredOutput"];</code>
     */
    public com.google.protobuf.StructOrBuilder getStructuredOutputOrBuilder() {
      if (structuredOutputBuilder_ != null) {
        return structuredOutputBuilder_.getMessageOrBuilder();
      } else {
        return structuredOutput_ == null ?
            com.google.protobuf.Struct.getDefaultInstance() : structuredOutput_;
      }
    }
    /**
     * <pre>
     * Structured output extracted from the agent's final response.
     *
     * Populated when ExecutionConfig.structured_output_schema was set and the
     * runner successfully extracted schema-conforming data. Only meaningful
     * when phase is EXECUTION_COMPLETED.
     *
     * For native harness: populated from LangGraph graphState.structuredResponse
     * (ToolStrategy enforcement). For Cursor harness: populated from 3-tier
     * extraction (JSON.parse → fence extraction → extraction LLM fallback).
     *
     * Consumers: frontend architect/refine hooks read this instead of parsing
     * YAML from messages. Workflow callback path reads structured_output from
     * the Temporal activity result (separate channel, same data).
     * </pre>
     *
     * <code>.google.protobuf.Struct structured_output = 21 [json_name = "structuredOutput"];</code>
     */
    private com.google.protobuf.SingleFieldBuilder<
        com.google.protobuf.Struct, com.google.protobuf.Struct.Builder, com.google.protobuf.StructOrBuilder> 
        internalGetStructuredOutputFieldBuilder() {
      if (structuredOutputBuilder_ == null) {
        structuredOutputBuilder_ = new com.google.protobuf.SingleFieldBuilder<
            com.google.protobuf.Struct, com.google.protobuf.Struct.Builder, com.google.protobuf.StructOrBuilder>(
                getStructuredOutput(),
                getParentForChildren(),
                isClean());
        structuredOutput_ = null;
      }
      return structuredOutputBuilder_;
    }

    private java.util.List<ai.stigmer.agentic.agentexecution.v1.FileChangeSet> fileChangeSets_ =
      java.util.Collections.emptyList();
    private void ensureFileChangeSetsIsMutable() {
      if (!((bitField0_ & 0x00020000) != 0)) {
        fileChangeSets_ = new java.util.ArrayList<ai.stigmer.agentic.agentexecution.v1.FileChangeSet>(fileChangeSets_);
        bitField0_ |= 0x00020000;
       }
    }

    private com.google.protobuf.RepeatedFieldBuilder<
        ai.stigmer.agentic.agentexecution.v1.FileChangeSet, ai.stigmer.agentic.agentexecution.v1.FileChangeSet.Builder, ai.stigmer.agentic.agentexecution.v1.FileChangeSetOrBuilder> fileChangeSetsBuilder_;

    /**
     * <pre>
     * Server-computed projection of file change sets awaiting or completing
     * review for this execution.
     *
     * &#64;internal
     *
     * Recomputed on every status write from file_review_event_stream (never
     * merged), so it is always consistent with the authoritative ledger — exactly
     * as pending_approvals is recomputed from the message scan / approval stream.
     * Empty until the runner's capture activity authors events (Phase 2). A
     * terminal execution projects no actionable review. See FileChangeSet.
     *
     * Field 23: appended after approval_event_stream (22), the prior maximum.
     * </pre>
     *
     * <code>repeated .ai.stigmer.agentic.agentexecution.v1.FileChangeSet file_change_sets = 23 [json_name = "fileChangeSets"];</code>
     */
    public java.util.List<ai.stigmer.agentic.agentexecution.v1.FileChangeSet> getFileChangeSetsList() {
      if (fileChangeSetsBuilder_ == null) {
        return java.util.Collections.unmodifiableList(fileChangeSets_);
      } else {
        return fileChangeSetsBuilder_.getMessageList();
      }
    }
    /**
     * <pre>
     * Server-computed projection of file change sets awaiting or completing
     * review for this execution.
     *
     * &#64;internal
     *
     * Recomputed on every status write from file_review_event_stream (never
     * merged), so it is always consistent with the authoritative ledger — exactly
     * as pending_approvals is recomputed from the message scan / approval stream.
     * Empty until the runner's capture activity authors events (Phase 2). A
     * terminal execution projects no actionable review. See FileChangeSet.
     *
     * Field 23: appended after approval_event_stream (22), the prior maximum.
     * </pre>
     *
     * <code>repeated .ai.stigmer.agentic.agentexecution.v1.FileChangeSet file_change_sets = 23 [json_name = "fileChangeSets"];</code>
     */
    public int getFileChangeSetsCount() {
      if (fileChangeSetsBuilder_ == null) {
        return fileChangeSets_.size();
      } else {
        return fileChangeSetsBuilder_.getCount();
      }
    }
    /**
     * <pre>
     * Server-computed projection of file change sets awaiting or completing
     * review for this execution.
     *
     * &#64;internal
     *
     * Recomputed on every status write from file_review_event_stream (never
     * merged), so it is always consistent with the authoritative ledger — exactly
     * as pending_approvals is recomputed from the message scan / approval stream.
     * Empty until the runner's capture activity authors events (Phase 2). A
     * terminal execution projects no actionable review. See FileChangeSet.
     *
     * Field 23: appended after approval_event_stream (22), the prior maximum.
     * </pre>
     *
     * <code>repeated .ai.stigmer.agentic.agentexecution.v1.FileChangeSet file_change_sets = 23 [json_name = "fileChangeSets"];</code>
     */
    public ai.stigmer.agentic.agentexecution.v1.FileChangeSet getFileChangeSets(int index) {
      if (fileChangeSetsBuilder_ == null) {
        return fileChangeSets_.get(index);
      } else {
        return fileChangeSetsBuilder_.getMessage(index);
      }
    }
    /**
     * <pre>
     * Server-computed projection of file change sets awaiting or completing
     * review for this execution.
     *
     * &#64;internal
     *
     * Recomputed on every status write from file_review_event_stream (never
     * merged), so it is always consistent with the authoritative ledger — exactly
     * as pending_approvals is recomputed from the message scan / approval stream.
     * Empty until the runner's capture activity authors events (Phase 2). A
     * terminal execution projects no actionable review. See FileChangeSet.
     *
     * Field 23: appended after approval_event_stream (22), the prior maximum.
     * </pre>
     *
     * <code>repeated .ai.stigmer.agentic.agentexecution.v1.FileChangeSet file_change_sets = 23 [json_name = "fileChangeSets"];</code>
     */
    public Builder setFileChangeSets(
        int index, ai.stigmer.agentic.agentexecution.v1.FileChangeSet value) {
      if (fileChangeSetsBuilder_ == null) {
        if (value == null) {
          throw new NullPointerException();
        }
        ensureFileChangeSetsIsMutable();
        fileChangeSets_.set(index, value);
        onChanged();
      } else {
        fileChangeSetsBuilder_.setMessage(index, value);
      }
      return this;
    }
    /**
     * <pre>
     * Server-computed projection of file change sets awaiting or completing
     * review for this execution.
     *
     * &#64;internal
     *
     * Recomputed on every status write from file_review_event_stream (never
     * merged), so it is always consistent with the authoritative ledger — exactly
     * as pending_approvals is recomputed from the message scan / approval stream.
     * Empty until the runner's capture activity authors events (Phase 2). A
     * terminal execution projects no actionable review. See FileChangeSet.
     *
     * Field 23: appended after approval_event_stream (22), the prior maximum.
     * </pre>
     *
     * <code>repeated .ai.stigmer.agentic.agentexecution.v1.FileChangeSet file_change_sets = 23 [json_name = "fileChangeSets"];</code>
     */
    public Builder setFileChangeSets(
        int index, ai.stigmer.agentic.agentexecution.v1.FileChangeSet.Builder builderForValue) {
      if (fileChangeSetsBuilder_ == null) {
        ensureFileChangeSetsIsMutable();
        fileChangeSets_.set(index, builderForValue.build());
        onChanged();
      } else {
        fileChangeSetsBuilder_.setMessage(index, builderForValue.build());
      }
      return this;
    }
    /**
     * <pre>
     * Server-computed projection of file change sets awaiting or completing
     * review for this execution.
     *
     * &#64;internal
     *
     * Recomputed on every status write from file_review_event_stream (never
     * merged), so it is always consistent with the authoritative ledger — exactly
     * as pending_approvals is recomputed from the message scan / approval stream.
     * Empty until the runner's capture activity authors events (Phase 2). A
     * terminal execution projects no actionable review. See FileChangeSet.
     *
     * Field 23: appended after approval_event_stream (22), the prior maximum.
     * </pre>
     *
     * <code>repeated .ai.stigmer.agentic.agentexecution.v1.FileChangeSet file_change_sets = 23 [json_name = "fileChangeSets"];</code>
     */
    public Builder addFileChangeSets(ai.stigmer.agentic.agentexecution.v1.FileChangeSet value) {
      if (fileChangeSetsBuilder_ == null) {
        if (value == null) {
          throw new NullPointerException();
        }
        ensureFileChangeSetsIsMutable();
        fileChangeSets_.add(value);
        onChanged();
      } else {
        fileChangeSetsBuilder_.addMessage(value);
      }
      return this;
    }
    /**
     * <pre>
     * Server-computed projection of file change sets awaiting or completing
     * review for this execution.
     *
     * &#64;internal
     *
     * Recomputed on every status write from file_review_event_stream (never
     * merged), so it is always consistent with the authoritative ledger — exactly
     * as pending_approvals is recomputed from the message scan / approval stream.
     * Empty until the runner's capture activity authors events (Phase 2). A
     * terminal execution projects no actionable review. See FileChangeSet.
     *
     * Field 23: appended after approval_event_stream (22), the prior maximum.
     * </pre>
     *
     * <code>repeated .ai.stigmer.agentic.agentexecution.v1.FileChangeSet file_change_sets = 23 [json_name = "fileChangeSets"];</code>
     */
    public Builder addFileChangeSets(
        int index, ai.stigmer.agentic.agentexecution.v1.FileChangeSet value) {
      if (fileChangeSetsBuilder_ == null) {
        if (value == null) {
          throw new NullPointerException();
        }
        ensureFileChangeSetsIsMutable();
        fileChangeSets_.add(index, value);
        onChanged();
      } else {
        fileChangeSetsBuilder_.addMessage(index, value);
      }
      return this;
    }
    /**
     * <pre>
     * Server-computed projection of file change sets awaiting or completing
     * review for this execution.
     *
     * &#64;internal
     *
     * Recomputed on every status write from file_review_event_stream (never
     * merged), so it is always consistent with the authoritative ledger — exactly
     * as pending_approvals is recomputed from the message scan / approval stream.
     * Empty until the runner's capture activity authors events (Phase 2). A
     * terminal execution projects no actionable review. See FileChangeSet.
     *
     * Field 23: appended after approval_event_stream (22), the prior maximum.
     * </pre>
     *
     * <code>repeated .ai.stigmer.agentic.agentexecution.v1.FileChangeSet file_change_sets = 23 [json_name = "fileChangeSets"];</code>
     */
    public Builder addFileChangeSets(
        ai.stigmer.agentic.agentexecution.v1.FileChangeSet.Builder builderForValue) {
      if (fileChangeSetsBuilder_ == null) {
        ensureFileChangeSetsIsMutable();
        fileChangeSets_.add(builderForValue.build());
        onChanged();
      } else {
        fileChangeSetsBuilder_.addMessage(builderForValue.build());
      }
      return this;
    }
    /**
     * <pre>
     * Server-computed projection of file change sets awaiting or completing
     * review for this execution.
     *
     * &#64;internal
     *
     * Recomputed on every status write from file_review_event_stream (never
     * merged), so it is always consistent with the authoritative ledger — exactly
     * as pending_approvals is recomputed from the message scan / approval stream.
     * Empty until the runner's capture activity authors events (Phase 2). A
     * terminal execution projects no actionable review. See FileChangeSet.
     *
     * Field 23: appended after approval_event_stream (22), the prior maximum.
     * </pre>
     *
     * <code>repeated .ai.stigmer.agentic.agentexecution.v1.FileChangeSet file_change_sets = 23 [json_name = "fileChangeSets"];</code>
     */
    public Builder addFileChangeSets(
        int index, ai.stigmer.agentic.agentexecution.v1.FileChangeSet.Builder builderForValue) {
      if (fileChangeSetsBuilder_ == null) {
        ensureFileChangeSetsIsMutable();
        fileChangeSets_.add(index, builderForValue.build());
        onChanged();
      } else {
        fileChangeSetsBuilder_.addMessage(index, builderForValue.build());
      }
      return this;
    }
    /**
     * <pre>
     * Server-computed projection of file change sets awaiting or completing
     * review for this execution.
     *
     * &#64;internal
     *
     * Recomputed on every status write from file_review_event_stream (never
     * merged), so it is always consistent with the authoritative ledger — exactly
     * as pending_approvals is recomputed from the message scan / approval stream.
     * Empty until the runner's capture activity authors events (Phase 2). A
     * terminal execution projects no actionable review. See FileChangeSet.
     *
     * Field 23: appended after approval_event_stream (22), the prior maximum.
     * </pre>
     *
     * <code>repeated .ai.stigmer.agentic.agentexecution.v1.FileChangeSet file_change_sets = 23 [json_name = "fileChangeSets"];</code>
     */
    public Builder addAllFileChangeSets(
        java.lang.Iterable<? extends ai.stigmer.agentic.agentexecution.v1.FileChangeSet> values) {
      if (fileChangeSetsBuilder_ == null) {
        ensureFileChangeSetsIsMutable();
        com.google.protobuf.AbstractMessageLite.Builder.addAll(
            values, fileChangeSets_);
        onChanged();
      } else {
        fileChangeSetsBuilder_.addAllMessages(values);
      }
      return this;
    }
    /**
     * <pre>
     * Server-computed projection of file change sets awaiting or completing
     * review for this execution.
     *
     * &#64;internal
     *
     * Recomputed on every status write from file_review_event_stream (never
     * merged), so it is always consistent with the authoritative ledger — exactly
     * as pending_approvals is recomputed from the message scan / approval stream.
     * Empty until the runner's capture activity authors events (Phase 2). A
     * terminal execution projects no actionable review. See FileChangeSet.
     *
     * Field 23: appended after approval_event_stream (22), the prior maximum.
     * </pre>
     *
     * <code>repeated .ai.stigmer.agentic.agentexecution.v1.FileChangeSet file_change_sets = 23 [json_name = "fileChangeSets"];</code>
     */
    public Builder clearFileChangeSets() {
      if (fileChangeSetsBuilder_ == null) {
        fileChangeSets_ = java.util.Collections.emptyList();
        bitField0_ = (bitField0_ & ~0x00020000);
        onChanged();
      } else {
        fileChangeSetsBuilder_.clear();
      }
      return this;
    }
    /**
     * <pre>
     * Server-computed projection of file change sets awaiting or completing
     * review for this execution.
     *
     * &#64;internal
     *
     * Recomputed on every status write from file_review_event_stream (never
     * merged), so it is always consistent with the authoritative ledger — exactly
     * as pending_approvals is recomputed from the message scan / approval stream.
     * Empty until the runner's capture activity authors events (Phase 2). A
     * terminal execution projects no actionable review. See FileChangeSet.
     *
     * Field 23: appended after approval_event_stream (22), the prior maximum.
     * </pre>
     *
     * <code>repeated .ai.stigmer.agentic.agentexecution.v1.FileChangeSet file_change_sets = 23 [json_name = "fileChangeSets"];</code>
     */
    public Builder removeFileChangeSets(int index) {
      if (fileChangeSetsBuilder_ == null) {
        ensureFileChangeSetsIsMutable();
        fileChangeSets_.remove(index);
        onChanged();
      } else {
        fileChangeSetsBuilder_.remove(index);
      }
      return this;
    }
    /**
     * <pre>
     * Server-computed projection of file change sets awaiting or completing
     * review for this execution.
     *
     * &#64;internal
     *
     * Recomputed on every status write from file_review_event_stream (never
     * merged), so it is always consistent with the authoritative ledger — exactly
     * as pending_approvals is recomputed from the message scan / approval stream.
     * Empty until the runner's capture activity authors events (Phase 2). A
     * terminal execution projects no actionable review. See FileChangeSet.
     *
     * Field 23: appended after approval_event_stream (22), the prior maximum.
     * </pre>
     *
     * <code>repeated .ai.stigmer.agentic.agentexecution.v1.FileChangeSet file_change_sets = 23 [json_name = "fileChangeSets"];</code>
     */
    public ai.stigmer.agentic.agentexecution.v1.FileChangeSet.Builder getFileChangeSetsBuilder(
        int index) {
      return internalGetFileChangeSetsFieldBuilder().getBuilder(index);
    }
    /**
     * <pre>
     * Server-computed projection of file change sets awaiting or completing
     * review for this execution.
     *
     * &#64;internal
     *
     * Recomputed on every status write from file_review_event_stream (never
     * merged), so it is always consistent with the authoritative ledger — exactly
     * as pending_approvals is recomputed from the message scan / approval stream.
     * Empty until the runner's capture activity authors events (Phase 2). A
     * terminal execution projects no actionable review. See FileChangeSet.
     *
     * Field 23: appended after approval_event_stream (22), the prior maximum.
     * </pre>
     *
     * <code>repeated .ai.stigmer.agentic.agentexecution.v1.FileChangeSet file_change_sets = 23 [json_name = "fileChangeSets"];</code>
     */
    public ai.stigmer.agentic.agentexecution.v1.FileChangeSetOrBuilder getFileChangeSetsOrBuilder(
        int index) {
      if (fileChangeSetsBuilder_ == null) {
        return fileChangeSets_.get(index);  } else {
        return fileChangeSetsBuilder_.getMessageOrBuilder(index);
      }
    }
    /**
     * <pre>
     * Server-computed projection of file change sets awaiting or completing
     * review for this execution.
     *
     * &#64;internal
     *
     * Recomputed on every status write from file_review_event_stream (never
     * merged), so it is always consistent with the authoritative ledger — exactly
     * as pending_approvals is recomputed from the message scan / approval stream.
     * Empty until the runner's capture activity authors events (Phase 2). A
     * terminal execution projects no actionable review. See FileChangeSet.
     *
     * Field 23: appended after approval_event_stream (22), the prior maximum.
     * </pre>
     *
     * <code>repeated .ai.stigmer.agentic.agentexecution.v1.FileChangeSet file_change_sets = 23 [json_name = "fileChangeSets"];</code>
     */
    public java.util.List<? extends ai.stigmer.agentic.agentexecution.v1.FileChangeSetOrBuilder> 
         getFileChangeSetsOrBuilderList() {
      if (fileChangeSetsBuilder_ != null) {
        return fileChangeSetsBuilder_.getMessageOrBuilderList();
      } else {
        return java.util.Collections.unmodifiableList(fileChangeSets_);
      }
    }
    /**
     * <pre>
     * Server-computed projection of file change sets awaiting or completing
     * review for this execution.
     *
     * &#64;internal
     *
     * Recomputed on every status write from file_review_event_stream (never
     * merged), so it is always consistent with the authoritative ledger — exactly
     * as pending_approvals is recomputed from the message scan / approval stream.
     * Empty until the runner's capture activity authors events (Phase 2). A
     * terminal execution projects no actionable review. See FileChangeSet.
     *
     * Field 23: appended after approval_event_stream (22), the prior maximum.
     * </pre>
     *
     * <code>repeated .ai.stigmer.agentic.agentexecution.v1.FileChangeSet file_change_sets = 23 [json_name = "fileChangeSets"];</code>
     */
    public ai.stigmer.agentic.agentexecution.v1.FileChangeSet.Builder addFileChangeSetsBuilder() {
      return internalGetFileChangeSetsFieldBuilder().addBuilder(
          ai.stigmer.agentic.agentexecution.v1.FileChangeSet.getDefaultInstance());
    }
    /**
     * <pre>
     * Server-computed projection of file change sets awaiting or completing
     * review for this execution.
     *
     * &#64;internal
     *
     * Recomputed on every status write from file_review_event_stream (never
     * merged), so it is always consistent with the authoritative ledger — exactly
     * as pending_approvals is recomputed from the message scan / approval stream.
     * Empty until the runner's capture activity authors events (Phase 2). A
     * terminal execution projects no actionable review. See FileChangeSet.
     *
     * Field 23: appended after approval_event_stream (22), the prior maximum.
     * </pre>
     *
     * <code>repeated .ai.stigmer.agentic.agentexecution.v1.FileChangeSet file_change_sets = 23 [json_name = "fileChangeSets"];</code>
     */
    public ai.stigmer.agentic.agentexecution.v1.FileChangeSet.Builder addFileChangeSetsBuilder(
        int index) {
      return internalGetFileChangeSetsFieldBuilder().addBuilder(
          index, ai.stigmer.agentic.agentexecution.v1.FileChangeSet.getDefaultInstance());
    }
    /**
     * <pre>
     * Server-computed projection of file change sets awaiting or completing
     * review for this execution.
     *
     * &#64;internal
     *
     * Recomputed on every status write from file_review_event_stream (never
     * merged), so it is always consistent with the authoritative ledger — exactly
     * as pending_approvals is recomputed from the message scan / approval stream.
     * Empty until the runner's capture activity authors events (Phase 2). A
     * terminal execution projects no actionable review. See FileChangeSet.
     *
     * Field 23: appended after approval_event_stream (22), the prior maximum.
     * </pre>
     *
     * <code>repeated .ai.stigmer.agentic.agentexecution.v1.FileChangeSet file_change_sets = 23 [json_name = "fileChangeSets"];</code>
     */
    public java.util.List<ai.stigmer.agentic.agentexecution.v1.FileChangeSet.Builder> 
         getFileChangeSetsBuilderList() {
      return internalGetFileChangeSetsFieldBuilder().getBuilderList();
    }
    private com.google.protobuf.RepeatedFieldBuilder<
        ai.stigmer.agentic.agentexecution.v1.FileChangeSet, ai.stigmer.agentic.agentexecution.v1.FileChangeSet.Builder, ai.stigmer.agentic.agentexecution.v1.FileChangeSetOrBuilder> 
        internalGetFileChangeSetsFieldBuilder() {
      if (fileChangeSetsBuilder_ == null) {
        fileChangeSetsBuilder_ = new com.google.protobuf.RepeatedFieldBuilder<
            ai.stigmer.agentic.agentexecution.v1.FileChangeSet, ai.stigmer.agentic.agentexecution.v1.FileChangeSet.Builder, ai.stigmer.agentic.agentexecution.v1.FileChangeSetOrBuilder>(
                fileChangeSets_,
                ((bitField0_ & 0x00020000) != 0),
                getParentForChildren(),
                isClean());
        fileChangeSets_ = null;
      }
      return fileChangeSetsBuilder_;
    }

    private ai.stigmer.agentic.agentexecution.v1.FileReviewEventStream fileReviewEventStream_;
    private com.google.protobuf.SingleFieldBuilder<
        ai.stigmer.agentic.agentexecution.v1.FileReviewEventStream, ai.stigmer.agentic.agentexecution.v1.FileReviewEventStream.Builder, ai.stigmer.agentic.agentexecution.v1.FileReviewEventStreamOrBuilder> fileReviewEventStreamBuilder_;
    /**
     * <pre>
     * Server-owned, append-only record of every file-review event (baseline /
     * candidate capture, file decisions, reconcile, failures).
     *
     * &#64;internal
     *
     * The persisted source of truth that file_change_sets is projected from — a
     * sibling of approval_event_stream for the file-review lifecycle (the
     * ApprovalEvent oneof is closed, so file events cannot ride it). Appends are
     * keyed by the deterministic FileReviewEvent.event_id, so the stream is
     * idempotent under retries.
     *
     * Write contract (one writer per event type): the runner CONTRIBUTES its
     * capture/reconcile events (BASELINE_CAPTURED / CANDIDATE_CAPTURED /
     * RECONCILED / FAILED) by carrying them here on UpdateStatus; the server folds
     * them into the stored stream append-only, by event_id, on the freshly-loaded
     * execution under the write lock. The runner can never replace an existing
     * event and never authors FILE_DECIDED (the server drops a runner-sent
     * decision); FILE_DECIDED is authored solely by SubmitFileDecision. The stored
     * stream is otherwise preserved in place across writes. See
     * FileReviewEventStream.
     *
     * Field 24: appended after file_change_sets (23), the prior maximum.
     * </pre>
     *
     * <code>.ai.stigmer.agentic.agentexecution.v1.FileReviewEventStream file_review_event_stream = 24 [json_name = "fileReviewEventStream"];</code>
     * @return Whether the fileReviewEventStream field is set.
     */
    public boolean hasFileReviewEventStream() {
      return ((bitField0_ & 0x00040000) != 0);
    }
    /**
     * <pre>
     * Server-owned, append-only record of every file-review event (baseline /
     * candidate capture, file decisions, reconcile, failures).
     *
     * &#64;internal
     *
     * The persisted source of truth that file_change_sets is projected from — a
     * sibling of approval_event_stream for the file-review lifecycle (the
     * ApprovalEvent oneof is closed, so file events cannot ride it). Appends are
     * keyed by the deterministic FileReviewEvent.event_id, so the stream is
     * idempotent under retries.
     *
     * Write contract (one writer per event type): the runner CONTRIBUTES its
     * capture/reconcile events (BASELINE_CAPTURED / CANDIDATE_CAPTURED /
     * RECONCILED / FAILED) by carrying them here on UpdateStatus; the server folds
     * them into the stored stream append-only, by event_id, on the freshly-loaded
     * execution under the write lock. The runner can never replace an existing
     * event and never authors FILE_DECIDED (the server drops a runner-sent
     * decision); FILE_DECIDED is authored solely by SubmitFileDecision. The stored
     * stream is otherwise preserved in place across writes. See
     * FileReviewEventStream.
     *
     * Field 24: appended after file_change_sets (23), the prior maximum.
     * </pre>
     *
     * <code>.ai.stigmer.agentic.agentexecution.v1.FileReviewEventStream file_review_event_stream = 24 [json_name = "fileReviewEventStream"];</code>
     * @return The fileReviewEventStream.
     */
    public ai.stigmer.agentic.agentexecution.v1.FileReviewEventStream getFileReviewEventStream() {
      if (fileReviewEventStreamBuilder_ == null) {
        return fileReviewEventStream_ == null ? ai.stigmer.agentic.agentexecution.v1.FileReviewEventStream.getDefaultInstance() : fileReviewEventStream_;
      } else {
        return fileReviewEventStreamBuilder_.getMessage();
      }
    }
    /**
     * <pre>
     * Server-owned, append-only record of every file-review event (baseline /
     * candidate capture, file decisions, reconcile, failures).
     *
     * &#64;internal
     *
     * The persisted source of truth that file_change_sets is projected from — a
     * sibling of approval_event_stream for the file-review lifecycle (the
     * ApprovalEvent oneof is closed, so file events cannot ride it). Appends are
     * keyed by the deterministic FileReviewEvent.event_id, so the stream is
     * idempotent under retries.
     *
     * Write contract (one writer per event type): the runner CONTRIBUTES its
     * capture/reconcile events (BASELINE_CAPTURED / CANDIDATE_CAPTURED /
     * RECONCILED / FAILED) by carrying them here on UpdateStatus; the server folds
     * them into the stored stream append-only, by event_id, on the freshly-loaded
     * execution under the write lock. The runner can never replace an existing
     * event and never authors FILE_DECIDED (the server drops a runner-sent
     * decision); FILE_DECIDED is authored solely by SubmitFileDecision. The stored
     * stream is otherwise preserved in place across writes. See
     * FileReviewEventStream.
     *
     * Field 24: appended after file_change_sets (23), the prior maximum.
     * </pre>
     *
     * <code>.ai.stigmer.agentic.agentexecution.v1.FileReviewEventStream file_review_event_stream = 24 [json_name = "fileReviewEventStream"];</code>
     */
    public Builder setFileReviewEventStream(ai.stigmer.agentic.agentexecution.v1.FileReviewEventStream value) {
      if (fileReviewEventStreamBuilder_ == null) {
        if (value == null) {
          throw new NullPointerException();
        }
        fileReviewEventStream_ = value;
      } else {
        fileReviewEventStreamBuilder_.setMessage(value);
      }
      bitField0_ |= 0x00040000;
      onChanged();
      return this;
    }
    /**
     * <pre>
     * Server-owned, append-only record of every file-review event (baseline /
     * candidate capture, file decisions, reconcile, failures).
     *
     * &#64;internal
     *
     * The persisted source of truth that file_change_sets is projected from — a
     * sibling of approval_event_stream for the file-review lifecycle (the
     * ApprovalEvent oneof is closed, so file events cannot ride it). Appends are
     * keyed by the deterministic FileReviewEvent.event_id, so the stream is
     * idempotent under retries.
     *
     * Write contract (one writer per event type): the runner CONTRIBUTES its
     * capture/reconcile events (BASELINE_CAPTURED / CANDIDATE_CAPTURED /
     * RECONCILED / FAILED) by carrying them here on UpdateStatus; the server folds
     * them into the stored stream append-only, by event_id, on the freshly-loaded
     * execution under the write lock. The runner can never replace an existing
     * event and never authors FILE_DECIDED (the server drops a runner-sent
     * decision); FILE_DECIDED is authored solely by SubmitFileDecision. The stored
     * stream is otherwise preserved in place across writes. See
     * FileReviewEventStream.
     *
     * Field 24: appended after file_change_sets (23), the prior maximum.
     * </pre>
     *
     * <code>.ai.stigmer.agentic.agentexecution.v1.FileReviewEventStream file_review_event_stream = 24 [json_name = "fileReviewEventStream"];</code>
     */
    public Builder setFileReviewEventStream(
        ai.stigmer.agentic.agentexecution.v1.FileReviewEventStream.Builder builderForValue) {
      if (fileReviewEventStreamBuilder_ == null) {
        fileReviewEventStream_ = builderForValue.build();
      } else {
        fileReviewEventStreamBuilder_.setMessage(builderForValue.build());
      }
      bitField0_ |= 0x00040000;
      onChanged();
      return this;
    }
    /**
     * <pre>
     * Server-owned, append-only record of every file-review event (baseline /
     * candidate capture, file decisions, reconcile, failures).
     *
     * &#64;internal
     *
     * The persisted source of truth that file_change_sets is projected from — a
     * sibling of approval_event_stream for the file-review lifecycle (the
     * ApprovalEvent oneof is closed, so file events cannot ride it). Appends are
     * keyed by the deterministic FileReviewEvent.event_id, so the stream is
     * idempotent under retries.
     *
     * Write contract (one writer per event type): the runner CONTRIBUTES its
     * capture/reconcile events (BASELINE_CAPTURED / CANDIDATE_CAPTURED /
     * RECONCILED / FAILED) by carrying them here on UpdateStatus; the server folds
     * them into the stored stream append-only, by event_id, on the freshly-loaded
     * execution under the write lock. The runner can never replace an existing
     * event and never authors FILE_DECIDED (the server drops a runner-sent
     * decision); FILE_DECIDED is authored solely by SubmitFileDecision. The stored
     * stream is otherwise preserved in place across writes. See
     * FileReviewEventStream.
     *
     * Field 24: appended after file_change_sets (23), the prior maximum.
     * </pre>
     *
     * <code>.ai.stigmer.agentic.agentexecution.v1.FileReviewEventStream file_review_event_stream = 24 [json_name = "fileReviewEventStream"];</code>
     */
    public Builder mergeFileReviewEventStream(ai.stigmer.agentic.agentexecution.v1.FileReviewEventStream value) {
      if (fileReviewEventStreamBuilder_ == null) {
        if (((bitField0_ & 0x00040000) != 0) &&
          fileReviewEventStream_ != null &&
          fileReviewEventStream_ != ai.stigmer.agentic.agentexecution.v1.FileReviewEventStream.getDefaultInstance()) {
          getFileReviewEventStreamBuilder().mergeFrom(value);
        } else {
          fileReviewEventStream_ = value;
        }
      } else {
        fileReviewEventStreamBuilder_.mergeFrom(value);
      }
      if (fileReviewEventStream_ != null) {
        bitField0_ |= 0x00040000;
        onChanged();
      }
      return this;
    }
    /**
     * <pre>
     * Server-owned, append-only record of every file-review event (baseline /
     * candidate capture, file decisions, reconcile, failures).
     *
     * &#64;internal
     *
     * The persisted source of truth that file_change_sets is projected from — a
     * sibling of approval_event_stream for the file-review lifecycle (the
     * ApprovalEvent oneof is closed, so file events cannot ride it). Appends are
     * keyed by the deterministic FileReviewEvent.event_id, so the stream is
     * idempotent under retries.
     *
     * Write contract (one writer per event type): the runner CONTRIBUTES its
     * capture/reconcile events (BASELINE_CAPTURED / CANDIDATE_CAPTURED /
     * RECONCILED / FAILED) by carrying them here on UpdateStatus; the server folds
     * them into the stored stream append-only, by event_id, on the freshly-loaded
     * execution under the write lock. The runner can never replace an existing
     * event and never authors FILE_DECIDED (the server drops a runner-sent
     * decision); FILE_DECIDED is authored solely by SubmitFileDecision. The stored
     * stream is otherwise preserved in place across writes. See
     * FileReviewEventStream.
     *
     * Field 24: appended after file_change_sets (23), the prior maximum.
     * </pre>
     *
     * <code>.ai.stigmer.agentic.agentexecution.v1.FileReviewEventStream file_review_event_stream = 24 [json_name = "fileReviewEventStream"];</code>
     */
    public Builder clearFileReviewEventStream() {
      bitField0_ = (bitField0_ & ~0x00040000);
      fileReviewEventStream_ = null;
      if (fileReviewEventStreamBuilder_ != null) {
        fileReviewEventStreamBuilder_.dispose();
        fileReviewEventStreamBuilder_ = null;
      }
      onChanged();
      return this;
    }
    /**
     * <pre>
     * Server-owned, append-only record of every file-review event (baseline /
     * candidate capture, file decisions, reconcile, failures).
     *
     * &#64;internal
     *
     * The persisted source of truth that file_change_sets is projected from — a
     * sibling of approval_event_stream for the file-review lifecycle (the
     * ApprovalEvent oneof is closed, so file events cannot ride it). Appends are
     * keyed by the deterministic FileReviewEvent.event_id, so the stream is
     * idempotent under retries.
     *
     * Write contract (one writer per event type): the runner CONTRIBUTES its
     * capture/reconcile events (BASELINE_CAPTURED / CANDIDATE_CAPTURED /
     * RECONCILED / FAILED) by carrying them here on UpdateStatus; the server folds
     * them into the stored stream append-only, by event_id, on the freshly-loaded
     * execution under the write lock. The runner can never replace an existing
     * event and never authors FILE_DECIDED (the server drops a runner-sent
     * decision); FILE_DECIDED is authored solely by SubmitFileDecision. The stored
     * stream is otherwise preserved in place across writes. See
     * FileReviewEventStream.
     *
     * Field 24: appended after file_change_sets (23), the prior maximum.
     * </pre>
     *
     * <code>.ai.stigmer.agentic.agentexecution.v1.FileReviewEventStream file_review_event_stream = 24 [json_name = "fileReviewEventStream"];</code>
     */
    public ai.stigmer.agentic.agentexecution.v1.FileReviewEventStream.Builder getFileReviewEventStreamBuilder() {
      bitField0_ |= 0x00040000;
      onChanged();
      return internalGetFileReviewEventStreamFieldBuilder().getBuilder();
    }
    /**
     * <pre>
     * Server-owned, append-only record of every file-review event (baseline /
     * candidate capture, file decisions, reconcile, failures).
     *
     * &#64;internal
     *
     * The persisted source of truth that file_change_sets is projected from — a
     * sibling of approval_event_stream for the file-review lifecycle (the
     * ApprovalEvent oneof is closed, so file events cannot ride it). Appends are
     * keyed by the deterministic FileReviewEvent.event_id, so the stream is
     * idempotent under retries.
     *
     * Write contract (one writer per event type): the runner CONTRIBUTES its
     * capture/reconcile events (BASELINE_CAPTURED / CANDIDATE_CAPTURED /
     * RECONCILED / FAILED) by carrying them here on UpdateStatus; the server folds
     * them into the stored stream append-only, by event_id, on the freshly-loaded
     * execution under the write lock. The runner can never replace an existing
     * event and never authors FILE_DECIDED (the server drops a runner-sent
     * decision); FILE_DECIDED is authored solely by SubmitFileDecision. The stored
     * stream is otherwise preserved in place across writes. See
     * FileReviewEventStream.
     *
     * Field 24: appended after file_change_sets (23), the prior maximum.
     * </pre>
     *
     * <code>.ai.stigmer.agentic.agentexecution.v1.FileReviewEventStream file_review_event_stream = 24 [json_name = "fileReviewEventStream"];</code>
     */
    public ai.stigmer.agentic.agentexecution.v1.FileReviewEventStreamOrBuilder getFileReviewEventStreamOrBuilder() {
      if (fileReviewEventStreamBuilder_ != null) {
        return fileReviewEventStreamBuilder_.getMessageOrBuilder();
      } else {
        return fileReviewEventStream_ == null ?
            ai.stigmer.agentic.agentexecution.v1.FileReviewEventStream.getDefaultInstance() : fileReviewEventStream_;
      }
    }
    /**
     * <pre>
     * Server-owned, append-only record of every file-review event (baseline /
     * candidate capture, file decisions, reconcile, failures).
     *
     * &#64;internal
     *
     * The persisted source of truth that file_change_sets is projected from — a
     * sibling of approval_event_stream for the file-review lifecycle (the
     * ApprovalEvent oneof is closed, so file events cannot ride it). Appends are
     * keyed by the deterministic FileReviewEvent.event_id, so the stream is
     * idempotent under retries.
     *
     * Write contract (one writer per event type): the runner CONTRIBUTES its
     * capture/reconcile events (BASELINE_CAPTURED / CANDIDATE_CAPTURED /
     * RECONCILED / FAILED) by carrying them here on UpdateStatus; the server folds
     * them into the stored stream append-only, by event_id, on the freshly-loaded
     * execution under the write lock. The runner can never replace an existing
     * event and never authors FILE_DECIDED (the server drops a runner-sent
     * decision); FILE_DECIDED is authored solely by SubmitFileDecision. The stored
     * stream is otherwise preserved in place across writes. See
     * FileReviewEventStream.
     *
     * Field 24: appended after file_change_sets (23), the prior maximum.
     * </pre>
     *
     * <code>.ai.stigmer.agentic.agentexecution.v1.FileReviewEventStream file_review_event_stream = 24 [json_name = "fileReviewEventStream"];</code>
     */
    private com.google.protobuf.SingleFieldBuilder<
        ai.stigmer.agentic.agentexecution.v1.FileReviewEventStream, ai.stigmer.agentic.agentexecution.v1.FileReviewEventStream.Builder, ai.stigmer.agentic.agentexecution.v1.FileReviewEventStreamOrBuilder> 
        internalGetFileReviewEventStreamFieldBuilder() {
      if (fileReviewEventStreamBuilder_ == null) {
        fileReviewEventStreamBuilder_ = new com.google.protobuf.SingleFieldBuilder<
            ai.stigmer.agentic.agentexecution.v1.FileReviewEventStream, ai.stigmer.agentic.agentexecution.v1.FileReviewEventStream.Builder, ai.stigmer.agentic.agentexecution.v1.FileReviewEventStreamOrBuilder>(
                getFileReviewEventStream(),
                getParentForChildren(),
                isClean());
        fileReviewEventStream_ = null;
      }
      return fileReviewEventStreamBuilder_;
    }

    private ai.stigmer.agentic.agentexecution.v1.FileChangeProgress fileChangeProgress_;
    private com.google.protobuf.SingleFieldBuilder<
        ai.stigmer.agentic.agentexecution.v1.FileChangeProgress, ai.stigmer.agentic.agentexecution.v1.FileChangeProgress.Builder, ai.stigmer.agentic.agentexecution.v1.FileChangeProgressOrBuilder> fileChangeProgressBuilder_;
    /**
     * <pre>
     * Transient, non-authoritative snapshot of the workspace delta accumulating
     * during the current turn — the live "N files changed so far" surface, shown
     * before any review is possible.
     *
     * &#64;internal
     *
     * NOT part of the file-review ledger. This is a runner-owned, latest-snapshot
     * display field in the family of setup_progress (18) / streaming_usage (20):
     * the runner overwrites it on each mid-run persist and the server clears it once
     * its change set leaves CAPTURING, exactly as setup_progress is cleared when the
     * phase leaves PENDING. It carries paths + kinds + line counts only (no bodies,
     * no digests) and is never decidable — the turn-boundary CANDIDATE_CAPTURED in
     * file_change_sets remains the single authoritative, reviewable diff. See
     * FileChangeProgress.
     *
     * Field 25: appended after file_review_event_stream (24), the prior maximum.
     * </pre>
     *
     * <code>.ai.stigmer.agentic.agentexecution.v1.FileChangeProgress file_change_progress = 25 [json_name = "fileChangeProgress"];</code>
     * @return Whether the fileChangeProgress field is set.
     */
    public boolean hasFileChangeProgress() {
      return ((bitField0_ & 0x00080000) != 0);
    }
    /**
     * <pre>
     * Transient, non-authoritative snapshot of the workspace delta accumulating
     * during the current turn — the live "N files changed so far" surface, shown
     * before any review is possible.
     *
     * &#64;internal
     *
     * NOT part of the file-review ledger. This is a runner-owned, latest-snapshot
     * display field in the family of setup_progress (18) / streaming_usage (20):
     * the runner overwrites it on each mid-run persist and the server clears it once
     * its change set leaves CAPTURING, exactly as setup_progress is cleared when the
     * phase leaves PENDING. It carries paths + kinds + line counts only (no bodies,
     * no digests) and is never decidable — the turn-boundary CANDIDATE_CAPTURED in
     * file_change_sets remains the single authoritative, reviewable diff. See
     * FileChangeProgress.
     *
     * Field 25: appended after file_review_event_stream (24), the prior maximum.
     * </pre>
     *
     * <code>.ai.stigmer.agentic.agentexecution.v1.FileChangeProgress file_change_progress = 25 [json_name = "fileChangeProgress"];</code>
     * @return The fileChangeProgress.
     */
    public ai.stigmer.agentic.agentexecution.v1.FileChangeProgress getFileChangeProgress() {
      if (fileChangeProgressBuilder_ == null) {
        return fileChangeProgress_ == null ? ai.stigmer.agentic.agentexecution.v1.FileChangeProgress.getDefaultInstance() : fileChangeProgress_;
      } else {
        return fileChangeProgressBuilder_.getMessage();
      }
    }
    /**
     * <pre>
     * Transient, non-authoritative snapshot of the workspace delta accumulating
     * during the current turn — the live "N files changed so far" surface, shown
     * before any review is possible.
     *
     * &#64;internal
     *
     * NOT part of the file-review ledger. This is a runner-owned, latest-snapshot
     * display field in the family of setup_progress (18) / streaming_usage (20):
     * the runner overwrites it on each mid-run persist and the server clears it once
     * its change set leaves CAPTURING, exactly as setup_progress is cleared when the
     * phase leaves PENDING. It carries paths + kinds + line counts only (no bodies,
     * no digests) and is never decidable — the turn-boundary CANDIDATE_CAPTURED in
     * file_change_sets remains the single authoritative, reviewable diff. See
     * FileChangeProgress.
     *
     * Field 25: appended after file_review_event_stream (24), the prior maximum.
     * </pre>
     *
     * <code>.ai.stigmer.agentic.agentexecution.v1.FileChangeProgress file_change_progress = 25 [json_name = "fileChangeProgress"];</code>
     */
    public Builder setFileChangeProgress(ai.stigmer.agentic.agentexecution.v1.FileChangeProgress value) {
      if (fileChangeProgressBuilder_ == null) {
        if (value == null) {
          throw new NullPointerException();
        }
        fileChangeProgress_ = value;
      } else {
        fileChangeProgressBuilder_.setMessage(value);
      }
      bitField0_ |= 0x00080000;
      onChanged();
      return this;
    }
    /**
     * <pre>
     * Transient, non-authoritative snapshot of the workspace delta accumulating
     * during the current turn — the live "N files changed so far" surface, shown
     * before any review is possible.
     *
     * &#64;internal
     *
     * NOT part of the file-review ledger. This is a runner-owned, latest-snapshot
     * display field in the family of setup_progress (18) / streaming_usage (20):
     * the runner overwrites it on each mid-run persist and the server clears it once
     * its change set leaves CAPTURING, exactly as setup_progress is cleared when the
     * phase leaves PENDING. It carries paths + kinds + line counts only (no bodies,
     * no digests) and is never decidable — the turn-boundary CANDIDATE_CAPTURED in
     * file_change_sets remains the single authoritative, reviewable diff. See
     * FileChangeProgress.
     *
     * Field 25: appended after file_review_event_stream (24), the prior maximum.
     * </pre>
     *
     * <code>.ai.stigmer.agentic.agentexecution.v1.FileChangeProgress file_change_progress = 25 [json_name = "fileChangeProgress"];</code>
     */
    public Builder setFileChangeProgress(
        ai.stigmer.agentic.agentexecution.v1.FileChangeProgress.Builder builderForValue) {
      if (fileChangeProgressBuilder_ == null) {
        fileChangeProgress_ = builderForValue.build();
      } else {
        fileChangeProgressBuilder_.setMessage(builderForValue.build());
      }
      bitField0_ |= 0x00080000;
      onChanged();
      return this;
    }
    /**
     * <pre>
     * Transient, non-authoritative snapshot of the workspace delta accumulating
     * during the current turn — the live "N files changed so far" surface, shown
     * before any review is possible.
     *
     * &#64;internal
     *
     * NOT part of the file-review ledger. This is a runner-owned, latest-snapshot
     * display field in the family of setup_progress (18) / streaming_usage (20):
     * the runner overwrites it on each mid-run persist and the server clears it once
     * its change set leaves CAPTURING, exactly as setup_progress is cleared when the
     * phase leaves PENDING. It carries paths + kinds + line counts only (no bodies,
     * no digests) and is never decidable — the turn-boundary CANDIDATE_CAPTURED in
     * file_change_sets remains the single authoritative, reviewable diff. See
     * FileChangeProgress.
     *
     * Field 25: appended after file_review_event_stream (24), the prior maximum.
     * </pre>
     *
     * <code>.ai.stigmer.agentic.agentexecution.v1.FileChangeProgress file_change_progress = 25 [json_name = "fileChangeProgress"];</code>
     */
    public Builder mergeFileChangeProgress(ai.stigmer.agentic.agentexecution.v1.FileChangeProgress value) {
      if (fileChangeProgressBuilder_ == null) {
        if (((bitField0_ & 0x00080000) != 0) &&
          fileChangeProgress_ != null &&
          fileChangeProgress_ != ai.stigmer.agentic.agentexecution.v1.FileChangeProgress.getDefaultInstance()) {
          getFileChangeProgressBuilder().mergeFrom(value);
        } else {
          fileChangeProgress_ = value;
        }
      } else {
        fileChangeProgressBuilder_.mergeFrom(value);
      }
      if (fileChangeProgress_ != null) {
        bitField0_ |= 0x00080000;
        onChanged();
      }
      return this;
    }
    /**
     * <pre>
     * Transient, non-authoritative snapshot of the workspace delta accumulating
     * during the current turn — the live "N files changed so far" surface, shown
     * before any review is possible.
     *
     * &#64;internal
     *
     * NOT part of the file-review ledger. This is a runner-owned, latest-snapshot
     * display field in the family of setup_progress (18) / streaming_usage (20):
     * the runner overwrites it on each mid-run persist and the server clears it once
     * its change set leaves CAPTURING, exactly as setup_progress is cleared when the
     * phase leaves PENDING. It carries paths + kinds + line counts only (no bodies,
     * no digests) and is never decidable — the turn-boundary CANDIDATE_CAPTURED in
     * file_change_sets remains the single authoritative, reviewable diff. See
     * FileChangeProgress.
     *
     * Field 25: appended after file_review_event_stream (24), the prior maximum.
     * </pre>
     *
     * <code>.ai.stigmer.agentic.agentexecution.v1.FileChangeProgress file_change_progress = 25 [json_name = "fileChangeProgress"];</code>
     */
    public Builder clearFileChangeProgress() {
      bitField0_ = (bitField0_ & ~0x00080000);
      fileChangeProgress_ = null;
      if (fileChangeProgressBuilder_ != null) {
        fileChangeProgressBuilder_.dispose();
        fileChangeProgressBuilder_ = null;
      }
      onChanged();
      return this;
    }
    /**
     * <pre>
     * Transient, non-authoritative snapshot of the workspace delta accumulating
     * during the current turn — the live "N files changed so far" surface, shown
     * before any review is possible.
     *
     * &#64;internal
     *
     * NOT part of the file-review ledger. This is a runner-owned, latest-snapshot
     * display field in the family of setup_progress (18) / streaming_usage (20):
     * the runner overwrites it on each mid-run persist and the server clears it once
     * its change set leaves CAPTURING, exactly as setup_progress is cleared when the
     * phase leaves PENDING. It carries paths + kinds + line counts only (no bodies,
     * no digests) and is never decidable — the turn-boundary CANDIDATE_CAPTURED in
     * file_change_sets remains the single authoritative, reviewable diff. See
     * FileChangeProgress.
     *
     * Field 25: appended after file_review_event_stream (24), the prior maximum.
     * </pre>
     *
     * <code>.ai.stigmer.agentic.agentexecution.v1.FileChangeProgress file_change_progress = 25 [json_name = "fileChangeProgress"];</code>
     */
    public ai.stigmer.agentic.agentexecution.v1.FileChangeProgress.Builder getFileChangeProgressBuilder() {
      bitField0_ |= 0x00080000;
      onChanged();
      return internalGetFileChangeProgressFieldBuilder().getBuilder();
    }
    /**
     * <pre>
     * Transient, non-authoritative snapshot of the workspace delta accumulating
     * during the current turn — the live "N files changed so far" surface, shown
     * before any review is possible.
     *
     * &#64;internal
     *
     * NOT part of the file-review ledger. This is a runner-owned, latest-snapshot
     * display field in the family of setup_progress (18) / streaming_usage (20):
     * the runner overwrites it on each mid-run persist and the server clears it once
     * its change set leaves CAPTURING, exactly as setup_progress is cleared when the
     * phase leaves PENDING. It carries paths + kinds + line counts only (no bodies,
     * no digests) and is never decidable — the turn-boundary CANDIDATE_CAPTURED in
     * file_change_sets remains the single authoritative, reviewable diff. See
     * FileChangeProgress.
     *
     * Field 25: appended after file_review_event_stream (24), the prior maximum.
     * </pre>
     *
     * <code>.ai.stigmer.agentic.agentexecution.v1.FileChangeProgress file_change_progress = 25 [json_name = "fileChangeProgress"];</code>
     */
    public ai.stigmer.agentic.agentexecution.v1.FileChangeProgressOrBuilder getFileChangeProgressOrBuilder() {
      if (fileChangeProgressBuilder_ != null) {
        return fileChangeProgressBuilder_.getMessageOrBuilder();
      } else {
        return fileChangeProgress_ == null ?
            ai.stigmer.agentic.agentexecution.v1.FileChangeProgress.getDefaultInstance() : fileChangeProgress_;
      }
    }
    /**
     * <pre>
     * Transient, non-authoritative snapshot of the workspace delta accumulating
     * during the current turn — the live "N files changed so far" surface, shown
     * before any review is possible.
     *
     * &#64;internal
     *
     * NOT part of the file-review ledger. This is a runner-owned, latest-snapshot
     * display field in the family of setup_progress (18) / streaming_usage (20):
     * the runner overwrites it on each mid-run persist and the server clears it once
     * its change set leaves CAPTURING, exactly as setup_progress is cleared when the
     * phase leaves PENDING. It carries paths + kinds + line counts only (no bodies,
     * no digests) and is never decidable — the turn-boundary CANDIDATE_CAPTURED in
     * file_change_sets remains the single authoritative, reviewable diff. See
     * FileChangeProgress.
     *
     * Field 25: appended after file_review_event_stream (24), the prior maximum.
     * </pre>
     *
     * <code>.ai.stigmer.agentic.agentexecution.v1.FileChangeProgress file_change_progress = 25 [json_name = "fileChangeProgress"];</code>
     */
    private com.google.protobuf.SingleFieldBuilder<
        ai.stigmer.agentic.agentexecution.v1.FileChangeProgress, ai.stigmer.agentic.agentexecution.v1.FileChangeProgress.Builder, ai.stigmer.agentic.agentexecution.v1.FileChangeProgressOrBuilder> 
        internalGetFileChangeProgressFieldBuilder() {
      if (fileChangeProgressBuilder_ == null) {
        fileChangeProgressBuilder_ = new com.google.protobuf.SingleFieldBuilder<
            ai.stigmer.agentic.agentexecution.v1.FileChangeProgress, ai.stigmer.agentic.agentexecution.v1.FileChangeProgress.Builder, ai.stigmer.agentic.agentexecution.v1.FileChangeProgressOrBuilder>(
                getFileChangeProgress(),
                getParentForChildren(),
                isClean());
        fileChangeProgress_ = null;
      }
      return fileChangeProgressBuilder_;
    }

    private ai.stigmer.agentic.agentexecution.v1.RecalledMemoriesReport recalledMemoriesReport_;
    private com.google.protobuf.SingleFieldBuilder<
        ai.stigmer.agentic.agentexecution.v1.RecalledMemoriesReport, ai.stigmer.agentic.agentexecution.v1.RecalledMemoriesReport.Builder, ai.stigmer.agentic.agentexecution.v1.RecalledMemoriesReportOrBuilder> recalledMemoriesReportBuilder_;
    /**
     * <pre>
     * Report of which recalled memories were injected into this execution's
     * prompt.
     *
     * Absent, or present with selection_active=false, means wholesale: every
     * fact in the spec.recalled_memories snapshot was injected.
     *
     * &#64;internal
     *
     * Runner-owned, single writer (the streaming_usage / context_info
     * posture): written at most once per execution, at prompt build, through
     * the runner's status lane. The server merges it presence-guarded and
     * never writes it — the create pipeline stamps only the spec snapshot
     * (DD-008 D5, stigmer/stigmer#293 Phase 3a).
     *
     * The audit contract: spec.recalled_memories is the server-composed
     * CANDIDATE set (everything the caller's consent state made recallable);
     * what-the-model-saw = this report joined to that snapshot. Absent
     * report = wholesale is true by construction — for every execution that
     * predates this field and for every wholesale path since (selection
     * below threshold, no embeddings-capable provider, embed/rank failure).
     *
     * Field 26: appended after file_change_progress (25), the prior maximum.
     * </pre>
     *
     * <code>.ai.stigmer.agentic.agentexecution.v1.RecalledMemoriesReport recalled_memories_report = 26 [json_name = "recalledMemoriesReport"];</code>
     * @return Whether the recalledMemoriesReport field is set.
     */
    public boolean hasRecalledMemoriesReport() {
      return ((bitField0_ & 0x00100000) != 0);
    }
    /**
     * <pre>
     * Report of which recalled memories were injected into this execution's
     * prompt.
     *
     * Absent, or present with selection_active=false, means wholesale: every
     * fact in the spec.recalled_memories snapshot was injected.
     *
     * &#64;internal
     *
     * Runner-owned, single writer (the streaming_usage / context_info
     * posture): written at most once per execution, at prompt build, through
     * the runner's status lane. The server merges it presence-guarded and
     * never writes it — the create pipeline stamps only the spec snapshot
     * (DD-008 D5, stigmer/stigmer#293 Phase 3a).
     *
     * The audit contract: spec.recalled_memories is the server-composed
     * CANDIDATE set (everything the caller's consent state made recallable);
     * what-the-model-saw = this report joined to that snapshot. Absent
     * report = wholesale is true by construction — for every execution that
     * predates this field and for every wholesale path since (selection
     * below threshold, no embeddings-capable provider, embed/rank failure).
     *
     * Field 26: appended after file_change_progress (25), the prior maximum.
     * </pre>
     *
     * <code>.ai.stigmer.agentic.agentexecution.v1.RecalledMemoriesReport recalled_memories_report = 26 [json_name = "recalledMemoriesReport"];</code>
     * @return The recalledMemoriesReport.
     */
    public ai.stigmer.agentic.agentexecution.v1.RecalledMemoriesReport getRecalledMemoriesReport() {
      if (recalledMemoriesReportBuilder_ == null) {
        return recalledMemoriesReport_ == null ? ai.stigmer.agentic.agentexecution.v1.RecalledMemoriesReport.getDefaultInstance() : recalledMemoriesReport_;
      } else {
        return recalledMemoriesReportBuilder_.getMessage();
      }
    }
    /**
     * <pre>
     * Report of which recalled memories were injected into this execution's
     * prompt.
     *
     * Absent, or present with selection_active=false, means wholesale: every
     * fact in the spec.recalled_memories snapshot was injected.
     *
     * &#64;internal
     *
     * Runner-owned, single writer (the streaming_usage / context_info
     * posture): written at most once per execution, at prompt build, through
     * the runner's status lane. The server merges it presence-guarded and
     * never writes it — the create pipeline stamps only the spec snapshot
     * (DD-008 D5, stigmer/stigmer#293 Phase 3a).
     *
     * The audit contract: spec.recalled_memories is the server-composed
     * CANDIDATE set (everything the caller's consent state made recallable);
     * what-the-model-saw = this report joined to that snapshot. Absent
     * report = wholesale is true by construction — for every execution that
     * predates this field and for every wholesale path since (selection
     * below threshold, no embeddings-capable provider, embed/rank failure).
     *
     * Field 26: appended after file_change_progress (25), the prior maximum.
     * </pre>
     *
     * <code>.ai.stigmer.agentic.agentexecution.v1.RecalledMemoriesReport recalled_memories_report = 26 [json_name = "recalledMemoriesReport"];</code>
     */
    public Builder setRecalledMemoriesReport(ai.stigmer.agentic.agentexecution.v1.RecalledMemoriesReport value) {
      if (recalledMemoriesReportBuilder_ == null) {
        if (value == null) {
          throw new NullPointerException();
        }
        recalledMemoriesReport_ = value;
      } else {
        recalledMemoriesReportBuilder_.setMessage(value);
      }
      bitField0_ |= 0x00100000;
      onChanged();
      return this;
    }
    /**
     * <pre>
     * Report of which recalled memories were injected into this execution's
     * prompt.
     *
     * Absent, or present with selection_active=false, means wholesale: every
     * fact in the spec.recalled_memories snapshot was injected.
     *
     * &#64;internal
     *
     * Runner-owned, single writer (the streaming_usage / context_info
     * posture): written at most once per execution, at prompt build, through
     * the runner's status lane. The server merges it presence-guarded and
     * never writes it — the create pipeline stamps only the spec snapshot
     * (DD-008 D5, stigmer/stigmer#293 Phase 3a).
     *
     * The audit contract: spec.recalled_memories is the server-composed
     * CANDIDATE set (everything the caller's consent state made recallable);
     * what-the-model-saw = this report joined to that snapshot. Absent
     * report = wholesale is true by construction — for every execution that
     * predates this field and for every wholesale path since (selection
     * below threshold, no embeddings-capable provider, embed/rank failure).
     *
     * Field 26: appended after file_change_progress (25), the prior maximum.
     * </pre>
     *
     * <code>.ai.stigmer.agentic.agentexecution.v1.RecalledMemoriesReport recalled_memories_report = 26 [json_name = "recalledMemoriesReport"];</code>
     */
    public Builder setRecalledMemoriesReport(
        ai.stigmer.agentic.agentexecution.v1.RecalledMemoriesReport.Builder builderForValue) {
      if (recalledMemoriesReportBuilder_ == null) {
        recalledMemoriesReport_ = builderForValue.build();
      } else {
        recalledMemoriesReportBuilder_.setMessage(builderForValue.build());
      }
      bitField0_ |= 0x00100000;
      onChanged();
      return this;
    }
    /**
     * <pre>
     * Report of which recalled memories were injected into this execution's
     * prompt.
     *
     * Absent, or present with selection_active=false, means wholesale: every
     * fact in the spec.recalled_memories snapshot was injected.
     *
     * &#64;internal
     *
     * Runner-owned, single writer (the streaming_usage / context_info
     * posture): written at most once per execution, at prompt build, through
     * the runner's status lane. The server merges it presence-guarded and
     * never writes it — the create pipeline stamps only the spec snapshot
     * (DD-008 D5, stigmer/stigmer#293 Phase 3a).
     *
     * The audit contract: spec.recalled_memories is the server-composed
     * CANDIDATE set (everything the caller's consent state made recallable);
     * what-the-model-saw = this report joined to that snapshot. Absent
     * report = wholesale is true by construction — for every execution that
     * predates this field and for every wholesale path since (selection
     * below threshold, no embeddings-capable provider, embed/rank failure).
     *
     * Field 26: appended after file_change_progress (25), the prior maximum.
     * </pre>
     *
     * <code>.ai.stigmer.agentic.agentexecution.v1.RecalledMemoriesReport recalled_memories_report = 26 [json_name = "recalledMemoriesReport"];</code>
     */
    public Builder mergeRecalledMemoriesReport(ai.stigmer.agentic.agentexecution.v1.RecalledMemoriesReport value) {
      if (recalledMemoriesReportBuilder_ == null) {
        if (((bitField0_ & 0x00100000) != 0) &&
          recalledMemoriesReport_ != null &&
          recalledMemoriesReport_ != ai.stigmer.agentic.agentexecution.v1.RecalledMemoriesReport.getDefaultInstance()) {
          getRecalledMemoriesReportBuilder().mergeFrom(value);
        } else {
          recalledMemoriesReport_ = value;
        }
      } else {
        recalledMemoriesReportBuilder_.mergeFrom(value);
      }
      if (recalledMemoriesReport_ != null) {
        bitField0_ |= 0x00100000;
        onChanged();
      }
      return this;
    }
    /**
     * <pre>
     * Report of which recalled memories were injected into this execution's
     * prompt.
     *
     * Absent, or present with selection_active=false, means wholesale: every
     * fact in the spec.recalled_memories snapshot was injected.
     *
     * &#64;internal
     *
     * Runner-owned, single writer (the streaming_usage / context_info
     * posture): written at most once per execution, at prompt build, through
     * the runner's status lane. The server merges it presence-guarded and
     * never writes it — the create pipeline stamps only the spec snapshot
     * (DD-008 D5, stigmer/stigmer#293 Phase 3a).
     *
     * The audit contract: spec.recalled_memories is the server-composed
     * CANDIDATE set (everything the caller's consent state made recallable);
     * what-the-model-saw = this report joined to that snapshot. Absent
     * report = wholesale is true by construction — for every execution that
     * predates this field and for every wholesale path since (selection
     * below threshold, no embeddings-capable provider, embed/rank failure).
     *
     * Field 26: appended after file_change_progress (25), the prior maximum.
     * </pre>
     *
     * <code>.ai.stigmer.agentic.agentexecution.v1.RecalledMemoriesReport recalled_memories_report = 26 [json_name = "recalledMemoriesReport"];</code>
     */
    public Builder clearRecalledMemoriesReport() {
      bitField0_ = (bitField0_ & ~0x00100000);
      recalledMemoriesReport_ = null;
      if (recalledMemoriesReportBuilder_ != null) {
        recalledMemoriesReportBuilder_.dispose();
        recalledMemoriesReportBuilder_ = null;
      }
      onChanged();
      return this;
    }
    /**
     * <pre>
     * Report of which recalled memories were injected into this execution's
     * prompt.
     *
     * Absent, or present with selection_active=false, means wholesale: every
     * fact in the spec.recalled_memories snapshot was injected.
     *
     * &#64;internal
     *
     * Runner-owned, single writer (the streaming_usage / context_info
     * posture): written at most once per execution, at prompt build, through
     * the runner's status lane. The server merges it presence-guarded and
     * never writes it — the create pipeline stamps only the spec snapshot
     * (DD-008 D5, stigmer/stigmer#293 Phase 3a).
     *
     * The audit contract: spec.recalled_memories is the server-composed
     * CANDIDATE set (everything the caller's consent state made recallable);
     * what-the-model-saw = this report joined to that snapshot. Absent
     * report = wholesale is true by construction — for every execution that
     * predates this field and for every wholesale path since (selection
     * below threshold, no embeddings-capable provider, embed/rank failure).
     *
     * Field 26: appended after file_change_progress (25), the prior maximum.
     * </pre>
     *
     * <code>.ai.stigmer.agentic.agentexecution.v1.RecalledMemoriesReport recalled_memories_report = 26 [json_name = "recalledMemoriesReport"];</code>
     */
    public ai.stigmer.agentic.agentexecution.v1.RecalledMemoriesReport.Builder getRecalledMemoriesReportBuilder() {
      bitField0_ |= 0x00100000;
      onChanged();
      return internalGetRecalledMemoriesReportFieldBuilder().getBuilder();
    }
    /**
     * <pre>
     * Report of which recalled memories were injected into this execution's
     * prompt.
     *
     * Absent, or present with selection_active=false, means wholesale: every
     * fact in the spec.recalled_memories snapshot was injected.
     *
     * &#64;internal
     *
     * Runner-owned, single writer (the streaming_usage / context_info
     * posture): written at most once per execution, at prompt build, through
     * the runner's status lane. The server merges it presence-guarded and
     * never writes it — the create pipeline stamps only the spec snapshot
     * (DD-008 D5, stigmer/stigmer#293 Phase 3a).
     *
     * The audit contract: spec.recalled_memories is the server-composed
     * CANDIDATE set (everything the caller's consent state made recallable);
     * what-the-model-saw = this report joined to that snapshot. Absent
     * report = wholesale is true by construction — for every execution that
     * predates this field and for every wholesale path since (selection
     * below threshold, no embeddings-capable provider, embed/rank failure).
     *
     * Field 26: appended after file_change_progress (25), the prior maximum.
     * </pre>
     *
     * <code>.ai.stigmer.agentic.agentexecution.v1.RecalledMemoriesReport recalled_memories_report = 26 [json_name = "recalledMemoriesReport"];</code>
     */
    public ai.stigmer.agentic.agentexecution.v1.RecalledMemoriesReportOrBuilder getRecalledMemoriesReportOrBuilder() {
      if (recalledMemoriesReportBuilder_ != null) {
        return recalledMemoriesReportBuilder_.getMessageOrBuilder();
      } else {
        return recalledMemoriesReport_ == null ?
            ai.stigmer.agentic.agentexecution.v1.RecalledMemoriesReport.getDefaultInstance() : recalledMemoriesReport_;
      }
    }
    /**
     * <pre>
     * Report of which recalled memories were injected into this execution's
     * prompt.
     *
     * Absent, or present with selection_active=false, means wholesale: every
     * fact in the spec.recalled_memories snapshot was injected.
     *
     * &#64;internal
     *
     * Runner-owned, single writer (the streaming_usage / context_info
     * posture): written at most once per execution, at prompt build, through
     * the runner's status lane. The server merges it presence-guarded and
     * never writes it — the create pipeline stamps only the spec snapshot
     * (DD-008 D5, stigmer/stigmer#293 Phase 3a).
     *
     * The audit contract: spec.recalled_memories is the server-composed
     * CANDIDATE set (everything the caller's consent state made recallable);
     * what-the-model-saw = this report joined to that snapshot. Absent
     * report = wholesale is true by construction — for every execution that
     * predates this field and for every wholesale path since (selection
     * below threshold, no embeddings-capable provider, embed/rank failure).
     *
     * Field 26: appended after file_change_progress (25), the prior maximum.
     * </pre>
     *
     * <code>.ai.stigmer.agentic.agentexecution.v1.RecalledMemoriesReport recalled_memories_report = 26 [json_name = "recalledMemoriesReport"];</code>
     */
    private com.google.protobuf.SingleFieldBuilder<
        ai.stigmer.agentic.agentexecution.v1.RecalledMemoriesReport, ai.stigmer.agentic.agentexecution.v1.RecalledMemoriesReport.Builder, ai.stigmer.agentic.agentexecution.v1.RecalledMemoriesReportOrBuilder> 
        internalGetRecalledMemoriesReportFieldBuilder() {
      if (recalledMemoriesReportBuilder_ == null) {
        recalledMemoriesReportBuilder_ = new com.google.protobuf.SingleFieldBuilder<
            ai.stigmer.agentic.agentexecution.v1.RecalledMemoriesReport, ai.stigmer.agentic.agentexecution.v1.RecalledMemoriesReport.Builder, ai.stigmer.agentic.agentexecution.v1.RecalledMemoriesReportOrBuilder>(
                getRecalledMemoriesReport(),
                getParentForChildren(),
                isClean());
        recalledMemoriesReport_ = null;
      }
      return recalledMemoriesReportBuilder_;
    }

    // @@protoc_insertion_point(builder_scope:ai.stigmer.agentic.agentexecution.v1.AgentExecutionStatus)
  }

  // @@protoc_insertion_point(class_scope:ai.stigmer.agentic.agentexecution.v1.AgentExecutionStatus)
  private static final ai.stigmer.agentic.agentexecution.v1.AgentExecutionStatus DEFAULT_INSTANCE;
  static {
    DEFAULT_INSTANCE = new ai.stigmer.agentic.agentexecution.v1.AgentExecutionStatus();
  }

  public static ai.stigmer.agentic.agentexecution.v1.AgentExecutionStatus getDefaultInstance() {
    return DEFAULT_INSTANCE;
  }

  private static final com.google.protobuf.Parser<AgentExecutionStatus>
      PARSER = new com.google.protobuf.AbstractParser<AgentExecutionStatus>() {
    @java.lang.Override
    public AgentExecutionStatus parsePartialFrom(
        com.google.protobuf.CodedInputStream input,
        com.google.protobuf.ExtensionRegistryLite extensionRegistry)
        throws com.google.protobuf.InvalidProtocolBufferException {
      Builder builder = newBuilder();
      try {
        builder.mergeFrom(input, extensionRegistry);
      } catch (com.google.protobuf.InvalidProtocolBufferException e) {
        throw e.setUnfinishedMessage(builder.buildPartial());
      } catch (com.google.protobuf.UninitializedMessageException e) {
        throw e.asInvalidProtocolBufferException().setUnfinishedMessage(builder.buildPartial());
      } catch (java.io.IOException e) {
        throw new com.google.protobuf.InvalidProtocolBufferException(e)
            .setUnfinishedMessage(builder.buildPartial());
      }
      return builder.buildPartial();
    }
  };

  public static com.google.protobuf.Parser<AgentExecutionStatus> parser() {
    return PARSER;
  }

  @java.lang.Override
  public com.google.protobuf.Parser<AgentExecutionStatus> getParserForType() {
    return PARSER;
  }

  @java.lang.Override
  public ai.stigmer.agentic.agentexecution.v1.AgentExecutionStatus getDefaultInstanceForType() {
    return DEFAULT_INSTANCE;
  }

}

