// Generated by the protocol buffer compiler.  DO NOT EDIT!
// NO CHECKED-IN PROTOBUF GENCODE
// source: ai/stigmer/agentic/mcpserver/v1/spec.proto
// Protobuf Java Version: 4.34.0

package ai.stigmer.agentic.mcpserver.v1;

/**
 * <pre>
 * McpServerSpec defines the configurable properties of an MCP server.
 *
 * &#64;internal
 * This is the "Template" layer — declares capabilities and requirements.
 * The overview.md file provides the SDK-facing description and example YAML.
 * </pre>
 *
 * Protobuf type {@code ai.stigmer.agentic.mcpserver.v1.McpServerSpec}
 */
@com.google.protobuf.Generated
public final class McpServerSpec extends
    com.google.protobuf.GeneratedMessage implements
    // @@protoc_insertion_point(message_implements:ai.stigmer.agentic.mcpserver.v1.McpServerSpec)
    McpServerSpecOrBuilder {
private static final long serialVersionUID = 0L;
  static {
    com.google.protobuf.RuntimeVersion.validateProtobufGencodeVersion(
      com.google.protobuf.RuntimeVersion.RuntimeDomain.PUBLIC,
      /* major= */ 4,
      /* minor= */ 34,
      /* patch= */ 0,
      /* suffix= */ "",
      "McpServerSpec");
  }
  // Use McpServerSpec.newBuilder() to construct.
  private McpServerSpec(com.google.protobuf.GeneratedMessage.Builder<?> builder) {
    super(builder);
  }
  private McpServerSpec() {
    description_ = "";
    iconUrl_ = "";
    tags_ =
        com.google.protobuf.LazyStringArrayList.emptyList();
    defaultEnabledTools_ =
        com.google.protobuf.LazyStringArrayList.emptyList();
    pinnedToolApprovals_ = java.util.Collections.emptyList();
    repositoryUrl_ = "";
  }

  public static final com.google.protobuf.Descriptors.Descriptor
      getDescriptor() {
    return ai.stigmer.agentic.mcpserver.v1.SpecProto.internal_static_ai_stigmer_agentic_mcpserver_v1_McpServerSpec_descriptor;
  }

  @java.lang.Override
  public com.google.protobuf.Descriptors.Descriptor getDescriptorForType() {
    return ai.stigmer.agentic.mcpserver.v1.SpecProto.internal_static_ai_stigmer_agentic_mcpserver_v1_McpServerSpec_descriptor;
  }

  @SuppressWarnings({"rawtypes"})
  @java.lang.Override
  protected com.google.protobuf.MapFieldReflectionAccessor internalGetMapFieldReflection(
      int number) {
    switch (number) {
      case 8:
        return internalGetEnv();
      default:
        throw new RuntimeException(
            "Invalid map field number: " + number);
    }
  }
  @java.lang.Override
  protected com.google.protobuf.GeneratedMessage.FieldAccessorTable
      internalGetFieldAccessorTable() {
    return ai.stigmer.agentic.mcpserver.v1.SpecProto.internal_static_ai_stigmer_agentic_mcpserver_v1_McpServerSpec_fieldAccessorTable
        .ensureFieldAccessorsInitialized(
            ai.stigmer.agentic.mcpserver.v1.McpServerSpec.class, ai.stigmer.agentic.mcpserver.v1.McpServerSpec.Builder.class);
  }

  private int bitField0_;
  private int serverTypeCase_ = 0;
  @SuppressWarnings("serial")
  private java.lang.Object serverType_;
  public enum ServerTypeCase
      implements com.google.protobuf.Internal.EnumLite,
          com.google.protobuf.AbstractMessage.InternalOneOfEnum {
    STDIO(4),
    HTTP(5),
    SERVERTYPE_NOT_SET(0);
    private final int value;
    private ServerTypeCase(int value) {
      this.value = value;
    }
    /**
     * @param value The number of the enum to look for.
     * @return The enum associated with the given number.
     * @deprecated Use {@link #forNumber(int)} instead.
     */
    @java.lang.Deprecated
    public static ServerTypeCase valueOf(int value) {
      return forNumber(value);
    }

    public static ServerTypeCase forNumber(int value) {
      switch (value) {
        case 4: return STDIO;
        case 5: return HTTP;
        case 0: return SERVERTYPE_NOT_SET;
        default: return null;
      }
    }
    public int getNumber() {
      return this.value;
    }
  };

  public ServerTypeCase
  getServerTypeCase() {
    return ServerTypeCase.forNumber(
        serverTypeCase_);
  }

  public static final int DESCRIPTION_FIELD_NUMBER = 1;
  @SuppressWarnings("serial")
  private volatile java.lang.Object description_ = "";
  /**
   * <pre>
   * Human-readable description for marketplace display and documentation.
   * Should explain what this MCP server does and its primary use cases.
   * Example: "GitHub MCP server for repository operations, code search, and PR management"
   * </pre>
   *
   * <code>string description = 1 [json_name = "description"];</code>
   * @return The description.
   */
  @java.lang.Override
  public java.lang.String getDescription() {
    java.lang.Object ref = description_;
    if (ref instanceof java.lang.String) {
      return (java.lang.String) ref;
    } else {
      com.google.protobuf.ByteString bs = 
          (com.google.protobuf.ByteString) ref;
      java.lang.String s = bs.toStringUtf8();
      description_ = s;
      return s;
    }
  }
  /**
   * <pre>
   * Human-readable description for marketplace display and documentation.
   * Should explain what this MCP server does and its primary use cases.
   * Example: "GitHub MCP server for repository operations, code search, and PR management"
   * </pre>
   *
   * <code>string description = 1 [json_name = "description"];</code>
   * @return The bytes for description.
   */
  @java.lang.Override
  public com.google.protobuf.ByteString
      getDescriptionBytes() {
    java.lang.Object ref = description_;
    if (ref instanceof java.lang.String) {
      com.google.protobuf.ByteString b = 
          com.google.protobuf.ByteString.copyFromUtf8(
              (java.lang.String) ref);
      description_ = b;
      return b;
    } else {
      return (com.google.protobuf.ByteString) ref;
    }
  }

  public static final int ICON_URL_FIELD_NUMBER = 2;
  @SuppressWarnings("serial")
  private volatile java.lang.Object iconUrl_ = "";
  /**
   * <pre>
   * Icon URL for UI display in marketplace and agent configuration screens.
   * Should be a publicly accessible URL to an image (SVG, PNG, or JPEG).
   * Example: "https://github.githubassets.com/favicons/favicon.svg"
   * </pre>
   *
   * <code>string icon_url = 2 [json_name = "iconUrl"];</code>
   * @return The iconUrl.
   */
  @java.lang.Override
  public java.lang.String getIconUrl() {
    java.lang.Object ref = iconUrl_;
    if (ref instanceof java.lang.String) {
      return (java.lang.String) ref;
    } else {
      com.google.protobuf.ByteString bs = 
          (com.google.protobuf.ByteString) ref;
      java.lang.String s = bs.toStringUtf8();
      iconUrl_ = s;
      return s;
    }
  }
  /**
   * <pre>
   * Icon URL for UI display in marketplace and agent configuration screens.
   * Should be a publicly accessible URL to an image (SVG, PNG, or JPEG).
   * Example: "https://github.githubassets.com/favicons/favicon.svg"
   * </pre>
   *
   * <code>string icon_url = 2 [json_name = "iconUrl"];</code>
   * @return The bytes for iconUrl.
   */
  @java.lang.Override
  public com.google.protobuf.ByteString
      getIconUrlBytes() {
    java.lang.Object ref = iconUrl_;
    if (ref instanceof java.lang.String) {
      com.google.protobuf.ByteString b = 
          com.google.protobuf.ByteString.copyFromUtf8(
              (java.lang.String) ref);
      iconUrl_ = b;
      return b;
    } else {
      return (com.google.protobuf.ByteString) ref;
    }
  }

  public static final int TAGS_FIELD_NUMBER = 3;
  @SuppressWarnings("serial")
  private com.google.protobuf.LazyStringArrayList tags_ =
      com.google.protobuf.LazyStringArrayList.emptyList();
  /**
   * <pre>
   * Categorization tags for discoverability in the marketplace.
   * Use lowercase, hyphenated tags that describe the server's domain and capabilities.
   * Examples: ["git", "vcs", "code-analysis"], ["aws", "cloud", "infrastructure"]
   * </pre>
   *
   * <code>repeated string tags = 3 [json_name = "tags"];</code>
   * @return A list containing the tags.
   */
  public com.google.protobuf.ProtocolStringList
      getTagsList() {
    return tags_;
  }
  /**
   * <pre>
   * Categorization tags for discoverability in the marketplace.
   * Use lowercase, hyphenated tags that describe the server's domain and capabilities.
   * Examples: ["git", "vcs", "code-analysis"], ["aws", "cloud", "infrastructure"]
   * </pre>
   *
   * <code>repeated string tags = 3 [json_name = "tags"];</code>
   * @return The count of tags.
   */
  public int getTagsCount() {
    return tags_.size();
  }
  /**
   * <pre>
   * Categorization tags for discoverability in the marketplace.
   * Use lowercase, hyphenated tags that describe the server's domain and capabilities.
   * Examples: ["git", "vcs", "code-analysis"], ["aws", "cloud", "infrastructure"]
   * </pre>
   *
   * <code>repeated string tags = 3 [json_name = "tags"];</code>
   * @param index The index of the element to return.
   * @return The tags at the given index.
   */
  public java.lang.String getTags(int index) {
    return tags_.get(index);
  }
  /**
   * <pre>
   * Categorization tags for discoverability in the marketplace.
   * Use lowercase, hyphenated tags that describe the server's domain and capabilities.
   * Examples: ["git", "vcs", "code-analysis"], ["aws", "cloud", "infrastructure"]
   * </pre>
   *
   * <code>repeated string tags = 3 [json_name = "tags"];</code>
   * @param index The index of the value to return.
   * @return The bytes of the tags at the given index.
   */
  public com.google.protobuf.ByteString
      getTagsBytes(int index) {
    return tags_.getByteString(index);
  }

  public static final int STDIO_FIELD_NUMBER = 4;
  /**
   * <pre>
   * stdio-based server (subprocess with stdin/stdout communication).
   * Most common type - used for Node.js, Python, and other CLI-based MCP servers.
   * </pre>
   *
   * <code>.ai.stigmer.agentic.mcpserver.v1.StdioServerConfig stdio = 4 [json_name = "stdio"];</code>
   * @return Whether the stdio field is set.
   */
  @java.lang.Override
  public boolean hasStdio() {
    return serverTypeCase_ == 4;
  }
  /**
   * <pre>
   * stdio-based server (subprocess with stdin/stdout communication).
   * Most common type - used for Node.js, Python, and other CLI-based MCP servers.
   * </pre>
   *
   * <code>.ai.stigmer.agentic.mcpserver.v1.StdioServerConfig stdio = 4 [json_name = "stdio"];</code>
   * @return The stdio.
   */
  @java.lang.Override
  public ai.stigmer.agentic.mcpserver.v1.StdioServerConfig getStdio() {
    if (serverTypeCase_ == 4) {
       return (ai.stigmer.agentic.mcpserver.v1.StdioServerConfig) serverType_;
    }
    return ai.stigmer.agentic.mcpserver.v1.StdioServerConfig.getDefaultInstance();
  }
  /**
   * <pre>
   * stdio-based server (subprocess with stdin/stdout communication).
   * Most common type - used for Node.js, Python, and other CLI-based MCP servers.
   * </pre>
   *
   * <code>.ai.stigmer.agentic.mcpserver.v1.StdioServerConfig stdio = 4 [json_name = "stdio"];</code>
   */
  @java.lang.Override
  public ai.stigmer.agentic.mcpserver.v1.StdioServerConfigOrBuilder getStdioOrBuilder() {
    if (serverTypeCase_ == 4) {
       return (ai.stigmer.agentic.mcpserver.v1.StdioServerConfig) serverType_;
    }
    return ai.stigmer.agentic.mcpserver.v1.StdioServerConfig.getDefaultInstance();
  }

  public static final int HTTP_FIELD_NUMBER = 5;
  /**
   * <pre>
   * HTTP-based server (HTTP + Server-Sent Events communication).
   * Used for remote/managed MCP services accessible over the network.
   * </pre>
   *
   * <code>.ai.stigmer.agentic.mcpserver.v1.HttpServerConfig http = 5 [json_name = "http"];</code>
   * @return Whether the http field is set.
   */
  @java.lang.Override
  public boolean hasHttp() {
    return serverTypeCase_ == 5;
  }
  /**
   * <pre>
   * HTTP-based server (HTTP + Server-Sent Events communication).
   * Used for remote/managed MCP services accessible over the network.
   * </pre>
   *
   * <code>.ai.stigmer.agentic.mcpserver.v1.HttpServerConfig http = 5 [json_name = "http"];</code>
   * @return The http.
   */
  @java.lang.Override
  public ai.stigmer.agentic.mcpserver.v1.HttpServerConfig getHttp() {
    if (serverTypeCase_ == 5) {
       return (ai.stigmer.agentic.mcpserver.v1.HttpServerConfig) serverType_;
    }
    return ai.stigmer.agentic.mcpserver.v1.HttpServerConfig.getDefaultInstance();
  }
  /**
   * <pre>
   * HTTP-based server (HTTP + Server-Sent Events communication).
   * Used for remote/managed MCP services accessible over the network.
   * </pre>
   *
   * <code>.ai.stigmer.agentic.mcpserver.v1.HttpServerConfig http = 5 [json_name = "http"];</code>
   */
  @java.lang.Override
  public ai.stigmer.agentic.mcpserver.v1.HttpServerConfigOrBuilder getHttpOrBuilder() {
    if (serverTypeCase_ == 5) {
       return (ai.stigmer.agentic.mcpserver.v1.HttpServerConfig) serverType_;
    }
    return ai.stigmer.agentic.mcpserver.v1.HttpServerConfig.getDefaultInstance();
  }

  public static final int DEFAULT_ENABLED_TOOLS_FIELD_NUMBER = 7;
  @SuppressWarnings("serial")
  private com.google.protobuf.LazyStringArrayList defaultEnabledTools_ =
      com.google.protobuf.LazyStringArrayList.emptyList();
  /**
   * <pre>
   * Default tools to enable from this MCP server.
   * Empty list means all tools are enabled by default.
   * Applies whenever an agent's McpServerUsage.enabled_tools is empty.
   *
   * &#64;internal
   * Tool names must match exactly what the MCP server reports via tools/list.
   * Only names from discovered_capabilities.tools are valid here.
   * Do NOT include names from discovered_capabilities.resource_templates —
   * resource templates are read-only data endpoints, not callable tools.
   *
   * Enforcement is two-layered:
   * - Apply time: mcpserver update/apply rejects (INVALID_ARGUMENT) any
   * name this server's own discovered_capabilities.tools does not
   * contain, with the valid names in the error. Skipped when the server
   * has never been connected (no capabilities yet) — which is always the
   * case on create, so the check only bites from the first re-apply
   * after discovery.
   * - Execution time: the runner enforces the INTERSECTION with the
   * server's live toolset — an unknown name is warned in the runner log
   * and ignored, so a stale entry narrows the toolset but never widens
   * it or fails the run. Enforcement is per harness — see
   * McpServerUsage.enabled_tools in agent/v1/spec.proto.
   * </pre>
   *
   * <code>repeated string default_enabled_tools = 7 [json_name = "defaultEnabledTools"];</code>
   * @return A list containing the defaultEnabledTools.
   */
  public com.google.protobuf.ProtocolStringList
      getDefaultEnabledToolsList() {
    return defaultEnabledTools_;
  }
  /**
   * <pre>
   * Default tools to enable from this MCP server.
   * Empty list means all tools are enabled by default.
   * Applies whenever an agent's McpServerUsage.enabled_tools is empty.
   *
   * &#64;internal
   * Tool names must match exactly what the MCP server reports via tools/list.
   * Only names from discovered_capabilities.tools are valid here.
   * Do NOT include names from discovered_capabilities.resource_templates —
   * resource templates are read-only data endpoints, not callable tools.
   *
   * Enforcement is two-layered:
   * - Apply time: mcpserver update/apply rejects (INVALID_ARGUMENT) any
   * name this server's own discovered_capabilities.tools does not
   * contain, with the valid names in the error. Skipped when the server
   * has never been connected (no capabilities yet) — which is always the
   * case on create, so the check only bites from the first re-apply
   * after discovery.
   * - Execution time: the runner enforces the INTERSECTION with the
   * server's live toolset — an unknown name is warned in the runner log
   * and ignored, so a stale entry narrows the toolset but never widens
   * it or fails the run. Enforcement is per harness — see
   * McpServerUsage.enabled_tools in agent/v1/spec.proto.
   * </pre>
   *
   * <code>repeated string default_enabled_tools = 7 [json_name = "defaultEnabledTools"];</code>
   * @return The count of defaultEnabledTools.
   */
  public int getDefaultEnabledToolsCount() {
    return defaultEnabledTools_.size();
  }
  /**
   * <pre>
   * Default tools to enable from this MCP server.
   * Empty list means all tools are enabled by default.
   * Applies whenever an agent's McpServerUsage.enabled_tools is empty.
   *
   * &#64;internal
   * Tool names must match exactly what the MCP server reports via tools/list.
   * Only names from discovered_capabilities.tools are valid here.
   * Do NOT include names from discovered_capabilities.resource_templates —
   * resource templates are read-only data endpoints, not callable tools.
   *
   * Enforcement is two-layered:
   * - Apply time: mcpserver update/apply rejects (INVALID_ARGUMENT) any
   * name this server's own discovered_capabilities.tools does not
   * contain, with the valid names in the error. Skipped when the server
   * has never been connected (no capabilities yet) — which is always the
   * case on create, so the check only bites from the first re-apply
   * after discovery.
   * - Execution time: the runner enforces the INTERSECTION with the
   * server's live toolset — an unknown name is warned in the runner log
   * and ignored, so a stale entry narrows the toolset but never widens
   * it or fails the run. Enforcement is per harness — see
   * McpServerUsage.enabled_tools in agent/v1/spec.proto.
   * </pre>
   *
   * <code>repeated string default_enabled_tools = 7 [json_name = "defaultEnabledTools"];</code>
   * @param index The index of the element to return.
   * @return The defaultEnabledTools at the given index.
   */
  public java.lang.String getDefaultEnabledTools(int index) {
    return defaultEnabledTools_.get(index);
  }
  /**
   * <pre>
   * Default tools to enable from this MCP server.
   * Empty list means all tools are enabled by default.
   * Applies whenever an agent's McpServerUsage.enabled_tools is empty.
   *
   * &#64;internal
   * Tool names must match exactly what the MCP server reports via tools/list.
   * Only names from discovered_capabilities.tools are valid here.
   * Do NOT include names from discovered_capabilities.resource_templates —
   * resource templates are read-only data endpoints, not callable tools.
   *
   * Enforcement is two-layered:
   * - Apply time: mcpserver update/apply rejects (INVALID_ARGUMENT) any
   * name this server's own discovered_capabilities.tools does not
   * contain, with the valid names in the error. Skipped when the server
   * has never been connected (no capabilities yet) — which is always the
   * case on create, so the check only bites from the first re-apply
   * after discovery.
   * - Execution time: the runner enforces the INTERSECTION with the
   * server's live toolset — an unknown name is warned in the runner log
   * and ignored, so a stale entry narrows the toolset but never widens
   * it or fails the run. Enforcement is per harness — see
   * McpServerUsage.enabled_tools in agent/v1/spec.proto.
   * </pre>
   *
   * <code>repeated string default_enabled_tools = 7 [json_name = "defaultEnabledTools"];</code>
   * @param index The index of the value to return.
   * @return The bytes of the defaultEnabledTools at the given index.
   */
  public com.google.protobuf.ByteString
      getDefaultEnabledToolsBytes(int index) {
    return defaultEnabledTools_.getByteString(index);
  }

  public static final int ENV_FIELD_NUMBER = 8;
  private static final class EnvDefaultEntryHolder {
    static final com.google.protobuf.MapEntry<
        java.lang.String, ai.stigmer.agentic.environment.v1.EnvVarDeclaration> defaultEntry =
            com.google.protobuf.MapEntry
            .<java.lang.String, ai.stigmer.agentic.environment.v1.EnvVarDeclaration>newDefaultInstance(
                ai.stigmer.agentic.mcpserver.v1.SpecProto.internal_static_ai_stigmer_agentic_mcpserver_v1_McpServerSpec_EnvEntry_descriptor, 
                com.google.protobuf.WireFormat.FieldType.STRING,
                "",
                com.google.protobuf.WireFormat.FieldType.MESSAGE,
                ai.stigmer.agentic.environment.v1.EnvVarDeclaration.getDefaultInstance());
  }
  @SuppressWarnings("serial")
  private com.google.protobuf.MapField<
      java.lang.String, ai.stigmer.agentic.environment.v1.EnvVarDeclaration> env_;
  private com.google.protobuf.MapField<java.lang.String, ai.stigmer.agentic.environment.v1.EnvVarDeclaration>
  internalGetEnv() {
    if (env_ == null) {
      return com.google.protobuf.MapField.emptyMapField(
          EnvDefaultEntryHolder.defaultEntry);
    }
    return env_;
  }
  public int getEnvCount() {
    return internalGetEnv().getMap().size();
  }
  /**
   * <pre>
   * Environment variable declarations for this MCP server.
   * Keys are variable names; values describe their metadata and optionality.
   *
   * &#64;internal
   * Reserved platform keys — declared here like any other variable, but
   * their values are injected by the runner per resolution context and are
   * authoritative over same-named user env entries:
   * STIGMER_CALLER_IDENTITY_KIND  — the verified caller's kind token
   * ("whatsapp_phone", "slack_user_id", "stigmer_user", "anonymous")
   * STIGMER_CALLER_IDENTITY_VALUE — the identity value (wa_id, user id,
   * email); empty for anonymous
   * STIGMER_SESSION_ID            — the session the identity was
   * resolved for; empty outside a session
   * Reserved keys MUST be declared `optional: true`: they have no value at
   * execution-create time, and a required declaration fails the pipeline's
   * env-completeness validation before the runner ever injects them.
   * Discovery (the connect workflow) runs with no session and injects the
   * anonymous sentinel — a server consuming these keys must answer
   * tools/list for anonymous callers and gate tool CALLS instead. The
   * injected header is runner-asserted, not signed: pair it with a shared
   * secret and treat it as trustworthy only for servers you operate.
   * </pre>
   *
   * <code>map&lt;string, .ai.stigmer.agentic.environment.v1.EnvVarDeclaration&gt; env = 8 [json_name = "env"];</code>
   */
  @java.lang.Override
  public boolean containsEnv(
      java.lang.String key) {
    if (key == null) { throw new NullPointerException("map key"); }
    return internalGetEnv().getMap().containsKey(key);
  }
  /**
   * Use {@link #getEnvMap()} instead.
   */
  @java.lang.Override
  @java.lang.Deprecated
  public java.util.Map<java.lang.String, ai.stigmer.agentic.environment.v1.EnvVarDeclaration> getEnv() {
    return getEnvMap();
  }
  /**
   * <pre>
   * Environment variable declarations for this MCP server.
   * Keys are variable names; values describe their metadata and optionality.
   *
   * &#64;internal
   * Reserved platform keys — declared here like any other variable, but
   * their values are injected by the runner per resolution context and are
   * authoritative over same-named user env entries:
   * STIGMER_CALLER_IDENTITY_KIND  — the verified caller's kind token
   * ("whatsapp_phone", "slack_user_id", "stigmer_user", "anonymous")
   * STIGMER_CALLER_IDENTITY_VALUE — the identity value (wa_id, user id,
   * email); empty for anonymous
   * STIGMER_SESSION_ID            — the session the identity was
   * resolved for; empty outside a session
   * Reserved keys MUST be declared `optional: true`: they have no value at
   * execution-create time, and a required declaration fails the pipeline's
   * env-completeness validation before the runner ever injects them.
   * Discovery (the connect workflow) runs with no session and injects the
   * anonymous sentinel — a server consuming these keys must answer
   * tools/list for anonymous callers and gate tool CALLS instead. The
   * injected header is runner-asserted, not signed: pair it with a shared
   * secret and treat it as trustworthy only for servers you operate.
   * </pre>
   *
   * <code>map&lt;string, .ai.stigmer.agentic.environment.v1.EnvVarDeclaration&gt; env = 8 [json_name = "env"];</code>
   */
  @java.lang.Override
  public java.util.Map<java.lang.String, ai.stigmer.agentic.environment.v1.EnvVarDeclaration> getEnvMap() {
    return internalGetEnv().getMap();
  }
  /**
   * <pre>
   * Environment variable declarations for this MCP server.
   * Keys are variable names; values describe their metadata and optionality.
   *
   * &#64;internal
   * Reserved platform keys — declared here like any other variable, but
   * their values are injected by the runner per resolution context and are
   * authoritative over same-named user env entries:
   * STIGMER_CALLER_IDENTITY_KIND  — the verified caller's kind token
   * ("whatsapp_phone", "slack_user_id", "stigmer_user", "anonymous")
   * STIGMER_CALLER_IDENTITY_VALUE — the identity value (wa_id, user id,
   * email); empty for anonymous
   * STIGMER_SESSION_ID            — the session the identity was
   * resolved for; empty outside a session
   * Reserved keys MUST be declared `optional: true`: they have no value at
   * execution-create time, and a required declaration fails the pipeline's
   * env-completeness validation before the runner ever injects them.
   * Discovery (the connect workflow) runs with no session and injects the
   * anonymous sentinel — a server consuming these keys must answer
   * tools/list for anonymous callers and gate tool CALLS instead. The
   * injected header is runner-asserted, not signed: pair it with a shared
   * secret and treat it as trustworthy only for servers you operate.
   * </pre>
   *
   * <code>map&lt;string, .ai.stigmer.agentic.environment.v1.EnvVarDeclaration&gt; env = 8 [json_name = "env"];</code>
   */
  @java.lang.Override
  public /* nullable */
ai.stigmer.agentic.environment.v1.EnvVarDeclaration getEnvOrDefault(
      java.lang.String key,
      /* nullable */
ai.stigmer.agentic.environment.v1.EnvVarDeclaration defaultValue) {
    if (key == null) { throw new NullPointerException("map key"); }
    java.util.Map<java.lang.String, ai.stigmer.agentic.environment.v1.EnvVarDeclaration> map =
        internalGetEnv().getMap();
    return map.containsKey(key) ? map.get(key) : defaultValue;
  }
  /**
   * <pre>
   * Environment variable declarations for this MCP server.
   * Keys are variable names; values describe their metadata and optionality.
   *
   * &#64;internal
   * Reserved platform keys — declared here like any other variable, but
   * their values are injected by the runner per resolution context and are
   * authoritative over same-named user env entries:
   * STIGMER_CALLER_IDENTITY_KIND  — the verified caller's kind token
   * ("whatsapp_phone", "slack_user_id", "stigmer_user", "anonymous")
   * STIGMER_CALLER_IDENTITY_VALUE — the identity value (wa_id, user id,
   * email); empty for anonymous
   * STIGMER_SESSION_ID            — the session the identity was
   * resolved for; empty outside a session
   * Reserved keys MUST be declared `optional: true`: they have no value at
   * execution-create time, and a required declaration fails the pipeline's
   * env-completeness validation before the runner ever injects them.
   * Discovery (the connect workflow) runs with no session and injects the
   * anonymous sentinel — a server consuming these keys must answer
   * tools/list for anonymous callers and gate tool CALLS instead. The
   * injected header is runner-asserted, not signed: pair it with a shared
   * secret and treat it as trustworthy only for servers you operate.
   * </pre>
   *
   * <code>map&lt;string, .ai.stigmer.agentic.environment.v1.EnvVarDeclaration&gt; env = 8 [json_name = "env"];</code>
   */
  @java.lang.Override
  public ai.stigmer.agentic.environment.v1.EnvVarDeclaration getEnvOrThrow(
      java.lang.String key) {
    if (key == null) { throw new NullPointerException("map key"); }
    java.util.Map<java.lang.String, ai.stigmer.agentic.environment.v1.EnvVarDeclaration> map =
        internalGetEnv().getMap();
    if (!map.containsKey(key)) {
      throw new java.lang.IllegalArgumentException();
    }
    return map.get(key);
  }

  public static final int PINNED_TOOL_APPROVALS_FIELD_NUMBER = 11;
  @SuppressWarnings("serial")
  private java.util.List<ai.stigmer.agentic.mcpserver.v1.ToolApprovalPolicy> pinnedToolApprovals_;
  /**
   * <pre>
   * Manual tool approval overrides set by the MCP server owner.
   *
   * &#64;internal
   * These take precedence over system-generated `McpServerStatus.tool_approvals`.
   * Never auto-modified — only changed by explicit user action (apply/update).
   *
   * Use cases:
   * - Force approval for a tool the classifier marked as auto-approve
   * - Exempt a safe tool the classifier flagged as needing approval
   * - Establish organization-wide safety policies for dangerous tools
   *
   * Policy chain (lowest to highest priority):
   * 1. McpServerStatus.tool_approvals - System-generated defaults
   * 2. McpServerSpec.pinned_tool_approvals - Manual overrides (this field)
   * 3. Agent.McpServerUsage.tool_approval_overrides - Per-agent customization
   * 4. AgentExecution.auto_approve_all - Runtime bypass
   * </pre>
   *
   * <code>repeated .ai.stigmer.agentic.mcpserver.v1.ToolApprovalPolicy pinned_tool_approvals = 11 [json_name = "pinnedToolApprovals"];</code>
   */
  @java.lang.Override
  public java.util.List<ai.stigmer.agentic.mcpserver.v1.ToolApprovalPolicy> getPinnedToolApprovalsList() {
    return pinnedToolApprovals_;
  }
  /**
   * <pre>
   * Manual tool approval overrides set by the MCP server owner.
   *
   * &#64;internal
   * These take precedence over system-generated `McpServerStatus.tool_approvals`.
   * Never auto-modified — only changed by explicit user action (apply/update).
   *
   * Use cases:
   * - Force approval for a tool the classifier marked as auto-approve
   * - Exempt a safe tool the classifier flagged as needing approval
   * - Establish organization-wide safety policies for dangerous tools
   *
   * Policy chain (lowest to highest priority):
   * 1. McpServerStatus.tool_approvals - System-generated defaults
   * 2. McpServerSpec.pinned_tool_approvals - Manual overrides (this field)
   * 3. Agent.McpServerUsage.tool_approval_overrides - Per-agent customization
   * 4. AgentExecution.auto_approve_all - Runtime bypass
   * </pre>
   *
   * <code>repeated .ai.stigmer.agentic.mcpserver.v1.ToolApprovalPolicy pinned_tool_approvals = 11 [json_name = "pinnedToolApprovals"];</code>
   */
  @java.lang.Override
  public java.util.List<? extends ai.stigmer.agentic.mcpserver.v1.ToolApprovalPolicyOrBuilder> 
      getPinnedToolApprovalsOrBuilderList() {
    return pinnedToolApprovals_;
  }
  /**
   * <pre>
   * Manual tool approval overrides set by the MCP server owner.
   *
   * &#64;internal
   * These take precedence over system-generated `McpServerStatus.tool_approvals`.
   * Never auto-modified — only changed by explicit user action (apply/update).
   *
   * Use cases:
   * - Force approval for a tool the classifier marked as auto-approve
   * - Exempt a safe tool the classifier flagged as needing approval
   * - Establish organization-wide safety policies for dangerous tools
   *
   * Policy chain (lowest to highest priority):
   * 1. McpServerStatus.tool_approvals - System-generated defaults
   * 2. McpServerSpec.pinned_tool_approvals - Manual overrides (this field)
   * 3. Agent.McpServerUsage.tool_approval_overrides - Per-agent customization
   * 4. AgentExecution.auto_approve_all - Runtime bypass
   * </pre>
   *
   * <code>repeated .ai.stigmer.agentic.mcpserver.v1.ToolApprovalPolicy pinned_tool_approvals = 11 [json_name = "pinnedToolApprovals"];</code>
   */
  @java.lang.Override
  public int getPinnedToolApprovalsCount() {
    return pinnedToolApprovals_.size();
  }
  /**
   * <pre>
   * Manual tool approval overrides set by the MCP server owner.
   *
   * &#64;internal
   * These take precedence over system-generated `McpServerStatus.tool_approvals`.
   * Never auto-modified — only changed by explicit user action (apply/update).
   *
   * Use cases:
   * - Force approval for a tool the classifier marked as auto-approve
   * - Exempt a safe tool the classifier flagged as needing approval
   * - Establish organization-wide safety policies for dangerous tools
   *
   * Policy chain (lowest to highest priority):
   * 1. McpServerStatus.tool_approvals - System-generated defaults
   * 2. McpServerSpec.pinned_tool_approvals - Manual overrides (this field)
   * 3. Agent.McpServerUsage.tool_approval_overrides - Per-agent customization
   * 4. AgentExecution.auto_approve_all - Runtime bypass
   * </pre>
   *
   * <code>repeated .ai.stigmer.agentic.mcpserver.v1.ToolApprovalPolicy pinned_tool_approvals = 11 [json_name = "pinnedToolApprovals"];</code>
   */
  @java.lang.Override
  public ai.stigmer.agentic.mcpserver.v1.ToolApprovalPolicy getPinnedToolApprovals(int index) {
    return pinnedToolApprovals_.get(index);
  }
  /**
   * <pre>
   * Manual tool approval overrides set by the MCP server owner.
   *
   * &#64;internal
   * These take precedence over system-generated `McpServerStatus.tool_approvals`.
   * Never auto-modified — only changed by explicit user action (apply/update).
   *
   * Use cases:
   * - Force approval for a tool the classifier marked as auto-approve
   * - Exempt a safe tool the classifier flagged as needing approval
   * - Establish organization-wide safety policies for dangerous tools
   *
   * Policy chain (lowest to highest priority):
   * 1. McpServerStatus.tool_approvals - System-generated defaults
   * 2. McpServerSpec.pinned_tool_approvals - Manual overrides (this field)
   * 3. Agent.McpServerUsage.tool_approval_overrides - Per-agent customization
   * 4. AgentExecution.auto_approve_all - Runtime bypass
   * </pre>
   *
   * <code>repeated .ai.stigmer.agentic.mcpserver.v1.ToolApprovalPolicy pinned_tool_approvals = 11 [json_name = "pinnedToolApprovals"];</code>
   */
  @java.lang.Override
  public ai.stigmer.agentic.mcpserver.v1.ToolApprovalPolicyOrBuilder getPinnedToolApprovalsOrBuilder(
      int index) {
    return pinnedToolApprovals_.get(index);
  }

  public static final int REPOSITORY_URL_FIELD_NUMBER = 12;
  @SuppressWarnings("serial")
  private volatile java.lang.Object repositoryUrl_ = "";
  /**
   * <pre>
   * URL of the upstream source repository for this MCP server.
   * Shown in the marketplace so users can inspect the implementation
   * for trust and transparency.
   * Example: "https://github.com/modelcontextprotocol/servers"
   * </pre>
   *
   * <code>string repository_url = 12 [json_name = "repositoryUrl"];</code>
   * @return The repositoryUrl.
   */
  @java.lang.Override
  public java.lang.String getRepositoryUrl() {
    java.lang.Object ref = repositoryUrl_;
    if (ref instanceof java.lang.String) {
      return (java.lang.String) ref;
    } else {
      com.google.protobuf.ByteString bs = 
          (com.google.protobuf.ByteString) ref;
      java.lang.String s = bs.toStringUtf8();
      repositoryUrl_ = s;
      return s;
    }
  }
  /**
   * <pre>
   * URL of the upstream source repository for this MCP server.
   * Shown in the marketplace so users can inspect the implementation
   * for trust and transparency.
   * Example: "https://github.com/modelcontextprotocol/servers"
   * </pre>
   *
   * <code>string repository_url = 12 [json_name = "repositoryUrl"];</code>
   * @return The bytes for repositoryUrl.
   */
  @java.lang.Override
  public com.google.protobuf.ByteString
      getRepositoryUrlBytes() {
    java.lang.Object ref = repositoryUrl_;
    if (ref instanceof java.lang.String) {
      com.google.protobuf.ByteString b = 
          com.google.protobuf.ByteString.copyFromUtf8(
              (java.lang.String) ref);
      repositoryUrl_ = b;
      return b;
    } else {
      return (com.google.protobuf.ByteString) ref;
    }
  }

  public static final int GITHUB_STARS_FIELD_NUMBER = 13;
  private int githubStars_ = 0;
  /**
   * <pre>
   * GitHub star count at the time of curation.
   * Used as a popularity signal in marketplace display.
   * 0 if unknown or non-GitHub repository.
   * </pre>
   *
   * <code>int32 github_stars = 13 [json_name = "githubStars"];</code>
   * @return The githubStars.
   */
  @java.lang.Override
  public int getGithubStars() {
    return githubStars_;
  }

  public static final int AUTH_FIELD_NUMBER = 14;
  private ai.stigmer.agentic.mcpserver.v1.McpServerAuth auth_;
  /**
   * <pre>
   * OAuth authentication configuration for automated credential acquisition.
   * When set, the MCP server's Connect page offers an OAuth flow instead of
   * (or in addition to) manual credential entry.
   *
   * The acquired access token is stored in a system-managed environment
   * (identified by grant.environment_id) as the env var named by
   * auth.target_env_var. That env var must also be declared in env so the
   * execution pipeline knows about it.
   * </pre>
   *
   * <code>.ai.stigmer.agentic.mcpserver.v1.McpServerAuth auth = 14 [json_name = "auth"];</code>
   * @return Whether the auth field is set.
   */
  @java.lang.Override
  public boolean hasAuth() {
    return ((bitField0_ & 0x00000001) != 0);
  }
  /**
   * <pre>
   * OAuth authentication configuration for automated credential acquisition.
   * When set, the MCP server's Connect page offers an OAuth flow instead of
   * (or in addition to) manual credential entry.
   *
   * The acquired access token is stored in a system-managed environment
   * (identified by grant.environment_id) as the env var named by
   * auth.target_env_var. That env var must also be declared in env so the
   * execution pipeline knows about it.
   * </pre>
   *
   * <code>.ai.stigmer.agentic.mcpserver.v1.McpServerAuth auth = 14 [json_name = "auth"];</code>
   * @return The auth.
   */
  @java.lang.Override
  public ai.stigmer.agentic.mcpserver.v1.McpServerAuth getAuth() {
    return auth_ == null ? ai.stigmer.agentic.mcpserver.v1.McpServerAuth.getDefaultInstance() : auth_;
  }
  /**
   * <pre>
   * OAuth authentication configuration for automated credential acquisition.
   * When set, the MCP server's Connect page offers an OAuth flow instead of
   * (or in addition to) manual credential entry.
   *
   * The acquired access token is stored in a system-managed environment
   * (identified by grant.environment_id) as the env var named by
   * auth.target_env_var. That env var must also be declared in env so the
   * execution pipeline knows about it.
   * </pre>
   *
   * <code>.ai.stigmer.agentic.mcpserver.v1.McpServerAuth auth = 14 [json_name = "auth"];</code>
   */
  @java.lang.Override
  public ai.stigmer.agentic.mcpserver.v1.McpServerAuthOrBuilder getAuthOrBuilder() {
    return auth_ == null ? ai.stigmer.agentic.mcpserver.v1.McpServerAuth.getDefaultInstance() : auth_;
  }

  private byte memoizedIsInitialized = -1;
  @java.lang.Override
  public final boolean isInitialized() {
    byte isInitialized = memoizedIsInitialized;
    if (isInitialized == 1) return true;
    if (isInitialized == 0) return false;

    memoizedIsInitialized = 1;
    return true;
  }

  @java.lang.Override
  public void writeTo(com.google.protobuf.CodedOutputStream output)
                      throws java.io.IOException {
    if (!com.google.protobuf.GeneratedMessage.isStringEmpty(description_)) {
      com.google.protobuf.GeneratedMessage.writeString(output, 1, description_);
    }
    if (!com.google.protobuf.GeneratedMessage.isStringEmpty(iconUrl_)) {
      com.google.protobuf.GeneratedMessage.writeString(output, 2, iconUrl_);
    }
    for (int i = 0; i < tags_.size(); i++) {
      com.google.protobuf.GeneratedMessage.writeString(output, 3, tags_.getRaw(i));
    }
    if (serverTypeCase_ == 4) {
      output.writeMessage(4, (ai.stigmer.agentic.mcpserver.v1.StdioServerConfig) serverType_);
    }
    if (serverTypeCase_ == 5) {
      output.writeMessage(5, (ai.stigmer.agentic.mcpserver.v1.HttpServerConfig) serverType_);
    }
    for (int i = 0; i < defaultEnabledTools_.size(); i++) {
      com.google.protobuf.GeneratedMessage.writeString(output, 7, defaultEnabledTools_.getRaw(i));
    }
    com.google.protobuf.GeneratedMessage
      .serializeStringMapTo(
        output,
        internalGetEnv(),
        EnvDefaultEntryHolder.defaultEntry,
        8);
    for (int i = 0; i < pinnedToolApprovals_.size(); i++) {
      output.writeMessage(11, pinnedToolApprovals_.get(i));
    }
    if (!com.google.protobuf.GeneratedMessage.isStringEmpty(repositoryUrl_)) {
      com.google.protobuf.GeneratedMessage.writeString(output, 12, repositoryUrl_);
    }
    if (githubStars_ != 0) {
      output.writeInt32(13, githubStars_);
    }
    if (((bitField0_ & 0x00000001) != 0)) {
      output.writeMessage(14, getAuth());
    }
    getUnknownFields().writeTo(output);
  }

  @java.lang.Override
  public int getSerializedSize() {
    int size = memoizedSize;
    if (size != -1) return size;

    size = 0;
    if (!com.google.protobuf.GeneratedMessage.isStringEmpty(description_)) {
      size += com.google.protobuf.GeneratedMessage.computeStringSize(1, description_);
    }
    if (!com.google.protobuf.GeneratedMessage.isStringEmpty(iconUrl_)) {
      size += com.google.protobuf.GeneratedMessage.computeStringSize(2, iconUrl_);
    }
    {
      int dataSize = 0;
      for (int i = 0; i < tags_.size(); i++) {
        dataSize += computeStringSizeNoTag(tags_.getRaw(i));
      }
      size += dataSize;
      size += 1 * getTagsList().size();
    }
    if (serverTypeCase_ == 4) {
      size += com.google.protobuf.CodedOutputStream
        .computeMessageSize(4, (ai.stigmer.agentic.mcpserver.v1.StdioServerConfig) serverType_);
    }
    if (serverTypeCase_ == 5) {
      size += com.google.protobuf.CodedOutputStream
        .computeMessageSize(5, (ai.stigmer.agentic.mcpserver.v1.HttpServerConfig) serverType_);
    }
    {
      int dataSize = 0;
      for (int i = 0; i < defaultEnabledTools_.size(); i++) {
        dataSize += computeStringSizeNoTag(defaultEnabledTools_.getRaw(i));
      }
      size += dataSize;
      size += 1 * getDefaultEnabledToolsList().size();
    }
    for (java.util.Map.Entry<java.lang.String, ai.stigmer.agentic.environment.v1.EnvVarDeclaration> entry
         : internalGetEnv().getMap().entrySet()) {
      com.google.protobuf.MapEntry<java.lang.String, ai.stigmer.agentic.environment.v1.EnvVarDeclaration>
      env__ = EnvDefaultEntryHolder.defaultEntry.newBuilderForType()
          .setKey(entry.getKey())
          .setValue(entry.getValue())
          .buildPartial();
      size += com.google.protobuf.CodedOutputStream
          .computeMessageSize(8, env__);
    }

        {
          final int count = pinnedToolApprovals_.size();
          for (int i = 0; i < count; i++) {
            size += com.google.protobuf.CodedOutputStream
              .computeMessageSizeNoTag(pinnedToolApprovals_.get(i));
          }
          size += 1 * count;
        }
    if (!com.google.protobuf.GeneratedMessage.isStringEmpty(repositoryUrl_)) {
      size += com.google.protobuf.GeneratedMessage.computeStringSize(12, repositoryUrl_);
    }
    if (githubStars_ != 0) {
      size += com.google.protobuf.CodedOutputStream
        .computeInt32Size(13, githubStars_);
    }
    if (((bitField0_ & 0x00000001) != 0)) {
      size += com.google.protobuf.CodedOutputStream
        .computeMessageSize(14, getAuth());
    }
    size += getUnknownFields().getSerializedSize();
    memoizedSize = size;
    return size;
  }

  @java.lang.Override
  public boolean equals(final java.lang.Object obj) {
    if (obj == this) {
     return true;
    }
    if (!(obj instanceof ai.stigmer.agentic.mcpserver.v1.McpServerSpec)) {
      return super.equals(obj);
    }
    ai.stigmer.agentic.mcpserver.v1.McpServerSpec other = (ai.stigmer.agentic.mcpserver.v1.McpServerSpec) obj;

    if (!getDescription()
        .equals(other.getDescription())) return false;
    if (!getIconUrl()
        .equals(other.getIconUrl())) return false;
    if (!getTagsList()
        .equals(other.getTagsList())) return false;
    if (!getDefaultEnabledToolsList()
        .equals(other.getDefaultEnabledToolsList())) return false;
    if (!internalGetEnv().equals(
        other.internalGetEnv())) return false;
    if (!getPinnedToolApprovalsList()
        .equals(other.getPinnedToolApprovalsList())) return false;
    if (!getRepositoryUrl()
        .equals(other.getRepositoryUrl())) return false;
    if (getGithubStars()
        != other.getGithubStars()) return false;
    if (hasAuth() != other.hasAuth()) return false;
    if (hasAuth()) {
      if (!getAuth()
          .equals(other.getAuth())) return false;
    }
    if (!getServerTypeCase().equals(other.getServerTypeCase())) return false;
    switch (serverTypeCase_) {
      case 4:
        if (!getStdio()
            .equals(other.getStdio())) return false;
        break;
      case 5:
        if (!getHttp()
            .equals(other.getHttp())) return false;
        break;
      case 0:
      default:
    }
    if (!getUnknownFields().equals(other.getUnknownFields())) return false;
    return true;
  }

  @java.lang.Override
  public int hashCode() {
    if (memoizedHashCode != 0) {
      return memoizedHashCode;
    }
    int hash = 41;
    hash = (19 * hash) + getDescriptor().hashCode();
    hash = (37 * hash) + DESCRIPTION_FIELD_NUMBER;
    hash = (53 * hash) + getDescription().hashCode();
    hash = (37 * hash) + ICON_URL_FIELD_NUMBER;
    hash = (53 * hash) + getIconUrl().hashCode();
    if (getTagsCount() > 0) {
      hash = (37 * hash) + TAGS_FIELD_NUMBER;
      hash = (53 * hash) + getTagsList().hashCode();
    }
    if (getDefaultEnabledToolsCount() > 0) {
      hash = (37 * hash) + DEFAULT_ENABLED_TOOLS_FIELD_NUMBER;
      hash = (53 * hash) + getDefaultEnabledToolsList().hashCode();
    }
    if (!internalGetEnv().getMap().isEmpty()) {
      hash = (37 * hash) + ENV_FIELD_NUMBER;
      hash = (53 * hash) + internalGetEnv().hashCode();
    }
    if (getPinnedToolApprovalsCount() > 0) {
      hash = (37 * hash) + PINNED_TOOL_APPROVALS_FIELD_NUMBER;
      hash = (53 * hash) + getPinnedToolApprovalsList().hashCode();
    }
    hash = (37 * hash) + REPOSITORY_URL_FIELD_NUMBER;
    hash = (53 * hash) + getRepositoryUrl().hashCode();
    hash = (37 * hash) + GITHUB_STARS_FIELD_NUMBER;
    hash = (53 * hash) + getGithubStars();
    if (hasAuth()) {
      hash = (37 * hash) + AUTH_FIELD_NUMBER;
      hash = (53 * hash) + getAuth().hashCode();
    }
    switch (serverTypeCase_) {
      case 4:
        hash = (37 * hash) + STDIO_FIELD_NUMBER;
        hash = (53 * hash) + getStdio().hashCode();
        break;
      case 5:
        hash = (37 * hash) + HTTP_FIELD_NUMBER;
        hash = (53 * hash) + getHttp().hashCode();
        break;
      case 0:
      default:
    }
    hash = (29 * hash) + getUnknownFields().hashCode();
    memoizedHashCode = hash;
    return hash;
  }

  public static ai.stigmer.agentic.mcpserver.v1.McpServerSpec parseFrom(
      java.nio.ByteBuffer data)
      throws com.google.protobuf.InvalidProtocolBufferException {
    return PARSER.parseFrom(data);
  }
  public static ai.stigmer.agentic.mcpserver.v1.McpServerSpec parseFrom(
      java.nio.ByteBuffer data,
      com.google.protobuf.ExtensionRegistryLite extensionRegistry)
      throws com.google.protobuf.InvalidProtocolBufferException {
    return PARSER.parseFrom(data, extensionRegistry);
  }
  public static ai.stigmer.agentic.mcpserver.v1.McpServerSpec parseFrom(
      com.google.protobuf.ByteString data)
      throws com.google.protobuf.InvalidProtocolBufferException {
    return PARSER.parseFrom(data);
  }
  public static ai.stigmer.agentic.mcpserver.v1.McpServerSpec parseFrom(
      com.google.protobuf.ByteString data,
      com.google.protobuf.ExtensionRegistryLite extensionRegistry)
      throws com.google.protobuf.InvalidProtocolBufferException {
    return PARSER.parseFrom(data, extensionRegistry);
  }
  public static ai.stigmer.agentic.mcpserver.v1.McpServerSpec parseFrom(byte[] data)
      throws com.google.protobuf.InvalidProtocolBufferException {
    return PARSER.parseFrom(data);
  }
  public static ai.stigmer.agentic.mcpserver.v1.McpServerSpec parseFrom(
      byte[] data,
      com.google.protobuf.ExtensionRegistryLite extensionRegistry)
      throws com.google.protobuf.InvalidProtocolBufferException {
    return PARSER.parseFrom(data, extensionRegistry);
  }
  public static ai.stigmer.agentic.mcpserver.v1.McpServerSpec parseFrom(java.io.InputStream input)
      throws java.io.IOException {
    return com.google.protobuf.GeneratedMessage
        .parseWithIOException(PARSER, input);
  }
  public static ai.stigmer.agentic.mcpserver.v1.McpServerSpec parseFrom(
      java.io.InputStream input,
      com.google.protobuf.ExtensionRegistryLite extensionRegistry)
      throws java.io.IOException {
    return com.google.protobuf.GeneratedMessage
        .parseWithIOException(PARSER, input, extensionRegistry);
  }

  public static ai.stigmer.agentic.mcpserver.v1.McpServerSpec parseDelimitedFrom(java.io.InputStream input)
      throws java.io.IOException {
    return com.google.protobuf.GeneratedMessage
        .parseDelimitedWithIOException(PARSER, input);
  }

  public static ai.stigmer.agentic.mcpserver.v1.McpServerSpec parseDelimitedFrom(
      java.io.InputStream input,
      com.google.protobuf.ExtensionRegistryLite extensionRegistry)
      throws java.io.IOException {
    return com.google.protobuf.GeneratedMessage
        .parseDelimitedWithIOException(PARSER, input, extensionRegistry);
  }
  public static ai.stigmer.agentic.mcpserver.v1.McpServerSpec parseFrom(
      com.google.protobuf.CodedInputStream input)
      throws java.io.IOException {
    return com.google.protobuf.GeneratedMessage
        .parseWithIOException(PARSER, input);
  }
  public static ai.stigmer.agentic.mcpserver.v1.McpServerSpec parseFrom(
      com.google.protobuf.CodedInputStream input,
      com.google.protobuf.ExtensionRegistryLite extensionRegistry)
      throws java.io.IOException {
    return com.google.protobuf.GeneratedMessage
        .parseWithIOException(PARSER, input, extensionRegistry);
  }

  @java.lang.Override
  public Builder newBuilderForType() { return newBuilder(); }
  public static Builder newBuilder() {
    return DEFAULT_INSTANCE.toBuilder();
  }
  public static Builder newBuilder(ai.stigmer.agentic.mcpserver.v1.McpServerSpec prototype) {
    return DEFAULT_INSTANCE.toBuilder().mergeFrom(prototype);
  }
  @java.lang.Override
  public Builder toBuilder() {
    return this == DEFAULT_INSTANCE
        ? new Builder() : new Builder().mergeFrom(this);
  }

  @java.lang.Override
  protected Builder newBuilderForType(
      com.google.protobuf.GeneratedMessage.BuilderParent parent) {
    Builder builder = new Builder(parent);
    return builder;
  }
  /**
   * <pre>
   * McpServerSpec defines the configurable properties of an MCP server.
   *
   * &#64;internal
   * This is the "Template" layer — declares capabilities and requirements.
   * The overview.md file provides the SDK-facing description and example YAML.
   * </pre>
   *
   * Protobuf type {@code ai.stigmer.agentic.mcpserver.v1.McpServerSpec}
   */
  public static final class Builder extends
      com.google.protobuf.GeneratedMessage.Builder<Builder> implements
      // @@protoc_insertion_point(builder_implements:ai.stigmer.agentic.mcpserver.v1.McpServerSpec)
      ai.stigmer.agentic.mcpserver.v1.McpServerSpecOrBuilder {
    public static final com.google.protobuf.Descriptors.Descriptor
        getDescriptor() {
      return ai.stigmer.agentic.mcpserver.v1.SpecProto.internal_static_ai_stigmer_agentic_mcpserver_v1_McpServerSpec_descriptor;
    }

    @SuppressWarnings({"rawtypes"})
    protected com.google.protobuf.MapFieldReflectionAccessor internalGetMapFieldReflection(
        int number) {
      switch (number) {
        case 8:
          return internalGetEnv();
        default:
          throw new RuntimeException(
              "Invalid map field number: " + number);
      }
    }
    @SuppressWarnings({"rawtypes"})
    protected com.google.protobuf.MapFieldReflectionAccessor internalGetMutableMapFieldReflection(
        int number) {
      switch (number) {
        case 8:
          return internalGetMutableEnv();
        default:
          throw new RuntimeException(
              "Invalid map field number: " + number);
      }
    }
    @java.lang.Override
    protected com.google.protobuf.GeneratedMessage.FieldAccessorTable
        internalGetFieldAccessorTable() {
      return ai.stigmer.agentic.mcpserver.v1.SpecProto.internal_static_ai_stigmer_agentic_mcpserver_v1_McpServerSpec_fieldAccessorTable
          .ensureFieldAccessorsInitialized(
              ai.stigmer.agentic.mcpserver.v1.McpServerSpec.class, ai.stigmer.agentic.mcpserver.v1.McpServerSpec.Builder.class);
    }

    // Construct using ai.stigmer.agentic.mcpserver.v1.McpServerSpec.newBuilder()
    private Builder() {
      maybeForceBuilderInitialization();
    }

    private Builder(
        com.google.protobuf.GeneratedMessage.BuilderParent parent) {
      super(parent);
      maybeForceBuilderInitialization();
    }
    private void maybeForceBuilderInitialization() {
      if (com.google.protobuf.GeneratedMessage
              .alwaysUseFieldBuilders) {
        internalGetPinnedToolApprovalsFieldBuilder();
        internalGetAuthFieldBuilder();
      }
    }
    @java.lang.Override
    public Builder clear() {
      super.clear();
      bitField0_ = 0;
      description_ = "";
      iconUrl_ = "";
      tags_ =
          com.google.protobuf.LazyStringArrayList.emptyList();
      if (stdioBuilder_ != null) {
        stdioBuilder_.clear();
      }
      if (httpBuilder_ != null) {
        httpBuilder_.clear();
      }
      defaultEnabledTools_ =
          com.google.protobuf.LazyStringArrayList.emptyList();
      internalGetMutableEnv().clear();
      if (pinnedToolApprovalsBuilder_ == null) {
        pinnedToolApprovals_ = java.util.Collections.emptyList();
      } else {
        pinnedToolApprovals_ = null;
        pinnedToolApprovalsBuilder_.clear();
      }
      bitField0_ = (bitField0_ & ~0x00000080);
      repositoryUrl_ = "";
      githubStars_ = 0;
      auth_ = null;
      if (authBuilder_ != null) {
        authBuilder_.dispose();
        authBuilder_ = null;
      }
      serverTypeCase_ = 0;
      serverType_ = null;
      return this;
    }

    @java.lang.Override
    public com.google.protobuf.Descriptors.Descriptor
        getDescriptorForType() {
      return ai.stigmer.agentic.mcpserver.v1.SpecProto.internal_static_ai_stigmer_agentic_mcpserver_v1_McpServerSpec_descriptor;
    }

    @java.lang.Override
    public ai.stigmer.agentic.mcpserver.v1.McpServerSpec getDefaultInstanceForType() {
      return ai.stigmer.agentic.mcpserver.v1.McpServerSpec.getDefaultInstance();
    }

    @java.lang.Override
    public ai.stigmer.agentic.mcpserver.v1.McpServerSpec build() {
      ai.stigmer.agentic.mcpserver.v1.McpServerSpec result = buildPartial();
      if (!result.isInitialized()) {
        throw newUninitializedMessageException(result);
      }
      return result;
    }

    @java.lang.Override
    public ai.stigmer.agentic.mcpserver.v1.McpServerSpec buildPartial() {
      ai.stigmer.agentic.mcpserver.v1.McpServerSpec result = new ai.stigmer.agentic.mcpserver.v1.McpServerSpec(this);
      buildPartialRepeatedFields(result);
      if (bitField0_ != 0) { buildPartial0(result); }
      buildPartialOneofs(result);
      onBuilt();
      return result;
    }

    private void buildPartialRepeatedFields(ai.stigmer.agentic.mcpserver.v1.McpServerSpec result) {
      if (pinnedToolApprovalsBuilder_ == null) {
        if (((bitField0_ & 0x00000080) != 0)) {
          pinnedToolApprovals_ = java.util.Collections.unmodifiableList(pinnedToolApprovals_);
          bitField0_ = (bitField0_ & ~0x00000080);
        }
        result.pinnedToolApprovals_ = pinnedToolApprovals_;
      } else {
        result.pinnedToolApprovals_ = pinnedToolApprovalsBuilder_.build();
      }
    }

    private void buildPartial0(ai.stigmer.agentic.mcpserver.v1.McpServerSpec result) {
      int from_bitField0_ = bitField0_;
      if (((from_bitField0_ & 0x00000001) != 0)) {
        result.description_ = description_;
      }
      if (((from_bitField0_ & 0x00000002) != 0)) {
        result.iconUrl_ = iconUrl_;
      }
      if (((from_bitField0_ & 0x00000004) != 0)) {
        tags_.makeImmutable();
        result.tags_ = tags_;
      }
      if (((from_bitField0_ & 0x00000020) != 0)) {
        defaultEnabledTools_.makeImmutable();
        result.defaultEnabledTools_ = defaultEnabledTools_;
      }
      if (((from_bitField0_ & 0x00000040) != 0)) {
        result.env_ = internalGetEnv().build(EnvDefaultEntryHolder.defaultEntry);
      }
      if (((from_bitField0_ & 0x00000100) != 0)) {
        result.repositoryUrl_ = repositoryUrl_;
      }
      if (((from_bitField0_ & 0x00000200) != 0)) {
        result.githubStars_ = githubStars_;
      }
      int to_bitField0_ = 0;
      if (((from_bitField0_ & 0x00000400) != 0)) {
        result.auth_ = authBuilder_ == null
            ? auth_
            : authBuilder_.build();
        to_bitField0_ |= 0x00000001;
      }
      result.bitField0_ |= to_bitField0_;
    }

    private void buildPartialOneofs(ai.stigmer.agentic.mcpserver.v1.McpServerSpec result) {
      result.serverTypeCase_ = serverTypeCase_;
      result.serverType_ = this.serverType_;
      if (serverTypeCase_ == 4 &&
          stdioBuilder_ != null) {
        result.serverType_ = stdioBuilder_.build();
      }
      if (serverTypeCase_ == 5 &&
          httpBuilder_ != null) {
        result.serverType_ = httpBuilder_.build();
      }
    }

    @java.lang.Override
    public Builder mergeFrom(com.google.protobuf.Message other) {
      if (other instanceof ai.stigmer.agentic.mcpserver.v1.McpServerSpec) {
        return mergeFrom((ai.stigmer.agentic.mcpserver.v1.McpServerSpec)other);
      } else {
        super.mergeFrom(other);
        return this;
      }
    }

    public Builder mergeFrom(ai.stigmer.agentic.mcpserver.v1.McpServerSpec other) {
      if (other == ai.stigmer.agentic.mcpserver.v1.McpServerSpec.getDefaultInstance()) return this;
      if (!other.getDescription().isEmpty()) {
        description_ = other.description_;
        bitField0_ |= 0x00000001;
        onChanged();
      }
      if (!other.getIconUrl().isEmpty()) {
        iconUrl_ = other.iconUrl_;
        bitField0_ |= 0x00000002;
        onChanged();
      }
      if (!other.tags_.isEmpty()) {
        if (tags_.isEmpty()) {
          tags_ = other.tags_;
          bitField0_ |= 0x00000004;
        } else {
          ensureTagsIsMutable();
          tags_.addAll(other.tags_);
        }
        onChanged();
      }
      if (!other.defaultEnabledTools_.isEmpty()) {
        if (defaultEnabledTools_.isEmpty()) {
          defaultEnabledTools_ = other.defaultEnabledTools_;
          bitField0_ |= 0x00000020;
        } else {
          ensureDefaultEnabledToolsIsMutable();
          defaultEnabledTools_.addAll(other.defaultEnabledTools_);
        }
        onChanged();
      }
      internalGetMutableEnv().mergeFrom(
          other.internalGetEnv());
      bitField0_ |= 0x00000040;
      if (pinnedToolApprovalsBuilder_ == null) {
        if (!other.pinnedToolApprovals_.isEmpty()) {
          if (pinnedToolApprovals_.isEmpty()) {
            pinnedToolApprovals_ = other.pinnedToolApprovals_;
            bitField0_ = (bitField0_ & ~0x00000080);
          } else {
            ensurePinnedToolApprovalsIsMutable();
            pinnedToolApprovals_.addAll(other.pinnedToolApprovals_);
          }
          onChanged();
        }
      } else {
        if (!other.pinnedToolApprovals_.isEmpty()) {
          if (pinnedToolApprovalsBuilder_.isEmpty()) {
            pinnedToolApprovalsBuilder_.dispose();
            pinnedToolApprovalsBuilder_ = null;
            pinnedToolApprovals_ = other.pinnedToolApprovals_;
            bitField0_ = (bitField0_ & ~0x00000080);
            pinnedToolApprovalsBuilder_ = 
              com.google.protobuf.GeneratedMessage.alwaysUseFieldBuilders ?
                 internalGetPinnedToolApprovalsFieldBuilder() : null;
          } else {
            pinnedToolApprovalsBuilder_.addAllMessages(other.pinnedToolApprovals_);
          }
        }
      }
      if (!other.getRepositoryUrl().isEmpty()) {
        repositoryUrl_ = other.repositoryUrl_;
        bitField0_ |= 0x00000100;
        onChanged();
      }
      if (other.getGithubStars() != 0) {
        setGithubStars(other.getGithubStars());
      }
      if (other.hasAuth()) {
        mergeAuth(other.getAuth());
      }
      switch (other.getServerTypeCase()) {
        case STDIO: {
          mergeStdio(other.getStdio());
          break;
        }
        case HTTP: {
          mergeHttp(other.getHttp());
          break;
        }
        case SERVERTYPE_NOT_SET: {
          break;
        }
      }
      this.mergeUnknownFields(other.getUnknownFields());
      onChanged();
      return this;
    }

    @java.lang.Override
    public final boolean isInitialized() {
      return true;
    }

    @java.lang.Override
    public Builder mergeFrom(
        com.google.protobuf.CodedInputStream input,
        com.google.protobuf.ExtensionRegistryLite extensionRegistry)
        throws java.io.IOException {
      if (extensionRegistry == null) {
        throw new java.lang.NullPointerException();
      }
      try {
        boolean done = false;
        while (!done) {
          int tag = input.readTag();
          switch (tag) {
            case 0:
              done = true;
              break;
            case 10: {
              description_ = input.readStringRequireUtf8();
              bitField0_ |= 0x00000001;
              break;
            } // case 10
            case 18: {
              iconUrl_ = input.readStringRequireUtf8();
              bitField0_ |= 0x00000002;
              break;
            } // case 18
            case 26: {
              ensureTagsIsMutable();
              tags_.add(input.readStringRequireUtf8());
              break;
            } // case 26
            case 34: {
              input.readMessage(
                  internalGetStdioFieldBuilder().getBuilder(),
                  extensionRegistry);
              serverTypeCase_ = 4;
              break;
            } // case 34
            case 42: {
              input.readMessage(
                  internalGetHttpFieldBuilder().getBuilder(),
                  extensionRegistry);
              serverTypeCase_ = 5;
              break;
            } // case 42
            case 58: {
              ensureDefaultEnabledToolsIsMutable();
              defaultEnabledTools_.add(input.readStringRequireUtf8());
              break;
            } // case 58
            case 66: {
              com.google.protobuf.MapEntry<java.lang.String, ai.stigmer.agentic.environment.v1.EnvVarDeclaration>
              env__ = input.readMessage(
                  EnvDefaultEntryHolder.defaultEntry.getParserForType(), extensionRegistry);
              internalGetMutableEnv().ensureBuilderMap().put(
                  env__.getKey(), env__.getValue());
              bitField0_ |= 0x00000040;
              break;
            } // case 66
            case 90: {
              ai.stigmer.agentic.mcpserver.v1.ToolApprovalPolicy m =
                  input.readMessage(
                      ai.stigmer.agentic.mcpserver.v1.ToolApprovalPolicy.parser(),
                      extensionRegistry);
              if (pinnedToolApprovalsBuilder_ == null) {
                ensurePinnedToolApprovalsIsMutable();
                pinnedToolApprovals_.add(m);
              } else {
                pinnedToolApprovalsBuilder_.addMessage(m);
              }
              break;
            } // case 90
            case 98: {
              repositoryUrl_ = input.readStringRequireUtf8();
              bitField0_ |= 0x00000100;
              break;
            } // case 98
            case 104: {
              githubStars_ = input.readInt32();
              bitField0_ |= 0x00000200;
              break;
            } // case 104
            case 114: {
              input.readMessage(
                  internalGetAuthFieldBuilder().getBuilder(),
                  extensionRegistry);
              bitField0_ |= 0x00000400;
              break;
            } // case 114
            default: {
              if (!super.parseUnknownField(input, extensionRegistry, tag)) {
                done = true; // was an endgroup tag
              }
              break;
            } // default:
          } // switch (tag)
        } // while (!done)
      } catch (com.google.protobuf.InvalidProtocolBufferException e) {
        throw e.unwrapIOException();
      } finally {
        onChanged();
      } // finally
      return this;
    }
    private int serverTypeCase_ = 0;
    private java.lang.Object serverType_;
    public ServerTypeCase
        getServerTypeCase() {
      return ServerTypeCase.forNumber(
          serverTypeCase_);
    }

    public Builder clearServerType() {
      serverTypeCase_ = 0;
      serverType_ = null;
      onChanged();
      return this;
    }

    private int bitField0_;

    private java.lang.Object description_ = "";
    /**
     * <pre>
     * Human-readable description for marketplace display and documentation.
     * Should explain what this MCP server does and its primary use cases.
     * Example: "GitHub MCP server for repository operations, code search, and PR management"
     * </pre>
     *
     * <code>string description = 1 [json_name = "description"];</code>
     * @return The description.
     */
    public java.lang.String getDescription() {
      java.lang.Object ref = description_;
      if (!(ref instanceof java.lang.String)) {
        com.google.protobuf.ByteString bs =
            (com.google.protobuf.ByteString) ref;
        java.lang.String s = bs.toStringUtf8();
        description_ = s;
        return s;
      } else {
        return (java.lang.String) ref;
      }
    }
    /**
     * <pre>
     * Human-readable description for marketplace display and documentation.
     * Should explain what this MCP server does and its primary use cases.
     * Example: "GitHub MCP server for repository operations, code search, and PR management"
     * </pre>
     *
     * <code>string description = 1 [json_name = "description"];</code>
     * @return The bytes for description.
     */
    public com.google.protobuf.ByteString
        getDescriptionBytes() {
      java.lang.Object ref = description_;
      if (ref instanceof String) {
        com.google.protobuf.ByteString b = 
            com.google.protobuf.ByteString.copyFromUtf8(
                (java.lang.String) ref);
        description_ = b;
        return b;
      } else {
        return (com.google.protobuf.ByteString) ref;
      }
    }
    /**
     * <pre>
     * Human-readable description for marketplace display and documentation.
     * Should explain what this MCP server does and its primary use cases.
     * Example: "GitHub MCP server for repository operations, code search, and PR management"
     * </pre>
     *
     * <code>string description = 1 [json_name = "description"];</code>
     * @param value The description to set.
     * @return This builder for chaining.
     */
    public Builder setDescription(
        java.lang.String value) {
      if (value == null) { throw new NullPointerException(); }
      description_ = value;
      bitField0_ |= 0x00000001;
      onChanged();
      return this;
    }
    /**
     * <pre>
     * Human-readable description for marketplace display and documentation.
     * Should explain what this MCP server does and its primary use cases.
     * Example: "GitHub MCP server for repository operations, code search, and PR management"
     * </pre>
     *
     * <code>string description = 1 [json_name = "description"];</code>
     * @return This builder for chaining.
     */
    public Builder clearDescription() {
      description_ = getDefaultInstance().getDescription();
      bitField0_ = (bitField0_ & ~0x00000001);
      onChanged();
      return this;
    }
    /**
     * <pre>
     * Human-readable description for marketplace display and documentation.
     * Should explain what this MCP server does and its primary use cases.
     * Example: "GitHub MCP server for repository operations, code search, and PR management"
     * </pre>
     *
     * <code>string description = 1 [json_name = "description"];</code>
     * @param value The bytes for description to set.
     * @return This builder for chaining.
     */
    public Builder setDescriptionBytes(
        com.google.protobuf.ByteString value) {
      if (value == null) { throw new NullPointerException(); }
      checkByteStringIsUtf8(value);
      description_ = value;
      bitField0_ |= 0x00000001;
      onChanged();
      return this;
    }

    private java.lang.Object iconUrl_ = "";
    /**
     * <pre>
     * Icon URL for UI display in marketplace and agent configuration screens.
     * Should be a publicly accessible URL to an image (SVG, PNG, or JPEG).
     * Example: "https://github.githubassets.com/favicons/favicon.svg"
     * </pre>
     *
     * <code>string icon_url = 2 [json_name = "iconUrl"];</code>
     * @return The iconUrl.
     */
    public java.lang.String getIconUrl() {
      java.lang.Object ref = iconUrl_;
      if (!(ref instanceof java.lang.String)) {
        com.google.protobuf.ByteString bs =
            (com.google.protobuf.ByteString) ref;
        java.lang.String s = bs.toStringUtf8();
        iconUrl_ = s;
        return s;
      } else {
        return (java.lang.String) ref;
      }
    }
    /**
     * <pre>
     * Icon URL for UI display in marketplace and agent configuration screens.
     * Should be a publicly accessible URL to an image (SVG, PNG, or JPEG).
     * Example: "https://github.githubassets.com/favicons/favicon.svg"
     * </pre>
     *
     * <code>string icon_url = 2 [json_name = "iconUrl"];</code>
     * @return The bytes for iconUrl.
     */
    public com.google.protobuf.ByteString
        getIconUrlBytes() {
      java.lang.Object ref = iconUrl_;
      if (ref instanceof String) {
        com.google.protobuf.ByteString b = 
            com.google.protobuf.ByteString.copyFromUtf8(
                (java.lang.String) ref);
        iconUrl_ = b;
        return b;
      } else {
        return (com.google.protobuf.ByteString) ref;
      }
    }
    /**
     * <pre>
     * Icon URL for UI display in marketplace and agent configuration screens.
     * Should be a publicly accessible URL to an image (SVG, PNG, or JPEG).
     * Example: "https://github.githubassets.com/favicons/favicon.svg"
     * </pre>
     *
     * <code>string icon_url = 2 [json_name = "iconUrl"];</code>
     * @param value The iconUrl to set.
     * @return This builder for chaining.
     */
    public Builder setIconUrl(
        java.lang.String value) {
      if (value == null) { throw new NullPointerException(); }
      iconUrl_ = value;
      bitField0_ |= 0x00000002;
      onChanged();
      return this;
    }
    /**
     * <pre>
     * Icon URL for UI display in marketplace and agent configuration screens.
     * Should be a publicly accessible URL to an image (SVG, PNG, or JPEG).
     * Example: "https://github.githubassets.com/favicons/favicon.svg"
     * </pre>
     *
     * <code>string icon_url = 2 [json_name = "iconUrl"];</code>
     * @return This builder for chaining.
     */
    public Builder clearIconUrl() {
      iconUrl_ = getDefaultInstance().getIconUrl();
      bitField0_ = (bitField0_ & ~0x00000002);
      onChanged();
      return this;
    }
    /**
     * <pre>
     * Icon URL for UI display in marketplace and agent configuration screens.
     * Should be a publicly accessible URL to an image (SVG, PNG, or JPEG).
     * Example: "https://github.githubassets.com/favicons/favicon.svg"
     * </pre>
     *
     * <code>string icon_url = 2 [json_name = "iconUrl"];</code>
     * @param value The bytes for iconUrl to set.
     * @return This builder for chaining.
     */
    public Builder setIconUrlBytes(
        com.google.protobuf.ByteString value) {
      if (value == null) { throw new NullPointerException(); }
      checkByteStringIsUtf8(value);
      iconUrl_ = value;
      bitField0_ |= 0x00000002;
      onChanged();
      return this;
    }

    private com.google.protobuf.LazyStringArrayList tags_ =
        com.google.protobuf.LazyStringArrayList.emptyList();
    private void ensureTagsIsMutable() {
      if (!tags_.isModifiable()) {
        tags_ = new com.google.protobuf.LazyStringArrayList(tags_);
      }
      bitField0_ |= 0x00000004;
    }
    /**
     * <pre>
     * Categorization tags for discoverability in the marketplace.
     * Use lowercase, hyphenated tags that describe the server's domain and capabilities.
     * Examples: ["git", "vcs", "code-analysis"], ["aws", "cloud", "infrastructure"]
     * </pre>
     *
     * <code>repeated string tags = 3 [json_name = "tags"];</code>
     * @return A list containing the tags.
     */
    public com.google.protobuf.ProtocolStringList
        getTagsList() {
      tags_.makeImmutable();
      return tags_;
    }
    /**
     * <pre>
     * Categorization tags for discoverability in the marketplace.
     * Use lowercase, hyphenated tags that describe the server's domain and capabilities.
     * Examples: ["git", "vcs", "code-analysis"], ["aws", "cloud", "infrastructure"]
     * </pre>
     *
     * <code>repeated string tags = 3 [json_name = "tags"];</code>
     * @return The count of tags.
     */
    public int getTagsCount() {
      return tags_.size();
    }
    /**
     * <pre>
     * Categorization tags for discoverability in the marketplace.
     * Use lowercase, hyphenated tags that describe the server's domain and capabilities.
     * Examples: ["git", "vcs", "code-analysis"], ["aws", "cloud", "infrastructure"]
     * </pre>
     *
     * <code>repeated string tags = 3 [json_name = "tags"];</code>
     * @param index The index of the element to return.
     * @return The tags at the given index.
     */
    public java.lang.String getTags(int index) {
      return tags_.get(index);
    }
    /**
     * <pre>
     * Categorization tags for discoverability in the marketplace.
     * Use lowercase, hyphenated tags that describe the server's domain and capabilities.
     * Examples: ["git", "vcs", "code-analysis"], ["aws", "cloud", "infrastructure"]
     * </pre>
     *
     * <code>repeated string tags = 3 [json_name = "tags"];</code>
     * @param index The index of the value to return.
     * @return The bytes of the tags at the given index.
     */
    public com.google.protobuf.ByteString
        getTagsBytes(int index) {
      return tags_.getByteString(index);
    }
    /**
     * <pre>
     * Categorization tags for discoverability in the marketplace.
     * Use lowercase, hyphenated tags that describe the server's domain and capabilities.
     * Examples: ["git", "vcs", "code-analysis"], ["aws", "cloud", "infrastructure"]
     * </pre>
     *
     * <code>repeated string tags = 3 [json_name = "tags"];</code>
     * @param index The index to set the value at.
     * @param value The tags to set.
     * @return This builder for chaining.
     */
    public Builder setTags(
        int index, java.lang.String value) {
      if (value == null) { throw new NullPointerException(); }
      ensureTagsIsMutable();
      tags_.set(index, value);
      bitField0_ |= 0x00000004;
      onChanged();
      return this;
    }
    /**
     * <pre>
     * Categorization tags for discoverability in the marketplace.
     * Use lowercase, hyphenated tags that describe the server's domain and capabilities.
     * Examples: ["git", "vcs", "code-analysis"], ["aws", "cloud", "infrastructure"]
     * </pre>
     *
     * <code>repeated string tags = 3 [json_name = "tags"];</code>
     * @param value The tags to add.
     * @return This builder for chaining.
     */
    public Builder addTags(
        java.lang.String value) {
      if (value == null) { throw new NullPointerException(); }
      ensureTagsIsMutable();
      tags_.add(value);
      bitField0_ |= 0x00000004;
      onChanged();
      return this;
    }
    /**
     * <pre>
     * Categorization tags for discoverability in the marketplace.
     * Use lowercase, hyphenated tags that describe the server's domain and capabilities.
     * Examples: ["git", "vcs", "code-analysis"], ["aws", "cloud", "infrastructure"]
     * </pre>
     *
     * <code>repeated string tags = 3 [json_name = "tags"];</code>
     * @param values The tags to add.
     * @return This builder for chaining.
     */
    public Builder addAllTags(
        java.lang.Iterable<java.lang.String> values) {
      ensureTagsIsMutable();
      com.google.protobuf.AbstractMessageLite.Builder.addAll(
          values, tags_);
      bitField0_ |= 0x00000004;
      onChanged();
      return this;
    }
    /**
     * <pre>
     * Categorization tags for discoverability in the marketplace.
     * Use lowercase, hyphenated tags that describe the server's domain and capabilities.
     * Examples: ["git", "vcs", "code-analysis"], ["aws", "cloud", "infrastructure"]
     * </pre>
     *
     * <code>repeated string tags = 3 [json_name = "tags"];</code>
     * @return This builder for chaining.
     */
    public Builder clearTags() {
      tags_ =
        com.google.protobuf.LazyStringArrayList.emptyList();
      bitField0_ = (bitField0_ & ~0x00000004);;
      onChanged();
      return this;
    }
    /**
     * <pre>
     * Categorization tags for discoverability in the marketplace.
     * Use lowercase, hyphenated tags that describe the server's domain and capabilities.
     * Examples: ["git", "vcs", "code-analysis"], ["aws", "cloud", "infrastructure"]
     * </pre>
     *
     * <code>repeated string tags = 3 [json_name = "tags"];</code>
     * @param value The bytes of the tags to add.
     * @return This builder for chaining.
     */
    public Builder addTagsBytes(
        com.google.protobuf.ByteString value) {
      if (value == null) { throw new NullPointerException(); }
      checkByteStringIsUtf8(value);
      ensureTagsIsMutable();
      tags_.add(value);
      bitField0_ |= 0x00000004;
      onChanged();
      return this;
    }

    private com.google.protobuf.SingleFieldBuilder<
        ai.stigmer.agentic.mcpserver.v1.StdioServerConfig, ai.stigmer.agentic.mcpserver.v1.StdioServerConfig.Builder, ai.stigmer.agentic.mcpserver.v1.StdioServerConfigOrBuilder> stdioBuilder_;
    /**
     * <pre>
     * stdio-based server (subprocess with stdin/stdout communication).
     * Most common type - used for Node.js, Python, and other CLI-based MCP servers.
     * </pre>
     *
     * <code>.ai.stigmer.agentic.mcpserver.v1.StdioServerConfig stdio = 4 [json_name = "stdio"];</code>
     * @return Whether the stdio field is set.
     */
    @java.lang.Override
    public boolean hasStdio() {
      return serverTypeCase_ == 4;
    }
    /**
     * <pre>
     * stdio-based server (subprocess with stdin/stdout communication).
     * Most common type - used for Node.js, Python, and other CLI-based MCP servers.
     * </pre>
     *
     * <code>.ai.stigmer.agentic.mcpserver.v1.StdioServerConfig stdio = 4 [json_name = "stdio"];</code>
     * @return The stdio.
     */
    @java.lang.Override
    public ai.stigmer.agentic.mcpserver.v1.StdioServerConfig getStdio() {
      if (stdioBuilder_ == null) {
        if (serverTypeCase_ == 4) {
          return (ai.stigmer.agentic.mcpserver.v1.StdioServerConfig) serverType_;
        }
        return ai.stigmer.agentic.mcpserver.v1.StdioServerConfig.getDefaultInstance();
      } else {
        if (serverTypeCase_ == 4) {
          return stdioBuilder_.getMessage();
        }
        return ai.stigmer.agentic.mcpserver.v1.StdioServerConfig.getDefaultInstance();
      }
    }
    /**
     * <pre>
     * stdio-based server (subprocess with stdin/stdout communication).
     * Most common type - used for Node.js, Python, and other CLI-based MCP servers.
     * </pre>
     *
     * <code>.ai.stigmer.agentic.mcpserver.v1.StdioServerConfig stdio = 4 [json_name = "stdio"];</code>
     */
    public Builder setStdio(ai.stigmer.agentic.mcpserver.v1.StdioServerConfig value) {
      if (stdioBuilder_ == null) {
        if (value == null) {
          throw new NullPointerException();
        }
        serverType_ = value;
        onChanged();
      } else {
        stdioBuilder_.setMessage(value);
      }
      serverTypeCase_ = 4;
      return this;
    }
    /**
     * <pre>
     * stdio-based server (subprocess with stdin/stdout communication).
     * Most common type - used for Node.js, Python, and other CLI-based MCP servers.
     * </pre>
     *
     * <code>.ai.stigmer.agentic.mcpserver.v1.StdioServerConfig stdio = 4 [json_name = "stdio"];</code>
     */
    public Builder setStdio(
        ai.stigmer.agentic.mcpserver.v1.StdioServerConfig.Builder builderForValue) {
      if (stdioBuilder_ == null) {
        serverType_ = builderForValue.build();
        onChanged();
      } else {
        stdioBuilder_.setMessage(builderForValue.build());
      }
      serverTypeCase_ = 4;
      return this;
    }
    /**
     * <pre>
     * stdio-based server (subprocess with stdin/stdout communication).
     * Most common type - used for Node.js, Python, and other CLI-based MCP servers.
     * </pre>
     *
     * <code>.ai.stigmer.agentic.mcpserver.v1.StdioServerConfig stdio = 4 [json_name = "stdio"];</code>
     */
    public Builder mergeStdio(ai.stigmer.agentic.mcpserver.v1.StdioServerConfig value) {
      if (stdioBuilder_ == null) {
        if (serverTypeCase_ == 4 &&
            serverType_ != ai.stigmer.agentic.mcpserver.v1.StdioServerConfig.getDefaultInstance()) {
          serverType_ = ai.stigmer.agentic.mcpserver.v1.StdioServerConfig.newBuilder((ai.stigmer.agentic.mcpserver.v1.StdioServerConfig) serverType_)
              .mergeFrom(value).buildPartial();
        } else {
          serverType_ = value;
        }
        onChanged();
      } else {
        if (serverTypeCase_ == 4) {
          stdioBuilder_.mergeFrom(value);
        } else {
          stdioBuilder_.setMessage(value);
        }
      }
      serverTypeCase_ = 4;
      return this;
    }
    /**
     * <pre>
     * stdio-based server (subprocess with stdin/stdout communication).
     * Most common type - used for Node.js, Python, and other CLI-based MCP servers.
     * </pre>
     *
     * <code>.ai.stigmer.agentic.mcpserver.v1.StdioServerConfig stdio = 4 [json_name = "stdio"];</code>
     */
    public Builder clearStdio() {
      if (stdioBuilder_ == null) {
        if (serverTypeCase_ == 4) {
          serverTypeCase_ = 0;
          serverType_ = null;
          onChanged();
        }
      } else {
        if (serverTypeCase_ == 4) {
          serverTypeCase_ = 0;
          serverType_ = null;
        }
        stdioBuilder_.clear();
      }
      return this;
    }
    /**
     * <pre>
     * stdio-based server (subprocess with stdin/stdout communication).
     * Most common type - used for Node.js, Python, and other CLI-based MCP servers.
     * </pre>
     *
     * <code>.ai.stigmer.agentic.mcpserver.v1.StdioServerConfig stdio = 4 [json_name = "stdio"];</code>
     */
    public ai.stigmer.agentic.mcpserver.v1.StdioServerConfig.Builder getStdioBuilder() {
      return internalGetStdioFieldBuilder().getBuilder();
    }
    /**
     * <pre>
     * stdio-based server (subprocess with stdin/stdout communication).
     * Most common type - used for Node.js, Python, and other CLI-based MCP servers.
     * </pre>
     *
     * <code>.ai.stigmer.agentic.mcpserver.v1.StdioServerConfig stdio = 4 [json_name = "stdio"];</code>
     */
    @java.lang.Override
    public ai.stigmer.agentic.mcpserver.v1.StdioServerConfigOrBuilder getStdioOrBuilder() {
      if ((serverTypeCase_ == 4) && (stdioBuilder_ != null)) {
        return stdioBuilder_.getMessageOrBuilder();
      } else {
        if (serverTypeCase_ == 4) {
          return (ai.stigmer.agentic.mcpserver.v1.StdioServerConfig) serverType_;
        }
        return ai.stigmer.agentic.mcpserver.v1.StdioServerConfig.getDefaultInstance();
      }
    }
    /**
     * <pre>
     * stdio-based server (subprocess with stdin/stdout communication).
     * Most common type - used for Node.js, Python, and other CLI-based MCP servers.
     * </pre>
     *
     * <code>.ai.stigmer.agentic.mcpserver.v1.StdioServerConfig stdio = 4 [json_name = "stdio"];</code>
     */
    private com.google.protobuf.SingleFieldBuilder<
        ai.stigmer.agentic.mcpserver.v1.StdioServerConfig, ai.stigmer.agentic.mcpserver.v1.StdioServerConfig.Builder, ai.stigmer.agentic.mcpserver.v1.StdioServerConfigOrBuilder> 
        internalGetStdioFieldBuilder() {
      if (stdioBuilder_ == null) {
        if (!(serverTypeCase_ == 4)) {
          serverType_ = ai.stigmer.agentic.mcpserver.v1.StdioServerConfig.getDefaultInstance();
        }
        stdioBuilder_ = new com.google.protobuf.SingleFieldBuilder<
            ai.stigmer.agentic.mcpserver.v1.StdioServerConfig, ai.stigmer.agentic.mcpserver.v1.StdioServerConfig.Builder, ai.stigmer.agentic.mcpserver.v1.StdioServerConfigOrBuilder>(
                (ai.stigmer.agentic.mcpserver.v1.StdioServerConfig) serverType_,
                getParentForChildren(),
                isClean());
        serverType_ = null;
      }
      serverTypeCase_ = 4;
      onChanged();
      return stdioBuilder_;
    }

    private com.google.protobuf.SingleFieldBuilder<
        ai.stigmer.agentic.mcpserver.v1.HttpServerConfig, ai.stigmer.agentic.mcpserver.v1.HttpServerConfig.Builder, ai.stigmer.agentic.mcpserver.v1.HttpServerConfigOrBuilder> httpBuilder_;
    /**
     * <pre>
     * HTTP-based server (HTTP + Server-Sent Events communication).
     * Used for remote/managed MCP services accessible over the network.
     * </pre>
     *
     * <code>.ai.stigmer.agentic.mcpserver.v1.HttpServerConfig http = 5 [json_name = "http"];</code>
     * @return Whether the http field is set.
     */
    @java.lang.Override
    public boolean hasHttp() {
      return serverTypeCase_ == 5;
    }
    /**
     * <pre>
     * HTTP-based server (HTTP + Server-Sent Events communication).
     * Used for remote/managed MCP services accessible over the network.
     * </pre>
     *
     * <code>.ai.stigmer.agentic.mcpserver.v1.HttpServerConfig http = 5 [json_name = "http"];</code>
     * @return The http.
     */
    @java.lang.Override
    public ai.stigmer.agentic.mcpserver.v1.HttpServerConfig getHttp() {
      if (httpBuilder_ == null) {
        if (serverTypeCase_ == 5) {
          return (ai.stigmer.agentic.mcpserver.v1.HttpServerConfig) serverType_;
        }
        return ai.stigmer.agentic.mcpserver.v1.HttpServerConfig.getDefaultInstance();
      } else {
        if (serverTypeCase_ == 5) {
          return httpBuilder_.getMessage();
        }
        return ai.stigmer.agentic.mcpserver.v1.HttpServerConfig.getDefaultInstance();
      }
    }
    /**
     * <pre>
     * HTTP-based server (HTTP + Server-Sent Events communication).
     * Used for remote/managed MCP services accessible over the network.
     * </pre>
     *
     * <code>.ai.stigmer.agentic.mcpserver.v1.HttpServerConfig http = 5 [json_name = "http"];</code>
     */
    public Builder setHttp(ai.stigmer.agentic.mcpserver.v1.HttpServerConfig value) {
      if (httpBuilder_ == null) {
        if (value == null) {
          throw new NullPointerException();
        }
        serverType_ = value;
        onChanged();
      } else {
        httpBuilder_.setMessage(value);
      }
      serverTypeCase_ = 5;
      return this;
    }
    /**
     * <pre>
     * HTTP-based server (HTTP + Server-Sent Events communication).
     * Used for remote/managed MCP services accessible over the network.
     * </pre>
     *
     * <code>.ai.stigmer.agentic.mcpserver.v1.HttpServerConfig http = 5 [json_name = "http"];</code>
     */
    public Builder setHttp(
        ai.stigmer.agentic.mcpserver.v1.HttpServerConfig.Builder builderForValue) {
      if (httpBuilder_ == null) {
        serverType_ = builderForValue.build();
        onChanged();
      } else {
        httpBuilder_.setMessage(builderForValue.build());
      }
      serverTypeCase_ = 5;
      return this;
    }
    /**
     * <pre>
     * HTTP-based server (HTTP + Server-Sent Events communication).
     * Used for remote/managed MCP services accessible over the network.
     * </pre>
     *
     * <code>.ai.stigmer.agentic.mcpserver.v1.HttpServerConfig http = 5 [json_name = "http"];</code>
     */
    public Builder mergeHttp(ai.stigmer.agentic.mcpserver.v1.HttpServerConfig value) {
      if (httpBuilder_ == null) {
        if (serverTypeCase_ == 5 &&
            serverType_ != ai.stigmer.agentic.mcpserver.v1.HttpServerConfig.getDefaultInstance()) {
          serverType_ = ai.stigmer.agentic.mcpserver.v1.HttpServerConfig.newBuilder((ai.stigmer.agentic.mcpserver.v1.HttpServerConfig) serverType_)
              .mergeFrom(value).buildPartial();
        } else {
          serverType_ = value;
        }
        onChanged();
      } else {
        if (serverTypeCase_ == 5) {
          httpBuilder_.mergeFrom(value);
        } else {
          httpBuilder_.setMessage(value);
        }
      }
      serverTypeCase_ = 5;
      return this;
    }
    /**
     * <pre>
     * HTTP-based server (HTTP + Server-Sent Events communication).
     * Used for remote/managed MCP services accessible over the network.
     * </pre>
     *
     * <code>.ai.stigmer.agentic.mcpserver.v1.HttpServerConfig http = 5 [json_name = "http"];</code>
     */
    public Builder clearHttp() {
      if (httpBuilder_ == null) {
        if (serverTypeCase_ == 5) {
          serverTypeCase_ = 0;
          serverType_ = null;
          onChanged();
        }
      } else {
        if (serverTypeCase_ == 5) {
          serverTypeCase_ = 0;
          serverType_ = null;
        }
        httpBuilder_.clear();
      }
      return this;
    }
    /**
     * <pre>
     * HTTP-based server (HTTP + Server-Sent Events communication).
     * Used for remote/managed MCP services accessible over the network.
     * </pre>
     *
     * <code>.ai.stigmer.agentic.mcpserver.v1.HttpServerConfig http = 5 [json_name = "http"];</code>
     */
    public ai.stigmer.agentic.mcpserver.v1.HttpServerConfig.Builder getHttpBuilder() {
      return internalGetHttpFieldBuilder().getBuilder();
    }
    /**
     * <pre>
     * HTTP-based server (HTTP + Server-Sent Events communication).
     * Used for remote/managed MCP services accessible over the network.
     * </pre>
     *
     * <code>.ai.stigmer.agentic.mcpserver.v1.HttpServerConfig http = 5 [json_name = "http"];</code>
     */
    @java.lang.Override
    public ai.stigmer.agentic.mcpserver.v1.HttpServerConfigOrBuilder getHttpOrBuilder() {
      if ((serverTypeCase_ == 5) && (httpBuilder_ != null)) {
        return httpBuilder_.getMessageOrBuilder();
      } else {
        if (serverTypeCase_ == 5) {
          return (ai.stigmer.agentic.mcpserver.v1.HttpServerConfig) serverType_;
        }
        return ai.stigmer.agentic.mcpserver.v1.HttpServerConfig.getDefaultInstance();
      }
    }
    /**
     * <pre>
     * HTTP-based server (HTTP + Server-Sent Events communication).
     * Used for remote/managed MCP services accessible over the network.
     * </pre>
     *
     * <code>.ai.stigmer.agentic.mcpserver.v1.HttpServerConfig http = 5 [json_name = "http"];</code>
     */
    private com.google.protobuf.SingleFieldBuilder<
        ai.stigmer.agentic.mcpserver.v1.HttpServerConfig, ai.stigmer.agentic.mcpserver.v1.HttpServerConfig.Builder, ai.stigmer.agentic.mcpserver.v1.HttpServerConfigOrBuilder> 
        internalGetHttpFieldBuilder() {
      if (httpBuilder_ == null) {
        if (!(serverTypeCase_ == 5)) {
          serverType_ = ai.stigmer.agentic.mcpserver.v1.HttpServerConfig.getDefaultInstance();
        }
        httpBuilder_ = new com.google.protobuf.SingleFieldBuilder<
            ai.stigmer.agentic.mcpserver.v1.HttpServerConfig, ai.stigmer.agentic.mcpserver.v1.HttpServerConfig.Builder, ai.stigmer.agentic.mcpserver.v1.HttpServerConfigOrBuilder>(
                (ai.stigmer.agentic.mcpserver.v1.HttpServerConfig) serverType_,
                getParentForChildren(),
                isClean());
        serverType_ = null;
      }
      serverTypeCase_ = 5;
      onChanged();
      return httpBuilder_;
    }

    private com.google.protobuf.LazyStringArrayList defaultEnabledTools_ =
        com.google.protobuf.LazyStringArrayList.emptyList();
    private void ensureDefaultEnabledToolsIsMutable() {
      if (!defaultEnabledTools_.isModifiable()) {
        defaultEnabledTools_ = new com.google.protobuf.LazyStringArrayList(defaultEnabledTools_);
      }
      bitField0_ |= 0x00000020;
    }
    /**
     * <pre>
     * Default tools to enable from this MCP server.
     * Empty list means all tools are enabled by default.
     * Applies whenever an agent's McpServerUsage.enabled_tools is empty.
     *
     * &#64;internal
     * Tool names must match exactly what the MCP server reports via tools/list.
     * Only names from discovered_capabilities.tools are valid here.
     * Do NOT include names from discovered_capabilities.resource_templates —
     * resource templates are read-only data endpoints, not callable tools.
     *
     * Enforcement is two-layered:
     * - Apply time: mcpserver update/apply rejects (INVALID_ARGUMENT) any
     * name this server's own discovered_capabilities.tools does not
     * contain, with the valid names in the error. Skipped when the server
     * has never been connected (no capabilities yet) — which is always the
     * case on create, so the check only bites from the first re-apply
     * after discovery.
     * - Execution time: the runner enforces the INTERSECTION with the
     * server's live toolset — an unknown name is warned in the runner log
     * and ignored, so a stale entry narrows the toolset but never widens
     * it or fails the run. Enforcement is per harness — see
     * McpServerUsage.enabled_tools in agent/v1/spec.proto.
     * </pre>
     *
     * <code>repeated string default_enabled_tools = 7 [json_name = "defaultEnabledTools"];</code>
     * @return A list containing the defaultEnabledTools.
     */
    public com.google.protobuf.ProtocolStringList
        getDefaultEnabledToolsList() {
      defaultEnabledTools_.makeImmutable();
      return defaultEnabledTools_;
    }
    /**
     * <pre>
     * Default tools to enable from this MCP server.
     * Empty list means all tools are enabled by default.
     * Applies whenever an agent's McpServerUsage.enabled_tools is empty.
     *
     * &#64;internal
     * Tool names must match exactly what the MCP server reports via tools/list.
     * Only names from discovered_capabilities.tools are valid here.
     * Do NOT include names from discovered_capabilities.resource_templates —
     * resource templates are read-only data endpoints, not callable tools.
     *
     * Enforcement is two-layered:
     * - Apply time: mcpserver update/apply rejects (INVALID_ARGUMENT) any
     * name this server's own discovered_capabilities.tools does not
     * contain, with the valid names in the error. Skipped when the server
     * has never been connected (no capabilities yet) — which is always the
     * case on create, so the check only bites from the first re-apply
     * after discovery.
     * - Execution time: the runner enforces the INTERSECTION with the
     * server's live toolset — an unknown name is warned in the runner log
     * and ignored, so a stale entry narrows the toolset but never widens
     * it or fails the run. Enforcement is per harness — see
     * McpServerUsage.enabled_tools in agent/v1/spec.proto.
     * </pre>
     *
     * <code>repeated string default_enabled_tools = 7 [json_name = "defaultEnabledTools"];</code>
     * @return The count of defaultEnabledTools.
     */
    public int getDefaultEnabledToolsCount() {
      return defaultEnabledTools_.size();
    }
    /**
     * <pre>
     * Default tools to enable from this MCP server.
     * Empty list means all tools are enabled by default.
     * Applies whenever an agent's McpServerUsage.enabled_tools is empty.
     *
     * &#64;internal
     * Tool names must match exactly what the MCP server reports via tools/list.
     * Only names from discovered_capabilities.tools are valid here.
     * Do NOT include names from discovered_capabilities.resource_templates —
     * resource templates are read-only data endpoints, not callable tools.
     *
     * Enforcement is two-layered:
     * - Apply time: mcpserver update/apply rejects (INVALID_ARGUMENT) any
     * name this server's own discovered_capabilities.tools does not
     * contain, with the valid names in the error. Skipped when the server
     * has never been connected (no capabilities yet) — which is always the
     * case on create, so the check only bites from the first re-apply
     * after discovery.
     * - Execution time: the runner enforces the INTERSECTION with the
     * server's live toolset — an unknown name is warned in the runner log
     * and ignored, so a stale entry narrows the toolset but never widens
     * it or fails the run. Enforcement is per harness — see
     * McpServerUsage.enabled_tools in agent/v1/spec.proto.
     * </pre>
     *
     * <code>repeated string default_enabled_tools = 7 [json_name = "defaultEnabledTools"];</code>
     * @param index The index of the element to return.
     * @return The defaultEnabledTools at the given index.
     */
    public java.lang.String getDefaultEnabledTools(int index) {
      return defaultEnabledTools_.get(index);
    }
    /**
     * <pre>
     * Default tools to enable from this MCP server.
     * Empty list means all tools are enabled by default.
     * Applies whenever an agent's McpServerUsage.enabled_tools is empty.
     *
     * &#64;internal
     * Tool names must match exactly what the MCP server reports via tools/list.
     * Only names from discovered_capabilities.tools are valid here.
     * Do NOT include names from discovered_capabilities.resource_templates —
     * resource templates are read-only data endpoints, not callable tools.
     *
     * Enforcement is two-layered:
     * - Apply time: mcpserver update/apply rejects (INVALID_ARGUMENT) any
     * name this server's own discovered_capabilities.tools does not
     * contain, with the valid names in the error. Skipped when the server
     * has never been connected (no capabilities yet) — which is always the
     * case on create, so the check only bites from the first re-apply
     * after discovery.
     * - Execution time: the runner enforces the INTERSECTION with the
     * server's live toolset — an unknown name is warned in the runner log
     * and ignored, so a stale entry narrows the toolset but never widens
     * it or fails the run. Enforcement is per harness — see
     * McpServerUsage.enabled_tools in agent/v1/spec.proto.
     * </pre>
     *
     * <code>repeated string default_enabled_tools = 7 [json_name = "defaultEnabledTools"];</code>
     * @param index The index of the value to return.
     * @return The bytes of the defaultEnabledTools at the given index.
     */
    public com.google.protobuf.ByteString
        getDefaultEnabledToolsBytes(int index) {
      return defaultEnabledTools_.getByteString(index);
    }
    /**
     * <pre>
     * Default tools to enable from this MCP server.
     * Empty list means all tools are enabled by default.
     * Applies whenever an agent's McpServerUsage.enabled_tools is empty.
     *
     * &#64;internal
     * Tool names must match exactly what the MCP server reports via tools/list.
     * Only names from discovered_capabilities.tools are valid here.
     * Do NOT include names from discovered_capabilities.resource_templates —
     * resource templates are read-only data endpoints, not callable tools.
     *
     * Enforcement is two-layered:
     * - Apply time: mcpserver update/apply rejects (INVALID_ARGUMENT) any
     * name this server's own discovered_capabilities.tools does not
     * contain, with the valid names in the error. Skipped when the server
     * has never been connected (no capabilities yet) — which is always the
     * case on create, so the check only bites from the first re-apply
     * after discovery.
     * - Execution time: the runner enforces the INTERSECTION with the
     * server's live toolset — an unknown name is warned in the runner log
     * and ignored, so a stale entry narrows the toolset but never widens
     * it or fails the run. Enforcement is per harness — see
     * McpServerUsage.enabled_tools in agent/v1/spec.proto.
     * </pre>
     *
     * <code>repeated string default_enabled_tools = 7 [json_name = "defaultEnabledTools"];</code>
     * @param index The index to set the value at.
     * @param value The defaultEnabledTools to set.
     * @return This builder for chaining.
     */
    public Builder setDefaultEnabledTools(
        int index, java.lang.String value) {
      if (value == null) { throw new NullPointerException(); }
      ensureDefaultEnabledToolsIsMutable();
      defaultEnabledTools_.set(index, value);
      bitField0_ |= 0x00000020;
      onChanged();
      return this;
    }
    /**
     * <pre>
     * Default tools to enable from this MCP server.
     * Empty list means all tools are enabled by default.
     * Applies whenever an agent's McpServerUsage.enabled_tools is empty.
     *
     * &#64;internal
     * Tool names must match exactly what the MCP server reports via tools/list.
     * Only names from discovered_capabilities.tools are valid here.
     * Do NOT include names from discovered_capabilities.resource_templates —
     * resource templates are read-only data endpoints, not callable tools.
     *
     * Enforcement is two-layered:
     * - Apply time: mcpserver update/apply rejects (INVALID_ARGUMENT) any
     * name this server's own discovered_capabilities.tools does not
     * contain, with the valid names in the error. Skipped when the server
     * has never been connected (no capabilities yet) — which is always the
     * case on create, so the check only bites from the first re-apply
     * after discovery.
     * - Execution time: the runner enforces the INTERSECTION with the
     * server's live toolset — an unknown name is warned in the runner log
     * and ignored, so a stale entry narrows the toolset but never widens
     * it or fails the run. Enforcement is per harness — see
     * McpServerUsage.enabled_tools in agent/v1/spec.proto.
     * </pre>
     *
     * <code>repeated string default_enabled_tools = 7 [json_name = "defaultEnabledTools"];</code>
     * @param value The defaultEnabledTools to add.
     * @return This builder for chaining.
     */
    public Builder addDefaultEnabledTools(
        java.lang.String value) {
      if (value == null) { throw new NullPointerException(); }
      ensureDefaultEnabledToolsIsMutable();
      defaultEnabledTools_.add(value);
      bitField0_ |= 0x00000020;
      onChanged();
      return this;
    }
    /**
     * <pre>
     * Default tools to enable from this MCP server.
     * Empty list means all tools are enabled by default.
     * Applies whenever an agent's McpServerUsage.enabled_tools is empty.
     *
     * &#64;internal
     * Tool names must match exactly what the MCP server reports via tools/list.
     * Only names from discovered_capabilities.tools are valid here.
     * Do NOT include names from discovered_capabilities.resource_templates —
     * resource templates are read-only data endpoints, not callable tools.
     *
     * Enforcement is two-layered:
     * - Apply time: mcpserver update/apply rejects (INVALID_ARGUMENT) any
     * name this server's own discovered_capabilities.tools does not
     * contain, with the valid names in the error. Skipped when the server
     * has never been connected (no capabilities yet) — which is always the
     * case on create, so the check only bites from the first re-apply
     * after discovery.
     * - Execution time: the runner enforces the INTERSECTION with the
     * server's live toolset — an unknown name is warned in the runner log
     * and ignored, so a stale entry narrows the toolset but never widens
     * it or fails the run. Enforcement is per harness — see
     * McpServerUsage.enabled_tools in agent/v1/spec.proto.
     * </pre>
     *
     * <code>repeated string default_enabled_tools = 7 [json_name = "defaultEnabledTools"];</code>
     * @param values The defaultEnabledTools to add.
     * @return This builder for chaining.
     */
    public Builder addAllDefaultEnabledTools(
        java.lang.Iterable<java.lang.String> values) {
      ensureDefaultEnabledToolsIsMutable();
      com.google.protobuf.AbstractMessageLite.Builder.addAll(
          values, defaultEnabledTools_);
      bitField0_ |= 0x00000020;
      onChanged();
      return this;
    }
    /**
     * <pre>
     * Default tools to enable from this MCP server.
     * Empty list means all tools are enabled by default.
     * Applies whenever an agent's McpServerUsage.enabled_tools is empty.
     *
     * &#64;internal
     * Tool names must match exactly what the MCP server reports via tools/list.
     * Only names from discovered_capabilities.tools are valid here.
     * Do NOT include names from discovered_capabilities.resource_templates —
     * resource templates are read-only data endpoints, not callable tools.
     *
     * Enforcement is two-layered:
     * - Apply time: mcpserver update/apply rejects (INVALID_ARGUMENT) any
     * name this server's own discovered_capabilities.tools does not
     * contain, with the valid names in the error. Skipped when the server
     * has never been connected (no capabilities yet) — which is always the
     * case on create, so the check only bites from the first re-apply
     * after discovery.
     * - Execution time: the runner enforces the INTERSECTION with the
     * server's live toolset — an unknown name is warned in the runner log
     * and ignored, so a stale entry narrows the toolset but never widens
     * it or fails the run. Enforcement is per harness — see
     * McpServerUsage.enabled_tools in agent/v1/spec.proto.
     * </pre>
     *
     * <code>repeated string default_enabled_tools = 7 [json_name = "defaultEnabledTools"];</code>
     * @return This builder for chaining.
     */
    public Builder clearDefaultEnabledTools() {
      defaultEnabledTools_ =
        com.google.protobuf.LazyStringArrayList.emptyList();
      bitField0_ = (bitField0_ & ~0x00000020);;
      onChanged();
      return this;
    }
    /**
     * <pre>
     * Default tools to enable from this MCP server.
     * Empty list means all tools are enabled by default.
     * Applies whenever an agent's McpServerUsage.enabled_tools is empty.
     *
     * &#64;internal
     * Tool names must match exactly what the MCP server reports via tools/list.
     * Only names from discovered_capabilities.tools are valid here.
     * Do NOT include names from discovered_capabilities.resource_templates —
     * resource templates are read-only data endpoints, not callable tools.
     *
     * Enforcement is two-layered:
     * - Apply time: mcpserver update/apply rejects (INVALID_ARGUMENT) any
     * name this server's own discovered_capabilities.tools does not
     * contain, with the valid names in the error. Skipped when the server
     * has never been connected (no capabilities yet) — which is always the
     * case on create, so the check only bites from the first re-apply
     * after discovery.
     * - Execution time: the runner enforces the INTERSECTION with the
     * server's live toolset — an unknown name is warned in the runner log
     * and ignored, so a stale entry narrows the toolset but never widens
     * it or fails the run. Enforcement is per harness — see
     * McpServerUsage.enabled_tools in agent/v1/spec.proto.
     * </pre>
     *
     * <code>repeated string default_enabled_tools = 7 [json_name = "defaultEnabledTools"];</code>
     * @param value The bytes of the defaultEnabledTools to add.
     * @return This builder for chaining.
     */
    public Builder addDefaultEnabledToolsBytes(
        com.google.protobuf.ByteString value) {
      if (value == null) { throw new NullPointerException(); }
      checkByteStringIsUtf8(value);
      ensureDefaultEnabledToolsIsMutable();
      defaultEnabledTools_.add(value);
      bitField0_ |= 0x00000020;
      onChanged();
      return this;
    }

    private static final class EnvConverter implements com.google.protobuf.MapFieldBuilder.Converter<java.lang.String, ai.stigmer.agentic.environment.v1.EnvVarDeclarationOrBuilder, ai.stigmer.agentic.environment.v1.EnvVarDeclaration> {
      @java.lang.Override
      public ai.stigmer.agentic.environment.v1.EnvVarDeclaration build(ai.stigmer.agentic.environment.v1.EnvVarDeclarationOrBuilder val) {
        if (val instanceof ai.stigmer.agentic.environment.v1.EnvVarDeclaration) { return (ai.stigmer.agentic.environment.v1.EnvVarDeclaration) val; }
        return ((ai.stigmer.agentic.environment.v1.EnvVarDeclaration.Builder) val).build();
      }

      @java.lang.Override
      public com.google.protobuf.MapEntry<java.lang.String, ai.stigmer.agentic.environment.v1.EnvVarDeclaration> defaultEntry() {
        return EnvDefaultEntryHolder.defaultEntry;
      }
    };
    private static final EnvConverter envConverter = new EnvConverter();

    private com.google.protobuf.MapFieldBuilder<
        java.lang.String, ai.stigmer.agentic.environment.v1.EnvVarDeclarationOrBuilder, ai.stigmer.agentic.environment.v1.EnvVarDeclaration, ai.stigmer.agentic.environment.v1.EnvVarDeclaration.Builder> env_;
    private com.google.protobuf.MapFieldBuilder<java.lang.String, ai.stigmer.agentic.environment.v1.EnvVarDeclarationOrBuilder, ai.stigmer.agentic.environment.v1.EnvVarDeclaration, ai.stigmer.agentic.environment.v1.EnvVarDeclaration.Builder>
        internalGetEnv() {
      if (env_ == null) {
        return new com.google.protobuf.MapFieldBuilder<>(envConverter);
      }
      return env_;
    }
    private com.google.protobuf.MapFieldBuilder<java.lang.String, ai.stigmer.agentic.environment.v1.EnvVarDeclarationOrBuilder, ai.stigmer.agentic.environment.v1.EnvVarDeclaration, ai.stigmer.agentic.environment.v1.EnvVarDeclaration.Builder>
        internalGetMutableEnv() {
      if (env_ == null) {
        env_ = new com.google.protobuf.MapFieldBuilder<>(envConverter);
      }
      bitField0_ |= 0x00000040;
      onChanged();
      return env_;
    }
    public int getEnvCount() {
      return internalGetEnv().ensureBuilderMap().size();
    }
    /**
     * <pre>
     * Environment variable declarations for this MCP server.
     * Keys are variable names; values describe their metadata and optionality.
     *
     * &#64;internal
     * Reserved platform keys — declared here like any other variable, but
     * their values are injected by the runner per resolution context and are
     * authoritative over same-named user env entries:
     * STIGMER_CALLER_IDENTITY_KIND  — the verified caller's kind token
     * ("whatsapp_phone", "slack_user_id", "stigmer_user", "anonymous")
     * STIGMER_CALLER_IDENTITY_VALUE — the identity value (wa_id, user id,
     * email); empty for anonymous
     * STIGMER_SESSION_ID            — the session the identity was
     * resolved for; empty outside a session
     * Reserved keys MUST be declared `optional: true`: they have no value at
     * execution-create time, and a required declaration fails the pipeline's
     * env-completeness validation before the runner ever injects them.
     * Discovery (the connect workflow) runs with no session and injects the
     * anonymous sentinel — a server consuming these keys must answer
     * tools/list for anonymous callers and gate tool CALLS instead. The
     * injected header is runner-asserted, not signed: pair it with a shared
     * secret and treat it as trustworthy only for servers you operate.
     * </pre>
     *
     * <code>map&lt;string, .ai.stigmer.agentic.environment.v1.EnvVarDeclaration&gt; env = 8 [json_name = "env"];</code>
     */
    @java.lang.Override
    public boolean containsEnv(
        java.lang.String key) {
      if (key == null) { throw new NullPointerException("map key"); }
      return internalGetEnv().ensureBuilderMap().containsKey(key);
    }
    /**
     * Use {@link #getEnvMap()} instead.
     */
    @java.lang.Override
    @java.lang.Deprecated
    public java.util.Map<java.lang.String, ai.stigmer.agentic.environment.v1.EnvVarDeclaration> getEnv() {
      return getEnvMap();
    }
    /**
     * <pre>
     * Environment variable declarations for this MCP server.
     * Keys are variable names; values describe their metadata and optionality.
     *
     * &#64;internal
     * Reserved platform keys — declared here like any other variable, but
     * their values are injected by the runner per resolution context and are
     * authoritative over same-named user env entries:
     * STIGMER_CALLER_IDENTITY_KIND  — the verified caller's kind token
     * ("whatsapp_phone", "slack_user_id", "stigmer_user", "anonymous")
     * STIGMER_CALLER_IDENTITY_VALUE — the identity value (wa_id, user id,
     * email); empty for anonymous
     * STIGMER_SESSION_ID            — the session the identity was
     * resolved for; empty outside a session
     * Reserved keys MUST be declared `optional: true`: they have no value at
     * execution-create time, and a required declaration fails the pipeline's
     * env-completeness validation before the runner ever injects them.
     * Discovery (the connect workflow) runs with no session and injects the
     * anonymous sentinel — a server consuming these keys must answer
     * tools/list for anonymous callers and gate tool CALLS instead. The
     * injected header is runner-asserted, not signed: pair it with a shared
     * secret and treat it as trustworthy only for servers you operate.
     * </pre>
     *
     * <code>map&lt;string, .ai.stigmer.agentic.environment.v1.EnvVarDeclaration&gt; env = 8 [json_name = "env"];</code>
     */
    @java.lang.Override
    public java.util.Map<java.lang.String, ai.stigmer.agentic.environment.v1.EnvVarDeclaration> getEnvMap() {
      return internalGetEnv().getImmutableMap();
    }
    /**
     * <pre>
     * Environment variable declarations for this MCP server.
     * Keys are variable names; values describe their metadata and optionality.
     *
     * &#64;internal
     * Reserved platform keys — declared here like any other variable, but
     * their values are injected by the runner per resolution context and are
     * authoritative over same-named user env entries:
     * STIGMER_CALLER_IDENTITY_KIND  — the verified caller's kind token
     * ("whatsapp_phone", "slack_user_id", "stigmer_user", "anonymous")
     * STIGMER_CALLER_IDENTITY_VALUE — the identity value (wa_id, user id,
     * email); empty for anonymous
     * STIGMER_SESSION_ID            — the session the identity was
     * resolved for; empty outside a session
     * Reserved keys MUST be declared `optional: true`: they have no value at
     * execution-create time, and a required declaration fails the pipeline's
     * env-completeness validation before the runner ever injects them.
     * Discovery (the connect workflow) runs with no session and injects the
     * anonymous sentinel — a server consuming these keys must answer
     * tools/list for anonymous callers and gate tool CALLS instead. The
     * injected header is runner-asserted, not signed: pair it with a shared
     * secret and treat it as trustworthy only for servers you operate.
     * </pre>
     *
     * <code>map&lt;string, .ai.stigmer.agentic.environment.v1.EnvVarDeclaration&gt; env = 8 [json_name = "env"];</code>
     */
    @java.lang.Override
    public /* nullable */
ai.stigmer.agentic.environment.v1.EnvVarDeclaration getEnvOrDefault(
        java.lang.String key,
        /* nullable */
ai.stigmer.agentic.environment.v1.EnvVarDeclaration defaultValue) {
      if (key == null) { throw new NullPointerException("map key"); }
      java.util.Map<java.lang.String, ai.stigmer.agentic.environment.v1.EnvVarDeclarationOrBuilder> map = internalGetMutableEnv().ensureBuilderMap();
      return map.containsKey(key) ? envConverter.build(map.get(key)) : defaultValue;
    }
    /**
     * <pre>
     * Environment variable declarations for this MCP server.
     * Keys are variable names; values describe their metadata and optionality.
     *
     * &#64;internal
     * Reserved platform keys — declared here like any other variable, but
     * their values are injected by the runner per resolution context and are
     * authoritative over same-named user env entries:
     * STIGMER_CALLER_IDENTITY_KIND  — the verified caller's kind token
     * ("whatsapp_phone", "slack_user_id", "stigmer_user", "anonymous")
     * STIGMER_CALLER_IDENTITY_VALUE — the identity value (wa_id, user id,
     * email); empty for anonymous
     * STIGMER_SESSION_ID            — the session the identity was
     * resolved for; empty outside a session
     * Reserved keys MUST be declared `optional: true`: they have no value at
     * execution-create time, and a required declaration fails the pipeline's
     * env-completeness validation before the runner ever injects them.
     * Discovery (the connect workflow) runs with no session and injects the
     * anonymous sentinel — a server consuming these keys must answer
     * tools/list for anonymous callers and gate tool CALLS instead. The
     * injected header is runner-asserted, not signed: pair it with a shared
     * secret and treat it as trustworthy only for servers you operate.
     * </pre>
     *
     * <code>map&lt;string, .ai.stigmer.agentic.environment.v1.EnvVarDeclaration&gt; env = 8 [json_name = "env"];</code>
     */
    @java.lang.Override
    public ai.stigmer.agentic.environment.v1.EnvVarDeclaration getEnvOrThrow(
        java.lang.String key) {
      if (key == null) { throw new NullPointerException("map key"); }
      java.util.Map<java.lang.String, ai.stigmer.agentic.environment.v1.EnvVarDeclarationOrBuilder> map = internalGetMutableEnv().ensureBuilderMap();
      if (!map.containsKey(key)) {
        throw new java.lang.IllegalArgumentException();
      }
      return envConverter.build(map.get(key));
    }
    public Builder clearEnv() {
      bitField0_ = (bitField0_ & ~0x00000040);
      internalGetMutableEnv().clear();
      return this;
    }
    /**
     * <pre>
     * Environment variable declarations for this MCP server.
     * Keys are variable names; values describe their metadata and optionality.
     *
     * &#64;internal
     * Reserved platform keys — declared here like any other variable, but
     * their values are injected by the runner per resolution context and are
     * authoritative over same-named user env entries:
     * STIGMER_CALLER_IDENTITY_KIND  — the verified caller's kind token
     * ("whatsapp_phone", "slack_user_id", "stigmer_user", "anonymous")
     * STIGMER_CALLER_IDENTITY_VALUE — the identity value (wa_id, user id,
     * email); empty for anonymous
     * STIGMER_SESSION_ID            — the session the identity was
     * resolved for; empty outside a session
     * Reserved keys MUST be declared `optional: true`: they have no value at
     * execution-create time, and a required declaration fails the pipeline's
     * env-completeness validation before the runner ever injects them.
     * Discovery (the connect workflow) runs with no session and injects the
     * anonymous sentinel — a server consuming these keys must answer
     * tools/list for anonymous callers and gate tool CALLS instead. The
     * injected header is runner-asserted, not signed: pair it with a shared
     * secret and treat it as trustworthy only for servers you operate.
     * </pre>
     *
     * <code>map&lt;string, .ai.stigmer.agentic.environment.v1.EnvVarDeclaration&gt; env = 8 [json_name = "env"];</code>
     */
    public Builder removeEnv(
        java.lang.String key) {
      if (key == null) { throw new NullPointerException("map key"); }
      internalGetMutableEnv().ensureBuilderMap()
          .remove(key);
      return this;
    }
    /**
     * Use alternate mutation accessors instead.
     */
    @java.lang.Deprecated
    public java.util.Map<java.lang.String, ai.stigmer.agentic.environment.v1.EnvVarDeclaration>
        getMutableEnv() {
      bitField0_ |= 0x00000040;
      return internalGetMutableEnv().ensureMessageMap();
    }
    /**
     * <pre>
     * Environment variable declarations for this MCP server.
     * Keys are variable names; values describe their metadata and optionality.
     *
     * &#64;internal
     * Reserved platform keys — declared here like any other variable, but
     * their values are injected by the runner per resolution context and are
     * authoritative over same-named user env entries:
     * STIGMER_CALLER_IDENTITY_KIND  — the verified caller's kind token
     * ("whatsapp_phone", "slack_user_id", "stigmer_user", "anonymous")
     * STIGMER_CALLER_IDENTITY_VALUE — the identity value (wa_id, user id,
     * email); empty for anonymous
     * STIGMER_SESSION_ID            — the session the identity was
     * resolved for; empty outside a session
     * Reserved keys MUST be declared `optional: true`: they have no value at
     * execution-create time, and a required declaration fails the pipeline's
     * env-completeness validation before the runner ever injects them.
     * Discovery (the connect workflow) runs with no session and injects the
     * anonymous sentinel — a server consuming these keys must answer
     * tools/list for anonymous callers and gate tool CALLS instead. The
     * injected header is runner-asserted, not signed: pair it with a shared
     * secret and treat it as trustworthy only for servers you operate.
     * </pre>
     *
     * <code>map&lt;string, .ai.stigmer.agentic.environment.v1.EnvVarDeclaration&gt; env = 8 [json_name = "env"];</code>
     */
    public Builder putEnv(
        java.lang.String key,
        ai.stigmer.agentic.environment.v1.EnvVarDeclaration value) {
      if (key == null) { throw new NullPointerException("map key"); }
      if (value == null) { throw new NullPointerException("map value"); }
      internalGetMutableEnv().ensureBuilderMap()
          .put(key, value);
      bitField0_ |= 0x00000040;
      return this;
    }
    /**
     * <pre>
     * Environment variable declarations for this MCP server.
     * Keys are variable names; values describe their metadata and optionality.
     *
     * &#64;internal
     * Reserved platform keys — declared here like any other variable, but
     * their values are injected by the runner per resolution context and are
     * authoritative over same-named user env entries:
     * STIGMER_CALLER_IDENTITY_KIND  — the verified caller's kind token
     * ("whatsapp_phone", "slack_user_id", "stigmer_user", "anonymous")
     * STIGMER_CALLER_IDENTITY_VALUE — the identity value (wa_id, user id,
     * email); empty for anonymous
     * STIGMER_SESSION_ID            — the session the identity was
     * resolved for; empty outside a session
     * Reserved keys MUST be declared `optional: true`: they have no value at
     * execution-create time, and a required declaration fails the pipeline's
     * env-completeness validation before the runner ever injects them.
     * Discovery (the connect workflow) runs with no session and injects the
     * anonymous sentinel — a server consuming these keys must answer
     * tools/list for anonymous callers and gate tool CALLS instead. The
     * injected header is runner-asserted, not signed: pair it with a shared
     * secret and treat it as trustworthy only for servers you operate.
     * </pre>
     *
     * <code>map&lt;string, .ai.stigmer.agentic.environment.v1.EnvVarDeclaration&gt; env = 8 [json_name = "env"];</code>
     */
    public Builder putAllEnv(
        java.util.Map<java.lang.String, ai.stigmer.agentic.environment.v1.EnvVarDeclaration> values) {
      for (java.util.Map.Entry<java.lang.String, ai.stigmer.agentic.environment.v1.EnvVarDeclaration> e : values.entrySet()) {
        if (e.getKey() == null || e.getValue() == null) {
          throw new NullPointerException();
        }
      }
      internalGetMutableEnv().ensureBuilderMap()
          .putAll(values);
      bitField0_ |= 0x00000040;
      return this;
    }
    /**
     * <pre>
     * Environment variable declarations for this MCP server.
     * Keys are variable names; values describe their metadata and optionality.
     *
     * &#64;internal
     * Reserved platform keys — declared here like any other variable, but
     * their values are injected by the runner per resolution context and are
     * authoritative over same-named user env entries:
     * STIGMER_CALLER_IDENTITY_KIND  — the verified caller's kind token
     * ("whatsapp_phone", "slack_user_id", "stigmer_user", "anonymous")
     * STIGMER_CALLER_IDENTITY_VALUE — the identity value (wa_id, user id,
     * email); empty for anonymous
     * STIGMER_SESSION_ID            — the session the identity was
     * resolved for; empty outside a session
     * Reserved keys MUST be declared `optional: true`: they have no value at
     * execution-create time, and a required declaration fails the pipeline's
     * env-completeness validation before the runner ever injects them.
     * Discovery (the connect workflow) runs with no session and injects the
     * anonymous sentinel — a server consuming these keys must answer
     * tools/list for anonymous callers and gate tool CALLS instead. The
     * injected header is runner-asserted, not signed: pair it with a shared
     * secret and treat it as trustworthy only for servers you operate.
     * </pre>
     *
     * <code>map&lt;string, .ai.stigmer.agentic.environment.v1.EnvVarDeclaration&gt; env = 8 [json_name = "env"];</code>
     */
    public ai.stigmer.agentic.environment.v1.EnvVarDeclaration.Builder putEnvBuilderIfAbsent(
        java.lang.String key) {
      java.util.Map<java.lang.String, ai.stigmer.agentic.environment.v1.EnvVarDeclarationOrBuilder> builderMap = internalGetMutableEnv().ensureBuilderMap();
      ai.stigmer.agentic.environment.v1.EnvVarDeclarationOrBuilder entry = builderMap.get(key);
      if (entry == null) {
        entry = ai.stigmer.agentic.environment.v1.EnvVarDeclaration.newBuilder();
        builderMap.put(key, entry);
      }
      if (entry instanceof ai.stigmer.agentic.environment.v1.EnvVarDeclaration) {
        entry = ((ai.stigmer.agentic.environment.v1.EnvVarDeclaration) entry).toBuilder();
        builderMap.put(key, entry);
      }
      return (ai.stigmer.agentic.environment.v1.EnvVarDeclaration.Builder) entry;
    }

    private java.util.List<ai.stigmer.agentic.mcpserver.v1.ToolApprovalPolicy> pinnedToolApprovals_ =
      java.util.Collections.emptyList();
    private void ensurePinnedToolApprovalsIsMutable() {
      if (!((bitField0_ & 0x00000080) != 0)) {
        pinnedToolApprovals_ = new java.util.ArrayList<ai.stigmer.agentic.mcpserver.v1.ToolApprovalPolicy>(pinnedToolApprovals_);
        bitField0_ |= 0x00000080;
       }
    }

    private com.google.protobuf.RepeatedFieldBuilder<
        ai.stigmer.agentic.mcpserver.v1.ToolApprovalPolicy, ai.stigmer.agentic.mcpserver.v1.ToolApprovalPolicy.Builder, ai.stigmer.agentic.mcpserver.v1.ToolApprovalPolicyOrBuilder> pinnedToolApprovalsBuilder_;

    /**
     * <pre>
     * Manual tool approval overrides set by the MCP server owner.
     *
     * &#64;internal
     * These take precedence over system-generated `McpServerStatus.tool_approvals`.
     * Never auto-modified — only changed by explicit user action (apply/update).
     *
     * Use cases:
     * - Force approval for a tool the classifier marked as auto-approve
     * - Exempt a safe tool the classifier flagged as needing approval
     * - Establish organization-wide safety policies for dangerous tools
     *
     * Policy chain (lowest to highest priority):
     * 1. McpServerStatus.tool_approvals - System-generated defaults
     * 2. McpServerSpec.pinned_tool_approvals - Manual overrides (this field)
     * 3. Agent.McpServerUsage.tool_approval_overrides - Per-agent customization
     * 4. AgentExecution.auto_approve_all - Runtime bypass
     * </pre>
     *
     * <code>repeated .ai.stigmer.agentic.mcpserver.v1.ToolApprovalPolicy pinned_tool_approvals = 11 [json_name = "pinnedToolApprovals"];</code>
     */
    public java.util.List<ai.stigmer.agentic.mcpserver.v1.ToolApprovalPolicy> getPinnedToolApprovalsList() {
      if (pinnedToolApprovalsBuilder_ == null) {
        return java.util.Collections.unmodifiableList(pinnedToolApprovals_);
      } else {
        return pinnedToolApprovalsBuilder_.getMessageList();
      }
    }
    /**
     * <pre>
     * Manual tool approval overrides set by the MCP server owner.
     *
     * &#64;internal
     * These take precedence over system-generated `McpServerStatus.tool_approvals`.
     * Never auto-modified — only changed by explicit user action (apply/update).
     *
     * Use cases:
     * - Force approval for a tool the classifier marked as auto-approve
     * - Exempt a safe tool the classifier flagged as needing approval
     * - Establish organization-wide safety policies for dangerous tools
     *
     * Policy chain (lowest to highest priority):
     * 1. McpServerStatus.tool_approvals - System-generated defaults
     * 2. McpServerSpec.pinned_tool_approvals - Manual overrides (this field)
     * 3. Agent.McpServerUsage.tool_approval_overrides - Per-agent customization
     * 4. AgentExecution.auto_approve_all - Runtime bypass
     * </pre>
     *
     * <code>repeated .ai.stigmer.agentic.mcpserver.v1.ToolApprovalPolicy pinned_tool_approvals = 11 [json_name = "pinnedToolApprovals"];</code>
     */
    public int getPinnedToolApprovalsCount() {
      if (pinnedToolApprovalsBuilder_ == null) {
        return pinnedToolApprovals_.size();
      } else {
        return pinnedToolApprovalsBuilder_.getCount();
      }
    }
    /**
     * <pre>
     * Manual tool approval overrides set by the MCP server owner.
     *
     * &#64;internal
     * These take precedence over system-generated `McpServerStatus.tool_approvals`.
     * Never auto-modified — only changed by explicit user action (apply/update).
     *
     * Use cases:
     * - Force approval for a tool the classifier marked as auto-approve
     * - Exempt a safe tool the classifier flagged as needing approval
     * - Establish organization-wide safety policies for dangerous tools
     *
     * Policy chain (lowest to highest priority):
     * 1. McpServerStatus.tool_approvals - System-generated defaults
     * 2. McpServerSpec.pinned_tool_approvals - Manual overrides (this field)
     * 3. Agent.McpServerUsage.tool_approval_overrides - Per-agent customization
     * 4. AgentExecution.auto_approve_all - Runtime bypass
     * </pre>
     *
     * <code>repeated .ai.stigmer.agentic.mcpserver.v1.ToolApprovalPolicy pinned_tool_approvals = 11 [json_name = "pinnedToolApprovals"];</code>
     */
    public ai.stigmer.agentic.mcpserver.v1.ToolApprovalPolicy getPinnedToolApprovals(int index) {
      if (pinnedToolApprovalsBuilder_ == null) {
        return pinnedToolApprovals_.get(index);
      } else {
        return pinnedToolApprovalsBuilder_.getMessage(index);
      }
    }
    /**
     * <pre>
     * Manual tool approval overrides set by the MCP server owner.
     *
     * &#64;internal
     * These take precedence over system-generated `McpServerStatus.tool_approvals`.
     * Never auto-modified — only changed by explicit user action (apply/update).
     *
     * Use cases:
     * - Force approval for a tool the classifier marked as auto-approve
     * - Exempt a safe tool the classifier flagged as needing approval
     * - Establish organization-wide safety policies for dangerous tools
     *
     * Policy chain (lowest to highest priority):
     * 1. McpServerStatus.tool_approvals - System-generated defaults
     * 2. McpServerSpec.pinned_tool_approvals - Manual overrides (this field)
     * 3. Agent.McpServerUsage.tool_approval_overrides - Per-agent customization
     * 4. AgentExecution.auto_approve_all - Runtime bypass
     * </pre>
     *
     * <code>repeated .ai.stigmer.agentic.mcpserver.v1.ToolApprovalPolicy pinned_tool_approvals = 11 [json_name = "pinnedToolApprovals"];</code>
     */
    public Builder setPinnedToolApprovals(
        int index, ai.stigmer.agentic.mcpserver.v1.ToolApprovalPolicy value) {
      if (pinnedToolApprovalsBuilder_ == null) {
        if (value == null) {
          throw new NullPointerException();
        }
        ensurePinnedToolApprovalsIsMutable();
        pinnedToolApprovals_.set(index, value);
        onChanged();
      } else {
        pinnedToolApprovalsBuilder_.setMessage(index, value);
      }
      return this;
    }
    /**
     * <pre>
     * Manual tool approval overrides set by the MCP server owner.
     *
     * &#64;internal
     * These take precedence over system-generated `McpServerStatus.tool_approvals`.
     * Never auto-modified — only changed by explicit user action (apply/update).
     *
     * Use cases:
     * - Force approval for a tool the classifier marked as auto-approve
     * - Exempt a safe tool the classifier flagged as needing approval
     * - Establish organization-wide safety policies for dangerous tools
     *
     * Policy chain (lowest to highest priority):
     * 1. McpServerStatus.tool_approvals - System-generated defaults
     * 2. McpServerSpec.pinned_tool_approvals - Manual overrides (this field)
     * 3. Agent.McpServerUsage.tool_approval_overrides - Per-agent customization
     * 4. AgentExecution.auto_approve_all - Runtime bypass
     * </pre>
     *
     * <code>repeated .ai.stigmer.agentic.mcpserver.v1.ToolApprovalPolicy pinned_tool_approvals = 11 [json_name = "pinnedToolApprovals"];</code>
     */
    public Builder setPinnedToolApprovals(
        int index, ai.stigmer.agentic.mcpserver.v1.ToolApprovalPolicy.Builder builderForValue) {
      if (pinnedToolApprovalsBuilder_ == null) {
        ensurePinnedToolApprovalsIsMutable();
        pinnedToolApprovals_.set(index, builderForValue.build());
        onChanged();
      } else {
        pinnedToolApprovalsBuilder_.setMessage(index, builderForValue.build());
      }
      return this;
    }
    /**
     * <pre>
     * Manual tool approval overrides set by the MCP server owner.
     *
     * &#64;internal
     * These take precedence over system-generated `McpServerStatus.tool_approvals`.
     * Never auto-modified — only changed by explicit user action (apply/update).
     *
     * Use cases:
     * - Force approval for a tool the classifier marked as auto-approve
     * - Exempt a safe tool the classifier flagged as needing approval
     * - Establish organization-wide safety policies for dangerous tools
     *
     * Policy chain (lowest to highest priority):
     * 1. McpServerStatus.tool_approvals - System-generated defaults
     * 2. McpServerSpec.pinned_tool_approvals - Manual overrides (this field)
     * 3. Agent.McpServerUsage.tool_approval_overrides - Per-agent customization
     * 4. AgentExecution.auto_approve_all - Runtime bypass
     * </pre>
     *
     * <code>repeated .ai.stigmer.agentic.mcpserver.v1.ToolApprovalPolicy pinned_tool_approvals = 11 [json_name = "pinnedToolApprovals"];</code>
     */
    public Builder addPinnedToolApprovals(ai.stigmer.agentic.mcpserver.v1.ToolApprovalPolicy value) {
      if (pinnedToolApprovalsBuilder_ == null) {
        if (value == null) {
          throw new NullPointerException();
        }
        ensurePinnedToolApprovalsIsMutable();
        pinnedToolApprovals_.add(value);
        onChanged();
      } else {
        pinnedToolApprovalsBuilder_.addMessage(value);
      }
      return this;
    }
    /**
     * <pre>
     * Manual tool approval overrides set by the MCP server owner.
     *
     * &#64;internal
     * These take precedence over system-generated `McpServerStatus.tool_approvals`.
     * Never auto-modified — only changed by explicit user action (apply/update).
     *
     * Use cases:
     * - Force approval for a tool the classifier marked as auto-approve
     * - Exempt a safe tool the classifier flagged as needing approval
     * - Establish organization-wide safety policies for dangerous tools
     *
     * Policy chain (lowest to highest priority):
     * 1. McpServerStatus.tool_approvals - System-generated defaults
     * 2. McpServerSpec.pinned_tool_approvals - Manual overrides (this field)
     * 3. Agent.McpServerUsage.tool_approval_overrides - Per-agent customization
     * 4. AgentExecution.auto_approve_all - Runtime bypass
     * </pre>
     *
     * <code>repeated .ai.stigmer.agentic.mcpserver.v1.ToolApprovalPolicy pinned_tool_approvals = 11 [json_name = "pinnedToolApprovals"];</code>
     */
    public Builder addPinnedToolApprovals(
        int index, ai.stigmer.agentic.mcpserver.v1.ToolApprovalPolicy value) {
      if (pinnedToolApprovalsBuilder_ == null) {
        if (value == null) {
          throw new NullPointerException();
        }
        ensurePinnedToolApprovalsIsMutable();
        pinnedToolApprovals_.add(index, value);
        onChanged();
      } else {
        pinnedToolApprovalsBuilder_.addMessage(index, value);
      }
      return this;
    }
    /**
     * <pre>
     * Manual tool approval overrides set by the MCP server owner.
     *
     * &#64;internal
     * These take precedence over system-generated `McpServerStatus.tool_approvals`.
     * Never auto-modified — only changed by explicit user action (apply/update).
     *
     * Use cases:
     * - Force approval for a tool the classifier marked as auto-approve
     * - Exempt a safe tool the classifier flagged as needing approval
     * - Establish organization-wide safety policies for dangerous tools
     *
     * Policy chain (lowest to highest priority):
     * 1. McpServerStatus.tool_approvals - System-generated defaults
     * 2. McpServerSpec.pinned_tool_approvals - Manual overrides (this field)
     * 3. Agent.McpServerUsage.tool_approval_overrides - Per-agent customization
     * 4. AgentExecution.auto_approve_all - Runtime bypass
     * </pre>
     *
     * <code>repeated .ai.stigmer.agentic.mcpserver.v1.ToolApprovalPolicy pinned_tool_approvals = 11 [json_name = "pinnedToolApprovals"];</code>
     */
    public Builder addPinnedToolApprovals(
        ai.stigmer.agentic.mcpserver.v1.ToolApprovalPolicy.Builder builderForValue) {
      if (pinnedToolApprovalsBuilder_ == null) {
        ensurePinnedToolApprovalsIsMutable();
        pinnedToolApprovals_.add(builderForValue.build());
        onChanged();
      } else {
        pinnedToolApprovalsBuilder_.addMessage(builderForValue.build());
      }
      return this;
    }
    /**
     * <pre>
     * Manual tool approval overrides set by the MCP server owner.
     *
     * &#64;internal
     * These take precedence over system-generated `McpServerStatus.tool_approvals`.
     * Never auto-modified — only changed by explicit user action (apply/update).
     *
     * Use cases:
     * - Force approval for a tool the classifier marked as auto-approve
     * - Exempt a safe tool the classifier flagged as needing approval
     * - Establish organization-wide safety policies for dangerous tools
     *
     * Policy chain (lowest to highest priority):
     * 1. McpServerStatus.tool_approvals - System-generated defaults
     * 2. McpServerSpec.pinned_tool_approvals - Manual overrides (this field)
     * 3. Agent.McpServerUsage.tool_approval_overrides - Per-agent customization
     * 4. AgentExecution.auto_approve_all - Runtime bypass
     * </pre>
     *
     * <code>repeated .ai.stigmer.agentic.mcpserver.v1.ToolApprovalPolicy pinned_tool_approvals = 11 [json_name = "pinnedToolApprovals"];</code>
     */
    public Builder addPinnedToolApprovals(
        int index, ai.stigmer.agentic.mcpserver.v1.ToolApprovalPolicy.Builder builderForValue) {
      if (pinnedToolApprovalsBuilder_ == null) {
        ensurePinnedToolApprovalsIsMutable();
        pinnedToolApprovals_.add(index, builderForValue.build());
        onChanged();
      } else {
        pinnedToolApprovalsBuilder_.addMessage(index, builderForValue.build());
      }
      return this;
    }
    /**
     * <pre>
     * Manual tool approval overrides set by the MCP server owner.
     *
     * &#64;internal
     * These take precedence over system-generated `McpServerStatus.tool_approvals`.
     * Never auto-modified — only changed by explicit user action (apply/update).
     *
     * Use cases:
     * - Force approval for a tool the classifier marked as auto-approve
     * - Exempt a safe tool the classifier flagged as needing approval
     * - Establish organization-wide safety policies for dangerous tools
     *
     * Policy chain (lowest to highest priority):
     * 1. McpServerStatus.tool_approvals - System-generated defaults
     * 2. McpServerSpec.pinned_tool_approvals - Manual overrides (this field)
     * 3. Agent.McpServerUsage.tool_approval_overrides - Per-agent customization
     * 4. AgentExecution.auto_approve_all - Runtime bypass
     * </pre>
     *
     * <code>repeated .ai.stigmer.agentic.mcpserver.v1.ToolApprovalPolicy pinned_tool_approvals = 11 [json_name = "pinnedToolApprovals"];</code>
     */
    public Builder addAllPinnedToolApprovals(
        java.lang.Iterable<? extends ai.stigmer.agentic.mcpserver.v1.ToolApprovalPolicy> values) {
      if (pinnedToolApprovalsBuilder_ == null) {
        ensurePinnedToolApprovalsIsMutable();
        com.google.protobuf.AbstractMessageLite.Builder.addAll(
            values, pinnedToolApprovals_);
        onChanged();
      } else {
        pinnedToolApprovalsBuilder_.addAllMessages(values);
      }
      return this;
    }
    /**
     * <pre>
     * Manual tool approval overrides set by the MCP server owner.
     *
     * &#64;internal
     * These take precedence over system-generated `McpServerStatus.tool_approvals`.
     * Never auto-modified — only changed by explicit user action (apply/update).
     *
     * Use cases:
     * - Force approval for a tool the classifier marked as auto-approve
     * - Exempt a safe tool the classifier flagged as needing approval
     * - Establish organization-wide safety policies for dangerous tools
     *
     * Policy chain (lowest to highest priority):
     * 1. McpServerStatus.tool_approvals - System-generated defaults
     * 2. McpServerSpec.pinned_tool_approvals - Manual overrides (this field)
     * 3. Agent.McpServerUsage.tool_approval_overrides - Per-agent customization
     * 4. AgentExecution.auto_approve_all - Runtime bypass
     * </pre>
     *
     * <code>repeated .ai.stigmer.agentic.mcpserver.v1.ToolApprovalPolicy pinned_tool_approvals = 11 [json_name = "pinnedToolApprovals"];</code>
     */
    public Builder clearPinnedToolApprovals() {
      if (pinnedToolApprovalsBuilder_ == null) {
        pinnedToolApprovals_ = java.util.Collections.emptyList();
        bitField0_ = (bitField0_ & ~0x00000080);
        onChanged();
      } else {
        pinnedToolApprovalsBuilder_.clear();
      }
      return this;
    }
    /**
     * <pre>
     * Manual tool approval overrides set by the MCP server owner.
     *
     * &#64;internal
     * These take precedence over system-generated `McpServerStatus.tool_approvals`.
     * Never auto-modified — only changed by explicit user action (apply/update).
     *
     * Use cases:
     * - Force approval for a tool the classifier marked as auto-approve
     * - Exempt a safe tool the classifier flagged as needing approval
     * - Establish organization-wide safety policies for dangerous tools
     *
     * Policy chain (lowest to highest priority):
     * 1. McpServerStatus.tool_approvals - System-generated defaults
     * 2. McpServerSpec.pinned_tool_approvals - Manual overrides (this field)
     * 3. Agent.McpServerUsage.tool_approval_overrides - Per-agent customization
     * 4. AgentExecution.auto_approve_all - Runtime bypass
     * </pre>
     *
     * <code>repeated .ai.stigmer.agentic.mcpserver.v1.ToolApprovalPolicy pinned_tool_approvals = 11 [json_name = "pinnedToolApprovals"];</code>
     */
    public Builder removePinnedToolApprovals(int index) {
      if (pinnedToolApprovalsBuilder_ == null) {
        ensurePinnedToolApprovalsIsMutable();
        pinnedToolApprovals_.remove(index);
        onChanged();
      } else {
        pinnedToolApprovalsBuilder_.remove(index);
      }
      return this;
    }
    /**
     * <pre>
     * Manual tool approval overrides set by the MCP server owner.
     *
     * &#64;internal
     * These take precedence over system-generated `McpServerStatus.tool_approvals`.
     * Never auto-modified — only changed by explicit user action (apply/update).
     *
     * Use cases:
     * - Force approval for a tool the classifier marked as auto-approve
     * - Exempt a safe tool the classifier flagged as needing approval
     * - Establish organization-wide safety policies for dangerous tools
     *
     * Policy chain (lowest to highest priority):
     * 1. McpServerStatus.tool_approvals - System-generated defaults
     * 2. McpServerSpec.pinned_tool_approvals - Manual overrides (this field)
     * 3. Agent.McpServerUsage.tool_approval_overrides - Per-agent customization
     * 4. AgentExecution.auto_approve_all - Runtime bypass
     * </pre>
     *
     * <code>repeated .ai.stigmer.agentic.mcpserver.v1.ToolApprovalPolicy pinned_tool_approvals = 11 [json_name = "pinnedToolApprovals"];</code>
     */
    public ai.stigmer.agentic.mcpserver.v1.ToolApprovalPolicy.Builder getPinnedToolApprovalsBuilder(
        int index) {
      return internalGetPinnedToolApprovalsFieldBuilder().getBuilder(index);
    }
    /**
     * <pre>
     * Manual tool approval overrides set by the MCP server owner.
     *
     * &#64;internal
     * These take precedence over system-generated `McpServerStatus.tool_approvals`.
     * Never auto-modified — only changed by explicit user action (apply/update).
     *
     * Use cases:
     * - Force approval for a tool the classifier marked as auto-approve
     * - Exempt a safe tool the classifier flagged as needing approval
     * - Establish organization-wide safety policies for dangerous tools
     *
     * Policy chain (lowest to highest priority):
     * 1. McpServerStatus.tool_approvals - System-generated defaults
     * 2. McpServerSpec.pinned_tool_approvals - Manual overrides (this field)
     * 3. Agent.McpServerUsage.tool_approval_overrides - Per-agent customization
     * 4. AgentExecution.auto_approve_all - Runtime bypass
     * </pre>
     *
     * <code>repeated .ai.stigmer.agentic.mcpserver.v1.ToolApprovalPolicy pinned_tool_approvals = 11 [json_name = "pinnedToolApprovals"];</code>
     */
    public ai.stigmer.agentic.mcpserver.v1.ToolApprovalPolicyOrBuilder getPinnedToolApprovalsOrBuilder(
        int index) {
      if (pinnedToolApprovalsBuilder_ == null) {
        return pinnedToolApprovals_.get(index);  } else {
        return pinnedToolApprovalsBuilder_.getMessageOrBuilder(index);
      }
    }
    /**
     * <pre>
     * Manual tool approval overrides set by the MCP server owner.
     *
     * &#64;internal
     * These take precedence over system-generated `McpServerStatus.tool_approvals`.
     * Never auto-modified — only changed by explicit user action (apply/update).
     *
     * Use cases:
     * - Force approval for a tool the classifier marked as auto-approve
     * - Exempt a safe tool the classifier flagged as needing approval
     * - Establish organization-wide safety policies for dangerous tools
     *
     * Policy chain (lowest to highest priority):
     * 1. McpServerStatus.tool_approvals - System-generated defaults
     * 2. McpServerSpec.pinned_tool_approvals - Manual overrides (this field)
     * 3. Agent.McpServerUsage.tool_approval_overrides - Per-agent customization
     * 4. AgentExecution.auto_approve_all - Runtime bypass
     * </pre>
     *
     * <code>repeated .ai.stigmer.agentic.mcpserver.v1.ToolApprovalPolicy pinned_tool_approvals = 11 [json_name = "pinnedToolApprovals"];</code>
     */
    public java.util.List<? extends ai.stigmer.agentic.mcpserver.v1.ToolApprovalPolicyOrBuilder> 
         getPinnedToolApprovalsOrBuilderList() {
      if (pinnedToolApprovalsBuilder_ != null) {
        return pinnedToolApprovalsBuilder_.getMessageOrBuilderList();
      } else {
        return java.util.Collections.unmodifiableList(pinnedToolApprovals_);
      }
    }
    /**
     * <pre>
     * Manual tool approval overrides set by the MCP server owner.
     *
     * &#64;internal
     * These take precedence over system-generated `McpServerStatus.tool_approvals`.
     * Never auto-modified — only changed by explicit user action (apply/update).
     *
     * Use cases:
     * - Force approval for a tool the classifier marked as auto-approve
     * - Exempt a safe tool the classifier flagged as needing approval
     * - Establish organization-wide safety policies for dangerous tools
     *
     * Policy chain (lowest to highest priority):
     * 1. McpServerStatus.tool_approvals - System-generated defaults
     * 2. McpServerSpec.pinned_tool_approvals - Manual overrides (this field)
     * 3. Agent.McpServerUsage.tool_approval_overrides - Per-agent customization
     * 4. AgentExecution.auto_approve_all - Runtime bypass
     * </pre>
     *
     * <code>repeated .ai.stigmer.agentic.mcpserver.v1.ToolApprovalPolicy pinned_tool_approvals = 11 [json_name = "pinnedToolApprovals"];</code>
     */
    public ai.stigmer.agentic.mcpserver.v1.ToolApprovalPolicy.Builder addPinnedToolApprovalsBuilder() {
      return internalGetPinnedToolApprovalsFieldBuilder().addBuilder(
          ai.stigmer.agentic.mcpserver.v1.ToolApprovalPolicy.getDefaultInstance());
    }
    /**
     * <pre>
     * Manual tool approval overrides set by the MCP server owner.
     *
     * &#64;internal
     * These take precedence over system-generated `McpServerStatus.tool_approvals`.
     * Never auto-modified — only changed by explicit user action (apply/update).
     *
     * Use cases:
     * - Force approval for a tool the classifier marked as auto-approve
     * - Exempt a safe tool the classifier flagged as needing approval
     * - Establish organization-wide safety policies for dangerous tools
     *
     * Policy chain (lowest to highest priority):
     * 1. McpServerStatus.tool_approvals - System-generated defaults
     * 2. McpServerSpec.pinned_tool_approvals - Manual overrides (this field)
     * 3. Agent.McpServerUsage.tool_approval_overrides - Per-agent customization
     * 4. AgentExecution.auto_approve_all - Runtime bypass
     * </pre>
     *
     * <code>repeated .ai.stigmer.agentic.mcpserver.v1.ToolApprovalPolicy pinned_tool_approvals = 11 [json_name = "pinnedToolApprovals"];</code>
     */
    public ai.stigmer.agentic.mcpserver.v1.ToolApprovalPolicy.Builder addPinnedToolApprovalsBuilder(
        int index) {
      return internalGetPinnedToolApprovalsFieldBuilder().addBuilder(
          index, ai.stigmer.agentic.mcpserver.v1.ToolApprovalPolicy.getDefaultInstance());
    }
    /**
     * <pre>
     * Manual tool approval overrides set by the MCP server owner.
     *
     * &#64;internal
     * These take precedence over system-generated `McpServerStatus.tool_approvals`.
     * Never auto-modified — only changed by explicit user action (apply/update).
     *
     * Use cases:
     * - Force approval for a tool the classifier marked as auto-approve
     * - Exempt a safe tool the classifier flagged as needing approval
     * - Establish organization-wide safety policies for dangerous tools
     *
     * Policy chain (lowest to highest priority):
     * 1. McpServerStatus.tool_approvals - System-generated defaults
     * 2. McpServerSpec.pinned_tool_approvals - Manual overrides (this field)
     * 3. Agent.McpServerUsage.tool_approval_overrides - Per-agent customization
     * 4. AgentExecution.auto_approve_all - Runtime bypass
     * </pre>
     *
     * <code>repeated .ai.stigmer.agentic.mcpserver.v1.ToolApprovalPolicy pinned_tool_approvals = 11 [json_name = "pinnedToolApprovals"];</code>
     */
    public java.util.List<ai.stigmer.agentic.mcpserver.v1.ToolApprovalPolicy.Builder> 
         getPinnedToolApprovalsBuilderList() {
      return internalGetPinnedToolApprovalsFieldBuilder().getBuilderList();
    }
    private com.google.protobuf.RepeatedFieldBuilder<
        ai.stigmer.agentic.mcpserver.v1.ToolApprovalPolicy, ai.stigmer.agentic.mcpserver.v1.ToolApprovalPolicy.Builder, ai.stigmer.agentic.mcpserver.v1.ToolApprovalPolicyOrBuilder> 
        internalGetPinnedToolApprovalsFieldBuilder() {
      if (pinnedToolApprovalsBuilder_ == null) {
        pinnedToolApprovalsBuilder_ = new com.google.protobuf.RepeatedFieldBuilder<
            ai.stigmer.agentic.mcpserver.v1.ToolApprovalPolicy, ai.stigmer.agentic.mcpserver.v1.ToolApprovalPolicy.Builder, ai.stigmer.agentic.mcpserver.v1.ToolApprovalPolicyOrBuilder>(
                pinnedToolApprovals_,
                ((bitField0_ & 0x00000080) != 0),
                getParentForChildren(),
                isClean());
        pinnedToolApprovals_ = null;
      }
      return pinnedToolApprovalsBuilder_;
    }

    private java.lang.Object repositoryUrl_ = "";
    /**
     * <pre>
     * URL of the upstream source repository for this MCP server.
     * Shown in the marketplace so users can inspect the implementation
     * for trust and transparency.
     * Example: "https://github.com/modelcontextprotocol/servers"
     * </pre>
     *
     * <code>string repository_url = 12 [json_name = "repositoryUrl"];</code>
     * @return The repositoryUrl.
     */
    public java.lang.String getRepositoryUrl() {
      java.lang.Object ref = repositoryUrl_;
      if (!(ref instanceof java.lang.String)) {
        com.google.protobuf.ByteString bs =
            (com.google.protobuf.ByteString) ref;
        java.lang.String s = bs.toStringUtf8();
        repositoryUrl_ = s;
        return s;
      } else {
        return (java.lang.String) ref;
      }
    }
    /**
     * <pre>
     * URL of the upstream source repository for this MCP server.
     * Shown in the marketplace so users can inspect the implementation
     * for trust and transparency.
     * Example: "https://github.com/modelcontextprotocol/servers"
     * </pre>
     *
     * <code>string repository_url = 12 [json_name = "repositoryUrl"];</code>
     * @return The bytes for repositoryUrl.
     */
    public com.google.protobuf.ByteString
        getRepositoryUrlBytes() {
      java.lang.Object ref = repositoryUrl_;
      if (ref instanceof String) {
        com.google.protobuf.ByteString b = 
            com.google.protobuf.ByteString.copyFromUtf8(
                (java.lang.String) ref);
        repositoryUrl_ = b;
        return b;
      } else {
        return (com.google.protobuf.ByteString) ref;
      }
    }
    /**
     * <pre>
     * URL of the upstream source repository for this MCP server.
     * Shown in the marketplace so users can inspect the implementation
     * for trust and transparency.
     * Example: "https://github.com/modelcontextprotocol/servers"
     * </pre>
     *
     * <code>string repository_url = 12 [json_name = "repositoryUrl"];</code>
     * @param value The repositoryUrl to set.
     * @return This builder for chaining.
     */
    public Builder setRepositoryUrl(
        java.lang.String value) {
      if (value == null) { throw new NullPointerException(); }
      repositoryUrl_ = value;
      bitField0_ |= 0x00000100;
      onChanged();
      return this;
    }
    /**
     * <pre>
     * URL of the upstream source repository for this MCP server.
     * Shown in the marketplace so users can inspect the implementation
     * for trust and transparency.
     * Example: "https://github.com/modelcontextprotocol/servers"
     * </pre>
     *
     * <code>string repository_url = 12 [json_name = "repositoryUrl"];</code>
     * @return This builder for chaining.
     */
    public Builder clearRepositoryUrl() {
      repositoryUrl_ = getDefaultInstance().getRepositoryUrl();
      bitField0_ = (bitField0_ & ~0x00000100);
      onChanged();
      return this;
    }
    /**
     * <pre>
     * URL of the upstream source repository for this MCP server.
     * Shown in the marketplace so users can inspect the implementation
     * for trust and transparency.
     * Example: "https://github.com/modelcontextprotocol/servers"
     * </pre>
     *
     * <code>string repository_url = 12 [json_name = "repositoryUrl"];</code>
     * @param value The bytes for repositoryUrl to set.
     * @return This builder for chaining.
     */
    public Builder setRepositoryUrlBytes(
        com.google.protobuf.ByteString value) {
      if (value == null) { throw new NullPointerException(); }
      checkByteStringIsUtf8(value);
      repositoryUrl_ = value;
      bitField0_ |= 0x00000100;
      onChanged();
      return this;
    }

    private int githubStars_ ;
    /**
     * <pre>
     * GitHub star count at the time of curation.
     * Used as a popularity signal in marketplace display.
     * 0 if unknown or non-GitHub repository.
     * </pre>
     *
     * <code>int32 github_stars = 13 [json_name = "githubStars"];</code>
     * @return The githubStars.
     */
    @java.lang.Override
    public int getGithubStars() {
      return githubStars_;
    }
    /**
     * <pre>
     * GitHub star count at the time of curation.
     * Used as a popularity signal in marketplace display.
     * 0 if unknown or non-GitHub repository.
     * </pre>
     *
     * <code>int32 github_stars = 13 [json_name = "githubStars"];</code>
     * @param value The githubStars to set.
     * @return This builder for chaining.
     */
    public Builder setGithubStars(int value) {

      githubStars_ = value;
      bitField0_ |= 0x00000200;
      onChanged();
      return this;
    }
    /**
     * <pre>
     * GitHub star count at the time of curation.
     * Used as a popularity signal in marketplace display.
     * 0 if unknown or non-GitHub repository.
     * </pre>
     *
     * <code>int32 github_stars = 13 [json_name = "githubStars"];</code>
     * @return This builder for chaining.
     */
    public Builder clearGithubStars() {
      bitField0_ = (bitField0_ & ~0x00000200);
      githubStars_ = 0;
      onChanged();
      return this;
    }

    private ai.stigmer.agentic.mcpserver.v1.McpServerAuth auth_;
    private com.google.protobuf.SingleFieldBuilder<
        ai.stigmer.agentic.mcpserver.v1.McpServerAuth, ai.stigmer.agentic.mcpserver.v1.McpServerAuth.Builder, ai.stigmer.agentic.mcpserver.v1.McpServerAuthOrBuilder> authBuilder_;
    /**
     * <pre>
     * OAuth authentication configuration for automated credential acquisition.
     * When set, the MCP server's Connect page offers an OAuth flow instead of
     * (or in addition to) manual credential entry.
     *
     * The acquired access token is stored in a system-managed environment
     * (identified by grant.environment_id) as the env var named by
     * auth.target_env_var. That env var must also be declared in env so the
     * execution pipeline knows about it.
     * </pre>
     *
     * <code>.ai.stigmer.agentic.mcpserver.v1.McpServerAuth auth = 14 [json_name = "auth"];</code>
     * @return Whether the auth field is set.
     */
    public boolean hasAuth() {
      return ((bitField0_ & 0x00000400) != 0);
    }
    /**
     * <pre>
     * OAuth authentication configuration for automated credential acquisition.
     * When set, the MCP server's Connect page offers an OAuth flow instead of
     * (or in addition to) manual credential entry.
     *
     * The acquired access token is stored in a system-managed environment
     * (identified by grant.environment_id) as the env var named by
     * auth.target_env_var. That env var must also be declared in env so the
     * execution pipeline knows about it.
     * </pre>
     *
     * <code>.ai.stigmer.agentic.mcpserver.v1.McpServerAuth auth = 14 [json_name = "auth"];</code>
     * @return The auth.
     */
    public ai.stigmer.agentic.mcpserver.v1.McpServerAuth getAuth() {
      if (authBuilder_ == null) {
        return auth_ == null ? ai.stigmer.agentic.mcpserver.v1.McpServerAuth.getDefaultInstance() : auth_;
      } else {
        return authBuilder_.getMessage();
      }
    }
    /**
     * <pre>
     * OAuth authentication configuration for automated credential acquisition.
     * When set, the MCP server's Connect page offers an OAuth flow instead of
     * (or in addition to) manual credential entry.
     *
     * The acquired access token is stored in a system-managed environment
     * (identified by grant.environment_id) as the env var named by
     * auth.target_env_var. That env var must also be declared in env so the
     * execution pipeline knows about it.
     * </pre>
     *
     * <code>.ai.stigmer.agentic.mcpserver.v1.McpServerAuth auth = 14 [json_name = "auth"];</code>
     */
    public Builder setAuth(ai.stigmer.agentic.mcpserver.v1.McpServerAuth value) {
      if (authBuilder_ == null) {
        if (value == null) {
          throw new NullPointerException();
        }
        auth_ = value;
      } else {
        authBuilder_.setMessage(value);
      }
      bitField0_ |= 0x00000400;
      onChanged();
      return this;
    }
    /**
     * <pre>
     * OAuth authentication configuration for automated credential acquisition.
     * When set, the MCP server's Connect page offers an OAuth flow instead of
     * (or in addition to) manual credential entry.
     *
     * The acquired access token is stored in a system-managed environment
     * (identified by grant.environment_id) as the env var named by
     * auth.target_env_var. That env var must also be declared in env so the
     * execution pipeline knows about it.
     * </pre>
     *
     * <code>.ai.stigmer.agentic.mcpserver.v1.McpServerAuth auth = 14 [json_name = "auth"];</code>
     */
    public Builder setAuth(
        ai.stigmer.agentic.mcpserver.v1.McpServerAuth.Builder builderForValue) {
      if (authBuilder_ == null) {
        auth_ = builderForValue.build();
      } else {
        authBuilder_.setMessage(builderForValue.build());
      }
      bitField0_ |= 0x00000400;
      onChanged();
      return this;
    }
    /**
     * <pre>
     * OAuth authentication configuration for automated credential acquisition.
     * When set, the MCP server's Connect page offers an OAuth flow instead of
     * (or in addition to) manual credential entry.
     *
     * The acquired access token is stored in a system-managed environment
     * (identified by grant.environment_id) as the env var named by
     * auth.target_env_var. That env var must also be declared in env so the
     * execution pipeline knows about it.
     * </pre>
     *
     * <code>.ai.stigmer.agentic.mcpserver.v1.McpServerAuth auth = 14 [json_name = "auth"];</code>
     */
    public Builder mergeAuth(ai.stigmer.agentic.mcpserver.v1.McpServerAuth value) {
      if (authBuilder_ == null) {
        if (((bitField0_ & 0x00000400) != 0) &&
          auth_ != null &&
          auth_ != ai.stigmer.agentic.mcpserver.v1.McpServerAuth.getDefaultInstance()) {
          getAuthBuilder().mergeFrom(value);
        } else {
          auth_ = value;
        }
      } else {
        authBuilder_.mergeFrom(value);
      }
      if (auth_ != null) {
        bitField0_ |= 0x00000400;
        onChanged();
      }
      return this;
    }
    /**
     * <pre>
     * OAuth authentication configuration for automated credential acquisition.
     * When set, the MCP server's Connect page offers an OAuth flow instead of
     * (or in addition to) manual credential entry.
     *
     * The acquired access token is stored in a system-managed environment
     * (identified by grant.environment_id) as the env var named by
     * auth.target_env_var. That env var must also be declared in env so the
     * execution pipeline knows about it.
     * </pre>
     *
     * <code>.ai.stigmer.agentic.mcpserver.v1.McpServerAuth auth = 14 [json_name = "auth"];</code>
     */
    public Builder clearAuth() {
      bitField0_ = (bitField0_ & ~0x00000400);
      auth_ = null;
      if (authBuilder_ != null) {
        authBuilder_.dispose();
        authBuilder_ = null;
      }
      onChanged();
      return this;
    }
    /**
     * <pre>
     * OAuth authentication configuration for automated credential acquisition.
     * When set, the MCP server's Connect page offers an OAuth flow instead of
     * (or in addition to) manual credential entry.
     *
     * The acquired access token is stored in a system-managed environment
     * (identified by grant.environment_id) as the env var named by
     * auth.target_env_var. That env var must also be declared in env so the
     * execution pipeline knows about it.
     * </pre>
     *
     * <code>.ai.stigmer.agentic.mcpserver.v1.McpServerAuth auth = 14 [json_name = "auth"];</code>
     */
    public ai.stigmer.agentic.mcpserver.v1.McpServerAuth.Builder getAuthBuilder() {
      bitField0_ |= 0x00000400;
      onChanged();
      return internalGetAuthFieldBuilder().getBuilder();
    }
    /**
     * <pre>
     * OAuth authentication configuration for automated credential acquisition.
     * When set, the MCP server's Connect page offers an OAuth flow instead of
     * (or in addition to) manual credential entry.
     *
     * The acquired access token is stored in a system-managed environment
     * (identified by grant.environment_id) as the env var named by
     * auth.target_env_var. That env var must also be declared in env so the
     * execution pipeline knows about it.
     * </pre>
     *
     * <code>.ai.stigmer.agentic.mcpserver.v1.McpServerAuth auth = 14 [json_name = "auth"];</code>
     */
    public ai.stigmer.agentic.mcpserver.v1.McpServerAuthOrBuilder getAuthOrBuilder() {
      if (authBuilder_ != null) {
        return authBuilder_.getMessageOrBuilder();
      } else {
        return auth_ == null ?
            ai.stigmer.agentic.mcpserver.v1.McpServerAuth.getDefaultInstance() : auth_;
      }
    }
    /**
     * <pre>
     * OAuth authentication configuration for automated credential acquisition.
     * When set, the MCP server's Connect page offers an OAuth flow instead of
     * (or in addition to) manual credential entry.
     *
     * The acquired access token is stored in a system-managed environment
     * (identified by grant.environment_id) as the env var named by
     * auth.target_env_var. That env var must also be declared in env so the
     * execution pipeline knows about it.
     * </pre>
     *
     * <code>.ai.stigmer.agentic.mcpserver.v1.McpServerAuth auth = 14 [json_name = "auth"];</code>
     */
    private com.google.protobuf.SingleFieldBuilder<
        ai.stigmer.agentic.mcpserver.v1.McpServerAuth, ai.stigmer.agentic.mcpserver.v1.McpServerAuth.Builder, ai.stigmer.agentic.mcpserver.v1.McpServerAuthOrBuilder> 
        internalGetAuthFieldBuilder() {
      if (authBuilder_ == null) {
        authBuilder_ = new com.google.protobuf.SingleFieldBuilder<
            ai.stigmer.agentic.mcpserver.v1.McpServerAuth, ai.stigmer.agentic.mcpserver.v1.McpServerAuth.Builder, ai.stigmer.agentic.mcpserver.v1.McpServerAuthOrBuilder>(
                getAuth(),
                getParentForChildren(),
                isClean());
        auth_ = null;
      }
      return authBuilder_;
    }

    // @@protoc_insertion_point(builder_scope:ai.stigmer.agentic.mcpserver.v1.McpServerSpec)
  }

  // @@protoc_insertion_point(class_scope:ai.stigmer.agentic.mcpserver.v1.McpServerSpec)
  private static final ai.stigmer.agentic.mcpserver.v1.McpServerSpec DEFAULT_INSTANCE;
  static {
    DEFAULT_INSTANCE = new ai.stigmer.agentic.mcpserver.v1.McpServerSpec();
  }

  public static ai.stigmer.agentic.mcpserver.v1.McpServerSpec getDefaultInstance() {
    return DEFAULT_INSTANCE;
  }

  private static final com.google.protobuf.Parser<McpServerSpec>
      PARSER = new com.google.protobuf.AbstractParser<McpServerSpec>() {
    @java.lang.Override
    public McpServerSpec parsePartialFrom(
        com.google.protobuf.CodedInputStream input,
        com.google.protobuf.ExtensionRegistryLite extensionRegistry)
        throws com.google.protobuf.InvalidProtocolBufferException {
      Builder builder = newBuilder();
      try {
        builder.mergeFrom(input, extensionRegistry);
      } catch (com.google.protobuf.InvalidProtocolBufferException e) {
        throw e.setUnfinishedMessage(builder.buildPartial());
      } catch (com.google.protobuf.UninitializedMessageException e) {
        throw e.asInvalidProtocolBufferException().setUnfinishedMessage(builder.buildPartial());
      } catch (java.io.IOException e) {
        throw new com.google.protobuf.InvalidProtocolBufferException(e)
            .setUnfinishedMessage(builder.buildPartial());
      }
      return builder.buildPartial();
    }
  };

  public static com.google.protobuf.Parser<McpServerSpec> parser() {
    return PARSER;
  }

  @java.lang.Override
  public com.google.protobuf.Parser<McpServerSpec> getParserForType() {
    return PARSER;
  }

  @java.lang.Override
  public ai.stigmer.agentic.mcpserver.v1.McpServerSpec getDefaultInstanceForType() {
    return DEFAULT_INSTANCE;
  }

}

