// Generated by the protocol buffer compiler.  DO NOT EDIT!
// NO CHECKED-IN PROTOBUF GENCODE
// source: ai/stigmer/agentic/workflowexecution/v1/api.proto
// Protobuf Java Version: 4.34.0

package ai.stigmer.agentic.workflowexecution.v1;

/**
 * <pre>
 * WorkflowExecutionStatus contains all system-managed execution state and results.
 *
 * &#64;internal
 * Everything populated during or after execution goes here, not in spec.
 * This message follows the standard pattern: audit information at field 99, custom fields at 1-98.
 *
 * Status Lifecycle:
 * 1. Created: audit.created_at set, phase = EXECUTION_PENDING
 * 2. Started: started_at set, phase = EXECUTION_IN_PROGRESS, tasks populated
 * 3. Progress: tasks updated as workflow runner sends status updates
 * 4. Completed: completed_at set, phase = EXECUTION_COMPLETED, output populated
 * 5. Failed: completed_at set, phase = EXECUTION_FAILED, error populated
 * 6. Cancelled: completed_at set, phase = EXECUTION_CANCELLED
 *
 * The status is read-only to users and continuously updated by the workflow execution engine.
 *
 * Progress Tracking Pattern (Matches Agent Execution):
 * - Agent execution: messages[], tool_calls[], sub_agent_executions[] (transformed from LLM streaming events)
 * - Workflow execution: tasks[] (transformed from workflow task execution)
 * - Both store structured state, not raw streaming events
 * - Both use updateStatus RPC for progressive updates during execution
 * </pre>
 *
 * Protobuf type {@code ai.stigmer.agentic.workflowexecution.v1.WorkflowExecutionStatus}
 */
@com.google.protobuf.Generated
public final class WorkflowExecutionStatus extends
    com.google.protobuf.GeneratedMessage implements
    // @@protoc_insertion_point(message_implements:ai.stigmer.agentic.workflowexecution.v1.WorkflowExecutionStatus)
    WorkflowExecutionStatusOrBuilder {
private static final long serialVersionUID = 0L;
  static {
    com.google.protobuf.RuntimeVersion.validateProtobufGencodeVersion(
      com.google.protobuf.RuntimeVersion.RuntimeDomain.PUBLIC,
      /* major= */ 4,
      /* minor= */ 34,
      /* patch= */ 0,
      /* suffix= */ "",
      "WorkflowExecutionStatus");
  }
  // Use WorkflowExecutionStatus.newBuilder() to construct.
  private WorkflowExecutionStatus(com.google.protobuf.GeneratedMessage.Builder<?> builder) {
    super(builder);
  }
  private WorkflowExecutionStatus() {
    phase_ = 0;
    tasks_ = java.util.Collections.emptyList();
    error_ = "";
    startedAt_ = "";
    completedAt_ = "";
    temporalWorkflowId_ = "";
    pendingApprovals_ = java.util.Collections.emptyList();
    workflowVersionHash_ = "";
    pendingFileReviews_ = java.util.Collections.emptyList();
  }

  public static final com.google.protobuf.Descriptors.Descriptor
      getDescriptor() {
    return ai.stigmer.agentic.workflowexecution.v1.ApiProto.internal_static_ai_stigmer_agentic_workflowexecution_v1_WorkflowExecutionStatus_descriptor;
  }

  @java.lang.Override
  public com.google.protobuf.Descriptors.Descriptor getDescriptorForType() {
    return ai.stigmer.agentic.workflowexecution.v1.ApiProto.internal_static_ai_stigmer_agentic_workflowexecution_v1_WorkflowExecutionStatus_descriptor;
  }

  @java.lang.Override
  protected com.google.protobuf.GeneratedMessage.FieldAccessorTable
      internalGetFieldAccessorTable() {
    return ai.stigmer.agentic.workflowexecution.v1.ApiProto.internal_static_ai_stigmer_agentic_workflowexecution_v1_WorkflowExecutionStatus_fieldAccessorTable
        .ensureFieldAccessorsInitialized(
            ai.stigmer.agentic.workflowexecution.v1.WorkflowExecutionStatus.class, ai.stigmer.agentic.workflowexecution.v1.WorkflowExecutionStatus.Builder.class);
  }

  private int bitField0_;
  public static final int AUDIT_FIELD_NUMBER = 99;
  private ai.stigmer.commons.apiresource.ApiResourceAudit audit_;
  /**
   * <pre>
   * Standard audit information including timestamps and created-by identity.
   *
   * &#64;internal
   * Always at field 99 for consistency across all Stigmer API resources.
   *
   * Contains:
   * - created_at: When this execution was created (ISO 8601 timestamp)
   * - updated_at: Last time status was updated (ISO 8601 timestamp)
   * - created_by: User or system that created this execution
   * </pre>
   *
   * <code>.ai.stigmer.commons.apiresource.ApiResourceAudit audit = 99 [json_name = "audit"];</code>
   * @return Whether the audit field is set.
   */
  @java.lang.Override
  public boolean hasAudit() {
    return ((bitField0_ & 0x00000001) != 0);
  }
  /**
   * <pre>
   * Standard audit information including timestamps and created-by identity.
   *
   * &#64;internal
   * Always at field 99 for consistency across all Stigmer API resources.
   *
   * Contains:
   * - created_at: When this execution was created (ISO 8601 timestamp)
   * - updated_at: Last time status was updated (ISO 8601 timestamp)
   * - created_by: User or system that created this execution
   * </pre>
   *
   * <code>.ai.stigmer.commons.apiresource.ApiResourceAudit audit = 99 [json_name = "audit"];</code>
   * @return The audit.
   */
  @java.lang.Override
  public ai.stigmer.commons.apiresource.ApiResourceAudit getAudit() {
    return audit_ == null ? ai.stigmer.commons.apiresource.ApiResourceAudit.getDefaultInstance() : audit_;
  }
  /**
   * <pre>
   * Standard audit information including timestamps and created-by identity.
   *
   * &#64;internal
   * Always at field 99 for consistency across all Stigmer API resources.
   *
   * Contains:
   * - created_at: When this execution was created (ISO 8601 timestamp)
   * - updated_at: Last time status was updated (ISO 8601 timestamp)
   * - created_by: User or system that created this execution
   * </pre>
   *
   * <code>.ai.stigmer.commons.apiresource.ApiResourceAudit audit = 99 [json_name = "audit"];</code>
   */
  @java.lang.Override
  public ai.stigmer.commons.apiresource.ApiResourceAuditOrBuilder getAuditOrBuilder() {
    return audit_ == null ? ai.stigmer.commons.apiresource.ApiResourceAudit.getDefaultInstance() : audit_;
  }

  public static final int PHASE_FIELD_NUMBER = 1;
  private int phase_ = 0;
  /**
   * <pre>
   * Current execution lifecycle phase.
   *
   * &#64;internal
   * Phase Transitions:
   * PENDING → IN_PROGRESS → COMPLETED
   * ↓              ↘ FAILED
   * ↓              ↘ CANCELLED
   *
   * The phase is used for:
   * - UI status indicators (progress bars, badges)
   * - Filtering executions (show only failed, show only in-progress)
   * - Alerting and notifications (notify on failure)
   * - Retry logic (retry failed executions)
   *
   * Validation: Must be a defined enum value (no unspecified/unknown).
   * </pre>
   *
   * <code>.ai.stigmer.agentic.workflowexecution.v1.ExecutionPhase phase = 1 [json_name = "phase", (.buf.validate.field) = { ... }</code>
   * @return The enum numeric value on the wire for phase.
   */
  @java.lang.Override public int getPhaseValue() {
    return phase_;
  }
  /**
   * <pre>
   * Current execution lifecycle phase.
   *
   * &#64;internal
   * Phase Transitions:
   * PENDING → IN_PROGRESS → COMPLETED
   * ↓              ↘ FAILED
   * ↓              ↘ CANCELLED
   *
   * The phase is used for:
   * - UI status indicators (progress bars, badges)
   * - Filtering executions (show only failed, show only in-progress)
   * - Alerting and notifications (notify on failure)
   * - Retry logic (retry failed executions)
   *
   * Validation: Must be a defined enum value (no unspecified/unknown).
   * </pre>
   *
   * <code>.ai.stigmer.agentic.workflowexecution.v1.ExecutionPhase phase = 1 [json_name = "phase", (.buf.validate.field) = { ... }</code>
   * @return The phase.
   */
  @java.lang.Override public ai.stigmer.agentic.workflowexecution.v1.ExecutionPhase getPhase() {
    ai.stigmer.agentic.workflowexecution.v1.ExecutionPhase result = ai.stigmer.agentic.workflowexecution.v1.ExecutionPhase.forNumber(phase_);
    return result == null ? ai.stigmer.agentic.workflowexecution.v1.ExecutionPhase.UNRECOGNIZED : result;
  }

  public static final int TASKS_FIELD_NUMBER = 2;
  @SuppressWarnings("serial")
  private java.util.List<ai.stigmer.agentic.workflowexecution.v1.WorkflowTask> tasks_;
  /**
   * <pre>
   * Workflow tasks with their individual execution state.
   *
   * &#64;internal
   * Tasks represent the atomic units of work within the workflow.
   * Each task has:
   * - task_id: Unique identifier within this execution
   * - task_name: Human-readable name (e.g., "validate_email", "send_notification")
   * - task_type: Type of task (agent_invocation, api_call, approval, etc.)
   * - status: Current task status (PENDING, IN_PROGRESS, COMPLETED, FAILED, SKIPPED)
   * - input: Task input parameters (JSON structure)
   * - output: Task output results (JSON structure, only for COMPLETED tasks)
   * - error: Error message (only for FAILED tasks)
   * - started_at: When task started executing
   * - completed_at: When task finished (COMPLETED/FAILED/SKIPPED)
   *
   * Tasks are populated as the workflow progresses through its task graph.
   * The order in this list reflects the execution order (sequential or parallel).
   *
   * Progress Calculation:
   * - Total tasks: tasks.length
   * - Completed tasks: count(tasks where status in [COMPLETED, FAILED, SKIPPED])
   * - Progress percentage: (completed_tasks / total_tasks) * 100
   * - Current task: tasks.find(status == IN_PROGRESS)
   * </pre>
   *
   * <code>repeated .ai.stigmer.agentic.workflowexecution.v1.WorkflowTask tasks = 2 [json_name = "tasks"];</code>
   */
  @java.lang.Override
  public java.util.List<ai.stigmer.agentic.workflowexecution.v1.WorkflowTask> getTasksList() {
    return tasks_;
  }
  /**
   * <pre>
   * Workflow tasks with their individual execution state.
   *
   * &#64;internal
   * Tasks represent the atomic units of work within the workflow.
   * Each task has:
   * - task_id: Unique identifier within this execution
   * - task_name: Human-readable name (e.g., "validate_email", "send_notification")
   * - task_type: Type of task (agent_invocation, api_call, approval, etc.)
   * - status: Current task status (PENDING, IN_PROGRESS, COMPLETED, FAILED, SKIPPED)
   * - input: Task input parameters (JSON structure)
   * - output: Task output results (JSON structure, only for COMPLETED tasks)
   * - error: Error message (only for FAILED tasks)
   * - started_at: When task started executing
   * - completed_at: When task finished (COMPLETED/FAILED/SKIPPED)
   *
   * Tasks are populated as the workflow progresses through its task graph.
   * The order in this list reflects the execution order (sequential or parallel).
   *
   * Progress Calculation:
   * - Total tasks: tasks.length
   * - Completed tasks: count(tasks where status in [COMPLETED, FAILED, SKIPPED])
   * - Progress percentage: (completed_tasks / total_tasks) * 100
   * - Current task: tasks.find(status == IN_PROGRESS)
   * </pre>
   *
   * <code>repeated .ai.stigmer.agentic.workflowexecution.v1.WorkflowTask tasks = 2 [json_name = "tasks"];</code>
   */
  @java.lang.Override
  public java.util.List<? extends ai.stigmer.agentic.workflowexecution.v1.WorkflowTaskOrBuilder> 
      getTasksOrBuilderList() {
    return tasks_;
  }
  /**
   * <pre>
   * Workflow tasks with their individual execution state.
   *
   * &#64;internal
   * Tasks represent the atomic units of work within the workflow.
   * Each task has:
   * - task_id: Unique identifier within this execution
   * - task_name: Human-readable name (e.g., "validate_email", "send_notification")
   * - task_type: Type of task (agent_invocation, api_call, approval, etc.)
   * - status: Current task status (PENDING, IN_PROGRESS, COMPLETED, FAILED, SKIPPED)
   * - input: Task input parameters (JSON structure)
   * - output: Task output results (JSON structure, only for COMPLETED tasks)
   * - error: Error message (only for FAILED tasks)
   * - started_at: When task started executing
   * - completed_at: When task finished (COMPLETED/FAILED/SKIPPED)
   *
   * Tasks are populated as the workflow progresses through its task graph.
   * The order in this list reflects the execution order (sequential or parallel).
   *
   * Progress Calculation:
   * - Total tasks: tasks.length
   * - Completed tasks: count(tasks where status in [COMPLETED, FAILED, SKIPPED])
   * - Progress percentage: (completed_tasks / total_tasks) * 100
   * - Current task: tasks.find(status == IN_PROGRESS)
   * </pre>
   *
   * <code>repeated .ai.stigmer.agentic.workflowexecution.v1.WorkflowTask tasks = 2 [json_name = "tasks"];</code>
   */
  @java.lang.Override
  public int getTasksCount() {
    return tasks_.size();
  }
  /**
   * <pre>
   * Workflow tasks with their individual execution state.
   *
   * &#64;internal
   * Tasks represent the atomic units of work within the workflow.
   * Each task has:
   * - task_id: Unique identifier within this execution
   * - task_name: Human-readable name (e.g., "validate_email", "send_notification")
   * - task_type: Type of task (agent_invocation, api_call, approval, etc.)
   * - status: Current task status (PENDING, IN_PROGRESS, COMPLETED, FAILED, SKIPPED)
   * - input: Task input parameters (JSON structure)
   * - output: Task output results (JSON structure, only for COMPLETED tasks)
   * - error: Error message (only for FAILED tasks)
   * - started_at: When task started executing
   * - completed_at: When task finished (COMPLETED/FAILED/SKIPPED)
   *
   * Tasks are populated as the workflow progresses through its task graph.
   * The order in this list reflects the execution order (sequential or parallel).
   *
   * Progress Calculation:
   * - Total tasks: tasks.length
   * - Completed tasks: count(tasks where status in [COMPLETED, FAILED, SKIPPED])
   * - Progress percentage: (completed_tasks / total_tasks) * 100
   * - Current task: tasks.find(status == IN_PROGRESS)
   * </pre>
   *
   * <code>repeated .ai.stigmer.agentic.workflowexecution.v1.WorkflowTask tasks = 2 [json_name = "tasks"];</code>
   */
  @java.lang.Override
  public ai.stigmer.agentic.workflowexecution.v1.WorkflowTask getTasks(int index) {
    return tasks_.get(index);
  }
  /**
   * <pre>
   * Workflow tasks with their individual execution state.
   *
   * &#64;internal
   * Tasks represent the atomic units of work within the workflow.
   * Each task has:
   * - task_id: Unique identifier within this execution
   * - task_name: Human-readable name (e.g., "validate_email", "send_notification")
   * - task_type: Type of task (agent_invocation, api_call, approval, etc.)
   * - status: Current task status (PENDING, IN_PROGRESS, COMPLETED, FAILED, SKIPPED)
   * - input: Task input parameters (JSON structure)
   * - output: Task output results (JSON structure, only for COMPLETED tasks)
   * - error: Error message (only for FAILED tasks)
   * - started_at: When task started executing
   * - completed_at: When task finished (COMPLETED/FAILED/SKIPPED)
   *
   * Tasks are populated as the workflow progresses through its task graph.
   * The order in this list reflects the execution order (sequential or parallel).
   *
   * Progress Calculation:
   * - Total tasks: tasks.length
   * - Completed tasks: count(tasks where status in [COMPLETED, FAILED, SKIPPED])
   * - Progress percentage: (completed_tasks / total_tasks) * 100
   * - Current task: tasks.find(status == IN_PROGRESS)
   * </pre>
   *
   * <code>repeated .ai.stigmer.agentic.workflowexecution.v1.WorkflowTask tasks = 2 [json_name = "tasks"];</code>
   */
  @java.lang.Override
  public ai.stigmer.agentic.workflowexecution.v1.WorkflowTaskOrBuilder getTasksOrBuilder(
      int index) {
    return tasks_.get(index);
  }

  public static final int OUTPUT_FIELD_NUMBER = 3;
  private com.google.protobuf.Struct output_;
  /**
   * <pre>
   * Final workflow output, populated only when phase is EXECUTION_COMPLETED.
   *
   * &#64;internal
   * The output structure is workflow-specific and defined by the Workflow template.
   * Common patterns:
   * - API response data (e.g., created user ID, order confirmation)
   * - Aggregated results from multiple tasks
   * - Links to generated artifacts (reports, files)
   * - Summary statistics (items processed, duration)
   * </pre>
   *
   * <code>.google.protobuf.Struct output = 3 [json_name = "output"];</code>
   * @return Whether the output field is set.
   */
  @java.lang.Override
  public boolean hasOutput() {
    return ((bitField0_ & 0x00000002) != 0);
  }
  /**
   * <pre>
   * Final workflow output, populated only when phase is EXECUTION_COMPLETED.
   *
   * &#64;internal
   * The output structure is workflow-specific and defined by the Workflow template.
   * Common patterns:
   * - API response data (e.g., created user ID, order confirmation)
   * - Aggregated results from multiple tasks
   * - Links to generated artifacts (reports, files)
   * - Summary statistics (items processed, duration)
   * </pre>
   *
   * <code>.google.protobuf.Struct output = 3 [json_name = "output"];</code>
   * @return The output.
   */
  @java.lang.Override
  public com.google.protobuf.Struct getOutput() {
    return output_ == null ? com.google.protobuf.Struct.getDefaultInstance() : output_;
  }
  /**
   * <pre>
   * Final workflow output, populated only when phase is EXECUTION_COMPLETED.
   *
   * &#64;internal
   * The output structure is workflow-specific and defined by the Workflow template.
   * Common patterns:
   * - API response data (e.g., created user ID, order confirmation)
   * - Aggregated results from multiple tasks
   * - Links to generated artifacts (reports, files)
   * - Summary statistics (items processed, duration)
   * </pre>
   *
   * <code>.google.protobuf.Struct output = 3 [json_name = "output"];</code>
   */
  @java.lang.Override
  public com.google.protobuf.StructOrBuilder getOutputOrBuilder() {
    return output_ == null ? com.google.protobuf.Struct.getDefaultInstance() : output_;
  }

  public static final int ERROR_FIELD_NUMBER = 4;
  @SuppressWarnings("serial")
  private volatile java.lang.Object error_ = "";
  /**
   * <pre>
   * Error message, populated only when phase is EXECUTION_FAILED.
   *
   * &#64;internal
   * Contains a human-readable description of what went wrong.
   *
   * Error message includes:
   * - What failed (which task or workflow step)
   * - Why it failed (validation error, API error, timeout, etc.)
   * - How to fix it (if known)
   *
   * For detailed debugging, inspect tasks[].error for task-specific error messages.
   * </pre>
   *
   * <code>string error = 4 [json_name = "error"];</code>
   * @return The error.
   */
  @java.lang.Override
  public java.lang.String getError() {
    java.lang.Object ref = error_;
    if (ref instanceof java.lang.String) {
      return (java.lang.String) ref;
    } else {
      com.google.protobuf.ByteString bs = 
          (com.google.protobuf.ByteString) ref;
      java.lang.String s = bs.toStringUtf8();
      error_ = s;
      return s;
    }
  }
  /**
   * <pre>
   * Error message, populated only when phase is EXECUTION_FAILED.
   *
   * &#64;internal
   * Contains a human-readable description of what went wrong.
   *
   * Error message includes:
   * - What failed (which task or workflow step)
   * - Why it failed (validation error, API error, timeout, etc.)
   * - How to fix it (if known)
   *
   * For detailed debugging, inspect tasks[].error for task-specific error messages.
   * </pre>
   *
   * <code>string error = 4 [json_name = "error"];</code>
   * @return The bytes for error.
   */
  @java.lang.Override
  public com.google.protobuf.ByteString
      getErrorBytes() {
    java.lang.Object ref = error_;
    if (ref instanceof java.lang.String) {
      com.google.protobuf.ByteString b = 
          com.google.protobuf.ByteString.copyFromUtf8(
              (java.lang.String) ref);
      error_ = b;
      return b;
    } else {
      return (com.google.protobuf.ByteString) ref;
    }
  }

  public static final int STARTED_AT_FIELD_NUMBER = 5;
  @SuppressWarnings("serial")
  private volatile java.lang.Object startedAt_ = "";
  /**
   * <pre>
   * ISO 8601 timestamp when execution started processing.
   *
   * &#64;internal
   * Set when phase transitions from PENDING to IN_PROGRESS.
   * Format: "YYYY-MM-DDTHH:MM:SSZ" (UTC timezone)
   * </pre>
   *
   * <code>string started_at = 5 [json_name = "startedAt"];</code>
   * @return The startedAt.
   */
  @java.lang.Override
  public java.lang.String getStartedAt() {
    java.lang.Object ref = startedAt_;
    if (ref instanceof java.lang.String) {
      return (java.lang.String) ref;
    } else {
      com.google.protobuf.ByteString bs = 
          (com.google.protobuf.ByteString) ref;
      java.lang.String s = bs.toStringUtf8();
      startedAt_ = s;
      return s;
    }
  }
  /**
   * <pre>
   * ISO 8601 timestamp when execution started processing.
   *
   * &#64;internal
   * Set when phase transitions from PENDING to IN_PROGRESS.
   * Format: "YYYY-MM-DDTHH:MM:SSZ" (UTC timezone)
   * </pre>
   *
   * <code>string started_at = 5 [json_name = "startedAt"];</code>
   * @return The bytes for startedAt.
   */
  @java.lang.Override
  public com.google.protobuf.ByteString
      getStartedAtBytes() {
    java.lang.Object ref = startedAt_;
    if (ref instanceof java.lang.String) {
      com.google.protobuf.ByteString b = 
          com.google.protobuf.ByteString.copyFromUtf8(
              (java.lang.String) ref);
      startedAt_ = b;
      return b;
    } else {
      return (com.google.protobuf.ByteString) ref;
    }
  }

  public static final int COMPLETED_AT_FIELD_NUMBER = 6;
  @SuppressWarnings("serial")
  private volatile java.lang.Object completedAt_ = "";
  /**
   * <pre>
   * ISO 8601 timestamp when execution reached a terminal state.
   *
   * &#64;internal
   * Set when the workflow reaches a terminal state:
   * - EXECUTION_COMPLETED: Successfully finished
   * - EXECUTION_FAILED: Failed during execution
   * - EXECUTION_CANCELLED: Cancelled by user or system
   *
   * Not set for PENDING or IN_PROGRESS executions.
   * Format: "YYYY-MM-DDTHH:MM:SSZ" (UTC timezone)
   * </pre>
   *
   * <code>string completed_at = 6 [json_name = "completedAt"];</code>
   * @return The completedAt.
   */
  @java.lang.Override
  public java.lang.String getCompletedAt() {
    java.lang.Object ref = completedAt_;
    if (ref instanceof java.lang.String) {
      return (java.lang.String) ref;
    } else {
      com.google.protobuf.ByteString bs = 
          (com.google.protobuf.ByteString) ref;
      java.lang.String s = bs.toStringUtf8();
      completedAt_ = s;
      return s;
    }
  }
  /**
   * <pre>
   * ISO 8601 timestamp when execution reached a terminal state.
   *
   * &#64;internal
   * Set when the workflow reaches a terminal state:
   * - EXECUTION_COMPLETED: Successfully finished
   * - EXECUTION_FAILED: Failed during execution
   * - EXECUTION_CANCELLED: Cancelled by user or system
   *
   * Not set for PENDING or IN_PROGRESS executions.
   * Format: "YYYY-MM-DDTHH:MM:SSZ" (UTC timezone)
   * </pre>
   *
   * <code>string completed_at = 6 [json_name = "completedAt"];</code>
   * @return The bytes for completedAt.
   */
  @java.lang.Override
  public com.google.protobuf.ByteString
      getCompletedAtBytes() {
    java.lang.Object ref = completedAt_;
    if (ref instanceof java.lang.String) {
      com.google.protobuf.ByteString b = 
          com.google.protobuf.ByteString.copyFromUtf8(
              (java.lang.String) ref);
      completedAt_ = b;
      return b;
    } else {
      return (com.google.protobuf.ByteString) ref;
    }
  }

  public static final int TEMPORAL_WORKFLOW_ID_FIELD_NUMBER = 7;
  @SuppressWarnings("serial")
  private volatile java.lang.Object temporalWorkflowId_ = "";
  /**
   * <pre>
   * Correlation ID for the underlying workflow engine.
   *
   * &#64;internal
   * This is the workflow ID in Temporal, used for:
   * - Correlation between Stigmer and Temporal (for debugging)
   * - Querying Temporal directly (for advanced troubleshooting)
   * - Signaling or cancelling Temporal workflows
   *
   * Format: Typically "{workflow_instance_id}-{execution_id}" or a UUID
   * Example: "wfi_prod-deploy-wfx_abc123xyz456" or "temporal-wf-uuid-12345"
   *
   * This field is optional and only relevant when Temporal is used as the execution engine.
   * Other workflow engines (Step Functions, Argo, etc.) may use different correlation IDs.
   * </pre>
   *
   * <code>string temporal_workflow_id = 7 [json_name = "temporalWorkflowId"];</code>
   * @return The temporalWorkflowId.
   */
  @java.lang.Override
  public java.lang.String getTemporalWorkflowId() {
    java.lang.Object ref = temporalWorkflowId_;
    if (ref instanceof java.lang.String) {
      return (java.lang.String) ref;
    } else {
      com.google.protobuf.ByteString bs = 
          (com.google.protobuf.ByteString) ref;
      java.lang.String s = bs.toStringUtf8();
      temporalWorkflowId_ = s;
      return s;
    }
  }
  /**
   * <pre>
   * Correlation ID for the underlying workflow engine.
   *
   * &#64;internal
   * This is the workflow ID in Temporal, used for:
   * - Correlation between Stigmer and Temporal (for debugging)
   * - Querying Temporal directly (for advanced troubleshooting)
   * - Signaling or cancelling Temporal workflows
   *
   * Format: Typically "{workflow_instance_id}-{execution_id}" or a UUID
   * Example: "wfi_prod-deploy-wfx_abc123xyz456" or "temporal-wf-uuid-12345"
   *
   * This field is optional and only relevant when Temporal is used as the execution engine.
   * Other workflow engines (Step Functions, Argo, etc.) may use different correlation IDs.
   * </pre>
   *
   * <code>string temporal_workflow_id = 7 [json_name = "temporalWorkflowId"];</code>
   * @return The bytes for temporalWorkflowId.
   */
  @java.lang.Override
  public com.google.protobuf.ByteString
      getTemporalWorkflowIdBytes() {
    java.lang.Object ref = temporalWorkflowId_;
    if (ref instanceof java.lang.String) {
      com.google.protobuf.ByteString b = 
          com.google.protobuf.ByteString.copyFromUtf8(
              (java.lang.String) ref);
      temporalWorkflowId_ = b;
      return b;
    } else {
      return (com.google.protobuf.ByteString) ref;
    }
  }

  public static final int PENDING_APPROVALS_FIELD_NUMBER = 9;
  @SuppressWarnings("serial")
  private java.util.List<ai.stigmer.agentic.workflowexecution.v1.WorkflowPendingApproval> pendingApprovals_;
  /**
   * <pre>
   * Pending approvals from child agent tool executions.
   *
   * &#64;internal
   * Populated when workflow tasks invoke agents that enter
   * EXECUTION_WAITING_FOR_APPROVAL phase. This surfaces all approval
   * requests at the workflow level for UI visibility.
   *
   * Guarded Update Protocol:
   * This field is only modified when UpdateStatusInput.update_pending_approvals
   * is explicitly set to true. Normal event emissions (which don't concern
   * approvals) leave this field untouched, preventing race conditions between
   * concurrent status writers.
   *
   * Only call-agent-status manages this field:
   * - Non-empty list: child agent(s) need approval
   * - Empty list + update_pending_approvals=true: all approvals resolved
   *
   * Parallel Agents:
   * When multiple child agents run in parallel, entries from different children
   * accumulate in this list. Each entry's child_agent_execution_id distinguishes
   * the source.
   * </pre>
   *
   * <code>repeated .ai.stigmer.agentic.workflowexecution.v1.WorkflowPendingApproval pending_approvals = 9 [json_name = "pendingApprovals"];</code>
   */
  @java.lang.Override
  public java.util.List<ai.stigmer.agentic.workflowexecution.v1.WorkflowPendingApproval> getPendingApprovalsList() {
    return pendingApprovals_;
  }
  /**
   * <pre>
   * Pending approvals from child agent tool executions.
   *
   * &#64;internal
   * Populated when workflow tasks invoke agents that enter
   * EXECUTION_WAITING_FOR_APPROVAL phase. This surfaces all approval
   * requests at the workflow level for UI visibility.
   *
   * Guarded Update Protocol:
   * This field is only modified when UpdateStatusInput.update_pending_approvals
   * is explicitly set to true. Normal event emissions (which don't concern
   * approvals) leave this field untouched, preventing race conditions between
   * concurrent status writers.
   *
   * Only call-agent-status manages this field:
   * - Non-empty list: child agent(s) need approval
   * - Empty list + update_pending_approvals=true: all approvals resolved
   *
   * Parallel Agents:
   * When multiple child agents run in parallel, entries from different children
   * accumulate in this list. Each entry's child_agent_execution_id distinguishes
   * the source.
   * </pre>
   *
   * <code>repeated .ai.stigmer.agentic.workflowexecution.v1.WorkflowPendingApproval pending_approvals = 9 [json_name = "pendingApprovals"];</code>
   */
  @java.lang.Override
  public java.util.List<? extends ai.stigmer.agentic.workflowexecution.v1.WorkflowPendingApprovalOrBuilder> 
      getPendingApprovalsOrBuilderList() {
    return pendingApprovals_;
  }
  /**
   * <pre>
   * Pending approvals from child agent tool executions.
   *
   * &#64;internal
   * Populated when workflow tasks invoke agents that enter
   * EXECUTION_WAITING_FOR_APPROVAL phase. This surfaces all approval
   * requests at the workflow level for UI visibility.
   *
   * Guarded Update Protocol:
   * This field is only modified when UpdateStatusInput.update_pending_approvals
   * is explicitly set to true. Normal event emissions (which don't concern
   * approvals) leave this field untouched, preventing race conditions between
   * concurrent status writers.
   *
   * Only call-agent-status manages this field:
   * - Non-empty list: child agent(s) need approval
   * - Empty list + update_pending_approvals=true: all approvals resolved
   *
   * Parallel Agents:
   * When multiple child agents run in parallel, entries from different children
   * accumulate in this list. Each entry's child_agent_execution_id distinguishes
   * the source.
   * </pre>
   *
   * <code>repeated .ai.stigmer.agentic.workflowexecution.v1.WorkflowPendingApproval pending_approvals = 9 [json_name = "pendingApprovals"];</code>
   */
  @java.lang.Override
  public int getPendingApprovalsCount() {
    return pendingApprovals_.size();
  }
  /**
   * <pre>
   * Pending approvals from child agent tool executions.
   *
   * &#64;internal
   * Populated when workflow tasks invoke agents that enter
   * EXECUTION_WAITING_FOR_APPROVAL phase. This surfaces all approval
   * requests at the workflow level for UI visibility.
   *
   * Guarded Update Protocol:
   * This field is only modified when UpdateStatusInput.update_pending_approvals
   * is explicitly set to true. Normal event emissions (which don't concern
   * approvals) leave this field untouched, preventing race conditions between
   * concurrent status writers.
   *
   * Only call-agent-status manages this field:
   * - Non-empty list: child agent(s) need approval
   * - Empty list + update_pending_approvals=true: all approvals resolved
   *
   * Parallel Agents:
   * When multiple child agents run in parallel, entries from different children
   * accumulate in this list. Each entry's child_agent_execution_id distinguishes
   * the source.
   * </pre>
   *
   * <code>repeated .ai.stigmer.agentic.workflowexecution.v1.WorkflowPendingApproval pending_approvals = 9 [json_name = "pendingApprovals"];</code>
   */
  @java.lang.Override
  public ai.stigmer.agentic.workflowexecution.v1.WorkflowPendingApproval getPendingApprovals(int index) {
    return pendingApprovals_.get(index);
  }
  /**
   * <pre>
   * Pending approvals from child agent tool executions.
   *
   * &#64;internal
   * Populated when workflow tasks invoke agents that enter
   * EXECUTION_WAITING_FOR_APPROVAL phase. This surfaces all approval
   * requests at the workflow level for UI visibility.
   *
   * Guarded Update Protocol:
   * This field is only modified when UpdateStatusInput.update_pending_approvals
   * is explicitly set to true. Normal event emissions (which don't concern
   * approvals) leave this field untouched, preventing race conditions between
   * concurrent status writers.
   *
   * Only call-agent-status manages this field:
   * - Non-empty list: child agent(s) need approval
   * - Empty list + update_pending_approvals=true: all approvals resolved
   *
   * Parallel Agents:
   * When multiple child agents run in parallel, entries from different children
   * accumulate in this list. Each entry's child_agent_execution_id distinguishes
   * the source.
   * </pre>
   *
   * <code>repeated .ai.stigmer.agentic.workflowexecution.v1.WorkflowPendingApproval pending_approvals = 9 [json_name = "pendingApprovals"];</code>
   */
  @java.lang.Override
  public ai.stigmer.agentic.workflowexecution.v1.WorkflowPendingApprovalOrBuilder getPendingApprovalsOrBuilder(
      int index) {
    return pendingApprovals_.get(index);
  }

  public static final int TOTAL_COST_MICROS_FIELD_NUMBER = 10;
  private long totalCostMicros_ = 0L;
  /**
   * <pre>
   * Cumulative cost across all tasks in micro-USD (1 USD = 1,000,000 micros).
   *
   * &#64;internal
   * Updated by the runner alongside each status update. Reflects the
   * budget tracker's accumulated cost at the time of the last status write.
   * Used by getExecutionSummary for fast aggregation without scanning events.
   *
   * Consistent with WorkflowBudget.max_cost_micros and the billing domain's
   * micro-USD convention (CostStamp.provider_cost_micros, etc.).
   *
   * &#64;since Cost Data Pipeline
   * </pre>
   *
   * <code>int64 total_cost_micros = 10 [json_name = "totalCostMicros"];</code>
   * @return The totalCostMicros.
   */
  @java.lang.Override
  public long getTotalCostMicros() {
    return totalCostMicros_;
  }

  public static final int TOTAL_INPUT_TOKENS_FIELD_NUMBER = 11;
  private long totalInputTokens_ = 0L;
  /**
   * <pre>
   * Cumulative input tokens consumed across all LLM and agent tasks.
   *
   * &#64;internal
   * Input tokens represent prompt/context tokens sent to the model.
   * Updated alongside total_cost_micros from the budget tracker.
   *
   * &#64;since Cost Data Pipeline
   * </pre>
   *
   * <code>int64 total_input_tokens = 11 [json_name = "totalInputTokens"];</code>
   * @return The totalInputTokens.
   */
  @java.lang.Override
  public long getTotalInputTokens() {
    return totalInputTokens_;
  }

  public static final int TOTAL_OUTPUT_TOKENS_FIELD_NUMBER = 12;
  private long totalOutputTokens_ = 0L;
  /**
   * <pre>
   * Cumulative output tokens consumed across all LLM and agent tasks.
   *
   * &#64;internal
   * Output tokens represent completion/generation tokens returned by the model.
   * Updated alongside total_cost_micros from the budget tracker.
   *
   * &#64;since Cost Data Pipeline
   * </pre>
   *
   * <code>int64 total_output_tokens = 12 [json_name = "totalOutputTokens"];</code>
   * @return The totalOutputTokens.
   */
  @java.lang.Override
  public long getTotalOutputTokens() {
    return totalOutputTokens_;
  }

  public static final int WORKFLOW_VERSION_HASH_FIELD_NUMBER = 13;
  @SuppressWarnings("serial")
  private volatile java.lang.Object workflowVersionHash_ = "";
  /**
   * <pre>
   * SHA-256 hash identifying which workflow version was used for this execution.
   *
   * &#64;internal
   * Pinned at execution creation time from Workflow.status.version_hash.
   * Immutable after creation — represents the exact workflow definition this
   * execution ran (or will run, if still pending).
   *
   * Consumers:
   * - Runner: fetches the version-specific CNCF YAML via getVersion() during
   * hydration, ensuring the execution runs the intended definition even if
   * the workflow has been updated since creation.
   * - Execution viewer: fetches the version entry to render the correct graph
   * for historical executions, eliminating the version mismatch problem.
   *
   * Empty for executions created before workflow versioning was introduced.
   * In that case, consumers fall back to fetching the current workflow
   * definition (legacy behavior with mismatch warning).
   *
   * &#64;since Workflow Versioning
   * </pre>
   *
   * <code>string workflow_version_hash = 13 [json_name = "workflowVersionHash"];</code>
   * @return The workflowVersionHash.
   */
  @java.lang.Override
  public java.lang.String getWorkflowVersionHash() {
    java.lang.Object ref = workflowVersionHash_;
    if (ref instanceof java.lang.String) {
      return (java.lang.String) ref;
    } else {
      com.google.protobuf.ByteString bs = 
          (com.google.protobuf.ByteString) ref;
      java.lang.String s = bs.toStringUtf8();
      workflowVersionHash_ = s;
      return s;
    }
  }
  /**
   * <pre>
   * SHA-256 hash identifying which workflow version was used for this execution.
   *
   * &#64;internal
   * Pinned at execution creation time from Workflow.status.version_hash.
   * Immutable after creation — represents the exact workflow definition this
   * execution ran (or will run, if still pending).
   *
   * Consumers:
   * - Runner: fetches the version-specific CNCF YAML via getVersion() during
   * hydration, ensuring the execution runs the intended definition even if
   * the workflow has been updated since creation.
   * - Execution viewer: fetches the version entry to render the correct graph
   * for historical executions, eliminating the version mismatch problem.
   *
   * Empty for executions created before workflow versioning was introduced.
   * In that case, consumers fall back to fetching the current workflow
   * definition (legacy behavior with mismatch warning).
   *
   * &#64;since Workflow Versioning
   * </pre>
   *
   * <code>string workflow_version_hash = 13 [json_name = "workflowVersionHash"];</code>
   * @return The bytes for workflowVersionHash.
   */
  @java.lang.Override
  public com.google.protobuf.ByteString
      getWorkflowVersionHashBytes() {
    java.lang.Object ref = workflowVersionHash_;
    if (ref instanceof java.lang.String) {
      com.google.protobuf.ByteString b = 
          com.google.protobuf.ByteString.copyFromUtf8(
              (java.lang.String) ref);
      workflowVersionHash_ = b;
      return b;
    } else {
      return (com.google.protobuf.ByteString) ref;
    }
  }

  public static final int PENDING_FILE_REVIEWS_FIELD_NUMBER = 14;
  @SuppressWarnings("serial")
  private java.util.List<ai.stigmer.agentic.workflowexecution.v1.WorkflowPendingFileReview> pendingFileReviews_;
  /**
   * <pre>
   * Pending file reviews from child agent executions, surfaced for
   * workflow-level visibility and in-place keep/discard decisioning.
   *
   * &#64;internal
   * A reference-only sibling of pending_approvals (field 9): each entry names a
   * child agent execution and the change_set ids it currently has
   * AWAITING_REVIEW. The heavy content (diffs, blobs) is NOT copied here — it
   * stays single-sourced on the child's
   * AgentExecution.status.file_change_sets and is read on demand by the UI.
   *
   * Guarded, per-child merge protocol:
   * Modified only when WorkflowExecutionUpdateStatusInput.update_pending_file_reviews
   * is true, scoped by pending_update_child_agent_execution_id. The write
   * replaces the entry for that one child and preserves every sibling child's
   * entry, so parallel child agents never clobber each other. A scoped write with
   * an empty change_set list removes that child's entry. Only call-agent-status
   * manages this field.
   *
   * &#64;since Workflow-Parent File Review
   * </pre>
   *
   * <code>repeated .ai.stigmer.agentic.workflowexecution.v1.WorkflowPendingFileReview pending_file_reviews = 14 [json_name = "pendingFileReviews"];</code>
   */
  @java.lang.Override
  public java.util.List<ai.stigmer.agentic.workflowexecution.v1.WorkflowPendingFileReview> getPendingFileReviewsList() {
    return pendingFileReviews_;
  }
  /**
   * <pre>
   * Pending file reviews from child agent executions, surfaced for
   * workflow-level visibility and in-place keep/discard decisioning.
   *
   * &#64;internal
   * A reference-only sibling of pending_approvals (field 9): each entry names a
   * child agent execution and the change_set ids it currently has
   * AWAITING_REVIEW. The heavy content (diffs, blobs) is NOT copied here — it
   * stays single-sourced on the child's
   * AgentExecution.status.file_change_sets and is read on demand by the UI.
   *
   * Guarded, per-child merge protocol:
   * Modified only when WorkflowExecutionUpdateStatusInput.update_pending_file_reviews
   * is true, scoped by pending_update_child_agent_execution_id. The write
   * replaces the entry for that one child and preserves every sibling child's
   * entry, so parallel child agents never clobber each other. A scoped write with
   * an empty change_set list removes that child's entry. Only call-agent-status
   * manages this field.
   *
   * &#64;since Workflow-Parent File Review
   * </pre>
   *
   * <code>repeated .ai.stigmer.agentic.workflowexecution.v1.WorkflowPendingFileReview pending_file_reviews = 14 [json_name = "pendingFileReviews"];</code>
   */
  @java.lang.Override
  public java.util.List<? extends ai.stigmer.agentic.workflowexecution.v1.WorkflowPendingFileReviewOrBuilder> 
      getPendingFileReviewsOrBuilderList() {
    return pendingFileReviews_;
  }
  /**
   * <pre>
   * Pending file reviews from child agent executions, surfaced for
   * workflow-level visibility and in-place keep/discard decisioning.
   *
   * &#64;internal
   * A reference-only sibling of pending_approvals (field 9): each entry names a
   * child agent execution and the change_set ids it currently has
   * AWAITING_REVIEW. The heavy content (diffs, blobs) is NOT copied here — it
   * stays single-sourced on the child's
   * AgentExecution.status.file_change_sets and is read on demand by the UI.
   *
   * Guarded, per-child merge protocol:
   * Modified only when WorkflowExecutionUpdateStatusInput.update_pending_file_reviews
   * is true, scoped by pending_update_child_agent_execution_id. The write
   * replaces the entry for that one child and preserves every sibling child's
   * entry, so parallel child agents never clobber each other. A scoped write with
   * an empty change_set list removes that child's entry. Only call-agent-status
   * manages this field.
   *
   * &#64;since Workflow-Parent File Review
   * </pre>
   *
   * <code>repeated .ai.stigmer.agentic.workflowexecution.v1.WorkflowPendingFileReview pending_file_reviews = 14 [json_name = "pendingFileReviews"];</code>
   */
  @java.lang.Override
  public int getPendingFileReviewsCount() {
    return pendingFileReviews_.size();
  }
  /**
   * <pre>
   * Pending file reviews from child agent executions, surfaced for
   * workflow-level visibility and in-place keep/discard decisioning.
   *
   * &#64;internal
   * A reference-only sibling of pending_approvals (field 9): each entry names a
   * child agent execution and the change_set ids it currently has
   * AWAITING_REVIEW. The heavy content (diffs, blobs) is NOT copied here — it
   * stays single-sourced on the child's
   * AgentExecution.status.file_change_sets and is read on demand by the UI.
   *
   * Guarded, per-child merge protocol:
   * Modified only when WorkflowExecutionUpdateStatusInput.update_pending_file_reviews
   * is true, scoped by pending_update_child_agent_execution_id. The write
   * replaces the entry for that one child and preserves every sibling child's
   * entry, so parallel child agents never clobber each other. A scoped write with
   * an empty change_set list removes that child's entry. Only call-agent-status
   * manages this field.
   *
   * &#64;since Workflow-Parent File Review
   * </pre>
   *
   * <code>repeated .ai.stigmer.agentic.workflowexecution.v1.WorkflowPendingFileReview pending_file_reviews = 14 [json_name = "pendingFileReviews"];</code>
   */
  @java.lang.Override
  public ai.stigmer.agentic.workflowexecution.v1.WorkflowPendingFileReview getPendingFileReviews(int index) {
    return pendingFileReviews_.get(index);
  }
  /**
   * <pre>
   * Pending file reviews from child agent executions, surfaced for
   * workflow-level visibility and in-place keep/discard decisioning.
   *
   * &#64;internal
   * A reference-only sibling of pending_approvals (field 9): each entry names a
   * child agent execution and the change_set ids it currently has
   * AWAITING_REVIEW. The heavy content (diffs, blobs) is NOT copied here — it
   * stays single-sourced on the child's
   * AgentExecution.status.file_change_sets and is read on demand by the UI.
   *
   * Guarded, per-child merge protocol:
   * Modified only when WorkflowExecutionUpdateStatusInput.update_pending_file_reviews
   * is true, scoped by pending_update_child_agent_execution_id. The write
   * replaces the entry for that one child and preserves every sibling child's
   * entry, so parallel child agents never clobber each other. A scoped write with
   * an empty change_set list removes that child's entry. Only call-agent-status
   * manages this field.
   *
   * &#64;since Workflow-Parent File Review
   * </pre>
   *
   * <code>repeated .ai.stigmer.agentic.workflowexecution.v1.WorkflowPendingFileReview pending_file_reviews = 14 [json_name = "pendingFileReviews"];</code>
   */
  @java.lang.Override
  public ai.stigmer.agentic.workflowexecution.v1.WorkflowPendingFileReviewOrBuilder getPendingFileReviewsOrBuilder(
      int index) {
    return pendingFileReviews_.get(index);
  }

  private byte memoizedIsInitialized = -1;
  @java.lang.Override
  public final boolean isInitialized() {
    byte isInitialized = memoizedIsInitialized;
    if (isInitialized == 1) return true;
    if (isInitialized == 0) return false;

    memoizedIsInitialized = 1;
    return true;
  }

  @java.lang.Override
  public void writeTo(com.google.protobuf.CodedOutputStream output)
                      throws java.io.IOException {
    if (phase_ != ai.stigmer.agentic.workflowexecution.v1.ExecutionPhase.EXECUTION_PHASE_UNSPECIFIED.getNumber()) {
      output.writeEnum(1, phase_);
    }
    for (int i = 0; i < tasks_.size(); i++) {
      output.writeMessage(2, tasks_.get(i));
    }
    if (((bitField0_ & 0x00000002) != 0)) {
      output.writeMessage(3, getOutput());
    }
    if (!com.google.protobuf.GeneratedMessage.isStringEmpty(error_)) {
      com.google.protobuf.GeneratedMessage.writeString(output, 4, error_);
    }
    if (!com.google.protobuf.GeneratedMessage.isStringEmpty(startedAt_)) {
      com.google.protobuf.GeneratedMessage.writeString(output, 5, startedAt_);
    }
    if (!com.google.protobuf.GeneratedMessage.isStringEmpty(completedAt_)) {
      com.google.protobuf.GeneratedMessage.writeString(output, 6, completedAt_);
    }
    if (!com.google.protobuf.GeneratedMessage.isStringEmpty(temporalWorkflowId_)) {
      com.google.protobuf.GeneratedMessage.writeString(output, 7, temporalWorkflowId_);
    }
    for (int i = 0; i < pendingApprovals_.size(); i++) {
      output.writeMessage(9, pendingApprovals_.get(i));
    }
    if (totalCostMicros_ != 0L) {
      output.writeInt64(10, totalCostMicros_);
    }
    if (totalInputTokens_ != 0L) {
      output.writeInt64(11, totalInputTokens_);
    }
    if (totalOutputTokens_ != 0L) {
      output.writeInt64(12, totalOutputTokens_);
    }
    if (!com.google.protobuf.GeneratedMessage.isStringEmpty(workflowVersionHash_)) {
      com.google.protobuf.GeneratedMessage.writeString(output, 13, workflowVersionHash_);
    }
    for (int i = 0; i < pendingFileReviews_.size(); i++) {
      output.writeMessage(14, pendingFileReviews_.get(i));
    }
    if (((bitField0_ & 0x00000001) != 0)) {
      output.writeMessage(99, getAudit());
    }
    getUnknownFields().writeTo(output);
  }

  @java.lang.Override
  public int getSerializedSize() {
    int size = memoizedSize;
    if (size != -1) return size;

    size = 0;
    if (phase_ != ai.stigmer.agentic.workflowexecution.v1.ExecutionPhase.EXECUTION_PHASE_UNSPECIFIED.getNumber()) {
      size += com.google.protobuf.CodedOutputStream
        .computeEnumSize(1, phase_);
    }

        {
          final int count = tasks_.size();
          for (int i = 0; i < count; i++) {
            size += com.google.protobuf.CodedOutputStream
              .computeMessageSizeNoTag(tasks_.get(i));
          }
          size += 1 * count;
        }
    if (((bitField0_ & 0x00000002) != 0)) {
      size += com.google.protobuf.CodedOutputStream
        .computeMessageSize(3, getOutput());
    }
    if (!com.google.protobuf.GeneratedMessage.isStringEmpty(error_)) {
      size += com.google.protobuf.GeneratedMessage.computeStringSize(4, error_);
    }
    if (!com.google.protobuf.GeneratedMessage.isStringEmpty(startedAt_)) {
      size += com.google.protobuf.GeneratedMessage.computeStringSize(5, startedAt_);
    }
    if (!com.google.protobuf.GeneratedMessage.isStringEmpty(completedAt_)) {
      size += com.google.protobuf.GeneratedMessage.computeStringSize(6, completedAt_);
    }
    if (!com.google.protobuf.GeneratedMessage.isStringEmpty(temporalWorkflowId_)) {
      size += com.google.protobuf.GeneratedMessage.computeStringSize(7, temporalWorkflowId_);
    }

        {
          final int count = pendingApprovals_.size();
          for (int i = 0; i < count; i++) {
            size += com.google.protobuf.CodedOutputStream
              .computeMessageSizeNoTag(pendingApprovals_.get(i));
          }
          size += 1 * count;
        }
    if (totalCostMicros_ != 0L) {
      size += com.google.protobuf.CodedOutputStream
        .computeInt64Size(10, totalCostMicros_);
    }
    if (totalInputTokens_ != 0L) {
      size += com.google.protobuf.CodedOutputStream
        .computeInt64Size(11, totalInputTokens_);
    }
    if (totalOutputTokens_ != 0L) {
      size += com.google.protobuf.CodedOutputStream
        .computeInt64Size(12, totalOutputTokens_);
    }
    if (!com.google.protobuf.GeneratedMessage.isStringEmpty(workflowVersionHash_)) {
      size += com.google.protobuf.GeneratedMessage.computeStringSize(13, workflowVersionHash_);
    }

        {
          final int count = pendingFileReviews_.size();
          for (int i = 0; i < count; i++) {
            size += com.google.protobuf.CodedOutputStream
              .computeMessageSizeNoTag(pendingFileReviews_.get(i));
          }
          size += 1 * count;
        }
    if (((bitField0_ & 0x00000001) != 0)) {
      size += com.google.protobuf.CodedOutputStream
        .computeMessageSize(99, getAudit());
    }
    size += getUnknownFields().getSerializedSize();
    memoizedSize = size;
    return size;
  }

  @java.lang.Override
  public boolean equals(final java.lang.Object obj) {
    if (obj == this) {
     return true;
    }
    if (!(obj instanceof ai.stigmer.agentic.workflowexecution.v1.WorkflowExecutionStatus)) {
      return super.equals(obj);
    }
    ai.stigmer.agentic.workflowexecution.v1.WorkflowExecutionStatus other = (ai.stigmer.agentic.workflowexecution.v1.WorkflowExecutionStatus) obj;

    if (hasAudit() != other.hasAudit()) return false;
    if (hasAudit()) {
      if (!getAudit()
          .equals(other.getAudit())) return false;
    }
    if (phase_ != other.phase_) return false;
    if (!getTasksList()
        .equals(other.getTasksList())) return false;
    if (hasOutput() != other.hasOutput()) return false;
    if (hasOutput()) {
      if (!getOutput()
          .equals(other.getOutput())) return false;
    }
    if (!getError()
        .equals(other.getError())) return false;
    if (!getStartedAt()
        .equals(other.getStartedAt())) return false;
    if (!getCompletedAt()
        .equals(other.getCompletedAt())) return false;
    if (!getTemporalWorkflowId()
        .equals(other.getTemporalWorkflowId())) return false;
    if (!getPendingApprovalsList()
        .equals(other.getPendingApprovalsList())) return false;
    if (getTotalCostMicros()
        != other.getTotalCostMicros()) return false;
    if (getTotalInputTokens()
        != other.getTotalInputTokens()) return false;
    if (getTotalOutputTokens()
        != other.getTotalOutputTokens()) return false;
    if (!getWorkflowVersionHash()
        .equals(other.getWorkflowVersionHash())) return false;
    if (!getPendingFileReviewsList()
        .equals(other.getPendingFileReviewsList())) return false;
    if (!getUnknownFields().equals(other.getUnknownFields())) return false;
    return true;
  }

  @java.lang.Override
  public int hashCode() {
    if (memoizedHashCode != 0) {
      return memoizedHashCode;
    }
    int hash = 41;
    hash = (19 * hash) + getDescriptor().hashCode();
    if (hasAudit()) {
      hash = (37 * hash) + AUDIT_FIELD_NUMBER;
      hash = (53 * hash) + getAudit().hashCode();
    }
    hash = (37 * hash) + PHASE_FIELD_NUMBER;
    hash = (53 * hash) + phase_;
    if (getTasksCount() > 0) {
      hash = (37 * hash) + TASKS_FIELD_NUMBER;
      hash = (53 * hash) + getTasksList().hashCode();
    }
    if (hasOutput()) {
      hash = (37 * hash) + OUTPUT_FIELD_NUMBER;
      hash = (53 * hash) + getOutput().hashCode();
    }
    hash = (37 * hash) + ERROR_FIELD_NUMBER;
    hash = (53 * hash) + getError().hashCode();
    hash = (37 * hash) + STARTED_AT_FIELD_NUMBER;
    hash = (53 * hash) + getStartedAt().hashCode();
    hash = (37 * hash) + COMPLETED_AT_FIELD_NUMBER;
    hash = (53 * hash) + getCompletedAt().hashCode();
    hash = (37 * hash) + TEMPORAL_WORKFLOW_ID_FIELD_NUMBER;
    hash = (53 * hash) + getTemporalWorkflowId().hashCode();
    if (getPendingApprovalsCount() > 0) {
      hash = (37 * hash) + PENDING_APPROVALS_FIELD_NUMBER;
      hash = (53 * hash) + getPendingApprovalsList().hashCode();
    }
    hash = (37 * hash) + TOTAL_COST_MICROS_FIELD_NUMBER;
    hash = (53 * hash) + com.google.protobuf.Internal.hashLong(
        getTotalCostMicros());
    hash = (37 * hash) + TOTAL_INPUT_TOKENS_FIELD_NUMBER;
    hash = (53 * hash) + com.google.protobuf.Internal.hashLong(
        getTotalInputTokens());
    hash = (37 * hash) + TOTAL_OUTPUT_TOKENS_FIELD_NUMBER;
    hash = (53 * hash) + com.google.protobuf.Internal.hashLong(
        getTotalOutputTokens());
    hash = (37 * hash) + WORKFLOW_VERSION_HASH_FIELD_NUMBER;
    hash = (53 * hash) + getWorkflowVersionHash().hashCode();
    if (getPendingFileReviewsCount() > 0) {
      hash = (37 * hash) + PENDING_FILE_REVIEWS_FIELD_NUMBER;
      hash = (53 * hash) + getPendingFileReviewsList().hashCode();
    }
    hash = (29 * hash) + getUnknownFields().hashCode();
    memoizedHashCode = hash;
    return hash;
  }

  public static ai.stigmer.agentic.workflowexecution.v1.WorkflowExecutionStatus parseFrom(
      java.nio.ByteBuffer data)
      throws com.google.protobuf.InvalidProtocolBufferException {
    return PARSER.parseFrom(data);
  }
  public static ai.stigmer.agentic.workflowexecution.v1.WorkflowExecutionStatus parseFrom(
      java.nio.ByteBuffer data,
      com.google.protobuf.ExtensionRegistryLite extensionRegistry)
      throws com.google.protobuf.InvalidProtocolBufferException {
    return PARSER.parseFrom(data, extensionRegistry);
  }
  public static ai.stigmer.agentic.workflowexecution.v1.WorkflowExecutionStatus parseFrom(
      com.google.protobuf.ByteString data)
      throws com.google.protobuf.InvalidProtocolBufferException {
    return PARSER.parseFrom(data);
  }
  public static ai.stigmer.agentic.workflowexecution.v1.WorkflowExecutionStatus parseFrom(
      com.google.protobuf.ByteString data,
      com.google.protobuf.ExtensionRegistryLite extensionRegistry)
      throws com.google.protobuf.InvalidProtocolBufferException {
    return PARSER.parseFrom(data, extensionRegistry);
  }
  public static ai.stigmer.agentic.workflowexecution.v1.WorkflowExecutionStatus parseFrom(byte[] data)
      throws com.google.protobuf.InvalidProtocolBufferException {
    return PARSER.parseFrom(data);
  }
  public static ai.stigmer.agentic.workflowexecution.v1.WorkflowExecutionStatus parseFrom(
      byte[] data,
      com.google.protobuf.ExtensionRegistryLite extensionRegistry)
      throws com.google.protobuf.InvalidProtocolBufferException {
    return PARSER.parseFrom(data, extensionRegistry);
  }
  public static ai.stigmer.agentic.workflowexecution.v1.WorkflowExecutionStatus parseFrom(java.io.InputStream input)
      throws java.io.IOException {
    return com.google.protobuf.GeneratedMessage
        .parseWithIOException(PARSER, input);
  }
  public static ai.stigmer.agentic.workflowexecution.v1.WorkflowExecutionStatus parseFrom(
      java.io.InputStream input,
      com.google.protobuf.ExtensionRegistryLite extensionRegistry)
      throws java.io.IOException {
    return com.google.protobuf.GeneratedMessage
        .parseWithIOException(PARSER, input, extensionRegistry);
  }

  public static ai.stigmer.agentic.workflowexecution.v1.WorkflowExecutionStatus parseDelimitedFrom(java.io.InputStream input)
      throws java.io.IOException {
    return com.google.protobuf.GeneratedMessage
        .parseDelimitedWithIOException(PARSER, input);
  }

  public static ai.stigmer.agentic.workflowexecution.v1.WorkflowExecutionStatus parseDelimitedFrom(
      java.io.InputStream input,
      com.google.protobuf.ExtensionRegistryLite extensionRegistry)
      throws java.io.IOException {
    return com.google.protobuf.GeneratedMessage
        .parseDelimitedWithIOException(PARSER, input, extensionRegistry);
  }
  public static ai.stigmer.agentic.workflowexecution.v1.WorkflowExecutionStatus parseFrom(
      com.google.protobuf.CodedInputStream input)
      throws java.io.IOException {
    return com.google.protobuf.GeneratedMessage
        .parseWithIOException(PARSER, input);
  }
  public static ai.stigmer.agentic.workflowexecution.v1.WorkflowExecutionStatus parseFrom(
      com.google.protobuf.CodedInputStream input,
      com.google.protobuf.ExtensionRegistryLite extensionRegistry)
      throws java.io.IOException {
    return com.google.protobuf.GeneratedMessage
        .parseWithIOException(PARSER, input, extensionRegistry);
  }

  @java.lang.Override
  public Builder newBuilderForType() { return newBuilder(); }
  public static Builder newBuilder() {
    return DEFAULT_INSTANCE.toBuilder();
  }
  public static Builder newBuilder(ai.stigmer.agentic.workflowexecution.v1.WorkflowExecutionStatus prototype) {
    return DEFAULT_INSTANCE.toBuilder().mergeFrom(prototype);
  }
  @java.lang.Override
  public Builder toBuilder() {
    return this == DEFAULT_INSTANCE
        ? new Builder() : new Builder().mergeFrom(this);
  }

  @java.lang.Override
  protected Builder newBuilderForType(
      com.google.protobuf.GeneratedMessage.BuilderParent parent) {
    Builder builder = new Builder(parent);
    return builder;
  }
  /**
   * <pre>
   * WorkflowExecutionStatus contains all system-managed execution state and results.
   *
   * &#64;internal
   * Everything populated during or after execution goes here, not in spec.
   * This message follows the standard pattern: audit information at field 99, custom fields at 1-98.
   *
   * Status Lifecycle:
   * 1. Created: audit.created_at set, phase = EXECUTION_PENDING
   * 2. Started: started_at set, phase = EXECUTION_IN_PROGRESS, tasks populated
   * 3. Progress: tasks updated as workflow runner sends status updates
   * 4. Completed: completed_at set, phase = EXECUTION_COMPLETED, output populated
   * 5. Failed: completed_at set, phase = EXECUTION_FAILED, error populated
   * 6. Cancelled: completed_at set, phase = EXECUTION_CANCELLED
   *
   * The status is read-only to users and continuously updated by the workflow execution engine.
   *
   * Progress Tracking Pattern (Matches Agent Execution):
   * - Agent execution: messages[], tool_calls[], sub_agent_executions[] (transformed from LLM streaming events)
   * - Workflow execution: tasks[] (transformed from workflow task execution)
   * - Both store structured state, not raw streaming events
   * - Both use updateStatus RPC for progressive updates during execution
   * </pre>
   *
   * Protobuf type {@code ai.stigmer.agentic.workflowexecution.v1.WorkflowExecutionStatus}
   */
  public static final class Builder extends
      com.google.protobuf.GeneratedMessage.Builder<Builder> implements
      // @@protoc_insertion_point(builder_implements:ai.stigmer.agentic.workflowexecution.v1.WorkflowExecutionStatus)
      ai.stigmer.agentic.workflowexecution.v1.WorkflowExecutionStatusOrBuilder {
    public static final com.google.protobuf.Descriptors.Descriptor
        getDescriptor() {
      return ai.stigmer.agentic.workflowexecution.v1.ApiProto.internal_static_ai_stigmer_agentic_workflowexecution_v1_WorkflowExecutionStatus_descriptor;
    }

    @java.lang.Override
    protected com.google.protobuf.GeneratedMessage.FieldAccessorTable
        internalGetFieldAccessorTable() {
      return ai.stigmer.agentic.workflowexecution.v1.ApiProto.internal_static_ai_stigmer_agentic_workflowexecution_v1_WorkflowExecutionStatus_fieldAccessorTable
          .ensureFieldAccessorsInitialized(
              ai.stigmer.agentic.workflowexecution.v1.WorkflowExecutionStatus.class, ai.stigmer.agentic.workflowexecution.v1.WorkflowExecutionStatus.Builder.class);
    }

    // Construct using ai.stigmer.agentic.workflowexecution.v1.WorkflowExecutionStatus.newBuilder()
    private Builder() {
      maybeForceBuilderInitialization();
    }

    private Builder(
        com.google.protobuf.GeneratedMessage.BuilderParent parent) {
      super(parent);
      maybeForceBuilderInitialization();
    }
    private void maybeForceBuilderInitialization() {
      if (com.google.protobuf.GeneratedMessage
              .alwaysUseFieldBuilders) {
        internalGetAuditFieldBuilder();
        internalGetTasksFieldBuilder();
        internalGetOutputFieldBuilder();
        internalGetPendingApprovalsFieldBuilder();
        internalGetPendingFileReviewsFieldBuilder();
      }
    }
    @java.lang.Override
    public Builder clear() {
      super.clear();
      bitField0_ = 0;
      audit_ = null;
      if (auditBuilder_ != null) {
        auditBuilder_.dispose();
        auditBuilder_ = null;
      }
      phase_ = 0;
      if (tasksBuilder_ == null) {
        tasks_ = java.util.Collections.emptyList();
      } else {
        tasks_ = null;
        tasksBuilder_.clear();
      }
      bitField0_ = (bitField0_ & ~0x00000004);
      output_ = null;
      if (outputBuilder_ != null) {
        outputBuilder_.dispose();
        outputBuilder_ = null;
      }
      error_ = "";
      startedAt_ = "";
      completedAt_ = "";
      temporalWorkflowId_ = "";
      if (pendingApprovalsBuilder_ == null) {
        pendingApprovals_ = java.util.Collections.emptyList();
      } else {
        pendingApprovals_ = null;
        pendingApprovalsBuilder_.clear();
      }
      bitField0_ = (bitField0_ & ~0x00000100);
      totalCostMicros_ = 0L;
      totalInputTokens_ = 0L;
      totalOutputTokens_ = 0L;
      workflowVersionHash_ = "";
      if (pendingFileReviewsBuilder_ == null) {
        pendingFileReviews_ = java.util.Collections.emptyList();
      } else {
        pendingFileReviews_ = null;
        pendingFileReviewsBuilder_.clear();
      }
      bitField0_ = (bitField0_ & ~0x00002000);
      return this;
    }

    @java.lang.Override
    public com.google.protobuf.Descriptors.Descriptor
        getDescriptorForType() {
      return ai.stigmer.agentic.workflowexecution.v1.ApiProto.internal_static_ai_stigmer_agentic_workflowexecution_v1_WorkflowExecutionStatus_descriptor;
    }

    @java.lang.Override
    public ai.stigmer.agentic.workflowexecution.v1.WorkflowExecutionStatus getDefaultInstanceForType() {
      return ai.stigmer.agentic.workflowexecution.v1.WorkflowExecutionStatus.getDefaultInstance();
    }

    @java.lang.Override
    public ai.stigmer.agentic.workflowexecution.v1.WorkflowExecutionStatus build() {
      ai.stigmer.agentic.workflowexecution.v1.WorkflowExecutionStatus result = buildPartial();
      if (!result.isInitialized()) {
        throw newUninitializedMessageException(result);
      }
      return result;
    }

    @java.lang.Override
    public ai.stigmer.agentic.workflowexecution.v1.WorkflowExecutionStatus buildPartial() {
      ai.stigmer.agentic.workflowexecution.v1.WorkflowExecutionStatus result = new ai.stigmer.agentic.workflowexecution.v1.WorkflowExecutionStatus(this);
      buildPartialRepeatedFields(result);
      if (bitField0_ != 0) { buildPartial0(result); }
      onBuilt();
      return result;
    }

    private void buildPartialRepeatedFields(ai.stigmer.agentic.workflowexecution.v1.WorkflowExecutionStatus result) {
      if (tasksBuilder_ == null) {
        if (((bitField0_ & 0x00000004) != 0)) {
          tasks_ = java.util.Collections.unmodifiableList(tasks_);
          bitField0_ = (bitField0_ & ~0x00000004);
        }
        result.tasks_ = tasks_;
      } else {
        result.tasks_ = tasksBuilder_.build();
      }
      if (pendingApprovalsBuilder_ == null) {
        if (((bitField0_ & 0x00000100) != 0)) {
          pendingApprovals_ = java.util.Collections.unmodifiableList(pendingApprovals_);
          bitField0_ = (bitField0_ & ~0x00000100);
        }
        result.pendingApprovals_ = pendingApprovals_;
      } else {
        result.pendingApprovals_ = pendingApprovalsBuilder_.build();
      }
      if (pendingFileReviewsBuilder_ == null) {
        if (((bitField0_ & 0x00002000) != 0)) {
          pendingFileReviews_ = java.util.Collections.unmodifiableList(pendingFileReviews_);
          bitField0_ = (bitField0_ & ~0x00002000);
        }
        result.pendingFileReviews_ = pendingFileReviews_;
      } else {
        result.pendingFileReviews_ = pendingFileReviewsBuilder_.build();
      }
    }

    private void buildPartial0(ai.stigmer.agentic.workflowexecution.v1.WorkflowExecutionStatus result) {
      int from_bitField0_ = bitField0_;
      int to_bitField0_ = 0;
      if (((from_bitField0_ & 0x00000001) != 0)) {
        result.audit_ = auditBuilder_ == null
            ? audit_
            : auditBuilder_.build();
        to_bitField0_ |= 0x00000001;
      }
      if (((from_bitField0_ & 0x00000002) != 0)) {
        result.phase_ = phase_;
      }
      if (((from_bitField0_ & 0x00000008) != 0)) {
        result.output_ = outputBuilder_ == null
            ? output_
            : outputBuilder_.build();
        to_bitField0_ |= 0x00000002;
      }
      if (((from_bitField0_ & 0x00000010) != 0)) {
        result.error_ = error_;
      }
      if (((from_bitField0_ & 0x00000020) != 0)) {
        result.startedAt_ = startedAt_;
      }
      if (((from_bitField0_ & 0x00000040) != 0)) {
        result.completedAt_ = completedAt_;
      }
      if (((from_bitField0_ & 0x00000080) != 0)) {
        result.temporalWorkflowId_ = temporalWorkflowId_;
      }
      if (((from_bitField0_ & 0x00000200) != 0)) {
        result.totalCostMicros_ = totalCostMicros_;
      }
      if (((from_bitField0_ & 0x00000400) != 0)) {
        result.totalInputTokens_ = totalInputTokens_;
      }
      if (((from_bitField0_ & 0x00000800) != 0)) {
        result.totalOutputTokens_ = totalOutputTokens_;
      }
      if (((from_bitField0_ & 0x00001000) != 0)) {
        result.workflowVersionHash_ = workflowVersionHash_;
      }
      result.bitField0_ |= to_bitField0_;
    }

    @java.lang.Override
    public Builder mergeFrom(com.google.protobuf.Message other) {
      if (other instanceof ai.stigmer.agentic.workflowexecution.v1.WorkflowExecutionStatus) {
        return mergeFrom((ai.stigmer.agentic.workflowexecution.v1.WorkflowExecutionStatus)other);
      } else {
        super.mergeFrom(other);
        return this;
      }
    }

    public Builder mergeFrom(ai.stigmer.agentic.workflowexecution.v1.WorkflowExecutionStatus other) {
      if (other == ai.stigmer.agentic.workflowexecution.v1.WorkflowExecutionStatus.getDefaultInstance()) return this;
      if (other.hasAudit()) {
        mergeAudit(other.getAudit());
      }
      if (other.phase_ != 0) {
        setPhaseValue(other.getPhaseValue());
      }
      if (tasksBuilder_ == null) {
        if (!other.tasks_.isEmpty()) {
          if (tasks_.isEmpty()) {
            tasks_ = other.tasks_;
            bitField0_ = (bitField0_ & ~0x00000004);
          } else {
            ensureTasksIsMutable();
            tasks_.addAll(other.tasks_);
          }
          onChanged();
        }
      } else {
        if (!other.tasks_.isEmpty()) {
          if (tasksBuilder_.isEmpty()) {
            tasksBuilder_.dispose();
            tasksBuilder_ = null;
            tasks_ = other.tasks_;
            bitField0_ = (bitField0_ & ~0x00000004);
            tasksBuilder_ = 
              com.google.protobuf.GeneratedMessage.alwaysUseFieldBuilders ?
                 internalGetTasksFieldBuilder() : null;
          } else {
            tasksBuilder_.addAllMessages(other.tasks_);
          }
        }
      }
      if (other.hasOutput()) {
        mergeOutput(other.getOutput());
      }
      if (!other.getError().isEmpty()) {
        error_ = other.error_;
        bitField0_ |= 0x00000010;
        onChanged();
      }
      if (!other.getStartedAt().isEmpty()) {
        startedAt_ = other.startedAt_;
        bitField0_ |= 0x00000020;
        onChanged();
      }
      if (!other.getCompletedAt().isEmpty()) {
        completedAt_ = other.completedAt_;
        bitField0_ |= 0x00000040;
        onChanged();
      }
      if (!other.getTemporalWorkflowId().isEmpty()) {
        temporalWorkflowId_ = other.temporalWorkflowId_;
        bitField0_ |= 0x00000080;
        onChanged();
      }
      if (pendingApprovalsBuilder_ == null) {
        if (!other.pendingApprovals_.isEmpty()) {
          if (pendingApprovals_.isEmpty()) {
            pendingApprovals_ = other.pendingApprovals_;
            bitField0_ = (bitField0_ & ~0x00000100);
          } else {
            ensurePendingApprovalsIsMutable();
            pendingApprovals_.addAll(other.pendingApprovals_);
          }
          onChanged();
        }
      } else {
        if (!other.pendingApprovals_.isEmpty()) {
          if (pendingApprovalsBuilder_.isEmpty()) {
            pendingApprovalsBuilder_.dispose();
            pendingApprovalsBuilder_ = null;
            pendingApprovals_ = other.pendingApprovals_;
            bitField0_ = (bitField0_ & ~0x00000100);
            pendingApprovalsBuilder_ = 
              com.google.protobuf.GeneratedMessage.alwaysUseFieldBuilders ?
                 internalGetPendingApprovalsFieldBuilder() : null;
          } else {
            pendingApprovalsBuilder_.addAllMessages(other.pendingApprovals_);
          }
        }
      }
      if (other.getTotalCostMicros() != 0L) {
        setTotalCostMicros(other.getTotalCostMicros());
      }
      if (other.getTotalInputTokens() != 0L) {
        setTotalInputTokens(other.getTotalInputTokens());
      }
      if (other.getTotalOutputTokens() != 0L) {
        setTotalOutputTokens(other.getTotalOutputTokens());
      }
      if (!other.getWorkflowVersionHash().isEmpty()) {
        workflowVersionHash_ = other.workflowVersionHash_;
        bitField0_ |= 0x00001000;
        onChanged();
      }
      if (pendingFileReviewsBuilder_ == null) {
        if (!other.pendingFileReviews_.isEmpty()) {
          if (pendingFileReviews_.isEmpty()) {
            pendingFileReviews_ = other.pendingFileReviews_;
            bitField0_ = (bitField0_ & ~0x00002000);
          } else {
            ensurePendingFileReviewsIsMutable();
            pendingFileReviews_.addAll(other.pendingFileReviews_);
          }
          onChanged();
        }
      } else {
        if (!other.pendingFileReviews_.isEmpty()) {
          if (pendingFileReviewsBuilder_.isEmpty()) {
            pendingFileReviewsBuilder_.dispose();
            pendingFileReviewsBuilder_ = null;
            pendingFileReviews_ = other.pendingFileReviews_;
            bitField0_ = (bitField0_ & ~0x00002000);
            pendingFileReviewsBuilder_ = 
              com.google.protobuf.GeneratedMessage.alwaysUseFieldBuilders ?
                 internalGetPendingFileReviewsFieldBuilder() : null;
          } else {
            pendingFileReviewsBuilder_.addAllMessages(other.pendingFileReviews_);
          }
        }
      }
      this.mergeUnknownFields(other.getUnknownFields());
      onChanged();
      return this;
    }

    @java.lang.Override
    public final boolean isInitialized() {
      return true;
    }

    @java.lang.Override
    public Builder mergeFrom(
        com.google.protobuf.CodedInputStream input,
        com.google.protobuf.ExtensionRegistryLite extensionRegistry)
        throws java.io.IOException {
      if (extensionRegistry == null) {
        throw new java.lang.NullPointerException();
      }
      try {
        boolean done = false;
        while (!done) {
          int tag = input.readTag();
          switch (tag) {
            case 0:
              done = true;
              break;
            case 8: {
              phase_ = input.readEnum();
              bitField0_ |= 0x00000002;
              break;
            } // case 8
            case 18: {
              ai.stigmer.agentic.workflowexecution.v1.WorkflowTask m =
                  input.readMessage(
                      ai.stigmer.agentic.workflowexecution.v1.WorkflowTask.parser(),
                      extensionRegistry);
              if (tasksBuilder_ == null) {
                ensureTasksIsMutable();
                tasks_.add(m);
              } else {
                tasksBuilder_.addMessage(m);
              }
              break;
            } // case 18
            case 26: {
              input.readMessage(
                  internalGetOutputFieldBuilder().getBuilder(),
                  extensionRegistry);
              bitField0_ |= 0x00000008;
              break;
            } // case 26
            case 34: {
              error_ = input.readStringRequireUtf8();
              bitField0_ |= 0x00000010;
              break;
            } // case 34
            case 42: {
              startedAt_ = input.readStringRequireUtf8();
              bitField0_ |= 0x00000020;
              break;
            } // case 42
            case 50: {
              completedAt_ = input.readStringRequireUtf8();
              bitField0_ |= 0x00000040;
              break;
            } // case 50
            case 58: {
              temporalWorkflowId_ = input.readStringRequireUtf8();
              bitField0_ |= 0x00000080;
              break;
            } // case 58
            case 74: {
              ai.stigmer.agentic.workflowexecution.v1.WorkflowPendingApproval m =
                  input.readMessage(
                      ai.stigmer.agentic.workflowexecution.v1.WorkflowPendingApproval.parser(),
                      extensionRegistry);
              if (pendingApprovalsBuilder_ == null) {
                ensurePendingApprovalsIsMutable();
                pendingApprovals_.add(m);
              } else {
                pendingApprovalsBuilder_.addMessage(m);
              }
              break;
            } // case 74
            case 80: {
              totalCostMicros_ = input.readInt64();
              bitField0_ |= 0x00000200;
              break;
            } // case 80
            case 88: {
              totalInputTokens_ = input.readInt64();
              bitField0_ |= 0x00000400;
              break;
            } // case 88
            case 96: {
              totalOutputTokens_ = input.readInt64();
              bitField0_ |= 0x00000800;
              break;
            } // case 96
            case 106: {
              workflowVersionHash_ = input.readStringRequireUtf8();
              bitField0_ |= 0x00001000;
              break;
            } // case 106
            case 114: {
              ai.stigmer.agentic.workflowexecution.v1.WorkflowPendingFileReview m =
                  input.readMessage(
                      ai.stigmer.agentic.workflowexecution.v1.WorkflowPendingFileReview.parser(),
                      extensionRegistry);
              if (pendingFileReviewsBuilder_ == null) {
                ensurePendingFileReviewsIsMutable();
                pendingFileReviews_.add(m);
              } else {
                pendingFileReviewsBuilder_.addMessage(m);
              }
              break;
            } // case 114
            case 794: {
              input.readMessage(
                  internalGetAuditFieldBuilder().getBuilder(),
                  extensionRegistry);
              bitField0_ |= 0x00000001;
              break;
            } // case 794
            default: {
              if (!super.parseUnknownField(input, extensionRegistry, tag)) {
                done = true; // was an endgroup tag
              }
              break;
            } // default:
          } // switch (tag)
        } // while (!done)
      } catch (com.google.protobuf.InvalidProtocolBufferException e) {
        throw e.unwrapIOException();
      } finally {
        onChanged();
      } // finally
      return this;
    }
    private int bitField0_;

    private ai.stigmer.commons.apiresource.ApiResourceAudit audit_;
    private com.google.protobuf.SingleFieldBuilder<
        ai.stigmer.commons.apiresource.ApiResourceAudit, ai.stigmer.commons.apiresource.ApiResourceAudit.Builder, ai.stigmer.commons.apiresource.ApiResourceAuditOrBuilder> auditBuilder_;
    /**
     * <pre>
     * Standard audit information including timestamps and created-by identity.
     *
     * &#64;internal
     * Always at field 99 for consistency across all Stigmer API resources.
     *
     * Contains:
     * - created_at: When this execution was created (ISO 8601 timestamp)
     * - updated_at: Last time status was updated (ISO 8601 timestamp)
     * - created_by: User or system that created this execution
     * </pre>
     *
     * <code>.ai.stigmer.commons.apiresource.ApiResourceAudit audit = 99 [json_name = "audit"];</code>
     * @return Whether the audit field is set.
     */
    public boolean hasAudit() {
      return ((bitField0_ & 0x00000001) != 0);
    }
    /**
     * <pre>
     * Standard audit information including timestamps and created-by identity.
     *
     * &#64;internal
     * Always at field 99 for consistency across all Stigmer API resources.
     *
     * Contains:
     * - created_at: When this execution was created (ISO 8601 timestamp)
     * - updated_at: Last time status was updated (ISO 8601 timestamp)
     * - created_by: User or system that created this execution
     * </pre>
     *
     * <code>.ai.stigmer.commons.apiresource.ApiResourceAudit audit = 99 [json_name = "audit"];</code>
     * @return The audit.
     */
    public ai.stigmer.commons.apiresource.ApiResourceAudit getAudit() {
      if (auditBuilder_ == null) {
        return audit_ == null ? ai.stigmer.commons.apiresource.ApiResourceAudit.getDefaultInstance() : audit_;
      } else {
        return auditBuilder_.getMessage();
      }
    }
    /**
     * <pre>
     * Standard audit information including timestamps and created-by identity.
     *
     * &#64;internal
     * Always at field 99 for consistency across all Stigmer API resources.
     *
     * Contains:
     * - created_at: When this execution was created (ISO 8601 timestamp)
     * - updated_at: Last time status was updated (ISO 8601 timestamp)
     * - created_by: User or system that created this execution
     * </pre>
     *
     * <code>.ai.stigmer.commons.apiresource.ApiResourceAudit audit = 99 [json_name = "audit"];</code>
     */
    public Builder setAudit(ai.stigmer.commons.apiresource.ApiResourceAudit value) {
      if (auditBuilder_ == null) {
        if (value == null) {
          throw new NullPointerException();
        }
        audit_ = value;
      } else {
        auditBuilder_.setMessage(value);
      }
      bitField0_ |= 0x00000001;
      onChanged();
      return this;
    }
    /**
     * <pre>
     * Standard audit information including timestamps and created-by identity.
     *
     * &#64;internal
     * Always at field 99 for consistency across all Stigmer API resources.
     *
     * Contains:
     * - created_at: When this execution was created (ISO 8601 timestamp)
     * - updated_at: Last time status was updated (ISO 8601 timestamp)
     * - created_by: User or system that created this execution
     * </pre>
     *
     * <code>.ai.stigmer.commons.apiresource.ApiResourceAudit audit = 99 [json_name = "audit"];</code>
     */
    public Builder setAudit(
        ai.stigmer.commons.apiresource.ApiResourceAudit.Builder builderForValue) {
      if (auditBuilder_ == null) {
        audit_ = builderForValue.build();
      } else {
        auditBuilder_.setMessage(builderForValue.build());
      }
      bitField0_ |= 0x00000001;
      onChanged();
      return this;
    }
    /**
     * <pre>
     * Standard audit information including timestamps and created-by identity.
     *
     * &#64;internal
     * Always at field 99 for consistency across all Stigmer API resources.
     *
     * Contains:
     * - created_at: When this execution was created (ISO 8601 timestamp)
     * - updated_at: Last time status was updated (ISO 8601 timestamp)
     * - created_by: User or system that created this execution
     * </pre>
     *
     * <code>.ai.stigmer.commons.apiresource.ApiResourceAudit audit = 99 [json_name = "audit"];</code>
     */
    public Builder mergeAudit(ai.stigmer.commons.apiresource.ApiResourceAudit value) {
      if (auditBuilder_ == null) {
        if (((bitField0_ & 0x00000001) != 0) &&
          audit_ != null &&
          audit_ != ai.stigmer.commons.apiresource.ApiResourceAudit.getDefaultInstance()) {
          getAuditBuilder().mergeFrom(value);
        } else {
          audit_ = value;
        }
      } else {
        auditBuilder_.mergeFrom(value);
      }
      if (audit_ != null) {
        bitField0_ |= 0x00000001;
        onChanged();
      }
      return this;
    }
    /**
     * <pre>
     * Standard audit information including timestamps and created-by identity.
     *
     * &#64;internal
     * Always at field 99 for consistency across all Stigmer API resources.
     *
     * Contains:
     * - created_at: When this execution was created (ISO 8601 timestamp)
     * - updated_at: Last time status was updated (ISO 8601 timestamp)
     * - created_by: User or system that created this execution
     * </pre>
     *
     * <code>.ai.stigmer.commons.apiresource.ApiResourceAudit audit = 99 [json_name = "audit"];</code>
     */
    public Builder clearAudit() {
      bitField0_ = (bitField0_ & ~0x00000001);
      audit_ = null;
      if (auditBuilder_ != null) {
        auditBuilder_.dispose();
        auditBuilder_ = null;
      }
      onChanged();
      return this;
    }
    /**
     * <pre>
     * Standard audit information including timestamps and created-by identity.
     *
     * &#64;internal
     * Always at field 99 for consistency across all Stigmer API resources.
     *
     * Contains:
     * - created_at: When this execution was created (ISO 8601 timestamp)
     * - updated_at: Last time status was updated (ISO 8601 timestamp)
     * - created_by: User or system that created this execution
     * </pre>
     *
     * <code>.ai.stigmer.commons.apiresource.ApiResourceAudit audit = 99 [json_name = "audit"];</code>
     */
    public ai.stigmer.commons.apiresource.ApiResourceAudit.Builder getAuditBuilder() {
      bitField0_ |= 0x00000001;
      onChanged();
      return internalGetAuditFieldBuilder().getBuilder();
    }
    /**
     * <pre>
     * Standard audit information including timestamps and created-by identity.
     *
     * &#64;internal
     * Always at field 99 for consistency across all Stigmer API resources.
     *
     * Contains:
     * - created_at: When this execution was created (ISO 8601 timestamp)
     * - updated_at: Last time status was updated (ISO 8601 timestamp)
     * - created_by: User or system that created this execution
     * </pre>
     *
     * <code>.ai.stigmer.commons.apiresource.ApiResourceAudit audit = 99 [json_name = "audit"];</code>
     */
    public ai.stigmer.commons.apiresource.ApiResourceAuditOrBuilder getAuditOrBuilder() {
      if (auditBuilder_ != null) {
        return auditBuilder_.getMessageOrBuilder();
      } else {
        return audit_ == null ?
            ai.stigmer.commons.apiresource.ApiResourceAudit.getDefaultInstance() : audit_;
      }
    }
    /**
     * <pre>
     * Standard audit information including timestamps and created-by identity.
     *
     * &#64;internal
     * Always at field 99 for consistency across all Stigmer API resources.
     *
     * Contains:
     * - created_at: When this execution was created (ISO 8601 timestamp)
     * - updated_at: Last time status was updated (ISO 8601 timestamp)
     * - created_by: User or system that created this execution
     * </pre>
     *
     * <code>.ai.stigmer.commons.apiresource.ApiResourceAudit audit = 99 [json_name = "audit"];</code>
     */
    private com.google.protobuf.SingleFieldBuilder<
        ai.stigmer.commons.apiresource.ApiResourceAudit, ai.stigmer.commons.apiresource.ApiResourceAudit.Builder, ai.stigmer.commons.apiresource.ApiResourceAuditOrBuilder> 
        internalGetAuditFieldBuilder() {
      if (auditBuilder_ == null) {
        auditBuilder_ = new com.google.protobuf.SingleFieldBuilder<
            ai.stigmer.commons.apiresource.ApiResourceAudit, ai.stigmer.commons.apiresource.ApiResourceAudit.Builder, ai.stigmer.commons.apiresource.ApiResourceAuditOrBuilder>(
                getAudit(),
                getParentForChildren(),
                isClean());
        audit_ = null;
      }
      return auditBuilder_;
    }

    private int phase_ = 0;
    /**
     * <pre>
     * Current execution lifecycle phase.
     *
     * &#64;internal
     * Phase Transitions:
     * PENDING → IN_PROGRESS → COMPLETED
     * ↓              ↘ FAILED
     * ↓              ↘ CANCELLED
     *
     * The phase is used for:
     * - UI status indicators (progress bars, badges)
     * - Filtering executions (show only failed, show only in-progress)
     * - Alerting and notifications (notify on failure)
     * - Retry logic (retry failed executions)
     *
     * Validation: Must be a defined enum value (no unspecified/unknown).
     * </pre>
     *
     * <code>.ai.stigmer.agentic.workflowexecution.v1.ExecutionPhase phase = 1 [json_name = "phase", (.buf.validate.field) = { ... }</code>
     * @return The enum numeric value on the wire for phase.
     */
    @java.lang.Override public int getPhaseValue() {
      return phase_;
    }
    /**
     * <pre>
     * Current execution lifecycle phase.
     *
     * &#64;internal
     * Phase Transitions:
     * PENDING → IN_PROGRESS → COMPLETED
     * ↓              ↘ FAILED
     * ↓              ↘ CANCELLED
     *
     * The phase is used for:
     * - UI status indicators (progress bars, badges)
     * - Filtering executions (show only failed, show only in-progress)
     * - Alerting and notifications (notify on failure)
     * - Retry logic (retry failed executions)
     *
     * Validation: Must be a defined enum value (no unspecified/unknown).
     * </pre>
     *
     * <code>.ai.stigmer.agentic.workflowexecution.v1.ExecutionPhase phase = 1 [json_name = "phase", (.buf.validate.field) = { ... }</code>
     * @param value The enum numeric value on the wire for phase to set.
     * @throws IllegalArgumentException if UNRECOGNIZED is provided.
     * @return This builder for chaining.
     */
    public Builder setPhaseValue(int value) {
      phase_ = value;
      bitField0_ |= 0x00000002;
      onChanged();
      return this;
    }
    /**
     * <pre>
     * Current execution lifecycle phase.
     *
     * &#64;internal
     * Phase Transitions:
     * PENDING → IN_PROGRESS → COMPLETED
     * ↓              ↘ FAILED
     * ↓              ↘ CANCELLED
     *
     * The phase is used for:
     * - UI status indicators (progress bars, badges)
     * - Filtering executions (show only failed, show only in-progress)
     * - Alerting and notifications (notify on failure)
     * - Retry logic (retry failed executions)
     *
     * Validation: Must be a defined enum value (no unspecified/unknown).
     * </pre>
     *
     * <code>.ai.stigmer.agentic.workflowexecution.v1.ExecutionPhase phase = 1 [json_name = "phase", (.buf.validate.field) = { ... }</code>
     * @return The phase.
     */
    @java.lang.Override
    public ai.stigmer.agentic.workflowexecution.v1.ExecutionPhase getPhase() {
      ai.stigmer.agentic.workflowexecution.v1.ExecutionPhase result = ai.stigmer.agentic.workflowexecution.v1.ExecutionPhase.forNumber(phase_);
      return result == null ? ai.stigmer.agentic.workflowexecution.v1.ExecutionPhase.UNRECOGNIZED : result;
    }
    /**
     * <pre>
     * Current execution lifecycle phase.
     *
     * &#64;internal
     * Phase Transitions:
     * PENDING → IN_PROGRESS → COMPLETED
     * ↓              ↘ FAILED
     * ↓              ↘ CANCELLED
     *
     * The phase is used for:
     * - UI status indicators (progress bars, badges)
     * - Filtering executions (show only failed, show only in-progress)
     * - Alerting and notifications (notify on failure)
     * - Retry logic (retry failed executions)
     *
     * Validation: Must be a defined enum value (no unspecified/unknown).
     * </pre>
     *
     * <code>.ai.stigmer.agentic.workflowexecution.v1.ExecutionPhase phase = 1 [json_name = "phase", (.buf.validate.field) = { ... }</code>
     * @param value The phase to set.
     * @return This builder for chaining.
     */
    public Builder setPhase(ai.stigmer.agentic.workflowexecution.v1.ExecutionPhase value) {
      if (value == null) { throw new NullPointerException(); }
      bitField0_ |= 0x00000002;
      phase_ = value.getNumber();
      onChanged();
      return this;
    }
    /**
     * <pre>
     * Current execution lifecycle phase.
     *
     * &#64;internal
     * Phase Transitions:
     * PENDING → IN_PROGRESS → COMPLETED
     * ↓              ↘ FAILED
     * ↓              ↘ CANCELLED
     *
     * The phase is used for:
     * - UI status indicators (progress bars, badges)
     * - Filtering executions (show only failed, show only in-progress)
     * - Alerting and notifications (notify on failure)
     * - Retry logic (retry failed executions)
     *
     * Validation: Must be a defined enum value (no unspecified/unknown).
     * </pre>
     *
     * <code>.ai.stigmer.agentic.workflowexecution.v1.ExecutionPhase phase = 1 [json_name = "phase", (.buf.validate.field) = { ... }</code>
     * @return This builder for chaining.
     */
    public Builder clearPhase() {
      bitField0_ = (bitField0_ & ~0x00000002);
      phase_ = 0;
      onChanged();
      return this;
    }

    private java.util.List<ai.stigmer.agentic.workflowexecution.v1.WorkflowTask> tasks_ =
      java.util.Collections.emptyList();
    private void ensureTasksIsMutable() {
      if (!((bitField0_ & 0x00000004) != 0)) {
        tasks_ = new java.util.ArrayList<ai.stigmer.agentic.workflowexecution.v1.WorkflowTask>(tasks_);
        bitField0_ |= 0x00000004;
       }
    }

    private com.google.protobuf.RepeatedFieldBuilder<
        ai.stigmer.agentic.workflowexecution.v1.WorkflowTask, ai.stigmer.agentic.workflowexecution.v1.WorkflowTask.Builder, ai.stigmer.agentic.workflowexecution.v1.WorkflowTaskOrBuilder> tasksBuilder_;

    /**
     * <pre>
     * Workflow tasks with their individual execution state.
     *
     * &#64;internal
     * Tasks represent the atomic units of work within the workflow.
     * Each task has:
     * - task_id: Unique identifier within this execution
     * - task_name: Human-readable name (e.g., "validate_email", "send_notification")
     * - task_type: Type of task (agent_invocation, api_call, approval, etc.)
     * - status: Current task status (PENDING, IN_PROGRESS, COMPLETED, FAILED, SKIPPED)
     * - input: Task input parameters (JSON structure)
     * - output: Task output results (JSON structure, only for COMPLETED tasks)
     * - error: Error message (only for FAILED tasks)
     * - started_at: When task started executing
     * - completed_at: When task finished (COMPLETED/FAILED/SKIPPED)
     *
     * Tasks are populated as the workflow progresses through its task graph.
     * The order in this list reflects the execution order (sequential or parallel).
     *
     * Progress Calculation:
     * - Total tasks: tasks.length
     * - Completed tasks: count(tasks where status in [COMPLETED, FAILED, SKIPPED])
     * - Progress percentage: (completed_tasks / total_tasks) * 100
     * - Current task: tasks.find(status == IN_PROGRESS)
     * </pre>
     *
     * <code>repeated .ai.stigmer.agentic.workflowexecution.v1.WorkflowTask tasks = 2 [json_name = "tasks"];</code>
     */
    public java.util.List<ai.stigmer.agentic.workflowexecution.v1.WorkflowTask> getTasksList() {
      if (tasksBuilder_ == null) {
        return java.util.Collections.unmodifiableList(tasks_);
      } else {
        return tasksBuilder_.getMessageList();
      }
    }
    /**
     * <pre>
     * Workflow tasks with their individual execution state.
     *
     * &#64;internal
     * Tasks represent the atomic units of work within the workflow.
     * Each task has:
     * - task_id: Unique identifier within this execution
     * - task_name: Human-readable name (e.g., "validate_email", "send_notification")
     * - task_type: Type of task (agent_invocation, api_call, approval, etc.)
     * - status: Current task status (PENDING, IN_PROGRESS, COMPLETED, FAILED, SKIPPED)
     * - input: Task input parameters (JSON structure)
     * - output: Task output results (JSON structure, only for COMPLETED tasks)
     * - error: Error message (only for FAILED tasks)
     * - started_at: When task started executing
     * - completed_at: When task finished (COMPLETED/FAILED/SKIPPED)
     *
     * Tasks are populated as the workflow progresses through its task graph.
     * The order in this list reflects the execution order (sequential or parallel).
     *
     * Progress Calculation:
     * - Total tasks: tasks.length
     * - Completed tasks: count(tasks where status in [COMPLETED, FAILED, SKIPPED])
     * - Progress percentage: (completed_tasks / total_tasks) * 100
     * - Current task: tasks.find(status == IN_PROGRESS)
     * </pre>
     *
     * <code>repeated .ai.stigmer.agentic.workflowexecution.v1.WorkflowTask tasks = 2 [json_name = "tasks"];</code>
     */
    public int getTasksCount() {
      if (tasksBuilder_ == null) {
        return tasks_.size();
      } else {
        return tasksBuilder_.getCount();
      }
    }
    /**
     * <pre>
     * Workflow tasks with their individual execution state.
     *
     * &#64;internal
     * Tasks represent the atomic units of work within the workflow.
     * Each task has:
     * - task_id: Unique identifier within this execution
     * - task_name: Human-readable name (e.g., "validate_email", "send_notification")
     * - task_type: Type of task (agent_invocation, api_call, approval, etc.)
     * - status: Current task status (PENDING, IN_PROGRESS, COMPLETED, FAILED, SKIPPED)
     * - input: Task input parameters (JSON structure)
     * - output: Task output results (JSON structure, only for COMPLETED tasks)
     * - error: Error message (only for FAILED tasks)
     * - started_at: When task started executing
     * - completed_at: When task finished (COMPLETED/FAILED/SKIPPED)
     *
     * Tasks are populated as the workflow progresses through its task graph.
     * The order in this list reflects the execution order (sequential or parallel).
     *
     * Progress Calculation:
     * - Total tasks: tasks.length
     * - Completed tasks: count(tasks where status in [COMPLETED, FAILED, SKIPPED])
     * - Progress percentage: (completed_tasks / total_tasks) * 100
     * - Current task: tasks.find(status == IN_PROGRESS)
     * </pre>
     *
     * <code>repeated .ai.stigmer.agentic.workflowexecution.v1.WorkflowTask tasks = 2 [json_name = "tasks"];</code>
     */
    public ai.stigmer.agentic.workflowexecution.v1.WorkflowTask getTasks(int index) {
      if (tasksBuilder_ == null) {
        return tasks_.get(index);
      } else {
        return tasksBuilder_.getMessage(index);
      }
    }
    /**
     * <pre>
     * Workflow tasks with their individual execution state.
     *
     * &#64;internal
     * Tasks represent the atomic units of work within the workflow.
     * Each task has:
     * - task_id: Unique identifier within this execution
     * - task_name: Human-readable name (e.g., "validate_email", "send_notification")
     * - task_type: Type of task (agent_invocation, api_call, approval, etc.)
     * - status: Current task status (PENDING, IN_PROGRESS, COMPLETED, FAILED, SKIPPED)
     * - input: Task input parameters (JSON structure)
     * - output: Task output results (JSON structure, only for COMPLETED tasks)
     * - error: Error message (only for FAILED tasks)
     * - started_at: When task started executing
     * - completed_at: When task finished (COMPLETED/FAILED/SKIPPED)
     *
     * Tasks are populated as the workflow progresses through its task graph.
     * The order in this list reflects the execution order (sequential or parallel).
     *
     * Progress Calculation:
     * - Total tasks: tasks.length
     * - Completed tasks: count(tasks where status in [COMPLETED, FAILED, SKIPPED])
     * - Progress percentage: (completed_tasks / total_tasks) * 100
     * - Current task: tasks.find(status == IN_PROGRESS)
     * </pre>
     *
     * <code>repeated .ai.stigmer.agentic.workflowexecution.v1.WorkflowTask tasks = 2 [json_name = "tasks"];</code>
     */
    public Builder setTasks(
        int index, ai.stigmer.agentic.workflowexecution.v1.WorkflowTask value) {
      if (tasksBuilder_ == null) {
        if (value == null) {
          throw new NullPointerException();
        }
        ensureTasksIsMutable();
        tasks_.set(index, value);
        onChanged();
      } else {
        tasksBuilder_.setMessage(index, value);
      }
      return this;
    }
    /**
     * <pre>
     * Workflow tasks with their individual execution state.
     *
     * &#64;internal
     * Tasks represent the atomic units of work within the workflow.
     * Each task has:
     * - task_id: Unique identifier within this execution
     * - task_name: Human-readable name (e.g., "validate_email", "send_notification")
     * - task_type: Type of task (agent_invocation, api_call, approval, etc.)
     * - status: Current task status (PENDING, IN_PROGRESS, COMPLETED, FAILED, SKIPPED)
     * - input: Task input parameters (JSON structure)
     * - output: Task output results (JSON structure, only for COMPLETED tasks)
     * - error: Error message (only for FAILED tasks)
     * - started_at: When task started executing
     * - completed_at: When task finished (COMPLETED/FAILED/SKIPPED)
     *
     * Tasks are populated as the workflow progresses through its task graph.
     * The order in this list reflects the execution order (sequential or parallel).
     *
     * Progress Calculation:
     * - Total tasks: tasks.length
     * - Completed tasks: count(tasks where status in [COMPLETED, FAILED, SKIPPED])
     * - Progress percentage: (completed_tasks / total_tasks) * 100
     * - Current task: tasks.find(status == IN_PROGRESS)
     * </pre>
     *
     * <code>repeated .ai.stigmer.agentic.workflowexecution.v1.WorkflowTask tasks = 2 [json_name = "tasks"];</code>
     */
    public Builder setTasks(
        int index, ai.stigmer.agentic.workflowexecution.v1.WorkflowTask.Builder builderForValue) {
      if (tasksBuilder_ == null) {
        ensureTasksIsMutable();
        tasks_.set(index, builderForValue.build());
        onChanged();
      } else {
        tasksBuilder_.setMessage(index, builderForValue.build());
      }
      return this;
    }
    /**
     * <pre>
     * Workflow tasks with their individual execution state.
     *
     * &#64;internal
     * Tasks represent the atomic units of work within the workflow.
     * Each task has:
     * - task_id: Unique identifier within this execution
     * - task_name: Human-readable name (e.g., "validate_email", "send_notification")
     * - task_type: Type of task (agent_invocation, api_call, approval, etc.)
     * - status: Current task status (PENDING, IN_PROGRESS, COMPLETED, FAILED, SKIPPED)
     * - input: Task input parameters (JSON structure)
     * - output: Task output results (JSON structure, only for COMPLETED tasks)
     * - error: Error message (only for FAILED tasks)
     * - started_at: When task started executing
     * - completed_at: When task finished (COMPLETED/FAILED/SKIPPED)
     *
     * Tasks are populated as the workflow progresses through its task graph.
     * The order in this list reflects the execution order (sequential or parallel).
     *
     * Progress Calculation:
     * - Total tasks: tasks.length
     * - Completed tasks: count(tasks where status in [COMPLETED, FAILED, SKIPPED])
     * - Progress percentage: (completed_tasks / total_tasks) * 100
     * - Current task: tasks.find(status == IN_PROGRESS)
     * </pre>
     *
     * <code>repeated .ai.stigmer.agentic.workflowexecution.v1.WorkflowTask tasks = 2 [json_name = "tasks"];</code>
     */
    public Builder addTasks(ai.stigmer.agentic.workflowexecution.v1.WorkflowTask value) {
      if (tasksBuilder_ == null) {
        if (value == null) {
          throw new NullPointerException();
        }
        ensureTasksIsMutable();
        tasks_.add(value);
        onChanged();
      } else {
        tasksBuilder_.addMessage(value);
      }
      return this;
    }
    /**
     * <pre>
     * Workflow tasks with their individual execution state.
     *
     * &#64;internal
     * Tasks represent the atomic units of work within the workflow.
     * Each task has:
     * - task_id: Unique identifier within this execution
     * - task_name: Human-readable name (e.g., "validate_email", "send_notification")
     * - task_type: Type of task (agent_invocation, api_call, approval, etc.)
     * - status: Current task status (PENDING, IN_PROGRESS, COMPLETED, FAILED, SKIPPED)
     * - input: Task input parameters (JSON structure)
     * - output: Task output results (JSON structure, only for COMPLETED tasks)
     * - error: Error message (only for FAILED tasks)
     * - started_at: When task started executing
     * - completed_at: When task finished (COMPLETED/FAILED/SKIPPED)
     *
     * Tasks are populated as the workflow progresses through its task graph.
     * The order in this list reflects the execution order (sequential or parallel).
     *
     * Progress Calculation:
     * - Total tasks: tasks.length
     * - Completed tasks: count(tasks where status in [COMPLETED, FAILED, SKIPPED])
     * - Progress percentage: (completed_tasks / total_tasks) * 100
     * - Current task: tasks.find(status == IN_PROGRESS)
     * </pre>
     *
     * <code>repeated .ai.stigmer.agentic.workflowexecution.v1.WorkflowTask tasks = 2 [json_name = "tasks"];</code>
     */
    public Builder addTasks(
        int index, ai.stigmer.agentic.workflowexecution.v1.WorkflowTask value) {
      if (tasksBuilder_ == null) {
        if (value == null) {
          throw new NullPointerException();
        }
        ensureTasksIsMutable();
        tasks_.add(index, value);
        onChanged();
      } else {
        tasksBuilder_.addMessage(index, value);
      }
      return this;
    }
    /**
     * <pre>
     * Workflow tasks with their individual execution state.
     *
     * &#64;internal
     * Tasks represent the atomic units of work within the workflow.
     * Each task has:
     * - task_id: Unique identifier within this execution
     * - task_name: Human-readable name (e.g., "validate_email", "send_notification")
     * - task_type: Type of task (agent_invocation, api_call, approval, etc.)
     * - status: Current task status (PENDING, IN_PROGRESS, COMPLETED, FAILED, SKIPPED)
     * - input: Task input parameters (JSON structure)
     * - output: Task output results (JSON structure, only for COMPLETED tasks)
     * - error: Error message (only for FAILED tasks)
     * - started_at: When task started executing
     * - completed_at: When task finished (COMPLETED/FAILED/SKIPPED)
     *
     * Tasks are populated as the workflow progresses through its task graph.
     * The order in this list reflects the execution order (sequential or parallel).
     *
     * Progress Calculation:
     * - Total tasks: tasks.length
     * - Completed tasks: count(tasks where status in [COMPLETED, FAILED, SKIPPED])
     * - Progress percentage: (completed_tasks / total_tasks) * 100
     * - Current task: tasks.find(status == IN_PROGRESS)
     * </pre>
     *
     * <code>repeated .ai.stigmer.agentic.workflowexecution.v1.WorkflowTask tasks = 2 [json_name = "tasks"];</code>
     */
    public Builder addTasks(
        ai.stigmer.agentic.workflowexecution.v1.WorkflowTask.Builder builderForValue) {
      if (tasksBuilder_ == null) {
        ensureTasksIsMutable();
        tasks_.add(builderForValue.build());
        onChanged();
      } else {
        tasksBuilder_.addMessage(builderForValue.build());
      }
      return this;
    }
    /**
     * <pre>
     * Workflow tasks with their individual execution state.
     *
     * &#64;internal
     * Tasks represent the atomic units of work within the workflow.
     * Each task has:
     * - task_id: Unique identifier within this execution
     * - task_name: Human-readable name (e.g., "validate_email", "send_notification")
     * - task_type: Type of task (agent_invocation, api_call, approval, etc.)
     * - status: Current task status (PENDING, IN_PROGRESS, COMPLETED, FAILED, SKIPPED)
     * - input: Task input parameters (JSON structure)
     * - output: Task output results (JSON structure, only for COMPLETED tasks)
     * - error: Error message (only for FAILED tasks)
     * - started_at: When task started executing
     * - completed_at: When task finished (COMPLETED/FAILED/SKIPPED)
     *
     * Tasks are populated as the workflow progresses through its task graph.
     * The order in this list reflects the execution order (sequential or parallel).
     *
     * Progress Calculation:
     * - Total tasks: tasks.length
     * - Completed tasks: count(tasks where status in [COMPLETED, FAILED, SKIPPED])
     * - Progress percentage: (completed_tasks / total_tasks) * 100
     * - Current task: tasks.find(status == IN_PROGRESS)
     * </pre>
     *
     * <code>repeated .ai.stigmer.agentic.workflowexecution.v1.WorkflowTask tasks = 2 [json_name = "tasks"];</code>
     */
    public Builder addTasks(
        int index, ai.stigmer.agentic.workflowexecution.v1.WorkflowTask.Builder builderForValue) {
      if (tasksBuilder_ == null) {
        ensureTasksIsMutable();
        tasks_.add(index, builderForValue.build());
        onChanged();
      } else {
        tasksBuilder_.addMessage(index, builderForValue.build());
      }
      return this;
    }
    /**
     * <pre>
     * Workflow tasks with their individual execution state.
     *
     * &#64;internal
     * Tasks represent the atomic units of work within the workflow.
     * Each task has:
     * - task_id: Unique identifier within this execution
     * - task_name: Human-readable name (e.g., "validate_email", "send_notification")
     * - task_type: Type of task (agent_invocation, api_call, approval, etc.)
     * - status: Current task status (PENDING, IN_PROGRESS, COMPLETED, FAILED, SKIPPED)
     * - input: Task input parameters (JSON structure)
     * - output: Task output results (JSON structure, only for COMPLETED tasks)
     * - error: Error message (only for FAILED tasks)
     * - started_at: When task started executing
     * - completed_at: When task finished (COMPLETED/FAILED/SKIPPED)
     *
     * Tasks are populated as the workflow progresses through its task graph.
     * The order in this list reflects the execution order (sequential or parallel).
     *
     * Progress Calculation:
     * - Total tasks: tasks.length
     * - Completed tasks: count(tasks where status in [COMPLETED, FAILED, SKIPPED])
     * - Progress percentage: (completed_tasks / total_tasks) * 100
     * - Current task: tasks.find(status == IN_PROGRESS)
     * </pre>
     *
     * <code>repeated .ai.stigmer.agentic.workflowexecution.v1.WorkflowTask tasks = 2 [json_name = "tasks"];</code>
     */
    public Builder addAllTasks(
        java.lang.Iterable<? extends ai.stigmer.agentic.workflowexecution.v1.WorkflowTask> values) {
      if (tasksBuilder_ == null) {
        ensureTasksIsMutable();
        com.google.protobuf.AbstractMessageLite.Builder.addAll(
            values, tasks_);
        onChanged();
      } else {
        tasksBuilder_.addAllMessages(values);
      }
      return this;
    }
    /**
     * <pre>
     * Workflow tasks with their individual execution state.
     *
     * &#64;internal
     * Tasks represent the atomic units of work within the workflow.
     * Each task has:
     * - task_id: Unique identifier within this execution
     * - task_name: Human-readable name (e.g., "validate_email", "send_notification")
     * - task_type: Type of task (agent_invocation, api_call, approval, etc.)
     * - status: Current task status (PENDING, IN_PROGRESS, COMPLETED, FAILED, SKIPPED)
     * - input: Task input parameters (JSON structure)
     * - output: Task output results (JSON structure, only for COMPLETED tasks)
     * - error: Error message (only for FAILED tasks)
     * - started_at: When task started executing
     * - completed_at: When task finished (COMPLETED/FAILED/SKIPPED)
     *
     * Tasks are populated as the workflow progresses through its task graph.
     * The order in this list reflects the execution order (sequential or parallel).
     *
     * Progress Calculation:
     * - Total tasks: tasks.length
     * - Completed tasks: count(tasks where status in [COMPLETED, FAILED, SKIPPED])
     * - Progress percentage: (completed_tasks / total_tasks) * 100
     * - Current task: tasks.find(status == IN_PROGRESS)
     * </pre>
     *
     * <code>repeated .ai.stigmer.agentic.workflowexecution.v1.WorkflowTask tasks = 2 [json_name = "tasks"];</code>
     */
    public Builder clearTasks() {
      if (tasksBuilder_ == null) {
        tasks_ = java.util.Collections.emptyList();
        bitField0_ = (bitField0_ & ~0x00000004);
        onChanged();
      } else {
        tasksBuilder_.clear();
      }
      return this;
    }
    /**
     * <pre>
     * Workflow tasks with their individual execution state.
     *
     * &#64;internal
     * Tasks represent the atomic units of work within the workflow.
     * Each task has:
     * - task_id: Unique identifier within this execution
     * - task_name: Human-readable name (e.g., "validate_email", "send_notification")
     * - task_type: Type of task (agent_invocation, api_call, approval, etc.)
     * - status: Current task status (PENDING, IN_PROGRESS, COMPLETED, FAILED, SKIPPED)
     * - input: Task input parameters (JSON structure)
     * - output: Task output results (JSON structure, only for COMPLETED tasks)
     * - error: Error message (only for FAILED tasks)
     * - started_at: When task started executing
     * - completed_at: When task finished (COMPLETED/FAILED/SKIPPED)
     *
     * Tasks are populated as the workflow progresses through its task graph.
     * The order in this list reflects the execution order (sequential or parallel).
     *
     * Progress Calculation:
     * - Total tasks: tasks.length
     * - Completed tasks: count(tasks where status in [COMPLETED, FAILED, SKIPPED])
     * - Progress percentage: (completed_tasks / total_tasks) * 100
     * - Current task: tasks.find(status == IN_PROGRESS)
     * </pre>
     *
     * <code>repeated .ai.stigmer.agentic.workflowexecution.v1.WorkflowTask tasks = 2 [json_name = "tasks"];</code>
     */
    public Builder removeTasks(int index) {
      if (tasksBuilder_ == null) {
        ensureTasksIsMutable();
        tasks_.remove(index);
        onChanged();
      } else {
        tasksBuilder_.remove(index);
      }
      return this;
    }
    /**
     * <pre>
     * Workflow tasks with their individual execution state.
     *
     * &#64;internal
     * Tasks represent the atomic units of work within the workflow.
     * Each task has:
     * - task_id: Unique identifier within this execution
     * - task_name: Human-readable name (e.g., "validate_email", "send_notification")
     * - task_type: Type of task (agent_invocation, api_call, approval, etc.)
     * - status: Current task status (PENDING, IN_PROGRESS, COMPLETED, FAILED, SKIPPED)
     * - input: Task input parameters (JSON structure)
     * - output: Task output results (JSON structure, only for COMPLETED tasks)
     * - error: Error message (only for FAILED tasks)
     * - started_at: When task started executing
     * - completed_at: When task finished (COMPLETED/FAILED/SKIPPED)
     *
     * Tasks are populated as the workflow progresses through its task graph.
     * The order in this list reflects the execution order (sequential or parallel).
     *
     * Progress Calculation:
     * - Total tasks: tasks.length
     * - Completed tasks: count(tasks where status in [COMPLETED, FAILED, SKIPPED])
     * - Progress percentage: (completed_tasks / total_tasks) * 100
     * - Current task: tasks.find(status == IN_PROGRESS)
     * </pre>
     *
     * <code>repeated .ai.stigmer.agentic.workflowexecution.v1.WorkflowTask tasks = 2 [json_name = "tasks"];</code>
     */
    public ai.stigmer.agentic.workflowexecution.v1.WorkflowTask.Builder getTasksBuilder(
        int index) {
      return internalGetTasksFieldBuilder().getBuilder(index);
    }
    /**
     * <pre>
     * Workflow tasks with their individual execution state.
     *
     * &#64;internal
     * Tasks represent the atomic units of work within the workflow.
     * Each task has:
     * - task_id: Unique identifier within this execution
     * - task_name: Human-readable name (e.g., "validate_email", "send_notification")
     * - task_type: Type of task (agent_invocation, api_call, approval, etc.)
     * - status: Current task status (PENDING, IN_PROGRESS, COMPLETED, FAILED, SKIPPED)
     * - input: Task input parameters (JSON structure)
     * - output: Task output results (JSON structure, only for COMPLETED tasks)
     * - error: Error message (only for FAILED tasks)
     * - started_at: When task started executing
     * - completed_at: When task finished (COMPLETED/FAILED/SKIPPED)
     *
     * Tasks are populated as the workflow progresses through its task graph.
     * The order in this list reflects the execution order (sequential or parallel).
     *
     * Progress Calculation:
     * - Total tasks: tasks.length
     * - Completed tasks: count(tasks where status in [COMPLETED, FAILED, SKIPPED])
     * - Progress percentage: (completed_tasks / total_tasks) * 100
     * - Current task: tasks.find(status == IN_PROGRESS)
     * </pre>
     *
     * <code>repeated .ai.stigmer.agentic.workflowexecution.v1.WorkflowTask tasks = 2 [json_name = "tasks"];</code>
     */
    public ai.stigmer.agentic.workflowexecution.v1.WorkflowTaskOrBuilder getTasksOrBuilder(
        int index) {
      if (tasksBuilder_ == null) {
        return tasks_.get(index);  } else {
        return tasksBuilder_.getMessageOrBuilder(index);
      }
    }
    /**
     * <pre>
     * Workflow tasks with their individual execution state.
     *
     * &#64;internal
     * Tasks represent the atomic units of work within the workflow.
     * Each task has:
     * - task_id: Unique identifier within this execution
     * - task_name: Human-readable name (e.g., "validate_email", "send_notification")
     * - task_type: Type of task (agent_invocation, api_call, approval, etc.)
     * - status: Current task status (PENDING, IN_PROGRESS, COMPLETED, FAILED, SKIPPED)
     * - input: Task input parameters (JSON structure)
     * - output: Task output results (JSON structure, only for COMPLETED tasks)
     * - error: Error message (only for FAILED tasks)
     * - started_at: When task started executing
     * - completed_at: When task finished (COMPLETED/FAILED/SKIPPED)
     *
     * Tasks are populated as the workflow progresses through its task graph.
     * The order in this list reflects the execution order (sequential or parallel).
     *
     * Progress Calculation:
     * - Total tasks: tasks.length
     * - Completed tasks: count(tasks where status in [COMPLETED, FAILED, SKIPPED])
     * - Progress percentage: (completed_tasks / total_tasks) * 100
     * - Current task: tasks.find(status == IN_PROGRESS)
     * </pre>
     *
     * <code>repeated .ai.stigmer.agentic.workflowexecution.v1.WorkflowTask tasks = 2 [json_name = "tasks"];</code>
     */
    public java.util.List<? extends ai.stigmer.agentic.workflowexecution.v1.WorkflowTaskOrBuilder> 
         getTasksOrBuilderList() {
      if (tasksBuilder_ != null) {
        return tasksBuilder_.getMessageOrBuilderList();
      } else {
        return java.util.Collections.unmodifiableList(tasks_);
      }
    }
    /**
     * <pre>
     * Workflow tasks with their individual execution state.
     *
     * &#64;internal
     * Tasks represent the atomic units of work within the workflow.
     * Each task has:
     * - task_id: Unique identifier within this execution
     * - task_name: Human-readable name (e.g., "validate_email", "send_notification")
     * - task_type: Type of task (agent_invocation, api_call, approval, etc.)
     * - status: Current task status (PENDING, IN_PROGRESS, COMPLETED, FAILED, SKIPPED)
     * - input: Task input parameters (JSON structure)
     * - output: Task output results (JSON structure, only for COMPLETED tasks)
     * - error: Error message (only for FAILED tasks)
     * - started_at: When task started executing
     * - completed_at: When task finished (COMPLETED/FAILED/SKIPPED)
     *
     * Tasks are populated as the workflow progresses through its task graph.
     * The order in this list reflects the execution order (sequential or parallel).
     *
     * Progress Calculation:
     * - Total tasks: tasks.length
     * - Completed tasks: count(tasks where status in [COMPLETED, FAILED, SKIPPED])
     * - Progress percentage: (completed_tasks / total_tasks) * 100
     * - Current task: tasks.find(status == IN_PROGRESS)
     * </pre>
     *
     * <code>repeated .ai.stigmer.agentic.workflowexecution.v1.WorkflowTask tasks = 2 [json_name = "tasks"];</code>
     */
    public ai.stigmer.agentic.workflowexecution.v1.WorkflowTask.Builder addTasksBuilder() {
      return internalGetTasksFieldBuilder().addBuilder(
          ai.stigmer.agentic.workflowexecution.v1.WorkflowTask.getDefaultInstance());
    }
    /**
     * <pre>
     * Workflow tasks with their individual execution state.
     *
     * &#64;internal
     * Tasks represent the atomic units of work within the workflow.
     * Each task has:
     * - task_id: Unique identifier within this execution
     * - task_name: Human-readable name (e.g., "validate_email", "send_notification")
     * - task_type: Type of task (agent_invocation, api_call, approval, etc.)
     * - status: Current task status (PENDING, IN_PROGRESS, COMPLETED, FAILED, SKIPPED)
     * - input: Task input parameters (JSON structure)
     * - output: Task output results (JSON structure, only for COMPLETED tasks)
     * - error: Error message (only for FAILED tasks)
     * - started_at: When task started executing
     * - completed_at: When task finished (COMPLETED/FAILED/SKIPPED)
     *
     * Tasks are populated as the workflow progresses through its task graph.
     * The order in this list reflects the execution order (sequential or parallel).
     *
     * Progress Calculation:
     * - Total tasks: tasks.length
     * - Completed tasks: count(tasks where status in [COMPLETED, FAILED, SKIPPED])
     * - Progress percentage: (completed_tasks / total_tasks) * 100
     * - Current task: tasks.find(status == IN_PROGRESS)
     * </pre>
     *
     * <code>repeated .ai.stigmer.agentic.workflowexecution.v1.WorkflowTask tasks = 2 [json_name = "tasks"];</code>
     */
    public ai.stigmer.agentic.workflowexecution.v1.WorkflowTask.Builder addTasksBuilder(
        int index) {
      return internalGetTasksFieldBuilder().addBuilder(
          index, ai.stigmer.agentic.workflowexecution.v1.WorkflowTask.getDefaultInstance());
    }
    /**
     * <pre>
     * Workflow tasks with their individual execution state.
     *
     * &#64;internal
     * Tasks represent the atomic units of work within the workflow.
     * Each task has:
     * - task_id: Unique identifier within this execution
     * - task_name: Human-readable name (e.g., "validate_email", "send_notification")
     * - task_type: Type of task (agent_invocation, api_call, approval, etc.)
     * - status: Current task status (PENDING, IN_PROGRESS, COMPLETED, FAILED, SKIPPED)
     * - input: Task input parameters (JSON structure)
     * - output: Task output results (JSON structure, only for COMPLETED tasks)
     * - error: Error message (only for FAILED tasks)
     * - started_at: When task started executing
     * - completed_at: When task finished (COMPLETED/FAILED/SKIPPED)
     *
     * Tasks are populated as the workflow progresses through its task graph.
     * The order in this list reflects the execution order (sequential or parallel).
     *
     * Progress Calculation:
     * - Total tasks: tasks.length
     * - Completed tasks: count(tasks where status in [COMPLETED, FAILED, SKIPPED])
     * - Progress percentage: (completed_tasks / total_tasks) * 100
     * - Current task: tasks.find(status == IN_PROGRESS)
     * </pre>
     *
     * <code>repeated .ai.stigmer.agentic.workflowexecution.v1.WorkflowTask tasks = 2 [json_name = "tasks"];</code>
     */
    public java.util.List<ai.stigmer.agentic.workflowexecution.v1.WorkflowTask.Builder> 
         getTasksBuilderList() {
      return internalGetTasksFieldBuilder().getBuilderList();
    }
    private com.google.protobuf.RepeatedFieldBuilder<
        ai.stigmer.agentic.workflowexecution.v1.WorkflowTask, ai.stigmer.agentic.workflowexecution.v1.WorkflowTask.Builder, ai.stigmer.agentic.workflowexecution.v1.WorkflowTaskOrBuilder> 
        internalGetTasksFieldBuilder() {
      if (tasksBuilder_ == null) {
        tasksBuilder_ = new com.google.protobuf.RepeatedFieldBuilder<
            ai.stigmer.agentic.workflowexecution.v1.WorkflowTask, ai.stigmer.agentic.workflowexecution.v1.WorkflowTask.Builder, ai.stigmer.agentic.workflowexecution.v1.WorkflowTaskOrBuilder>(
                tasks_,
                ((bitField0_ & 0x00000004) != 0),
                getParentForChildren(),
                isClean());
        tasks_ = null;
      }
      return tasksBuilder_;
    }

    private com.google.protobuf.Struct output_;
    private com.google.protobuf.SingleFieldBuilder<
        com.google.protobuf.Struct, com.google.protobuf.Struct.Builder, com.google.protobuf.StructOrBuilder> outputBuilder_;
    /**
     * <pre>
     * Final workflow output, populated only when phase is EXECUTION_COMPLETED.
     *
     * &#64;internal
     * The output structure is workflow-specific and defined by the Workflow template.
     * Common patterns:
     * - API response data (e.g., created user ID, order confirmation)
     * - Aggregated results from multiple tasks
     * - Links to generated artifacts (reports, files)
     * - Summary statistics (items processed, duration)
     * </pre>
     *
     * <code>.google.protobuf.Struct output = 3 [json_name = "output"];</code>
     * @return Whether the output field is set.
     */
    public boolean hasOutput() {
      return ((bitField0_ & 0x00000008) != 0);
    }
    /**
     * <pre>
     * Final workflow output, populated only when phase is EXECUTION_COMPLETED.
     *
     * &#64;internal
     * The output structure is workflow-specific and defined by the Workflow template.
     * Common patterns:
     * - API response data (e.g., created user ID, order confirmation)
     * - Aggregated results from multiple tasks
     * - Links to generated artifacts (reports, files)
     * - Summary statistics (items processed, duration)
     * </pre>
     *
     * <code>.google.protobuf.Struct output = 3 [json_name = "output"];</code>
     * @return The output.
     */
    public com.google.protobuf.Struct getOutput() {
      if (outputBuilder_ == null) {
        return output_ == null ? com.google.protobuf.Struct.getDefaultInstance() : output_;
      } else {
        return outputBuilder_.getMessage();
      }
    }
    /**
     * <pre>
     * Final workflow output, populated only when phase is EXECUTION_COMPLETED.
     *
     * &#64;internal
     * The output structure is workflow-specific and defined by the Workflow template.
     * Common patterns:
     * - API response data (e.g., created user ID, order confirmation)
     * - Aggregated results from multiple tasks
     * - Links to generated artifacts (reports, files)
     * - Summary statistics (items processed, duration)
     * </pre>
     *
     * <code>.google.protobuf.Struct output = 3 [json_name = "output"];</code>
     */
    public Builder setOutput(com.google.protobuf.Struct value) {
      if (outputBuilder_ == null) {
        if (value == null) {
          throw new NullPointerException();
        }
        output_ = value;
      } else {
        outputBuilder_.setMessage(value);
      }
      bitField0_ |= 0x00000008;
      onChanged();
      return this;
    }
    /**
     * <pre>
     * Final workflow output, populated only when phase is EXECUTION_COMPLETED.
     *
     * &#64;internal
     * The output structure is workflow-specific and defined by the Workflow template.
     * Common patterns:
     * - API response data (e.g., created user ID, order confirmation)
     * - Aggregated results from multiple tasks
     * - Links to generated artifacts (reports, files)
     * - Summary statistics (items processed, duration)
     * </pre>
     *
     * <code>.google.protobuf.Struct output = 3 [json_name = "output"];</code>
     */
    public Builder setOutput(
        com.google.protobuf.Struct.Builder builderForValue) {
      if (outputBuilder_ == null) {
        output_ = builderForValue.build();
      } else {
        outputBuilder_.setMessage(builderForValue.build());
      }
      bitField0_ |= 0x00000008;
      onChanged();
      return this;
    }
    /**
     * <pre>
     * Final workflow output, populated only when phase is EXECUTION_COMPLETED.
     *
     * &#64;internal
     * The output structure is workflow-specific and defined by the Workflow template.
     * Common patterns:
     * - API response data (e.g., created user ID, order confirmation)
     * - Aggregated results from multiple tasks
     * - Links to generated artifacts (reports, files)
     * - Summary statistics (items processed, duration)
     * </pre>
     *
     * <code>.google.protobuf.Struct output = 3 [json_name = "output"];</code>
     */
    public Builder mergeOutput(com.google.protobuf.Struct value) {
      if (outputBuilder_ == null) {
        if (((bitField0_ & 0x00000008) != 0) &&
          output_ != null &&
          output_ != com.google.protobuf.Struct.getDefaultInstance()) {
          getOutputBuilder().mergeFrom(value);
        } else {
          output_ = value;
        }
      } else {
        outputBuilder_.mergeFrom(value);
      }
      if (output_ != null) {
        bitField0_ |= 0x00000008;
        onChanged();
      }
      return this;
    }
    /**
     * <pre>
     * Final workflow output, populated only when phase is EXECUTION_COMPLETED.
     *
     * &#64;internal
     * The output structure is workflow-specific and defined by the Workflow template.
     * Common patterns:
     * - API response data (e.g., created user ID, order confirmation)
     * - Aggregated results from multiple tasks
     * - Links to generated artifacts (reports, files)
     * - Summary statistics (items processed, duration)
     * </pre>
     *
     * <code>.google.protobuf.Struct output = 3 [json_name = "output"];</code>
     */
    public Builder clearOutput() {
      bitField0_ = (bitField0_ & ~0x00000008);
      output_ = null;
      if (outputBuilder_ != null) {
        outputBuilder_.dispose();
        outputBuilder_ = null;
      }
      onChanged();
      return this;
    }
    /**
     * <pre>
     * Final workflow output, populated only when phase is EXECUTION_COMPLETED.
     *
     * &#64;internal
     * The output structure is workflow-specific and defined by the Workflow template.
     * Common patterns:
     * - API response data (e.g., created user ID, order confirmation)
     * - Aggregated results from multiple tasks
     * - Links to generated artifacts (reports, files)
     * - Summary statistics (items processed, duration)
     * </pre>
     *
     * <code>.google.protobuf.Struct output = 3 [json_name = "output"];</code>
     */
    public com.google.protobuf.Struct.Builder getOutputBuilder() {
      bitField0_ |= 0x00000008;
      onChanged();
      return internalGetOutputFieldBuilder().getBuilder();
    }
    /**
     * <pre>
     * Final workflow output, populated only when phase is EXECUTION_COMPLETED.
     *
     * &#64;internal
     * The output structure is workflow-specific and defined by the Workflow template.
     * Common patterns:
     * - API response data (e.g., created user ID, order confirmation)
     * - Aggregated results from multiple tasks
     * - Links to generated artifacts (reports, files)
     * - Summary statistics (items processed, duration)
     * </pre>
     *
     * <code>.google.protobuf.Struct output = 3 [json_name = "output"];</code>
     */
    public com.google.protobuf.StructOrBuilder getOutputOrBuilder() {
      if (outputBuilder_ != null) {
        return outputBuilder_.getMessageOrBuilder();
      } else {
        return output_ == null ?
            com.google.protobuf.Struct.getDefaultInstance() : output_;
      }
    }
    /**
     * <pre>
     * Final workflow output, populated only when phase is EXECUTION_COMPLETED.
     *
     * &#64;internal
     * The output structure is workflow-specific and defined by the Workflow template.
     * Common patterns:
     * - API response data (e.g., created user ID, order confirmation)
     * - Aggregated results from multiple tasks
     * - Links to generated artifacts (reports, files)
     * - Summary statistics (items processed, duration)
     * </pre>
     *
     * <code>.google.protobuf.Struct output = 3 [json_name = "output"];</code>
     */
    private com.google.protobuf.SingleFieldBuilder<
        com.google.protobuf.Struct, com.google.protobuf.Struct.Builder, com.google.protobuf.StructOrBuilder> 
        internalGetOutputFieldBuilder() {
      if (outputBuilder_ == null) {
        outputBuilder_ = new com.google.protobuf.SingleFieldBuilder<
            com.google.protobuf.Struct, com.google.protobuf.Struct.Builder, com.google.protobuf.StructOrBuilder>(
                getOutput(),
                getParentForChildren(),
                isClean());
        output_ = null;
      }
      return outputBuilder_;
    }

    private java.lang.Object error_ = "";
    /**
     * <pre>
     * Error message, populated only when phase is EXECUTION_FAILED.
     *
     * &#64;internal
     * Contains a human-readable description of what went wrong.
     *
     * Error message includes:
     * - What failed (which task or workflow step)
     * - Why it failed (validation error, API error, timeout, etc.)
     * - How to fix it (if known)
     *
     * For detailed debugging, inspect tasks[].error for task-specific error messages.
     * </pre>
     *
     * <code>string error = 4 [json_name = "error"];</code>
     * @return The error.
     */
    public java.lang.String getError() {
      java.lang.Object ref = error_;
      if (!(ref instanceof java.lang.String)) {
        com.google.protobuf.ByteString bs =
            (com.google.protobuf.ByteString) ref;
        java.lang.String s = bs.toStringUtf8();
        error_ = s;
        return s;
      } else {
        return (java.lang.String) ref;
      }
    }
    /**
     * <pre>
     * Error message, populated only when phase is EXECUTION_FAILED.
     *
     * &#64;internal
     * Contains a human-readable description of what went wrong.
     *
     * Error message includes:
     * - What failed (which task or workflow step)
     * - Why it failed (validation error, API error, timeout, etc.)
     * - How to fix it (if known)
     *
     * For detailed debugging, inspect tasks[].error for task-specific error messages.
     * </pre>
     *
     * <code>string error = 4 [json_name = "error"];</code>
     * @return The bytes for error.
     */
    public com.google.protobuf.ByteString
        getErrorBytes() {
      java.lang.Object ref = error_;
      if (ref instanceof String) {
        com.google.protobuf.ByteString b = 
            com.google.protobuf.ByteString.copyFromUtf8(
                (java.lang.String) ref);
        error_ = b;
        return b;
      } else {
        return (com.google.protobuf.ByteString) ref;
      }
    }
    /**
     * <pre>
     * Error message, populated only when phase is EXECUTION_FAILED.
     *
     * &#64;internal
     * Contains a human-readable description of what went wrong.
     *
     * Error message includes:
     * - What failed (which task or workflow step)
     * - Why it failed (validation error, API error, timeout, etc.)
     * - How to fix it (if known)
     *
     * For detailed debugging, inspect tasks[].error for task-specific error messages.
     * </pre>
     *
     * <code>string error = 4 [json_name = "error"];</code>
     * @param value The error to set.
     * @return This builder for chaining.
     */
    public Builder setError(
        java.lang.String value) {
      if (value == null) { throw new NullPointerException(); }
      error_ = value;
      bitField0_ |= 0x00000010;
      onChanged();
      return this;
    }
    /**
     * <pre>
     * Error message, populated only when phase is EXECUTION_FAILED.
     *
     * &#64;internal
     * Contains a human-readable description of what went wrong.
     *
     * Error message includes:
     * - What failed (which task or workflow step)
     * - Why it failed (validation error, API error, timeout, etc.)
     * - How to fix it (if known)
     *
     * For detailed debugging, inspect tasks[].error for task-specific error messages.
     * </pre>
     *
     * <code>string error = 4 [json_name = "error"];</code>
     * @return This builder for chaining.
     */
    public Builder clearError() {
      error_ = getDefaultInstance().getError();
      bitField0_ = (bitField0_ & ~0x00000010);
      onChanged();
      return this;
    }
    /**
     * <pre>
     * Error message, populated only when phase is EXECUTION_FAILED.
     *
     * &#64;internal
     * Contains a human-readable description of what went wrong.
     *
     * Error message includes:
     * - What failed (which task or workflow step)
     * - Why it failed (validation error, API error, timeout, etc.)
     * - How to fix it (if known)
     *
     * For detailed debugging, inspect tasks[].error for task-specific error messages.
     * </pre>
     *
     * <code>string error = 4 [json_name = "error"];</code>
     * @param value The bytes for error to set.
     * @return This builder for chaining.
     */
    public Builder setErrorBytes(
        com.google.protobuf.ByteString value) {
      if (value == null) { throw new NullPointerException(); }
      checkByteStringIsUtf8(value);
      error_ = value;
      bitField0_ |= 0x00000010;
      onChanged();
      return this;
    }

    private java.lang.Object startedAt_ = "";
    /**
     * <pre>
     * ISO 8601 timestamp when execution started processing.
     *
     * &#64;internal
     * Set when phase transitions from PENDING to IN_PROGRESS.
     * Format: "YYYY-MM-DDTHH:MM:SSZ" (UTC timezone)
     * </pre>
     *
     * <code>string started_at = 5 [json_name = "startedAt"];</code>
     * @return The startedAt.
     */
    public java.lang.String getStartedAt() {
      java.lang.Object ref = startedAt_;
      if (!(ref instanceof java.lang.String)) {
        com.google.protobuf.ByteString bs =
            (com.google.protobuf.ByteString) ref;
        java.lang.String s = bs.toStringUtf8();
        startedAt_ = s;
        return s;
      } else {
        return (java.lang.String) ref;
      }
    }
    /**
     * <pre>
     * ISO 8601 timestamp when execution started processing.
     *
     * &#64;internal
     * Set when phase transitions from PENDING to IN_PROGRESS.
     * Format: "YYYY-MM-DDTHH:MM:SSZ" (UTC timezone)
     * </pre>
     *
     * <code>string started_at = 5 [json_name = "startedAt"];</code>
     * @return The bytes for startedAt.
     */
    public com.google.protobuf.ByteString
        getStartedAtBytes() {
      java.lang.Object ref = startedAt_;
      if (ref instanceof String) {
        com.google.protobuf.ByteString b = 
            com.google.protobuf.ByteString.copyFromUtf8(
                (java.lang.String) ref);
        startedAt_ = b;
        return b;
      } else {
        return (com.google.protobuf.ByteString) ref;
      }
    }
    /**
     * <pre>
     * ISO 8601 timestamp when execution started processing.
     *
     * &#64;internal
     * Set when phase transitions from PENDING to IN_PROGRESS.
     * Format: "YYYY-MM-DDTHH:MM:SSZ" (UTC timezone)
     * </pre>
     *
     * <code>string started_at = 5 [json_name = "startedAt"];</code>
     * @param value The startedAt to set.
     * @return This builder for chaining.
     */
    public Builder setStartedAt(
        java.lang.String value) {
      if (value == null) { throw new NullPointerException(); }
      startedAt_ = value;
      bitField0_ |= 0x00000020;
      onChanged();
      return this;
    }
    /**
     * <pre>
     * ISO 8601 timestamp when execution started processing.
     *
     * &#64;internal
     * Set when phase transitions from PENDING to IN_PROGRESS.
     * Format: "YYYY-MM-DDTHH:MM:SSZ" (UTC timezone)
     * </pre>
     *
     * <code>string started_at = 5 [json_name = "startedAt"];</code>
     * @return This builder for chaining.
     */
    public Builder clearStartedAt() {
      startedAt_ = getDefaultInstance().getStartedAt();
      bitField0_ = (bitField0_ & ~0x00000020);
      onChanged();
      return this;
    }
    /**
     * <pre>
     * ISO 8601 timestamp when execution started processing.
     *
     * &#64;internal
     * Set when phase transitions from PENDING to IN_PROGRESS.
     * Format: "YYYY-MM-DDTHH:MM:SSZ" (UTC timezone)
     * </pre>
     *
     * <code>string started_at = 5 [json_name = "startedAt"];</code>
     * @param value The bytes for startedAt to set.
     * @return This builder for chaining.
     */
    public Builder setStartedAtBytes(
        com.google.protobuf.ByteString value) {
      if (value == null) { throw new NullPointerException(); }
      checkByteStringIsUtf8(value);
      startedAt_ = value;
      bitField0_ |= 0x00000020;
      onChanged();
      return this;
    }

    private java.lang.Object completedAt_ = "";
    /**
     * <pre>
     * ISO 8601 timestamp when execution reached a terminal state.
     *
     * &#64;internal
     * Set when the workflow reaches a terminal state:
     * - EXECUTION_COMPLETED: Successfully finished
     * - EXECUTION_FAILED: Failed during execution
     * - EXECUTION_CANCELLED: Cancelled by user or system
     *
     * Not set for PENDING or IN_PROGRESS executions.
     * Format: "YYYY-MM-DDTHH:MM:SSZ" (UTC timezone)
     * </pre>
     *
     * <code>string completed_at = 6 [json_name = "completedAt"];</code>
     * @return The completedAt.
     */
    public java.lang.String getCompletedAt() {
      java.lang.Object ref = completedAt_;
      if (!(ref instanceof java.lang.String)) {
        com.google.protobuf.ByteString bs =
            (com.google.protobuf.ByteString) ref;
        java.lang.String s = bs.toStringUtf8();
        completedAt_ = s;
        return s;
      } else {
        return (java.lang.String) ref;
      }
    }
    /**
     * <pre>
     * ISO 8601 timestamp when execution reached a terminal state.
     *
     * &#64;internal
     * Set when the workflow reaches a terminal state:
     * - EXECUTION_COMPLETED: Successfully finished
     * - EXECUTION_FAILED: Failed during execution
     * - EXECUTION_CANCELLED: Cancelled by user or system
     *
     * Not set for PENDING or IN_PROGRESS executions.
     * Format: "YYYY-MM-DDTHH:MM:SSZ" (UTC timezone)
     * </pre>
     *
     * <code>string completed_at = 6 [json_name = "completedAt"];</code>
     * @return The bytes for completedAt.
     */
    public com.google.protobuf.ByteString
        getCompletedAtBytes() {
      java.lang.Object ref = completedAt_;
      if (ref instanceof String) {
        com.google.protobuf.ByteString b = 
            com.google.protobuf.ByteString.copyFromUtf8(
                (java.lang.String) ref);
        completedAt_ = b;
        return b;
      } else {
        return (com.google.protobuf.ByteString) ref;
      }
    }
    /**
     * <pre>
     * ISO 8601 timestamp when execution reached a terminal state.
     *
     * &#64;internal
     * Set when the workflow reaches a terminal state:
     * - EXECUTION_COMPLETED: Successfully finished
     * - EXECUTION_FAILED: Failed during execution
     * - EXECUTION_CANCELLED: Cancelled by user or system
     *
     * Not set for PENDING or IN_PROGRESS executions.
     * Format: "YYYY-MM-DDTHH:MM:SSZ" (UTC timezone)
     * </pre>
     *
     * <code>string completed_at = 6 [json_name = "completedAt"];</code>
     * @param value The completedAt to set.
     * @return This builder for chaining.
     */
    public Builder setCompletedAt(
        java.lang.String value) {
      if (value == null) { throw new NullPointerException(); }
      completedAt_ = value;
      bitField0_ |= 0x00000040;
      onChanged();
      return this;
    }
    /**
     * <pre>
     * ISO 8601 timestamp when execution reached a terminal state.
     *
     * &#64;internal
     * Set when the workflow reaches a terminal state:
     * - EXECUTION_COMPLETED: Successfully finished
     * - EXECUTION_FAILED: Failed during execution
     * - EXECUTION_CANCELLED: Cancelled by user or system
     *
     * Not set for PENDING or IN_PROGRESS executions.
     * Format: "YYYY-MM-DDTHH:MM:SSZ" (UTC timezone)
     * </pre>
     *
     * <code>string completed_at = 6 [json_name = "completedAt"];</code>
     * @return This builder for chaining.
     */
    public Builder clearCompletedAt() {
      completedAt_ = getDefaultInstance().getCompletedAt();
      bitField0_ = (bitField0_ & ~0x00000040);
      onChanged();
      return this;
    }
    /**
     * <pre>
     * ISO 8601 timestamp when execution reached a terminal state.
     *
     * &#64;internal
     * Set when the workflow reaches a terminal state:
     * - EXECUTION_COMPLETED: Successfully finished
     * - EXECUTION_FAILED: Failed during execution
     * - EXECUTION_CANCELLED: Cancelled by user or system
     *
     * Not set for PENDING or IN_PROGRESS executions.
     * Format: "YYYY-MM-DDTHH:MM:SSZ" (UTC timezone)
     * </pre>
     *
     * <code>string completed_at = 6 [json_name = "completedAt"];</code>
     * @param value The bytes for completedAt to set.
     * @return This builder for chaining.
     */
    public Builder setCompletedAtBytes(
        com.google.protobuf.ByteString value) {
      if (value == null) { throw new NullPointerException(); }
      checkByteStringIsUtf8(value);
      completedAt_ = value;
      bitField0_ |= 0x00000040;
      onChanged();
      return this;
    }

    private java.lang.Object temporalWorkflowId_ = "";
    /**
     * <pre>
     * Correlation ID for the underlying workflow engine.
     *
     * &#64;internal
     * This is the workflow ID in Temporal, used for:
     * - Correlation between Stigmer and Temporal (for debugging)
     * - Querying Temporal directly (for advanced troubleshooting)
     * - Signaling or cancelling Temporal workflows
     *
     * Format: Typically "{workflow_instance_id}-{execution_id}" or a UUID
     * Example: "wfi_prod-deploy-wfx_abc123xyz456" or "temporal-wf-uuid-12345"
     *
     * This field is optional and only relevant when Temporal is used as the execution engine.
     * Other workflow engines (Step Functions, Argo, etc.) may use different correlation IDs.
     * </pre>
     *
     * <code>string temporal_workflow_id = 7 [json_name = "temporalWorkflowId"];</code>
     * @return The temporalWorkflowId.
     */
    public java.lang.String getTemporalWorkflowId() {
      java.lang.Object ref = temporalWorkflowId_;
      if (!(ref instanceof java.lang.String)) {
        com.google.protobuf.ByteString bs =
            (com.google.protobuf.ByteString) ref;
        java.lang.String s = bs.toStringUtf8();
        temporalWorkflowId_ = s;
        return s;
      } else {
        return (java.lang.String) ref;
      }
    }
    /**
     * <pre>
     * Correlation ID for the underlying workflow engine.
     *
     * &#64;internal
     * This is the workflow ID in Temporal, used for:
     * - Correlation between Stigmer and Temporal (for debugging)
     * - Querying Temporal directly (for advanced troubleshooting)
     * - Signaling or cancelling Temporal workflows
     *
     * Format: Typically "{workflow_instance_id}-{execution_id}" or a UUID
     * Example: "wfi_prod-deploy-wfx_abc123xyz456" or "temporal-wf-uuid-12345"
     *
     * This field is optional and only relevant when Temporal is used as the execution engine.
     * Other workflow engines (Step Functions, Argo, etc.) may use different correlation IDs.
     * </pre>
     *
     * <code>string temporal_workflow_id = 7 [json_name = "temporalWorkflowId"];</code>
     * @return The bytes for temporalWorkflowId.
     */
    public com.google.protobuf.ByteString
        getTemporalWorkflowIdBytes() {
      java.lang.Object ref = temporalWorkflowId_;
      if (ref instanceof String) {
        com.google.protobuf.ByteString b = 
            com.google.protobuf.ByteString.copyFromUtf8(
                (java.lang.String) ref);
        temporalWorkflowId_ = b;
        return b;
      } else {
        return (com.google.protobuf.ByteString) ref;
      }
    }
    /**
     * <pre>
     * Correlation ID for the underlying workflow engine.
     *
     * &#64;internal
     * This is the workflow ID in Temporal, used for:
     * - Correlation between Stigmer and Temporal (for debugging)
     * - Querying Temporal directly (for advanced troubleshooting)
     * - Signaling or cancelling Temporal workflows
     *
     * Format: Typically "{workflow_instance_id}-{execution_id}" or a UUID
     * Example: "wfi_prod-deploy-wfx_abc123xyz456" or "temporal-wf-uuid-12345"
     *
     * This field is optional and only relevant when Temporal is used as the execution engine.
     * Other workflow engines (Step Functions, Argo, etc.) may use different correlation IDs.
     * </pre>
     *
     * <code>string temporal_workflow_id = 7 [json_name = "temporalWorkflowId"];</code>
     * @param value The temporalWorkflowId to set.
     * @return This builder for chaining.
     */
    public Builder setTemporalWorkflowId(
        java.lang.String value) {
      if (value == null) { throw new NullPointerException(); }
      temporalWorkflowId_ = value;
      bitField0_ |= 0x00000080;
      onChanged();
      return this;
    }
    /**
     * <pre>
     * Correlation ID for the underlying workflow engine.
     *
     * &#64;internal
     * This is the workflow ID in Temporal, used for:
     * - Correlation between Stigmer and Temporal (for debugging)
     * - Querying Temporal directly (for advanced troubleshooting)
     * - Signaling or cancelling Temporal workflows
     *
     * Format: Typically "{workflow_instance_id}-{execution_id}" or a UUID
     * Example: "wfi_prod-deploy-wfx_abc123xyz456" or "temporal-wf-uuid-12345"
     *
     * This field is optional and only relevant when Temporal is used as the execution engine.
     * Other workflow engines (Step Functions, Argo, etc.) may use different correlation IDs.
     * </pre>
     *
     * <code>string temporal_workflow_id = 7 [json_name = "temporalWorkflowId"];</code>
     * @return This builder for chaining.
     */
    public Builder clearTemporalWorkflowId() {
      temporalWorkflowId_ = getDefaultInstance().getTemporalWorkflowId();
      bitField0_ = (bitField0_ & ~0x00000080);
      onChanged();
      return this;
    }
    /**
     * <pre>
     * Correlation ID for the underlying workflow engine.
     *
     * &#64;internal
     * This is the workflow ID in Temporal, used for:
     * - Correlation between Stigmer and Temporal (for debugging)
     * - Querying Temporal directly (for advanced troubleshooting)
     * - Signaling or cancelling Temporal workflows
     *
     * Format: Typically "{workflow_instance_id}-{execution_id}" or a UUID
     * Example: "wfi_prod-deploy-wfx_abc123xyz456" or "temporal-wf-uuid-12345"
     *
     * This field is optional and only relevant when Temporal is used as the execution engine.
     * Other workflow engines (Step Functions, Argo, etc.) may use different correlation IDs.
     * </pre>
     *
     * <code>string temporal_workflow_id = 7 [json_name = "temporalWorkflowId"];</code>
     * @param value The bytes for temporalWorkflowId to set.
     * @return This builder for chaining.
     */
    public Builder setTemporalWorkflowIdBytes(
        com.google.protobuf.ByteString value) {
      if (value == null) { throw new NullPointerException(); }
      checkByteStringIsUtf8(value);
      temporalWorkflowId_ = value;
      bitField0_ |= 0x00000080;
      onChanged();
      return this;
    }

    private java.util.List<ai.stigmer.agentic.workflowexecution.v1.WorkflowPendingApproval> pendingApprovals_ =
      java.util.Collections.emptyList();
    private void ensurePendingApprovalsIsMutable() {
      if (!((bitField0_ & 0x00000100) != 0)) {
        pendingApprovals_ = new java.util.ArrayList<ai.stigmer.agentic.workflowexecution.v1.WorkflowPendingApproval>(pendingApprovals_);
        bitField0_ |= 0x00000100;
       }
    }

    private com.google.protobuf.RepeatedFieldBuilder<
        ai.stigmer.agentic.workflowexecution.v1.WorkflowPendingApproval, ai.stigmer.agentic.workflowexecution.v1.WorkflowPendingApproval.Builder, ai.stigmer.agentic.workflowexecution.v1.WorkflowPendingApprovalOrBuilder> pendingApprovalsBuilder_;

    /**
     * <pre>
     * Pending approvals from child agent tool executions.
     *
     * &#64;internal
     * Populated when workflow tasks invoke agents that enter
     * EXECUTION_WAITING_FOR_APPROVAL phase. This surfaces all approval
     * requests at the workflow level for UI visibility.
     *
     * Guarded Update Protocol:
     * This field is only modified when UpdateStatusInput.update_pending_approvals
     * is explicitly set to true. Normal event emissions (which don't concern
     * approvals) leave this field untouched, preventing race conditions between
     * concurrent status writers.
     *
     * Only call-agent-status manages this field:
     * - Non-empty list: child agent(s) need approval
     * - Empty list + update_pending_approvals=true: all approvals resolved
     *
     * Parallel Agents:
     * When multiple child agents run in parallel, entries from different children
     * accumulate in this list. Each entry's child_agent_execution_id distinguishes
     * the source.
     * </pre>
     *
     * <code>repeated .ai.stigmer.agentic.workflowexecution.v1.WorkflowPendingApproval pending_approvals = 9 [json_name = "pendingApprovals"];</code>
     */
    public java.util.List<ai.stigmer.agentic.workflowexecution.v1.WorkflowPendingApproval> getPendingApprovalsList() {
      if (pendingApprovalsBuilder_ == null) {
        return java.util.Collections.unmodifiableList(pendingApprovals_);
      } else {
        return pendingApprovalsBuilder_.getMessageList();
      }
    }
    /**
     * <pre>
     * Pending approvals from child agent tool executions.
     *
     * &#64;internal
     * Populated when workflow tasks invoke agents that enter
     * EXECUTION_WAITING_FOR_APPROVAL phase. This surfaces all approval
     * requests at the workflow level for UI visibility.
     *
     * Guarded Update Protocol:
     * This field is only modified when UpdateStatusInput.update_pending_approvals
     * is explicitly set to true. Normal event emissions (which don't concern
     * approvals) leave this field untouched, preventing race conditions between
     * concurrent status writers.
     *
     * Only call-agent-status manages this field:
     * - Non-empty list: child agent(s) need approval
     * - Empty list + update_pending_approvals=true: all approvals resolved
     *
     * Parallel Agents:
     * When multiple child agents run in parallel, entries from different children
     * accumulate in this list. Each entry's child_agent_execution_id distinguishes
     * the source.
     * </pre>
     *
     * <code>repeated .ai.stigmer.agentic.workflowexecution.v1.WorkflowPendingApproval pending_approvals = 9 [json_name = "pendingApprovals"];</code>
     */
    public int getPendingApprovalsCount() {
      if (pendingApprovalsBuilder_ == null) {
        return pendingApprovals_.size();
      } else {
        return pendingApprovalsBuilder_.getCount();
      }
    }
    /**
     * <pre>
     * Pending approvals from child agent tool executions.
     *
     * &#64;internal
     * Populated when workflow tasks invoke agents that enter
     * EXECUTION_WAITING_FOR_APPROVAL phase. This surfaces all approval
     * requests at the workflow level for UI visibility.
     *
     * Guarded Update Protocol:
     * This field is only modified when UpdateStatusInput.update_pending_approvals
     * is explicitly set to true. Normal event emissions (which don't concern
     * approvals) leave this field untouched, preventing race conditions between
     * concurrent status writers.
     *
     * Only call-agent-status manages this field:
     * - Non-empty list: child agent(s) need approval
     * - Empty list + update_pending_approvals=true: all approvals resolved
     *
     * Parallel Agents:
     * When multiple child agents run in parallel, entries from different children
     * accumulate in this list. Each entry's child_agent_execution_id distinguishes
     * the source.
     * </pre>
     *
     * <code>repeated .ai.stigmer.agentic.workflowexecution.v1.WorkflowPendingApproval pending_approvals = 9 [json_name = "pendingApprovals"];</code>
     */
    public ai.stigmer.agentic.workflowexecution.v1.WorkflowPendingApproval getPendingApprovals(int index) {
      if (pendingApprovalsBuilder_ == null) {
        return pendingApprovals_.get(index);
      } else {
        return pendingApprovalsBuilder_.getMessage(index);
      }
    }
    /**
     * <pre>
     * Pending approvals from child agent tool executions.
     *
     * &#64;internal
     * Populated when workflow tasks invoke agents that enter
     * EXECUTION_WAITING_FOR_APPROVAL phase. This surfaces all approval
     * requests at the workflow level for UI visibility.
     *
     * Guarded Update Protocol:
     * This field is only modified when UpdateStatusInput.update_pending_approvals
     * is explicitly set to true. Normal event emissions (which don't concern
     * approvals) leave this field untouched, preventing race conditions between
     * concurrent status writers.
     *
     * Only call-agent-status manages this field:
     * - Non-empty list: child agent(s) need approval
     * - Empty list + update_pending_approvals=true: all approvals resolved
     *
     * Parallel Agents:
     * When multiple child agents run in parallel, entries from different children
     * accumulate in this list. Each entry's child_agent_execution_id distinguishes
     * the source.
     * </pre>
     *
     * <code>repeated .ai.stigmer.agentic.workflowexecution.v1.WorkflowPendingApproval pending_approvals = 9 [json_name = "pendingApprovals"];</code>
     */
    public Builder setPendingApprovals(
        int index, ai.stigmer.agentic.workflowexecution.v1.WorkflowPendingApproval value) {
      if (pendingApprovalsBuilder_ == null) {
        if (value == null) {
          throw new NullPointerException();
        }
        ensurePendingApprovalsIsMutable();
        pendingApprovals_.set(index, value);
        onChanged();
      } else {
        pendingApprovalsBuilder_.setMessage(index, value);
      }
      return this;
    }
    /**
     * <pre>
     * Pending approvals from child agent tool executions.
     *
     * &#64;internal
     * Populated when workflow tasks invoke agents that enter
     * EXECUTION_WAITING_FOR_APPROVAL phase. This surfaces all approval
     * requests at the workflow level for UI visibility.
     *
     * Guarded Update Protocol:
     * This field is only modified when UpdateStatusInput.update_pending_approvals
     * is explicitly set to true. Normal event emissions (which don't concern
     * approvals) leave this field untouched, preventing race conditions between
     * concurrent status writers.
     *
     * Only call-agent-status manages this field:
     * - Non-empty list: child agent(s) need approval
     * - Empty list + update_pending_approvals=true: all approvals resolved
     *
     * Parallel Agents:
     * When multiple child agents run in parallel, entries from different children
     * accumulate in this list. Each entry's child_agent_execution_id distinguishes
     * the source.
     * </pre>
     *
     * <code>repeated .ai.stigmer.agentic.workflowexecution.v1.WorkflowPendingApproval pending_approvals = 9 [json_name = "pendingApprovals"];</code>
     */
    public Builder setPendingApprovals(
        int index, ai.stigmer.agentic.workflowexecution.v1.WorkflowPendingApproval.Builder builderForValue) {
      if (pendingApprovalsBuilder_ == null) {
        ensurePendingApprovalsIsMutable();
        pendingApprovals_.set(index, builderForValue.build());
        onChanged();
      } else {
        pendingApprovalsBuilder_.setMessage(index, builderForValue.build());
      }
      return this;
    }
    /**
     * <pre>
     * Pending approvals from child agent tool executions.
     *
     * &#64;internal
     * Populated when workflow tasks invoke agents that enter
     * EXECUTION_WAITING_FOR_APPROVAL phase. This surfaces all approval
     * requests at the workflow level for UI visibility.
     *
     * Guarded Update Protocol:
     * This field is only modified when UpdateStatusInput.update_pending_approvals
     * is explicitly set to true. Normal event emissions (which don't concern
     * approvals) leave this field untouched, preventing race conditions between
     * concurrent status writers.
     *
     * Only call-agent-status manages this field:
     * - Non-empty list: child agent(s) need approval
     * - Empty list + update_pending_approvals=true: all approvals resolved
     *
     * Parallel Agents:
     * When multiple child agents run in parallel, entries from different children
     * accumulate in this list. Each entry's child_agent_execution_id distinguishes
     * the source.
     * </pre>
     *
     * <code>repeated .ai.stigmer.agentic.workflowexecution.v1.WorkflowPendingApproval pending_approvals = 9 [json_name = "pendingApprovals"];</code>
     */
    public Builder addPendingApprovals(ai.stigmer.agentic.workflowexecution.v1.WorkflowPendingApproval value) {
      if (pendingApprovalsBuilder_ == null) {
        if (value == null) {
          throw new NullPointerException();
        }
        ensurePendingApprovalsIsMutable();
        pendingApprovals_.add(value);
        onChanged();
      } else {
        pendingApprovalsBuilder_.addMessage(value);
      }
      return this;
    }
    /**
     * <pre>
     * Pending approvals from child agent tool executions.
     *
     * &#64;internal
     * Populated when workflow tasks invoke agents that enter
     * EXECUTION_WAITING_FOR_APPROVAL phase. This surfaces all approval
     * requests at the workflow level for UI visibility.
     *
     * Guarded Update Protocol:
     * This field is only modified when UpdateStatusInput.update_pending_approvals
     * is explicitly set to true. Normal event emissions (which don't concern
     * approvals) leave this field untouched, preventing race conditions between
     * concurrent status writers.
     *
     * Only call-agent-status manages this field:
     * - Non-empty list: child agent(s) need approval
     * - Empty list + update_pending_approvals=true: all approvals resolved
     *
     * Parallel Agents:
     * When multiple child agents run in parallel, entries from different children
     * accumulate in this list. Each entry's child_agent_execution_id distinguishes
     * the source.
     * </pre>
     *
     * <code>repeated .ai.stigmer.agentic.workflowexecution.v1.WorkflowPendingApproval pending_approvals = 9 [json_name = "pendingApprovals"];</code>
     */
    public Builder addPendingApprovals(
        int index, ai.stigmer.agentic.workflowexecution.v1.WorkflowPendingApproval value) {
      if (pendingApprovalsBuilder_ == null) {
        if (value == null) {
          throw new NullPointerException();
        }
        ensurePendingApprovalsIsMutable();
        pendingApprovals_.add(index, value);
        onChanged();
      } else {
        pendingApprovalsBuilder_.addMessage(index, value);
      }
      return this;
    }
    /**
     * <pre>
     * Pending approvals from child agent tool executions.
     *
     * &#64;internal
     * Populated when workflow tasks invoke agents that enter
     * EXECUTION_WAITING_FOR_APPROVAL phase. This surfaces all approval
     * requests at the workflow level for UI visibility.
     *
     * Guarded Update Protocol:
     * This field is only modified when UpdateStatusInput.update_pending_approvals
     * is explicitly set to true. Normal event emissions (which don't concern
     * approvals) leave this field untouched, preventing race conditions between
     * concurrent status writers.
     *
     * Only call-agent-status manages this field:
     * - Non-empty list: child agent(s) need approval
     * - Empty list + update_pending_approvals=true: all approvals resolved
     *
     * Parallel Agents:
     * When multiple child agents run in parallel, entries from different children
     * accumulate in this list. Each entry's child_agent_execution_id distinguishes
     * the source.
     * </pre>
     *
     * <code>repeated .ai.stigmer.agentic.workflowexecution.v1.WorkflowPendingApproval pending_approvals = 9 [json_name = "pendingApprovals"];</code>
     */
    public Builder addPendingApprovals(
        ai.stigmer.agentic.workflowexecution.v1.WorkflowPendingApproval.Builder builderForValue) {
      if (pendingApprovalsBuilder_ == null) {
        ensurePendingApprovalsIsMutable();
        pendingApprovals_.add(builderForValue.build());
        onChanged();
      } else {
        pendingApprovalsBuilder_.addMessage(builderForValue.build());
      }
      return this;
    }
    /**
     * <pre>
     * Pending approvals from child agent tool executions.
     *
     * &#64;internal
     * Populated when workflow tasks invoke agents that enter
     * EXECUTION_WAITING_FOR_APPROVAL phase. This surfaces all approval
     * requests at the workflow level for UI visibility.
     *
     * Guarded Update Protocol:
     * This field is only modified when UpdateStatusInput.update_pending_approvals
     * is explicitly set to true. Normal event emissions (which don't concern
     * approvals) leave this field untouched, preventing race conditions between
     * concurrent status writers.
     *
     * Only call-agent-status manages this field:
     * - Non-empty list: child agent(s) need approval
     * - Empty list + update_pending_approvals=true: all approvals resolved
     *
     * Parallel Agents:
     * When multiple child agents run in parallel, entries from different children
     * accumulate in this list. Each entry's child_agent_execution_id distinguishes
     * the source.
     * </pre>
     *
     * <code>repeated .ai.stigmer.agentic.workflowexecution.v1.WorkflowPendingApproval pending_approvals = 9 [json_name = "pendingApprovals"];</code>
     */
    public Builder addPendingApprovals(
        int index, ai.stigmer.agentic.workflowexecution.v1.WorkflowPendingApproval.Builder builderForValue) {
      if (pendingApprovalsBuilder_ == null) {
        ensurePendingApprovalsIsMutable();
        pendingApprovals_.add(index, builderForValue.build());
        onChanged();
      } else {
        pendingApprovalsBuilder_.addMessage(index, builderForValue.build());
      }
      return this;
    }
    /**
     * <pre>
     * Pending approvals from child agent tool executions.
     *
     * &#64;internal
     * Populated when workflow tasks invoke agents that enter
     * EXECUTION_WAITING_FOR_APPROVAL phase. This surfaces all approval
     * requests at the workflow level for UI visibility.
     *
     * Guarded Update Protocol:
     * This field is only modified when UpdateStatusInput.update_pending_approvals
     * is explicitly set to true. Normal event emissions (which don't concern
     * approvals) leave this field untouched, preventing race conditions between
     * concurrent status writers.
     *
     * Only call-agent-status manages this field:
     * - Non-empty list: child agent(s) need approval
     * - Empty list + update_pending_approvals=true: all approvals resolved
     *
     * Parallel Agents:
     * When multiple child agents run in parallel, entries from different children
     * accumulate in this list. Each entry's child_agent_execution_id distinguishes
     * the source.
     * </pre>
     *
     * <code>repeated .ai.stigmer.agentic.workflowexecution.v1.WorkflowPendingApproval pending_approvals = 9 [json_name = "pendingApprovals"];</code>
     */
    public Builder addAllPendingApprovals(
        java.lang.Iterable<? extends ai.stigmer.agentic.workflowexecution.v1.WorkflowPendingApproval> values) {
      if (pendingApprovalsBuilder_ == null) {
        ensurePendingApprovalsIsMutable();
        com.google.protobuf.AbstractMessageLite.Builder.addAll(
            values, pendingApprovals_);
        onChanged();
      } else {
        pendingApprovalsBuilder_.addAllMessages(values);
      }
      return this;
    }
    /**
     * <pre>
     * Pending approvals from child agent tool executions.
     *
     * &#64;internal
     * Populated when workflow tasks invoke agents that enter
     * EXECUTION_WAITING_FOR_APPROVAL phase. This surfaces all approval
     * requests at the workflow level for UI visibility.
     *
     * Guarded Update Protocol:
     * This field is only modified when UpdateStatusInput.update_pending_approvals
     * is explicitly set to true. Normal event emissions (which don't concern
     * approvals) leave this field untouched, preventing race conditions between
     * concurrent status writers.
     *
     * Only call-agent-status manages this field:
     * - Non-empty list: child agent(s) need approval
     * - Empty list + update_pending_approvals=true: all approvals resolved
     *
     * Parallel Agents:
     * When multiple child agents run in parallel, entries from different children
     * accumulate in this list. Each entry's child_agent_execution_id distinguishes
     * the source.
     * </pre>
     *
     * <code>repeated .ai.stigmer.agentic.workflowexecution.v1.WorkflowPendingApproval pending_approvals = 9 [json_name = "pendingApprovals"];</code>
     */
    public Builder clearPendingApprovals() {
      if (pendingApprovalsBuilder_ == null) {
        pendingApprovals_ = java.util.Collections.emptyList();
        bitField0_ = (bitField0_ & ~0x00000100);
        onChanged();
      } else {
        pendingApprovalsBuilder_.clear();
      }
      return this;
    }
    /**
     * <pre>
     * Pending approvals from child agent tool executions.
     *
     * &#64;internal
     * Populated when workflow tasks invoke agents that enter
     * EXECUTION_WAITING_FOR_APPROVAL phase. This surfaces all approval
     * requests at the workflow level for UI visibility.
     *
     * Guarded Update Protocol:
     * This field is only modified when UpdateStatusInput.update_pending_approvals
     * is explicitly set to true. Normal event emissions (which don't concern
     * approvals) leave this field untouched, preventing race conditions between
     * concurrent status writers.
     *
     * Only call-agent-status manages this field:
     * - Non-empty list: child agent(s) need approval
     * - Empty list + update_pending_approvals=true: all approvals resolved
     *
     * Parallel Agents:
     * When multiple child agents run in parallel, entries from different children
     * accumulate in this list. Each entry's child_agent_execution_id distinguishes
     * the source.
     * </pre>
     *
     * <code>repeated .ai.stigmer.agentic.workflowexecution.v1.WorkflowPendingApproval pending_approvals = 9 [json_name = "pendingApprovals"];</code>
     */
    public Builder removePendingApprovals(int index) {
      if (pendingApprovalsBuilder_ == null) {
        ensurePendingApprovalsIsMutable();
        pendingApprovals_.remove(index);
        onChanged();
      } else {
        pendingApprovalsBuilder_.remove(index);
      }
      return this;
    }
    /**
     * <pre>
     * Pending approvals from child agent tool executions.
     *
     * &#64;internal
     * Populated when workflow tasks invoke agents that enter
     * EXECUTION_WAITING_FOR_APPROVAL phase. This surfaces all approval
     * requests at the workflow level for UI visibility.
     *
     * Guarded Update Protocol:
     * This field is only modified when UpdateStatusInput.update_pending_approvals
     * is explicitly set to true. Normal event emissions (which don't concern
     * approvals) leave this field untouched, preventing race conditions between
     * concurrent status writers.
     *
     * Only call-agent-status manages this field:
     * - Non-empty list: child agent(s) need approval
     * - Empty list + update_pending_approvals=true: all approvals resolved
     *
     * Parallel Agents:
     * When multiple child agents run in parallel, entries from different children
     * accumulate in this list. Each entry's child_agent_execution_id distinguishes
     * the source.
     * </pre>
     *
     * <code>repeated .ai.stigmer.agentic.workflowexecution.v1.WorkflowPendingApproval pending_approvals = 9 [json_name = "pendingApprovals"];</code>
     */
    public ai.stigmer.agentic.workflowexecution.v1.WorkflowPendingApproval.Builder getPendingApprovalsBuilder(
        int index) {
      return internalGetPendingApprovalsFieldBuilder().getBuilder(index);
    }
    /**
     * <pre>
     * Pending approvals from child agent tool executions.
     *
     * &#64;internal
     * Populated when workflow tasks invoke agents that enter
     * EXECUTION_WAITING_FOR_APPROVAL phase. This surfaces all approval
     * requests at the workflow level for UI visibility.
     *
     * Guarded Update Protocol:
     * This field is only modified when UpdateStatusInput.update_pending_approvals
     * is explicitly set to true. Normal event emissions (which don't concern
     * approvals) leave this field untouched, preventing race conditions between
     * concurrent status writers.
     *
     * Only call-agent-status manages this field:
     * - Non-empty list: child agent(s) need approval
     * - Empty list + update_pending_approvals=true: all approvals resolved
     *
     * Parallel Agents:
     * When multiple child agents run in parallel, entries from different children
     * accumulate in this list. Each entry's child_agent_execution_id distinguishes
     * the source.
     * </pre>
     *
     * <code>repeated .ai.stigmer.agentic.workflowexecution.v1.WorkflowPendingApproval pending_approvals = 9 [json_name = "pendingApprovals"];</code>
     */
    public ai.stigmer.agentic.workflowexecution.v1.WorkflowPendingApprovalOrBuilder getPendingApprovalsOrBuilder(
        int index) {
      if (pendingApprovalsBuilder_ == null) {
        return pendingApprovals_.get(index);  } else {
        return pendingApprovalsBuilder_.getMessageOrBuilder(index);
      }
    }
    /**
     * <pre>
     * Pending approvals from child agent tool executions.
     *
     * &#64;internal
     * Populated when workflow tasks invoke agents that enter
     * EXECUTION_WAITING_FOR_APPROVAL phase. This surfaces all approval
     * requests at the workflow level for UI visibility.
     *
     * Guarded Update Protocol:
     * This field is only modified when UpdateStatusInput.update_pending_approvals
     * is explicitly set to true. Normal event emissions (which don't concern
     * approvals) leave this field untouched, preventing race conditions between
     * concurrent status writers.
     *
     * Only call-agent-status manages this field:
     * - Non-empty list: child agent(s) need approval
     * - Empty list + update_pending_approvals=true: all approvals resolved
     *
     * Parallel Agents:
     * When multiple child agents run in parallel, entries from different children
     * accumulate in this list. Each entry's child_agent_execution_id distinguishes
     * the source.
     * </pre>
     *
     * <code>repeated .ai.stigmer.agentic.workflowexecution.v1.WorkflowPendingApproval pending_approvals = 9 [json_name = "pendingApprovals"];</code>
     */
    public java.util.List<? extends ai.stigmer.agentic.workflowexecution.v1.WorkflowPendingApprovalOrBuilder> 
         getPendingApprovalsOrBuilderList() {
      if (pendingApprovalsBuilder_ != null) {
        return pendingApprovalsBuilder_.getMessageOrBuilderList();
      } else {
        return java.util.Collections.unmodifiableList(pendingApprovals_);
      }
    }
    /**
     * <pre>
     * Pending approvals from child agent tool executions.
     *
     * &#64;internal
     * Populated when workflow tasks invoke agents that enter
     * EXECUTION_WAITING_FOR_APPROVAL phase. This surfaces all approval
     * requests at the workflow level for UI visibility.
     *
     * Guarded Update Protocol:
     * This field is only modified when UpdateStatusInput.update_pending_approvals
     * is explicitly set to true. Normal event emissions (which don't concern
     * approvals) leave this field untouched, preventing race conditions between
     * concurrent status writers.
     *
     * Only call-agent-status manages this field:
     * - Non-empty list: child agent(s) need approval
     * - Empty list + update_pending_approvals=true: all approvals resolved
     *
     * Parallel Agents:
     * When multiple child agents run in parallel, entries from different children
     * accumulate in this list. Each entry's child_agent_execution_id distinguishes
     * the source.
     * </pre>
     *
     * <code>repeated .ai.stigmer.agentic.workflowexecution.v1.WorkflowPendingApproval pending_approvals = 9 [json_name = "pendingApprovals"];</code>
     */
    public ai.stigmer.agentic.workflowexecution.v1.WorkflowPendingApproval.Builder addPendingApprovalsBuilder() {
      return internalGetPendingApprovalsFieldBuilder().addBuilder(
          ai.stigmer.agentic.workflowexecution.v1.WorkflowPendingApproval.getDefaultInstance());
    }
    /**
     * <pre>
     * Pending approvals from child agent tool executions.
     *
     * &#64;internal
     * Populated when workflow tasks invoke agents that enter
     * EXECUTION_WAITING_FOR_APPROVAL phase. This surfaces all approval
     * requests at the workflow level for UI visibility.
     *
     * Guarded Update Protocol:
     * This field is only modified when UpdateStatusInput.update_pending_approvals
     * is explicitly set to true. Normal event emissions (which don't concern
     * approvals) leave this field untouched, preventing race conditions between
     * concurrent status writers.
     *
     * Only call-agent-status manages this field:
     * - Non-empty list: child agent(s) need approval
     * - Empty list + update_pending_approvals=true: all approvals resolved
     *
     * Parallel Agents:
     * When multiple child agents run in parallel, entries from different children
     * accumulate in this list. Each entry's child_agent_execution_id distinguishes
     * the source.
     * </pre>
     *
     * <code>repeated .ai.stigmer.agentic.workflowexecution.v1.WorkflowPendingApproval pending_approvals = 9 [json_name = "pendingApprovals"];</code>
     */
    public ai.stigmer.agentic.workflowexecution.v1.WorkflowPendingApproval.Builder addPendingApprovalsBuilder(
        int index) {
      return internalGetPendingApprovalsFieldBuilder().addBuilder(
          index, ai.stigmer.agentic.workflowexecution.v1.WorkflowPendingApproval.getDefaultInstance());
    }
    /**
     * <pre>
     * Pending approvals from child agent tool executions.
     *
     * &#64;internal
     * Populated when workflow tasks invoke agents that enter
     * EXECUTION_WAITING_FOR_APPROVAL phase. This surfaces all approval
     * requests at the workflow level for UI visibility.
     *
     * Guarded Update Protocol:
     * This field is only modified when UpdateStatusInput.update_pending_approvals
     * is explicitly set to true. Normal event emissions (which don't concern
     * approvals) leave this field untouched, preventing race conditions between
     * concurrent status writers.
     *
     * Only call-agent-status manages this field:
     * - Non-empty list: child agent(s) need approval
     * - Empty list + update_pending_approvals=true: all approvals resolved
     *
     * Parallel Agents:
     * When multiple child agents run in parallel, entries from different children
     * accumulate in this list. Each entry's child_agent_execution_id distinguishes
     * the source.
     * </pre>
     *
     * <code>repeated .ai.stigmer.agentic.workflowexecution.v1.WorkflowPendingApproval pending_approvals = 9 [json_name = "pendingApprovals"];</code>
     */
    public java.util.List<ai.stigmer.agentic.workflowexecution.v1.WorkflowPendingApproval.Builder> 
         getPendingApprovalsBuilderList() {
      return internalGetPendingApprovalsFieldBuilder().getBuilderList();
    }
    private com.google.protobuf.RepeatedFieldBuilder<
        ai.stigmer.agentic.workflowexecution.v1.WorkflowPendingApproval, ai.stigmer.agentic.workflowexecution.v1.WorkflowPendingApproval.Builder, ai.stigmer.agentic.workflowexecution.v1.WorkflowPendingApprovalOrBuilder> 
        internalGetPendingApprovalsFieldBuilder() {
      if (pendingApprovalsBuilder_ == null) {
        pendingApprovalsBuilder_ = new com.google.protobuf.RepeatedFieldBuilder<
            ai.stigmer.agentic.workflowexecution.v1.WorkflowPendingApproval, ai.stigmer.agentic.workflowexecution.v1.WorkflowPendingApproval.Builder, ai.stigmer.agentic.workflowexecution.v1.WorkflowPendingApprovalOrBuilder>(
                pendingApprovals_,
                ((bitField0_ & 0x00000100) != 0),
                getParentForChildren(),
                isClean());
        pendingApprovals_ = null;
      }
      return pendingApprovalsBuilder_;
    }

    private long totalCostMicros_ ;
    /**
     * <pre>
     * Cumulative cost across all tasks in micro-USD (1 USD = 1,000,000 micros).
     *
     * &#64;internal
     * Updated by the runner alongside each status update. Reflects the
     * budget tracker's accumulated cost at the time of the last status write.
     * Used by getExecutionSummary for fast aggregation without scanning events.
     *
     * Consistent with WorkflowBudget.max_cost_micros and the billing domain's
     * micro-USD convention (CostStamp.provider_cost_micros, etc.).
     *
     * &#64;since Cost Data Pipeline
     * </pre>
     *
     * <code>int64 total_cost_micros = 10 [json_name = "totalCostMicros"];</code>
     * @return The totalCostMicros.
     */
    @java.lang.Override
    public long getTotalCostMicros() {
      return totalCostMicros_;
    }
    /**
     * <pre>
     * Cumulative cost across all tasks in micro-USD (1 USD = 1,000,000 micros).
     *
     * &#64;internal
     * Updated by the runner alongside each status update. Reflects the
     * budget tracker's accumulated cost at the time of the last status write.
     * Used by getExecutionSummary for fast aggregation without scanning events.
     *
     * Consistent with WorkflowBudget.max_cost_micros and the billing domain's
     * micro-USD convention (CostStamp.provider_cost_micros, etc.).
     *
     * &#64;since Cost Data Pipeline
     * </pre>
     *
     * <code>int64 total_cost_micros = 10 [json_name = "totalCostMicros"];</code>
     * @param value The totalCostMicros to set.
     * @return This builder for chaining.
     */
    public Builder setTotalCostMicros(long value) {

      totalCostMicros_ = value;
      bitField0_ |= 0x00000200;
      onChanged();
      return this;
    }
    /**
     * <pre>
     * Cumulative cost across all tasks in micro-USD (1 USD = 1,000,000 micros).
     *
     * &#64;internal
     * Updated by the runner alongside each status update. Reflects the
     * budget tracker's accumulated cost at the time of the last status write.
     * Used by getExecutionSummary for fast aggregation without scanning events.
     *
     * Consistent with WorkflowBudget.max_cost_micros and the billing domain's
     * micro-USD convention (CostStamp.provider_cost_micros, etc.).
     *
     * &#64;since Cost Data Pipeline
     * </pre>
     *
     * <code>int64 total_cost_micros = 10 [json_name = "totalCostMicros"];</code>
     * @return This builder for chaining.
     */
    public Builder clearTotalCostMicros() {
      bitField0_ = (bitField0_ & ~0x00000200);
      totalCostMicros_ = 0L;
      onChanged();
      return this;
    }

    private long totalInputTokens_ ;
    /**
     * <pre>
     * Cumulative input tokens consumed across all LLM and agent tasks.
     *
     * &#64;internal
     * Input tokens represent prompt/context tokens sent to the model.
     * Updated alongside total_cost_micros from the budget tracker.
     *
     * &#64;since Cost Data Pipeline
     * </pre>
     *
     * <code>int64 total_input_tokens = 11 [json_name = "totalInputTokens"];</code>
     * @return The totalInputTokens.
     */
    @java.lang.Override
    public long getTotalInputTokens() {
      return totalInputTokens_;
    }
    /**
     * <pre>
     * Cumulative input tokens consumed across all LLM and agent tasks.
     *
     * &#64;internal
     * Input tokens represent prompt/context tokens sent to the model.
     * Updated alongside total_cost_micros from the budget tracker.
     *
     * &#64;since Cost Data Pipeline
     * </pre>
     *
     * <code>int64 total_input_tokens = 11 [json_name = "totalInputTokens"];</code>
     * @param value The totalInputTokens to set.
     * @return This builder for chaining.
     */
    public Builder setTotalInputTokens(long value) {

      totalInputTokens_ = value;
      bitField0_ |= 0x00000400;
      onChanged();
      return this;
    }
    /**
     * <pre>
     * Cumulative input tokens consumed across all LLM and agent tasks.
     *
     * &#64;internal
     * Input tokens represent prompt/context tokens sent to the model.
     * Updated alongside total_cost_micros from the budget tracker.
     *
     * &#64;since Cost Data Pipeline
     * </pre>
     *
     * <code>int64 total_input_tokens = 11 [json_name = "totalInputTokens"];</code>
     * @return This builder for chaining.
     */
    public Builder clearTotalInputTokens() {
      bitField0_ = (bitField0_ & ~0x00000400);
      totalInputTokens_ = 0L;
      onChanged();
      return this;
    }

    private long totalOutputTokens_ ;
    /**
     * <pre>
     * Cumulative output tokens consumed across all LLM and agent tasks.
     *
     * &#64;internal
     * Output tokens represent completion/generation tokens returned by the model.
     * Updated alongside total_cost_micros from the budget tracker.
     *
     * &#64;since Cost Data Pipeline
     * </pre>
     *
     * <code>int64 total_output_tokens = 12 [json_name = "totalOutputTokens"];</code>
     * @return The totalOutputTokens.
     */
    @java.lang.Override
    public long getTotalOutputTokens() {
      return totalOutputTokens_;
    }
    /**
     * <pre>
     * Cumulative output tokens consumed across all LLM and agent tasks.
     *
     * &#64;internal
     * Output tokens represent completion/generation tokens returned by the model.
     * Updated alongside total_cost_micros from the budget tracker.
     *
     * &#64;since Cost Data Pipeline
     * </pre>
     *
     * <code>int64 total_output_tokens = 12 [json_name = "totalOutputTokens"];</code>
     * @param value The totalOutputTokens to set.
     * @return This builder for chaining.
     */
    public Builder setTotalOutputTokens(long value) {

      totalOutputTokens_ = value;
      bitField0_ |= 0x00000800;
      onChanged();
      return this;
    }
    /**
     * <pre>
     * Cumulative output tokens consumed across all LLM and agent tasks.
     *
     * &#64;internal
     * Output tokens represent completion/generation tokens returned by the model.
     * Updated alongside total_cost_micros from the budget tracker.
     *
     * &#64;since Cost Data Pipeline
     * </pre>
     *
     * <code>int64 total_output_tokens = 12 [json_name = "totalOutputTokens"];</code>
     * @return This builder for chaining.
     */
    public Builder clearTotalOutputTokens() {
      bitField0_ = (bitField0_ & ~0x00000800);
      totalOutputTokens_ = 0L;
      onChanged();
      return this;
    }

    private java.lang.Object workflowVersionHash_ = "";
    /**
     * <pre>
     * SHA-256 hash identifying which workflow version was used for this execution.
     *
     * &#64;internal
     * Pinned at execution creation time from Workflow.status.version_hash.
     * Immutable after creation — represents the exact workflow definition this
     * execution ran (or will run, if still pending).
     *
     * Consumers:
     * - Runner: fetches the version-specific CNCF YAML via getVersion() during
     * hydration, ensuring the execution runs the intended definition even if
     * the workflow has been updated since creation.
     * - Execution viewer: fetches the version entry to render the correct graph
     * for historical executions, eliminating the version mismatch problem.
     *
     * Empty for executions created before workflow versioning was introduced.
     * In that case, consumers fall back to fetching the current workflow
     * definition (legacy behavior with mismatch warning).
     *
     * &#64;since Workflow Versioning
     * </pre>
     *
     * <code>string workflow_version_hash = 13 [json_name = "workflowVersionHash"];</code>
     * @return The workflowVersionHash.
     */
    public java.lang.String getWorkflowVersionHash() {
      java.lang.Object ref = workflowVersionHash_;
      if (!(ref instanceof java.lang.String)) {
        com.google.protobuf.ByteString bs =
            (com.google.protobuf.ByteString) ref;
        java.lang.String s = bs.toStringUtf8();
        workflowVersionHash_ = s;
        return s;
      } else {
        return (java.lang.String) ref;
      }
    }
    /**
     * <pre>
     * SHA-256 hash identifying which workflow version was used for this execution.
     *
     * &#64;internal
     * Pinned at execution creation time from Workflow.status.version_hash.
     * Immutable after creation — represents the exact workflow definition this
     * execution ran (or will run, if still pending).
     *
     * Consumers:
     * - Runner: fetches the version-specific CNCF YAML via getVersion() during
     * hydration, ensuring the execution runs the intended definition even if
     * the workflow has been updated since creation.
     * - Execution viewer: fetches the version entry to render the correct graph
     * for historical executions, eliminating the version mismatch problem.
     *
     * Empty for executions created before workflow versioning was introduced.
     * In that case, consumers fall back to fetching the current workflow
     * definition (legacy behavior with mismatch warning).
     *
     * &#64;since Workflow Versioning
     * </pre>
     *
     * <code>string workflow_version_hash = 13 [json_name = "workflowVersionHash"];</code>
     * @return The bytes for workflowVersionHash.
     */
    public com.google.protobuf.ByteString
        getWorkflowVersionHashBytes() {
      java.lang.Object ref = workflowVersionHash_;
      if (ref instanceof String) {
        com.google.protobuf.ByteString b = 
            com.google.protobuf.ByteString.copyFromUtf8(
                (java.lang.String) ref);
        workflowVersionHash_ = b;
        return b;
      } else {
        return (com.google.protobuf.ByteString) ref;
      }
    }
    /**
     * <pre>
     * SHA-256 hash identifying which workflow version was used for this execution.
     *
     * &#64;internal
     * Pinned at execution creation time from Workflow.status.version_hash.
     * Immutable after creation — represents the exact workflow definition this
     * execution ran (or will run, if still pending).
     *
     * Consumers:
     * - Runner: fetches the version-specific CNCF YAML via getVersion() during
     * hydration, ensuring the execution runs the intended definition even if
     * the workflow has been updated since creation.
     * - Execution viewer: fetches the version entry to render the correct graph
     * for historical executions, eliminating the version mismatch problem.
     *
     * Empty for executions created before workflow versioning was introduced.
     * In that case, consumers fall back to fetching the current workflow
     * definition (legacy behavior with mismatch warning).
     *
     * &#64;since Workflow Versioning
     * </pre>
     *
     * <code>string workflow_version_hash = 13 [json_name = "workflowVersionHash"];</code>
     * @param value The workflowVersionHash to set.
     * @return This builder for chaining.
     */
    public Builder setWorkflowVersionHash(
        java.lang.String value) {
      if (value == null) { throw new NullPointerException(); }
      workflowVersionHash_ = value;
      bitField0_ |= 0x00001000;
      onChanged();
      return this;
    }
    /**
     * <pre>
     * SHA-256 hash identifying which workflow version was used for this execution.
     *
     * &#64;internal
     * Pinned at execution creation time from Workflow.status.version_hash.
     * Immutable after creation — represents the exact workflow definition this
     * execution ran (or will run, if still pending).
     *
     * Consumers:
     * - Runner: fetches the version-specific CNCF YAML via getVersion() during
     * hydration, ensuring the execution runs the intended definition even if
     * the workflow has been updated since creation.
     * - Execution viewer: fetches the version entry to render the correct graph
     * for historical executions, eliminating the version mismatch problem.
     *
     * Empty for executions created before workflow versioning was introduced.
     * In that case, consumers fall back to fetching the current workflow
     * definition (legacy behavior with mismatch warning).
     *
     * &#64;since Workflow Versioning
     * </pre>
     *
     * <code>string workflow_version_hash = 13 [json_name = "workflowVersionHash"];</code>
     * @return This builder for chaining.
     */
    public Builder clearWorkflowVersionHash() {
      workflowVersionHash_ = getDefaultInstance().getWorkflowVersionHash();
      bitField0_ = (bitField0_ & ~0x00001000);
      onChanged();
      return this;
    }
    /**
     * <pre>
     * SHA-256 hash identifying which workflow version was used for this execution.
     *
     * &#64;internal
     * Pinned at execution creation time from Workflow.status.version_hash.
     * Immutable after creation — represents the exact workflow definition this
     * execution ran (or will run, if still pending).
     *
     * Consumers:
     * - Runner: fetches the version-specific CNCF YAML via getVersion() during
     * hydration, ensuring the execution runs the intended definition even if
     * the workflow has been updated since creation.
     * - Execution viewer: fetches the version entry to render the correct graph
     * for historical executions, eliminating the version mismatch problem.
     *
     * Empty for executions created before workflow versioning was introduced.
     * In that case, consumers fall back to fetching the current workflow
     * definition (legacy behavior with mismatch warning).
     *
     * &#64;since Workflow Versioning
     * </pre>
     *
     * <code>string workflow_version_hash = 13 [json_name = "workflowVersionHash"];</code>
     * @param value The bytes for workflowVersionHash to set.
     * @return This builder for chaining.
     */
    public Builder setWorkflowVersionHashBytes(
        com.google.protobuf.ByteString value) {
      if (value == null) { throw new NullPointerException(); }
      checkByteStringIsUtf8(value);
      workflowVersionHash_ = value;
      bitField0_ |= 0x00001000;
      onChanged();
      return this;
    }

    private java.util.List<ai.stigmer.agentic.workflowexecution.v1.WorkflowPendingFileReview> pendingFileReviews_ =
      java.util.Collections.emptyList();
    private void ensurePendingFileReviewsIsMutable() {
      if (!((bitField0_ & 0x00002000) != 0)) {
        pendingFileReviews_ = new java.util.ArrayList<ai.stigmer.agentic.workflowexecution.v1.WorkflowPendingFileReview>(pendingFileReviews_);
        bitField0_ |= 0x00002000;
       }
    }

    private com.google.protobuf.RepeatedFieldBuilder<
        ai.stigmer.agentic.workflowexecution.v1.WorkflowPendingFileReview, ai.stigmer.agentic.workflowexecution.v1.WorkflowPendingFileReview.Builder, ai.stigmer.agentic.workflowexecution.v1.WorkflowPendingFileReviewOrBuilder> pendingFileReviewsBuilder_;

    /**
     * <pre>
     * Pending file reviews from child agent executions, surfaced for
     * workflow-level visibility and in-place keep/discard decisioning.
     *
     * &#64;internal
     * A reference-only sibling of pending_approvals (field 9): each entry names a
     * child agent execution and the change_set ids it currently has
     * AWAITING_REVIEW. The heavy content (diffs, blobs) is NOT copied here — it
     * stays single-sourced on the child's
     * AgentExecution.status.file_change_sets and is read on demand by the UI.
     *
     * Guarded, per-child merge protocol:
     * Modified only when WorkflowExecutionUpdateStatusInput.update_pending_file_reviews
     * is true, scoped by pending_update_child_agent_execution_id. The write
     * replaces the entry for that one child and preserves every sibling child's
     * entry, so parallel child agents never clobber each other. A scoped write with
     * an empty change_set list removes that child's entry. Only call-agent-status
     * manages this field.
     *
     * &#64;since Workflow-Parent File Review
     * </pre>
     *
     * <code>repeated .ai.stigmer.agentic.workflowexecution.v1.WorkflowPendingFileReview pending_file_reviews = 14 [json_name = "pendingFileReviews"];</code>
     */
    public java.util.List<ai.stigmer.agentic.workflowexecution.v1.WorkflowPendingFileReview> getPendingFileReviewsList() {
      if (pendingFileReviewsBuilder_ == null) {
        return java.util.Collections.unmodifiableList(pendingFileReviews_);
      } else {
        return pendingFileReviewsBuilder_.getMessageList();
      }
    }
    /**
     * <pre>
     * Pending file reviews from child agent executions, surfaced for
     * workflow-level visibility and in-place keep/discard decisioning.
     *
     * &#64;internal
     * A reference-only sibling of pending_approvals (field 9): each entry names a
     * child agent execution and the change_set ids it currently has
     * AWAITING_REVIEW. The heavy content (diffs, blobs) is NOT copied here — it
     * stays single-sourced on the child's
     * AgentExecution.status.file_change_sets and is read on demand by the UI.
     *
     * Guarded, per-child merge protocol:
     * Modified only when WorkflowExecutionUpdateStatusInput.update_pending_file_reviews
     * is true, scoped by pending_update_child_agent_execution_id. The write
     * replaces the entry for that one child and preserves every sibling child's
     * entry, so parallel child agents never clobber each other. A scoped write with
     * an empty change_set list removes that child's entry. Only call-agent-status
     * manages this field.
     *
     * &#64;since Workflow-Parent File Review
     * </pre>
     *
     * <code>repeated .ai.stigmer.agentic.workflowexecution.v1.WorkflowPendingFileReview pending_file_reviews = 14 [json_name = "pendingFileReviews"];</code>
     */
    public int getPendingFileReviewsCount() {
      if (pendingFileReviewsBuilder_ == null) {
        return pendingFileReviews_.size();
      } else {
        return pendingFileReviewsBuilder_.getCount();
      }
    }
    /**
     * <pre>
     * Pending file reviews from child agent executions, surfaced for
     * workflow-level visibility and in-place keep/discard decisioning.
     *
     * &#64;internal
     * A reference-only sibling of pending_approvals (field 9): each entry names a
     * child agent execution and the change_set ids it currently has
     * AWAITING_REVIEW. The heavy content (diffs, blobs) is NOT copied here — it
     * stays single-sourced on the child's
     * AgentExecution.status.file_change_sets and is read on demand by the UI.
     *
     * Guarded, per-child merge protocol:
     * Modified only when WorkflowExecutionUpdateStatusInput.update_pending_file_reviews
     * is true, scoped by pending_update_child_agent_execution_id. The write
     * replaces the entry for that one child and preserves every sibling child's
     * entry, so parallel child agents never clobber each other. A scoped write with
     * an empty change_set list removes that child's entry. Only call-agent-status
     * manages this field.
     *
     * &#64;since Workflow-Parent File Review
     * </pre>
     *
     * <code>repeated .ai.stigmer.agentic.workflowexecution.v1.WorkflowPendingFileReview pending_file_reviews = 14 [json_name = "pendingFileReviews"];</code>
     */
    public ai.stigmer.agentic.workflowexecution.v1.WorkflowPendingFileReview getPendingFileReviews(int index) {
      if (pendingFileReviewsBuilder_ == null) {
        return pendingFileReviews_.get(index);
      } else {
        return pendingFileReviewsBuilder_.getMessage(index);
      }
    }
    /**
     * <pre>
     * Pending file reviews from child agent executions, surfaced for
     * workflow-level visibility and in-place keep/discard decisioning.
     *
     * &#64;internal
     * A reference-only sibling of pending_approvals (field 9): each entry names a
     * child agent execution and the change_set ids it currently has
     * AWAITING_REVIEW. The heavy content (diffs, blobs) is NOT copied here — it
     * stays single-sourced on the child's
     * AgentExecution.status.file_change_sets and is read on demand by the UI.
     *
     * Guarded, per-child merge protocol:
     * Modified only when WorkflowExecutionUpdateStatusInput.update_pending_file_reviews
     * is true, scoped by pending_update_child_agent_execution_id. The write
     * replaces the entry for that one child and preserves every sibling child's
     * entry, so parallel child agents never clobber each other. A scoped write with
     * an empty change_set list removes that child's entry. Only call-agent-status
     * manages this field.
     *
     * &#64;since Workflow-Parent File Review
     * </pre>
     *
     * <code>repeated .ai.stigmer.agentic.workflowexecution.v1.WorkflowPendingFileReview pending_file_reviews = 14 [json_name = "pendingFileReviews"];</code>
     */
    public Builder setPendingFileReviews(
        int index, ai.stigmer.agentic.workflowexecution.v1.WorkflowPendingFileReview value) {
      if (pendingFileReviewsBuilder_ == null) {
        if (value == null) {
          throw new NullPointerException();
        }
        ensurePendingFileReviewsIsMutable();
        pendingFileReviews_.set(index, value);
        onChanged();
      } else {
        pendingFileReviewsBuilder_.setMessage(index, value);
      }
      return this;
    }
    /**
     * <pre>
     * Pending file reviews from child agent executions, surfaced for
     * workflow-level visibility and in-place keep/discard decisioning.
     *
     * &#64;internal
     * A reference-only sibling of pending_approvals (field 9): each entry names a
     * child agent execution and the change_set ids it currently has
     * AWAITING_REVIEW. The heavy content (diffs, blobs) is NOT copied here — it
     * stays single-sourced on the child's
     * AgentExecution.status.file_change_sets and is read on demand by the UI.
     *
     * Guarded, per-child merge protocol:
     * Modified only when WorkflowExecutionUpdateStatusInput.update_pending_file_reviews
     * is true, scoped by pending_update_child_agent_execution_id. The write
     * replaces the entry for that one child and preserves every sibling child's
     * entry, so parallel child agents never clobber each other. A scoped write with
     * an empty change_set list removes that child's entry. Only call-agent-status
     * manages this field.
     *
     * &#64;since Workflow-Parent File Review
     * </pre>
     *
     * <code>repeated .ai.stigmer.agentic.workflowexecution.v1.WorkflowPendingFileReview pending_file_reviews = 14 [json_name = "pendingFileReviews"];</code>
     */
    public Builder setPendingFileReviews(
        int index, ai.stigmer.agentic.workflowexecution.v1.WorkflowPendingFileReview.Builder builderForValue) {
      if (pendingFileReviewsBuilder_ == null) {
        ensurePendingFileReviewsIsMutable();
        pendingFileReviews_.set(index, builderForValue.build());
        onChanged();
      } else {
        pendingFileReviewsBuilder_.setMessage(index, builderForValue.build());
      }
      return this;
    }
    /**
     * <pre>
     * Pending file reviews from child agent executions, surfaced for
     * workflow-level visibility and in-place keep/discard decisioning.
     *
     * &#64;internal
     * A reference-only sibling of pending_approvals (field 9): each entry names a
     * child agent execution and the change_set ids it currently has
     * AWAITING_REVIEW. The heavy content (diffs, blobs) is NOT copied here — it
     * stays single-sourced on the child's
     * AgentExecution.status.file_change_sets and is read on demand by the UI.
     *
     * Guarded, per-child merge protocol:
     * Modified only when WorkflowExecutionUpdateStatusInput.update_pending_file_reviews
     * is true, scoped by pending_update_child_agent_execution_id. The write
     * replaces the entry for that one child and preserves every sibling child's
     * entry, so parallel child agents never clobber each other. A scoped write with
     * an empty change_set list removes that child's entry. Only call-agent-status
     * manages this field.
     *
     * &#64;since Workflow-Parent File Review
     * </pre>
     *
     * <code>repeated .ai.stigmer.agentic.workflowexecution.v1.WorkflowPendingFileReview pending_file_reviews = 14 [json_name = "pendingFileReviews"];</code>
     */
    public Builder addPendingFileReviews(ai.stigmer.agentic.workflowexecution.v1.WorkflowPendingFileReview value) {
      if (pendingFileReviewsBuilder_ == null) {
        if (value == null) {
          throw new NullPointerException();
        }
        ensurePendingFileReviewsIsMutable();
        pendingFileReviews_.add(value);
        onChanged();
      } else {
        pendingFileReviewsBuilder_.addMessage(value);
      }
      return this;
    }
    /**
     * <pre>
     * Pending file reviews from child agent executions, surfaced for
     * workflow-level visibility and in-place keep/discard decisioning.
     *
     * &#64;internal
     * A reference-only sibling of pending_approvals (field 9): each entry names a
     * child agent execution and the change_set ids it currently has
     * AWAITING_REVIEW. The heavy content (diffs, blobs) is NOT copied here — it
     * stays single-sourced on the child's
     * AgentExecution.status.file_change_sets and is read on demand by the UI.
     *
     * Guarded, per-child merge protocol:
     * Modified only when WorkflowExecutionUpdateStatusInput.update_pending_file_reviews
     * is true, scoped by pending_update_child_agent_execution_id. The write
     * replaces the entry for that one child and preserves every sibling child's
     * entry, so parallel child agents never clobber each other. A scoped write with
     * an empty change_set list removes that child's entry. Only call-agent-status
     * manages this field.
     *
     * &#64;since Workflow-Parent File Review
     * </pre>
     *
     * <code>repeated .ai.stigmer.agentic.workflowexecution.v1.WorkflowPendingFileReview pending_file_reviews = 14 [json_name = "pendingFileReviews"];</code>
     */
    public Builder addPendingFileReviews(
        int index, ai.stigmer.agentic.workflowexecution.v1.WorkflowPendingFileReview value) {
      if (pendingFileReviewsBuilder_ == null) {
        if (value == null) {
          throw new NullPointerException();
        }
        ensurePendingFileReviewsIsMutable();
        pendingFileReviews_.add(index, value);
        onChanged();
      } else {
        pendingFileReviewsBuilder_.addMessage(index, value);
      }
      return this;
    }
    /**
     * <pre>
     * Pending file reviews from child agent executions, surfaced for
     * workflow-level visibility and in-place keep/discard decisioning.
     *
     * &#64;internal
     * A reference-only sibling of pending_approvals (field 9): each entry names a
     * child agent execution and the change_set ids it currently has
     * AWAITING_REVIEW. The heavy content (diffs, blobs) is NOT copied here — it
     * stays single-sourced on the child's
     * AgentExecution.status.file_change_sets and is read on demand by the UI.
     *
     * Guarded, per-child merge protocol:
     * Modified only when WorkflowExecutionUpdateStatusInput.update_pending_file_reviews
     * is true, scoped by pending_update_child_agent_execution_id. The write
     * replaces the entry for that one child and preserves every sibling child's
     * entry, so parallel child agents never clobber each other. A scoped write with
     * an empty change_set list removes that child's entry. Only call-agent-status
     * manages this field.
     *
     * &#64;since Workflow-Parent File Review
     * </pre>
     *
     * <code>repeated .ai.stigmer.agentic.workflowexecution.v1.WorkflowPendingFileReview pending_file_reviews = 14 [json_name = "pendingFileReviews"];</code>
     */
    public Builder addPendingFileReviews(
        ai.stigmer.agentic.workflowexecution.v1.WorkflowPendingFileReview.Builder builderForValue) {
      if (pendingFileReviewsBuilder_ == null) {
        ensurePendingFileReviewsIsMutable();
        pendingFileReviews_.add(builderForValue.build());
        onChanged();
      } else {
        pendingFileReviewsBuilder_.addMessage(builderForValue.build());
      }
      return this;
    }
    /**
     * <pre>
     * Pending file reviews from child agent executions, surfaced for
     * workflow-level visibility and in-place keep/discard decisioning.
     *
     * &#64;internal
     * A reference-only sibling of pending_approvals (field 9): each entry names a
     * child agent execution and the change_set ids it currently has
     * AWAITING_REVIEW. The heavy content (diffs, blobs) is NOT copied here — it
     * stays single-sourced on the child's
     * AgentExecution.status.file_change_sets and is read on demand by the UI.
     *
     * Guarded, per-child merge protocol:
     * Modified only when WorkflowExecutionUpdateStatusInput.update_pending_file_reviews
     * is true, scoped by pending_update_child_agent_execution_id. The write
     * replaces the entry for that one child and preserves every sibling child's
     * entry, so parallel child agents never clobber each other. A scoped write with
     * an empty change_set list removes that child's entry. Only call-agent-status
     * manages this field.
     *
     * &#64;since Workflow-Parent File Review
     * </pre>
     *
     * <code>repeated .ai.stigmer.agentic.workflowexecution.v1.WorkflowPendingFileReview pending_file_reviews = 14 [json_name = "pendingFileReviews"];</code>
     */
    public Builder addPendingFileReviews(
        int index, ai.stigmer.agentic.workflowexecution.v1.WorkflowPendingFileReview.Builder builderForValue) {
      if (pendingFileReviewsBuilder_ == null) {
        ensurePendingFileReviewsIsMutable();
        pendingFileReviews_.add(index, builderForValue.build());
        onChanged();
      } else {
        pendingFileReviewsBuilder_.addMessage(index, builderForValue.build());
      }
      return this;
    }
    /**
     * <pre>
     * Pending file reviews from child agent executions, surfaced for
     * workflow-level visibility and in-place keep/discard decisioning.
     *
     * &#64;internal
     * A reference-only sibling of pending_approvals (field 9): each entry names a
     * child agent execution and the change_set ids it currently has
     * AWAITING_REVIEW. The heavy content (diffs, blobs) is NOT copied here — it
     * stays single-sourced on the child's
     * AgentExecution.status.file_change_sets and is read on demand by the UI.
     *
     * Guarded, per-child merge protocol:
     * Modified only when WorkflowExecutionUpdateStatusInput.update_pending_file_reviews
     * is true, scoped by pending_update_child_agent_execution_id. The write
     * replaces the entry for that one child and preserves every sibling child's
     * entry, so parallel child agents never clobber each other. A scoped write with
     * an empty change_set list removes that child's entry. Only call-agent-status
     * manages this field.
     *
     * &#64;since Workflow-Parent File Review
     * </pre>
     *
     * <code>repeated .ai.stigmer.agentic.workflowexecution.v1.WorkflowPendingFileReview pending_file_reviews = 14 [json_name = "pendingFileReviews"];</code>
     */
    public Builder addAllPendingFileReviews(
        java.lang.Iterable<? extends ai.stigmer.agentic.workflowexecution.v1.WorkflowPendingFileReview> values) {
      if (pendingFileReviewsBuilder_ == null) {
        ensurePendingFileReviewsIsMutable();
        com.google.protobuf.AbstractMessageLite.Builder.addAll(
            values, pendingFileReviews_);
        onChanged();
      } else {
        pendingFileReviewsBuilder_.addAllMessages(values);
      }
      return this;
    }
    /**
     * <pre>
     * Pending file reviews from child agent executions, surfaced for
     * workflow-level visibility and in-place keep/discard decisioning.
     *
     * &#64;internal
     * A reference-only sibling of pending_approvals (field 9): each entry names a
     * child agent execution and the change_set ids it currently has
     * AWAITING_REVIEW. The heavy content (diffs, blobs) is NOT copied here — it
     * stays single-sourced on the child's
     * AgentExecution.status.file_change_sets and is read on demand by the UI.
     *
     * Guarded, per-child merge protocol:
     * Modified only when WorkflowExecutionUpdateStatusInput.update_pending_file_reviews
     * is true, scoped by pending_update_child_agent_execution_id. The write
     * replaces the entry for that one child and preserves every sibling child's
     * entry, so parallel child agents never clobber each other. A scoped write with
     * an empty change_set list removes that child's entry. Only call-agent-status
     * manages this field.
     *
     * &#64;since Workflow-Parent File Review
     * </pre>
     *
     * <code>repeated .ai.stigmer.agentic.workflowexecution.v1.WorkflowPendingFileReview pending_file_reviews = 14 [json_name = "pendingFileReviews"];</code>
     */
    public Builder clearPendingFileReviews() {
      if (pendingFileReviewsBuilder_ == null) {
        pendingFileReviews_ = java.util.Collections.emptyList();
        bitField0_ = (bitField0_ & ~0x00002000);
        onChanged();
      } else {
        pendingFileReviewsBuilder_.clear();
      }
      return this;
    }
    /**
     * <pre>
     * Pending file reviews from child agent executions, surfaced for
     * workflow-level visibility and in-place keep/discard decisioning.
     *
     * &#64;internal
     * A reference-only sibling of pending_approvals (field 9): each entry names a
     * child agent execution and the change_set ids it currently has
     * AWAITING_REVIEW. The heavy content (diffs, blobs) is NOT copied here — it
     * stays single-sourced on the child's
     * AgentExecution.status.file_change_sets and is read on demand by the UI.
     *
     * Guarded, per-child merge protocol:
     * Modified only when WorkflowExecutionUpdateStatusInput.update_pending_file_reviews
     * is true, scoped by pending_update_child_agent_execution_id. The write
     * replaces the entry for that one child and preserves every sibling child's
     * entry, so parallel child agents never clobber each other. A scoped write with
     * an empty change_set list removes that child's entry. Only call-agent-status
     * manages this field.
     *
     * &#64;since Workflow-Parent File Review
     * </pre>
     *
     * <code>repeated .ai.stigmer.agentic.workflowexecution.v1.WorkflowPendingFileReview pending_file_reviews = 14 [json_name = "pendingFileReviews"];</code>
     */
    public Builder removePendingFileReviews(int index) {
      if (pendingFileReviewsBuilder_ == null) {
        ensurePendingFileReviewsIsMutable();
        pendingFileReviews_.remove(index);
        onChanged();
      } else {
        pendingFileReviewsBuilder_.remove(index);
      }
      return this;
    }
    /**
     * <pre>
     * Pending file reviews from child agent executions, surfaced for
     * workflow-level visibility and in-place keep/discard decisioning.
     *
     * &#64;internal
     * A reference-only sibling of pending_approvals (field 9): each entry names a
     * child agent execution and the change_set ids it currently has
     * AWAITING_REVIEW. The heavy content (diffs, blobs) is NOT copied here — it
     * stays single-sourced on the child's
     * AgentExecution.status.file_change_sets and is read on demand by the UI.
     *
     * Guarded, per-child merge protocol:
     * Modified only when WorkflowExecutionUpdateStatusInput.update_pending_file_reviews
     * is true, scoped by pending_update_child_agent_execution_id. The write
     * replaces the entry for that one child and preserves every sibling child's
     * entry, so parallel child agents never clobber each other. A scoped write with
     * an empty change_set list removes that child's entry. Only call-agent-status
     * manages this field.
     *
     * &#64;since Workflow-Parent File Review
     * </pre>
     *
     * <code>repeated .ai.stigmer.agentic.workflowexecution.v1.WorkflowPendingFileReview pending_file_reviews = 14 [json_name = "pendingFileReviews"];</code>
     */
    public ai.stigmer.agentic.workflowexecution.v1.WorkflowPendingFileReview.Builder getPendingFileReviewsBuilder(
        int index) {
      return internalGetPendingFileReviewsFieldBuilder().getBuilder(index);
    }
    /**
     * <pre>
     * Pending file reviews from child agent executions, surfaced for
     * workflow-level visibility and in-place keep/discard decisioning.
     *
     * &#64;internal
     * A reference-only sibling of pending_approvals (field 9): each entry names a
     * child agent execution and the change_set ids it currently has
     * AWAITING_REVIEW. The heavy content (diffs, blobs) is NOT copied here — it
     * stays single-sourced on the child's
     * AgentExecution.status.file_change_sets and is read on demand by the UI.
     *
     * Guarded, per-child merge protocol:
     * Modified only when WorkflowExecutionUpdateStatusInput.update_pending_file_reviews
     * is true, scoped by pending_update_child_agent_execution_id. The write
     * replaces the entry for that one child and preserves every sibling child's
     * entry, so parallel child agents never clobber each other. A scoped write with
     * an empty change_set list removes that child's entry. Only call-agent-status
     * manages this field.
     *
     * &#64;since Workflow-Parent File Review
     * </pre>
     *
     * <code>repeated .ai.stigmer.agentic.workflowexecution.v1.WorkflowPendingFileReview pending_file_reviews = 14 [json_name = "pendingFileReviews"];</code>
     */
    public ai.stigmer.agentic.workflowexecution.v1.WorkflowPendingFileReviewOrBuilder getPendingFileReviewsOrBuilder(
        int index) {
      if (pendingFileReviewsBuilder_ == null) {
        return pendingFileReviews_.get(index);  } else {
        return pendingFileReviewsBuilder_.getMessageOrBuilder(index);
      }
    }
    /**
     * <pre>
     * Pending file reviews from child agent executions, surfaced for
     * workflow-level visibility and in-place keep/discard decisioning.
     *
     * &#64;internal
     * A reference-only sibling of pending_approvals (field 9): each entry names a
     * child agent execution and the change_set ids it currently has
     * AWAITING_REVIEW. The heavy content (diffs, blobs) is NOT copied here — it
     * stays single-sourced on the child's
     * AgentExecution.status.file_change_sets and is read on demand by the UI.
     *
     * Guarded, per-child merge protocol:
     * Modified only when WorkflowExecutionUpdateStatusInput.update_pending_file_reviews
     * is true, scoped by pending_update_child_agent_execution_id. The write
     * replaces the entry for that one child and preserves every sibling child's
     * entry, so parallel child agents never clobber each other. A scoped write with
     * an empty change_set list removes that child's entry. Only call-agent-status
     * manages this field.
     *
     * &#64;since Workflow-Parent File Review
     * </pre>
     *
     * <code>repeated .ai.stigmer.agentic.workflowexecution.v1.WorkflowPendingFileReview pending_file_reviews = 14 [json_name = "pendingFileReviews"];</code>
     */
    public java.util.List<? extends ai.stigmer.agentic.workflowexecution.v1.WorkflowPendingFileReviewOrBuilder> 
         getPendingFileReviewsOrBuilderList() {
      if (pendingFileReviewsBuilder_ != null) {
        return pendingFileReviewsBuilder_.getMessageOrBuilderList();
      } else {
        return java.util.Collections.unmodifiableList(pendingFileReviews_);
      }
    }
    /**
     * <pre>
     * Pending file reviews from child agent executions, surfaced for
     * workflow-level visibility and in-place keep/discard decisioning.
     *
     * &#64;internal
     * A reference-only sibling of pending_approvals (field 9): each entry names a
     * child agent execution and the change_set ids it currently has
     * AWAITING_REVIEW. The heavy content (diffs, blobs) is NOT copied here — it
     * stays single-sourced on the child's
     * AgentExecution.status.file_change_sets and is read on demand by the UI.
     *
     * Guarded, per-child merge protocol:
     * Modified only when WorkflowExecutionUpdateStatusInput.update_pending_file_reviews
     * is true, scoped by pending_update_child_agent_execution_id. The write
     * replaces the entry for that one child and preserves every sibling child's
     * entry, so parallel child agents never clobber each other. A scoped write with
     * an empty change_set list removes that child's entry. Only call-agent-status
     * manages this field.
     *
     * &#64;since Workflow-Parent File Review
     * </pre>
     *
     * <code>repeated .ai.stigmer.agentic.workflowexecution.v1.WorkflowPendingFileReview pending_file_reviews = 14 [json_name = "pendingFileReviews"];</code>
     */
    public ai.stigmer.agentic.workflowexecution.v1.WorkflowPendingFileReview.Builder addPendingFileReviewsBuilder() {
      return internalGetPendingFileReviewsFieldBuilder().addBuilder(
          ai.stigmer.agentic.workflowexecution.v1.WorkflowPendingFileReview.getDefaultInstance());
    }
    /**
     * <pre>
     * Pending file reviews from child agent executions, surfaced for
     * workflow-level visibility and in-place keep/discard decisioning.
     *
     * &#64;internal
     * A reference-only sibling of pending_approvals (field 9): each entry names a
     * child agent execution and the change_set ids it currently has
     * AWAITING_REVIEW. The heavy content (diffs, blobs) is NOT copied here — it
     * stays single-sourced on the child's
     * AgentExecution.status.file_change_sets and is read on demand by the UI.
     *
     * Guarded, per-child merge protocol:
     * Modified only when WorkflowExecutionUpdateStatusInput.update_pending_file_reviews
     * is true, scoped by pending_update_child_agent_execution_id. The write
     * replaces the entry for that one child and preserves every sibling child's
     * entry, so parallel child agents never clobber each other. A scoped write with
     * an empty change_set list removes that child's entry. Only call-agent-status
     * manages this field.
     *
     * &#64;since Workflow-Parent File Review
     * </pre>
     *
     * <code>repeated .ai.stigmer.agentic.workflowexecution.v1.WorkflowPendingFileReview pending_file_reviews = 14 [json_name = "pendingFileReviews"];</code>
     */
    public ai.stigmer.agentic.workflowexecution.v1.WorkflowPendingFileReview.Builder addPendingFileReviewsBuilder(
        int index) {
      return internalGetPendingFileReviewsFieldBuilder().addBuilder(
          index, ai.stigmer.agentic.workflowexecution.v1.WorkflowPendingFileReview.getDefaultInstance());
    }
    /**
     * <pre>
     * Pending file reviews from child agent executions, surfaced for
     * workflow-level visibility and in-place keep/discard decisioning.
     *
     * &#64;internal
     * A reference-only sibling of pending_approvals (field 9): each entry names a
     * child agent execution and the change_set ids it currently has
     * AWAITING_REVIEW. The heavy content (diffs, blobs) is NOT copied here — it
     * stays single-sourced on the child's
     * AgentExecution.status.file_change_sets and is read on demand by the UI.
     *
     * Guarded, per-child merge protocol:
     * Modified only when WorkflowExecutionUpdateStatusInput.update_pending_file_reviews
     * is true, scoped by pending_update_child_agent_execution_id. The write
     * replaces the entry for that one child and preserves every sibling child's
     * entry, so parallel child agents never clobber each other. A scoped write with
     * an empty change_set list removes that child's entry. Only call-agent-status
     * manages this field.
     *
     * &#64;since Workflow-Parent File Review
     * </pre>
     *
     * <code>repeated .ai.stigmer.agentic.workflowexecution.v1.WorkflowPendingFileReview pending_file_reviews = 14 [json_name = "pendingFileReviews"];</code>
     */
    public java.util.List<ai.stigmer.agentic.workflowexecution.v1.WorkflowPendingFileReview.Builder> 
         getPendingFileReviewsBuilderList() {
      return internalGetPendingFileReviewsFieldBuilder().getBuilderList();
    }
    private com.google.protobuf.RepeatedFieldBuilder<
        ai.stigmer.agentic.workflowexecution.v1.WorkflowPendingFileReview, ai.stigmer.agentic.workflowexecution.v1.WorkflowPendingFileReview.Builder, ai.stigmer.agentic.workflowexecution.v1.WorkflowPendingFileReviewOrBuilder> 
        internalGetPendingFileReviewsFieldBuilder() {
      if (pendingFileReviewsBuilder_ == null) {
        pendingFileReviewsBuilder_ = new com.google.protobuf.RepeatedFieldBuilder<
            ai.stigmer.agentic.workflowexecution.v1.WorkflowPendingFileReview, ai.stigmer.agentic.workflowexecution.v1.WorkflowPendingFileReview.Builder, ai.stigmer.agentic.workflowexecution.v1.WorkflowPendingFileReviewOrBuilder>(
                pendingFileReviews_,
                ((bitField0_ & 0x00002000) != 0),
                getParentForChildren(),
                isClean());
        pendingFileReviews_ = null;
      }
      return pendingFileReviewsBuilder_;
    }

    // @@protoc_insertion_point(builder_scope:ai.stigmer.agentic.workflowexecution.v1.WorkflowExecutionStatus)
  }

  // @@protoc_insertion_point(class_scope:ai.stigmer.agentic.workflowexecution.v1.WorkflowExecutionStatus)
  private static final ai.stigmer.agentic.workflowexecution.v1.WorkflowExecutionStatus DEFAULT_INSTANCE;
  static {
    DEFAULT_INSTANCE = new ai.stigmer.agentic.workflowexecution.v1.WorkflowExecutionStatus();
  }

  public static ai.stigmer.agentic.workflowexecution.v1.WorkflowExecutionStatus getDefaultInstance() {
    return DEFAULT_INSTANCE;
  }

  private static final com.google.protobuf.Parser<WorkflowExecutionStatus>
      PARSER = new com.google.protobuf.AbstractParser<WorkflowExecutionStatus>() {
    @java.lang.Override
    public WorkflowExecutionStatus parsePartialFrom(
        com.google.protobuf.CodedInputStream input,
        com.google.protobuf.ExtensionRegistryLite extensionRegistry)
        throws com.google.protobuf.InvalidProtocolBufferException {
      Builder builder = newBuilder();
      try {
        builder.mergeFrom(input, extensionRegistry);
      } catch (com.google.protobuf.InvalidProtocolBufferException e) {
        throw e.setUnfinishedMessage(builder.buildPartial());
      } catch (com.google.protobuf.UninitializedMessageException e) {
        throw e.asInvalidProtocolBufferException().setUnfinishedMessage(builder.buildPartial());
      } catch (java.io.IOException e) {
        throw new com.google.protobuf.InvalidProtocolBufferException(e)
            .setUnfinishedMessage(builder.buildPartial());
      }
      return builder.buildPartial();
    }
  };

  public static com.google.protobuf.Parser<WorkflowExecutionStatus> parser() {
    return PARSER;
  }

  @java.lang.Override
  public com.google.protobuf.Parser<WorkflowExecutionStatus> getParserForType() {
    return PARSER;
  }

  @java.lang.Override
  public ai.stigmer.agentic.workflowexecution.v1.WorkflowExecutionStatus getDefaultInstanceForType() {
    return DEFAULT_INSTANCE;
  }

}

