// Generated by the protocol buffer compiler.  DO NOT EDIT!
// NO CHECKED-IN PROTOBUF GENCODE
// source: ai/stigmer/agentic/schedule/v1/spec.proto
// Protobuf Java Version: 4.34.0

package ai.stigmer.agentic.schedule.v1;

/**
 * <pre>
 * ScheduleSpec defines when a schedule fires and what it runs.
 *
 * The spec is deliberately small: a cron expression, the IANA time zone
 * it is evaluated in, the owner's enablement switch, and the target each
 * fire runs. Everything the platform observes about firing lives in
 * status — a declarative apply can never clobber it.
 *
 * &#64;internal
 * DD-008 D8 as amended by DD-009 and DD-018: the target oneof is inline
 * (the AgentChannelSpec.provider_config shape, C-3) and the agent arm is
 * the shared AgentInvocation (DD-018 D-3) — the owner-settable subset of
 * an agent run, so the schedule speaks the composer's vocabulary instead
 * of a schedule-only one. Apply-time cron validation is purely lexical
 * (C-4) so no cron parser exists in either edition; the Temporal server
 * owns calendar/DST semantics (D2).
 * </pre>
 *
 * Protobuf type {@code ai.stigmer.agentic.schedule.v1.ScheduleSpec}
 */
@com.google.protobuf.Generated
public final class ScheduleSpec extends
    com.google.protobuf.GeneratedMessage implements
    // @@protoc_insertion_point(message_implements:ai.stigmer.agentic.schedule.v1.ScheduleSpec)
    ScheduleSpecOrBuilder {
private static final long serialVersionUID = 0L;
  static {
    com.google.protobuf.RuntimeVersion.validateProtobufGencodeVersion(
      com.google.protobuf.RuntimeVersion.RuntimeDomain.PUBLIC,
      /* major= */ 4,
      /* minor= */ 34,
      /* patch= */ 0,
      /* suffix= */ "",
      "ScheduleSpec");
  }
  // Use ScheduleSpec.newBuilder() to construct.
  private ScheduleSpec(com.google.protobuf.GeneratedMessage.Builder<?> builder) {
    super(builder);
  }
  private ScheduleSpec() {
    cron_ = "";
    timeZone_ = "";
  }

  public static final com.google.protobuf.Descriptors.Descriptor
      getDescriptor() {
    return ai.stigmer.agentic.schedule.v1.SpecProto.internal_static_ai_stigmer_agentic_schedule_v1_ScheduleSpec_descriptor;
  }

  @java.lang.Override
  public com.google.protobuf.Descriptors.Descriptor getDescriptorForType() {
    return ai.stigmer.agentic.schedule.v1.SpecProto.internal_static_ai_stigmer_agentic_schedule_v1_ScheduleSpec_descriptor;
  }

  @java.lang.Override
  protected com.google.protobuf.GeneratedMessage.FieldAccessorTable
      internalGetFieldAccessorTable() {
    return ai.stigmer.agentic.schedule.v1.SpecProto.internal_static_ai_stigmer_agentic_schedule_v1_ScheduleSpec_fieldAccessorTable
        .ensureFieldAccessorsInitialized(
            ai.stigmer.agentic.schedule.v1.ScheduleSpec.class, ai.stigmer.agentic.schedule.v1.ScheduleSpec.Builder.class);
  }

  private int targetCase_ = 0;
  @SuppressWarnings("serial")
  private java.lang.Object target_;
  public enum TargetCase
      implements com.google.protobuf.Internal.EnumLite,
          com.google.protobuf.AbstractMessage.InternalOneOfEnum {
    AGENT(4),
    TARGET_NOT_SET(0);
    private final int value;
    private TargetCase(int value) {
      this.value = value;
    }
    /**
     * @param value The number of the enum to look for.
     * @return The enum associated with the given number.
     * @deprecated Use {@link #forNumber(int)} instead.
     */
    @java.lang.Deprecated
    public static TargetCase valueOf(int value) {
      return forNumber(value);
    }

    public static TargetCase forNumber(int value) {
      switch (value) {
        case 4: return AGENT;
        case 0: return TARGET_NOT_SET;
        default: return null;
      }
    }
    public int getNumber() {
      return this.value;
    }
  };

  public TargetCase
  getTargetCase() {
    return TargetCase.forNumber(
        targetCase_);
  }

  public static final int CRON_FIELD_NUMBER = 1;
  @SuppressWarnings("serial")
  private volatile java.lang.Object cron_ = "";
  /**
   * <pre>
   * Cron expression in the classic 5-field form (minute, hour,
   * day-of-month, month, day-of-week), evaluated in `time_zone` — for
   * example "0 9 * * *" for daily at 09:00. The shorthands &#64;hourly,
   * &#64;daily, &#64;weekly, &#64;monthly, and &#64;yearly are also accepted. Timezone
   * prefixes (CRON_TZ=/TZ=), &#64;every intervals, 6- or 7-field forms, and
   * trailing comments are rejected.
   *
   * &#64;internal
   * DD-009 C-4: deliberately narrower than the Temporal server's cron
   * grammar. CRON_TZ=/TZ= would carry a second timezone authority beside
   * `time_zone` (Temporal itself rejects the combination at
   * schedule-create time — accepting it here would store a spec that
   * detonates when the clock lands); &#64;every compiles to an interval spec
   * and bypasses the calendar model; the 7-field form has a seconds
   * column. The 5-field restriction is a structural one-minute firing
   * floor with zero cron parsing. The configurable interval floor
   * (stigmer.schedules.limits, DD-008 D7) lands with the clock, enforced
   * against Temporal's own computed fire times. Widening this grammar
   * later is additive; narrowing it would break stored specs.
   * </pre>
   *
   * <code>string cron = 1 [json_name = "cron", (.buf.validate.field) = { ... }</code>
   * @return The cron.
   */
  @java.lang.Override
  public java.lang.String getCron() {
    java.lang.Object ref = cron_;
    if (ref instanceof java.lang.String) {
      return (java.lang.String) ref;
    } else {
      com.google.protobuf.ByteString bs = 
          (com.google.protobuf.ByteString) ref;
      java.lang.String s = bs.toStringUtf8();
      cron_ = s;
      return s;
    }
  }
  /**
   * <pre>
   * Cron expression in the classic 5-field form (minute, hour,
   * day-of-month, month, day-of-week), evaluated in `time_zone` — for
   * example "0 9 * * *" for daily at 09:00. The shorthands &#64;hourly,
   * &#64;daily, &#64;weekly, &#64;monthly, and &#64;yearly are also accepted. Timezone
   * prefixes (CRON_TZ=/TZ=), &#64;every intervals, 6- or 7-field forms, and
   * trailing comments are rejected.
   *
   * &#64;internal
   * DD-009 C-4: deliberately narrower than the Temporal server's cron
   * grammar. CRON_TZ=/TZ= would carry a second timezone authority beside
   * `time_zone` (Temporal itself rejects the combination at
   * schedule-create time — accepting it here would store a spec that
   * detonates when the clock lands); &#64;every compiles to an interval spec
   * and bypasses the calendar model; the 7-field form has a seconds
   * column. The 5-field restriction is a structural one-minute firing
   * floor with zero cron parsing. The configurable interval floor
   * (stigmer.schedules.limits, DD-008 D7) lands with the clock, enforced
   * against Temporal's own computed fire times. Widening this grammar
   * later is additive; narrowing it would break stored specs.
   * </pre>
   *
   * <code>string cron = 1 [json_name = "cron", (.buf.validate.field) = { ... }</code>
   * @return The bytes for cron.
   */
  @java.lang.Override
  public com.google.protobuf.ByteString
      getCronBytes() {
    java.lang.Object ref = cron_;
    if (ref instanceof java.lang.String) {
      com.google.protobuf.ByteString b = 
          com.google.protobuf.ByteString.copyFromUtf8(
              (java.lang.String) ref);
      cron_ = b;
      return b;
    } else {
      return (com.google.protobuf.ByteString) ref;
    }
  }

  public static final int TIME_ZONE_FIELD_NUMBER = 2;
  @SuppressWarnings("serial")
  private volatile java.lang.Object timeZone_ = "";
  /**
   * <pre>
   * IANA time zone the cron is evaluated in, e.g. "Asia/Kolkata".
   *
   * &#64;internal
   * The single timezone authority (DD-009 C-4): validation refuses cron
   * strings carrying their own CRON_TZ=/TZ= prefix. Zone-name validity is
   * checked against the platform tz database in both editions
   * (time.LoadLocation / ZoneId.of); DST evaluation semantics are the
   * Temporal server's concern (DD-008 D2), identical for both editions.
   * </pre>
   *
   * <code>string time_zone = 2 [json_name = "timeZone", (.buf.validate.field) = { ... }</code>
   * @return The timeZone.
   */
  @java.lang.Override
  public java.lang.String getTimeZone() {
    java.lang.Object ref = timeZone_;
    if (ref instanceof java.lang.String) {
      return (java.lang.String) ref;
    } else {
      com.google.protobuf.ByteString bs = 
          (com.google.protobuf.ByteString) ref;
      java.lang.String s = bs.toStringUtf8();
      timeZone_ = s;
      return s;
    }
  }
  /**
   * <pre>
   * IANA time zone the cron is evaluated in, e.g. "Asia/Kolkata".
   *
   * &#64;internal
   * The single timezone authority (DD-009 C-4): validation refuses cron
   * strings carrying their own CRON_TZ=/TZ= prefix. Zone-name validity is
   * checked against the platform tz database in both editions
   * (time.LoadLocation / ZoneId.of); DST evaluation semantics are the
   * Temporal server's concern (DD-008 D2), identical for both editions.
   * </pre>
   *
   * <code>string time_zone = 2 [json_name = "timeZone", (.buf.validate.field) = { ... }</code>
   * @return The bytes for timeZone.
   */
  @java.lang.Override
  public com.google.protobuf.ByteString
      getTimeZoneBytes() {
    java.lang.Object ref = timeZone_;
    if (ref instanceof java.lang.String) {
      com.google.protobuf.ByteString b = 
          com.google.protobuf.ByteString.copyFromUtf8(
              (java.lang.String) ref);
      timeZone_ = b;
      return b;
    } else {
      return (com.google.protobuf.ByteString) ref;
    }
  }

  public static final int ENABLED_FIELD_NUMBER = 3;
  private boolean enabled_ = false;
  /**
   * <pre>
   * Whether the schedule fires. Off disables firing without deleting
   * the schedule or its history.
   *
   * &#64;internal
   * The owner's switch — "disabled", deliberately distinct from
   * "paused", the platform's failure-streak latch recorded on
   * status.paused_reason and never on spec (DD-008 D7, DD-013 D-E:
   * the two words name two levers with two writers). Sibling
   * bool shape (AgentShareSpec/AgentChannelSpec.enabled): manifests set
   * `enabled: true` explicitly, no edition defaults it, and no status
   * field echoes it (DD-009 pinned behaviors — consoles derive
   * owner-disabled state from spec on read).
   * </pre>
   *
   * <code>bool enabled = 3 [json_name = "enabled"];</code>
   * @return The enabled.
   */
  @java.lang.Override
  public boolean getEnabled() {
    return enabled_;
  }

  public static final int AGENT_FIELD_NUMBER = 4;
  /**
   * <pre>
   * Run an agent with a configured prompt at each fire.
   *
   * &#64;internal
   * The shared owner-settable run shape (DD-018 D-3), replacing the
   * deleted schedule-only AgentTarget/ScheduleRunConfig pair.
   * Schedule-specific invariants enforced in create/update/apply
   * handlers of both editions, never on the shared message:
   * agent_ref.org must equal metadata.org (the schedule-owning org
   * is the billing org for every fire; creation requires can_edit
   * on the referenced agent, DD-009 C-6); workspace sources must be
   * git_repo (no client is connected at fire time to serve a
   * local_path); agent_ref is immutable across updates while
   * harness, model, workspace, message, and environments stay
   * mutable — every fire builds a fresh session, so nothing
   * session-immutable is ever mutated mid-flight. environment_refs
   * resolution is unchanged from DD-017 D-2/D-4: claim-driven in
   * the execution-context step, org-shared environments only,
   * LOWEST merge priority, enforcement solely at runtime
   * resolution. run_config clamping is unchanged from DD-017 D-3:
   * per-field min(owner, platform) in the run starter.
   * </pre>
   *
   * <code>.ai.stigmer.agentic.agentexecution.v1.AgentInvocation agent = 4 [json_name = "agent"];</code>
   * @return Whether the agent field is set.
   */
  @java.lang.Override
  public boolean hasAgent() {
    return targetCase_ == 4;
  }
  /**
   * <pre>
   * Run an agent with a configured prompt at each fire.
   *
   * &#64;internal
   * The shared owner-settable run shape (DD-018 D-3), replacing the
   * deleted schedule-only AgentTarget/ScheduleRunConfig pair.
   * Schedule-specific invariants enforced in create/update/apply
   * handlers of both editions, never on the shared message:
   * agent_ref.org must equal metadata.org (the schedule-owning org
   * is the billing org for every fire; creation requires can_edit
   * on the referenced agent, DD-009 C-6); workspace sources must be
   * git_repo (no client is connected at fire time to serve a
   * local_path); agent_ref is immutable across updates while
   * harness, model, workspace, message, and environments stay
   * mutable — every fire builds a fresh session, so nothing
   * session-immutable is ever mutated mid-flight. environment_refs
   * resolution is unchanged from DD-017 D-2/D-4: claim-driven in
   * the execution-context step, org-shared environments only,
   * LOWEST merge priority, enforcement solely at runtime
   * resolution. run_config clamping is unchanged from DD-017 D-3:
   * per-field min(owner, platform) in the run starter.
   * </pre>
   *
   * <code>.ai.stigmer.agentic.agentexecution.v1.AgentInvocation agent = 4 [json_name = "agent"];</code>
   * @return The agent.
   */
  @java.lang.Override
  public ai.stigmer.agentic.agentexecution.v1.AgentInvocation getAgent() {
    if (targetCase_ == 4) {
       return (ai.stigmer.agentic.agentexecution.v1.AgentInvocation) target_;
    }
    return ai.stigmer.agentic.agentexecution.v1.AgentInvocation.getDefaultInstance();
  }
  /**
   * <pre>
   * Run an agent with a configured prompt at each fire.
   *
   * &#64;internal
   * The shared owner-settable run shape (DD-018 D-3), replacing the
   * deleted schedule-only AgentTarget/ScheduleRunConfig pair.
   * Schedule-specific invariants enforced in create/update/apply
   * handlers of both editions, never on the shared message:
   * agent_ref.org must equal metadata.org (the schedule-owning org
   * is the billing org for every fire; creation requires can_edit
   * on the referenced agent, DD-009 C-6); workspace sources must be
   * git_repo (no client is connected at fire time to serve a
   * local_path); agent_ref is immutable across updates while
   * harness, model, workspace, message, and environments stay
   * mutable — every fire builds a fresh session, so nothing
   * session-immutable is ever mutated mid-flight. environment_refs
   * resolution is unchanged from DD-017 D-2/D-4: claim-driven in
   * the execution-context step, org-shared environments only,
   * LOWEST merge priority, enforcement solely at runtime
   * resolution. run_config clamping is unchanged from DD-017 D-3:
   * per-field min(owner, platform) in the run starter.
   * </pre>
   *
   * <code>.ai.stigmer.agentic.agentexecution.v1.AgentInvocation agent = 4 [json_name = "agent"];</code>
   */
  @java.lang.Override
  public ai.stigmer.agentic.agentexecution.v1.AgentInvocationOrBuilder getAgentOrBuilder() {
    if (targetCase_ == 4) {
       return (ai.stigmer.agentic.agentexecution.v1.AgentInvocation) target_;
    }
    return ai.stigmer.agentic.agentexecution.v1.AgentInvocation.getDefaultInstance();
  }

  private byte memoizedIsInitialized = -1;
  @java.lang.Override
  public final boolean isInitialized() {
    byte isInitialized = memoizedIsInitialized;
    if (isInitialized == 1) return true;
    if (isInitialized == 0) return false;

    memoizedIsInitialized = 1;
    return true;
  }

  @java.lang.Override
  public void writeTo(com.google.protobuf.CodedOutputStream output)
                      throws java.io.IOException {
    if (!com.google.protobuf.GeneratedMessage.isStringEmpty(cron_)) {
      com.google.protobuf.GeneratedMessage.writeString(output, 1, cron_);
    }
    if (!com.google.protobuf.GeneratedMessage.isStringEmpty(timeZone_)) {
      com.google.protobuf.GeneratedMessage.writeString(output, 2, timeZone_);
    }
    if (enabled_ != false) {
      output.writeBool(3, enabled_);
    }
    if (targetCase_ == 4) {
      output.writeMessage(4, (ai.stigmer.agentic.agentexecution.v1.AgentInvocation) target_);
    }
    getUnknownFields().writeTo(output);
  }

  @java.lang.Override
  public int getSerializedSize() {
    int size = memoizedSize;
    if (size != -1) return size;

    size = 0;
    if (!com.google.protobuf.GeneratedMessage.isStringEmpty(cron_)) {
      size += com.google.protobuf.GeneratedMessage.computeStringSize(1, cron_);
    }
    if (!com.google.protobuf.GeneratedMessage.isStringEmpty(timeZone_)) {
      size += com.google.protobuf.GeneratedMessage.computeStringSize(2, timeZone_);
    }
    if (enabled_ != false) {
      size += com.google.protobuf.CodedOutputStream
        .computeBoolSize(3, enabled_);
    }
    if (targetCase_ == 4) {
      size += com.google.protobuf.CodedOutputStream
        .computeMessageSize(4, (ai.stigmer.agentic.agentexecution.v1.AgentInvocation) target_);
    }
    size += getUnknownFields().getSerializedSize();
    memoizedSize = size;
    return size;
  }

  @java.lang.Override
  public boolean equals(final java.lang.Object obj) {
    if (obj == this) {
     return true;
    }
    if (!(obj instanceof ai.stigmer.agentic.schedule.v1.ScheduleSpec)) {
      return super.equals(obj);
    }
    ai.stigmer.agentic.schedule.v1.ScheduleSpec other = (ai.stigmer.agentic.schedule.v1.ScheduleSpec) obj;

    if (!getCron()
        .equals(other.getCron())) return false;
    if (!getTimeZone()
        .equals(other.getTimeZone())) return false;
    if (getEnabled()
        != other.getEnabled()) return false;
    if (!getTargetCase().equals(other.getTargetCase())) return false;
    switch (targetCase_) {
      case 4:
        if (!getAgent()
            .equals(other.getAgent())) return false;
        break;
      case 0:
      default:
    }
    if (!getUnknownFields().equals(other.getUnknownFields())) return false;
    return true;
  }

  @java.lang.Override
  public int hashCode() {
    if (memoizedHashCode != 0) {
      return memoizedHashCode;
    }
    int hash = 41;
    hash = (19 * hash) + getDescriptor().hashCode();
    hash = (37 * hash) + CRON_FIELD_NUMBER;
    hash = (53 * hash) + getCron().hashCode();
    hash = (37 * hash) + TIME_ZONE_FIELD_NUMBER;
    hash = (53 * hash) + getTimeZone().hashCode();
    hash = (37 * hash) + ENABLED_FIELD_NUMBER;
    hash = (53 * hash) + com.google.protobuf.Internal.hashBoolean(
        getEnabled());
    switch (targetCase_) {
      case 4:
        hash = (37 * hash) + AGENT_FIELD_NUMBER;
        hash = (53 * hash) + getAgent().hashCode();
        break;
      case 0:
      default:
    }
    hash = (29 * hash) + getUnknownFields().hashCode();
    memoizedHashCode = hash;
    return hash;
  }

  public static ai.stigmer.agentic.schedule.v1.ScheduleSpec parseFrom(
      java.nio.ByteBuffer data)
      throws com.google.protobuf.InvalidProtocolBufferException {
    return PARSER.parseFrom(data);
  }
  public static ai.stigmer.agentic.schedule.v1.ScheduleSpec parseFrom(
      java.nio.ByteBuffer data,
      com.google.protobuf.ExtensionRegistryLite extensionRegistry)
      throws com.google.protobuf.InvalidProtocolBufferException {
    return PARSER.parseFrom(data, extensionRegistry);
  }
  public static ai.stigmer.agentic.schedule.v1.ScheduleSpec parseFrom(
      com.google.protobuf.ByteString data)
      throws com.google.protobuf.InvalidProtocolBufferException {
    return PARSER.parseFrom(data);
  }
  public static ai.stigmer.agentic.schedule.v1.ScheduleSpec parseFrom(
      com.google.protobuf.ByteString data,
      com.google.protobuf.ExtensionRegistryLite extensionRegistry)
      throws com.google.protobuf.InvalidProtocolBufferException {
    return PARSER.parseFrom(data, extensionRegistry);
  }
  public static ai.stigmer.agentic.schedule.v1.ScheduleSpec parseFrom(byte[] data)
      throws com.google.protobuf.InvalidProtocolBufferException {
    return PARSER.parseFrom(data);
  }
  public static ai.stigmer.agentic.schedule.v1.ScheduleSpec parseFrom(
      byte[] data,
      com.google.protobuf.ExtensionRegistryLite extensionRegistry)
      throws com.google.protobuf.InvalidProtocolBufferException {
    return PARSER.parseFrom(data, extensionRegistry);
  }
  public static ai.stigmer.agentic.schedule.v1.ScheduleSpec parseFrom(java.io.InputStream input)
      throws java.io.IOException {
    return com.google.protobuf.GeneratedMessage
        .parseWithIOException(PARSER, input);
  }
  public static ai.stigmer.agentic.schedule.v1.ScheduleSpec parseFrom(
      java.io.InputStream input,
      com.google.protobuf.ExtensionRegistryLite extensionRegistry)
      throws java.io.IOException {
    return com.google.protobuf.GeneratedMessage
        .parseWithIOException(PARSER, input, extensionRegistry);
  }

  public static ai.stigmer.agentic.schedule.v1.ScheduleSpec parseDelimitedFrom(java.io.InputStream input)
      throws java.io.IOException {
    return com.google.protobuf.GeneratedMessage
        .parseDelimitedWithIOException(PARSER, input);
  }

  public static ai.stigmer.agentic.schedule.v1.ScheduleSpec parseDelimitedFrom(
      java.io.InputStream input,
      com.google.protobuf.ExtensionRegistryLite extensionRegistry)
      throws java.io.IOException {
    return com.google.protobuf.GeneratedMessage
        .parseDelimitedWithIOException(PARSER, input, extensionRegistry);
  }
  public static ai.stigmer.agentic.schedule.v1.ScheduleSpec parseFrom(
      com.google.protobuf.CodedInputStream input)
      throws java.io.IOException {
    return com.google.protobuf.GeneratedMessage
        .parseWithIOException(PARSER, input);
  }
  public static ai.stigmer.agentic.schedule.v1.ScheduleSpec parseFrom(
      com.google.protobuf.CodedInputStream input,
      com.google.protobuf.ExtensionRegistryLite extensionRegistry)
      throws java.io.IOException {
    return com.google.protobuf.GeneratedMessage
        .parseWithIOException(PARSER, input, extensionRegistry);
  }

  @java.lang.Override
  public Builder newBuilderForType() { return newBuilder(); }
  public static Builder newBuilder() {
    return DEFAULT_INSTANCE.toBuilder();
  }
  public static Builder newBuilder(ai.stigmer.agentic.schedule.v1.ScheduleSpec prototype) {
    return DEFAULT_INSTANCE.toBuilder().mergeFrom(prototype);
  }
  @java.lang.Override
  public Builder toBuilder() {
    return this == DEFAULT_INSTANCE
        ? new Builder() : new Builder().mergeFrom(this);
  }

  @java.lang.Override
  protected Builder newBuilderForType(
      com.google.protobuf.GeneratedMessage.BuilderParent parent) {
    Builder builder = new Builder(parent);
    return builder;
  }
  /**
   * <pre>
   * ScheduleSpec defines when a schedule fires and what it runs.
   *
   * The spec is deliberately small: a cron expression, the IANA time zone
   * it is evaluated in, the owner's enablement switch, and the target each
   * fire runs. Everything the platform observes about firing lives in
   * status — a declarative apply can never clobber it.
   *
   * &#64;internal
   * DD-008 D8 as amended by DD-009 and DD-018: the target oneof is inline
   * (the AgentChannelSpec.provider_config shape, C-3) and the agent arm is
   * the shared AgentInvocation (DD-018 D-3) — the owner-settable subset of
   * an agent run, so the schedule speaks the composer's vocabulary instead
   * of a schedule-only one. Apply-time cron validation is purely lexical
   * (C-4) so no cron parser exists in either edition; the Temporal server
   * owns calendar/DST semantics (D2).
   * </pre>
   *
   * Protobuf type {@code ai.stigmer.agentic.schedule.v1.ScheduleSpec}
   */
  public static final class Builder extends
      com.google.protobuf.GeneratedMessage.Builder<Builder> implements
      // @@protoc_insertion_point(builder_implements:ai.stigmer.agentic.schedule.v1.ScheduleSpec)
      ai.stigmer.agentic.schedule.v1.ScheduleSpecOrBuilder {
    public static final com.google.protobuf.Descriptors.Descriptor
        getDescriptor() {
      return ai.stigmer.agentic.schedule.v1.SpecProto.internal_static_ai_stigmer_agentic_schedule_v1_ScheduleSpec_descriptor;
    }

    @java.lang.Override
    protected com.google.protobuf.GeneratedMessage.FieldAccessorTable
        internalGetFieldAccessorTable() {
      return ai.stigmer.agentic.schedule.v1.SpecProto.internal_static_ai_stigmer_agentic_schedule_v1_ScheduleSpec_fieldAccessorTable
          .ensureFieldAccessorsInitialized(
              ai.stigmer.agentic.schedule.v1.ScheduleSpec.class, ai.stigmer.agentic.schedule.v1.ScheduleSpec.Builder.class);
    }

    // Construct using ai.stigmer.agentic.schedule.v1.ScheduleSpec.newBuilder()
    private Builder() {

    }

    private Builder(
        com.google.protobuf.GeneratedMessage.BuilderParent parent) {
      super(parent);

    }
    @java.lang.Override
    public Builder clear() {
      super.clear();
      bitField0_ = 0;
      cron_ = "";
      timeZone_ = "";
      enabled_ = false;
      if (agentBuilder_ != null) {
        agentBuilder_.clear();
      }
      targetCase_ = 0;
      target_ = null;
      return this;
    }

    @java.lang.Override
    public com.google.protobuf.Descriptors.Descriptor
        getDescriptorForType() {
      return ai.stigmer.agentic.schedule.v1.SpecProto.internal_static_ai_stigmer_agentic_schedule_v1_ScheduleSpec_descriptor;
    }

    @java.lang.Override
    public ai.stigmer.agentic.schedule.v1.ScheduleSpec getDefaultInstanceForType() {
      return ai.stigmer.agentic.schedule.v1.ScheduleSpec.getDefaultInstance();
    }

    @java.lang.Override
    public ai.stigmer.agentic.schedule.v1.ScheduleSpec build() {
      ai.stigmer.agentic.schedule.v1.ScheduleSpec result = buildPartial();
      if (!result.isInitialized()) {
        throw newUninitializedMessageException(result);
      }
      return result;
    }

    @java.lang.Override
    public ai.stigmer.agentic.schedule.v1.ScheduleSpec buildPartial() {
      ai.stigmer.agentic.schedule.v1.ScheduleSpec result = new ai.stigmer.agentic.schedule.v1.ScheduleSpec(this);
      if (bitField0_ != 0) { buildPartial0(result); }
      buildPartialOneofs(result);
      onBuilt();
      return result;
    }

    private void buildPartial0(ai.stigmer.agentic.schedule.v1.ScheduleSpec result) {
      int from_bitField0_ = bitField0_;
      if (((from_bitField0_ & 0x00000001) != 0)) {
        result.cron_ = cron_;
      }
      if (((from_bitField0_ & 0x00000002) != 0)) {
        result.timeZone_ = timeZone_;
      }
      if (((from_bitField0_ & 0x00000004) != 0)) {
        result.enabled_ = enabled_;
      }
    }

    private void buildPartialOneofs(ai.stigmer.agentic.schedule.v1.ScheduleSpec result) {
      result.targetCase_ = targetCase_;
      result.target_ = this.target_;
      if (targetCase_ == 4 &&
          agentBuilder_ != null) {
        result.target_ = agentBuilder_.build();
      }
    }

    @java.lang.Override
    public Builder mergeFrom(com.google.protobuf.Message other) {
      if (other instanceof ai.stigmer.agentic.schedule.v1.ScheduleSpec) {
        return mergeFrom((ai.stigmer.agentic.schedule.v1.ScheduleSpec)other);
      } else {
        super.mergeFrom(other);
        return this;
      }
    }

    public Builder mergeFrom(ai.stigmer.agentic.schedule.v1.ScheduleSpec other) {
      if (other == ai.stigmer.agentic.schedule.v1.ScheduleSpec.getDefaultInstance()) return this;
      if (!other.getCron().isEmpty()) {
        cron_ = other.cron_;
        bitField0_ |= 0x00000001;
        onChanged();
      }
      if (!other.getTimeZone().isEmpty()) {
        timeZone_ = other.timeZone_;
        bitField0_ |= 0x00000002;
        onChanged();
      }
      if (other.getEnabled() != false) {
        setEnabled(other.getEnabled());
      }
      switch (other.getTargetCase()) {
        case AGENT: {
          mergeAgent(other.getAgent());
          break;
        }
        case TARGET_NOT_SET: {
          break;
        }
      }
      this.mergeUnknownFields(other.getUnknownFields());
      onChanged();
      return this;
    }

    @java.lang.Override
    public final boolean isInitialized() {
      return true;
    }

    @java.lang.Override
    public Builder mergeFrom(
        com.google.protobuf.CodedInputStream input,
        com.google.protobuf.ExtensionRegistryLite extensionRegistry)
        throws java.io.IOException {
      if (extensionRegistry == null) {
        throw new java.lang.NullPointerException();
      }
      try {
        boolean done = false;
        while (!done) {
          int tag = input.readTag();
          switch (tag) {
            case 0:
              done = true;
              break;
            case 10: {
              cron_ = input.readStringRequireUtf8();
              bitField0_ |= 0x00000001;
              break;
            } // case 10
            case 18: {
              timeZone_ = input.readStringRequireUtf8();
              bitField0_ |= 0x00000002;
              break;
            } // case 18
            case 24: {
              enabled_ = input.readBool();
              bitField0_ |= 0x00000004;
              break;
            } // case 24
            case 34: {
              input.readMessage(
                  internalGetAgentFieldBuilder().getBuilder(),
                  extensionRegistry);
              targetCase_ = 4;
              break;
            } // case 34
            default: {
              if (!super.parseUnknownField(input, extensionRegistry, tag)) {
                done = true; // was an endgroup tag
              }
              break;
            } // default:
          } // switch (tag)
        } // while (!done)
      } catch (com.google.protobuf.InvalidProtocolBufferException e) {
        throw e.unwrapIOException();
      } finally {
        onChanged();
      } // finally
      return this;
    }
    private int targetCase_ = 0;
    private java.lang.Object target_;
    public TargetCase
        getTargetCase() {
      return TargetCase.forNumber(
          targetCase_);
    }

    public Builder clearTarget() {
      targetCase_ = 0;
      target_ = null;
      onChanged();
      return this;
    }

    private int bitField0_;

    private java.lang.Object cron_ = "";
    /**
     * <pre>
     * Cron expression in the classic 5-field form (minute, hour,
     * day-of-month, month, day-of-week), evaluated in `time_zone` — for
     * example "0 9 * * *" for daily at 09:00. The shorthands &#64;hourly,
     * &#64;daily, &#64;weekly, &#64;monthly, and &#64;yearly are also accepted. Timezone
     * prefixes (CRON_TZ=/TZ=), &#64;every intervals, 6- or 7-field forms, and
     * trailing comments are rejected.
     *
     * &#64;internal
     * DD-009 C-4: deliberately narrower than the Temporal server's cron
     * grammar. CRON_TZ=/TZ= would carry a second timezone authority beside
     * `time_zone` (Temporal itself rejects the combination at
     * schedule-create time — accepting it here would store a spec that
     * detonates when the clock lands); &#64;every compiles to an interval spec
     * and bypasses the calendar model; the 7-field form has a seconds
     * column. The 5-field restriction is a structural one-minute firing
     * floor with zero cron parsing. The configurable interval floor
     * (stigmer.schedules.limits, DD-008 D7) lands with the clock, enforced
     * against Temporal's own computed fire times. Widening this grammar
     * later is additive; narrowing it would break stored specs.
     * </pre>
     *
     * <code>string cron = 1 [json_name = "cron", (.buf.validate.field) = { ... }</code>
     * @return The cron.
     */
    public java.lang.String getCron() {
      java.lang.Object ref = cron_;
      if (!(ref instanceof java.lang.String)) {
        com.google.protobuf.ByteString bs =
            (com.google.protobuf.ByteString) ref;
        java.lang.String s = bs.toStringUtf8();
        cron_ = s;
        return s;
      } else {
        return (java.lang.String) ref;
      }
    }
    /**
     * <pre>
     * Cron expression in the classic 5-field form (minute, hour,
     * day-of-month, month, day-of-week), evaluated in `time_zone` — for
     * example "0 9 * * *" for daily at 09:00. The shorthands &#64;hourly,
     * &#64;daily, &#64;weekly, &#64;monthly, and &#64;yearly are also accepted. Timezone
     * prefixes (CRON_TZ=/TZ=), &#64;every intervals, 6- or 7-field forms, and
     * trailing comments are rejected.
     *
     * &#64;internal
     * DD-009 C-4: deliberately narrower than the Temporal server's cron
     * grammar. CRON_TZ=/TZ= would carry a second timezone authority beside
     * `time_zone` (Temporal itself rejects the combination at
     * schedule-create time — accepting it here would store a spec that
     * detonates when the clock lands); &#64;every compiles to an interval spec
     * and bypasses the calendar model; the 7-field form has a seconds
     * column. The 5-field restriction is a structural one-minute firing
     * floor with zero cron parsing. The configurable interval floor
     * (stigmer.schedules.limits, DD-008 D7) lands with the clock, enforced
     * against Temporal's own computed fire times. Widening this grammar
     * later is additive; narrowing it would break stored specs.
     * </pre>
     *
     * <code>string cron = 1 [json_name = "cron", (.buf.validate.field) = { ... }</code>
     * @return The bytes for cron.
     */
    public com.google.protobuf.ByteString
        getCronBytes() {
      java.lang.Object ref = cron_;
      if (ref instanceof String) {
        com.google.protobuf.ByteString b = 
            com.google.protobuf.ByteString.copyFromUtf8(
                (java.lang.String) ref);
        cron_ = b;
        return b;
      } else {
        return (com.google.protobuf.ByteString) ref;
      }
    }
    /**
     * <pre>
     * Cron expression in the classic 5-field form (minute, hour,
     * day-of-month, month, day-of-week), evaluated in `time_zone` — for
     * example "0 9 * * *" for daily at 09:00. The shorthands &#64;hourly,
     * &#64;daily, &#64;weekly, &#64;monthly, and &#64;yearly are also accepted. Timezone
     * prefixes (CRON_TZ=/TZ=), &#64;every intervals, 6- or 7-field forms, and
     * trailing comments are rejected.
     *
     * &#64;internal
     * DD-009 C-4: deliberately narrower than the Temporal server's cron
     * grammar. CRON_TZ=/TZ= would carry a second timezone authority beside
     * `time_zone` (Temporal itself rejects the combination at
     * schedule-create time — accepting it here would store a spec that
     * detonates when the clock lands); &#64;every compiles to an interval spec
     * and bypasses the calendar model; the 7-field form has a seconds
     * column. The 5-field restriction is a structural one-minute firing
     * floor with zero cron parsing. The configurable interval floor
     * (stigmer.schedules.limits, DD-008 D7) lands with the clock, enforced
     * against Temporal's own computed fire times. Widening this grammar
     * later is additive; narrowing it would break stored specs.
     * </pre>
     *
     * <code>string cron = 1 [json_name = "cron", (.buf.validate.field) = { ... }</code>
     * @param value The cron to set.
     * @return This builder for chaining.
     */
    public Builder setCron(
        java.lang.String value) {
      if (value == null) { throw new NullPointerException(); }
      cron_ = value;
      bitField0_ |= 0x00000001;
      onChanged();
      return this;
    }
    /**
     * <pre>
     * Cron expression in the classic 5-field form (minute, hour,
     * day-of-month, month, day-of-week), evaluated in `time_zone` — for
     * example "0 9 * * *" for daily at 09:00. The shorthands &#64;hourly,
     * &#64;daily, &#64;weekly, &#64;monthly, and &#64;yearly are also accepted. Timezone
     * prefixes (CRON_TZ=/TZ=), &#64;every intervals, 6- or 7-field forms, and
     * trailing comments are rejected.
     *
     * &#64;internal
     * DD-009 C-4: deliberately narrower than the Temporal server's cron
     * grammar. CRON_TZ=/TZ= would carry a second timezone authority beside
     * `time_zone` (Temporal itself rejects the combination at
     * schedule-create time — accepting it here would store a spec that
     * detonates when the clock lands); &#64;every compiles to an interval spec
     * and bypasses the calendar model; the 7-field form has a seconds
     * column. The 5-field restriction is a structural one-minute firing
     * floor with zero cron parsing. The configurable interval floor
     * (stigmer.schedules.limits, DD-008 D7) lands with the clock, enforced
     * against Temporal's own computed fire times. Widening this grammar
     * later is additive; narrowing it would break stored specs.
     * </pre>
     *
     * <code>string cron = 1 [json_name = "cron", (.buf.validate.field) = { ... }</code>
     * @return This builder for chaining.
     */
    public Builder clearCron() {
      cron_ = getDefaultInstance().getCron();
      bitField0_ = (bitField0_ & ~0x00000001);
      onChanged();
      return this;
    }
    /**
     * <pre>
     * Cron expression in the classic 5-field form (minute, hour,
     * day-of-month, month, day-of-week), evaluated in `time_zone` — for
     * example "0 9 * * *" for daily at 09:00. The shorthands &#64;hourly,
     * &#64;daily, &#64;weekly, &#64;monthly, and &#64;yearly are also accepted. Timezone
     * prefixes (CRON_TZ=/TZ=), &#64;every intervals, 6- or 7-field forms, and
     * trailing comments are rejected.
     *
     * &#64;internal
     * DD-009 C-4: deliberately narrower than the Temporal server's cron
     * grammar. CRON_TZ=/TZ= would carry a second timezone authority beside
     * `time_zone` (Temporal itself rejects the combination at
     * schedule-create time — accepting it here would store a spec that
     * detonates when the clock lands); &#64;every compiles to an interval spec
     * and bypasses the calendar model; the 7-field form has a seconds
     * column. The 5-field restriction is a structural one-minute firing
     * floor with zero cron parsing. The configurable interval floor
     * (stigmer.schedules.limits, DD-008 D7) lands with the clock, enforced
     * against Temporal's own computed fire times. Widening this grammar
     * later is additive; narrowing it would break stored specs.
     * </pre>
     *
     * <code>string cron = 1 [json_name = "cron", (.buf.validate.field) = { ... }</code>
     * @param value The bytes for cron to set.
     * @return This builder for chaining.
     */
    public Builder setCronBytes(
        com.google.protobuf.ByteString value) {
      if (value == null) { throw new NullPointerException(); }
      checkByteStringIsUtf8(value);
      cron_ = value;
      bitField0_ |= 0x00000001;
      onChanged();
      return this;
    }

    private java.lang.Object timeZone_ = "";
    /**
     * <pre>
     * IANA time zone the cron is evaluated in, e.g. "Asia/Kolkata".
     *
     * &#64;internal
     * The single timezone authority (DD-009 C-4): validation refuses cron
     * strings carrying their own CRON_TZ=/TZ= prefix. Zone-name validity is
     * checked against the platform tz database in both editions
     * (time.LoadLocation / ZoneId.of); DST evaluation semantics are the
     * Temporal server's concern (DD-008 D2), identical for both editions.
     * </pre>
     *
     * <code>string time_zone = 2 [json_name = "timeZone", (.buf.validate.field) = { ... }</code>
     * @return The timeZone.
     */
    public java.lang.String getTimeZone() {
      java.lang.Object ref = timeZone_;
      if (!(ref instanceof java.lang.String)) {
        com.google.protobuf.ByteString bs =
            (com.google.protobuf.ByteString) ref;
        java.lang.String s = bs.toStringUtf8();
        timeZone_ = s;
        return s;
      } else {
        return (java.lang.String) ref;
      }
    }
    /**
     * <pre>
     * IANA time zone the cron is evaluated in, e.g. "Asia/Kolkata".
     *
     * &#64;internal
     * The single timezone authority (DD-009 C-4): validation refuses cron
     * strings carrying their own CRON_TZ=/TZ= prefix. Zone-name validity is
     * checked against the platform tz database in both editions
     * (time.LoadLocation / ZoneId.of); DST evaluation semantics are the
     * Temporal server's concern (DD-008 D2), identical for both editions.
     * </pre>
     *
     * <code>string time_zone = 2 [json_name = "timeZone", (.buf.validate.field) = { ... }</code>
     * @return The bytes for timeZone.
     */
    public com.google.protobuf.ByteString
        getTimeZoneBytes() {
      java.lang.Object ref = timeZone_;
      if (ref instanceof String) {
        com.google.protobuf.ByteString b = 
            com.google.protobuf.ByteString.copyFromUtf8(
                (java.lang.String) ref);
        timeZone_ = b;
        return b;
      } else {
        return (com.google.protobuf.ByteString) ref;
      }
    }
    /**
     * <pre>
     * IANA time zone the cron is evaluated in, e.g. "Asia/Kolkata".
     *
     * &#64;internal
     * The single timezone authority (DD-009 C-4): validation refuses cron
     * strings carrying their own CRON_TZ=/TZ= prefix. Zone-name validity is
     * checked against the platform tz database in both editions
     * (time.LoadLocation / ZoneId.of); DST evaluation semantics are the
     * Temporal server's concern (DD-008 D2), identical for both editions.
     * </pre>
     *
     * <code>string time_zone = 2 [json_name = "timeZone", (.buf.validate.field) = { ... }</code>
     * @param value The timeZone to set.
     * @return This builder for chaining.
     */
    public Builder setTimeZone(
        java.lang.String value) {
      if (value == null) { throw new NullPointerException(); }
      timeZone_ = value;
      bitField0_ |= 0x00000002;
      onChanged();
      return this;
    }
    /**
     * <pre>
     * IANA time zone the cron is evaluated in, e.g. "Asia/Kolkata".
     *
     * &#64;internal
     * The single timezone authority (DD-009 C-4): validation refuses cron
     * strings carrying their own CRON_TZ=/TZ= prefix. Zone-name validity is
     * checked against the platform tz database in both editions
     * (time.LoadLocation / ZoneId.of); DST evaluation semantics are the
     * Temporal server's concern (DD-008 D2), identical for both editions.
     * </pre>
     *
     * <code>string time_zone = 2 [json_name = "timeZone", (.buf.validate.field) = { ... }</code>
     * @return This builder for chaining.
     */
    public Builder clearTimeZone() {
      timeZone_ = getDefaultInstance().getTimeZone();
      bitField0_ = (bitField0_ & ~0x00000002);
      onChanged();
      return this;
    }
    /**
     * <pre>
     * IANA time zone the cron is evaluated in, e.g. "Asia/Kolkata".
     *
     * &#64;internal
     * The single timezone authority (DD-009 C-4): validation refuses cron
     * strings carrying their own CRON_TZ=/TZ= prefix. Zone-name validity is
     * checked against the platform tz database in both editions
     * (time.LoadLocation / ZoneId.of); DST evaluation semantics are the
     * Temporal server's concern (DD-008 D2), identical for both editions.
     * </pre>
     *
     * <code>string time_zone = 2 [json_name = "timeZone", (.buf.validate.field) = { ... }</code>
     * @param value The bytes for timeZone to set.
     * @return This builder for chaining.
     */
    public Builder setTimeZoneBytes(
        com.google.protobuf.ByteString value) {
      if (value == null) { throw new NullPointerException(); }
      checkByteStringIsUtf8(value);
      timeZone_ = value;
      bitField0_ |= 0x00000002;
      onChanged();
      return this;
    }

    private boolean enabled_ ;
    /**
     * <pre>
     * Whether the schedule fires. Off disables firing without deleting
     * the schedule or its history.
     *
     * &#64;internal
     * The owner's switch — "disabled", deliberately distinct from
     * "paused", the platform's failure-streak latch recorded on
     * status.paused_reason and never on spec (DD-008 D7, DD-013 D-E:
     * the two words name two levers with two writers). Sibling
     * bool shape (AgentShareSpec/AgentChannelSpec.enabled): manifests set
     * `enabled: true` explicitly, no edition defaults it, and no status
     * field echoes it (DD-009 pinned behaviors — consoles derive
     * owner-disabled state from spec on read).
     * </pre>
     *
     * <code>bool enabled = 3 [json_name = "enabled"];</code>
     * @return The enabled.
     */
    @java.lang.Override
    public boolean getEnabled() {
      return enabled_;
    }
    /**
     * <pre>
     * Whether the schedule fires. Off disables firing without deleting
     * the schedule or its history.
     *
     * &#64;internal
     * The owner's switch — "disabled", deliberately distinct from
     * "paused", the platform's failure-streak latch recorded on
     * status.paused_reason and never on spec (DD-008 D7, DD-013 D-E:
     * the two words name two levers with two writers). Sibling
     * bool shape (AgentShareSpec/AgentChannelSpec.enabled): manifests set
     * `enabled: true` explicitly, no edition defaults it, and no status
     * field echoes it (DD-009 pinned behaviors — consoles derive
     * owner-disabled state from spec on read).
     * </pre>
     *
     * <code>bool enabled = 3 [json_name = "enabled"];</code>
     * @param value The enabled to set.
     * @return This builder for chaining.
     */
    public Builder setEnabled(boolean value) {

      enabled_ = value;
      bitField0_ |= 0x00000004;
      onChanged();
      return this;
    }
    /**
     * <pre>
     * Whether the schedule fires. Off disables firing without deleting
     * the schedule or its history.
     *
     * &#64;internal
     * The owner's switch — "disabled", deliberately distinct from
     * "paused", the platform's failure-streak latch recorded on
     * status.paused_reason and never on spec (DD-008 D7, DD-013 D-E:
     * the two words name two levers with two writers). Sibling
     * bool shape (AgentShareSpec/AgentChannelSpec.enabled): manifests set
     * `enabled: true` explicitly, no edition defaults it, and no status
     * field echoes it (DD-009 pinned behaviors — consoles derive
     * owner-disabled state from spec on read).
     * </pre>
     *
     * <code>bool enabled = 3 [json_name = "enabled"];</code>
     * @return This builder for chaining.
     */
    public Builder clearEnabled() {
      bitField0_ = (bitField0_ & ~0x00000004);
      enabled_ = false;
      onChanged();
      return this;
    }

    private com.google.protobuf.SingleFieldBuilder<
        ai.stigmer.agentic.agentexecution.v1.AgentInvocation, ai.stigmer.agentic.agentexecution.v1.AgentInvocation.Builder, ai.stigmer.agentic.agentexecution.v1.AgentInvocationOrBuilder> agentBuilder_;
    /**
     * <pre>
     * Run an agent with a configured prompt at each fire.
     *
     * &#64;internal
     * The shared owner-settable run shape (DD-018 D-3), replacing the
     * deleted schedule-only AgentTarget/ScheduleRunConfig pair.
     * Schedule-specific invariants enforced in create/update/apply
     * handlers of both editions, never on the shared message:
     * agent_ref.org must equal metadata.org (the schedule-owning org
     * is the billing org for every fire; creation requires can_edit
     * on the referenced agent, DD-009 C-6); workspace sources must be
     * git_repo (no client is connected at fire time to serve a
     * local_path); agent_ref is immutable across updates while
     * harness, model, workspace, message, and environments stay
     * mutable — every fire builds a fresh session, so nothing
     * session-immutable is ever mutated mid-flight. environment_refs
     * resolution is unchanged from DD-017 D-2/D-4: claim-driven in
     * the execution-context step, org-shared environments only,
     * LOWEST merge priority, enforcement solely at runtime
     * resolution. run_config clamping is unchanged from DD-017 D-3:
     * per-field min(owner, platform) in the run starter.
     * </pre>
     *
     * <code>.ai.stigmer.agentic.agentexecution.v1.AgentInvocation agent = 4 [json_name = "agent"];</code>
     * @return Whether the agent field is set.
     */
    @java.lang.Override
    public boolean hasAgent() {
      return targetCase_ == 4;
    }
    /**
     * <pre>
     * Run an agent with a configured prompt at each fire.
     *
     * &#64;internal
     * The shared owner-settable run shape (DD-018 D-3), replacing the
     * deleted schedule-only AgentTarget/ScheduleRunConfig pair.
     * Schedule-specific invariants enforced in create/update/apply
     * handlers of both editions, never on the shared message:
     * agent_ref.org must equal metadata.org (the schedule-owning org
     * is the billing org for every fire; creation requires can_edit
     * on the referenced agent, DD-009 C-6); workspace sources must be
     * git_repo (no client is connected at fire time to serve a
     * local_path); agent_ref is immutable across updates while
     * harness, model, workspace, message, and environments stay
     * mutable — every fire builds a fresh session, so nothing
     * session-immutable is ever mutated mid-flight. environment_refs
     * resolution is unchanged from DD-017 D-2/D-4: claim-driven in
     * the execution-context step, org-shared environments only,
     * LOWEST merge priority, enforcement solely at runtime
     * resolution. run_config clamping is unchanged from DD-017 D-3:
     * per-field min(owner, platform) in the run starter.
     * </pre>
     *
     * <code>.ai.stigmer.agentic.agentexecution.v1.AgentInvocation agent = 4 [json_name = "agent"];</code>
     * @return The agent.
     */
    @java.lang.Override
    public ai.stigmer.agentic.agentexecution.v1.AgentInvocation getAgent() {
      if (agentBuilder_ == null) {
        if (targetCase_ == 4) {
          return (ai.stigmer.agentic.agentexecution.v1.AgentInvocation) target_;
        }
        return ai.stigmer.agentic.agentexecution.v1.AgentInvocation.getDefaultInstance();
      } else {
        if (targetCase_ == 4) {
          return agentBuilder_.getMessage();
        }
        return ai.stigmer.agentic.agentexecution.v1.AgentInvocation.getDefaultInstance();
      }
    }
    /**
     * <pre>
     * Run an agent with a configured prompt at each fire.
     *
     * &#64;internal
     * The shared owner-settable run shape (DD-018 D-3), replacing the
     * deleted schedule-only AgentTarget/ScheduleRunConfig pair.
     * Schedule-specific invariants enforced in create/update/apply
     * handlers of both editions, never on the shared message:
     * agent_ref.org must equal metadata.org (the schedule-owning org
     * is the billing org for every fire; creation requires can_edit
     * on the referenced agent, DD-009 C-6); workspace sources must be
     * git_repo (no client is connected at fire time to serve a
     * local_path); agent_ref is immutable across updates while
     * harness, model, workspace, message, and environments stay
     * mutable — every fire builds a fresh session, so nothing
     * session-immutable is ever mutated mid-flight. environment_refs
     * resolution is unchanged from DD-017 D-2/D-4: claim-driven in
     * the execution-context step, org-shared environments only,
     * LOWEST merge priority, enforcement solely at runtime
     * resolution. run_config clamping is unchanged from DD-017 D-3:
     * per-field min(owner, platform) in the run starter.
     * </pre>
     *
     * <code>.ai.stigmer.agentic.agentexecution.v1.AgentInvocation agent = 4 [json_name = "agent"];</code>
     */
    public Builder setAgent(ai.stigmer.agentic.agentexecution.v1.AgentInvocation value) {
      if (agentBuilder_ == null) {
        if (value == null) {
          throw new NullPointerException();
        }
        target_ = value;
        onChanged();
      } else {
        agentBuilder_.setMessage(value);
      }
      targetCase_ = 4;
      return this;
    }
    /**
     * <pre>
     * Run an agent with a configured prompt at each fire.
     *
     * &#64;internal
     * The shared owner-settable run shape (DD-018 D-3), replacing the
     * deleted schedule-only AgentTarget/ScheduleRunConfig pair.
     * Schedule-specific invariants enforced in create/update/apply
     * handlers of both editions, never on the shared message:
     * agent_ref.org must equal metadata.org (the schedule-owning org
     * is the billing org for every fire; creation requires can_edit
     * on the referenced agent, DD-009 C-6); workspace sources must be
     * git_repo (no client is connected at fire time to serve a
     * local_path); agent_ref is immutable across updates while
     * harness, model, workspace, message, and environments stay
     * mutable — every fire builds a fresh session, so nothing
     * session-immutable is ever mutated mid-flight. environment_refs
     * resolution is unchanged from DD-017 D-2/D-4: claim-driven in
     * the execution-context step, org-shared environments only,
     * LOWEST merge priority, enforcement solely at runtime
     * resolution. run_config clamping is unchanged from DD-017 D-3:
     * per-field min(owner, platform) in the run starter.
     * </pre>
     *
     * <code>.ai.stigmer.agentic.agentexecution.v1.AgentInvocation agent = 4 [json_name = "agent"];</code>
     */
    public Builder setAgent(
        ai.stigmer.agentic.agentexecution.v1.AgentInvocation.Builder builderForValue) {
      if (agentBuilder_ == null) {
        target_ = builderForValue.build();
        onChanged();
      } else {
        agentBuilder_.setMessage(builderForValue.build());
      }
      targetCase_ = 4;
      return this;
    }
    /**
     * <pre>
     * Run an agent with a configured prompt at each fire.
     *
     * &#64;internal
     * The shared owner-settable run shape (DD-018 D-3), replacing the
     * deleted schedule-only AgentTarget/ScheduleRunConfig pair.
     * Schedule-specific invariants enforced in create/update/apply
     * handlers of both editions, never on the shared message:
     * agent_ref.org must equal metadata.org (the schedule-owning org
     * is the billing org for every fire; creation requires can_edit
     * on the referenced agent, DD-009 C-6); workspace sources must be
     * git_repo (no client is connected at fire time to serve a
     * local_path); agent_ref is immutable across updates while
     * harness, model, workspace, message, and environments stay
     * mutable — every fire builds a fresh session, so nothing
     * session-immutable is ever mutated mid-flight. environment_refs
     * resolution is unchanged from DD-017 D-2/D-4: claim-driven in
     * the execution-context step, org-shared environments only,
     * LOWEST merge priority, enforcement solely at runtime
     * resolution. run_config clamping is unchanged from DD-017 D-3:
     * per-field min(owner, platform) in the run starter.
     * </pre>
     *
     * <code>.ai.stigmer.agentic.agentexecution.v1.AgentInvocation agent = 4 [json_name = "agent"];</code>
     */
    public Builder mergeAgent(ai.stigmer.agentic.agentexecution.v1.AgentInvocation value) {
      if (agentBuilder_ == null) {
        if (targetCase_ == 4 &&
            target_ != ai.stigmer.agentic.agentexecution.v1.AgentInvocation.getDefaultInstance()) {
          target_ = ai.stigmer.agentic.agentexecution.v1.AgentInvocation.newBuilder((ai.stigmer.agentic.agentexecution.v1.AgentInvocation) target_)
              .mergeFrom(value).buildPartial();
        } else {
          target_ = value;
        }
        onChanged();
      } else {
        if (targetCase_ == 4) {
          agentBuilder_.mergeFrom(value);
        } else {
          agentBuilder_.setMessage(value);
        }
      }
      targetCase_ = 4;
      return this;
    }
    /**
     * <pre>
     * Run an agent with a configured prompt at each fire.
     *
     * &#64;internal
     * The shared owner-settable run shape (DD-018 D-3), replacing the
     * deleted schedule-only AgentTarget/ScheduleRunConfig pair.
     * Schedule-specific invariants enforced in create/update/apply
     * handlers of both editions, never on the shared message:
     * agent_ref.org must equal metadata.org (the schedule-owning org
     * is the billing org for every fire; creation requires can_edit
     * on the referenced agent, DD-009 C-6); workspace sources must be
     * git_repo (no client is connected at fire time to serve a
     * local_path); agent_ref is immutable across updates while
     * harness, model, workspace, message, and environments stay
     * mutable — every fire builds a fresh session, so nothing
     * session-immutable is ever mutated mid-flight. environment_refs
     * resolution is unchanged from DD-017 D-2/D-4: claim-driven in
     * the execution-context step, org-shared environments only,
     * LOWEST merge priority, enforcement solely at runtime
     * resolution. run_config clamping is unchanged from DD-017 D-3:
     * per-field min(owner, platform) in the run starter.
     * </pre>
     *
     * <code>.ai.stigmer.agentic.agentexecution.v1.AgentInvocation agent = 4 [json_name = "agent"];</code>
     */
    public Builder clearAgent() {
      if (agentBuilder_ == null) {
        if (targetCase_ == 4) {
          targetCase_ = 0;
          target_ = null;
          onChanged();
        }
      } else {
        if (targetCase_ == 4) {
          targetCase_ = 0;
          target_ = null;
        }
        agentBuilder_.clear();
      }
      return this;
    }
    /**
     * <pre>
     * Run an agent with a configured prompt at each fire.
     *
     * &#64;internal
     * The shared owner-settable run shape (DD-018 D-3), replacing the
     * deleted schedule-only AgentTarget/ScheduleRunConfig pair.
     * Schedule-specific invariants enforced in create/update/apply
     * handlers of both editions, never on the shared message:
     * agent_ref.org must equal metadata.org (the schedule-owning org
     * is the billing org for every fire; creation requires can_edit
     * on the referenced agent, DD-009 C-6); workspace sources must be
     * git_repo (no client is connected at fire time to serve a
     * local_path); agent_ref is immutable across updates while
     * harness, model, workspace, message, and environments stay
     * mutable — every fire builds a fresh session, so nothing
     * session-immutable is ever mutated mid-flight. environment_refs
     * resolution is unchanged from DD-017 D-2/D-4: claim-driven in
     * the execution-context step, org-shared environments only,
     * LOWEST merge priority, enforcement solely at runtime
     * resolution. run_config clamping is unchanged from DD-017 D-3:
     * per-field min(owner, platform) in the run starter.
     * </pre>
     *
     * <code>.ai.stigmer.agentic.agentexecution.v1.AgentInvocation agent = 4 [json_name = "agent"];</code>
     */
    public ai.stigmer.agentic.agentexecution.v1.AgentInvocation.Builder getAgentBuilder() {
      return internalGetAgentFieldBuilder().getBuilder();
    }
    /**
     * <pre>
     * Run an agent with a configured prompt at each fire.
     *
     * &#64;internal
     * The shared owner-settable run shape (DD-018 D-3), replacing the
     * deleted schedule-only AgentTarget/ScheduleRunConfig pair.
     * Schedule-specific invariants enforced in create/update/apply
     * handlers of both editions, never on the shared message:
     * agent_ref.org must equal metadata.org (the schedule-owning org
     * is the billing org for every fire; creation requires can_edit
     * on the referenced agent, DD-009 C-6); workspace sources must be
     * git_repo (no client is connected at fire time to serve a
     * local_path); agent_ref is immutable across updates while
     * harness, model, workspace, message, and environments stay
     * mutable — every fire builds a fresh session, so nothing
     * session-immutable is ever mutated mid-flight. environment_refs
     * resolution is unchanged from DD-017 D-2/D-4: claim-driven in
     * the execution-context step, org-shared environments only,
     * LOWEST merge priority, enforcement solely at runtime
     * resolution. run_config clamping is unchanged from DD-017 D-3:
     * per-field min(owner, platform) in the run starter.
     * </pre>
     *
     * <code>.ai.stigmer.agentic.agentexecution.v1.AgentInvocation agent = 4 [json_name = "agent"];</code>
     */
    @java.lang.Override
    public ai.stigmer.agentic.agentexecution.v1.AgentInvocationOrBuilder getAgentOrBuilder() {
      if ((targetCase_ == 4) && (agentBuilder_ != null)) {
        return agentBuilder_.getMessageOrBuilder();
      } else {
        if (targetCase_ == 4) {
          return (ai.stigmer.agentic.agentexecution.v1.AgentInvocation) target_;
        }
        return ai.stigmer.agentic.agentexecution.v1.AgentInvocation.getDefaultInstance();
      }
    }
    /**
     * <pre>
     * Run an agent with a configured prompt at each fire.
     *
     * &#64;internal
     * The shared owner-settable run shape (DD-018 D-3), replacing the
     * deleted schedule-only AgentTarget/ScheduleRunConfig pair.
     * Schedule-specific invariants enforced in create/update/apply
     * handlers of both editions, never on the shared message:
     * agent_ref.org must equal metadata.org (the schedule-owning org
     * is the billing org for every fire; creation requires can_edit
     * on the referenced agent, DD-009 C-6); workspace sources must be
     * git_repo (no client is connected at fire time to serve a
     * local_path); agent_ref is immutable across updates while
     * harness, model, workspace, message, and environments stay
     * mutable — every fire builds a fresh session, so nothing
     * session-immutable is ever mutated mid-flight. environment_refs
     * resolution is unchanged from DD-017 D-2/D-4: claim-driven in
     * the execution-context step, org-shared environments only,
     * LOWEST merge priority, enforcement solely at runtime
     * resolution. run_config clamping is unchanged from DD-017 D-3:
     * per-field min(owner, platform) in the run starter.
     * </pre>
     *
     * <code>.ai.stigmer.agentic.agentexecution.v1.AgentInvocation agent = 4 [json_name = "agent"];</code>
     */
    private com.google.protobuf.SingleFieldBuilder<
        ai.stigmer.agentic.agentexecution.v1.AgentInvocation, ai.stigmer.agentic.agentexecution.v1.AgentInvocation.Builder, ai.stigmer.agentic.agentexecution.v1.AgentInvocationOrBuilder> 
        internalGetAgentFieldBuilder() {
      if (agentBuilder_ == null) {
        if (!(targetCase_ == 4)) {
          target_ = ai.stigmer.agentic.agentexecution.v1.AgentInvocation.getDefaultInstance();
        }
        agentBuilder_ = new com.google.protobuf.SingleFieldBuilder<
            ai.stigmer.agentic.agentexecution.v1.AgentInvocation, ai.stigmer.agentic.agentexecution.v1.AgentInvocation.Builder, ai.stigmer.agentic.agentexecution.v1.AgentInvocationOrBuilder>(
                (ai.stigmer.agentic.agentexecution.v1.AgentInvocation) target_,
                getParentForChildren(),
                isClean());
        target_ = null;
      }
      targetCase_ = 4;
      onChanged();
      return agentBuilder_;
    }

    // @@protoc_insertion_point(builder_scope:ai.stigmer.agentic.schedule.v1.ScheduleSpec)
  }

  // @@protoc_insertion_point(class_scope:ai.stigmer.agentic.schedule.v1.ScheduleSpec)
  private static final ai.stigmer.agentic.schedule.v1.ScheduleSpec DEFAULT_INSTANCE;
  static {
    DEFAULT_INSTANCE = new ai.stigmer.agentic.schedule.v1.ScheduleSpec();
  }

  public static ai.stigmer.agentic.schedule.v1.ScheduleSpec getDefaultInstance() {
    return DEFAULT_INSTANCE;
  }

  private static final com.google.protobuf.Parser<ScheduleSpec>
      PARSER = new com.google.protobuf.AbstractParser<ScheduleSpec>() {
    @java.lang.Override
    public ScheduleSpec parsePartialFrom(
        com.google.protobuf.CodedInputStream input,
        com.google.protobuf.ExtensionRegistryLite extensionRegistry)
        throws com.google.protobuf.InvalidProtocolBufferException {
      Builder builder = newBuilder();
      try {
        builder.mergeFrom(input, extensionRegistry);
      } catch (com.google.protobuf.InvalidProtocolBufferException e) {
        throw e.setUnfinishedMessage(builder.buildPartial());
      } catch (com.google.protobuf.UninitializedMessageException e) {
        throw e.asInvalidProtocolBufferException().setUnfinishedMessage(builder.buildPartial());
      } catch (java.io.IOException e) {
        throw new com.google.protobuf.InvalidProtocolBufferException(e)
            .setUnfinishedMessage(builder.buildPartial());
      }
      return builder.buildPartial();
    }
  };

  public static com.google.protobuf.Parser<ScheduleSpec> parser() {
    return PARSER;
  }

  @java.lang.Override
  public com.google.protobuf.Parser<ScheduleSpec> getParserForType() {
    return PARSER;
  }

  @java.lang.Override
  public ai.stigmer.agentic.schedule.v1.ScheduleSpec getDefaultInstanceForType() {
    return DEFAULT_INSTANCE;
  }

}

