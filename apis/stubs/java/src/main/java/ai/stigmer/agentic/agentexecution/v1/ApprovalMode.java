// Generated by the protocol buffer compiler.  DO NOT EDIT!
// NO CHECKED-IN PROTOBUF GENCODE
// source: ai/stigmer/agentic/agentexecution/v1/enum.proto
// Protobuf Java Version: 4.34.0

package ai.stigmer.agentic.agentexecution.v1;

/**
 * <pre>
 * ApprovalMode controls how an execution resolves HITL approval gates.
 *
 * The four-level approval policy chain (McpServerStatus.tool_approvals →
 * McpServerSpec.pinned_tool_approvals → Agent tool_approval_overrides →
 * AgentExecutionSpec.auto_approve_all) decides WHICH tools are gated; this
 * mode decides WHAT HAPPENS when a gate fires. It exists for surfaces where
 * no approver is present at the conversation — messaging channels
 * (Slack/WhatsApp) and guest shares — where an interactive pause would park
 * the execution in EXECUTION_WAITING_FOR_APPROVAL forever.
 *
 * The mode is stamped by the SURFACE that creates the execution (the channel
 * session broker, the guest execution scope step), never chosen by the
 * external user: it is runtime policy owned by the delivery surface, exactly
 * like the bounded channel execution profile. Interactive surfaces (console,
 * CLI) leave it unset.
 *
 * Two different consents, deliberately separated:
 * - Operator consent (this gate) protects the org's tools and data and is
 * never delegated to an external user — a channel guest cannot authorize
 * the org's destructive operations.
 * - End-user intent confirmation ("book Monday 10 AM — shall I?") is
 * conversational, owned by the agent's instructions; the tool itself is
 * then un-gated per agent via tool_approval_overrides.
 * </pre>
 *
 * Protobuf enum {@code ai.stigmer.agentic.agentexecution.v1.ApprovalMode}
 */
@com.google.protobuf.Generated
public enum ApprovalMode
    implements com.google.protobuf.ProtocolMessageEnum {
  /**
   * <pre>
   * Default — resolves to APPROVAL_MODE_INTERACTIVE.
   * </pre>
   *
   * <code>APPROVAL_MODE_UNSPECIFIED = 0;</code>
   */
  APPROVAL_MODE_UNSPECIFIED(0),
  /**
   * <pre>
   * Interactive approval (today's behavior): a gated tool pauses the
   * execution (EXECUTION_WAITING_FOR_APPROVAL) until a human submits a
   * decision via the submitApproval RPC. No timeout — the execution waits
   * indefinitely.
   * </pre>
   *
   * <code>APPROVAL_MODE_INTERACTIVE = 1;</code>
   */
  APPROVAL_MODE_INTERACTIVE(1),
  /**
   * <pre>
   * Unattended: no approver exists on the creating surface. A gated tool is
   * resolved as an automatic SKIP — the tool does NOT run, the model is told
   * the action requires an approval that is not available in this
   * conversation and to adapt and explain in plain language, and the turn
   * continues to normal completion. The skipped call is stamped
   * TOOL_CALL_SKIPPED with approval_policy_source = UNATTENDED_SKIP; no
   * approval-request event is authored (nothing is ever pending).
   *
   * A future "park the turn and notify an org approver asynchronously"
   * behavior will be a NEW enum value, not a reinterpretation of this one.
   * </pre>
   *
   * <code>APPROVAL_MODE_UNATTENDED = 2;</code>
   */
  APPROVAL_MODE_UNATTENDED(2),
  UNRECOGNIZED(-1),
  ;

  static {
    com.google.protobuf.RuntimeVersion.validateProtobufGencodeVersion(
      com.google.protobuf.RuntimeVersion.RuntimeDomain.PUBLIC,
      /* major= */ 4,
      /* minor= */ 34,
      /* patch= */ 0,
      /* suffix= */ "",
      "ApprovalMode");
  }
  /**
   * <pre>
   * Default — resolves to APPROVAL_MODE_INTERACTIVE.
   * </pre>
   *
   * <code>APPROVAL_MODE_UNSPECIFIED = 0;</code>
   */
  public static final int APPROVAL_MODE_UNSPECIFIED_VALUE = 0;
  /**
   * <pre>
   * Interactive approval (today's behavior): a gated tool pauses the
   * execution (EXECUTION_WAITING_FOR_APPROVAL) until a human submits a
   * decision via the submitApproval RPC. No timeout — the execution waits
   * indefinitely.
   * </pre>
   *
   * <code>APPROVAL_MODE_INTERACTIVE = 1;</code>
   */
  public static final int APPROVAL_MODE_INTERACTIVE_VALUE = 1;
  /**
   * <pre>
   * Unattended: no approver exists on the creating surface. A gated tool is
   * resolved as an automatic SKIP — the tool does NOT run, the model is told
   * the action requires an approval that is not available in this
   * conversation and to adapt and explain in plain language, and the turn
   * continues to normal completion. The skipped call is stamped
   * TOOL_CALL_SKIPPED with approval_policy_source = UNATTENDED_SKIP; no
   * approval-request event is authored (nothing is ever pending).
   *
   * A future "park the turn and notify an org approver asynchronously"
   * behavior will be a NEW enum value, not a reinterpretation of this one.
   * </pre>
   *
   * <code>APPROVAL_MODE_UNATTENDED = 2;</code>
   */
  public static final int APPROVAL_MODE_UNATTENDED_VALUE = 2;


  public final int getNumber() {
    if (this == UNRECOGNIZED) {
      throw new java.lang.IllegalArgumentException(
          "Can't get the number of an unknown enum value.");
    }
    return value;
  }

  /**
   * @param value The numeric wire value of the corresponding enum entry.
   * @return The enum associated with the given numeric wire value.
   * @deprecated Use {@link #forNumber(int)} instead.
   */
  @java.lang.Deprecated
  public static ApprovalMode valueOf(int value) {
    return forNumber(value);
  }

  /**
   * @param value The numeric wire value of the corresponding enum entry.
   * @return The enum associated with the given numeric wire value.
   */
  public static ApprovalMode forNumber(int value) {
    switch (value) {
      case 0: return APPROVAL_MODE_UNSPECIFIED;
      case 1: return APPROVAL_MODE_INTERACTIVE;
      case 2: return APPROVAL_MODE_UNATTENDED;
      default: return null;
    }
  }

  public static com.google.protobuf.Internal.EnumLiteMap<ApprovalMode>
      internalGetValueMap() {
    return internalValueMap;
  }
  private static final com.google.protobuf.Internal.EnumLiteMap<
      ApprovalMode> internalValueMap =
        new com.google.protobuf.Internal.EnumLiteMap<ApprovalMode>() {
          public ApprovalMode findValueByNumber(int number) {
            return ApprovalMode.forNumber(number);
          }
        };

  public final com.google.protobuf.Descriptors.EnumValueDescriptor
      getValueDescriptor() {
    if (this == UNRECOGNIZED) {
      throw new java.lang.IllegalStateException(
          "Can't get the descriptor of an unrecognized enum value.");
    }
    return getDescriptor().getValue(ordinal());
  }
  public final com.google.protobuf.Descriptors.EnumDescriptor
      getDescriptorForType() {
    return getDescriptor();
  }
  public static com.google.protobuf.Descriptors.EnumDescriptor
      getDescriptor() {
    return ai.stigmer.agentic.agentexecution.v1.EnumProto.getDescriptor().getEnumType(15);
  }

  private static final ApprovalMode[] VALUES = values();

  public static ApprovalMode valueOf(
      com.google.protobuf.Descriptors.EnumValueDescriptor desc) {
    if (desc.getType() != getDescriptor()) {
      throw new java.lang.IllegalArgumentException(
        "EnumValueDescriptor is not for this type.");
    }
    if (desc.getIndex() == -1) {
      return UNRECOGNIZED;
    }
    return VALUES[desc.getIndex()];
  }

  private final int value;

  private ApprovalMode(int value) {
    this.value = value;
  }

  // @@protoc_insertion_point(enum_scope:ai.stigmer.agentic.agentexecution.v1.ApprovalMode)
}

