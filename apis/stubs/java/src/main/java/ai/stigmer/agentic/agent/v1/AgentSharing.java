// Generated by the protocol buffer compiler.  DO NOT EDIT!
// NO CHECKED-IN PROTOBUF GENCODE
// source: ai/stigmer/agentic/agent/v1/spec.proto
// Protobuf Java Version: 4.34.0

package ai.stigmer.agentic.agent.v1;

/**
 * <pre>
 * AgentSharing controls public, anyone-with-link access to an agent's
 * hosted chat experience.
 *
 * When enabled, the agent's trimmed profile becomes resolvable through the
 * public getSharedProfile RPC and the hosted chat page can serve visitors
 * without Stigmer accounts. When disabled (or unset), the shared link
 * behaves as if the agent does not exist (NOT_FOUND).
 *
 * This message is intentionally a wrapper around a single flag so later
 * phases can add fields (usage caps, org-only mode, expiry) without a
 * breaking change.
 * </pre>
 *
 * Protobuf type {@code ai.stigmer.agentic.agent.v1.AgentSharing}
 */
@com.google.protobuf.Generated
public final class AgentSharing extends
    com.google.protobuf.GeneratedMessage implements
    // @@protoc_insertion_point(message_implements:ai.stigmer.agentic.agent.v1.AgentSharing)
    AgentSharingOrBuilder {
private static final long serialVersionUID = 0L;
  static {
    com.google.protobuf.RuntimeVersion.validateProtobufGencodeVersion(
      com.google.protobuf.RuntimeVersion.RuntimeDomain.PUBLIC,
      /* major= */ 4,
      /* minor= */ 34,
      /* patch= */ 0,
      /* suffix= */ "",
      "AgentSharing");
  }
  // Use AgentSharing.newBuilder() to construct.
  private AgentSharing(com.google.protobuf.GeneratedMessage.Builder<?> builder) {
    super(builder);
  }
  private AgentSharing() {
    allowedOrigins_ =
        com.google.protobuf.LazyStringArrayList.emptyList();
  }

  public static final com.google.protobuf.Descriptors.Descriptor
      getDescriptor() {
    return ai.stigmer.agentic.agent.v1.SpecProto.internal_static_ai_stigmer_agentic_agent_v1_AgentSharing_descriptor;
  }

  @java.lang.Override
  public com.google.protobuf.Descriptors.Descriptor getDescriptorForType() {
    return ai.stigmer.agentic.agent.v1.SpecProto.internal_static_ai_stigmer_agentic_agent_v1_AgentSharing_descriptor;
  }

  @java.lang.Override
  protected com.google.protobuf.GeneratedMessage.FieldAccessorTable
      internalGetFieldAccessorTable() {
    return ai.stigmer.agentic.agent.v1.SpecProto.internal_static_ai_stigmer_agentic_agent_v1_AgentSharing_fieldAccessorTable
        .ensureFieldAccessorsInitialized(
            ai.stigmer.agentic.agent.v1.AgentSharing.class, ai.stigmer.agentic.agent.v1.AgentSharing.Builder.class);
  }

  private int bitField0_;
  public static final int ENABLED_FIELD_NUMBER = 1;
  private boolean enabled_ = false;
  /**
   * <pre>
   * Whether anyone-with-link access to the hosted chat is enabled.
   * </pre>
   *
   * <code>bool enabled = 1 [json_name = "enabled"];</code>
   * @return The enabled.
   */
  @java.lang.Override
  public boolean getEnabled() {
    return enabled_;
  }

  public static final int ALLOWED_ORIGINS_FIELD_NUMBER = 2;
  @SuppressWarnings("serial")
  private com.google.protobuf.LazyStringArrayList allowedOrigins_ =
      com.google.protobuf.LazyStringArrayList.emptyList();
  /**
   * <pre>
   * Origins permitted to embed this agent's chat widget.
   *
   * Each entry is an exact web origin (scheme://host[:port]) with no path,
   * for example "https://docs.example.com". An empty list allows embedding
   * from any site; listing origins restricts embedding to those sites. The
   * first-party hosted chat page is always exempt.
   *
   * &#64;internal
   * Enforced since T04 against the embed_origin the widget reports at
   * mintGuestToken time (stamped into the guest JWT as a claim) and
   * re-validated against this live list by the guest create-time gate
   * (SharedSessionBlueprintAccess) on every session/execution create — the
   * same gate that re-checks sharing.enabled, so revocation latency is
   * identical (immediate). Unframed hosted-page visitors report no origin
   * and are exempt by construction. Exact origins only — loosening to
   * wildcards later is a non-breaking change, tightening would not be.
   * </pre>
   *
   * <code>repeated string allowed_origins = 2 [json_name = "allowedOrigins", (.buf.validate.field) = { ... }</code>
   * @return A list containing the allowedOrigins.
   */
  public com.google.protobuf.ProtocolStringList
      getAllowedOriginsList() {
    return allowedOrigins_;
  }
  /**
   * <pre>
   * Origins permitted to embed this agent's chat widget.
   *
   * Each entry is an exact web origin (scheme://host[:port]) with no path,
   * for example "https://docs.example.com". An empty list allows embedding
   * from any site; listing origins restricts embedding to those sites. The
   * first-party hosted chat page is always exempt.
   *
   * &#64;internal
   * Enforced since T04 against the embed_origin the widget reports at
   * mintGuestToken time (stamped into the guest JWT as a claim) and
   * re-validated against this live list by the guest create-time gate
   * (SharedSessionBlueprintAccess) on every session/execution create — the
   * same gate that re-checks sharing.enabled, so revocation latency is
   * identical (immediate). Unframed hosted-page visitors report no origin
   * and are exempt by construction. Exact origins only — loosening to
   * wildcards later is a non-breaking change, tightening would not be.
   * </pre>
   *
   * <code>repeated string allowed_origins = 2 [json_name = "allowedOrigins", (.buf.validate.field) = { ... }</code>
   * @return The count of allowedOrigins.
   */
  public int getAllowedOriginsCount() {
    return allowedOrigins_.size();
  }
  /**
   * <pre>
   * Origins permitted to embed this agent's chat widget.
   *
   * Each entry is an exact web origin (scheme://host[:port]) with no path,
   * for example "https://docs.example.com". An empty list allows embedding
   * from any site; listing origins restricts embedding to those sites. The
   * first-party hosted chat page is always exempt.
   *
   * &#64;internal
   * Enforced since T04 against the embed_origin the widget reports at
   * mintGuestToken time (stamped into the guest JWT as a claim) and
   * re-validated against this live list by the guest create-time gate
   * (SharedSessionBlueprintAccess) on every session/execution create — the
   * same gate that re-checks sharing.enabled, so revocation latency is
   * identical (immediate). Unframed hosted-page visitors report no origin
   * and are exempt by construction. Exact origins only — loosening to
   * wildcards later is a non-breaking change, tightening would not be.
   * </pre>
   *
   * <code>repeated string allowed_origins = 2 [json_name = "allowedOrigins", (.buf.validate.field) = { ... }</code>
   * @param index The index of the element to return.
   * @return The allowedOrigins at the given index.
   */
  public java.lang.String getAllowedOrigins(int index) {
    return allowedOrigins_.get(index);
  }
  /**
   * <pre>
   * Origins permitted to embed this agent's chat widget.
   *
   * Each entry is an exact web origin (scheme://host[:port]) with no path,
   * for example "https://docs.example.com". An empty list allows embedding
   * from any site; listing origins restricts embedding to those sites. The
   * first-party hosted chat page is always exempt.
   *
   * &#64;internal
   * Enforced since T04 against the embed_origin the widget reports at
   * mintGuestToken time (stamped into the guest JWT as a claim) and
   * re-validated against this live list by the guest create-time gate
   * (SharedSessionBlueprintAccess) on every session/execution create — the
   * same gate that re-checks sharing.enabled, so revocation latency is
   * identical (immediate). Unframed hosted-page visitors report no origin
   * and are exempt by construction. Exact origins only — loosening to
   * wildcards later is a non-breaking change, tightening would not be.
   * </pre>
   *
   * <code>repeated string allowed_origins = 2 [json_name = "allowedOrigins", (.buf.validate.field) = { ... }</code>
   * @param index The index of the value to return.
   * @return The bytes of the allowedOrigins at the given index.
   */
  public com.google.protobuf.ByteString
      getAllowedOriginsBytes(int index) {
    return allowedOrigins_.getByteString(index);
  }

  public static final int MESSAGES_FIELD_NUMBER = 3;
  private ai.stigmer.agentic.agent.v1.AgentSharingMessages messages_;
  /**
   * <pre>
   * Owner-customizable copy shown to visitors when a launch-gate limit
   * refuses their message. Unset fields fall back to platform defaults.
   *
   * &#64;internal
   * Resolved server-side at the refusal point and carried in the gRPC
   * status description — deliberately NOT surfaced on SharedAgentProfile
   * and NOT mapped client-side, so the copy reaches every client (web,
   * embed, CLI) through the existing error-message path.
   * </pre>
   *
   * <code>.ai.stigmer.agentic.agent.v1.AgentSharingMessages messages = 3 [json_name = "messages"];</code>
   * @return Whether the messages field is set.
   */
  @java.lang.Override
  public boolean hasMessages() {
    return ((bitField0_ & 0x00000001) != 0);
  }
  /**
   * <pre>
   * Owner-customizable copy shown to visitors when a launch-gate limit
   * refuses their message. Unset fields fall back to platform defaults.
   *
   * &#64;internal
   * Resolved server-side at the refusal point and carried in the gRPC
   * status description — deliberately NOT surfaced on SharedAgentProfile
   * and NOT mapped client-side, so the copy reaches every client (web,
   * embed, CLI) through the existing error-message path.
   * </pre>
   *
   * <code>.ai.stigmer.agentic.agent.v1.AgentSharingMessages messages = 3 [json_name = "messages"];</code>
   * @return The messages.
   */
  @java.lang.Override
  public ai.stigmer.agentic.agent.v1.AgentSharingMessages getMessages() {
    return messages_ == null ? ai.stigmer.agentic.agent.v1.AgentSharingMessages.getDefaultInstance() : messages_;
  }
  /**
   * <pre>
   * Owner-customizable copy shown to visitors when a launch-gate limit
   * refuses their message. Unset fields fall back to platform defaults.
   *
   * &#64;internal
   * Resolved server-side at the refusal point and carried in the gRPC
   * status description — deliberately NOT surfaced on SharedAgentProfile
   * and NOT mapped client-side, so the copy reaches every client (web,
   * embed, CLI) through the existing error-message path.
   * </pre>
   *
   * <code>.ai.stigmer.agentic.agent.v1.AgentSharingMessages messages = 3 [json_name = "messages"];</code>
   */
  @java.lang.Override
  public ai.stigmer.agentic.agent.v1.AgentSharingMessagesOrBuilder getMessagesOrBuilder() {
    return messages_ == null ? ai.stigmer.agentic.agent.v1.AgentSharingMessages.getDefaultInstance() : messages_;
  }

  private byte memoizedIsInitialized = -1;
  @java.lang.Override
  public final boolean isInitialized() {
    byte isInitialized = memoizedIsInitialized;
    if (isInitialized == 1) return true;
    if (isInitialized == 0) return false;

    memoizedIsInitialized = 1;
    return true;
  }

  @java.lang.Override
  public void writeTo(com.google.protobuf.CodedOutputStream output)
                      throws java.io.IOException {
    if (enabled_ != false) {
      output.writeBool(1, enabled_);
    }
    for (int i = 0; i < allowedOrigins_.size(); i++) {
      com.google.protobuf.GeneratedMessage.writeString(output, 2, allowedOrigins_.getRaw(i));
    }
    if (((bitField0_ & 0x00000001) != 0)) {
      output.writeMessage(3, getMessages());
    }
    getUnknownFields().writeTo(output);
  }

  @java.lang.Override
  public int getSerializedSize() {
    int size = memoizedSize;
    if (size != -1) return size;

    size = 0;
    if (enabled_ != false) {
      size += com.google.protobuf.CodedOutputStream
        .computeBoolSize(1, enabled_);
    }
    {
      int dataSize = 0;
      for (int i = 0; i < allowedOrigins_.size(); i++) {
        dataSize += computeStringSizeNoTag(allowedOrigins_.getRaw(i));
      }
      size += dataSize;
      size += 1 * getAllowedOriginsList().size();
    }
    if (((bitField0_ & 0x00000001) != 0)) {
      size += com.google.protobuf.CodedOutputStream
        .computeMessageSize(3, getMessages());
    }
    size += getUnknownFields().getSerializedSize();
    memoizedSize = size;
    return size;
  }

  @java.lang.Override
  public boolean equals(final java.lang.Object obj) {
    if (obj == this) {
     return true;
    }
    if (!(obj instanceof ai.stigmer.agentic.agent.v1.AgentSharing)) {
      return super.equals(obj);
    }
    ai.stigmer.agentic.agent.v1.AgentSharing other = (ai.stigmer.agentic.agent.v1.AgentSharing) obj;

    if (getEnabled()
        != other.getEnabled()) return false;
    if (!getAllowedOriginsList()
        .equals(other.getAllowedOriginsList())) return false;
    if (hasMessages() != other.hasMessages()) return false;
    if (hasMessages()) {
      if (!getMessages()
          .equals(other.getMessages())) return false;
    }
    if (!getUnknownFields().equals(other.getUnknownFields())) return false;
    return true;
  }

  @java.lang.Override
  public int hashCode() {
    if (memoizedHashCode != 0) {
      return memoizedHashCode;
    }
    int hash = 41;
    hash = (19 * hash) + getDescriptor().hashCode();
    hash = (37 * hash) + ENABLED_FIELD_NUMBER;
    hash = (53 * hash) + com.google.protobuf.Internal.hashBoolean(
        getEnabled());
    if (getAllowedOriginsCount() > 0) {
      hash = (37 * hash) + ALLOWED_ORIGINS_FIELD_NUMBER;
      hash = (53 * hash) + getAllowedOriginsList().hashCode();
    }
    if (hasMessages()) {
      hash = (37 * hash) + MESSAGES_FIELD_NUMBER;
      hash = (53 * hash) + getMessages().hashCode();
    }
    hash = (29 * hash) + getUnknownFields().hashCode();
    memoizedHashCode = hash;
    return hash;
  }

  public static ai.stigmer.agentic.agent.v1.AgentSharing parseFrom(
      java.nio.ByteBuffer data)
      throws com.google.protobuf.InvalidProtocolBufferException {
    return PARSER.parseFrom(data);
  }
  public static ai.stigmer.agentic.agent.v1.AgentSharing parseFrom(
      java.nio.ByteBuffer data,
      com.google.protobuf.ExtensionRegistryLite extensionRegistry)
      throws com.google.protobuf.InvalidProtocolBufferException {
    return PARSER.parseFrom(data, extensionRegistry);
  }
  public static ai.stigmer.agentic.agent.v1.AgentSharing parseFrom(
      com.google.protobuf.ByteString data)
      throws com.google.protobuf.InvalidProtocolBufferException {
    return PARSER.parseFrom(data);
  }
  public static ai.stigmer.agentic.agent.v1.AgentSharing parseFrom(
      com.google.protobuf.ByteString data,
      com.google.protobuf.ExtensionRegistryLite extensionRegistry)
      throws com.google.protobuf.InvalidProtocolBufferException {
    return PARSER.parseFrom(data, extensionRegistry);
  }
  public static ai.stigmer.agentic.agent.v1.AgentSharing parseFrom(byte[] data)
      throws com.google.protobuf.InvalidProtocolBufferException {
    return PARSER.parseFrom(data);
  }
  public static ai.stigmer.agentic.agent.v1.AgentSharing parseFrom(
      byte[] data,
      com.google.protobuf.ExtensionRegistryLite extensionRegistry)
      throws com.google.protobuf.InvalidProtocolBufferException {
    return PARSER.parseFrom(data, extensionRegistry);
  }
  public static ai.stigmer.agentic.agent.v1.AgentSharing parseFrom(java.io.InputStream input)
      throws java.io.IOException {
    return com.google.protobuf.GeneratedMessage
        .parseWithIOException(PARSER, input);
  }
  public static ai.stigmer.agentic.agent.v1.AgentSharing parseFrom(
      java.io.InputStream input,
      com.google.protobuf.ExtensionRegistryLite extensionRegistry)
      throws java.io.IOException {
    return com.google.protobuf.GeneratedMessage
        .parseWithIOException(PARSER, input, extensionRegistry);
  }

  public static ai.stigmer.agentic.agent.v1.AgentSharing parseDelimitedFrom(java.io.InputStream input)
      throws java.io.IOException {
    return com.google.protobuf.GeneratedMessage
        .parseDelimitedWithIOException(PARSER, input);
  }

  public static ai.stigmer.agentic.agent.v1.AgentSharing parseDelimitedFrom(
      java.io.InputStream input,
      com.google.protobuf.ExtensionRegistryLite extensionRegistry)
      throws java.io.IOException {
    return com.google.protobuf.GeneratedMessage
        .parseDelimitedWithIOException(PARSER, input, extensionRegistry);
  }
  public static ai.stigmer.agentic.agent.v1.AgentSharing parseFrom(
      com.google.protobuf.CodedInputStream input)
      throws java.io.IOException {
    return com.google.protobuf.GeneratedMessage
        .parseWithIOException(PARSER, input);
  }
  public static ai.stigmer.agentic.agent.v1.AgentSharing parseFrom(
      com.google.protobuf.CodedInputStream input,
      com.google.protobuf.ExtensionRegistryLite extensionRegistry)
      throws java.io.IOException {
    return com.google.protobuf.GeneratedMessage
        .parseWithIOException(PARSER, input, extensionRegistry);
  }

  @java.lang.Override
  public Builder newBuilderForType() { return newBuilder(); }
  public static Builder newBuilder() {
    return DEFAULT_INSTANCE.toBuilder();
  }
  public static Builder newBuilder(ai.stigmer.agentic.agent.v1.AgentSharing prototype) {
    return DEFAULT_INSTANCE.toBuilder().mergeFrom(prototype);
  }
  @java.lang.Override
  public Builder toBuilder() {
    return this == DEFAULT_INSTANCE
        ? new Builder() : new Builder().mergeFrom(this);
  }

  @java.lang.Override
  protected Builder newBuilderForType(
      com.google.protobuf.GeneratedMessage.BuilderParent parent) {
    Builder builder = new Builder(parent);
    return builder;
  }
  /**
   * <pre>
   * AgentSharing controls public, anyone-with-link access to an agent's
   * hosted chat experience.
   *
   * When enabled, the agent's trimmed profile becomes resolvable through the
   * public getSharedProfile RPC and the hosted chat page can serve visitors
   * without Stigmer accounts. When disabled (or unset), the shared link
   * behaves as if the agent does not exist (NOT_FOUND).
   *
   * This message is intentionally a wrapper around a single flag so later
   * phases can add fields (usage caps, org-only mode, expiry) without a
   * breaking change.
   * </pre>
   *
   * Protobuf type {@code ai.stigmer.agentic.agent.v1.AgentSharing}
   */
  public static final class Builder extends
      com.google.protobuf.GeneratedMessage.Builder<Builder> implements
      // @@protoc_insertion_point(builder_implements:ai.stigmer.agentic.agent.v1.AgentSharing)
      ai.stigmer.agentic.agent.v1.AgentSharingOrBuilder {
    public static final com.google.protobuf.Descriptors.Descriptor
        getDescriptor() {
      return ai.stigmer.agentic.agent.v1.SpecProto.internal_static_ai_stigmer_agentic_agent_v1_AgentSharing_descriptor;
    }

    @java.lang.Override
    protected com.google.protobuf.GeneratedMessage.FieldAccessorTable
        internalGetFieldAccessorTable() {
      return ai.stigmer.agentic.agent.v1.SpecProto.internal_static_ai_stigmer_agentic_agent_v1_AgentSharing_fieldAccessorTable
          .ensureFieldAccessorsInitialized(
              ai.stigmer.agentic.agent.v1.AgentSharing.class, ai.stigmer.agentic.agent.v1.AgentSharing.Builder.class);
    }

    // Construct using ai.stigmer.agentic.agent.v1.AgentSharing.newBuilder()
    private Builder() {
      maybeForceBuilderInitialization();
    }

    private Builder(
        com.google.protobuf.GeneratedMessage.BuilderParent parent) {
      super(parent);
      maybeForceBuilderInitialization();
    }
    private void maybeForceBuilderInitialization() {
      if (com.google.protobuf.GeneratedMessage
              .alwaysUseFieldBuilders) {
        internalGetMessagesFieldBuilder();
      }
    }
    @java.lang.Override
    public Builder clear() {
      super.clear();
      bitField0_ = 0;
      enabled_ = false;
      allowedOrigins_ =
          com.google.protobuf.LazyStringArrayList.emptyList();
      messages_ = null;
      if (messagesBuilder_ != null) {
        messagesBuilder_.dispose();
        messagesBuilder_ = null;
      }
      return this;
    }

    @java.lang.Override
    public com.google.protobuf.Descriptors.Descriptor
        getDescriptorForType() {
      return ai.stigmer.agentic.agent.v1.SpecProto.internal_static_ai_stigmer_agentic_agent_v1_AgentSharing_descriptor;
    }

    @java.lang.Override
    public ai.stigmer.agentic.agent.v1.AgentSharing getDefaultInstanceForType() {
      return ai.stigmer.agentic.agent.v1.AgentSharing.getDefaultInstance();
    }

    @java.lang.Override
    public ai.stigmer.agentic.agent.v1.AgentSharing build() {
      ai.stigmer.agentic.agent.v1.AgentSharing result = buildPartial();
      if (!result.isInitialized()) {
        throw newUninitializedMessageException(result);
      }
      return result;
    }

    @java.lang.Override
    public ai.stigmer.agentic.agent.v1.AgentSharing buildPartial() {
      ai.stigmer.agentic.agent.v1.AgentSharing result = new ai.stigmer.agentic.agent.v1.AgentSharing(this);
      if (bitField0_ != 0) { buildPartial0(result); }
      onBuilt();
      return result;
    }

    private void buildPartial0(ai.stigmer.agentic.agent.v1.AgentSharing result) {
      int from_bitField0_ = bitField0_;
      if (((from_bitField0_ & 0x00000001) != 0)) {
        result.enabled_ = enabled_;
      }
      if (((from_bitField0_ & 0x00000002) != 0)) {
        allowedOrigins_.makeImmutable();
        result.allowedOrigins_ = allowedOrigins_;
      }
      int to_bitField0_ = 0;
      if (((from_bitField0_ & 0x00000004) != 0)) {
        result.messages_ = messagesBuilder_ == null
            ? messages_
            : messagesBuilder_.build();
        to_bitField0_ |= 0x00000001;
      }
      result.bitField0_ |= to_bitField0_;
    }

    @java.lang.Override
    public Builder mergeFrom(com.google.protobuf.Message other) {
      if (other instanceof ai.stigmer.agentic.agent.v1.AgentSharing) {
        return mergeFrom((ai.stigmer.agentic.agent.v1.AgentSharing)other);
      } else {
        super.mergeFrom(other);
        return this;
      }
    }

    public Builder mergeFrom(ai.stigmer.agentic.agent.v1.AgentSharing other) {
      if (other == ai.stigmer.agentic.agent.v1.AgentSharing.getDefaultInstance()) return this;
      if (other.getEnabled() != false) {
        setEnabled(other.getEnabled());
      }
      if (!other.allowedOrigins_.isEmpty()) {
        if (allowedOrigins_.isEmpty()) {
          allowedOrigins_ = other.allowedOrigins_;
          bitField0_ |= 0x00000002;
        } else {
          ensureAllowedOriginsIsMutable();
          allowedOrigins_.addAll(other.allowedOrigins_);
        }
        onChanged();
      }
      if (other.hasMessages()) {
        mergeMessages(other.getMessages());
      }
      this.mergeUnknownFields(other.getUnknownFields());
      onChanged();
      return this;
    }

    @java.lang.Override
    public final boolean isInitialized() {
      return true;
    }

    @java.lang.Override
    public Builder mergeFrom(
        com.google.protobuf.CodedInputStream input,
        com.google.protobuf.ExtensionRegistryLite extensionRegistry)
        throws java.io.IOException {
      if (extensionRegistry == null) {
        throw new java.lang.NullPointerException();
      }
      try {
        boolean done = false;
        while (!done) {
          int tag = input.readTag();
          switch (tag) {
            case 0:
              done = true;
              break;
            case 8: {
              enabled_ = input.readBool();
              bitField0_ |= 0x00000001;
              break;
            } // case 8
            case 18: {
              ensureAllowedOriginsIsMutable();
              allowedOrigins_.add(input.readStringRequireUtf8());
              break;
            } // case 18
            case 26: {
              input.readMessage(
                  internalGetMessagesFieldBuilder().getBuilder(),
                  extensionRegistry);
              bitField0_ |= 0x00000004;
              break;
            } // case 26
            default: {
              if (!super.parseUnknownField(input, extensionRegistry, tag)) {
                done = true; // was an endgroup tag
              }
              break;
            } // default:
          } // switch (tag)
        } // while (!done)
      } catch (com.google.protobuf.InvalidProtocolBufferException e) {
        throw e.unwrapIOException();
      } finally {
        onChanged();
      } // finally
      return this;
    }
    private int bitField0_;

    private boolean enabled_ ;
    /**
     * <pre>
     * Whether anyone-with-link access to the hosted chat is enabled.
     * </pre>
     *
     * <code>bool enabled = 1 [json_name = "enabled"];</code>
     * @return The enabled.
     */
    @java.lang.Override
    public boolean getEnabled() {
      return enabled_;
    }
    /**
     * <pre>
     * Whether anyone-with-link access to the hosted chat is enabled.
     * </pre>
     *
     * <code>bool enabled = 1 [json_name = "enabled"];</code>
     * @param value The enabled to set.
     * @return This builder for chaining.
     */
    public Builder setEnabled(boolean value) {

      enabled_ = value;
      bitField0_ |= 0x00000001;
      onChanged();
      return this;
    }
    /**
     * <pre>
     * Whether anyone-with-link access to the hosted chat is enabled.
     * </pre>
     *
     * <code>bool enabled = 1 [json_name = "enabled"];</code>
     * @return This builder for chaining.
     */
    public Builder clearEnabled() {
      bitField0_ = (bitField0_ & ~0x00000001);
      enabled_ = false;
      onChanged();
      return this;
    }

    private com.google.protobuf.LazyStringArrayList allowedOrigins_ =
        com.google.protobuf.LazyStringArrayList.emptyList();
    private void ensureAllowedOriginsIsMutable() {
      if (!allowedOrigins_.isModifiable()) {
        allowedOrigins_ = new com.google.protobuf.LazyStringArrayList(allowedOrigins_);
      }
      bitField0_ |= 0x00000002;
    }
    /**
     * <pre>
     * Origins permitted to embed this agent's chat widget.
     *
     * Each entry is an exact web origin (scheme://host[:port]) with no path,
     * for example "https://docs.example.com". An empty list allows embedding
     * from any site; listing origins restricts embedding to those sites. The
     * first-party hosted chat page is always exempt.
     *
     * &#64;internal
     * Enforced since T04 against the embed_origin the widget reports at
     * mintGuestToken time (stamped into the guest JWT as a claim) and
     * re-validated against this live list by the guest create-time gate
     * (SharedSessionBlueprintAccess) on every session/execution create — the
     * same gate that re-checks sharing.enabled, so revocation latency is
     * identical (immediate). Unframed hosted-page visitors report no origin
     * and are exempt by construction. Exact origins only — loosening to
     * wildcards later is a non-breaking change, tightening would not be.
     * </pre>
     *
     * <code>repeated string allowed_origins = 2 [json_name = "allowedOrigins", (.buf.validate.field) = { ... }</code>
     * @return A list containing the allowedOrigins.
     */
    public com.google.protobuf.ProtocolStringList
        getAllowedOriginsList() {
      allowedOrigins_.makeImmutable();
      return allowedOrigins_;
    }
    /**
     * <pre>
     * Origins permitted to embed this agent's chat widget.
     *
     * Each entry is an exact web origin (scheme://host[:port]) with no path,
     * for example "https://docs.example.com". An empty list allows embedding
     * from any site; listing origins restricts embedding to those sites. The
     * first-party hosted chat page is always exempt.
     *
     * &#64;internal
     * Enforced since T04 against the embed_origin the widget reports at
     * mintGuestToken time (stamped into the guest JWT as a claim) and
     * re-validated against this live list by the guest create-time gate
     * (SharedSessionBlueprintAccess) on every session/execution create — the
     * same gate that re-checks sharing.enabled, so revocation latency is
     * identical (immediate). Unframed hosted-page visitors report no origin
     * and are exempt by construction. Exact origins only — loosening to
     * wildcards later is a non-breaking change, tightening would not be.
     * </pre>
     *
     * <code>repeated string allowed_origins = 2 [json_name = "allowedOrigins", (.buf.validate.field) = { ... }</code>
     * @return The count of allowedOrigins.
     */
    public int getAllowedOriginsCount() {
      return allowedOrigins_.size();
    }
    /**
     * <pre>
     * Origins permitted to embed this agent's chat widget.
     *
     * Each entry is an exact web origin (scheme://host[:port]) with no path,
     * for example "https://docs.example.com". An empty list allows embedding
     * from any site; listing origins restricts embedding to those sites. The
     * first-party hosted chat page is always exempt.
     *
     * &#64;internal
     * Enforced since T04 against the embed_origin the widget reports at
     * mintGuestToken time (stamped into the guest JWT as a claim) and
     * re-validated against this live list by the guest create-time gate
     * (SharedSessionBlueprintAccess) on every session/execution create — the
     * same gate that re-checks sharing.enabled, so revocation latency is
     * identical (immediate). Unframed hosted-page visitors report no origin
     * and are exempt by construction. Exact origins only — loosening to
     * wildcards later is a non-breaking change, tightening would not be.
     * </pre>
     *
     * <code>repeated string allowed_origins = 2 [json_name = "allowedOrigins", (.buf.validate.field) = { ... }</code>
     * @param index The index of the element to return.
     * @return The allowedOrigins at the given index.
     */
    public java.lang.String getAllowedOrigins(int index) {
      return allowedOrigins_.get(index);
    }
    /**
     * <pre>
     * Origins permitted to embed this agent's chat widget.
     *
     * Each entry is an exact web origin (scheme://host[:port]) with no path,
     * for example "https://docs.example.com". An empty list allows embedding
     * from any site; listing origins restricts embedding to those sites. The
     * first-party hosted chat page is always exempt.
     *
     * &#64;internal
     * Enforced since T04 against the embed_origin the widget reports at
     * mintGuestToken time (stamped into the guest JWT as a claim) and
     * re-validated against this live list by the guest create-time gate
     * (SharedSessionBlueprintAccess) on every session/execution create — the
     * same gate that re-checks sharing.enabled, so revocation latency is
     * identical (immediate). Unframed hosted-page visitors report no origin
     * and are exempt by construction. Exact origins only — loosening to
     * wildcards later is a non-breaking change, tightening would not be.
     * </pre>
     *
     * <code>repeated string allowed_origins = 2 [json_name = "allowedOrigins", (.buf.validate.field) = { ... }</code>
     * @param index The index of the value to return.
     * @return The bytes of the allowedOrigins at the given index.
     */
    public com.google.protobuf.ByteString
        getAllowedOriginsBytes(int index) {
      return allowedOrigins_.getByteString(index);
    }
    /**
     * <pre>
     * Origins permitted to embed this agent's chat widget.
     *
     * Each entry is an exact web origin (scheme://host[:port]) with no path,
     * for example "https://docs.example.com". An empty list allows embedding
     * from any site; listing origins restricts embedding to those sites. The
     * first-party hosted chat page is always exempt.
     *
     * &#64;internal
     * Enforced since T04 against the embed_origin the widget reports at
     * mintGuestToken time (stamped into the guest JWT as a claim) and
     * re-validated against this live list by the guest create-time gate
     * (SharedSessionBlueprintAccess) on every session/execution create — the
     * same gate that re-checks sharing.enabled, so revocation latency is
     * identical (immediate). Unframed hosted-page visitors report no origin
     * and are exempt by construction. Exact origins only — loosening to
     * wildcards later is a non-breaking change, tightening would not be.
     * </pre>
     *
     * <code>repeated string allowed_origins = 2 [json_name = "allowedOrigins", (.buf.validate.field) = { ... }</code>
     * @param index The index to set the value at.
     * @param value The allowedOrigins to set.
     * @return This builder for chaining.
     */
    public Builder setAllowedOrigins(
        int index, java.lang.String value) {
      if (value == null) { throw new NullPointerException(); }
      ensureAllowedOriginsIsMutable();
      allowedOrigins_.set(index, value);
      bitField0_ |= 0x00000002;
      onChanged();
      return this;
    }
    /**
     * <pre>
     * Origins permitted to embed this agent's chat widget.
     *
     * Each entry is an exact web origin (scheme://host[:port]) with no path,
     * for example "https://docs.example.com". An empty list allows embedding
     * from any site; listing origins restricts embedding to those sites. The
     * first-party hosted chat page is always exempt.
     *
     * &#64;internal
     * Enforced since T04 against the embed_origin the widget reports at
     * mintGuestToken time (stamped into the guest JWT as a claim) and
     * re-validated against this live list by the guest create-time gate
     * (SharedSessionBlueprintAccess) on every session/execution create — the
     * same gate that re-checks sharing.enabled, so revocation latency is
     * identical (immediate). Unframed hosted-page visitors report no origin
     * and are exempt by construction. Exact origins only — loosening to
     * wildcards later is a non-breaking change, tightening would not be.
     * </pre>
     *
     * <code>repeated string allowed_origins = 2 [json_name = "allowedOrigins", (.buf.validate.field) = { ... }</code>
     * @param value The allowedOrigins to add.
     * @return This builder for chaining.
     */
    public Builder addAllowedOrigins(
        java.lang.String value) {
      if (value == null) { throw new NullPointerException(); }
      ensureAllowedOriginsIsMutable();
      allowedOrigins_.add(value);
      bitField0_ |= 0x00000002;
      onChanged();
      return this;
    }
    /**
     * <pre>
     * Origins permitted to embed this agent's chat widget.
     *
     * Each entry is an exact web origin (scheme://host[:port]) with no path,
     * for example "https://docs.example.com". An empty list allows embedding
     * from any site; listing origins restricts embedding to those sites. The
     * first-party hosted chat page is always exempt.
     *
     * &#64;internal
     * Enforced since T04 against the embed_origin the widget reports at
     * mintGuestToken time (stamped into the guest JWT as a claim) and
     * re-validated against this live list by the guest create-time gate
     * (SharedSessionBlueprintAccess) on every session/execution create — the
     * same gate that re-checks sharing.enabled, so revocation latency is
     * identical (immediate). Unframed hosted-page visitors report no origin
     * and are exempt by construction. Exact origins only — loosening to
     * wildcards later is a non-breaking change, tightening would not be.
     * </pre>
     *
     * <code>repeated string allowed_origins = 2 [json_name = "allowedOrigins", (.buf.validate.field) = { ... }</code>
     * @param values The allowedOrigins to add.
     * @return This builder for chaining.
     */
    public Builder addAllAllowedOrigins(
        java.lang.Iterable<java.lang.String> values) {
      ensureAllowedOriginsIsMutable();
      com.google.protobuf.AbstractMessageLite.Builder.addAll(
          values, allowedOrigins_);
      bitField0_ |= 0x00000002;
      onChanged();
      return this;
    }
    /**
     * <pre>
     * Origins permitted to embed this agent's chat widget.
     *
     * Each entry is an exact web origin (scheme://host[:port]) with no path,
     * for example "https://docs.example.com". An empty list allows embedding
     * from any site; listing origins restricts embedding to those sites. The
     * first-party hosted chat page is always exempt.
     *
     * &#64;internal
     * Enforced since T04 against the embed_origin the widget reports at
     * mintGuestToken time (stamped into the guest JWT as a claim) and
     * re-validated against this live list by the guest create-time gate
     * (SharedSessionBlueprintAccess) on every session/execution create — the
     * same gate that re-checks sharing.enabled, so revocation latency is
     * identical (immediate). Unframed hosted-page visitors report no origin
     * and are exempt by construction. Exact origins only — loosening to
     * wildcards later is a non-breaking change, tightening would not be.
     * </pre>
     *
     * <code>repeated string allowed_origins = 2 [json_name = "allowedOrigins", (.buf.validate.field) = { ... }</code>
     * @return This builder for chaining.
     */
    public Builder clearAllowedOrigins() {
      allowedOrigins_ =
        com.google.protobuf.LazyStringArrayList.emptyList();
      bitField0_ = (bitField0_ & ~0x00000002);;
      onChanged();
      return this;
    }
    /**
     * <pre>
     * Origins permitted to embed this agent's chat widget.
     *
     * Each entry is an exact web origin (scheme://host[:port]) with no path,
     * for example "https://docs.example.com". An empty list allows embedding
     * from any site; listing origins restricts embedding to those sites. The
     * first-party hosted chat page is always exempt.
     *
     * &#64;internal
     * Enforced since T04 against the embed_origin the widget reports at
     * mintGuestToken time (stamped into the guest JWT as a claim) and
     * re-validated against this live list by the guest create-time gate
     * (SharedSessionBlueprintAccess) on every session/execution create — the
     * same gate that re-checks sharing.enabled, so revocation latency is
     * identical (immediate). Unframed hosted-page visitors report no origin
     * and are exempt by construction. Exact origins only — loosening to
     * wildcards later is a non-breaking change, tightening would not be.
     * </pre>
     *
     * <code>repeated string allowed_origins = 2 [json_name = "allowedOrigins", (.buf.validate.field) = { ... }</code>
     * @param value The bytes of the allowedOrigins to add.
     * @return This builder for chaining.
     */
    public Builder addAllowedOriginsBytes(
        com.google.protobuf.ByteString value) {
      if (value == null) { throw new NullPointerException(); }
      checkByteStringIsUtf8(value);
      ensureAllowedOriginsIsMutable();
      allowedOrigins_.add(value);
      bitField0_ |= 0x00000002;
      onChanged();
      return this;
    }

    private ai.stigmer.agentic.agent.v1.AgentSharingMessages messages_;
    private com.google.protobuf.SingleFieldBuilder<
        ai.stigmer.agentic.agent.v1.AgentSharingMessages, ai.stigmer.agentic.agent.v1.AgentSharingMessages.Builder, ai.stigmer.agentic.agent.v1.AgentSharingMessagesOrBuilder> messagesBuilder_;
    /**
     * <pre>
     * Owner-customizable copy shown to visitors when a launch-gate limit
     * refuses their message. Unset fields fall back to platform defaults.
     *
     * &#64;internal
     * Resolved server-side at the refusal point and carried in the gRPC
     * status description — deliberately NOT surfaced on SharedAgentProfile
     * and NOT mapped client-side, so the copy reaches every client (web,
     * embed, CLI) through the existing error-message path.
     * </pre>
     *
     * <code>.ai.stigmer.agentic.agent.v1.AgentSharingMessages messages = 3 [json_name = "messages"];</code>
     * @return Whether the messages field is set.
     */
    public boolean hasMessages() {
      return ((bitField0_ & 0x00000004) != 0);
    }
    /**
     * <pre>
     * Owner-customizable copy shown to visitors when a launch-gate limit
     * refuses their message. Unset fields fall back to platform defaults.
     *
     * &#64;internal
     * Resolved server-side at the refusal point and carried in the gRPC
     * status description — deliberately NOT surfaced on SharedAgentProfile
     * and NOT mapped client-side, so the copy reaches every client (web,
     * embed, CLI) through the existing error-message path.
     * </pre>
     *
     * <code>.ai.stigmer.agentic.agent.v1.AgentSharingMessages messages = 3 [json_name = "messages"];</code>
     * @return The messages.
     */
    public ai.stigmer.agentic.agent.v1.AgentSharingMessages getMessages() {
      if (messagesBuilder_ == null) {
        return messages_ == null ? ai.stigmer.agentic.agent.v1.AgentSharingMessages.getDefaultInstance() : messages_;
      } else {
        return messagesBuilder_.getMessage();
      }
    }
    /**
     * <pre>
     * Owner-customizable copy shown to visitors when a launch-gate limit
     * refuses their message. Unset fields fall back to platform defaults.
     *
     * &#64;internal
     * Resolved server-side at the refusal point and carried in the gRPC
     * status description — deliberately NOT surfaced on SharedAgentProfile
     * and NOT mapped client-side, so the copy reaches every client (web,
     * embed, CLI) through the existing error-message path.
     * </pre>
     *
     * <code>.ai.stigmer.agentic.agent.v1.AgentSharingMessages messages = 3 [json_name = "messages"];</code>
     */
    public Builder setMessages(ai.stigmer.agentic.agent.v1.AgentSharingMessages value) {
      if (messagesBuilder_ == null) {
        if (value == null) {
          throw new NullPointerException();
        }
        messages_ = value;
      } else {
        messagesBuilder_.setMessage(value);
      }
      bitField0_ |= 0x00000004;
      onChanged();
      return this;
    }
    /**
     * <pre>
     * Owner-customizable copy shown to visitors when a launch-gate limit
     * refuses their message. Unset fields fall back to platform defaults.
     *
     * &#64;internal
     * Resolved server-side at the refusal point and carried in the gRPC
     * status description — deliberately NOT surfaced on SharedAgentProfile
     * and NOT mapped client-side, so the copy reaches every client (web,
     * embed, CLI) through the existing error-message path.
     * </pre>
     *
     * <code>.ai.stigmer.agentic.agent.v1.AgentSharingMessages messages = 3 [json_name = "messages"];</code>
     */
    public Builder setMessages(
        ai.stigmer.agentic.agent.v1.AgentSharingMessages.Builder builderForValue) {
      if (messagesBuilder_ == null) {
        messages_ = builderForValue.build();
      } else {
        messagesBuilder_.setMessage(builderForValue.build());
      }
      bitField0_ |= 0x00000004;
      onChanged();
      return this;
    }
    /**
     * <pre>
     * Owner-customizable copy shown to visitors when a launch-gate limit
     * refuses their message. Unset fields fall back to platform defaults.
     *
     * &#64;internal
     * Resolved server-side at the refusal point and carried in the gRPC
     * status description — deliberately NOT surfaced on SharedAgentProfile
     * and NOT mapped client-side, so the copy reaches every client (web,
     * embed, CLI) through the existing error-message path.
     * </pre>
     *
     * <code>.ai.stigmer.agentic.agent.v1.AgentSharingMessages messages = 3 [json_name = "messages"];</code>
     */
    public Builder mergeMessages(ai.stigmer.agentic.agent.v1.AgentSharingMessages value) {
      if (messagesBuilder_ == null) {
        if (((bitField0_ & 0x00000004) != 0) &&
          messages_ != null &&
          messages_ != ai.stigmer.agentic.agent.v1.AgentSharingMessages.getDefaultInstance()) {
          getMessagesBuilder().mergeFrom(value);
        } else {
          messages_ = value;
        }
      } else {
        messagesBuilder_.mergeFrom(value);
      }
      if (messages_ != null) {
        bitField0_ |= 0x00000004;
        onChanged();
      }
      return this;
    }
    /**
     * <pre>
     * Owner-customizable copy shown to visitors when a launch-gate limit
     * refuses their message. Unset fields fall back to platform defaults.
     *
     * &#64;internal
     * Resolved server-side at the refusal point and carried in the gRPC
     * status description — deliberately NOT surfaced on SharedAgentProfile
     * and NOT mapped client-side, so the copy reaches every client (web,
     * embed, CLI) through the existing error-message path.
     * </pre>
     *
     * <code>.ai.stigmer.agentic.agent.v1.AgentSharingMessages messages = 3 [json_name = "messages"];</code>
     */
    public Builder clearMessages() {
      bitField0_ = (bitField0_ & ~0x00000004);
      messages_ = null;
      if (messagesBuilder_ != null) {
        messagesBuilder_.dispose();
        messagesBuilder_ = null;
      }
      onChanged();
      return this;
    }
    /**
     * <pre>
     * Owner-customizable copy shown to visitors when a launch-gate limit
     * refuses their message. Unset fields fall back to platform defaults.
     *
     * &#64;internal
     * Resolved server-side at the refusal point and carried in the gRPC
     * status description — deliberately NOT surfaced on SharedAgentProfile
     * and NOT mapped client-side, so the copy reaches every client (web,
     * embed, CLI) through the existing error-message path.
     * </pre>
     *
     * <code>.ai.stigmer.agentic.agent.v1.AgentSharingMessages messages = 3 [json_name = "messages"];</code>
     */
    public ai.stigmer.agentic.agent.v1.AgentSharingMessages.Builder getMessagesBuilder() {
      bitField0_ |= 0x00000004;
      onChanged();
      return internalGetMessagesFieldBuilder().getBuilder();
    }
    /**
     * <pre>
     * Owner-customizable copy shown to visitors when a launch-gate limit
     * refuses their message. Unset fields fall back to platform defaults.
     *
     * &#64;internal
     * Resolved server-side at the refusal point and carried in the gRPC
     * status description — deliberately NOT surfaced on SharedAgentProfile
     * and NOT mapped client-side, so the copy reaches every client (web,
     * embed, CLI) through the existing error-message path.
     * </pre>
     *
     * <code>.ai.stigmer.agentic.agent.v1.AgentSharingMessages messages = 3 [json_name = "messages"];</code>
     */
    public ai.stigmer.agentic.agent.v1.AgentSharingMessagesOrBuilder getMessagesOrBuilder() {
      if (messagesBuilder_ != null) {
        return messagesBuilder_.getMessageOrBuilder();
      } else {
        return messages_ == null ?
            ai.stigmer.agentic.agent.v1.AgentSharingMessages.getDefaultInstance() : messages_;
      }
    }
    /**
     * <pre>
     * Owner-customizable copy shown to visitors when a launch-gate limit
     * refuses their message. Unset fields fall back to platform defaults.
     *
     * &#64;internal
     * Resolved server-side at the refusal point and carried in the gRPC
     * status description — deliberately NOT surfaced on SharedAgentProfile
     * and NOT mapped client-side, so the copy reaches every client (web,
     * embed, CLI) through the existing error-message path.
     * </pre>
     *
     * <code>.ai.stigmer.agentic.agent.v1.AgentSharingMessages messages = 3 [json_name = "messages"];</code>
     */
    private com.google.protobuf.SingleFieldBuilder<
        ai.stigmer.agentic.agent.v1.AgentSharingMessages, ai.stigmer.agentic.agent.v1.AgentSharingMessages.Builder, ai.stigmer.agentic.agent.v1.AgentSharingMessagesOrBuilder> 
        internalGetMessagesFieldBuilder() {
      if (messagesBuilder_ == null) {
        messagesBuilder_ = new com.google.protobuf.SingleFieldBuilder<
            ai.stigmer.agentic.agent.v1.AgentSharingMessages, ai.stigmer.agentic.agent.v1.AgentSharingMessages.Builder, ai.stigmer.agentic.agent.v1.AgentSharingMessagesOrBuilder>(
                getMessages(),
                getParentForChildren(),
                isClean());
        messages_ = null;
      }
      return messagesBuilder_;
    }

    // @@protoc_insertion_point(builder_scope:ai.stigmer.agentic.agent.v1.AgentSharing)
  }

  // @@protoc_insertion_point(class_scope:ai.stigmer.agentic.agent.v1.AgentSharing)
  private static final ai.stigmer.agentic.agent.v1.AgentSharing DEFAULT_INSTANCE;
  static {
    DEFAULT_INSTANCE = new ai.stigmer.agentic.agent.v1.AgentSharing();
  }

  public static ai.stigmer.agentic.agent.v1.AgentSharing getDefaultInstance() {
    return DEFAULT_INSTANCE;
  }

  private static final com.google.protobuf.Parser<AgentSharing>
      PARSER = new com.google.protobuf.AbstractParser<AgentSharing>() {
    @java.lang.Override
    public AgentSharing parsePartialFrom(
        com.google.protobuf.CodedInputStream input,
        com.google.protobuf.ExtensionRegistryLite extensionRegistry)
        throws com.google.protobuf.InvalidProtocolBufferException {
      Builder builder = newBuilder();
      try {
        builder.mergeFrom(input, extensionRegistry);
      } catch (com.google.protobuf.InvalidProtocolBufferException e) {
        throw e.setUnfinishedMessage(builder.buildPartial());
      } catch (com.google.protobuf.UninitializedMessageException e) {
        throw e.asInvalidProtocolBufferException().setUnfinishedMessage(builder.buildPartial());
      } catch (java.io.IOException e) {
        throw new com.google.protobuf.InvalidProtocolBufferException(e)
            .setUnfinishedMessage(builder.buildPartial());
      }
      return builder.buildPartial();
    }
  };

  public static com.google.protobuf.Parser<AgentSharing> parser() {
    return PARSER;
  }

  @java.lang.Override
  public com.google.protobuf.Parser<AgentSharing> getParserForType() {
    return PARSER;
  }

  @java.lang.Override
  public ai.stigmer.agentic.agent.v1.AgentSharing getDefaultInstanceForType() {
    return DEFAULT_INSTANCE;
  }

}

