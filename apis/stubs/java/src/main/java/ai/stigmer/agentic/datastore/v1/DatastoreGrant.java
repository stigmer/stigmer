// Generated by the protocol buffer compiler.  DO NOT EDIT!
// NO CHECKED-IN PROTOBUF GENCODE
// source: ai/stigmer/agentic/datastore/v1/spec.proto
// Protobuf Java Version: 4.34.0

package ai.stigmer.agentic.datastore.v1;

/**
 * <pre>
 * DatastoreGrant gives a role a set of verbs on one collection's records.
 *
 * &#64;internal
 * Apply-time integrity (both editions, the shared spec validators): a
 * (role, verb) pair may appear in at most one grant per collection —
 * with one read grant per role, the resolved access is exactly
 * {scope, read_fields} and no merge rules exist (a duplicate would
 * reopen the composition ambiguity read_fields closes). Stored specs
 * that predate this rule keep their widest-scope-wins resolution; they
 * can never carry read_fields (the rule ships with the field).
 * </pre>
 *
 * Protobuf type {@code ai.stigmer.agentic.datastore.v1.DatastoreGrant}
 */
@com.google.protobuf.Generated
public final class DatastoreGrant extends
    com.google.protobuf.GeneratedMessage implements
    // @@protoc_insertion_point(message_implements:ai.stigmer.agentic.datastore.v1.DatastoreGrant)
    DatastoreGrantOrBuilder {
private static final long serialVersionUID = 0L;
  static {
    com.google.protobuf.RuntimeVersion.validateProtobufGencodeVersion(
      com.google.protobuf.RuntimeVersion.RuntimeDomain.PUBLIC,
      /* major= */ 4,
      /* minor= */ 34,
      /* patch= */ 0,
      /* suffix= */ "",
      "DatastoreGrant");
  }
  // Use DatastoreGrant.newBuilder() to construct.
  private DatastoreGrant(com.google.protobuf.GeneratedMessage.Builder<?> builder) {
    super(builder);
  }
  private DatastoreGrant() {
    role_ = "";
    verbs_ = emptyIntList();
    scope_ = 0;
    readFields_ =
        com.google.protobuf.LazyStringArrayList.emptyList();
  }

  public static final com.google.protobuf.Descriptors.Descriptor
      getDescriptor() {
    return ai.stigmer.agentic.datastore.v1.SpecProto.internal_static_ai_stigmer_agentic_datastore_v1_DatastoreGrant_descriptor;
  }

  @java.lang.Override
  public com.google.protobuf.Descriptors.Descriptor getDescriptorForType() {
    return ai.stigmer.agentic.datastore.v1.SpecProto.internal_static_ai_stigmer_agentic_datastore_v1_DatastoreGrant_descriptor;
  }

  @java.lang.Override
  protected com.google.protobuf.GeneratedMessage.FieldAccessorTable
      internalGetFieldAccessorTable() {
    return ai.stigmer.agentic.datastore.v1.SpecProto.internal_static_ai_stigmer_agentic_datastore_v1_DatastoreGrant_fieldAccessorTable
        .ensureFieldAccessorsInitialized(
            ai.stigmer.agentic.datastore.v1.DatastoreGrant.class, ai.stigmer.agentic.datastore.v1.DatastoreGrant.Builder.class);
  }

  public static final int ROLE_FIELD_NUMBER = 1;
  @SuppressWarnings("serial")
  private volatile java.lang.Object role_ = "";
  /**
   * <pre>
   * Name of a declared role.
   * </pre>
   *
   * <code>string role = 1 [json_name = "role", (.buf.validate.field) = { ... }</code>
   * @return The role.
   */
  @java.lang.Override
  public java.lang.String getRole() {
    java.lang.Object ref = role_;
    if (ref instanceof java.lang.String) {
      return (java.lang.String) ref;
    } else {
      com.google.protobuf.ByteString bs = 
          (com.google.protobuf.ByteString) ref;
      java.lang.String s = bs.toStringUtf8();
      role_ = s;
      return s;
    }
  }
  /**
   * <pre>
   * Name of a declared role.
   * </pre>
   *
   * <code>string role = 1 [json_name = "role", (.buf.validate.field) = { ... }</code>
   * @return The bytes for role.
   */
  @java.lang.Override
  public com.google.protobuf.ByteString
      getRoleBytes() {
    java.lang.Object ref = role_;
    if (ref instanceof java.lang.String) {
      com.google.protobuf.ByteString b = 
          com.google.protobuf.ByteString.copyFromUtf8(
              (java.lang.String) ref);
      role_ = b;
      return b;
    } else {
      return (com.google.protobuf.ByteString) ref;
    }
  }

  public static final int VERBS_FIELD_NUMBER = 2;
  @SuppressWarnings("serial")
  private com.google.protobuf.Internal.IntList verbs_ =
      emptyIntList();
  private static final     com.google.protobuf.Internal.IntListAdapter.IntConverter<
      ai.stigmer.agentic.datastore.v1.DatastoreVerb> verbs_converter_ =
          new com.google.protobuf.Internal.IntListAdapter.IntConverter<
              ai.stigmer.agentic.datastore.v1.DatastoreVerb>() {
            public ai.stigmer.agentic.datastore.v1.DatastoreVerb convert(int from) {
              ai.stigmer.agentic.datastore.v1.DatastoreVerb result = ai.stigmer.agentic.datastore.v1.DatastoreVerb.forNumber(from);
              return result == null ? ai.stigmer.agentic.datastore.v1.DatastoreVerb.UNRECOGNIZED : result;
            }
          };
  /**
   * <pre>
   * Verbs granted to the role on this collection.
   * </pre>
   *
   * <code>repeated .ai.stigmer.agentic.datastore.v1.DatastoreVerb verbs = 2 [json_name = "verbs", (.buf.validate.field) = { ... }</code>
   * @return A list containing the verbs.
   */
  @java.lang.Override
  public java.util.List<ai.stigmer.agentic.datastore.v1.DatastoreVerb> getVerbsList() {
    return new com.google.protobuf.Internal.IntListAdapter<
        ai.stigmer.agentic.datastore.v1.DatastoreVerb>(verbs_, verbs_converter_);
  }
  /**
   * <pre>
   * Verbs granted to the role on this collection.
   * </pre>
   *
   * <code>repeated .ai.stigmer.agentic.datastore.v1.DatastoreVerb verbs = 2 [json_name = "verbs", (.buf.validate.field) = { ... }</code>
   * @return The count of verbs.
   */
  @java.lang.Override
  public int getVerbsCount() {
    return verbs_.size();
  }
  /**
   * <pre>
   * Verbs granted to the role on this collection.
   * </pre>
   *
   * <code>repeated .ai.stigmer.agentic.datastore.v1.DatastoreVerb verbs = 2 [json_name = "verbs", (.buf.validate.field) = { ... }</code>
   * @param index The index of the element to return.
   * @return The verbs at the given index.
   */
  @java.lang.Override
  public ai.stigmer.agentic.datastore.v1.DatastoreVerb getVerbs(int index) {
    return verbs_converter_.convert(verbs_.getInt(index));
  }
  /**
   * <pre>
   * Verbs granted to the role on this collection.
   * </pre>
   *
   * <code>repeated .ai.stigmer.agentic.datastore.v1.DatastoreVerb verbs = 2 [json_name = "verbs", (.buf.validate.field) = { ... }</code>
   * @return A list containing the enum numeric values on the wire for verbs.
   */
  @java.lang.Override
  public java.util.List<java.lang.Integer>
  getVerbsValueList() {
    return verbs_;
  }
  /**
   * <pre>
   * Verbs granted to the role on this collection.
   * </pre>
   *
   * <code>repeated .ai.stigmer.agentic.datastore.v1.DatastoreVerb verbs = 2 [json_name = "verbs", (.buf.validate.field) = { ... }</code>
   * @param index The index of the value to return.
   * @return The enum numeric value on the wire of verbs at the given index.
   */
  @java.lang.Override
  public int getVerbsValue(int index) {
    return verbs_.getInt(index);
  }
  private int verbsMemoizedSerializedSize;

  public static final int SCOPE_FIELD_NUMBER = 3;
  private int scope_ = 0;
  /**
   * <pre>
   * Row scope of the grant.
   *
   * Defaults to all records. `own` limits update and delete (and read)
   * to records whose server-stamped created_by matches the caller.
   * </pre>
   *
   * <code>.ai.stigmer.agentic.datastore.v1.DatastoreGrantScope scope = 3 [json_name = "scope", (.buf.validate.field) = { ... }</code>
   * @return The enum numeric value on the wire for scope.
   */
  @java.lang.Override public int getScopeValue() {
    return scope_;
  }
  /**
   * <pre>
   * Row scope of the grant.
   *
   * Defaults to all records. `own` limits update and delete (and read)
   * to records whose server-stamped created_by matches the caller.
   * </pre>
   *
   * <code>.ai.stigmer.agentic.datastore.v1.DatastoreGrantScope scope = 3 [json_name = "scope", (.buf.validate.field) = { ... }</code>
   * @return The scope.
   */
  @java.lang.Override public ai.stigmer.agentic.datastore.v1.DatastoreGrantScope getScope() {
    ai.stigmer.agentic.datastore.v1.DatastoreGrantScope result = ai.stigmer.agentic.datastore.v1.DatastoreGrantScope.forNumber(scope_);
    return result == null ? ai.stigmer.agentic.datastore.v1.DatastoreGrantScope.UNRECOGNIZED : result;
  }

  public static final int READ_FIELDS_FIELD_NUMBER = 4;
  @SuppressWarnings("serial")
  private com.google.protobuf.LazyStringArrayList readFields_ =
      com.google.protobuf.LazyStringArrayList.emptyList();
  /**
   * <pre>
   * Field allowlist for the read verb: when set, reads by this role
   * return only these fields, and filter conditions and order_by may
   * reference only these fields. Empty means every field is readable.
   *
   * Entries name declared fields of the collection, or `created_by` to
   * expose the attribution subject. The system fields id, created_at,
   * and updated_at are always readable.
   *
   * &#64;internal
   * The column-level GRANT SELECT of the record layer, closing the
   * scope-all-reads-carry-PII residual (dont-dos/002): every record RPC
   * response — find results AND write echoes — is projected to the
   * caller's read-grant field set, and created_by is included only when
   * listed (for channel senders it is the phone number, the most direct
   * PII in the envelope). A caller with no read grant at all receives
   * envelopes with id and timestamps only (write-only access). Empty =
   * unrestricted is forced by proto3 repeated-field semantics (no
   * presence) and mirrors `scope` unset = all. Requires `read` among
   * the grant's verbs — read_fields on a write-only grant is declared
   * intent with no effect and is refused at apply, same posture as the
   * binding relation qualifier. Entry-to-declared-field resolution is
   * domain validation (both editions, byte-identical messages).
   * </pre>
   *
   * <code>repeated string read_fields = 4 [json_name = "readFields", (.buf.validate.field) = { ... }</code>
   * @return A list containing the readFields.
   */
  public com.google.protobuf.ProtocolStringList
      getReadFieldsList() {
    return readFields_;
  }
  /**
   * <pre>
   * Field allowlist for the read verb: when set, reads by this role
   * return only these fields, and filter conditions and order_by may
   * reference only these fields. Empty means every field is readable.
   *
   * Entries name declared fields of the collection, or `created_by` to
   * expose the attribution subject. The system fields id, created_at,
   * and updated_at are always readable.
   *
   * &#64;internal
   * The column-level GRANT SELECT of the record layer, closing the
   * scope-all-reads-carry-PII residual (dont-dos/002): every record RPC
   * response — find results AND write echoes — is projected to the
   * caller's read-grant field set, and created_by is included only when
   * listed (for channel senders it is the phone number, the most direct
   * PII in the envelope). A caller with no read grant at all receives
   * envelopes with id and timestamps only (write-only access). Empty =
   * unrestricted is forced by proto3 repeated-field semantics (no
   * presence) and mirrors `scope` unset = all. Requires `read` among
   * the grant's verbs — read_fields on a write-only grant is declared
   * intent with no effect and is refused at apply, same posture as the
   * binding relation qualifier. Entry-to-declared-field resolution is
   * domain validation (both editions, byte-identical messages).
   * </pre>
   *
   * <code>repeated string read_fields = 4 [json_name = "readFields", (.buf.validate.field) = { ... }</code>
   * @return The count of readFields.
   */
  public int getReadFieldsCount() {
    return readFields_.size();
  }
  /**
   * <pre>
   * Field allowlist for the read verb: when set, reads by this role
   * return only these fields, and filter conditions and order_by may
   * reference only these fields. Empty means every field is readable.
   *
   * Entries name declared fields of the collection, or `created_by` to
   * expose the attribution subject. The system fields id, created_at,
   * and updated_at are always readable.
   *
   * &#64;internal
   * The column-level GRANT SELECT of the record layer, closing the
   * scope-all-reads-carry-PII residual (dont-dos/002): every record RPC
   * response — find results AND write echoes — is projected to the
   * caller's read-grant field set, and created_by is included only when
   * listed (for channel senders it is the phone number, the most direct
   * PII in the envelope). A caller with no read grant at all receives
   * envelopes with id and timestamps only (write-only access). Empty =
   * unrestricted is forced by proto3 repeated-field semantics (no
   * presence) and mirrors `scope` unset = all. Requires `read` among
   * the grant's verbs — read_fields on a write-only grant is declared
   * intent with no effect and is refused at apply, same posture as the
   * binding relation qualifier. Entry-to-declared-field resolution is
   * domain validation (both editions, byte-identical messages).
   * </pre>
   *
   * <code>repeated string read_fields = 4 [json_name = "readFields", (.buf.validate.field) = { ... }</code>
   * @param index The index of the element to return.
   * @return The readFields at the given index.
   */
  public java.lang.String getReadFields(int index) {
    return readFields_.get(index);
  }
  /**
   * <pre>
   * Field allowlist for the read verb: when set, reads by this role
   * return only these fields, and filter conditions and order_by may
   * reference only these fields. Empty means every field is readable.
   *
   * Entries name declared fields of the collection, or `created_by` to
   * expose the attribution subject. The system fields id, created_at,
   * and updated_at are always readable.
   *
   * &#64;internal
   * The column-level GRANT SELECT of the record layer, closing the
   * scope-all-reads-carry-PII residual (dont-dos/002): every record RPC
   * response — find results AND write echoes — is projected to the
   * caller's read-grant field set, and created_by is included only when
   * listed (for channel senders it is the phone number, the most direct
   * PII in the envelope). A caller with no read grant at all receives
   * envelopes with id and timestamps only (write-only access). Empty =
   * unrestricted is forced by proto3 repeated-field semantics (no
   * presence) and mirrors `scope` unset = all. Requires `read` among
   * the grant's verbs — read_fields on a write-only grant is declared
   * intent with no effect and is refused at apply, same posture as the
   * binding relation qualifier. Entry-to-declared-field resolution is
   * domain validation (both editions, byte-identical messages).
   * </pre>
   *
   * <code>repeated string read_fields = 4 [json_name = "readFields", (.buf.validate.field) = { ... }</code>
   * @param index The index of the value to return.
   * @return The bytes of the readFields at the given index.
   */
  public com.google.protobuf.ByteString
      getReadFieldsBytes(int index) {
    return readFields_.getByteString(index);
  }

  private byte memoizedIsInitialized = -1;
  @java.lang.Override
  public final boolean isInitialized() {
    byte isInitialized = memoizedIsInitialized;
    if (isInitialized == 1) return true;
    if (isInitialized == 0) return false;

    memoizedIsInitialized = 1;
    return true;
  }

  @java.lang.Override
  public void writeTo(com.google.protobuf.CodedOutputStream output)
                      throws java.io.IOException {
    getSerializedSize();
    if (!com.google.protobuf.GeneratedMessage.isStringEmpty(role_)) {
      com.google.protobuf.GeneratedMessage.writeString(output, 1, role_);
    }
    if (getVerbsList().size() > 0) {
      output.writeUInt32NoTag(18);
      output.writeUInt32NoTag(verbsMemoizedSerializedSize);
    }
    for (int i = 0; i < verbs_.size(); i++) {
      output.writeEnumNoTag(verbs_.getInt(i));
    }
    if (scope_ != ai.stigmer.agentic.datastore.v1.DatastoreGrantScope.datastore_grant_scope_unspecified.getNumber()) {
      output.writeEnum(3, scope_);
    }
    for (int i = 0; i < readFields_.size(); i++) {
      com.google.protobuf.GeneratedMessage.writeString(output, 4, readFields_.getRaw(i));
    }
    getUnknownFields().writeTo(output);
  }

  @java.lang.Override
  public int getSerializedSize() {
    int size = memoizedSize;
    if (size != -1) return size;

    size = 0;
    if (!com.google.protobuf.GeneratedMessage.isStringEmpty(role_)) {
      size += com.google.protobuf.GeneratedMessage.computeStringSize(1, role_);
    }
    {
      int dataSize = 0;
      for (int i = 0; i < verbs_.size(); i++) {
        dataSize += com.google.protobuf.CodedOutputStream
          .computeEnumSizeNoTag(verbs_.getInt(i));
      }
      size += dataSize;
      if (!getVerbsList().isEmpty()) {  size += 1;
        size += com.google.protobuf.CodedOutputStream
          .computeUInt32SizeNoTag(dataSize);
      }verbsMemoizedSerializedSize = dataSize;
    }
    if (scope_ != ai.stigmer.agentic.datastore.v1.DatastoreGrantScope.datastore_grant_scope_unspecified.getNumber()) {
      size += com.google.protobuf.CodedOutputStream
        .computeEnumSize(3, scope_);
    }
    {
      int dataSize = 0;
      for (int i = 0; i < readFields_.size(); i++) {
        dataSize += computeStringSizeNoTag(readFields_.getRaw(i));
      }
      size += dataSize;
      size += 1 * getReadFieldsList().size();
    }
    size += getUnknownFields().getSerializedSize();
    memoizedSize = size;
    return size;
  }

  @java.lang.Override
  public boolean equals(final java.lang.Object obj) {
    if (obj == this) {
     return true;
    }
    if (!(obj instanceof ai.stigmer.agentic.datastore.v1.DatastoreGrant)) {
      return super.equals(obj);
    }
    ai.stigmer.agentic.datastore.v1.DatastoreGrant other = (ai.stigmer.agentic.datastore.v1.DatastoreGrant) obj;

    if (!getRole()
        .equals(other.getRole())) return false;
    if (!verbs_.equals(other.verbs_)) return false;
    if (scope_ != other.scope_) return false;
    if (!getReadFieldsList()
        .equals(other.getReadFieldsList())) return false;
    if (!getUnknownFields().equals(other.getUnknownFields())) return false;
    return true;
  }

  @java.lang.Override
  public int hashCode() {
    if (memoizedHashCode != 0) {
      return memoizedHashCode;
    }
    int hash = 41;
    hash = (19 * hash) + getDescriptor().hashCode();
    hash = (37 * hash) + ROLE_FIELD_NUMBER;
    hash = (53 * hash) + getRole().hashCode();
    if (getVerbsCount() > 0) {
      hash = (37 * hash) + VERBS_FIELD_NUMBER;
      hash = (53 * hash) + verbs_.hashCode();
    }
    hash = (37 * hash) + SCOPE_FIELD_NUMBER;
    hash = (53 * hash) + scope_;
    if (getReadFieldsCount() > 0) {
      hash = (37 * hash) + READ_FIELDS_FIELD_NUMBER;
      hash = (53 * hash) + getReadFieldsList().hashCode();
    }
    hash = (29 * hash) + getUnknownFields().hashCode();
    memoizedHashCode = hash;
    return hash;
  }

  public static ai.stigmer.agentic.datastore.v1.DatastoreGrant parseFrom(
      java.nio.ByteBuffer data)
      throws com.google.protobuf.InvalidProtocolBufferException {
    return PARSER.parseFrom(data);
  }
  public static ai.stigmer.agentic.datastore.v1.DatastoreGrant parseFrom(
      java.nio.ByteBuffer data,
      com.google.protobuf.ExtensionRegistryLite extensionRegistry)
      throws com.google.protobuf.InvalidProtocolBufferException {
    return PARSER.parseFrom(data, extensionRegistry);
  }
  public static ai.stigmer.agentic.datastore.v1.DatastoreGrant parseFrom(
      com.google.protobuf.ByteString data)
      throws com.google.protobuf.InvalidProtocolBufferException {
    return PARSER.parseFrom(data);
  }
  public static ai.stigmer.agentic.datastore.v1.DatastoreGrant parseFrom(
      com.google.protobuf.ByteString data,
      com.google.protobuf.ExtensionRegistryLite extensionRegistry)
      throws com.google.protobuf.InvalidProtocolBufferException {
    return PARSER.parseFrom(data, extensionRegistry);
  }
  public static ai.stigmer.agentic.datastore.v1.DatastoreGrant parseFrom(byte[] data)
      throws com.google.protobuf.InvalidProtocolBufferException {
    return PARSER.parseFrom(data);
  }
  public static ai.stigmer.agentic.datastore.v1.DatastoreGrant parseFrom(
      byte[] data,
      com.google.protobuf.ExtensionRegistryLite extensionRegistry)
      throws com.google.protobuf.InvalidProtocolBufferException {
    return PARSER.parseFrom(data, extensionRegistry);
  }
  public static ai.stigmer.agentic.datastore.v1.DatastoreGrant parseFrom(java.io.InputStream input)
      throws java.io.IOException {
    return com.google.protobuf.GeneratedMessage
        .parseWithIOException(PARSER, input);
  }
  public static ai.stigmer.agentic.datastore.v1.DatastoreGrant parseFrom(
      java.io.InputStream input,
      com.google.protobuf.ExtensionRegistryLite extensionRegistry)
      throws java.io.IOException {
    return com.google.protobuf.GeneratedMessage
        .parseWithIOException(PARSER, input, extensionRegistry);
  }

  public static ai.stigmer.agentic.datastore.v1.DatastoreGrant parseDelimitedFrom(java.io.InputStream input)
      throws java.io.IOException {
    return com.google.protobuf.GeneratedMessage
        .parseDelimitedWithIOException(PARSER, input);
  }

  public static ai.stigmer.agentic.datastore.v1.DatastoreGrant parseDelimitedFrom(
      java.io.InputStream input,
      com.google.protobuf.ExtensionRegistryLite extensionRegistry)
      throws java.io.IOException {
    return com.google.protobuf.GeneratedMessage
        .parseDelimitedWithIOException(PARSER, input, extensionRegistry);
  }
  public static ai.stigmer.agentic.datastore.v1.DatastoreGrant parseFrom(
      com.google.protobuf.CodedInputStream input)
      throws java.io.IOException {
    return com.google.protobuf.GeneratedMessage
        .parseWithIOException(PARSER, input);
  }
  public static ai.stigmer.agentic.datastore.v1.DatastoreGrant parseFrom(
      com.google.protobuf.CodedInputStream input,
      com.google.protobuf.ExtensionRegistryLite extensionRegistry)
      throws java.io.IOException {
    return com.google.protobuf.GeneratedMessage
        .parseWithIOException(PARSER, input, extensionRegistry);
  }

  @java.lang.Override
  public Builder newBuilderForType() { return newBuilder(); }
  public static Builder newBuilder() {
    return DEFAULT_INSTANCE.toBuilder();
  }
  public static Builder newBuilder(ai.stigmer.agentic.datastore.v1.DatastoreGrant prototype) {
    return DEFAULT_INSTANCE.toBuilder().mergeFrom(prototype);
  }
  @java.lang.Override
  public Builder toBuilder() {
    return this == DEFAULT_INSTANCE
        ? new Builder() : new Builder().mergeFrom(this);
  }

  @java.lang.Override
  protected Builder newBuilderForType(
      com.google.protobuf.GeneratedMessage.BuilderParent parent) {
    Builder builder = new Builder(parent);
    return builder;
  }
  /**
   * <pre>
   * DatastoreGrant gives a role a set of verbs on one collection's records.
   *
   * &#64;internal
   * Apply-time integrity (both editions, the shared spec validators): a
   * (role, verb) pair may appear in at most one grant per collection —
   * with one read grant per role, the resolved access is exactly
   * {scope, read_fields} and no merge rules exist (a duplicate would
   * reopen the composition ambiguity read_fields closes). Stored specs
   * that predate this rule keep their widest-scope-wins resolution; they
   * can never carry read_fields (the rule ships with the field).
   * </pre>
   *
   * Protobuf type {@code ai.stigmer.agentic.datastore.v1.DatastoreGrant}
   */
  public static final class Builder extends
      com.google.protobuf.GeneratedMessage.Builder<Builder> implements
      // @@protoc_insertion_point(builder_implements:ai.stigmer.agentic.datastore.v1.DatastoreGrant)
      ai.stigmer.agentic.datastore.v1.DatastoreGrantOrBuilder {
    public static final com.google.protobuf.Descriptors.Descriptor
        getDescriptor() {
      return ai.stigmer.agentic.datastore.v1.SpecProto.internal_static_ai_stigmer_agentic_datastore_v1_DatastoreGrant_descriptor;
    }

    @java.lang.Override
    protected com.google.protobuf.GeneratedMessage.FieldAccessorTable
        internalGetFieldAccessorTable() {
      return ai.stigmer.agentic.datastore.v1.SpecProto.internal_static_ai_stigmer_agentic_datastore_v1_DatastoreGrant_fieldAccessorTable
          .ensureFieldAccessorsInitialized(
              ai.stigmer.agentic.datastore.v1.DatastoreGrant.class, ai.stigmer.agentic.datastore.v1.DatastoreGrant.Builder.class);
    }

    // Construct using ai.stigmer.agentic.datastore.v1.DatastoreGrant.newBuilder()
    private Builder() {

    }

    private Builder(
        com.google.protobuf.GeneratedMessage.BuilderParent parent) {
      super(parent);

    }
    @java.lang.Override
    public Builder clear() {
      super.clear();
      bitField0_ = 0;
      role_ = "";
      verbs_ = emptyIntList();
      scope_ = 0;
      readFields_ =
          com.google.protobuf.LazyStringArrayList.emptyList();
      return this;
    }

    @java.lang.Override
    public com.google.protobuf.Descriptors.Descriptor
        getDescriptorForType() {
      return ai.stigmer.agentic.datastore.v1.SpecProto.internal_static_ai_stigmer_agentic_datastore_v1_DatastoreGrant_descriptor;
    }

    @java.lang.Override
    public ai.stigmer.agentic.datastore.v1.DatastoreGrant getDefaultInstanceForType() {
      return ai.stigmer.agentic.datastore.v1.DatastoreGrant.getDefaultInstance();
    }

    @java.lang.Override
    public ai.stigmer.agentic.datastore.v1.DatastoreGrant build() {
      ai.stigmer.agentic.datastore.v1.DatastoreGrant result = buildPartial();
      if (!result.isInitialized()) {
        throw newUninitializedMessageException(result);
      }
      return result;
    }

    @java.lang.Override
    public ai.stigmer.agentic.datastore.v1.DatastoreGrant buildPartial() {
      ai.stigmer.agentic.datastore.v1.DatastoreGrant result = new ai.stigmer.agentic.datastore.v1.DatastoreGrant(this);
      if (bitField0_ != 0) { buildPartial0(result); }
      onBuilt();
      return result;
    }

    private void buildPartial0(ai.stigmer.agentic.datastore.v1.DatastoreGrant result) {
      int from_bitField0_ = bitField0_;
      if (((from_bitField0_ & 0x00000001) != 0)) {
        result.role_ = role_;
      }
      if (((from_bitField0_ & 0x00000002) != 0)) {
        verbs_.makeImmutable();
        result.verbs_ = verbs_;
      }
      if (((from_bitField0_ & 0x00000004) != 0)) {
        result.scope_ = scope_;
      }
      if (((from_bitField0_ & 0x00000008) != 0)) {
        readFields_.makeImmutable();
        result.readFields_ = readFields_;
      }
    }

    @java.lang.Override
    public Builder mergeFrom(com.google.protobuf.Message other) {
      if (other instanceof ai.stigmer.agentic.datastore.v1.DatastoreGrant) {
        return mergeFrom((ai.stigmer.agentic.datastore.v1.DatastoreGrant)other);
      } else {
        super.mergeFrom(other);
        return this;
      }
    }

    public Builder mergeFrom(ai.stigmer.agentic.datastore.v1.DatastoreGrant other) {
      if (other == ai.stigmer.agentic.datastore.v1.DatastoreGrant.getDefaultInstance()) return this;
      if (!other.getRole().isEmpty()) {
        role_ = other.role_;
        bitField0_ |= 0x00000001;
        onChanged();
      }
      if (!other.verbs_.isEmpty()) {
        if (verbs_.isEmpty()) {
          verbs_ = other.verbs_;
          verbs_.makeImmutable();
          bitField0_ |= 0x00000002;
        } else {
          ensureVerbsIsMutable();
          verbs_.addAll(other.verbs_);
        }
        onChanged();
      }
      if (other.scope_ != 0) {
        setScopeValue(other.getScopeValue());
      }
      if (!other.readFields_.isEmpty()) {
        if (readFields_.isEmpty()) {
          readFields_ = other.readFields_;
          bitField0_ |= 0x00000008;
        } else {
          ensureReadFieldsIsMutable();
          readFields_.addAll(other.readFields_);
        }
        onChanged();
      }
      this.mergeUnknownFields(other.getUnknownFields());
      onChanged();
      return this;
    }

    @java.lang.Override
    public final boolean isInitialized() {
      return true;
    }

    @java.lang.Override
    public Builder mergeFrom(
        com.google.protobuf.CodedInputStream input,
        com.google.protobuf.ExtensionRegistryLite extensionRegistry)
        throws java.io.IOException {
      if (extensionRegistry == null) {
        throw new java.lang.NullPointerException();
      }
      try {
        boolean done = false;
        while (!done) {
          int tag = input.readTag();
          switch (tag) {
            case 0:
              done = true;
              break;
            case 10: {
              role_ = input.readStringRequireUtf8();
              bitField0_ |= 0x00000001;
              break;
            } // case 10
            case 16: {
              int tmpRaw = input.readEnum();
              ensureVerbsIsMutable();
              verbs_.addInt(tmpRaw);
              break;
            } // case 16
            case 18: {
              int length = input.readRawVarint32();
              int limit = input.pushLimit(length);
              ensureVerbsIsMutable();
              while (input.getBytesUntilLimit() > 0) {
                verbs_.addInt(input.readEnum());
              }
              input.popLimit(limit);
              break;
            } // case 18
            case 24: {
              scope_ = input.readEnum();
              bitField0_ |= 0x00000004;
              break;
            } // case 24
            case 34: {
              ensureReadFieldsIsMutable();
              readFields_.add(input.readStringRequireUtf8());
              break;
            } // case 34
            default: {
              if (!super.parseUnknownField(input, extensionRegistry, tag)) {
                done = true; // was an endgroup tag
              }
              break;
            } // default:
          } // switch (tag)
        } // while (!done)
      } catch (com.google.protobuf.InvalidProtocolBufferException e) {
        throw e.unwrapIOException();
      } finally {
        onChanged();
      } // finally
      return this;
    }
    private int bitField0_;

    private java.lang.Object role_ = "";
    /**
     * <pre>
     * Name of a declared role.
     * </pre>
     *
     * <code>string role = 1 [json_name = "role", (.buf.validate.field) = { ... }</code>
     * @return The role.
     */
    public java.lang.String getRole() {
      java.lang.Object ref = role_;
      if (!(ref instanceof java.lang.String)) {
        com.google.protobuf.ByteString bs =
            (com.google.protobuf.ByteString) ref;
        java.lang.String s = bs.toStringUtf8();
        role_ = s;
        return s;
      } else {
        return (java.lang.String) ref;
      }
    }
    /**
     * <pre>
     * Name of a declared role.
     * </pre>
     *
     * <code>string role = 1 [json_name = "role", (.buf.validate.field) = { ... }</code>
     * @return The bytes for role.
     */
    public com.google.protobuf.ByteString
        getRoleBytes() {
      java.lang.Object ref = role_;
      if (ref instanceof String) {
        com.google.protobuf.ByteString b = 
            com.google.protobuf.ByteString.copyFromUtf8(
                (java.lang.String) ref);
        role_ = b;
        return b;
      } else {
        return (com.google.protobuf.ByteString) ref;
      }
    }
    /**
     * <pre>
     * Name of a declared role.
     * </pre>
     *
     * <code>string role = 1 [json_name = "role", (.buf.validate.field) = { ... }</code>
     * @param value The role to set.
     * @return This builder for chaining.
     */
    public Builder setRole(
        java.lang.String value) {
      if (value == null) { throw new NullPointerException(); }
      role_ = value;
      bitField0_ |= 0x00000001;
      onChanged();
      return this;
    }
    /**
     * <pre>
     * Name of a declared role.
     * </pre>
     *
     * <code>string role = 1 [json_name = "role", (.buf.validate.field) = { ... }</code>
     * @return This builder for chaining.
     */
    public Builder clearRole() {
      role_ = getDefaultInstance().getRole();
      bitField0_ = (bitField0_ & ~0x00000001);
      onChanged();
      return this;
    }
    /**
     * <pre>
     * Name of a declared role.
     * </pre>
     *
     * <code>string role = 1 [json_name = "role", (.buf.validate.field) = { ... }</code>
     * @param value The bytes for role to set.
     * @return This builder for chaining.
     */
    public Builder setRoleBytes(
        com.google.protobuf.ByteString value) {
      if (value == null) { throw new NullPointerException(); }
      checkByteStringIsUtf8(value);
      role_ = value;
      bitField0_ |= 0x00000001;
      onChanged();
      return this;
    }

    private com.google.protobuf.Internal.IntList verbs_ = emptyIntList();
    private void ensureVerbsIsMutable() {
      if (!verbs_.isModifiable()) {
        verbs_ = makeMutableCopy(verbs_);
      }
      bitField0_ |= 0x00000002;
    }
    /**
     * <pre>
     * Verbs granted to the role on this collection.
     * </pre>
     *
     * <code>repeated .ai.stigmer.agentic.datastore.v1.DatastoreVerb verbs = 2 [json_name = "verbs", (.buf.validate.field) = { ... }</code>
     * @return A list containing the verbs.
     */
    public java.util.List<ai.stigmer.agentic.datastore.v1.DatastoreVerb> getVerbsList() {
      return new com.google.protobuf.Internal.IntListAdapter<
          ai.stigmer.agentic.datastore.v1.DatastoreVerb>(verbs_, verbs_converter_);
    }
    /**
     * <pre>
     * Verbs granted to the role on this collection.
     * </pre>
     *
     * <code>repeated .ai.stigmer.agentic.datastore.v1.DatastoreVerb verbs = 2 [json_name = "verbs", (.buf.validate.field) = { ... }</code>
     * @return The count of verbs.
     */
    public int getVerbsCount() {
      return verbs_.size();
    }
    /**
     * <pre>
     * Verbs granted to the role on this collection.
     * </pre>
     *
     * <code>repeated .ai.stigmer.agentic.datastore.v1.DatastoreVerb verbs = 2 [json_name = "verbs", (.buf.validate.field) = { ... }</code>
     * @param index The index of the element to return.
     * @return The verbs at the given index.
     */
    public ai.stigmer.agentic.datastore.v1.DatastoreVerb getVerbs(int index) {
      return verbs_converter_.convert(verbs_.getInt(index));
    }
    /**
     * <pre>
     * Verbs granted to the role on this collection.
     * </pre>
     *
     * <code>repeated .ai.stigmer.agentic.datastore.v1.DatastoreVerb verbs = 2 [json_name = "verbs", (.buf.validate.field) = { ... }</code>
     * @param index The index to set the value at.
     * @param value The verbs to set.
     * @return This builder for chaining.
     */
    public Builder setVerbs(
        int index, ai.stigmer.agentic.datastore.v1.DatastoreVerb value) {
      if (value == null) { throw new NullPointerException(); }
      ensureVerbsIsMutable();
      verbs_.setInt(index, value.getNumber());
      onChanged();
      return this;
    }
    /**
     * <pre>
     * Verbs granted to the role on this collection.
     * </pre>
     *
     * <code>repeated .ai.stigmer.agentic.datastore.v1.DatastoreVerb verbs = 2 [json_name = "verbs", (.buf.validate.field) = { ... }</code>
     * @param value The verbs to add.
     * @return This builder for chaining.
     */
    public Builder addVerbs(ai.stigmer.agentic.datastore.v1.DatastoreVerb value) {
      if (value == null) { throw new NullPointerException(); }
      ensureVerbsIsMutable();
      verbs_.addInt(value.getNumber());
      onChanged();
      return this;
    }
    /**
     * <pre>
     * Verbs granted to the role on this collection.
     * </pre>
     *
     * <code>repeated .ai.stigmer.agentic.datastore.v1.DatastoreVerb verbs = 2 [json_name = "verbs", (.buf.validate.field) = { ... }</code>
     * @param values The verbs to add.
     * @return This builder for chaining.
     */
    public Builder addAllVerbs(
        java.lang.Iterable<? extends ai.stigmer.agentic.datastore.v1.DatastoreVerb> values) {
      ensureVerbsIsMutable();
      for (ai.stigmer.agentic.datastore.v1.DatastoreVerb value : values) {
        verbs_.addInt(value.getNumber());
      }
      onChanged();
      return this;
    }
    /**
     * <pre>
     * Verbs granted to the role on this collection.
     * </pre>
     *
     * <code>repeated .ai.stigmer.agentic.datastore.v1.DatastoreVerb verbs = 2 [json_name = "verbs", (.buf.validate.field) = { ... }</code>
     * @return This builder for chaining.
     */
    public Builder clearVerbs() {
      verbs_ = emptyIntList();
      bitField0_ = (bitField0_ & ~0x00000002);
      onChanged();
      return this;
    }
    /**
     * <pre>
     * Verbs granted to the role on this collection.
     * </pre>
     *
     * <code>repeated .ai.stigmer.agentic.datastore.v1.DatastoreVerb verbs = 2 [json_name = "verbs", (.buf.validate.field) = { ... }</code>
     * @return A list containing the enum numeric values on the wire for verbs.
     */
    public java.util.List<java.lang.Integer>
    getVerbsValueList() {
      verbs_.makeImmutable();
      return verbs_;
    }
    /**
     * <pre>
     * Verbs granted to the role on this collection.
     * </pre>
     *
     * <code>repeated .ai.stigmer.agentic.datastore.v1.DatastoreVerb verbs = 2 [json_name = "verbs", (.buf.validate.field) = { ... }</code>
     * @param index The index of the value to return.
     * @return The enum numeric value on the wire of verbs at the given index.
     */
    public int getVerbsValue(int index) {
      return verbs_.getInt(index);
    }
    /**
     * <pre>
     * Verbs granted to the role on this collection.
     * </pre>
     *
     * <code>repeated .ai.stigmer.agentic.datastore.v1.DatastoreVerb verbs = 2 [json_name = "verbs", (.buf.validate.field) = { ... }</code>
     * @param index The index to set the value at.
     * @param value The enum numeric value on the wire for verbs to set.
     * @return This builder for chaining.
     */
    public Builder setVerbsValue(
        int index, int value) {
      ensureVerbsIsMutable();
      verbs_.setInt(index, value);
      onChanged();
      return this;
    }
    /**
     * <pre>
     * Verbs granted to the role on this collection.
     * </pre>
     *
     * <code>repeated .ai.stigmer.agentic.datastore.v1.DatastoreVerb verbs = 2 [json_name = "verbs", (.buf.validate.field) = { ... }</code>
     * @param value The enum numeric value on the wire for verbs to add.
     * @throws IllegalArgumentException if UNRECOGNIZED is provided.
     * @return This builder for chaining.
     */
    public Builder addVerbsValue(int value) {
      ensureVerbsIsMutable();
      verbs_.addInt(value);
      onChanged();
      return this;
    }
    /**
     * <pre>
     * Verbs granted to the role on this collection.
     * </pre>
     *
     * <code>repeated .ai.stigmer.agentic.datastore.v1.DatastoreVerb verbs = 2 [json_name = "verbs", (.buf.validate.field) = { ... }</code>
     * @param values The enum numeric values on the wire for verbs to add.
     * @throws IllegalArgumentException if UNRECOGNIZED is provided.
     * @return This builder for chaining.
     */
    public Builder addAllVerbsValue(
        java.lang.Iterable<java.lang.Integer> values) {
      ensureVerbsIsMutable();
      for (int value : values) {
        verbs_.addInt(value);
      }
      onChanged();
      return this;
    }

    private int scope_ = 0;
    /**
     * <pre>
     * Row scope of the grant.
     *
     * Defaults to all records. `own` limits update and delete (and read)
     * to records whose server-stamped created_by matches the caller.
     * </pre>
     *
     * <code>.ai.stigmer.agentic.datastore.v1.DatastoreGrantScope scope = 3 [json_name = "scope", (.buf.validate.field) = { ... }</code>
     * @return The enum numeric value on the wire for scope.
     */
    @java.lang.Override public int getScopeValue() {
      return scope_;
    }
    /**
     * <pre>
     * Row scope of the grant.
     *
     * Defaults to all records. `own` limits update and delete (and read)
     * to records whose server-stamped created_by matches the caller.
     * </pre>
     *
     * <code>.ai.stigmer.agentic.datastore.v1.DatastoreGrantScope scope = 3 [json_name = "scope", (.buf.validate.field) = { ... }</code>
     * @param value The enum numeric value on the wire for scope to set.
     * @throws IllegalArgumentException if UNRECOGNIZED is provided.
     * @return This builder for chaining.
     */
    public Builder setScopeValue(int value) {
      scope_ = value;
      bitField0_ |= 0x00000004;
      onChanged();
      return this;
    }
    /**
     * <pre>
     * Row scope of the grant.
     *
     * Defaults to all records. `own` limits update and delete (and read)
     * to records whose server-stamped created_by matches the caller.
     * </pre>
     *
     * <code>.ai.stigmer.agentic.datastore.v1.DatastoreGrantScope scope = 3 [json_name = "scope", (.buf.validate.field) = { ... }</code>
     * @return The scope.
     */
    @java.lang.Override
    public ai.stigmer.agentic.datastore.v1.DatastoreGrantScope getScope() {
      ai.stigmer.agentic.datastore.v1.DatastoreGrantScope result = ai.stigmer.agentic.datastore.v1.DatastoreGrantScope.forNumber(scope_);
      return result == null ? ai.stigmer.agentic.datastore.v1.DatastoreGrantScope.UNRECOGNIZED : result;
    }
    /**
     * <pre>
     * Row scope of the grant.
     *
     * Defaults to all records. `own` limits update and delete (and read)
     * to records whose server-stamped created_by matches the caller.
     * </pre>
     *
     * <code>.ai.stigmer.agentic.datastore.v1.DatastoreGrantScope scope = 3 [json_name = "scope", (.buf.validate.field) = { ... }</code>
     * @param value The scope to set.
     * @return This builder for chaining.
     */
    public Builder setScope(ai.stigmer.agentic.datastore.v1.DatastoreGrantScope value) {
      if (value == null) { throw new NullPointerException(); }
      bitField0_ |= 0x00000004;
      scope_ = value.getNumber();
      onChanged();
      return this;
    }
    /**
     * <pre>
     * Row scope of the grant.
     *
     * Defaults to all records. `own` limits update and delete (and read)
     * to records whose server-stamped created_by matches the caller.
     * </pre>
     *
     * <code>.ai.stigmer.agentic.datastore.v1.DatastoreGrantScope scope = 3 [json_name = "scope", (.buf.validate.field) = { ... }</code>
     * @return This builder for chaining.
     */
    public Builder clearScope() {
      bitField0_ = (bitField0_ & ~0x00000004);
      scope_ = 0;
      onChanged();
      return this;
    }

    private com.google.protobuf.LazyStringArrayList readFields_ =
        com.google.protobuf.LazyStringArrayList.emptyList();
    private void ensureReadFieldsIsMutable() {
      if (!readFields_.isModifiable()) {
        readFields_ = new com.google.protobuf.LazyStringArrayList(readFields_);
      }
      bitField0_ |= 0x00000008;
    }
    /**
     * <pre>
     * Field allowlist for the read verb: when set, reads by this role
     * return only these fields, and filter conditions and order_by may
     * reference only these fields. Empty means every field is readable.
     *
     * Entries name declared fields of the collection, or `created_by` to
     * expose the attribution subject. The system fields id, created_at,
     * and updated_at are always readable.
     *
     * &#64;internal
     * The column-level GRANT SELECT of the record layer, closing the
     * scope-all-reads-carry-PII residual (dont-dos/002): every record RPC
     * response — find results AND write echoes — is projected to the
     * caller's read-grant field set, and created_by is included only when
     * listed (for channel senders it is the phone number, the most direct
     * PII in the envelope). A caller with no read grant at all receives
     * envelopes with id and timestamps only (write-only access). Empty =
     * unrestricted is forced by proto3 repeated-field semantics (no
     * presence) and mirrors `scope` unset = all. Requires `read` among
     * the grant's verbs — read_fields on a write-only grant is declared
     * intent with no effect and is refused at apply, same posture as the
     * binding relation qualifier. Entry-to-declared-field resolution is
     * domain validation (both editions, byte-identical messages).
     * </pre>
     *
     * <code>repeated string read_fields = 4 [json_name = "readFields", (.buf.validate.field) = { ... }</code>
     * @return A list containing the readFields.
     */
    public com.google.protobuf.ProtocolStringList
        getReadFieldsList() {
      readFields_.makeImmutable();
      return readFields_;
    }
    /**
     * <pre>
     * Field allowlist for the read verb: when set, reads by this role
     * return only these fields, and filter conditions and order_by may
     * reference only these fields. Empty means every field is readable.
     *
     * Entries name declared fields of the collection, or `created_by` to
     * expose the attribution subject. The system fields id, created_at,
     * and updated_at are always readable.
     *
     * &#64;internal
     * The column-level GRANT SELECT of the record layer, closing the
     * scope-all-reads-carry-PII residual (dont-dos/002): every record RPC
     * response — find results AND write echoes — is projected to the
     * caller's read-grant field set, and created_by is included only when
     * listed (for channel senders it is the phone number, the most direct
     * PII in the envelope). A caller with no read grant at all receives
     * envelopes with id and timestamps only (write-only access). Empty =
     * unrestricted is forced by proto3 repeated-field semantics (no
     * presence) and mirrors `scope` unset = all. Requires `read` among
     * the grant's verbs — read_fields on a write-only grant is declared
     * intent with no effect and is refused at apply, same posture as the
     * binding relation qualifier. Entry-to-declared-field resolution is
     * domain validation (both editions, byte-identical messages).
     * </pre>
     *
     * <code>repeated string read_fields = 4 [json_name = "readFields", (.buf.validate.field) = { ... }</code>
     * @return The count of readFields.
     */
    public int getReadFieldsCount() {
      return readFields_.size();
    }
    /**
     * <pre>
     * Field allowlist for the read verb: when set, reads by this role
     * return only these fields, and filter conditions and order_by may
     * reference only these fields. Empty means every field is readable.
     *
     * Entries name declared fields of the collection, or `created_by` to
     * expose the attribution subject. The system fields id, created_at,
     * and updated_at are always readable.
     *
     * &#64;internal
     * The column-level GRANT SELECT of the record layer, closing the
     * scope-all-reads-carry-PII residual (dont-dos/002): every record RPC
     * response — find results AND write echoes — is projected to the
     * caller's read-grant field set, and created_by is included only when
     * listed (for channel senders it is the phone number, the most direct
     * PII in the envelope). A caller with no read grant at all receives
     * envelopes with id and timestamps only (write-only access). Empty =
     * unrestricted is forced by proto3 repeated-field semantics (no
     * presence) and mirrors `scope` unset = all. Requires `read` among
     * the grant's verbs — read_fields on a write-only grant is declared
     * intent with no effect and is refused at apply, same posture as the
     * binding relation qualifier. Entry-to-declared-field resolution is
     * domain validation (both editions, byte-identical messages).
     * </pre>
     *
     * <code>repeated string read_fields = 4 [json_name = "readFields", (.buf.validate.field) = { ... }</code>
     * @param index The index of the element to return.
     * @return The readFields at the given index.
     */
    public java.lang.String getReadFields(int index) {
      return readFields_.get(index);
    }
    /**
     * <pre>
     * Field allowlist for the read verb: when set, reads by this role
     * return only these fields, and filter conditions and order_by may
     * reference only these fields. Empty means every field is readable.
     *
     * Entries name declared fields of the collection, or `created_by` to
     * expose the attribution subject. The system fields id, created_at,
     * and updated_at are always readable.
     *
     * &#64;internal
     * The column-level GRANT SELECT of the record layer, closing the
     * scope-all-reads-carry-PII residual (dont-dos/002): every record RPC
     * response — find results AND write echoes — is projected to the
     * caller's read-grant field set, and created_by is included only when
     * listed (for channel senders it is the phone number, the most direct
     * PII in the envelope). A caller with no read grant at all receives
     * envelopes with id and timestamps only (write-only access). Empty =
     * unrestricted is forced by proto3 repeated-field semantics (no
     * presence) and mirrors `scope` unset = all. Requires `read` among
     * the grant's verbs — read_fields on a write-only grant is declared
     * intent with no effect and is refused at apply, same posture as the
     * binding relation qualifier. Entry-to-declared-field resolution is
     * domain validation (both editions, byte-identical messages).
     * </pre>
     *
     * <code>repeated string read_fields = 4 [json_name = "readFields", (.buf.validate.field) = { ... }</code>
     * @param index The index of the value to return.
     * @return The bytes of the readFields at the given index.
     */
    public com.google.protobuf.ByteString
        getReadFieldsBytes(int index) {
      return readFields_.getByteString(index);
    }
    /**
     * <pre>
     * Field allowlist for the read verb: when set, reads by this role
     * return only these fields, and filter conditions and order_by may
     * reference only these fields. Empty means every field is readable.
     *
     * Entries name declared fields of the collection, or `created_by` to
     * expose the attribution subject. The system fields id, created_at,
     * and updated_at are always readable.
     *
     * &#64;internal
     * The column-level GRANT SELECT of the record layer, closing the
     * scope-all-reads-carry-PII residual (dont-dos/002): every record RPC
     * response — find results AND write echoes — is projected to the
     * caller's read-grant field set, and created_by is included only when
     * listed (for channel senders it is the phone number, the most direct
     * PII in the envelope). A caller with no read grant at all receives
     * envelopes with id and timestamps only (write-only access). Empty =
     * unrestricted is forced by proto3 repeated-field semantics (no
     * presence) and mirrors `scope` unset = all. Requires `read` among
     * the grant's verbs — read_fields on a write-only grant is declared
     * intent with no effect and is refused at apply, same posture as the
     * binding relation qualifier. Entry-to-declared-field resolution is
     * domain validation (both editions, byte-identical messages).
     * </pre>
     *
     * <code>repeated string read_fields = 4 [json_name = "readFields", (.buf.validate.field) = { ... }</code>
     * @param index The index to set the value at.
     * @param value The readFields to set.
     * @return This builder for chaining.
     */
    public Builder setReadFields(
        int index, java.lang.String value) {
      if (value == null) { throw new NullPointerException(); }
      ensureReadFieldsIsMutable();
      readFields_.set(index, value);
      bitField0_ |= 0x00000008;
      onChanged();
      return this;
    }
    /**
     * <pre>
     * Field allowlist for the read verb: when set, reads by this role
     * return only these fields, and filter conditions and order_by may
     * reference only these fields. Empty means every field is readable.
     *
     * Entries name declared fields of the collection, or `created_by` to
     * expose the attribution subject. The system fields id, created_at,
     * and updated_at are always readable.
     *
     * &#64;internal
     * The column-level GRANT SELECT of the record layer, closing the
     * scope-all-reads-carry-PII residual (dont-dos/002): every record RPC
     * response — find results AND write echoes — is projected to the
     * caller's read-grant field set, and created_by is included only when
     * listed (for channel senders it is the phone number, the most direct
     * PII in the envelope). A caller with no read grant at all receives
     * envelopes with id and timestamps only (write-only access). Empty =
     * unrestricted is forced by proto3 repeated-field semantics (no
     * presence) and mirrors `scope` unset = all. Requires `read` among
     * the grant's verbs — read_fields on a write-only grant is declared
     * intent with no effect and is refused at apply, same posture as the
     * binding relation qualifier. Entry-to-declared-field resolution is
     * domain validation (both editions, byte-identical messages).
     * </pre>
     *
     * <code>repeated string read_fields = 4 [json_name = "readFields", (.buf.validate.field) = { ... }</code>
     * @param value The readFields to add.
     * @return This builder for chaining.
     */
    public Builder addReadFields(
        java.lang.String value) {
      if (value == null) { throw new NullPointerException(); }
      ensureReadFieldsIsMutable();
      readFields_.add(value);
      bitField0_ |= 0x00000008;
      onChanged();
      return this;
    }
    /**
     * <pre>
     * Field allowlist for the read verb: when set, reads by this role
     * return only these fields, and filter conditions and order_by may
     * reference only these fields. Empty means every field is readable.
     *
     * Entries name declared fields of the collection, or `created_by` to
     * expose the attribution subject. The system fields id, created_at,
     * and updated_at are always readable.
     *
     * &#64;internal
     * The column-level GRANT SELECT of the record layer, closing the
     * scope-all-reads-carry-PII residual (dont-dos/002): every record RPC
     * response — find results AND write echoes — is projected to the
     * caller's read-grant field set, and created_by is included only when
     * listed (for channel senders it is the phone number, the most direct
     * PII in the envelope). A caller with no read grant at all receives
     * envelopes with id and timestamps only (write-only access). Empty =
     * unrestricted is forced by proto3 repeated-field semantics (no
     * presence) and mirrors `scope` unset = all. Requires `read` among
     * the grant's verbs — read_fields on a write-only grant is declared
     * intent with no effect and is refused at apply, same posture as the
     * binding relation qualifier. Entry-to-declared-field resolution is
     * domain validation (both editions, byte-identical messages).
     * </pre>
     *
     * <code>repeated string read_fields = 4 [json_name = "readFields", (.buf.validate.field) = { ... }</code>
     * @param values The readFields to add.
     * @return This builder for chaining.
     */
    public Builder addAllReadFields(
        java.lang.Iterable<java.lang.String> values) {
      ensureReadFieldsIsMutable();
      com.google.protobuf.AbstractMessageLite.Builder.addAll(
          values, readFields_);
      bitField0_ |= 0x00000008;
      onChanged();
      return this;
    }
    /**
     * <pre>
     * Field allowlist for the read verb: when set, reads by this role
     * return only these fields, and filter conditions and order_by may
     * reference only these fields. Empty means every field is readable.
     *
     * Entries name declared fields of the collection, or `created_by` to
     * expose the attribution subject. The system fields id, created_at,
     * and updated_at are always readable.
     *
     * &#64;internal
     * The column-level GRANT SELECT of the record layer, closing the
     * scope-all-reads-carry-PII residual (dont-dos/002): every record RPC
     * response — find results AND write echoes — is projected to the
     * caller's read-grant field set, and created_by is included only when
     * listed (for channel senders it is the phone number, the most direct
     * PII in the envelope). A caller with no read grant at all receives
     * envelopes with id and timestamps only (write-only access). Empty =
     * unrestricted is forced by proto3 repeated-field semantics (no
     * presence) and mirrors `scope` unset = all. Requires `read` among
     * the grant's verbs — read_fields on a write-only grant is declared
     * intent with no effect and is refused at apply, same posture as the
     * binding relation qualifier. Entry-to-declared-field resolution is
     * domain validation (both editions, byte-identical messages).
     * </pre>
     *
     * <code>repeated string read_fields = 4 [json_name = "readFields", (.buf.validate.field) = { ... }</code>
     * @return This builder for chaining.
     */
    public Builder clearReadFields() {
      readFields_ =
        com.google.protobuf.LazyStringArrayList.emptyList();
      bitField0_ = (bitField0_ & ~0x00000008);;
      onChanged();
      return this;
    }
    /**
     * <pre>
     * Field allowlist for the read verb: when set, reads by this role
     * return only these fields, and filter conditions and order_by may
     * reference only these fields. Empty means every field is readable.
     *
     * Entries name declared fields of the collection, or `created_by` to
     * expose the attribution subject. The system fields id, created_at,
     * and updated_at are always readable.
     *
     * &#64;internal
     * The column-level GRANT SELECT of the record layer, closing the
     * scope-all-reads-carry-PII residual (dont-dos/002): every record RPC
     * response — find results AND write echoes — is projected to the
     * caller's read-grant field set, and created_by is included only when
     * listed (for channel senders it is the phone number, the most direct
     * PII in the envelope). A caller with no read grant at all receives
     * envelopes with id and timestamps only (write-only access). Empty =
     * unrestricted is forced by proto3 repeated-field semantics (no
     * presence) and mirrors `scope` unset = all. Requires `read` among
     * the grant's verbs — read_fields on a write-only grant is declared
     * intent with no effect and is refused at apply, same posture as the
     * binding relation qualifier. Entry-to-declared-field resolution is
     * domain validation (both editions, byte-identical messages).
     * </pre>
     *
     * <code>repeated string read_fields = 4 [json_name = "readFields", (.buf.validate.field) = { ... }</code>
     * @param value The bytes of the readFields to add.
     * @return This builder for chaining.
     */
    public Builder addReadFieldsBytes(
        com.google.protobuf.ByteString value) {
      if (value == null) { throw new NullPointerException(); }
      checkByteStringIsUtf8(value);
      ensureReadFieldsIsMutable();
      readFields_.add(value);
      bitField0_ |= 0x00000008;
      onChanged();
      return this;
    }

    // @@protoc_insertion_point(builder_scope:ai.stigmer.agentic.datastore.v1.DatastoreGrant)
  }

  // @@protoc_insertion_point(class_scope:ai.stigmer.agentic.datastore.v1.DatastoreGrant)
  private static final ai.stigmer.agentic.datastore.v1.DatastoreGrant DEFAULT_INSTANCE;
  static {
    DEFAULT_INSTANCE = new ai.stigmer.agentic.datastore.v1.DatastoreGrant();
  }

  public static ai.stigmer.agentic.datastore.v1.DatastoreGrant getDefaultInstance() {
    return DEFAULT_INSTANCE;
  }

  private static final com.google.protobuf.Parser<DatastoreGrant>
      PARSER = new com.google.protobuf.AbstractParser<DatastoreGrant>() {
    @java.lang.Override
    public DatastoreGrant parsePartialFrom(
        com.google.protobuf.CodedInputStream input,
        com.google.protobuf.ExtensionRegistryLite extensionRegistry)
        throws com.google.protobuf.InvalidProtocolBufferException {
      Builder builder = newBuilder();
      try {
        builder.mergeFrom(input, extensionRegistry);
      } catch (com.google.protobuf.InvalidProtocolBufferException e) {
        throw e.setUnfinishedMessage(builder.buildPartial());
      } catch (com.google.protobuf.UninitializedMessageException e) {
        throw e.asInvalidProtocolBufferException().setUnfinishedMessage(builder.buildPartial());
      } catch (java.io.IOException e) {
        throw new com.google.protobuf.InvalidProtocolBufferException(e)
            .setUnfinishedMessage(builder.buildPartial());
      }
      return builder.buildPartial();
    }
  };

  public static com.google.protobuf.Parser<DatastoreGrant> parser() {
    return PARSER;
  }

  @java.lang.Override
  public com.google.protobuf.Parser<DatastoreGrant> getParserForType() {
    return PARSER;
  }

  @java.lang.Override
  public ai.stigmer.agentic.datastore.v1.DatastoreGrant getDefaultInstanceForType() {
    return DEFAULT_INSTANCE;
  }

}

