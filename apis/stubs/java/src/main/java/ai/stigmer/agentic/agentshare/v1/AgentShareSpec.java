// Generated by the protocol buffer compiler.  DO NOT EDIT!
// NO CHECKED-IN PROTOBUF GENCODE
// source: ai/stigmer/agentic/agentshare/v1/spec.proto
// Protobuf Java Version: 4.34.0

package ai.stigmer.agentic.agentshare.v1;

/**
 * <pre>
 * AgentShareSpec defines the configurable properties of an agent share.
 *
 * A share is a distribution channel for an agent's hosted chat experience.
 * It carries its own audience, embed origins, visitor-facing messages, and
 * tool credentials — all independent of the agent blueprint it references.
 *
 * &#64;internal
 * Sharing is channel configuration, not agent behavior (decision 011):
 * applying an agent manifest never touches a share, and the share has its
 * own writer, lifecycle, and change cadence. Guest admission is enforced
 * app-level in the shared-profile/mint/create-time gates — a share writes
 * NO FGA visibility tuples, because a public wildcard viewer tuple would
 * expose the referenced agent's full blueprint (instructions included) to
 * any authenticated account, conflating blueprint visibility with chat
 * access. The overview.md file provides the SDK-facing description and
 * example YAML.
 * </pre>
 *
 * Protobuf type {@code ai.stigmer.agentic.agentshare.v1.AgentShareSpec}
 */
@com.google.protobuf.Generated
public final class AgentShareSpec extends
    com.google.protobuf.GeneratedMessage implements
    // @@protoc_insertion_point(message_implements:ai.stigmer.agentic.agentshare.v1.AgentShareSpec)
    AgentShareSpecOrBuilder {
private static final long serialVersionUID = 0L;
  static {
    com.google.protobuf.RuntimeVersion.validateProtobufGencodeVersion(
      com.google.protobuf.RuntimeVersion.RuntimeDomain.PUBLIC,
      /* major= */ 4,
      /* minor= */ 34,
      /* patch= */ 0,
      /* suffix= */ "",
      "AgentShareSpec");
  }
  // Use AgentShareSpec.newBuilder() to construct.
  private AgentShareSpec(com.google.protobuf.GeneratedMessage.Builder<?> builder) {
    super(builder);
  }
  private AgentShareSpec() {
    audience_ = 0;
    allowedOrigins_ =
        com.google.protobuf.LazyStringArrayList.emptyList();
    environmentRefs_ = java.util.Collections.emptyList();
  }

  public static final com.google.protobuf.Descriptors.Descriptor
      getDescriptor() {
    return ai.stigmer.agentic.agentshare.v1.SpecProto.internal_static_ai_stigmer_agentic_agentshare_v1_AgentShareSpec_descriptor;
  }

  @java.lang.Override
  public com.google.protobuf.Descriptors.Descriptor getDescriptorForType() {
    return ai.stigmer.agentic.agentshare.v1.SpecProto.internal_static_ai_stigmer_agentic_agentshare_v1_AgentShareSpec_descriptor;
  }

  @java.lang.Override
  protected com.google.protobuf.GeneratedMessage.FieldAccessorTable
      internalGetFieldAccessorTable() {
    return ai.stigmer.agentic.agentshare.v1.SpecProto.internal_static_ai_stigmer_agentic_agentshare_v1_AgentShareSpec_fieldAccessorTable
        .ensureFieldAccessorsInitialized(
            ai.stigmer.agentic.agentshare.v1.AgentShareSpec.class, ai.stigmer.agentic.agentshare.v1.AgentShareSpec.Builder.class);
  }

  private int bitField0_;
  public static final int AGENT_REF_FIELD_NUMBER = 1;
  private ai.stigmer.commons.apiresource.ApiResourceReference agentRef_;
  /**
   * <pre>
   * Reference to the agent this share distributes.
   *
   * The referenced agent may live in another organization when it is
   * marketplace-public: the share then offers that agent from this share's
   * org, billed to this share's org, with tool credentials bound from this
   * share's org. Cross-org shares must have a public audience.
   *
   * &#64;internal
   * Cross-org contract (decision 013): making an agent visibility_public
   * IS the origin org's consent to external shares — enforced app-level in
   * both editions' defaults resolvers (spec-level CEL cannot see
   * metadata.org), together with the public-audience-only rule and the
   * dependency-publicness sweep (every skill/MCP the agent declares must be
   * visibility_public, refused loudly naming blockers). The sharing org is
   * always the billing org (decision 001 generalized) and guest tool
   * credentials resolve in the share's org (secrets never cross orgs —
   * org A's environments are structurally unreachable from org B's
   * executions via OrgSharedEnvironmentPolicy). The agent's public
   * visibility is re-verified on every guest turn, at the shared profile,
   * and at guest mint, so revoking visibility kills external channels on
   * the visitor's next message. status.agent_id pins the resolved agent
   * against slug-reuse rebind.
   * </pre>
   *
   * <code>.ai.stigmer.commons.apiresource.ApiResourceReference agent_ref = 1 [json_name = "agentRef", (.buf.validate.field) = { ... }</code>
   * @return Whether the agentRef field is set.
   */
  @java.lang.Override
  public boolean hasAgentRef() {
    return ((bitField0_ & 0x00000001) != 0);
  }
  /**
   * <pre>
   * Reference to the agent this share distributes.
   *
   * The referenced agent may live in another organization when it is
   * marketplace-public: the share then offers that agent from this share's
   * org, billed to this share's org, with tool credentials bound from this
   * share's org. Cross-org shares must have a public audience.
   *
   * &#64;internal
   * Cross-org contract (decision 013): making an agent visibility_public
   * IS the origin org's consent to external shares — enforced app-level in
   * both editions' defaults resolvers (spec-level CEL cannot see
   * metadata.org), together with the public-audience-only rule and the
   * dependency-publicness sweep (every skill/MCP the agent declares must be
   * visibility_public, refused loudly naming blockers). The sharing org is
   * always the billing org (decision 001 generalized) and guest tool
   * credentials resolve in the share's org (secrets never cross orgs —
   * org A's environments are structurally unreachable from org B's
   * executions via OrgSharedEnvironmentPolicy). The agent's public
   * visibility is re-verified on every guest turn, at the shared profile,
   * and at guest mint, so revoking visibility kills external channels on
   * the visitor's next message. status.agent_id pins the resolved agent
   * against slug-reuse rebind.
   * </pre>
   *
   * <code>.ai.stigmer.commons.apiresource.ApiResourceReference agent_ref = 1 [json_name = "agentRef", (.buf.validate.field) = { ... }</code>
   * @return The agentRef.
   */
  @java.lang.Override
  public ai.stigmer.commons.apiresource.ApiResourceReference getAgentRef() {
    return agentRef_ == null ? ai.stigmer.commons.apiresource.ApiResourceReference.getDefaultInstance() : agentRef_;
  }
  /**
   * <pre>
   * Reference to the agent this share distributes.
   *
   * The referenced agent may live in another organization when it is
   * marketplace-public: the share then offers that agent from this share's
   * org, billed to this share's org, with tool credentials bound from this
   * share's org. Cross-org shares must have a public audience.
   *
   * &#64;internal
   * Cross-org contract (decision 013): making an agent visibility_public
   * IS the origin org's consent to external shares — enforced app-level in
   * both editions' defaults resolvers (spec-level CEL cannot see
   * metadata.org), together with the public-audience-only rule and the
   * dependency-publicness sweep (every skill/MCP the agent declares must be
   * visibility_public, refused loudly naming blockers). The sharing org is
   * always the billing org (decision 001 generalized) and guest tool
   * credentials resolve in the share's org (secrets never cross orgs —
   * org A's environments are structurally unreachable from org B's
   * executions via OrgSharedEnvironmentPolicy). The agent's public
   * visibility is re-verified on every guest turn, at the shared profile,
   * and at guest mint, so revoking visibility kills external channels on
   * the visitor's next message. status.agent_id pins the resolved agent
   * against slug-reuse rebind.
   * </pre>
   *
   * <code>.ai.stigmer.commons.apiresource.ApiResourceReference agent_ref = 1 [json_name = "agentRef", (.buf.validate.field) = { ... }</code>
   */
  @java.lang.Override
  public ai.stigmer.commons.apiresource.ApiResourceReferenceOrBuilder getAgentRefOrBuilder() {
    return agentRef_ == null ? ai.stigmer.commons.apiresource.ApiResourceReference.getDefaultInstance() : agentRef_;
  }

  public static final int ENABLED_FIELD_NUMBER = 2;
  private boolean enabled_ = false;
  /**
   * <pre>
   * Whether hosted-chat access for the configured audience is enabled.
   *
   * Disabling is a config-preserving pause: the share keeps its origins,
   * messages, credentials, and link token, but the hosted link answers
   * NOT_FOUND until re-enabled. Delete the share instead for a full
   * teardown.
   * </pre>
   *
   * <code>bool enabled = 2 [json_name = "enabled"];</code>
   * @return The enabled.
   */
  @java.lang.Override
  public boolean getEnabled() {
    return enabled_;
  }

  public static final int AUDIENCE_FIELD_NUMBER = 3;
  private int audience_ = 0;
  /**
   * <pre>
   * Who can chat over this share. Unspecified means public (anyone with
   * the link), so a manifest that omits audience creates a public share.
   *
   * To keep a share org-only, audience must be present in every apply:
   * update/apply replace the spec wholesale, so a manifest that sets
   * enabled without audience resets the share to public.
   *
   * &#64;internal
   * Org audience is enforced cloud-side only (org membership is a
   * multi-tenant IAM concept): the guest mint and public profile handlers
   * treat audience=org as NOT_FOUND, and the create-time blueprint gate
   * admits authenticated org members via an app-level FGA member check
   * (no visibility tuples written). The OSS single-user server stores and
   * echoes the field.
   * </pre>
   *
   * <code>.ai.stigmer.agentic.agentshare.v1.AgentShareAudience audience = 3 [json_name = "audience"];</code>
   * @return The enum numeric value on the wire for audience.
   */
  @java.lang.Override public int getAudienceValue() {
    return audience_;
  }
  /**
   * <pre>
   * Who can chat over this share. Unspecified means public (anyone with
   * the link), so a manifest that omits audience creates a public share.
   *
   * To keep a share org-only, audience must be present in every apply:
   * update/apply replace the spec wholesale, so a manifest that sets
   * enabled without audience resets the share to public.
   *
   * &#64;internal
   * Org audience is enforced cloud-side only (org membership is a
   * multi-tenant IAM concept): the guest mint and public profile handlers
   * treat audience=org as NOT_FOUND, and the create-time blueprint gate
   * admits authenticated org members via an app-level FGA member check
   * (no visibility tuples written). The OSS single-user server stores and
   * echoes the field.
   * </pre>
   *
   * <code>.ai.stigmer.agentic.agentshare.v1.AgentShareAudience audience = 3 [json_name = "audience"];</code>
   * @return The audience.
   */
  @java.lang.Override public ai.stigmer.agentic.agentshare.v1.AgentShareAudience getAudience() {
    ai.stigmer.agentic.agentshare.v1.AgentShareAudience result = ai.stigmer.agentic.agentshare.v1.AgentShareAudience.forNumber(audience_);
    return result == null ? ai.stigmer.agentic.agentshare.v1.AgentShareAudience.UNRECOGNIZED : result;
  }

  public static final int ALLOWED_ORIGINS_FIELD_NUMBER = 4;
  @SuppressWarnings("serial")
  private com.google.protobuf.LazyStringArrayList allowedOrigins_ =
      com.google.protobuf.LazyStringArrayList.emptyList();
  /**
   * <pre>
   * Origins permitted to embed this share's chat widget.
   *
   * Each entry is an exact web origin (scheme://host[:port]) with no path,
   * for example "https://docs.example.com". An empty list allows embedding
   * from any site; listing origins restricts embedding to those sites. The
   * first-party hosted chat page is always exempt.
   *
   * &#64;internal
   * Enforced since T04 against the embed_origin the widget reports at
   * mintGuestToken time (stamped into the guest JWT as a claim) and
   * re-validated against this live list by the guest create-time gate
   * (SharedSessionBlueprintAccess) on every session/execution create — the
   * same gate that re-checks enabled, so revocation latency is identical
   * (immediate). Unframed hosted-page visitors report no origin and are
   * exempt by construction. Exact origins only — loosening to wildcards
   * later is a non-breaking change, tightening would not be.
   * </pre>
   *
   * <code>repeated string allowed_origins = 4 [json_name = "allowedOrigins", (.buf.validate.field) = { ... }</code>
   * @return A list containing the allowedOrigins.
   */
  public com.google.protobuf.ProtocolStringList
      getAllowedOriginsList() {
    return allowedOrigins_;
  }
  /**
   * <pre>
   * Origins permitted to embed this share's chat widget.
   *
   * Each entry is an exact web origin (scheme://host[:port]) with no path,
   * for example "https://docs.example.com". An empty list allows embedding
   * from any site; listing origins restricts embedding to those sites. The
   * first-party hosted chat page is always exempt.
   *
   * &#64;internal
   * Enforced since T04 against the embed_origin the widget reports at
   * mintGuestToken time (stamped into the guest JWT as a claim) and
   * re-validated against this live list by the guest create-time gate
   * (SharedSessionBlueprintAccess) on every session/execution create — the
   * same gate that re-checks enabled, so revocation latency is identical
   * (immediate). Unframed hosted-page visitors report no origin and are
   * exempt by construction. Exact origins only — loosening to wildcards
   * later is a non-breaking change, tightening would not be.
   * </pre>
   *
   * <code>repeated string allowed_origins = 4 [json_name = "allowedOrigins", (.buf.validate.field) = { ... }</code>
   * @return The count of allowedOrigins.
   */
  public int getAllowedOriginsCount() {
    return allowedOrigins_.size();
  }
  /**
   * <pre>
   * Origins permitted to embed this share's chat widget.
   *
   * Each entry is an exact web origin (scheme://host[:port]) with no path,
   * for example "https://docs.example.com". An empty list allows embedding
   * from any site; listing origins restricts embedding to those sites. The
   * first-party hosted chat page is always exempt.
   *
   * &#64;internal
   * Enforced since T04 against the embed_origin the widget reports at
   * mintGuestToken time (stamped into the guest JWT as a claim) and
   * re-validated against this live list by the guest create-time gate
   * (SharedSessionBlueprintAccess) on every session/execution create — the
   * same gate that re-checks enabled, so revocation latency is identical
   * (immediate). Unframed hosted-page visitors report no origin and are
   * exempt by construction. Exact origins only — loosening to wildcards
   * later is a non-breaking change, tightening would not be.
   * </pre>
   *
   * <code>repeated string allowed_origins = 4 [json_name = "allowedOrigins", (.buf.validate.field) = { ... }</code>
   * @param index The index of the element to return.
   * @return The allowedOrigins at the given index.
   */
  public java.lang.String getAllowedOrigins(int index) {
    return allowedOrigins_.get(index);
  }
  /**
   * <pre>
   * Origins permitted to embed this share's chat widget.
   *
   * Each entry is an exact web origin (scheme://host[:port]) with no path,
   * for example "https://docs.example.com". An empty list allows embedding
   * from any site; listing origins restricts embedding to those sites. The
   * first-party hosted chat page is always exempt.
   *
   * &#64;internal
   * Enforced since T04 against the embed_origin the widget reports at
   * mintGuestToken time (stamped into the guest JWT as a claim) and
   * re-validated against this live list by the guest create-time gate
   * (SharedSessionBlueprintAccess) on every session/execution create — the
   * same gate that re-checks enabled, so revocation latency is identical
   * (immediate). Unframed hosted-page visitors report no origin and are
   * exempt by construction. Exact origins only — loosening to wildcards
   * later is a non-breaking change, tightening would not be.
   * </pre>
   *
   * <code>repeated string allowed_origins = 4 [json_name = "allowedOrigins", (.buf.validate.field) = { ... }</code>
   * @param index The index of the value to return.
   * @return The bytes of the allowedOrigins at the given index.
   */
  public com.google.protobuf.ByteString
      getAllowedOriginsBytes(int index) {
    return allowedOrigins_.getByteString(index);
  }

  public static final int MESSAGES_FIELD_NUMBER = 5;
  private ai.stigmer.agentic.agentshare.v1.AgentShareMessages messages_;
  /**
   * <pre>
   * Owner-customizable copy shown to visitors when a launch-gate limit
   * refuses their message. Unset fields fall back to platform defaults.
   *
   * &#64;internal
   * Resolved server-side at the refusal point and carried in the gRPC
   * status description — deliberately NOT surfaced on SharedAgentProfile
   * and NOT mapped client-side, so the copy reaches every client (web,
   * embed, CLI) through the existing error-message path.
   * </pre>
   *
   * <code>.ai.stigmer.agentic.agentshare.v1.AgentShareMessages messages = 5 [json_name = "messages"];</code>
   * @return Whether the messages field is set.
   */
  @java.lang.Override
  public boolean hasMessages() {
    return ((bitField0_ & 0x00000002) != 0);
  }
  /**
   * <pre>
   * Owner-customizable copy shown to visitors when a launch-gate limit
   * refuses their message. Unset fields fall back to platform defaults.
   *
   * &#64;internal
   * Resolved server-side at the refusal point and carried in the gRPC
   * status description — deliberately NOT surfaced on SharedAgentProfile
   * and NOT mapped client-side, so the copy reaches every client (web,
   * embed, CLI) through the existing error-message path.
   * </pre>
   *
   * <code>.ai.stigmer.agentic.agentshare.v1.AgentShareMessages messages = 5 [json_name = "messages"];</code>
   * @return The messages.
   */
  @java.lang.Override
  public ai.stigmer.agentic.agentshare.v1.AgentShareMessages getMessages() {
    return messages_ == null ? ai.stigmer.agentic.agentshare.v1.AgentShareMessages.getDefaultInstance() : messages_;
  }
  /**
   * <pre>
   * Owner-customizable copy shown to visitors when a launch-gate limit
   * refuses their message. Unset fields fall back to platform defaults.
   *
   * &#64;internal
   * Resolved server-side at the refusal point and carried in the gRPC
   * status description — deliberately NOT surfaced on SharedAgentProfile
   * and NOT mapped client-side, so the copy reaches every client (web,
   * embed, CLI) through the existing error-message path.
   * </pre>
   *
   * <code>.ai.stigmer.agentic.agentshare.v1.AgentShareMessages messages = 5 [json_name = "messages"];</code>
   */
  @java.lang.Override
  public ai.stigmer.agentic.agentshare.v1.AgentShareMessagesOrBuilder getMessagesOrBuilder() {
    return messages_ == null ? ai.stigmer.agentic.agentshare.v1.AgentShareMessages.getDefaultInstance() : messages_;
  }

  public static final int ENVIRONMENT_REFS_FIELD_NUMBER = 6;
  @SuppressWarnings("serial")
  private java.util.List<ai.stigmer.commons.apiresource.ApiResourceReference> environmentRefs_;
  /**
   * <pre>
   * References to Environment resources whose values are provided to guest
   * conversations on this share.
   *
   * This is how a tool-using agent becomes chattable over a share link:
   * bind an org-shared environment holding the needed credentials (for
   * example a read-only API token), and guest executions receive its
   * values at runtime. The agent and its default instance stay untouched.
   * Valid on public-audience shares only.
   *
   * &#64;internal
   * Resolved in the share's org through the org-shared environment
   * resolution seam (EnvironmentRuntimeResolutionService /
   * OrgSharedEnvironmentPolicy — decision 006): each referenced environment
   * must be visibility_org in the share's org, or the merge skips it with
   * a diagnostic. Merged at guest execution-context build time only —
   * never bound to the agent's system-managed default instance, which is
   * recreated empty on lifecycle events and shared by every org member's
   * casual chat (decision 011). Org-audience shares reject this field via
   * the message-level CEL rule: member sessions carry no share linkage in
   * Phase A, so bound credentials would silently never apply.
   * </pre>
   *
   * <code>repeated .ai.stigmer.commons.apiresource.ApiResourceReference environment_refs = 6 [json_name = "environmentRefs", (.buf.validate.field) = { ... }</code>
   */
  @java.lang.Override
  public java.util.List<ai.stigmer.commons.apiresource.ApiResourceReference> getEnvironmentRefsList() {
    return environmentRefs_;
  }
  /**
   * <pre>
   * References to Environment resources whose values are provided to guest
   * conversations on this share.
   *
   * This is how a tool-using agent becomes chattable over a share link:
   * bind an org-shared environment holding the needed credentials (for
   * example a read-only API token), and guest executions receive its
   * values at runtime. The agent and its default instance stay untouched.
   * Valid on public-audience shares only.
   *
   * &#64;internal
   * Resolved in the share's org through the org-shared environment
   * resolution seam (EnvironmentRuntimeResolutionService /
   * OrgSharedEnvironmentPolicy — decision 006): each referenced environment
   * must be visibility_org in the share's org, or the merge skips it with
   * a diagnostic. Merged at guest execution-context build time only —
   * never bound to the agent's system-managed default instance, which is
   * recreated empty on lifecycle events and shared by every org member's
   * casual chat (decision 011). Org-audience shares reject this field via
   * the message-level CEL rule: member sessions carry no share linkage in
   * Phase A, so bound credentials would silently never apply.
   * </pre>
   *
   * <code>repeated .ai.stigmer.commons.apiresource.ApiResourceReference environment_refs = 6 [json_name = "environmentRefs", (.buf.validate.field) = { ... }</code>
   */
  @java.lang.Override
  public java.util.List<? extends ai.stigmer.commons.apiresource.ApiResourceReferenceOrBuilder> 
      getEnvironmentRefsOrBuilderList() {
    return environmentRefs_;
  }
  /**
   * <pre>
   * References to Environment resources whose values are provided to guest
   * conversations on this share.
   *
   * This is how a tool-using agent becomes chattable over a share link:
   * bind an org-shared environment holding the needed credentials (for
   * example a read-only API token), and guest executions receive its
   * values at runtime. The agent and its default instance stay untouched.
   * Valid on public-audience shares only.
   *
   * &#64;internal
   * Resolved in the share's org through the org-shared environment
   * resolution seam (EnvironmentRuntimeResolutionService /
   * OrgSharedEnvironmentPolicy — decision 006): each referenced environment
   * must be visibility_org in the share's org, or the merge skips it with
   * a diagnostic. Merged at guest execution-context build time only —
   * never bound to the agent's system-managed default instance, which is
   * recreated empty on lifecycle events and shared by every org member's
   * casual chat (decision 011). Org-audience shares reject this field via
   * the message-level CEL rule: member sessions carry no share linkage in
   * Phase A, so bound credentials would silently never apply.
   * </pre>
   *
   * <code>repeated .ai.stigmer.commons.apiresource.ApiResourceReference environment_refs = 6 [json_name = "environmentRefs", (.buf.validate.field) = { ... }</code>
   */
  @java.lang.Override
  public int getEnvironmentRefsCount() {
    return environmentRefs_.size();
  }
  /**
   * <pre>
   * References to Environment resources whose values are provided to guest
   * conversations on this share.
   *
   * This is how a tool-using agent becomes chattable over a share link:
   * bind an org-shared environment holding the needed credentials (for
   * example a read-only API token), and guest executions receive its
   * values at runtime. The agent and its default instance stay untouched.
   * Valid on public-audience shares only.
   *
   * &#64;internal
   * Resolved in the share's org through the org-shared environment
   * resolution seam (EnvironmentRuntimeResolutionService /
   * OrgSharedEnvironmentPolicy — decision 006): each referenced environment
   * must be visibility_org in the share's org, or the merge skips it with
   * a diagnostic. Merged at guest execution-context build time only —
   * never bound to the agent's system-managed default instance, which is
   * recreated empty on lifecycle events and shared by every org member's
   * casual chat (decision 011). Org-audience shares reject this field via
   * the message-level CEL rule: member sessions carry no share linkage in
   * Phase A, so bound credentials would silently never apply.
   * </pre>
   *
   * <code>repeated .ai.stigmer.commons.apiresource.ApiResourceReference environment_refs = 6 [json_name = "environmentRefs", (.buf.validate.field) = { ... }</code>
   */
  @java.lang.Override
  public ai.stigmer.commons.apiresource.ApiResourceReference getEnvironmentRefs(int index) {
    return environmentRefs_.get(index);
  }
  /**
   * <pre>
   * References to Environment resources whose values are provided to guest
   * conversations on this share.
   *
   * This is how a tool-using agent becomes chattable over a share link:
   * bind an org-shared environment holding the needed credentials (for
   * example a read-only API token), and guest executions receive its
   * values at runtime. The agent and its default instance stay untouched.
   * Valid on public-audience shares only.
   *
   * &#64;internal
   * Resolved in the share's org through the org-shared environment
   * resolution seam (EnvironmentRuntimeResolutionService /
   * OrgSharedEnvironmentPolicy — decision 006): each referenced environment
   * must be visibility_org in the share's org, or the merge skips it with
   * a diagnostic. Merged at guest execution-context build time only —
   * never bound to the agent's system-managed default instance, which is
   * recreated empty on lifecycle events and shared by every org member's
   * casual chat (decision 011). Org-audience shares reject this field via
   * the message-level CEL rule: member sessions carry no share linkage in
   * Phase A, so bound credentials would silently never apply.
   * </pre>
   *
   * <code>repeated .ai.stigmer.commons.apiresource.ApiResourceReference environment_refs = 6 [json_name = "environmentRefs", (.buf.validate.field) = { ... }</code>
   */
  @java.lang.Override
  public ai.stigmer.commons.apiresource.ApiResourceReferenceOrBuilder getEnvironmentRefsOrBuilder(
      int index) {
    return environmentRefs_.get(index);
  }

  public static final int RUN_CONFIG_FIELD_NUMBER = 7;
  private ai.stigmer.agentic.agentexecution.v1.RunConfig runConfig_;
  /**
   * <pre>
   * Per-turn model choice and run bounds for guest conversations on this
   * share, overriding the platform's guest execution profile.
   *
   * Unset fields inherit the platform default. model_name replaces the
   * platform model outright, while max_cost_usd and max_tool_rounds can
   * only lower the platform caps — a share owner can reduce what one
   * guest turn may spend, never raise it past the platform profile.
   * Valid on public-audience shares only.
   *
   * &#64;internal
   * DD-018 D-2's second embedder (stigmer/stigmer#360). This is the
   * OWNER's stored config, not the guest's: the guest scope step still
   * discards caller-supplied execution_config unconditionally, then
   * merges this field — loaded server-side from the share — over the
   * platform guest profile (GuestAgentExecutionCreateScopeStep, the
   * promise recorded on GuestExecutionProfileProperties). The trust
   * posture is unchanged. Org-audience shares reject the field via the
   * message-level CEL rule for the same reason as environment_refs:
   * member sessions carry no share linkage, so it would be stored but
   * silently never applied. Enforcement is cloud-only — guest runtime
   * is a cloud concept; OSS stores and echoes the field.
   * </pre>
   *
   * <code>.ai.stigmer.agentic.agentexecution.v1.RunConfig run_config = 7 [json_name = "runConfig"];</code>
   * @return Whether the runConfig field is set.
   */
  @java.lang.Override
  public boolean hasRunConfig() {
    return ((bitField0_ & 0x00000004) != 0);
  }
  /**
   * <pre>
   * Per-turn model choice and run bounds for guest conversations on this
   * share, overriding the platform's guest execution profile.
   *
   * Unset fields inherit the platform default. model_name replaces the
   * platform model outright, while max_cost_usd and max_tool_rounds can
   * only lower the platform caps — a share owner can reduce what one
   * guest turn may spend, never raise it past the platform profile.
   * Valid on public-audience shares only.
   *
   * &#64;internal
   * DD-018 D-2's second embedder (stigmer/stigmer#360). This is the
   * OWNER's stored config, not the guest's: the guest scope step still
   * discards caller-supplied execution_config unconditionally, then
   * merges this field — loaded server-side from the share — over the
   * platform guest profile (GuestAgentExecutionCreateScopeStep, the
   * promise recorded on GuestExecutionProfileProperties). The trust
   * posture is unchanged. Org-audience shares reject the field via the
   * message-level CEL rule for the same reason as environment_refs:
   * member sessions carry no share linkage, so it would be stored but
   * silently never applied. Enforcement is cloud-only — guest runtime
   * is a cloud concept; OSS stores and echoes the field.
   * </pre>
   *
   * <code>.ai.stigmer.agentic.agentexecution.v1.RunConfig run_config = 7 [json_name = "runConfig"];</code>
   * @return The runConfig.
   */
  @java.lang.Override
  public ai.stigmer.agentic.agentexecution.v1.RunConfig getRunConfig() {
    return runConfig_ == null ? ai.stigmer.agentic.agentexecution.v1.RunConfig.getDefaultInstance() : runConfig_;
  }
  /**
   * <pre>
   * Per-turn model choice and run bounds for guest conversations on this
   * share, overriding the platform's guest execution profile.
   *
   * Unset fields inherit the platform default. model_name replaces the
   * platform model outright, while max_cost_usd and max_tool_rounds can
   * only lower the platform caps — a share owner can reduce what one
   * guest turn may spend, never raise it past the platform profile.
   * Valid on public-audience shares only.
   *
   * &#64;internal
   * DD-018 D-2's second embedder (stigmer/stigmer#360). This is the
   * OWNER's stored config, not the guest's: the guest scope step still
   * discards caller-supplied execution_config unconditionally, then
   * merges this field — loaded server-side from the share — over the
   * platform guest profile (GuestAgentExecutionCreateScopeStep, the
   * promise recorded on GuestExecutionProfileProperties). The trust
   * posture is unchanged. Org-audience shares reject the field via the
   * message-level CEL rule for the same reason as environment_refs:
   * member sessions carry no share linkage, so it would be stored but
   * silently never applied. Enforcement is cloud-only — guest runtime
   * is a cloud concept; OSS stores and echoes the field.
   * </pre>
   *
   * <code>.ai.stigmer.agentic.agentexecution.v1.RunConfig run_config = 7 [json_name = "runConfig"];</code>
   */
  @java.lang.Override
  public ai.stigmer.agentic.agentexecution.v1.RunConfigOrBuilder getRunConfigOrBuilder() {
    return runConfig_ == null ? ai.stigmer.agentic.agentexecution.v1.RunConfig.getDefaultInstance() : runConfig_;
  }

  private byte memoizedIsInitialized = -1;
  @java.lang.Override
  public final boolean isInitialized() {
    byte isInitialized = memoizedIsInitialized;
    if (isInitialized == 1) return true;
    if (isInitialized == 0) return false;

    memoizedIsInitialized = 1;
    return true;
  }

  @java.lang.Override
  public void writeTo(com.google.protobuf.CodedOutputStream output)
                      throws java.io.IOException {
    if (((bitField0_ & 0x00000001) != 0)) {
      output.writeMessage(1, getAgentRef());
    }
    if (enabled_ != false) {
      output.writeBool(2, enabled_);
    }
    if (audience_ != ai.stigmer.agentic.agentshare.v1.AgentShareAudience.agent_share_audience_unspecified.getNumber()) {
      output.writeEnum(3, audience_);
    }
    for (int i = 0; i < allowedOrigins_.size(); i++) {
      com.google.protobuf.GeneratedMessage.writeString(output, 4, allowedOrigins_.getRaw(i));
    }
    if (((bitField0_ & 0x00000002) != 0)) {
      output.writeMessage(5, getMessages());
    }
    for (int i = 0; i < environmentRefs_.size(); i++) {
      output.writeMessage(6, environmentRefs_.get(i));
    }
    if (((bitField0_ & 0x00000004) != 0)) {
      output.writeMessage(7, getRunConfig());
    }
    getUnknownFields().writeTo(output);
  }

  @java.lang.Override
  public int getSerializedSize() {
    int size = memoizedSize;
    if (size != -1) return size;

    size = 0;
    if (((bitField0_ & 0x00000001) != 0)) {
      size += com.google.protobuf.CodedOutputStream
        .computeMessageSize(1, getAgentRef());
    }
    if (enabled_ != false) {
      size += com.google.protobuf.CodedOutputStream
        .computeBoolSize(2, enabled_);
    }
    if (audience_ != ai.stigmer.agentic.agentshare.v1.AgentShareAudience.agent_share_audience_unspecified.getNumber()) {
      size += com.google.protobuf.CodedOutputStream
        .computeEnumSize(3, audience_);
    }
    {
      int dataSize = 0;
      for (int i = 0; i < allowedOrigins_.size(); i++) {
        dataSize += computeStringSizeNoTag(allowedOrigins_.getRaw(i));
      }
      size += dataSize;
      size += 1 * getAllowedOriginsList().size();
    }
    if (((bitField0_ & 0x00000002) != 0)) {
      size += com.google.protobuf.CodedOutputStream
        .computeMessageSize(5, getMessages());
    }

        {
          final int count = environmentRefs_.size();
          for (int i = 0; i < count; i++) {
            size += com.google.protobuf.CodedOutputStream
              .computeMessageSizeNoTag(environmentRefs_.get(i));
          }
          size += 1 * count;
        }
    if (((bitField0_ & 0x00000004) != 0)) {
      size += com.google.protobuf.CodedOutputStream
        .computeMessageSize(7, getRunConfig());
    }
    size += getUnknownFields().getSerializedSize();
    memoizedSize = size;
    return size;
  }

  @java.lang.Override
  public boolean equals(final java.lang.Object obj) {
    if (obj == this) {
     return true;
    }
    if (!(obj instanceof ai.stigmer.agentic.agentshare.v1.AgentShareSpec)) {
      return super.equals(obj);
    }
    ai.stigmer.agentic.agentshare.v1.AgentShareSpec other = (ai.stigmer.agentic.agentshare.v1.AgentShareSpec) obj;

    if (hasAgentRef() != other.hasAgentRef()) return false;
    if (hasAgentRef()) {
      if (!getAgentRef()
          .equals(other.getAgentRef())) return false;
    }
    if (getEnabled()
        != other.getEnabled()) return false;
    if (audience_ != other.audience_) return false;
    if (!getAllowedOriginsList()
        .equals(other.getAllowedOriginsList())) return false;
    if (hasMessages() != other.hasMessages()) return false;
    if (hasMessages()) {
      if (!getMessages()
          .equals(other.getMessages())) return false;
    }
    if (!getEnvironmentRefsList()
        .equals(other.getEnvironmentRefsList())) return false;
    if (hasRunConfig() != other.hasRunConfig()) return false;
    if (hasRunConfig()) {
      if (!getRunConfig()
          .equals(other.getRunConfig())) return false;
    }
    if (!getUnknownFields().equals(other.getUnknownFields())) return false;
    return true;
  }

  @java.lang.Override
  public int hashCode() {
    if (memoizedHashCode != 0) {
      return memoizedHashCode;
    }
    int hash = 41;
    hash = (19 * hash) + getDescriptor().hashCode();
    if (hasAgentRef()) {
      hash = (37 * hash) + AGENT_REF_FIELD_NUMBER;
      hash = (53 * hash) + getAgentRef().hashCode();
    }
    hash = (37 * hash) + ENABLED_FIELD_NUMBER;
    hash = (53 * hash) + com.google.protobuf.Internal.hashBoolean(
        getEnabled());
    hash = (37 * hash) + AUDIENCE_FIELD_NUMBER;
    hash = (53 * hash) + audience_;
    if (getAllowedOriginsCount() > 0) {
      hash = (37 * hash) + ALLOWED_ORIGINS_FIELD_NUMBER;
      hash = (53 * hash) + getAllowedOriginsList().hashCode();
    }
    if (hasMessages()) {
      hash = (37 * hash) + MESSAGES_FIELD_NUMBER;
      hash = (53 * hash) + getMessages().hashCode();
    }
    if (getEnvironmentRefsCount() > 0) {
      hash = (37 * hash) + ENVIRONMENT_REFS_FIELD_NUMBER;
      hash = (53 * hash) + getEnvironmentRefsList().hashCode();
    }
    if (hasRunConfig()) {
      hash = (37 * hash) + RUN_CONFIG_FIELD_NUMBER;
      hash = (53 * hash) + getRunConfig().hashCode();
    }
    hash = (29 * hash) + getUnknownFields().hashCode();
    memoizedHashCode = hash;
    return hash;
  }

  public static ai.stigmer.agentic.agentshare.v1.AgentShareSpec parseFrom(
      java.nio.ByteBuffer data)
      throws com.google.protobuf.InvalidProtocolBufferException {
    return PARSER.parseFrom(data);
  }
  public static ai.stigmer.agentic.agentshare.v1.AgentShareSpec parseFrom(
      java.nio.ByteBuffer data,
      com.google.protobuf.ExtensionRegistryLite extensionRegistry)
      throws com.google.protobuf.InvalidProtocolBufferException {
    return PARSER.parseFrom(data, extensionRegistry);
  }
  public static ai.stigmer.agentic.agentshare.v1.AgentShareSpec parseFrom(
      com.google.protobuf.ByteString data)
      throws com.google.protobuf.InvalidProtocolBufferException {
    return PARSER.parseFrom(data);
  }
  public static ai.stigmer.agentic.agentshare.v1.AgentShareSpec parseFrom(
      com.google.protobuf.ByteString data,
      com.google.protobuf.ExtensionRegistryLite extensionRegistry)
      throws com.google.protobuf.InvalidProtocolBufferException {
    return PARSER.parseFrom(data, extensionRegistry);
  }
  public static ai.stigmer.agentic.agentshare.v1.AgentShareSpec parseFrom(byte[] data)
      throws com.google.protobuf.InvalidProtocolBufferException {
    return PARSER.parseFrom(data);
  }
  public static ai.stigmer.agentic.agentshare.v1.AgentShareSpec parseFrom(
      byte[] data,
      com.google.protobuf.ExtensionRegistryLite extensionRegistry)
      throws com.google.protobuf.InvalidProtocolBufferException {
    return PARSER.parseFrom(data, extensionRegistry);
  }
  public static ai.stigmer.agentic.agentshare.v1.AgentShareSpec parseFrom(java.io.InputStream input)
      throws java.io.IOException {
    return com.google.protobuf.GeneratedMessage
        .parseWithIOException(PARSER, input);
  }
  public static ai.stigmer.agentic.agentshare.v1.AgentShareSpec parseFrom(
      java.io.InputStream input,
      com.google.protobuf.ExtensionRegistryLite extensionRegistry)
      throws java.io.IOException {
    return com.google.protobuf.GeneratedMessage
        .parseWithIOException(PARSER, input, extensionRegistry);
  }

  public static ai.stigmer.agentic.agentshare.v1.AgentShareSpec parseDelimitedFrom(java.io.InputStream input)
      throws java.io.IOException {
    return com.google.protobuf.GeneratedMessage
        .parseDelimitedWithIOException(PARSER, input);
  }

  public static ai.stigmer.agentic.agentshare.v1.AgentShareSpec parseDelimitedFrom(
      java.io.InputStream input,
      com.google.protobuf.ExtensionRegistryLite extensionRegistry)
      throws java.io.IOException {
    return com.google.protobuf.GeneratedMessage
        .parseDelimitedWithIOException(PARSER, input, extensionRegistry);
  }
  public static ai.stigmer.agentic.agentshare.v1.AgentShareSpec parseFrom(
      com.google.protobuf.CodedInputStream input)
      throws java.io.IOException {
    return com.google.protobuf.GeneratedMessage
        .parseWithIOException(PARSER, input);
  }
  public static ai.stigmer.agentic.agentshare.v1.AgentShareSpec parseFrom(
      com.google.protobuf.CodedInputStream input,
      com.google.protobuf.ExtensionRegistryLite extensionRegistry)
      throws java.io.IOException {
    return com.google.protobuf.GeneratedMessage
        .parseWithIOException(PARSER, input, extensionRegistry);
  }

  @java.lang.Override
  public Builder newBuilderForType() { return newBuilder(); }
  public static Builder newBuilder() {
    return DEFAULT_INSTANCE.toBuilder();
  }
  public static Builder newBuilder(ai.stigmer.agentic.agentshare.v1.AgentShareSpec prototype) {
    return DEFAULT_INSTANCE.toBuilder().mergeFrom(prototype);
  }
  @java.lang.Override
  public Builder toBuilder() {
    return this == DEFAULT_INSTANCE
        ? new Builder() : new Builder().mergeFrom(this);
  }

  @java.lang.Override
  protected Builder newBuilderForType(
      com.google.protobuf.GeneratedMessage.BuilderParent parent) {
    Builder builder = new Builder(parent);
    return builder;
  }
  /**
   * <pre>
   * AgentShareSpec defines the configurable properties of an agent share.
   *
   * A share is a distribution channel for an agent's hosted chat experience.
   * It carries its own audience, embed origins, visitor-facing messages, and
   * tool credentials — all independent of the agent blueprint it references.
   *
   * &#64;internal
   * Sharing is channel configuration, not agent behavior (decision 011):
   * applying an agent manifest never touches a share, and the share has its
   * own writer, lifecycle, and change cadence. Guest admission is enforced
   * app-level in the shared-profile/mint/create-time gates — a share writes
   * NO FGA visibility tuples, because a public wildcard viewer tuple would
   * expose the referenced agent's full blueprint (instructions included) to
   * any authenticated account, conflating blueprint visibility with chat
   * access. The overview.md file provides the SDK-facing description and
   * example YAML.
   * </pre>
   *
   * Protobuf type {@code ai.stigmer.agentic.agentshare.v1.AgentShareSpec}
   */
  public static final class Builder extends
      com.google.protobuf.GeneratedMessage.Builder<Builder> implements
      // @@protoc_insertion_point(builder_implements:ai.stigmer.agentic.agentshare.v1.AgentShareSpec)
      ai.stigmer.agentic.agentshare.v1.AgentShareSpecOrBuilder {
    public static final com.google.protobuf.Descriptors.Descriptor
        getDescriptor() {
      return ai.stigmer.agentic.agentshare.v1.SpecProto.internal_static_ai_stigmer_agentic_agentshare_v1_AgentShareSpec_descriptor;
    }

    @java.lang.Override
    protected com.google.protobuf.GeneratedMessage.FieldAccessorTable
        internalGetFieldAccessorTable() {
      return ai.stigmer.agentic.agentshare.v1.SpecProto.internal_static_ai_stigmer_agentic_agentshare_v1_AgentShareSpec_fieldAccessorTable
          .ensureFieldAccessorsInitialized(
              ai.stigmer.agentic.agentshare.v1.AgentShareSpec.class, ai.stigmer.agentic.agentshare.v1.AgentShareSpec.Builder.class);
    }

    // Construct using ai.stigmer.agentic.agentshare.v1.AgentShareSpec.newBuilder()
    private Builder() {
      maybeForceBuilderInitialization();
    }

    private Builder(
        com.google.protobuf.GeneratedMessage.BuilderParent parent) {
      super(parent);
      maybeForceBuilderInitialization();
    }
    private void maybeForceBuilderInitialization() {
      if (com.google.protobuf.GeneratedMessage
              .alwaysUseFieldBuilders) {
        internalGetAgentRefFieldBuilder();
        internalGetMessagesFieldBuilder();
        internalGetEnvironmentRefsFieldBuilder();
        internalGetRunConfigFieldBuilder();
      }
    }
    @java.lang.Override
    public Builder clear() {
      super.clear();
      bitField0_ = 0;
      agentRef_ = null;
      if (agentRefBuilder_ != null) {
        agentRefBuilder_.dispose();
        agentRefBuilder_ = null;
      }
      enabled_ = false;
      audience_ = 0;
      allowedOrigins_ =
          com.google.protobuf.LazyStringArrayList.emptyList();
      messages_ = null;
      if (messagesBuilder_ != null) {
        messagesBuilder_.dispose();
        messagesBuilder_ = null;
      }
      if (environmentRefsBuilder_ == null) {
        environmentRefs_ = java.util.Collections.emptyList();
      } else {
        environmentRefs_ = null;
        environmentRefsBuilder_.clear();
      }
      bitField0_ = (bitField0_ & ~0x00000020);
      runConfig_ = null;
      if (runConfigBuilder_ != null) {
        runConfigBuilder_.dispose();
        runConfigBuilder_ = null;
      }
      return this;
    }

    @java.lang.Override
    public com.google.protobuf.Descriptors.Descriptor
        getDescriptorForType() {
      return ai.stigmer.agentic.agentshare.v1.SpecProto.internal_static_ai_stigmer_agentic_agentshare_v1_AgentShareSpec_descriptor;
    }

    @java.lang.Override
    public ai.stigmer.agentic.agentshare.v1.AgentShareSpec getDefaultInstanceForType() {
      return ai.stigmer.agentic.agentshare.v1.AgentShareSpec.getDefaultInstance();
    }

    @java.lang.Override
    public ai.stigmer.agentic.agentshare.v1.AgentShareSpec build() {
      ai.stigmer.agentic.agentshare.v1.AgentShareSpec result = buildPartial();
      if (!result.isInitialized()) {
        throw newUninitializedMessageException(result);
      }
      return result;
    }

    @java.lang.Override
    public ai.stigmer.agentic.agentshare.v1.AgentShareSpec buildPartial() {
      ai.stigmer.agentic.agentshare.v1.AgentShareSpec result = new ai.stigmer.agentic.agentshare.v1.AgentShareSpec(this);
      buildPartialRepeatedFields(result);
      if (bitField0_ != 0) { buildPartial0(result); }
      onBuilt();
      return result;
    }

    private void buildPartialRepeatedFields(ai.stigmer.agentic.agentshare.v1.AgentShareSpec result) {
      if (environmentRefsBuilder_ == null) {
        if (((bitField0_ & 0x00000020) != 0)) {
          environmentRefs_ = java.util.Collections.unmodifiableList(environmentRefs_);
          bitField0_ = (bitField0_ & ~0x00000020);
        }
        result.environmentRefs_ = environmentRefs_;
      } else {
        result.environmentRefs_ = environmentRefsBuilder_.build();
      }
    }

    private void buildPartial0(ai.stigmer.agentic.agentshare.v1.AgentShareSpec result) {
      int from_bitField0_ = bitField0_;
      int to_bitField0_ = 0;
      if (((from_bitField0_ & 0x00000001) != 0)) {
        result.agentRef_ = agentRefBuilder_ == null
            ? agentRef_
            : agentRefBuilder_.build();
        to_bitField0_ |= 0x00000001;
      }
      if (((from_bitField0_ & 0x00000002) != 0)) {
        result.enabled_ = enabled_;
      }
      if (((from_bitField0_ & 0x00000004) != 0)) {
        result.audience_ = audience_;
      }
      if (((from_bitField0_ & 0x00000008) != 0)) {
        allowedOrigins_.makeImmutable();
        result.allowedOrigins_ = allowedOrigins_;
      }
      if (((from_bitField0_ & 0x00000010) != 0)) {
        result.messages_ = messagesBuilder_ == null
            ? messages_
            : messagesBuilder_.build();
        to_bitField0_ |= 0x00000002;
      }
      if (((from_bitField0_ & 0x00000040) != 0)) {
        result.runConfig_ = runConfigBuilder_ == null
            ? runConfig_
            : runConfigBuilder_.build();
        to_bitField0_ |= 0x00000004;
      }
      result.bitField0_ |= to_bitField0_;
    }

    @java.lang.Override
    public Builder mergeFrom(com.google.protobuf.Message other) {
      if (other instanceof ai.stigmer.agentic.agentshare.v1.AgentShareSpec) {
        return mergeFrom((ai.stigmer.agentic.agentshare.v1.AgentShareSpec)other);
      } else {
        super.mergeFrom(other);
        return this;
      }
    }

    public Builder mergeFrom(ai.stigmer.agentic.agentshare.v1.AgentShareSpec other) {
      if (other == ai.stigmer.agentic.agentshare.v1.AgentShareSpec.getDefaultInstance()) return this;
      if (other.hasAgentRef()) {
        mergeAgentRef(other.getAgentRef());
      }
      if (other.getEnabled() != false) {
        setEnabled(other.getEnabled());
      }
      if (other.audience_ != 0) {
        setAudienceValue(other.getAudienceValue());
      }
      if (!other.allowedOrigins_.isEmpty()) {
        if (allowedOrigins_.isEmpty()) {
          allowedOrigins_ = other.allowedOrigins_;
          bitField0_ |= 0x00000008;
        } else {
          ensureAllowedOriginsIsMutable();
          allowedOrigins_.addAll(other.allowedOrigins_);
        }
        onChanged();
      }
      if (other.hasMessages()) {
        mergeMessages(other.getMessages());
      }
      if (environmentRefsBuilder_ == null) {
        if (!other.environmentRefs_.isEmpty()) {
          if (environmentRefs_.isEmpty()) {
            environmentRefs_ = other.environmentRefs_;
            bitField0_ = (bitField0_ & ~0x00000020);
          } else {
            ensureEnvironmentRefsIsMutable();
            environmentRefs_.addAll(other.environmentRefs_);
          }
          onChanged();
        }
      } else {
        if (!other.environmentRefs_.isEmpty()) {
          if (environmentRefsBuilder_.isEmpty()) {
            environmentRefsBuilder_.dispose();
            environmentRefsBuilder_ = null;
            environmentRefs_ = other.environmentRefs_;
            bitField0_ = (bitField0_ & ~0x00000020);
            environmentRefsBuilder_ = 
              com.google.protobuf.GeneratedMessage.alwaysUseFieldBuilders ?
                 internalGetEnvironmentRefsFieldBuilder() : null;
          } else {
            environmentRefsBuilder_.addAllMessages(other.environmentRefs_);
          }
        }
      }
      if (other.hasRunConfig()) {
        mergeRunConfig(other.getRunConfig());
      }
      this.mergeUnknownFields(other.getUnknownFields());
      onChanged();
      return this;
    }

    @java.lang.Override
    public final boolean isInitialized() {
      return true;
    }

    @java.lang.Override
    public Builder mergeFrom(
        com.google.protobuf.CodedInputStream input,
        com.google.protobuf.ExtensionRegistryLite extensionRegistry)
        throws java.io.IOException {
      if (extensionRegistry == null) {
        throw new java.lang.NullPointerException();
      }
      try {
        boolean done = false;
        while (!done) {
          int tag = input.readTag();
          switch (tag) {
            case 0:
              done = true;
              break;
            case 10: {
              input.readMessage(
                  internalGetAgentRefFieldBuilder().getBuilder(),
                  extensionRegistry);
              bitField0_ |= 0x00000001;
              break;
            } // case 10
            case 16: {
              enabled_ = input.readBool();
              bitField0_ |= 0x00000002;
              break;
            } // case 16
            case 24: {
              audience_ = input.readEnum();
              bitField0_ |= 0x00000004;
              break;
            } // case 24
            case 34: {
              ensureAllowedOriginsIsMutable();
              allowedOrigins_.add(input.readStringRequireUtf8());
              break;
            } // case 34
            case 42: {
              input.readMessage(
                  internalGetMessagesFieldBuilder().getBuilder(),
                  extensionRegistry);
              bitField0_ |= 0x00000010;
              break;
            } // case 42
            case 50: {
              ai.stigmer.commons.apiresource.ApiResourceReference m =
                  input.readMessage(
                      ai.stigmer.commons.apiresource.ApiResourceReference.parser(),
                      extensionRegistry);
              if (environmentRefsBuilder_ == null) {
                ensureEnvironmentRefsIsMutable();
                environmentRefs_.add(m);
              } else {
                environmentRefsBuilder_.addMessage(m);
              }
              break;
            } // case 50
            case 58: {
              input.readMessage(
                  internalGetRunConfigFieldBuilder().getBuilder(),
                  extensionRegistry);
              bitField0_ |= 0x00000040;
              break;
            } // case 58
            default: {
              if (!super.parseUnknownField(input, extensionRegistry, tag)) {
                done = true; // was an endgroup tag
              }
              break;
            } // default:
          } // switch (tag)
        } // while (!done)
      } catch (com.google.protobuf.InvalidProtocolBufferException e) {
        throw e.unwrapIOException();
      } finally {
        onChanged();
      } // finally
      return this;
    }
    private int bitField0_;

    private ai.stigmer.commons.apiresource.ApiResourceReference agentRef_;
    private com.google.protobuf.SingleFieldBuilder<
        ai.stigmer.commons.apiresource.ApiResourceReference, ai.stigmer.commons.apiresource.ApiResourceReference.Builder, ai.stigmer.commons.apiresource.ApiResourceReferenceOrBuilder> agentRefBuilder_;
    /**
     * <pre>
     * Reference to the agent this share distributes.
     *
     * The referenced agent may live in another organization when it is
     * marketplace-public: the share then offers that agent from this share's
     * org, billed to this share's org, with tool credentials bound from this
     * share's org. Cross-org shares must have a public audience.
     *
     * &#64;internal
     * Cross-org contract (decision 013): making an agent visibility_public
     * IS the origin org's consent to external shares — enforced app-level in
     * both editions' defaults resolvers (spec-level CEL cannot see
     * metadata.org), together with the public-audience-only rule and the
     * dependency-publicness sweep (every skill/MCP the agent declares must be
     * visibility_public, refused loudly naming blockers). The sharing org is
     * always the billing org (decision 001 generalized) and guest tool
     * credentials resolve in the share's org (secrets never cross orgs —
     * org A's environments are structurally unreachable from org B's
     * executions via OrgSharedEnvironmentPolicy). The agent's public
     * visibility is re-verified on every guest turn, at the shared profile,
     * and at guest mint, so revoking visibility kills external channels on
     * the visitor's next message. status.agent_id pins the resolved agent
     * against slug-reuse rebind.
     * </pre>
     *
     * <code>.ai.stigmer.commons.apiresource.ApiResourceReference agent_ref = 1 [json_name = "agentRef", (.buf.validate.field) = { ... }</code>
     * @return Whether the agentRef field is set.
     */
    public boolean hasAgentRef() {
      return ((bitField0_ & 0x00000001) != 0);
    }
    /**
     * <pre>
     * Reference to the agent this share distributes.
     *
     * The referenced agent may live in another organization when it is
     * marketplace-public: the share then offers that agent from this share's
     * org, billed to this share's org, with tool credentials bound from this
     * share's org. Cross-org shares must have a public audience.
     *
     * &#64;internal
     * Cross-org contract (decision 013): making an agent visibility_public
     * IS the origin org's consent to external shares — enforced app-level in
     * both editions' defaults resolvers (spec-level CEL cannot see
     * metadata.org), together with the public-audience-only rule and the
     * dependency-publicness sweep (every skill/MCP the agent declares must be
     * visibility_public, refused loudly naming blockers). The sharing org is
     * always the billing org (decision 001 generalized) and guest tool
     * credentials resolve in the share's org (secrets never cross orgs —
     * org A's environments are structurally unreachable from org B's
     * executions via OrgSharedEnvironmentPolicy). The agent's public
     * visibility is re-verified on every guest turn, at the shared profile,
     * and at guest mint, so revoking visibility kills external channels on
     * the visitor's next message. status.agent_id pins the resolved agent
     * against slug-reuse rebind.
     * </pre>
     *
     * <code>.ai.stigmer.commons.apiresource.ApiResourceReference agent_ref = 1 [json_name = "agentRef", (.buf.validate.field) = { ... }</code>
     * @return The agentRef.
     */
    public ai.stigmer.commons.apiresource.ApiResourceReference getAgentRef() {
      if (agentRefBuilder_ == null) {
        return agentRef_ == null ? ai.stigmer.commons.apiresource.ApiResourceReference.getDefaultInstance() : agentRef_;
      } else {
        return agentRefBuilder_.getMessage();
      }
    }
    /**
     * <pre>
     * Reference to the agent this share distributes.
     *
     * The referenced agent may live in another organization when it is
     * marketplace-public: the share then offers that agent from this share's
     * org, billed to this share's org, with tool credentials bound from this
     * share's org. Cross-org shares must have a public audience.
     *
     * &#64;internal
     * Cross-org contract (decision 013): making an agent visibility_public
     * IS the origin org's consent to external shares — enforced app-level in
     * both editions' defaults resolvers (spec-level CEL cannot see
     * metadata.org), together with the public-audience-only rule and the
     * dependency-publicness sweep (every skill/MCP the agent declares must be
     * visibility_public, refused loudly naming blockers). The sharing org is
     * always the billing org (decision 001 generalized) and guest tool
     * credentials resolve in the share's org (secrets never cross orgs —
     * org A's environments are structurally unreachable from org B's
     * executions via OrgSharedEnvironmentPolicy). The agent's public
     * visibility is re-verified on every guest turn, at the shared profile,
     * and at guest mint, so revoking visibility kills external channels on
     * the visitor's next message. status.agent_id pins the resolved agent
     * against slug-reuse rebind.
     * </pre>
     *
     * <code>.ai.stigmer.commons.apiresource.ApiResourceReference agent_ref = 1 [json_name = "agentRef", (.buf.validate.field) = { ... }</code>
     */
    public Builder setAgentRef(ai.stigmer.commons.apiresource.ApiResourceReference value) {
      if (agentRefBuilder_ == null) {
        if (value == null) {
          throw new NullPointerException();
        }
        agentRef_ = value;
      } else {
        agentRefBuilder_.setMessage(value);
      }
      bitField0_ |= 0x00000001;
      onChanged();
      return this;
    }
    /**
     * <pre>
     * Reference to the agent this share distributes.
     *
     * The referenced agent may live in another organization when it is
     * marketplace-public: the share then offers that agent from this share's
     * org, billed to this share's org, with tool credentials bound from this
     * share's org. Cross-org shares must have a public audience.
     *
     * &#64;internal
     * Cross-org contract (decision 013): making an agent visibility_public
     * IS the origin org's consent to external shares — enforced app-level in
     * both editions' defaults resolvers (spec-level CEL cannot see
     * metadata.org), together with the public-audience-only rule and the
     * dependency-publicness sweep (every skill/MCP the agent declares must be
     * visibility_public, refused loudly naming blockers). The sharing org is
     * always the billing org (decision 001 generalized) and guest tool
     * credentials resolve in the share's org (secrets never cross orgs —
     * org A's environments are structurally unreachable from org B's
     * executions via OrgSharedEnvironmentPolicy). The agent's public
     * visibility is re-verified on every guest turn, at the shared profile,
     * and at guest mint, so revoking visibility kills external channels on
     * the visitor's next message. status.agent_id pins the resolved agent
     * against slug-reuse rebind.
     * </pre>
     *
     * <code>.ai.stigmer.commons.apiresource.ApiResourceReference agent_ref = 1 [json_name = "agentRef", (.buf.validate.field) = { ... }</code>
     */
    public Builder setAgentRef(
        ai.stigmer.commons.apiresource.ApiResourceReference.Builder builderForValue) {
      if (agentRefBuilder_ == null) {
        agentRef_ = builderForValue.build();
      } else {
        agentRefBuilder_.setMessage(builderForValue.build());
      }
      bitField0_ |= 0x00000001;
      onChanged();
      return this;
    }
    /**
     * <pre>
     * Reference to the agent this share distributes.
     *
     * The referenced agent may live in another organization when it is
     * marketplace-public: the share then offers that agent from this share's
     * org, billed to this share's org, with tool credentials bound from this
     * share's org. Cross-org shares must have a public audience.
     *
     * &#64;internal
     * Cross-org contract (decision 013): making an agent visibility_public
     * IS the origin org's consent to external shares — enforced app-level in
     * both editions' defaults resolvers (spec-level CEL cannot see
     * metadata.org), together with the public-audience-only rule and the
     * dependency-publicness sweep (every skill/MCP the agent declares must be
     * visibility_public, refused loudly naming blockers). The sharing org is
     * always the billing org (decision 001 generalized) and guest tool
     * credentials resolve in the share's org (secrets never cross orgs —
     * org A's environments are structurally unreachable from org B's
     * executions via OrgSharedEnvironmentPolicy). The agent's public
     * visibility is re-verified on every guest turn, at the shared profile,
     * and at guest mint, so revoking visibility kills external channels on
     * the visitor's next message. status.agent_id pins the resolved agent
     * against slug-reuse rebind.
     * </pre>
     *
     * <code>.ai.stigmer.commons.apiresource.ApiResourceReference agent_ref = 1 [json_name = "agentRef", (.buf.validate.field) = { ... }</code>
     */
    public Builder mergeAgentRef(ai.stigmer.commons.apiresource.ApiResourceReference value) {
      if (agentRefBuilder_ == null) {
        if (((bitField0_ & 0x00000001) != 0) &&
          agentRef_ != null &&
          agentRef_ != ai.stigmer.commons.apiresource.ApiResourceReference.getDefaultInstance()) {
          getAgentRefBuilder().mergeFrom(value);
        } else {
          agentRef_ = value;
        }
      } else {
        agentRefBuilder_.mergeFrom(value);
      }
      if (agentRef_ != null) {
        bitField0_ |= 0x00000001;
        onChanged();
      }
      return this;
    }
    /**
     * <pre>
     * Reference to the agent this share distributes.
     *
     * The referenced agent may live in another organization when it is
     * marketplace-public: the share then offers that agent from this share's
     * org, billed to this share's org, with tool credentials bound from this
     * share's org. Cross-org shares must have a public audience.
     *
     * &#64;internal
     * Cross-org contract (decision 013): making an agent visibility_public
     * IS the origin org's consent to external shares — enforced app-level in
     * both editions' defaults resolvers (spec-level CEL cannot see
     * metadata.org), together with the public-audience-only rule and the
     * dependency-publicness sweep (every skill/MCP the agent declares must be
     * visibility_public, refused loudly naming blockers). The sharing org is
     * always the billing org (decision 001 generalized) and guest tool
     * credentials resolve in the share's org (secrets never cross orgs —
     * org A's environments are structurally unreachable from org B's
     * executions via OrgSharedEnvironmentPolicy). The agent's public
     * visibility is re-verified on every guest turn, at the shared profile,
     * and at guest mint, so revoking visibility kills external channels on
     * the visitor's next message. status.agent_id pins the resolved agent
     * against slug-reuse rebind.
     * </pre>
     *
     * <code>.ai.stigmer.commons.apiresource.ApiResourceReference agent_ref = 1 [json_name = "agentRef", (.buf.validate.field) = { ... }</code>
     */
    public Builder clearAgentRef() {
      bitField0_ = (bitField0_ & ~0x00000001);
      agentRef_ = null;
      if (agentRefBuilder_ != null) {
        agentRefBuilder_.dispose();
        agentRefBuilder_ = null;
      }
      onChanged();
      return this;
    }
    /**
     * <pre>
     * Reference to the agent this share distributes.
     *
     * The referenced agent may live in another organization when it is
     * marketplace-public: the share then offers that agent from this share's
     * org, billed to this share's org, with tool credentials bound from this
     * share's org. Cross-org shares must have a public audience.
     *
     * &#64;internal
     * Cross-org contract (decision 013): making an agent visibility_public
     * IS the origin org's consent to external shares — enforced app-level in
     * both editions' defaults resolvers (spec-level CEL cannot see
     * metadata.org), together with the public-audience-only rule and the
     * dependency-publicness sweep (every skill/MCP the agent declares must be
     * visibility_public, refused loudly naming blockers). The sharing org is
     * always the billing org (decision 001 generalized) and guest tool
     * credentials resolve in the share's org (secrets never cross orgs —
     * org A's environments are structurally unreachable from org B's
     * executions via OrgSharedEnvironmentPolicy). The agent's public
     * visibility is re-verified on every guest turn, at the shared profile,
     * and at guest mint, so revoking visibility kills external channels on
     * the visitor's next message. status.agent_id pins the resolved agent
     * against slug-reuse rebind.
     * </pre>
     *
     * <code>.ai.stigmer.commons.apiresource.ApiResourceReference agent_ref = 1 [json_name = "agentRef", (.buf.validate.field) = { ... }</code>
     */
    public ai.stigmer.commons.apiresource.ApiResourceReference.Builder getAgentRefBuilder() {
      bitField0_ |= 0x00000001;
      onChanged();
      return internalGetAgentRefFieldBuilder().getBuilder();
    }
    /**
     * <pre>
     * Reference to the agent this share distributes.
     *
     * The referenced agent may live in another organization when it is
     * marketplace-public: the share then offers that agent from this share's
     * org, billed to this share's org, with tool credentials bound from this
     * share's org. Cross-org shares must have a public audience.
     *
     * &#64;internal
     * Cross-org contract (decision 013): making an agent visibility_public
     * IS the origin org's consent to external shares — enforced app-level in
     * both editions' defaults resolvers (spec-level CEL cannot see
     * metadata.org), together with the public-audience-only rule and the
     * dependency-publicness sweep (every skill/MCP the agent declares must be
     * visibility_public, refused loudly naming blockers). The sharing org is
     * always the billing org (decision 001 generalized) and guest tool
     * credentials resolve in the share's org (secrets never cross orgs —
     * org A's environments are structurally unreachable from org B's
     * executions via OrgSharedEnvironmentPolicy). The agent's public
     * visibility is re-verified on every guest turn, at the shared profile,
     * and at guest mint, so revoking visibility kills external channels on
     * the visitor's next message. status.agent_id pins the resolved agent
     * against slug-reuse rebind.
     * </pre>
     *
     * <code>.ai.stigmer.commons.apiresource.ApiResourceReference agent_ref = 1 [json_name = "agentRef", (.buf.validate.field) = { ... }</code>
     */
    public ai.stigmer.commons.apiresource.ApiResourceReferenceOrBuilder getAgentRefOrBuilder() {
      if (agentRefBuilder_ != null) {
        return agentRefBuilder_.getMessageOrBuilder();
      } else {
        return agentRef_ == null ?
            ai.stigmer.commons.apiresource.ApiResourceReference.getDefaultInstance() : agentRef_;
      }
    }
    /**
     * <pre>
     * Reference to the agent this share distributes.
     *
     * The referenced agent may live in another organization when it is
     * marketplace-public: the share then offers that agent from this share's
     * org, billed to this share's org, with tool credentials bound from this
     * share's org. Cross-org shares must have a public audience.
     *
     * &#64;internal
     * Cross-org contract (decision 013): making an agent visibility_public
     * IS the origin org's consent to external shares — enforced app-level in
     * both editions' defaults resolvers (spec-level CEL cannot see
     * metadata.org), together with the public-audience-only rule and the
     * dependency-publicness sweep (every skill/MCP the agent declares must be
     * visibility_public, refused loudly naming blockers). The sharing org is
     * always the billing org (decision 001 generalized) and guest tool
     * credentials resolve in the share's org (secrets never cross orgs —
     * org A's environments are structurally unreachable from org B's
     * executions via OrgSharedEnvironmentPolicy). The agent's public
     * visibility is re-verified on every guest turn, at the shared profile,
     * and at guest mint, so revoking visibility kills external channels on
     * the visitor's next message. status.agent_id pins the resolved agent
     * against slug-reuse rebind.
     * </pre>
     *
     * <code>.ai.stigmer.commons.apiresource.ApiResourceReference agent_ref = 1 [json_name = "agentRef", (.buf.validate.field) = { ... }</code>
     */
    private com.google.protobuf.SingleFieldBuilder<
        ai.stigmer.commons.apiresource.ApiResourceReference, ai.stigmer.commons.apiresource.ApiResourceReference.Builder, ai.stigmer.commons.apiresource.ApiResourceReferenceOrBuilder> 
        internalGetAgentRefFieldBuilder() {
      if (agentRefBuilder_ == null) {
        agentRefBuilder_ = new com.google.protobuf.SingleFieldBuilder<
            ai.stigmer.commons.apiresource.ApiResourceReference, ai.stigmer.commons.apiresource.ApiResourceReference.Builder, ai.stigmer.commons.apiresource.ApiResourceReferenceOrBuilder>(
                getAgentRef(),
                getParentForChildren(),
                isClean());
        agentRef_ = null;
      }
      return agentRefBuilder_;
    }

    private boolean enabled_ ;
    /**
     * <pre>
     * Whether hosted-chat access for the configured audience is enabled.
     *
     * Disabling is a config-preserving pause: the share keeps its origins,
     * messages, credentials, and link token, but the hosted link answers
     * NOT_FOUND until re-enabled. Delete the share instead for a full
     * teardown.
     * </pre>
     *
     * <code>bool enabled = 2 [json_name = "enabled"];</code>
     * @return The enabled.
     */
    @java.lang.Override
    public boolean getEnabled() {
      return enabled_;
    }
    /**
     * <pre>
     * Whether hosted-chat access for the configured audience is enabled.
     *
     * Disabling is a config-preserving pause: the share keeps its origins,
     * messages, credentials, and link token, but the hosted link answers
     * NOT_FOUND until re-enabled. Delete the share instead for a full
     * teardown.
     * </pre>
     *
     * <code>bool enabled = 2 [json_name = "enabled"];</code>
     * @param value The enabled to set.
     * @return This builder for chaining.
     */
    public Builder setEnabled(boolean value) {

      enabled_ = value;
      bitField0_ |= 0x00000002;
      onChanged();
      return this;
    }
    /**
     * <pre>
     * Whether hosted-chat access for the configured audience is enabled.
     *
     * Disabling is a config-preserving pause: the share keeps its origins,
     * messages, credentials, and link token, but the hosted link answers
     * NOT_FOUND until re-enabled. Delete the share instead for a full
     * teardown.
     * </pre>
     *
     * <code>bool enabled = 2 [json_name = "enabled"];</code>
     * @return This builder for chaining.
     */
    public Builder clearEnabled() {
      bitField0_ = (bitField0_ & ~0x00000002);
      enabled_ = false;
      onChanged();
      return this;
    }

    private int audience_ = 0;
    /**
     * <pre>
     * Who can chat over this share. Unspecified means public (anyone with
     * the link), so a manifest that omits audience creates a public share.
     *
     * To keep a share org-only, audience must be present in every apply:
     * update/apply replace the spec wholesale, so a manifest that sets
     * enabled without audience resets the share to public.
     *
     * &#64;internal
     * Org audience is enforced cloud-side only (org membership is a
     * multi-tenant IAM concept): the guest mint and public profile handlers
     * treat audience=org as NOT_FOUND, and the create-time blueprint gate
     * admits authenticated org members via an app-level FGA member check
     * (no visibility tuples written). The OSS single-user server stores and
     * echoes the field.
     * </pre>
     *
     * <code>.ai.stigmer.agentic.agentshare.v1.AgentShareAudience audience = 3 [json_name = "audience"];</code>
     * @return The enum numeric value on the wire for audience.
     */
    @java.lang.Override public int getAudienceValue() {
      return audience_;
    }
    /**
     * <pre>
     * Who can chat over this share. Unspecified means public (anyone with
     * the link), so a manifest that omits audience creates a public share.
     *
     * To keep a share org-only, audience must be present in every apply:
     * update/apply replace the spec wholesale, so a manifest that sets
     * enabled without audience resets the share to public.
     *
     * &#64;internal
     * Org audience is enforced cloud-side only (org membership is a
     * multi-tenant IAM concept): the guest mint and public profile handlers
     * treat audience=org as NOT_FOUND, and the create-time blueprint gate
     * admits authenticated org members via an app-level FGA member check
     * (no visibility tuples written). The OSS single-user server stores and
     * echoes the field.
     * </pre>
     *
     * <code>.ai.stigmer.agentic.agentshare.v1.AgentShareAudience audience = 3 [json_name = "audience"];</code>
     * @param value The enum numeric value on the wire for audience to set.
     * @throws IllegalArgumentException if UNRECOGNIZED is provided.
     * @return This builder for chaining.
     */
    public Builder setAudienceValue(int value) {
      audience_ = value;
      bitField0_ |= 0x00000004;
      onChanged();
      return this;
    }
    /**
     * <pre>
     * Who can chat over this share. Unspecified means public (anyone with
     * the link), so a manifest that omits audience creates a public share.
     *
     * To keep a share org-only, audience must be present in every apply:
     * update/apply replace the spec wholesale, so a manifest that sets
     * enabled without audience resets the share to public.
     *
     * &#64;internal
     * Org audience is enforced cloud-side only (org membership is a
     * multi-tenant IAM concept): the guest mint and public profile handlers
     * treat audience=org as NOT_FOUND, and the create-time blueprint gate
     * admits authenticated org members via an app-level FGA member check
     * (no visibility tuples written). The OSS single-user server stores and
     * echoes the field.
     * </pre>
     *
     * <code>.ai.stigmer.agentic.agentshare.v1.AgentShareAudience audience = 3 [json_name = "audience"];</code>
     * @return The audience.
     */
    @java.lang.Override
    public ai.stigmer.agentic.agentshare.v1.AgentShareAudience getAudience() {
      ai.stigmer.agentic.agentshare.v1.AgentShareAudience result = ai.stigmer.agentic.agentshare.v1.AgentShareAudience.forNumber(audience_);
      return result == null ? ai.stigmer.agentic.agentshare.v1.AgentShareAudience.UNRECOGNIZED : result;
    }
    /**
     * <pre>
     * Who can chat over this share. Unspecified means public (anyone with
     * the link), so a manifest that omits audience creates a public share.
     *
     * To keep a share org-only, audience must be present in every apply:
     * update/apply replace the spec wholesale, so a manifest that sets
     * enabled without audience resets the share to public.
     *
     * &#64;internal
     * Org audience is enforced cloud-side only (org membership is a
     * multi-tenant IAM concept): the guest mint and public profile handlers
     * treat audience=org as NOT_FOUND, and the create-time blueprint gate
     * admits authenticated org members via an app-level FGA member check
     * (no visibility tuples written). The OSS single-user server stores and
     * echoes the field.
     * </pre>
     *
     * <code>.ai.stigmer.agentic.agentshare.v1.AgentShareAudience audience = 3 [json_name = "audience"];</code>
     * @param value The audience to set.
     * @return This builder for chaining.
     */
    public Builder setAudience(ai.stigmer.agentic.agentshare.v1.AgentShareAudience value) {
      if (value == null) { throw new NullPointerException(); }
      bitField0_ |= 0x00000004;
      audience_ = value.getNumber();
      onChanged();
      return this;
    }
    /**
     * <pre>
     * Who can chat over this share. Unspecified means public (anyone with
     * the link), so a manifest that omits audience creates a public share.
     *
     * To keep a share org-only, audience must be present in every apply:
     * update/apply replace the spec wholesale, so a manifest that sets
     * enabled without audience resets the share to public.
     *
     * &#64;internal
     * Org audience is enforced cloud-side only (org membership is a
     * multi-tenant IAM concept): the guest mint and public profile handlers
     * treat audience=org as NOT_FOUND, and the create-time blueprint gate
     * admits authenticated org members via an app-level FGA member check
     * (no visibility tuples written). The OSS single-user server stores and
     * echoes the field.
     * </pre>
     *
     * <code>.ai.stigmer.agentic.agentshare.v1.AgentShareAudience audience = 3 [json_name = "audience"];</code>
     * @return This builder for chaining.
     */
    public Builder clearAudience() {
      bitField0_ = (bitField0_ & ~0x00000004);
      audience_ = 0;
      onChanged();
      return this;
    }

    private com.google.protobuf.LazyStringArrayList allowedOrigins_ =
        com.google.protobuf.LazyStringArrayList.emptyList();
    private void ensureAllowedOriginsIsMutable() {
      if (!allowedOrigins_.isModifiable()) {
        allowedOrigins_ = new com.google.protobuf.LazyStringArrayList(allowedOrigins_);
      }
      bitField0_ |= 0x00000008;
    }
    /**
     * <pre>
     * Origins permitted to embed this share's chat widget.
     *
     * Each entry is an exact web origin (scheme://host[:port]) with no path,
     * for example "https://docs.example.com". An empty list allows embedding
     * from any site; listing origins restricts embedding to those sites. The
     * first-party hosted chat page is always exempt.
     *
     * &#64;internal
     * Enforced since T04 against the embed_origin the widget reports at
     * mintGuestToken time (stamped into the guest JWT as a claim) and
     * re-validated against this live list by the guest create-time gate
     * (SharedSessionBlueprintAccess) on every session/execution create — the
     * same gate that re-checks enabled, so revocation latency is identical
     * (immediate). Unframed hosted-page visitors report no origin and are
     * exempt by construction. Exact origins only — loosening to wildcards
     * later is a non-breaking change, tightening would not be.
     * </pre>
     *
     * <code>repeated string allowed_origins = 4 [json_name = "allowedOrigins", (.buf.validate.field) = { ... }</code>
     * @return A list containing the allowedOrigins.
     */
    public com.google.protobuf.ProtocolStringList
        getAllowedOriginsList() {
      allowedOrigins_.makeImmutable();
      return allowedOrigins_;
    }
    /**
     * <pre>
     * Origins permitted to embed this share's chat widget.
     *
     * Each entry is an exact web origin (scheme://host[:port]) with no path,
     * for example "https://docs.example.com". An empty list allows embedding
     * from any site; listing origins restricts embedding to those sites. The
     * first-party hosted chat page is always exempt.
     *
     * &#64;internal
     * Enforced since T04 against the embed_origin the widget reports at
     * mintGuestToken time (stamped into the guest JWT as a claim) and
     * re-validated against this live list by the guest create-time gate
     * (SharedSessionBlueprintAccess) on every session/execution create — the
     * same gate that re-checks enabled, so revocation latency is identical
     * (immediate). Unframed hosted-page visitors report no origin and are
     * exempt by construction. Exact origins only — loosening to wildcards
     * later is a non-breaking change, tightening would not be.
     * </pre>
     *
     * <code>repeated string allowed_origins = 4 [json_name = "allowedOrigins", (.buf.validate.field) = { ... }</code>
     * @return The count of allowedOrigins.
     */
    public int getAllowedOriginsCount() {
      return allowedOrigins_.size();
    }
    /**
     * <pre>
     * Origins permitted to embed this share's chat widget.
     *
     * Each entry is an exact web origin (scheme://host[:port]) with no path,
     * for example "https://docs.example.com". An empty list allows embedding
     * from any site; listing origins restricts embedding to those sites. The
     * first-party hosted chat page is always exempt.
     *
     * &#64;internal
     * Enforced since T04 against the embed_origin the widget reports at
     * mintGuestToken time (stamped into the guest JWT as a claim) and
     * re-validated against this live list by the guest create-time gate
     * (SharedSessionBlueprintAccess) on every session/execution create — the
     * same gate that re-checks enabled, so revocation latency is identical
     * (immediate). Unframed hosted-page visitors report no origin and are
     * exempt by construction. Exact origins only — loosening to wildcards
     * later is a non-breaking change, tightening would not be.
     * </pre>
     *
     * <code>repeated string allowed_origins = 4 [json_name = "allowedOrigins", (.buf.validate.field) = { ... }</code>
     * @param index The index of the element to return.
     * @return The allowedOrigins at the given index.
     */
    public java.lang.String getAllowedOrigins(int index) {
      return allowedOrigins_.get(index);
    }
    /**
     * <pre>
     * Origins permitted to embed this share's chat widget.
     *
     * Each entry is an exact web origin (scheme://host[:port]) with no path,
     * for example "https://docs.example.com". An empty list allows embedding
     * from any site; listing origins restricts embedding to those sites. The
     * first-party hosted chat page is always exempt.
     *
     * &#64;internal
     * Enforced since T04 against the embed_origin the widget reports at
     * mintGuestToken time (stamped into the guest JWT as a claim) and
     * re-validated against this live list by the guest create-time gate
     * (SharedSessionBlueprintAccess) on every session/execution create — the
     * same gate that re-checks enabled, so revocation latency is identical
     * (immediate). Unframed hosted-page visitors report no origin and are
     * exempt by construction. Exact origins only — loosening to wildcards
     * later is a non-breaking change, tightening would not be.
     * </pre>
     *
     * <code>repeated string allowed_origins = 4 [json_name = "allowedOrigins", (.buf.validate.field) = { ... }</code>
     * @param index The index of the value to return.
     * @return The bytes of the allowedOrigins at the given index.
     */
    public com.google.protobuf.ByteString
        getAllowedOriginsBytes(int index) {
      return allowedOrigins_.getByteString(index);
    }
    /**
     * <pre>
     * Origins permitted to embed this share's chat widget.
     *
     * Each entry is an exact web origin (scheme://host[:port]) with no path,
     * for example "https://docs.example.com". An empty list allows embedding
     * from any site; listing origins restricts embedding to those sites. The
     * first-party hosted chat page is always exempt.
     *
     * &#64;internal
     * Enforced since T04 against the embed_origin the widget reports at
     * mintGuestToken time (stamped into the guest JWT as a claim) and
     * re-validated against this live list by the guest create-time gate
     * (SharedSessionBlueprintAccess) on every session/execution create — the
     * same gate that re-checks enabled, so revocation latency is identical
     * (immediate). Unframed hosted-page visitors report no origin and are
     * exempt by construction. Exact origins only — loosening to wildcards
     * later is a non-breaking change, tightening would not be.
     * </pre>
     *
     * <code>repeated string allowed_origins = 4 [json_name = "allowedOrigins", (.buf.validate.field) = { ... }</code>
     * @param index The index to set the value at.
     * @param value The allowedOrigins to set.
     * @return This builder for chaining.
     */
    public Builder setAllowedOrigins(
        int index, java.lang.String value) {
      if (value == null) { throw new NullPointerException(); }
      ensureAllowedOriginsIsMutable();
      allowedOrigins_.set(index, value);
      bitField0_ |= 0x00000008;
      onChanged();
      return this;
    }
    /**
     * <pre>
     * Origins permitted to embed this share's chat widget.
     *
     * Each entry is an exact web origin (scheme://host[:port]) with no path,
     * for example "https://docs.example.com". An empty list allows embedding
     * from any site; listing origins restricts embedding to those sites. The
     * first-party hosted chat page is always exempt.
     *
     * &#64;internal
     * Enforced since T04 against the embed_origin the widget reports at
     * mintGuestToken time (stamped into the guest JWT as a claim) and
     * re-validated against this live list by the guest create-time gate
     * (SharedSessionBlueprintAccess) on every session/execution create — the
     * same gate that re-checks enabled, so revocation latency is identical
     * (immediate). Unframed hosted-page visitors report no origin and are
     * exempt by construction. Exact origins only — loosening to wildcards
     * later is a non-breaking change, tightening would not be.
     * </pre>
     *
     * <code>repeated string allowed_origins = 4 [json_name = "allowedOrigins", (.buf.validate.field) = { ... }</code>
     * @param value The allowedOrigins to add.
     * @return This builder for chaining.
     */
    public Builder addAllowedOrigins(
        java.lang.String value) {
      if (value == null) { throw new NullPointerException(); }
      ensureAllowedOriginsIsMutable();
      allowedOrigins_.add(value);
      bitField0_ |= 0x00000008;
      onChanged();
      return this;
    }
    /**
     * <pre>
     * Origins permitted to embed this share's chat widget.
     *
     * Each entry is an exact web origin (scheme://host[:port]) with no path,
     * for example "https://docs.example.com". An empty list allows embedding
     * from any site; listing origins restricts embedding to those sites. The
     * first-party hosted chat page is always exempt.
     *
     * &#64;internal
     * Enforced since T04 against the embed_origin the widget reports at
     * mintGuestToken time (stamped into the guest JWT as a claim) and
     * re-validated against this live list by the guest create-time gate
     * (SharedSessionBlueprintAccess) on every session/execution create — the
     * same gate that re-checks enabled, so revocation latency is identical
     * (immediate). Unframed hosted-page visitors report no origin and are
     * exempt by construction. Exact origins only — loosening to wildcards
     * later is a non-breaking change, tightening would not be.
     * </pre>
     *
     * <code>repeated string allowed_origins = 4 [json_name = "allowedOrigins", (.buf.validate.field) = { ... }</code>
     * @param values The allowedOrigins to add.
     * @return This builder for chaining.
     */
    public Builder addAllAllowedOrigins(
        java.lang.Iterable<java.lang.String> values) {
      ensureAllowedOriginsIsMutable();
      com.google.protobuf.AbstractMessageLite.Builder.addAll(
          values, allowedOrigins_);
      bitField0_ |= 0x00000008;
      onChanged();
      return this;
    }
    /**
     * <pre>
     * Origins permitted to embed this share's chat widget.
     *
     * Each entry is an exact web origin (scheme://host[:port]) with no path,
     * for example "https://docs.example.com". An empty list allows embedding
     * from any site; listing origins restricts embedding to those sites. The
     * first-party hosted chat page is always exempt.
     *
     * &#64;internal
     * Enforced since T04 against the embed_origin the widget reports at
     * mintGuestToken time (stamped into the guest JWT as a claim) and
     * re-validated against this live list by the guest create-time gate
     * (SharedSessionBlueprintAccess) on every session/execution create — the
     * same gate that re-checks enabled, so revocation latency is identical
     * (immediate). Unframed hosted-page visitors report no origin and are
     * exempt by construction. Exact origins only — loosening to wildcards
     * later is a non-breaking change, tightening would not be.
     * </pre>
     *
     * <code>repeated string allowed_origins = 4 [json_name = "allowedOrigins", (.buf.validate.field) = { ... }</code>
     * @return This builder for chaining.
     */
    public Builder clearAllowedOrigins() {
      allowedOrigins_ =
        com.google.protobuf.LazyStringArrayList.emptyList();
      bitField0_ = (bitField0_ & ~0x00000008);;
      onChanged();
      return this;
    }
    /**
     * <pre>
     * Origins permitted to embed this share's chat widget.
     *
     * Each entry is an exact web origin (scheme://host[:port]) with no path,
     * for example "https://docs.example.com". An empty list allows embedding
     * from any site; listing origins restricts embedding to those sites. The
     * first-party hosted chat page is always exempt.
     *
     * &#64;internal
     * Enforced since T04 against the embed_origin the widget reports at
     * mintGuestToken time (stamped into the guest JWT as a claim) and
     * re-validated against this live list by the guest create-time gate
     * (SharedSessionBlueprintAccess) on every session/execution create — the
     * same gate that re-checks enabled, so revocation latency is identical
     * (immediate). Unframed hosted-page visitors report no origin and are
     * exempt by construction. Exact origins only — loosening to wildcards
     * later is a non-breaking change, tightening would not be.
     * </pre>
     *
     * <code>repeated string allowed_origins = 4 [json_name = "allowedOrigins", (.buf.validate.field) = { ... }</code>
     * @param value The bytes of the allowedOrigins to add.
     * @return This builder for chaining.
     */
    public Builder addAllowedOriginsBytes(
        com.google.protobuf.ByteString value) {
      if (value == null) { throw new NullPointerException(); }
      checkByteStringIsUtf8(value);
      ensureAllowedOriginsIsMutable();
      allowedOrigins_.add(value);
      bitField0_ |= 0x00000008;
      onChanged();
      return this;
    }

    private ai.stigmer.agentic.agentshare.v1.AgentShareMessages messages_;
    private com.google.protobuf.SingleFieldBuilder<
        ai.stigmer.agentic.agentshare.v1.AgentShareMessages, ai.stigmer.agentic.agentshare.v1.AgentShareMessages.Builder, ai.stigmer.agentic.agentshare.v1.AgentShareMessagesOrBuilder> messagesBuilder_;
    /**
     * <pre>
     * Owner-customizable copy shown to visitors when a launch-gate limit
     * refuses their message. Unset fields fall back to platform defaults.
     *
     * &#64;internal
     * Resolved server-side at the refusal point and carried in the gRPC
     * status description — deliberately NOT surfaced on SharedAgentProfile
     * and NOT mapped client-side, so the copy reaches every client (web,
     * embed, CLI) through the existing error-message path.
     * </pre>
     *
     * <code>.ai.stigmer.agentic.agentshare.v1.AgentShareMessages messages = 5 [json_name = "messages"];</code>
     * @return Whether the messages field is set.
     */
    public boolean hasMessages() {
      return ((bitField0_ & 0x00000010) != 0);
    }
    /**
     * <pre>
     * Owner-customizable copy shown to visitors when a launch-gate limit
     * refuses their message. Unset fields fall back to platform defaults.
     *
     * &#64;internal
     * Resolved server-side at the refusal point and carried in the gRPC
     * status description — deliberately NOT surfaced on SharedAgentProfile
     * and NOT mapped client-side, so the copy reaches every client (web,
     * embed, CLI) through the existing error-message path.
     * </pre>
     *
     * <code>.ai.stigmer.agentic.agentshare.v1.AgentShareMessages messages = 5 [json_name = "messages"];</code>
     * @return The messages.
     */
    public ai.stigmer.agentic.agentshare.v1.AgentShareMessages getMessages() {
      if (messagesBuilder_ == null) {
        return messages_ == null ? ai.stigmer.agentic.agentshare.v1.AgentShareMessages.getDefaultInstance() : messages_;
      } else {
        return messagesBuilder_.getMessage();
      }
    }
    /**
     * <pre>
     * Owner-customizable copy shown to visitors when a launch-gate limit
     * refuses their message. Unset fields fall back to platform defaults.
     *
     * &#64;internal
     * Resolved server-side at the refusal point and carried in the gRPC
     * status description — deliberately NOT surfaced on SharedAgentProfile
     * and NOT mapped client-side, so the copy reaches every client (web,
     * embed, CLI) through the existing error-message path.
     * </pre>
     *
     * <code>.ai.stigmer.agentic.agentshare.v1.AgentShareMessages messages = 5 [json_name = "messages"];</code>
     */
    public Builder setMessages(ai.stigmer.agentic.agentshare.v1.AgentShareMessages value) {
      if (messagesBuilder_ == null) {
        if (value == null) {
          throw new NullPointerException();
        }
        messages_ = value;
      } else {
        messagesBuilder_.setMessage(value);
      }
      bitField0_ |= 0x00000010;
      onChanged();
      return this;
    }
    /**
     * <pre>
     * Owner-customizable copy shown to visitors when a launch-gate limit
     * refuses their message. Unset fields fall back to platform defaults.
     *
     * &#64;internal
     * Resolved server-side at the refusal point and carried in the gRPC
     * status description — deliberately NOT surfaced on SharedAgentProfile
     * and NOT mapped client-side, so the copy reaches every client (web,
     * embed, CLI) through the existing error-message path.
     * </pre>
     *
     * <code>.ai.stigmer.agentic.agentshare.v1.AgentShareMessages messages = 5 [json_name = "messages"];</code>
     */
    public Builder setMessages(
        ai.stigmer.agentic.agentshare.v1.AgentShareMessages.Builder builderForValue) {
      if (messagesBuilder_ == null) {
        messages_ = builderForValue.build();
      } else {
        messagesBuilder_.setMessage(builderForValue.build());
      }
      bitField0_ |= 0x00000010;
      onChanged();
      return this;
    }
    /**
     * <pre>
     * Owner-customizable copy shown to visitors when a launch-gate limit
     * refuses their message. Unset fields fall back to platform defaults.
     *
     * &#64;internal
     * Resolved server-side at the refusal point and carried in the gRPC
     * status description — deliberately NOT surfaced on SharedAgentProfile
     * and NOT mapped client-side, so the copy reaches every client (web,
     * embed, CLI) through the existing error-message path.
     * </pre>
     *
     * <code>.ai.stigmer.agentic.agentshare.v1.AgentShareMessages messages = 5 [json_name = "messages"];</code>
     */
    public Builder mergeMessages(ai.stigmer.agentic.agentshare.v1.AgentShareMessages value) {
      if (messagesBuilder_ == null) {
        if (((bitField0_ & 0x00000010) != 0) &&
          messages_ != null &&
          messages_ != ai.stigmer.agentic.agentshare.v1.AgentShareMessages.getDefaultInstance()) {
          getMessagesBuilder().mergeFrom(value);
        } else {
          messages_ = value;
        }
      } else {
        messagesBuilder_.mergeFrom(value);
      }
      if (messages_ != null) {
        bitField0_ |= 0x00000010;
        onChanged();
      }
      return this;
    }
    /**
     * <pre>
     * Owner-customizable copy shown to visitors when a launch-gate limit
     * refuses their message. Unset fields fall back to platform defaults.
     *
     * &#64;internal
     * Resolved server-side at the refusal point and carried in the gRPC
     * status description — deliberately NOT surfaced on SharedAgentProfile
     * and NOT mapped client-side, so the copy reaches every client (web,
     * embed, CLI) through the existing error-message path.
     * </pre>
     *
     * <code>.ai.stigmer.agentic.agentshare.v1.AgentShareMessages messages = 5 [json_name = "messages"];</code>
     */
    public Builder clearMessages() {
      bitField0_ = (bitField0_ & ~0x00000010);
      messages_ = null;
      if (messagesBuilder_ != null) {
        messagesBuilder_.dispose();
        messagesBuilder_ = null;
      }
      onChanged();
      return this;
    }
    /**
     * <pre>
     * Owner-customizable copy shown to visitors when a launch-gate limit
     * refuses their message. Unset fields fall back to platform defaults.
     *
     * &#64;internal
     * Resolved server-side at the refusal point and carried in the gRPC
     * status description — deliberately NOT surfaced on SharedAgentProfile
     * and NOT mapped client-side, so the copy reaches every client (web,
     * embed, CLI) through the existing error-message path.
     * </pre>
     *
     * <code>.ai.stigmer.agentic.agentshare.v1.AgentShareMessages messages = 5 [json_name = "messages"];</code>
     */
    public ai.stigmer.agentic.agentshare.v1.AgentShareMessages.Builder getMessagesBuilder() {
      bitField0_ |= 0x00000010;
      onChanged();
      return internalGetMessagesFieldBuilder().getBuilder();
    }
    /**
     * <pre>
     * Owner-customizable copy shown to visitors when a launch-gate limit
     * refuses their message. Unset fields fall back to platform defaults.
     *
     * &#64;internal
     * Resolved server-side at the refusal point and carried in the gRPC
     * status description — deliberately NOT surfaced on SharedAgentProfile
     * and NOT mapped client-side, so the copy reaches every client (web,
     * embed, CLI) through the existing error-message path.
     * </pre>
     *
     * <code>.ai.stigmer.agentic.agentshare.v1.AgentShareMessages messages = 5 [json_name = "messages"];</code>
     */
    public ai.stigmer.agentic.agentshare.v1.AgentShareMessagesOrBuilder getMessagesOrBuilder() {
      if (messagesBuilder_ != null) {
        return messagesBuilder_.getMessageOrBuilder();
      } else {
        return messages_ == null ?
            ai.stigmer.agentic.agentshare.v1.AgentShareMessages.getDefaultInstance() : messages_;
      }
    }
    /**
     * <pre>
     * Owner-customizable copy shown to visitors when a launch-gate limit
     * refuses their message. Unset fields fall back to platform defaults.
     *
     * &#64;internal
     * Resolved server-side at the refusal point and carried in the gRPC
     * status description — deliberately NOT surfaced on SharedAgentProfile
     * and NOT mapped client-side, so the copy reaches every client (web,
     * embed, CLI) through the existing error-message path.
     * </pre>
     *
     * <code>.ai.stigmer.agentic.agentshare.v1.AgentShareMessages messages = 5 [json_name = "messages"];</code>
     */
    private com.google.protobuf.SingleFieldBuilder<
        ai.stigmer.agentic.agentshare.v1.AgentShareMessages, ai.stigmer.agentic.agentshare.v1.AgentShareMessages.Builder, ai.stigmer.agentic.agentshare.v1.AgentShareMessagesOrBuilder> 
        internalGetMessagesFieldBuilder() {
      if (messagesBuilder_ == null) {
        messagesBuilder_ = new com.google.protobuf.SingleFieldBuilder<
            ai.stigmer.agentic.agentshare.v1.AgentShareMessages, ai.stigmer.agentic.agentshare.v1.AgentShareMessages.Builder, ai.stigmer.agentic.agentshare.v1.AgentShareMessagesOrBuilder>(
                getMessages(),
                getParentForChildren(),
                isClean());
        messages_ = null;
      }
      return messagesBuilder_;
    }

    private java.util.List<ai.stigmer.commons.apiresource.ApiResourceReference> environmentRefs_ =
      java.util.Collections.emptyList();
    private void ensureEnvironmentRefsIsMutable() {
      if (!((bitField0_ & 0x00000020) != 0)) {
        environmentRefs_ = new java.util.ArrayList<ai.stigmer.commons.apiresource.ApiResourceReference>(environmentRefs_);
        bitField0_ |= 0x00000020;
       }
    }

    private com.google.protobuf.RepeatedFieldBuilder<
        ai.stigmer.commons.apiresource.ApiResourceReference, ai.stigmer.commons.apiresource.ApiResourceReference.Builder, ai.stigmer.commons.apiresource.ApiResourceReferenceOrBuilder> environmentRefsBuilder_;

    /**
     * <pre>
     * References to Environment resources whose values are provided to guest
     * conversations on this share.
     *
     * This is how a tool-using agent becomes chattable over a share link:
     * bind an org-shared environment holding the needed credentials (for
     * example a read-only API token), and guest executions receive its
     * values at runtime. The agent and its default instance stay untouched.
     * Valid on public-audience shares only.
     *
     * &#64;internal
     * Resolved in the share's org through the org-shared environment
     * resolution seam (EnvironmentRuntimeResolutionService /
     * OrgSharedEnvironmentPolicy — decision 006): each referenced environment
     * must be visibility_org in the share's org, or the merge skips it with
     * a diagnostic. Merged at guest execution-context build time only —
     * never bound to the agent's system-managed default instance, which is
     * recreated empty on lifecycle events and shared by every org member's
     * casual chat (decision 011). Org-audience shares reject this field via
     * the message-level CEL rule: member sessions carry no share linkage in
     * Phase A, so bound credentials would silently never apply.
     * </pre>
     *
     * <code>repeated .ai.stigmer.commons.apiresource.ApiResourceReference environment_refs = 6 [json_name = "environmentRefs", (.buf.validate.field) = { ... }</code>
     */
    public java.util.List<ai.stigmer.commons.apiresource.ApiResourceReference> getEnvironmentRefsList() {
      if (environmentRefsBuilder_ == null) {
        return java.util.Collections.unmodifiableList(environmentRefs_);
      } else {
        return environmentRefsBuilder_.getMessageList();
      }
    }
    /**
     * <pre>
     * References to Environment resources whose values are provided to guest
     * conversations on this share.
     *
     * This is how a tool-using agent becomes chattable over a share link:
     * bind an org-shared environment holding the needed credentials (for
     * example a read-only API token), and guest executions receive its
     * values at runtime. The agent and its default instance stay untouched.
     * Valid on public-audience shares only.
     *
     * &#64;internal
     * Resolved in the share's org through the org-shared environment
     * resolution seam (EnvironmentRuntimeResolutionService /
     * OrgSharedEnvironmentPolicy — decision 006): each referenced environment
     * must be visibility_org in the share's org, or the merge skips it with
     * a diagnostic. Merged at guest execution-context build time only —
     * never bound to the agent's system-managed default instance, which is
     * recreated empty on lifecycle events and shared by every org member's
     * casual chat (decision 011). Org-audience shares reject this field via
     * the message-level CEL rule: member sessions carry no share linkage in
     * Phase A, so bound credentials would silently never apply.
     * </pre>
     *
     * <code>repeated .ai.stigmer.commons.apiresource.ApiResourceReference environment_refs = 6 [json_name = "environmentRefs", (.buf.validate.field) = { ... }</code>
     */
    public int getEnvironmentRefsCount() {
      if (environmentRefsBuilder_ == null) {
        return environmentRefs_.size();
      } else {
        return environmentRefsBuilder_.getCount();
      }
    }
    /**
     * <pre>
     * References to Environment resources whose values are provided to guest
     * conversations on this share.
     *
     * This is how a tool-using agent becomes chattable over a share link:
     * bind an org-shared environment holding the needed credentials (for
     * example a read-only API token), and guest executions receive its
     * values at runtime. The agent and its default instance stay untouched.
     * Valid on public-audience shares only.
     *
     * &#64;internal
     * Resolved in the share's org through the org-shared environment
     * resolution seam (EnvironmentRuntimeResolutionService /
     * OrgSharedEnvironmentPolicy — decision 006): each referenced environment
     * must be visibility_org in the share's org, or the merge skips it with
     * a diagnostic. Merged at guest execution-context build time only —
     * never bound to the agent's system-managed default instance, which is
     * recreated empty on lifecycle events and shared by every org member's
     * casual chat (decision 011). Org-audience shares reject this field via
     * the message-level CEL rule: member sessions carry no share linkage in
     * Phase A, so bound credentials would silently never apply.
     * </pre>
     *
     * <code>repeated .ai.stigmer.commons.apiresource.ApiResourceReference environment_refs = 6 [json_name = "environmentRefs", (.buf.validate.field) = { ... }</code>
     */
    public ai.stigmer.commons.apiresource.ApiResourceReference getEnvironmentRefs(int index) {
      if (environmentRefsBuilder_ == null) {
        return environmentRefs_.get(index);
      } else {
        return environmentRefsBuilder_.getMessage(index);
      }
    }
    /**
     * <pre>
     * References to Environment resources whose values are provided to guest
     * conversations on this share.
     *
     * This is how a tool-using agent becomes chattable over a share link:
     * bind an org-shared environment holding the needed credentials (for
     * example a read-only API token), and guest executions receive its
     * values at runtime. The agent and its default instance stay untouched.
     * Valid on public-audience shares only.
     *
     * &#64;internal
     * Resolved in the share's org through the org-shared environment
     * resolution seam (EnvironmentRuntimeResolutionService /
     * OrgSharedEnvironmentPolicy — decision 006): each referenced environment
     * must be visibility_org in the share's org, or the merge skips it with
     * a diagnostic. Merged at guest execution-context build time only —
     * never bound to the agent's system-managed default instance, which is
     * recreated empty on lifecycle events and shared by every org member's
     * casual chat (decision 011). Org-audience shares reject this field via
     * the message-level CEL rule: member sessions carry no share linkage in
     * Phase A, so bound credentials would silently never apply.
     * </pre>
     *
     * <code>repeated .ai.stigmer.commons.apiresource.ApiResourceReference environment_refs = 6 [json_name = "environmentRefs", (.buf.validate.field) = { ... }</code>
     */
    public Builder setEnvironmentRefs(
        int index, ai.stigmer.commons.apiresource.ApiResourceReference value) {
      if (environmentRefsBuilder_ == null) {
        if (value == null) {
          throw new NullPointerException();
        }
        ensureEnvironmentRefsIsMutable();
        environmentRefs_.set(index, value);
        onChanged();
      } else {
        environmentRefsBuilder_.setMessage(index, value);
      }
      return this;
    }
    /**
     * <pre>
     * References to Environment resources whose values are provided to guest
     * conversations on this share.
     *
     * This is how a tool-using agent becomes chattable over a share link:
     * bind an org-shared environment holding the needed credentials (for
     * example a read-only API token), and guest executions receive its
     * values at runtime. The agent and its default instance stay untouched.
     * Valid on public-audience shares only.
     *
     * &#64;internal
     * Resolved in the share's org through the org-shared environment
     * resolution seam (EnvironmentRuntimeResolutionService /
     * OrgSharedEnvironmentPolicy — decision 006): each referenced environment
     * must be visibility_org in the share's org, or the merge skips it with
     * a diagnostic. Merged at guest execution-context build time only —
     * never bound to the agent's system-managed default instance, which is
     * recreated empty on lifecycle events and shared by every org member's
     * casual chat (decision 011). Org-audience shares reject this field via
     * the message-level CEL rule: member sessions carry no share linkage in
     * Phase A, so bound credentials would silently never apply.
     * </pre>
     *
     * <code>repeated .ai.stigmer.commons.apiresource.ApiResourceReference environment_refs = 6 [json_name = "environmentRefs", (.buf.validate.field) = { ... }</code>
     */
    public Builder setEnvironmentRefs(
        int index, ai.stigmer.commons.apiresource.ApiResourceReference.Builder builderForValue) {
      if (environmentRefsBuilder_ == null) {
        ensureEnvironmentRefsIsMutable();
        environmentRefs_.set(index, builderForValue.build());
        onChanged();
      } else {
        environmentRefsBuilder_.setMessage(index, builderForValue.build());
      }
      return this;
    }
    /**
     * <pre>
     * References to Environment resources whose values are provided to guest
     * conversations on this share.
     *
     * This is how a tool-using agent becomes chattable over a share link:
     * bind an org-shared environment holding the needed credentials (for
     * example a read-only API token), and guest executions receive its
     * values at runtime. The agent and its default instance stay untouched.
     * Valid on public-audience shares only.
     *
     * &#64;internal
     * Resolved in the share's org through the org-shared environment
     * resolution seam (EnvironmentRuntimeResolutionService /
     * OrgSharedEnvironmentPolicy — decision 006): each referenced environment
     * must be visibility_org in the share's org, or the merge skips it with
     * a diagnostic. Merged at guest execution-context build time only —
     * never bound to the agent's system-managed default instance, which is
     * recreated empty on lifecycle events and shared by every org member's
     * casual chat (decision 011). Org-audience shares reject this field via
     * the message-level CEL rule: member sessions carry no share linkage in
     * Phase A, so bound credentials would silently never apply.
     * </pre>
     *
     * <code>repeated .ai.stigmer.commons.apiresource.ApiResourceReference environment_refs = 6 [json_name = "environmentRefs", (.buf.validate.field) = { ... }</code>
     */
    public Builder addEnvironmentRefs(ai.stigmer.commons.apiresource.ApiResourceReference value) {
      if (environmentRefsBuilder_ == null) {
        if (value == null) {
          throw new NullPointerException();
        }
        ensureEnvironmentRefsIsMutable();
        environmentRefs_.add(value);
        onChanged();
      } else {
        environmentRefsBuilder_.addMessage(value);
      }
      return this;
    }
    /**
     * <pre>
     * References to Environment resources whose values are provided to guest
     * conversations on this share.
     *
     * This is how a tool-using agent becomes chattable over a share link:
     * bind an org-shared environment holding the needed credentials (for
     * example a read-only API token), and guest executions receive its
     * values at runtime. The agent and its default instance stay untouched.
     * Valid on public-audience shares only.
     *
     * &#64;internal
     * Resolved in the share's org through the org-shared environment
     * resolution seam (EnvironmentRuntimeResolutionService /
     * OrgSharedEnvironmentPolicy — decision 006): each referenced environment
     * must be visibility_org in the share's org, or the merge skips it with
     * a diagnostic. Merged at guest execution-context build time only —
     * never bound to the agent's system-managed default instance, which is
     * recreated empty on lifecycle events and shared by every org member's
     * casual chat (decision 011). Org-audience shares reject this field via
     * the message-level CEL rule: member sessions carry no share linkage in
     * Phase A, so bound credentials would silently never apply.
     * </pre>
     *
     * <code>repeated .ai.stigmer.commons.apiresource.ApiResourceReference environment_refs = 6 [json_name = "environmentRefs", (.buf.validate.field) = { ... }</code>
     */
    public Builder addEnvironmentRefs(
        int index, ai.stigmer.commons.apiresource.ApiResourceReference value) {
      if (environmentRefsBuilder_ == null) {
        if (value == null) {
          throw new NullPointerException();
        }
        ensureEnvironmentRefsIsMutable();
        environmentRefs_.add(index, value);
        onChanged();
      } else {
        environmentRefsBuilder_.addMessage(index, value);
      }
      return this;
    }
    /**
     * <pre>
     * References to Environment resources whose values are provided to guest
     * conversations on this share.
     *
     * This is how a tool-using agent becomes chattable over a share link:
     * bind an org-shared environment holding the needed credentials (for
     * example a read-only API token), and guest executions receive its
     * values at runtime. The agent and its default instance stay untouched.
     * Valid on public-audience shares only.
     *
     * &#64;internal
     * Resolved in the share's org through the org-shared environment
     * resolution seam (EnvironmentRuntimeResolutionService /
     * OrgSharedEnvironmentPolicy — decision 006): each referenced environment
     * must be visibility_org in the share's org, or the merge skips it with
     * a diagnostic. Merged at guest execution-context build time only —
     * never bound to the agent's system-managed default instance, which is
     * recreated empty on lifecycle events and shared by every org member's
     * casual chat (decision 011). Org-audience shares reject this field via
     * the message-level CEL rule: member sessions carry no share linkage in
     * Phase A, so bound credentials would silently never apply.
     * </pre>
     *
     * <code>repeated .ai.stigmer.commons.apiresource.ApiResourceReference environment_refs = 6 [json_name = "environmentRefs", (.buf.validate.field) = { ... }</code>
     */
    public Builder addEnvironmentRefs(
        ai.stigmer.commons.apiresource.ApiResourceReference.Builder builderForValue) {
      if (environmentRefsBuilder_ == null) {
        ensureEnvironmentRefsIsMutable();
        environmentRefs_.add(builderForValue.build());
        onChanged();
      } else {
        environmentRefsBuilder_.addMessage(builderForValue.build());
      }
      return this;
    }
    /**
     * <pre>
     * References to Environment resources whose values are provided to guest
     * conversations on this share.
     *
     * This is how a tool-using agent becomes chattable over a share link:
     * bind an org-shared environment holding the needed credentials (for
     * example a read-only API token), and guest executions receive its
     * values at runtime. The agent and its default instance stay untouched.
     * Valid on public-audience shares only.
     *
     * &#64;internal
     * Resolved in the share's org through the org-shared environment
     * resolution seam (EnvironmentRuntimeResolutionService /
     * OrgSharedEnvironmentPolicy — decision 006): each referenced environment
     * must be visibility_org in the share's org, or the merge skips it with
     * a diagnostic. Merged at guest execution-context build time only —
     * never bound to the agent's system-managed default instance, which is
     * recreated empty on lifecycle events and shared by every org member's
     * casual chat (decision 011). Org-audience shares reject this field via
     * the message-level CEL rule: member sessions carry no share linkage in
     * Phase A, so bound credentials would silently never apply.
     * </pre>
     *
     * <code>repeated .ai.stigmer.commons.apiresource.ApiResourceReference environment_refs = 6 [json_name = "environmentRefs", (.buf.validate.field) = { ... }</code>
     */
    public Builder addEnvironmentRefs(
        int index, ai.stigmer.commons.apiresource.ApiResourceReference.Builder builderForValue) {
      if (environmentRefsBuilder_ == null) {
        ensureEnvironmentRefsIsMutable();
        environmentRefs_.add(index, builderForValue.build());
        onChanged();
      } else {
        environmentRefsBuilder_.addMessage(index, builderForValue.build());
      }
      return this;
    }
    /**
     * <pre>
     * References to Environment resources whose values are provided to guest
     * conversations on this share.
     *
     * This is how a tool-using agent becomes chattable over a share link:
     * bind an org-shared environment holding the needed credentials (for
     * example a read-only API token), and guest executions receive its
     * values at runtime. The agent and its default instance stay untouched.
     * Valid on public-audience shares only.
     *
     * &#64;internal
     * Resolved in the share's org through the org-shared environment
     * resolution seam (EnvironmentRuntimeResolutionService /
     * OrgSharedEnvironmentPolicy — decision 006): each referenced environment
     * must be visibility_org in the share's org, or the merge skips it with
     * a diagnostic. Merged at guest execution-context build time only —
     * never bound to the agent's system-managed default instance, which is
     * recreated empty on lifecycle events and shared by every org member's
     * casual chat (decision 011). Org-audience shares reject this field via
     * the message-level CEL rule: member sessions carry no share linkage in
     * Phase A, so bound credentials would silently never apply.
     * </pre>
     *
     * <code>repeated .ai.stigmer.commons.apiresource.ApiResourceReference environment_refs = 6 [json_name = "environmentRefs", (.buf.validate.field) = { ... }</code>
     */
    public Builder addAllEnvironmentRefs(
        java.lang.Iterable<? extends ai.stigmer.commons.apiresource.ApiResourceReference> values) {
      if (environmentRefsBuilder_ == null) {
        ensureEnvironmentRefsIsMutable();
        com.google.protobuf.AbstractMessageLite.Builder.addAll(
            values, environmentRefs_);
        onChanged();
      } else {
        environmentRefsBuilder_.addAllMessages(values);
      }
      return this;
    }
    /**
     * <pre>
     * References to Environment resources whose values are provided to guest
     * conversations on this share.
     *
     * This is how a tool-using agent becomes chattable over a share link:
     * bind an org-shared environment holding the needed credentials (for
     * example a read-only API token), and guest executions receive its
     * values at runtime. The agent and its default instance stay untouched.
     * Valid on public-audience shares only.
     *
     * &#64;internal
     * Resolved in the share's org through the org-shared environment
     * resolution seam (EnvironmentRuntimeResolutionService /
     * OrgSharedEnvironmentPolicy — decision 006): each referenced environment
     * must be visibility_org in the share's org, or the merge skips it with
     * a diagnostic. Merged at guest execution-context build time only —
     * never bound to the agent's system-managed default instance, which is
     * recreated empty on lifecycle events and shared by every org member's
     * casual chat (decision 011). Org-audience shares reject this field via
     * the message-level CEL rule: member sessions carry no share linkage in
     * Phase A, so bound credentials would silently never apply.
     * </pre>
     *
     * <code>repeated .ai.stigmer.commons.apiresource.ApiResourceReference environment_refs = 6 [json_name = "environmentRefs", (.buf.validate.field) = { ... }</code>
     */
    public Builder clearEnvironmentRefs() {
      if (environmentRefsBuilder_ == null) {
        environmentRefs_ = java.util.Collections.emptyList();
        bitField0_ = (bitField0_ & ~0x00000020);
        onChanged();
      } else {
        environmentRefsBuilder_.clear();
      }
      return this;
    }
    /**
     * <pre>
     * References to Environment resources whose values are provided to guest
     * conversations on this share.
     *
     * This is how a tool-using agent becomes chattable over a share link:
     * bind an org-shared environment holding the needed credentials (for
     * example a read-only API token), and guest executions receive its
     * values at runtime. The agent and its default instance stay untouched.
     * Valid on public-audience shares only.
     *
     * &#64;internal
     * Resolved in the share's org through the org-shared environment
     * resolution seam (EnvironmentRuntimeResolutionService /
     * OrgSharedEnvironmentPolicy — decision 006): each referenced environment
     * must be visibility_org in the share's org, or the merge skips it with
     * a diagnostic. Merged at guest execution-context build time only —
     * never bound to the agent's system-managed default instance, which is
     * recreated empty on lifecycle events and shared by every org member's
     * casual chat (decision 011). Org-audience shares reject this field via
     * the message-level CEL rule: member sessions carry no share linkage in
     * Phase A, so bound credentials would silently never apply.
     * </pre>
     *
     * <code>repeated .ai.stigmer.commons.apiresource.ApiResourceReference environment_refs = 6 [json_name = "environmentRefs", (.buf.validate.field) = { ... }</code>
     */
    public Builder removeEnvironmentRefs(int index) {
      if (environmentRefsBuilder_ == null) {
        ensureEnvironmentRefsIsMutable();
        environmentRefs_.remove(index);
        onChanged();
      } else {
        environmentRefsBuilder_.remove(index);
      }
      return this;
    }
    /**
     * <pre>
     * References to Environment resources whose values are provided to guest
     * conversations on this share.
     *
     * This is how a tool-using agent becomes chattable over a share link:
     * bind an org-shared environment holding the needed credentials (for
     * example a read-only API token), and guest executions receive its
     * values at runtime. The agent and its default instance stay untouched.
     * Valid on public-audience shares only.
     *
     * &#64;internal
     * Resolved in the share's org through the org-shared environment
     * resolution seam (EnvironmentRuntimeResolutionService /
     * OrgSharedEnvironmentPolicy — decision 006): each referenced environment
     * must be visibility_org in the share's org, or the merge skips it with
     * a diagnostic. Merged at guest execution-context build time only —
     * never bound to the agent's system-managed default instance, which is
     * recreated empty on lifecycle events and shared by every org member's
     * casual chat (decision 011). Org-audience shares reject this field via
     * the message-level CEL rule: member sessions carry no share linkage in
     * Phase A, so bound credentials would silently never apply.
     * </pre>
     *
     * <code>repeated .ai.stigmer.commons.apiresource.ApiResourceReference environment_refs = 6 [json_name = "environmentRefs", (.buf.validate.field) = { ... }</code>
     */
    public ai.stigmer.commons.apiresource.ApiResourceReference.Builder getEnvironmentRefsBuilder(
        int index) {
      return internalGetEnvironmentRefsFieldBuilder().getBuilder(index);
    }
    /**
     * <pre>
     * References to Environment resources whose values are provided to guest
     * conversations on this share.
     *
     * This is how a tool-using agent becomes chattable over a share link:
     * bind an org-shared environment holding the needed credentials (for
     * example a read-only API token), and guest executions receive its
     * values at runtime. The agent and its default instance stay untouched.
     * Valid on public-audience shares only.
     *
     * &#64;internal
     * Resolved in the share's org through the org-shared environment
     * resolution seam (EnvironmentRuntimeResolutionService /
     * OrgSharedEnvironmentPolicy — decision 006): each referenced environment
     * must be visibility_org in the share's org, or the merge skips it with
     * a diagnostic. Merged at guest execution-context build time only —
     * never bound to the agent's system-managed default instance, which is
     * recreated empty on lifecycle events and shared by every org member's
     * casual chat (decision 011). Org-audience shares reject this field via
     * the message-level CEL rule: member sessions carry no share linkage in
     * Phase A, so bound credentials would silently never apply.
     * </pre>
     *
     * <code>repeated .ai.stigmer.commons.apiresource.ApiResourceReference environment_refs = 6 [json_name = "environmentRefs", (.buf.validate.field) = { ... }</code>
     */
    public ai.stigmer.commons.apiresource.ApiResourceReferenceOrBuilder getEnvironmentRefsOrBuilder(
        int index) {
      if (environmentRefsBuilder_ == null) {
        return environmentRefs_.get(index);  } else {
        return environmentRefsBuilder_.getMessageOrBuilder(index);
      }
    }
    /**
     * <pre>
     * References to Environment resources whose values are provided to guest
     * conversations on this share.
     *
     * This is how a tool-using agent becomes chattable over a share link:
     * bind an org-shared environment holding the needed credentials (for
     * example a read-only API token), and guest executions receive its
     * values at runtime. The agent and its default instance stay untouched.
     * Valid on public-audience shares only.
     *
     * &#64;internal
     * Resolved in the share's org through the org-shared environment
     * resolution seam (EnvironmentRuntimeResolutionService /
     * OrgSharedEnvironmentPolicy — decision 006): each referenced environment
     * must be visibility_org in the share's org, or the merge skips it with
     * a diagnostic. Merged at guest execution-context build time only —
     * never bound to the agent's system-managed default instance, which is
     * recreated empty on lifecycle events and shared by every org member's
     * casual chat (decision 011). Org-audience shares reject this field via
     * the message-level CEL rule: member sessions carry no share linkage in
     * Phase A, so bound credentials would silently never apply.
     * </pre>
     *
     * <code>repeated .ai.stigmer.commons.apiresource.ApiResourceReference environment_refs = 6 [json_name = "environmentRefs", (.buf.validate.field) = { ... }</code>
     */
    public java.util.List<? extends ai.stigmer.commons.apiresource.ApiResourceReferenceOrBuilder> 
         getEnvironmentRefsOrBuilderList() {
      if (environmentRefsBuilder_ != null) {
        return environmentRefsBuilder_.getMessageOrBuilderList();
      } else {
        return java.util.Collections.unmodifiableList(environmentRefs_);
      }
    }
    /**
     * <pre>
     * References to Environment resources whose values are provided to guest
     * conversations on this share.
     *
     * This is how a tool-using agent becomes chattable over a share link:
     * bind an org-shared environment holding the needed credentials (for
     * example a read-only API token), and guest executions receive its
     * values at runtime. The agent and its default instance stay untouched.
     * Valid on public-audience shares only.
     *
     * &#64;internal
     * Resolved in the share's org through the org-shared environment
     * resolution seam (EnvironmentRuntimeResolutionService /
     * OrgSharedEnvironmentPolicy — decision 006): each referenced environment
     * must be visibility_org in the share's org, or the merge skips it with
     * a diagnostic. Merged at guest execution-context build time only —
     * never bound to the agent's system-managed default instance, which is
     * recreated empty on lifecycle events and shared by every org member's
     * casual chat (decision 011). Org-audience shares reject this field via
     * the message-level CEL rule: member sessions carry no share linkage in
     * Phase A, so bound credentials would silently never apply.
     * </pre>
     *
     * <code>repeated .ai.stigmer.commons.apiresource.ApiResourceReference environment_refs = 6 [json_name = "environmentRefs", (.buf.validate.field) = { ... }</code>
     */
    public ai.stigmer.commons.apiresource.ApiResourceReference.Builder addEnvironmentRefsBuilder() {
      return internalGetEnvironmentRefsFieldBuilder().addBuilder(
          ai.stigmer.commons.apiresource.ApiResourceReference.getDefaultInstance());
    }
    /**
     * <pre>
     * References to Environment resources whose values are provided to guest
     * conversations on this share.
     *
     * This is how a tool-using agent becomes chattable over a share link:
     * bind an org-shared environment holding the needed credentials (for
     * example a read-only API token), and guest executions receive its
     * values at runtime. The agent and its default instance stay untouched.
     * Valid on public-audience shares only.
     *
     * &#64;internal
     * Resolved in the share's org through the org-shared environment
     * resolution seam (EnvironmentRuntimeResolutionService /
     * OrgSharedEnvironmentPolicy — decision 006): each referenced environment
     * must be visibility_org in the share's org, or the merge skips it with
     * a diagnostic. Merged at guest execution-context build time only —
     * never bound to the agent's system-managed default instance, which is
     * recreated empty on lifecycle events and shared by every org member's
     * casual chat (decision 011). Org-audience shares reject this field via
     * the message-level CEL rule: member sessions carry no share linkage in
     * Phase A, so bound credentials would silently never apply.
     * </pre>
     *
     * <code>repeated .ai.stigmer.commons.apiresource.ApiResourceReference environment_refs = 6 [json_name = "environmentRefs", (.buf.validate.field) = { ... }</code>
     */
    public ai.stigmer.commons.apiresource.ApiResourceReference.Builder addEnvironmentRefsBuilder(
        int index) {
      return internalGetEnvironmentRefsFieldBuilder().addBuilder(
          index, ai.stigmer.commons.apiresource.ApiResourceReference.getDefaultInstance());
    }
    /**
     * <pre>
     * References to Environment resources whose values are provided to guest
     * conversations on this share.
     *
     * This is how a tool-using agent becomes chattable over a share link:
     * bind an org-shared environment holding the needed credentials (for
     * example a read-only API token), and guest executions receive its
     * values at runtime. The agent and its default instance stay untouched.
     * Valid on public-audience shares only.
     *
     * &#64;internal
     * Resolved in the share's org through the org-shared environment
     * resolution seam (EnvironmentRuntimeResolutionService /
     * OrgSharedEnvironmentPolicy — decision 006): each referenced environment
     * must be visibility_org in the share's org, or the merge skips it with
     * a diagnostic. Merged at guest execution-context build time only —
     * never bound to the agent's system-managed default instance, which is
     * recreated empty on lifecycle events and shared by every org member's
     * casual chat (decision 011). Org-audience shares reject this field via
     * the message-level CEL rule: member sessions carry no share linkage in
     * Phase A, so bound credentials would silently never apply.
     * </pre>
     *
     * <code>repeated .ai.stigmer.commons.apiresource.ApiResourceReference environment_refs = 6 [json_name = "environmentRefs", (.buf.validate.field) = { ... }</code>
     */
    public java.util.List<ai.stigmer.commons.apiresource.ApiResourceReference.Builder> 
         getEnvironmentRefsBuilderList() {
      return internalGetEnvironmentRefsFieldBuilder().getBuilderList();
    }
    private com.google.protobuf.RepeatedFieldBuilder<
        ai.stigmer.commons.apiresource.ApiResourceReference, ai.stigmer.commons.apiresource.ApiResourceReference.Builder, ai.stigmer.commons.apiresource.ApiResourceReferenceOrBuilder> 
        internalGetEnvironmentRefsFieldBuilder() {
      if (environmentRefsBuilder_ == null) {
        environmentRefsBuilder_ = new com.google.protobuf.RepeatedFieldBuilder<
            ai.stigmer.commons.apiresource.ApiResourceReference, ai.stigmer.commons.apiresource.ApiResourceReference.Builder, ai.stigmer.commons.apiresource.ApiResourceReferenceOrBuilder>(
                environmentRefs_,
                ((bitField0_ & 0x00000020) != 0),
                getParentForChildren(),
                isClean());
        environmentRefs_ = null;
      }
      return environmentRefsBuilder_;
    }

    private ai.stigmer.agentic.agentexecution.v1.RunConfig runConfig_;
    private com.google.protobuf.SingleFieldBuilder<
        ai.stigmer.agentic.agentexecution.v1.RunConfig, ai.stigmer.agentic.agentexecution.v1.RunConfig.Builder, ai.stigmer.agentic.agentexecution.v1.RunConfigOrBuilder> runConfigBuilder_;
    /**
     * <pre>
     * Per-turn model choice and run bounds for guest conversations on this
     * share, overriding the platform's guest execution profile.
     *
     * Unset fields inherit the platform default. model_name replaces the
     * platform model outright, while max_cost_usd and max_tool_rounds can
     * only lower the platform caps — a share owner can reduce what one
     * guest turn may spend, never raise it past the platform profile.
     * Valid on public-audience shares only.
     *
     * &#64;internal
     * DD-018 D-2's second embedder (stigmer/stigmer#360). This is the
     * OWNER's stored config, not the guest's: the guest scope step still
     * discards caller-supplied execution_config unconditionally, then
     * merges this field — loaded server-side from the share — over the
     * platform guest profile (GuestAgentExecutionCreateScopeStep, the
     * promise recorded on GuestExecutionProfileProperties). The trust
     * posture is unchanged. Org-audience shares reject the field via the
     * message-level CEL rule for the same reason as environment_refs:
     * member sessions carry no share linkage, so it would be stored but
     * silently never applied. Enforcement is cloud-only — guest runtime
     * is a cloud concept; OSS stores and echoes the field.
     * </pre>
     *
     * <code>.ai.stigmer.agentic.agentexecution.v1.RunConfig run_config = 7 [json_name = "runConfig"];</code>
     * @return Whether the runConfig field is set.
     */
    public boolean hasRunConfig() {
      return ((bitField0_ & 0x00000040) != 0);
    }
    /**
     * <pre>
     * Per-turn model choice and run bounds for guest conversations on this
     * share, overriding the platform's guest execution profile.
     *
     * Unset fields inherit the platform default. model_name replaces the
     * platform model outright, while max_cost_usd and max_tool_rounds can
     * only lower the platform caps — a share owner can reduce what one
     * guest turn may spend, never raise it past the platform profile.
     * Valid on public-audience shares only.
     *
     * &#64;internal
     * DD-018 D-2's second embedder (stigmer/stigmer#360). This is the
     * OWNER's stored config, not the guest's: the guest scope step still
     * discards caller-supplied execution_config unconditionally, then
     * merges this field — loaded server-side from the share — over the
     * platform guest profile (GuestAgentExecutionCreateScopeStep, the
     * promise recorded on GuestExecutionProfileProperties). The trust
     * posture is unchanged. Org-audience shares reject the field via the
     * message-level CEL rule for the same reason as environment_refs:
     * member sessions carry no share linkage, so it would be stored but
     * silently never applied. Enforcement is cloud-only — guest runtime
     * is a cloud concept; OSS stores and echoes the field.
     * </pre>
     *
     * <code>.ai.stigmer.agentic.agentexecution.v1.RunConfig run_config = 7 [json_name = "runConfig"];</code>
     * @return The runConfig.
     */
    public ai.stigmer.agentic.agentexecution.v1.RunConfig getRunConfig() {
      if (runConfigBuilder_ == null) {
        return runConfig_ == null ? ai.stigmer.agentic.agentexecution.v1.RunConfig.getDefaultInstance() : runConfig_;
      } else {
        return runConfigBuilder_.getMessage();
      }
    }
    /**
     * <pre>
     * Per-turn model choice and run bounds for guest conversations on this
     * share, overriding the platform's guest execution profile.
     *
     * Unset fields inherit the platform default. model_name replaces the
     * platform model outright, while max_cost_usd and max_tool_rounds can
     * only lower the platform caps — a share owner can reduce what one
     * guest turn may spend, never raise it past the platform profile.
     * Valid on public-audience shares only.
     *
     * &#64;internal
     * DD-018 D-2's second embedder (stigmer/stigmer#360). This is the
     * OWNER's stored config, not the guest's: the guest scope step still
     * discards caller-supplied execution_config unconditionally, then
     * merges this field — loaded server-side from the share — over the
     * platform guest profile (GuestAgentExecutionCreateScopeStep, the
     * promise recorded on GuestExecutionProfileProperties). The trust
     * posture is unchanged. Org-audience shares reject the field via the
     * message-level CEL rule for the same reason as environment_refs:
     * member sessions carry no share linkage, so it would be stored but
     * silently never applied. Enforcement is cloud-only — guest runtime
     * is a cloud concept; OSS stores and echoes the field.
     * </pre>
     *
     * <code>.ai.stigmer.agentic.agentexecution.v1.RunConfig run_config = 7 [json_name = "runConfig"];</code>
     */
    public Builder setRunConfig(ai.stigmer.agentic.agentexecution.v1.RunConfig value) {
      if (runConfigBuilder_ == null) {
        if (value == null) {
          throw new NullPointerException();
        }
        runConfig_ = value;
      } else {
        runConfigBuilder_.setMessage(value);
      }
      bitField0_ |= 0x00000040;
      onChanged();
      return this;
    }
    /**
     * <pre>
     * Per-turn model choice and run bounds for guest conversations on this
     * share, overriding the platform's guest execution profile.
     *
     * Unset fields inherit the platform default. model_name replaces the
     * platform model outright, while max_cost_usd and max_tool_rounds can
     * only lower the platform caps — a share owner can reduce what one
     * guest turn may spend, never raise it past the platform profile.
     * Valid on public-audience shares only.
     *
     * &#64;internal
     * DD-018 D-2's second embedder (stigmer/stigmer#360). This is the
     * OWNER's stored config, not the guest's: the guest scope step still
     * discards caller-supplied execution_config unconditionally, then
     * merges this field — loaded server-side from the share — over the
     * platform guest profile (GuestAgentExecutionCreateScopeStep, the
     * promise recorded on GuestExecutionProfileProperties). The trust
     * posture is unchanged. Org-audience shares reject the field via the
     * message-level CEL rule for the same reason as environment_refs:
     * member sessions carry no share linkage, so it would be stored but
     * silently never applied. Enforcement is cloud-only — guest runtime
     * is a cloud concept; OSS stores and echoes the field.
     * </pre>
     *
     * <code>.ai.stigmer.agentic.agentexecution.v1.RunConfig run_config = 7 [json_name = "runConfig"];</code>
     */
    public Builder setRunConfig(
        ai.stigmer.agentic.agentexecution.v1.RunConfig.Builder builderForValue) {
      if (runConfigBuilder_ == null) {
        runConfig_ = builderForValue.build();
      } else {
        runConfigBuilder_.setMessage(builderForValue.build());
      }
      bitField0_ |= 0x00000040;
      onChanged();
      return this;
    }
    /**
     * <pre>
     * Per-turn model choice and run bounds for guest conversations on this
     * share, overriding the platform's guest execution profile.
     *
     * Unset fields inherit the platform default. model_name replaces the
     * platform model outright, while max_cost_usd and max_tool_rounds can
     * only lower the platform caps — a share owner can reduce what one
     * guest turn may spend, never raise it past the platform profile.
     * Valid on public-audience shares only.
     *
     * &#64;internal
     * DD-018 D-2's second embedder (stigmer/stigmer#360). This is the
     * OWNER's stored config, not the guest's: the guest scope step still
     * discards caller-supplied execution_config unconditionally, then
     * merges this field — loaded server-side from the share — over the
     * platform guest profile (GuestAgentExecutionCreateScopeStep, the
     * promise recorded on GuestExecutionProfileProperties). The trust
     * posture is unchanged. Org-audience shares reject the field via the
     * message-level CEL rule for the same reason as environment_refs:
     * member sessions carry no share linkage, so it would be stored but
     * silently never applied. Enforcement is cloud-only — guest runtime
     * is a cloud concept; OSS stores and echoes the field.
     * </pre>
     *
     * <code>.ai.stigmer.agentic.agentexecution.v1.RunConfig run_config = 7 [json_name = "runConfig"];</code>
     */
    public Builder mergeRunConfig(ai.stigmer.agentic.agentexecution.v1.RunConfig value) {
      if (runConfigBuilder_ == null) {
        if (((bitField0_ & 0x00000040) != 0) &&
          runConfig_ != null &&
          runConfig_ != ai.stigmer.agentic.agentexecution.v1.RunConfig.getDefaultInstance()) {
          getRunConfigBuilder().mergeFrom(value);
        } else {
          runConfig_ = value;
        }
      } else {
        runConfigBuilder_.mergeFrom(value);
      }
      if (runConfig_ != null) {
        bitField0_ |= 0x00000040;
        onChanged();
      }
      return this;
    }
    /**
     * <pre>
     * Per-turn model choice and run bounds for guest conversations on this
     * share, overriding the platform's guest execution profile.
     *
     * Unset fields inherit the platform default. model_name replaces the
     * platform model outright, while max_cost_usd and max_tool_rounds can
     * only lower the platform caps — a share owner can reduce what one
     * guest turn may spend, never raise it past the platform profile.
     * Valid on public-audience shares only.
     *
     * &#64;internal
     * DD-018 D-2's second embedder (stigmer/stigmer#360). This is the
     * OWNER's stored config, not the guest's: the guest scope step still
     * discards caller-supplied execution_config unconditionally, then
     * merges this field — loaded server-side from the share — over the
     * platform guest profile (GuestAgentExecutionCreateScopeStep, the
     * promise recorded on GuestExecutionProfileProperties). The trust
     * posture is unchanged. Org-audience shares reject the field via the
     * message-level CEL rule for the same reason as environment_refs:
     * member sessions carry no share linkage, so it would be stored but
     * silently never applied. Enforcement is cloud-only — guest runtime
     * is a cloud concept; OSS stores and echoes the field.
     * </pre>
     *
     * <code>.ai.stigmer.agentic.agentexecution.v1.RunConfig run_config = 7 [json_name = "runConfig"];</code>
     */
    public Builder clearRunConfig() {
      bitField0_ = (bitField0_ & ~0x00000040);
      runConfig_ = null;
      if (runConfigBuilder_ != null) {
        runConfigBuilder_.dispose();
        runConfigBuilder_ = null;
      }
      onChanged();
      return this;
    }
    /**
     * <pre>
     * Per-turn model choice and run bounds for guest conversations on this
     * share, overriding the platform's guest execution profile.
     *
     * Unset fields inherit the platform default. model_name replaces the
     * platform model outright, while max_cost_usd and max_tool_rounds can
     * only lower the platform caps — a share owner can reduce what one
     * guest turn may spend, never raise it past the platform profile.
     * Valid on public-audience shares only.
     *
     * &#64;internal
     * DD-018 D-2's second embedder (stigmer/stigmer#360). This is the
     * OWNER's stored config, not the guest's: the guest scope step still
     * discards caller-supplied execution_config unconditionally, then
     * merges this field — loaded server-side from the share — over the
     * platform guest profile (GuestAgentExecutionCreateScopeStep, the
     * promise recorded on GuestExecutionProfileProperties). The trust
     * posture is unchanged. Org-audience shares reject the field via the
     * message-level CEL rule for the same reason as environment_refs:
     * member sessions carry no share linkage, so it would be stored but
     * silently never applied. Enforcement is cloud-only — guest runtime
     * is a cloud concept; OSS stores and echoes the field.
     * </pre>
     *
     * <code>.ai.stigmer.agentic.agentexecution.v1.RunConfig run_config = 7 [json_name = "runConfig"];</code>
     */
    public ai.stigmer.agentic.agentexecution.v1.RunConfig.Builder getRunConfigBuilder() {
      bitField0_ |= 0x00000040;
      onChanged();
      return internalGetRunConfigFieldBuilder().getBuilder();
    }
    /**
     * <pre>
     * Per-turn model choice and run bounds for guest conversations on this
     * share, overriding the platform's guest execution profile.
     *
     * Unset fields inherit the platform default. model_name replaces the
     * platform model outright, while max_cost_usd and max_tool_rounds can
     * only lower the platform caps — a share owner can reduce what one
     * guest turn may spend, never raise it past the platform profile.
     * Valid on public-audience shares only.
     *
     * &#64;internal
     * DD-018 D-2's second embedder (stigmer/stigmer#360). This is the
     * OWNER's stored config, not the guest's: the guest scope step still
     * discards caller-supplied execution_config unconditionally, then
     * merges this field — loaded server-side from the share — over the
     * platform guest profile (GuestAgentExecutionCreateScopeStep, the
     * promise recorded on GuestExecutionProfileProperties). The trust
     * posture is unchanged. Org-audience shares reject the field via the
     * message-level CEL rule for the same reason as environment_refs:
     * member sessions carry no share linkage, so it would be stored but
     * silently never applied. Enforcement is cloud-only — guest runtime
     * is a cloud concept; OSS stores and echoes the field.
     * </pre>
     *
     * <code>.ai.stigmer.agentic.agentexecution.v1.RunConfig run_config = 7 [json_name = "runConfig"];</code>
     */
    public ai.stigmer.agentic.agentexecution.v1.RunConfigOrBuilder getRunConfigOrBuilder() {
      if (runConfigBuilder_ != null) {
        return runConfigBuilder_.getMessageOrBuilder();
      } else {
        return runConfig_ == null ?
            ai.stigmer.agentic.agentexecution.v1.RunConfig.getDefaultInstance() : runConfig_;
      }
    }
    /**
     * <pre>
     * Per-turn model choice and run bounds for guest conversations on this
     * share, overriding the platform's guest execution profile.
     *
     * Unset fields inherit the platform default. model_name replaces the
     * platform model outright, while max_cost_usd and max_tool_rounds can
     * only lower the platform caps — a share owner can reduce what one
     * guest turn may spend, never raise it past the platform profile.
     * Valid on public-audience shares only.
     *
     * &#64;internal
     * DD-018 D-2's second embedder (stigmer/stigmer#360). This is the
     * OWNER's stored config, not the guest's: the guest scope step still
     * discards caller-supplied execution_config unconditionally, then
     * merges this field — loaded server-side from the share — over the
     * platform guest profile (GuestAgentExecutionCreateScopeStep, the
     * promise recorded on GuestExecutionProfileProperties). The trust
     * posture is unchanged. Org-audience shares reject the field via the
     * message-level CEL rule for the same reason as environment_refs:
     * member sessions carry no share linkage, so it would be stored but
     * silently never applied. Enforcement is cloud-only — guest runtime
     * is a cloud concept; OSS stores and echoes the field.
     * </pre>
     *
     * <code>.ai.stigmer.agentic.agentexecution.v1.RunConfig run_config = 7 [json_name = "runConfig"];</code>
     */
    private com.google.protobuf.SingleFieldBuilder<
        ai.stigmer.agentic.agentexecution.v1.RunConfig, ai.stigmer.agentic.agentexecution.v1.RunConfig.Builder, ai.stigmer.agentic.agentexecution.v1.RunConfigOrBuilder> 
        internalGetRunConfigFieldBuilder() {
      if (runConfigBuilder_ == null) {
        runConfigBuilder_ = new com.google.protobuf.SingleFieldBuilder<
            ai.stigmer.agentic.agentexecution.v1.RunConfig, ai.stigmer.agentic.agentexecution.v1.RunConfig.Builder, ai.stigmer.agentic.agentexecution.v1.RunConfigOrBuilder>(
                getRunConfig(),
                getParentForChildren(),
                isClean());
        runConfig_ = null;
      }
      return runConfigBuilder_;
    }

    // @@protoc_insertion_point(builder_scope:ai.stigmer.agentic.agentshare.v1.AgentShareSpec)
  }

  // @@protoc_insertion_point(class_scope:ai.stigmer.agentic.agentshare.v1.AgentShareSpec)
  private static final ai.stigmer.agentic.agentshare.v1.AgentShareSpec DEFAULT_INSTANCE;
  static {
    DEFAULT_INSTANCE = new ai.stigmer.agentic.agentshare.v1.AgentShareSpec();
  }

  public static ai.stigmer.agentic.agentshare.v1.AgentShareSpec getDefaultInstance() {
    return DEFAULT_INSTANCE;
  }

  private static final com.google.protobuf.Parser<AgentShareSpec>
      PARSER = new com.google.protobuf.AbstractParser<AgentShareSpec>() {
    @java.lang.Override
    public AgentShareSpec parsePartialFrom(
        com.google.protobuf.CodedInputStream input,
        com.google.protobuf.ExtensionRegistryLite extensionRegistry)
        throws com.google.protobuf.InvalidProtocolBufferException {
      Builder builder = newBuilder();
      try {
        builder.mergeFrom(input, extensionRegistry);
      } catch (com.google.protobuf.InvalidProtocolBufferException e) {
        throw e.setUnfinishedMessage(builder.buildPartial());
      } catch (com.google.protobuf.UninitializedMessageException e) {
        throw e.asInvalidProtocolBufferException().setUnfinishedMessage(builder.buildPartial());
      } catch (java.io.IOException e) {
        throw new com.google.protobuf.InvalidProtocolBufferException(e)
            .setUnfinishedMessage(builder.buildPartial());
      }
      return builder.buildPartial();
    }
  };

  public static com.google.protobuf.Parser<AgentShareSpec> parser() {
    return PARSER;
  }

  @java.lang.Override
  public com.google.protobuf.Parser<AgentShareSpec> getParserForType() {
    return PARSER;
  }

  @java.lang.Override
  public ai.stigmer.agentic.agentshare.v1.AgentShareSpec getDefaultInstanceForType() {
    return DEFAULT_INSTANCE;
  }

}

