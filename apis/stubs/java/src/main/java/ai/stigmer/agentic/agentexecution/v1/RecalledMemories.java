// Generated by the protocol buffer compiler.  DO NOT EDIT!
// NO CHECKED-IN PROTOBUF GENCODE
// source: ai/stigmer/agentic/agentexecution/v1/spec.proto
// Protobuf Java Version: 4.34.0

package ai.stigmer.agentic.agentexecution.v1;

/**
 * <pre>
 * RecalledMemories is the server-composed snapshot of the caller's
 * confirmed memories for one execution — the candidate set for prompt
 * injection.
 *
 * &#64;internal
 * See the recalled_memories field comment for the full ownership and
 * composition contract, and status.recalled_memories_report for which
 * candidates the runner actually injected (DD-008 D5). enabled=true with
 * zero facts is a meaningful state: memory is on for this caller, nothing
 * is stored yet — the runner still offers the remember tool (DD-005 D1).
 * </pre>
 *
 * Protobuf type {@code ai.stigmer.agentic.agentexecution.v1.RecalledMemories}
 */
@com.google.protobuf.Generated
public final class RecalledMemories extends
    com.google.protobuf.GeneratedMessage implements
    // @@protoc_insertion_point(message_implements:ai.stigmer.agentic.agentexecution.v1.RecalledMemories)
    RecalledMemoriesOrBuilder {
private static final long serialVersionUID = 0L;
  static {
    com.google.protobuf.RuntimeVersion.validateProtobufGencodeVersion(
      com.google.protobuf.RuntimeVersion.RuntimeDomain.PUBLIC,
      /* major= */ 4,
      /* minor= */ 34,
      /* patch= */ 0,
      /* suffix= */ "",
      "RecalledMemories");
  }
  // Use RecalledMemories.newBuilder() to construct.
  private RecalledMemories(com.google.protobuf.GeneratedMessage.Builder<?> builder) {
    super(builder);
  }
  private RecalledMemories() {
    facts_ = java.util.Collections.emptyList();
  }

  public static final com.google.protobuf.Descriptors.Descriptor
      getDescriptor() {
    return ai.stigmer.agentic.agentexecution.v1.SpecProto.internal_static_ai_stigmer_agentic_agentexecution_v1_RecalledMemories_descriptor;
  }

  @java.lang.Override
  public com.google.protobuf.Descriptors.Descriptor getDescriptorForType() {
    return ai.stigmer.agentic.agentexecution.v1.SpecProto.internal_static_ai_stigmer_agentic_agentexecution_v1_RecalledMemories_descriptor;
  }

  @java.lang.Override
  protected com.google.protobuf.GeneratedMessage.FieldAccessorTable
      internalGetFieldAccessorTable() {
    return ai.stigmer.agentic.agentexecution.v1.SpecProto.internal_static_ai_stigmer_agentic_agentexecution_v1_RecalledMemories_fieldAccessorTable
        .ensureFieldAccessorsInitialized(
            ai.stigmer.agentic.agentexecution.v1.RecalledMemories.class, ai.stigmer.agentic.agentexecution.v1.RecalledMemories.Builder.class);
  }

  public static final int ENABLED_FIELD_NUMBER = 1;
  private boolean enabled_ = false;
  /**
   * <pre>
   * Whether memory is enabled for this execution's caller.
   * </pre>
   *
   * <code>bool enabled = 1 [json_name = "enabled"];</code>
   * @return The enabled.
   */
  @java.lang.Override
  public boolean getEnabled() {
    return enabled_;
  }

  public static final int FACTS_FIELD_NUMBER = 2;
  @SuppressWarnings("serial")
  private java.util.List<ai.stigmer.agentic.agentexecution.v1.RecalledMemoryFact> facts_;
  /**
   * <pre>
   * The confirmed facts — every confirmed memory of the caller in this
   * organization, the candidate set for prompt injection.
   *
   * &#64;internal
   * The compose steps stamp EVERY confirmed fact, never a compose-time
   * truncation (DD-006 D5 as revised by DD-008): this is the auditable
   * CANDIDATE set, not necessarily the injected set. Above the
   * retriever's activation threshold the runner selects the most
   * relevant subset at prompt build and records it in
   * status.recalled_memories_report (DD-008 D3/D5); at or below the
   * threshold, or with no embeddings-capable provider, injection is
   * wholesale and the report (when present) says so. Selection is
   * recorded, never silent: what the model saw always equals what the
   * user can audit — this snapshot joined to the report.
   * </pre>
   *
   * <code>repeated .ai.stigmer.agentic.agentexecution.v1.RecalledMemoryFact facts = 2 [json_name = "facts"];</code>
   */
  @java.lang.Override
  public java.util.List<ai.stigmer.agentic.agentexecution.v1.RecalledMemoryFact> getFactsList() {
    return facts_;
  }
  /**
   * <pre>
   * The confirmed facts — every confirmed memory of the caller in this
   * organization, the candidate set for prompt injection.
   *
   * &#64;internal
   * The compose steps stamp EVERY confirmed fact, never a compose-time
   * truncation (DD-006 D5 as revised by DD-008): this is the auditable
   * CANDIDATE set, not necessarily the injected set. Above the
   * retriever's activation threshold the runner selects the most
   * relevant subset at prompt build and records it in
   * status.recalled_memories_report (DD-008 D3/D5); at or below the
   * threshold, or with no embeddings-capable provider, injection is
   * wholesale and the report (when present) says so. Selection is
   * recorded, never silent: what the model saw always equals what the
   * user can audit — this snapshot joined to the report.
   * </pre>
   *
   * <code>repeated .ai.stigmer.agentic.agentexecution.v1.RecalledMemoryFact facts = 2 [json_name = "facts"];</code>
   */
  @java.lang.Override
  public java.util.List<? extends ai.stigmer.agentic.agentexecution.v1.RecalledMemoryFactOrBuilder> 
      getFactsOrBuilderList() {
    return facts_;
  }
  /**
   * <pre>
   * The confirmed facts — every confirmed memory of the caller in this
   * organization, the candidate set for prompt injection.
   *
   * &#64;internal
   * The compose steps stamp EVERY confirmed fact, never a compose-time
   * truncation (DD-006 D5 as revised by DD-008): this is the auditable
   * CANDIDATE set, not necessarily the injected set. Above the
   * retriever's activation threshold the runner selects the most
   * relevant subset at prompt build and records it in
   * status.recalled_memories_report (DD-008 D3/D5); at or below the
   * threshold, or with no embeddings-capable provider, injection is
   * wholesale and the report (when present) says so. Selection is
   * recorded, never silent: what the model saw always equals what the
   * user can audit — this snapshot joined to the report.
   * </pre>
   *
   * <code>repeated .ai.stigmer.agentic.agentexecution.v1.RecalledMemoryFact facts = 2 [json_name = "facts"];</code>
   */
  @java.lang.Override
  public int getFactsCount() {
    return facts_.size();
  }
  /**
   * <pre>
   * The confirmed facts — every confirmed memory of the caller in this
   * organization, the candidate set for prompt injection.
   *
   * &#64;internal
   * The compose steps stamp EVERY confirmed fact, never a compose-time
   * truncation (DD-006 D5 as revised by DD-008): this is the auditable
   * CANDIDATE set, not necessarily the injected set. Above the
   * retriever's activation threshold the runner selects the most
   * relevant subset at prompt build and records it in
   * status.recalled_memories_report (DD-008 D3/D5); at or below the
   * threshold, or with no embeddings-capable provider, injection is
   * wholesale and the report (when present) says so. Selection is
   * recorded, never silent: what the model saw always equals what the
   * user can audit — this snapshot joined to the report.
   * </pre>
   *
   * <code>repeated .ai.stigmer.agentic.agentexecution.v1.RecalledMemoryFact facts = 2 [json_name = "facts"];</code>
   */
  @java.lang.Override
  public ai.stigmer.agentic.agentexecution.v1.RecalledMemoryFact getFacts(int index) {
    return facts_.get(index);
  }
  /**
   * <pre>
   * The confirmed facts — every confirmed memory of the caller in this
   * organization, the candidate set for prompt injection.
   *
   * &#64;internal
   * The compose steps stamp EVERY confirmed fact, never a compose-time
   * truncation (DD-006 D5 as revised by DD-008): this is the auditable
   * CANDIDATE set, not necessarily the injected set. Above the
   * retriever's activation threshold the runner selects the most
   * relevant subset at prompt build and records it in
   * status.recalled_memories_report (DD-008 D3/D5); at or below the
   * threshold, or with no embeddings-capable provider, injection is
   * wholesale and the report (when present) says so. Selection is
   * recorded, never silent: what the model saw always equals what the
   * user can audit — this snapshot joined to the report.
   * </pre>
   *
   * <code>repeated .ai.stigmer.agentic.agentexecution.v1.RecalledMemoryFact facts = 2 [json_name = "facts"];</code>
   */
  @java.lang.Override
  public ai.stigmer.agentic.agentexecution.v1.RecalledMemoryFactOrBuilder getFactsOrBuilder(
      int index) {
    return facts_.get(index);
  }

  private byte memoizedIsInitialized = -1;
  @java.lang.Override
  public final boolean isInitialized() {
    byte isInitialized = memoizedIsInitialized;
    if (isInitialized == 1) return true;
    if (isInitialized == 0) return false;

    memoizedIsInitialized = 1;
    return true;
  }

  @java.lang.Override
  public void writeTo(com.google.protobuf.CodedOutputStream output)
                      throws java.io.IOException {
    if (enabled_ != false) {
      output.writeBool(1, enabled_);
    }
    for (int i = 0; i < facts_.size(); i++) {
      output.writeMessage(2, facts_.get(i));
    }
    getUnknownFields().writeTo(output);
  }

  @java.lang.Override
  public int getSerializedSize() {
    int size = memoizedSize;
    if (size != -1) return size;

    size = 0;
    if (enabled_ != false) {
      size += com.google.protobuf.CodedOutputStream
        .computeBoolSize(1, enabled_);
    }

        {
          final int count = facts_.size();
          for (int i = 0; i < count; i++) {
            size += com.google.protobuf.CodedOutputStream
              .computeMessageSizeNoTag(facts_.get(i));
          }
          size += 1 * count;
        }
    size += getUnknownFields().getSerializedSize();
    memoizedSize = size;
    return size;
  }

  @java.lang.Override
  public boolean equals(final java.lang.Object obj) {
    if (obj == this) {
     return true;
    }
    if (!(obj instanceof ai.stigmer.agentic.agentexecution.v1.RecalledMemories)) {
      return super.equals(obj);
    }
    ai.stigmer.agentic.agentexecution.v1.RecalledMemories other = (ai.stigmer.agentic.agentexecution.v1.RecalledMemories) obj;

    if (getEnabled()
        != other.getEnabled()) return false;
    if (!getFactsList()
        .equals(other.getFactsList())) return false;
    if (!getUnknownFields().equals(other.getUnknownFields())) return false;
    return true;
  }

  @java.lang.Override
  public int hashCode() {
    if (memoizedHashCode != 0) {
      return memoizedHashCode;
    }
    int hash = 41;
    hash = (19 * hash) + getDescriptor().hashCode();
    hash = (37 * hash) + ENABLED_FIELD_NUMBER;
    hash = (53 * hash) + com.google.protobuf.Internal.hashBoolean(
        getEnabled());
    if (getFactsCount() > 0) {
      hash = (37 * hash) + FACTS_FIELD_NUMBER;
      hash = (53 * hash) + getFactsList().hashCode();
    }
    hash = (29 * hash) + getUnknownFields().hashCode();
    memoizedHashCode = hash;
    return hash;
  }

  public static ai.stigmer.agentic.agentexecution.v1.RecalledMemories parseFrom(
      java.nio.ByteBuffer data)
      throws com.google.protobuf.InvalidProtocolBufferException {
    return PARSER.parseFrom(data);
  }
  public static ai.stigmer.agentic.agentexecution.v1.RecalledMemories parseFrom(
      java.nio.ByteBuffer data,
      com.google.protobuf.ExtensionRegistryLite extensionRegistry)
      throws com.google.protobuf.InvalidProtocolBufferException {
    return PARSER.parseFrom(data, extensionRegistry);
  }
  public static ai.stigmer.agentic.agentexecution.v1.RecalledMemories parseFrom(
      com.google.protobuf.ByteString data)
      throws com.google.protobuf.InvalidProtocolBufferException {
    return PARSER.parseFrom(data);
  }
  public static ai.stigmer.agentic.agentexecution.v1.RecalledMemories parseFrom(
      com.google.protobuf.ByteString data,
      com.google.protobuf.ExtensionRegistryLite extensionRegistry)
      throws com.google.protobuf.InvalidProtocolBufferException {
    return PARSER.parseFrom(data, extensionRegistry);
  }
  public static ai.stigmer.agentic.agentexecution.v1.RecalledMemories parseFrom(byte[] data)
      throws com.google.protobuf.InvalidProtocolBufferException {
    return PARSER.parseFrom(data);
  }
  public static ai.stigmer.agentic.agentexecution.v1.RecalledMemories parseFrom(
      byte[] data,
      com.google.protobuf.ExtensionRegistryLite extensionRegistry)
      throws com.google.protobuf.InvalidProtocolBufferException {
    return PARSER.parseFrom(data, extensionRegistry);
  }
  public static ai.stigmer.agentic.agentexecution.v1.RecalledMemories parseFrom(java.io.InputStream input)
      throws java.io.IOException {
    return com.google.protobuf.GeneratedMessage
        .parseWithIOException(PARSER, input);
  }
  public static ai.stigmer.agentic.agentexecution.v1.RecalledMemories parseFrom(
      java.io.InputStream input,
      com.google.protobuf.ExtensionRegistryLite extensionRegistry)
      throws java.io.IOException {
    return com.google.protobuf.GeneratedMessage
        .parseWithIOException(PARSER, input, extensionRegistry);
  }

  public static ai.stigmer.agentic.agentexecution.v1.RecalledMemories parseDelimitedFrom(java.io.InputStream input)
      throws java.io.IOException {
    return com.google.protobuf.GeneratedMessage
        .parseDelimitedWithIOException(PARSER, input);
  }

  public static ai.stigmer.agentic.agentexecution.v1.RecalledMemories parseDelimitedFrom(
      java.io.InputStream input,
      com.google.protobuf.ExtensionRegistryLite extensionRegistry)
      throws java.io.IOException {
    return com.google.protobuf.GeneratedMessage
        .parseDelimitedWithIOException(PARSER, input, extensionRegistry);
  }
  public static ai.stigmer.agentic.agentexecution.v1.RecalledMemories parseFrom(
      com.google.protobuf.CodedInputStream input)
      throws java.io.IOException {
    return com.google.protobuf.GeneratedMessage
        .parseWithIOException(PARSER, input);
  }
  public static ai.stigmer.agentic.agentexecution.v1.RecalledMemories parseFrom(
      com.google.protobuf.CodedInputStream input,
      com.google.protobuf.ExtensionRegistryLite extensionRegistry)
      throws java.io.IOException {
    return com.google.protobuf.GeneratedMessage
        .parseWithIOException(PARSER, input, extensionRegistry);
  }

  @java.lang.Override
  public Builder newBuilderForType() { return newBuilder(); }
  public static Builder newBuilder() {
    return DEFAULT_INSTANCE.toBuilder();
  }
  public static Builder newBuilder(ai.stigmer.agentic.agentexecution.v1.RecalledMemories prototype) {
    return DEFAULT_INSTANCE.toBuilder().mergeFrom(prototype);
  }
  @java.lang.Override
  public Builder toBuilder() {
    return this == DEFAULT_INSTANCE
        ? new Builder() : new Builder().mergeFrom(this);
  }

  @java.lang.Override
  protected Builder newBuilderForType(
      com.google.protobuf.GeneratedMessage.BuilderParent parent) {
    Builder builder = new Builder(parent);
    return builder;
  }
  /**
   * <pre>
   * RecalledMemories is the server-composed snapshot of the caller's
   * confirmed memories for one execution — the candidate set for prompt
   * injection.
   *
   * &#64;internal
   * See the recalled_memories field comment for the full ownership and
   * composition contract, and status.recalled_memories_report for which
   * candidates the runner actually injected (DD-008 D5). enabled=true with
   * zero facts is a meaningful state: memory is on for this caller, nothing
   * is stored yet — the runner still offers the remember tool (DD-005 D1).
   * </pre>
   *
   * Protobuf type {@code ai.stigmer.agentic.agentexecution.v1.RecalledMemories}
   */
  public static final class Builder extends
      com.google.protobuf.GeneratedMessage.Builder<Builder> implements
      // @@protoc_insertion_point(builder_implements:ai.stigmer.agentic.agentexecution.v1.RecalledMemories)
      ai.stigmer.agentic.agentexecution.v1.RecalledMemoriesOrBuilder {
    public static final com.google.protobuf.Descriptors.Descriptor
        getDescriptor() {
      return ai.stigmer.agentic.agentexecution.v1.SpecProto.internal_static_ai_stigmer_agentic_agentexecution_v1_RecalledMemories_descriptor;
    }

    @java.lang.Override
    protected com.google.protobuf.GeneratedMessage.FieldAccessorTable
        internalGetFieldAccessorTable() {
      return ai.stigmer.agentic.agentexecution.v1.SpecProto.internal_static_ai_stigmer_agentic_agentexecution_v1_RecalledMemories_fieldAccessorTable
          .ensureFieldAccessorsInitialized(
              ai.stigmer.agentic.agentexecution.v1.RecalledMemories.class, ai.stigmer.agentic.agentexecution.v1.RecalledMemories.Builder.class);
    }

    // Construct using ai.stigmer.agentic.agentexecution.v1.RecalledMemories.newBuilder()
    private Builder() {

    }

    private Builder(
        com.google.protobuf.GeneratedMessage.BuilderParent parent) {
      super(parent);

    }
    @java.lang.Override
    public Builder clear() {
      super.clear();
      bitField0_ = 0;
      enabled_ = false;
      if (factsBuilder_ == null) {
        facts_ = java.util.Collections.emptyList();
      } else {
        facts_ = null;
        factsBuilder_.clear();
      }
      bitField0_ = (bitField0_ & ~0x00000002);
      return this;
    }

    @java.lang.Override
    public com.google.protobuf.Descriptors.Descriptor
        getDescriptorForType() {
      return ai.stigmer.agentic.agentexecution.v1.SpecProto.internal_static_ai_stigmer_agentic_agentexecution_v1_RecalledMemories_descriptor;
    }

    @java.lang.Override
    public ai.stigmer.agentic.agentexecution.v1.RecalledMemories getDefaultInstanceForType() {
      return ai.stigmer.agentic.agentexecution.v1.RecalledMemories.getDefaultInstance();
    }

    @java.lang.Override
    public ai.stigmer.agentic.agentexecution.v1.RecalledMemories build() {
      ai.stigmer.agentic.agentexecution.v1.RecalledMemories result = buildPartial();
      if (!result.isInitialized()) {
        throw newUninitializedMessageException(result);
      }
      return result;
    }

    @java.lang.Override
    public ai.stigmer.agentic.agentexecution.v1.RecalledMemories buildPartial() {
      ai.stigmer.agentic.agentexecution.v1.RecalledMemories result = new ai.stigmer.agentic.agentexecution.v1.RecalledMemories(this);
      buildPartialRepeatedFields(result);
      if (bitField0_ != 0) { buildPartial0(result); }
      onBuilt();
      return result;
    }

    private void buildPartialRepeatedFields(ai.stigmer.agentic.agentexecution.v1.RecalledMemories result) {
      if (factsBuilder_ == null) {
        if (((bitField0_ & 0x00000002) != 0)) {
          facts_ = java.util.Collections.unmodifiableList(facts_);
          bitField0_ = (bitField0_ & ~0x00000002);
        }
        result.facts_ = facts_;
      } else {
        result.facts_ = factsBuilder_.build();
      }
    }

    private void buildPartial0(ai.stigmer.agentic.agentexecution.v1.RecalledMemories result) {
      int from_bitField0_ = bitField0_;
      if (((from_bitField0_ & 0x00000001) != 0)) {
        result.enabled_ = enabled_;
      }
    }

    @java.lang.Override
    public Builder mergeFrom(com.google.protobuf.Message other) {
      if (other instanceof ai.stigmer.agentic.agentexecution.v1.RecalledMemories) {
        return mergeFrom((ai.stigmer.agentic.agentexecution.v1.RecalledMemories)other);
      } else {
        super.mergeFrom(other);
        return this;
      }
    }

    public Builder mergeFrom(ai.stigmer.agentic.agentexecution.v1.RecalledMemories other) {
      if (other == ai.stigmer.agentic.agentexecution.v1.RecalledMemories.getDefaultInstance()) return this;
      if (other.getEnabled() != false) {
        setEnabled(other.getEnabled());
      }
      if (factsBuilder_ == null) {
        if (!other.facts_.isEmpty()) {
          if (facts_.isEmpty()) {
            facts_ = other.facts_;
            bitField0_ = (bitField0_ & ~0x00000002);
          } else {
            ensureFactsIsMutable();
            facts_.addAll(other.facts_);
          }
          onChanged();
        }
      } else {
        if (!other.facts_.isEmpty()) {
          if (factsBuilder_.isEmpty()) {
            factsBuilder_.dispose();
            factsBuilder_ = null;
            facts_ = other.facts_;
            bitField0_ = (bitField0_ & ~0x00000002);
            factsBuilder_ = 
              com.google.protobuf.GeneratedMessage.alwaysUseFieldBuilders ?
                 internalGetFactsFieldBuilder() : null;
          } else {
            factsBuilder_.addAllMessages(other.facts_);
          }
        }
      }
      this.mergeUnknownFields(other.getUnknownFields());
      onChanged();
      return this;
    }

    @java.lang.Override
    public final boolean isInitialized() {
      return true;
    }

    @java.lang.Override
    public Builder mergeFrom(
        com.google.protobuf.CodedInputStream input,
        com.google.protobuf.ExtensionRegistryLite extensionRegistry)
        throws java.io.IOException {
      if (extensionRegistry == null) {
        throw new java.lang.NullPointerException();
      }
      try {
        boolean done = false;
        while (!done) {
          int tag = input.readTag();
          switch (tag) {
            case 0:
              done = true;
              break;
            case 8: {
              enabled_ = input.readBool();
              bitField0_ |= 0x00000001;
              break;
            } // case 8
            case 18: {
              ai.stigmer.agentic.agentexecution.v1.RecalledMemoryFact m =
                  input.readMessage(
                      ai.stigmer.agentic.agentexecution.v1.RecalledMemoryFact.parser(),
                      extensionRegistry);
              if (factsBuilder_ == null) {
                ensureFactsIsMutable();
                facts_.add(m);
              } else {
                factsBuilder_.addMessage(m);
              }
              break;
            } // case 18
            default: {
              if (!super.parseUnknownField(input, extensionRegistry, tag)) {
                done = true; // was an endgroup tag
              }
              break;
            } // default:
          } // switch (tag)
        } // while (!done)
      } catch (com.google.protobuf.InvalidProtocolBufferException e) {
        throw e.unwrapIOException();
      } finally {
        onChanged();
      } // finally
      return this;
    }
    private int bitField0_;

    private boolean enabled_ ;
    /**
     * <pre>
     * Whether memory is enabled for this execution's caller.
     * </pre>
     *
     * <code>bool enabled = 1 [json_name = "enabled"];</code>
     * @return The enabled.
     */
    @java.lang.Override
    public boolean getEnabled() {
      return enabled_;
    }
    /**
     * <pre>
     * Whether memory is enabled for this execution's caller.
     * </pre>
     *
     * <code>bool enabled = 1 [json_name = "enabled"];</code>
     * @param value The enabled to set.
     * @return This builder for chaining.
     */
    public Builder setEnabled(boolean value) {

      enabled_ = value;
      bitField0_ |= 0x00000001;
      onChanged();
      return this;
    }
    /**
     * <pre>
     * Whether memory is enabled for this execution's caller.
     * </pre>
     *
     * <code>bool enabled = 1 [json_name = "enabled"];</code>
     * @return This builder for chaining.
     */
    public Builder clearEnabled() {
      bitField0_ = (bitField0_ & ~0x00000001);
      enabled_ = false;
      onChanged();
      return this;
    }

    private java.util.List<ai.stigmer.agentic.agentexecution.v1.RecalledMemoryFact> facts_ =
      java.util.Collections.emptyList();
    private void ensureFactsIsMutable() {
      if (!((bitField0_ & 0x00000002) != 0)) {
        facts_ = new java.util.ArrayList<ai.stigmer.agentic.agentexecution.v1.RecalledMemoryFact>(facts_);
        bitField0_ |= 0x00000002;
       }
    }

    private com.google.protobuf.RepeatedFieldBuilder<
        ai.stigmer.agentic.agentexecution.v1.RecalledMemoryFact, ai.stigmer.agentic.agentexecution.v1.RecalledMemoryFact.Builder, ai.stigmer.agentic.agentexecution.v1.RecalledMemoryFactOrBuilder> factsBuilder_;

    /**
     * <pre>
     * The confirmed facts — every confirmed memory of the caller in this
     * organization, the candidate set for prompt injection.
     *
     * &#64;internal
     * The compose steps stamp EVERY confirmed fact, never a compose-time
     * truncation (DD-006 D5 as revised by DD-008): this is the auditable
     * CANDIDATE set, not necessarily the injected set. Above the
     * retriever's activation threshold the runner selects the most
     * relevant subset at prompt build and records it in
     * status.recalled_memories_report (DD-008 D3/D5); at or below the
     * threshold, or with no embeddings-capable provider, injection is
     * wholesale and the report (when present) says so. Selection is
     * recorded, never silent: what the model saw always equals what the
     * user can audit — this snapshot joined to the report.
     * </pre>
     *
     * <code>repeated .ai.stigmer.agentic.agentexecution.v1.RecalledMemoryFact facts = 2 [json_name = "facts"];</code>
     */
    public java.util.List<ai.stigmer.agentic.agentexecution.v1.RecalledMemoryFact> getFactsList() {
      if (factsBuilder_ == null) {
        return java.util.Collections.unmodifiableList(facts_);
      } else {
        return factsBuilder_.getMessageList();
      }
    }
    /**
     * <pre>
     * The confirmed facts — every confirmed memory of the caller in this
     * organization, the candidate set for prompt injection.
     *
     * &#64;internal
     * The compose steps stamp EVERY confirmed fact, never a compose-time
     * truncation (DD-006 D5 as revised by DD-008): this is the auditable
     * CANDIDATE set, not necessarily the injected set. Above the
     * retriever's activation threshold the runner selects the most
     * relevant subset at prompt build and records it in
     * status.recalled_memories_report (DD-008 D3/D5); at or below the
     * threshold, or with no embeddings-capable provider, injection is
     * wholesale and the report (when present) says so. Selection is
     * recorded, never silent: what the model saw always equals what the
     * user can audit — this snapshot joined to the report.
     * </pre>
     *
     * <code>repeated .ai.stigmer.agentic.agentexecution.v1.RecalledMemoryFact facts = 2 [json_name = "facts"];</code>
     */
    public int getFactsCount() {
      if (factsBuilder_ == null) {
        return facts_.size();
      } else {
        return factsBuilder_.getCount();
      }
    }
    /**
     * <pre>
     * The confirmed facts — every confirmed memory of the caller in this
     * organization, the candidate set for prompt injection.
     *
     * &#64;internal
     * The compose steps stamp EVERY confirmed fact, never a compose-time
     * truncation (DD-006 D5 as revised by DD-008): this is the auditable
     * CANDIDATE set, not necessarily the injected set. Above the
     * retriever's activation threshold the runner selects the most
     * relevant subset at prompt build and records it in
     * status.recalled_memories_report (DD-008 D3/D5); at or below the
     * threshold, or with no embeddings-capable provider, injection is
     * wholesale and the report (when present) says so. Selection is
     * recorded, never silent: what the model saw always equals what the
     * user can audit — this snapshot joined to the report.
     * </pre>
     *
     * <code>repeated .ai.stigmer.agentic.agentexecution.v1.RecalledMemoryFact facts = 2 [json_name = "facts"];</code>
     */
    public ai.stigmer.agentic.agentexecution.v1.RecalledMemoryFact getFacts(int index) {
      if (factsBuilder_ == null) {
        return facts_.get(index);
      } else {
        return factsBuilder_.getMessage(index);
      }
    }
    /**
     * <pre>
     * The confirmed facts — every confirmed memory of the caller in this
     * organization, the candidate set for prompt injection.
     *
     * &#64;internal
     * The compose steps stamp EVERY confirmed fact, never a compose-time
     * truncation (DD-006 D5 as revised by DD-008): this is the auditable
     * CANDIDATE set, not necessarily the injected set. Above the
     * retriever's activation threshold the runner selects the most
     * relevant subset at prompt build and records it in
     * status.recalled_memories_report (DD-008 D3/D5); at or below the
     * threshold, or with no embeddings-capable provider, injection is
     * wholesale and the report (when present) says so. Selection is
     * recorded, never silent: what the model saw always equals what the
     * user can audit — this snapshot joined to the report.
     * </pre>
     *
     * <code>repeated .ai.stigmer.agentic.agentexecution.v1.RecalledMemoryFact facts = 2 [json_name = "facts"];</code>
     */
    public Builder setFacts(
        int index, ai.stigmer.agentic.agentexecution.v1.RecalledMemoryFact value) {
      if (factsBuilder_ == null) {
        if (value == null) {
          throw new NullPointerException();
        }
        ensureFactsIsMutable();
        facts_.set(index, value);
        onChanged();
      } else {
        factsBuilder_.setMessage(index, value);
      }
      return this;
    }
    /**
     * <pre>
     * The confirmed facts — every confirmed memory of the caller in this
     * organization, the candidate set for prompt injection.
     *
     * &#64;internal
     * The compose steps stamp EVERY confirmed fact, never a compose-time
     * truncation (DD-006 D5 as revised by DD-008): this is the auditable
     * CANDIDATE set, not necessarily the injected set. Above the
     * retriever's activation threshold the runner selects the most
     * relevant subset at prompt build and records it in
     * status.recalled_memories_report (DD-008 D3/D5); at or below the
     * threshold, or with no embeddings-capable provider, injection is
     * wholesale and the report (when present) says so. Selection is
     * recorded, never silent: what the model saw always equals what the
     * user can audit — this snapshot joined to the report.
     * </pre>
     *
     * <code>repeated .ai.stigmer.agentic.agentexecution.v1.RecalledMemoryFact facts = 2 [json_name = "facts"];</code>
     */
    public Builder setFacts(
        int index, ai.stigmer.agentic.agentexecution.v1.RecalledMemoryFact.Builder builderForValue) {
      if (factsBuilder_ == null) {
        ensureFactsIsMutable();
        facts_.set(index, builderForValue.build());
        onChanged();
      } else {
        factsBuilder_.setMessage(index, builderForValue.build());
      }
      return this;
    }
    /**
     * <pre>
     * The confirmed facts — every confirmed memory of the caller in this
     * organization, the candidate set for prompt injection.
     *
     * &#64;internal
     * The compose steps stamp EVERY confirmed fact, never a compose-time
     * truncation (DD-006 D5 as revised by DD-008): this is the auditable
     * CANDIDATE set, not necessarily the injected set. Above the
     * retriever's activation threshold the runner selects the most
     * relevant subset at prompt build and records it in
     * status.recalled_memories_report (DD-008 D3/D5); at or below the
     * threshold, or with no embeddings-capable provider, injection is
     * wholesale and the report (when present) says so. Selection is
     * recorded, never silent: what the model saw always equals what the
     * user can audit — this snapshot joined to the report.
     * </pre>
     *
     * <code>repeated .ai.stigmer.agentic.agentexecution.v1.RecalledMemoryFact facts = 2 [json_name = "facts"];</code>
     */
    public Builder addFacts(ai.stigmer.agentic.agentexecution.v1.RecalledMemoryFact value) {
      if (factsBuilder_ == null) {
        if (value == null) {
          throw new NullPointerException();
        }
        ensureFactsIsMutable();
        facts_.add(value);
        onChanged();
      } else {
        factsBuilder_.addMessage(value);
      }
      return this;
    }
    /**
     * <pre>
     * The confirmed facts — every confirmed memory of the caller in this
     * organization, the candidate set for prompt injection.
     *
     * &#64;internal
     * The compose steps stamp EVERY confirmed fact, never a compose-time
     * truncation (DD-006 D5 as revised by DD-008): this is the auditable
     * CANDIDATE set, not necessarily the injected set. Above the
     * retriever's activation threshold the runner selects the most
     * relevant subset at prompt build and records it in
     * status.recalled_memories_report (DD-008 D3/D5); at or below the
     * threshold, or with no embeddings-capable provider, injection is
     * wholesale and the report (when present) says so. Selection is
     * recorded, never silent: what the model saw always equals what the
     * user can audit — this snapshot joined to the report.
     * </pre>
     *
     * <code>repeated .ai.stigmer.agentic.agentexecution.v1.RecalledMemoryFact facts = 2 [json_name = "facts"];</code>
     */
    public Builder addFacts(
        int index, ai.stigmer.agentic.agentexecution.v1.RecalledMemoryFact value) {
      if (factsBuilder_ == null) {
        if (value == null) {
          throw new NullPointerException();
        }
        ensureFactsIsMutable();
        facts_.add(index, value);
        onChanged();
      } else {
        factsBuilder_.addMessage(index, value);
      }
      return this;
    }
    /**
     * <pre>
     * The confirmed facts — every confirmed memory of the caller in this
     * organization, the candidate set for prompt injection.
     *
     * &#64;internal
     * The compose steps stamp EVERY confirmed fact, never a compose-time
     * truncation (DD-006 D5 as revised by DD-008): this is the auditable
     * CANDIDATE set, not necessarily the injected set. Above the
     * retriever's activation threshold the runner selects the most
     * relevant subset at prompt build and records it in
     * status.recalled_memories_report (DD-008 D3/D5); at or below the
     * threshold, or with no embeddings-capable provider, injection is
     * wholesale and the report (when present) says so. Selection is
     * recorded, never silent: what the model saw always equals what the
     * user can audit — this snapshot joined to the report.
     * </pre>
     *
     * <code>repeated .ai.stigmer.agentic.agentexecution.v1.RecalledMemoryFact facts = 2 [json_name = "facts"];</code>
     */
    public Builder addFacts(
        ai.stigmer.agentic.agentexecution.v1.RecalledMemoryFact.Builder builderForValue) {
      if (factsBuilder_ == null) {
        ensureFactsIsMutable();
        facts_.add(builderForValue.build());
        onChanged();
      } else {
        factsBuilder_.addMessage(builderForValue.build());
      }
      return this;
    }
    /**
     * <pre>
     * The confirmed facts — every confirmed memory of the caller in this
     * organization, the candidate set for prompt injection.
     *
     * &#64;internal
     * The compose steps stamp EVERY confirmed fact, never a compose-time
     * truncation (DD-006 D5 as revised by DD-008): this is the auditable
     * CANDIDATE set, not necessarily the injected set. Above the
     * retriever's activation threshold the runner selects the most
     * relevant subset at prompt build and records it in
     * status.recalled_memories_report (DD-008 D3/D5); at or below the
     * threshold, or with no embeddings-capable provider, injection is
     * wholesale and the report (when present) says so. Selection is
     * recorded, never silent: what the model saw always equals what the
     * user can audit — this snapshot joined to the report.
     * </pre>
     *
     * <code>repeated .ai.stigmer.agentic.agentexecution.v1.RecalledMemoryFact facts = 2 [json_name = "facts"];</code>
     */
    public Builder addFacts(
        int index, ai.stigmer.agentic.agentexecution.v1.RecalledMemoryFact.Builder builderForValue) {
      if (factsBuilder_ == null) {
        ensureFactsIsMutable();
        facts_.add(index, builderForValue.build());
        onChanged();
      } else {
        factsBuilder_.addMessage(index, builderForValue.build());
      }
      return this;
    }
    /**
     * <pre>
     * The confirmed facts — every confirmed memory of the caller in this
     * organization, the candidate set for prompt injection.
     *
     * &#64;internal
     * The compose steps stamp EVERY confirmed fact, never a compose-time
     * truncation (DD-006 D5 as revised by DD-008): this is the auditable
     * CANDIDATE set, not necessarily the injected set. Above the
     * retriever's activation threshold the runner selects the most
     * relevant subset at prompt build and records it in
     * status.recalled_memories_report (DD-008 D3/D5); at or below the
     * threshold, or with no embeddings-capable provider, injection is
     * wholesale and the report (when present) says so. Selection is
     * recorded, never silent: what the model saw always equals what the
     * user can audit — this snapshot joined to the report.
     * </pre>
     *
     * <code>repeated .ai.stigmer.agentic.agentexecution.v1.RecalledMemoryFact facts = 2 [json_name = "facts"];</code>
     */
    public Builder addAllFacts(
        java.lang.Iterable<? extends ai.stigmer.agentic.agentexecution.v1.RecalledMemoryFact> values) {
      if (factsBuilder_ == null) {
        ensureFactsIsMutable();
        com.google.protobuf.AbstractMessageLite.Builder.addAll(
            values, facts_);
        onChanged();
      } else {
        factsBuilder_.addAllMessages(values);
      }
      return this;
    }
    /**
     * <pre>
     * The confirmed facts — every confirmed memory of the caller in this
     * organization, the candidate set for prompt injection.
     *
     * &#64;internal
     * The compose steps stamp EVERY confirmed fact, never a compose-time
     * truncation (DD-006 D5 as revised by DD-008): this is the auditable
     * CANDIDATE set, not necessarily the injected set. Above the
     * retriever's activation threshold the runner selects the most
     * relevant subset at prompt build and records it in
     * status.recalled_memories_report (DD-008 D3/D5); at or below the
     * threshold, or with no embeddings-capable provider, injection is
     * wholesale and the report (when present) says so. Selection is
     * recorded, never silent: what the model saw always equals what the
     * user can audit — this snapshot joined to the report.
     * </pre>
     *
     * <code>repeated .ai.stigmer.agentic.agentexecution.v1.RecalledMemoryFact facts = 2 [json_name = "facts"];</code>
     */
    public Builder clearFacts() {
      if (factsBuilder_ == null) {
        facts_ = java.util.Collections.emptyList();
        bitField0_ = (bitField0_ & ~0x00000002);
        onChanged();
      } else {
        factsBuilder_.clear();
      }
      return this;
    }
    /**
     * <pre>
     * The confirmed facts — every confirmed memory of the caller in this
     * organization, the candidate set for prompt injection.
     *
     * &#64;internal
     * The compose steps stamp EVERY confirmed fact, never a compose-time
     * truncation (DD-006 D5 as revised by DD-008): this is the auditable
     * CANDIDATE set, not necessarily the injected set. Above the
     * retriever's activation threshold the runner selects the most
     * relevant subset at prompt build and records it in
     * status.recalled_memories_report (DD-008 D3/D5); at or below the
     * threshold, or with no embeddings-capable provider, injection is
     * wholesale and the report (when present) says so. Selection is
     * recorded, never silent: what the model saw always equals what the
     * user can audit — this snapshot joined to the report.
     * </pre>
     *
     * <code>repeated .ai.stigmer.agentic.agentexecution.v1.RecalledMemoryFact facts = 2 [json_name = "facts"];</code>
     */
    public Builder removeFacts(int index) {
      if (factsBuilder_ == null) {
        ensureFactsIsMutable();
        facts_.remove(index);
        onChanged();
      } else {
        factsBuilder_.remove(index);
      }
      return this;
    }
    /**
     * <pre>
     * The confirmed facts — every confirmed memory of the caller in this
     * organization, the candidate set for prompt injection.
     *
     * &#64;internal
     * The compose steps stamp EVERY confirmed fact, never a compose-time
     * truncation (DD-006 D5 as revised by DD-008): this is the auditable
     * CANDIDATE set, not necessarily the injected set. Above the
     * retriever's activation threshold the runner selects the most
     * relevant subset at prompt build and records it in
     * status.recalled_memories_report (DD-008 D3/D5); at or below the
     * threshold, or with no embeddings-capable provider, injection is
     * wholesale and the report (when present) says so. Selection is
     * recorded, never silent: what the model saw always equals what the
     * user can audit — this snapshot joined to the report.
     * </pre>
     *
     * <code>repeated .ai.stigmer.agentic.agentexecution.v1.RecalledMemoryFact facts = 2 [json_name = "facts"];</code>
     */
    public ai.stigmer.agentic.agentexecution.v1.RecalledMemoryFact.Builder getFactsBuilder(
        int index) {
      return internalGetFactsFieldBuilder().getBuilder(index);
    }
    /**
     * <pre>
     * The confirmed facts — every confirmed memory of the caller in this
     * organization, the candidate set for prompt injection.
     *
     * &#64;internal
     * The compose steps stamp EVERY confirmed fact, never a compose-time
     * truncation (DD-006 D5 as revised by DD-008): this is the auditable
     * CANDIDATE set, not necessarily the injected set. Above the
     * retriever's activation threshold the runner selects the most
     * relevant subset at prompt build and records it in
     * status.recalled_memories_report (DD-008 D3/D5); at or below the
     * threshold, or with no embeddings-capable provider, injection is
     * wholesale and the report (when present) says so. Selection is
     * recorded, never silent: what the model saw always equals what the
     * user can audit — this snapshot joined to the report.
     * </pre>
     *
     * <code>repeated .ai.stigmer.agentic.agentexecution.v1.RecalledMemoryFact facts = 2 [json_name = "facts"];</code>
     */
    public ai.stigmer.agentic.agentexecution.v1.RecalledMemoryFactOrBuilder getFactsOrBuilder(
        int index) {
      if (factsBuilder_ == null) {
        return facts_.get(index);  } else {
        return factsBuilder_.getMessageOrBuilder(index);
      }
    }
    /**
     * <pre>
     * The confirmed facts — every confirmed memory of the caller in this
     * organization, the candidate set for prompt injection.
     *
     * &#64;internal
     * The compose steps stamp EVERY confirmed fact, never a compose-time
     * truncation (DD-006 D5 as revised by DD-008): this is the auditable
     * CANDIDATE set, not necessarily the injected set. Above the
     * retriever's activation threshold the runner selects the most
     * relevant subset at prompt build and records it in
     * status.recalled_memories_report (DD-008 D3/D5); at or below the
     * threshold, or with no embeddings-capable provider, injection is
     * wholesale and the report (when present) says so. Selection is
     * recorded, never silent: what the model saw always equals what the
     * user can audit — this snapshot joined to the report.
     * </pre>
     *
     * <code>repeated .ai.stigmer.agentic.agentexecution.v1.RecalledMemoryFact facts = 2 [json_name = "facts"];</code>
     */
    public java.util.List<? extends ai.stigmer.agentic.agentexecution.v1.RecalledMemoryFactOrBuilder> 
         getFactsOrBuilderList() {
      if (factsBuilder_ != null) {
        return factsBuilder_.getMessageOrBuilderList();
      } else {
        return java.util.Collections.unmodifiableList(facts_);
      }
    }
    /**
     * <pre>
     * The confirmed facts — every confirmed memory of the caller in this
     * organization, the candidate set for prompt injection.
     *
     * &#64;internal
     * The compose steps stamp EVERY confirmed fact, never a compose-time
     * truncation (DD-006 D5 as revised by DD-008): this is the auditable
     * CANDIDATE set, not necessarily the injected set. Above the
     * retriever's activation threshold the runner selects the most
     * relevant subset at prompt build and records it in
     * status.recalled_memories_report (DD-008 D3/D5); at or below the
     * threshold, or with no embeddings-capable provider, injection is
     * wholesale and the report (when present) says so. Selection is
     * recorded, never silent: what the model saw always equals what the
     * user can audit — this snapshot joined to the report.
     * </pre>
     *
     * <code>repeated .ai.stigmer.agentic.agentexecution.v1.RecalledMemoryFact facts = 2 [json_name = "facts"];</code>
     */
    public ai.stigmer.agentic.agentexecution.v1.RecalledMemoryFact.Builder addFactsBuilder() {
      return internalGetFactsFieldBuilder().addBuilder(
          ai.stigmer.agentic.agentexecution.v1.RecalledMemoryFact.getDefaultInstance());
    }
    /**
     * <pre>
     * The confirmed facts — every confirmed memory of the caller in this
     * organization, the candidate set for prompt injection.
     *
     * &#64;internal
     * The compose steps stamp EVERY confirmed fact, never a compose-time
     * truncation (DD-006 D5 as revised by DD-008): this is the auditable
     * CANDIDATE set, not necessarily the injected set. Above the
     * retriever's activation threshold the runner selects the most
     * relevant subset at prompt build and records it in
     * status.recalled_memories_report (DD-008 D3/D5); at or below the
     * threshold, or with no embeddings-capable provider, injection is
     * wholesale and the report (when present) says so. Selection is
     * recorded, never silent: what the model saw always equals what the
     * user can audit — this snapshot joined to the report.
     * </pre>
     *
     * <code>repeated .ai.stigmer.agentic.agentexecution.v1.RecalledMemoryFact facts = 2 [json_name = "facts"];</code>
     */
    public ai.stigmer.agentic.agentexecution.v1.RecalledMemoryFact.Builder addFactsBuilder(
        int index) {
      return internalGetFactsFieldBuilder().addBuilder(
          index, ai.stigmer.agentic.agentexecution.v1.RecalledMemoryFact.getDefaultInstance());
    }
    /**
     * <pre>
     * The confirmed facts — every confirmed memory of the caller in this
     * organization, the candidate set for prompt injection.
     *
     * &#64;internal
     * The compose steps stamp EVERY confirmed fact, never a compose-time
     * truncation (DD-006 D5 as revised by DD-008): this is the auditable
     * CANDIDATE set, not necessarily the injected set. Above the
     * retriever's activation threshold the runner selects the most
     * relevant subset at prompt build and records it in
     * status.recalled_memories_report (DD-008 D3/D5); at or below the
     * threshold, or with no embeddings-capable provider, injection is
     * wholesale and the report (when present) says so. Selection is
     * recorded, never silent: what the model saw always equals what the
     * user can audit — this snapshot joined to the report.
     * </pre>
     *
     * <code>repeated .ai.stigmer.agentic.agentexecution.v1.RecalledMemoryFact facts = 2 [json_name = "facts"];</code>
     */
    public java.util.List<ai.stigmer.agentic.agentexecution.v1.RecalledMemoryFact.Builder> 
         getFactsBuilderList() {
      return internalGetFactsFieldBuilder().getBuilderList();
    }
    private com.google.protobuf.RepeatedFieldBuilder<
        ai.stigmer.agentic.agentexecution.v1.RecalledMemoryFact, ai.stigmer.agentic.agentexecution.v1.RecalledMemoryFact.Builder, ai.stigmer.agentic.agentexecution.v1.RecalledMemoryFactOrBuilder> 
        internalGetFactsFieldBuilder() {
      if (factsBuilder_ == null) {
        factsBuilder_ = new com.google.protobuf.RepeatedFieldBuilder<
            ai.stigmer.agentic.agentexecution.v1.RecalledMemoryFact, ai.stigmer.agentic.agentexecution.v1.RecalledMemoryFact.Builder, ai.stigmer.agentic.agentexecution.v1.RecalledMemoryFactOrBuilder>(
                facts_,
                ((bitField0_ & 0x00000002) != 0),
                getParentForChildren(),
                isClean());
        facts_ = null;
      }
      return factsBuilder_;
    }

    // @@protoc_insertion_point(builder_scope:ai.stigmer.agentic.agentexecution.v1.RecalledMemories)
  }

  // @@protoc_insertion_point(class_scope:ai.stigmer.agentic.agentexecution.v1.RecalledMemories)
  private static final ai.stigmer.agentic.agentexecution.v1.RecalledMemories DEFAULT_INSTANCE;
  static {
    DEFAULT_INSTANCE = new ai.stigmer.agentic.agentexecution.v1.RecalledMemories();
  }

  public static ai.stigmer.agentic.agentexecution.v1.RecalledMemories getDefaultInstance() {
    return DEFAULT_INSTANCE;
  }

  private static final com.google.protobuf.Parser<RecalledMemories>
      PARSER = new com.google.protobuf.AbstractParser<RecalledMemories>() {
    @java.lang.Override
    public RecalledMemories parsePartialFrom(
        com.google.protobuf.CodedInputStream input,
        com.google.protobuf.ExtensionRegistryLite extensionRegistry)
        throws com.google.protobuf.InvalidProtocolBufferException {
      Builder builder = newBuilder();
      try {
        builder.mergeFrom(input, extensionRegistry);
      } catch (com.google.protobuf.InvalidProtocolBufferException e) {
        throw e.setUnfinishedMessage(builder.buildPartial());
      } catch (com.google.protobuf.UninitializedMessageException e) {
        throw e.asInvalidProtocolBufferException().setUnfinishedMessage(builder.buildPartial());
      } catch (java.io.IOException e) {
        throw new com.google.protobuf.InvalidProtocolBufferException(e)
            .setUnfinishedMessage(builder.buildPartial());
      }
      return builder.buildPartial();
    }
  };

  public static com.google.protobuf.Parser<RecalledMemories> parser() {
    return PARSER;
  }

  @java.lang.Override
  public com.google.protobuf.Parser<RecalledMemories> getParserForType() {
    return PARSER;
  }

  @java.lang.Override
  public ai.stigmer.agentic.agentexecution.v1.RecalledMemories getDefaultInstanceForType() {
    return DEFAULT_INSTANCE;
  }

}

