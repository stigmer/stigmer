// Generated by the protocol buffer compiler.  DO NOT EDIT!
// NO CHECKED-IN PROTOBUF GENCODE
// source: ai/stigmer/agentic/agentchannel/v1/spec.proto
// Protobuf Java Version: 4.34.0

package ai.stigmer.agentic.agentchannel.v1;

/**
 * <pre>
 * AgentChannelSpec defines the configurable properties of an agent channel.
 *
 * The spec is deliberately small: which agent serves the channel, whether
 * serving is enabled, which provider the channel targets, and which
 * environments supply the agent's tool credentials. Workspace identity and
 * provider credentials are produced by the install flow and live in
 * status — a declarative apply can never clobber them.
 *
 * &#64;internal
 * P1: the provider arms carry only genuinely user-declarable fields. For
 * the multi-tenant Slack app everything concrete (team_id, bot_user_id,
 * scopes, credentials env) is OAuth-observed → status. Owner-customizable
 * refusal copy (the AgentShareMessages analog) is deliberately deferred:
 * platform-default copy lives in the deliverer (decision 006); adding a
 * messages block later is a non-breaking change.
 * </pre>
 *
 * Protobuf type {@code ai.stigmer.agentic.agentchannel.v1.AgentChannelSpec}
 */
@com.google.protobuf.Generated
public final class AgentChannelSpec extends
    com.google.protobuf.GeneratedMessage implements
    // @@protoc_insertion_point(message_implements:ai.stigmer.agentic.agentchannel.v1.AgentChannelSpec)
    AgentChannelSpecOrBuilder {
private static final long serialVersionUID = 0L;
  static {
    com.google.protobuf.RuntimeVersion.validateProtobufGencodeVersion(
      com.google.protobuf.RuntimeVersion.RuntimeDomain.PUBLIC,
      /* major= */ 4,
      /* minor= */ 34,
      /* patch= */ 0,
      /* suffix= */ "",
      "AgentChannelSpec");
  }
  // Use AgentChannelSpec.newBuilder() to construct.
  private AgentChannelSpec(com.google.protobuf.GeneratedMessage.Builder<?> builder) {
    super(builder);
  }
  private AgentChannelSpec() {
    environmentRefs_ = java.util.Collections.emptyList();
  }

  public static final com.google.protobuf.Descriptors.Descriptor
      getDescriptor() {
    return ai.stigmer.agentic.agentchannel.v1.SpecProto.internal_static_ai_stigmer_agentic_agentchannel_v1_AgentChannelSpec_descriptor;
  }

  @java.lang.Override
  public com.google.protobuf.Descriptors.Descriptor getDescriptorForType() {
    return ai.stigmer.agentic.agentchannel.v1.SpecProto.internal_static_ai_stigmer_agentic_agentchannel_v1_AgentChannelSpec_descriptor;
  }

  @java.lang.Override
  protected com.google.protobuf.GeneratedMessage.FieldAccessorTable
      internalGetFieldAccessorTable() {
    return ai.stigmer.agentic.agentchannel.v1.SpecProto.internal_static_ai_stigmer_agentic_agentchannel_v1_AgentChannelSpec_fieldAccessorTable
        .ensureFieldAccessorsInitialized(
            ai.stigmer.agentic.agentchannel.v1.AgentChannelSpec.class, ai.stigmer.agentic.agentchannel.v1.AgentChannelSpec.Builder.class);
  }

  private int bitField0_;
  private int providerConfigCase_ = 0;
  @SuppressWarnings("serial")
  private java.lang.Object providerConfig_;
  public enum ProviderConfigCase
      implements com.google.protobuf.Internal.EnumLite,
          com.google.protobuf.AbstractMessage.InternalOneOfEnum {
    SLACK(3),
    PROVIDERCONFIG_NOT_SET(0);
    private final int value;
    private ProviderConfigCase(int value) {
      this.value = value;
    }
    /**
     * @param value The number of the enum to look for.
     * @return The enum associated with the given number.
     * @deprecated Use {@link #forNumber(int)} instead.
     */
    @java.lang.Deprecated
    public static ProviderConfigCase valueOf(int value) {
      return forNumber(value);
    }

    public static ProviderConfigCase forNumber(int value) {
      switch (value) {
        case 3: return SLACK;
        case 0: return PROVIDERCONFIG_NOT_SET;
        default: return null;
      }
    }
    public int getNumber() {
      return this.value;
    }
  };

  public ProviderConfigCase
  getProviderConfigCase() {
    return ProviderConfigCase.forNumber(
        providerConfigCase_);
  }

  public static final int AGENT_REF_FIELD_NUMBER = 1;
  private ai.stigmer.commons.apiresource.ApiResourceReference agentRef_;
  /**
   * <pre>
   * Reference to the agent this channel serves.
   *
   * &#64;internal
   * Invariant (enforced in create/update/apply handlers of both editions,
   * mirroring AgentShare's Phase A rule): agent_ref.org must equal
   * metadata.org — the connection-owning org is the billing org and
   * provider credentials resolve in that org (decision 004; T06 invariant).
   * </pre>
   *
   * <code>.ai.stigmer.commons.apiresource.ApiResourceReference agent_ref = 1 [json_name = "agentRef", (.buf.validate.field) = { ... }</code>
   * @return Whether the agentRef field is set.
   */
  @java.lang.Override
  public boolean hasAgentRef() {
    return ((bitField0_ & 0x00000001) != 0);
  }
  /**
   * <pre>
   * Reference to the agent this channel serves.
   *
   * &#64;internal
   * Invariant (enforced in create/update/apply handlers of both editions,
   * mirroring AgentShare's Phase A rule): agent_ref.org must equal
   * metadata.org — the connection-owning org is the billing org and
   * provider credentials resolve in that org (decision 004; T06 invariant).
   * </pre>
   *
   * <code>.ai.stigmer.commons.apiresource.ApiResourceReference agent_ref = 1 [json_name = "agentRef", (.buf.validate.field) = { ... }</code>
   * @return The agentRef.
   */
  @java.lang.Override
  public ai.stigmer.commons.apiresource.ApiResourceReference getAgentRef() {
    return agentRef_ == null ? ai.stigmer.commons.apiresource.ApiResourceReference.getDefaultInstance() : agentRef_;
  }
  /**
   * <pre>
   * Reference to the agent this channel serves.
   *
   * &#64;internal
   * Invariant (enforced in create/update/apply handlers of both editions,
   * mirroring AgentShare's Phase A rule): agent_ref.org must equal
   * metadata.org — the connection-owning org is the billing org and
   * provider credentials resolve in that org (decision 004; T06 invariant).
   * </pre>
   *
   * <code>.ai.stigmer.commons.apiresource.ApiResourceReference agent_ref = 1 [json_name = "agentRef", (.buf.validate.field) = { ... }</code>
   */
  @java.lang.Override
  public ai.stigmer.commons.apiresource.ApiResourceReferenceOrBuilder getAgentRefOrBuilder() {
    return agentRef_ == null ? ai.stigmer.commons.apiresource.ApiResourceReference.getDefaultInstance() : agentRef_;
  }

  public static final int ENABLED_FIELD_NUMBER = 2;
  private boolean enabled_ = false;
  /**
   * <pre>
   * Whether serving traffic on this channel is enabled.
   *
   * Disabling is a config-preserving pause: the connection keeps its
   * install state and credentials, but inbound events are refused until
   * re-enabled. Delete the channel instead for a full teardown.
   *
   * &#64;internal
   * Decision 001 D-d. Pause must never drop the install: re-installing a
   * Slack app to re-auth is painful. The per-turn gate re-reads this live
   * (decision 003), so disabling takes effect on the next message.
   * </pre>
   *
   * <code>bool enabled = 2 [json_name = "enabled"];</code>
   * @return The enabled.
   */
  @java.lang.Override
  public boolean getEnabled() {
    return enabled_;
  }

  public static final int SLACK_FIELD_NUMBER = 3;
  /**
   * <pre>
   * Slack workspace connection (multi-tenant Stigmer Slack app).
   * </pre>
   *
   * <code>.ai.stigmer.agentic.agentchannel.v1.SlackChannelConfig slack = 3 [json_name = "slack"];</code>
   * @return Whether the slack field is set.
   */
  @java.lang.Override
  public boolean hasSlack() {
    return providerConfigCase_ == 3;
  }
  /**
   * <pre>
   * Slack workspace connection (multi-tenant Stigmer Slack app).
   * </pre>
   *
   * <code>.ai.stigmer.agentic.agentchannel.v1.SlackChannelConfig slack = 3 [json_name = "slack"];</code>
   * @return The slack.
   */
  @java.lang.Override
  public ai.stigmer.agentic.agentchannel.v1.SlackChannelConfig getSlack() {
    if (providerConfigCase_ == 3) {
       return (ai.stigmer.agentic.agentchannel.v1.SlackChannelConfig) providerConfig_;
    }
    return ai.stigmer.agentic.agentchannel.v1.SlackChannelConfig.getDefaultInstance();
  }
  /**
   * <pre>
   * Slack workspace connection (multi-tenant Stigmer Slack app).
   * </pre>
   *
   * <code>.ai.stigmer.agentic.agentchannel.v1.SlackChannelConfig slack = 3 [json_name = "slack"];</code>
   */
  @java.lang.Override
  public ai.stigmer.agentic.agentchannel.v1.SlackChannelConfigOrBuilder getSlackOrBuilder() {
    if (providerConfigCase_ == 3) {
       return (ai.stigmer.agentic.agentchannel.v1.SlackChannelConfig) providerConfig_;
    }
    return ai.stigmer.agentic.agentchannel.v1.SlackChannelConfig.getDefaultInstance();
  }

  public static final int ENVIRONMENT_REFS_FIELD_NUMBER = 4;
  @SuppressWarnings("serial")
  private java.util.List<ai.stigmer.commons.apiresource.ApiResourceReference> environmentRefs_;
  /**
   * <pre>
   * References to Environment resources whose values are provided to
   * conversations on this channel.
   *
   * This is how a tool-using agent becomes chattable over a channel: bind
   * an org-shared environment holding the needed credentials (for example
   * a read-only API token), and channel executions receive its values at
   * runtime. The agent and its default instance stay untouched.
   *
   * &#64;internal
   * The AgentShareSpec.environment_refs analog (decision 011: sharing is
   * a channel — both connection kinds carry their own credentials).
   * Resolved in the channel's org through the org-shared environment
   * resolution seam (EnvironmentRuntimeResolutionService /
   * OrgSharedEnvironmentPolicy): each referenced environment must be
   * visibility_org in the channel's org, or the merge skips it with a
   * diagnostic. Merged at channel execution-context build time only,
   * lowest priority (instance refs and runtime_env override on key
   * conflicts) — never bound to the agent's system-managed default
   * instance. No write-time existence or visibility check, matching the
   * share: enforcement lives solely at runtime resolution, which fails
   * closed. Unlike the share there is no audience CEL — channels have no
   * audience concept, and the same-org invariant (agent_ref.org ==
   * metadata.org) already scopes resolution.
   * </pre>
   *
   * <code>repeated .ai.stigmer.commons.apiresource.ApiResourceReference environment_refs = 4 [json_name = "environmentRefs", (.buf.validate.field) = { ... }</code>
   */
  @java.lang.Override
  public java.util.List<ai.stigmer.commons.apiresource.ApiResourceReference> getEnvironmentRefsList() {
    return environmentRefs_;
  }
  /**
   * <pre>
   * References to Environment resources whose values are provided to
   * conversations on this channel.
   *
   * This is how a tool-using agent becomes chattable over a channel: bind
   * an org-shared environment holding the needed credentials (for example
   * a read-only API token), and channel executions receive its values at
   * runtime. The agent and its default instance stay untouched.
   *
   * &#64;internal
   * The AgentShareSpec.environment_refs analog (decision 011: sharing is
   * a channel — both connection kinds carry their own credentials).
   * Resolved in the channel's org through the org-shared environment
   * resolution seam (EnvironmentRuntimeResolutionService /
   * OrgSharedEnvironmentPolicy): each referenced environment must be
   * visibility_org in the channel's org, or the merge skips it with a
   * diagnostic. Merged at channel execution-context build time only,
   * lowest priority (instance refs and runtime_env override on key
   * conflicts) — never bound to the agent's system-managed default
   * instance. No write-time existence or visibility check, matching the
   * share: enforcement lives solely at runtime resolution, which fails
   * closed. Unlike the share there is no audience CEL — channels have no
   * audience concept, and the same-org invariant (agent_ref.org ==
   * metadata.org) already scopes resolution.
   * </pre>
   *
   * <code>repeated .ai.stigmer.commons.apiresource.ApiResourceReference environment_refs = 4 [json_name = "environmentRefs", (.buf.validate.field) = { ... }</code>
   */
  @java.lang.Override
  public java.util.List<? extends ai.stigmer.commons.apiresource.ApiResourceReferenceOrBuilder> 
      getEnvironmentRefsOrBuilderList() {
    return environmentRefs_;
  }
  /**
   * <pre>
   * References to Environment resources whose values are provided to
   * conversations on this channel.
   *
   * This is how a tool-using agent becomes chattable over a channel: bind
   * an org-shared environment holding the needed credentials (for example
   * a read-only API token), and channel executions receive its values at
   * runtime. The agent and its default instance stay untouched.
   *
   * &#64;internal
   * The AgentShareSpec.environment_refs analog (decision 011: sharing is
   * a channel — both connection kinds carry their own credentials).
   * Resolved in the channel's org through the org-shared environment
   * resolution seam (EnvironmentRuntimeResolutionService /
   * OrgSharedEnvironmentPolicy): each referenced environment must be
   * visibility_org in the channel's org, or the merge skips it with a
   * diagnostic. Merged at channel execution-context build time only,
   * lowest priority (instance refs and runtime_env override on key
   * conflicts) — never bound to the agent's system-managed default
   * instance. No write-time existence or visibility check, matching the
   * share: enforcement lives solely at runtime resolution, which fails
   * closed. Unlike the share there is no audience CEL — channels have no
   * audience concept, and the same-org invariant (agent_ref.org ==
   * metadata.org) already scopes resolution.
   * </pre>
   *
   * <code>repeated .ai.stigmer.commons.apiresource.ApiResourceReference environment_refs = 4 [json_name = "environmentRefs", (.buf.validate.field) = { ... }</code>
   */
  @java.lang.Override
  public int getEnvironmentRefsCount() {
    return environmentRefs_.size();
  }
  /**
   * <pre>
   * References to Environment resources whose values are provided to
   * conversations on this channel.
   *
   * This is how a tool-using agent becomes chattable over a channel: bind
   * an org-shared environment holding the needed credentials (for example
   * a read-only API token), and channel executions receive its values at
   * runtime. The agent and its default instance stay untouched.
   *
   * &#64;internal
   * The AgentShareSpec.environment_refs analog (decision 011: sharing is
   * a channel — both connection kinds carry their own credentials).
   * Resolved in the channel's org through the org-shared environment
   * resolution seam (EnvironmentRuntimeResolutionService /
   * OrgSharedEnvironmentPolicy): each referenced environment must be
   * visibility_org in the channel's org, or the merge skips it with a
   * diagnostic. Merged at channel execution-context build time only,
   * lowest priority (instance refs and runtime_env override on key
   * conflicts) — never bound to the agent's system-managed default
   * instance. No write-time existence or visibility check, matching the
   * share: enforcement lives solely at runtime resolution, which fails
   * closed. Unlike the share there is no audience CEL — channels have no
   * audience concept, and the same-org invariant (agent_ref.org ==
   * metadata.org) already scopes resolution.
   * </pre>
   *
   * <code>repeated .ai.stigmer.commons.apiresource.ApiResourceReference environment_refs = 4 [json_name = "environmentRefs", (.buf.validate.field) = { ... }</code>
   */
  @java.lang.Override
  public ai.stigmer.commons.apiresource.ApiResourceReference getEnvironmentRefs(int index) {
    return environmentRefs_.get(index);
  }
  /**
   * <pre>
   * References to Environment resources whose values are provided to
   * conversations on this channel.
   *
   * This is how a tool-using agent becomes chattable over a channel: bind
   * an org-shared environment holding the needed credentials (for example
   * a read-only API token), and channel executions receive its values at
   * runtime. The agent and its default instance stay untouched.
   *
   * &#64;internal
   * The AgentShareSpec.environment_refs analog (decision 011: sharing is
   * a channel — both connection kinds carry their own credentials).
   * Resolved in the channel's org through the org-shared environment
   * resolution seam (EnvironmentRuntimeResolutionService /
   * OrgSharedEnvironmentPolicy): each referenced environment must be
   * visibility_org in the channel's org, or the merge skips it with a
   * diagnostic. Merged at channel execution-context build time only,
   * lowest priority (instance refs and runtime_env override on key
   * conflicts) — never bound to the agent's system-managed default
   * instance. No write-time existence or visibility check, matching the
   * share: enforcement lives solely at runtime resolution, which fails
   * closed. Unlike the share there is no audience CEL — channels have no
   * audience concept, and the same-org invariant (agent_ref.org ==
   * metadata.org) already scopes resolution.
   * </pre>
   *
   * <code>repeated .ai.stigmer.commons.apiresource.ApiResourceReference environment_refs = 4 [json_name = "environmentRefs", (.buf.validate.field) = { ... }</code>
   */
  @java.lang.Override
  public ai.stigmer.commons.apiresource.ApiResourceReferenceOrBuilder getEnvironmentRefsOrBuilder(
      int index) {
    return environmentRefs_.get(index);
  }

  private byte memoizedIsInitialized = -1;
  @java.lang.Override
  public final boolean isInitialized() {
    byte isInitialized = memoizedIsInitialized;
    if (isInitialized == 1) return true;
    if (isInitialized == 0) return false;

    memoizedIsInitialized = 1;
    return true;
  }

  @java.lang.Override
  public void writeTo(com.google.protobuf.CodedOutputStream output)
                      throws java.io.IOException {
    if (((bitField0_ & 0x00000001) != 0)) {
      output.writeMessage(1, getAgentRef());
    }
    if (enabled_ != false) {
      output.writeBool(2, enabled_);
    }
    if (providerConfigCase_ == 3) {
      output.writeMessage(3, (ai.stigmer.agentic.agentchannel.v1.SlackChannelConfig) providerConfig_);
    }
    for (int i = 0; i < environmentRefs_.size(); i++) {
      output.writeMessage(4, environmentRefs_.get(i));
    }
    getUnknownFields().writeTo(output);
  }

  @java.lang.Override
  public int getSerializedSize() {
    int size = memoizedSize;
    if (size != -1) return size;

    size = 0;
    if (((bitField0_ & 0x00000001) != 0)) {
      size += com.google.protobuf.CodedOutputStream
        .computeMessageSize(1, getAgentRef());
    }
    if (enabled_ != false) {
      size += com.google.protobuf.CodedOutputStream
        .computeBoolSize(2, enabled_);
    }
    if (providerConfigCase_ == 3) {
      size += com.google.protobuf.CodedOutputStream
        .computeMessageSize(3, (ai.stigmer.agentic.agentchannel.v1.SlackChannelConfig) providerConfig_);
    }

        {
          final int count = environmentRefs_.size();
          for (int i = 0; i < count; i++) {
            size += com.google.protobuf.CodedOutputStream
              .computeMessageSizeNoTag(environmentRefs_.get(i));
          }
          size += 1 * count;
        }
    size += getUnknownFields().getSerializedSize();
    memoizedSize = size;
    return size;
  }

  @java.lang.Override
  public boolean equals(final java.lang.Object obj) {
    if (obj == this) {
     return true;
    }
    if (!(obj instanceof ai.stigmer.agentic.agentchannel.v1.AgentChannelSpec)) {
      return super.equals(obj);
    }
    ai.stigmer.agentic.agentchannel.v1.AgentChannelSpec other = (ai.stigmer.agentic.agentchannel.v1.AgentChannelSpec) obj;

    if (hasAgentRef() != other.hasAgentRef()) return false;
    if (hasAgentRef()) {
      if (!getAgentRef()
          .equals(other.getAgentRef())) return false;
    }
    if (getEnabled()
        != other.getEnabled()) return false;
    if (!getEnvironmentRefsList()
        .equals(other.getEnvironmentRefsList())) return false;
    if (!getProviderConfigCase().equals(other.getProviderConfigCase())) return false;
    switch (providerConfigCase_) {
      case 3:
        if (!getSlack()
            .equals(other.getSlack())) return false;
        break;
      case 0:
      default:
    }
    if (!getUnknownFields().equals(other.getUnknownFields())) return false;
    return true;
  }

  @java.lang.Override
  public int hashCode() {
    if (memoizedHashCode != 0) {
      return memoizedHashCode;
    }
    int hash = 41;
    hash = (19 * hash) + getDescriptor().hashCode();
    if (hasAgentRef()) {
      hash = (37 * hash) + AGENT_REF_FIELD_NUMBER;
      hash = (53 * hash) + getAgentRef().hashCode();
    }
    hash = (37 * hash) + ENABLED_FIELD_NUMBER;
    hash = (53 * hash) + com.google.protobuf.Internal.hashBoolean(
        getEnabled());
    if (getEnvironmentRefsCount() > 0) {
      hash = (37 * hash) + ENVIRONMENT_REFS_FIELD_NUMBER;
      hash = (53 * hash) + getEnvironmentRefsList().hashCode();
    }
    switch (providerConfigCase_) {
      case 3:
        hash = (37 * hash) + SLACK_FIELD_NUMBER;
        hash = (53 * hash) + getSlack().hashCode();
        break;
      case 0:
      default:
    }
    hash = (29 * hash) + getUnknownFields().hashCode();
    memoizedHashCode = hash;
    return hash;
  }

  public static ai.stigmer.agentic.agentchannel.v1.AgentChannelSpec parseFrom(
      java.nio.ByteBuffer data)
      throws com.google.protobuf.InvalidProtocolBufferException {
    return PARSER.parseFrom(data);
  }
  public static ai.stigmer.agentic.agentchannel.v1.AgentChannelSpec parseFrom(
      java.nio.ByteBuffer data,
      com.google.protobuf.ExtensionRegistryLite extensionRegistry)
      throws com.google.protobuf.InvalidProtocolBufferException {
    return PARSER.parseFrom(data, extensionRegistry);
  }
  public static ai.stigmer.agentic.agentchannel.v1.AgentChannelSpec parseFrom(
      com.google.protobuf.ByteString data)
      throws com.google.protobuf.InvalidProtocolBufferException {
    return PARSER.parseFrom(data);
  }
  public static ai.stigmer.agentic.agentchannel.v1.AgentChannelSpec parseFrom(
      com.google.protobuf.ByteString data,
      com.google.protobuf.ExtensionRegistryLite extensionRegistry)
      throws com.google.protobuf.InvalidProtocolBufferException {
    return PARSER.parseFrom(data, extensionRegistry);
  }
  public static ai.stigmer.agentic.agentchannel.v1.AgentChannelSpec parseFrom(byte[] data)
      throws com.google.protobuf.InvalidProtocolBufferException {
    return PARSER.parseFrom(data);
  }
  public static ai.stigmer.agentic.agentchannel.v1.AgentChannelSpec parseFrom(
      byte[] data,
      com.google.protobuf.ExtensionRegistryLite extensionRegistry)
      throws com.google.protobuf.InvalidProtocolBufferException {
    return PARSER.parseFrom(data, extensionRegistry);
  }
  public static ai.stigmer.agentic.agentchannel.v1.AgentChannelSpec parseFrom(java.io.InputStream input)
      throws java.io.IOException {
    return com.google.protobuf.GeneratedMessage
        .parseWithIOException(PARSER, input);
  }
  public static ai.stigmer.agentic.agentchannel.v1.AgentChannelSpec parseFrom(
      java.io.InputStream input,
      com.google.protobuf.ExtensionRegistryLite extensionRegistry)
      throws java.io.IOException {
    return com.google.protobuf.GeneratedMessage
        .parseWithIOException(PARSER, input, extensionRegistry);
  }

  public static ai.stigmer.agentic.agentchannel.v1.AgentChannelSpec parseDelimitedFrom(java.io.InputStream input)
      throws java.io.IOException {
    return com.google.protobuf.GeneratedMessage
        .parseDelimitedWithIOException(PARSER, input);
  }

  public static ai.stigmer.agentic.agentchannel.v1.AgentChannelSpec parseDelimitedFrom(
      java.io.InputStream input,
      com.google.protobuf.ExtensionRegistryLite extensionRegistry)
      throws java.io.IOException {
    return com.google.protobuf.GeneratedMessage
        .parseDelimitedWithIOException(PARSER, input, extensionRegistry);
  }
  public static ai.stigmer.agentic.agentchannel.v1.AgentChannelSpec parseFrom(
      com.google.protobuf.CodedInputStream input)
      throws java.io.IOException {
    return com.google.protobuf.GeneratedMessage
        .parseWithIOException(PARSER, input);
  }
  public static ai.stigmer.agentic.agentchannel.v1.AgentChannelSpec parseFrom(
      com.google.protobuf.CodedInputStream input,
      com.google.protobuf.ExtensionRegistryLite extensionRegistry)
      throws java.io.IOException {
    return com.google.protobuf.GeneratedMessage
        .parseWithIOException(PARSER, input, extensionRegistry);
  }

  @java.lang.Override
  public Builder newBuilderForType() { return newBuilder(); }
  public static Builder newBuilder() {
    return DEFAULT_INSTANCE.toBuilder();
  }
  public static Builder newBuilder(ai.stigmer.agentic.agentchannel.v1.AgentChannelSpec prototype) {
    return DEFAULT_INSTANCE.toBuilder().mergeFrom(prototype);
  }
  @java.lang.Override
  public Builder toBuilder() {
    return this == DEFAULT_INSTANCE
        ? new Builder() : new Builder().mergeFrom(this);
  }

  @java.lang.Override
  protected Builder newBuilderForType(
      com.google.protobuf.GeneratedMessage.BuilderParent parent) {
    Builder builder = new Builder(parent);
    return builder;
  }
  /**
   * <pre>
   * AgentChannelSpec defines the configurable properties of an agent channel.
   *
   * The spec is deliberately small: which agent serves the channel, whether
   * serving is enabled, which provider the channel targets, and which
   * environments supply the agent's tool credentials. Workspace identity and
   * provider credentials are produced by the install flow and live in
   * status — a declarative apply can never clobber them.
   *
   * &#64;internal
   * P1: the provider arms carry only genuinely user-declarable fields. For
   * the multi-tenant Slack app everything concrete (team_id, bot_user_id,
   * scopes, credentials env) is OAuth-observed → status. Owner-customizable
   * refusal copy (the AgentShareMessages analog) is deliberately deferred:
   * platform-default copy lives in the deliverer (decision 006); adding a
   * messages block later is a non-breaking change.
   * </pre>
   *
   * Protobuf type {@code ai.stigmer.agentic.agentchannel.v1.AgentChannelSpec}
   */
  public static final class Builder extends
      com.google.protobuf.GeneratedMessage.Builder<Builder> implements
      // @@protoc_insertion_point(builder_implements:ai.stigmer.agentic.agentchannel.v1.AgentChannelSpec)
      ai.stigmer.agentic.agentchannel.v1.AgentChannelSpecOrBuilder {
    public static final com.google.protobuf.Descriptors.Descriptor
        getDescriptor() {
      return ai.stigmer.agentic.agentchannel.v1.SpecProto.internal_static_ai_stigmer_agentic_agentchannel_v1_AgentChannelSpec_descriptor;
    }

    @java.lang.Override
    protected com.google.protobuf.GeneratedMessage.FieldAccessorTable
        internalGetFieldAccessorTable() {
      return ai.stigmer.agentic.agentchannel.v1.SpecProto.internal_static_ai_stigmer_agentic_agentchannel_v1_AgentChannelSpec_fieldAccessorTable
          .ensureFieldAccessorsInitialized(
              ai.stigmer.agentic.agentchannel.v1.AgentChannelSpec.class, ai.stigmer.agentic.agentchannel.v1.AgentChannelSpec.Builder.class);
    }

    // Construct using ai.stigmer.agentic.agentchannel.v1.AgentChannelSpec.newBuilder()
    private Builder() {
      maybeForceBuilderInitialization();
    }

    private Builder(
        com.google.protobuf.GeneratedMessage.BuilderParent parent) {
      super(parent);
      maybeForceBuilderInitialization();
    }
    private void maybeForceBuilderInitialization() {
      if (com.google.protobuf.GeneratedMessage
              .alwaysUseFieldBuilders) {
        internalGetAgentRefFieldBuilder();
        internalGetEnvironmentRefsFieldBuilder();
      }
    }
    @java.lang.Override
    public Builder clear() {
      super.clear();
      bitField0_ = 0;
      agentRef_ = null;
      if (agentRefBuilder_ != null) {
        agentRefBuilder_.dispose();
        agentRefBuilder_ = null;
      }
      enabled_ = false;
      if (slackBuilder_ != null) {
        slackBuilder_.clear();
      }
      if (environmentRefsBuilder_ == null) {
        environmentRefs_ = java.util.Collections.emptyList();
      } else {
        environmentRefs_ = null;
        environmentRefsBuilder_.clear();
      }
      bitField0_ = (bitField0_ & ~0x00000008);
      providerConfigCase_ = 0;
      providerConfig_ = null;
      return this;
    }

    @java.lang.Override
    public com.google.protobuf.Descriptors.Descriptor
        getDescriptorForType() {
      return ai.stigmer.agentic.agentchannel.v1.SpecProto.internal_static_ai_stigmer_agentic_agentchannel_v1_AgentChannelSpec_descriptor;
    }

    @java.lang.Override
    public ai.stigmer.agentic.agentchannel.v1.AgentChannelSpec getDefaultInstanceForType() {
      return ai.stigmer.agentic.agentchannel.v1.AgentChannelSpec.getDefaultInstance();
    }

    @java.lang.Override
    public ai.stigmer.agentic.agentchannel.v1.AgentChannelSpec build() {
      ai.stigmer.agentic.agentchannel.v1.AgentChannelSpec result = buildPartial();
      if (!result.isInitialized()) {
        throw newUninitializedMessageException(result);
      }
      return result;
    }

    @java.lang.Override
    public ai.stigmer.agentic.agentchannel.v1.AgentChannelSpec buildPartial() {
      ai.stigmer.agentic.agentchannel.v1.AgentChannelSpec result = new ai.stigmer.agentic.agentchannel.v1.AgentChannelSpec(this);
      buildPartialRepeatedFields(result);
      if (bitField0_ != 0) { buildPartial0(result); }
      buildPartialOneofs(result);
      onBuilt();
      return result;
    }

    private void buildPartialRepeatedFields(ai.stigmer.agentic.agentchannel.v1.AgentChannelSpec result) {
      if (environmentRefsBuilder_ == null) {
        if (((bitField0_ & 0x00000008) != 0)) {
          environmentRefs_ = java.util.Collections.unmodifiableList(environmentRefs_);
          bitField0_ = (bitField0_ & ~0x00000008);
        }
        result.environmentRefs_ = environmentRefs_;
      } else {
        result.environmentRefs_ = environmentRefsBuilder_.build();
      }
    }

    private void buildPartial0(ai.stigmer.agentic.agentchannel.v1.AgentChannelSpec result) {
      int from_bitField0_ = bitField0_;
      int to_bitField0_ = 0;
      if (((from_bitField0_ & 0x00000001) != 0)) {
        result.agentRef_ = agentRefBuilder_ == null
            ? agentRef_
            : agentRefBuilder_.build();
        to_bitField0_ |= 0x00000001;
      }
      if (((from_bitField0_ & 0x00000002) != 0)) {
        result.enabled_ = enabled_;
      }
      result.bitField0_ |= to_bitField0_;
    }

    private void buildPartialOneofs(ai.stigmer.agentic.agentchannel.v1.AgentChannelSpec result) {
      result.providerConfigCase_ = providerConfigCase_;
      result.providerConfig_ = this.providerConfig_;
      if (providerConfigCase_ == 3 &&
          slackBuilder_ != null) {
        result.providerConfig_ = slackBuilder_.build();
      }
    }

    @java.lang.Override
    public Builder mergeFrom(com.google.protobuf.Message other) {
      if (other instanceof ai.stigmer.agentic.agentchannel.v1.AgentChannelSpec) {
        return mergeFrom((ai.stigmer.agentic.agentchannel.v1.AgentChannelSpec)other);
      } else {
        super.mergeFrom(other);
        return this;
      }
    }

    public Builder mergeFrom(ai.stigmer.agentic.agentchannel.v1.AgentChannelSpec other) {
      if (other == ai.stigmer.agentic.agentchannel.v1.AgentChannelSpec.getDefaultInstance()) return this;
      if (other.hasAgentRef()) {
        mergeAgentRef(other.getAgentRef());
      }
      if (other.getEnabled() != false) {
        setEnabled(other.getEnabled());
      }
      if (environmentRefsBuilder_ == null) {
        if (!other.environmentRefs_.isEmpty()) {
          if (environmentRefs_.isEmpty()) {
            environmentRefs_ = other.environmentRefs_;
            bitField0_ = (bitField0_ & ~0x00000008);
          } else {
            ensureEnvironmentRefsIsMutable();
            environmentRefs_.addAll(other.environmentRefs_);
          }
          onChanged();
        }
      } else {
        if (!other.environmentRefs_.isEmpty()) {
          if (environmentRefsBuilder_.isEmpty()) {
            environmentRefsBuilder_.dispose();
            environmentRefsBuilder_ = null;
            environmentRefs_ = other.environmentRefs_;
            bitField0_ = (bitField0_ & ~0x00000008);
            environmentRefsBuilder_ = 
              com.google.protobuf.GeneratedMessage.alwaysUseFieldBuilders ?
                 internalGetEnvironmentRefsFieldBuilder() : null;
          } else {
            environmentRefsBuilder_.addAllMessages(other.environmentRefs_);
          }
        }
      }
      switch (other.getProviderConfigCase()) {
        case SLACK: {
          mergeSlack(other.getSlack());
          break;
        }
        case PROVIDERCONFIG_NOT_SET: {
          break;
        }
      }
      this.mergeUnknownFields(other.getUnknownFields());
      onChanged();
      return this;
    }

    @java.lang.Override
    public final boolean isInitialized() {
      return true;
    }

    @java.lang.Override
    public Builder mergeFrom(
        com.google.protobuf.CodedInputStream input,
        com.google.protobuf.ExtensionRegistryLite extensionRegistry)
        throws java.io.IOException {
      if (extensionRegistry == null) {
        throw new java.lang.NullPointerException();
      }
      try {
        boolean done = false;
        while (!done) {
          int tag = input.readTag();
          switch (tag) {
            case 0:
              done = true;
              break;
            case 10: {
              input.readMessage(
                  internalGetAgentRefFieldBuilder().getBuilder(),
                  extensionRegistry);
              bitField0_ |= 0x00000001;
              break;
            } // case 10
            case 16: {
              enabled_ = input.readBool();
              bitField0_ |= 0x00000002;
              break;
            } // case 16
            case 26: {
              input.readMessage(
                  internalGetSlackFieldBuilder().getBuilder(),
                  extensionRegistry);
              providerConfigCase_ = 3;
              break;
            } // case 26
            case 34: {
              ai.stigmer.commons.apiresource.ApiResourceReference m =
                  input.readMessage(
                      ai.stigmer.commons.apiresource.ApiResourceReference.parser(),
                      extensionRegistry);
              if (environmentRefsBuilder_ == null) {
                ensureEnvironmentRefsIsMutable();
                environmentRefs_.add(m);
              } else {
                environmentRefsBuilder_.addMessage(m);
              }
              break;
            } // case 34
            default: {
              if (!super.parseUnknownField(input, extensionRegistry, tag)) {
                done = true; // was an endgroup tag
              }
              break;
            } // default:
          } // switch (tag)
        } // while (!done)
      } catch (com.google.protobuf.InvalidProtocolBufferException e) {
        throw e.unwrapIOException();
      } finally {
        onChanged();
      } // finally
      return this;
    }
    private int providerConfigCase_ = 0;
    private java.lang.Object providerConfig_;
    public ProviderConfigCase
        getProviderConfigCase() {
      return ProviderConfigCase.forNumber(
          providerConfigCase_);
    }

    public Builder clearProviderConfig() {
      providerConfigCase_ = 0;
      providerConfig_ = null;
      onChanged();
      return this;
    }

    private int bitField0_;

    private ai.stigmer.commons.apiresource.ApiResourceReference agentRef_;
    private com.google.protobuf.SingleFieldBuilder<
        ai.stigmer.commons.apiresource.ApiResourceReference, ai.stigmer.commons.apiresource.ApiResourceReference.Builder, ai.stigmer.commons.apiresource.ApiResourceReferenceOrBuilder> agentRefBuilder_;
    /**
     * <pre>
     * Reference to the agent this channel serves.
     *
     * &#64;internal
     * Invariant (enforced in create/update/apply handlers of both editions,
     * mirroring AgentShare's Phase A rule): agent_ref.org must equal
     * metadata.org — the connection-owning org is the billing org and
     * provider credentials resolve in that org (decision 004; T06 invariant).
     * </pre>
     *
     * <code>.ai.stigmer.commons.apiresource.ApiResourceReference agent_ref = 1 [json_name = "agentRef", (.buf.validate.field) = { ... }</code>
     * @return Whether the agentRef field is set.
     */
    public boolean hasAgentRef() {
      return ((bitField0_ & 0x00000001) != 0);
    }
    /**
     * <pre>
     * Reference to the agent this channel serves.
     *
     * &#64;internal
     * Invariant (enforced in create/update/apply handlers of both editions,
     * mirroring AgentShare's Phase A rule): agent_ref.org must equal
     * metadata.org — the connection-owning org is the billing org and
     * provider credentials resolve in that org (decision 004; T06 invariant).
     * </pre>
     *
     * <code>.ai.stigmer.commons.apiresource.ApiResourceReference agent_ref = 1 [json_name = "agentRef", (.buf.validate.field) = { ... }</code>
     * @return The agentRef.
     */
    public ai.stigmer.commons.apiresource.ApiResourceReference getAgentRef() {
      if (agentRefBuilder_ == null) {
        return agentRef_ == null ? ai.stigmer.commons.apiresource.ApiResourceReference.getDefaultInstance() : agentRef_;
      } else {
        return agentRefBuilder_.getMessage();
      }
    }
    /**
     * <pre>
     * Reference to the agent this channel serves.
     *
     * &#64;internal
     * Invariant (enforced in create/update/apply handlers of both editions,
     * mirroring AgentShare's Phase A rule): agent_ref.org must equal
     * metadata.org — the connection-owning org is the billing org and
     * provider credentials resolve in that org (decision 004; T06 invariant).
     * </pre>
     *
     * <code>.ai.stigmer.commons.apiresource.ApiResourceReference agent_ref = 1 [json_name = "agentRef", (.buf.validate.field) = { ... }</code>
     */
    public Builder setAgentRef(ai.stigmer.commons.apiresource.ApiResourceReference value) {
      if (agentRefBuilder_ == null) {
        if (value == null) {
          throw new NullPointerException();
        }
        agentRef_ = value;
      } else {
        agentRefBuilder_.setMessage(value);
      }
      bitField0_ |= 0x00000001;
      onChanged();
      return this;
    }
    /**
     * <pre>
     * Reference to the agent this channel serves.
     *
     * &#64;internal
     * Invariant (enforced in create/update/apply handlers of both editions,
     * mirroring AgentShare's Phase A rule): agent_ref.org must equal
     * metadata.org — the connection-owning org is the billing org and
     * provider credentials resolve in that org (decision 004; T06 invariant).
     * </pre>
     *
     * <code>.ai.stigmer.commons.apiresource.ApiResourceReference agent_ref = 1 [json_name = "agentRef", (.buf.validate.field) = { ... }</code>
     */
    public Builder setAgentRef(
        ai.stigmer.commons.apiresource.ApiResourceReference.Builder builderForValue) {
      if (agentRefBuilder_ == null) {
        agentRef_ = builderForValue.build();
      } else {
        agentRefBuilder_.setMessage(builderForValue.build());
      }
      bitField0_ |= 0x00000001;
      onChanged();
      return this;
    }
    /**
     * <pre>
     * Reference to the agent this channel serves.
     *
     * &#64;internal
     * Invariant (enforced in create/update/apply handlers of both editions,
     * mirroring AgentShare's Phase A rule): agent_ref.org must equal
     * metadata.org — the connection-owning org is the billing org and
     * provider credentials resolve in that org (decision 004; T06 invariant).
     * </pre>
     *
     * <code>.ai.stigmer.commons.apiresource.ApiResourceReference agent_ref = 1 [json_name = "agentRef", (.buf.validate.field) = { ... }</code>
     */
    public Builder mergeAgentRef(ai.stigmer.commons.apiresource.ApiResourceReference value) {
      if (agentRefBuilder_ == null) {
        if (((bitField0_ & 0x00000001) != 0) &&
          agentRef_ != null &&
          agentRef_ != ai.stigmer.commons.apiresource.ApiResourceReference.getDefaultInstance()) {
          getAgentRefBuilder().mergeFrom(value);
        } else {
          agentRef_ = value;
        }
      } else {
        agentRefBuilder_.mergeFrom(value);
      }
      if (agentRef_ != null) {
        bitField0_ |= 0x00000001;
        onChanged();
      }
      return this;
    }
    /**
     * <pre>
     * Reference to the agent this channel serves.
     *
     * &#64;internal
     * Invariant (enforced in create/update/apply handlers of both editions,
     * mirroring AgentShare's Phase A rule): agent_ref.org must equal
     * metadata.org — the connection-owning org is the billing org and
     * provider credentials resolve in that org (decision 004; T06 invariant).
     * </pre>
     *
     * <code>.ai.stigmer.commons.apiresource.ApiResourceReference agent_ref = 1 [json_name = "agentRef", (.buf.validate.field) = { ... }</code>
     */
    public Builder clearAgentRef() {
      bitField0_ = (bitField0_ & ~0x00000001);
      agentRef_ = null;
      if (agentRefBuilder_ != null) {
        agentRefBuilder_.dispose();
        agentRefBuilder_ = null;
      }
      onChanged();
      return this;
    }
    /**
     * <pre>
     * Reference to the agent this channel serves.
     *
     * &#64;internal
     * Invariant (enforced in create/update/apply handlers of both editions,
     * mirroring AgentShare's Phase A rule): agent_ref.org must equal
     * metadata.org — the connection-owning org is the billing org and
     * provider credentials resolve in that org (decision 004; T06 invariant).
     * </pre>
     *
     * <code>.ai.stigmer.commons.apiresource.ApiResourceReference agent_ref = 1 [json_name = "agentRef", (.buf.validate.field) = { ... }</code>
     */
    public ai.stigmer.commons.apiresource.ApiResourceReference.Builder getAgentRefBuilder() {
      bitField0_ |= 0x00000001;
      onChanged();
      return internalGetAgentRefFieldBuilder().getBuilder();
    }
    /**
     * <pre>
     * Reference to the agent this channel serves.
     *
     * &#64;internal
     * Invariant (enforced in create/update/apply handlers of both editions,
     * mirroring AgentShare's Phase A rule): agent_ref.org must equal
     * metadata.org — the connection-owning org is the billing org and
     * provider credentials resolve in that org (decision 004; T06 invariant).
     * </pre>
     *
     * <code>.ai.stigmer.commons.apiresource.ApiResourceReference agent_ref = 1 [json_name = "agentRef", (.buf.validate.field) = { ... }</code>
     */
    public ai.stigmer.commons.apiresource.ApiResourceReferenceOrBuilder getAgentRefOrBuilder() {
      if (agentRefBuilder_ != null) {
        return agentRefBuilder_.getMessageOrBuilder();
      } else {
        return agentRef_ == null ?
            ai.stigmer.commons.apiresource.ApiResourceReference.getDefaultInstance() : agentRef_;
      }
    }
    /**
     * <pre>
     * Reference to the agent this channel serves.
     *
     * &#64;internal
     * Invariant (enforced in create/update/apply handlers of both editions,
     * mirroring AgentShare's Phase A rule): agent_ref.org must equal
     * metadata.org — the connection-owning org is the billing org and
     * provider credentials resolve in that org (decision 004; T06 invariant).
     * </pre>
     *
     * <code>.ai.stigmer.commons.apiresource.ApiResourceReference agent_ref = 1 [json_name = "agentRef", (.buf.validate.field) = { ... }</code>
     */
    private com.google.protobuf.SingleFieldBuilder<
        ai.stigmer.commons.apiresource.ApiResourceReference, ai.stigmer.commons.apiresource.ApiResourceReference.Builder, ai.stigmer.commons.apiresource.ApiResourceReferenceOrBuilder> 
        internalGetAgentRefFieldBuilder() {
      if (agentRefBuilder_ == null) {
        agentRefBuilder_ = new com.google.protobuf.SingleFieldBuilder<
            ai.stigmer.commons.apiresource.ApiResourceReference, ai.stigmer.commons.apiresource.ApiResourceReference.Builder, ai.stigmer.commons.apiresource.ApiResourceReferenceOrBuilder>(
                getAgentRef(),
                getParentForChildren(),
                isClean());
        agentRef_ = null;
      }
      return agentRefBuilder_;
    }

    private boolean enabled_ ;
    /**
     * <pre>
     * Whether serving traffic on this channel is enabled.
     *
     * Disabling is a config-preserving pause: the connection keeps its
     * install state and credentials, but inbound events are refused until
     * re-enabled. Delete the channel instead for a full teardown.
     *
     * &#64;internal
     * Decision 001 D-d. Pause must never drop the install: re-installing a
     * Slack app to re-auth is painful. The per-turn gate re-reads this live
     * (decision 003), so disabling takes effect on the next message.
     * </pre>
     *
     * <code>bool enabled = 2 [json_name = "enabled"];</code>
     * @return The enabled.
     */
    @java.lang.Override
    public boolean getEnabled() {
      return enabled_;
    }
    /**
     * <pre>
     * Whether serving traffic on this channel is enabled.
     *
     * Disabling is a config-preserving pause: the connection keeps its
     * install state and credentials, but inbound events are refused until
     * re-enabled. Delete the channel instead for a full teardown.
     *
     * &#64;internal
     * Decision 001 D-d. Pause must never drop the install: re-installing a
     * Slack app to re-auth is painful. The per-turn gate re-reads this live
     * (decision 003), so disabling takes effect on the next message.
     * </pre>
     *
     * <code>bool enabled = 2 [json_name = "enabled"];</code>
     * @param value The enabled to set.
     * @return This builder for chaining.
     */
    public Builder setEnabled(boolean value) {

      enabled_ = value;
      bitField0_ |= 0x00000002;
      onChanged();
      return this;
    }
    /**
     * <pre>
     * Whether serving traffic on this channel is enabled.
     *
     * Disabling is a config-preserving pause: the connection keeps its
     * install state and credentials, but inbound events are refused until
     * re-enabled. Delete the channel instead for a full teardown.
     *
     * &#64;internal
     * Decision 001 D-d. Pause must never drop the install: re-installing a
     * Slack app to re-auth is painful. The per-turn gate re-reads this live
     * (decision 003), so disabling takes effect on the next message.
     * </pre>
     *
     * <code>bool enabled = 2 [json_name = "enabled"];</code>
     * @return This builder for chaining.
     */
    public Builder clearEnabled() {
      bitField0_ = (bitField0_ & ~0x00000002);
      enabled_ = false;
      onChanged();
      return this;
    }

    private com.google.protobuf.SingleFieldBuilder<
        ai.stigmer.agentic.agentchannel.v1.SlackChannelConfig, ai.stigmer.agentic.agentchannel.v1.SlackChannelConfig.Builder, ai.stigmer.agentic.agentchannel.v1.SlackChannelConfigOrBuilder> slackBuilder_;
    /**
     * <pre>
     * Slack workspace connection (multi-tenant Stigmer Slack app).
     * </pre>
     *
     * <code>.ai.stigmer.agentic.agentchannel.v1.SlackChannelConfig slack = 3 [json_name = "slack"];</code>
     * @return Whether the slack field is set.
     */
    @java.lang.Override
    public boolean hasSlack() {
      return providerConfigCase_ == 3;
    }
    /**
     * <pre>
     * Slack workspace connection (multi-tenant Stigmer Slack app).
     * </pre>
     *
     * <code>.ai.stigmer.agentic.agentchannel.v1.SlackChannelConfig slack = 3 [json_name = "slack"];</code>
     * @return The slack.
     */
    @java.lang.Override
    public ai.stigmer.agentic.agentchannel.v1.SlackChannelConfig getSlack() {
      if (slackBuilder_ == null) {
        if (providerConfigCase_ == 3) {
          return (ai.stigmer.agentic.agentchannel.v1.SlackChannelConfig) providerConfig_;
        }
        return ai.stigmer.agentic.agentchannel.v1.SlackChannelConfig.getDefaultInstance();
      } else {
        if (providerConfigCase_ == 3) {
          return slackBuilder_.getMessage();
        }
        return ai.stigmer.agentic.agentchannel.v1.SlackChannelConfig.getDefaultInstance();
      }
    }
    /**
     * <pre>
     * Slack workspace connection (multi-tenant Stigmer Slack app).
     * </pre>
     *
     * <code>.ai.stigmer.agentic.agentchannel.v1.SlackChannelConfig slack = 3 [json_name = "slack"];</code>
     */
    public Builder setSlack(ai.stigmer.agentic.agentchannel.v1.SlackChannelConfig value) {
      if (slackBuilder_ == null) {
        if (value == null) {
          throw new NullPointerException();
        }
        providerConfig_ = value;
        onChanged();
      } else {
        slackBuilder_.setMessage(value);
      }
      providerConfigCase_ = 3;
      return this;
    }
    /**
     * <pre>
     * Slack workspace connection (multi-tenant Stigmer Slack app).
     * </pre>
     *
     * <code>.ai.stigmer.agentic.agentchannel.v1.SlackChannelConfig slack = 3 [json_name = "slack"];</code>
     */
    public Builder setSlack(
        ai.stigmer.agentic.agentchannel.v1.SlackChannelConfig.Builder builderForValue) {
      if (slackBuilder_ == null) {
        providerConfig_ = builderForValue.build();
        onChanged();
      } else {
        slackBuilder_.setMessage(builderForValue.build());
      }
      providerConfigCase_ = 3;
      return this;
    }
    /**
     * <pre>
     * Slack workspace connection (multi-tenant Stigmer Slack app).
     * </pre>
     *
     * <code>.ai.stigmer.agentic.agentchannel.v1.SlackChannelConfig slack = 3 [json_name = "slack"];</code>
     */
    public Builder mergeSlack(ai.stigmer.agentic.agentchannel.v1.SlackChannelConfig value) {
      if (slackBuilder_ == null) {
        if (providerConfigCase_ == 3 &&
            providerConfig_ != ai.stigmer.agentic.agentchannel.v1.SlackChannelConfig.getDefaultInstance()) {
          providerConfig_ = ai.stigmer.agentic.agentchannel.v1.SlackChannelConfig.newBuilder((ai.stigmer.agentic.agentchannel.v1.SlackChannelConfig) providerConfig_)
              .mergeFrom(value).buildPartial();
        } else {
          providerConfig_ = value;
        }
        onChanged();
      } else {
        if (providerConfigCase_ == 3) {
          slackBuilder_.mergeFrom(value);
        } else {
          slackBuilder_.setMessage(value);
        }
      }
      providerConfigCase_ = 3;
      return this;
    }
    /**
     * <pre>
     * Slack workspace connection (multi-tenant Stigmer Slack app).
     * </pre>
     *
     * <code>.ai.stigmer.agentic.agentchannel.v1.SlackChannelConfig slack = 3 [json_name = "slack"];</code>
     */
    public Builder clearSlack() {
      if (slackBuilder_ == null) {
        if (providerConfigCase_ == 3) {
          providerConfigCase_ = 0;
          providerConfig_ = null;
          onChanged();
        }
      } else {
        if (providerConfigCase_ == 3) {
          providerConfigCase_ = 0;
          providerConfig_ = null;
        }
        slackBuilder_.clear();
      }
      return this;
    }
    /**
     * <pre>
     * Slack workspace connection (multi-tenant Stigmer Slack app).
     * </pre>
     *
     * <code>.ai.stigmer.agentic.agentchannel.v1.SlackChannelConfig slack = 3 [json_name = "slack"];</code>
     */
    public ai.stigmer.agentic.agentchannel.v1.SlackChannelConfig.Builder getSlackBuilder() {
      return internalGetSlackFieldBuilder().getBuilder();
    }
    /**
     * <pre>
     * Slack workspace connection (multi-tenant Stigmer Slack app).
     * </pre>
     *
     * <code>.ai.stigmer.agentic.agentchannel.v1.SlackChannelConfig slack = 3 [json_name = "slack"];</code>
     */
    @java.lang.Override
    public ai.stigmer.agentic.agentchannel.v1.SlackChannelConfigOrBuilder getSlackOrBuilder() {
      if ((providerConfigCase_ == 3) && (slackBuilder_ != null)) {
        return slackBuilder_.getMessageOrBuilder();
      } else {
        if (providerConfigCase_ == 3) {
          return (ai.stigmer.agentic.agentchannel.v1.SlackChannelConfig) providerConfig_;
        }
        return ai.stigmer.agentic.agentchannel.v1.SlackChannelConfig.getDefaultInstance();
      }
    }
    /**
     * <pre>
     * Slack workspace connection (multi-tenant Stigmer Slack app).
     * </pre>
     *
     * <code>.ai.stigmer.agentic.agentchannel.v1.SlackChannelConfig slack = 3 [json_name = "slack"];</code>
     */
    private com.google.protobuf.SingleFieldBuilder<
        ai.stigmer.agentic.agentchannel.v1.SlackChannelConfig, ai.stigmer.agentic.agentchannel.v1.SlackChannelConfig.Builder, ai.stigmer.agentic.agentchannel.v1.SlackChannelConfigOrBuilder> 
        internalGetSlackFieldBuilder() {
      if (slackBuilder_ == null) {
        if (!(providerConfigCase_ == 3)) {
          providerConfig_ = ai.stigmer.agentic.agentchannel.v1.SlackChannelConfig.getDefaultInstance();
        }
        slackBuilder_ = new com.google.protobuf.SingleFieldBuilder<
            ai.stigmer.agentic.agentchannel.v1.SlackChannelConfig, ai.stigmer.agentic.agentchannel.v1.SlackChannelConfig.Builder, ai.stigmer.agentic.agentchannel.v1.SlackChannelConfigOrBuilder>(
                (ai.stigmer.agentic.agentchannel.v1.SlackChannelConfig) providerConfig_,
                getParentForChildren(),
                isClean());
        providerConfig_ = null;
      }
      providerConfigCase_ = 3;
      onChanged();
      return slackBuilder_;
    }

    private java.util.List<ai.stigmer.commons.apiresource.ApiResourceReference> environmentRefs_ =
      java.util.Collections.emptyList();
    private void ensureEnvironmentRefsIsMutable() {
      if (!((bitField0_ & 0x00000008) != 0)) {
        environmentRefs_ = new java.util.ArrayList<ai.stigmer.commons.apiresource.ApiResourceReference>(environmentRefs_);
        bitField0_ |= 0x00000008;
       }
    }

    private com.google.protobuf.RepeatedFieldBuilder<
        ai.stigmer.commons.apiresource.ApiResourceReference, ai.stigmer.commons.apiresource.ApiResourceReference.Builder, ai.stigmer.commons.apiresource.ApiResourceReferenceOrBuilder> environmentRefsBuilder_;

    /**
     * <pre>
     * References to Environment resources whose values are provided to
     * conversations on this channel.
     *
     * This is how a tool-using agent becomes chattable over a channel: bind
     * an org-shared environment holding the needed credentials (for example
     * a read-only API token), and channel executions receive its values at
     * runtime. The agent and its default instance stay untouched.
     *
     * &#64;internal
     * The AgentShareSpec.environment_refs analog (decision 011: sharing is
     * a channel — both connection kinds carry their own credentials).
     * Resolved in the channel's org through the org-shared environment
     * resolution seam (EnvironmentRuntimeResolutionService /
     * OrgSharedEnvironmentPolicy): each referenced environment must be
     * visibility_org in the channel's org, or the merge skips it with a
     * diagnostic. Merged at channel execution-context build time only,
     * lowest priority (instance refs and runtime_env override on key
     * conflicts) — never bound to the agent's system-managed default
     * instance. No write-time existence or visibility check, matching the
     * share: enforcement lives solely at runtime resolution, which fails
     * closed. Unlike the share there is no audience CEL — channels have no
     * audience concept, and the same-org invariant (agent_ref.org ==
     * metadata.org) already scopes resolution.
     * </pre>
     *
     * <code>repeated .ai.stigmer.commons.apiresource.ApiResourceReference environment_refs = 4 [json_name = "environmentRefs", (.buf.validate.field) = { ... }</code>
     */
    public java.util.List<ai.stigmer.commons.apiresource.ApiResourceReference> getEnvironmentRefsList() {
      if (environmentRefsBuilder_ == null) {
        return java.util.Collections.unmodifiableList(environmentRefs_);
      } else {
        return environmentRefsBuilder_.getMessageList();
      }
    }
    /**
     * <pre>
     * References to Environment resources whose values are provided to
     * conversations on this channel.
     *
     * This is how a tool-using agent becomes chattable over a channel: bind
     * an org-shared environment holding the needed credentials (for example
     * a read-only API token), and channel executions receive its values at
     * runtime. The agent and its default instance stay untouched.
     *
     * &#64;internal
     * The AgentShareSpec.environment_refs analog (decision 011: sharing is
     * a channel — both connection kinds carry their own credentials).
     * Resolved in the channel's org through the org-shared environment
     * resolution seam (EnvironmentRuntimeResolutionService /
     * OrgSharedEnvironmentPolicy): each referenced environment must be
     * visibility_org in the channel's org, or the merge skips it with a
     * diagnostic. Merged at channel execution-context build time only,
     * lowest priority (instance refs and runtime_env override on key
     * conflicts) — never bound to the agent's system-managed default
     * instance. No write-time existence or visibility check, matching the
     * share: enforcement lives solely at runtime resolution, which fails
     * closed. Unlike the share there is no audience CEL — channels have no
     * audience concept, and the same-org invariant (agent_ref.org ==
     * metadata.org) already scopes resolution.
     * </pre>
     *
     * <code>repeated .ai.stigmer.commons.apiresource.ApiResourceReference environment_refs = 4 [json_name = "environmentRefs", (.buf.validate.field) = { ... }</code>
     */
    public int getEnvironmentRefsCount() {
      if (environmentRefsBuilder_ == null) {
        return environmentRefs_.size();
      } else {
        return environmentRefsBuilder_.getCount();
      }
    }
    /**
     * <pre>
     * References to Environment resources whose values are provided to
     * conversations on this channel.
     *
     * This is how a tool-using agent becomes chattable over a channel: bind
     * an org-shared environment holding the needed credentials (for example
     * a read-only API token), and channel executions receive its values at
     * runtime. The agent and its default instance stay untouched.
     *
     * &#64;internal
     * The AgentShareSpec.environment_refs analog (decision 011: sharing is
     * a channel — both connection kinds carry their own credentials).
     * Resolved in the channel's org through the org-shared environment
     * resolution seam (EnvironmentRuntimeResolutionService /
     * OrgSharedEnvironmentPolicy): each referenced environment must be
     * visibility_org in the channel's org, or the merge skips it with a
     * diagnostic. Merged at channel execution-context build time only,
     * lowest priority (instance refs and runtime_env override on key
     * conflicts) — never bound to the agent's system-managed default
     * instance. No write-time existence or visibility check, matching the
     * share: enforcement lives solely at runtime resolution, which fails
     * closed. Unlike the share there is no audience CEL — channels have no
     * audience concept, and the same-org invariant (agent_ref.org ==
     * metadata.org) already scopes resolution.
     * </pre>
     *
     * <code>repeated .ai.stigmer.commons.apiresource.ApiResourceReference environment_refs = 4 [json_name = "environmentRefs", (.buf.validate.field) = { ... }</code>
     */
    public ai.stigmer.commons.apiresource.ApiResourceReference getEnvironmentRefs(int index) {
      if (environmentRefsBuilder_ == null) {
        return environmentRefs_.get(index);
      } else {
        return environmentRefsBuilder_.getMessage(index);
      }
    }
    /**
     * <pre>
     * References to Environment resources whose values are provided to
     * conversations on this channel.
     *
     * This is how a tool-using agent becomes chattable over a channel: bind
     * an org-shared environment holding the needed credentials (for example
     * a read-only API token), and channel executions receive its values at
     * runtime. The agent and its default instance stay untouched.
     *
     * &#64;internal
     * The AgentShareSpec.environment_refs analog (decision 011: sharing is
     * a channel — both connection kinds carry their own credentials).
     * Resolved in the channel's org through the org-shared environment
     * resolution seam (EnvironmentRuntimeResolutionService /
     * OrgSharedEnvironmentPolicy): each referenced environment must be
     * visibility_org in the channel's org, or the merge skips it with a
     * diagnostic. Merged at channel execution-context build time only,
     * lowest priority (instance refs and runtime_env override on key
     * conflicts) — never bound to the agent's system-managed default
     * instance. No write-time existence or visibility check, matching the
     * share: enforcement lives solely at runtime resolution, which fails
     * closed. Unlike the share there is no audience CEL — channels have no
     * audience concept, and the same-org invariant (agent_ref.org ==
     * metadata.org) already scopes resolution.
     * </pre>
     *
     * <code>repeated .ai.stigmer.commons.apiresource.ApiResourceReference environment_refs = 4 [json_name = "environmentRefs", (.buf.validate.field) = { ... }</code>
     */
    public Builder setEnvironmentRefs(
        int index, ai.stigmer.commons.apiresource.ApiResourceReference value) {
      if (environmentRefsBuilder_ == null) {
        if (value == null) {
          throw new NullPointerException();
        }
        ensureEnvironmentRefsIsMutable();
        environmentRefs_.set(index, value);
        onChanged();
      } else {
        environmentRefsBuilder_.setMessage(index, value);
      }
      return this;
    }
    /**
     * <pre>
     * References to Environment resources whose values are provided to
     * conversations on this channel.
     *
     * This is how a tool-using agent becomes chattable over a channel: bind
     * an org-shared environment holding the needed credentials (for example
     * a read-only API token), and channel executions receive its values at
     * runtime. The agent and its default instance stay untouched.
     *
     * &#64;internal
     * The AgentShareSpec.environment_refs analog (decision 011: sharing is
     * a channel — both connection kinds carry their own credentials).
     * Resolved in the channel's org through the org-shared environment
     * resolution seam (EnvironmentRuntimeResolutionService /
     * OrgSharedEnvironmentPolicy): each referenced environment must be
     * visibility_org in the channel's org, or the merge skips it with a
     * diagnostic. Merged at channel execution-context build time only,
     * lowest priority (instance refs and runtime_env override on key
     * conflicts) — never bound to the agent's system-managed default
     * instance. No write-time existence or visibility check, matching the
     * share: enforcement lives solely at runtime resolution, which fails
     * closed. Unlike the share there is no audience CEL — channels have no
     * audience concept, and the same-org invariant (agent_ref.org ==
     * metadata.org) already scopes resolution.
     * </pre>
     *
     * <code>repeated .ai.stigmer.commons.apiresource.ApiResourceReference environment_refs = 4 [json_name = "environmentRefs", (.buf.validate.field) = { ... }</code>
     */
    public Builder setEnvironmentRefs(
        int index, ai.stigmer.commons.apiresource.ApiResourceReference.Builder builderForValue) {
      if (environmentRefsBuilder_ == null) {
        ensureEnvironmentRefsIsMutable();
        environmentRefs_.set(index, builderForValue.build());
        onChanged();
      } else {
        environmentRefsBuilder_.setMessage(index, builderForValue.build());
      }
      return this;
    }
    /**
     * <pre>
     * References to Environment resources whose values are provided to
     * conversations on this channel.
     *
     * This is how a tool-using agent becomes chattable over a channel: bind
     * an org-shared environment holding the needed credentials (for example
     * a read-only API token), and channel executions receive its values at
     * runtime. The agent and its default instance stay untouched.
     *
     * &#64;internal
     * The AgentShareSpec.environment_refs analog (decision 011: sharing is
     * a channel — both connection kinds carry their own credentials).
     * Resolved in the channel's org through the org-shared environment
     * resolution seam (EnvironmentRuntimeResolutionService /
     * OrgSharedEnvironmentPolicy): each referenced environment must be
     * visibility_org in the channel's org, or the merge skips it with a
     * diagnostic. Merged at channel execution-context build time only,
     * lowest priority (instance refs and runtime_env override on key
     * conflicts) — never bound to the agent's system-managed default
     * instance. No write-time existence or visibility check, matching the
     * share: enforcement lives solely at runtime resolution, which fails
     * closed. Unlike the share there is no audience CEL — channels have no
     * audience concept, and the same-org invariant (agent_ref.org ==
     * metadata.org) already scopes resolution.
     * </pre>
     *
     * <code>repeated .ai.stigmer.commons.apiresource.ApiResourceReference environment_refs = 4 [json_name = "environmentRefs", (.buf.validate.field) = { ... }</code>
     */
    public Builder addEnvironmentRefs(ai.stigmer.commons.apiresource.ApiResourceReference value) {
      if (environmentRefsBuilder_ == null) {
        if (value == null) {
          throw new NullPointerException();
        }
        ensureEnvironmentRefsIsMutable();
        environmentRefs_.add(value);
        onChanged();
      } else {
        environmentRefsBuilder_.addMessage(value);
      }
      return this;
    }
    /**
     * <pre>
     * References to Environment resources whose values are provided to
     * conversations on this channel.
     *
     * This is how a tool-using agent becomes chattable over a channel: bind
     * an org-shared environment holding the needed credentials (for example
     * a read-only API token), and channel executions receive its values at
     * runtime. The agent and its default instance stay untouched.
     *
     * &#64;internal
     * The AgentShareSpec.environment_refs analog (decision 011: sharing is
     * a channel — both connection kinds carry their own credentials).
     * Resolved in the channel's org through the org-shared environment
     * resolution seam (EnvironmentRuntimeResolutionService /
     * OrgSharedEnvironmentPolicy): each referenced environment must be
     * visibility_org in the channel's org, or the merge skips it with a
     * diagnostic. Merged at channel execution-context build time only,
     * lowest priority (instance refs and runtime_env override on key
     * conflicts) — never bound to the agent's system-managed default
     * instance. No write-time existence or visibility check, matching the
     * share: enforcement lives solely at runtime resolution, which fails
     * closed. Unlike the share there is no audience CEL — channels have no
     * audience concept, and the same-org invariant (agent_ref.org ==
     * metadata.org) already scopes resolution.
     * </pre>
     *
     * <code>repeated .ai.stigmer.commons.apiresource.ApiResourceReference environment_refs = 4 [json_name = "environmentRefs", (.buf.validate.field) = { ... }</code>
     */
    public Builder addEnvironmentRefs(
        int index, ai.stigmer.commons.apiresource.ApiResourceReference value) {
      if (environmentRefsBuilder_ == null) {
        if (value == null) {
          throw new NullPointerException();
        }
        ensureEnvironmentRefsIsMutable();
        environmentRefs_.add(index, value);
        onChanged();
      } else {
        environmentRefsBuilder_.addMessage(index, value);
      }
      return this;
    }
    /**
     * <pre>
     * References to Environment resources whose values are provided to
     * conversations on this channel.
     *
     * This is how a tool-using agent becomes chattable over a channel: bind
     * an org-shared environment holding the needed credentials (for example
     * a read-only API token), and channel executions receive its values at
     * runtime. The agent and its default instance stay untouched.
     *
     * &#64;internal
     * The AgentShareSpec.environment_refs analog (decision 011: sharing is
     * a channel — both connection kinds carry their own credentials).
     * Resolved in the channel's org through the org-shared environment
     * resolution seam (EnvironmentRuntimeResolutionService /
     * OrgSharedEnvironmentPolicy): each referenced environment must be
     * visibility_org in the channel's org, or the merge skips it with a
     * diagnostic. Merged at channel execution-context build time only,
     * lowest priority (instance refs and runtime_env override on key
     * conflicts) — never bound to the agent's system-managed default
     * instance. No write-time existence or visibility check, matching the
     * share: enforcement lives solely at runtime resolution, which fails
     * closed. Unlike the share there is no audience CEL — channels have no
     * audience concept, and the same-org invariant (agent_ref.org ==
     * metadata.org) already scopes resolution.
     * </pre>
     *
     * <code>repeated .ai.stigmer.commons.apiresource.ApiResourceReference environment_refs = 4 [json_name = "environmentRefs", (.buf.validate.field) = { ... }</code>
     */
    public Builder addEnvironmentRefs(
        ai.stigmer.commons.apiresource.ApiResourceReference.Builder builderForValue) {
      if (environmentRefsBuilder_ == null) {
        ensureEnvironmentRefsIsMutable();
        environmentRefs_.add(builderForValue.build());
        onChanged();
      } else {
        environmentRefsBuilder_.addMessage(builderForValue.build());
      }
      return this;
    }
    /**
     * <pre>
     * References to Environment resources whose values are provided to
     * conversations on this channel.
     *
     * This is how a tool-using agent becomes chattable over a channel: bind
     * an org-shared environment holding the needed credentials (for example
     * a read-only API token), and channel executions receive its values at
     * runtime. The agent and its default instance stay untouched.
     *
     * &#64;internal
     * The AgentShareSpec.environment_refs analog (decision 011: sharing is
     * a channel — both connection kinds carry their own credentials).
     * Resolved in the channel's org through the org-shared environment
     * resolution seam (EnvironmentRuntimeResolutionService /
     * OrgSharedEnvironmentPolicy): each referenced environment must be
     * visibility_org in the channel's org, or the merge skips it with a
     * diagnostic. Merged at channel execution-context build time only,
     * lowest priority (instance refs and runtime_env override on key
     * conflicts) — never bound to the agent's system-managed default
     * instance. No write-time existence or visibility check, matching the
     * share: enforcement lives solely at runtime resolution, which fails
     * closed. Unlike the share there is no audience CEL — channels have no
     * audience concept, and the same-org invariant (agent_ref.org ==
     * metadata.org) already scopes resolution.
     * </pre>
     *
     * <code>repeated .ai.stigmer.commons.apiresource.ApiResourceReference environment_refs = 4 [json_name = "environmentRefs", (.buf.validate.field) = { ... }</code>
     */
    public Builder addEnvironmentRefs(
        int index, ai.stigmer.commons.apiresource.ApiResourceReference.Builder builderForValue) {
      if (environmentRefsBuilder_ == null) {
        ensureEnvironmentRefsIsMutable();
        environmentRefs_.add(index, builderForValue.build());
        onChanged();
      } else {
        environmentRefsBuilder_.addMessage(index, builderForValue.build());
      }
      return this;
    }
    /**
     * <pre>
     * References to Environment resources whose values are provided to
     * conversations on this channel.
     *
     * This is how a tool-using agent becomes chattable over a channel: bind
     * an org-shared environment holding the needed credentials (for example
     * a read-only API token), and channel executions receive its values at
     * runtime. The agent and its default instance stay untouched.
     *
     * &#64;internal
     * The AgentShareSpec.environment_refs analog (decision 011: sharing is
     * a channel — both connection kinds carry their own credentials).
     * Resolved in the channel's org through the org-shared environment
     * resolution seam (EnvironmentRuntimeResolutionService /
     * OrgSharedEnvironmentPolicy): each referenced environment must be
     * visibility_org in the channel's org, or the merge skips it with a
     * diagnostic. Merged at channel execution-context build time only,
     * lowest priority (instance refs and runtime_env override on key
     * conflicts) — never bound to the agent's system-managed default
     * instance. No write-time existence or visibility check, matching the
     * share: enforcement lives solely at runtime resolution, which fails
     * closed. Unlike the share there is no audience CEL — channels have no
     * audience concept, and the same-org invariant (agent_ref.org ==
     * metadata.org) already scopes resolution.
     * </pre>
     *
     * <code>repeated .ai.stigmer.commons.apiresource.ApiResourceReference environment_refs = 4 [json_name = "environmentRefs", (.buf.validate.field) = { ... }</code>
     */
    public Builder addAllEnvironmentRefs(
        java.lang.Iterable<? extends ai.stigmer.commons.apiresource.ApiResourceReference> values) {
      if (environmentRefsBuilder_ == null) {
        ensureEnvironmentRefsIsMutable();
        com.google.protobuf.AbstractMessageLite.Builder.addAll(
            values, environmentRefs_);
        onChanged();
      } else {
        environmentRefsBuilder_.addAllMessages(values);
      }
      return this;
    }
    /**
     * <pre>
     * References to Environment resources whose values are provided to
     * conversations on this channel.
     *
     * This is how a tool-using agent becomes chattable over a channel: bind
     * an org-shared environment holding the needed credentials (for example
     * a read-only API token), and channel executions receive its values at
     * runtime. The agent and its default instance stay untouched.
     *
     * &#64;internal
     * The AgentShareSpec.environment_refs analog (decision 011: sharing is
     * a channel — both connection kinds carry their own credentials).
     * Resolved in the channel's org through the org-shared environment
     * resolution seam (EnvironmentRuntimeResolutionService /
     * OrgSharedEnvironmentPolicy): each referenced environment must be
     * visibility_org in the channel's org, or the merge skips it with a
     * diagnostic. Merged at channel execution-context build time only,
     * lowest priority (instance refs and runtime_env override on key
     * conflicts) — never bound to the agent's system-managed default
     * instance. No write-time existence or visibility check, matching the
     * share: enforcement lives solely at runtime resolution, which fails
     * closed. Unlike the share there is no audience CEL — channels have no
     * audience concept, and the same-org invariant (agent_ref.org ==
     * metadata.org) already scopes resolution.
     * </pre>
     *
     * <code>repeated .ai.stigmer.commons.apiresource.ApiResourceReference environment_refs = 4 [json_name = "environmentRefs", (.buf.validate.field) = { ... }</code>
     */
    public Builder clearEnvironmentRefs() {
      if (environmentRefsBuilder_ == null) {
        environmentRefs_ = java.util.Collections.emptyList();
        bitField0_ = (bitField0_ & ~0x00000008);
        onChanged();
      } else {
        environmentRefsBuilder_.clear();
      }
      return this;
    }
    /**
     * <pre>
     * References to Environment resources whose values are provided to
     * conversations on this channel.
     *
     * This is how a tool-using agent becomes chattable over a channel: bind
     * an org-shared environment holding the needed credentials (for example
     * a read-only API token), and channel executions receive its values at
     * runtime. The agent and its default instance stay untouched.
     *
     * &#64;internal
     * The AgentShareSpec.environment_refs analog (decision 011: sharing is
     * a channel — both connection kinds carry their own credentials).
     * Resolved in the channel's org through the org-shared environment
     * resolution seam (EnvironmentRuntimeResolutionService /
     * OrgSharedEnvironmentPolicy): each referenced environment must be
     * visibility_org in the channel's org, or the merge skips it with a
     * diagnostic. Merged at channel execution-context build time only,
     * lowest priority (instance refs and runtime_env override on key
     * conflicts) — never bound to the agent's system-managed default
     * instance. No write-time existence or visibility check, matching the
     * share: enforcement lives solely at runtime resolution, which fails
     * closed. Unlike the share there is no audience CEL — channels have no
     * audience concept, and the same-org invariant (agent_ref.org ==
     * metadata.org) already scopes resolution.
     * </pre>
     *
     * <code>repeated .ai.stigmer.commons.apiresource.ApiResourceReference environment_refs = 4 [json_name = "environmentRefs", (.buf.validate.field) = { ... }</code>
     */
    public Builder removeEnvironmentRefs(int index) {
      if (environmentRefsBuilder_ == null) {
        ensureEnvironmentRefsIsMutable();
        environmentRefs_.remove(index);
        onChanged();
      } else {
        environmentRefsBuilder_.remove(index);
      }
      return this;
    }
    /**
     * <pre>
     * References to Environment resources whose values are provided to
     * conversations on this channel.
     *
     * This is how a tool-using agent becomes chattable over a channel: bind
     * an org-shared environment holding the needed credentials (for example
     * a read-only API token), and channel executions receive its values at
     * runtime. The agent and its default instance stay untouched.
     *
     * &#64;internal
     * The AgentShareSpec.environment_refs analog (decision 011: sharing is
     * a channel — both connection kinds carry their own credentials).
     * Resolved in the channel's org through the org-shared environment
     * resolution seam (EnvironmentRuntimeResolutionService /
     * OrgSharedEnvironmentPolicy): each referenced environment must be
     * visibility_org in the channel's org, or the merge skips it with a
     * diagnostic. Merged at channel execution-context build time only,
     * lowest priority (instance refs and runtime_env override on key
     * conflicts) — never bound to the agent's system-managed default
     * instance. No write-time existence or visibility check, matching the
     * share: enforcement lives solely at runtime resolution, which fails
     * closed. Unlike the share there is no audience CEL — channels have no
     * audience concept, and the same-org invariant (agent_ref.org ==
     * metadata.org) already scopes resolution.
     * </pre>
     *
     * <code>repeated .ai.stigmer.commons.apiresource.ApiResourceReference environment_refs = 4 [json_name = "environmentRefs", (.buf.validate.field) = { ... }</code>
     */
    public ai.stigmer.commons.apiresource.ApiResourceReference.Builder getEnvironmentRefsBuilder(
        int index) {
      return internalGetEnvironmentRefsFieldBuilder().getBuilder(index);
    }
    /**
     * <pre>
     * References to Environment resources whose values are provided to
     * conversations on this channel.
     *
     * This is how a tool-using agent becomes chattable over a channel: bind
     * an org-shared environment holding the needed credentials (for example
     * a read-only API token), and channel executions receive its values at
     * runtime. The agent and its default instance stay untouched.
     *
     * &#64;internal
     * The AgentShareSpec.environment_refs analog (decision 011: sharing is
     * a channel — both connection kinds carry their own credentials).
     * Resolved in the channel's org through the org-shared environment
     * resolution seam (EnvironmentRuntimeResolutionService /
     * OrgSharedEnvironmentPolicy): each referenced environment must be
     * visibility_org in the channel's org, or the merge skips it with a
     * diagnostic. Merged at channel execution-context build time only,
     * lowest priority (instance refs and runtime_env override on key
     * conflicts) — never bound to the agent's system-managed default
     * instance. No write-time existence or visibility check, matching the
     * share: enforcement lives solely at runtime resolution, which fails
     * closed. Unlike the share there is no audience CEL — channels have no
     * audience concept, and the same-org invariant (agent_ref.org ==
     * metadata.org) already scopes resolution.
     * </pre>
     *
     * <code>repeated .ai.stigmer.commons.apiresource.ApiResourceReference environment_refs = 4 [json_name = "environmentRefs", (.buf.validate.field) = { ... }</code>
     */
    public ai.stigmer.commons.apiresource.ApiResourceReferenceOrBuilder getEnvironmentRefsOrBuilder(
        int index) {
      if (environmentRefsBuilder_ == null) {
        return environmentRefs_.get(index);  } else {
        return environmentRefsBuilder_.getMessageOrBuilder(index);
      }
    }
    /**
     * <pre>
     * References to Environment resources whose values are provided to
     * conversations on this channel.
     *
     * This is how a tool-using agent becomes chattable over a channel: bind
     * an org-shared environment holding the needed credentials (for example
     * a read-only API token), and channel executions receive its values at
     * runtime. The agent and its default instance stay untouched.
     *
     * &#64;internal
     * The AgentShareSpec.environment_refs analog (decision 011: sharing is
     * a channel — both connection kinds carry their own credentials).
     * Resolved in the channel's org through the org-shared environment
     * resolution seam (EnvironmentRuntimeResolutionService /
     * OrgSharedEnvironmentPolicy): each referenced environment must be
     * visibility_org in the channel's org, or the merge skips it with a
     * diagnostic. Merged at channel execution-context build time only,
     * lowest priority (instance refs and runtime_env override on key
     * conflicts) — never bound to the agent's system-managed default
     * instance. No write-time existence or visibility check, matching the
     * share: enforcement lives solely at runtime resolution, which fails
     * closed. Unlike the share there is no audience CEL — channels have no
     * audience concept, and the same-org invariant (agent_ref.org ==
     * metadata.org) already scopes resolution.
     * </pre>
     *
     * <code>repeated .ai.stigmer.commons.apiresource.ApiResourceReference environment_refs = 4 [json_name = "environmentRefs", (.buf.validate.field) = { ... }</code>
     */
    public java.util.List<? extends ai.stigmer.commons.apiresource.ApiResourceReferenceOrBuilder> 
         getEnvironmentRefsOrBuilderList() {
      if (environmentRefsBuilder_ != null) {
        return environmentRefsBuilder_.getMessageOrBuilderList();
      } else {
        return java.util.Collections.unmodifiableList(environmentRefs_);
      }
    }
    /**
     * <pre>
     * References to Environment resources whose values are provided to
     * conversations on this channel.
     *
     * This is how a tool-using agent becomes chattable over a channel: bind
     * an org-shared environment holding the needed credentials (for example
     * a read-only API token), and channel executions receive its values at
     * runtime. The agent and its default instance stay untouched.
     *
     * &#64;internal
     * The AgentShareSpec.environment_refs analog (decision 011: sharing is
     * a channel — both connection kinds carry their own credentials).
     * Resolved in the channel's org through the org-shared environment
     * resolution seam (EnvironmentRuntimeResolutionService /
     * OrgSharedEnvironmentPolicy): each referenced environment must be
     * visibility_org in the channel's org, or the merge skips it with a
     * diagnostic. Merged at channel execution-context build time only,
     * lowest priority (instance refs and runtime_env override on key
     * conflicts) — never bound to the agent's system-managed default
     * instance. No write-time existence or visibility check, matching the
     * share: enforcement lives solely at runtime resolution, which fails
     * closed. Unlike the share there is no audience CEL — channels have no
     * audience concept, and the same-org invariant (agent_ref.org ==
     * metadata.org) already scopes resolution.
     * </pre>
     *
     * <code>repeated .ai.stigmer.commons.apiresource.ApiResourceReference environment_refs = 4 [json_name = "environmentRefs", (.buf.validate.field) = { ... }</code>
     */
    public ai.stigmer.commons.apiresource.ApiResourceReference.Builder addEnvironmentRefsBuilder() {
      return internalGetEnvironmentRefsFieldBuilder().addBuilder(
          ai.stigmer.commons.apiresource.ApiResourceReference.getDefaultInstance());
    }
    /**
     * <pre>
     * References to Environment resources whose values are provided to
     * conversations on this channel.
     *
     * This is how a tool-using agent becomes chattable over a channel: bind
     * an org-shared environment holding the needed credentials (for example
     * a read-only API token), and channel executions receive its values at
     * runtime. The agent and its default instance stay untouched.
     *
     * &#64;internal
     * The AgentShareSpec.environment_refs analog (decision 011: sharing is
     * a channel — both connection kinds carry their own credentials).
     * Resolved in the channel's org through the org-shared environment
     * resolution seam (EnvironmentRuntimeResolutionService /
     * OrgSharedEnvironmentPolicy): each referenced environment must be
     * visibility_org in the channel's org, or the merge skips it with a
     * diagnostic. Merged at channel execution-context build time only,
     * lowest priority (instance refs and runtime_env override on key
     * conflicts) — never bound to the agent's system-managed default
     * instance. No write-time existence or visibility check, matching the
     * share: enforcement lives solely at runtime resolution, which fails
     * closed. Unlike the share there is no audience CEL — channels have no
     * audience concept, and the same-org invariant (agent_ref.org ==
     * metadata.org) already scopes resolution.
     * </pre>
     *
     * <code>repeated .ai.stigmer.commons.apiresource.ApiResourceReference environment_refs = 4 [json_name = "environmentRefs", (.buf.validate.field) = { ... }</code>
     */
    public ai.stigmer.commons.apiresource.ApiResourceReference.Builder addEnvironmentRefsBuilder(
        int index) {
      return internalGetEnvironmentRefsFieldBuilder().addBuilder(
          index, ai.stigmer.commons.apiresource.ApiResourceReference.getDefaultInstance());
    }
    /**
     * <pre>
     * References to Environment resources whose values are provided to
     * conversations on this channel.
     *
     * This is how a tool-using agent becomes chattable over a channel: bind
     * an org-shared environment holding the needed credentials (for example
     * a read-only API token), and channel executions receive its values at
     * runtime. The agent and its default instance stay untouched.
     *
     * &#64;internal
     * The AgentShareSpec.environment_refs analog (decision 011: sharing is
     * a channel — both connection kinds carry their own credentials).
     * Resolved in the channel's org through the org-shared environment
     * resolution seam (EnvironmentRuntimeResolutionService /
     * OrgSharedEnvironmentPolicy): each referenced environment must be
     * visibility_org in the channel's org, or the merge skips it with a
     * diagnostic. Merged at channel execution-context build time only,
     * lowest priority (instance refs and runtime_env override on key
     * conflicts) — never bound to the agent's system-managed default
     * instance. No write-time existence or visibility check, matching the
     * share: enforcement lives solely at runtime resolution, which fails
     * closed. Unlike the share there is no audience CEL — channels have no
     * audience concept, and the same-org invariant (agent_ref.org ==
     * metadata.org) already scopes resolution.
     * </pre>
     *
     * <code>repeated .ai.stigmer.commons.apiresource.ApiResourceReference environment_refs = 4 [json_name = "environmentRefs", (.buf.validate.field) = { ... }</code>
     */
    public java.util.List<ai.stigmer.commons.apiresource.ApiResourceReference.Builder> 
         getEnvironmentRefsBuilderList() {
      return internalGetEnvironmentRefsFieldBuilder().getBuilderList();
    }
    private com.google.protobuf.RepeatedFieldBuilder<
        ai.stigmer.commons.apiresource.ApiResourceReference, ai.stigmer.commons.apiresource.ApiResourceReference.Builder, ai.stigmer.commons.apiresource.ApiResourceReferenceOrBuilder> 
        internalGetEnvironmentRefsFieldBuilder() {
      if (environmentRefsBuilder_ == null) {
        environmentRefsBuilder_ = new com.google.protobuf.RepeatedFieldBuilder<
            ai.stigmer.commons.apiresource.ApiResourceReference, ai.stigmer.commons.apiresource.ApiResourceReference.Builder, ai.stigmer.commons.apiresource.ApiResourceReferenceOrBuilder>(
                environmentRefs_,
                ((bitField0_ & 0x00000008) != 0),
                getParentForChildren(),
                isClean());
        environmentRefs_ = null;
      }
      return environmentRefsBuilder_;
    }

    // @@protoc_insertion_point(builder_scope:ai.stigmer.agentic.agentchannel.v1.AgentChannelSpec)
  }

  // @@protoc_insertion_point(class_scope:ai.stigmer.agentic.agentchannel.v1.AgentChannelSpec)
  private static final ai.stigmer.agentic.agentchannel.v1.AgentChannelSpec DEFAULT_INSTANCE;
  static {
    DEFAULT_INSTANCE = new ai.stigmer.agentic.agentchannel.v1.AgentChannelSpec();
  }

  public static ai.stigmer.agentic.agentchannel.v1.AgentChannelSpec getDefaultInstance() {
    return DEFAULT_INSTANCE;
  }

  private static final com.google.protobuf.Parser<AgentChannelSpec>
      PARSER = new com.google.protobuf.AbstractParser<AgentChannelSpec>() {
    @java.lang.Override
    public AgentChannelSpec parsePartialFrom(
        com.google.protobuf.CodedInputStream input,
        com.google.protobuf.ExtensionRegistryLite extensionRegistry)
        throws com.google.protobuf.InvalidProtocolBufferException {
      Builder builder = newBuilder();
      try {
        builder.mergeFrom(input, extensionRegistry);
      } catch (com.google.protobuf.InvalidProtocolBufferException e) {
        throw e.setUnfinishedMessage(builder.buildPartial());
      } catch (com.google.protobuf.UninitializedMessageException e) {
        throw e.asInvalidProtocolBufferException().setUnfinishedMessage(builder.buildPartial());
      } catch (java.io.IOException e) {
        throw new com.google.protobuf.InvalidProtocolBufferException(e)
            .setUnfinishedMessage(builder.buildPartial());
      }
      return builder.buildPartial();
    }
  };

  public static com.google.protobuf.Parser<AgentChannelSpec> parser() {
    return PARSER;
  }

  @java.lang.Override
  public com.google.protobuf.Parser<AgentChannelSpec> getParserForType() {
    return PARSER;
  }

  @java.lang.Override
  public ai.stigmer.agentic.agentchannel.v1.AgentChannelSpec getDefaultInstanceForType() {
    return DEFAULT_INSTANCE;
  }

}

