// Generated by the protocol buffer compiler.  DO NOT EDIT!
// NO CHECKED-IN PROTOBUF GENCODE
// source: ai/stigmer/agentic/agentexecution/v1/spec.proto
// Protobuf Java Version: 4.34.0

package ai.stigmer.agentic.agentexecution.v1;

/**
 * <pre>
 * AgentExecutionSpec contains only user-provided inputs for triggering an execution.
 * All execution results and state live in AgentExecutionStatus (in api.proto).
 * </pre>
 *
 * Protobuf type {@code ai.stigmer.agentic.agentexecution.v1.AgentExecutionSpec}
 */
@com.google.protobuf.Generated
public final class AgentExecutionSpec extends
    com.google.protobuf.GeneratedMessage implements
    // @@protoc_insertion_point(message_implements:ai.stigmer.agentic.agentexecution.v1.AgentExecutionSpec)
    AgentExecutionSpecOrBuilder {
private static final long serialVersionUID = 0L;
  static {
    com.google.protobuf.RuntimeVersion.validateProtobufGencodeVersion(
      com.google.protobuf.RuntimeVersion.RuntimeDomain.PUBLIC,
      /* major= */ 4,
      /* minor= */ 34,
      /* patch= */ 0,
      /* suffix= */ "",
      "AgentExecutionSpec");
  }
  // Use AgentExecutionSpec.newBuilder() to construct.
  private AgentExecutionSpec(com.google.protobuf.GeneratedMessage.Builder<?> builder) {
    super(builder);
  }
  private AgentExecutionSpec() {
    sessionId_ = "";
    agentId_ = "";
    message_ = "";
    callbackToken_ = com.google.protobuf.ByteString.EMPTY;
    parentWorkflowId_ = "";
    attachments_ = java.util.Collections.emptyList();
    workspaceFileRefs_ =
        com.google.protobuf.LazyStringArrayList.emptyList();
    activityTaskQueue_ = "";
    supersedesExecutionId_ = "";
  }

  public static final com.google.protobuf.Descriptors.Descriptor
      getDescriptor() {
    return ai.stigmer.agentic.agentexecution.v1.SpecProto.internal_static_ai_stigmer_agentic_agentexecution_v1_AgentExecutionSpec_descriptor;
  }

  @java.lang.Override
  public com.google.protobuf.Descriptors.Descriptor getDescriptorForType() {
    return ai.stigmer.agentic.agentexecution.v1.SpecProto.internal_static_ai_stigmer_agentic_agentexecution_v1_AgentExecutionSpec_descriptor;
  }

  @SuppressWarnings({"rawtypes"})
  @java.lang.Override
  protected com.google.protobuf.MapFieldReflectionAccessor internalGetMapFieldReflection(
      int number) {
    switch (number) {
      case 5:
        return internalGetRuntimeEnv();
      default:
        throw new RuntimeException(
            "Invalid map field number: " + number);
    }
  }
  @java.lang.Override
  protected com.google.protobuf.GeneratedMessage.FieldAccessorTable
      internalGetFieldAccessorTable() {
    return ai.stigmer.agentic.agentexecution.v1.SpecProto.internal_static_ai_stigmer_agentic_agentexecution_v1_AgentExecutionSpec_fieldAccessorTable
        .ensureFieldAccessorsInitialized(
            ai.stigmer.agentic.agentexecution.v1.AgentExecutionSpec.class, ai.stigmer.agentic.agentexecution.v1.AgentExecutionSpec.Builder.class);
  }

  private int bitField0_;
  public static final int SESSION_ID_FIELD_NUMBER = 1;
  @SuppressWarnings("serial")
  private volatile java.lang.Object sessionId_ = "";
  /**
   * <pre>
   * Session ID this execution belongs to (optional).
   *
   * Resolution priority (enforced in handler pipeline):
   * 1. session_id provided     -&gt; use existing session
   * 2. session_spec provided   -&gt; auto-create session from the embedded spec
   * 3. agent_id provided       -&gt; auto-create session using agent's default instance
   * 4. neither provided        -&gt; resolve platform default agent (label
   * stigmer.ai/default-agent + visibility_public), then auto-create session
   *
   * session_id and agent_id may both be set — when both are present, session_id
   * is used for session resolution and agent_id is preserved as metadata for
   * downstream consumers (e.g., session subject generation). session_id and
   * session_spec are mutually exclusive.
   * </pre>
   *
   * <code>string session_id = 1 [json_name = "sessionId"];</code>
   * @return The sessionId.
   */
  @java.lang.Override
  public java.lang.String getSessionId() {
    java.lang.Object ref = sessionId_;
    if (ref instanceof java.lang.String) {
      return (java.lang.String) ref;
    } else {
      com.google.protobuf.ByteString bs = 
          (com.google.protobuf.ByteString) ref;
      java.lang.String s = bs.toStringUtf8();
      sessionId_ = s;
      return s;
    }
  }
  /**
   * <pre>
   * Session ID this execution belongs to (optional).
   *
   * Resolution priority (enforced in handler pipeline):
   * 1. session_id provided     -&gt; use existing session
   * 2. session_spec provided   -&gt; auto-create session from the embedded spec
   * 3. agent_id provided       -&gt; auto-create session using agent's default instance
   * 4. neither provided        -&gt; resolve platform default agent (label
   * stigmer.ai/default-agent + visibility_public), then auto-create session
   *
   * session_id and agent_id may both be set — when both are present, session_id
   * is used for session resolution and agent_id is preserved as metadata for
   * downstream consumers (e.g., session subject generation). session_id and
   * session_spec are mutually exclusive.
   * </pre>
   *
   * <code>string session_id = 1 [json_name = "sessionId"];</code>
   * @return The bytes for sessionId.
   */
  @java.lang.Override
  public com.google.protobuf.ByteString
      getSessionIdBytes() {
    java.lang.Object ref = sessionId_;
    if (ref instanceof java.lang.String) {
      com.google.protobuf.ByteString b = 
          com.google.protobuf.ByteString.copyFromUtf8(
              (java.lang.String) ref);
      sessionId_ = b;
      return b;
    } else {
      return (com.google.protobuf.ByteString) ref;
    }
  }

  public static final int AGENT_ID_FIELD_NUMBER = 2;
  @SuppressWarnings("serial")
  private volatile java.lang.Object agentId_ = "";
  /**
   * <pre>
   * Agent ID (optional).
   *
   * When absent along with session_id, the backend resolves the platform's
   * public default agent — the agent labeled stigmer.ai/default-agent: "true"
   * with visibility_public. This enables session-first UX where users start
   * a conversation without choosing an agent.
   *
   * Both may be set — agent_id is preserved on the execution record even when
   * session_id is present, so downstream consumers can access the agent
   * without resolving through the session chain.
   *
   * When provided without session_id, a new session is auto-created using
   * the agent's default instance ID.
   * </pre>
   *
   * <code>string agent_id = 2 [json_name = "agentId"];</code>
   * @return The agentId.
   */
  @java.lang.Override
  public java.lang.String getAgentId() {
    java.lang.Object ref = agentId_;
    if (ref instanceof java.lang.String) {
      return (java.lang.String) ref;
    } else {
      com.google.protobuf.ByteString bs = 
          (com.google.protobuf.ByteString) ref;
      java.lang.String s = bs.toStringUtf8();
      agentId_ = s;
      return s;
    }
  }
  /**
   * <pre>
   * Agent ID (optional).
   *
   * When absent along with session_id, the backend resolves the platform's
   * public default agent — the agent labeled stigmer.ai/default-agent: "true"
   * with visibility_public. This enables session-first UX where users start
   * a conversation without choosing an agent.
   *
   * Both may be set — agent_id is preserved on the execution record even when
   * session_id is present, so downstream consumers can access the agent
   * without resolving through the session chain.
   *
   * When provided without session_id, a new session is auto-created using
   * the agent's default instance ID.
   * </pre>
   *
   * <code>string agent_id = 2 [json_name = "agentId"];</code>
   * @return The bytes for agentId.
   */
  @java.lang.Override
  public com.google.protobuf.ByteString
      getAgentIdBytes() {
    java.lang.Object ref = agentId_;
    if (ref instanceof java.lang.String) {
      com.google.protobuf.ByteString b = 
          com.google.protobuf.ByteString.copyFromUtf8(
              (java.lang.String) ref);
      agentId_ = b;
      return b;
    } else {
      return (com.google.protobuf.ByteString) ref;
    }
  }

  public static final int SESSION_SPEC_FIELD_NUMBER = 13;
  private ai.stigmer.agentic.session.v1.SessionSpec sessionSpec_;
  /**
   * <pre>
   * Spec for the session to auto-create when session_id is empty (optional).
   *
   * This is the one-call session bootstrap: a single create carries the full
   * session shape (workspace_entries, harness, execution_target, MCP servers,
   * skills) together with the first message, so embedders do not need to
   * orchestrate session.create followed by agentExecution.create. The created
   * session's ID is returned on the persisted execution's session_id.
   *
   * Fields that must be set at session-creation time and are immutable once
   * an execution has run — harness and execution_target — can only reach an
   * auto-created session through this field.
   *
   * When session_spec.agent_instance_id is set, the session runs against that
   * instance and agent_id must not also be resolved from it. When empty, the
   * normal resolution applies: agent_id's default instance, or the platform
   * default agent when agent_id is also empty.
   *
   * Mutually exclusive with session_id. session_spec.harness_state_id must be
   * empty — it is server-owned harness continuity state, created by the runner
   * after the first execution.
   *
   * &#64;internal
   * The Session resource created from this spec is the single source of truth
   * for session configuration. The handler clears this field after the session
   * is created (before persist), so the execution record never carries a
   * second copy of session config that could drift as the session evolves.
   * Extends the existing auto-create path in createSessionIfNeededStep rather
   * than adding a parallel one (stigmer/stigmer#249).
   * </pre>
   *
   * <code>.ai.stigmer.agentic.session.v1.SessionSpec session_spec = 13 [json_name = "sessionSpec"];</code>
   * @return Whether the sessionSpec field is set.
   */
  @java.lang.Override
  public boolean hasSessionSpec() {
    return ((bitField0_ & 0x00000001) != 0);
  }
  /**
   * <pre>
   * Spec for the session to auto-create when session_id is empty (optional).
   *
   * This is the one-call session bootstrap: a single create carries the full
   * session shape (workspace_entries, harness, execution_target, MCP servers,
   * skills) together with the first message, so embedders do not need to
   * orchestrate session.create followed by agentExecution.create. The created
   * session's ID is returned on the persisted execution's session_id.
   *
   * Fields that must be set at session-creation time and are immutable once
   * an execution has run — harness and execution_target — can only reach an
   * auto-created session through this field.
   *
   * When session_spec.agent_instance_id is set, the session runs against that
   * instance and agent_id must not also be resolved from it. When empty, the
   * normal resolution applies: agent_id's default instance, or the platform
   * default agent when agent_id is also empty.
   *
   * Mutually exclusive with session_id. session_spec.harness_state_id must be
   * empty — it is server-owned harness continuity state, created by the runner
   * after the first execution.
   *
   * &#64;internal
   * The Session resource created from this spec is the single source of truth
   * for session configuration. The handler clears this field after the session
   * is created (before persist), so the execution record never carries a
   * second copy of session config that could drift as the session evolves.
   * Extends the existing auto-create path in createSessionIfNeededStep rather
   * than adding a parallel one (stigmer/stigmer#249).
   * </pre>
   *
   * <code>.ai.stigmer.agentic.session.v1.SessionSpec session_spec = 13 [json_name = "sessionSpec"];</code>
   * @return The sessionSpec.
   */
  @java.lang.Override
  public ai.stigmer.agentic.session.v1.SessionSpec getSessionSpec() {
    return sessionSpec_ == null ? ai.stigmer.agentic.session.v1.SessionSpec.getDefaultInstance() : sessionSpec_;
  }
  /**
   * <pre>
   * Spec for the session to auto-create when session_id is empty (optional).
   *
   * This is the one-call session bootstrap: a single create carries the full
   * session shape (workspace_entries, harness, execution_target, MCP servers,
   * skills) together with the first message, so embedders do not need to
   * orchestrate session.create followed by agentExecution.create. The created
   * session's ID is returned on the persisted execution's session_id.
   *
   * Fields that must be set at session-creation time and are immutable once
   * an execution has run — harness and execution_target — can only reach an
   * auto-created session through this field.
   *
   * When session_spec.agent_instance_id is set, the session runs against that
   * instance and agent_id must not also be resolved from it. When empty, the
   * normal resolution applies: agent_id's default instance, or the platform
   * default agent when agent_id is also empty.
   *
   * Mutually exclusive with session_id. session_spec.harness_state_id must be
   * empty — it is server-owned harness continuity state, created by the runner
   * after the first execution.
   *
   * &#64;internal
   * The Session resource created from this spec is the single source of truth
   * for session configuration. The handler clears this field after the session
   * is created (before persist), so the execution record never carries a
   * second copy of session config that could drift as the session evolves.
   * Extends the existing auto-create path in createSessionIfNeededStep rather
   * than adding a parallel one (stigmer/stigmer#249).
   * </pre>
   *
   * <code>.ai.stigmer.agentic.session.v1.SessionSpec session_spec = 13 [json_name = "sessionSpec"];</code>
   */
  @java.lang.Override
  public ai.stigmer.agentic.session.v1.SessionSpecOrBuilder getSessionSpecOrBuilder() {
    return sessionSpec_ == null ? ai.stigmer.agentic.session.v1.SessionSpec.getDefaultInstance() : sessionSpec_;
  }

  public static final int MESSAGE_FIELD_NUMBER = 3;
  @SuppressWarnings("serial")
  private volatile java.lang.Object message_ = "";
  /**
   * <pre>
   * User input message that triggers this execution.
   * Each execution represents one user message and the agent's response.
   * </pre>
   *
   * <code>string message = 3 [json_name = "message", (.buf.validate.field) = { ... }</code>
   * @return The message.
   */
  @java.lang.Override
  public java.lang.String getMessage() {
    java.lang.Object ref = message_;
    if (ref instanceof java.lang.String) {
      return (java.lang.String) ref;
    } else {
      com.google.protobuf.ByteString bs = 
          (com.google.protobuf.ByteString) ref;
      java.lang.String s = bs.toStringUtf8();
      message_ = s;
      return s;
    }
  }
  /**
   * <pre>
   * User input message that triggers this execution.
   * Each execution represents one user message and the agent's response.
   * </pre>
   *
   * <code>string message = 3 [json_name = "message", (.buf.validate.field) = { ... }</code>
   * @return The bytes for message.
   */
  @java.lang.Override
  public com.google.protobuf.ByteString
      getMessageBytes() {
    java.lang.Object ref = message_;
    if (ref instanceof java.lang.String) {
      com.google.protobuf.ByteString b = 
          com.google.protobuf.ByteString.copyFromUtf8(
              (java.lang.String) ref);
      message_ = b;
      return b;
    } else {
      return (com.google.protobuf.ByteString) ref;
    }
  }

  public static final int EXECUTION_CONFIG_FIELD_NUMBER = 4;
  private ai.stigmer.agentic.agentexecution.v1.ExecutionConfig executionConfig_;
  /**
   * <pre>
   * Optional execution-time configuration overrides.
   * Example: Specify the model to use for this execution.
   * </pre>
   *
   * <code>.ai.stigmer.agentic.agentexecution.v1.ExecutionConfig execution_config = 4 [json_name = "executionConfig"];</code>
   * @return Whether the executionConfig field is set.
   */
  @java.lang.Override
  public boolean hasExecutionConfig() {
    return ((bitField0_ & 0x00000002) != 0);
  }
  /**
   * <pre>
   * Optional execution-time configuration overrides.
   * Example: Specify the model to use for this execution.
   * </pre>
   *
   * <code>.ai.stigmer.agentic.agentexecution.v1.ExecutionConfig execution_config = 4 [json_name = "executionConfig"];</code>
   * @return The executionConfig.
   */
  @java.lang.Override
  public ai.stigmer.agentic.agentexecution.v1.ExecutionConfig getExecutionConfig() {
    return executionConfig_ == null ? ai.stigmer.agentic.agentexecution.v1.ExecutionConfig.getDefaultInstance() : executionConfig_;
  }
  /**
   * <pre>
   * Optional execution-time configuration overrides.
   * Example: Specify the model to use for this execution.
   * </pre>
   *
   * <code>.ai.stigmer.agentic.agentexecution.v1.ExecutionConfig execution_config = 4 [json_name = "executionConfig"];</code>
   */
  @java.lang.Override
  public ai.stigmer.agentic.agentexecution.v1.ExecutionConfigOrBuilder getExecutionConfigOrBuilder() {
    return executionConfig_ == null ? ai.stigmer.agentic.agentexecution.v1.ExecutionConfig.getDefaultInstance() : executionConfig_;
  }

  public static final int RUNTIME_ENV_FIELD_NUMBER = 5;
  private static final class RuntimeEnvDefaultEntryHolder {
    static final com.google.protobuf.MapEntry<
        java.lang.String, ai.stigmer.agentic.executioncontext.v1.ExecutionValue> defaultEntry =
            com.google.protobuf.MapEntry
            .<java.lang.String, ai.stigmer.agentic.executioncontext.v1.ExecutionValue>newDefaultInstance(
                ai.stigmer.agentic.agentexecution.v1.SpecProto.internal_static_ai_stigmer_agentic_agentexecution_v1_AgentExecutionSpec_RuntimeEnvEntry_descriptor, 
                com.google.protobuf.WireFormat.FieldType.STRING,
                "",
                com.google.protobuf.WireFormat.FieldType.MESSAGE,
                ai.stigmer.agentic.executioncontext.v1.ExecutionValue.getDefaultInstance());
  }
  @SuppressWarnings("serial")
  private com.google.protobuf.MapField<
      java.lang.String, ai.stigmer.agentic.executioncontext.v1.ExecutionValue> runtimeEnv_;
  private com.google.protobuf.MapField<java.lang.String, ai.stigmer.agentic.executioncontext.v1.ExecutionValue>
  internalGetRuntimeEnv() {
    if (runtimeEnv_ == null) {
      return com.google.protobuf.MapField.emptyMapField(
          RuntimeEnvDefaultEntryHolder.defaultEntry);
    }
    return runtimeEnv_;
  }
  public int getRuntimeEnvCount() {
    return internalGetRuntimeEnv().getMap().size();
  }
  /**
   * <pre>
   * Runtime environment variables and secrets (execution-scoped).
   * These values are only available for this specific execution and take the
   * highest merge priority, overriding values from Environments bound via
   * environment_refs. A key must be declared in Agent.spec.env to survive the
   * merge: the agent env map is a declaration whitelist (name + is_secret +
   * optional), never a value source — undeclared keys are dropped.
   * Use case: B2B integrations where secrets are injected at runtime per call.
   * These values are consumed into the ExecutionContext (deleted when the
   * execution completes) and cleared from the persisted execution.
   *
   * &#64;internal
   * Merge priority (lowest to highest): resolved Environment values — from the
   * creating schedule's or agent_call task's environment_refs (when present),
   * then the instance's environment_refs; a later ref wins — then runtime_env
   * (this field). The merged map is filtered to the keys declared in
   * Agent.spec.env (no filtering when the agent declares none); a missing
   * required key only logs a warning — the run is not failed. The merge is
   * owned by backend/libs/go/envmerge and the agentexecution controller's
   * executionContextBuilder, and asserted by
   * test/conformance/src/suites-execution/envmerge.conformance.test.ts.
   * </pre>
   *
   * <code>map&lt;string, .ai.stigmer.agentic.executioncontext.v1.ExecutionValue&gt; runtime_env = 5 [json_name = "runtimeEnv"];</code>
   */
  @java.lang.Override
  public boolean containsRuntimeEnv(
      java.lang.String key) {
    if (key == null) { throw new NullPointerException("map key"); }
    return internalGetRuntimeEnv().getMap().containsKey(key);
  }
  /**
   * Use {@link #getRuntimeEnvMap()} instead.
   */
  @java.lang.Override
  @java.lang.Deprecated
  public java.util.Map<java.lang.String, ai.stigmer.agentic.executioncontext.v1.ExecutionValue> getRuntimeEnv() {
    return getRuntimeEnvMap();
  }
  /**
   * <pre>
   * Runtime environment variables and secrets (execution-scoped).
   * These values are only available for this specific execution and take the
   * highest merge priority, overriding values from Environments bound via
   * environment_refs. A key must be declared in Agent.spec.env to survive the
   * merge: the agent env map is a declaration whitelist (name + is_secret +
   * optional), never a value source — undeclared keys are dropped.
   * Use case: B2B integrations where secrets are injected at runtime per call.
   * These values are consumed into the ExecutionContext (deleted when the
   * execution completes) and cleared from the persisted execution.
   *
   * &#64;internal
   * Merge priority (lowest to highest): resolved Environment values — from the
   * creating schedule's or agent_call task's environment_refs (when present),
   * then the instance's environment_refs; a later ref wins — then runtime_env
   * (this field). The merged map is filtered to the keys declared in
   * Agent.spec.env (no filtering when the agent declares none); a missing
   * required key only logs a warning — the run is not failed. The merge is
   * owned by backend/libs/go/envmerge and the agentexecution controller's
   * executionContextBuilder, and asserted by
   * test/conformance/src/suites-execution/envmerge.conformance.test.ts.
   * </pre>
   *
   * <code>map&lt;string, .ai.stigmer.agentic.executioncontext.v1.ExecutionValue&gt; runtime_env = 5 [json_name = "runtimeEnv"];</code>
   */
  @java.lang.Override
  public java.util.Map<java.lang.String, ai.stigmer.agentic.executioncontext.v1.ExecutionValue> getRuntimeEnvMap() {
    return internalGetRuntimeEnv().getMap();
  }
  /**
   * <pre>
   * Runtime environment variables and secrets (execution-scoped).
   * These values are only available for this specific execution and take the
   * highest merge priority, overriding values from Environments bound via
   * environment_refs. A key must be declared in Agent.spec.env to survive the
   * merge: the agent env map is a declaration whitelist (name + is_secret +
   * optional), never a value source — undeclared keys are dropped.
   * Use case: B2B integrations where secrets are injected at runtime per call.
   * These values are consumed into the ExecutionContext (deleted when the
   * execution completes) and cleared from the persisted execution.
   *
   * &#64;internal
   * Merge priority (lowest to highest): resolved Environment values — from the
   * creating schedule's or agent_call task's environment_refs (when present),
   * then the instance's environment_refs; a later ref wins — then runtime_env
   * (this field). The merged map is filtered to the keys declared in
   * Agent.spec.env (no filtering when the agent declares none); a missing
   * required key only logs a warning — the run is not failed. The merge is
   * owned by backend/libs/go/envmerge and the agentexecution controller's
   * executionContextBuilder, and asserted by
   * test/conformance/src/suites-execution/envmerge.conformance.test.ts.
   * </pre>
   *
   * <code>map&lt;string, .ai.stigmer.agentic.executioncontext.v1.ExecutionValue&gt; runtime_env = 5 [json_name = "runtimeEnv"];</code>
   */
  @java.lang.Override
  public /* nullable */
ai.stigmer.agentic.executioncontext.v1.ExecutionValue getRuntimeEnvOrDefault(
      java.lang.String key,
      /* nullable */
ai.stigmer.agentic.executioncontext.v1.ExecutionValue defaultValue) {
    if (key == null) { throw new NullPointerException("map key"); }
    java.util.Map<java.lang.String, ai.stigmer.agentic.executioncontext.v1.ExecutionValue> map =
        internalGetRuntimeEnv().getMap();
    return map.containsKey(key) ? map.get(key) : defaultValue;
  }
  /**
   * <pre>
   * Runtime environment variables and secrets (execution-scoped).
   * These values are only available for this specific execution and take the
   * highest merge priority, overriding values from Environments bound via
   * environment_refs. A key must be declared in Agent.spec.env to survive the
   * merge: the agent env map is a declaration whitelist (name + is_secret +
   * optional), never a value source — undeclared keys are dropped.
   * Use case: B2B integrations where secrets are injected at runtime per call.
   * These values are consumed into the ExecutionContext (deleted when the
   * execution completes) and cleared from the persisted execution.
   *
   * &#64;internal
   * Merge priority (lowest to highest): resolved Environment values — from the
   * creating schedule's or agent_call task's environment_refs (when present),
   * then the instance's environment_refs; a later ref wins — then runtime_env
   * (this field). The merged map is filtered to the keys declared in
   * Agent.spec.env (no filtering when the agent declares none); a missing
   * required key only logs a warning — the run is not failed. The merge is
   * owned by backend/libs/go/envmerge and the agentexecution controller's
   * executionContextBuilder, and asserted by
   * test/conformance/src/suites-execution/envmerge.conformance.test.ts.
   * </pre>
   *
   * <code>map&lt;string, .ai.stigmer.agentic.executioncontext.v1.ExecutionValue&gt; runtime_env = 5 [json_name = "runtimeEnv"];</code>
   */
  @java.lang.Override
  public ai.stigmer.agentic.executioncontext.v1.ExecutionValue getRuntimeEnvOrThrow(
      java.lang.String key) {
    if (key == null) { throw new NullPointerException("map key"); }
    java.util.Map<java.lang.String, ai.stigmer.agentic.executioncontext.v1.ExecutionValue> map =
        internalGetRuntimeEnv().getMap();
    if (!map.containsKey(key)) {
      throw new java.lang.IllegalArgumentException();
    }
    return map.get(key);
  }

  public static final int CALLBACK_TOKEN_FIELD_NUMBER = 6;
  private com.google.protobuf.ByteString callbackToken_ = com.google.protobuf.ByteString.EMPTY;
  /**
   * <pre>
   * Callback token for async activity completion (optional).
   *
   * When a workflow invokes an agent, this token enables the workflow to
   * wait for the agent to finish without blocking. When empty, the
   * execution runs independently (CLI, API calls, non-workflow triggers).
   *
   * &#64;internal
   *
   * Enables async completion pattern where the caller (typically a
   * workflow activity) waits for actual agent completion without blocking
   * worker threads.
   *
   * Flow:
   * 1. Caller (Go Temporal activity) extracts its task token
   * 2. Passes token in this field when creating AgentExecution
   * 3. Returns activity.ErrResultPending (activity paused, thread released)
   * 4. Agent workflow completes (minutes/hours later)
   * 5. Agent workflow calls ActivityCompletionClient.complete(token, result)
   * 6. Temporal resumes the paused activity with the result
   *
   * Token Format:
   * - Opaque binary blob from Temporal SDK (typically 100-200 bytes)
   * - Contains: namespace, workflow ID, run ID, activity ID, attempt
   * - DO NOT parse or modify - treat as opaque handle
   *
   * Security:
   * - Token grants ability to complete the activity (bearer token)
   * - Should only be passed through trusted internal services
   * - Logged as Base64-encoded string (truncated for security)
   *
   * Timeout:
   * - Caller should set StartToCloseTimeout (e.g., 24 hours)
   * - If token callback never arrives, activity times out
   *
   * Example Usage (Go):
   * ```go
   * func CallAgentActivity(ctx context.Context, config *AgentCallTaskConfig) {
   * taskToken := activity.GetInfo(ctx).TaskToken
   * execution := &amp;AgentExecution{
   * Spec: &amp;AgentExecutionSpec{
   * AgentId: config.Agent,
   * Message: config.Message,
   * CallbackToken: taskToken,
   * },
   * }
   * client.Create(ctx, execution)
   * return nil, activity.ErrResultPending
   * }
   * ```
   *
   * References:
   * - ADR: docs/adr/20260122-async-agent-execution-temporal-token-handshake.md
   * - Temporal Docs: https://docs.temporal.io/activities#asynchronous-activity-completion
   *
   * &#64;since 2026-01-22 (Phase 2: Async Agent Execution Integration)
   * </pre>
   *
   * <code>bytes callback_token = 6 [json_name = "callbackToken"];</code>
   * @return The callbackToken.
   */
  @java.lang.Override
  public com.google.protobuf.ByteString getCallbackToken() {
    return callbackToken_;
  }

  public static final int AUTO_APPROVE_ALL_FIELD_NUMBER = 7;
  private boolean autoApproveAll_ = false;
  /**
   * <pre>
   * Auto-approve all tool executions for this execution.
   *
   * When true, tools that would normally require approval are automatically
   * approved without user intervention. This is the highest-priority override
   * in the approval policy chain:
   * McpServerStatus.tool_approvals → McpServerSpec.pinned_tool_approvals → Agent.tool_approval_overrides → auto_approve_all
   *
   * Use cases:
   * - Automated CI/CD pipelines where human approval isn't practical
   * - Trusted batch operations with pre-validated inputs
   * - Development/testing environments
   * - Scheduled jobs where approval would block automation
   *
   * Security consideration: This flag bypasses all approval checks.
   * Ensure appropriate access controls on who can set this flag.
   * Consider auditing executions where this flag is used.
   *
   * Default: false (approvals required as configured in policies)
   * </pre>
   *
   * <code>bool auto_approve_all = 7 [json_name = "autoApproveAll"];</code>
   * @return The autoApproveAll.
   */
  @java.lang.Override
  public boolean getAutoApproveAll() {
    return autoApproveAll_;
  }

  public static final int PARENT_WORKFLOW_ID_FIELD_NUMBER = 8;
  @SuppressWarnings("serial")
  private volatile java.lang.Object parentWorkflowId_ = "";
  /**
   * <pre>
   * Parent workflow ID when this execution was triggered by a workflow (optional).
   *
   * When set, the platform notifies the parent workflow about approval
   * requests instead of requiring polling. When empty, approvals are
   * submitted directly via the SubmitApproval RPC.
   *
   * &#64;internal
   *
   * Signal Pattern:
   * 1. Go workflow passes its workflow ID when creating AgentExecution
   * 2. Go workflow starts signal listener for "child_approval_required"
   * 3. When agent enters WAITING_FOR_APPROVAL, Java sends signal to parent
   * 4. Go workflow receives signal, updates task status to WAITING_APPROVAL
   *
   * Format:
   * Temporal workflow ID, typically: "stigmer/workflow-execution/invoke/{execution-id}"
   *
   * Backward Compatibility:
   * This field is optional. Agents invoked without a parent workflow ID will
   * continue to work normally - approval is submitted directly via the
   * AgentExecution.SubmitApproval RPC.
   *
   * &#64;since Phase 5.1 (Events-Based Approval Notification)
   * </pre>
   *
   * <code>string parent_workflow_id = 8 [json_name = "parentWorkflowId"];</code>
   * @return The parentWorkflowId.
   */
  @java.lang.Override
  public java.lang.String getParentWorkflowId() {
    java.lang.Object ref = parentWorkflowId_;
    if (ref instanceof java.lang.String) {
      return (java.lang.String) ref;
    } else {
      com.google.protobuf.ByteString bs = 
          (com.google.protobuf.ByteString) ref;
      java.lang.String s = bs.toStringUtf8();
      parentWorkflowId_ = s;
      return s;
    }
  }
  /**
   * <pre>
   * Parent workflow ID when this execution was triggered by a workflow (optional).
   *
   * When set, the platform notifies the parent workflow about approval
   * requests instead of requiring polling. When empty, approvals are
   * submitted directly via the SubmitApproval RPC.
   *
   * &#64;internal
   *
   * Signal Pattern:
   * 1. Go workflow passes its workflow ID when creating AgentExecution
   * 2. Go workflow starts signal listener for "child_approval_required"
   * 3. When agent enters WAITING_FOR_APPROVAL, Java sends signal to parent
   * 4. Go workflow receives signal, updates task status to WAITING_APPROVAL
   *
   * Format:
   * Temporal workflow ID, typically: "stigmer/workflow-execution/invoke/{execution-id}"
   *
   * Backward Compatibility:
   * This field is optional. Agents invoked without a parent workflow ID will
   * continue to work normally - approval is submitted directly via the
   * AgentExecution.SubmitApproval RPC.
   *
   * &#64;since Phase 5.1 (Events-Based Approval Notification)
   * </pre>
   *
   * <code>string parent_workflow_id = 8 [json_name = "parentWorkflowId"];</code>
   * @return The bytes for parentWorkflowId.
   */
  @java.lang.Override
  public com.google.protobuf.ByteString
      getParentWorkflowIdBytes() {
    java.lang.Object ref = parentWorkflowId_;
    if (ref instanceof java.lang.String) {
      com.google.protobuf.ByteString b = 
          com.google.protobuf.ByteString.copyFromUtf8(
              (java.lang.String) ref);
      parentWorkflowId_ = b;
      return b;
    } else {
      return (com.google.protobuf.ByteString) ref;
    }
  }

  public static final int ATTACHMENTS_FIELD_NUMBER = 9;
  @SuppressWarnings("serial")
  private java.util.List<ai.stigmer.agentic.agentexecution.v1.Attachment> attachments_;
  /**
   * <pre>
   * Files attached to this execution.
   *
   * Attachments are injected into the sandbox before execution begins.
   * The agent can read these files from the specified mount paths.
   *
   * Use cases:
   * - Provide input data files to process
   * - Pass configuration files
   * - Supply reference documents for analysis
   *
   * Example CLI usage:
   * stigmer run agent my-agent --attach ./config.yaml -m "Process this config"
   *
   * &#64;since Artifact Lifecycle (Attachments &amp; Outputs)
   * </pre>
   *
   * <code>repeated .ai.stigmer.agentic.agentexecution.v1.Attachment attachments = 9 [json_name = "attachments"];</code>
   */
  @java.lang.Override
  public java.util.List<ai.stigmer.agentic.agentexecution.v1.Attachment> getAttachmentsList() {
    return attachments_;
  }
  /**
   * <pre>
   * Files attached to this execution.
   *
   * Attachments are injected into the sandbox before execution begins.
   * The agent can read these files from the specified mount paths.
   *
   * Use cases:
   * - Provide input data files to process
   * - Pass configuration files
   * - Supply reference documents for analysis
   *
   * Example CLI usage:
   * stigmer run agent my-agent --attach ./config.yaml -m "Process this config"
   *
   * &#64;since Artifact Lifecycle (Attachments &amp; Outputs)
   * </pre>
   *
   * <code>repeated .ai.stigmer.agentic.agentexecution.v1.Attachment attachments = 9 [json_name = "attachments"];</code>
   */
  @java.lang.Override
  public java.util.List<? extends ai.stigmer.agentic.agentexecution.v1.AttachmentOrBuilder> 
      getAttachmentsOrBuilderList() {
    return attachments_;
  }
  /**
   * <pre>
   * Files attached to this execution.
   *
   * Attachments are injected into the sandbox before execution begins.
   * The agent can read these files from the specified mount paths.
   *
   * Use cases:
   * - Provide input data files to process
   * - Pass configuration files
   * - Supply reference documents for analysis
   *
   * Example CLI usage:
   * stigmer run agent my-agent --attach ./config.yaml -m "Process this config"
   *
   * &#64;since Artifact Lifecycle (Attachments &amp; Outputs)
   * </pre>
   *
   * <code>repeated .ai.stigmer.agentic.agentexecution.v1.Attachment attachments = 9 [json_name = "attachments"];</code>
   */
  @java.lang.Override
  public int getAttachmentsCount() {
    return attachments_.size();
  }
  /**
   * <pre>
   * Files attached to this execution.
   *
   * Attachments are injected into the sandbox before execution begins.
   * The agent can read these files from the specified mount paths.
   *
   * Use cases:
   * - Provide input data files to process
   * - Pass configuration files
   * - Supply reference documents for analysis
   *
   * Example CLI usage:
   * stigmer run agent my-agent --attach ./config.yaml -m "Process this config"
   *
   * &#64;since Artifact Lifecycle (Attachments &amp; Outputs)
   * </pre>
   *
   * <code>repeated .ai.stigmer.agentic.agentexecution.v1.Attachment attachments = 9 [json_name = "attachments"];</code>
   */
  @java.lang.Override
  public ai.stigmer.agentic.agentexecution.v1.Attachment getAttachments(int index) {
    return attachments_.get(index);
  }
  /**
   * <pre>
   * Files attached to this execution.
   *
   * Attachments are injected into the sandbox before execution begins.
   * The agent can read these files from the specified mount paths.
   *
   * Use cases:
   * - Provide input data files to process
   * - Pass configuration files
   * - Supply reference documents for analysis
   *
   * Example CLI usage:
   * stigmer run agent my-agent --attach ./config.yaml -m "Process this config"
   *
   * &#64;since Artifact Lifecycle (Attachments &amp; Outputs)
   * </pre>
   *
   * <code>repeated .ai.stigmer.agentic.agentexecution.v1.Attachment attachments = 9 [json_name = "attachments"];</code>
   */
  @java.lang.Override
  public ai.stigmer.agentic.agentexecution.v1.AttachmentOrBuilder getAttachmentsOrBuilder(
      int index) {
    return attachments_.get(index);
  }

  public static final int WORKSPACE_FILE_REFS_FIELD_NUMBER = 10;
  @SuppressWarnings("serial")
  private com.google.protobuf.LazyStringArrayList workspaceFileRefs_ =
      com.google.protobuf.LazyStringArrayList.emptyList();
  /**
   * <pre>
   * Workspace-relative file paths the user wants the agent to focus on.
   *
   * When users reference files that exist inside the session's workspace,
   * the client records the workspace-relative path here instead of uploading.
   * The agent accesses these files directly via the workspace filesystem --
   * no upload, no copy, no injection.
   *
   * This separates the "attention" signal ("look at this file") from the
   * "transport" mechanism (upload + inject) that regular attachments use.
   *
   * Works for any workspace type with a resolvable filesystem post-provisioning
   * (local-path workspaces, git-cloned workspaces, or any future source type).
   * The runner resolves paths after workspace provisioning regardless of source.
   *
   * Populated by:
   * - CLI: detects --attach paths inside workspace roots automatically
   * - Web/Desktop: drag-to-reference from workspace file tree to composer
   *
   * Example CLI usage:
   * stigmer run agent reviewer --workspace . --attach ./src/config.yaml -m "Review"
   * # CLI detects src/config.yaml is inside workspace -&gt; workspace_file_ref
   * # No upload, no injection; agent reads directly from src/config.yaml
   *
   * &#64;since Workspace-Aware File Referencing
   * </pre>
   *
   * <code>repeated string workspace_file_refs = 10 [json_name = "workspaceFileRefs"];</code>
   * @return A list containing the workspaceFileRefs.
   */
  public com.google.protobuf.ProtocolStringList
      getWorkspaceFileRefsList() {
    return workspaceFileRefs_;
  }
  /**
   * <pre>
   * Workspace-relative file paths the user wants the agent to focus on.
   *
   * When users reference files that exist inside the session's workspace,
   * the client records the workspace-relative path here instead of uploading.
   * The agent accesses these files directly via the workspace filesystem --
   * no upload, no copy, no injection.
   *
   * This separates the "attention" signal ("look at this file") from the
   * "transport" mechanism (upload + inject) that regular attachments use.
   *
   * Works for any workspace type with a resolvable filesystem post-provisioning
   * (local-path workspaces, git-cloned workspaces, or any future source type).
   * The runner resolves paths after workspace provisioning regardless of source.
   *
   * Populated by:
   * - CLI: detects --attach paths inside workspace roots automatically
   * - Web/Desktop: drag-to-reference from workspace file tree to composer
   *
   * Example CLI usage:
   * stigmer run agent reviewer --workspace . --attach ./src/config.yaml -m "Review"
   * # CLI detects src/config.yaml is inside workspace -&gt; workspace_file_ref
   * # No upload, no injection; agent reads directly from src/config.yaml
   *
   * &#64;since Workspace-Aware File Referencing
   * </pre>
   *
   * <code>repeated string workspace_file_refs = 10 [json_name = "workspaceFileRefs"];</code>
   * @return The count of workspaceFileRefs.
   */
  public int getWorkspaceFileRefsCount() {
    return workspaceFileRefs_.size();
  }
  /**
   * <pre>
   * Workspace-relative file paths the user wants the agent to focus on.
   *
   * When users reference files that exist inside the session's workspace,
   * the client records the workspace-relative path here instead of uploading.
   * The agent accesses these files directly via the workspace filesystem --
   * no upload, no copy, no injection.
   *
   * This separates the "attention" signal ("look at this file") from the
   * "transport" mechanism (upload + inject) that regular attachments use.
   *
   * Works for any workspace type with a resolvable filesystem post-provisioning
   * (local-path workspaces, git-cloned workspaces, or any future source type).
   * The runner resolves paths after workspace provisioning regardless of source.
   *
   * Populated by:
   * - CLI: detects --attach paths inside workspace roots automatically
   * - Web/Desktop: drag-to-reference from workspace file tree to composer
   *
   * Example CLI usage:
   * stigmer run agent reviewer --workspace . --attach ./src/config.yaml -m "Review"
   * # CLI detects src/config.yaml is inside workspace -&gt; workspace_file_ref
   * # No upload, no injection; agent reads directly from src/config.yaml
   *
   * &#64;since Workspace-Aware File Referencing
   * </pre>
   *
   * <code>repeated string workspace_file_refs = 10 [json_name = "workspaceFileRefs"];</code>
   * @param index The index of the element to return.
   * @return The workspaceFileRefs at the given index.
   */
  public java.lang.String getWorkspaceFileRefs(int index) {
    return workspaceFileRefs_.get(index);
  }
  /**
   * <pre>
   * Workspace-relative file paths the user wants the agent to focus on.
   *
   * When users reference files that exist inside the session's workspace,
   * the client records the workspace-relative path here instead of uploading.
   * The agent accesses these files directly via the workspace filesystem --
   * no upload, no copy, no injection.
   *
   * This separates the "attention" signal ("look at this file") from the
   * "transport" mechanism (upload + inject) that regular attachments use.
   *
   * Works for any workspace type with a resolvable filesystem post-provisioning
   * (local-path workspaces, git-cloned workspaces, or any future source type).
   * The runner resolves paths after workspace provisioning regardless of source.
   *
   * Populated by:
   * - CLI: detects --attach paths inside workspace roots automatically
   * - Web/Desktop: drag-to-reference from workspace file tree to composer
   *
   * Example CLI usage:
   * stigmer run agent reviewer --workspace . --attach ./src/config.yaml -m "Review"
   * # CLI detects src/config.yaml is inside workspace -&gt; workspace_file_ref
   * # No upload, no injection; agent reads directly from src/config.yaml
   *
   * &#64;since Workspace-Aware File Referencing
   * </pre>
   *
   * <code>repeated string workspace_file_refs = 10 [json_name = "workspaceFileRefs"];</code>
   * @param index The index of the value to return.
   * @return The bytes of the workspaceFileRefs at the given index.
   */
  public com.google.protobuf.ByteString
      getWorkspaceFileRefsBytes(int index) {
    return workspaceFileRefs_.getByteString(index);
  }

  public static final int ACTIVITY_TASK_QUEUE_FIELD_NUMBER = 11;
  @SuppressWarnings("serial")
  private volatile java.lang.Object activityTaskQueue_ = "";
  /**
   * <pre>
   * Explicit Temporal task queue override for activity routing.
   *
   * &#64;internal
   * When set, the agent execution's activities are routed to this queue
   * instead of the normally-resolved queue (session:{id} or global stigmer_runner).
   * This enables sandbox sharing: a parent workflow execution passes its own
   * queue so child agents run in the same sandbox without provisioning new VMs.
   *
   * When empty: normal dispatch resolution applies (default behavior).
   *
   * Security: Only accepted from internal callers (workflow engine's CallAgent
   * activity). External API callers cannot set this field — the create handler
   * strips it during input sanitization. Presence of parent_workflow_id is used
   * as a co-validation signal.
   *
   * Format: "wfexec:{workflow_execution_id}" (matches the parent workflow's
   * sandbox queue). The prefix ensures no collision with session:{id} queues.
   *
   * &#64;since Workflow Sandbox Affinity
   * </pre>
   *
   * <code>string activity_task_queue = 11 [json_name = "activityTaskQueue"];</code>
   * @return The activityTaskQueue.
   */
  @java.lang.Override
  public java.lang.String getActivityTaskQueue() {
    java.lang.Object ref = activityTaskQueue_;
    if (ref instanceof java.lang.String) {
      return (java.lang.String) ref;
    } else {
      com.google.protobuf.ByteString bs = 
          (com.google.protobuf.ByteString) ref;
      java.lang.String s = bs.toStringUtf8();
      activityTaskQueue_ = s;
      return s;
    }
  }
  /**
   * <pre>
   * Explicit Temporal task queue override for activity routing.
   *
   * &#64;internal
   * When set, the agent execution's activities are routed to this queue
   * instead of the normally-resolved queue (session:{id} or global stigmer_runner).
   * This enables sandbox sharing: a parent workflow execution passes its own
   * queue so child agents run in the same sandbox without provisioning new VMs.
   *
   * When empty: normal dispatch resolution applies (default behavior).
   *
   * Security: Only accepted from internal callers (workflow engine's CallAgent
   * activity). External API callers cannot set this field — the create handler
   * strips it during input sanitization. Presence of parent_workflow_id is used
   * as a co-validation signal.
   *
   * Format: "wfexec:{workflow_execution_id}" (matches the parent workflow's
   * sandbox queue). The prefix ensures no collision with session:{id} queues.
   *
   * &#64;since Workflow Sandbox Affinity
   * </pre>
   *
   * <code>string activity_task_queue = 11 [json_name = "activityTaskQueue"];</code>
   * @return The bytes for activityTaskQueue.
   */
  @java.lang.Override
  public com.google.protobuf.ByteString
      getActivityTaskQueueBytes() {
    java.lang.Object ref = activityTaskQueue_;
    if (ref instanceof java.lang.String) {
      com.google.protobuf.ByteString b = 
          com.google.protobuf.ByteString.copyFromUtf8(
              (java.lang.String) ref);
      activityTaskQueue_ = b;
      return b;
    } else {
      return (com.google.protobuf.ByteString) ref;
    }
  }

  public static final int SUPERSEDES_EXECUTION_ID_FIELD_NUMBER = 12;
  @SuppressWarnings("serial")
  private volatile java.lang.Object supersedesExecutionId_ = "";
  /**
   * <pre>
   * ID of the execution this one supersedes via edit-and-resubmit (optional).
   *
   * When a user stops an in-flight turn, edits the message, and resubmits,
   * the client sets this field on the NEW execution to point at the stopped
   * one. Chat-thread consumers hide the superseded execution so the edited
   * message replaces the original in place; history surfaces (CLI, execution
   * lists) keep showing the full record.
   *
   * Empty means this execution is not an edit of another turn.
   *
   * &#64;internal
   *
   * The link always lives on the successor — the superseded record is never
   * mutated (append-only execution log, single writer). Chained edits form a
   * chain of links (A &lt;- B &lt;- C), each persisted on its own successor.
   *
   * Display-level semantics only: the runner does NOT rewind model context.
   * Conversation history lives in an opaque session-keyed store (LangGraph
   * checkpoint thread / Cursor SDK agent store) that is resumed as-is, so the
   * superseded turn's partial messages remain visible to the model. Do not
   * build features that assume the runner skips superseded turns.
   *
   * &#64;since Edit-and-Resubmit In Place (stigmer/stigmer#181)
   * </pre>
   *
   * <code>string supersedes_execution_id = 12 [json_name = "supersedesExecutionId"];</code>
   * @return The supersedesExecutionId.
   */
  @java.lang.Override
  public java.lang.String getSupersedesExecutionId() {
    java.lang.Object ref = supersedesExecutionId_;
    if (ref instanceof java.lang.String) {
      return (java.lang.String) ref;
    } else {
      com.google.protobuf.ByteString bs = 
          (com.google.protobuf.ByteString) ref;
      java.lang.String s = bs.toStringUtf8();
      supersedesExecutionId_ = s;
      return s;
    }
  }
  /**
   * <pre>
   * ID of the execution this one supersedes via edit-and-resubmit (optional).
   *
   * When a user stops an in-flight turn, edits the message, and resubmits,
   * the client sets this field on the NEW execution to point at the stopped
   * one. Chat-thread consumers hide the superseded execution so the edited
   * message replaces the original in place; history surfaces (CLI, execution
   * lists) keep showing the full record.
   *
   * Empty means this execution is not an edit of another turn.
   *
   * &#64;internal
   *
   * The link always lives on the successor — the superseded record is never
   * mutated (append-only execution log, single writer). Chained edits form a
   * chain of links (A &lt;- B &lt;- C), each persisted on its own successor.
   *
   * Display-level semantics only: the runner does NOT rewind model context.
   * Conversation history lives in an opaque session-keyed store (LangGraph
   * checkpoint thread / Cursor SDK agent store) that is resumed as-is, so the
   * superseded turn's partial messages remain visible to the model. Do not
   * build features that assume the runner skips superseded turns.
   *
   * &#64;since Edit-and-Resubmit In Place (stigmer/stigmer#181)
   * </pre>
   *
   * <code>string supersedes_execution_id = 12 [json_name = "supersedesExecutionId"];</code>
   * @return The bytes for supersedesExecutionId.
   */
  @java.lang.Override
  public com.google.protobuf.ByteString
      getSupersedesExecutionIdBytes() {
    java.lang.Object ref = supersedesExecutionId_;
    if (ref instanceof java.lang.String) {
      com.google.protobuf.ByteString b = 
          com.google.protobuf.ByteString.copyFromUtf8(
              (java.lang.String) ref);
      supersedesExecutionId_ = b;
      return b;
    } else {
      return (com.google.protobuf.ByteString) ref;
    }
  }

  public static final int CONVERSATION_CATCHUP_FIELD_NUMBER = 14;
  private ai.stigmer.agentic.agentexecution.v1.ConversationCatchup conversationCatchup_;
  /**
   * <pre>
   * Conversation events the agent has not yet seen, composed by the platform
   * for this turn (optional).
   *
   * Set on live channel-conversation turns (WhatsApp, Slack): when a human
   * teammate handled the conversation or messages otherwise landed while the
   * agent was not watching, the digest carries what happened so the agent
   * re-enters informed. Absent on every other execution surface.
   *
   * &#64;internal
   * Composed per turn by the cloud channel runtime — ChannelSessionBroker
   * over the DD-004 timeline stitch, windowed by the conversation's
   * agent_witnessed_through watermark (channel-conversations DD-006/DD-007).
   * The OSS server never sets it. The runner prepends the framed digest to
   * the turn's user message on both harnesses (A27) and must never read
   * window_end. Top-level rather than inside ExecutionConfig by design:
   * conversation content must not vanish with the execution-profile
   * kill-switch (DD-006 D-a).
   * </pre>
   *
   * <code>.ai.stigmer.agentic.agentexecution.v1.ConversationCatchup conversation_catchup = 14 [json_name = "conversationCatchup"];</code>
   * @return Whether the conversationCatchup field is set.
   */
  @java.lang.Override
  public boolean hasConversationCatchup() {
    return ((bitField0_ & 0x00000004) != 0);
  }
  /**
   * <pre>
   * Conversation events the agent has not yet seen, composed by the platform
   * for this turn (optional).
   *
   * Set on live channel-conversation turns (WhatsApp, Slack): when a human
   * teammate handled the conversation or messages otherwise landed while the
   * agent was not watching, the digest carries what happened so the agent
   * re-enters informed. Absent on every other execution surface.
   *
   * &#64;internal
   * Composed per turn by the cloud channel runtime — ChannelSessionBroker
   * over the DD-004 timeline stitch, windowed by the conversation's
   * agent_witnessed_through watermark (channel-conversations DD-006/DD-007).
   * The OSS server never sets it. The runner prepends the framed digest to
   * the turn's user message on both harnesses (A27) and must never read
   * window_end. Top-level rather than inside ExecutionConfig by design:
   * conversation content must not vanish with the execution-profile
   * kill-switch (DD-006 D-a).
   * </pre>
   *
   * <code>.ai.stigmer.agentic.agentexecution.v1.ConversationCatchup conversation_catchup = 14 [json_name = "conversationCatchup"];</code>
   * @return The conversationCatchup.
   */
  @java.lang.Override
  public ai.stigmer.agentic.agentexecution.v1.ConversationCatchup getConversationCatchup() {
    return conversationCatchup_ == null ? ai.stigmer.agentic.agentexecution.v1.ConversationCatchup.getDefaultInstance() : conversationCatchup_;
  }
  /**
   * <pre>
   * Conversation events the agent has not yet seen, composed by the platform
   * for this turn (optional).
   *
   * Set on live channel-conversation turns (WhatsApp, Slack): when a human
   * teammate handled the conversation or messages otherwise landed while the
   * agent was not watching, the digest carries what happened so the agent
   * re-enters informed. Absent on every other execution surface.
   *
   * &#64;internal
   * Composed per turn by the cloud channel runtime — ChannelSessionBroker
   * over the DD-004 timeline stitch, windowed by the conversation's
   * agent_witnessed_through watermark (channel-conversations DD-006/DD-007).
   * The OSS server never sets it. The runner prepends the framed digest to
   * the turn's user message on both harnesses (A27) and must never read
   * window_end. Top-level rather than inside ExecutionConfig by design:
   * conversation content must not vanish with the execution-profile
   * kill-switch (DD-006 D-a).
   * </pre>
   *
   * <code>.ai.stigmer.agentic.agentexecution.v1.ConversationCatchup conversation_catchup = 14 [json_name = "conversationCatchup"];</code>
   */
  @java.lang.Override
  public ai.stigmer.agentic.agentexecution.v1.ConversationCatchupOrBuilder getConversationCatchupOrBuilder() {
    return conversationCatchup_ == null ? ai.stigmer.agentic.agentexecution.v1.ConversationCatchup.getDefaultInstance() : conversationCatchup_;
  }

  public static final int DECLARED_PREFERENCES_FIELD_NUMBER = 15;
  private ai.stigmer.agentic.agentexecution.v1.DeclaredPreferences declaredPreferences_;
  /**
   * <pre>
   * Standing preferences declared by the organization and the calling user,
   * snapshotted into this execution at create time (optional).
   *
   * &#64;internal
   * Server-owned: the create pipeline stamps this field UNCONDITIONALLY —
   * empty for non-eligible callers — overwriting any caller-supplied value,
   * so external injection is moot by construction. Composed only for
   * first-party human operators (DD-002 D4 as amended 2026-08-15: never for
   * machine, impersonated, guest, channel, schedule, runner, or
   * platform-client-user callers). The OSS server composes org_context only
   * (single-user local mode; no IdentityAccount). A snapshot by design:
   * preferences are mutable, executions are immutable audit records — this
   * field records what the model actually saw. Composition is best-effort;
   * a preference load failure never fails the create. Top-level rather than
   * inside ExecutionConfig: preference content must not vanish with the
   * execution-profile kill-switch (same rationale as conversation_catchup).
   * </pre>
   *
   * <code>.ai.stigmer.agentic.agentexecution.v1.DeclaredPreferences declared_preferences = 15 [json_name = "declaredPreferences"];</code>
   * @return Whether the declaredPreferences field is set.
   */
  @java.lang.Override
  public boolean hasDeclaredPreferences() {
    return ((bitField0_ & 0x00000008) != 0);
  }
  /**
   * <pre>
   * Standing preferences declared by the organization and the calling user,
   * snapshotted into this execution at create time (optional).
   *
   * &#64;internal
   * Server-owned: the create pipeline stamps this field UNCONDITIONALLY —
   * empty for non-eligible callers — overwriting any caller-supplied value,
   * so external injection is moot by construction. Composed only for
   * first-party human operators (DD-002 D4 as amended 2026-08-15: never for
   * machine, impersonated, guest, channel, schedule, runner, or
   * platform-client-user callers). The OSS server composes org_context only
   * (single-user local mode; no IdentityAccount). A snapshot by design:
   * preferences are mutable, executions are immutable audit records — this
   * field records what the model actually saw. Composition is best-effort;
   * a preference load failure never fails the create. Top-level rather than
   * inside ExecutionConfig: preference content must not vanish with the
   * execution-profile kill-switch (same rationale as conversation_catchup).
   * </pre>
   *
   * <code>.ai.stigmer.agentic.agentexecution.v1.DeclaredPreferences declared_preferences = 15 [json_name = "declaredPreferences"];</code>
   * @return The declaredPreferences.
   */
  @java.lang.Override
  public ai.stigmer.agentic.agentexecution.v1.DeclaredPreferences getDeclaredPreferences() {
    return declaredPreferences_ == null ? ai.stigmer.agentic.agentexecution.v1.DeclaredPreferences.getDefaultInstance() : declaredPreferences_;
  }
  /**
   * <pre>
   * Standing preferences declared by the organization and the calling user,
   * snapshotted into this execution at create time (optional).
   *
   * &#64;internal
   * Server-owned: the create pipeline stamps this field UNCONDITIONALLY —
   * empty for non-eligible callers — overwriting any caller-supplied value,
   * so external injection is moot by construction. Composed only for
   * first-party human operators (DD-002 D4 as amended 2026-08-15: never for
   * machine, impersonated, guest, channel, schedule, runner, or
   * platform-client-user callers). The OSS server composes org_context only
   * (single-user local mode; no IdentityAccount). A snapshot by design:
   * preferences are mutable, executions are immutable audit records — this
   * field records what the model actually saw. Composition is best-effort;
   * a preference load failure never fails the create. Top-level rather than
   * inside ExecutionConfig: preference content must not vanish with the
   * execution-profile kill-switch (same rationale as conversation_catchup).
   * </pre>
   *
   * <code>.ai.stigmer.agentic.agentexecution.v1.DeclaredPreferences declared_preferences = 15 [json_name = "declaredPreferences"];</code>
   */
  @java.lang.Override
  public ai.stigmer.agentic.agentexecution.v1.DeclaredPreferencesOrBuilder getDeclaredPreferencesOrBuilder() {
    return declaredPreferences_ == null ? ai.stigmer.agentic.agentexecution.v1.DeclaredPreferences.getDefaultInstance() : declaredPreferences_;
  }

  public static final int RECALLED_MEMORIES_FIELD_NUMBER = 16;
  private ai.stigmer.agentic.agentexecution.v1.RecalledMemories recalledMemories_;
  /**
   * <pre>
   * The caller's confirmed memories, snapshotted into this execution at
   * create time (optional).
   *
   * &#64;internal
   * Every DD-002 D2 invariant carries over from declared_preferences
   * verbatim (DD-006 D2): SERVER-OWNED (the create pipeline stamps this
   * field unconditionally — enabled=false with no facts for excluded
   * callers — overwriting any caller-supplied value, so injection via
   * the create request is moot by construction); composed only for
   * first-party human operators with BOTH memory_enabled flags on (org
   * AND caller; OSS: the org flag alone); confirmed records only —
   * proposed and rejected are never injected (the consent gate is
   * meaningless otherwise); a SNAPSHOT (memories are mutable, executions
   * are immutable audit records — this field records the CANDIDATE set
   * the prompt was built from, and each fact carries its memory_id so
   * the audit links back to the addressable record; what the model
   * actually saw is this snapshot joined to
   * status.recalled_memories_report, whose absence means wholesale —
   * the full set — by construction, DD-008 D5); best-effort (a memory
   * load failure degrades to disabled, never fails the create);
   * top-level rather than inside ExecutionConfig (must not vanish with
   * the execution-profile kill-switch). The enabled bit doubles as the
   * runner's signal to offer the remember tool (DD-005 D1) — one
   * server-owned field, one writer, no parallel flag. Composed by
   * Stage 2's compose steps; the field is reserved here by Stage 1 so
   * all Phase 2 contract changes land in one codegen pass.
   * </pre>
   *
   * <code>.ai.stigmer.agentic.agentexecution.v1.RecalledMemories recalled_memories = 16 [json_name = "recalledMemories"];</code>
   * @return Whether the recalledMemories field is set.
   */
  @java.lang.Override
  public boolean hasRecalledMemories() {
    return ((bitField0_ & 0x00000010) != 0);
  }
  /**
   * <pre>
   * The caller's confirmed memories, snapshotted into this execution at
   * create time (optional).
   *
   * &#64;internal
   * Every DD-002 D2 invariant carries over from declared_preferences
   * verbatim (DD-006 D2): SERVER-OWNED (the create pipeline stamps this
   * field unconditionally — enabled=false with no facts for excluded
   * callers — overwriting any caller-supplied value, so injection via
   * the create request is moot by construction); composed only for
   * first-party human operators with BOTH memory_enabled flags on (org
   * AND caller; OSS: the org flag alone); confirmed records only —
   * proposed and rejected are never injected (the consent gate is
   * meaningless otherwise); a SNAPSHOT (memories are mutable, executions
   * are immutable audit records — this field records the CANDIDATE set
   * the prompt was built from, and each fact carries its memory_id so
   * the audit links back to the addressable record; what the model
   * actually saw is this snapshot joined to
   * status.recalled_memories_report, whose absence means wholesale —
   * the full set — by construction, DD-008 D5); best-effort (a memory
   * load failure degrades to disabled, never fails the create);
   * top-level rather than inside ExecutionConfig (must not vanish with
   * the execution-profile kill-switch). The enabled bit doubles as the
   * runner's signal to offer the remember tool (DD-005 D1) — one
   * server-owned field, one writer, no parallel flag. Composed by
   * Stage 2's compose steps; the field is reserved here by Stage 1 so
   * all Phase 2 contract changes land in one codegen pass.
   * </pre>
   *
   * <code>.ai.stigmer.agentic.agentexecution.v1.RecalledMemories recalled_memories = 16 [json_name = "recalledMemories"];</code>
   * @return The recalledMemories.
   */
  @java.lang.Override
  public ai.stigmer.agentic.agentexecution.v1.RecalledMemories getRecalledMemories() {
    return recalledMemories_ == null ? ai.stigmer.agentic.agentexecution.v1.RecalledMemories.getDefaultInstance() : recalledMemories_;
  }
  /**
   * <pre>
   * The caller's confirmed memories, snapshotted into this execution at
   * create time (optional).
   *
   * &#64;internal
   * Every DD-002 D2 invariant carries over from declared_preferences
   * verbatim (DD-006 D2): SERVER-OWNED (the create pipeline stamps this
   * field unconditionally — enabled=false with no facts for excluded
   * callers — overwriting any caller-supplied value, so injection via
   * the create request is moot by construction); composed only for
   * first-party human operators with BOTH memory_enabled flags on (org
   * AND caller; OSS: the org flag alone); confirmed records only —
   * proposed and rejected are never injected (the consent gate is
   * meaningless otherwise); a SNAPSHOT (memories are mutable, executions
   * are immutable audit records — this field records the CANDIDATE set
   * the prompt was built from, and each fact carries its memory_id so
   * the audit links back to the addressable record; what the model
   * actually saw is this snapshot joined to
   * status.recalled_memories_report, whose absence means wholesale —
   * the full set — by construction, DD-008 D5); best-effort (a memory
   * load failure degrades to disabled, never fails the create);
   * top-level rather than inside ExecutionConfig (must not vanish with
   * the execution-profile kill-switch). The enabled bit doubles as the
   * runner's signal to offer the remember tool (DD-005 D1) — one
   * server-owned field, one writer, no parallel flag. Composed by
   * Stage 2's compose steps; the field is reserved here by Stage 1 so
   * all Phase 2 contract changes land in one codegen pass.
   * </pre>
   *
   * <code>.ai.stigmer.agentic.agentexecution.v1.RecalledMemories recalled_memories = 16 [json_name = "recalledMemories"];</code>
   */
  @java.lang.Override
  public ai.stigmer.agentic.agentexecution.v1.RecalledMemoriesOrBuilder getRecalledMemoriesOrBuilder() {
    return recalledMemories_ == null ? ai.stigmer.agentic.agentexecution.v1.RecalledMemories.getDefaultInstance() : recalledMemories_;
  }

  private byte memoizedIsInitialized = -1;
  @java.lang.Override
  public final boolean isInitialized() {
    byte isInitialized = memoizedIsInitialized;
    if (isInitialized == 1) return true;
    if (isInitialized == 0) return false;

    memoizedIsInitialized = 1;
    return true;
  }

  @java.lang.Override
  public void writeTo(com.google.protobuf.CodedOutputStream output)
                      throws java.io.IOException {
    if (!com.google.protobuf.GeneratedMessage.isStringEmpty(sessionId_)) {
      com.google.protobuf.GeneratedMessage.writeString(output, 1, sessionId_);
    }
    if (!com.google.protobuf.GeneratedMessage.isStringEmpty(agentId_)) {
      com.google.protobuf.GeneratedMessage.writeString(output, 2, agentId_);
    }
    if (!com.google.protobuf.GeneratedMessage.isStringEmpty(message_)) {
      com.google.protobuf.GeneratedMessage.writeString(output, 3, message_);
    }
    if (((bitField0_ & 0x00000002) != 0)) {
      output.writeMessage(4, getExecutionConfig());
    }
    com.google.protobuf.GeneratedMessage
      .serializeStringMapTo(
        output,
        internalGetRuntimeEnv(),
        RuntimeEnvDefaultEntryHolder.defaultEntry,
        5);
    if (!callbackToken_.isEmpty()) {
      output.writeBytes(6, callbackToken_);
    }
    if (autoApproveAll_ != false) {
      output.writeBool(7, autoApproveAll_);
    }
    if (!com.google.protobuf.GeneratedMessage.isStringEmpty(parentWorkflowId_)) {
      com.google.protobuf.GeneratedMessage.writeString(output, 8, parentWorkflowId_);
    }
    for (int i = 0; i < attachments_.size(); i++) {
      output.writeMessage(9, attachments_.get(i));
    }
    for (int i = 0; i < workspaceFileRefs_.size(); i++) {
      com.google.protobuf.GeneratedMessage.writeString(output, 10, workspaceFileRefs_.getRaw(i));
    }
    if (!com.google.protobuf.GeneratedMessage.isStringEmpty(activityTaskQueue_)) {
      com.google.protobuf.GeneratedMessage.writeString(output, 11, activityTaskQueue_);
    }
    if (!com.google.protobuf.GeneratedMessage.isStringEmpty(supersedesExecutionId_)) {
      com.google.protobuf.GeneratedMessage.writeString(output, 12, supersedesExecutionId_);
    }
    if (((bitField0_ & 0x00000001) != 0)) {
      output.writeMessage(13, getSessionSpec());
    }
    if (((bitField0_ & 0x00000004) != 0)) {
      output.writeMessage(14, getConversationCatchup());
    }
    if (((bitField0_ & 0x00000008) != 0)) {
      output.writeMessage(15, getDeclaredPreferences());
    }
    if (((bitField0_ & 0x00000010) != 0)) {
      output.writeMessage(16, getRecalledMemories());
    }
    getUnknownFields().writeTo(output);
  }

  @java.lang.Override
  public int getSerializedSize() {
    int size = memoizedSize;
    if (size != -1) return size;

    size = 0;
    if (!com.google.protobuf.GeneratedMessage.isStringEmpty(sessionId_)) {
      size += com.google.protobuf.GeneratedMessage.computeStringSize(1, sessionId_);
    }
    if (!com.google.protobuf.GeneratedMessage.isStringEmpty(agentId_)) {
      size += com.google.protobuf.GeneratedMessage.computeStringSize(2, agentId_);
    }
    if (!com.google.protobuf.GeneratedMessage.isStringEmpty(message_)) {
      size += com.google.protobuf.GeneratedMessage.computeStringSize(3, message_);
    }
    if (((bitField0_ & 0x00000002) != 0)) {
      size += com.google.protobuf.CodedOutputStream
        .computeMessageSize(4, getExecutionConfig());
    }
    for (java.util.Map.Entry<java.lang.String, ai.stigmer.agentic.executioncontext.v1.ExecutionValue> entry
         : internalGetRuntimeEnv().getMap().entrySet()) {
      com.google.protobuf.MapEntry<java.lang.String, ai.stigmer.agentic.executioncontext.v1.ExecutionValue>
      runtimeEnv__ = RuntimeEnvDefaultEntryHolder.defaultEntry.newBuilderForType()
          .setKey(entry.getKey())
          .setValue(entry.getValue())
          .buildPartial();
      size += com.google.protobuf.CodedOutputStream
          .computeMessageSize(5, runtimeEnv__);
    }
    if (!callbackToken_.isEmpty()) {
      size += com.google.protobuf.CodedOutputStream
        .computeBytesSize(6, callbackToken_);
    }
    if (autoApproveAll_ != false) {
      size += com.google.protobuf.CodedOutputStream
        .computeBoolSize(7, autoApproveAll_);
    }
    if (!com.google.protobuf.GeneratedMessage.isStringEmpty(parentWorkflowId_)) {
      size += com.google.protobuf.GeneratedMessage.computeStringSize(8, parentWorkflowId_);
    }

        {
          final int count = attachments_.size();
          for (int i = 0; i < count; i++) {
            size += com.google.protobuf.CodedOutputStream
              .computeMessageSizeNoTag(attachments_.get(i));
          }
          size += 1 * count;
        }
    {
      int dataSize = 0;
      for (int i = 0; i < workspaceFileRefs_.size(); i++) {
        dataSize += computeStringSizeNoTag(workspaceFileRefs_.getRaw(i));
      }
      size += dataSize;
      size += 1 * getWorkspaceFileRefsList().size();
    }
    if (!com.google.protobuf.GeneratedMessage.isStringEmpty(activityTaskQueue_)) {
      size += com.google.protobuf.GeneratedMessage.computeStringSize(11, activityTaskQueue_);
    }
    if (!com.google.protobuf.GeneratedMessage.isStringEmpty(supersedesExecutionId_)) {
      size += com.google.protobuf.GeneratedMessage.computeStringSize(12, supersedesExecutionId_);
    }
    if (((bitField0_ & 0x00000001) != 0)) {
      size += com.google.protobuf.CodedOutputStream
        .computeMessageSize(13, getSessionSpec());
    }
    if (((bitField0_ & 0x00000004) != 0)) {
      size += com.google.protobuf.CodedOutputStream
        .computeMessageSize(14, getConversationCatchup());
    }
    if (((bitField0_ & 0x00000008) != 0)) {
      size += com.google.protobuf.CodedOutputStream
        .computeMessageSize(15, getDeclaredPreferences());
    }
    if (((bitField0_ & 0x00000010) != 0)) {
      size += com.google.protobuf.CodedOutputStream
        .computeMessageSize(16, getRecalledMemories());
    }
    size += getUnknownFields().getSerializedSize();
    memoizedSize = size;
    return size;
  }

  @java.lang.Override
  public boolean equals(final java.lang.Object obj) {
    if (obj == this) {
     return true;
    }
    if (!(obj instanceof ai.stigmer.agentic.agentexecution.v1.AgentExecutionSpec)) {
      return super.equals(obj);
    }
    ai.stigmer.agentic.agentexecution.v1.AgentExecutionSpec other = (ai.stigmer.agentic.agentexecution.v1.AgentExecutionSpec) obj;

    if (!getSessionId()
        .equals(other.getSessionId())) return false;
    if (!getAgentId()
        .equals(other.getAgentId())) return false;
    if (hasSessionSpec() != other.hasSessionSpec()) return false;
    if (hasSessionSpec()) {
      if (!getSessionSpec()
          .equals(other.getSessionSpec())) return false;
    }
    if (!getMessage()
        .equals(other.getMessage())) return false;
    if (hasExecutionConfig() != other.hasExecutionConfig()) return false;
    if (hasExecutionConfig()) {
      if (!getExecutionConfig()
          .equals(other.getExecutionConfig())) return false;
    }
    if (!internalGetRuntimeEnv().equals(
        other.internalGetRuntimeEnv())) return false;
    if (!getCallbackToken()
        .equals(other.getCallbackToken())) return false;
    if (getAutoApproveAll()
        != other.getAutoApproveAll()) return false;
    if (!getParentWorkflowId()
        .equals(other.getParentWorkflowId())) return false;
    if (!getAttachmentsList()
        .equals(other.getAttachmentsList())) return false;
    if (!getWorkspaceFileRefsList()
        .equals(other.getWorkspaceFileRefsList())) return false;
    if (!getActivityTaskQueue()
        .equals(other.getActivityTaskQueue())) return false;
    if (!getSupersedesExecutionId()
        .equals(other.getSupersedesExecutionId())) return false;
    if (hasConversationCatchup() != other.hasConversationCatchup()) return false;
    if (hasConversationCatchup()) {
      if (!getConversationCatchup()
          .equals(other.getConversationCatchup())) return false;
    }
    if (hasDeclaredPreferences() != other.hasDeclaredPreferences()) return false;
    if (hasDeclaredPreferences()) {
      if (!getDeclaredPreferences()
          .equals(other.getDeclaredPreferences())) return false;
    }
    if (hasRecalledMemories() != other.hasRecalledMemories()) return false;
    if (hasRecalledMemories()) {
      if (!getRecalledMemories()
          .equals(other.getRecalledMemories())) return false;
    }
    if (!getUnknownFields().equals(other.getUnknownFields())) return false;
    return true;
  }

  @java.lang.Override
  public int hashCode() {
    if (memoizedHashCode != 0) {
      return memoizedHashCode;
    }
    int hash = 41;
    hash = (19 * hash) + getDescriptor().hashCode();
    hash = (37 * hash) + SESSION_ID_FIELD_NUMBER;
    hash = (53 * hash) + getSessionId().hashCode();
    hash = (37 * hash) + AGENT_ID_FIELD_NUMBER;
    hash = (53 * hash) + getAgentId().hashCode();
    if (hasSessionSpec()) {
      hash = (37 * hash) + SESSION_SPEC_FIELD_NUMBER;
      hash = (53 * hash) + getSessionSpec().hashCode();
    }
    hash = (37 * hash) + MESSAGE_FIELD_NUMBER;
    hash = (53 * hash) + getMessage().hashCode();
    if (hasExecutionConfig()) {
      hash = (37 * hash) + EXECUTION_CONFIG_FIELD_NUMBER;
      hash = (53 * hash) + getExecutionConfig().hashCode();
    }
    if (!internalGetRuntimeEnv().getMap().isEmpty()) {
      hash = (37 * hash) + RUNTIME_ENV_FIELD_NUMBER;
      hash = (53 * hash) + internalGetRuntimeEnv().hashCode();
    }
    hash = (37 * hash) + CALLBACK_TOKEN_FIELD_NUMBER;
    hash = (53 * hash) + getCallbackToken().hashCode();
    hash = (37 * hash) + AUTO_APPROVE_ALL_FIELD_NUMBER;
    hash = (53 * hash) + com.google.protobuf.Internal.hashBoolean(
        getAutoApproveAll());
    hash = (37 * hash) + PARENT_WORKFLOW_ID_FIELD_NUMBER;
    hash = (53 * hash) + getParentWorkflowId().hashCode();
    if (getAttachmentsCount() > 0) {
      hash = (37 * hash) + ATTACHMENTS_FIELD_NUMBER;
      hash = (53 * hash) + getAttachmentsList().hashCode();
    }
    if (getWorkspaceFileRefsCount() > 0) {
      hash = (37 * hash) + WORKSPACE_FILE_REFS_FIELD_NUMBER;
      hash = (53 * hash) + getWorkspaceFileRefsList().hashCode();
    }
    hash = (37 * hash) + ACTIVITY_TASK_QUEUE_FIELD_NUMBER;
    hash = (53 * hash) + getActivityTaskQueue().hashCode();
    hash = (37 * hash) + SUPERSEDES_EXECUTION_ID_FIELD_NUMBER;
    hash = (53 * hash) + getSupersedesExecutionId().hashCode();
    if (hasConversationCatchup()) {
      hash = (37 * hash) + CONVERSATION_CATCHUP_FIELD_NUMBER;
      hash = (53 * hash) + getConversationCatchup().hashCode();
    }
    if (hasDeclaredPreferences()) {
      hash = (37 * hash) + DECLARED_PREFERENCES_FIELD_NUMBER;
      hash = (53 * hash) + getDeclaredPreferences().hashCode();
    }
    if (hasRecalledMemories()) {
      hash = (37 * hash) + RECALLED_MEMORIES_FIELD_NUMBER;
      hash = (53 * hash) + getRecalledMemories().hashCode();
    }
    hash = (29 * hash) + getUnknownFields().hashCode();
    memoizedHashCode = hash;
    return hash;
  }

  public static ai.stigmer.agentic.agentexecution.v1.AgentExecutionSpec parseFrom(
      java.nio.ByteBuffer data)
      throws com.google.protobuf.InvalidProtocolBufferException {
    return PARSER.parseFrom(data);
  }
  public static ai.stigmer.agentic.agentexecution.v1.AgentExecutionSpec parseFrom(
      java.nio.ByteBuffer data,
      com.google.protobuf.ExtensionRegistryLite extensionRegistry)
      throws com.google.protobuf.InvalidProtocolBufferException {
    return PARSER.parseFrom(data, extensionRegistry);
  }
  public static ai.stigmer.agentic.agentexecution.v1.AgentExecutionSpec parseFrom(
      com.google.protobuf.ByteString data)
      throws com.google.protobuf.InvalidProtocolBufferException {
    return PARSER.parseFrom(data);
  }
  public static ai.stigmer.agentic.agentexecution.v1.AgentExecutionSpec parseFrom(
      com.google.protobuf.ByteString data,
      com.google.protobuf.ExtensionRegistryLite extensionRegistry)
      throws com.google.protobuf.InvalidProtocolBufferException {
    return PARSER.parseFrom(data, extensionRegistry);
  }
  public static ai.stigmer.agentic.agentexecution.v1.AgentExecutionSpec parseFrom(byte[] data)
      throws com.google.protobuf.InvalidProtocolBufferException {
    return PARSER.parseFrom(data);
  }
  public static ai.stigmer.agentic.agentexecution.v1.AgentExecutionSpec parseFrom(
      byte[] data,
      com.google.protobuf.ExtensionRegistryLite extensionRegistry)
      throws com.google.protobuf.InvalidProtocolBufferException {
    return PARSER.parseFrom(data, extensionRegistry);
  }
  public static ai.stigmer.agentic.agentexecution.v1.AgentExecutionSpec parseFrom(java.io.InputStream input)
      throws java.io.IOException {
    return com.google.protobuf.GeneratedMessage
        .parseWithIOException(PARSER, input);
  }
  public static ai.stigmer.agentic.agentexecution.v1.AgentExecutionSpec parseFrom(
      java.io.InputStream input,
      com.google.protobuf.ExtensionRegistryLite extensionRegistry)
      throws java.io.IOException {
    return com.google.protobuf.GeneratedMessage
        .parseWithIOException(PARSER, input, extensionRegistry);
  }

  public static ai.stigmer.agentic.agentexecution.v1.AgentExecutionSpec parseDelimitedFrom(java.io.InputStream input)
      throws java.io.IOException {
    return com.google.protobuf.GeneratedMessage
        .parseDelimitedWithIOException(PARSER, input);
  }

  public static ai.stigmer.agentic.agentexecution.v1.AgentExecutionSpec parseDelimitedFrom(
      java.io.InputStream input,
      com.google.protobuf.ExtensionRegistryLite extensionRegistry)
      throws java.io.IOException {
    return com.google.protobuf.GeneratedMessage
        .parseDelimitedWithIOException(PARSER, input, extensionRegistry);
  }
  public static ai.stigmer.agentic.agentexecution.v1.AgentExecutionSpec parseFrom(
      com.google.protobuf.CodedInputStream input)
      throws java.io.IOException {
    return com.google.protobuf.GeneratedMessage
        .parseWithIOException(PARSER, input);
  }
  public static ai.stigmer.agentic.agentexecution.v1.AgentExecutionSpec parseFrom(
      com.google.protobuf.CodedInputStream input,
      com.google.protobuf.ExtensionRegistryLite extensionRegistry)
      throws java.io.IOException {
    return com.google.protobuf.GeneratedMessage
        .parseWithIOException(PARSER, input, extensionRegistry);
  }

  @java.lang.Override
  public Builder newBuilderForType() { return newBuilder(); }
  public static Builder newBuilder() {
    return DEFAULT_INSTANCE.toBuilder();
  }
  public static Builder newBuilder(ai.stigmer.agentic.agentexecution.v1.AgentExecutionSpec prototype) {
    return DEFAULT_INSTANCE.toBuilder().mergeFrom(prototype);
  }
  @java.lang.Override
  public Builder toBuilder() {
    return this == DEFAULT_INSTANCE
        ? new Builder() : new Builder().mergeFrom(this);
  }

  @java.lang.Override
  protected Builder newBuilderForType(
      com.google.protobuf.GeneratedMessage.BuilderParent parent) {
    Builder builder = new Builder(parent);
    return builder;
  }
  /**
   * <pre>
   * AgentExecutionSpec contains only user-provided inputs for triggering an execution.
   * All execution results and state live in AgentExecutionStatus (in api.proto).
   * </pre>
   *
   * Protobuf type {@code ai.stigmer.agentic.agentexecution.v1.AgentExecutionSpec}
   */
  public static final class Builder extends
      com.google.protobuf.GeneratedMessage.Builder<Builder> implements
      // @@protoc_insertion_point(builder_implements:ai.stigmer.agentic.agentexecution.v1.AgentExecutionSpec)
      ai.stigmer.agentic.agentexecution.v1.AgentExecutionSpecOrBuilder {
    public static final com.google.protobuf.Descriptors.Descriptor
        getDescriptor() {
      return ai.stigmer.agentic.agentexecution.v1.SpecProto.internal_static_ai_stigmer_agentic_agentexecution_v1_AgentExecutionSpec_descriptor;
    }

    @SuppressWarnings({"rawtypes"})
    protected com.google.protobuf.MapFieldReflectionAccessor internalGetMapFieldReflection(
        int number) {
      switch (number) {
        case 5:
          return internalGetRuntimeEnv();
        default:
          throw new RuntimeException(
              "Invalid map field number: " + number);
      }
    }
    @SuppressWarnings({"rawtypes"})
    protected com.google.protobuf.MapFieldReflectionAccessor internalGetMutableMapFieldReflection(
        int number) {
      switch (number) {
        case 5:
          return internalGetMutableRuntimeEnv();
        default:
          throw new RuntimeException(
              "Invalid map field number: " + number);
      }
    }
    @java.lang.Override
    protected com.google.protobuf.GeneratedMessage.FieldAccessorTable
        internalGetFieldAccessorTable() {
      return ai.stigmer.agentic.agentexecution.v1.SpecProto.internal_static_ai_stigmer_agentic_agentexecution_v1_AgentExecutionSpec_fieldAccessorTable
          .ensureFieldAccessorsInitialized(
              ai.stigmer.agentic.agentexecution.v1.AgentExecutionSpec.class, ai.stigmer.agentic.agentexecution.v1.AgentExecutionSpec.Builder.class);
    }

    // Construct using ai.stigmer.agentic.agentexecution.v1.AgentExecutionSpec.newBuilder()
    private Builder() {
      maybeForceBuilderInitialization();
    }

    private Builder(
        com.google.protobuf.GeneratedMessage.BuilderParent parent) {
      super(parent);
      maybeForceBuilderInitialization();
    }
    private void maybeForceBuilderInitialization() {
      if (com.google.protobuf.GeneratedMessage
              .alwaysUseFieldBuilders) {
        internalGetSessionSpecFieldBuilder();
        internalGetExecutionConfigFieldBuilder();
        internalGetAttachmentsFieldBuilder();
        internalGetConversationCatchupFieldBuilder();
        internalGetDeclaredPreferencesFieldBuilder();
        internalGetRecalledMemoriesFieldBuilder();
      }
    }
    @java.lang.Override
    public Builder clear() {
      super.clear();
      bitField0_ = 0;
      sessionId_ = "";
      agentId_ = "";
      sessionSpec_ = null;
      if (sessionSpecBuilder_ != null) {
        sessionSpecBuilder_.dispose();
        sessionSpecBuilder_ = null;
      }
      message_ = "";
      executionConfig_ = null;
      if (executionConfigBuilder_ != null) {
        executionConfigBuilder_.dispose();
        executionConfigBuilder_ = null;
      }
      internalGetMutableRuntimeEnv().clear();
      callbackToken_ = com.google.protobuf.ByteString.EMPTY;
      autoApproveAll_ = false;
      parentWorkflowId_ = "";
      if (attachmentsBuilder_ == null) {
        attachments_ = java.util.Collections.emptyList();
      } else {
        attachments_ = null;
        attachmentsBuilder_.clear();
      }
      bitField0_ = (bitField0_ & ~0x00000200);
      workspaceFileRefs_ =
          com.google.protobuf.LazyStringArrayList.emptyList();
      activityTaskQueue_ = "";
      supersedesExecutionId_ = "";
      conversationCatchup_ = null;
      if (conversationCatchupBuilder_ != null) {
        conversationCatchupBuilder_.dispose();
        conversationCatchupBuilder_ = null;
      }
      declaredPreferences_ = null;
      if (declaredPreferencesBuilder_ != null) {
        declaredPreferencesBuilder_.dispose();
        declaredPreferencesBuilder_ = null;
      }
      recalledMemories_ = null;
      if (recalledMemoriesBuilder_ != null) {
        recalledMemoriesBuilder_.dispose();
        recalledMemoriesBuilder_ = null;
      }
      return this;
    }

    @java.lang.Override
    public com.google.protobuf.Descriptors.Descriptor
        getDescriptorForType() {
      return ai.stigmer.agentic.agentexecution.v1.SpecProto.internal_static_ai_stigmer_agentic_agentexecution_v1_AgentExecutionSpec_descriptor;
    }

    @java.lang.Override
    public ai.stigmer.agentic.agentexecution.v1.AgentExecutionSpec getDefaultInstanceForType() {
      return ai.stigmer.agentic.agentexecution.v1.AgentExecutionSpec.getDefaultInstance();
    }

    @java.lang.Override
    public ai.stigmer.agentic.agentexecution.v1.AgentExecutionSpec build() {
      ai.stigmer.agentic.agentexecution.v1.AgentExecutionSpec result = buildPartial();
      if (!result.isInitialized()) {
        throw newUninitializedMessageException(result);
      }
      return result;
    }

    @java.lang.Override
    public ai.stigmer.agentic.agentexecution.v1.AgentExecutionSpec buildPartial() {
      ai.stigmer.agentic.agentexecution.v1.AgentExecutionSpec result = new ai.stigmer.agentic.agentexecution.v1.AgentExecutionSpec(this);
      buildPartialRepeatedFields(result);
      if (bitField0_ != 0) { buildPartial0(result); }
      onBuilt();
      return result;
    }

    private void buildPartialRepeatedFields(ai.stigmer.agentic.agentexecution.v1.AgentExecutionSpec result) {
      if (attachmentsBuilder_ == null) {
        if (((bitField0_ & 0x00000200) != 0)) {
          attachments_ = java.util.Collections.unmodifiableList(attachments_);
          bitField0_ = (bitField0_ & ~0x00000200);
        }
        result.attachments_ = attachments_;
      } else {
        result.attachments_ = attachmentsBuilder_.build();
      }
    }

    private void buildPartial0(ai.stigmer.agentic.agentexecution.v1.AgentExecutionSpec result) {
      int from_bitField0_ = bitField0_;
      if (((from_bitField0_ & 0x00000001) != 0)) {
        result.sessionId_ = sessionId_;
      }
      if (((from_bitField0_ & 0x00000002) != 0)) {
        result.agentId_ = agentId_;
      }
      int to_bitField0_ = 0;
      if (((from_bitField0_ & 0x00000004) != 0)) {
        result.sessionSpec_ = sessionSpecBuilder_ == null
            ? sessionSpec_
            : sessionSpecBuilder_.build();
        to_bitField0_ |= 0x00000001;
      }
      if (((from_bitField0_ & 0x00000008) != 0)) {
        result.message_ = message_;
      }
      if (((from_bitField0_ & 0x00000010) != 0)) {
        result.executionConfig_ = executionConfigBuilder_ == null
            ? executionConfig_
            : executionConfigBuilder_.build();
        to_bitField0_ |= 0x00000002;
      }
      if (((from_bitField0_ & 0x00000020) != 0)) {
        result.runtimeEnv_ = internalGetRuntimeEnv().build(RuntimeEnvDefaultEntryHolder.defaultEntry);
      }
      if (((from_bitField0_ & 0x00000040) != 0)) {
        result.callbackToken_ = callbackToken_;
      }
      if (((from_bitField0_ & 0x00000080) != 0)) {
        result.autoApproveAll_ = autoApproveAll_;
      }
      if (((from_bitField0_ & 0x00000100) != 0)) {
        result.parentWorkflowId_ = parentWorkflowId_;
      }
      if (((from_bitField0_ & 0x00000400) != 0)) {
        workspaceFileRefs_.makeImmutable();
        result.workspaceFileRefs_ = workspaceFileRefs_;
      }
      if (((from_bitField0_ & 0x00000800) != 0)) {
        result.activityTaskQueue_ = activityTaskQueue_;
      }
      if (((from_bitField0_ & 0x00001000) != 0)) {
        result.supersedesExecutionId_ = supersedesExecutionId_;
      }
      if (((from_bitField0_ & 0x00002000) != 0)) {
        result.conversationCatchup_ = conversationCatchupBuilder_ == null
            ? conversationCatchup_
            : conversationCatchupBuilder_.build();
        to_bitField0_ |= 0x00000004;
      }
      if (((from_bitField0_ & 0x00004000) != 0)) {
        result.declaredPreferences_ = declaredPreferencesBuilder_ == null
            ? declaredPreferences_
            : declaredPreferencesBuilder_.build();
        to_bitField0_ |= 0x00000008;
      }
      if (((from_bitField0_ & 0x00008000) != 0)) {
        result.recalledMemories_ = recalledMemoriesBuilder_ == null
            ? recalledMemories_
            : recalledMemoriesBuilder_.build();
        to_bitField0_ |= 0x00000010;
      }
      result.bitField0_ |= to_bitField0_;
    }

    @java.lang.Override
    public Builder mergeFrom(com.google.protobuf.Message other) {
      if (other instanceof ai.stigmer.agentic.agentexecution.v1.AgentExecutionSpec) {
        return mergeFrom((ai.stigmer.agentic.agentexecution.v1.AgentExecutionSpec)other);
      } else {
        super.mergeFrom(other);
        return this;
      }
    }

    public Builder mergeFrom(ai.stigmer.agentic.agentexecution.v1.AgentExecutionSpec other) {
      if (other == ai.stigmer.agentic.agentexecution.v1.AgentExecutionSpec.getDefaultInstance()) return this;
      if (!other.getSessionId().isEmpty()) {
        sessionId_ = other.sessionId_;
        bitField0_ |= 0x00000001;
        onChanged();
      }
      if (!other.getAgentId().isEmpty()) {
        agentId_ = other.agentId_;
        bitField0_ |= 0x00000002;
        onChanged();
      }
      if (other.hasSessionSpec()) {
        mergeSessionSpec(other.getSessionSpec());
      }
      if (!other.getMessage().isEmpty()) {
        message_ = other.message_;
        bitField0_ |= 0x00000008;
        onChanged();
      }
      if (other.hasExecutionConfig()) {
        mergeExecutionConfig(other.getExecutionConfig());
      }
      internalGetMutableRuntimeEnv().mergeFrom(
          other.internalGetRuntimeEnv());
      bitField0_ |= 0x00000020;
      if (!other.getCallbackToken().isEmpty()) {
        setCallbackToken(other.getCallbackToken());
      }
      if (other.getAutoApproveAll() != false) {
        setAutoApproveAll(other.getAutoApproveAll());
      }
      if (!other.getParentWorkflowId().isEmpty()) {
        parentWorkflowId_ = other.parentWorkflowId_;
        bitField0_ |= 0x00000100;
        onChanged();
      }
      if (attachmentsBuilder_ == null) {
        if (!other.attachments_.isEmpty()) {
          if (attachments_.isEmpty()) {
            attachments_ = other.attachments_;
            bitField0_ = (bitField0_ & ~0x00000200);
          } else {
            ensureAttachmentsIsMutable();
            attachments_.addAll(other.attachments_);
          }
          onChanged();
        }
      } else {
        if (!other.attachments_.isEmpty()) {
          if (attachmentsBuilder_.isEmpty()) {
            attachmentsBuilder_.dispose();
            attachmentsBuilder_ = null;
            attachments_ = other.attachments_;
            bitField0_ = (bitField0_ & ~0x00000200);
            attachmentsBuilder_ = 
              com.google.protobuf.GeneratedMessage.alwaysUseFieldBuilders ?
                 internalGetAttachmentsFieldBuilder() : null;
          } else {
            attachmentsBuilder_.addAllMessages(other.attachments_);
          }
        }
      }
      if (!other.workspaceFileRefs_.isEmpty()) {
        if (workspaceFileRefs_.isEmpty()) {
          workspaceFileRefs_ = other.workspaceFileRefs_;
          bitField0_ |= 0x00000400;
        } else {
          ensureWorkspaceFileRefsIsMutable();
          workspaceFileRefs_.addAll(other.workspaceFileRefs_);
        }
        onChanged();
      }
      if (!other.getActivityTaskQueue().isEmpty()) {
        activityTaskQueue_ = other.activityTaskQueue_;
        bitField0_ |= 0x00000800;
        onChanged();
      }
      if (!other.getSupersedesExecutionId().isEmpty()) {
        supersedesExecutionId_ = other.supersedesExecutionId_;
        bitField0_ |= 0x00001000;
        onChanged();
      }
      if (other.hasConversationCatchup()) {
        mergeConversationCatchup(other.getConversationCatchup());
      }
      if (other.hasDeclaredPreferences()) {
        mergeDeclaredPreferences(other.getDeclaredPreferences());
      }
      if (other.hasRecalledMemories()) {
        mergeRecalledMemories(other.getRecalledMemories());
      }
      this.mergeUnknownFields(other.getUnknownFields());
      onChanged();
      return this;
    }

    @java.lang.Override
    public final boolean isInitialized() {
      return true;
    }

    @java.lang.Override
    public Builder mergeFrom(
        com.google.protobuf.CodedInputStream input,
        com.google.protobuf.ExtensionRegistryLite extensionRegistry)
        throws java.io.IOException {
      if (extensionRegistry == null) {
        throw new java.lang.NullPointerException();
      }
      try {
        boolean done = false;
        while (!done) {
          int tag = input.readTag();
          switch (tag) {
            case 0:
              done = true;
              break;
            case 10: {
              sessionId_ = input.readStringRequireUtf8();
              bitField0_ |= 0x00000001;
              break;
            } // case 10
            case 18: {
              agentId_ = input.readStringRequireUtf8();
              bitField0_ |= 0x00000002;
              break;
            } // case 18
            case 26: {
              message_ = input.readStringRequireUtf8();
              bitField0_ |= 0x00000008;
              break;
            } // case 26
            case 34: {
              input.readMessage(
                  internalGetExecutionConfigFieldBuilder().getBuilder(),
                  extensionRegistry);
              bitField0_ |= 0x00000010;
              break;
            } // case 34
            case 42: {
              com.google.protobuf.MapEntry<java.lang.String, ai.stigmer.agentic.executioncontext.v1.ExecutionValue>
              runtimeEnv__ = input.readMessage(
                  RuntimeEnvDefaultEntryHolder.defaultEntry.getParserForType(), extensionRegistry);
              internalGetMutableRuntimeEnv().ensureBuilderMap().put(
                  runtimeEnv__.getKey(), runtimeEnv__.getValue());
              bitField0_ |= 0x00000020;
              break;
            } // case 42
            case 50: {
              callbackToken_ = input.readBytes();
              bitField0_ |= 0x00000040;
              break;
            } // case 50
            case 56: {
              autoApproveAll_ = input.readBool();
              bitField0_ |= 0x00000080;
              break;
            } // case 56
            case 66: {
              parentWorkflowId_ = input.readStringRequireUtf8();
              bitField0_ |= 0x00000100;
              break;
            } // case 66
            case 74: {
              ai.stigmer.agentic.agentexecution.v1.Attachment m =
                  input.readMessage(
                      ai.stigmer.agentic.agentexecution.v1.Attachment.parser(),
                      extensionRegistry);
              if (attachmentsBuilder_ == null) {
                ensureAttachmentsIsMutable();
                attachments_.add(m);
              } else {
                attachmentsBuilder_.addMessage(m);
              }
              break;
            } // case 74
            case 82: {
              ensureWorkspaceFileRefsIsMutable();
              workspaceFileRefs_.add(input.readStringRequireUtf8());
              break;
            } // case 82
            case 90: {
              activityTaskQueue_ = input.readStringRequireUtf8();
              bitField0_ |= 0x00000800;
              break;
            } // case 90
            case 98: {
              supersedesExecutionId_ = input.readStringRequireUtf8();
              bitField0_ |= 0x00001000;
              break;
            } // case 98
            case 106: {
              input.readMessage(
                  internalGetSessionSpecFieldBuilder().getBuilder(),
                  extensionRegistry);
              bitField0_ |= 0x00000004;
              break;
            } // case 106
            case 114: {
              input.readMessage(
                  internalGetConversationCatchupFieldBuilder().getBuilder(),
                  extensionRegistry);
              bitField0_ |= 0x00002000;
              break;
            } // case 114
            case 122: {
              input.readMessage(
                  internalGetDeclaredPreferencesFieldBuilder().getBuilder(),
                  extensionRegistry);
              bitField0_ |= 0x00004000;
              break;
            } // case 122
            case 130: {
              input.readMessage(
                  internalGetRecalledMemoriesFieldBuilder().getBuilder(),
                  extensionRegistry);
              bitField0_ |= 0x00008000;
              break;
            } // case 130
            default: {
              if (!super.parseUnknownField(input, extensionRegistry, tag)) {
                done = true; // was an endgroup tag
              }
              break;
            } // default:
          } // switch (tag)
        } // while (!done)
      } catch (com.google.protobuf.InvalidProtocolBufferException e) {
        throw e.unwrapIOException();
      } finally {
        onChanged();
      } // finally
      return this;
    }
    private int bitField0_;

    private java.lang.Object sessionId_ = "";
    /**
     * <pre>
     * Session ID this execution belongs to (optional).
     *
     * Resolution priority (enforced in handler pipeline):
     * 1. session_id provided     -&gt; use existing session
     * 2. session_spec provided   -&gt; auto-create session from the embedded spec
     * 3. agent_id provided       -&gt; auto-create session using agent's default instance
     * 4. neither provided        -&gt; resolve platform default agent (label
     * stigmer.ai/default-agent + visibility_public), then auto-create session
     *
     * session_id and agent_id may both be set — when both are present, session_id
     * is used for session resolution and agent_id is preserved as metadata for
     * downstream consumers (e.g., session subject generation). session_id and
     * session_spec are mutually exclusive.
     * </pre>
     *
     * <code>string session_id = 1 [json_name = "sessionId"];</code>
     * @return The sessionId.
     */
    public java.lang.String getSessionId() {
      java.lang.Object ref = sessionId_;
      if (!(ref instanceof java.lang.String)) {
        com.google.protobuf.ByteString bs =
            (com.google.protobuf.ByteString) ref;
        java.lang.String s = bs.toStringUtf8();
        sessionId_ = s;
        return s;
      } else {
        return (java.lang.String) ref;
      }
    }
    /**
     * <pre>
     * Session ID this execution belongs to (optional).
     *
     * Resolution priority (enforced in handler pipeline):
     * 1. session_id provided     -&gt; use existing session
     * 2. session_spec provided   -&gt; auto-create session from the embedded spec
     * 3. agent_id provided       -&gt; auto-create session using agent's default instance
     * 4. neither provided        -&gt; resolve platform default agent (label
     * stigmer.ai/default-agent + visibility_public), then auto-create session
     *
     * session_id and agent_id may both be set — when both are present, session_id
     * is used for session resolution and agent_id is preserved as metadata for
     * downstream consumers (e.g., session subject generation). session_id and
     * session_spec are mutually exclusive.
     * </pre>
     *
     * <code>string session_id = 1 [json_name = "sessionId"];</code>
     * @return The bytes for sessionId.
     */
    public com.google.protobuf.ByteString
        getSessionIdBytes() {
      java.lang.Object ref = sessionId_;
      if (ref instanceof String) {
        com.google.protobuf.ByteString b = 
            com.google.protobuf.ByteString.copyFromUtf8(
                (java.lang.String) ref);
        sessionId_ = b;
        return b;
      } else {
        return (com.google.protobuf.ByteString) ref;
      }
    }
    /**
     * <pre>
     * Session ID this execution belongs to (optional).
     *
     * Resolution priority (enforced in handler pipeline):
     * 1. session_id provided     -&gt; use existing session
     * 2. session_spec provided   -&gt; auto-create session from the embedded spec
     * 3. agent_id provided       -&gt; auto-create session using agent's default instance
     * 4. neither provided        -&gt; resolve platform default agent (label
     * stigmer.ai/default-agent + visibility_public), then auto-create session
     *
     * session_id and agent_id may both be set — when both are present, session_id
     * is used for session resolution and agent_id is preserved as metadata for
     * downstream consumers (e.g., session subject generation). session_id and
     * session_spec are mutually exclusive.
     * </pre>
     *
     * <code>string session_id = 1 [json_name = "sessionId"];</code>
     * @param value The sessionId to set.
     * @return This builder for chaining.
     */
    public Builder setSessionId(
        java.lang.String value) {
      if (value == null) { throw new NullPointerException(); }
      sessionId_ = value;
      bitField0_ |= 0x00000001;
      onChanged();
      return this;
    }
    /**
     * <pre>
     * Session ID this execution belongs to (optional).
     *
     * Resolution priority (enforced in handler pipeline):
     * 1. session_id provided     -&gt; use existing session
     * 2. session_spec provided   -&gt; auto-create session from the embedded spec
     * 3. agent_id provided       -&gt; auto-create session using agent's default instance
     * 4. neither provided        -&gt; resolve platform default agent (label
     * stigmer.ai/default-agent + visibility_public), then auto-create session
     *
     * session_id and agent_id may both be set — when both are present, session_id
     * is used for session resolution and agent_id is preserved as metadata for
     * downstream consumers (e.g., session subject generation). session_id and
     * session_spec are mutually exclusive.
     * </pre>
     *
     * <code>string session_id = 1 [json_name = "sessionId"];</code>
     * @return This builder for chaining.
     */
    public Builder clearSessionId() {
      sessionId_ = getDefaultInstance().getSessionId();
      bitField0_ = (bitField0_ & ~0x00000001);
      onChanged();
      return this;
    }
    /**
     * <pre>
     * Session ID this execution belongs to (optional).
     *
     * Resolution priority (enforced in handler pipeline):
     * 1. session_id provided     -&gt; use existing session
     * 2. session_spec provided   -&gt; auto-create session from the embedded spec
     * 3. agent_id provided       -&gt; auto-create session using agent's default instance
     * 4. neither provided        -&gt; resolve platform default agent (label
     * stigmer.ai/default-agent + visibility_public), then auto-create session
     *
     * session_id and agent_id may both be set — when both are present, session_id
     * is used for session resolution and agent_id is preserved as metadata for
     * downstream consumers (e.g., session subject generation). session_id and
     * session_spec are mutually exclusive.
     * </pre>
     *
     * <code>string session_id = 1 [json_name = "sessionId"];</code>
     * @param value The bytes for sessionId to set.
     * @return This builder for chaining.
     */
    public Builder setSessionIdBytes(
        com.google.protobuf.ByteString value) {
      if (value == null) { throw new NullPointerException(); }
      checkByteStringIsUtf8(value);
      sessionId_ = value;
      bitField0_ |= 0x00000001;
      onChanged();
      return this;
    }

    private java.lang.Object agentId_ = "";
    /**
     * <pre>
     * Agent ID (optional).
     *
     * When absent along with session_id, the backend resolves the platform's
     * public default agent — the agent labeled stigmer.ai/default-agent: "true"
     * with visibility_public. This enables session-first UX where users start
     * a conversation without choosing an agent.
     *
     * Both may be set — agent_id is preserved on the execution record even when
     * session_id is present, so downstream consumers can access the agent
     * without resolving through the session chain.
     *
     * When provided without session_id, a new session is auto-created using
     * the agent's default instance ID.
     * </pre>
     *
     * <code>string agent_id = 2 [json_name = "agentId"];</code>
     * @return The agentId.
     */
    public java.lang.String getAgentId() {
      java.lang.Object ref = agentId_;
      if (!(ref instanceof java.lang.String)) {
        com.google.protobuf.ByteString bs =
            (com.google.protobuf.ByteString) ref;
        java.lang.String s = bs.toStringUtf8();
        agentId_ = s;
        return s;
      } else {
        return (java.lang.String) ref;
      }
    }
    /**
     * <pre>
     * Agent ID (optional).
     *
     * When absent along with session_id, the backend resolves the platform's
     * public default agent — the agent labeled stigmer.ai/default-agent: "true"
     * with visibility_public. This enables session-first UX where users start
     * a conversation without choosing an agent.
     *
     * Both may be set — agent_id is preserved on the execution record even when
     * session_id is present, so downstream consumers can access the agent
     * without resolving through the session chain.
     *
     * When provided without session_id, a new session is auto-created using
     * the agent's default instance ID.
     * </pre>
     *
     * <code>string agent_id = 2 [json_name = "agentId"];</code>
     * @return The bytes for agentId.
     */
    public com.google.protobuf.ByteString
        getAgentIdBytes() {
      java.lang.Object ref = agentId_;
      if (ref instanceof String) {
        com.google.protobuf.ByteString b = 
            com.google.protobuf.ByteString.copyFromUtf8(
                (java.lang.String) ref);
        agentId_ = b;
        return b;
      } else {
        return (com.google.protobuf.ByteString) ref;
      }
    }
    /**
     * <pre>
     * Agent ID (optional).
     *
     * When absent along with session_id, the backend resolves the platform's
     * public default agent — the agent labeled stigmer.ai/default-agent: "true"
     * with visibility_public. This enables session-first UX where users start
     * a conversation without choosing an agent.
     *
     * Both may be set — agent_id is preserved on the execution record even when
     * session_id is present, so downstream consumers can access the agent
     * without resolving through the session chain.
     *
     * When provided without session_id, a new session is auto-created using
     * the agent's default instance ID.
     * </pre>
     *
     * <code>string agent_id = 2 [json_name = "agentId"];</code>
     * @param value The agentId to set.
     * @return This builder for chaining.
     */
    public Builder setAgentId(
        java.lang.String value) {
      if (value == null) { throw new NullPointerException(); }
      agentId_ = value;
      bitField0_ |= 0x00000002;
      onChanged();
      return this;
    }
    /**
     * <pre>
     * Agent ID (optional).
     *
     * When absent along with session_id, the backend resolves the platform's
     * public default agent — the agent labeled stigmer.ai/default-agent: "true"
     * with visibility_public. This enables session-first UX where users start
     * a conversation without choosing an agent.
     *
     * Both may be set — agent_id is preserved on the execution record even when
     * session_id is present, so downstream consumers can access the agent
     * without resolving through the session chain.
     *
     * When provided without session_id, a new session is auto-created using
     * the agent's default instance ID.
     * </pre>
     *
     * <code>string agent_id = 2 [json_name = "agentId"];</code>
     * @return This builder for chaining.
     */
    public Builder clearAgentId() {
      agentId_ = getDefaultInstance().getAgentId();
      bitField0_ = (bitField0_ & ~0x00000002);
      onChanged();
      return this;
    }
    /**
     * <pre>
     * Agent ID (optional).
     *
     * When absent along with session_id, the backend resolves the platform's
     * public default agent — the agent labeled stigmer.ai/default-agent: "true"
     * with visibility_public. This enables session-first UX where users start
     * a conversation without choosing an agent.
     *
     * Both may be set — agent_id is preserved on the execution record even when
     * session_id is present, so downstream consumers can access the agent
     * without resolving through the session chain.
     *
     * When provided without session_id, a new session is auto-created using
     * the agent's default instance ID.
     * </pre>
     *
     * <code>string agent_id = 2 [json_name = "agentId"];</code>
     * @param value The bytes for agentId to set.
     * @return This builder for chaining.
     */
    public Builder setAgentIdBytes(
        com.google.protobuf.ByteString value) {
      if (value == null) { throw new NullPointerException(); }
      checkByteStringIsUtf8(value);
      agentId_ = value;
      bitField0_ |= 0x00000002;
      onChanged();
      return this;
    }

    private ai.stigmer.agentic.session.v1.SessionSpec sessionSpec_;
    private com.google.protobuf.SingleFieldBuilder<
        ai.stigmer.agentic.session.v1.SessionSpec, ai.stigmer.agentic.session.v1.SessionSpec.Builder, ai.stigmer.agentic.session.v1.SessionSpecOrBuilder> sessionSpecBuilder_;
    /**
     * <pre>
     * Spec for the session to auto-create when session_id is empty (optional).
     *
     * This is the one-call session bootstrap: a single create carries the full
     * session shape (workspace_entries, harness, execution_target, MCP servers,
     * skills) together with the first message, so embedders do not need to
     * orchestrate session.create followed by agentExecution.create. The created
     * session's ID is returned on the persisted execution's session_id.
     *
     * Fields that must be set at session-creation time and are immutable once
     * an execution has run — harness and execution_target — can only reach an
     * auto-created session through this field.
     *
     * When session_spec.agent_instance_id is set, the session runs against that
     * instance and agent_id must not also be resolved from it. When empty, the
     * normal resolution applies: agent_id's default instance, or the platform
     * default agent when agent_id is also empty.
     *
     * Mutually exclusive with session_id. session_spec.harness_state_id must be
     * empty — it is server-owned harness continuity state, created by the runner
     * after the first execution.
     *
     * &#64;internal
     * The Session resource created from this spec is the single source of truth
     * for session configuration. The handler clears this field after the session
     * is created (before persist), so the execution record never carries a
     * second copy of session config that could drift as the session evolves.
     * Extends the existing auto-create path in createSessionIfNeededStep rather
     * than adding a parallel one (stigmer/stigmer#249).
     * </pre>
     *
     * <code>.ai.stigmer.agentic.session.v1.SessionSpec session_spec = 13 [json_name = "sessionSpec"];</code>
     * @return Whether the sessionSpec field is set.
     */
    public boolean hasSessionSpec() {
      return ((bitField0_ & 0x00000004) != 0);
    }
    /**
     * <pre>
     * Spec for the session to auto-create when session_id is empty (optional).
     *
     * This is the one-call session bootstrap: a single create carries the full
     * session shape (workspace_entries, harness, execution_target, MCP servers,
     * skills) together with the first message, so embedders do not need to
     * orchestrate session.create followed by agentExecution.create. The created
     * session's ID is returned on the persisted execution's session_id.
     *
     * Fields that must be set at session-creation time and are immutable once
     * an execution has run — harness and execution_target — can only reach an
     * auto-created session through this field.
     *
     * When session_spec.agent_instance_id is set, the session runs against that
     * instance and agent_id must not also be resolved from it. When empty, the
     * normal resolution applies: agent_id's default instance, or the platform
     * default agent when agent_id is also empty.
     *
     * Mutually exclusive with session_id. session_spec.harness_state_id must be
     * empty — it is server-owned harness continuity state, created by the runner
     * after the first execution.
     *
     * &#64;internal
     * The Session resource created from this spec is the single source of truth
     * for session configuration. The handler clears this field after the session
     * is created (before persist), so the execution record never carries a
     * second copy of session config that could drift as the session evolves.
     * Extends the existing auto-create path in createSessionIfNeededStep rather
     * than adding a parallel one (stigmer/stigmer#249).
     * </pre>
     *
     * <code>.ai.stigmer.agentic.session.v1.SessionSpec session_spec = 13 [json_name = "sessionSpec"];</code>
     * @return The sessionSpec.
     */
    public ai.stigmer.agentic.session.v1.SessionSpec getSessionSpec() {
      if (sessionSpecBuilder_ == null) {
        return sessionSpec_ == null ? ai.stigmer.agentic.session.v1.SessionSpec.getDefaultInstance() : sessionSpec_;
      } else {
        return sessionSpecBuilder_.getMessage();
      }
    }
    /**
     * <pre>
     * Spec for the session to auto-create when session_id is empty (optional).
     *
     * This is the one-call session bootstrap: a single create carries the full
     * session shape (workspace_entries, harness, execution_target, MCP servers,
     * skills) together with the first message, so embedders do not need to
     * orchestrate session.create followed by agentExecution.create. The created
     * session's ID is returned on the persisted execution's session_id.
     *
     * Fields that must be set at session-creation time and are immutable once
     * an execution has run — harness and execution_target — can only reach an
     * auto-created session through this field.
     *
     * When session_spec.agent_instance_id is set, the session runs against that
     * instance and agent_id must not also be resolved from it. When empty, the
     * normal resolution applies: agent_id's default instance, or the platform
     * default agent when agent_id is also empty.
     *
     * Mutually exclusive with session_id. session_spec.harness_state_id must be
     * empty — it is server-owned harness continuity state, created by the runner
     * after the first execution.
     *
     * &#64;internal
     * The Session resource created from this spec is the single source of truth
     * for session configuration. The handler clears this field after the session
     * is created (before persist), so the execution record never carries a
     * second copy of session config that could drift as the session evolves.
     * Extends the existing auto-create path in createSessionIfNeededStep rather
     * than adding a parallel one (stigmer/stigmer#249).
     * </pre>
     *
     * <code>.ai.stigmer.agentic.session.v1.SessionSpec session_spec = 13 [json_name = "sessionSpec"];</code>
     */
    public Builder setSessionSpec(ai.stigmer.agentic.session.v1.SessionSpec value) {
      if (sessionSpecBuilder_ == null) {
        if (value == null) {
          throw new NullPointerException();
        }
        sessionSpec_ = value;
      } else {
        sessionSpecBuilder_.setMessage(value);
      }
      bitField0_ |= 0x00000004;
      onChanged();
      return this;
    }
    /**
     * <pre>
     * Spec for the session to auto-create when session_id is empty (optional).
     *
     * This is the one-call session bootstrap: a single create carries the full
     * session shape (workspace_entries, harness, execution_target, MCP servers,
     * skills) together with the first message, so embedders do not need to
     * orchestrate session.create followed by agentExecution.create. The created
     * session's ID is returned on the persisted execution's session_id.
     *
     * Fields that must be set at session-creation time and are immutable once
     * an execution has run — harness and execution_target — can only reach an
     * auto-created session through this field.
     *
     * When session_spec.agent_instance_id is set, the session runs against that
     * instance and agent_id must not also be resolved from it. When empty, the
     * normal resolution applies: agent_id's default instance, or the platform
     * default agent when agent_id is also empty.
     *
     * Mutually exclusive with session_id. session_spec.harness_state_id must be
     * empty — it is server-owned harness continuity state, created by the runner
     * after the first execution.
     *
     * &#64;internal
     * The Session resource created from this spec is the single source of truth
     * for session configuration. The handler clears this field after the session
     * is created (before persist), so the execution record never carries a
     * second copy of session config that could drift as the session evolves.
     * Extends the existing auto-create path in createSessionIfNeededStep rather
     * than adding a parallel one (stigmer/stigmer#249).
     * </pre>
     *
     * <code>.ai.stigmer.agentic.session.v1.SessionSpec session_spec = 13 [json_name = "sessionSpec"];</code>
     */
    public Builder setSessionSpec(
        ai.stigmer.agentic.session.v1.SessionSpec.Builder builderForValue) {
      if (sessionSpecBuilder_ == null) {
        sessionSpec_ = builderForValue.build();
      } else {
        sessionSpecBuilder_.setMessage(builderForValue.build());
      }
      bitField0_ |= 0x00000004;
      onChanged();
      return this;
    }
    /**
     * <pre>
     * Spec for the session to auto-create when session_id is empty (optional).
     *
     * This is the one-call session bootstrap: a single create carries the full
     * session shape (workspace_entries, harness, execution_target, MCP servers,
     * skills) together with the first message, so embedders do not need to
     * orchestrate session.create followed by agentExecution.create. The created
     * session's ID is returned on the persisted execution's session_id.
     *
     * Fields that must be set at session-creation time and are immutable once
     * an execution has run — harness and execution_target — can only reach an
     * auto-created session through this field.
     *
     * When session_spec.agent_instance_id is set, the session runs against that
     * instance and agent_id must not also be resolved from it. When empty, the
     * normal resolution applies: agent_id's default instance, or the platform
     * default agent when agent_id is also empty.
     *
     * Mutually exclusive with session_id. session_spec.harness_state_id must be
     * empty — it is server-owned harness continuity state, created by the runner
     * after the first execution.
     *
     * &#64;internal
     * The Session resource created from this spec is the single source of truth
     * for session configuration. The handler clears this field after the session
     * is created (before persist), so the execution record never carries a
     * second copy of session config that could drift as the session evolves.
     * Extends the existing auto-create path in createSessionIfNeededStep rather
     * than adding a parallel one (stigmer/stigmer#249).
     * </pre>
     *
     * <code>.ai.stigmer.agentic.session.v1.SessionSpec session_spec = 13 [json_name = "sessionSpec"];</code>
     */
    public Builder mergeSessionSpec(ai.stigmer.agentic.session.v1.SessionSpec value) {
      if (sessionSpecBuilder_ == null) {
        if (((bitField0_ & 0x00000004) != 0) &&
          sessionSpec_ != null &&
          sessionSpec_ != ai.stigmer.agentic.session.v1.SessionSpec.getDefaultInstance()) {
          getSessionSpecBuilder().mergeFrom(value);
        } else {
          sessionSpec_ = value;
        }
      } else {
        sessionSpecBuilder_.mergeFrom(value);
      }
      if (sessionSpec_ != null) {
        bitField0_ |= 0x00000004;
        onChanged();
      }
      return this;
    }
    /**
     * <pre>
     * Spec for the session to auto-create when session_id is empty (optional).
     *
     * This is the one-call session bootstrap: a single create carries the full
     * session shape (workspace_entries, harness, execution_target, MCP servers,
     * skills) together with the first message, so embedders do not need to
     * orchestrate session.create followed by agentExecution.create. The created
     * session's ID is returned on the persisted execution's session_id.
     *
     * Fields that must be set at session-creation time and are immutable once
     * an execution has run — harness and execution_target — can only reach an
     * auto-created session through this field.
     *
     * When session_spec.agent_instance_id is set, the session runs against that
     * instance and agent_id must not also be resolved from it. When empty, the
     * normal resolution applies: agent_id's default instance, or the platform
     * default agent when agent_id is also empty.
     *
     * Mutually exclusive with session_id. session_spec.harness_state_id must be
     * empty — it is server-owned harness continuity state, created by the runner
     * after the first execution.
     *
     * &#64;internal
     * The Session resource created from this spec is the single source of truth
     * for session configuration. The handler clears this field after the session
     * is created (before persist), so the execution record never carries a
     * second copy of session config that could drift as the session evolves.
     * Extends the existing auto-create path in createSessionIfNeededStep rather
     * than adding a parallel one (stigmer/stigmer#249).
     * </pre>
     *
     * <code>.ai.stigmer.agentic.session.v1.SessionSpec session_spec = 13 [json_name = "sessionSpec"];</code>
     */
    public Builder clearSessionSpec() {
      bitField0_ = (bitField0_ & ~0x00000004);
      sessionSpec_ = null;
      if (sessionSpecBuilder_ != null) {
        sessionSpecBuilder_.dispose();
        sessionSpecBuilder_ = null;
      }
      onChanged();
      return this;
    }
    /**
     * <pre>
     * Spec for the session to auto-create when session_id is empty (optional).
     *
     * This is the one-call session bootstrap: a single create carries the full
     * session shape (workspace_entries, harness, execution_target, MCP servers,
     * skills) together with the first message, so embedders do not need to
     * orchestrate session.create followed by agentExecution.create. The created
     * session's ID is returned on the persisted execution's session_id.
     *
     * Fields that must be set at session-creation time and are immutable once
     * an execution has run — harness and execution_target — can only reach an
     * auto-created session through this field.
     *
     * When session_spec.agent_instance_id is set, the session runs against that
     * instance and agent_id must not also be resolved from it. When empty, the
     * normal resolution applies: agent_id's default instance, or the platform
     * default agent when agent_id is also empty.
     *
     * Mutually exclusive with session_id. session_spec.harness_state_id must be
     * empty — it is server-owned harness continuity state, created by the runner
     * after the first execution.
     *
     * &#64;internal
     * The Session resource created from this spec is the single source of truth
     * for session configuration. The handler clears this field after the session
     * is created (before persist), so the execution record never carries a
     * second copy of session config that could drift as the session evolves.
     * Extends the existing auto-create path in createSessionIfNeededStep rather
     * than adding a parallel one (stigmer/stigmer#249).
     * </pre>
     *
     * <code>.ai.stigmer.agentic.session.v1.SessionSpec session_spec = 13 [json_name = "sessionSpec"];</code>
     */
    public ai.stigmer.agentic.session.v1.SessionSpec.Builder getSessionSpecBuilder() {
      bitField0_ |= 0x00000004;
      onChanged();
      return internalGetSessionSpecFieldBuilder().getBuilder();
    }
    /**
     * <pre>
     * Spec for the session to auto-create when session_id is empty (optional).
     *
     * This is the one-call session bootstrap: a single create carries the full
     * session shape (workspace_entries, harness, execution_target, MCP servers,
     * skills) together with the first message, so embedders do not need to
     * orchestrate session.create followed by agentExecution.create. The created
     * session's ID is returned on the persisted execution's session_id.
     *
     * Fields that must be set at session-creation time and are immutable once
     * an execution has run — harness and execution_target — can only reach an
     * auto-created session through this field.
     *
     * When session_spec.agent_instance_id is set, the session runs against that
     * instance and agent_id must not also be resolved from it. When empty, the
     * normal resolution applies: agent_id's default instance, or the platform
     * default agent when agent_id is also empty.
     *
     * Mutually exclusive with session_id. session_spec.harness_state_id must be
     * empty — it is server-owned harness continuity state, created by the runner
     * after the first execution.
     *
     * &#64;internal
     * The Session resource created from this spec is the single source of truth
     * for session configuration. The handler clears this field after the session
     * is created (before persist), so the execution record never carries a
     * second copy of session config that could drift as the session evolves.
     * Extends the existing auto-create path in createSessionIfNeededStep rather
     * than adding a parallel one (stigmer/stigmer#249).
     * </pre>
     *
     * <code>.ai.stigmer.agentic.session.v1.SessionSpec session_spec = 13 [json_name = "sessionSpec"];</code>
     */
    public ai.stigmer.agentic.session.v1.SessionSpecOrBuilder getSessionSpecOrBuilder() {
      if (sessionSpecBuilder_ != null) {
        return sessionSpecBuilder_.getMessageOrBuilder();
      } else {
        return sessionSpec_ == null ?
            ai.stigmer.agentic.session.v1.SessionSpec.getDefaultInstance() : sessionSpec_;
      }
    }
    /**
     * <pre>
     * Spec for the session to auto-create when session_id is empty (optional).
     *
     * This is the one-call session bootstrap: a single create carries the full
     * session shape (workspace_entries, harness, execution_target, MCP servers,
     * skills) together with the first message, so embedders do not need to
     * orchestrate session.create followed by agentExecution.create. The created
     * session's ID is returned on the persisted execution's session_id.
     *
     * Fields that must be set at session-creation time and are immutable once
     * an execution has run — harness and execution_target — can only reach an
     * auto-created session through this field.
     *
     * When session_spec.agent_instance_id is set, the session runs against that
     * instance and agent_id must not also be resolved from it. When empty, the
     * normal resolution applies: agent_id's default instance, or the platform
     * default agent when agent_id is also empty.
     *
     * Mutually exclusive with session_id. session_spec.harness_state_id must be
     * empty — it is server-owned harness continuity state, created by the runner
     * after the first execution.
     *
     * &#64;internal
     * The Session resource created from this spec is the single source of truth
     * for session configuration. The handler clears this field after the session
     * is created (before persist), so the execution record never carries a
     * second copy of session config that could drift as the session evolves.
     * Extends the existing auto-create path in createSessionIfNeededStep rather
     * than adding a parallel one (stigmer/stigmer#249).
     * </pre>
     *
     * <code>.ai.stigmer.agentic.session.v1.SessionSpec session_spec = 13 [json_name = "sessionSpec"];</code>
     */
    private com.google.protobuf.SingleFieldBuilder<
        ai.stigmer.agentic.session.v1.SessionSpec, ai.stigmer.agentic.session.v1.SessionSpec.Builder, ai.stigmer.agentic.session.v1.SessionSpecOrBuilder> 
        internalGetSessionSpecFieldBuilder() {
      if (sessionSpecBuilder_ == null) {
        sessionSpecBuilder_ = new com.google.protobuf.SingleFieldBuilder<
            ai.stigmer.agentic.session.v1.SessionSpec, ai.stigmer.agentic.session.v1.SessionSpec.Builder, ai.stigmer.agentic.session.v1.SessionSpecOrBuilder>(
                getSessionSpec(),
                getParentForChildren(),
                isClean());
        sessionSpec_ = null;
      }
      return sessionSpecBuilder_;
    }

    private java.lang.Object message_ = "";
    /**
     * <pre>
     * User input message that triggers this execution.
     * Each execution represents one user message and the agent's response.
     * </pre>
     *
     * <code>string message = 3 [json_name = "message", (.buf.validate.field) = { ... }</code>
     * @return The message.
     */
    public java.lang.String getMessage() {
      java.lang.Object ref = message_;
      if (!(ref instanceof java.lang.String)) {
        com.google.protobuf.ByteString bs =
            (com.google.protobuf.ByteString) ref;
        java.lang.String s = bs.toStringUtf8();
        message_ = s;
        return s;
      } else {
        return (java.lang.String) ref;
      }
    }
    /**
     * <pre>
     * User input message that triggers this execution.
     * Each execution represents one user message and the agent's response.
     * </pre>
     *
     * <code>string message = 3 [json_name = "message", (.buf.validate.field) = { ... }</code>
     * @return The bytes for message.
     */
    public com.google.protobuf.ByteString
        getMessageBytes() {
      java.lang.Object ref = message_;
      if (ref instanceof String) {
        com.google.protobuf.ByteString b = 
            com.google.protobuf.ByteString.copyFromUtf8(
                (java.lang.String) ref);
        message_ = b;
        return b;
      } else {
        return (com.google.protobuf.ByteString) ref;
      }
    }
    /**
     * <pre>
     * User input message that triggers this execution.
     * Each execution represents one user message and the agent's response.
     * </pre>
     *
     * <code>string message = 3 [json_name = "message", (.buf.validate.field) = { ... }</code>
     * @param value The message to set.
     * @return This builder for chaining.
     */
    public Builder setMessage(
        java.lang.String value) {
      if (value == null) { throw new NullPointerException(); }
      message_ = value;
      bitField0_ |= 0x00000008;
      onChanged();
      return this;
    }
    /**
     * <pre>
     * User input message that triggers this execution.
     * Each execution represents one user message and the agent's response.
     * </pre>
     *
     * <code>string message = 3 [json_name = "message", (.buf.validate.field) = { ... }</code>
     * @return This builder for chaining.
     */
    public Builder clearMessage() {
      message_ = getDefaultInstance().getMessage();
      bitField0_ = (bitField0_ & ~0x00000008);
      onChanged();
      return this;
    }
    /**
     * <pre>
     * User input message that triggers this execution.
     * Each execution represents one user message and the agent's response.
     * </pre>
     *
     * <code>string message = 3 [json_name = "message", (.buf.validate.field) = { ... }</code>
     * @param value The bytes for message to set.
     * @return This builder for chaining.
     */
    public Builder setMessageBytes(
        com.google.protobuf.ByteString value) {
      if (value == null) { throw new NullPointerException(); }
      checkByteStringIsUtf8(value);
      message_ = value;
      bitField0_ |= 0x00000008;
      onChanged();
      return this;
    }

    private ai.stigmer.agentic.agentexecution.v1.ExecutionConfig executionConfig_;
    private com.google.protobuf.SingleFieldBuilder<
        ai.stigmer.agentic.agentexecution.v1.ExecutionConfig, ai.stigmer.agentic.agentexecution.v1.ExecutionConfig.Builder, ai.stigmer.agentic.agentexecution.v1.ExecutionConfigOrBuilder> executionConfigBuilder_;
    /**
     * <pre>
     * Optional execution-time configuration overrides.
     * Example: Specify the model to use for this execution.
     * </pre>
     *
     * <code>.ai.stigmer.agentic.agentexecution.v1.ExecutionConfig execution_config = 4 [json_name = "executionConfig"];</code>
     * @return Whether the executionConfig field is set.
     */
    public boolean hasExecutionConfig() {
      return ((bitField0_ & 0x00000010) != 0);
    }
    /**
     * <pre>
     * Optional execution-time configuration overrides.
     * Example: Specify the model to use for this execution.
     * </pre>
     *
     * <code>.ai.stigmer.agentic.agentexecution.v1.ExecutionConfig execution_config = 4 [json_name = "executionConfig"];</code>
     * @return The executionConfig.
     */
    public ai.stigmer.agentic.agentexecution.v1.ExecutionConfig getExecutionConfig() {
      if (executionConfigBuilder_ == null) {
        return executionConfig_ == null ? ai.stigmer.agentic.agentexecution.v1.ExecutionConfig.getDefaultInstance() : executionConfig_;
      } else {
        return executionConfigBuilder_.getMessage();
      }
    }
    /**
     * <pre>
     * Optional execution-time configuration overrides.
     * Example: Specify the model to use for this execution.
     * </pre>
     *
     * <code>.ai.stigmer.agentic.agentexecution.v1.ExecutionConfig execution_config = 4 [json_name = "executionConfig"];</code>
     */
    public Builder setExecutionConfig(ai.stigmer.agentic.agentexecution.v1.ExecutionConfig value) {
      if (executionConfigBuilder_ == null) {
        if (value == null) {
          throw new NullPointerException();
        }
        executionConfig_ = value;
      } else {
        executionConfigBuilder_.setMessage(value);
      }
      bitField0_ |= 0x00000010;
      onChanged();
      return this;
    }
    /**
     * <pre>
     * Optional execution-time configuration overrides.
     * Example: Specify the model to use for this execution.
     * </pre>
     *
     * <code>.ai.stigmer.agentic.agentexecution.v1.ExecutionConfig execution_config = 4 [json_name = "executionConfig"];</code>
     */
    public Builder setExecutionConfig(
        ai.stigmer.agentic.agentexecution.v1.ExecutionConfig.Builder builderForValue) {
      if (executionConfigBuilder_ == null) {
        executionConfig_ = builderForValue.build();
      } else {
        executionConfigBuilder_.setMessage(builderForValue.build());
      }
      bitField0_ |= 0x00000010;
      onChanged();
      return this;
    }
    /**
     * <pre>
     * Optional execution-time configuration overrides.
     * Example: Specify the model to use for this execution.
     * </pre>
     *
     * <code>.ai.stigmer.agentic.agentexecution.v1.ExecutionConfig execution_config = 4 [json_name = "executionConfig"];</code>
     */
    public Builder mergeExecutionConfig(ai.stigmer.agentic.agentexecution.v1.ExecutionConfig value) {
      if (executionConfigBuilder_ == null) {
        if (((bitField0_ & 0x00000010) != 0) &&
          executionConfig_ != null &&
          executionConfig_ != ai.stigmer.agentic.agentexecution.v1.ExecutionConfig.getDefaultInstance()) {
          getExecutionConfigBuilder().mergeFrom(value);
        } else {
          executionConfig_ = value;
        }
      } else {
        executionConfigBuilder_.mergeFrom(value);
      }
      if (executionConfig_ != null) {
        bitField0_ |= 0x00000010;
        onChanged();
      }
      return this;
    }
    /**
     * <pre>
     * Optional execution-time configuration overrides.
     * Example: Specify the model to use for this execution.
     * </pre>
     *
     * <code>.ai.stigmer.agentic.agentexecution.v1.ExecutionConfig execution_config = 4 [json_name = "executionConfig"];</code>
     */
    public Builder clearExecutionConfig() {
      bitField0_ = (bitField0_ & ~0x00000010);
      executionConfig_ = null;
      if (executionConfigBuilder_ != null) {
        executionConfigBuilder_.dispose();
        executionConfigBuilder_ = null;
      }
      onChanged();
      return this;
    }
    /**
     * <pre>
     * Optional execution-time configuration overrides.
     * Example: Specify the model to use for this execution.
     * </pre>
     *
     * <code>.ai.stigmer.agentic.agentexecution.v1.ExecutionConfig execution_config = 4 [json_name = "executionConfig"];</code>
     */
    public ai.stigmer.agentic.agentexecution.v1.ExecutionConfig.Builder getExecutionConfigBuilder() {
      bitField0_ |= 0x00000010;
      onChanged();
      return internalGetExecutionConfigFieldBuilder().getBuilder();
    }
    /**
     * <pre>
     * Optional execution-time configuration overrides.
     * Example: Specify the model to use for this execution.
     * </pre>
     *
     * <code>.ai.stigmer.agentic.agentexecution.v1.ExecutionConfig execution_config = 4 [json_name = "executionConfig"];</code>
     */
    public ai.stigmer.agentic.agentexecution.v1.ExecutionConfigOrBuilder getExecutionConfigOrBuilder() {
      if (executionConfigBuilder_ != null) {
        return executionConfigBuilder_.getMessageOrBuilder();
      } else {
        return executionConfig_ == null ?
            ai.stigmer.agentic.agentexecution.v1.ExecutionConfig.getDefaultInstance() : executionConfig_;
      }
    }
    /**
     * <pre>
     * Optional execution-time configuration overrides.
     * Example: Specify the model to use for this execution.
     * </pre>
     *
     * <code>.ai.stigmer.agentic.agentexecution.v1.ExecutionConfig execution_config = 4 [json_name = "executionConfig"];</code>
     */
    private com.google.protobuf.SingleFieldBuilder<
        ai.stigmer.agentic.agentexecution.v1.ExecutionConfig, ai.stigmer.agentic.agentexecution.v1.ExecutionConfig.Builder, ai.stigmer.agentic.agentexecution.v1.ExecutionConfigOrBuilder> 
        internalGetExecutionConfigFieldBuilder() {
      if (executionConfigBuilder_ == null) {
        executionConfigBuilder_ = new com.google.protobuf.SingleFieldBuilder<
            ai.stigmer.agentic.agentexecution.v1.ExecutionConfig, ai.stigmer.agentic.agentexecution.v1.ExecutionConfig.Builder, ai.stigmer.agentic.agentexecution.v1.ExecutionConfigOrBuilder>(
                getExecutionConfig(),
                getParentForChildren(),
                isClean());
        executionConfig_ = null;
      }
      return executionConfigBuilder_;
    }

    private static final class RuntimeEnvConverter implements com.google.protobuf.MapFieldBuilder.Converter<java.lang.String, ai.stigmer.agentic.executioncontext.v1.ExecutionValueOrBuilder, ai.stigmer.agentic.executioncontext.v1.ExecutionValue> {
      @java.lang.Override
      public ai.stigmer.agentic.executioncontext.v1.ExecutionValue build(ai.stigmer.agentic.executioncontext.v1.ExecutionValueOrBuilder val) {
        if (val instanceof ai.stigmer.agentic.executioncontext.v1.ExecutionValue) { return (ai.stigmer.agentic.executioncontext.v1.ExecutionValue) val; }
        return ((ai.stigmer.agentic.executioncontext.v1.ExecutionValue.Builder) val).build();
      }

      @java.lang.Override
      public com.google.protobuf.MapEntry<java.lang.String, ai.stigmer.agentic.executioncontext.v1.ExecutionValue> defaultEntry() {
        return RuntimeEnvDefaultEntryHolder.defaultEntry;
      }
    };
    private static final RuntimeEnvConverter runtimeEnvConverter = new RuntimeEnvConverter();

    private com.google.protobuf.MapFieldBuilder<
        java.lang.String, ai.stigmer.agentic.executioncontext.v1.ExecutionValueOrBuilder, ai.stigmer.agentic.executioncontext.v1.ExecutionValue, ai.stigmer.agentic.executioncontext.v1.ExecutionValue.Builder> runtimeEnv_;
    private com.google.protobuf.MapFieldBuilder<java.lang.String, ai.stigmer.agentic.executioncontext.v1.ExecutionValueOrBuilder, ai.stigmer.agentic.executioncontext.v1.ExecutionValue, ai.stigmer.agentic.executioncontext.v1.ExecutionValue.Builder>
        internalGetRuntimeEnv() {
      if (runtimeEnv_ == null) {
        return new com.google.protobuf.MapFieldBuilder<>(runtimeEnvConverter);
      }
      return runtimeEnv_;
    }
    private com.google.protobuf.MapFieldBuilder<java.lang.String, ai.stigmer.agentic.executioncontext.v1.ExecutionValueOrBuilder, ai.stigmer.agentic.executioncontext.v1.ExecutionValue, ai.stigmer.agentic.executioncontext.v1.ExecutionValue.Builder>
        internalGetMutableRuntimeEnv() {
      if (runtimeEnv_ == null) {
        runtimeEnv_ = new com.google.protobuf.MapFieldBuilder<>(runtimeEnvConverter);
      }
      bitField0_ |= 0x00000020;
      onChanged();
      return runtimeEnv_;
    }
    public int getRuntimeEnvCount() {
      return internalGetRuntimeEnv().ensureBuilderMap().size();
    }
    /**
     * <pre>
     * Runtime environment variables and secrets (execution-scoped).
     * These values are only available for this specific execution and take the
     * highest merge priority, overriding values from Environments bound via
     * environment_refs. A key must be declared in Agent.spec.env to survive the
     * merge: the agent env map is a declaration whitelist (name + is_secret +
     * optional), never a value source — undeclared keys are dropped.
     * Use case: B2B integrations where secrets are injected at runtime per call.
     * These values are consumed into the ExecutionContext (deleted when the
     * execution completes) and cleared from the persisted execution.
     *
     * &#64;internal
     * Merge priority (lowest to highest): resolved Environment values — from the
     * creating schedule's or agent_call task's environment_refs (when present),
     * then the instance's environment_refs; a later ref wins — then runtime_env
     * (this field). The merged map is filtered to the keys declared in
     * Agent.spec.env (no filtering when the agent declares none); a missing
     * required key only logs a warning — the run is not failed. The merge is
     * owned by backend/libs/go/envmerge and the agentexecution controller's
     * executionContextBuilder, and asserted by
     * test/conformance/src/suites-execution/envmerge.conformance.test.ts.
     * </pre>
     *
     * <code>map&lt;string, .ai.stigmer.agentic.executioncontext.v1.ExecutionValue&gt; runtime_env = 5 [json_name = "runtimeEnv"];</code>
     */
    @java.lang.Override
    public boolean containsRuntimeEnv(
        java.lang.String key) {
      if (key == null) { throw new NullPointerException("map key"); }
      return internalGetRuntimeEnv().ensureBuilderMap().containsKey(key);
    }
    /**
     * Use {@link #getRuntimeEnvMap()} instead.
     */
    @java.lang.Override
    @java.lang.Deprecated
    public java.util.Map<java.lang.String, ai.stigmer.agentic.executioncontext.v1.ExecutionValue> getRuntimeEnv() {
      return getRuntimeEnvMap();
    }
    /**
     * <pre>
     * Runtime environment variables and secrets (execution-scoped).
     * These values are only available for this specific execution and take the
     * highest merge priority, overriding values from Environments bound via
     * environment_refs. A key must be declared in Agent.spec.env to survive the
     * merge: the agent env map is a declaration whitelist (name + is_secret +
     * optional), never a value source — undeclared keys are dropped.
     * Use case: B2B integrations where secrets are injected at runtime per call.
     * These values are consumed into the ExecutionContext (deleted when the
     * execution completes) and cleared from the persisted execution.
     *
     * &#64;internal
     * Merge priority (lowest to highest): resolved Environment values — from the
     * creating schedule's or agent_call task's environment_refs (when present),
     * then the instance's environment_refs; a later ref wins — then runtime_env
     * (this field). The merged map is filtered to the keys declared in
     * Agent.spec.env (no filtering when the agent declares none); a missing
     * required key only logs a warning — the run is not failed. The merge is
     * owned by backend/libs/go/envmerge and the agentexecution controller's
     * executionContextBuilder, and asserted by
     * test/conformance/src/suites-execution/envmerge.conformance.test.ts.
     * </pre>
     *
     * <code>map&lt;string, .ai.stigmer.agentic.executioncontext.v1.ExecutionValue&gt; runtime_env = 5 [json_name = "runtimeEnv"];</code>
     */
    @java.lang.Override
    public java.util.Map<java.lang.String, ai.stigmer.agentic.executioncontext.v1.ExecutionValue> getRuntimeEnvMap() {
      return internalGetRuntimeEnv().getImmutableMap();
    }
    /**
     * <pre>
     * Runtime environment variables and secrets (execution-scoped).
     * These values are only available for this specific execution and take the
     * highest merge priority, overriding values from Environments bound via
     * environment_refs. A key must be declared in Agent.spec.env to survive the
     * merge: the agent env map is a declaration whitelist (name + is_secret +
     * optional), never a value source — undeclared keys are dropped.
     * Use case: B2B integrations where secrets are injected at runtime per call.
     * These values are consumed into the ExecutionContext (deleted when the
     * execution completes) and cleared from the persisted execution.
     *
     * &#64;internal
     * Merge priority (lowest to highest): resolved Environment values — from the
     * creating schedule's or agent_call task's environment_refs (when present),
     * then the instance's environment_refs; a later ref wins — then runtime_env
     * (this field). The merged map is filtered to the keys declared in
     * Agent.spec.env (no filtering when the agent declares none); a missing
     * required key only logs a warning — the run is not failed. The merge is
     * owned by backend/libs/go/envmerge and the agentexecution controller's
     * executionContextBuilder, and asserted by
     * test/conformance/src/suites-execution/envmerge.conformance.test.ts.
     * </pre>
     *
     * <code>map&lt;string, .ai.stigmer.agentic.executioncontext.v1.ExecutionValue&gt; runtime_env = 5 [json_name = "runtimeEnv"];</code>
     */
    @java.lang.Override
    public /* nullable */
ai.stigmer.agentic.executioncontext.v1.ExecutionValue getRuntimeEnvOrDefault(
        java.lang.String key,
        /* nullable */
ai.stigmer.agentic.executioncontext.v1.ExecutionValue defaultValue) {
      if (key == null) { throw new NullPointerException("map key"); }
      java.util.Map<java.lang.String, ai.stigmer.agentic.executioncontext.v1.ExecutionValueOrBuilder> map = internalGetMutableRuntimeEnv().ensureBuilderMap();
      return map.containsKey(key) ? runtimeEnvConverter.build(map.get(key)) : defaultValue;
    }
    /**
     * <pre>
     * Runtime environment variables and secrets (execution-scoped).
     * These values are only available for this specific execution and take the
     * highest merge priority, overriding values from Environments bound via
     * environment_refs. A key must be declared in Agent.spec.env to survive the
     * merge: the agent env map is a declaration whitelist (name + is_secret +
     * optional), never a value source — undeclared keys are dropped.
     * Use case: B2B integrations where secrets are injected at runtime per call.
     * These values are consumed into the ExecutionContext (deleted when the
     * execution completes) and cleared from the persisted execution.
     *
     * &#64;internal
     * Merge priority (lowest to highest): resolved Environment values — from the
     * creating schedule's or agent_call task's environment_refs (when present),
     * then the instance's environment_refs; a later ref wins — then runtime_env
     * (this field). The merged map is filtered to the keys declared in
     * Agent.spec.env (no filtering when the agent declares none); a missing
     * required key only logs a warning — the run is not failed. The merge is
     * owned by backend/libs/go/envmerge and the agentexecution controller's
     * executionContextBuilder, and asserted by
     * test/conformance/src/suites-execution/envmerge.conformance.test.ts.
     * </pre>
     *
     * <code>map&lt;string, .ai.stigmer.agentic.executioncontext.v1.ExecutionValue&gt; runtime_env = 5 [json_name = "runtimeEnv"];</code>
     */
    @java.lang.Override
    public ai.stigmer.agentic.executioncontext.v1.ExecutionValue getRuntimeEnvOrThrow(
        java.lang.String key) {
      if (key == null) { throw new NullPointerException("map key"); }
      java.util.Map<java.lang.String, ai.stigmer.agentic.executioncontext.v1.ExecutionValueOrBuilder> map = internalGetMutableRuntimeEnv().ensureBuilderMap();
      if (!map.containsKey(key)) {
        throw new java.lang.IllegalArgumentException();
      }
      return runtimeEnvConverter.build(map.get(key));
    }
    public Builder clearRuntimeEnv() {
      bitField0_ = (bitField0_ & ~0x00000020);
      internalGetMutableRuntimeEnv().clear();
      return this;
    }
    /**
     * <pre>
     * Runtime environment variables and secrets (execution-scoped).
     * These values are only available for this specific execution and take the
     * highest merge priority, overriding values from Environments bound via
     * environment_refs. A key must be declared in Agent.spec.env to survive the
     * merge: the agent env map is a declaration whitelist (name + is_secret +
     * optional), never a value source — undeclared keys are dropped.
     * Use case: B2B integrations where secrets are injected at runtime per call.
     * These values are consumed into the ExecutionContext (deleted when the
     * execution completes) and cleared from the persisted execution.
     *
     * &#64;internal
     * Merge priority (lowest to highest): resolved Environment values — from the
     * creating schedule's or agent_call task's environment_refs (when present),
     * then the instance's environment_refs; a later ref wins — then runtime_env
     * (this field). The merged map is filtered to the keys declared in
     * Agent.spec.env (no filtering when the agent declares none); a missing
     * required key only logs a warning — the run is not failed. The merge is
     * owned by backend/libs/go/envmerge and the agentexecution controller's
     * executionContextBuilder, and asserted by
     * test/conformance/src/suites-execution/envmerge.conformance.test.ts.
     * </pre>
     *
     * <code>map&lt;string, .ai.stigmer.agentic.executioncontext.v1.ExecutionValue&gt; runtime_env = 5 [json_name = "runtimeEnv"];</code>
     */
    public Builder removeRuntimeEnv(
        java.lang.String key) {
      if (key == null) { throw new NullPointerException("map key"); }
      internalGetMutableRuntimeEnv().ensureBuilderMap()
          .remove(key);
      return this;
    }
    /**
     * Use alternate mutation accessors instead.
     */
    @java.lang.Deprecated
    public java.util.Map<java.lang.String, ai.stigmer.agentic.executioncontext.v1.ExecutionValue>
        getMutableRuntimeEnv() {
      bitField0_ |= 0x00000020;
      return internalGetMutableRuntimeEnv().ensureMessageMap();
    }
    /**
     * <pre>
     * Runtime environment variables and secrets (execution-scoped).
     * These values are only available for this specific execution and take the
     * highest merge priority, overriding values from Environments bound via
     * environment_refs. A key must be declared in Agent.spec.env to survive the
     * merge: the agent env map is a declaration whitelist (name + is_secret +
     * optional), never a value source — undeclared keys are dropped.
     * Use case: B2B integrations where secrets are injected at runtime per call.
     * These values are consumed into the ExecutionContext (deleted when the
     * execution completes) and cleared from the persisted execution.
     *
     * &#64;internal
     * Merge priority (lowest to highest): resolved Environment values — from the
     * creating schedule's or agent_call task's environment_refs (when present),
     * then the instance's environment_refs; a later ref wins — then runtime_env
     * (this field). The merged map is filtered to the keys declared in
     * Agent.spec.env (no filtering when the agent declares none); a missing
     * required key only logs a warning — the run is not failed. The merge is
     * owned by backend/libs/go/envmerge and the agentexecution controller's
     * executionContextBuilder, and asserted by
     * test/conformance/src/suites-execution/envmerge.conformance.test.ts.
     * </pre>
     *
     * <code>map&lt;string, .ai.stigmer.agentic.executioncontext.v1.ExecutionValue&gt; runtime_env = 5 [json_name = "runtimeEnv"];</code>
     */
    public Builder putRuntimeEnv(
        java.lang.String key,
        ai.stigmer.agentic.executioncontext.v1.ExecutionValue value) {
      if (key == null) { throw new NullPointerException("map key"); }
      if (value == null) { throw new NullPointerException("map value"); }
      internalGetMutableRuntimeEnv().ensureBuilderMap()
          .put(key, value);
      bitField0_ |= 0x00000020;
      return this;
    }
    /**
     * <pre>
     * Runtime environment variables and secrets (execution-scoped).
     * These values are only available for this specific execution and take the
     * highest merge priority, overriding values from Environments bound via
     * environment_refs. A key must be declared in Agent.spec.env to survive the
     * merge: the agent env map is a declaration whitelist (name + is_secret +
     * optional), never a value source — undeclared keys are dropped.
     * Use case: B2B integrations where secrets are injected at runtime per call.
     * These values are consumed into the ExecutionContext (deleted when the
     * execution completes) and cleared from the persisted execution.
     *
     * &#64;internal
     * Merge priority (lowest to highest): resolved Environment values — from the
     * creating schedule's or agent_call task's environment_refs (when present),
     * then the instance's environment_refs; a later ref wins — then runtime_env
     * (this field). The merged map is filtered to the keys declared in
     * Agent.spec.env (no filtering when the agent declares none); a missing
     * required key only logs a warning — the run is not failed. The merge is
     * owned by backend/libs/go/envmerge and the agentexecution controller's
     * executionContextBuilder, and asserted by
     * test/conformance/src/suites-execution/envmerge.conformance.test.ts.
     * </pre>
     *
     * <code>map&lt;string, .ai.stigmer.agentic.executioncontext.v1.ExecutionValue&gt; runtime_env = 5 [json_name = "runtimeEnv"];</code>
     */
    public Builder putAllRuntimeEnv(
        java.util.Map<java.lang.String, ai.stigmer.agentic.executioncontext.v1.ExecutionValue> values) {
      for (java.util.Map.Entry<java.lang.String, ai.stigmer.agentic.executioncontext.v1.ExecutionValue> e : values.entrySet()) {
        if (e.getKey() == null || e.getValue() == null) {
          throw new NullPointerException();
        }
      }
      internalGetMutableRuntimeEnv().ensureBuilderMap()
          .putAll(values);
      bitField0_ |= 0x00000020;
      return this;
    }
    /**
     * <pre>
     * Runtime environment variables and secrets (execution-scoped).
     * These values are only available for this specific execution and take the
     * highest merge priority, overriding values from Environments bound via
     * environment_refs. A key must be declared in Agent.spec.env to survive the
     * merge: the agent env map is a declaration whitelist (name + is_secret +
     * optional), never a value source — undeclared keys are dropped.
     * Use case: B2B integrations where secrets are injected at runtime per call.
     * These values are consumed into the ExecutionContext (deleted when the
     * execution completes) and cleared from the persisted execution.
     *
     * &#64;internal
     * Merge priority (lowest to highest): resolved Environment values — from the
     * creating schedule's or agent_call task's environment_refs (when present),
     * then the instance's environment_refs; a later ref wins — then runtime_env
     * (this field). The merged map is filtered to the keys declared in
     * Agent.spec.env (no filtering when the agent declares none); a missing
     * required key only logs a warning — the run is not failed. The merge is
     * owned by backend/libs/go/envmerge and the agentexecution controller's
     * executionContextBuilder, and asserted by
     * test/conformance/src/suites-execution/envmerge.conformance.test.ts.
     * </pre>
     *
     * <code>map&lt;string, .ai.stigmer.agentic.executioncontext.v1.ExecutionValue&gt; runtime_env = 5 [json_name = "runtimeEnv"];</code>
     */
    public ai.stigmer.agentic.executioncontext.v1.ExecutionValue.Builder putRuntimeEnvBuilderIfAbsent(
        java.lang.String key) {
      java.util.Map<java.lang.String, ai.stigmer.agentic.executioncontext.v1.ExecutionValueOrBuilder> builderMap = internalGetMutableRuntimeEnv().ensureBuilderMap();
      ai.stigmer.agentic.executioncontext.v1.ExecutionValueOrBuilder entry = builderMap.get(key);
      if (entry == null) {
        entry = ai.stigmer.agentic.executioncontext.v1.ExecutionValue.newBuilder();
        builderMap.put(key, entry);
      }
      if (entry instanceof ai.stigmer.agentic.executioncontext.v1.ExecutionValue) {
        entry = ((ai.stigmer.agentic.executioncontext.v1.ExecutionValue) entry).toBuilder();
        builderMap.put(key, entry);
      }
      return (ai.stigmer.agentic.executioncontext.v1.ExecutionValue.Builder) entry;
    }

    private com.google.protobuf.ByteString callbackToken_ = com.google.protobuf.ByteString.EMPTY;
    /**
     * <pre>
     * Callback token for async activity completion (optional).
     *
     * When a workflow invokes an agent, this token enables the workflow to
     * wait for the agent to finish without blocking. When empty, the
     * execution runs independently (CLI, API calls, non-workflow triggers).
     *
     * &#64;internal
     *
     * Enables async completion pattern where the caller (typically a
     * workflow activity) waits for actual agent completion without blocking
     * worker threads.
     *
     * Flow:
     * 1. Caller (Go Temporal activity) extracts its task token
     * 2. Passes token in this field when creating AgentExecution
     * 3. Returns activity.ErrResultPending (activity paused, thread released)
     * 4. Agent workflow completes (minutes/hours later)
     * 5. Agent workflow calls ActivityCompletionClient.complete(token, result)
     * 6. Temporal resumes the paused activity with the result
     *
     * Token Format:
     * - Opaque binary blob from Temporal SDK (typically 100-200 bytes)
     * - Contains: namespace, workflow ID, run ID, activity ID, attempt
     * - DO NOT parse or modify - treat as opaque handle
     *
     * Security:
     * - Token grants ability to complete the activity (bearer token)
     * - Should only be passed through trusted internal services
     * - Logged as Base64-encoded string (truncated for security)
     *
     * Timeout:
     * - Caller should set StartToCloseTimeout (e.g., 24 hours)
     * - If token callback never arrives, activity times out
     *
     * Example Usage (Go):
     * ```go
     * func CallAgentActivity(ctx context.Context, config *AgentCallTaskConfig) {
     * taskToken := activity.GetInfo(ctx).TaskToken
     * execution := &amp;AgentExecution{
     * Spec: &amp;AgentExecutionSpec{
     * AgentId: config.Agent,
     * Message: config.Message,
     * CallbackToken: taskToken,
     * },
     * }
     * client.Create(ctx, execution)
     * return nil, activity.ErrResultPending
     * }
     * ```
     *
     * References:
     * - ADR: docs/adr/20260122-async-agent-execution-temporal-token-handshake.md
     * - Temporal Docs: https://docs.temporal.io/activities#asynchronous-activity-completion
     *
     * &#64;since 2026-01-22 (Phase 2: Async Agent Execution Integration)
     * </pre>
     *
     * <code>bytes callback_token = 6 [json_name = "callbackToken"];</code>
     * @return The callbackToken.
     */
    @java.lang.Override
    public com.google.protobuf.ByteString getCallbackToken() {
      return callbackToken_;
    }
    /**
     * <pre>
     * Callback token for async activity completion (optional).
     *
     * When a workflow invokes an agent, this token enables the workflow to
     * wait for the agent to finish without blocking. When empty, the
     * execution runs independently (CLI, API calls, non-workflow triggers).
     *
     * &#64;internal
     *
     * Enables async completion pattern where the caller (typically a
     * workflow activity) waits for actual agent completion without blocking
     * worker threads.
     *
     * Flow:
     * 1. Caller (Go Temporal activity) extracts its task token
     * 2. Passes token in this field when creating AgentExecution
     * 3. Returns activity.ErrResultPending (activity paused, thread released)
     * 4. Agent workflow completes (minutes/hours later)
     * 5. Agent workflow calls ActivityCompletionClient.complete(token, result)
     * 6. Temporal resumes the paused activity with the result
     *
     * Token Format:
     * - Opaque binary blob from Temporal SDK (typically 100-200 bytes)
     * - Contains: namespace, workflow ID, run ID, activity ID, attempt
     * - DO NOT parse or modify - treat as opaque handle
     *
     * Security:
     * - Token grants ability to complete the activity (bearer token)
     * - Should only be passed through trusted internal services
     * - Logged as Base64-encoded string (truncated for security)
     *
     * Timeout:
     * - Caller should set StartToCloseTimeout (e.g., 24 hours)
     * - If token callback never arrives, activity times out
     *
     * Example Usage (Go):
     * ```go
     * func CallAgentActivity(ctx context.Context, config *AgentCallTaskConfig) {
     * taskToken := activity.GetInfo(ctx).TaskToken
     * execution := &amp;AgentExecution{
     * Spec: &amp;AgentExecutionSpec{
     * AgentId: config.Agent,
     * Message: config.Message,
     * CallbackToken: taskToken,
     * },
     * }
     * client.Create(ctx, execution)
     * return nil, activity.ErrResultPending
     * }
     * ```
     *
     * References:
     * - ADR: docs/adr/20260122-async-agent-execution-temporal-token-handshake.md
     * - Temporal Docs: https://docs.temporal.io/activities#asynchronous-activity-completion
     *
     * &#64;since 2026-01-22 (Phase 2: Async Agent Execution Integration)
     * </pre>
     *
     * <code>bytes callback_token = 6 [json_name = "callbackToken"];</code>
     * @param value The callbackToken to set.
     * @return This builder for chaining.
     */
    public Builder setCallbackToken(com.google.protobuf.ByteString value) {
      if (value == null) { throw new NullPointerException(); }
      callbackToken_ = value;
      bitField0_ |= 0x00000040;
      onChanged();
      return this;
    }
    /**
     * <pre>
     * Callback token for async activity completion (optional).
     *
     * When a workflow invokes an agent, this token enables the workflow to
     * wait for the agent to finish without blocking. When empty, the
     * execution runs independently (CLI, API calls, non-workflow triggers).
     *
     * &#64;internal
     *
     * Enables async completion pattern where the caller (typically a
     * workflow activity) waits for actual agent completion without blocking
     * worker threads.
     *
     * Flow:
     * 1. Caller (Go Temporal activity) extracts its task token
     * 2. Passes token in this field when creating AgentExecution
     * 3. Returns activity.ErrResultPending (activity paused, thread released)
     * 4. Agent workflow completes (minutes/hours later)
     * 5. Agent workflow calls ActivityCompletionClient.complete(token, result)
     * 6. Temporal resumes the paused activity with the result
     *
     * Token Format:
     * - Opaque binary blob from Temporal SDK (typically 100-200 bytes)
     * - Contains: namespace, workflow ID, run ID, activity ID, attempt
     * - DO NOT parse or modify - treat as opaque handle
     *
     * Security:
     * - Token grants ability to complete the activity (bearer token)
     * - Should only be passed through trusted internal services
     * - Logged as Base64-encoded string (truncated for security)
     *
     * Timeout:
     * - Caller should set StartToCloseTimeout (e.g., 24 hours)
     * - If token callback never arrives, activity times out
     *
     * Example Usage (Go):
     * ```go
     * func CallAgentActivity(ctx context.Context, config *AgentCallTaskConfig) {
     * taskToken := activity.GetInfo(ctx).TaskToken
     * execution := &amp;AgentExecution{
     * Spec: &amp;AgentExecutionSpec{
     * AgentId: config.Agent,
     * Message: config.Message,
     * CallbackToken: taskToken,
     * },
     * }
     * client.Create(ctx, execution)
     * return nil, activity.ErrResultPending
     * }
     * ```
     *
     * References:
     * - ADR: docs/adr/20260122-async-agent-execution-temporal-token-handshake.md
     * - Temporal Docs: https://docs.temporal.io/activities#asynchronous-activity-completion
     *
     * &#64;since 2026-01-22 (Phase 2: Async Agent Execution Integration)
     * </pre>
     *
     * <code>bytes callback_token = 6 [json_name = "callbackToken"];</code>
     * @return This builder for chaining.
     */
    public Builder clearCallbackToken() {
      bitField0_ = (bitField0_ & ~0x00000040);
      callbackToken_ = getDefaultInstance().getCallbackToken();
      onChanged();
      return this;
    }

    private boolean autoApproveAll_ ;
    /**
     * <pre>
     * Auto-approve all tool executions for this execution.
     *
     * When true, tools that would normally require approval are automatically
     * approved without user intervention. This is the highest-priority override
     * in the approval policy chain:
     * McpServerStatus.tool_approvals → McpServerSpec.pinned_tool_approvals → Agent.tool_approval_overrides → auto_approve_all
     *
     * Use cases:
     * - Automated CI/CD pipelines where human approval isn't practical
     * - Trusted batch operations with pre-validated inputs
     * - Development/testing environments
     * - Scheduled jobs where approval would block automation
     *
     * Security consideration: This flag bypasses all approval checks.
     * Ensure appropriate access controls on who can set this flag.
     * Consider auditing executions where this flag is used.
     *
     * Default: false (approvals required as configured in policies)
     * </pre>
     *
     * <code>bool auto_approve_all = 7 [json_name = "autoApproveAll"];</code>
     * @return The autoApproveAll.
     */
    @java.lang.Override
    public boolean getAutoApproveAll() {
      return autoApproveAll_;
    }
    /**
     * <pre>
     * Auto-approve all tool executions for this execution.
     *
     * When true, tools that would normally require approval are automatically
     * approved without user intervention. This is the highest-priority override
     * in the approval policy chain:
     * McpServerStatus.tool_approvals → McpServerSpec.pinned_tool_approvals → Agent.tool_approval_overrides → auto_approve_all
     *
     * Use cases:
     * - Automated CI/CD pipelines where human approval isn't practical
     * - Trusted batch operations with pre-validated inputs
     * - Development/testing environments
     * - Scheduled jobs where approval would block automation
     *
     * Security consideration: This flag bypasses all approval checks.
     * Ensure appropriate access controls on who can set this flag.
     * Consider auditing executions where this flag is used.
     *
     * Default: false (approvals required as configured in policies)
     * </pre>
     *
     * <code>bool auto_approve_all = 7 [json_name = "autoApproveAll"];</code>
     * @param value The autoApproveAll to set.
     * @return This builder for chaining.
     */
    public Builder setAutoApproveAll(boolean value) {

      autoApproveAll_ = value;
      bitField0_ |= 0x00000080;
      onChanged();
      return this;
    }
    /**
     * <pre>
     * Auto-approve all tool executions for this execution.
     *
     * When true, tools that would normally require approval are automatically
     * approved without user intervention. This is the highest-priority override
     * in the approval policy chain:
     * McpServerStatus.tool_approvals → McpServerSpec.pinned_tool_approvals → Agent.tool_approval_overrides → auto_approve_all
     *
     * Use cases:
     * - Automated CI/CD pipelines where human approval isn't practical
     * - Trusted batch operations with pre-validated inputs
     * - Development/testing environments
     * - Scheduled jobs where approval would block automation
     *
     * Security consideration: This flag bypasses all approval checks.
     * Ensure appropriate access controls on who can set this flag.
     * Consider auditing executions where this flag is used.
     *
     * Default: false (approvals required as configured in policies)
     * </pre>
     *
     * <code>bool auto_approve_all = 7 [json_name = "autoApproveAll"];</code>
     * @return This builder for chaining.
     */
    public Builder clearAutoApproveAll() {
      bitField0_ = (bitField0_ & ~0x00000080);
      autoApproveAll_ = false;
      onChanged();
      return this;
    }

    private java.lang.Object parentWorkflowId_ = "";
    /**
     * <pre>
     * Parent workflow ID when this execution was triggered by a workflow (optional).
     *
     * When set, the platform notifies the parent workflow about approval
     * requests instead of requiring polling. When empty, approvals are
     * submitted directly via the SubmitApproval RPC.
     *
     * &#64;internal
     *
     * Signal Pattern:
     * 1. Go workflow passes its workflow ID when creating AgentExecution
     * 2. Go workflow starts signal listener for "child_approval_required"
     * 3. When agent enters WAITING_FOR_APPROVAL, Java sends signal to parent
     * 4. Go workflow receives signal, updates task status to WAITING_APPROVAL
     *
     * Format:
     * Temporal workflow ID, typically: "stigmer/workflow-execution/invoke/{execution-id}"
     *
     * Backward Compatibility:
     * This field is optional. Agents invoked without a parent workflow ID will
     * continue to work normally - approval is submitted directly via the
     * AgentExecution.SubmitApproval RPC.
     *
     * &#64;since Phase 5.1 (Events-Based Approval Notification)
     * </pre>
     *
     * <code>string parent_workflow_id = 8 [json_name = "parentWorkflowId"];</code>
     * @return The parentWorkflowId.
     */
    public java.lang.String getParentWorkflowId() {
      java.lang.Object ref = parentWorkflowId_;
      if (!(ref instanceof java.lang.String)) {
        com.google.protobuf.ByteString bs =
            (com.google.protobuf.ByteString) ref;
        java.lang.String s = bs.toStringUtf8();
        parentWorkflowId_ = s;
        return s;
      } else {
        return (java.lang.String) ref;
      }
    }
    /**
     * <pre>
     * Parent workflow ID when this execution was triggered by a workflow (optional).
     *
     * When set, the platform notifies the parent workflow about approval
     * requests instead of requiring polling. When empty, approvals are
     * submitted directly via the SubmitApproval RPC.
     *
     * &#64;internal
     *
     * Signal Pattern:
     * 1. Go workflow passes its workflow ID when creating AgentExecution
     * 2. Go workflow starts signal listener for "child_approval_required"
     * 3. When agent enters WAITING_FOR_APPROVAL, Java sends signal to parent
     * 4. Go workflow receives signal, updates task status to WAITING_APPROVAL
     *
     * Format:
     * Temporal workflow ID, typically: "stigmer/workflow-execution/invoke/{execution-id}"
     *
     * Backward Compatibility:
     * This field is optional. Agents invoked without a parent workflow ID will
     * continue to work normally - approval is submitted directly via the
     * AgentExecution.SubmitApproval RPC.
     *
     * &#64;since Phase 5.1 (Events-Based Approval Notification)
     * </pre>
     *
     * <code>string parent_workflow_id = 8 [json_name = "parentWorkflowId"];</code>
     * @return The bytes for parentWorkflowId.
     */
    public com.google.protobuf.ByteString
        getParentWorkflowIdBytes() {
      java.lang.Object ref = parentWorkflowId_;
      if (ref instanceof String) {
        com.google.protobuf.ByteString b = 
            com.google.protobuf.ByteString.copyFromUtf8(
                (java.lang.String) ref);
        parentWorkflowId_ = b;
        return b;
      } else {
        return (com.google.protobuf.ByteString) ref;
      }
    }
    /**
     * <pre>
     * Parent workflow ID when this execution was triggered by a workflow (optional).
     *
     * When set, the platform notifies the parent workflow about approval
     * requests instead of requiring polling. When empty, approvals are
     * submitted directly via the SubmitApproval RPC.
     *
     * &#64;internal
     *
     * Signal Pattern:
     * 1. Go workflow passes its workflow ID when creating AgentExecution
     * 2. Go workflow starts signal listener for "child_approval_required"
     * 3. When agent enters WAITING_FOR_APPROVAL, Java sends signal to parent
     * 4. Go workflow receives signal, updates task status to WAITING_APPROVAL
     *
     * Format:
     * Temporal workflow ID, typically: "stigmer/workflow-execution/invoke/{execution-id}"
     *
     * Backward Compatibility:
     * This field is optional. Agents invoked without a parent workflow ID will
     * continue to work normally - approval is submitted directly via the
     * AgentExecution.SubmitApproval RPC.
     *
     * &#64;since Phase 5.1 (Events-Based Approval Notification)
     * </pre>
     *
     * <code>string parent_workflow_id = 8 [json_name = "parentWorkflowId"];</code>
     * @param value The parentWorkflowId to set.
     * @return This builder for chaining.
     */
    public Builder setParentWorkflowId(
        java.lang.String value) {
      if (value == null) { throw new NullPointerException(); }
      parentWorkflowId_ = value;
      bitField0_ |= 0x00000100;
      onChanged();
      return this;
    }
    /**
     * <pre>
     * Parent workflow ID when this execution was triggered by a workflow (optional).
     *
     * When set, the platform notifies the parent workflow about approval
     * requests instead of requiring polling. When empty, approvals are
     * submitted directly via the SubmitApproval RPC.
     *
     * &#64;internal
     *
     * Signal Pattern:
     * 1. Go workflow passes its workflow ID when creating AgentExecution
     * 2. Go workflow starts signal listener for "child_approval_required"
     * 3. When agent enters WAITING_FOR_APPROVAL, Java sends signal to parent
     * 4. Go workflow receives signal, updates task status to WAITING_APPROVAL
     *
     * Format:
     * Temporal workflow ID, typically: "stigmer/workflow-execution/invoke/{execution-id}"
     *
     * Backward Compatibility:
     * This field is optional. Agents invoked without a parent workflow ID will
     * continue to work normally - approval is submitted directly via the
     * AgentExecution.SubmitApproval RPC.
     *
     * &#64;since Phase 5.1 (Events-Based Approval Notification)
     * </pre>
     *
     * <code>string parent_workflow_id = 8 [json_name = "parentWorkflowId"];</code>
     * @return This builder for chaining.
     */
    public Builder clearParentWorkflowId() {
      parentWorkflowId_ = getDefaultInstance().getParentWorkflowId();
      bitField0_ = (bitField0_ & ~0x00000100);
      onChanged();
      return this;
    }
    /**
     * <pre>
     * Parent workflow ID when this execution was triggered by a workflow (optional).
     *
     * When set, the platform notifies the parent workflow about approval
     * requests instead of requiring polling. When empty, approvals are
     * submitted directly via the SubmitApproval RPC.
     *
     * &#64;internal
     *
     * Signal Pattern:
     * 1. Go workflow passes its workflow ID when creating AgentExecution
     * 2. Go workflow starts signal listener for "child_approval_required"
     * 3. When agent enters WAITING_FOR_APPROVAL, Java sends signal to parent
     * 4. Go workflow receives signal, updates task status to WAITING_APPROVAL
     *
     * Format:
     * Temporal workflow ID, typically: "stigmer/workflow-execution/invoke/{execution-id}"
     *
     * Backward Compatibility:
     * This field is optional. Agents invoked without a parent workflow ID will
     * continue to work normally - approval is submitted directly via the
     * AgentExecution.SubmitApproval RPC.
     *
     * &#64;since Phase 5.1 (Events-Based Approval Notification)
     * </pre>
     *
     * <code>string parent_workflow_id = 8 [json_name = "parentWorkflowId"];</code>
     * @param value The bytes for parentWorkflowId to set.
     * @return This builder for chaining.
     */
    public Builder setParentWorkflowIdBytes(
        com.google.protobuf.ByteString value) {
      if (value == null) { throw new NullPointerException(); }
      checkByteStringIsUtf8(value);
      parentWorkflowId_ = value;
      bitField0_ |= 0x00000100;
      onChanged();
      return this;
    }

    private java.util.List<ai.stigmer.agentic.agentexecution.v1.Attachment> attachments_ =
      java.util.Collections.emptyList();
    private void ensureAttachmentsIsMutable() {
      if (!((bitField0_ & 0x00000200) != 0)) {
        attachments_ = new java.util.ArrayList<ai.stigmer.agentic.agentexecution.v1.Attachment>(attachments_);
        bitField0_ |= 0x00000200;
       }
    }

    private com.google.protobuf.RepeatedFieldBuilder<
        ai.stigmer.agentic.agentexecution.v1.Attachment, ai.stigmer.agentic.agentexecution.v1.Attachment.Builder, ai.stigmer.agentic.agentexecution.v1.AttachmentOrBuilder> attachmentsBuilder_;

    /**
     * <pre>
     * Files attached to this execution.
     *
     * Attachments are injected into the sandbox before execution begins.
     * The agent can read these files from the specified mount paths.
     *
     * Use cases:
     * - Provide input data files to process
     * - Pass configuration files
     * - Supply reference documents for analysis
     *
     * Example CLI usage:
     * stigmer run agent my-agent --attach ./config.yaml -m "Process this config"
     *
     * &#64;since Artifact Lifecycle (Attachments &amp; Outputs)
     * </pre>
     *
     * <code>repeated .ai.stigmer.agentic.agentexecution.v1.Attachment attachments = 9 [json_name = "attachments"];</code>
     */
    public java.util.List<ai.stigmer.agentic.agentexecution.v1.Attachment> getAttachmentsList() {
      if (attachmentsBuilder_ == null) {
        return java.util.Collections.unmodifiableList(attachments_);
      } else {
        return attachmentsBuilder_.getMessageList();
      }
    }
    /**
     * <pre>
     * Files attached to this execution.
     *
     * Attachments are injected into the sandbox before execution begins.
     * The agent can read these files from the specified mount paths.
     *
     * Use cases:
     * - Provide input data files to process
     * - Pass configuration files
     * - Supply reference documents for analysis
     *
     * Example CLI usage:
     * stigmer run agent my-agent --attach ./config.yaml -m "Process this config"
     *
     * &#64;since Artifact Lifecycle (Attachments &amp; Outputs)
     * </pre>
     *
     * <code>repeated .ai.stigmer.agentic.agentexecution.v1.Attachment attachments = 9 [json_name = "attachments"];</code>
     */
    public int getAttachmentsCount() {
      if (attachmentsBuilder_ == null) {
        return attachments_.size();
      } else {
        return attachmentsBuilder_.getCount();
      }
    }
    /**
     * <pre>
     * Files attached to this execution.
     *
     * Attachments are injected into the sandbox before execution begins.
     * The agent can read these files from the specified mount paths.
     *
     * Use cases:
     * - Provide input data files to process
     * - Pass configuration files
     * - Supply reference documents for analysis
     *
     * Example CLI usage:
     * stigmer run agent my-agent --attach ./config.yaml -m "Process this config"
     *
     * &#64;since Artifact Lifecycle (Attachments &amp; Outputs)
     * </pre>
     *
     * <code>repeated .ai.stigmer.agentic.agentexecution.v1.Attachment attachments = 9 [json_name = "attachments"];</code>
     */
    public ai.stigmer.agentic.agentexecution.v1.Attachment getAttachments(int index) {
      if (attachmentsBuilder_ == null) {
        return attachments_.get(index);
      } else {
        return attachmentsBuilder_.getMessage(index);
      }
    }
    /**
     * <pre>
     * Files attached to this execution.
     *
     * Attachments are injected into the sandbox before execution begins.
     * The agent can read these files from the specified mount paths.
     *
     * Use cases:
     * - Provide input data files to process
     * - Pass configuration files
     * - Supply reference documents for analysis
     *
     * Example CLI usage:
     * stigmer run agent my-agent --attach ./config.yaml -m "Process this config"
     *
     * &#64;since Artifact Lifecycle (Attachments &amp; Outputs)
     * </pre>
     *
     * <code>repeated .ai.stigmer.agentic.agentexecution.v1.Attachment attachments = 9 [json_name = "attachments"];</code>
     */
    public Builder setAttachments(
        int index, ai.stigmer.agentic.agentexecution.v1.Attachment value) {
      if (attachmentsBuilder_ == null) {
        if (value == null) {
          throw new NullPointerException();
        }
        ensureAttachmentsIsMutable();
        attachments_.set(index, value);
        onChanged();
      } else {
        attachmentsBuilder_.setMessage(index, value);
      }
      return this;
    }
    /**
     * <pre>
     * Files attached to this execution.
     *
     * Attachments are injected into the sandbox before execution begins.
     * The agent can read these files from the specified mount paths.
     *
     * Use cases:
     * - Provide input data files to process
     * - Pass configuration files
     * - Supply reference documents for analysis
     *
     * Example CLI usage:
     * stigmer run agent my-agent --attach ./config.yaml -m "Process this config"
     *
     * &#64;since Artifact Lifecycle (Attachments &amp; Outputs)
     * </pre>
     *
     * <code>repeated .ai.stigmer.agentic.agentexecution.v1.Attachment attachments = 9 [json_name = "attachments"];</code>
     */
    public Builder setAttachments(
        int index, ai.stigmer.agentic.agentexecution.v1.Attachment.Builder builderForValue) {
      if (attachmentsBuilder_ == null) {
        ensureAttachmentsIsMutable();
        attachments_.set(index, builderForValue.build());
        onChanged();
      } else {
        attachmentsBuilder_.setMessage(index, builderForValue.build());
      }
      return this;
    }
    /**
     * <pre>
     * Files attached to this execution.
     *
     * Attachments are injected into the sandbox before execution begins.
     * The agent can read these files from the specified mount paths.
     *
     * Use cases:
     * - Provide input data files to process
     * - Pass configuration files
     * - Supply reference documents for analysis
     *
     * Example CLI usage:
     * stigmer run agent my-agent --attach ./config.yaml -m "Process this config"
     *
     * &#64;since Artifact Lifecycle (Attachments &amp; Outputs)
     * </pre>
     *
     * <code>repeated .ai.stigmer.agentic.agentexecution.v1.Attachment attachments = 9 [json_name = "attachments"];</code>
     */
    public Builder addAttachments(ai.stigmer.agentic.agentexecution.v1.Attachment value) {
      if (attachmentsBuilder_ == null) {
        if (value == null) {
          throw new NullPointerException();
        }
        ensureAttachmentsIsMutable();
        attachments_.add(value);
        onChanged();
      } else {
        attachmentsBuilder_.addMessage(value);
      }
      return this;
    }
    /**
     * <pre>
     * Files attached to this execution.
     *
     * Attachments are injected into the sandbox before execution begins.
     * The agent can read these files from the specified mount paths.
     *
     * Use cases:
     * - Provide input data files to process
     * - Pass configuration files
     * - Supply reference documents for analysis
     *
     * Example CLI usage:
     * stigmer run agent my-agent --attach ./config.yaml -m "Process this config"
     *
     * &#64;since Artifact Lifecycle (Attachments &amp; Outputs)
     * </pre>
     *
     * <code>repeated .ai.stigmer.agentic.agentexecution.v1.Attachment attachments = 9 [json_name = "attachments"];</code>
     */
    public Builder addAttachments(
        int index, ai.stigmer.agentic.agentexecution.v1.Attachment value) {
      if (attachmentsBuilder_ == null) {
        if (value == null) {
          throw new NullPointerException();
        }
        ensureAttachmentsIsMutable();
        attachments_.add(index, value);
        onChanged();
      } else {
        attachmentsBuilder_.addMessage(index, value);
      }
      return this;
    }
    /**
     * <pre>
     * Files attached to this execution.
     *
     * Attachments are injected into the sandbox before execution begins.
     * The agent can read these files from the specified mount paths.
     *
     * Use cases:
     * - Provide input data files to process
     * - Pass configuration files
     * - Supply reference documents for analysis
     *
     * Example CLI usage:
     * stigmer run agent my-agent --attach ./config.yaml -m "Process this config"
     *
     * &#64;since Artifact Lifecycle (Attachments &amp; Outputs)
     * </pre>
     *
     * <code>repeated .ai.stigmer.agentic.agentexecution.v1.Attachment attachments = 9 [json_name = "attachments"];</code>
     */
    public Builder addAttachments(
        ai.stigmer.agentic.agentexecution.v1.Attachment.Builder builderForValue) {
      if (attachmentsBuilder_ == null) {
        ensureAttachmentsIsMutable();
        attachments_.add(builderForValue.build());
        onChanged();
      } else {
        attachmentsBuilder_.addMessage(builderForValue.build());
      }
      return this;
    }
    /**
     * <pre>
     * Files attached to this execution.
     *
     * Attachments are injected into the sandbox before execution begins.
     * The agent can read these files from the specified mount paths.
     *
     * Use cases:
     * - Provide input data files to process
     * - Pass configuration files
     * - Supply reference documents for analysis
     *
     * Example CLI usage:
     * stigmer run agent my-agent --attach ./config.yaml -m "Process this config"
     *
     * &#64;since Artifact Lifecycle (Attachments &amp; Outputs)
     * </pre>
     *
     * <code>repeated .ai.stigmer.agentic.agentexecution.v1.Attachment attachments = 9 [json_name = "attachments"];</code>
     */
    public Builder addAttachments(
        int index, ai.stigmer.agentic.agentexecution.v1.Attachment.Builder builderForValue) {
      if (attachmentsBuilder_ == null) {
        ensureAttachmentsIsMutable();
        attachments_.add(index, builderForValue.build());
        onChanged();
      } else {
        attachmentsBuilder_.addMessage(index, builderForValue.build());
      }
      return this;
    }
    /**
     * <pre>
     * Files attached to this execution.
     *
     * Attachments are injected into the sandbox before execution begins.
     * The agent can read these files from the specified mount paths.
     *
     * Use cases:
     * - Provide input data files to process
     * - Pass configuration files
     * - Supply reference documents for analysis
     *
     * Example CLI usage:
     * stigmer run agent my-agent --attach ./config.yaml -m "Process this config"
     *
     * &#64;since Artifact Lifecycle (Attachments &amp; Outputs)
     * </pre>
     *
     * <code>repeated .ai.stigmer.agentic.agentexecution.v1.Attachment attachments = 9 [json_name = "attachments"];</code>
     */
    public Builder addAllAttachments(
        java.lang.Iterable<? extends ai.stigmer.agentic.agentexecution.v1.Attachment> values) {
      if (attachmentsBuilder_ == null) {
        ensureAttachmentsIsMutable();
        com.google.protobuf.AbstractMessageLite.Builder.addAll(
            values, attachments_);
        onChanged();
      } else {
        attachmentsBuilder_.addAllMessages(values);
      }
      return this;
    }
    /**
     * <pre>
     * Files attached to this execution.
     *
     * Attachments are injected into the sandbox before execution begins.
     * The agent can read these files from the specified mount paths.
     *
     * Use cases:
     * - Provide input data files to process
     * - Pass configuration files
     * - Supply reference documents for analysis
     *
     * Example CLI usage:
     * stigmer run agent my-agent --attach ./config.yaml -m "Process this config"
     *
     * &#64;since Artifact Lifecycle (Attachments &amp; Outputs)
     * </pre>
     *
     * <code>repeated .ai.stigmer.agentic.agentexecution.v1.Attachment attachments = 9 [json_name = "attachments"];</code>
     */
    public Builder clearAttachments() {
      if (attachmentsBuilder_ == null) {
        attachments_ = java.util.Collections.emptyList();
        bitField0_ = (bitField0_ & ~0x00000200);
        onChanged();
      } else {
        attachmentsBuilder_.clear();
      }
      return this;
    }
    /**
     * <pre>
     * Files attached to this execution.
     *
     * Attachments are injected into the sandbox before execution begins.
     * The agent can read these files from the specified mount paths.
     *
     * Use cases:
     * - Provide input data files to process
     * - Pass configuration files
     * - Supply reference documents for analysis
     *
     * Example CLI usage:
     * stigmer run agent my-agent --attach ./config.yaml -m "Process this config"
     *
     * &#64;since Artifact Lifecycle (Attachments &amp; Outputs)
     * </pre>
     *
     * <code>repeated .ai.stigmer.agentic.agentexecution.v1.Attachment attachments = 9 [json_name = "attachments"];</code>
     */
    public Builder removeAttachments(int index) {
      if (attachmentsBuilder_ == null) {
        ensureAttachmentsIsMutable();
        attachments_.remove(index);
        onChanged();
      } else {
        attachmentsBuilder_.remove(index);
      }
      return this;
    }
    /**
     * <pre>
     * Files attached to this execution.
     *
     * Attachments are injected into the sandbox before execution begins.
     * The agent can read these files from the specified mount paths.
     *
     * Use cases:
     * - Provide input data files to process
     * - Pass configuration files
     * - Supply reference documents for analysis
     *
     * Example CLI usage:
     * stigmer run agent my-agent --attach ./config.yaml -m "Process this config"
     *
     * &#64;since Artifact Lifecycle (Attachments &amp; Outputs)
     * </pre>
     *
     * <code>repeated .ai.stigmer.agentic.agentexecution.v1.Attachment attachments = 9 [json_name = "attachments"];</code>
     */
    public ai.stigmer.agentic.agentexecution.v1.Attachment.Builder getAttachmentsBuilder(
        int index) {
      return internalGetAttachmentsFieldBuilder().getBuilder(index);
    }
    /**
     * <pre>
     * Files attached to this execution.
     *
     * Attachments are injected into the sandbox before execution begins.
     * The agent can read these files from the specified mount paths.
     *
     * Use cases:
     * - Provide input data files to process
     * - Pass configuration files
     * - Supply reference documents for analysis
     *
     * Example CLI usage:
     * stigmer run agent my-agent --attach ./config.yaml -m "Process this config"
     *
     * &#64;since Artifact Lifecycle (Attachments &amp; Outputs)
     * </pre>
     *
     * <code>repeated .ai.stigmer.agentic.agentexecution.v1.Attachment attachments = 9 [json_name = "attachments"];</code>
     */
    public ai.stigmer.agentic.agentexecution.v1.AttachmentOrBuilder getAttachmentsOrBuilder(
        int index) {
      if (attachmentsBuilder_ == null) {
        return attachments_.get(index);  } else {
        return attachmentsBuilder_.getMessageOrBuilder(index);
      }
    }
    /**
     * <pre>
     * Files attached to this execution.
     *
     * Attachments are injected into the sandbox before execution begins.
     * The agent can read these files from the specified mount paths.
     *
     * Use cases:
     * - Provide input data files to process
     * - Pass configuration files
     * - Supply reference documents for analysis
     *
     * Example CLI usage:
     * stigmer run agent my-agent --attach ./config.yaml -m "Process this config"
     *
     * &#64;since Artifact Lifecycle (Attachments &amp; Outputs)
     * </pre>
     *
     * <code>repeated .ai.stigmer.agentic.agentexecution.v1.Attachment attachments = 9 [json_name = "attachments"];</code>
     */
    public java.util.List<? extends ai.stigmer.agentic.agentexecution.v1.AttachmentOrBuilder> 
         getAttachmentsOrBuilderList() {
      if (attachmentsBuilder_ != null) {
        return attachmentsBuilder_.getMessageOrBuilderList();
      } else {
        return java.util.Collections.unmodifiableList(attachments_);
      }
    }
    /**
     * <pre>
     * Files attached to this execution.
     *
     * Attachments are injected into the sandbox before execution begins.
     * The agent can read these files from the specified mount paths.
     *
     * Use cases:
     * - Provide input data files to process
     * - Pass configuration files
     * - Supply reference documents for analysis
     *
     * Example CLI usage:
     * stigmer run agent my-agent --attach ./config.yaml -m "Process this config"
     *
     * &#64;since Artifact Lifecycle (Attachments &amp; Outputs)
     * </pre>
     *
     * <code>repeated .ai.stigmer.agentic.agentexecution.v1.Attachment attachments = 9 [json_name = "attachments"];</code>
     */
    public ai.stigmer.agentic.agentexecution.v1.Attachment.Builder addAttachmentsBuilder() {
      return internalGetAttachmentsFieldBuilder().addBuilder(
          ai.stigmer.agentic.agentexecution.v1.Attachment.getDefaultInstance());
    }
    /**
     * <pre>
     * Files attached to this execution.
     *
     * Attachments are injected into the sandbox before execution begins.
     * The agent can read these files from the specified mount paths.
     *
     * Use cases:
     * - Provide input data files to process
     * - Pass configuration files
     * - Supply reference documents for analysis
     *
     * Example CLI usage:
     * stigmer run agent my-agent --attach ./config.yaml -m "Process this config"
     *
     * &#64;since Artifact Lifecycle (Attachments &amp; Outputs)
     * </pre>
     *
     * <code>repeated .ai.stigmer.agentic.agentexecution.v1.Attachment attachments = 9 [json_name = "attachments"];</code>
     */
    public ai.stigmer.agentic.agentexecution.v1.Attachment.Builder addAttachmentsBuilder(
        int index) {
      return internalGetAttachmentsFieldBuilder().addBuilder(
          index, ai.stigmer.agentic.agentexecution.v1.Attachment.getDefaultInstance());
    }
    /**
     * <pre>
     * Files attached to this execution.
     *
     * Attachments are injected into the sandbox before execution begins.
     * The agent can read these files from the specified mount paths.
     *
     * Use cases:
     * - Provide input data files to process
     * - Pass configuration files
     * - Supply reference documents for analysis
     *
     * Example CLI usage:
     * stigmer run agent my-agent --attach ./config.yaml -m "Process this config"
     *
     * &#64;since Artifact Lifecycle (Attachments &amp; Outputs)
     * </pre>
     *
     * <code>repeated .ai.stigmer.agentic.agentexecution.v1.Attachment attachments = 9 [json_name = "attachments"];</code>
     */
    public java.util.List<ai.stigmer.agentic.agentexecution.v1.Attachment.Builder> 
         getAttachmentsBuilderList() {
      return internalGetAttachmentsFieldBuilder().getBuilderList();
    }
    private com.google.protobuf.RepeatedFieldBuilder<
        ai.stigmer.agentic.agentexecution.v1.Attachment, ai.stigmer.agentic.agentexecution.v1.Attachment.Builder, ai.stigmer.agentic.agentexecution.v1.AttachmentOrBuilder> 
        internalGetAttachmentsFieldBuilder() {
      if (attachmentsBuilder_ == null) {
        attachmentsBuilder_ = new com.google.protobuf.RepeatedFieldBuilder<
            ai.stigmer.agentic.agentexecution.v1.Attachment, ai.stigmer.agentic.agentexecution.v1.Attachment.Builder, ai.stigmer.agentic.agentexecution.v1.AttachmentOrBuilder>(
                attachments_,
                ((bitField0_ & 0x00000200) != 0),
                getParentForChildren(),
                isClean());
        attachments_ = null;
      }
      return attachmentsBuilder_;
    }

    private com.google.protobuf.LazyStringArrayList workspaceFileRefs_ =
        com.google.protobuf.LazyStringArrayList.emptyList();
    private void ensureWorkspaceFileRefsIsMutable() {
      if (!workspaceFileRefs_.isModifiable()) {
        workspaceFileRefs_ = new com.google.protobuf.LazyStringArrayList(workspaceFileRefs_);
      }
      bitField0_ |= 0x00000400;
    }
    /**
     * <pre>
     * Workspace-relative file paths the user wants the agent to focus on.
     *
     * When users reference files that exist inside the session's workspace,
     * the client records the workspace-relative path here instead of uploading.
     * The agent accesses these files directly via the workspace filesystem --
     * no upload, no copy, no injection.
     *
     * This separates the "attention" signal ("look at this file") from the
     * "transport" mechanism (upload + inject) that regular attachments use.
     *
     * Works for any workspace type with a resolvable filesystem post-provisioning
     * (local-path workspaces, git-cloned workspaces, or any future source type).
     * The runner resolves paths after workspace provisioning regardless of source.
     *
     * Populated by:
     * - CLI: detects --attach paths inside workspace roots automatically
     * - Web/Desktop: drag-to-reference from workspace file tree to composer
     *
     * Example CLI usage:
     * stigmer run agent reviewer --workspace . --attach ./src/config.yaml -m "Review"
     * # CLI detects src/config.yaml is inside workspace -&gt; workspace_file_ref
     * # No upload, no injection; agent reads directly from src/config.yaml
     *
     * &#64;since Workspace-Aware File Referencing
     * </pre>
     *
     * <code>repeated string workspace_file_refs = 10 [json_name = "workspaceFileRefs"];</code>
     * @return A list containing the workspaceFileRefs.
     */
    public com.google.protobuf.ProtocolStringList
        getWorkspaceFileRefsList() {
      workspaceFileRefs_.makeImmutable();
      return workspaceFileRefs_;
    }
    /**
     * <pre>
     * Workspace-relative file paths the user wants the agent to focus on.
     *
     * When users reference files that exist inside the session's workspace,
     * the client records the workspace-relative path here instead of uploading.
     * The agent accesses these files directly via the workspace filesystem --
     * no upload, no copy, no injection.
     *
     * This separates the "attention" signal ("look at this file") from the
     * "transport" mechanism (upload + inject) that regular attachments use.
     *
     * Works for any workspace type with a resolvable filesystem post-provisioning
     * (local-path workspaces, git-cloned workspaces, or any future source type).
     * The runner resolves paths after workspace provisioning regardless of source.
     *
     * Populated by:
     * - CLI: detects --attach paths inside workspace roots automatically
     * - Web/Desktop: drag-to-reference from workspace file tree to composer
     *
     * Example CLI usage:
     * stigmer run agent reviewer --workspace . --attach ./src/config.yaml -m "Review"
     * # CLI detects src/config.yaml is inside workspace -&gt; workspace_file_ref
     * # No upload, no injection; agent reads directly from src/config.yaml
     *
     * &#64;since Workspace-Aware File Referencing
     * </pre>
     *
     * <code>repeated string workspace_file_refs = 10 [json_name = "workspaceFileRefs"];</code>
     * @return The count of workspaceFileRefs.
     */
    public int getWorkspaceFileRefsCount() {
      return workspaceFileRefs_.size();
    }
    /**
     * <pre>
     * Workspace-relative file paths the user wants the agent to focus on.
     *
     * When users reference files that exist inside the session's workspace,
     * the client records the workspace-relative path here instead of uploading.
     * The agent accesses these files directly via the workspace filesystem --
     * no upload, no copy, no injection.
     *
     * This separates the "attention" signal ("look at this file") from the
     * "transport" mechanism (upload + inject) that regular attachments use.
     *
     * Works for any workspace type with a resolvable filesystem post-provisioning
     * (local-path workspaces, git-cloned workspaces, or any future source type).
     * The runner resolves paths after workspace provisioning regardless of source.
     *
     * Populated by:
     * - CLI: detects --attach paths inside workspace roots automatically
     * - Web/Desktop: drag-to-reference from workspace file tree to composer
     *
     * Example CLI usage:
     * stigmer run agent reviewer --workspace . --attach ./src/config.yaml -m "Review"
     * # CLI detects src/config.yaml is inside workspace -&gt; workspace_file_ref
     * # No upload, no injection; agent reads directly from src/config.yaml
     *
     * &#64;since Workspace-Aware File Referencing
     * </pre>
     *
     * <code>repeated string workspace_file_refs = 10 [json_name = "workspaceFileRefs"];</code>
     * @param index The index of the element to return.
     * @return The workspaceFileRefs at the given index.
     */
    public java.lang.String getWorkspaceFileRefs(int index) {
      return workspaceFileRefs_.get(index);
    }
    /**
     * <pre>
     * Workspace-relative file paths the user wants the agent to focus on.
     *
     * When users reference files that exist inside the session's workspace,
     * the client records the workspace-relative path here instead of uploading.
     * The agent accesses these files directly via the workspace filesystem --
     * no upload, no copy, no injection.
     *
     * This separates the "attention" signal ("look at this file") from the
     * "transport" mechanism (upload + inject) that regular attachments use.
     *
     * Works for any workspace type with a resolvable filesystem post-provisioning
     * (local-path workspaces, git-cloned workspaces, or any future source type).
     * The runner resolves paths after workspace provisioning regardless of source.
     *
     * Populated by:
     * - CLI: detects --attach paths inside workspace roots automatically
     * - Web/Desktop: drag-to-reference from workspace file tree to composer
     *
     * Example CLI usage:
     * stigmer run agent reviewer --workspace . --attach ./src/config.yaml -m "Review"
     * # CLI detects src/config.yaml is inside workspace -&gt; workspace_file_ref
     * # No upload, no injection; agent reads directly from src/config.yaml
     *
     * &#64;since Workspace-Aware File Referencing
     * </pre>
     *
     * <code>repeated string workspace_file_refs = 10 [json_name = "workspaceFileRefs"];</code>
     * @param index The index of the value to return.
     * @return The bytes of the workspaceFileRefs at the given index.
     */
    public com.google.protobuf.ByteString
        getWorkspaceFileRefsBytes(int index) {
      return workspaceFileRefs_.getByteString(index);
    }
    /**
     * <pre>
     * Workspace-relative file paths the user wants the agent to focus on.
     *
     * When users reference files that exist inside the session's workspace,
     * the client records the workspace-relative path here instead of uploading.
     * The agent accesses these files directly via the workspace filesystem --
     * no upload, no copy, no injection.
     *
     * This separates the "attention" signal ("look at this file") from the
     * "transport" mechanism (upload + inject) that regular attachments use.
     *
     * Works for any workspace type with a resolvable filesystem post-provisioning
     * (local-path workspaces, git-cloned workspaces, or any future source type).
     * The runner resolves paths after workspace provisioning regardless of source.
     *
     * Populated by:
     * - CLI: detects --attach paths inside workspace roots automatically
     * - Web/Desktop: drag-to-reference from workspace file tree to composer
     *
     * Example CLI usage:
     * stigmer run agent reviewer --workspace . --attach ./src/config.yaml -m "Review"
     * # CLI detects src/config.yaml is inside workspace -&gt; workspace_file_ref
     * # No upload, no injection; agent reads directly from src/config.yaml
     *
     * &#64;since Workspace-Aware File Referencing
     * </pre>
     *
     * <code>repeated string workspace_file_refs = 10 [json_name = "workspaceFileRefs"];</code>
     * @param index The index to set the value at.
     * @param value The workspaceFileRefs to set.
     * @return This builder for chaining.
     */
    public Builder setWorkspaceFileRefs(
        int index, java.lang.String value) {
      if (value == null) { throw new NullPointerException(); }
      ensureWorkspaceFileRefsIsMutable();
      workspaceFileRefs_.set(index, value);
      bitField0_ |= 0x00000400;
      onChanged();
      return this;
    }
    /**
     * <pre>
     * Workspace-relative file paths the user wants the agent to focus on.
     *
     * When users reference files that exist inside the session's workspace,
     * the client records the workspace-relative path here instead of uploading.
     * The agent accesses these files directly via the workspace filesystem --
     * no upload, no copy, no injection.
     *
     * This separates the "attention" signal ("look at this file") from the
     * "transport" mechanism (upload + inject) that regular attachments use.
     *
     * Works for any workspace type with a resolvable filesystem post-provisioning
     * (local-path workspaces, git-cloned workspaces, or any future source type).
     * The runner resolves paths after workspace provisioning regardless of source.
     *
     * Populated by:
     * - CLI: detects --attach paths inside workspace roots automatically
     * - Web/Desktop: drag-to-reference from workspace file tree to composer
     *
     * Example CLI usage:
     * stigmer run agent reviewer --workspace . --attach ./src/config.yaml -m "Review"
     * # CLI detects src/config.yaml is inside workspace -&gt; workspace_file_ref
     * # No upload, no injection; agent reads directly from src/config.yaml
     *
     * &#64;since Workspace-Aware File Referencing
     * </pre>
     *
     * <code>repeated string workspace_file_refs = 10 [json_name = "workspaceFileRefs"];</code>
     * @param value The workspaceFileRefs to add.
     * @return This builder for chaining.
     */
    public Builder addWorkspaceFileRefs(
        java.lang.String value) {
      if (value == null) { throw new NullPointerException(); }
      ensureWorkspaceFileRefsIsMutable();
      workspaceFileRefs_.add(value);
      bitField0_ |= 0x00000400;
      onChanged();
      return this;
    }
    /**
     * <pre>
     * Workspace-relative file paths the user wants the agent to focus on.
     *
     * When users reference files that exist inside the session's workspace,
     * the client records the workspace-relative path here instead of uploading.
     * The agent accesses these files directly via the workspace filesystem --
     * no upload, no copy, no injection.
     *
     * This separates the "attention" signal ("look at this file") from the
     * "transport" mechanism (upload + inject) that regular attachments use.
     *
     * Works for any workspace type with a resolvable filesystem post-provisioning
     * (local-path workspaces, git-cloned workspaces, or any future source type).
     * The runner resolves paths after workspace provisioning regardless of source.
     *
     * Populated by:
     * - CLI: detects --attach paths inside workspace roots automatically
     * - Web/Desktop: drag-to-reference from workspace file tree to composer
     *
     * Example CLI usage:
     * stigmer run agent reviewer --workspace . --attach ./src/config.yaml -m "Review"
     * # CLI detects src/config.yaml is inside workspace -&gt; workspace_file_ref
     * # No upload, no injection; agent reads directly from src/config.yaml
     *
     * &#64;since Workspace-Aware File Referencing
     * </pre>
     *
     * <code>repeated string workspace_file_refs = 10 [json_name = "workspaceFileRefs"];</code>
     * @param values The workspaceFileRefs to add.
     * @return This builder for chaining.
     */
    public Builder addAllWorkspaceFileRefs(
        java.lang.Iterable<java.lang.String> values) {
      ensureWorkspaceFileRefsIsMutable();
      com.google.protobuf.AbstractMessageLite.Builder.addAll(
          values, workspaceFileRefs_);
      bitField0_ |= 0x00000400;
      onChanged();
      return this;
    }
    /**
     * <pre>
     * Workspace-relative file paths the user wants the agent to focus on.
     *
     * When users reference files that exist inside the session's workspace,
     * the client records the workspace-relative path here instead of uploading.
     * The agent accesses these files directly via the workspace filesystem --
     * no upload, no copy, no injection.
     *
     * This separates the "attention" signal ("look at this file") from the
     * "transport" mechanism (upload + inject) that regular attachments use.
     *
     * Works for any workspace type with a resolvable filesystem post-provisioning
     * (local-path workspaces, git-cloned workspaces, or any future source type).
     * The runner resolves paths after workspace provisioning regardless of source.
     *
     * Populated by:
     * - CLI: detects --attach paths inside workspace roots automatically
     * - Web/Desktop: drag-to-reference from workspace file tree to composer
     *
     * Example CLI usage:
     * stigmer run agent reviewer --workspace . --attach ./src/config.yaml -m "Review"
     * # CLI detects src/config.yaml is inside workspace -&gt; workspace_file_ref
     * # No upload, no injection; agent reads directly from src/config.yaml
     *
     * &#64;since Workspace-Aware File Referencing
     * </pre>
     *
     * <code>repeated string workspace_file_refs = 10 [json_name = "workspaceFileRefs"];</code>
     * @return This builder for chaining.
     */
    public Builder clearWorkspaceFileRefs() {
      workspaceFileRefs_ =
        com.google.protobuf.LazyStringArrayList.emptyList();
      bitField0_ = (bitField0_ & ~0x00000400);;
      onChanged();
      return this;
    }
    /**
     * <pre>
     * Workspace-relative file paths the user wants the agent to focus on.
     *
     * When users reference files that exist inside the session's workspace,
     * the client records the workspace-relative path here instead of uploading.
     * The agent accesses these files directly via the workspace filesystem --
     * no upload, no copy, no injection.
     *
     * This separates the "attention" signal ("look at this file") from the
     * "transport" mechanism (upload + inject) that regular attachments use.
     *
     * Works for any workspace type with a resolvable filesystem post-provisioning
     * (local-path workspaces, git-cloned workspaces, or any future source type).
     * The runner resolves paths after workspace provisioning regardless of source.
     *
     * Populated by:
     * - CLI: detects --attach paths inside workspace roots automatically
     * - Web/Desktop: drag-to-reference from workspace file tree to composer
     *
     * Example CLI usage:
     * stigmer run agent reviewer --workspace . --attach ./src/config.yaml -m "Review"
     * # CLI detects src/config.yaml is inside workspace -&gt; workspace_file_ref
     * # No upload, no injection; agent reads directly from src/config.yaml
     *
     * &#64;since Workspace-Aware File Referencing
     * </pre>
     *
     * <code>repeated string workspace_file_refs = 10 [json_name = "workspaceFileRefs"];</code>
     * @param value The bytes of the workspaceFileRefs to add.
     * @return This builder for chaining.
     */
    public Builder addWorkspaceFileRefsBytes(
        com.google.protobuf.ByteString value) {
      if (value == null) { throw new NullPointerException(); }
      checkByteStringIsUtf8(value);
      ensureWorkspaceFileRefsIsMutable();
      workspaceFileRefs_.add(value);
      bitField0_ |= 0x00000400;
      onChanged();
      return this;
    }

    private java.lang.Object activityTaskQueue_ = "";
    /**
     * <pre>
     * Explicit Temporal task queue override for activity routing.
     *
     * &#64;internal
     * When set, the agent execution's activities are routed to this queue
     * instead of the normally-resolved queue (session:{id} or global stigmer_runner).
     * This enables sandbox sharing: a parent workflow execution passes its own
     * queue so child agents run in the same sandbox without provisioning new VMs.
     *
     * When empty: normal dispatch resolution applies (default behavior).
     *
     * Security: Only accepted from internal callers (workflow engine's CallAgent
     * activity). External API callers cannot set this field — the create handler
     * strips it during input sanitization. Presence of parent_workflow_id is used
     * as a co-validation signal.
     *
     * Format: "wfexec:{workflow_execution_id}" (matches the parent workflow's
     * sandbox queue). The prefix ensures no collision with session:{id} queues.
     *
     * &#64;since Workflow Sandbox Affinity
     * </pre>
     *
     * <code>string activity_task_queue = 11 [json_name = "activityTaskQueue"];</code>
     * @return The activityTaskQueue.
     */
    public java.lang.String getActivityTaskQueue() {
      java.lang.Object ref = activityTaskQueue_;
      if (!(ref instanceof java.lang.String)) {
        com.google.protobuf.ByteString bs =
            (com.google.protobuf.ByteString) ref;
        java.lang.String s = bs.toStringUtf8();
        activityTaskQueue_ = s;
        return s;
      } else {
        return (java.lang.String) ref;
      }
    }
    /**
     * <pre>
     * Explicit Temporal task queue override for activity routing.
     *
     * &#64;internal
     * When set, the agent execution's activities are routed to this queue
     * instead of the normally-resolved queue (session:{id} or global stigmer_runner).
     * This enables sandbox sharing: a parent workflow execution passes its own
     * queue so child agents run in the same sandbox without provisioning new VMs.
     *
     * When empty: normal dispatch resolution applies (default behavior).
     *
     * Security: Only accepted from internal callers (workflow engine's CallAgent
     * activity). External API callers cannot set this field — the create handler
     * strips it during input sanitization. Presence of parent_workflow_id is used
     * as a co-validation signal.
     *
     * Format: "wfexec:{workflow_execution_id}" (matches the parent workflow's
     * sandbox queue). The prefix ensures no collision with session:{id} queues.
     *
     * &#64;since Workflow Sandbox Affinity
     * </pre>
     *
     * <code>string activity_task_queue = 11 [json_name = "activityTaskQueue"];</code>
     * @return The bytes for activityTaskQueue.
     */
    public com.google.protobuf.ByteString
        getActivityTaskQueueBytes() {
      java.lang.Object ref = activityTaskQueue_;
      if (ref instanceof String) {
        com.google.protobuf.ByteString b = 
            com.google.protobuf.ByteString.copyFromUtf8(
                (java.lang.String) ref);
        activityTaskQueue_ = b;
        return b;
      } else {
        return (com.google.protobuf.ByteString) ref;
      }
    }
    /**
     * <pre>
     * Explicit Temporal task queue override for activity routing.
     *
     * &#64;internal
     * When set, the agent execution's activities are routed to this queue
     * instead of the normally-resolved queue (session:{id} or global stigmer_runner).
     * This enables sandbox sharing: a parent workflow execution passes its own
     * queue so child agents run in the same sandbox without provisioning new VMs.
     *
     * When empty: normal dispatch resolution applies (default behavior).
     *
     * Security: Only accepted from internal callers (workflow engine's CallAgent
     * activity). External API callers cannot set this field — the create handler
     * strips it during input sanitization. Presence of parent_workflow_id is used
     * as a co-validation signal.
     *
     * Format: "wfexec:{workflow_execution_id}" (matches the parent workflow's
     * sandbox queue). The prefix ensures no collision with session:{id} queues.
     *
     * &#64;since Workflow Sandbox Affinity
     * </pre>
     *
     * <code>string activity_task_queue = 11 [json_name = "activityTaskQueue"];</code>
     * @param value The activityTaskQueue to set.
     * @return This builder for chaining.
     */
    public Builder setActivityTaskQueue(
        java.lang.String value) {
      if (value == null) { throw new NullPointerException(); }
      activityTaskQueue_ = value;
      bitField0_ |= 0x00000800;
      onChanged();
      return this;
    }
    /**
     * <pre>
     * Explicit Temporal task queue override for activity routing.
     *
     * &#64;internal
     * When set, the agent execution's activities are routed to this queue
     * instead of the normally-resolved queue (session:{id} or global stigmer_runner).
     * This enables sandbox sharing: a parent workflow execution passes its own
     * queue so child agents run in the same sandbox without provisioning new VMs.
     *
     * When empty: normal dispatch resolution applies (default behavior).
     *
     * Security: Only accepted from internal callers (workflow engine's CallAgent
     * activity). External API callers cannot set this field — the create handler
     * strips it during input sanitization. Presence of parent_workflow_id is used
     * as a co-validation signal.
     *
     * Format: "wfexec:{workflow_execution_id}" (matches the parent workflow's
     * sandbox queue). The prefix ensures no collision with session:{id} queues.
     *
     * &#64;since Workflow Sandbox Affinity
     * </pre>
     *
     * <code>string activity_task_queue = 11 [json_name = "activityTaskQueue"];</code>
     * @return This builder for chaining.
     */
    public Builder clearActivityTaskQueue() {
      activityTaskQueue_ = getDefaultInstance().getActivityTaskQueue();
      bitField0_ = (bitField0_ & ~0x00000800);
      onChanged();
      return this;
    }
    /**
     * <pre>
     * Explicit Temporal task queue override for activity routing.
     *
     * &#64;internal
     * When set, the agent execution's activities are routed to this queue
     * instead of the normally-resolved queue (session:{id} or global stigmer_runner).
     * This enables sandbox sharing: a parent workflow execution passes its own
     * queue so child agents run in the same sandbox without provisioning new VMs.
     *
     * When empty: normal dispatch resolution applies (default behavior).
     *
     * Security: Only accepted from internal callers (workflow engine's CallAgent
     * activity). External API callers cannot set this field — the create handler
     * strips it during input sanitization. Presence of parent_workflow_id is used
     * as a co-validation signal.
     *
     * Format: "wfexec:{workflow_execution_id}" (matches the parent workflow's
     * sandbox queue). The prefix ensures no collision with session:{id} queues.
     *
     * &#64;since Workflow Sandbox Affinity
     * </pre>
     *
     * <code>string activity_task_queue = 11 [json_name = "activityTaskQueue"];</code>
     * @param value The bytes for activityTaskQueue to set.
     * @return This builder for chaining.
     */
    public Builder setActivityTaskQueueBytes(
        com.google.protobuf.ByteString value) {
      if (value == null) { throw new NullPointerException(); }
      checkByteStringIsUtf8(value);
      activityTaskQueue_ = value;
      bitField0_ |= 0x00000800;
      onChanged();
      return this;
    }

    private java.lang.Object supersedesExecutionId_ = "";
    /**
     * <pre>
     * ID of the execution this one supersedes via edit-and-resubmit (optional).
     *
     * When a user stops an in-flight turn, edits the message, and resubmits,
     * the client sets this field on the NEW execution to point at the stopped
     * one. Chat-thread consumers hide the superseded execution so the edited
     * message replaces the original in place; history surfaces (CLI, execution
     * lists) keep showing the full record.
     *
     * Empty means this execution is not an edit of another turn.
     *
     * &#64;internal
     *
     * The link always lives on the successor — the superseded record is never
     * mutated (append-only execution log, single writer). Chained edits form a
     * chain of links (A &lt;- B &lt;- C), each persisted on its own successor.
     *
     * Display-level semantics only: the runner does NOT rewind model context.
     * Conversation history lives in an opaque session-keyed store (LangGraph
     * checkpoint thread / Cursor SDK agent store) that is resumed as-is, so the
     * superseded turn's partial messages remain visible to the model. Do not
     * build features that assume the runner skips superseded turns.
     *
     * &#64;since Edit-and-Resubmit In Place (stigmer/stigmer#181)
     * </pre>
     *
     * <code>string supersedes_execution_id = 12 [json_name = "supersedesExecutionId"];</code>
     * @return The supersedesExecutionId.
     */
    public java.lang.String getSupersedesExecutionId() {
      java.lang.Object ref = supersedesExecutionId_;
      if (!(ref instanceof java.lang.String)) {
        com.google.protobuf.ByteString bs =
            (com.google.protobuf.ByteString) ref;
        java.lang.String s = bs.toStringUtf8();
        supersedesExecutionId_ = s;
        return s;
      } else {
        return (java.lang.String) ref;
      }
    }
    /**
     * <pre>
     * ID of the execution this one supersedes via edit-and-resubmit (optional).
     *
     * When a user stops an in-flight turn, edits the message, and resubmits,
     * the client sets this field on the NEW execution to point at the stopped
     * one. Chat-thread consumers hide the superseded execution so the edited
     * message replaces the original in place; history surfaces (CLI, execution
     * lists) keep showing the full record.
     *
     * Empty means this execution is not an edit of another turn.
     *
     * &#64;internal
     *
     * The link always lives on the successor — the superseded record is never
     * mutated (append-only execution log, single writer). Chained edits form a
     * chain of links (A &lt;- B &lt;- C), each persisted on its own successor.
     *
     * Display-level semantics only: the runner does NOT rewind model context.
     * Conversation history lives in an opaque session-keyed store (LangGraph
     * checkpoint thread / Cursor SDK agent store) that is resumed as-is, so the
     * superseded turn's partial messages remain visible to the model. Do not
     * build features that assume the runner skips superseded turns.
     *
     * &#64;since Edit-and-Resubmit In Place (stigmer/stigmer#181)
     * </pre>
     *
     * <code>string supersedes_execution_id = 12 [json_name = "supersedesExecutionId"];</code>
     * @return The bytes for supersedesExecutionId.
     */
    public com.google.protobuf.ByteString
        getSupersedesExecutionIdBytes() {
      java.lang.Object ref = supersedesExecutionId_;
      if (ref instanceof String) {
        com.google.protobuf.ByteString b = 
            com.google.protobuf.ByteString.copyFromUtf8(
                (java.lang.String) ref);
        supersedesExecutionId_ = b;
        return b;
      } else {
        return (com.google.protobuf.ByteString) ref;
      }
    }
    /**
     * <pre>
     * ID of the execution this one supersedes via edit-and-resubmit (optional).
     *
     * When a user stops an in-flight turn, edits the message, and resubmits,
     * the client sets this field on the NEW execution to point at the stopped
     * one. Chat-thread consumers hide the superseded execution so the edited
     * message replaces the original in place; history surfaces (CLI, execution
     * lists) keep showing the full record.
     *
     * Empty means this execution is not an edit of another turn.
     *
     * &#64;internal
     *
     * The link always lives on the successor — the superseded record is never
     * mutated (append-only execution log, single writer). Chained edits form a
     * chain of links (A &lt;- B &lt;- C), each persisted on its own successor.
     *
     * Display-level semantics only: the runner does NOT rewind model context.
     * Conversation history lives in an opaque session-keyed store (LangGraph
     * checkpoint thread / Cursor SDK agent store) that is resumed as-is, so the
     * superseded turn's partial messages remain visible to the model. Do not
     * build features that assume the runner skips superseded turns.
     *
     * &#64;since Edit-and-Resubmit In Place (stigmer/stigmer#181)
     * </pre>
     *
     * <code>string supersedes_execution_id = 12 [json_name = "supersedesExecutionId"];</code>
     * @param value The supersedesExecutionId to set.
     * @return This builder for chaining.
     */
    public Builder setSupersedesExecutionId(
        java.lang.String value) {
      if (value == null) { throw new NullPointerException(); }
      supersedesExecutionId_ = value;
      bitField0_ |= 0x00001000;
      onChanged();
      return this;
    }
    /**
     * <pre>
     * ID of the execution this one supersedes via edit-and-resubmit (optional).
     *
     * When a user stops an in-flight turn, edits the message, and resubmits,
     * the client sets this field on the NEW execution to point at the stopped
     * one. Chat-thread consumers hide the superseded execution so the edited
     * message replaces the original in place; history surfaces (CLI, execution
     * lists) keep showing the full record.
     *
     * Empty means this execution is not an edit of another turn.
     *
     * &#64;internal
     *
     * The link always lives on the successor — the superseded record is never
     * mutated (append-only execution log, single writer). Chained edits form a
     * chain of links (A &lt;- B &lt;- C), each persisted on its own successor.
     *
     * Display-level semantics only: the runner does NOT rewind model context.
     * Conversation history lives in an opaque session-keyed store (LangGraph
     * checkpoint thread / Cursor SDK agent store) that is resumed as-is, so the
     * superseded turn's partial messages remain visible to the model. Do not
     * build features that assume the runner skips superseded turns.
     *
     * &#64;since Edit-and-Resubmit In Place (stigmer/stigmer#181)
     * </pre>
     *
     * <code>string supersedes_execution_id = 12 [json_name = "supersedesExecutionId"];</code>
     * @return This builder for chaining.
     */
    public Builder clearSupersedesExecutionId() {
      supersedesExecutionId_ = getDefaultInstance().getSupersedesExecutionId();
      bitField0_ = (bitField0_ & ~0x00001000);
      onChanged();
      return this;
    }
    /**
     * <pre>
     * ID of the execution this one supersedes via edit-and-resubmit (optional).
     *
     * When a user stops an in-flight turn, edits the message, and resubmits,
     * the client sets this field on the NEW execution to point at the stopped
     * one. Chat-thread consumers hide the superseded execution so the edited
     * message replaces the original in place; history surfaces (CLI, execution
     * lists) keep showing the full record.
     *
     * Empty means this execution is not an edit of another turn.
     *
     * &#64;internal
     *
     * The link always lives on the successor — the superseded record is never
     * mutated (append-only execution log, single writer). Chained edits form a
     * chain of links (A &lt;- B &lt;- C), each persisted on its own successor.
     *
     * Display-level semantics only: the runner does NOT rewind model context.
     * Conversation history lives in an opaque session-keyed store (LangGraph
     * checkpoint thread / Cursor SDK agent store) that is resumed as-is, so the
     * superseded turn's partial messages remain visible to the model. Do not
     * build features that assume the runner skips superseded turns.
     *
     * &#64;since Edit-and-Resubmit In Place (stigmer/stigmer#181)
     * </pre>
     *
     * <code>string supersedes_execution_id = 12 [json_name = "supersedesExecutionId"];</code>
     * @param value The bytes for supersedesExecutionId to set.
     * @return This builder for chaining.
     */
    public Builder setSupersedesExecutionIdBytes(
        com.google.protobuf.ByteString value) {
      if (value == null) { throw new NullPointerException(); }
      checkByteStringIsUtf8(value);
      supersedesExecutionId_ = value;
      bitField0_ |= 0x00001000;
      onChanged();
      return this;
    }

    private ai.stigmer.agentic.agentexecution.v1.ConversationCatchup conversationCatchup_;
    private com.google.protobuf.SingleFieldBuilder<
        ai.stigmer.agentic.agentexecution.v1.ConversationCatchup, ai.stigmer.agentic.agentexecution.v1.ConversationCatchup.Builder, ai.stigmer.agentic.agentexecution.v1.ConversationCatchupOrBuilder> conversationCatchupBuilder_;
    /**
     * <pre>
     * Conversation events the agent has not yet seen, composed by the platform
     * for this turn (optional).
     *
     * Set on live channel-conversation turns (WhatsApp, Slack): when a human
     * teammate handled the conversation or messages otherwise landed while the
     * agent was not watching, the digest carries what happened so the agent
     * re-enters informed. Absent on every other execution surface.
     *
     * &#64;internal
     * Composed per turn by the cloud channel runtime — ChannelSessionBroker
     * over the DD-004 timeline stitch, windowed by the conversation's
     * agent_witnessed_through watermark (channel-conversations DD-006/DD-007).
     * The OSS server never sets it. The runner prepends the framed digest to
     * the turn's user message on both harnesses (A27) and must never read
     * window_end. Top-level rather than inside ExecutionConfig by design:
     * conversation content must not vanish with the execution-profile
     * kill-switch (DD-006 D-a).
     * </pre>
     *
     * <code>.ai.stigmer.agentic.agentexecution.v1.ConversationCatchup conversation_catchup = 14 [json_name = "conversationCatchup"];</code>
     * @return Whether the conversationCatchup field is set.
     */
    public boolean hasConversationCatchup() {
      return ((bitField0_ & 0x00002000) != 0);
    }
    /**
     * <pre>
     * Conversation events the agent has not yet seen, composed by the platform
     * for this turn (optional).
     *
     * Set on live channel-conversation turns (WhatsApp, Slack): when a human
     * teammate handled the conversation or messages otherwise landed while the
     * agent was not watching, the digest carries what happened so the agent
     * re-enters informed. Absent on every other execution surface.
     *
     * &#64;internal
     * Composed per turn by the cloud channel runtime — ChannelSessionBroker
     * over the DD-004 timeline stitch, windowed by the conversation's
     * agent_witnessed_through watermark (channel-conversations DD-006/DD-007).
     * The OSS server never sets it. The runner prepends the framed digest to
     * the turn's user message on both harnesses (A27) and must never read
     * window_end. Top-level rather than inside ExecutionConfig by design:
     * conversation content must not vanish with the execution-profile
     * kill-switch (DD-006 D-a).
     * </pre>
     *
     * <code>.ai.stigmer.agentic.agentexecution.v1.ConversationCatchup conversation_catchup = 14 [json_name = "conversationCatchup"];</code>
     * @return The conversationCatchup.
     */
    public ai.stigmer.agentic.agentexecution.v1.ConversationCatchup getConversationCatchup() {
      if (conversationCatchupBuilder_ == null) {
        return conversationCatchup_ == null ? ai.stigmer.agentic.agentexecution.v1.ConversationCatchup.getDefaultInstance() : conversationCatchup_;
      } else {
        return conversationCatchupBuilder_.getMessage();
      }
    }
    /**
     * <pre>
     * Conversation events the agent has not yet seen, composed by the platform
     * for this turn (optional).
     *
     * Set on live channel-conversation turns (WhatsApp, Slack): when a human
     * teammate handled the conversation or messages otherwise landed while the
     * agent was not watching, the digest carries what happened so the agent
     * re-enters informed. Absent on every other execution surface.
     *
     * &#64;internal
     * Composed per turn by the cloud channel runtime — ChannelSessionBroker
     * over the DD-004 timeline stitch, windowed by the conversation's
     * agent_witnessed_through watermark (channel-conversations DD-006/DD-007).
     * The OSS server never sets it. The runner prepends the framed digest to
     * the turn's user message on both harnesses (A27) and must never read
     * window_end. Top-level rather than inside ExecutionConfig by design:
     * conversation content must not vanish with the execution-profile
     * kill-switch (DD-006 D-a).
     * </pre>
     *
     * <code>.ai.stigmer.agentic.agentexecution.v1.ConversationCatchup conversation_catchup = 14 [json_name = "conversationCatchup"];</code>
     */
    public Builder setConversationCatchup(ai.stigmer.agentic.agentexecution.v1.ConversationCatchup value) {
      if (conversationCatchupBuilder_ == null) {
        if (value == null) {
          throw new NullPointerException();
        }
        conversationCatchup_ = value;
      } else {
        conversationCatchupBuilder_.setMessage(value);
      }
      bitField0_ |= 0x00002000;
      onChanged();
      return this;
    }
    /**
     * <pre>
     * Conversation events the agent has not yet seen, composed by the platform
     * for this turn (optional).
     *
     * Set on live channel-conversation turns (WhatsApp, Slack): when a human
     * teammate handled the conversation or messages otherwise landed while the
     * agent was not watching, the digest carries what happened so the agent
     * re-enters informed. Absent on every other execution surface.
     *
     * &#64;internal
     * Composed per turn by the cloud channel runtime — ChannelSessionBroker
     * over the DD-004 timeline stitch, windowed by the conversation's
     * agent_witnessed_through watermark (channel-conversations DD-006/DD-007).
     * The OSS server never sets it. The runner prepends the framed digest to
     * the turn's user message on both harnesses (A27) and must never read
     * window_end. Top-level rather than inside ExecutionConfig by design:
     * conversation content must not vanish with the execution-profile
     * kill-switch (DD-006 D-a).
     * </pre>
     *
     * <code>.ai.stigmer.agentic.agentexecution.v1.ConversationCatchup conversation_catchup = 14 [json_name = "conversationCatchup"];</code>
     */
    public Builder setConversationCatchup(
        ai.stigmer.agentic.agentexecution.v1.ConversationCatchup.Builder builderForValue) {
      if (conversationCatchupBuilder_ == null) {
        conversationCatchup_ = builderForValue.build();
      } else {
        conversationCatchupBuilder_.setMessage(builderForValue.build());
      }
      bitField0_ |= 0x00002000;
      onChanged();
      return this;
    }
    /**
     * <pre>
     * Conversation events the agent has not yet seen, composed by the platform
     * for this turn (optional).
     *
     * Set on live channel-conversation turns (WhatsApp, Slack): when a human
     * teammate handled the conversation or messages otherwise landed while the
     * agent was not watching, the digest carries what happened so the agent
     * re-enters informed. Absent on every other execution surface.
     *
     * &#64;internal
     * Composed per turn by the cloud channel runtime — ChannelSessionBroker
     * over the DD-004 timeline stitch, windowed by the conversation's
     * agent_witnessed_through watermark (channel-conversations DD-006/DD-007).
     * The OSS server never sets it. The runner prepends the framed digest to
     * the turn's user message on both harnesses (A27) and must never read
     * window_end. Top-level rather than inside ExecutionConfig by design:
     * conversation content must not vanish with the execution-profile
     * kill-switch (DD-006 D-a).
     * </pre>
     *
     * <code>.ai.stigmer.agentic.agentexecution.v1.ConversationCatchup conversation_catchup = 14 [json_name = "conversationCatchup"];</code>
     */
    public Builder mergeConversationCatchup(ai.stigmer.agentic.agentexecution.v1.ConversationCatchup value) {
      if (conversationCatchupBuilder_ == null) {
        if (((bitField0_ & 0x00002000) != 0) &&
          conversationCatchup_ != null &&
          conversationCatchup_ != ai.stigmer.agentic.agentexecution.v1.ConversationCatchup.getDefaultInstance()) {
          getConversationCatchupBuilder().mergeFrom(value);
        } else {
          conversationCatchup_ = value;
        }
      } else {
        conversationCatchupBuilder_.mergeFrom(value);
      }
      if (conversationCatchup_ != null) {
        bitField0_ |= 0x00002000;
        onChanged();
      }
      return this;
    }
    /**
     * <pre>
     * Conversation events the agent has not yet seen, composed by the platform
     * for this turn (optional).
     *
     * Set on live channel-conversation turns (WhatsApp, Slack): when a human
     * teammate handled the conversation or messages otherwise landed while the
     * agent was not watching, the digest carries what happened so the agent
     * re-enters informed. Absent on every other execution surface.
     *
     * &#64;internal
     * Composed per turn by the cloud channel runtime — ChannelSessionBroker
     * over the DD-004 timeline stitch, windowed by the conversation's
     * agent_witnessed_through watermark (channel-conversations DD-006/DD-007).
     * The OSS server never sets it. The runner prepends the framed digest to
     * the turn's user message on both harnesses (A27) and must never read
     * window_end. Top-level rather than inside ExecutionConfig by design:
     * conversation content must not vanish with the execution-profile
     * kill-switch (DD-006 D-a).
     * </pre>
     *
     * <code>.ai.stigmer.agentic.agentexecution.v1.ConversationCatchup conversation_catchup = 14 [json_name = "conversationCatchup"];</code>
     */
    public Builder clearConversationCatchup() {
      bitField0_ = (bitField0_ & ~0x00002000);
      conversationCatchup_ = null;
      if (conversationCatchupBuilder_ != null) {
        conversationCatchupBuilder_.dispose();
        conversationCatchupBuilder_ = null;
      }
      onChanged();
      return this;
    }
    /**
     * <pre>
     * Conversation events the agent has not yet seen, composed by the platform
     * for this turn (optional).
     *
     * Set on live channel-conversation turns (WhatsApp, Slack): when a human
     * teammate handled the conversation or messages otherwise landed while the
     * agent was not watching, the digest carries what happened so the agent
     * re-enters informed. Absent on every other execution surface.
     *
     * &#64;internal
     * Composed per turn by the cloud channel runtime — ChannelSessionBroker
     * over the DD-004 timeline stitch, windowed by the conversation's
     * agent_witnessed_through watermark (channel-conversations DD-006/DD-007).
     * The OSS server never sets it. The runner prepends the framed digest to
     * the turn's user message on both harnesses (A27) and must never read
     * window_end. Top-level rather than inside ExecutionConfig by design:
     * conversation content must not vanish with the execution-profile
     * kill-switch (DD-006 D-a).
     * </pre>
     *
     * <code>.ai.stigmer.agentic.agentexecution.v1.ConversationCatchup conversation_catchup = 14 [json_name = "conversationCatchup"];</code>
     */
    public ai.stigmer.agentic.agentexecution.v1.ConversationCatchup.Builder getConversationCatchupBuilder() {
      bitField0_ |= 0x00002000;
      onChanged();
      return internalGetConversationCatchupFieldBuilder().getBuilder();
    }
    /**
     * <pre>
     * Conversation events the agent has not yet seen, composed by the platform
     * for this turn (optional).
     *
     * Set on live channel-conversation turns (WhatsApp, Slack): when a human
     * teammate handled the conversation or messages otherwise landed while the
     * agent was not watching, the digest carries what happened so the agent
     * re-enters informed. Absent on every other execution surface.
     *
     * &#64;internal
     * Composed per turn by the cloud channel runtime — ChannelSessionBroker
     * over the DD-004 timeline stitch, windowed by the conversation's
     * agent_witnessed_through watermark (channel-conversations DD-006/DD-007).
     * The OSS server never sets it. The runner prepends the framed digest to
     * the turn's user message on both harnesses (A27) and must never read
     * window_end. Top-level rather than inside ExecutionConfig by design:
     * conversation content must not vanish with the execution-profile
     * kill-switch (DD-006 D-a).
     * </pre>
     *
     * <code>.ai.stigmer.agentic.agentexecution.v1.ConversationCatchup conversation_catchup = 14 [json_name = "conversationCatchup"];</code>
     */
    public ai.stigmer.agentic.agentexecution.v1.ConversationCatchupOrBuilder getConversationCatchupOrBuilder() {
      if (conversationCatchupBuilder_ != null) {
        return conversationCatchupBuilder_.getMessageOrBuilder();
      } else {
        return conversationCatchup_ == null ?
            ai.stigmer.agentic.agentexecution.v1.ConversationCatchup.getDefaultInstance() : conversationCatchup_;
      }
    }
    /**
     * <pre>
     * Conversation events the agent has not yet seen, composed by the platform
     * for this turn (optional).
     *
     * Set on live channel-conversation turns (WhatsApp, Slack): when a human
     * teammate handled the conversation or messages otherwise landed while the
     * agent was not watching, the digest carries what happened so the agent
     * re-enters informed. Absent on every other execution surface.
     *
     * &#64;internal
     * Composed per turn by the cloud channel runtime — ChannelSessionBroker
     * over the DD-004 timeline stitch, windowed by the conversation's
     * agent_witnessed_through watermark (channel-conversations DD-006/DD-007).
     * The OSS server never sets it. The runner prepends the framed digest to
     * the turn's user message on both harnesses (A27) and must never read
     * window_end. Top-level rather than inside ExecutionConfig by design:
     * conversation content must not vanish with the execution-profile
     * kill-switch (DD-006 D-a).
     * </pre>
     *
     * <code>.ai.stigmer.agentic.agentexecution.v1.ConversationCatchup conversation_catchup = 14 [json_name = "conversationCatchup"];</code>
     */
    private com.google.protobuf.SingleFieldBuilder<
        ai.stigmer.agentic.agentexecution.v1.ConversationCatchup, ai.stigmer.agentic.agentexecution.v1.ConversationCatchup.Builder, ai.stigmer.agentic.agentexecution.v1.ConversationCatchupOrBuilder> 
        internalGetConversationCatchupFieldBuilder() {
      if (conversationCatchupBuilder_ == null) {
        conversationCatchupBuilder_ = new com.google.protobuf.SingleFieldBuilder<
            ai.stigmer.agentic.agentexecution.v1.ConversationCatchup, ai.stigmer.agentic.agentexecution.v1.ConversationCatchup.Builder, ai.stigmer.agentic.agentexecution.v1.ConversationCatchupOrBuilder>(
                getConversationCatchup(),
                getParentForChildren(),
                isClean());
        conversationCatchup_ = null;
      }
      return conversationCatchupBuilder_;
    }

    private ai.stigmer.agentic.agentexecution.v1.DeclaredPreferences declaredPreferences_;
    private com.google.protobuf.SingleFieldBuilder<
        ai.stigmer.agentic.agentexecution.v1.DeclaredPreferences, ai.stigmer.agentic.agentexecution.v1.DeclaredPreferences.Builder, ai.stigmer.agentic.agentexecution.v1.DeclaredPreferencesOrBuilder> declaredPreferencesBuilder_;
    /**
     * <pre>
     * Standing preferences declared by the organization and the calling user,
     * snapshotted into this execution at create time (optional).
     *
     * &#64;internal
     * Server-owned: the create pipeline stamps this field UNCONDITIONALLY —
     * empty for non-eligible callers — overwriting any caller-supplied value,
     * so external injection is moot by construction. Composed only for
     * first-party human operators (DD-002 D4 as amended 2026-08-15: never for
     * machine, impersonated, guest, channel, schedule, runner, or
     * platform-client-user callers). The OSS server composes org_context only
     * (single-user local mode; no IdentityAccount). A snapshot by design:
     * preferences are mutable, executions are immutable audit records — this
     * field records what the model actually saw. Composition is best-effort;
     * a preference load failure never fails the create. Top-level rather than
     * inside ExecutionConfig: preference content must not vanish with the
     * execution-profile kill-switch (same rationale as conversation_catchup).
     * </pre>
     *
     * <code>.ai.stigmer.agentic.agentexecution.v1.DeclaredPreferences declared_preferences = 15 [json_name = "declaredPreferences"];</code>
     * @return Whether the declaredPreferences field is set.
     */
    public boolean hasDeclaredPreferences() {
      return ((bitField0_ & 0x00004000) != 0);
    }
    /**
     * <pre>
     * Standing preferences declared by the organization and the calling user,
     * snapshotted into this execution at create time (optional).
     *
     * &#64;internal
     * Server-owned: the create pipeline stamps this field UNCONDITIONALLY —
     * empty for non-eligible callers — overwriting any caller-supplied value,
     * so external injection is moot by construction. Composed only for
     * first-party human operators (DD-002 D4 as amended 2026-08-15: never for
     * machine, impersonated, guest, channel, schedule, runner, or
     * platform-client-user callers). The OSS server composes org_context only
     * (single-user local mode; no IdentityAccount). A snapshot by design:
     * preferences are mutable, executions are immutable audit records — this
     * field records what the model actually saw. Composition is best-effort;
     * a preference load failure never fails the create. Top-level rather than
     * inside ExecutionConfig: preference content must not vanish with the
     * execution-profile kill-switch (same rationale as conversation_catchup).
     * </pre>
     *
     * <code>.ai.stigmer.agentic.agentexecution.v1.DeclaredPreferences declared_preferences = 15 [json_name = "declaredPreferences"];</code>
     * @return The declaredPreferences.
     */
    public ai.stigmer.agentic.agentexecution.v1.DeclaredPreferences getDeclaredPreferences() {
      if (declaredPreferencesBuilder_ == null) {
        return declaredPreferences_ == null ? ai.stigmer.agentic.agentexecution.v1.DeclaredPreferences.getDefaultInstance() : declaredPreferences_;
      } else {
        return declaredPreferencesBuilder_.getMessage();
      }
    }
    /**
     * <pre>
     * Standing preferences declared by the organization and the calling user,
     * snapshotted into this execution at create time (optional).
     *
     * &#64;internal
     * Server-owned: the create pipeline stamps this field UNCONDITIONALLY —
     * empty for non-eligible callers — overwriting any caller-supplied value,
     * so external injection is moot by construction. Composed only for
     * first-party human operators (DD-002 D4 as amended 2026-08-15: never for
     * machine, impersonated, guest, channel, schedule, runner, or
     * platform-client-user callers). The OSS server composes org_context only
     * (single-user local mode; no IdentityAccount). A snapshot by design:
     * preferences are mutable, executions are immutable audit records — this
     * field records what the model actually saw. Composition is best-effort;
     * a preference load failure never fails the create. Top-level rather than
     * inside ExecutionConfig: preference content must not vanish with the
     * execution-profile kill-switch (same rationale as conversation_catchup).
     * </pre>
     *
     * <code>.ai.stigmer.agentic.agentexecution.v1.DeclaredPreferences declared_preferences = 15 [json_name = "declaredPreferences"];</code>
     */
    public Builder setDeclaredPreferences(ai.stigmer.agentic.agentexecution.v1.DeclaredPreferences value) {
      if (declaredPreferencesBuilder_ == null) {
        if (value == null) {
          throw new NullPointerException();
        }
        declaredPreferences_ = value;
      } else {
        declaredPreferencesBuilder_.setMessage(value);
      }
      bitField0_ |= 0x00004000;
      onChanged();
      return this;
    }
    /**
     * <pre>
     * Standing preferences declared by the organization and the calling user,
     * snapshotted into this execution at create time (optional).
     *
     * &#64;internal
     * Server-owned: the create pipeline stamps this field UNCONDITIONALLY —
     * empty for non-eligible callers — overwriting any caller-supplied value,
     * so external injection is moot by construction. Composed only for
     * first-party human operators (DD-002 D4 as amended 2026-08-15: never for
     * machine, impersonated, guest, channel, schedule, runner, or
     * platform-client-user callers). The OSS server composes org_context only
     * (single-user local mode; no IdentityAccount). A snapshot by design:
     * preferences are mutable, executions are immutable audit records — this
     * field records what the model actually saw. Composition is best-effort;
     * a preference load failure never fails the create. Top-level rather than
     * inside ExecutionConfig: preference content must not vanish with the
     * execution-profile kill-switch (same rationale as conversation_catchup).
     * </pre>
     *
     * <code>.ai.stigmer.agentic.agentexecution.v1.DeclaredPreferences declared_preferences = 15 [json_name = "declaredPreferences"];</code>
     */
    public Builder setDeclaredPreferences(
        ai.stigmer.agentic.agentexecution.v1.DeclaredPreferences.Builder builderForValue) {
      if (declaredPreferencesBuilder_ == null) {
        declaredPreferences_ = builderForValue.build();
      } else {
        declaredPreferencesBuilder_.setMessage(builderForValue.build());
      }
      bitField0_ |= 0x00004000;
      onChanged();
      return this;
    }
    /**
     * <pre>
     * Standing preferences declared by the organization and the calling user,
     * snapshotted into this execution at create time (optional).
     *
     * &#64;internal
     * Server-owned: the create pipeline stamps this field UNCONDITIONALLY —
     * empty for non-eligible callers — overwriting any caller-supplied value,
     * so external injection is moot by construction. Composed only for
     * first-party human operators (DD-002 D4 as amended 2026-08-15: never for
     * machine, impersonated, guest, channel, schedule, runner, or
     * platform-client-user callers). The OSS server composes org_context only
     * (single-user local mode; no IdentityAccount). A snapshot by design:
     * preferences are mutable, executions are immutable audit records — this
     * field records what the model actually saw. Composition is best-effort;
     * a preference load failure never fails the create. Top-level rather than
     * inside ExecutionConfig: preference content must not vanish with the
     * execution-profile kill-switch (same rationale as conversation_catchup).
     * </pre>
     *
     * <code>.ai.stigmer.agentic.agentexecution.v1.DeclaredPreferences declared_preferences = 15 [json_name = "declaredPreferences"];</code>
     */
    public Builder mergeDeclaredPreferences(ai.stigmer.agentic.agentexecution.v1.DeclaredPreferences value) {
      if (declaredPreferencesBuilder_ == null) {
        if (((bitField0_ & 0x00004000) != 0) &&
          declaredPreferences_ != null &&
          declaredPreferences_ != ai.stigmer.agentic.agentexecution.v1.DeclaredPreferences.getDefaultInstance()) {
          getDeclaredPreferencesBuilder().mergeFrom(value);
        } else {
          declaredPreferences_ = value;
        }
      } else {
        declaredPreferencesBuilder_.mergeFrom(value);
      }
      if (declaredPreferences_ != null) {
        bitField0_ |= 0x00004000;
        onChanged();
      }
      return this;
    }
    /**
     * <pre>
     * Standing preferences declared by the organization and the calling user,
     * snapshotted into this execution at create time (optional).
     *
     * &#64;internal
     * Server-owned: the create pipeline stamps this field UNCONDITIONALLY —
     * empty for non-eligible callers — overwriting any caller-supplied value,
     * so external injection is moot by construction. Composed only for
     * first-party human operators (DD-002 D4 as amended 2026-08-15: never for
     * machine, impersonated, guest, channel, schedule, runner, or
     * platform-client-user callers). The OSS server composes org_context only
     * (single-user local mode; no IdentityAccount). A snapshot by design:
     * preferences are mutable, executions are immutable audit records — this
     * field records what the model actually saw. Composition is best-effort;
     * a preference load failure never fails the create. Top-level rather than
     * inside ExecutionConfig: preference content must not vanish with the
     * execution-profile kill-switch (same rationale as conversation_catchup).
     * </pre>
     *
     * <code>.ai.stigmer.agentic.agentexecution.v1.DeclaredPreferences declared_preferences = 15 [json_name = "declaredPreferences"];</code>
     */
    public Builder clearDeclaredPreferences() {
      bitField0_ = (bitField0_ & ~0x00004000);
      declaredPreferences_ = null;
      if (declaredPreferencesBuilder_ != null) {
        declaredPreferencesBuilder_.dispose();
        declaredPreferencesBuilder_ = null;
      }
      onChanged();
      return this;
    }
    /**
     * <pre>
     * Standing preferences declared by the organization and the calling user,
     * snapshotted into this execution at create time (optional).
     *
     * &#64;internal
     * Server-owned: the create pipeline stamps this field UNCONDITIONALLY —
     * empty for non-eligible callers — overwriting any caller-supplied value,
     * so external injection is moot by construction. Composed only for
     * first-party human operators (DD-002 D4 as amended 2026-08-15: never for
     * machine, impersonated, guest, channel, schedule, runner, or
     * platform-client-user callers). The OSS server composes org_context only
     * (single-user local mode; no IdentityAccount). A snapshot by design:
     * preferences are mutable, executions are immutable audit records — this
     * field records what the model actually saw. Composition is best-effort;
     * a preference load failure never fails the create. Top-level rather than
     * inside ExecutionConfig: preference content must not vanish with the
     * execution-profile kill-switch (same rationale as conversation_catchup).
     * </pre>
     *
     * <code>.ai.stigmer.agentic.agentexecution.v1.DeclaredPreferences declared_preferences = 15 [json_name = "declaredPreferences"];</code>
     */
    public ai.stigmer.agentic.agentexecution.v1.DeclaredPreferences.Builder getDeclaredPreferencesBuilder() {
      bitField0_ |= 0x00004000;
      onChanged();
      return internalGetDeclaredPreferencesFieldBuilder().getBuilder();
    }
    /**
     * <pre>
     * Standing preferences declared by the organization and the calling user,
     * snapshotted into this execution at create time (optional).
     *
     * &#64;internal
     * Server-owned: the create pipeline stamps this field UNCONDITIONALLY —
     * empty for non-eligible callers — overwriting any caller-supplied value,
     * so external injection is moot by construction. Composed only for
     * first-party human operators (DD-002 D4 as amended 2026-08-15: never for
     * machine, impersonated, guest, channel, schedule, runner, or
     * platform-client-user callers). The OSS server composes org_context only
     * (single-user local mode; no IdentityAccount). A snapshot by design:
     * preferences are mutable, executions are immutable audit records — this
     * field records what the model actually saw. Composition is best-effort;
     * a preference load failure never fails the create. Top-level rather than
     * inside ExecutionConfig: preference content must not vanish with the
     * execution-profile kill-switch (same rationale as conversation_catchup).
     * </pre>
     *
     * <code>.ai.stigmer.agentic.agentexecution.v1.DeclaredPreferences declared_preferences = 15 [json_name = "declaredPreferences"];</code>
     */
    public ai.stigmer.agentic.agentexecution.v1.DeclaredPreferencesOrBuilder getDeclaredPreferencesOrBuilder() {
      if (declaredPreferencesBuilder_ != null) {
        return declaredPreferencesBuilder_.getMessageOrBuilder();
      } else {
        return declaredPreferences_ == null ?
            ai.stigmer.agentic.agentexecution.v1.DeclaredPreferences.getDefaultInstance() : declaredPreferences_;
      }
    }
    /**
     * <pre>
     * Standing preferences declared by the organization and the calling user,
     * snapshotted into this execution at create time (optional).
     *
     * &#64;internal
     * Server-owned: the create pipeline stamps this field UNCONDITIONALLY —
     * empty for non-eligible callers — overwriting any caller-supplied value,
     * so external injection is moot by construction. Composed only for
     * first-party human operators (DD-002 D4 as amended 2026-08-15: never for
     * machine, impersonated, guest, channel, schedule, runner, or
     * platform-client-user callers). The OSS server composes org_context only
     * (single-user local mode; no IdentityAccount). A snapshot by design:
     * preferences are mutable, executions are immutable audit records — this
     * field records what the model actually saw. Composition is best-effort;
     * a preference load failure never fails the create. Top-level rather than
     * inside ExecutionConfig: preference content must not vanish with the
     * execution-profile kill-switch (same rationale as conversation_catchup).
     * </pre>
     *
     * <code>.ai.stigmer.agentic.agentexecution.v1.DeclaredPreferences declared_preferences = 15 [json_name = "declaredPreferences"];</code>
     */
    private com.google.protobuf.SingleFieldBuilder<
        ai.stigmer.agentic.agentexecution.v1.DeclaredPreferences, ai.stigmer.agentic.agentexecution.v1.DeclaredPreferences.Builder, ai.stigmer.agentic.agentexecution.v1.DeclaredPreferencesOrBuilder> 
        internalGetDeclaredPreferencesFieldBuilder() {
      if (declaredPreferencesBuilder_ == null) {
        declaredPreferencesBuilder_ = new com.google.protobuf.SingleFieldBuilder<
            ai.stigmer.agentic.agentexecution.v1.DeclaredPreferences, ai.stigmer.agentic.agentexecution.v1.DeclaredPreferences.Builder, ai.stigmer.agentic.agentexecution.v1.DeclaredPreferencesOrBuilder>(
                getDeclaredPreferences(),
                getParentForChildren(),
                isClean());
        declaredPreferences_ = null;
      }
      return declaredPreferencesBuilder_;
    }

    private ai.stigmer.agentic.agentexecution.v1.RecalledMemories recalledMemories_;
    private com.google.protobuf.SingleFieldBuilder<
        ai.stigmer.agentic.agentexecution.v1.RecalledMemories, ai.stigmer.agentic.agentexecution.v1.RecalledMemories.Builder, ai.stigmer.agentic.agentexecution.v1.RecalledMemoriesOrBuilder> recalledMemoriesBuilder_;
    /**
     * <pre>
     * The caller's confirmed memories, snapshotted into this execution at
     * create time (optional).
     *
     * &#64;internal
     * Every DD-002 D2 invariant carries over from declared_preferences
     * verbatim (DD-006 D2): SERVER-OWNED (the create pipeline stamps this
     * field unconditionally — enabled=false with no facts for excluded
     * callers — overwriting any caller-supplied value, so injection via
     * the create request is moot by construction); composed only for
     * first-party human operators with BOTH memory_enabled flags on (org
     * AND caller; OSS: the org flag alone); confirmed records only —
     * proposed and rejected are never injected (the consent gate is
     * meaningless otherwise); a SNAPSHOT (memories are mutable, executions
     * are immutable audit records — this field records the CANDIDATE set
     * the prompt was built from, and each fact carries its memory_id so
     * the audit links back to the addressable record; what the model
     * actually saw is this snapshot joined to
     * status.recalled_memories_report, whose absence means wholesale —
     * the full set — by construction, DD-008 D5); best-effort (a memory
     * load failure degrades to disabled, never fails the create);
     * top-level rather than inside ExecutionConfig (must not vanish with
     * the execution-profile kill-switch). The enabled bit doubles as the
     * runner's signal to offer the remember tool (DD-005 D1) — one
     * server-owned field, one writer, no parallel flag. Composed by
     * Stage 2's compose steps; the field is reserved here by Stage 1 so
     * all Phase 2 contract changes land in one codegen pass.
     * </pre>
     *
     * <code>.ai.stigmer.agentic.agentexecution.v1.RecalledMemories recalled_memories = 16 [json_name = "recalledMemories"];</code>
     * @return Whether the recalledMemories field is set.
     */
    public boolean hasRecalledMemories() {
      return ((bitField0_ & 0x00008000) != 0);
    }
    /**
     * <pre>
     * The caller's confirmed memories, snapshotted into this execution at
     * create time (optional).
     *
     * &#64;internal
     * Every DD-002 D2 invariant carries over from declared_preferences
     * verbatim (DD-006 D2): SERVER-OWNED (the create pipeline stamps this
     * field unconditionally — enabled=false with no facts for excluded
     * callers — overwriting any caller-supplied value, so injection via
     * the create request is moot by construction); composed only for
     * first-party human operators with BOTH memory_enabled flags on (org
     * AND caller; OSS: the org flag alone); confirmed records only —
     * proposed and rejected are never injected (the consent gate is
     * meaningless otherwise); a SNAPSHOT (memories are mutable, executions
     * are immutable audit records — this field records the CANDIDATE set
     * the prompt was built from, and each fact carries its memory_id so
     * the audit links back to the addressable record; what the model
     * actually saw is this snapshot joined to
     * status.recalled_memories_report, whose absence means wholesale —
     * the full set — by construction, DD-008 D5); best-effort (a memory
     * load failure degrades to disabled, never fails the create);
     * top-level rather than inside ExecutionConfig (must not vanish with
     * the execution-profile kill-switch). The enabled bit doubles as the
     * runner's signal to offer the remember tool (DD-005 D1) — one
     * server-owned field, one writer, no parallel flag. Composed by
     * Stage 2's compose steps; the field is reserved here by Stage 1 so
     * all Phase 2 contract changes land in one codegen pass.
     * </pre>
     *
     * <code>.ai.stigmer.agentic.agentexecution.v1.RecalledMemories recalled_memories = 16 [json_name = "recalledMemories"];</code>
     * @return The recalledMemories.
     */
    public ai.stigmer.agentic.agentexecution.v1.RecalledMemories getRecalledMemories() {
      if (recalledMemoriesBuilder_ == null) {
        return recalledMemories_ == null ? ai.stigmer.agentic.agentexecution.v1.RecalledMemories.getDefaultInstance() : recalledMemories_;
      } else {
        return recalledMemoriesBuilder_.getMessage();
      }
    }
    /**
     * <pre>
     * The caller's confirmed memories, snapshotted into this execution at
     * create time (optional).
     *
     * &#64;internal
     * Every DD-002 D2 invariant carries over from declared_preferences
     * verbatim (DD-006 D2): SERVER-OWNED (the create pipeline stamps this
     * field unconditionally — enabled=false with no facts for excluded
     * callers — overwriting any caller-supplied value, so injection via
     * the create request is moot by construction); composed only for
     * first-party human operators with BOTH memory_enabled flags on (org
     * AND caller; OSS: the org flag alone); confirmed records only —
     * proposed and rejected are never injected (the consent gate is
     * meaningless otherwise); a SNAPSHOT (memories are mutable, executions
     * are immutable audit records — this field records the CANDIDATE set
     * the prompt was built from, and each fact carries its memory_id so
     * the audit links back to the addressable record; what the model
     * actually saw is this snapshot joined to
     * status.recalled_memories_report, whose absence means wholesale —
     * the full set — by construction, DD-008 D5); best-effort (a memory
     * load failure degrades to disabled, never fails the create);
     * top-level rather than inside ExecutionConfig (must not vanish with
     * the execution-profile kill-switch). The enabled bit doubles as the
     * runner's signal to offer the remember tool (DD-005 D1) — one
     * server-owned field, one writer, no parallel flag. Composed by
     * Stage 2's compose steps; the field is reserved here by Stage 1 so
     * all Phase 2 contract changes land in one codegen pass.
     * </pre>
     *
     * <code>.ai.stigmer.agentic.agentexecution.v1.RecalledMemories recalled_memories = 16 [json_name = "recalledMemories"];</code>
     */
    public Builder setRecalledMemories(ai.stigmer.agentic.agentexecution.v1.RecalledMemories value) {
      if (recalledMemoriesBuilder_ == null) {
        if (value == null) {
          throw new NullPointerException();
        }
        recalledMemories_ = value;
      } else {
        recalledMemoriesBuilder_.setMessage(value);
      }
      bitField0_ |= 0x00008000;
      onChanged();
      return this;
    }
    /**
     * <pre>
     * The caller's confirmed memories, snapshotted into this execution at
     * create time (optional).
     *
     * &#64;internal
     * Every DD-002 D2 invariant carries over from declared_preferences
     * verbatim (DD-006 D2): SERVER-OWNED (the create pipeline stamps this
     * field unconditionally — enabled=false with no facts for excluded
     * callers — overwriting any caller-supplied value, so injection via
     * the create request is moot by construction); composed only for
     * first-party human operators with BOTH memory_enabled flags on (org
     * AND caller; OSS: the org flag alone); confirmed records only —
     * proposed and rejected are never injected (the consent gate is
     * meaningless otherwise); a SNAPSHOT (memories are mutable, executions
     * are immutable audit records — this field records the CANDIDATE set
     * the prompt was built from, and each fact carries its memory_id so
     * the audit links back to the addressable record; what the model
     * actually saw is this snapshot joined to
     * status.recalled_memories_report, whose absence means wholesale —
     * the full set — by construction, DD-008 D5); best-effort (a memory
     * load failure degrades to disabled, never fails the create);
     * top-level rather than inside ExecutionConfig (must not vanish with
     * the execution-profile kill-switch). The enabled bit doubles as the
     * runner's signal to offer the remember tool (DD-005 D1) — one
     * server-owned field, one writer, no parallel flag. Composed by
     * Stage 2's compose steps; the field is reserved here by Stage 1 so
     * all Phase 2 contract changes land in one codegen pass.
     * </pre>
     *
     * <code>.ai.stigmer.agentic.agentexecution.v1.RecalledMemories recalled_memories = 16 [json_name = "recalledMemories"];</code>
     */
    public Builder setRecalledMemories(
        ai.stigmer.agentic.agentexecution.v1.RecalledMemories.Builder builderForValue) {
      if (recalledMemoriesBuilder_ == null) {
        recalledMemories_ = builderForValue.build();
      } else {
        recalledMemoriesBuilder_.setMessage(builderForValue.build());
      }
      bitField0_ |= 0x00008000;
      onChanged();
      return this;
    }
    /**
     * <pre>
     * The caller's confirmed memories, snapshotted into this execution at
     * create time (optional).
     *
     * &#64;internal
     * Every DD-002 D2 invariant carries over from declared_preferences
     * verbatim (DD-006 D2): SERVER-OWNED (the create pipeline stamps this
     * field unconditionally — enabled=false with no facts for excluded
     * callers — overwriting any caller-supplied value, so injection via
     * the create request is moot by construction); composed only for
     * first-party human operators with BOTH memory_enabled flags on (org
     * AND caller; OSS: the org flag alone); confirmed records only —
     * proposed and rejected are never injected (the consent gate is
     * meaningless otherwise); a SNAPSHOT (memories are mutable, executions
     * are immutable audit records — this field records the CANDIDATE set
     * the prompt was built from, and each fact carries its memory_id so
     * the audit links back to the addressable record; what the model
     * actually saw is this snapshot joined to
     * status.recalled_memories_report, whose absence means wholesale —
     * the full set — by construction, DD-008 D5); best-effort (a memory
     * load failure degrades to disabled, never fails the create);
     * top-level rather than inside ExecutionConfig (must not vanish with
     * the execution-profile kill-switch). The enabled bit doubles as the
     * runner's signal to offer the remember tool (DD-005 D1) — one
     * server-owned field, one writer, no parallel flag. Composed by
     * Stage 2's compose steps; the field is reserved here by Stage 1 so
     * all Phase 2 contract changes land in one codegen pass.
     * </pre>
     *
     * <code>.ai.stigmer.agentic.agentexecution.v1.RecalledMemories recalled_memories = 16 [json_name = "recalledMemories"];</code>
     */
    public Builder mergeRecalledMemories(ai.stigmer.agentic.agentexecution.v1.RecalledMemories value) {
      if (recalledMemoriesBuilder_ == null) {
        if (((bitField0_ & 0x00008000) != 0) &&
          recalledMemories_ != null &&
          recalledMemories_ != ai.stigmer.agentic.agentexecution.v1.RecalledMemories.getDefaultInstance()) {
          getRecalledMemoriesBuilder().mergeFrom(value);
        } else {
          recalledMemories_ = value;
        }
      } else {
        recalledMemoriesBuilder_.mergeFrom(value);
      }
      if (recalledMemories_ != null) {
        bitField0_ |= 0x00008000;
        onChanged();
      }
      return this;
    }
    /**
     * <pre>
     * The caller's confirmed memories, snapshotted into this execution at
     * create time (optional).
     *
     * &#64;internal
     * Every DD-002 D2 invariant carries over from declared_preferences
     * verbatim (DD-006 D2): SERVER-OWNED (the create pipeline stamps this
     * field unconditionally — enabled=false with no facts for excluded
     * callers — overwriting any caller-supplied value, so injection via
     * the create request is moot by construction); composed only for
     * first-party human operators with BOTH memory_enabled flags on (org
     * AND caller; OSS: the org flag alone); confirmed records only —
     * proposed and rejected are never injected (the consent gate is
     * meaningless otherwise); a SNAPSHOT (memories are mutable, executions
     * are immutable audit records — this field records the CANDIDATE set
     * the prompt was built from, and each fact carries its memory_id so
     * the audit links back to the addressable record; what the model
     * actually saw is this snapshot joined to
     * status.recalled_memories_report, whose absence means wholesale —
     * the full set — by construction, DD-008 D5); best-effort (a memory
     * load failure degrades to disabled, never fails the create);
     * top-level rather than inside ExecutionConfig (must not vanish with
     * the execution-profile kill-switch). The enabled bit doubles as the
     * runner's signal to offer the remember tool (DD-005 D1) — one
     * server-owned field, one writer, no parallel flag. Composed by
     * Stage 2's compose steps; the field is reserved here by Stage 1 so
     * all Phase 2 contract changes land in one codegen pass.
     * </pre>
     *
     * <code>.ai.stigmer.agentic.agentexecution.v1.RecalledMemories recalled_memories = 16 [json_name = "recalledMemories"];</code>
     */
    public Builder clearRecalledMemories() {
      bitField0_ = (bitField0_ & ~0x00008000);
      recalledMemories_ = null;
      if (recalledMemoriesBuilder_ != null) {
        recalledMemoriesBuilder_.dispose();
        recalledMemoriesBuilder_ = null;
      }
      onChanged();
      return this;
    }
    /**
     * <pre>
     * The caller's confirmed memories, snapshotted into this execution at
     * create time (optional).
     *
     * &#64;internal
     * Every DD-002 D2 invariant carries over from declared_preferences
     * verbatim (DD-006 D2): SERVER-OWNED (the create pipeline stamps this
     * field unconditionally — enabled=false with no facts for excluded
     * callers — overwriting any caller-supplied value, so injection via
     * the create request is moot by construction); composed only for
     * first-party human operators with BOTH memory_enabled flags on (org
     * AND caller; OSS: the org flag alone); confirmed records only —
     * proposed and rejected are never injected (the consent gate is
     * meaningless otherwise); a SNAPSHOT (memories are mutable, executions
     * are immutable audit records — this field records the CANDIDATE set
     * the prompt was built from, and each fact carries its memory_id so
     * the audit links back to the addressable record; what the model
     * actually saw is this snapshot joined to
     * status.recalled_memories_report, whose absence means wholesale —
     * the full set — by construction, DD-008 D5); best-effort (a memory
     * load failure degrades to disabled, never fails the create);
     * top-level rather than inside ExecutionConfig (must not vanish with
     * the execution-profile kill-switch). The enabled bit doubles as the
     * runner's signal to offer the remember tool (DD-005 D1) — one
     * server-owned field, one writer, no parallel flag. Composed by
     * Stage 2's compose steps; the field is reserved here by Stage 1 so
     * all Phase 2 contract changes land in one codegen pass.
     * </pre>
     *
     * <code>.ai.stigmer.agentic.agentexecution.v1.RecalledMemories recalled_memories = 16 [json_name = "recalledMemories"];</code>
     */
    public ai.stigmer.agentic.agentexecution.v1.RecalledMemories.Builder getRecalledMemoriesBuilder() {
      bitField0_ |= 0x00008000;
      onChanged();
      return internalGetRecalledMemoriesFieldBuilder().getBuilder();
    }
    /**
     * <pre>
     * The caller's confirmed memories, snapshotted into this execution at
     * create time (optional).
     *
     * &#64;internal
     * Every DD-002 D2 invariant carries over from declared_preferences
     * verbatim (DD-006 D2): SERVER-OWNED (the create pipeline stamps this
     * field unconditionally — enabled=false with no facts for excluded
     * callers — overwriting any caller-supplied value, so injection via
     * the create request is moot by construction); composed only for
     * first-party human operators with BOTH memory_enabled flags on (org
     * AND caller; OSS: the org flag alone); confirmed records only —
     * proposed and rejected are never injected (the consent gate is
     * meaningless otherwise); a SNAPSHOT (memories are mutable, executions
     * are immutable audit records — this field records the CANDIDATE set
     * the prompt was built from, and each fact carries its memory_id so
     * the audit links back to the addressable record; what the model
     * actually saw is this snapshot joined to
     * status.recalled_memories_report, whose absence means wholesale —
     * the full set — by construction, DD-008 D5); best-effort (a memory
     * load failure degrades to disabled, never fails the create);
     * top-level rather than inside ExecutionConfig (must not vanish with
     * the execution-profile kill-switch). The enabled bit doubles as the
     * runner's signal to offer the remember tool (DD-005 D1) — one
     * server-owned field, one writer, no parallel flag. Composed by
     * Stage 2's compose steps; the field is reserved here by Stage 1 so
     * all Phase 2 contract changes land in one codegen pass.
     * </pre>
     *
     * <code>.ai.stigmer.agentic.agentexecution.v1.RecalledMemories recalled_memories = 16 [json_name = "recalledMemories"];</code>
     */
    public ai.stigmer.agentic.agentexecution.v1.RecalledMemoriesOrBuilder getRecalledMemoriesOrBuilder() {
      if (recalledMemoriesBuilder_ != null) {
        return recalledMemoriesBuilder_.getMessageOrBuilder();
      } else {
        return recalledMemories_ == null ?
            ai.stigmer.agentic.agentexecution.v1.RecalledMemories.getDefaultInstance() : recalledMemories_;
      }
    }
    /**
     * <pre>
     * The caller's confirmed memories, snapshotted into this execution at
     * create time (optional).
     *
     * &#64;internal
     * Every DD-002 D2 invariant carries over from declared_preferences
     * verbatim (DD-006 D2): SERVER-OWNED (the create pipeline stamps this
     * field unconditionally — enabled=false with no facts for excluded
     * callers — overwriting any caller-supplied value, so injection via
     * the create request is moot by construction); composed only for
     * first-party human operators with BOTH memory_enabled flags on (org
     * AND caller; OSS: the org flag alone); confirmed records only —
     * proposed and rejected are never injected (the consent gate is
     * meaningless otherwise); a SNAPSHOT (memories are mutable, executions
     * are immutable audit records — this field records the CANDIDATE set
     * the prompt was built from, and each fact carries its memory_id so
     * the audit links back to the addressable record; what the model
     * actually saw is this snapshot joined to
     * status.recalled_memories_report, whose absence means wholesale —
     * the full set — by construction, DD-008 D5); best-effort (a memory
     * load failure degrades to disabled, never fails the create);
     * top-level rather than inside ExecutionConfig (must not vanish with
     * the execution-profile kill-switch). The enabled bit doubles as the
     * runner's signal to offer the remember tool (DD-005 D1) — one
     * server-owned field, one writer, no parallel flag. Composed by
     * Stage 2's compose steps; the field is reserved here by Stage 1 so
     * all Phase 2 contract changes land in one codegen pass.
     * </pre>
     *
     * <code>.ai.stigmer.agentic.agentexecution.v1.RecalledMemories recalled_memories = 16 [json_name = "recalledMemories"];</code>
     */
    private com.google.protobuf.SingleFieldBuilder<
        ai.stigmer.agentic.agentexecution.v1.RecalledMemories, ai.stigmer.agentic.agentexecution.v1.RecalledMemories.Builder, ai.stigmer.agentic.agentexecution.v1.RecalledMemoriesOrBuilder> 
        internalGetRecalledMemoriesFieldBuilder() {
      if (recalledMemoriesBuilder_ == null) {
        recalledMemoriesBuilder_ = new com.google.protobuf.SingleFieldBuilder<
            ai.stigmer.agentic.agentexecution.v1.RecalledMemories, ai.stigmer.agentic.agentexecution.v1.RecalledMemories.Builder, ai.stigmer.agentic.agentexecution.v1.RecalledMemoriesOrBuilder>(
                getRecalledMemories(),
                getParentForChildren(),
                isClean());
        recalledMemories_ = null;
      }
      return recalledMemoriesBuilder_;
    }

    // @@protoc_insertion_point(builder_scope:ai.stigmer.agentic.agentexecution.v1.AgentExecutionSpec)
  }

  // @@protoc_insertion_point(class_scope:ai.stigmer.agentic.agentexecution.v1.AgentExecutionSpec)
  private static final ai.stigmer.agentic.agentexecution.v1.AgentExecutionSpec DEFAULT_INSTANCE;
  static {
    DEFAULT_INSTANCE = new ai.stigmer.agentic.agentexecution.v1.AgentExecutionSpec();
  }

  public static ai.stigmer.agentic.agentexecution.v1.AgentExecutionSpec getDefaultInstance() {
    return DEFAULT_INSTANCE;
  }

  private static final com.google.protobuf.Parser<AgentExecutionSpec>
      PARSER = new com.google.protobuf.AbstractParser<AgentExecutionSpec>() {
    @java.lang.Override
    public AgentExecutionSpec parsePartialFrom(
        com.google.protobuf.CodedInputStream input,
        com.google.protobuf.ExtensionRegistryLite extensionRegistry)
        throws com.google.protobuf.InvalidProtocolBufferException {
      Builder builder = newBuilder();
      try {
        builder.mergeFrom(input, extensionRegistry);
      } catch (com.google.protobuf.InvalidProtocolBufferException e) {
        throw e.setUnfinishedMessage(builder.buildPartial());
      } catch (com.google.protobuf.UninitializedMessageException e) {
        throw e.asInvalidProtocolBufferException().setUnfinishedMessage(builder.buildPartial());
      } catch (java.io.IOException e) {
        throw new com.google.protobuf.InvalidProtocolBufferException(e)
            .setUnfinishedMessage(builder.buildPartial());
      }
      return builder.buildPartial();
    }
  };

  public static com.google.protobuf.Parser<AgentExecutionSpec> parser() {
    return PARSER;
  }

  @java.lang.Override
  public com.google.protobuf.Parser<AgentExecutionSpec> getParserForType() {
    return PARSER;
  }

  @java.lang.Override
  public ai.stigmer.agentic.agentexecution.v1.AgentExecutionSpec getDefaultInstanceForType() {
    return DEFAULT_INSTANCE;
  }

}

