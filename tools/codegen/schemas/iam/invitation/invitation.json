{
  "name": "InvitationSpec",
  "kind": "INVITATION_SPEC",
  "description": "InvitationSpec defines the user-provided configuration for an invitation.\n\n An invitation is a shareable link that grants a specific role to anyone\n who redeems it. Invitations can be multi-use (persistent org invite link)\n or single-use (targeted invitation for a specific person).\n\n Example YAML (persistent org invite link):\n   apiVersion: iam.stigmer.ai/v1\n   kind: Invitation\n   metadata:\n     name: engineering-team-link\n     org: acme\n   spec:\n     role: viewer\n     max_redemptions: 0\n     expires_at: \"2026-05-06T00:00:00Z\"\n     label: \"Engineering team invite\"\n\n Example YAML (single-use targeted invite):\n   apiVersion: iam.stigmer.ai/v1\n   kind: Invitation\n   metadata:\n     name: contractor-invite\n     org: acme\n   spec:\n     role: member\n     max_redemptions: 1\n     expires_at: \"2026-04-13T00:00:00Z\"\n     label: \"Contractor onboarding\"",
  "protoType": "ai.stigmer.iam.invitation.v1.InvitationSpec",
  "protoFile": "apis/ai/stigmer/iam/invitation/v1/spec.proto",
  "fields": [
    {
      "name": "Role",
      "jsonName": "role",
      "protoField": "role",
      "type": {
        "kind": "string",
        "enumType": "ai.stigmer.iam.v1.IamRole",
        "enumValues": [
          "owner",
          "admin",
          "member",
          "viewer",
          "participant"
        ]
      },
      "description": "The role granted to the identity account upon redemption.\n Must be a role listed in the organization's grantable_roles.",
      "required": true,
      "validation": {
        "required": true
      }
    },
    {
      "name": "MaxRedemptions",
      "jsonName": "maxRedemptions",
      "protoField": "max_redemptions",
      "type": {
        "kind": "int32"
      },
      "description": "Maximum number of times this invitation can be redeemed.\n 0 means unlimited (use for persistent org invite links).\n 1 means single-use (use for targeted invitations).",
      "required": false
    },
    {
      "name": "ExpiresAt",
      "jsonName": "expiresAt",
      "protoField": "expires_at",
      "type": {
        "kind": "timestamp"
      },
      "description": "When this invitation expires. Required.\n The backend enforces a maximum expiration window (configurable per org).",
      "required": true,
      "validation": {
        "required": true
      }
    },
    {
      "name": "Label",
      "jsonName": "label",
      "protoField": "label",
      "type": {
        "kind": "string"
      },
      "description": "Human-readable label for organizational purposes.\n Helps admins distinguish between multiple active invitations.",
      "required": false,
      "validation": {
        "maxLength": 200
      }
    }
  ]
}