{
  "name": "DatastoreSpec",
  "kind": "DATASTORE_SPEC",
  "description": "DatastoreSpec declares typed record collections with constraints,\n role-aware access grants, and seed data.\n\n @internal\n The manifest is authoritative for structure, never for living data:\n schema changes sync on apply (additive-plus change matrix, gating),\n seed records insert once on first materialization, and re-applies\n never mutate records. Structural limits here are v1 guardrails —\n raising a max_items later is a non-breaking change. Max datastores\n per org (25) cannot be expressed on this message; it is a domain\n validation constant in the create pipelines.",
  "protoType": "ai.stigmer.agentic.datastore.v1.DatastoreSpec",
  "protoFile": "apis/ai/stigmer/agentic/datastore/v1/spec.proto",
  "fields": [
    {
      "name": "Description",
      "jsonName": "description",
      "protoField": "description",
      "type": {
        "kind": "string"
      },
      "description": "Human-readable description of what this datastore holds.",
      "required": false
    },
    {
      "name": "Timezone",
      "jsonName": "timezone",
      "protoField": "timezone",
      "type": {
        "kind": "string"
      },
      "description": "IANA timezone (e.g. \"Asia/Kolkata\") for constraint evaluation.\n\n Bound as the constant `tz` in check and exists constraint\n expressions. Optional; when unset, expressions that reference `tz`\n are rejected at apply time.\n\n @internal\n IANA-name validity is domain validation (the proto layer cannot\n carry a timezone database).",
      "required": false,
      "validation": {
        "maxLength": 64
      }
    },
    {
      "name": "Authorization",
      "jsonName": "authorization",
      "protoField": "authorization",
      "type": {
        "kind": "message",
        "messageType": "DatastoreAuthorization"
      },
      "description": "Record-layer authorization: roles, subject bindings, and the\n default role for unbound callers.",
      "required": false
    },
    {
      "name": "Collections",
      "jsonName": "collections",
      "protoField": "collections",
      "type": {
        "kind": "array",
        "elementType": {
          "kind": "message",
          "messageType": "CollectionDeclaration"
        }
      },
      "description": "Record collections declared by this datastore.\n\n @internal\n Collection-name uniqueness within the datastore is domain\n validation (protovalidate CEL has no portable uniqueness check\n over message lists).",
      "required": false,
      "validation": {
        "maxItems": 50
      }
    }
  ]
}