{
  "name": "AgentInvocation",
  "description": "AgentInvocation is what a caller may ask an agent to do.\n\n It is the owner-settable subset of an agent run — the interactive\n composer's vocabulary (agent, message, harness, workspace,\n environments, run bounds) minus what an unattended surface makes\n structurally impossible. Surfaces that trigger agents on someone's\n behalf embed this message instead of re-deriving the shape:\n schedules embed it whole; the workflow agent_call task adopts it at\n the TYPE level (embedding RunConfig and documenting a field-by-field\n correspondence) because its task config is a kind+Struct authoring\n DSL whose field names are the YAML keys — see\n workflow/v1/tasks/agent_call.proto; channels embed RunConfig.",
  "protoType": "ai.stigmer.agentic.agentexecution.v1.AgentInvocation",
  "protoFile": "apis/ai/stigmer/agentic/agentexecution/v1/invocation.proto",
  "fields": [
    {
      "name": "AgentRef",
      "jsonName": "agentRef",
      "protoField": "agent_ref",
      "type": {
        "kind": "message",
        "messageType": "ApiResourceReference"
      },
      "description": "Reference to the agent to run.",
      "required": true,
      "referenceKind": 40,
      "validation": {
        "required": true
      }
    },
    {
      "name": "Message",
      "jsonName": "message",
      "protoField": "message",
      "type": {
        "kind": "string"
      },
      "description": "Prompt the run starts from.",
      "required": false,
      "validation": {
        "minLength": 1,
        "maxLength": 8192
      }
    },
    {
      "name": "Harness",
      "jsonName": "harness",
      "protoField": "harness",
      "type": {
        "kind": "string",
        "enumType": "ai.stigmer.agentic.session.v1.Harness",
        "enumValues": [
          "HARNESS_NATIVE",
          "HARNESS_CURSOR"
        ]
      },
      "description": "Execution engine for the run's session. Unspecified inherits the\n embedding surface's platform default.",
      "required": false
    },
    {
      "name": "WorkspaceEntries",
      "jsonName": "workspaceEntries",
      "protoField": "workspace_entries",
      "type": {
        "kind": "array",
        "elementType": {
          "kind": "message",
          "messageType": "WorkspaceEntry"
        }
      },
      "description": "Workspace the run's session operates on. Empty means no workspace.",
      "required": false
    },
    {
      "name": "EnvironmentRefs",
      "jsonName": "environmentRefs",
      "protoField": "environment_refs",
      "type": {
        "kind": "array",
        "elementType": {
          "kind": "message",
          "messageType": "ApiResourceReference"
        }
      },
      "description": "References to Environment resources whose values are provided to\n the runs this invocation creates.\n\n This is how a tool-using agent becomes runnable unattended: bind\n an org-shared environment holding the needed credentials (for\n example an MCP server's shared secret), and the runs receive its\n values at runtime. The agent and its default instance stay\n untouched.",
      "required": false,
      "referenceKind": 53
    },
    {
      "name": "RunConfig",
      "jsonName": "runConfig",
      "protoField": "run_config",
      "type": {
        "kind": "message",
        "messageType": "RunConfig"
      },
      "description": "Per-invocation model choice and run bounds. Unset fields inherit\n the embedding surface's platform execution profile.",
      "required": false
    }
  ]
}