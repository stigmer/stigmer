{
  "name": "AgentChannelSpec",
  "kind": "AGENT_CHANNEL_SPEC",
  "description": "AgentChannelSpec defines the configurable properties of an agent channel.\n\n The spec is deliberately small: which agent serves the channel, whether\n serving is enabled, which provider the channel targets, and which\n environments supply the agent's tool credentials. Workspace identity and\n provider credentials are produced by the install flow and live in\n status — a declarative apply can never clobber them.",
  "protoType": "ai.stigmer.agentic.agentchannel.v1.AgentChannelSpec",
  "protoFile": "apis/ai/stigmer/agentic/agentchannel/v1/spec.proto",
  "fields": [
    {
      "name": "AgentRef",
      "jsonName": "agentRef",
      "protoField": "agent_ref",
      "type": {
        "kind": "message",
        "messageType": "ApiResourceReference"
      },
      "description": "Reference to the agent this channel serves.",
      "required": true,
      "referenceKind": 40,
      "validation": {
        "required": true
      }
    },
    {
      "name": "Enabled",
      "jsonName": "enabled",
      "protoField": "enabled",
      "type": {
        "kind": "bool"
      },
      "description": "Whether serving traffic on this channel is enabled.\n\n Disabling is a config-preserving pause: the connection keeps its\n install state and credentials, but inbound events are refused until\n re-enabled. Delete the channel instead for a full teardown.",
      "required": false
    },
    {
      "name": "Slack",
      "jsonName": "slack",
      "protoField": "slack",
      "type": {
        "kind": "message",
        "messageType": "SlackChannelConfig"
      },
      "description": "Slack workspace connection (multi-tenant Stigmer Slack app).",
      "required": false,
      "oneofGroup": "provider_config"
    },
    {
      "name": "Whatsapp",
      "jsonName": "whatsapp",
      "protoField": "whatsapp",
      "type": {
        "kind": "message",
        "messageType": "WhatsAppChannelConfig"
      },
      "description": "WhatsApp Business number connection (Meta Cloud API).",
      "required": false,
      "oneofGroup": "provider_config"
    },
    {
      "name": "EnvironmentRefs",
      "jsonName": "environmentRefs",
      "protoField": "environment_refs",
      "type": {
        "kind": "array",
        "elementType": {
          "kind": "message",
          "messageType": "ApiResourceReference"
        }
      },
      "description": "References to Environment resources whose values are provided to\n conversations on this channel.\n\n This is how a tool-using agent becomes chattable over a channel: bind\n an org-shared environment holding the needed credentials (for example\n a read-only API token), and channel executions receive its values at\n runtime. The agent and its default instance stay untouched.",
      "required": false,
      "referenceKind": 53
    },
    {
      "name": "AppRef",
      "jsonName": "appRef",
      "protoField": "app_ref",
      "type": {
        "kind": "message",
        "messageType": "ApiResourceReference"
      },
      "description": "Reference to the ChannelApp this channel installs through.\n\n For Slack, absent means the channel uses the platform's shared\n Stigmer app — the zero-setup default. Set it to install through your\n own provider app instead: the bot carries the app's name and icon,\n and each app is its own bot identity, so multiple agents can serve\n one workspace. For WhatsApp the reference is required — every\n WhatsApp channel installs through your own Meta app (DD-WA-2).",
      "required": false,
      "referenceKind": 48
    },
    {
      "name": "ProactiveMessagingEnabled",
      "jsonName": "proactiveMessagingEnabled",
      "protoField": "proactive_messaging_enabled",
      "type": {
        "kind": "bool"
      },
      "description": "Whether the serving agent may send business-initiated (proactive)\n messages on this channel. Off by default: a channel is reply-only\n until its owner grants this.",
      "required": false
    },
    {
      "name": "RunConfig",
      "jsonName": "runConfig",
      "protoField": "run_config",
      "type": {
        "kind": "message",
        "messageType": "RunConfig"
      },
      "description": "Per-turn model choice and run bounds for conversations on this\n channel, overriding the platform's channel execution profile.\n\n Unset fields inherit the platform default. model_name replaces the\n platform model outright, while max_cost_usd and max_tool_rounds can\n only lower the platform caps — a channel owner can reduce what one\n turn may spend, never raise it past the platform profile.",
      "required": false
    }
  ]
}