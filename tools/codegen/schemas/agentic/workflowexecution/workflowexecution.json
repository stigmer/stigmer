{
  "name": "WorkflowExecutionSpec",
  "kind": "WORKFLOW_EXECUTION_SPEC",
  "description": "WorkflowExecutionSpec defines the user-provided inputs for a workflow execution.",
  "protoType": "ai.stigmer.agentic.workflowexecution.v1.WorkflowExecutionSpec",
  "protoFile": "apis/ai/stigmer/agentic/workflowexecution/v1/spec.proto",
  "fields": [
    {
      "name": "WorkflowInstanceId",
      "jsonName": "workflowInstanceId",
      "protoField": "workflow_instance_id",
      "type": {
        "kind": "string"
      },
      "description": "ID of the WorkflowInstance to execute.",
      "required": false
    },
    {
      "name": "WorkflowId",
      "jsonName": "workflowId",
      "protoField": "workflow_id",
      "type": {
        "kind": "string"
      },
      "description": "ID of the Workflow template to execute (alternative to workflow_instance_id).",
      "required": false
    },
    {
      "name": "TriggerMessage",
      "jsonName": "triggerMessage",
      "protoField": "trigger_message",
      "type": {
        "kind": "string"
      },
      "description": "Input message or payload that triggers the workflow.",
      "required": false
    },
    {
      "name": "TriggerMetadata",
      "jsonName": "triggerMetadata",
      "protoField": "trigger_metadata",
      "type": {
        "kind": "map",
        "keyType": {
          "kind": "string"
        },
        "valueType": {
          "kind": "string"
        }
      },
      "description": "Contextual metadata about what triggered this execution.",
      "required": false
    },
    {
      "name": "RuntimeEnv",
      "jsonName": "runtimeEnv",
      "protoField": "runtime_env",
      "type": {
        "kind": "map",
        "keyType": {
          "kind": "string"
        },
        "valueType": {
          "kind": "message",
          "messageType": "ExecutionValue"
        }
      },
      "description": "Execution-scoped environment variables and secrets, available only to this\n execution. Values here take the highest merge priority, overriding values\n from Environments bound via the instance's environment_refs. A key must be\n declared in Workflow.spec.env to survive the merge: the workflow env map is\n a declaration whitelist (name + is_secret + optional), never a value source\n — undeclared keys are dropped.",
      "required": false
    },
    {
      "name": "CallbackToken",
      "jsonName": "callbackToken",
      "protoField": "callback_token",
      "type": {
        "kind": "bytes"
      },
      "description": "Opaque callback token for asynchronous completion by a parent orchestrator.",
      "required": false
    },
    {
      "name": "ExecutionTarget",
      "jsonName": "executionTarget",
      "protoField": "execution_target",
      "type": {
        "kind": "string",
        "enumType": "ai.stigmer.agentic.session.v1.ExecutionTarget",
        "enumValues": [
          "EXECUTION_TARGET_LOCAL",
          "EXECUTION_TARGET_CLOUD"
        ]
      },
      "description": "Where workflow activities are executed — shared runner pool or dedicated sandbox.",
      "required": false
    }
  ]
}