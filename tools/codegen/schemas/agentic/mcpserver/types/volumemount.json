{
  "name": "VolumeMount",
  "description": "VolumeMount defines a Docker volume mount configuration.\n Maps a path on the host to a path inside the container.",
  "protoType": "ai.stigmer.agentic.mcpserver.v1.VolumeMount",
  "protoFile": "apis/ai/stigmer/agentic/mcpserver/v1/spec.proto",
  "fields": [
    {
      "name": "HostPath",
      "jsonName": "hostPath",
      "protoField": "host_path",
      "type": {
        "kind": "string"
      },
      "description": "Path on the host machine to mount.\n Can be an absolute path or a named Docker volume.\n Examples:\n - \"/home/user/data\" (absolute path)\n - \"mcp-data-volume\" (named volume)",
      "required": true,
      "validation": {
        "required": true
      }
    },
    {
      "name": "ContainerPath",
      "jsonName": "containerPath",
      "protoField": "container_path",
      "type": {
        "kind": "string"
      },
      "description": "Path inside the container where the volume is mounted.\n Must be an absolute path.\n Example: \"/data\"",
      "required": true,
      "validation": {
        "required": true
      }
    },
    {
      "name": "ReadOnly",
      "jsonName": "readOnly",
      "protoField": "read_only",
      "type": {
        "kind": "bool"
      },
      "description": "Whether the mount should be read-only.\n Default: false (read-write).\n Set to true for configuration files or sensitive data that shouldn't be modified.",
      "required": false
    }
  ]
}