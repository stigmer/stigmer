{
  "name": "RaiseTaskConfig",
  "kind": "RAISE",
  "description": "RaiseTaskConfig defines the configuration for raise_error tasks that raise errors.",
  "protoType": "ai.stigmer.agentic.workflow.v1.tasks.RaiseTaskConfig",
  "protoFile": "apis/ai/stigmer/agentic/workflow/v1/tasks/raise.proto",
  "discriminatorValue": "raise_error",
  "fields": [
    {
      "name": "Error",
      "jsonName": "error",
      "protoField": "error",
      "type": {
        "kind": "string"
      },
      "description": "Error type/name.",
      "required": true,
      "isExpression": true,
      "validation": {
        "required": true,
        "minLength": 1
      }
    },
    {
      "name": "Message",
      "jsonName": "message",
      "protoField": "message",
      "type": {
        "kind": "string"
      },
      "description": "Error message.\n Can contain expressions: \"${ .errorMessage }\"\n Optional — when omitted, the raised error carries only the error\n type/name. (The runtime maps this to the problem-details `detail`\n field only when present; requiring it was contract fiction, #685.)",
      "required": false,
      "isExpression": true
    }
  ]
}