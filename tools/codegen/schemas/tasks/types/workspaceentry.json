{
  "name": "WorkspaceEntry",
  "description": "WorkspaceEntry pairs a WorkspaceSource with a human-readable name, forming an addressable unit within a session's workspace.\n\n Each entry is a separate directory or repository that the agent can\n operate on. The name serves as the entry's identity and must be unique\n within a session's workspace_entries list.",
  "protoType": "ai.stigmer.agentic.session.v1.WorkspaceEntry",
  "protoFile": "apis/ai/stigmer/agentic/session/v1/workspace.proto",
  "fields": [
    {
      "name": "Name",
      "jsonName": "name",
      "protoField": "name",
      "type": {
        "kind": "string"
      },
      "description": "Short identifier for this workspace entry.",
      "required": false,
      "validation": {
        "minLength": 1
      }
    },
    {
      "name": "Source",
      "jsonName": "source",
      "protoField": "source",
      "type": {
        "kind": "message",
        "messageType": "WorkspaceSource"
      },
      "description": "The source that provides this entry's content.",
      "required": true,
      "validation": {
        "required": true
      }
    }
  ]
}