Start the local Stigmer stack.

`stigmer up` brings up the complete local stack — managed Temporal, the
control-plane `stigmer-server`, and a unified execution runner — as a
supervised background daemon. This is the quickstart command for local
development: no cloud account needed.

On first run the CLI acquires the `stigmer-server` and runner packages at the
matching version, then bootstraps the backend: it creates the `stigmer`
organization, the one the CLI uses when a command names none. Nothing else is
installed: a conversation started with no agent
runs the built-in assistant, so a fresh install answers its first message at
once. An install seeded by an earlier release keeps its `assistant` agent as an
ordinary agent beside the built-in one; every conversation with it continues.

Use `stigmer up server` to start only the control plane (Temporal and
`stigmer-server`) when you manage your own runners — for example, the desktop
app with an embedded runner, your own customer code, or cloud sandboxes.

{/* AUTO_USAGE */}

### Common patterns

```bash
# Start the full local stack (server, runner, Temporal)
stigmer up

# Start only the control plane (no runners)
stigmer up server

# Check what is running
stigmer status

# Stop everything
stigmer down
```

{/* AUTO_FLAGS */}

## Examples

### Start the full local stack

```bash
stigmer up
```

The first run may take a moment while the server and runner are downloaded and
verified. Subsequent runs start immediately.

### Start only the control plane

```bash
stigmer up server
```

Starts Temporal and `stigmer-server` without any runner processes. Use this
when a separate process (desktop app, your own code, or a cloud sandbox)
provides the runner.

### Inspect and tear down

```bash
stigmer status   # show component health and the effective LLM configuration
stigmer down     # stop the stack
```

{/* AUTO_SUBCOMMANDS */}

{/* AUTO_GLOBAL_FLAGS */}

## Related commands

| Command | Description |
|---------|-------------|
| [`stigmer down`](./down) | Stop the local stack |
| [`stigmer status`](./status) | Show local stack status |
| [`stigmer logs`](./logs) | View logs from the local stack |
| [`stigmer setup`](./setup) | Configure the LLM provider |
| [`stigmer bootstrap`](./bootstrap) | Bootstrap a raw self-hosted server the same way |

## See also

- [Local Quickstart](/docs/getting-started/local) — Set up Stigmer and run your first agent
- [Command Reference](./) — All available commands
