Manage authentication with Stigmer Cloud.

`stigmer auth` handles browser-based PKCE OAuth authentication. Use it to
log in, log out, and check your current identity.

{/* AUTO_USAGE */}

### Common patterns

```bash
# Log in to Stigmer Cloud
stigmer auth login

# Check who you're logged in as
stigmer auth whoami

# Log out
stigmer auth logout
```

{/* AUTO_FLAGS */}

### How authentication works

`stigmer auth login` opens your default browser to the Stigmer login page.
After you authenticate, the CLI receives an access token and saves it to
`~/.stigmer/config.yaml`. The backend is automatically set to cloud mode.

For programmatic access (CI/CD, scripts), use
[API keys](./apikey) instead of browser-based auth.

{/* AUTO_SUBCOMMANDS */}

{/* AUTO_GLOBAL_FLAGS */}

## Related commands

| Command | Description |
|---------|-------------|
| [`stigmer apikey`](./apikey) | Manage API keys for programmatic access |
| [`stigmer config`](./config) | Manage CLI configuration |
| [`stigmer config backend`](./config) | Switch between local and cloud backends |

## See also

- [Command Reference](./) — All available commands
