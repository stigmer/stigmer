// The CLI's reported version. The 0.0.0-dev sentinel matches the workspace
// convention for unpublished packages; the release pipeline injects the real
// semver at build time (T06).
export const VERSION = "0.0.0-dev";
