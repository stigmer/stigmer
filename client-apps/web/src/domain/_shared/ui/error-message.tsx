/**
 * Re-export from `@stigmer/react` — the canonical error display component
 * now lives in the SDK for use by platform builders.
 *
 * Console pages import from this path for convenience; the implementation
 * is in `sdk/react/src/error/ErrorMessage.tsx`.
 */
export { ErrorMessage, type ErrorMessageProps } from "@stigmer/react";
