// Code generated by stigmer-codegen. DO NOT EDIT.

import { wrapError } from "./errors.js";
import { stripUndefined } from "./proto-utils.js";
import { create } from "@bufbuild/protobuf";
import { createClient, type Client, type Transport } from "@connectrpc/connect";
import { SubscriptionSchema, type Subscription } from "@stigmer/protos/ai/stigmer/billing/subscription/v1/api_pb";
import { SubscriptionCommandController } from "@stigmer/protos/ai/stigmer/billing/subscription/v1/command_pb";
import { ChangePlanInputSchema, CancelSubscriptionInputSchema, GetSubscriptionForOrganizationInputSchema, GetEntitlementsInputSchema, GetEntitlementsOutputSchema, type ChangePlanInput, type CancelSubscriptionInput, type GetSubscriptionForOrganizationInput, type GetEntitlementsInput, type GetEntitlementsOutput } from "@stigmer/protos/ai/stigmer/billing/subscription/v1/io_pb";
import { SubscriptionQueryController } from "@stigmer/protos/ai/stigmer/billing/subscription/v1/query_pb";
import { SubscriptionSpecSchema } from "@stigmer/protos/ai/stigmer/billing/subscription/v1/spec_pb";
import { ApiResourceVisibility } from "@stigmer/protos/ai/stigmer/commons/apiresource/enum_pb";
import { ApiResourceMetadataSchema } from "@stigmer/protos/ai/stigmer/commons/apiresource/metadata_pb";

/** Provides operations on subscription resources. */
export class SubscriptionClient {
  private readonly command: Client<typeof SubscriptionCommandController>;
  private readonly query: Client<typeof SubscriptionQueryController>;

  constructor(transport: Transport) {
    this.command = createClient(SubscriptionCommandController, transport);
    this.query = createClient(SubscriptionQueryController, transport);
  }

  async changePlan(input: ChangePlanInput): Promise<Subscription> {
    try {
      return await this.command.changePlan(input);
    } catch (e) { throw wrapError(e); }
  }

  async cancel(input: CancelSubscriptionInput): Promise<Subscription> {
    try {
      return await this.command.cancel(input);
    } catch (e) { throw wrapError(e); }
  }

  async getForOrganization(input: GetSubscriptionForOrganizationInput): Promise<Subscription> {
    try {
      return await this.query.getForOrganization(input);
    } catch (e) { throw wrapError(e); }
  }

  async getEntitlements(input: GetEntitlementsInput): Promise<GetEntitlementsOutput> {
    try {
      return await this.query.getEntitlements(input);
    } catch (e) { throw wrapError(e); }
  }
}

/** Input for creating/updating a Subscription. */
export interface SubscriptionInput {
  /**
   * The resource's `metadata.id`, for exact update addressing when set
   * from a loaded resource. Required for updates to platform-scoped
   * (org-less) kinds, where the org+slug fallback cannot match. On
   * create, the cloud server stamps its own id regardless; the OSS
   * server honors a caller-supplied id (existing apply semantics).
   */
  id?: string;
  name: string;
  slug?: string;
  org: string;
  labels?: Record<string, string>;
  visibility?: ApiResourceVisibility;
  planId: string;
}

export function buildSubscriptionProto(input: SubscriptionInput): Subscription {
  return Object.assign(create(SubscriptionSchema), {
    apiVersion: "billing.stigmer.ai/v1",
    kind: "Subscription",
    metadata: Object.assign(create(ApiResourceMetadataSchema), {
      ...(input.id && { id: input.id }),
      name: input.name,
      org: input.org,
      ...(input.slug && { slug: input.slug }),
      ...(input.labels && { labels: input.labels }),
      ...(input.visibility && { visibility: input.visibility }),
    }),
    spec: Object.assign(create(SubscriptionSpecSchema), stripUndefined({
      planId: input.planId,
    })),
  }) as Subscription;
}
