// Code generated by stigmer-codegen. DO NOT EDIT.

import { wrapError } from "./errors.js";
import { stripUndefined } from "./proto-utils.js";
import { type DeleteResourceInput, type ResourceRef } from "./types.js";
import { create } from "@bufbuild/protobuf";
import { createClient, type Client, type Transport } from "@connectrpc/connect";
import { ChannelAppSchema, type ChannelApp } from "@stigmer/protos/ai/stigmer/agentic/channelapp/v1/api_pb";
import { ChannelAppCommandController } from "@stigmer/protos/ai/stigmer/agentic/channelapp/v1/command_pb";
import { ListChannelAppsByOrgInputSchema, ChannelAppsSchema, type ListChannelAppsByOrgInput, type ChannelApps } from "@stigmer/protos/ai/stigmer/agentic/channelapp/v1/io_pb";
import { ChannelAppQueryController } from "@stigmer/protos/ai/stigmer/agentic/channelapp/v1/query_pb";
import { ChannelAppSpecSchema, SlackChannelAppConfigSchema, WhatsAppChannelAppConfigSchema } from "@stigmer/protos/ai/stigmer/agentic/channelapp/v1/spec_pb";
import { ApiResourceKind } from "@stigmer/protos/ai/stigmer/commons/apiresource/apiresourcekind/api_resource_kind_pb";
import { ApiResourceVisibility } from "@stigmer/protos/ai/stigmer/commons/apiresource/enum_pb";
import { ApiResourceIdSchema, ApiResourceReferenceSchema, ApiResourceDeleteInputSchema } from "@stigmer/protos/ai/stigmer/commons/apiresource/io_pb";
import { ApiResourceMetadataSchema } from "@stigmer/protos/ai/stigmer/commons/apiresource/metadata_pb";

/** Provides operations on channelapp resources. */
export class ChannelAppClient {
  private readonly command: Client<typeof ChannelAppCommandController>;
  private readonly query: Client<typeof ChannelAppQueryController>;

  constructor(transport: Transport) {
    this.command = createClient(ChannelAppCommandController, transport);
    this.query = createClient(ChannelAppQueryController, transport);
  }

  async apply(input: ChannelAppInput): Promise<ChannelApp> {
    try {
      return await this.command.apply(buildChannelAppProto(input));
    } catch (e) { throw wrapError(e); }
  }

  async create(input: ChannelAppInput): Promise<ChannelApp> {
    try {
      return await this.command.create(buildChannelAppProto(input));
    } catch (e) { throw wrapError(e); }
  }

  async update(input: ChannelAppInput): Promise<ChannelApp> {
    try {
      return await this.command.update(buildChannelAppProto(input));
    } catch (e) { throw wrapError(e); }
  }

  async delete(input: DeleteResourceInput): Promise<ChannelApp> {
    try {
      return await this.command.delete(create(ApiResourceDeleteInputSchema, {
        resourceId: input.resourceId,
        versionMessage: input.versionMessage,
        force: input.force,
      }));
    } catch (e) { throw wrapError(e); }
  }

  async get(id: string): Promise<ChannelApp> {
    try {
      return await this.query.get(create(ApiResourceIdSchema, { value: id }));
    } catch (e) { throw wrapError(e); }
  }

  async getByReference(ref: ResourceRef): Promise<ChannelApp> {
    try {
      return await this.query.getByReference(create(ApiResourceReferenceSchema, { ...ref, kind: ApiResourceKind.channel_app }));
    } catch (e) { throw wrapError(e); }
  }

  async listByOrg(input: ListChannelAppsByOrgInput): Promise<ChannelApps> {
    try {
      return await this.query.listByOrg(input);
    } catch (e) { throw wrapError(e); }
  }
}

/** Input for creating/updating a ChannelApp. */
export interface ChannelAppInput {
  name: string;
  slug?: string;
  org: string;
  labels?: Record<string, string>;
  visibility?: ApiResourceVisibility;
  slack?: SlackChannelAppConfigInput;
  whatsapp?: WhatsAppChannelAppConfigInput;
}

/** SDK input type for SlackChannelAppConfig. */
export interface SlackChannelAppConfigInput {
  clientId?: string;
  clientSecret?: string;
  signingSecret?: string;
}

/** SDK input type for WhatsAppChannelAppConfig. */
export interface WhatsAppChannelAppConfigInput {
  appId?: string;
  appSecret?: string;
  accessToken?: string;
  verifyToken?: string;
}

function buildSlackChannelAppConfigProto(input: SlackChannelAppConfigInput) {
  return Object.assign(create(SlackChannelAppConfigSchema), stripUndefined({
    clientId: input.clientId,
    clientSecret: input.clientSecret,
    signingSecret: input.signingSecret,
  }));
}

function buildWhatsAppChannelAppConfigProto(input: WhatsAppChannelAppConfigInput) {
  return Object.assign(create(WhatsAppChannelAppConfigSchema), stripUndefined({
    appId: input.appId,
    appSecret: input.appSecret,
    accessToken: input.accessToken,
    verifyToken: input.verifyToken,
  }));
}

export function buildChannelAppProto(input: ChannelAppInput): ChannelApp {
  const spec = Object.assign(create(ChannelAppSpecSchema), stripUndefined({
  }));
  if (input.slack) {
    spec.providerConfig = { case: "slack", value: buildSlackChannelAppConfigProto(input.slack) };
  } else if (input.whatsapp) {
    spec.providerConfig = { case: "whatsapp", value: buildWhatsAppChannelAppConfigProto(input.whatsapp) };
  }
  return Object.assign(create(ChannelAppSchema), {
    apiVersion: "agentic.stigmer.ai/v1",
    kind: "ChannelApp",
    metadata: Object.assign(create(ApiResourceMetadataSchema), {
      name: input.name,
      org: input.org,
      ...(input.slug && { slug: input.slug }),
      ...(input.labels && { labels: input.labels }),
      ...(input.visibility && { visibility: input.visibility }),
    }),
    spec,
  }) as ChannelApp;
}
