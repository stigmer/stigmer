// Code generated by stigmer-codegen. DO NOT EDIT.

import { wrapError } from "./errors.js";
import { stripUndefined } from "./proto-utils.js";
import { type DeleteResourceInput, type ResourceRef } from "./types.js";
import { create } from "@bufbuild/protobuf";
import { createClient, type Client, type Transport } from "@connectrpc/connect";
import { ApiResourceKind } from "@stigmer/protos/ai/stigmer/commons/apiresource/apiresourcekind/api_resource_kind_pb";
import { ApiResourceVisibility } from "@stigmer/protos/ai/stigmer/commons/apiresource/enum_pb";
import { ApiResourceIdSchema, ApiResourceReferenceSchema, ApiResourceDeleteInputSchema } from "@stigmer/protos/ai/stigmer/commons/apiresource/io_pb";
import { ApiResourceMetadataSchema } from "@stigmer/protos/ai/stigmer/commons/apiresource/metadata_pb";
import { OAuthAppSchema, type OAuthApp } from "@stigmer/protos/ai/stigmer/iam/oauthapp/v1/api_pb";
import { OAuthAppCommandController } from "@stigmer/protos/ai/stigmer/iam/oauthapp/v1/command_pb";
import { ListOAuthAppsByOrgInputSchema, OAuthAppsSchema, type ListOAuthAppsByOrgInput, type OAuthApps } from "@stigmer/protos/ai/stigmer/iam/oauthapp/v1/io_pb";
import { OAuthAppQueryController } from "@stigmer/protos/ai/stigmer/iam/oauthapp/v1/query_pb";
import { OAuthAppSpecSchema, VendorApprovalStatus, TokenEndpointAuthMethod } from "@stigmer/protos/ai/stigmer/iam/oauthapp/v1/spec_pb";

/** Provides operations on oauthapp resources. */
export class OAuthAppClient {
  private readonly command: Client<typeof OAuthAppCommandController>;
  private readonly query: Client<typeof OAuthAppQueryController>;

  constructor(transport: Transport) {
    this.command = createClient(OAuthAppCommandController, transport);
    this.query = createClient(OAuthAppQueryController, transport);
  }

  async apply(input: OAuthAppInput): Promise<OAuthApp> {
    try {
      return await this.command.apply(buildOAuthAppProto(input));
    } catch (e) { throw wrapError(e); }
  }

  async create(input: OAuthAppInput): Promise<OAuthApp> {
    try {
      return await this.command.create(buildOAuthAppProto(input));
    } catch (e) { throw wrapError(e); }
  }

  async update(input: OAuthAppInput): Promise<OAuthApp> {
    try {
      return await this.command.update(buildOAuthAppProto(input));
    } catch (e) { throw wrapError(e); }
  }

  async delete(input: DeleteResourceInput): Promise<OAuthApp> {
    try {
      return await this.command.delete(create(ApiResourceDeleteInputSchema, {
        resourceId: input.resourceId,
        versionMessage: input.versionMessage,
        force: input.force,
      }));
    } catch (e) { throw wrapError(e); }
  }

  async get(id: string): Promise<OAuthApp> {
    try {
      return await this.query.get(create(ApiResourceIdSchema, { value: id }));
    } catch (e) { throw wrapError(e); }
  }

  async getByReference(ref: ResourceRef): Promise<OAuthApp> {
    try {
      return await this.query.getByReference(create(ApiResourceReferenceSchema, { ...ref, kind: ApiResourceKind.oauth_app }));
    } catch (e) { throw wrapError(e); }
  }

  async listByOrg(input: ListOAuthAppsByOrgInput): Promise<OAuthApps> {
    try {
      return await this.query.listByOrg(input);
    } catch (e) { throw wrapError(e); }
  }
}

/** Input for creating/updating a OAuthApp. */
export interface OAuthAppInput {
  /**
   * The resource's `metadata.id`, for exact update addressing when set
   * from a loaded resource. Required for updates to platform-scoped
   * (org-less) kinds, where the org+slug fallback cannot match. On
   * create, the cloud server stamps its own id regardless; the OSS
   * server honors a caller-supplied id (existing apply semantics).
   */
  id?: string;
  name: string;
  slug?: string;
  org: string;
  labels?: Record<string, string>;
  visibility?: ApiResourceVisibility;
  provider?: string;
  clientId?: string;
  clientSecret?: string;
  authorizationUrl?: string;
  tokenUrl?: string;
  scopes?: string[];
  userinfoUrl?: string;
  scopeParameterName?: string;
  vendorApprovalStatus?: VendorApprovalStatus;
  vendorApprovalDocsUrl?: string;
  tokenEndpointAuthMethod?: TokenEndpointAuthMethod;
}

export function buildOAuthAppProto(input: OAuthAppInput): OAuthApp {
  return Object.assign(create(OAuthAppSchema), {
    apiVersion: "iam.stigmer.ai/v1",
    kind: "OAuthApp",
    metadata: Object.assign(create(ApiResourceMetadataSchema), {
      ...(input.id && { id: input.id }),
      name: input.name,
      org: input.org,
      ...(input.slug && { slug: input.slug }),
      ...(input.labels && { labels: input.labels }),
      ...(input.visibility && { visibility: input.visibility }),
    }),
    spec: Object.assign(create(OAuthAppSpecSchema), stripUndefined({
      provider: input.provider,
      clientId: input.clientId,
      clientSecret: input.clientSecret,
      authorizationUrl: input.authorizationUrl,
      tokenUrl: input.tokenUrl,
      scopes: input.scopes,
      userinfoUrl: input.userinfoUrl,
      scopeParameterName: input.scopeParameterName,
      vendorApprovalStatus: input.vendorApprovalStatus,
      vendorApprovalDocsUrl: input.vendorApprovalDocsUrl,
      tokenEndpointAuthMethod: input.tokenEndpointAuthMethod,
    })),
  }) as OAuthApp;
}
