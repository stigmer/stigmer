// Code generated by stigmer-codegen. DO NOT EDIT.

import { wrapError } from "./errors";
import { stripUndefined } from "./proto-utils";
import { type ResourceRef } from "./types";
import { create } from "@bufbuild/protobuf";
import { createClient, type Client, type Transport } from "@connectrpc/connect";
import { AgentRunnerSchema, type AgentRunner } from "@stigmer/protos/ai/stigmer/agentic/agentrunner/v1/api_pb";
import { AgentRunnerCommandController } from "@stigmer/protos/ai/stigmer/agentic/agentrunner/v1/command_pb";
import { AgentRunnerIdSchema, AgentRunnerHeartbeatInputSchema, ListAgentRunnersRequestSchema, AgentRunnerListSchema, type AgentRunnerHeartbeatInput, type ListAgentRunnersRequest, type AgentRunnerList } from "@stigmer/protos/ai/stigmer/agentic/agentrunner/v1/io_pb";
import { AgentRunnerQueryController } from "@stigmer/protos/ai/stigmer/agentic/agentrunner/v1/query_pb";
import { AgentRunnerSpecSchema } from "@stigmer/protos/ai/stigmer/agentic/agentrunner/v1/spec_pb";
import { ApiResourceKind } from "@stigmer/protos/ai/stigmer/commons/apiresource/apiresourcekind/api_resource_kind_pb";
import { ApiResourceReferenceSchema } from "@stigmer/protos/ai/stigmer/commons/apiresource/io_pb";
import { ApiResourceMetadataSchema } from "@stigmer/protos/ai/stigmer/commons/apiresource/metadata_pb";

/** Provides operations on agentrunner resources. */
export class AgentRunnerClient {
  private readonly command: Client<typeof AgentRunnerCommandController>;
  private readonly query: Client<typeof AgentRunnerQueryController>;

  constructor(transport: Transport) {
    this.command = createClient(AgentRunnerCommandController, transport);
    this.query = createClient(AgentRunnerQueryController, transport);
  }

  async apply(input: AgentRunnerInput): Promise<AgentRunner> {
    try {
      return await this.command.apply(buildAgentRunnerProto(input));
    } catch (e) { throw wrapError(e); }
  }

  async create(input: AgentRunnerInput): Promise<AgentRunner> {
    try {
      return await this.command.create(buildAgentRunnerProto(input));
    } catch (e) { throw wrapError(e); }
  }

  async update(input: AgentRunnerInput): Promise<AgentRunner> {
    try {
      return await this.command.update(buildAgentRunnerProto(input));
    } catch (e) { throw wrapError(e); }
  }

  async delete(id: string): Promise<AgentRunner> {
    try {
      return await this.command.delete(create(AgentRunnerIdSchema, { value: id }));
    } catch (e) { throw wrapError(e); }
  }

  async heartbeat(input: AgentRunnerHeartbeatInput): Promise<AgentRunner> {
    try {
      return await this.command.heartbeat(input);
    } catch (e) { throw wrapError(e); }
  }

  async get(id: string): Promise<AgentRunner> {
    try {
      return await this.query.get(create(AgentRunnerIdSchema, { value: id }));
    } catch (e) { throw wrapError(e); }
  }

  async getByReference(ref: ResourceRef): Promise<AgentRunner> {
    try {
      return await this.query.getByReference(create(ApiResourceReferenceSchema, { ...ref, kind: ApiResourceKind.agent_runner }));
    } catch (e) { throw wrapError(e); }
  }

  async list(input: ListAgentRunnersRequest): Promise<AgentRunnerList> {
    try {
      return await this.query.list(input);
    } catch (e) { throw wrapError(e); }
  }
}

/** Input for creating/updating a AgentRunner. */
export interface AgentRunnerInput {
  name: string;
  slug?: string;
  org: string;
  labels?: Record<string, string>;
  description?: string;
}

function buildAgentRunnerProto(input: AgentRunnerInput): AgentRunner {
  return Object.assign(create(AgentRunnerSchema), {
    apiVersion: "agentic.stigmer.ai/v1",
    kind: "AgentRunner",
    metadata: Object.assign(create(ApiResourceMetadataSchema), {
      name: input.name,
      org: input.org,
      ...(input.slug && { slug: input.slug }),
      ...(input.labels && { labels: input.labels }),
    }),
    spec: Object.assign(create(AgentRunnerSpecSchema), stripUndefined({
      description: input.description,
    })),
  }) as AgentRunner;
}
