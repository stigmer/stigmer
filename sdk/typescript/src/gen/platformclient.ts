// Code generated by stigmer-codegen. DO NOT EDIT.

import { wrapError } from "./errors.js";
import { stripUndefined, toTimestamp } from "./proto-utils.js";
import { type DeleteResourceInput, type ResourceRef } from "./types.js";
import { create } from "@bufbuild/protobuf";
import { createClient, type Client, type Transport } from "@connectrpc/connect";
import { ApiResourceKind } from "@stigmer/protos/ai/stigmer/commons/apiresource/apiresourcekind/api_resource_kind_pb";
import { ApiResourceVisibility } from "@stigmer/protos/ai/stigmer/commons/apiresource/enum_pb";
import { ApiResourceIdSchema, ApiResourceReferenceSchema, ApiResourceDeleteInputSchema } from "@stigmer/protos/ai/stigmer/commons/apiresource/io_pb";
import { ApiResourceMetadataSchema } from "@stigmer/protos/ai/stigmer/commons/apiresource/metadata_pb";
import { PlatformClientSchema, type PlatformClient } from "@stigmer/protos/ai/stigmer/iam/platformclient/v1/api_pb";
import { PlatformClientCommandController } from "@stigmer/protos/ai/stigmer/iam/platformclient/v1/command_pb";
import { PlatformClientCreateResponseSchema, PlatformClientIdSchema, ListPlatformClientsByOrgInputSchema, PlatformClientsSchema, type PlatformClientCreateResponse, type ListPlatformClientsByOrgInput, type PlatformClients } from "@stigmer/protos/ai/stigmer/iam/platformclient/v1/io_pb";
import { PlatformClientQueryController } from "@stigmer/protos/ai/stigmer/iam/platformclient/v1/query_pb";
import { PlatformClientSpecSchema } from "@stigmer/protos/ai/stigmer/iam/platformclient/v1/spec_pb";
import { PlatformClientTokenController, MintUserTokenRequestSchema, MintUserTokenResponseSchema, MintGuestTokenRequestSchema, MintGuestTokenResponseSchema, type MintUserTokenRequest, type MintUserTokenResponse, type MintGuestTokenRequest, type MintGuestTokenResponse } from "@stigmer/protos/ai/stigmer/iam/platformclient/v1/token_pb";
import { IamRole } from "@stigmer/protos/ai/stigmer/iam/v1/enum_pb";

/** Provides operations on platformclient resources. */
export class PlatformClientClient {
  private readonly command: Client<typeof PlatformClientCommandController>;
  private readonly query: Client<typeof PlatformClientQueryController>;
  private readonly token: Client<typeof PlatformClientTokenController>;

  constructor(transport: Transport) {
    this.command = createClient(PlatformClientCommandController, transport);
    this.query = createClient(PlatformClientQueryController, transport);
    this.token = createClient(PlatformClientTokenController, transport);
  }

  async create(input: PlatformClientInput): Promise<PlatformClientCreateResponse> {
    try {
      return await this.command.create(buildPlatformClientProto(input));
    } catch (e) { throw wrapError(e); }
  }

  async update(input: PlatformClientInput): Promise<PlatformClient> {
    try {
      return await this.command.update(buildPlatformClientProto(input));
    } catch (e) { throw wrapError(e); }
  }

  async delete(input: DeleteResourceInput): Promise<PlatformClient> {
    try {
      return await this.command.delete(create(ApiResourceDeleteInputSchema, {
        resourceId: input.resourceId,
        versionMessage: input.versionMessage,
        force: input.force,
      }));
    } catch (e) { throw wrapError(e); }
  }

  async rotateSecret(id: string): Promise<PlatformClientCreateResponse> {
    try {
      return await this.command.rotateSecret(create(PlatformClientIdSchema, { value: id }));
    } catch (e) { throw wrapError(e); }
  }

  async get(id: string): Promise<PlatformClient> {
    try {
      return await this.query.get(create(ApiResourceIdSchema, { value: id }));
    } catch (e) { throw wrapError(e); }
  }

  async getByReference(ref: ResourceRef): Promise<PlatformClient> {
    try {
      return await this.query.getByReference(create(ApiResourceReferenceSchema, { ...ref, kind: ApiResourceKind.platform_client }));
    } catch (e) { throw wrapError(e); }
  }

  async listByOrg(input: ListPlatformClientsByOrgInput): Promise<PlatformClients> {
    try {
      return await this.query.listByOrg(input);
    } catch (e) { throw wrapError(e); }
  }

  async mintUserToken(input: MintUserTokenRequest): Promise<MintUserTokenResponse> {
    try {
      return await this.token.mintUserToken(input);
    } catch (e) { throw wrapError(e); }
  }

  async mintGuestToken(input: MintGuestTokenRequest): Promise<MintGuestTokenResponse> {
    try {
      return await this.token.mintGuestToken(input);
    } catch (e) { throw wrapError(e); }
  }
}

/** Input for creating/updating a PlatformClient. */
export interface PlatformClientInput {
  name: string;
  slug?: string;
  org: string;
  labels?: Record<string, string>;
  visibility?: ApiResourceVisibility;
  clientId?: string;
  clientSecretHash?: string;
  secretFingerprint?: string;
  expiresAt?: Date | string;
  neverExpires?: boolean;
  autoProvisionAccounts?: boolean;
  autoGrantOnOrg?: boolean;
  autoGrantRole?: IamRole;
  allowedOrigins?: string[];
}

export function buildPlatformClientProto(input: PlatformClientInput): PlatformClient {
  const expiresAt = input.expiresAt !== undefined ? toTimestamp(input.expiresAt) : undefined;
  return Object.assign(create(PlatformClientSchema), {
    apiVersion: "iam.stigmer.ai/v1",
    kind: "PlatformClient",
    metadata: Object.assign(create(ApiResourceMetadataSchema), {
      name: input.name,
      org: input.org,
      ...(input.slug && { slug: input.slug }),
      ...(input.labels && { labels: input.labels }),
      ...(input.visibility && { visibility: input.visibility }),
    }),
    spec: Object.assign(create(PlatformClientSpecSchema), stripUndefined({
      clientId: input.clientId,
      clientSecretHash: input.clientSecretHash,
      secretFingerprint: input.secretFingerprint,
      expiresAt,
      neverExpires: input.neverExpires,
      autoProvisionAccounts: input.autoProvisionAccounts,
      autoGrantOnOrg: input.autoGrantOnOrg,
      autoGrantRole: input.autoGrantRole,
      allowedOrigins: input.allowedOrigins,
    })),
  }) as PlatformClient;
}
