// Code generated by stigmer-codegen. DO NOT EDIT.

import { wrapError } from "./errors.js";
import { stripUndefined, toTimestamp } from "./proto-utils.js";
import { create } from "@bufbuild/protobuf";
import { createClient, type Client, type Transport } from "@connectrpc/connect";
import { LicenseSchema, type License } from "@stigmer/protos/ai/stigmer/billing/license/v1/api_pb";
import { LicenseCommandController } from "@stigmer/protos/ai/stigmer/billing/license/v1/command_pb";
import { LicenseIdSchema, ListLicensesInputSchema, LicensesSchema, type ListLicensesInput, type Licenses } from "@stigmer/protos/ai/stigmer/billing/license/v1/io_pb";
import { LicenseQueryController } from "@stigmer/protos/ai/stigmer/billing/license/v1/query_pb";
import { LicenseSpecSchema } from "@stigmer/protos/ai/stigmer/billing/license/v1/spec_pb";
import { ApiResourceVisibility } from "@stigmer/protos/ai/stigmer/commons/apiresource/enum_pb";
import { ApiResourceMetadataSchema } from "@stigmer/protos/ai/stigmer/commons/apiresource/metadata_pb";
import { Feature, EntitlementLimitsSchema, EntitlementsSchema } from "@stigmer/protos/ai/stigmer/platform/v1/entitlement_pb";
import { LicenseTerm, LicenseCustomerSchema } from "@stigmer/protos/ai/stigmer/platform/v1/license_pb";

/** Provides operations on license resources. */
export class LicenseClient {
  private readonly command: Client<typeof LicenseCommandController>;
  private readonly query: Client<typeof LicenseQueryController>;

  constructor(transport: Transport) {
    this.command = createClient(LicenseCommandController, transport);
    this.query = createClient(LicenseQueryController, transport);
  }

  async create(input: LicenseInput): Promise<License> {
    try {
      return await this.command.create(buildLicenseProto(input));
    } catch (e) { throw wrapError(e); }
  }

  async get(id: string): Promise<License> {
    try {
      return await this.query.get(create(LicenseIdSchema, { value: id }));
    } catch (e) { throw wrapError(e); }
  }

  async list(input: ListLicensesInput): Promise<Licenses> {
    try {
      return await this.query.list(input);
    } catch (e) { throw wrapError(e); }
  }
}

/** Input for creating/updating a License. */
export interface LicenseInput {
  /**
   * The resource's `metadata.id`, for exact update addressing when set
   * from a loaded resource. Required for updates to platform-scoped
   * (org-less) kinds, where the org+slug fallback cannot match. On
   * create, the cloud server stamps its own id regardless; the OSS
   * server honors a caller-supplied id (existing apply semantics).
   */
  id?: string;
  name: string;
  slug?: string;
  org: string;
  labels?: Record<string, string>;
  visibility?: ApiResourceVisibility;
  customer: LicenseCustomerInput;
  planId?: string;
  entitlements: EntitlementsInput;
  term: LicenseTerm;
  expiresAt: Date | string;
  graceUntil: Date | string;
  notes?: string;
}

/** SDK input type for LicenseCustomer. */
export interface LicenseCustomerInput {
  id: string;
  displayName: string;
  contactEmail: string;
  organization?: string;
}

/** SDK input type for Entitlements. */
export interface EntitlementsInput {
  limits?: EntitlementLimitsInput;
  features?: Feature[];
}

/** SDK input type for EntitlementLimits. */
export interface EntitlementLimitsInput {
  maxOrganizations?: number;
  maxUsers?: number;
  includedManagedOrganizations?: number;
}

function buildLicenseCustomerProto(input: LicenseCustomerInput) {
  return Object.assign(create(LicenseCustomerSchema), stripUndefined({
    id: input.id,
    displayName: input.displayName,
    contactEmail: input.contactEmail,
    organization: input.organization,
  }));
}

function buildEntitlementLimitsProto(input: EntitlementLimitsInput) {
  return Object.assign(create(EntitlementLimitsSchema), stripUndefined({
    maxOrganizations: input.maxOrganizations,
    maxUsers: input.maxUsers,
    includedManagedOrganizations: input.includedManagedOrganizations,
  }));
}

function buildEntitlementsProto(input: EntitlementsInput) {
  const msg = create(EntitlementsSchema);
  if (input.limits) msg.limits = buildEntitlementLimitsProto(input.limits);
  if (input.features) msg.features = input.features;
  return msg;
}

export function buildLicenseProto(input: LicenseInput): License {
  const customer = input.customer ? buildLicenseCustomerProto(input.customer) : undefined;
  const entitlements = input.entitlements ? buildEntitlementsProto(input.entitlements) : undefined;
  const expiresAt = input.expiresAt !== undefined ? toTimestamp(input.expiresAt) : undefined;
  const graceUntil = input.graceUntil !== undefined ? toTimestamp(input.graceUntil) : undefined;
  return Object.assign(create(LicenseSchema), {
    apiVersion: "billing.stigmer.ai/v1",
    kind: "License",
    metadata: Object.assign(create(ApiResourceMetadataSchema), {
      ...(input.id && { id: input.id }),
      name: input.name,
      org: input.org,
      ...(input.slug && { slug: input.slug }),
      ...(input.labels && { labels: input.labels }),
      ...(input.visibility && { visibility: input.visibility }),
    }),
    spec: Object.assign(create(LicenseSpecSchema), stripUndefined({
      customer,
      planId: input.planId,
      entitlements,
      term: input.term,
      expiresAt,
      graceUntil,
      notes: input.notes,
    })),
  }) as License;
}
