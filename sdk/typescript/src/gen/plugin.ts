// Code generated by stigmer-codegen. DO NOT EDIT.

import { wrapError } from "./errors.js";
import { stripUndefined } from "./proto-utils.js";
import { type ListParams, type ListResult, type ResourceRef } from "./types.js";
import { create } from "@bufbuild/protobuf";
import { createClient, type Client, type Transport } from "@connectrpc/connect";
import { PluginSchema, type Plugin } from "@stigmer/protos/ai/stigmer/agentic/plugin/v1/api_pb";
import { PluginCommandController } from "@stigmer/protos/ai/stigmer/agentic/plugin/v1/command_pb";
import { PluginIdSchema, PushPluginRequestSchema, CreatePluginArtifactUploadUrlRequestSchema, PluginArtifactUploadUrlSchema, ListPluginMembersResponseSchema, ListPluginVersionsInputSchema, ListPluginVersionsResponseSchema, type PushPluginRequest, type CreatePluginArtifactUploadUrlRequest, type PluginArtifactUploadUrl, type ListPluginMembersResponse, type ListPluginVersionsInput, type ListPluginVersionsResponse } from "@stigmer/protos/ai/stigmer/agentic/plugin/v1/io_pb";
import { PluginQueryController } from "@stigmer/protos/ai/stigmer/agentic/plugin/v1/query_pb";
import { PluginSpecSchema, PluginDialect, PluginAuthorSchema } from "@stigmer/protos/ai/stigmer/agentic/plugin/v1/spec_pb";
import { ApiResourceKind } from "@stigmer/protos/ai/stigmer/commons/apiresource/apiresourcekind/api_resource_kind_pb";
import { ApiResourceVisibility } from "@stigmer/protos/ai/stigmer/commons/apiresource/enum_pb";
import { ApiResourceReferenceSchema, type UpdateVisibilityInput } from "@stigmer/protos/ai/stigmer/commons/apiresource/io_pb";
import { ApiResourceMetadataSchema, ApiResourceMetadataVersionSchema } from "@stigmer/protos/ai/stigmer/commons/apiresource/metadata_pb";
import { PageInfoSchema } from "@stigmer/protos/ai/stigmer/commons/rpc/pagination_pb";
import { SearchRequestSchema } from "@stigmer/protos/ai/stigmer/search/v1/io_pb";
import { SearchService } from "@stigmer/protos/ai/stigmer/search/v1/query_pb";

/** Provides operations on plugin resources. */
export class PluginClient {
  private readonly command: Client<typeof PluginCommandController>;
  private readonly query: Client<typeof PluginQueryController>;
  private readonly search: Client<typeof SearchService>;

  constructor(transport: Transport) {
    this.command = createClient(PluginCommandController, transport);
    this.query = createClient(PluginQueryController, transport);
    this.search = createClient(SearchService, transport);
  }

  async push(input: PushPluginRequest): Promise<Plugin> {
    try {
      return await this.command.push(input);
    } catch (e) { throw wrapError(e); }
  }

  async createArtifactUploadUrl(input: CreatePluginArtifactUploadUrlRequest): Promise<PluginArtifactUploadUrl> {
    try {
      return await this.command.createArtifactUploadUrl(input);
    } catch (e) { throw wrapError(e); }
  }

  async updateVisibility(input: UpdateVisibilityInput): Promise<Plugin> {
    try {
      return await this.command.updateVisibility(input);
    } catch (e) { throw wrapError(e); }
  }

  async delete(id: string): Promise<Plugin> {
    try {
      return await this.command.delete(create(PluginIdSchema, { value: id }));
    } catch (e) { throw wrapError(e); }
  }

  async get(id: string): Promise<Plugin> {
    try {
      return await this.query.get(create(PluginIdSchema, { value: id }));
    } catch (e) { throw wrapError(e); }
  }

  async getByReference(ref: ResourceRef): Promise<Plugin> {
    try {
      return await this.query.getByReference(create(ApiResourceReferenceSchema, { ...ref, kind: ApiResourceKind.plugin }));
    } catch (e) { throw wrapError(e); }
  }

  async listMembers(id: string): Promise<ListPluginMembersResponse> {
    try {
      return await this.query.listMembers(create(PluginIdSchema, { value: id }));
    } catch (e) { throw wrapError(e); }
  }

  async listVersions(input: ListPluginVersionsInput): Promise<ListPluginVersionsResponse> {
    try {
      return await this.query.listVersions(input);
    } catch (e) { throw wrapError(e); }
  }

  async list(params: ListParams): Promise<ListResult> {
    try {
      const resp = await this.search.search(create(SearchRequestSchema, {
        kinds: [ApiResourceKind.plugin],
        query: params.query,
        org: params.org,
        page: params.page ? create(PageInfoSchema, params.page) : undefined,
      }));
      return {
        entries: resp.entries,
        totalCount: resp.totalCount,
        totalPages: resp.totalPages,
      };
    } catch (e) { throw wrapError(e); }
  }
}

/** Input for creating/updating a Plugin. */
export interface PluginInput {
  /**
   * The resource's `metadata.id`, for exact update addressing when set
   * from a loaded resource. Required for updates to platform-scoped
   * (org-less) kinds, where the org+slug fallback cannot match. On
   * create, the cloud server stamps its own id regardless; the OSS
   * server honors a caller-supplied id (existing apply semantics).
   */
  id?: string;
  name: string;
  slug?: string;
  org: string;
  labels?: Record<string, string>;
  visibility?: ApiResourceVisibility;
  versionMessage?: string;
  version?: string;
  description?: string;
  author?: PluginAuthorInput;
  homepage?: string;
  repository?: string;
  license?: string;
  keywords?: string[];
  dialect?: PluginDialect;
}

/** SDK input type for PluginAuthor. */
export interface PluginAuthorInput {
  name?: string;
  email?: string;
  url?: string;
}

function buildPluginAuthorProto(input: PluginAuthorInput) {
  return Object.assign(create(PluginAuthorSchema), stripUndefined({
    name: input.name,
    email: input.email,
    url: input.url,
  }));
}

export function buildPluginProto(input: PluginInput): Plugin {
  const author = input.author ? buildPluginAuthorProto(input.author) : undefined;
  return Object.assign(create(PluginSchema), {
    apiVersion: "agentic.stigmer.ai/v1",
    kind: "Plugin",
    metadata: Object.assign(create(ApiResourceMetadataSchema), {
      ...(input.id && { id: input.id }),
      name: input.name,
      org: input.org,
      ...(input.slug && { slug: input.slug }),
      ...(input.labels && { labels: input.labels }),
      ...(input.visibility && { visibility: input.visibility }),
      ...(input.versionMessage && { version: Object.assign(create(ApiResourceMetadataVersionSchema), { message: input.versionMessage }) }),
    }),
    spec: Object.assign(create(PluginSpecSchema), stripUndefined({
      version: input.version,
      description: input.description,
      author,
      homepage: input.homepage,
      repository: input.repository,
      license: input.license,
      keywords: input.keywords,
      dialect: input.dialect,
    })),
  }) as Plugin;
}
