// Code generated by stigmer-codegen. DO NOT EDIT.

import { wrapError } from "./errors.js";
import { stripUndefined } from "./proto-utils.js";
import { type ResourceRef } from "./types.js";
import { create } from "@bufbuild/protobuf";
import { createClient, type Client, type Transport } from "@connectrpc/connect";
import { AgentChannelSchema, type AgentChannel } from "@stigmer/protos/ai/stigmer/agentic/agentchannel/v1/api_pb";
import { AgentChannelCommandController } from "@stigmer/protos/ai/stigmer/agentic/agentchannel/v1/command_pb";
import { AgentChannelIdSchema, InitiateChannelInstallInputSchema, InitiateChannelInstallOutputSchema, CompleteChannelInstallInputSchema, GetAgentChannelsByAgentRequestSchema, AgentChannelListSchema, ListAgentChannelsRequestSchema, type InitiateChannelInstallInput, type InitiateChannelInstallOutput, type CompleteChannelInstallInput, type GetAgentChannelsByAgentRequest, type AgentChannelList, type ListAgentChannelsRequest } from "@stigmer/protos/ai/stigmer/agentic/agentchannel/v1/io_pb";
import { ChannelMessageCommandController } from "@stigmer/protos/ai/stigmer/agentic/agentchannel/v1/message_command_pb";
import { SendChannelMessageInputSchema, SendChannelMessageOutputSchema, ListChannelTemplatesInputSchema, ChannelTemplatesSchema, ListMessagingChannelsInputSchema, MessagingChannelsSchema, type SendChannelMessageInput, type SendChannelMessageOutput, type ListChannelTemplatesInput, type ChannelTemplates, type ListMessagingChannelsInput, type MessagingChannels } from "@stigmer/protos/ai/stigmer/agentic/agentchannel/v1/message_io_pb";
import { ChannelMessageQueryController } from "@stigmer/protos/ai/stigmer/agentic/agentchannel/v1/message_query_pb";
import { AgentChannelQueryController } from "@stigmer/protos/ai/stigmer/agentic/agentchannel/v1/query_pb";
import { AgentChannelSpecSchema, SlackChannelConfigSchema, WhatsAppChannelConfigSchema } from "@stigmer/protos/ai/stigmer/agentic/agentchannel/v1/spec_pb";
import { ApiResourceKind } from "@stigmer/protos/ai/stigmer/commons/apiresource/apiresourcekind/api_resource_kind_pb";
import { ApiResourceVisibility } from "@stigmer/protos/ai/stigmer/commons/apiresource/enum_pb";
import { ApiResourceReferenceSchema } from "@stigmer/protos/ai/stigmer/commons/apiresource/io_pb";
import { ApiResourceMetadataSchema } from "@stigmer/protos/ai/stigmer/commons/apiresource/metadata_pb";

/** Provides operations on agentchannel resources. */
export class AgentChannelClient {
  private readonly command: Client<typeof AgentChannelCommandController>;
  private readonly channelMessageCommand: Client<typeof ChannelMessageCommandController>;
  private readonly channelMessageQuery: Client<typeof ChannelMessageQueryController>;
  private readonly query: Client<typeof AgentChannelQueryController>;

  constructor(transport: Transport) {
    this.command = createClient(AgentChannelCommandController, transport);
    this.channelMessageCommand = createClient(ChannelMessageCommandController, transport);
    this.channelMessageQuery = createClient(ChannelMessageQueryController, transport);
    this.query = createClient(AgentChannelQueryController, transport);
  }

  async apply(input: AgentChannelInput): Promise<AgentChannel> {
    try {
      return await this.command.apply(buildAgentChannelProto(input));
    } catch (e) { throw wrapError(e); }
  }

  async create(input: AgentChannelInput): Promise<AgentChannel> {
    try {
      return await this.command.create(buildAgentChannelProto(input));
    } catch (e) { throw wrapError(e); }
  }

  async update(input: AgentChannelInput): Promise<AgentChannel> {
    try {
      return await this.command.update(buildAgentChannelProto(input));
    } catch (e) { throw wrapError(e); }
  }

  async initiateInstall(input: InitiateChannelInstallInput): Promise<InitiateChannelInstallOutput> {
    try {
      return await this.command.initiateInstall(input);
    } catch (e) { throw wrapError(e); }
  }

  async completeInstall(input: CompleteChannelInstallInput): Promise<AgentChannel> {
    try {
      return await this.command.completeInstall(input);
    } catch (e) { throw wrapError(e); }
  }

  async delete(id: string): Promise<AgentChannel> {
    try {
      return await this.command.delete(create(AgentChannelIdSchema, { value: id }));
    } catch (e) { throw wrapError(e); }
  }

  async sendMessage(input: SendChannelMessageInput): Promise<SendChannelMessageOutput> {
    try {
      return await this.channelMessageCommand.sendMessage(input);
    } catch (e) { throw wrapError(e); }
  }

  async listTemplates(input: ListChannelTemplatesInput): Promise<ChannelTemplates> {
    try {
      return await this.channelMessageQuery.listTemplates(input);
    } catch (e) { throw wrapError(e); }
  }

  async listMessagingChannels(input: ListMessagingChannelsInput): Promise<MessagingChannels> {
    try {
      return await this.channelMessageQuery.listMessagingChannels(input);
    } catch (e) { throw wrapError(e); }
  }

  async get(id: string): Promise<AgentChannel> {
    try {
      return await this.query.get(create(AgentChannelIdSchema, { value: id }));
    } catch (e) { throw wrapError(e); }
  }

  async getByReference(ref: ResourceRef): Promise<AgentChannel> {
    try {
      return await this.query.getByReference(create(ApiResourceReferenceSchema, { ...ref, kind: ApiResourceKind.agent_channel }));
    } catch (e) { throw wrapError(e); }
  }

  async getByAgent(input: GetAgentChannelsByAgentRequest): Promise<AgentChannelList> {
    try {
      return await this.query.getByAgent(input);
    } catch (e) { throw wrapError(e); }
  }

  async list(input: ListAgentChannelsRequest): Promise<AgentChannelList> {
    try {
      return await this.query.list(input);
    } catch (e) { throw wrapError(e); }
  }
}

/** Input for creating/updating a AgentChannel. */
export interface AgentChannelInput {
  name: string;
  slug?: string;
  org: string;
  labels?: Record<string, string>;
  visibility?: ApiResourceVisibility;
  agentRef: ResourceRef;
  enabled?: boolean;
  slack?: SlackChannelConfigInput;
  whatsapp?: WhatsAppChannelConfigInput;
  environmentRefs?: ResourceRef[];
  appRef?: ResourceRef;
  proactiveMessagingEnabled?: boolean;
}

/** SDK input type for SlackChannelConfig. */
export interface SlackChannelConfigInput {
}

/** SDK input type for WhatsAppChannelConfig. */
export interface WhatsAppChannelConfigInput {
  phoneNumberId?: string;
}

function buildSlackChannelConfigProto(input: SlackChannelConfigInput) {
  return Object.assign(create(SlackChannelConfigSchema), stripUndefined({
  }));
}

function buildWhatsAppChannelConfigProto(input: WhatsAppChannelConfigInput) {
  return Object.assign(create(WhatsAppChannelConfigSchema), stripUndefined({
    phoneNumberId: input.phoneNumberId,
  }));
}

export function buildAgentChannelProto(input: AgentChannelInput): AgentChannel {
  const agentRef = (input.agentRef?.slug || input.agentRef?.org) ? create(ApiResourceReferenceSchema, { ...input.agentRef, kind: 40 }) : undefined;
  const environmentRefs = input.environmentRefs?.map(r => create(ApiResourceReferenceSchema, { ...r, kind: 53 }));
  const appRef = (input.appRef?.slug || input.appRef?.org) ? create(ApiResourceReferenceSchema, { ...input.appRef, kind: 48 }) : undefined;
  const spec = Object.assign(create(AgentChannelSpecSchema), stripUndefined({
    agentRef,
    enabled: input.enabled,
    environmentRefs,
    appRef,
    proactiveMessagingEnabled: input.proactiveMessagingEnabled,
  }));
  if (input.slack) {
    spec.providerConfig = { case: "slack", value: buildSlackChannelConfigProto(input.slack) };
  } else if (input.whatsapp) {
    spec.providerConfig = { case: "whatsapp", value: buildWhatsAppChannelConfigProto(input.whatsapp) };
  }
  return Object.assign(create(AgentChannelSchema), {
    apiVersion: "agentic.stigmer.ai/v1",
    kind: "AgentChannel",
    metadata: Object.assign(create(ApiResourceMetadataSchema), {
      name: input.name,
      org: input.org,
      ...(input.slug && { slug: input.slug }),
      ...(input.labels && { labels: input.labels }),
      ...(input.visibility && { visibility: input.visibility }),
    }),
    spec,
  }) as AgentChannel;
}
