// Code generated by stigmer-codegen. DO NOT EDIT.

package ai.stigmer.sdk.gen;

import ai.stigmer.agentic.environment.v1.Environment;
import ai.stigmer.agentic.environment.v1.EnvironmentSpec;
import ai.stigmer.agentic.environment.v1.EnvironmentValue;
import ai.stigmer.commons.apiresource.ApiResourceMetadata;

/** Input for creating/updating a Environment. */
public final class EnvironmentInput {
    private final String name;
    private final String org;
    private final String description;
    private final java.util.Map<String, EnvVarInput> data;

    private EnvironmentInput(Builder builder) {
        this.name = builder.name;
        this.org = builder.org;
        this.description = builder.description;
        this.data = builder.data;
    }

    Environment toProto() {
        EnvironmentSpec.Builder spec = EnvironmentSpec.newBuilder();
        if (this.description != null) {
            spec.setDescription(this.description);
        }
        if (this.data != null && !this.data.isEmpty()) {
            for (java.util.Map.Entry<String, EnvVarInput> entry : this.data.entrySet()) {
                EnvironmentValue.Builder vb = EnvironmentValue.newBuilder()
                    .setValue(entry.getValue().getValue())
                    .setIsSecret(entry.getValue().isSecret());
                if (entry.getValue().getDescription() != null) {
                    vb.setDescription(entry.getValue().getDescription());
                }
                spec.putData(entry.getKey(), vb.build());
            }
        }
        return Environment.newBuilder()
            .setApiVersion("agentic.stigmer.ai/v1")
            .setKind("Environment")
            .setMetadata(ApiResourceMetadata.newBuilder()
                .setName(this.name)
                .setOrg(this.org)
                .build())
            .setSpec(spec.build())
            .build();
    }

    public static Builder builder() { return new Builder(); }

    public static final class Builder {
        private String name;
        private String org;
        private String description;
        private java.util.Map<String, EnvVarInput> data;

        private Builder() {}

        public Builder name(String name) { this.name = name; return this; }
        public Builder org(String org) { this.org = org; return this; }
        public Builder description(String description) { this.description = description; return this; }
        public Builder data(java.util.Map<String, EnvVarInput> data) { this.data = data; return this; }

        public EnvironmentInput build() { return new EnvironmentInput(this); }
    }
}
