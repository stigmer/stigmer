// Code generated by stigmer-codegen. DO NOT EDIT.

package ai.stigmer.sdk.gen;

import ai.stigmer.agentic.agent.v1.Agent;
import ai.stigmer.agentic.agent.v1.AgentCommandControllerGrpc;
import ai.stigmer.agentic.agent.v1.AgentId;
import ai.stigmer.agentic.agent.v1.AgentQueryControllerGrpc;
import ai.stigmer.agentic.agent.v1.GetDefaultAgentRequest;
import ai.stigmer.agentic.agent.v1.SharedAgentProfile;
import ai.stigmer.agentic.agent.v1.UpdateAgentSharingInput;
import ai.stigmer.commons.apiresource.UpdateVisibilityInput;
import ai.stigmer.commons.apiresource.apiresourcekind.ApiResourceKind;
import ai.stigmer.commons.rpc.PageInfo;
import ai.stigmer.search.v1.SearchRequest;
import ai.stigmer.search.v1.SearchResponse;
import ai.stigmer.search.v1.SearchServiceGrpc;
import io.grpc.Channel;
import io.grpc.StatusRuntimeException;

/** Provides operations on agent resources. */
public final class AgentClient {
    private final AgentCommandControllerGrpc.AgentCommandControllerBlockingStub command;
    private final AgentQueryControllerGrpc.AgentQueryControllerBlockingStub query;
    private final SearchServiceGrpc.SearchServiceBlockingStub search;

    AgentClient(Channel channel) {
        this.command = AgentCommandControllerGrpc.newBlockingStub(channel);
        this.query = AgentQueryControllerGrpc.newBlockingStub(channel);
        this.search = SearchServiceGrpc.newBlockingStub(channel);
    }

    public Agent apply(AgentInput input) {
        try {
            return command.apply(input.toProto());
        } catch (StatusRuntimeException e) { throw StigmerException.wrap(e); }
    }

    public Agent create(AgentInput input) {
        try {
            return command.create(input.toProto());
        } catch (StatusRuntimeException e) { throw StigmerException.wrap(e); }
    }

    public Agent update(AgentInput input) {
        try {
            return command.update(input.toProto());
        } catch (StatusRuntimeException e) { throw StigmerException.wrap(e); }
    }

    public Agent updateVisibility(UpdateVisibilityInput input) {
        try {
            return command.updateVisibility(input);
        } catch (StatusRuntimeException e) { throw StigmerException.wrap(e); }
    }

    public Agent updateSharing(UpdateAgentSharingInput input) {
        try {
            return command.updateSharing(input);
        } catch (StatusRuntimeException e) { throw StigmerException.wrap(e); }
    }

    public Agent delete(String id) {
        try {
            return command.delete(AgentId.newBuilder().setValue(id).build());
        } catch (StatusRuntimeException e) { throw StigmerException.wrap(e); }
    }

    public Agent get(String id) {
        try {
            return query.get(AgentId.newBuilder().setValue(id).build());
        } catch (StatusRuntimeException e) { throw StigmerException.wrap(e); }
    }

    public Agent getByReference(ResourceRef ref) {
        try {
            return query.getByReference(ref.toProto().toBuilder().setKind(ApiResourceKind.agent).build());
        } catch (StatusRuntimeException e) { throw StigmerException.wrap(e); }
    }

    public Agent getDefault(GetDefaultAgentRequest input) {
        try {
            return query.getDefault(input);
        } catch (StatusRuntimeException e) { throw StigmerException.wrap(e); }
    }

    public SharedAgentProfile getSharedProfile(ResourceRef ref) {
        try {
            return query.getSharedProfile(ref.toProto().toBuilder().setKind(ApiResourceKind.agent).build());
        } catch (StatusRuntimeException e) { throw StigmerException.wrap(e); }
    }

    public SharedAgentProfile getSharedProfileForMember(ResourceRef ref) {
        try {
            return query.getSharedProfileForMember(ref.toProto().toBuilder().setKind(ApiResourceKind.agent).build());
        } catch (StatusRuntimeException e) { throw StigmerException.wrap(e); }
    }

    public ListResult list(ListParams params) {
        try {
            SearchRequest.Builder req = SearchRequest.newBuilder()
                .addKinds(ApiResourceKind.agent);
            if (params.getOrg() != null) {
                req.setOrg(params.getOrg());
            }
            if (params.getQuery() != null) {
                req.setQuery(params.getQuery());
            }
            req.setExcludePublic(params.isExcludePublic());
            req.setCrossOrgPublic(params.isCrossOrgPublic());
            if (params.getPage() != null) {
                req.setPage(PageInfo.newBuilder()
                    .setNum(params.getPage().getNum())
                    .setSize(params.getPage().getSize())
                    .build());
            }
            SearchResponse resp = search.search(req.build());
            return new ListResult(resp.getEntriesList(), resp.getTotalCount(), resp.getTotalPages());
        } catch (StatusRuntimeException e) { throw StigmerException.wrap(e); }
    }
}
