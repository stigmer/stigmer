// Code generated by protoc-gen-go-grpc. DO NOT EDIT.
// versions:
// - protoc-gen-go-grpc v1.5.1
// - protoc             (unknown)
// source: ai/stigmer/agentic/agentchannel/v1/conversation_query.proto

package agentchannelv1

import (
	context "context"
	grpc "google.golang.org/grpc"
	codes "google.golang.org/grpc/codes"
	status "google.golang.org/grpc/status"
)

// This is a compile-time assertion to ensure that this generated file
// is compatible with the grpc package it is being compiled against.
// Requires gRPC-Go v1.64.0 or later.
const _ = grpc.SupportPackageIsVersion9

const (
	ChannelConversationQueryController_ListConversations_FullMethodName   = "/ai.stigmer.agentic.agentchannel.v1.ChannelConversationQueryController/listConversations"
	ChannelConversationQueryController_GetConversation_FullMethodName     = "/ai.stigmer.agentic.agentchannel.v1.ChannelConversationQueryController/getConversation"
	ChannelConversationQueryController_GetTimeline_FullMethodName         = "/ai.stigmer.agentic.agentchannel.v1.ChannelConversationQueryController/getTimeline"
	ChannelConversationQueryController_GetMediaDownloadUrl_FullMethodName = "/ai.stigmer.agentic.agentchannel.v1.ChannelConversationQueryController/getMediaDownloadUrl"
)

// ChannelConversationQueryControllerClient is the client API for ChannelConversationQueryController service.
//
// For semantics around ctx use and closing/ending streaming RPCs, please refer to https://pkg.go.dev/google.golang.org/grpc/?tab=doc#ClientConn.NewStream.
//
// ChannelConversationQueryController serves the console's conversation
// reads: the org-wide conversation list and each conversation's
// customer-visible timeline.
//
// @internal
// channel-conversations DD-003/DD-004: the query sibling of
// ChannelConversationCommandController, on the runtime surface beside the
// message_* triple (resource CRUD and runtime traffic never mix).
// Supersedes SessionQueryController.listByChannel as the console's
// conversation read (DD-004 D-g); listByChannel remains the session-level
// forensics read underneath a conversation. Cloud-first runtime: the OSS
// edition answers queries with empty results (the listMessagingChannels
// discovery-read posture) — "none" is the honest answer, not an error.
type ChannelConversationQueryControllerClient interface {
	// List an organization's channel conversations, newest activity first.
	//
	// Returns conversations across all of the org's channels the caller can
	// view, optionally filtered to one channel. Entries carry participation
	// state and the customer's display name.
	//
	// @internal
	// Org-wide read: no single object to authorize, so authorization is
	// in-handler — an FGA ListObjects over agent_channel#can_view scopes
	// the scan (the listByChannel two-stage precedent; DD-010 D-b). A
	// caller who can view no channel receives an empty list, never an
	// error. OSS answers empty (cloud-only runtime).
	ListConversations(ctx context.Context, in *ListChannelConversationsInput, opts ...grpc.CallOption) (*ChannelConversationList, error)
	// Get one conversation's identity and participation state.
	//
	// The single-row read behind a conversation detail view: who holds
	// control, whether the conversation needs attention and why, the
	// customer's display name, and the activity clocks. Answers NOT_FOUND
	// until the customer's first message creates the conversation.
	//
	// @internal
	// channel-conversations T04: the get sibling of listConversations, so
	// a deep-linked console view or an embedded conversation surface never
	// reconstructs one row by scanning list pages — and the open
	// conversation can poll its own participation state instead of riding
	// the list's slower refresh. Authorization is declarative on the
	// channel, exactly getTimeline's shape (DD-003 D-a: conversations
	// carry no per-conversation FGA tuples — the channel is the trust
	// boundary). NOT_FOUND deliberately covers the timeline-without-row
	// case (a proactive cold-send the customer never answered): getTimeline
	// may serve items while this read refuses, the same "the customer
	// wrote first" asymmetry reply's existing-conversation precondition
	// enforces (T03 Sitting 2's A8) — consoles render that as "controls
	// unlock when the customer writes", not as an error. OSS answers
	// NOT_FOUND unconditionally: this edition never materializes
	// conversations (cloud-only runtime), and a single-row get cannot
	// answer "empty" the way the sibling discovery reads do.
	GetConversation(ctx context.Context, in *GetChannelConversationInput, opts ...grpc.CallOption) (*ChannelConversation, error)
	// Get one conversation's timeline, newest first, cursor-paged.
	//
	// The timeline contains customer-visible items only: inbound customer
	// messages (including non-text kinds the platform cannot render),
	// delivered agent replies, and operator or platform sends. Execution
	// internals never appear.
	//
	// @internal
	// channel-conversations DD-004: stitched on read from the webhook
	// event store, the delivery store (via the same last-AI-message
	// extraction the delivery posted — never execution transcripts), and
	// the outbound ledger; internal-lane events join as the fourth source
	// in T03. Authorization is declarative on the channel: conversations
	// carry no per-conversation FGA tuples (DD-003 D-a) — the channel is
	// the trust boundary.
	GetTimeline(ctx context.Context, in *GetConversationTimelineInput, opts ...grpc.CallOption) (*ConversationTimeline, error)
	// Mint a short-lived download URL for one inbound timeline item's
	// media file (an image or document the customer sent).
	//
	// Answers NOT_FOUND when the item does not exist in this conversation
	// or carries no ingested media (a text item, or media the platform
	// declined to ingest).
	//
	// @internal
	// whatsapp-media DD-001 D4: addressed by (channel, conversation,
	// item_id) so the server resolves the storage key from its own row —
	// the wire never carries blob capabilities, and authorization is
	// declarative on the channel exactly like getTimeline (the channel is
	// the trust boundary, DD-003 D-a). Deliberately stricter than the
	// attachments-blob posture (authentication-only, ULID-as-capability)
	// that the runner's download path rides: this is the human-facing
	// read surface and law-firm client documents travel this pipeline.
	// OSS answers NOT_FOUND unconditionally (cloud-only runtime, the
	// getConversation posture).
	GetMediaDownloadUrl(ctx context.Context, in *GetConversationMediaDownloadUrlInput, opts ...grpc.CallOption) (*ConversationMediaDownloadUrl, error)
}

type channelConversationQueryControllerClient struct {
	cc grpc.ClientConnInterface
}

func NewChannelConversationQueryControllerClient(cc grpc.ClientConnInterface) ChannelConversationQueryControllerClient {
	return &channelConversationQueryControllerClient{cc}
}

func (c *channelConversationQueryControllerClient) ListConversations(ctx context.Context, in *ListChannelConversationsInput, opts ...grpc.CallOption) (*ChannelConversationList, error) {
	cOpts := append([]grpc.CallOption{grpc.StaticMethod()}, opts...)
	out := new(ChannelConversationList)
	err := c.cc.Invoke(ctx, ChannelConversationQueryController_ListConversations_FullMethodName, in, out, cOpts...)
	if err != nil {
		return nil, err
	}
	return out, nil
}

func (c *channelConversationQueryControllerClient) GetConversation(ctx context.Context, in *GetChannelConversationInput, opts ...grpc.CallOption) (*ChannelConversation, error) {
	cOpts := append([]grpc.CallOption{grpc.StaticMethod()}, opts...)
	out := new(ChannelConversation)
	err := c.cc.Invoke(ctx, ChannelConversationQueryController_GetConversation_FullMethodName, in, out, cOpts...)
	if err != nil {
		return nil, err
	}
	return out, nil
}

func (c *channelConversationQueryControllerClient) GetTimeline(ctx context.Context, in *GetConversationTimelineInput, opts ...grpc.CallOption) (*ConversationTimeline, error) {
	cOpts := append([]grpc.CallOption{grpc.StaticMethod()}, opts...)
	out := new(ConversationTimeline)
	err := c.cc.Invoke(ctx, ChannelConversationQueryController_GetTimeline_FullMethodName, in, out, cOpts...)
	if err != nil {
		return nil, err
	}
	return out, nil
}

func (c *channelConversationQueryControllerClient) GetMediaDownloadUrl(ctx context.Context, in *GetConversationMediaDownloadUrlInput, opts ...grpc.CallOption) (*ConversationMediaDownloadUrl, error) {
	cOpts := append([]grpc.CallOption{grpc.StaticMethod()}, opts...)
	out := new(ConversationMediaDownloadUrl)
	err := c.cc.Invoke(ctx, ChannelConversationQueryController_GetMediaDownloadUrl_FullMethodName, in, out, cOpts...)
	if err != nil {
		return nil, err
	}
	return out, nil
}

// ChannelConversationQueryControllerServer is the server API for ChannelConversationQueryController service.
// All implementations should embed UnimplementedChannelConversationQueryControllerServer
// for forward compatibility.
//
// ChannelConversationQueryController serves the console's conversation
// reads: the org-wide conversation list and each conversation's
// customer-visible timeline.
//
// @internal
// channel-conversations DD-003/DD-004: the query sibling of
// ChannelConversationCommandController, on the runtime surface beside the
// message_* triple (resource CRUD and runtime traffic never mix).
// Supersedes SessionQueryController.listByChannel as the console's
// conversation read (DD-004 D-g); listByChannel remains the session-level
// forensics read underneath a conversation. Cloud-first runtime: the OSS
// edition answers queries with empty results (the listMessagingChannels
// discovery-read posture) — "none" is the honest answer, not an error.
type ChannelConversationQueryControllerServer interface {
	// List an organization's channel conversations, newest activity first.
	//
	// Returns conversations across all of the org's channels the caller can
	// view, optionally filtered to one channel. Entries carry participation
	// state and the customer's display name.
	//
	// @internal
	// Org-wide read: no single object to authorize, so authorization is
	// in-handler — an FGA ListObjects over agent_channel#can_view scopes
	// the scan (the listByChannel two-stage precedent; DD-010 D-b). A
	// caller who can view no channel receives an empty list, never an
	// error. OSS answers empty (cloud-only runtime).
	ListConversations(context.Context, *ListChannelConversationsInput) (*ChannelConversationList, error)
	// Get one conversation's identity and participation state.
	//
	// The single-row read behind a conversation detail view: who holds
	// control, whether the conversation needs attention and why, the
	// customer's display name, and the activity clocks. Answers NOT_FOUND
	// until the customer's first message creates the conversation.
	//
	// @internal
	// channel-conversations T04: the get sibling of listConversations, so
	// a deep-linked console view or an embedded conversation surface never
	// reconstructs one row by scanning list pages — and the open
	// conversation can poll its own participation state instead of riding
	// the list's slower refresh. Authorization is declarative on the
	// channel, exactly getTimeline's shape (DD-003 D-a: conversations
	// carry no per-conversation FGA tuples — the channel is the trust
	// boundary). NOT_FOUND deliberately covers the timeline-without-row
	// case (a proactive cold-send the customer never answered): getTimeline
	// may serve items while this read refuses, the same "the customer
	// wrote first" asymmetry reply's existing-conversation precondition
	// enforces (T03 Sitting 2's A8) — consoles render that as "controls
	// unlock when the customer writes", not as an error. OSS answers
	// NOT_FOUND unconditionally: this edition never materializes
	// conversations (cloud-only runtime), and a single-row get cannot
	// answer "empty" the way the sibling discovery reads do.
	GetConversation(context.Context, *GetChannelConversationInput) (*ChannelConversation, error)
	// Get one conversation's timeline, newest first, cursor-paged.
	//
	// The timeline contains customer-visible items only: inbound customer
	// messages (including non-text kinds the platform cannot render),
	// delivered agent replies, and operator or platform sends. Execution
	// internals never appear.
	//
	// @internal
	// channel-conversations DD-004: stitched on read from the webhook
	// event store, the delivery store (via the same last-AI-message
	// extraction the delivery posted — never execution transcripts), and
	// the outbound ledger; internal-lane events join as the fourth source
	// in T03. Authorization is declarative on the channel: conversations
	// carry no per-conversation FGA tuples (DD-003 D-a) — the channel is
	// the trust boundary.
	GetTimeline(context.Context, *GetConversationTimelineInput) (*ConversationTimeline, error)
	// Mint a short-lived download URL for one inbound timeline item's
	// media file (an image or document the customer sent).
	//
	// Answers NOT_FOUND when the item does not exist in this conversation
	// or carries no ingested media (a text item, or media the platform
	// declined to ingest).
	//
	// @internal
	// whatsapp-media DD-001 D4: addressed by (channel, conversation,
	// item_id) so the server resolves the storage key from its own row —
	// the wire never carries blob capabilities, and authorization is
	// declarative on the channel exactly like getTimeline (the channel is
	// the trust boundary, DD-003 D-a). Deliberately stricter than the
	// attachments-blob posture (authentication-only, ULID-as-capability)
	// that the runner's download path rides: this is the human-facing
	// read surface and law-firm client documents travel this pipeline.
	// OSS answers NOT_FOUND unconditionally (cloud-only runtime, the
	// getConversation posture).
	GetMediaDownloadUrl(context.Context, *GetConversationMediaDownloadUrlInput) (*ConversationMediaDownloadUrl, error)
}

// UnimplementedChannelConversationQueryControllerServer should be embedded to have
// forward compatible implementations.
//
// NOTE: this should be embedded by value instead of pointer to avoid a nil
// pointer dereference when methods are called.
type UnimplementedChannelConversationQueryControllerServer struct{}

func (UnimplementedChannelConversationQueryControllerServer) ListConversations(context.Context, *ListChannelConversationsInput) (*ChannelConversationList, error) {
	return nil, status.Errorf(codes.Unimplemented, "method ListConversations not implemented")
}
func (UnimplementedChannelConversationQueryControllerServer) GetConversation(context.Context, *GetChannelConversationInput) (*ChannelConversation, error) {
	return nil, status.Errorf(codes.Unimplemented, "method GetConversation not implemented")
}
func (UnimplementedChannelConversationQueryControllerServer) GetTimeline(context.Context, *GetConversationTimelineInput) (*ConversationTimeline, error) {
	return nil, status.Errorf(codes.Unimplemented, "method GetTimeline not implemented")
}
func (UnimplementedChannelConversationQueryControllerServer) GetMediaDownloadUrl(context.Context, *GetConversationMediaDownloadUrlInput) (*ConversationMediaDownloadUrl, error) {
	return nil, status.Errorf(codes.Unimplemented, "method GetMediaDownloadUrl not implemented")
}
func (UnimplementedChannelConversationQueryControllerServer) testEmbeddedByValue() {}

// UnsafeChannelConversationQueryControllerServer may be embedded to opt out of forward compatibility for this service.
// Use of this interface is not recommended, as added methods to ChannelConversationQueryControllerServer will
// result in compilation errors.
type UnsafeChannelConversationQueryControllerServer interface {
	mustEmbedUnimplementedChannelConversationQueryControllerServer()
}

func RegisterChannelConversationQueryControllerServer(s grpc.ServiceRegistrar, srv ChannelConversationQueryControllerServer) {
	// If the following call pancis, it indicates UnimplementedChannelConversationQueryControllerServer was
	// embedded by pointer and is nil.  This will cause panics if an
	// unimplemented method is ever invoked, so we test this at initialization
	// time to prevent it from happening at runtime later due to I/O.
	if t, ok := srv.(interface{ testEmbeddedByValue() }); ok {
		t.testEmbeddedByValue()
	}
	s.RegisterService(&ChannelConversationQueryController_ServiceDesc, srv)
}

func _ChannelConversationQueryController_ListConversations_Handler(srv interface{}, ctx context.Context, dec func(interface{}) error, interceptor grpc.UnaryServerInterceptor) (interface{}, error) {
	in := new(ListChannelConversationsInput)
	if err := dec(in); err != nil {
		return nil, err
	}
	if interceptor == nil {
		return srv.(ChannelConversationQueryControllerServer).ListConversations(ctx, in)
	}
	info := &grpc.UnaryServerInfo{
		Server:     srv,
		FullMethod: ChannelConversationQueryController_ListConversations_FullMethodName,
	}
	handler := func(ctx context.Context, req interface{}) (interface{}, error) {
		return srv.(ChannelConversationQueryControllerServer).ListConversations(ctx, req.(*ListChannelConversationsInput))
	}
	return interceptor(ctx, in, info, handler)
}

func _ChannelConversationQueryController_GetConversation_Handler(srv interface{}, ctx context.Context, dec func(interface{}) error, interceptor grpc.UnaryServerInterceptor) (interface{}, error) {
	in := new(GetChannelConversationInput)
	if err := dec(in); err != nil {
		return nil, err
	}
	if interceptor == nil {
		return srv.(ChannelConversationQueryControllerServer).GetConversation(ctx, in)
	}
	info := &grpc.UnaryServerInfo{
		Server:     srv,
		FullMethod: ChannelConversationQueryController_GetConversation_FullMethodName,
	}
	handler := func(ctx context.Context, req interface{}) (interface{}, error) {
		return srv.(ChannelConversationQueryControllerServer).GetConversation(ctx, req.(*GetChannelConversationInput))
	}
	return interceptor(ctx, in, info, handler)
}

func _ChannelConversationQueryController_GetTimeline_Handler(srv interface{}, ctx context.Context, dec func(interface{}) error, interceptor grpc.UnaryServerInterceptor) (interface{}, error) {
	in := new(GetConversationTimelineInput)
	if err := dec(in); err != nil {
		return nil, err
	}
	if interceptor == nil {
		return srv.(ChannelConversationQueryControllerServer).GetTimeline(ctx, in)
	}
	info := &grpc.UnaryServerInfo{
		Server:     srv,
		FullMethod: ChannelConversationQueryController_GetTimeline_FullMethodName,
	}
	handler := func(ctx context.Context, req interface{}) (interface{}, error) {
		return srv.(ChannelConversationQueryControllerServer).GetTimeline(ctx, req.(*GetConversationTimelineInput))
	}
	return interceptor(ctx, in, info, handler)
}

func _ChannelConversationQueryController_GetMediaDownloadUrl_Handler(srv interface{}, ctx context.Context, dec func(interface{}) error, interceptor grpc.UnaryServerInterceptor) (interface{}, error) {
	in := new(GetConversationMediaDownloadUrlInput)
	if err := dec(in); err != nil {
		return nil, err
	}
	if interceptor == nil {
		return srv.(ChannelConversationQueryControllerServer).GetMediaDownloadUrl(ctx, in)
	}
	info := &grpc.UnaryServerInfo{
		Server:     srv,
		FullMethod: ChannelConversationQueryController_GetMediaDownloadUrl_FullMethodName,
	}
	handler := func(ctx context.Context, req interface{}) (interface{}, error) {
		return srv.(ChannelConversationQueryControllerServer).GetMediaDownloadUrl(ctx, req.(*GetConversationMediaDownloadUrlInput))
	}
	return interceptor(ctx, in, info, handler)
}

// ChannelConversationQueryController_ServiceDesc is the grpc.ServiceDesc for ChannelConversationQueryController service.
// It's only intended for direct use with grpc.RegisterService,
// and not to be introspected or modified (even as a copy)
var ChannelConversationQueryController_ServiceDesc = grpc.ServiceDesc{
	ServiceName: "ai.stigmer.agentic.agentchannel.v1.ChannelConversationQueryController",
	HandlerType: (*ChannelConversationQueryControllerServer)(nil),
	Methods: []grpc.MethodDesc{
		{
			MethodName: "listConversations",
			Handler:    _ChannelConversationQueryController_ListConversations_Handler,
		},
		{
			MethodName: "getConversation",
			Handler:    _ChannelConversationQueryController_GetConversation_Handler,
		},
		{
			MethodName: "getTimeline",
			Handler:    _ChannelConversationQueryController_GetTimeline_Handler,
		},
		{
			MethodName: "getMediaDownloadUrl",
			Handler:    _ChannelConversationQueryController_GetMediaDownloadUrl_Handler,
		},
	},
	Streams:  []grpc.StreamDesc{},
	Metadata: "ai/stigmer/agentic/agentchannel/v1/conversation_query.proto",
}
