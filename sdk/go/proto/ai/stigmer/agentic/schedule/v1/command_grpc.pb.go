// Code generated by protoc-gen-go-grpc. DO NOT EDIT.
// versions:
// - protoc-gen-go-grpc v1.5.1
// - protoc             (unknown)
// source: ai/stigmer/agentic/schedule/v1/command.proto

package schedulev1

import (
	context "context"
	grpc "google.golang.org/grpc"
	codes "google.golang.org/grpc/codes"
	status "google.golang.org/grpc/status"
)

// This is a compile-time assertion to ensure that this generated file
// is compatible with the grpc package it is being compiled against.
// Requires gRPC-Go v1.64.0 or later.
const _ = grpc.SupportPackageIsVersion9

const (
	ScheduleCommandController_Apply_FullMethodName   = "/ai.stigmer.agentic.schedule.v1.ScheduleCommandController/apply"
	ScheduleCommandController_Create_FullMethodName  = "/ai.stigmer.agentic.schedule.v1.ScheduleCommandController/create"
	ScheduleCommandController_Update_FullMethodName  = "/ai.stigmer.agentic.schedule.v1.ScheduleCommandController/update"
	ScheduleCommandController_Delete_FullMethodName  = "/ai.stigmer.agentic.schedule.v1.ScheduleCommandController/delete"
	ScheduleCommandController_Resume_FullMethodName  = "/ai.stigmer.agentic.schedule.v1.ScheduleCommandController/resume"
	ScheduleCommandController_Trigger_FullMethodName = "/ai.stigmer.agentic.schedule.v1.ScheduleCommandController/trigger"
)

// ScheduleCommandControllerClient is the client API for ScheduleCommandController service.
//
// For semantics around ctx use and closing/ending streaming RPCs, please refer to https://pkg.go.dev/google.golang.org/grpc/?tab=doc#ClientConn.NewStream.
//
// ScheduleCommandController handles write operations for schedules.
type ScheduleCommandControllerClient interface {
	// Create or update a schedule.
	//
	// @internal
	// The authorization and state-operation are determined depending on
	// whether the schedule is going to be created or updated, resolved as
	// part of request execution. Status is preserved verbatim across
	// apply-as-update (the AgentChannel decision-004 posture): status is
	// written only by the scheduling runtime and by the explicit resume
	// command, and a routine manifest apply must never reset the failure
	// streak or un-pause a platform-paused schedule (DD-008 D7 / DD-009
	// pinned behaviors / DD-013 D-D).
	Apply(ctx context.Context, in *Schedule, opts ...grpc.CallOption) (*Schedule, error)
	// Create a schedule.
	//
	// Scheduling an agent is a billing-affecting decision: every fire
	// creates an execution that consumes the schedule-owning
	// organization's credits, unattended.
	//
	// @internal
	// Authorization: requires can_edit on the REFERENCED AGENT
	// (spec.agent.agent_ref), checked in-handler — the AgentChannel /
	// same-org AgentShare Phase A bar (DD-009 C-6): whoever may edit the
	// agent may schedule it; there is deliberately no org-level
	// can_create_schedule permission. Standard org-scoped create tuples
	// (owner = creator); no visibility tuples (the kind has no visibility
	// block). Invariant enforced here: metadata.org must equal
	// spec.agent.agent_ref.org.
	Create(ctx context.Context, in *Schedule, opts ...grpc.CallOption) (*Schedule, error)
	// Update an existing schedule.
	//
	// Replaces the spec wholesale. The slug, the referenced agent, and the
	// target arm are immutable; cron, time zone, enablement, and the
	// message may all change. Status (firing observations, the platform
	// pause) is never touched by updates — use resume to clear a platform
	// pause.
	//
	// @internal
	// Authorization: requires can_edit permission on the schedule.
	// agent_ref immutability is a consent-bar guarantee, not convenience
	// (DD-009 C-7): create's bar is can_edit on the REFERENCED agent, and
	// a repointing update would let a schedule owner drive an agent they
	// may not edit — the AgentChannel rule for the AgentChannel reason.
	// Target-arm immutability (an agent schedule cannot become a workflow
	// schedule once that arm exists) is enforced in-handler: the two
	// targets enter different execution pipelines. Update deliberately
	// does NOT clear a platform auto-pause (DD-013 D-D, superseding the
	// DD-008 D7 ensure-on-mutate sketch): apply routes through this same
	// handler, so any update-clears-pause behavior would let a routine
	// GitOps re-apply silently un-pause a failing schedule. resume is the
	// one clearing path.
	Update(ctx context.Context, in *Schedule, opts ...grpc.CallOption) (*Schedule, error)
	// Delete a schedule.
	//
	// Firing stops permanently. Executions created by past fires are
	// untouched. To stop firing while keeping the schedule and its
	// history, disable it (enabled=false) instead.
	//
	// @internal
	// Authorization: requires can_delete permission on the schedule. The
	// referenced agent is untouched. The Temporal artifact teardown
	// (best-effort, AFTER the row delete — DD-008 D9) arrives with the
	// clock; until then delete is the row alone, and an orphaned artifact
	// is harmless by construction (fire-time revalidation no-ops it).
	Delete(ctx context.Context, in *ScheduleId, opts ...grpc.CallOption) (*Schedule, error)
	// Resume a schedule the platform paused after repeated run failures.
	//
	// Clears status.paused_reason, resets status.consecutive_failures,
	// and re-arms the clock. Resuming a schedule that is not paused
	// succeeds and changes nothing. A disabled schedule stays disabled:
	// resume clears the platform's pause, not the owner's switch
	// (spec.enabled).
	//
	// @internal
	// Authorization: requires can_edit permission on the schedule — the
	// update bar; resuming does not change which agent runs, so it does
	// not re-open create's consent bar (DD-009 C-6). This command is
	// deliberately the ONLY path that clears a platform auto-pause
	// (DD-013 D-D): apply and update preserve status byte-for-byte, so a
	// routine manifest apply can never silently un-pause a failing
	// schedule. The cloud handler loads before authorizing (#224: a
	// missing schedule answers NOT_FOUND, not PERMISSION_DENIED) and
	// patches status leaves rather than saving the row — the tick is a
	// concurrent status writer. OSS excludes the authorization step, per
	// its recorded single-user posture.
	Resume(ctx context.Context, in *ScheduleId, opts ...grpc.CallOption) (*Schedule, error)
	// Trigger a schedule to fire once, immediately, and answer with the
	// run's real outcome.
	//
	// The manual fire runs synchronously through the standard execution
	// create pipeline — every launch gate runs — and the result names what
	// happened: the created execution's id, or the refusing gate's own
	// copy verbatim. status.last_fire_at and status.last_execution_id
	// record a started run. Manual fires do NOT feed the failure streak —
	// the streak is the unattended (cron) health signal, and a test fire
	// of a broken schedule must not race its owner to the pause threshold.
	//
	// A disabled schedule refuses (enable it first). A platform-paused
	// schedule MAY be triggered — a test fire is exactly how an owner
	// verifies a fix before resuming; resume remains the one path that
	// clears the pause.
	//
	// @internal
	// Authorization: requires can_edit permission on the schedule — the
	// update bar (DD-014 D-A). Project DD-017 D-5/D-6 amends DD-014: the
	// artifact round-trip is gone for manual fires (it made the fire
	// asynchronous, so the RPC answered before the launch gates ran — a
	// false "run started" beside a climbing failure counter). Cron fires
	// keep the tracked artifact tick unchanged. The disabled refusal
	// SURVIVES for a different reason than DD-014 D-B recorded:
	// ScheduleBlueprintAccess requires spec.enabled at the create gate AND
	// the mid-run sandbox read predicate, so a disabled-schedule run would
	// die mid-execution after billing side effects (consoles offer
	// "Enable & run now" instead; the fire-legitimacy model that would
	// lift this is DD-017's named follow-up). Two-level contract: a gRPC
	// error means the trigger itself was refused (disabled →
	// FAILED_PRECONDITION with the byte-pinned copy, missing → NOT_FOUND
	// before authorize, #224); a gRPC success means the fire happened and
	// ScheduleTriggerResult names the run's outcome — a deterministically
	// refused run is a successful trigger honestly reported, never an
	// exception. The handler stamps last_fire_at/last_execution_id and
	// writes the fire-ledger row (origin=manual) because the tick is not
	// in the path to do it; status writes ride updateFields, never save()
	// (DD-010 D-B). OSS mirrors the semantics; it excludes the
	// authorization step per its recorded single-user posture.
	Trigger(ctx context.Context, in *ScheduleId, opts ...grpc.CallOption) (*ScheduleTriggerResult, error)
}

type scheduleCommandControllerClient struct {
	cc grpc.ClientConnInterface
}

func NewScheduleCommandControllerClient(cc grpc.ClientConnInterface) ScheduleCommandControllerClient {
	return &scheduleCommandControllerClient{cc}
}

func (c *scheduleCommandControllerClient) Apply(ctx context.Context, in *Schedule, opts ...grpc.CallOption) (*Schedule, error) {
	cOpts := append([]grpc.CallOption{grpc.StaticMethod()}, opts...)
	out := new(Schedule)
	err := c.cc.Invoke(ctx, ScheduleCommandController_Apply_FullMethodName, in, out, cOpts...)
	if err != nil {
		return nil, err
	}
	return out, nil
}

func (c *scheduleCommandControllerClient) Create(ctx context.Context, in *Schedule, opts ...grpc.CallOption) (*Schedule, error) {
	cOpts := append([]grpc.CallOption{grpc.StaticMethod()}, opts...)
	out := new(Schedule)
	err := c.cc.Invoke(ctx, ScheduleCommandController_Create_FullMethodName, in, out, cOpts...)
	if err != nil {
		return nil, err
	}
	return out, nil
}

func (c *scheduleCommandControllerClient) Update(ctx context.Context, in *Schedule, opts ...grpc.CallOption) (*Schedule, error) {
	cOpts := append([]grpc.CallOption{grpc.StaticMethod()}, opts...)
	out := new(Schedule)
	err := c.cc.Invoke(ctx, ScheduleCommandController_Update_FullMethodName, in, out, cOpts...)
	if err != nil {
		return nil, err
	}
	return out, nil
}

func (c *scheduleCommandControllerClient) Delete(ctx context.Context, in *ScheduleId, opts ...grpc.CallOption) (*Schedule, error) {
	cOpts := append([]grpc.CallOption{grpc.StaticMethod()}, opts...)
	out := new(Schedule)
	err := c.cc.Invoke(ctx, ScheduleCommandController_Delete_FullMethodName, in, out, cOpts...)
	if err != nil {
		return nil, err
	}
	return out, nil
}

func (c *scheduleCommandControllerClient) Resume(ctx context.Context, in *ScheduleId, opts ...grpc.CallOption) (*Schedule, error) {
	cOpts := append([]grpc.CallOption{grpc.StaticMethod()}, opts...)
	out := new(Schedule)
	err := c.cc.Invoke(ctx, ScheduleCommandController_Resume_FullMethodName, in, out, cOpts...)
	if err != nil {
		return nil, err
	}
	return out, nil
}

func (c *scheduleCommandControllerClient) Trigger(ctx context.Context, in *ScheduleId, opts ...grpc.CallOption) (*ScheduleTriggerResult, error) {
	cOpts := append([]grpc.CallOption{grpc.StaticMethod()}, opts...)
	out := new(ScheduleTriggerResult)
	err := c.cc.Invoke(ctx, ScheduleCommandController_Trigger_FullMethodName, in, out, cOpts...)
	if err != nil {
		return nil, err
	}
	return out, nil
}

// ScheduleCommandControllerServer is the server API for ScheduleCommandController service.
// All implementations should embed UnimplementedScheduleCommandControllerServer
// for forward compatibility.
//
// ScheduleCommandController handles write operations for schedules.
type ScheduleCommandControllerServer interface {
	// Create or update a schedule.
	//
	// @internal
	// The authorization and state-operation are determined depending on
	// whether the schedule is going to be created or updated, resolved as
	// part of request execution. Status is preserved verbatim across
	// apply-as-update (the AgentChannel decision-004 posture): status is
	// written only by the scheduling runtime and by the explicit resume
	// command, and a routine manifest apply must never reset the failure
	// streak or un-pause a platform-paused schedule (DD-008 D7 / DD-009
	// pinned behaviors / DD-013 D-D).
	Apply(context.Context, *Schedule) (*Schedule, error)
	// Create a schedule.
	//
	// Scheduling an agent is a billing-affecting decision: every fire
	// creates an execution that consumes the schedule-owning
	// organization's credits, unattended.
	//
	// @internal
	// Authorization: requires can_edit on the REFERENCED AGENT
	// (spec.agent.agent_ref), checked in-handler — the AgentChannel /
	// same-org AgentShare Phase A bar (DD-009 C-6): whoever may edit the
	// agent may schedule it; there is deliberately no org-level
	// can_create_schedule permission. Standard org-scoped create tuples
	// (owner = creator); no visibility tuples (the kind has no visibility
	// block). Invariant enforced here: metadata.org must equal
	// spec.agent.agent_ref.org.
	Create(context.Context, *Schedule) (*Schedule, error)
	// Update an existing schedule.
	//
	// Replaces the spec wholesale. The slug, the referenced agent, and the
	// target arm are immutable; cron, time zone, enablement, and the
	// message may all change. Status (firing observations, the platform
	// pause) is never touched by updates — use resume to clear a platform
	// pause.
	//
	// @internal
	// Authorization: requires can_edit permission on the schedule.
	// agent_ref immutability is a consent-bar guarantee, not convenience
	// (DD-009 C-7): create's bar is can_edit on the REFERENCED agent, and
	// a repointing update would let a schedule owner drive an agent they
	// may not edit — the AgentChannel rule for the AgentChannel reason.
	// Target-arm immutability (an agent schedule cannot become a workflow
	// schedule once that arm exists) is enforced in-handler: the two
	// targets enter different execution pipelines. Update deliberately
	// does NOT clear a platform auto-pause (DD-013 D-D, superseding the
	// DD-008 D7 ensure-on-mutate sketch): apply routes through this same
	// handler, so any update-clears-pause behavior would let a routine
	// GitOps re-apply silently un-pause a failing schedule. resume is the
	// one clearing path.
	Update(context.Context, *Schedule) (*Schedule, error)
	// Delete a schedule.
	//
	// Firing stops permanently. Executions created by past fires are
	// untouched. To stop firing while keeping the schedule and its
	// history, disable it (enabled=false) instead.
	//
	// @internal
	// Authorization: requires can_delete permission on the schedule. The
	// referenced agent is untouched. The Temporal artifact teardown
	// (best-effort, AFTER the row delete — DD-008 D9) arrives with the
	// clock; until then delete is the row alone, and an orphaned artifact
	// is harmless by construction (fire-time revalidation no-ops it).
	Delete(context.Context, *ScheduleId) (*Schedule, error)
	// Resume a schedule the platform paused after repeated run failures.
	//
	// Clears status.paused_reason, resets status.consecutive_failures,
	// and re-arms the clock. Resuming a schedule that is not paused
	// succeeds and changes nothing. A disabled schedule stays disabled:
	// resume clears the platform's pause, not the owner's switch
	// (spec.enabled).
	//
	// @internal
	// Authorization: requires can_edit permission on the schedule — the
	// update bar; resuming does not change which agent runs, so it does
	// not re-open create's consent bar (DD-009 C-6). This command is
	// deliberately the ONLY path that clears a platform auto-pause
	// (DD-013 D-D): apply and update preserve status byte-for-byte, so a
	// routine manifest apply can never silently un-pause a failing
	// schedule. The cloud handler loads before authorizing (#224: a
	// missing schedule answers NOT_FOUND, not PERMISSION_DENIED) and
	// patches status leaves rather than saving the row — the tick is a
	// concurrent status writer. OSS excludes the authorization step, per
	// its recorded single-user posture.
	Resume(context.Context, *ScheduleId) (*Schedule, error)
	// Trigger a schedule to fire once, immediately, and answer with the
	// run's real outcome.
	//
	// The manual fire runs synchronously through the standard execution
	// create pipeline — every launch gate runs — and the result names what
	// happened: the created execution's id, or the refusing gate's own
	// copy verbatim. status.last_fire_at and status.last_execution_id
	// record a started run. Manual fires do NOT feed the failure streak —
	// the streak is the unattended (cron) health signal, and a test fire
	// of a broken schedule must not race its owner to the pause threshold.
	//
	// A disabled schedule refuses (enable it first). A platform-paused
	// schedule MAY be triggered — a test fire is exactly how an owner
	// verifies a fix before resuming; resume remains the one path that
	// clears the pause.
	//
	// @internal
	// Authorization: requires can_edit permission on the schedule — the
	// update bar (DD-014 D-A). Project DD-017 D-5/D-6 amends DD-014: the
	// artifact round-trip is gone for manual fires (it made the fire
	// asynchronous, so the RPC answered before the launch gates ran — a
	// false "run started" beside a climbing failure counter). Cron fires
	// keep the tracked artifact tick unchanged. The disabled refusal
	// SURVIVES for a different reason than DD-014 D-B recorded:
	// ScheduleBlueprintAccess requires spec.enabled at the create gate AND
	// the mid-run sandbox read predicate, so a disabled-schedule run would
	// die mid-execution after billing side effects (consoles offer
	// "Enable & run now" instead; the fire-legitimacy model that would
	// lift this is DD-017's named follow-up). Two-level contract: a gRPC
	// error means the trigger itself was refused (disabled →
	// FAILED_PRECONDITION with the byte-pinned copy, missing → NOT_FOUND
	// before authorize, #224); a gRPC success means the fire happened and
	// ScheduleTriggerResult names the run's outcome — a deterministically
	// refused run is a successful trigger honestly reported, never an
	// exception. The handler stamps last_fire_at/last_execution_id and
	// writes the fire-ledger row (origin=manual) because the tick is not
	// in the path to do it; status writes ride updateFields, never save()
	// (DD-010 D-B). OSS mirrors the semantics; it excludes the
	// authorization step per its recorded single-user posture.
	Trigger(context.Context, *ScheduleId) (*ScheduleTriggerResult, error)
}

// UnimplementedScheduleCommandControllerServer should be embedded to have
// forward compatible implementations.
//
// NOTE: this should be embedded by value instead of pointer to avoid a nil
// pointer dereference when methods are called.
type UnimplementedScheduleCommandControllerServer struct{}

func (UnimplementedScheduleCommandControllerServer) Apply(context.Context, *Schedule) (*Schedule, error) {
	return nil, status.Errorf(codes.Unimplemented, "method Apply not implemented")
}
func (UnimplementedScheduleCommandControllerServer) Create(context.Context, *Schedule) (*Schedule, error) {
	return nil, status.Errorf(codes.Unimplemented, "method Create not implemented")
}
func (UnimplementedScheduleCommandControllerServer) Update(context.Context, *Schedule) (*Schedule, error) {
	return nil, status.Errorf(codes.Unimplemented, "method Update not implemented")
}
func (UnimplementedScheduleCommandControllerServer) Delete(context.Context, *ScheduleId) (*Schedule, error) {
	return nil, status.Errorf(codes.Unimplemented, "method Delete not implemented")
}
func (UnimplementedScheduleCommandControllerServer) Resume(context.Context, *ScheduleId) (*Schedule, error) {
	return nil, status.Errorf(codes.Unimplemented, "method Resume not implemented")
}
func (UnimplementedScheduleCommandControllerServer) Trigger(context.Context, *ScheduleId) (*ScheduleTriggerResult, error) {
	return nil, status.Errorf(codes.Unimplemented, "method Trigger not implemented")
}
func (UnimplementedScheduleCommandControllerServer) testEmbeddedByValue() {}

// UnsafeScheduleCommandControllerServer may be embedded to opt out of forward compatibility for this service.
// Use of this interface is not recommended, as added methods to ScheduleCommandControllerServer will
// result in compilation errors.
type UnsafeScheduleCommandControllerServer interface {
	mustEmbedUnimplementedScheduleCommandControllerServer()
}

func RegisterScheduleCommandControllerServer(s grpc.ServiceRegistrar, srv ScheduleCommandControllerServer) {
	// If the following call pancis, it indicates UnimplementedScheduleCommandControllerServer was
	// embedded by pointer and is nil.  This will cause panics if an
	// unimplemented method is ever invoked, so we test this at initialization
	// time to prevent it from happening at runtime later due to I/O.
	if t, ok := srv.(interface{ testEmbeddedByValue() }); ok {
		t.testEmbeddedByValue()
	}
	s.RegisterService(&ScheduleCommandController_ServiceDesc, srv)
}

func _ScheduleCommandController_Apply_Handler(srv interface{}, ctx context.Context, dec func(interface{}) error, interceptor grpc.UnaryServerInterceptor) (interface{}, error) {
	in := new(Schedule)
	if err := dec(in); err != nil {
		return nil, err
	}
	if interceptor == nil {
		return srv.(ScheduleCommandControllerServer).Apply(ctx, in)
	}
	info := &grpc.UnaryServerInfo{
		Server:     srv,
		FullMethod: ScheduleCommandController_Apply_FullMethodName,
	}
	handler := func(ctx context.Context, req interface{}) (interface{}, error) {
		return srv.(ScheduleCommandControllerServer).Apply(ctx, req.(*Schedule))
	}
	return interceptor(ctx, in, info, handler)
}

func _ScheduleCommandController_Create_Handler(srv interface{}, ctx context.Context, dec func(interface{}) error, interceptor grpc.UnaryServerInterceptor) (interface{}, error) {
	in := new(Schedule)
	if err := dec(in); err != nil {
		return nil, err
	}
	if interceptor == nil {
		return srv.(ScheduleCommandControllerServer).Create(ctx, in)
	}
	info := &grpc.UnaryServerInfo{
		Server:     srv,
		FullMethod: ScheduleCommandController_Create_FullMethodName,
	}
	handler := func(ctx context.Context, req interface{}) (interface{}, error) {
		return srv.(ScheduleCommandControllerServer).Create(ctx, req.(*Schedule))
	}
	return interceptor(ctx, in, info, handler)
}

func _ScheduleCommandController_Update_Handler(srv interface{}, ctx context.Context, dec func(interface{}) error, interceptor grpc.UnaryServerInterceptor) (interface{}, error) {
	in := new(Schedule)
	if err := dec(in); err != nil {
		return nil, err
	}
	if interceptor == nil {
		return srv.(ScheduleCommandControllerServer).Update(ctx, in)
	}
	info := &grpc.UnaryServerInfo{
		Server:     srv,
		FullMethod: ScheduleCommandController_Update_FullMethodName,
	}
	handler := func(ctx context.Context, req interface{}) (interface{}, error) {
		return srv.(ScheduleCommandControllerServer).Update(ctx, req.(*Schedule))
	}
	return interceptor(ctx, in, info, handler)
}

func _ScheduleCommandController_Delete_Handler(srv interface{}, ctx context.Context, dec func(interface{}) error, interceptor grpc.UnaryServerInterceptor) (interface{}, error) {
	in := new(ScheduleId)
	if err := dec(in); err != nil {
		return nil, err
	}
	if interceptor == nil {
		return srv.(ScheduleCommandControllerServer).Delete(ctx, in)
	}
	info := &grpc.UnaryServerInfo{
		Server:     srv,
		FullMethod: ScheduleCommandController_Delete_FullMethodName,
	}
	handler := func(ctx context.Context, req interface{}) (interface{}, error) {
		return srv.(ScheduleCommandControllerServer).Delete(ctx, req.(*ScheduleId))
	}
	return interceptor(ctx, in, info, handler)
}

func _ScheduleCommandController_Resume_Handler(srv interface{}, ctx context.Context, dec func(interface{}) error, interceptor grpc.UnaryServerInterceptor) (interface{}, error) {
	in := new(ScheduleId)
	if err := dec(in); err != nil {
		return nil, err
	}
	if interceptor == nil {
		return srv.(ScheduleCommandControllerServer).Resume(ctx, in)
	}
	info := &grpc.UnaryServerInfo{
		Server:     srv,
		FullMethod: ScheduleCommandController_Resume_FullMethodName,
	}
	handler := func(ctx context.Context, req interface{}) (interface{}, error) {
		return srv.(ScheduleCommandControllerServer).Resume(ctx, req.(*ScheduleId))
	}
	return interceptor(ctx, in, info, handler)
}

func _ScheduleCommandController_Trigger_Handler(srv interface{}, ctx context.Context, dec func(interface{}) error, interceptor grpc.UnaryServerInterceptor) (interface{}, error) {
	in := new(ScheduleId)
	if err := dec(in); err != nil {
		return nil, err
	}
	if interceptor == nil {
		return srv.(ScheduleCommandControllerServer).Trigger(ctx, in)
	}
	info := &grpc.UnaryServerInfo{
		Server:     srv,
		FullMethod: ScheduleCommandController_Trigger_FullMethodName,
	}
	handler := func(ctx context.Context, req interface{}) (interface{}, error) {
		return srv.(ScheduleCommandControllerServer).Trigger(ctx, req.(*ScheduleId))
	}
	return interceptor(ctx, in, info, handler)
}

// ScheduleCommandController_ServiceDesc is the grpc.ServiceDesc for ScheduleCommandController service.
// It's only intended for direct use with grpc.RegisterService,
// and not to be introspected or modified (even as a copy)
var ScheduleCommandController_ServiceDesc = grpc.ServiceDesc{
	ServiceName: "ai.stigmer.agentic.schedule.v1.ScheduleCommandController",
	HandlerType: (*ScheduleCommandControllerServer)(nil),
	Methods: []grpc.MethodDesc{
		{
			MethodName: "apply",
			Handler:    _ScheduleCommandController_Apply_Handler,
		},
		{
			MethodName: "create",
			Handler:    _ScheduleCommandController_Create_Handler,
		},
		{
			MethodName: "update",
			Handler:    _ScheduleCommandController_Update_Handler,
		},
		{
			MethodName: "delete",
			Handler:    _ScheduleCommandController_Delete_Handler,
		},
		{
			MethodName: "resume",
			Handler:    _ScheduleCommandController_Resume_Handler,
		},
		{
			MethodName: "trigger",
			Handler:    _ScheduleCommandController_Trigger_Handler,
		},
	},
	Streams:  []grpc.StreamDesc{},
	Metadata: "ai/stigmer/agentic/schedule/v1/command.proto",
}
