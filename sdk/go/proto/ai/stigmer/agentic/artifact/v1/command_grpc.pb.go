// Code generated by protoc-gen-go-grpc. DO NOT EDIT.
// versions:
// - protoc-gen-go-grpc v1.5.1
// - protoc             (unknown)
// source: ai/stigmer/agentic/artifact/v1/command.proto

package artifactv1

import (
	context "context"
	apiresource "github.com/stigmer/stigmer/sdk/go/v3/proto/ai/stigmer/commons/apiresource"
	grpc "google.golang.org/grpc"
	codes "google.golang.org/grpc/codes"
	status "google.golang.org/grpc/status"
)

// This is a compile-time assertion to ensure that this generated file
// is compatible with the grpc package it is being compiled against.
// Requires gRPC-Go v1.64.0 or later.
const _ = grpc.SupportPackageIsVersion9

const (
	ArtifactCommandController_Create_FullMethodName = "/ai.stigmer.agentic.artifact.v1.ArtifactCommandController/create"
	ArtifactCommandController_Delete_FullMethodName = "/ai.stigmer.agentic.artifact.v1.ArtifactCommandController/delete"
)

// ArtifactCommandControllerClient is the client API for ArtifactCommandController service.
//
// For semantics around ctx use and closing/ending streaming RPCs, please refer to https://pkg.go.dev/google.golang.org/grpc/?tab=doc#ClientConn.NewStream.
//
// ArtifactCommandController handles write operations for Artifact resources.
//
// @internal
// Follows the Command-Query Separation (CQS) pattern.
//
// These RPCs are system-level — used by the runner (stigmer-runner)
// to persist task outputs. They are NOT exposed to end users or the SDK.
//
// Artifact creation flow:
// 1. Runner detects output exceeding auto-promotion threshold (256KB)
// 2. Runner calls create() with spec (metadata) + content (bytes)
// 3. Backend hashes content, deduplicates, stores blob, creates record
// 4. Runner receives Artifact with ID and status
// 5. Runner replaces inline task output with artifact reference
// 6. Runner includes artifact_created event in next updateStatus call
//
// @since T07 (Artifact Store)
type ArtifactCommandControllerClient interface {
	// Create a new artifact with content.
	//
	// Persists the artifact's metadata and content blob. The backend:
	// 1. Computes SHA-256 hash of the content bytes
	// 2. Checks for existing blob with same hash (content-addressable dedup)
	// 3. If new: writes blob to storage (filesystem in OSS, S3 in Cloud)
	// 4. Creates Artifact metadata record with status populated
	// 5. Returns the created Artifact
	//
	// @internal
	// Authorization: skip_authorization (system-level RPC, called by runners)
	// The runner authenticates via service identity, not user credentials.
	//
	// Idempotent by content hash: creating the same content twice returns
	// two distinct Artifact metadata records pointing to the same blob.
	// This is intentional — different tasks may independently produce the
	// same content, and each needs its own provenance trail.
	//
	// Error Cases:
	//
	// - INVALID_ARGUMENT: spec or content is missing/invalid
	// - RESOURCE_EXHAUSTED: content exceeds 50MB limit
	// - INTERNAL: blob storage write failure
	Create(ctx context.Context, in *CreateArtifactInput, opts ...grpc.CallOption) (*Artifact, error)
	// Delete an artifact.
	//
	// Marks the artifact for deletion. The backend transitions the storage
	// state to deleted and schedules the blob for garbage collection.
	// If other artifacts reference the same content hash, the blob is
	// retained until all references are deleted.
	//
	// @internal
	// Authorization: requires can_edit permission on the artifact.
	//
	// Error Cases:
	//
	// - NOT_FOUND: Artifact with given ID doesn't exist
	// - PERMISSION_DENIED: User lacks can_edit permission
	Delete(ctx context.Context, in *apiresource.ApiResourceId, opts ...grpc.CallOption) (*Artifact, error)
}

type artifactCommandControllerClient struct {
	cc grpc.ClientConnInterface
}

func NewArtifactCommandControllerClient(cc grpc.ClientConnInterface) ArtifactCommandControllerClient {
	return &artifactCommandControllerClient{cc}
}

func (c *artifactCommandControllerClient) Create(ctx context.Context, in *CreateArtifactInput, opts ...grpc.CallOption) (*Artifact, error) {
	cOpts := append([]grpc.CallOption{grpc.StaticMethod()}, opts...)
	out := new(Artifact)
	err := c.cc.Invoke(ctx, ArtifactCommandController_Create_FullMethodName, in, out, cOpts...)
	if err != nil {
		return nil, err
	}
	return out, nil
}

func (c *artifactCommandControllerClient) Delete(ctx context.Context, in *apiresource.ApiResourceId, opts ...grpc.CallOption) (*Artifact, error) {
	cOpts := append([]grpc.CallOption{grpc.StaticMethod()}, opts...)
	out := new(Artifact)
	err := c.cc.Invoke(ctx, ArtifactCommandController_Delete_FullMethodName, in, out, cOpts...)
	if err != nil {
		return nil, err
	}
	return out, nil
}

// ArtifactCommandControllerServer is the server API for ArtifactCommandController service.
// All implementations should embed UnimplementedArtifactCommandControllerServer
// for forward compatibility.
//
// ArtifactCommandController handles write operations for Artifact resources.
//
// @internal
// Follows the Command-Query Separation (CQS) pattern.
//
// These RPCs are system-level — used by the runner (stigmer-runner)
// to persist task outputs. They are NOT exposed to end users or the SDK.
//
// Artifact creation flow:
// 1. Runner detects output exceeding auto-promotion threshold (256KB)
// 2. Runner calls create() with spec (metadata) + content (bytes)
// 3. Backend hashes content, deduplicates, stores blob, creates record
// 4. Runner receives Artifact with ID and status
// 5. Runner replaces inline task output with artifact reference
// 6. Runner includes artifact_created event in next updateStatus call
//
// @since T07 (Artifact Store)
type ArtifactCommandControllerServer interface {
	// Create a new artifact with content.
	//
	// Persists the artifact's metadata and content blob. The backend:
	// 1. Computes SHA-256 hash of the content bytes
	// 2. Checks for existing blob with same hash (content-addressable dedup)
	// 3. If new: writes blob to storage (filesystem in OSS, S3 in Cloud)
	// 4. Creates Artifact metadata record with status populated
	// 5. Returns the created Artifact
	//
	// @internal
	// Authorization: skip_authorization (system-level RPC, called by runners)
	// The runner authenticates via service identity, not user credentials.
	//
	// Idempotent by content hash: creating the same content twice returns
	// two distinct Artifact metadata records pointing to the same blob.
	// This is intentional — different tasks may independently produce the
	// same content, and each needs its own provenance trail.
	//
	// Error Cases:
	//
	// - INVALID_ARGUMENT: spec or content is missing/invalid
	// - RESOURCE_EXHAUSTED: content exceeds 50MB limit
	// - INTERNAL: blob storage write failure
	Create(context.Context, *CreateArtifactInput) (*Artifact, error)
	// Delete an artifact.
	//
	// Marks the artifact for deletion. The backend transitions the storage
	// state to deleted and schedules the blob for garbage collection.
	// If other artifacts reference the same content hash, the blob is
	// retained until all references are deleted.
	//
	// @internal
	// Authorization: requires can_edit permission on the artifact.
	//
	// Error Cases:
	//
	// - NOT_FOUND: Artifact with given ID doesn't exist
	// - PERMISSION_DENIED: User lacks can_edit permission
	Delete(context.Context, *apiresource.ApiResourceId) (*Artifact, error)
}

// UnimplementedArtifactCommandControllerServer should be embedded to have
// forward compatible implementations.
//
// NOTE: this should be embedded by value instead of pointer to avoid a nil
// pointer dereference when methods are called.
type UnimplementedArtifactCommandControllerServer struct{}

func (UnimplementedArtifactCommandControllerServer) Create(context.Context, *CreateArtifactInput) (*Artifact, error) {
	return nil, status.Errorf(codes.Unimplemented, "method Create not implemented")
}
func (UnimplementedArtifactCommandControllerServer) Delete(context.Context, *apiresource.ApiResourceId) (*Artifact, error) {
	return nil, status.Errorf(codes.Unimplemented, "method Delete not implemented")
}
func (UnimplementedArtifactCommandControllerServer) testEmbeddedByValue() {}

// UnsafeArtifactCommandControllerServer may be embedded to opt out of forward compatibility for this service.
// Use of this interface is not recommended, as added methods to ArtifactCommandControllerServer will
// result in compilation errors.
type UnsafeArtifactCommandControllerServer interface {
	mustEmbedUnimplementedArtifactCommandControllerServer()
}

func RegisterArtifactCommandControllerServer(s grpc.ServiceRegistrar, srv ArtifactCommandControllerServer) {
	// If the following call pancis, it indicates UnimplementedArtifactCommandControllerServer was
	// embedded by pointer and is nil.  This will cause panics if an
	// unimplemented method is ever invoked, so we test this at initialization
	// time to prevent it from happening at runtime later due to I/O.
	if t, ok := srv.(interface{ testEmbeddedByValue() }); ok {
		t.testEmbeddedByValue()
	}
	s.RegisterService(&ArtifactCommandController_ServiceDesc, srv)
}

func _ArtifactCommandController_Create_Handler(srv interface{}, ctx context.Context, dec func(interface{}) error, interceptor grpc.UnaryServerInterceptor) (interface{}, error) {
	in := new(CreateArtifactInput)
	if err := dec(in); err != nil {
		return nil, err
	}
	if interceptor == nil {
		return srv.(ArtifactCommandControllerServer).Create(ctx, in)
	}
	info := &grpc.UnaryServerInfo{
		Server:     srv,
		FullMethod: ArtifactCommandController_Create_FullMethodName,
	}
	handler := func(ctx context.Context, req interface{}) (interface{}, error) {
		return srv.(ArtifactCommandControllerServer).Create(ctx, req.(*CreateArtifactInput))
	}
	return interceptor(ctx, in, info, handler)
}

func _ArtifactCommandController_Delete_Handler(srv interface{}, ctx context.Context, dec func(interface{}) error, interceptor grpc.UnaryServerInterceptor) (interface{}, error) {
	in := new(apiresource.ApiResourceId)
	if err := dec(in); err != nil {
		return nil, err
	}
	if interceptor == nil {
		return srv.(ArtifactCommandControllerServer).Delete(ctx, in)
	}
	info := &grpc.UnaryServerInfo{
		Server:     srv,
		FullMethod: ArtifactCommandController_Delete_FullMethodName,
	}
	handler := func(ctx context.Context, req interface{}) (interface{}, error) {
		return srv.(ArtifactCommandControllerServer).Delete(ctx, req.(*apiresource.ApiResourceId))
	}
	return interceptor(ctx, in, info, handler)
}

// ArtifactCommandController_ServiceDesc is the grpc.ServiceDesc for ArtifactCommandController service.
// It's only intended for direct use with grpc.RegisterService,
// and not to be introspected or modified (even as a copy)
var ArtifactCommandController_ServiceDesc = grpc.ServiceDesc{
	ServiceName: "ai.stigmer.agentic.artifact.v1.ArtifactCommandController",
	HandlerType: (*ArtifactCommandControllerServer)(nil),
	Methods: []grpc.MethodDesc{
		{
			MethodName: "create",
			Handler:    _ArtifactCommandController_Create_Handler,
		},
		{
			MethodName: "delete",
			Handler:    _ArtifactCommandController_Delete_Handler,
		},
	},
	Streams:  []grpc.StreamDesc{},
	Metadata: "ai/stigmer/agentic/artifact/v1/command.proto",
}
