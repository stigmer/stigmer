// Code generated by protoc-gen-go-grpc. DO NOT EDIT.
// versions:
// - protoc-gen-go-grpc v1.5.1
// - protoc             (unknown)
// source: ai/stigmer/iam/iampolicy/v1/command.proto

package iampolicyv1

import (
	context "context"
	grpc "google.golang.org/grpc"
	codes "google.golang.org/grpc/codes"
	status "google.golang.org/grpc/status"
	emptypb "google.golang.org/protobuf/types/known/emptypb"
)

// This is a compile-time assertion to ensure that this generated file
// is compatible with the grpc package it is being compiled against.
// Requires gRPC-Go v1.64.0 or later.
const _ = grpc.SupportPackageIsVersion9

const (
	IamPolicyCommandController_Create_FullMethodName                   = "/ai.stigmer.iam.iampolicy.v1.IamPolicyCommandController/create"
	IamPolicyCommandController_Delete_FullMethodName                   = "/ai.stigmer.iam.iampolicy.v1.IamPolicyCommandController/delete"
	IamPolicyCommandController_BootstrapPolicy_FullMethodName          = "/ai.stigmer.iam.iampolicy.v1.IamPolicyCommandController/bootstrapPolicy"
	IamPolicyCommandController_CleanupResourcePolicies_FullMethodName  = "/ai.stigmer.iam.iampolicy.v1.IamPolicyCommandController/cleanupResourcePolicies"
	IamPolicyCommandController_RevokeOrgAccess_FullMethodName          = "/ai.stigmer.iam.iampolicy.v1.IamPolicyCommandController/revokeOrgAccess"
	IamPolicyCommandController_BootstrapRevokeOrgAccess_FullMethodName = "/ai.stigmer.iam.iampolicy.v1.IamPolicyCommandController/bootstrapRevokeOrgAccess"
)

// IamPolicyCommandControllerClient is the client API for IamPolicyCommandController service.
//
// For semantics around ctx use and closing/ending streaming RPCs, please refer to https://pkg.go.dev/google.golang.org/grpc/?tab=doc#ClientConn.NewStream.
//
// # IAM Policy Command Controller
//
// This service manages the lifecycle of IAM policies in Stigmer.
// IAM policies define access control rules by connecting three key elements:
// - Principal: WHO gets access (user, team, etc.)
// - Resource: WHAT is being accessed (any API resource)
// - Relation: HOW they can access it (viewer, admin, user, etc.)
//
// Under the hood, each IAM policy creates an OpenFGA tuple that enforces
// the permission in the authorization system.
//
// Common Use Cases:
// - Granting users access to organizations
// - Setting up team-based access control
// - Managing fine-grained permissions on any resource
type IamPolicyCommandControllerClient interface {
	// Create a new IAM policy
	//
	// Creates a single IAM policy that grants a principal access to a resource with a specific relation.
	// This is the fundamental operation for establishing permissions.
	//
	// @internal
	// The operation:
	// 1. Validates the input (principal, resource, relation are all valid)
	// 2. Checks for duplicates (skips if the exact policy already exists, idempotent)
	// 3. Creates the policy in the database with auto-generated ID and metadata
	// 4. Writes the corresponding tuple to OpenFGA (where authorization is enforced)
	//
	// Authorization:
	// - Caller must have 'can_grant_access' permission on the RESOURCE being shared
	// - This ensures only resource owners/admins can grant access to their resources
	//
	// Example:
	// Input:
	//
	//	principal: {kind: "identity_account", id: "ia_alice-123"}
	//	resource: {kind: "organization", id: "org_demo-456"}
	//	relation: "viewer"
	//
	// Result:
	//
	//	Created IamPolicy with auto-generated ID (e.g., "iamp_01HQ...")
	//	Alice can view (but not modify) the organization
	//
	// Input: IamPolicySpec containing principal, resource, and relation
	// Output: The created IamPolicy with generated ID and metadata
	Create(ctx context.Context, in *IamPolicySpec, opts ...grpc.CallOption) (*IamPolicy, error)
	// Delete a single IAM policy by spec
	//
	// Removes an existing IAM policy by matching the principal, resource, and relation.
	// This is a surgical operation — it removes one specific policy without affecting others.
	//
	// @internal
	// The operation:
	// 1. Finds the policy by matching principal+resource+relation
	// 2. Removes it from the database
	// 3. Deletes the corresponding tuple from OpenFGA
	// 4. If no matching policy exists, the operation is idempotent (no error)
	//
	// Authorization:
	// - Caller must have 'can_grant_access' permission on the RESOURCE referenced in the policy
	//
	// Use Cases:
	// - Revoking a specific permission from a user
	// - Removing access after a team member leaves
	// - Cleaning up individual policies
	//
	// Example:
	// Input:
	//
	//	principal: {kind: "identity_account", id: "ia_alice-123"}
	//	resource: {kind: "organization", id: "org_demo-456"}
	//	relation: "viewer"
	//
	// Result: The policy granting Alice viewer access to the organization is deleted
	//
	// Input: IamPolicySpec identifying the policy to delete (principal, resource, relation)
	// Output: The deleted IamPolicy object (for audit/confirmation purposes)
	Delete(ctx context.Context, in *IamPolicySpec, opts ...grpc.CallOption) (*IamPolicy, error)
	// Bootstrap IAM policy during resource creation
	//
	// Creates IAM policies during resource creation when standard authorization cannot work yet
	// because no tuples exist.
	//
	// @internal
	// Solves the chicken-and-egg problem where creating the first policy for a resource
	// requires authorization, but authorization requires that first policy.
	//
	// The operation:
	// 1. Validates that caller has can_bootstrap_iam permission on platform:stigmer
	// 2. Validates the input (principal, resource, relation are all valid)
	// 3. Checks for duplicates (skips if the exact policy already exists, idempotent)
	// 4. Creates the policy in the database with auto-generated ID and metadata
	// 5. Writes the corresponding tuple to OpenFGA (where authorization is enforced)
	//
	// Authorization:
	// - Caller must have 'can_bootstrap_iam' permission on platform:stigmer
	// - This is typically only called by resource creation handlers running as machine accounts
	//
	// Use Cases:
	// - Creating scope links (agent#organization@organization:acme) during agent creation
	// - Creating owner relations (agent#owner@identity_account:alice) during agent creation
	// - Establishing initial authorization tuples for any newly created resource
	//
	// Example:
	// Input:
	//
	//	principal: {kind: "organization", id: "org_demo-123"}
	//	resource: {kind: "agent", id: "agt_abc-456"}
	//	relation: "organization"
	//
	// Result:
	//
	//	Created IamPolicy establishing agent's organization scope
	//	Subsequent IAM policy operations can now use standard authorization
	//
	// Note: After the bootstrap policies are created, subsequent IAM policy modifications
	// must use the standard 'create' RPC which requires 'can_grant_access' permission.
	//
	// Input: IamPolicySpec containing principal, resource, and relation
	// Output: The created IamPolicy with generated ID and metadata
	BootstrapPolicy(ctx context.Context, in *IamPolicySpec, opts ...grpc.CallOption) (*IamPolicy, error)
	// Cleanup all IAM policies for a deleted resource.
	//
	// Removes all IAM policies associated with a deleted resource.
	//
	// @internal
	// Performs bidirectional cleanup:
	// 1. Policies where resource is the TARGET (policies granting access TO this resource)
	// 2. Policies where resource is the PRINCIPAL (policies where this resource HAS access)
	//
	// The operation:
	// 1. Validates can_bootstrap_iam permission on platform:stigmer
	// 2. Finds all policies where resource_id appears (as principal OR resource)
	// 3. Deletes all matching policies from MongoDB
	// 4. Removes all corresponding tuples from OpenFGA
	// 5. Returns Empty (idempotent if no policies exist)
	//
	// Authorization:
	// - Caller must have 'can_bootstrap_iam' permission on platform:stigmer
	// - This is typically only granted to platform services
	//
	// Use Cases:
	// - Resource deletion cleanup
	// - Preventing orphaned FGA tuples
	// - Maintaining authorization system integrity
	//
	// Example:
	// Input: {kind: "organization", id: "org_demo-123"}
	// Result: All policies referencing org_demo-123 are deleted
	//
	// Input: ApiResourceRef with resource kind and ID
	// Output: Empty (google.protobuf.Empty)
	CleanupResourcePolicies(ctx context.Context, in *ApiResourceRef, opts ...grpc.CallOption) (*emptypb.Empty, error)
	// Revoke all of a user's access to an organization.
	//
	// Removes every IAM policy that grants the specified identity account access to
	// resources within the given organization, including policies on the organization
	// itself and on child resources (environments, agents, etc.).
	//
	// @internal
	// The operation:
	// 1. Validates the input (identity_account_id and organization_id are present)
	// 2. Authorizes caller (can_grant_access on the organization)
	// 3. Loads all policies where the user is principal within the org scope
	// 4. Deletes all matching policies from MongoDB
	// 5. Removes all corresponding tuples from OpenFGA
	//
	// Authorization:
	//   - Caller must have 'can_grant_access' permission on the organization
	//   - System flows running as the platform machine account cannot satisfy this
	//     check (the machine account holds no org-scoped grants by design) and must
	//     use bootstrapRevokeOrgAccess instead
	//
	// Use Cases:
	// - Removing a member from an organization
	// - Offboarding a user from all org resources in one operation
	//
	// Input: RevokeOrgAccessInput with identity_account_id and organization_id
	// Output: Empty (google.protobuf.Empty)
	RevokeOrgAccess(ctx context.Context, in *RevokeOrgAccessInput, opts ...grpc.CallOption) (*emptypb.Empty, error)
	// Revoke all of a user's access to an organization via the system (bootstrap) path.
	//
	// The system-flow twin of revokeOrgAccess: identical revocation behavior, but
	// authorized by can_bootstrap_iam on platform:stigmer instead of
	// can_grant_access on the organization.
	//
	// @internal
	// Exists because system flows execute the revoke as the platform machine
	// account, which by design holds no org-scoped grants. The system channel does
	// NOT bypass authorization — it authenticates as the machine account, which
	// can only satisfy platform-scoped permissions. revokeOrgAccess therefore
	// always fails with PERMISSION_DENIED on the system channel; this RPC is the
	// sanctioned path, mirroring how bootstrapPolicy is the system-path twin of
	// create (see https://github.com/stigmer/stigmer/issues/332).
	//
	// The operation (identical to revokeOrgAccess after authorization):
	//  1. Validates can_bootstrap_iam permission on platform:stigmer
	//  2. Loads all policies where the identity account is principal within the org
	//     scope, plus policies directly on the organization itself
	//  3. Deletes all matching policies from MongoDB
	//  4. Removes all corresponding tuples from OpenFGA (idempotent if none exist)
	//
	// Authorization:
	// - Caller must have 'can_bootstrap_iam' permission on platform:stigmer
	// - This is typically only granted to platform services (machine accounts)
	//
	// Use Cases:
	// - Federated account deprovisioning (deprovisionFederatedAccount's revoke step)
	// - Any platform-driven offboarding that runs under system credentials
	//
	// End-user member removal must use revokeOrgAccess, which checks
	// can_grant_access on the organization.
	//
	// Input: RevokeOrgAccessInput with identity_account_id and organization_id
	// Output: Empty (google.protobuf.Empty)
	BootstrapRevokeOrgAccess(ctx context.Context, in *RevokeOrgAccessInput, opts ...grpc.CallOption) (*emptypb.Empty, error)
}

type iamPolicyCommandControllerClient struct {
	cc grpc.ClientConnInterface
}

func NewIamPolicyCommandControllerClient(cc grpc.ClientConnInterface) IamPolicyCommandControllerClient {
	return &iamPolicyCommandControllerClient{cc}
}

func (c *iamPolicyCommandControllerClient) Create(ctx context.Context, in *IamPolicySpec, opts ...grpc.CallOption) (*IamPolicy, error) {
	cOpts := append([]grpc.CallOption{grpc.StaticMethod()}, opts...)
	out := new(IamPolicy)
	err := c.cc.Invoke(ctx, IamPolicyCommandController_Create_FullMethodName, in, out, cOpts...)
	if err != nil {
		return nil, err
	}
	return out, nil
}

func (c *iamPolicyCommandControllerClient) Delete(ctx context.Context, in *IamPolicySpec, opts ...grpc.CallOption) (*IamPolicy, error) {
	cOpts := append([]grpc.CallOption{grpc.StaticMethod()}, opts...)
	out := new(IamPolicy)
	err := c.cc.Invoke(ctx, IamPolicyCommandController_Delete_FullMethodName, in, out, cOpts...)
	if err != nil {
		return nil, err
	}
	return out, nil
}

func (c *iamPolicyCommandControllerClient) BootstrapPolicy(ctx context.Context, in *IamPolicySpec, opts ...grpc.CallOption) (*IamPolicy, error) {
	cOpts := append([]grpc.CallOption{grpc.StaticMethod()}, opts...)
	out := new(IamPolicy)
	err := c.cc.Invoke(ctx, IamPolicyCommandController_BootstrapPolicy_FullMethodName, in, out, cOpts...)
	if err != nil {
		return nil, err
	}
	return out, nil
}

func (c *iamPolicyCommandControllerClient) CleanupResourcePolicies(ctx context.Context, in *ApiResourceRef, opts ...grpc.CallOption) (*emptypb.Empty, error) {
	cOpts := append([]grpc.CallOption{grpc.StaticMethod()}, opts...)
	out := new(emptypb.Empty)
	err := c.cc.Invoke(ctx, IamPolicyCommandController_CleanupResourcePolicies_FullMethodName, in, out, cOpts...)
	if err != nil {
		return nil, err
	}
	return out, nil
}

func (c *iamPolicyCommandControllerClient) RevokeOrgAccess(ctx context.Context, in *RevokeOrgAccessInput, opts ...grpc.CallOption) (*emptypb.Empty, error) {
	cOpts := append([]grpc.CallOption{grpc.StaticMethod()}, opts...)
	out := new(emptypb.Empty)
	err := c.cc.Invoke(ctx, IamPolicyCommandController_RevokeOrgAccess_FullMethodName, in, out, cOpts...)
	if err != nil {
		return nil, err
	}
	return out, nil
}

func (c *iamPolicyCommandControllerClient) BootstrapRevokeOrgAccess(ctx context.Context, in *RevokeOrgAccessInput, opts ...grpc.CallOption) (*emptypb.Empty, error) {
	cOpts := append([]grpc.CallOption{grpc.StaticMethod()}, opts...)
	out := new(emptypb.Empty)
	err := c.cc.Invoke(ctx, IamPolicyCommandController_BootstrapRevokeOrgAccess_FullMethodName, in, out, cOpts...)
	if err != nil {
		return nil, err
	}
	return out, nil
}

// IamPolicyCommandControllerServer is the server API for IamPolicyCommandController service.
// All implementations should embed UnimplementedIamPolicyCommandControllerServer
// for forward compatibility.
//
// # IAM Policy Command Controller
//
// This service manages the lifecycle of IAM policies in Stigmer.
// IAM policies define access control rules by connecting three key elements:
// - Principal: WHO gets access (user, team, etc.)
// - Resource: WHAT is being accessed (any API resource)
// - Relation: HOW they can access it (viewer, admin, user, etc.)
//
// Under the hood, each IAM policy creates an OpenFGA tuple that enforces
// the permission in the authorization system.
//
// Common Use Cases:
// - Granting users access to organizations
// - Setting up team-based access control
// - Managing fine-grained permissions on any resource
type IamPolicyCommandControllerServer interface {
	// Create a new IAM policy
	//
	// Creates a single IAM policy that grants a principal access to a resource with a specific relation.
	// This is the fundamental operation for establishing permissions.
	//
	// @internal
	// The operation:
	// 1. Validates the input (principal, resource, relation are all valid)
	// 2. Checks for duplicates (skips if the exact policy already exists, idempotent)
	// 3. Creates the policy in the database with auto-generated ID and metadata
	// 4. Writes the corresponding tuple to OpenFGA (where authorization is enforced)
	//
	// Authorization:
	// - Caller must have 'can_grant_access' permission on the RESOURCE being shared
	// - This ensures only resource owners/admins can grant access to their resources
	//
	// Example:
	// Input:
	//
	//	principal: {kind: "identity_account", id: "ia_alice-123"}
	//	resource: {kind: "organization", id: "org_demo-456"}
	//	relation: "viewer"
	//
	// Result:
	//
	//	Created IamPolicy with auto-generated ID (e.g., "iamp_01HQ...")
	//	Alice can view (but not modify) the organization
	//
	// Input: IamPolicySpec containing principal, resource, and relation
	// Output: The created IamPolicy with generated ID and metadata
	Create(context.Context, *IamPolicySpec) (*IamPolicy, error)
	// Delete a single IAM policy by spec
	//
	// Removes an existing IAM policy by matching the principal, resource, and relation.
	// This is a surgical operation — it removes one specific policy without affecting others.
	//
	// @internal
	// The operation:
	// 1. Finds the policy by matching principal+resource+relation
	// 2. Removes it from the database
	// 3. Deletes the corresponding tuple from OpenFGA
	// 4. If no matching policy exists, the operation is idempotent (no error)
	//
	// Authorization:
	// - Caller must have 'can_grant_access' permission on the RESOURCE referenced in the policy
	//
	// Use Cases:
	// - Revoking a specific permission from a user
	// - Removing access after a team member leaves
	// - Cleaning up individual policies
	//
	// Example:
	// Input:
	//
	//	principal: {kind: "identity_account", id: "ia_alice-123"}
	//	resource: {kind: "organization", id: "org_demo-456"}
	//	relation: "viewer"
	//
	// Result: The policy granting Alice viewer access to the organization is deleted
	//
	// Input: IamPolicySpec identifying the policy to delete (principal, resource, relation)
	// Output: The deleted IamPolicy object (for audit/confirmation purposes)
	Delete(context.Context, *IamPolicySpec) (*IamPolicy, error)
	// Bootstrap IAM policy during resource creation
	//
	// Creates IAM policies during resource creation when standard authorization cannot work yet
	// because no tuples exist.
	//
	// @internal
	// Solves the chicken-and-egg problem where creating the first policy for a resource
	// requires authorization, but authorization requires that first policy.
	//
	// The operation:
	// 1. Validates that caller has can_bootstrap_iam permission on platform:stigmer
	// 2. Validates the input (principal, resource, relation are all valid)
	// 3. Checks for duplicates (skips if the exact policy already exists, idempotent)
	// 4. Creates the policy in the database with auto-generated ID and metadata
	// 5. Writes the corresponding tuple to OpenFGA (where authorization is enforced)
	//
	// Authorization:
	// - Caller must have 'can_bootstrap_iam' permission on platform:stigmer
	// - This is typically only called by resource creation handlers running as machine accounts
	//
	// Use Cases:
	// - Creating scope links (agent#organization@organization:acme) during agent creation
	// - Creating owner relations (agent#owner@identity_account:alice) during agent creation
	// - Establishing initial authorization tuples for any newly created resource
	//
	// Example:
	// Input:
	//
	//	principal: {kind: "organization", id: "org_demo-123"}
	//	resource: {kind: "agent", id: "agt_abc-456"}
	//	relation: "organization"
	//
	// Result:
	//
	//	Created IamPolicy establishing agent's organization scope
	//	Subsequent IAM policy operations can now use standard authorization
	//
	// Note: After the bootstrap policies are created, subsequent IAM policy modifications
	// must use the standard 'create' RPC which requires 'can_grant_access' permission.
	//
	// Input: IamPolicySpec containing principal, resource, and relation
	// Output: The created IamPolicy with generated ID and metadata
	BootstrapPolicy(context.Context, *IamPolicySpec) (*IamPolicy, error)
	// Cleanup all IAM policies for a deleted resource.
	//
	// Removes all IAM policies associated with a deleted resource.
	//
	// @internal
	// Performs bidirectional cleanup:
	// 1. Policies where resource is the TARGET (policies granting access TO this resource)
	// 2. Policies where resource is the PRINCIPAL (policies where this resource HAS access)
	//
	// The operation:
	// 1. Validates can_bootstrap_iam permission on platform:stigmer
	// 2. Finds all policies where resource_id appears (as principal OR resource)
	// 3. Deletes all matching policies from MongoDB
	// 4. Removes all corresponding tuples from OpenFGA
	// 5. Returns Empty (idempotent if no policies exist)
	//
	// Authorization:
	// - Caller must have 'can_bootstrap_iam' permission on platform:stigmer
	// - This is typically only granted to platform services
	//
	// Use Cases:
	// - Resource deletion cleanup
	// - Preventing orphaned FGA tuples
	// - Maintaining authorization system integrity
	//
	// Example:
	// Input: {kind: "organization", id: "org_demo-123"}
	// Result: All policies referencing org_demo-123 are deleted
	//
	// Input: ApiResourceRef with resource kind and ID
	// Output: Empty (google.protobuf.Empty)
	CleanupResourcePolicies(context.Context, *ApiResourceRef) (*emptypb.Empty, error)
	// Revoke all of a user's access to an organization.
	//
	// Removes every IAM policy that grants the specified identity account access to
	// resources within the given organization, including policies on the organization
	// itself and on child resources (environments, agents, etc.).
	//
	// @internal
	// The operation:
	// 1. Validates the input (identity_account_id and organization_id are present)
	// 2. Authorizes caller (can_grant_access on the organization)
	// 3. Loads all policies where the user is principal within the org scope
	// 4. Deletes all matching policies from MongoDB
	// 5. Removes all corresponding tuples from OpenFGA
	//
	// Authorization:
	//   - Caller must have 'can_grant_access' permission on the organization
	//   - System flows running as the platform machine account cannot satisfy this
	//     check (the machine account holds no org-scoped grants by design) and must
	//     use bootstrapRevokeOrgAccess instead
	//
	// Use Cases:
	// - Removing a member from an organization
	// - Offboarding a user from all org resources in one operation
	//
	// Input: RevokeOrgAccessInput with identity_account_id and organization_id
	// Output: Empty (google.protobuf.Empty)
	RevokeOrgAccess(context.Context, *RevokeOrgAccessInput) (*emptypb.Empty, error)
	// Revoke all of a user's access to an organization via the system (bootstrap) path.
	//
	// The system-flow twin of revokeOrgAccess: identical revocation behavior, but
	// authorized by can_bootstrap_iam on platform:stigmer instead of
	// can_grant_access on the organization.
	//
	// @internal
	// Exists because system flows execute the revoke as the platform machine
	// account, which by design holds no org-scoped grants. The system channel does
	// NOT bypass authorization — it authenticates as the machine account, which
	// can only satisfy platform-scoped permissions. revokeOrgAccess therefore
	// always fails with PERMISSION_DENIED on the system channel; this RPC is the
	// sanctioned path, mirroring how bootstrapPolicy is the system-path twin of
	// create (see https://github.com/stigmer/stigmer/issues/332).
	//
	// The operation (identical to revokeOrgAccess after authorization):
	//  1. Validates can_bootstrap_iam permission on platform:stigmer
	//  2. Loads all policies where the identity account is principal within the org
	//     scope, plus policies directly on the organization itself
	//  3. Deletes all matching policies from MongoDB
	//  4. Removes all corresponding tuples from OpenFGA (idempotent if none exist)
	//
	// Authorization:
	// - Caller must have 'can_bootstrap_iam' permission on platform:stigmer
	// - This is typically only granted to platform services (machine accounts)
	//
	// Use Cases:
	// - Federated account deprovisioning (deprovisionFederatedAccount's revoke step)
	// - Any platform-driven offboarding that runs under system credentials
	//
	// End-user member removal must use revokeOrgAccess, which checks
	// can_grant_access on the organization.
	//
	// Input: RevokeOrgAccessInput with identity_account_id and organization_id
	// Output: Empty (google.protobuf.Empty)
	BootstrapRevokeOrgAccess(context.Context, *RevokeOrgAccessInput) (*emptypb.Empty, error)
}

// UnimplementedIamPolicyCommandControllerServer should be embedded to have
// forward compatible implementations.
//
// NOTE: this should be embedded by value instead of pointer to avoid a nil
// pointer dereference when methods are called.
type UnimplementedIamPolicyCommandControllerServer struct{}

func (UnimplementedIamPolicyCommandControllerServer) Create(context.Context, *IamPolicySpec) (*IamPolicy, error) {
	return nil, status.Errorf(codes.Unimplemented, "method Create not implemented")
}
func (UnimplementedIamPolicyCommandControllerServer) Delete(context.Context, *IamPolicySpec) (*IamPolicy, error) {
	return nil, status.Errorf(codes.Unimplemented, "method Delete not implemented")
}
func (UnimplementedIamPolicyCommandControllerServer) BootstrapPolicy(context.Context, *IamPolicySpec) (*IamPolicy, error) {
	return nil, status.Errorf(codes.Unimplemented, "method BootstrapPolicy not implemented")
}
func (UnimplementedIamPolicyCommandControllerServer) CleanupResourcePolicies(context.Context, *ApiResourceRef) (*emptypb.Empty, error) {
	return nil, status.Errorf(codes.Unimplemented, "method CleanupResourcePolicies not implemented")
}
func (UnimplementedIamPolicyCommandControllerServer) RevokeOrgAccess(context.Context, *RevokeOrgAccessInput) (*emptypb.Empty, error) {
	return nil, status.Errorf(codes.Unimplemented, "method RevokeOrgAccess not implemented")
}
func (UnimplementedIamPolicyCommandControllerServer) BootstrapRevokeOrgAccess(context.Context, *RevokeOrgAccessInput) (*emptypb.Empty, error) {
	return nil, status.Errorf(codes.Unimplemented, "method BootstrapRevokeOrgAccess not implemented")
}
func (UnimplementedIamPolicyCommandControllerServer) testEmbeddedByValue() {}

// UnsafeIamPolicyCommandControllerServer may be embedded to opt out of forward compatibility for this service.
// Use of this interface is not recommended, as added methods to IamPolicyCommandControllerServer will
// result in compilation errors.
type UnsafeIamPolicyCommandControllerServer interface {
	mustEmbedUnimplementedIamPolicyCommandControllerServer()
}

func RegisterIamPolicyCommandControllerServer(s grpc.ServiceRegistrar, srv IamPolicyCommandControllerServer) {
	// If the following call pancis, it indicates UnimplementedIamPolicyCommandControllerServer was
	// embedded by pointer and is nil.  This will cause panics if an
	// unimplemented method is ever invoked, so we test this at initialization
	// time to prevent it from happening at runtime later due to I/O.
	if t, ok := srv.(interface{ testEmbeddedByValue() }); ok {
		t.testEmbeddedByValue()
	}
	s.RegisterService(&IamPolicyCommandController_ServiceDesc, srv)
}

func _IamPolicyCommandController_Create_Handler(srv interface{}, ctx context.Context, dec func(interface{}) error, interceptor grpc.UnaryServerInterceptor) (interface{}, error) {
	in := new(IamPolicySpec)
	if err := dec(in); err != nil {
		return nil, err
	}
	if interceptor == nil {
		return srv.(IamPolicyCommandControllerServer).Create(ctx, in)
	}
	info := &grpc.UnaryServerInfo{
		Server:     srv,
		FullMethod: IamPolicyCommandController_Create_FullMethodName,
	}
	handler := func(ctx context.Context, req interface{}) (interface{}, error) {
		return srv.(IamPolicyCommandControllerServer).Create(ctx, req.(*IamPolicySpec))
	}
	return interceptor(ctx, in, info, handler)
}

func _IamPolicyCommandController_Delete_Handler(srv interface{}, ctx context.Context, dec func(interface{}) error, interceptor grpc.UnaryServerInterceptor) (interface{}, error) {
	in := new(IamPolicySpec)
	if err := dec(in); err != nil {
		return nil, err
	}
	if interceptor == nil {
		return srv.(IamPolicyCommandControllerServer).Delete(ctx, in)
	}
	info := &grpc.UnaryServerInfo{
		Server:     srv,
		FullMethod: IamPolicyCommandController_Delete_FullMethodName,
	}
	handler := func(ctx context.Context, req interface{}) (interface{}, error) {
		return srv.(IamPolicyCommandControllerServer).Delete(ctx, req.(*IamPolicySpec))
	}
	return interceptor(ctx, in, info, handler)
}

func _IamPolicyCommandController_BootstrapPolicy_Handler(srv interface{}, ctx context.Context, dec func(interface{}) error, interceptor grpc.UnaryServerInterceptor) (interface{}, error) {
	in := new(IamPolicySpec)
	if err := dec(in); err != nil {
		return nil, err
	}
	if interceptor == nil {
		return srv.(IamPolicyCommandControllerServer).BootstrapPolicy(ctx, in)
	}
	info := &grpc.UnaryServerInfo{
		Server:     srv,
		FullMethod: IamPolicyCommandController_BootstrapPolicy_FullMethodName,
	}
	handler := func(ctx context.Context, req interface{}) (interface{}, error) {
		return srv.(IamPolicyCommandControllerServer).BootstrapPolicy(ctx, req.(*IamPolicySpec))
	}
	return interceptor(ctx, in, info, handler)
}

func _IamPolicyCommandController_CleanupResourcePolicies_Handler(srv interface{}, ctx context.Context, dec func(interface{}) error, interceptor grpc.UnaryServerInterceptor) (interface{}, error) {
	in := new(ApiResourceRef)
	if err := dec(in); err != nil {
		return nil, err
	}
	if interceptor == nil {
		return srv.(IamPolicyCommandControllerServer).CleanupResourcePolicies(ctx, in)
	}
	info := &grpc.UnaryServerInfo{
		Server:     srv,
		FullMethod: IamPolicyCommandController_CleanupResourcePolicies_FullMethodName,
	}
	handler := func(ctx context.Context, req interface{}) (interface{}, error) {
		return srv.(IamPolicyCommandControllerServer).CleanupResourcePolicies(ctx, req.(*ApiResourceRef))
	}
	return interceptor(ctx, in, info, handler)
}

func _IamPolicyCommandController_RevokeOrgAccess_Handler(srv interface{}, ctx context.Context, dec func(interface{}) error, interceptor grpc.UnaryServerInterceptor) (interface{}, error) {
	in := new(RevokeOrgAccessInput)
	if err := dec(in); err != nil {
		return nil, err
	}
	if interceptor == nil {
		return srv.(IamPolicyCommandControllerServer).RevokeOrgAccess(ctx, in)
	}
	info := &grpc.UnaryServerInfo{
		Server:     srv,
		FullMethod: IamPolicyCommandController_RevokeOrgAccess_FullMethodName,
	}
	handler := func(ctx context.Context, req interface{}) (interface{}, error) {
		return srv.(IamPolicyCommandControllerServer).RevokeOrgAccess(ctx, req.(*RevokeOrgAccessInput))
	}
	return interceptor(ctx, in, info, handler)
}

func _IamPolicyCommandController_BootstrapRevokeOrgAccess_Handler(srv interface{}, ctx context.Context, dec func(interface{}) error, interceptor grpc.UnaryServerInterceptor) (interface{}, error) {
	in := new(RevokeOrgAccessInput)
	if err := dec(in); err != nil {
		return nil, err
	}
	if interceptor == nil {
		return srv.(IamPolicyCommandControllerServer).BootstrapRevokeOrgAccess(ctx, in)
	}
	info := &grpc.UnaryServerInfo{
		Server:     srv,
		FullMethod: IamPolicyCommandController_BootstrapRevokeOrgAccess_FullMethodName,
	}
	handler := func(ctx context.Context, req interface{}) (interface{}, error) {
		return srv.(IamPolicyCommandControllerServer).BootstrapRevokeOrgAccess(ctx, req.(*RevokeOrgAccessInput))
	}
	return interceptor(ctx, in, info, handler)
}

// IamPolicyCommandController_ServiceDesc is the grpc.ServiceDesc for IamPolicyCommandController service.
// It's only intended for direct use with grpc.RegisterService,
// and not to be introspected or modified (even as a copy)
var IamPolicyCommandController_ServiceDesc = grpc.ServiceDesc{
	ServiceName: "ai.stigmer.iam.iampolicy.v1.IamPolicyCommandController",
	HandlerType: (*IamPolicyCommandControllerServer)(nil),
	Methods: []grpc.MethodDesc{
		{
			MethodName: "create",
			Handler:    _IamPolicyCommandController_Create_Handler,
		},
		{
			MethodName: "delete",
			Handler:    _IamPolicyCommandController_Delete_Handler,
		},
		{
			MethodName: "bootstrapPolicy",
			Handler:    _IamPolicyCommandController_BootstrapPolicy_Handler,
		},
		{
			MethodName: "cleanupResourcePolicies",
			Handler:    _IamPolicyCommandController_CleanupResourcePolicies_Handler,
		},
		{
			MethodName: "revokeOrgAccess",
			Handler:    _IamPolicyCommandController_RevokeOrgAccess_Handler,
		},
		{
			MethodName: "bootstrapRevokeOrgAccess",
			Handler:    _IamPolicyCommandController_BootstrapRevokeOrgAccess_Handler,
		},
	},
	Streams:  []grpc.StreamDesc{},
	Metadata: "ai/stigmer/iam/iampolicy/v1/command.proto",
}
