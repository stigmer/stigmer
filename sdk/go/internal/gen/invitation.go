// Code generated by stigmer-codegen. DO NOT EDIT.

package gen

import (
	"context"
	"time"

	apiresource "github.com/stigmer/stigmer/sdk/go/v3/proto/ai/stigmer/commons/apiresource"
	invitationv1 "github.com/stigmer/stigmer/sdk/go/v3/proto/ai/stigmer/iam/invitation/v1"
	iamv1 "github.com/stigmer/stigmer/sdk/go/v3/proto/ai/stigmer/iam/v1"
	"google.golang.org/grpc"
	"google.golang.org/protobuf/types/known/timestamppb"
)

// InvitationClient provides operations on invitation resources.
type InvitationClient struct {
	command invitationv1.InvitationCommandControllerClient
	query   invitationv1.InvitationQueryControllerClient
}

func NewInvitationClient(conn grpc.ClientConnInterface) *InvitationClient {
	return &InvitationClient{
		command: invitationv1.NewInvitationCommandControllerClient(conn),
		query:   invitationv1.NewInvitationQueryControllerClient(conn),
	}
}

func (i *InvitationClient) Create(ctx context.Context, input *InvitationInput) (*invitationv1.Invitation, error) {
	req, err := input.toProto()
	if err != nil {
		return nil, invalidInputErr(err)
	}
	resp, err := i.command.Create(ctx, req)
	return resp, wrapErr(err)
}

func (i *InvitationClient) Revoke(ctx context.Context, id string) (*invitationv1.Invitation, error) {
	resp, err := i.command.Revoke(ctx, &invitationv1.InvitationId{Value: id})
	return resp, wrapErr(err)
}

func (i *InvitationClient) Redeem(ctx context.Context, input *invitationv1.RedeemInvitationInput) (*invitationv1.Invitation, error) {
	resp, err := i.command.Redeem(ctx, input)
	return resp, wrapErr(err)
}

func (i *InvitationClient) Get(ctx context.Context, id string) (*invitationv1.Invitation, error) {
	resp, err := i.query.Get(ctx, &invitationv1.InvitationId{Value: id})
	return resp, wrapErr(err)
}

func (i *InvitationClient) ListByOrg(ctx context.Context, input *invitationv1.ListInvitationsByOrgInput) (*invitationv1.Invitations, error) {
	resp, err := i.query.ListByOrg(ctx, input)
	return resp, wrapErr(err)
}

func (i *InvitationClient) GetByToken(ctx context.Context, input *invitationv1.InvitationTokenInput) (*invitationv1.InvitationPreview, error) {
	resp, err := i.query.GetByToken(ctx, input)
	return resp, wrapErr(err)
}

// InvitationInput holds the fields for creating/updating a Invitation.
type InvitationInput struct {
	// Id is the resource's metadata.id, for exact update addressing when
	// set from a loaded resource. Required for updates to platform-scoped
	// (org-less) kinds, where the org+slug fallback cannot match. On
	// create, the cloud server stamps its own id regardless; the OSS
	// server honors a caller-supplied id (existing apply semantics).
	Id             string
	Name           string
	Slug           string
	Org            string
	Labels         map[string]string
	Visibility     apiresource.ApiResourceVisibility
	Role           iamv1.IamRole
	MaxRedemptions int32
	ExpiresAt      string
	Label          string
}

func (i *InvitationInput) toProto() (*invitationv1.Invitation, error) {
	resource := &invitationv1.Invitation{
		ApiVersion: "iam.stigmer.ai/v1",
		Kind:       "Invitation",
		Metadata: &apiresource.ApiResourceMetadata{
			Id:         i.Id,
			Name:       i.Name,
			Slug:       i.Slug,
			Org:        i.Org,
			Labels:     i.Labels,
			Visibility: i.Visibility,
		},
		Spec: &invitationv1.InvitationSpec{},
	}
	resource.Spec.Role = i.Role
	resource.Spec.MaxRedemptions = i.MaxRedemptions
	if i.ExpiresAt != "" {
		t, err := time.Parse(time.RFC3339, i.ExpiresAt)
		if err != nil {
			return nil, fieldErr("ExpiresAt", err)
		}
		resource.Spec.ExpiresAt = timestamppb.New(t)
	}
	resource.Spec.Label = i.Label
	return resource, nil
}

// InvitationInputFromProto creates a InvitationInput from a proto Invitation resource.
func InvitationInputFromProto(p *invitationv1.Invitation) *InvitationInput {
	if p == nil {
		return &InvitationInput{}
	}
	input := &InvitationInput{}
	if m := p.GetMetadata(); m != nil {
		input.Id = m.GetId()
		input.Name = m.GetName()
		input.Slug = m.GetSlug()
		input.Org = m.GetOrg()
		input.Labels = m.GetLabels()
		input.Visibility = m.GetVisibility()
	}
	if s := p.GetSpec(); s != nil {
		input.Role = s.GetRole()
		input.MaxRedemptions = s.GetMaxRedemptions()
		if ts := s.GetExpiresAt(); ts != nil {
			input.ExpiresAt = ts.AsTime().Format(time.RFC3339)
		}
		input.Label = s.GetLabel()
	}
	return input
}
