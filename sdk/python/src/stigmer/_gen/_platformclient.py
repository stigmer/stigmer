# Code generated by stigmer-codegen. DO NOT EDIT.

from __future__ import annotations

from dataclasses import dataclass, field

import grpc

from ai.stigmer.iam.platformclient.v1 import api_pb2
from ai.stigmer.iam.platformclient.v1 import command_pb2_grpc
from ai.stigmer.iam.platformclient.v1 import query_pb2_grpc
from ai.stigmer.iam.platformclient.v1 import token_pb2_grpc
from ai.stigmer.iam.platformclient.v1 import io_pb2
from ai.stigmer.iam.platformclient.v1 import spec_pb2
from ai.stigmer.commons.apiresource import io_pb2 as apiresource_io_pb2
from ai.stigmer.commons.apiresource import metadata_pb2
from ai.stigmer.commons.apiresource.apiresourcekind import api_resource_kind_pb2

from ._errors import wrap_error
from ._types import DeleteResourceInput, ResourceRef


class PlatformClientClient:
    """Provides operations on platformclient resources."""

    def __init__(self, channel: grpc.Channel) -> None:
        self._command = command_pb2_grpc.PlatformClientCommandControllerStub(channel)
        self._query = query_pb2_grpc.PlatformClientQueryControllerStub(channel)
        self._token = token_pb2_grpc.PlatformClientTokenControllerStub(channel)

    def apply(self, input: PlatformClientInput) -> api_pb2.PlatformClient:
        try:
            return self._command.apply(input._to_proto())
        except grpc.RpcError as e:
            raise wrap_error(e) from e

    def create(self, input: PlatformClientInput) -> io_pb2.PlatformClientCreateResponse:
        try:
            return self._command.create(input._to_proto())
        except grpc.RpcError as e:
            raise wrap_error(e) from e

    def update(self, input: PlatformClientInput) -> api_pb2.PlatformClient:
        try:
            return self._command.update(input._to_proto())
        except grpc.RpcError as e:
            raise wrap_error(e) from e

    def delete(self, input: DeleteResourceInput) -> api_pb2.PlatformClient:
        try:
            return self._command.delete(input._to_proto())
        except grpc.RpcError as e:
            raise wrap_error(e) from e

    def rotate_secret(self, id: str) -> io_pb2.PlatformClientCreateResponse:
        try:
            return self._command.rotateSecret(io_pb2.PlatformClientId(value=id))
        except grpc.RpcError as e:
            raise wrap_error(e) from e

    def get(self, id: str) -> api_pb2.PlatformClient:
        try:
            return self._query.get(apiresource_io_pb2.ApiResourceId(value=id))
        except grpc.RpcError as e:
            raise wrap_error(e) from e

    def get_by_reference(self, ref: ResourceRef) -> api_pb2.PlatformClient:
        try:
            proto = ref._to_proto()
            proto.kind = api_resource_kind_pb2.platform_client
            return self._query.getByReference(proto)
        except grpc.RpcError as e:
            raise wrap_error(e) from e

    def list_by_org(self, input: io_pb2.ListPlatformClientsByOrgInput) -> io_pb2.PlatformClients:
        try:
            return self._query.listByOrg(input)
        except grpc.RpcError as e:
            raise wrap_error(e) from e

    def mint_user_token(self, input: io_pb2.MintUserTokenRequest) -> io_pb2.MintUserTokenResponse:
        try:
            return self._token.mintUserToken(input)
        except grpc.RpcError as e:
            raise wrap_error(e) from e


@dataclass
class PlatformClientInput:
    """Input for creating or updating a PlatformClient."""

    name: str
    org: str
    slug: str | None = None
    labels: dict[str, str] | None = None
    client_id: str = ""
    client_secret_hash: str = ""
    secret_fingerprint: str = ""
    expires_at: str = ""
    never_expires: bool = False
    auto_provision_accounts: bool = False
    auto_grant_on_org: bool = False
    auto_grant_role: int = 0
    allowed_origins: list[str] = field(default_factory=list)

    def _to_proto(self) -> api_pb2.PlatformClient:
        spec = spec_pb2.PlatformClientSpec(
            client_id=self.client_id,
            client_secret_hash=self.client_secret_hash,
            secret_fingerprint=self.secret_fingerprint,
            never_expires=self.never_expires,
            auto_provision_accounts=self.auto_provision_accounts,
            auto_grant_on_org=self.auto_grant_on_org,
            auto_grant_role=self.auto_grant_role,
        )
        if self.expires_at:
            spec.expires_at.FromJsonString(self.expires_at)
        if self.allowed_origins:
            spec.allowed_origins.extend(self.allowed_origins)
        metadata = metadata_pb2.ApiResourceMetadata(
            name=self.name,
            org=self.org,
        )
        if self.slug:
            metadata.slug = self.slug
        if self.labels:
            metadata.labels.update(self.labels)
        return api_pb2.PlatformClient(
            api_version="iam.stigmer.ai/v1",
            kind="PlatformClient",
            metadata=metadata,
            spec=spec,
        )

