# Code generated by stigmer-codegen. DO NOT EDIT.

from __future__ import annotations

from dataclasses import dataclass, field

import grpc

from ai.stigmer.agentic.session.v1 import api_pb2
from ai.stigmer.agentic.session.v1 import command_pb2_grpc
from ai.stigmer.agentic.session.v1 import query_pb2_grpc
from ai.stigmer.agentic.session.v1 import io_pb2
from ai.stigmer.agentic.session.v1 import spec_pb2
from ai.stigmer.commons.apiresource import metadata_pb2

from ._errors import wrap_error
from ._types import ResourceRef
from ._agent import McpServerUsageInput, ToolApprovalOverrideInput
from ._agentexecution import GitRepoSourceInput, LocalPathSourceInput, WorkspaceEntryInput, WorkspaceSourceInput


class SessionClient:
    """Provides operations on session resources."""

    def __init__(self, channel: grpc.Channel) -> None:
        self._command = command_pb2_grpc.SessionCommandControllerStub(channel)
        self._query = query_pb2_grpc.SessionQueryControllerStub(channel)

    def apply(self, input: SessionInput) -> api_pb2.Session:
        try:
            return self._command.apply(input._to_proto())
        except grpc.RpcError as e:
            raise wrap_error(e) from e

    def create(self, input: SessionInput) -> api_pb2.Session:
        try:
            return self._command.create(input._to_proto())
        except grpc.RpcError as e:
            raise wrap_error(e) from e

    def update(self, input: SessionInput) -> api_pb2.Session:
        try:
            return self._command.update(input._to_proto())
        except grpc.RpcError as e:
            raise wrap_error(e) from e

    def update_subject(self, input: io_pb2.UpdateSessionSubjectRequest) -> api_pb2.Session:
        try:
            return self._command.updateSubject(input)
        except grpc.RpcError as e:
            raise wrap_error(e) from e

    def delete(self, id: str) -> api_pb2.Session:
        try:
            return self._command.delete(io_pb2.SessionId(value=id))
        except grpc.RpcError as e:
            raise wrap_error(e) from e

    def get(self, id: str) -> api_pb2.Session:
        try:
            return self._query.get(io_pb2.SessionId(value=id))
        except grpc.RpcError as e:
            raise wrap_error(e) from e

    def list(self, input: io_pb2.ListSessionsRequest) -> io_pb2.SessionList:
        try:
            return self._query.list(input)
        except grpc.RpcError as e:
            raise wrap_error(e) from e

    def list_by_agent_instance(self, input: io_pb2.ListSessionsByAgentInstanceRequest) -> io_pb2.SessionList:
        try:
            return self._query.listByAgentInstance(input)
        except grpc.RpcError as e:
            raise wrap_error(e) from e

    def list_by_channel(self, input: io_pb2.ListSessionsByChannelRequest) -> io_pb2.SessionList:
        try:
            return self._query.listByChannel(input)
        except grpc.RpcError as e:
            raise wrap_error(e) from e


@dataclass
class SessionInput:
    """Input for creating or updating a Session."""

    name: str
    org: str
    id: str | None = None
    slug: str | None = None
    labels: dict[str, str] | None = None
    visibility: int = 0
    agent_instance_id: str = ""
    subject: str = ""
    harness_state_id: str = ""
    harness_state_id_history: list[str] = field(default_factory=list)
    metadata: dict[str, str] = field(default_factory=dict)
    workspace_entries: list[WorkspaceEntryInput] = field(default_factory=list)
    mcp_server_usages: list[McpServerUsageInput] = field(default_factory=list)
    skill_refs: list[ResourceRef] = field(default_factory=list)
    harness: int = 0
    cursor_mode: int = 0
    execution_target: int = 0

    def _to_proto(self) -> api_pb2.Session:
        spec = spec_pb2.SessionSpec(
            agent_instance_id=self.agent_instance_id,
            subject=self.subject,
            harness_state_id=self.harness_state_id,
            harness=self.harness,
            cursor_mode=self.cursor_mode,
            execution_target=self.execution_target,
        )
        if self.harness_state_id_history:
            spec.harness_state_id_history.extend(self.harness_state_id_history)
        if self.metadata:
            spec.metadata.update(self.metadata)
        for item in self.workspace_entries:
            spec.workspace_entries.append(item._to_proto())
        for item in self.mcp_server_usages:
            spec.mcp_server_usages.append(item._to_proto())
        for ref in self.skill_refs:
            _ref = ref._to_proto()
            _ref.kind = 43
            spec.skill_refs.append(_ref)
        metadata = metadata_pb2.ApiResourceMetadata(
            name=self.name,
            org=self.org,
        )
        if self.id:
            metadata.id = self.id
        if self.slug:
            metadata.slug = self.slug
        if self.labels:
            metadata.labels.update(self.labels)
        if self.visibility:
            metadata.visibility = self.visibility
        return api_pb2.Session(
            api_version="agentic.stigmer.ai/v1",
            kind="Session",
            metadata=metadata,
            spec=spec,
        )

