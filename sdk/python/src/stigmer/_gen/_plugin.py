# Code generated by stigmer-codegen. DO NOT EDIT.

from __future__ import annotations

from dataclasses import dataclass, field

import grpc

from ai.stigmer.agentic.plugin.v1 import api_pb2
from ai.stigmer.agentic.plugin.v1 import command_pb2_grpc
from ai.stigmer.agentic.plugin.v1 import query_pb2_grpc
from ai.stigmer.agentic.plugin.v1 import io_pb2
from ai.stigmer.agentic.plugin.v1 import spec_pb2
from ai.stigmer.commons.apiresource import io_pb2 as apiresource_io_pb2
from ai.stigmer.commons.apiresource import metadata_pb2
from ai.stigmer.commons.apiresource.apiresourcekind import api_resource_kind_pb2
from ai.stigmer.search.v1 import query_pb2_grpc as search_query_pb2_grpc
from ai.stigmer.search.v1 import io_pb2 as search_io_pb2
from ai.stigmer.commons.rpc import pagination_pb2

from ._errors import wrap_error
from ._types import ListParams, ListResult, ResourceRef


class PluginClient:
    """Provides operations on plugin resources."""

    def __init__(self, channel: grpc.Channel) -> None:
        self._command = command_pb2_grpc.PluginCommandControllerStub(channel)
        self._query = query_pb2_grpc.PluginQueryControllerStub(channel)
        self._search = search_query_pb2_grpc.SearchServiceStub(channel)

    def push(self, input: io_pb2.PushPluginRequest) -> api_pb2.Plugin:
        try:
            return self._command.push(input)
        except grpc.RpcError as e:
            raise wrap_error(e) from e

    def create_artifact_upload_url(self, input: io_pb2.CreatePluginArtifactUploadUrlRequest) -> io_pb2.PluginArtifactUploadUrl:
        try:
            return self._command.createArtifactUploadUrl(input)
        except grpc.RpcError as e:
            raise wrap_error(e) from e

    def update_visibility(self, input: io_pb2.UpdateVisibilityInput) -> api_pb2.Plugin:
        try:
            return self._command.updateVisibility(input)
        except grpc.RpcError as e:
            raise wrap_error(e) from e

    def delete(self, id: str) -> api_pb2.Plugin:
        try:
            return self._command.delete(io_pb2.PluginId(value=id))
        except grpc.RpcError as e:
            raise wrap_error(e) from e

    def get(self, id: str) -> api_pb2.Plugin:
        try:
            return self._query.get(io_pb2.PluginId(value=id))
        except grpc.RpcError as e:
            raise wrap_error(e) from e

    def get_by_reference(self, ref: ResourceRef) -> api_pb2.Plugin:
        try:
            proto = ref._to_proto()
            proto.kind = api_resource_kind_pb2.plugin
            return self._query.getByReference(proto)
        except grpc.RpcError as e:
            raise wrap_error(e) from e

    def list_members(self, id: str) -> io_pb2.ListPluginMembersResponse:
        try:
            return self._query.listMembers(io_pb2.PluginId(value=id))
        except grpc.RpcError as e:
            raise wrap_error(e) from e

    def list_versions(self, input: io_pb2.ListPluginVersionsInput) -> io_pb2.ListPluginVersionsResponse:
        try:
            return self._query.listVersions(input)
        except grpc.RpcError as e:
            raise wrap_error(e) from e

    def list(self, params: ListParams) -> ListResult:
        try:
            req = search_io_pb2.SearchRequest(
                kinds=[api_resource_kind_pb2.ApiResourceKind.plugin],
                query=params.query,
                org=params.org,
            )
            if params.page is not None:
                req.page.CopyFrom(pagination_pb2.PageInfo(
                    num=params.page.num,
                    size=params.page.size,
                ))
            resp = self._search.search(req)
            return ListResult(
                entries=list(resp.entries),
                total_count=resp.total_count,
                total_pages=resp.total_pages,
            )
        except grpc.RpcError as e:
            raise wrap_error(e) from e


@dataclass
class PluginInput:
    """Input for creating or updating a Plugin."""

    name: str
    org: str
    id: str | None = None
    slug: str | None = None
    labels: dict[str, str] | None = None
    visibility: int = 0
    version_message: str = ""
    version: str = ""
    description: str = ""
    author: PluginAuthorInput | None = None
    homepage: str = ""
    repository: str = ""
    license: str = ""
    keywords: list[str] = field(default_factory=list)
    dialect: int = 0

    def _to_proto(self) -> api_pb2.Plugin:
        spec = spec_pb2.PluginSpec(
            version=self.version,
            description=self.description,
            homepage=self.homepage,
            repository=self.repository,
            license=self.license,
            dialect=self.dialect,
        )
        if self.author is not None:
            spec.author.CopyFrom(self.author._to_proto())
        if self.keywords:
            spec.keywords.extend(self.keywords)
        metadata = metadata_pb2.ApiResourceMetadata(
            name=self.name,
            org=self.org,
        )
        if self.id:
            metadata.id = self.id
        if self.slug:
            metadata.slug = self.slug
        if self.labels:
            metadata.labels.update(self.labels)
        if self.visibility:
            metadata.visibility = self.visibility
        if self.version_message:
            metadata.version.CopyFrom(metadata_pb2.ApiResourceMetadataVersion(
                message=self.version_message,
            ))
        return api_pb2.Plugin(
            api_version="agentic.stigmer.ai/v1",
            kind="Plugin",
            metadata=metadata,
            spec=spec,
        )


@dataclass
class PluginAuthorInput:
    """SDK input type for PluginAuthor."""

    name: str = ""
    email: str = ""
    url: str = ""

    def _to_proto(self) -> spec_pb2.PluginAuthor:
        msg = spec_pb2.PluginAuthor(
            name=self.name,
            email=self.email,
            url=self.url,
        )
        return msg

