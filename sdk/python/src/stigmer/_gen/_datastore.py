# Code generated by stigmer-codegen. DO NOT EDIT.

from __future__ import annotations

from dataclasses import dataclass, field
from typing import Any

import grpc

from google.protobuf import json_format

from ai.stigmer.agentic.datastore.v1 import api_pb2
from ai.stigmer.agentic.datastore.v1 import command_pb2_grpc
from ai.stigmer.agentic.datastore.v1 import record_command_pb2_grpc
from ai.stigmer.agentic.datastore.v1 import record_query_pb2_grpc
from ai.stigmer.agentic.datastore.v1 import query_pb2_grpc
from ai.stigmer.agentic.datastore.v1 import io_pb2
from ai.stigmer.agentic.datastore.v1 import record_io_pb2
from ai.stigmer.agentic.datastore.v1 import spec_pb2
from ai.stigmer.commons.apiresource import io_pb2 as apiresource_io_pb2
from ai.stigmer.commons.apiresource import metadata_pb2
from ai.stigmer.commons.apiresource.apiresourcekind import api_resource_kind_pb2
from ai.stigmer.search.v1 import query_pb2_grpc as search_query_pb2_grpc
from ai.stigmer.search.v1 import io_pb2 as search_io_pb2
from ai.stigmer.commons.rpc import pagination_pb2
from ai.stigmer.iam.iampolicy.v1 import spec_pb2 as iampolicy_spec_pb2

from ._errors import wrap_error
from ._types import DeleteResourceInput, ListParams, ListResult, ResourceRef


class DatastoreClient:
    """Provides operations on datastore resources."""

    def __init__(self, channel: grpc.Channel) -> None:
        self._command = command_pb2_grpc.DatastoreCommandControllerStub(channel)
        self._query = query_pb2_grpc.DatastoreQueryControllerStub(channel)
        self._datastoreRecordCommand = record_command_pb2_grpc.DatastoreRecordCommandControllerStub(channel)
        self._datastoreRecordQuery = record_query_pb2_grpc.DatastoreRecordQueryControllerStub(channel)
        self._search = search_query_pb2_grpc.SearchServiceStub(channel)

    def apply(self, input: DatastoreInput) -> api_pb2.Datastore:
        try:
            return self._command.apply(input._to_proto())
        except grpc.RpcError as e:
            raise wrap_error(e) from e

    def create(self, input: DatastoreInput) -> api_pb2.Datastore:
        try:
            return self._command.create(input._to_proto())
        except grpc.RpcError as e:
            raise wrap_error(e) from e

    def update(self, input: DatastoreInput) -> api_pb2.Datastore:
        try:
            return self._command.update(input._to_proto())
        except grpc.RpcError as e:
            raise wrap_error(e) from e

    def update_visibility(self, input: io_pb2.UpdateVisibilityInput) -> api_pb2.Datastore:
        try:
            return self._command.updateVisibility(input)
        except grpc.RpcError as e:
            raise wrap_error(e) from e

    def delete(self, input: DeleteResourceInput) -> api_pb2.Datastore:
        try:
            return self._command.delete(input._to_proto())
        except grpc.RpcError as e:
            raise wrap_error(e) from e

    def get(self, id: str) -> api_pb2.Datastore:
        try:
            return self._query.get(apiresource_io_pb2.ApiResourceId(value=id))
        except grpc.RpcError as e:
            raise wrap_error(e) from e

    def get_by_reference(self, ref: ResourceRef) -> api_pb2.Datastore:
        try:
            proto = ref._to_proto()
            proto.kind = api_resource_kind_pb2.datastore
            return self._query.getByReference(proto)
        except grpc.RpcError as e:
            raise wrap_error(e) from e

    def insert_record(self, input: record_io_pb2.InsertRecordRequest) -> record_io_pb2.RecordEnvelope:
        try:
            return self._datastoreRecordCommand.insertRecord(input)
        except grpc.RpcError as e:
            raise wrap_error(e) from e

    def update_record(self, input: record_io_pb2.UpdateRecordRequest) -> record_io_pb2.RecordEnvelope:
        try:
            return self._datastoreRecordCommand.updateRecord(input)
        except grpc.RpcError as e:
            raise wrap_error(e) from e

    def delete_record(self, input: record_io_pb2.DeleteRecordRequest) -> record_io_pb2.RecordEnvelope:
        try:
            return self._datastoreRecordCommand.deleteRecord(input)
        except grpc.RpcError as e:
            raise wrap_error(e) from e

    def find_records(self, input: record_io_pb2.FindRecordsRequest) -> record_io_pb2.RecordList:
        try:
            return self._datastoreRecordQuery.findRecords(input)
        except grpc.RpcError as e:
            raise wrap_error(e) from e

    def describe_datastore(self, input: record_io_pb2.DescribeDatastoreRequest) -> record_io_pb2.DatastoreDescription:
        try:
            return self._datastoreRecordQuery.describeDatastore(input)
        except grpc.RpcError as e:
            raise wrap_error(e) from e

    def list(self, params: ListParams) -> ListResult:
        try:
            req = search_io_pb2.SearchRequest(
                kinds=[api_resource_kind_pb2.ApiResourceKind.datastore],
                query=params.query,
                org=params.org,
                exclude_public=params.exclude_public,
                cross_org_public=params.cross_org_public,
            )
            if params.page is not None:
                req.page.CopyFrom(pagination_pb2.PageInfo(
                    num=params.page.num,
                    size=params.page.size,
                ))
            resp = self._search.search(req)
            return ListResult(
                entries=list(resp.entries),
                total_count=resp.total_count,
                total_pages=resp.total_pages,
            )
        except grpc.RpcError as e:
            raise wrap_error(e) from e


@dataclass
class DatastoreInput:
    """Input for creating or updating a Datastore."""

    name: str
    org: str
    slug: str | None = None
    labels: dict[str, str] | None = None
    visibility: int = 0
    description: str = ""
    timezone: str = ""
    authorization: DatastoreAuthorizationInput | None = None
    collections: list[CollectionDeclarationInput] = field(default_factory=list)

    def _to_proto(self) -> api_pb2.Datastore:
        spec = spec_pb2.DatastoreSpec(
            description=self.description,
            timezone=self.timezone,
        )
        if self.authorization is not None:
            spec.authorization.CopyFrom(self.authorization._to_proto())
        for item in self.collections:
            spec.collections.append(item._to_proto())
        metadata = metadata_pb2.ApiResourceMetadata(
            name=self.name,
            org=self.org,
        )
        if self.slug:
            metadata.slug = self.slug
        if self.labels:
            metadata.labels.update(self.labels)
        if self.visibility:
            metadata.visibility = self.visibility
        return api_pb2.Datastore(
            api_version="agentic.stigmer.ai/v1",
            kind="Datastore",
            metadata=metadata,
            spec=spec,
        )


@dataclass
class DatastoreAuthorizationInput:
    """SDK input type for DatastoreAuthorization."""

    roles: list[DatastoreRoleInput] = field(default_factory=list)
    bindings: list[DatastoreRoleBindingInput] = field(default_factory=list)
    default_role: str = ""

    def _to_proto(self) -> spec_pb2.DatastoreAuthorization:
        msg = spec_pb2.DatastoreAuthorization(
            default_role=self.default_role,
        )
        for item in self.roles:
            msg.roles.append(item._to_proto())
        for item in self.bindings:
            msg.bindings.append(item._to_proto())
        return msg


@dataclass
class DatastoreRoleInput:
    """SDK input type for DatastoreRole."""

    name: str

    def _to_proto(self) -> spec_pb2.DatastoreRole:
        msg = spec_pb2.DatastoreRole(
            name=self.name,
        )
        return msg


@dataclass
class DatastoreRoleBindingInput:
    """SDK input type for DatastoreRoleBinding."""

    subject: DatastoreSubjectInput | None
    role: str

    def _to_proto(self) -> spec_pb2.DatastoreRoleBinding:
        msg = spec_pb2.DatastoreRoleBinding(
            role=self.role,
        )
        if self.subject is not None:
            msg.subject.CopyFrom(self.subject._to_proto())
        return msg


@dataclass
class DatastoreSubjectInput:
    """SDK input type for DatastoreSubject."""

    channel_sender: ChannelSenderSubjectInput | None = None
    principal: ApiResourceRefInput | None = None

    def _to_proto(self) -> spec_pb2.DatastoreSubject:
        msg = spec_pb2.DatastoreSubject()
        if self.channel_sender is not None:
            msg.channel_sender.CopyFrom(self.channel_sender._to_proto())
        if self.principal is not None:
            msg.principal.CopyFrom(self.principal._to_proto())
        return msg


@dataclass
class ChannelSenderSubjectInput:
    """SDK input type for ChannelSenderSubject."""

    sender_kind: str
    value: str

    def _to_proto(self) -> spec_pb2.ChannelSenderSubject:
        msg = spec_pb2.ChannelSenderSubject(
            sender_kind=self.sender_kind,
            value=self.value,
        )
        return msg


@dataclass
class ApiResourceRefInput:
    """SDK input type for ApiResourceRef."""

    kind: str
    id: str
    relation: str = ""

    def _to_proto(self) -> iampolicy_spec_pb2.ApiResourceRef:
        msg = iampolicy_spec_pb2.ApiResourceRef(
            kind=self.kind,
            id=self.id,
            relation=self.relation,
        )
        return msg


@dataclass
class CollectionDeclarationInput:
    """SDK input type for CollectionDeclaration."""

    name: str
    description: str = ""
    fields: list[FieldDeclarationInput] = field(default_factory=list)
    uniques: list[UniqueConstraintInput] = field(default_factory=list)
    checks: list[CheckConstraintInput] = field(default_factory=list)
    exists: list[ExistsConstraintInput] = field(default_factory=list)
    not_exists: list[ExistsConstraintInput] = field(default_factory=list)
    grants: list[DatastoreGrantInput] = field(default_factory=list)

    def _to_proto(self) -> spec_pb2.CollectionDeclaration:
        msg = spec_pb2.CollectionDeclaration(
            name=self.name,
            description=self.description,
        )
        for item in self.fields:
            msg.fields.append(item._to_proto())
        for item in self.uniques:
            msg.uniques.append(item._to_proto())
        for item in self.checks:
            msg.checks.append(item._to_proto())
        for item in self.exists:
            msg.exists.append(item._to_proto())
        for item in self.not_exists:
            msg.not_exists.append(item._to_proto())
        for item in self.grants:
            msg.grants.append(item._to_proto())
        return msg


@dataclass
class FieldDeclarationInput:
    """SDK input type for FieldDeclaration."""

    name: str
    type: int
    required: bool = False
    default: Any = None
    enum_values: list[str] = field(default_factory=list)
    description: str = ""

    def _to_proto(self) -> spec_pb2.FieldDeclaration:
        msg = spec_pb2.FieldDeclaration(
            name=self.name,
            type=self.type,
            required=self.required,
            description=self.description,
        )
        if self.default is not None:
            json_format.ParseDict(self.default, msg.default)
        if self.enum_values:
            msg.enum_values.extend(self.enum_values)
        return msg


@dataclass
class UniqueConstraintInput:
    """SDK input type for UniqueConstraint."""

    name: str
    message: str
    fields: list[str] = field(default_factory=list)
    where: UniqueWhereInput | None = None

    def _to_proto(self) -> spec_pb2.UniqueConstraint:
        msg = spec_pb2.UniqueConstraint(
            name=self.name,
            message=self.message,
        )
        if self.fields:
            msg.fields.extend(self.fields)
        if self.where is not None:
            msg.where.CopyFrom(self.where._to_proto())
        return msg


@dataclass
class UniqueWhereInput:
    """SDK input type for UniqueWhere."""

    field: str
    equals: Any

    def _to_proto(self) -> spec_pb2.UniqueWhere:
        msg = spec_pb2.UniqueWhere(
            field=self.field,
        )
        if self.equals is not None:
            json_format.ParseDict(self.equals, msg.equals)
        return msg


@dataclass
class CheckConstraintInput:
    """SDK input type for CheckConstraint."""

    name: str
    expression: str
    message: str
    when: str = ""

    def _to_proto(self) -> spec_pb2.CheckConstraint:
        msg = spec_pb2.CheckConstraint(
            name=self.name,
            expression=self.expression,
            when=self.when,
            message=self.message,
        )
        return msg


@dataclass
class ExistsConstraintInput:
    """SDK input type for ExistsConstraint."""

    name: str
    collection: str
    where: str
    message: str
    when: str = ""

    def _to_proto(self) -> spec_pb2.ExistsConstraint:
        msg = spec_pb2.ExistsConstraint(
            name=self.name,
            collection=self.collection,
            where=self.where,
            when=self.when,
            message=self.message,
        )
        return msg


@dataclass
class DatastoreGrantInput:
    """SDK input type for DatastoreGrant."""

    role: str
    verbs: list[int] = field(default_factory=list)
    scope: int = 0

    def _to_proto(self) -> spec_pb2.DatastoreGrant:
        msg = spec_pb2.DatastoreGrant(
            role=self.role,
            scope=self.scope,
        )
        if self.verbs:
            msg.verbs.extend(self.verbs)
        return msg

