# Code generated by stigmer-codegen. DO NOT EDIT.

from __future__ import annotations

from dataclasses import dataclass

import grpc

from ai.stigmer.agentic.schedule.v1 import api_pb2
from ai.stigmer.agentic.schedule.v1 import command_pb2_grpc
from ai.stigmer.agentic.schedule.v1 import query_pb2_grpc
from ai.stigmer.agentic.schedule.v1 import io_pb2
from ai.stigmer.agentic.schedule.v1 import spec_pb2
from ai.stigmer.commons.apiresource import io_pb2 as apiresource_io_pb2
from ai.stigmer.commons.apiresource import metadata_pb2
from ai.stigmer.commons.apiresource.apiresourcekind import api_resource_kind_pb2

from ._errors import wrap_error
from ._types import ResourceRef


class ScheduleClient:
    """Provides operations on schedule resources."""

    def __init__(self, channel: grpc.Channel) -> None:
        self._command = command_pb2_grpc.ScheduleCommandControllerStub(channel)
        self._query = query_pb2_grpc.ScheduleQueryControllerStub(channel)

    def apply(self, input: ScheduleInput) -> api_pb2.Schedule:
        try:
            return self._command.apply(input._to_proto())
        except grpc.RpcError as e:
            raise wrap_error(e) from e

    def create(self, input: ScheduleInput) -> api_pb2.Schedule:
        try:
            return self._command.create(input._to_proto())
        except grpc.RpcError as e:
            raise wrap_error(e) from e

    def update(self, input: ScheduleInput) -> api_pb2.Schedule:
        try:
            return self._command.update(input._to_proto())
        except grpc.RpcError as e:
            raise wrap_error(e) from e

    def delete(self, id: str) -> api_pb2.Schedule:
        try:
            return self._command.delete(io_pb2.ScheduleId(value=id))
        except grpc.RpcError as e:
            raise wrap_error(e) from e

    def get(self, id: str) -> api_pb2.Schedule:
        try:
            return self._query.get(io_pb2.ScheduleId(value=id))
        except grpc.RpcError as e:
            raise wrap_error(e) from e

    def get_by_reference(self, ref: ResourceRef) -> api_pb2.Schedule:
        try:
            proto = ref._to_proto()
            proto.kind = api_resource_kind_pb2.schedule
            return self._query.getByReference(proto)
        except grpc.RpcError as e:
            raise wrap_error(e) from e

    def get_by_agent(self, input: io_pb2.GetSchedulesByAgentRequest) -> io_pb2.ScheduleList:
        try:
            return self._query.getByAgent(input)
        except grpc.RpcError as e:
            raise wrap_error(e) from e

    def list(self, input: io_pb2.ListSchedulesRequest) -> io_pb2.ScheduleList:
        try:
            return self._query.list(input)
        except grpc.RpcError as e:
            raise wrap_error(e) from e


@dataclass
class ScheduleInput:
    """Input for creating or updating a Schedule."""

    name: str
    org: str
    slug: str | None = None
    labels: dict[str, str] | None = None
    visibility: int = 0
    cron: str = ""
    time_zone: str = ""
    enabled: bool = False
    agent: AgentTargetInput | None = None

    def _to_proto(self) -> api_pb2.Schedule:
        spec = spec_pb2.ScheduleSpec(
            cron=self.cron,
            time_zone=self.time_zone,
            enabled=self.enabled,
        )
        if self.agent is not None:
            spec.agent.CopyFrom(self.agent._to_proto())
        metadata = metadata_pb2.ApiResourceMetadata(
            name=self.name,
            org=self.org,
        )
        if self.slug:
            metadata.slug = self.slug
        if self.labels:
            metadata.labels.update(self.labels)
        if self.visibility:
            metadata.visibility = self.visibility
        return api_pb2.Schedule(
            api_version="agentic.stigmer.ai/v1",
            kind="Schedule",
            metadata=metadata,
            spec=spec,
        )


@dataclass
class AgentTargetInput:
    """SDK input type for AgentTarget."""

    agent_ref: ResourceRef | None
    message: str = ""

    def _to_proto(self) -> spec_pb2.AgentTarget:
        msg = spec_pb2.AgentTarget(
            message=self.message,
        )
        if self.agent_ref is not None and (self.agent_ref.org or self.agent_ref.slug):
            _ref = self.agent_ref._to_proto()
            _ref.kind = 40
            msg.agent_ref.CopyFrom(_ref)
        return msg

