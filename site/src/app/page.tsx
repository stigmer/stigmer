import { HomePage } from "@/components/pages/HomePage";

/**
 * Root page - renders the Stigmer landing page.
 *
 * This is the entry point for https://stigmer.ai/
 * All page composition is handled by the HomePage component.
 */
export default function Page() {
  return <HomePage />;
}
