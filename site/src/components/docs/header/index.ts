/**
 * Docs header (DD-02) — the Cursor-style top bar for the docs layout.
 * Layout chrome, not page content: wired via `nav.component` in
 * `app/docs/layout.tsx`, not exported through the docs MDX barrel.
 */

export { DocsHeader } from "./DocsHeader";
export { selectActiveTab } from "./active-tab";
