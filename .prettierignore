# Auto-generated files — regenerated by `make codegen`
docs/sdk/

# The server's bundled registry artifacts are byte-exact copies of the
# generator output (gen-task-registry-check diffs them literally), so
# formatting must not touch them.
backend/services/stigmer-server/src/domain/workflow/registry/data/

# The compiled authorization model is the generator's canonical output
# (gen-authorization-model-check compares it byte for byte), so formatting
# must not touch it.
backend/services/stigmer-server/src/authorization/model/data/

# Vendored plugins from cursor/plugins (the plugin reader's real-world
# fixtures); the NOTICE names the source and commit and the files must stay
# byte-identical to it, so formatting must not touch them.
backend/libs/ts/plugin-package/src/__tests__/fixtures/cursor-plugins/

# The Cursor hook configuration is rendered by `make agents-sync` and compared
# byte for byte by `make agents-check`, so formatting must not touch it.
.cursor/hooks.json
