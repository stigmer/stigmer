{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "additionalProperties": false,
  "properties": {
    "catch": {
      "additionalProperties": false,
      "description": "Catch block for error handling (optional). If not provided, errors propagate to parent.",
      "properties": {
        "as": {
          "description": "Variable name to store the error (optional). Accessible via ${ .error } in catch tasks.",
          "type": "string"
        },
        "compensate": {
          "description": "Whether to run compensation tasks for already-completed try tasks before executing the catch do block. When true, the runtime walks the compensation stack (completed tasks that declared a `compensate` block) in reverse order and executes each task's compensation actions. After all compensations complete (or fail), the catch `do` block runs as normal. Compensation failures do not prevent the catch block from running. They are logged and included in the task output for diagnostics. Default: false (no compensation — preserves pre-T17 behavior).",
          "type": "boolean"
        },
        "do": {
          "description": "Tasks to execute when error is caught.",
          "items": {
            "additionalProperties": false,
            "properties": {
              "compensate": {
                "description": "Compensation tasks to execute if this task needs to be \"undone.\"",
                "items": {
                  "type": "object"
                },
                "type": "array"
              },
              "export": {
                "additionalProperties": false,
                "description": "Export configuration (how to save task output to context). Optional - if not set, output is not saved.",
                "properties": {
                  "as": {
                    "description": "Expression defining how to export output using ${...} syntax.",
                    "minLength": 1,
                    "type": "string"
                  }
                },
                "type": "object"
              },
              "flow": {
                "additionalProperties": false,
                "description": "Flow control (which task executes next). Optional - if not set, continues to next task in sequence.",
                "properties": {
                  "then": {
                    "description": "Target task name or \"end\" to terminate workflow.",
                    "type": "string"
                  }
                },
                "type": "object"
              },
              "kind": {
                "description": "Task type (determines how to interpret task_config).",
                "enum": [
                  "set_vars",
                  "http_call",
                  "grpc_call",
                  "activity_call",
                  "switch_case",
                  "for_each",
                  "fork",
                  "try_catch",
                  "listen",
                  "wait",
                  "raise_error",
                  "run_workflow",
                  "agent_call",
                  "llm_call",
                  "transform",
                  "human_input",
                  "validate",
                  "emit_event",
                  "notification",
                  "eval"
                ],
                "type": "string"
              },
              "name": {
                "description": "Task name/identifier (must be unique within workflow). Must be a valid C-style identifier: starts with a letter or underscore, followed by letters, digits, or underscores.",
                "minLength": 1,
                "pattern": "^[a-zA-Z_][a-zA-Z0-9_]*$",
                "type": "string"
              },
              "task_config": {
                "description": "Task-specific configuration whose structure depends on the `kind` field.",
                "type": "object"
              }
            },
            "required": [
              "kind",
              "task_config"
            ],
            "type": "object"
          },
          "minItems": 1,
          "type": "array"
        }
      },
      "type": "object"
    },
    "try": {
      "description": "Tasks to attempt (at least one required). If any task fails, execution jumps to catch block.",
      "items": {
        "additionalProperties": false,
        "properties": {
          "compensate": {
            "description": "Compensation tasks to execute if this task needs to be \"undone.\"",
            "items": {
              "type": "object"
            },
            "type": "array"
          },
          "export": {
            "additionalProperties": false,
            "description": "Export configuration (how to save task output to context). Optional - if not set, output is not saved.",
            "properties": {
              "as": {
                "description": "Expression defining how to export output using ${...} syntax.",
                "minLength": 1,
                "type": "string"
              }
            },
            "type": "object"
          },
          "flow": {
            "additionalProperties": false,
            "description": "Flow control (which task executes next). Optional - if not set, continues to next task in sequence.",
            "properties": {
              "then": {
                "description": "Target task name or \"end\" to terminate workflow.",
                "type": "string"
              }
            },
            "type": "object"
          },
          "kind": {
            "description": "Task type (determines how to interpret task_config).",
            "enum": [
              "set_vars",
              "http_call",
              "grpc_call",
              "activity_call",
              "switch_case",
              "for_each",
              "fork",
              "try_catch",
              "listen",
              "wait",
              "raise_error",
              "run_workflow",
              "agent_call",
              "llm_call",
              "transform",
              "human_input",
              "validate",
              "emit_event",
              "notification",
              "eval"
            ],
            "type": "string"
          },
          "name": {
            "description": "Task name/identifier (must be unique within workflow). Must be a valid C-style identifier: starts with a letter or underscore, followed by letters, digits, or underscores.",
            "minLength": 1,
            "pattern": "^[a-zA-Z_][a-zA-Z0-9_]*$",
            "type": "string"
          },
          "task_config": {
            "description": "Task-specific configuration whose structure depends on the `kind` field.",
            "type": "object"
          }
        },
        "required": [
          "kind",
          "task_config"
        ],
        "type": "object"
      },
      "minItems": 1,
      "type": "array"
    }
  },
  "title": "TryTaskConfig",
  "type": "object"
}