apiVersion: kubernetes.project-planton.org/v1
kind: KubernetesDeployment
metadata:
  name: agent-runner
  env: prod
  labels:
    pulumi.project-planton.org/stack.name: prod.KubernetesDeployment.agent-runner
spec:
  namespace:
    value: stigmer-prod
  container:
    app:
      resources:
        limits:
          cpu: 2000m
          memory: 4Gi
        requests:
          cpu: 200m
          memory: 512Mi
      env:
        variables:
          ENV:
            value: prod
          # Temporal Configuration (using internal Kubernetes endpoints)
          TEMPORAL_SERVICE_ADDRESS:
            value: $variables-group/stigmer-temporal-config/prod.kube-endpoint
          TEMPORAL_NAMESPACE:
            value: default
          TEMPORAL_MAX_CONCURRENCY:
            value: "20"
          # Stigmer API Configuration (internal Kubernetes endpoint, plain gRPC)
          STIGMER_BACKEND_ENDPOINT:
            value: $variables-group/stigmer-api/prod.kube-endpoint
          # Side-Channel Proxy endpoint (same hostname as gRPC API, path-routed
          # to port 8081 via supplementary HTTPRoute on /v1/proxy prefix)
          STIGMER_PROXY_ENDPOINT:
            value: https://api.stigmer.ai
          # Public gRPC endpoint injected into MCP server subprocesses.
          # Daytona sandboxes run outside K8s and cannot reach the internal
          # kube-endpoint, so this must be the public ingress with TLS.
          STIGMER_MCP_PUBLIC_ENDPOINT:
            value: $variables-group/stigmer-api/prod.grpc-endpoint
          # LLM Configuration (production uses Anthropic)
          # Provider API keys are now in stigmer-service (injected by proxy)
          STIGMER_LLM_PROVIDER:
            value: anthropic
          STIGMER_LLM_MODEL:
            value: claude-sonnet-4.5
          # Artifact storage routed through Side-Channel Proxy
          ARTIFACT_STORAGE_TYPE:
            value: proxy
          # Checkpointer routed through Side-Channel Proxy
          STIGMER_CHECKPOINTER_TYPE:
            value: http
        secrets:
          # Stigmer API Key (used for gRPC + proxy auth)
          STIGMER_API_KEY:
            value: $secrets-group/stigmer-api/prod.api-key
          # Daytona API Key (remains until Phase 2 moves sandbox creation server-side)
          DAYTONA_API_KEY:
            value: $secrets-group/daytona/prod.api-key
  availability:
    minReplicas: 1
    # Zero-downtime deployment strategy
    deploymentStrategy:
      maxUnavailable: "0"
      maxSurge: "1"
    # Pod disruption budget for cluster maintenance protection
    podDisruptionBudget:
      enabled: true
      minAvailable: "1"
