# Session Notes: May 3, 2026 — Session 24

## Accomplishments

- Created comprehensive user-facing documentation for the Cursor harness
  feature across the docs site
- Added 2 new MDX pages (concept + guide), updated 4 existing pages, added 3
  vocabulary entries, and updated 2 navigation files
- All changes pass Vale lint (0 errors) and Prettier format check
- Committed as `98cf4876a`

## Decisions Made

- **Naming**: "Harnesses" as page title (matching API term); vocabulary entry
  bridges to "execution engine" for sales/quickstart contexts
- **No interactive demo**: Explanation page uses comparison table and Mermaid
  diagram; demo deferred until harness selector UI stabilizes
- **Guide scope**: Happy path only; cloud proxy internals excluded as
  backend/operator detail
- **Progressive disclosure**: Did not add harness to getting-started/quickstart;
  users discover it through concepts or the model picker
- **SDK refs untouched**: Auto-generated pages already document harness types
  correctly via codegen

## Key Code Changes

- `docs/concepts/harnesses.mdx`: New explanation page — what harnesses are,
  native vs Cursor comparison table, Mermaid architecture diagram, SDK examples
  (TypeScript/Go), "What's next" bridging to guide
- `docs/guides/runners/cursor-harness.mdx`: New how-to guide — prerequisites,
  step-by-step local setup, cloud setup, session creation, model selection with
  compound keys, approval flow parity, limitations table, troubleshooting
- `docs/vocabulary.md`: Harness (Tier 1), Cursor harness (Tier 2),
  cursor-runner (Tier 3), quick-reference table row
- `docs/concepts/sessions.mdx`: Harness section with YAML example
- `docs/concepts/runners.mdx`: Dual-worker paragraph, guide link, Harnesses
  concept page bridge in What's next
- `docs/guides/runners/overview.mdx`: Cursor harness card in guides grid
- `docs/concepts/meta.json`: `"harnesses"` after `"runners"`
- `docs/guides/runners/meta.json`: `"cursor-harness"` after `"cloud-mode"`

## Learnings

- The existing Diataxis + vocabulary framework made it straightforward to place
  new content — the three-layer strategy (concept anchor, actionable guide,
  cross-cutting updates) fits cleanly
- Auto-generated SDK reference pages already had adequate harness coverage from
  codegen — no manual enrichment needed
- Vale term warnings are pervasive in existing docs (pre-existing lowercase
  "agent", "session", etc.) — not introduced by this session

## Open Questions

- None blocking

## Next Session Plan

- Consider adding a Scenar interactive demo when the harness selector UI
  stabilizes and has a visual moment worth showcasing
