# Session Notes: 2026-04-23

## Accomplishments

Completed Workstream A in full — all 14 deliverables shipped:

### Design Decisions (8 files)
- **DD-001**: SDK-first development — build in `@stigmer/react` first, consume from Console second
- **DD-002**: Console is a thin shell — `app/` is routes + page layout only, zero domain logic
- **DD-003**: Headless-first architecture — data hooks → behavior hooks → styled components
- **DD-004**: Zero framework deps in SDK — no `next/*`, no Console routing, no app-shell auth
- **DD-005**: Theme token compliance — all visuals via `--stgm-*` tokens, scoped `.stgm` + `@layer stgm`
- **DD-006**: Error messages as UX — what happened, why, what to do
- **DD-007**: Generated types are source of truth — protos → SDK → React, never hand-rolled
- **DD-008**: Single provider model — `StigmerProvider` + `useStigmer()` as the only IoC point

### Dont-Dos (5 files)
- **001**: No Console-specific imports in SDK
- **002**: No framework dependencies in SDK
- **003**: No hardcoded colors or sizes
- **004**: No opacity modifiers on design tokens
- **005**: No technical-function grouping in Console

### Cursor Rule (1 file)
- `.cursor/rules/client-apps/web/sdk-console-architecture.mdc` — 87 lines, auto-loads for `client-apps/web/src/` and `sdk/react/src/` files

## Decisions Made

- The user's chat preamble text (architecture philosophy they paste into every session) was formalized into the cursor rule's opening section rather than kept as a standalone document. This means agents get it automatically without manual paste.
- DD-005 cross-references `theme-token-guidelines.mdc` for operational details rather than duplicating token-specific patterns. The DD establishes the principle; the existing rule provides the reference tables.
- Dont-do 005 (no technical-function grouping) explicitly notes it describes the target state — the current Console still uses feature-folder structure. Workstream B will perform the restructuring.

## Key Code Changes

No code changes — Workstream A is pure documentation. Files created:
- `_projects/.../design-decisions/DD-001-*.md` through `DD-008-*.md`
- `_projects/.../dont-dos/001-*.md` through `005-*.md`
- `.cursor/rules/client-apps/web/sdk-console-architecture.mdc`

## Next Session Plan

1. **Workstream C: Architectural Metrics** — establish baseline before restructuring
2. **Workstream B: Console Domain Organization** — the actual restructuring
