# Session Notes: 2026-04-26, Session 11 — Promote Runners to Top-Level Navigation

## Accomplishments

- Promoted Runners from Settings > Infrastructure to a top-level navigation item alongside Sessions and Library
- Changes applied consistently across both web console and desktop app
- Backward-compatible redirects in place for old `/settings/runners` paths

## Decisions Made

- **Sidebar order**: New Session, Library, Runners, [separator], Recents — frequency-of-use ordering
- **Domain folder move**: `RunnersSection` moved from `domain/settings/` to `domain/runner/` following dont-do #5 (domain-based organization)
- **Settings cleanup**: "Infrastructure" group removed from `SETTINGS_NAV_GROUPS` entirely — it only contained Runners and the remaining groups (Organization, Configuration, Billing & Usage) are genuinely settings concerns
- **Zone logic**: No AppShell changes needed — `/runners` naturally falls through to `children` rendering since it's neither `isManagementZone` nor `isSessionZone`

## Key Code Changes

- `sdk/react/src/settings/settings-nav.ts`: Removed Infrastructure group, removed `Server` icon import
- `client-apps/web/src/domain/_shared/layout/Sidebar.tsx`: Added Runners link with Server icon, isRunnersActive state
- `client-apps/web/src/app/runners/page.tsx`: New top-level route
- `client-apps/web/src/domain/runner/RunnersSection.tsx`: Moved from `domain/settings/`
- `client-apps/web/src/app/settings/runners/page.tsx`: Converted to redirect
- `client-apps/desktop/src/shell/Sidebar.tsx`: Added Runners NavLink with Server icon
- `client-apps/desktop/src/routes.tsx`: Moved runners route to top-level, added Navigate redirect at old path

## Verification

All targets pass:
- SDK lint + typecheck: clean
- Web console lint: clean
- Desktop lint + typecheck + cargo check: 0 errors, 9 pre-existing warnings

## Commit

`076575038 feat(sdk,web,desktop): promote Runners from Settings to top-level navigation`
