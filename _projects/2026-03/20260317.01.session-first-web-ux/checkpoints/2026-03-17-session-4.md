# Session Notes: 2026-03-17 (Session 4)

## Accomplishments

- Completed React SDK teardown -- the SDK counterpart to the T01.3 web UI teardown
- Deleted 33 files across 7 directories (2,959 lines) from `sdk/react/`
- Cleaned up `package.json`: removed 6 sub-path exports, 5 unused peer dependencies, updated description and keywords
- Stripped `README.md` to cover only the provider/hook/styles infrastructure
- Verified typecheck and build pass clean for both `sdk/react` and `client-apps/web`
- Created changelog: `_changelog/2026-03/2026-03-17-102117-react-sdk-teardown-session-first-clean-slate.md`

## Decisions Made

- **Full teardown, not selective cleanup** -- deleted all feature modules rather than keeping some "useful" ones. Same rationale as T01.3: legacy patterns should not influence the new platform-for-platforms design.
- **Internal primitives deleted too** -- the `internal/` directory (Button, Textarea, Badge, Section, Collapsible) was only consumed by deleted feature modules. New primitives will be designed alongside the new components.
- **Peer dependencies pruned aggressively** -- removed `@base-ui/react`, `class-variance-authority`, `lucide-react`, `react-markdown`, `remark-gfm`. These will be re-evaluated when feature components are rebuilt.

## Key Code Changes

- `sdk/react/src/agent/` -- deleted (AgentPicker, AgentCard, AgentOverview, useAgentSearch)
- `sdk/react/src/session/` -- deleted (SessionCard, AgentSessionHistory, useAgentSessionList)
- `sdk/react/src/agent-execution/` -- deleted (ExecutionStream, MessageEntry, MessageInput, ToolCallCard, ApprovalControls, SubAgentCard, OutputBlock, ExecutionStatus, useAgentExecution, useApproval, helpers)
- `sdk/react/src/catalog/` -- deleted (ResourceSearchCard, time utils)
- `sdk/react/src/skill/` -- deleted (useSkillSearch)
- `sdk/react/src/mcp-server/` -- deleted (useMcpServerSearch)
- `sdk/react/src/internal/` -- deleted (Button, Textarea, Badge, Section, Collapsible)
- `sdk/react/package.json` -- cleaned exports, peerDependencies, description, keywords
- `sdk/react/README.md` -- stripped to provider-only documentation

## Learnings

- The web app only imports 3 things from `@stigmer/react`: `StigmerProvider`, `useStigmer`, and `styles.css`. All feature module imports had already been removed by T01.3.
- The `dist/` directory contained stale build output from the pre-teardown codebase and needed to be deleted separately.

## Open Questions

- None for this session. The teardown is complete and clean.

## Next Session Plan

- Continue with T01.4: Web App Shell (three-panel layout)
- Components to create: `AppHeader.tsx`, `Sidebar.tsx`, `ContextPanel.tsx`, `AppShell.tsx` in `client-apps/web/src/components/layout/`
- As the web UI is rebuilt (T01.5/T01.6), new SDK components will be designed with embeddability as a primary constraint
