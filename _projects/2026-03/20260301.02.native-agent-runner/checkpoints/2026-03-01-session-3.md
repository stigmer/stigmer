# Session Notes: 2026-03-01 (Session 3)

## Accomplishments

- Audited CI pipeline (`release.cli.yaml`) and Makefile for post-migration gaps
- Identified 8 gaps: release-blocking CI bug, dead Docker job, broken Makefile target, stale docs, sync.sh path bug
- Fixed all 4 affected files: release.cli.yaml, Makefile, release-workflow.md, sync.sh
- Net result: 54 insertions, 121 deletions across 4 files

## Decisions Made

- `release-local` uses dev mode (no embed tag) since source is auto-located from repo tree — simpler and correct for local dev
- Added `build-release` target for developers who want to verify the production embed path locally
- All 3 platform build jobs now gate on `lint-and-typecheck-agent-runner` (previously only the dead Docker job did)
- `clean` target now removes `client-apps/cli/embedded/agentrunner/source/` (the sync output) instead of the dead sentinel file

## Key Code Changes

- `.github/workflows/release.cli.yaml`: Removed `build-agent-runner-image` job, added sync+embed to all 3 platform builds, fixed release job needs, cleaned Docker refs from changelog
- `Makefile`: Removed sentinel, rewrote release-local, added build-release, cleaned clean target
- `.github/workflows/docs/release-workflow.md`: Replaced PyInstaller references with sync.sh + //go:embed
- `client-apps/cli/embedded/agentrunner/sync.sh`: Fixed graphton path

## Learnings

- The CI pipeline had a release-blocking bug — no released binary would have had agent-runner source embedded
- The `sync.sh` graphton path bug was pre-existing (not from the migration) — the `if [ -d ]` guard masked it silently

## Open Questions

- None

## Next Session Plan

- T01.6: End-to-End Validation on macOS arm64
