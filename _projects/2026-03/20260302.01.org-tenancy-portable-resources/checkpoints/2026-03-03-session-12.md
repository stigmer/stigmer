# Session Notes: 2026-03-03 (Session 12) — T01.9: Product Documentation Update

## Accomplishments

- Completed T01.9: Eliminated all `org: local` from documentation that feeds into skill generation
- Fixed 7 product docs (`docs/product/what-is-*.md`, `docs/guides/uploading-skills.md`)
- Fixed 13 API resource docs (`agent/docs/` 7 files, `mcpserver/docs/` 4 files, `skill/docs/` 2 files)
- Rewrote `resource-references.md` to document optional org and relative vs absolute references (matching T01.3 proto changes)
- Consolidated CLI config docs: merged `configuration.md` + `configuration-cascade.md` into single file, added Organization Context section
- Documented: `--org` flag, `stigmer context show/set`, `context.organization`, `STIGMER_ORG_ID`, resolution chain
- Verified all skill-generation-attached directories clean (zero `org: local`)

## Decisions Made

- **No `what-is-cli-configuration.md`**: CLI configuration is a tool concern, not a domain resource. The `docs/product/what-is-*.md` pattern is reserved for domain resources. The existing `docs/cli/configuration.md` serves this purpose.
- **YAML example org strategy**: `default` for getting-started metadata (the bootstrapped org), omit org for cross-references (relative pattern from T01.3), `acme-corp` for illustrative examples.
- **resource-references.md rewrite**: Updated org from "Required" to optional, documented relative (empty org) vs absolute (explicit org) references. This reflects the T01.3 proto change that was made but never documented.
- **"Local Mode" / "Cloud Mode" removal**: Replaced all local/cloud dichotomy prose with unified org model documentation. Org resolution is the same code path regardless of backend type.

## Key Code Changes

- `docs/cli/configuration.md`: Complete rewrite with new Organization Context section
- `docs/cli/configuration-cascade.md`: Deleted (content merged)
- `apis/ai/stigmer/agentic/agent/docs/resource-references.md`: Complete rewrite for optional org
- `apis/ai/stigmer/agentic/agent/docs/examples.md`: All examples use relative references
- `docs/product/what-is-organization.md`: "Default Organization" section replaces "Local Mode" / "Cloud Mode"

## Learnings

- The API resource docs (`apis/.../docs/`) are the PRIMARY input to skill generation (spotlight-attached), while `docs/product/what-is-*.md` are secondary (workspace-browsable). The original plan only identified the secondary files — the primary ones were a surprise discovery.
- The `resource-references.md` file was documenting org as "Required" even though T01.3 made it optional weeks ago. Proto changes need corresponding doc updates at the same time.

## Open Questions

- ~14 `org: local` hits remain in secondary API docs (workflow, workflowinstance, environment, agentinstance, agentexecution, project). Not urgent since they're not spotlight-attached to skill generation scripts.

## Next Session Plan

- Optional: clean up remaining secondary API docs
- Run `seedpack/tools/regenerate_all.sh` to verify skills generate cleanly with updated docs
