# Session Notes: 2026-03-03

## Accomplishments

- Completed T01.1: Migrate Project Proto from Agentic to Management Domain
- All 7 phases executed successfully:
  1. Proto files moved and updated (6 files)
  2. Stubs regenerated via `make protos` (Go + Python)
  3. Go import paths updated across 33 hand-written files
  4. apiVersion string literals updated in 15 Project-context files
  5. Codegen schema migrated + MCP server code regenerated
  6. Seedpack, examples, and documentation updated
  7. Full build + test verification passed

## Decisions Made

- **Clean break, no backward compatibility**: User explicitly chose to skip dual-apiVersion support. Proto enforces `management.stigmer.ai/v1` exclusively via `buf.validate` `string.const`
- **MCP codegen regeneration over manual edit**: Used `stigmer-codegen` comprehensive mode to regenerate `project_gen.go` rather than manually editing, ensuring the output stays in sync with the schema
- **Agent examples preserved**: `agentic.stigmer.ai/v1` references in Agent/Workflow/McpServer/Skill contexts left untouched -- only Project-specific occurrences changed
- **stigmer-cloud not modified**: Cloud team handles their own proto bump on next tag

## Key Code Changes

- `apis/ai/stigmer/management/project/v1/*.proto`: New home for Project proto (package `ai.stigmer.management.project.v1`)
- `apis/ai/stigmer/agentic/project/v1/`: Deleted entirely
- Backend + CLI + e2e Go files: Import path `agentic/project/v1` → `management/project/v1`
- Test fixtures: `"agentic.stigmer.ai/v1"` → `"management.stigmer.ai/v1"` in Project contexts
- `tools/codegen/schemas/management/project/project.json`: Schema relocated with updated proto references
- `mcp-server/gen/management/project/project_gen.go`: Regenerated at new path
- `seedpack/stigmer.yaml`: Uses `management.stigmer.ai/v1`

## Learnings

- The `make protos` target's `go-stubs-clean` step handles old stub cleanup automatically -- no need to manually delete generated stubs
- The `stigmer-codegen` comprehensive mode auto-discovers schemas from `tools/codegen/schemas/<domain>/<resource>/` and generates to `mcp-server/gen/<domain>/<resource>/` -- moving the schema is sufficient to redirect output
- `apply_declarative_test.go` and `apply_file_test.go` contain Agent/Workflow test fixtures with `agentic.stigmer.ai/v1` that must NOT be changed during Project migration

## Open Questions

- None -- T01.1 is cleanly complete

## Next Session Plan

- T01.2: Add Organization as a supported resource kind in the Project apply pipeline
- T01.3+: Org-agnostic cross-references, real Organization bootstrap in seedpack
