# Session Notes: 2026-03-19 (Session 6)

## Accomplishments

- Implemented T01.6: `useAgentInstance` data hook
  - New file: `sdk/react/src/agent-instance/useAgentInstance.ts` (~85 lines)
  - New file: `sdk/react/src/agent-instance/index.ts` (barrel export)

## Decisions Made

- **Return property naming**: Changed from plan's `instance` to `agentInstance` to maintain consistency with the established convention (`useSession` → `session`, `useEnvironment` → `environment`). The full domain noun avoids ambiguity when platform builders destructure multiple hooks in the same component.

## Key Code Changes

- **sdk/react/src/agent-instance/useAgentInstance.ts**: New Layer 1 data hook. Fetches a single `AgentInstance` by `ResourceRef` via `stigmer.agentInstance.getByReference()`. Structurally identical to `useEnvironment`: `ResourceRef | null` input, primitive destructuring for `useEffect` deps, cancellation flag, `refetch()` via `fetchKey` increment. JSDoc documents Layer 1 building-block role and cross-references `usePersonalAgentInstance` for Layer 2.
- **sdk/react/src/agent-instance/index.ts**: Barrel export for the new module — `useAgentInstance` + `UseAgentInstanceReturn`.

## Learnings

- No new discoveries; the `useEnvironment` pattern held exactly. The agent-instance module is the first new directory created in this project (`sdk/react/src/agent-instance/`).

## Open Questions

- None.

## Next Session Plan

- T01.7: `useCreateAgentInstance` behavior hook (wraps `stigmer.agentInstance.create()`, same pattern as `useCreateEnvironment`).
