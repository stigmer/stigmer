# Session Notes: 2026-03-19, Session 7

## Accomplishments

- Implemented T01.7: `useCreateAgentInstance` behavior hook
  - New file: `sdk/react/src/agent-instance/useCreateAgentInstance.ts` (~80 lines)
  - Updated barrel: `sdk/react/src/agent-instance/index.ts` — added `useCreateAgentInstance` + `UseCreateAgentInstanceReturn` exports
  - TypeScript compiles cleanly, zero linter errors

## Decisions Made

- Followed the `useCreateEnvironment` pattern exactly — no deviations needed
- Error fallback message: `"Failed to create agent instance"` (consistent with domain noun convention across all behavior hooks)
- JSDoc includes forward reference to `usePersonalAgentInstance` (Phase 2) for discoverability

## Key Code Changes

- `sdk/react/src/agent-instance/useCreateAgentInstance.ts`: New behavior hook wrapping `stigmer.agentInstance.create()` with `{ create, isCreating, error, clearError }` return shape. Accepts `AgentInstanceInput` from `@stigmer/sdk` (fields: `name`, `org`, optional `agentId`, `description`, `environmentRefs`). Returns full `AgentInstance` proto.
- `sdk/react/src/agent-instance/index.ts`: Added two export lines for the new hook and its return type.

## Learnings

- The behavior hook pattern is now fully stable across five implementations (`useCreateEnvironment`, `useUpdateEnvironment`, `useUpdateSession`, `useCreateSession`, `useCreateAgentInstance`). The pattern is mechanical: wrap a single SDK client call in `useCallback` with `useState` for `isCreating`/`isUpdating` and `error`.

## Open Questions

- None — this was a straightforward pattern application.

## Next Session Plan

- T01.8: Barrel exports — wire agent, environment, agent-instance modules into main `sdk/react/src/index.ts`
- T01.9: SessionComposer integration — add AgentPicker to the toolbar
- T01.10: `useCreateSession` wiring — add `agentRef` / `agentInstanceId` to `CreateSessionInput`
- T01.11: Console integration — wire agent state into `SessionLauncher`
