# Session Notes: 2026-03-14

## Accomplishments

- **T01 Complete**: Proto TypeScript Codegen Setup
  - Added TypeScript/Connect-RPC codegen to the stigmer OSS build pipeline
  - 169 generated TypeScript stubs covering all proto definitions
  - Fully integrated into `make protos` / `make build` workflow

## Files Created

- `apis/buf.gen.ts.yaml` — Buf codegen config (bufbuild/es v2.2.2, connectrpc/es v1.6.1)
- `apis/stubs/ts/package.json` — `@stigmer/protos` internal package manifest
- `apis/stubs/ts/ai/stigmer/**/*_pb.ts` — Generated protobuf message types
- `apis/stubs/ts/ai/stigmer/**/*_connect.ts` — Generated Connect-RPC service definitions
- `apis/stubs/ts/buf/validate/validate_pb.ts` — Generated protovalidate types

## Files Modified

- `apis/Makefile` — Added `ts-stubs`, `ts-stubs-clean`, `ts-stubs-init` targets
- `.gitignore` — Added `node_modules/` exclusion

## Decisions Made

- **Plugin versions pinned** to match stigmer-cloud (v2.2.2 / v1.6.1) for consistency; upgrading is a separate action
- **`@connectrpc/connect` omitted from package.json** — follows stigmer-cloud pattern, relies on workspace hoisting (to be set up in T02)
- **Generated stubs committed to git** — matches existing Go/Python convention
- **Option A chosen** for dependency declaration (pragmatic match over theoretical correctness)

## Learnings

- stigmer-cloud `ts-stubs-clean` has a bug: cleans `com/` (never exists) instead of `ai/` (the actual generated directory), causing 435 stale files to accumulate
- All 169 OSS-generated files have exact counterparts in stigmer-cloud (verified via `comm` comparison)
- `include_imports: true` on the `bufbuild/es` plugin generates `buf/validate/` types locally, required for proto files that use `buf.validate` constraints

## Next Session Plan

- Start T02: Migrate Web Source to Stigmer Repo
  - Copy `client-apps/web/` from stigmer-cloud
  - Set up npm workspaces at root level
  - Rewire `@stigmer/protos` from `workspace:*` to local path
  - Strip `next-auth` / `@auth/*` hard dependencies
  - Verify `npm install && npm run dev` works
