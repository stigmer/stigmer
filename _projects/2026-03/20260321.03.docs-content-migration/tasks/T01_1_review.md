# Task T01: Review Feedback

**Date**: 2026-03-21
**Reviewer**: Developer

## Feedback

### Decision: Clean Slate, Not Migration

The existing documentation is too unreliable to migrate. Content was generated by AI sessions that made assumptions about the implementation. Instead of validating and patching ~120 stale files, **delete everything and rebuild from ground truth**.

Ground truth sources:
- Protobuf definitions (API contracts, resource models)
- SDK source code
- Actual CLI behavior
- Running implementation

### Specific Decisions

1. **Mermaid** — confirmed for diagrams.
2. **Delete all legacy docs** — every `.md` file in `docs/` except `docs/standards/`. Also delete the existing `.mdx` content (quickstart, concepts) since they contain stale information.
3. **Rebuild from protos** — derive concept docs, CLI reference, and guides from the actual API contracts and implementation, not from existing stale docs.
4. **SDK icons** — show all 5 SDKs: Go, Java, Python, React, TypeScript.
5. **Architecture docs** — delete these too. No validated confidence that they're accurate. Rebuild when needed from actual code.
6. **Phase 5 (internal triage)** — eliminated. Everything gets deleted. No triage needed.

### What to Keep

- `docs/standards/` (standards, templates, terminology — output of previous project)
- Fumadocs framework setup in `site/` (routes, layout, source config)
- `docs/meta.json` structural skeleton (rewrite entries)
