# Session Notes: 2026-03-21 (Session 5)

## Phase Completed

**Phase 3: Top 5 Concept Docs from Protos** — Complete

## Accomplishments

- Created `docs/concepts/` directory with full Fumadocs integration (meta.json, index.mdx)
- Wrote 5 concept docs from proto ground truth:
  1. `what-is-stigmer.mdx` — Platform overview, 4-layer resource hierarchy, dual execution model (Agents + Workflows), container analogies
  2. `agent.mdx` — Template layer: AgentSpec (instructions, MCP server usages, skill refs, sub-agents, env spec), YAML examples from `agent/docs/examples.md`
  3. `agent-execution.mdx` — Runtime layer: ExecutionPhase state machine (8 phases), HITL approvals, cost caps, attachments, context management
  4. `session.mdx` — Conversation context: thread continuity, workspace persistence (git + local sources), session-level tool/skill augmentation
  5. `workflow.mdx` — Deterministic orchestration: 13 task types, `agent_call` integration, Temporal durability, Workflow vs Agent comparison
- Updated `docs/meta.json` to include `"concepts"` in sidebar
- Updated `docs/index.mdx` Concepts card with `href="/docs/concepts"` (removed "Coming soon")
- Build verification: `yarn build` passes cleanly — 13 static pages including all 5 concept routes

## Source Material Used

Each doc was anchored to proto definitions and their companion `docs/` directories:
- `apis/ai/stigmer/agentic/agent/v1/spec.proto` + `agent/docs/`
- `apis/ai/stigmer/agentic/agentexecution/v1/spec.proto` + `enum.proto` + `agentexecution/docs/`
- `apis/ai/stigmer/agentic/session/v1/spec.proto` + `workspace.proto` + `session/docs/`
- `apis/ai/stigmer/agentic/workflow/v1/spec.proto` + `enum.proto` + `tasks/agent_call.proto` + `workflow/docs/`

## Decisions Made

- **CLI examples for AgentExecution and Session**: Used `stigmer run` and `stigmer session` CLI commands instead of YAML for the "Getting started" sections, since these resources are created at runtime, not authored as YAML files
- **AgentInstance gap**: Mentioned AgentInstance inline in all docs (it's central to the 4-layer narrative) with brief explanations rather than swapping one of the top 5
- **Template adherence**: Followed `docs/standards/templates/concept.mdx` section order but adapted YAML examples section for runtime resources (AgentExecution, Session) that use CLI instead of `stigmer apply`
- **Mermaid diagrams**: Used graph (flowcharts) for resource hierarchies and lifecycle flows; used stateDiagram-v2 for the AgentExecution phase state machine

## Key Code Changes

- `docs/concepts/meta.json` — sidebar ordering: what-is-stigmer, agent, agent-execution, session, workflow
- `docs/concepts/index.mdx` — section landing page with Cards linking to 5 docs
- `docs/concepts/what-is-stigmer.mdx` — 2 Mermaid diagrams (4-layer lifecycle, dual model overview), layer comparison table, Without vs With table
- `docs/concepts/agent.mdx` — 1 Mermaid diagram (runtime stack), key properties table from AgentSpec, full YAML examples
- `docs/concepts/agent-execution.mdx` — 1 Mermaid state diagram (ExecutionPhase), spec and status properties tables
- `docs/concepts/session.mdx` — 1 Mermaid diagram (session grouping), two creation modes (explicit vs automatic)
- `docs/concepts/workflow.mdx` — 1 Mermaid diagram (workflow lifecycle), 13 task types table, Agent vs Workflow comparison
- `docs/meta.json` — added `"concepts"` to pages array
- `docs/index.mdx` — Concepts card now links to `/docs/concepts`

## Learnings

- The proto `docs/` directories are rich source material — each resource has a README.md with clear hierarchy diagrams, analogies, and capability summaries that transfer well to concept docs
- The container analogy (Agent=Docker image, AgentInstance=container config, Session=terminal session, AgentExecution=docker run) is the single most effective framing for platform builders
- The `agent_call` task type is the key connection point between the Agent and Workflow execution models — it deserves prominence in both the Workflow and AgentExecution docs

## Open Questions

- None — Phase 3 is complete

## Next Session Plan

1. **Phase 4: Validated CLI Quickstart** — every command tested against the real CLI implementation
2. **Phase 5: Verification** — lint, build, link check, visual review
