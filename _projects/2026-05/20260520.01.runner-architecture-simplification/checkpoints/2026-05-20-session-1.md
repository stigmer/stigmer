# Session Notes: 2026-05-20

## Accomplishments

- Completed T01: Codebase Inventory & Impact Audit
- Mapped the full blast radius across both repos (stigmer OSS + stigmer-cloud)
- Produced comprehensive audit report with exact file paths for every deletion/refactor target

## Audit Coverage

| Area | Status | Key output |
|------|--------|------------|
| Runner API protos | Mapped | 6 proto files + stubs in 4 languages |
| Runner TS service | Mapped | ~120 production files, 3 execution planes, heartbeat/control-plane coupling identified |
| Java control plane | Mapped | 24 prod + 5 test files to delete, 8+ cross-domain files to refactor |
| Session-to-runner routing | Mapped | Current dispatch flow documented end-to-end, memo-based routing confirmed as correct abstraction |
| Desktop app (Tauri) | Mapped | Sidecar-based architecture, 15+ frontend hooks/pages, launch token flow |
| Cloud sandbox (Daytona) | Mapped | Launcher coupled to Runner create, env injection, side-channel proxy |

## Key Discoveries

1. Desktop app is Tauri v2, not Electron — research report assumption was wrong
2. Desktop already uses native folder dialogs — no bidi `ListDirectory` dependency
3. Desktop doesn't embed the TS runner — orchestrates Go CLI which spawns Python/cursor-runner processes
4. The Temporal memo `activityTaskQueue` pattern is already the right decoupling point
5. Cloud never writes back `runner_id` to session after ephemeral provisioning — proto comment is aspirational
6. `RunnerDispatchService` and `DaytonaSandboxRunnerLauncher` are the two highest-coupling Java classes

## Decisions Made

- Task execution order: T02+T03 (parallel) → T04 → T05 → T06 → T07 → T08
- T05 (Java control plane) is the critical risk — flagged for extra planning when reached
- No design decisions or coding guidelines created yet — will establish as implementation begins

## Open Questions

- Should the desktop app eventually embed `@stigmer/runner` directly (bypassing CLI sidecar), or keep the sidecar pattern with session-based queues?
- Should `session:{sessionId}` be the queue naming convention, or something more specific like `stigmer:session:{sessionId}`?
- How to handle the transition period where existing Runner resources exist in MongoDB?

## Next Session Plan

1. Start T02 (delete Runner API protos) and/or T03 (scaffold NPM factory) — both are independent and low-risk
2. T02 should be a clean proto deletion + stub regeneration + cross-reference cleanup
3. T03 should extract `createStigmerRunner()` from current `main.ts` and make heartbeat.ts deletable
