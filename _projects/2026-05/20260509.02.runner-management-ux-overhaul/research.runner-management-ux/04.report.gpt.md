# Runner Management UX and Architecture

## Executive synthesis

The clearest pattern across comparable products is that **runner/agent lifecycle is either treated as an infrastructure concern or hidden behind a service boundary**. GitHub Actions, GitLab Runner, and Buildkite all separate **one-time registration/configuration** from **ongoing execution**, and they provide persistent background service modes plus explicit status surfaces. Cursor Cloud Agents, Devin, and Windsurf’s newer cloud-agent workflows go further: they make the execution substrate mostly invisible by running work in isolated cloud environments and surfacing runs, sessions, or task boards instead of machine daemons. citeturn3view0turn3view1turn10view0turn10view1turn18view0turn22view0turn31view0turn29search0turn29search5

For Stigmer, the strongest recommendation is to **stop treating “Start Runner” as a repeated user action**. The default desktop experience should instead be: **“This machine is connected”**. On first use, the app should install or enable a single background runner service for that machine and organisation, generate a stable machine identity, and then simply keep it healthy. After first setup, the UI should show **Connected / Reconnecting / Needs attention / Paused**, not an empty start form. This follows the service-managed model used by GitHub, GitLab, and Buildkite, while borrowing the “work with tasks, not daemons” UX from Cursor, Devin, and Windsurf. citeturn3view1turn41view0turn10view1turn10view2turn13view2turn16view2turn16view4turn22view0turn26view0turn29search0turn29search5

In practice, that means the best answer to your current “runner already running” problem is **Option A with a touch of Option B**: **auto-adopt the existing healthy runner, and present that as success**. A transparent stop-and-replace flow should be reserved for unhealthy, stale, or version-incompatible processes, not for the normal path. Removing the “Start Runner” button entirely once a machine has been connected is also the right long-term direction; after setup, the primary controls should be things like **Resume, Restart, Pause, Open logs**, and **Use this machine for local runs**. This recommendation is an inference from how mature systems expose status and control planes rather than repeatedly asking users to start agents from scratch. citeturn41view0turn10view3turn17search0turn18view0turn20view3turn33view1turn29search0

## Comparative platform patterns

The table below focuses on the workflow most relevant to Stigmer: registration, identity, duplicate handling, persistence, and what the user actually sees.

| Platform | Setup and start model | Identity model | Duplicate or multi-instance model | Persistence and status surface |
|---|---|---|---|---|
| **GitHub Actions self-hosted runner** | One-time `config` with a **time-limited registration token**, then run the runner and optionally install it as a **systemd / launchd** service so it auto-starts on boot. citeturn3view0turn3view1 | GitHub surfaces **runner name, labels, and status**. The runner CLI also exposes a `Replace` configure flag in code, but the documented operating model is still register/manage/remove rather than “adopt an already-running local process”. citeturn41view0turn7search6turn34view0 | GitHub’s own guidance is effectively **one runner process per directory**, with **separate directories** for multiple runners on one machine; one runner processes one job at a time. citeturn8search15turn3view2 | UI statuses are **Idle / Active / Offline**. Offline runners stay visible until restarted or removed; persistent runners are auto-removed after **14 days** offline. citeturn41view0turn34view0 |
| **GitLab Runner** | `gitlab-runner register` writes config to `config.toml`; `gitlab-runner run` is the long-running process, and `install/start/status` manage it as a service. citeturn10view0turn10view1 | GitLab distinguishes **runner configuration** from **runner manager** and gives machines a persistent **`system_id`** so multiple machines sharing the same runner configuration can still be distinguished in the UI. citeturn10view4turn10view5 | Multiple runners on one host are first-class: repeat `register`, add multiple `[[runners]]` sections, and the runner manager runs them concurrently. citeturn10view0turn10view1turn10view5 | GitLab supports **pause/resume**, and the UI exposes **online / offline / stale** statuses. Its service layer supports Linux init systems and macOS `launchd`. citeturn10view2turn10view3 |
| **Buildkite Agent** | `buildkite-agent start` both **starts and registers** the agent with Buildkite using an agent token; newer `bk agent install` can install the binary and create token/config locally. citeturn13view2turn18view0turn11search2 | Buildkite’s primary identity is a **UUID agent ID**; `name`, `hostname`, and tags are metadata layered on top. That is a stronger separation of human-readable label from machine identity than hostname-only schemes. citeturn13view4turn13view1 | Buildkite supports **multiple workers** via `--spawn`, explicit one-job ephemeral modes via `--disconnect-after-job`, and pause/resume instead of forcing users to kill processes for routine maintenance. citeturn14view3turn13view2turn17search0turn17search2turn17search3 | On Linux, packages enable/start via `systemctl`; on macOS, docs recommend `launchd` / login-start. The platform exposes agents through API/CLI status and browser views rather than “blind start” actions. citeturn16view2turn16view4turn18view0turn13view4 |
| **Cursor Cloud Agents** | Users start **agent runs**, not machine daemons. Cloud agents run in isolated cloud VMs and can be launched from **web, desktop, Slack, GitHub, Linear, and API**. citeturn22view0 | Identity is **run/session/repo/environment** oriented, with reusable **environment setup and snapshots**, not user-managed daemon names. citeturn22view0turn22view1 | The model is explicitly **parallel**: many agents can run at once, and they do **not require the local machine to stay online**. citeturn22view0turn28search8 | Status is surfaced through the **Agents Window**, web controls, and automations; Cursor also supports scheduled automations that create cloud-agent runs in the background. citeturn19search1turn26view0 |
| **Windsurf** | Windsurf increasingly frames agent work as a mix of **local and cloud agents** managed from inside the IDE, rather than as standalone background daemons the user must name and babysit. citeturn29search0turn29search1 | Public docs emphasise an **Agent Command Center** and **Spaces** that group sessions, PRs, and context by task/status. citeturn29search0turn29search3 | The core UX is “see all agents in one place”, with a **Kanban view grouped by status**. That is a management-plane surface, not a raw process-start screen. citeturn29search0turn29search1 | Windsurf 2.0 explicitly shows **local and cloud agents together** and integrates cloud work review directly in-editor. citeturn29search0turn29search1turn29search5 |
| **Devin** | Devin’s core model is **cloud sessions** backed by configured environments and snapshots; more recently, **Devin for Terminal** offers a local entry point that can be handed off to the cloud. citeturn31view0turn30search2turn33view0 | Identity is **session + environment snapshot + organisation**, not a user-managed local runner name. citeturn33view0turn33view1 | Sessions are created on demand from snapshots, and scheduled sessions can run automatically. The product exposes **session states and schedule states**, not PID collision errors. citeturn33view1turn32search14 | Devin offers web, Slack, IDE extension, API, and scheduled-session workflows; its own docs position scheduled sessions as recurring background automation. citeturn31view0turn33view1turn30search13 |

The most important commonality is not that all of these products behave identically. It is that **the user’s day-to-day mental model is either “my machine is connected” or “my task/session is running”**. It is almost never **“I must remember whether I started the daemon five hours ago, what I named it, and whether the current button will create a new identity or collide with the old one.”** That distinction is the core UX lesson for Stigmer. citeturn41view0turn10view3turn18view0turn22view0turn29search0turn33view1

## What invisible runner UX looks like

The strongest local-runner pattern is **one-time onboarding, ongoing service management**. GitHub has users run a `config` script once, then optionally install a service; GitLab separates `register` from `run` and service management; Buildkite installs config, starts the agent, and then relies on service managers and dashboards for normal operation. In all three, there is a clear difference between **provisioning** and **being available for work**. citeturn3view0turn3view1turn10view0turn10view1turn13view2turn16view2turn16view4

The strongest cloud-agent pattern is **task-first UX**. Cursor agents are started from where the user already works and run in isolated VMs, with environment setup and snapshots handled separately. Devin similarly treats the environment as a bootable snapshot and lets users create regular or scheduled sessions without thinking about a local daemon. Windsurf’s Agent Command Center points in the same direction: a single management surface grouped by status, spanning local and cloud agents. citeturn22view0turn22view1turn26view0turn33view0turn33view1turn29search0turn29search1

A second best-in-class pattern is **stable machine identity separate from display name**. GitLab’s `system_id` is especially instructive: one runner configuration can be reused across machines, but each machine still has its own persistent identifier. Buildkite is similarly explicit that the platform’s real identity is an agent UUID, while name and hostname are attributes. This is the right answer to the hostname-collision problem that your current slugging scheme creates. A human-readable label is useful in UI; it is a poor primary key. citeturn10view4turn10view5turn13view4

A third pattern is **status first, errors second**. GitHub shows Idle, Active, and Offline. GitLab shows online, offline, and stale, and supports pause/resume. Buildkite exposes connected, disconnected, stopping, and stopped states in its API and provides pause/resume commands and UI/API controls. These systems do not eliminate operational problems, but they make the primary user experience “something is already there; here is its state” rather than “you clicked the wrong button; now interpret a PID error”. citeturn41view0turn10view3turn13view4turn17search0turn18view0

The final pattern is **background continuity across user absence**. GitHub, GitLab, and Buildkite all document OS-native service operation. Cursor and Devin instead move the work to cloud environments that continue whether or not the laptop is awake. Both approaches are valid. The less successful middle ground is a foreground process spawned from a GUI that looks temporary to the user, yet is persistent enough to collide hours later. citeturn3view1turn10view2turn16view2turn16view4turn22view0turn33view0turn30search0

## Anti-patterns to avoid

The first anti-pattern is **conflating “create/connect a machine” with “start a new process right now”**. GitLab’s docs are clear that registration creates configuration while `run` is the long-lived runtime; Buildkite distinguishes install/configure from agent runtime; GitHub’s own UX is centred on adding/configuring a runner, then checking status in the runners page. Stigmer’s current modal collapses those into a single repeated action, which is why a user who merely wants local execution ends up in a naming conflict path. citeturn10view0turn10view1turn3view0turn41view0turn18view0

The second anti-pattern is **using a prompt-time human label as the default identity key**. Hostname defaults are fine as display names, but mature systems either assign stable machine identifiers beneath that layer or identify runtime entities with UUIDs. GitLab’s `system_id` and Buildkite’s UUID-based agent model are better references for Stigmer than hostname-slug-as-primary-key. citeturn10view4turn10view5turn13view4

The third anti-pattern is **treating “already running” as a failure for the common path**. GitHub, GitLab, and Buildkite all assume that a runner or agent may already exist and therefore emphasise status pages, service status commands, and pause/resume/stop controls. Cursor, Devin, and Windsurf largely avoid the issue by shifting the user’s mental model from daemon management to task/session management. A blocking collision error is appropriate for advanced multi-runner provisioning; it is not appropriate for the default developer-machine path. citeturn41view0turn10view3turn17search0turn18view0turn22view0turn29search0turn33view1

The fourth anti-pattern is **making the desktop app a thin launcher for a foreground CLI sidecar, with no stable adoption model**. Once a runner can outlive the app session, the desktop needs to become a **control plane** for that service, not a fire-and-forget process spawner. Buildkite’s pause/resume and agent list, GitLab’s service commands and statuses, and GitHub’s runner status UI all reflect that distinction. citeturn18view0turn10view1turn10view3turn41view0

## Recommended Stigmer architecture

The right default for Stigmer’s desktop app is **one default runner per machine per organisation**, backed by a **stable `machine_id`** stored locally and reused forever. The user-facing runner name should become a display label only. Internally, routing and local adoption should key off something like **`(machine_id, org_id, backend_endpoint)`**, with a separate mutable display name defaulting to the hostname. This recommendation is directly aligned with GitLab’s persistent `system_id` model and Buildkite’s separation of UUID identity from agent name. citeturn10view4turn10view5turn13view4

The desktop flow should be split into **initial connection** and **ongoing health management**. On first use, the app should silently or near-silently perform the following: create the machine record, persist credentials, install an OS-managed background service, start it, and then show **Connected** once the local health check and backend heartbeat succeed. After that, opening the app should never present an empty “Start Runner” form unless the machine is genuinely unconfigured or the user is doing something advanced. This is the same provisioning/runtime separation used by GitHub, GitLab, and Buildkite. citeturn3view0turn3view1turn10view0turn10view1turn13view2turn16view2turn16view4

Your existing backend semantics already sound compatible with this, because you described `runner.apply()` as **create-or-update**. That should be exploited at the UX layer. On “connect this machine”, the desktop should not try to mint a fresh logical runner identity every time; it should **reconcile** the local machine with the canonical machine record and either reuse the current runtime session or rotate it in place if needed. In other words, the backend’s idempotent semantics should become the user experience too.

The immediate remediation for the current failure mode should be:

- When the user clicks the current button and a healthy local runner service already exists for this machine and org, **treat it as success** and bring the UI to the connected runner.
- If local metadata exists but the process is dead or the last heartbeat is stale, **reap and restart silently**.
- If a process exists but is unhealthy, version-skewed, or attached to the wrong org/backend, show a guided **Restart this machine’s runner** action.
- Only in an **Advanced** flow should the app allow **Start another runner** with a separate display name/capability set. citeturn41view0turn10view3turn17search0turn18view0

That leads to a better button model. On first use the primary action should be **Connect this machine**. Once configured, that action should disappear and be replaced with a status card such as **Local runner: Connected** with secondary controls: **Restart**, **Pause**, **Open logs**, **Show details**, and perhaps **Disable local execution**. This is closer to GitHub’s runners page, GitLab’s runners settings, Buildkite’s agent controls, and Windsurf’s status-based Command Center than to a generic process launcher. citeturn41view0turn10view3turn18view0turn29search0

For multi-runner scenarios, Stigmer should explicitly **optimise for the common case and demote the expert case**. GitHub and GitLab both support multiple runners on one machine, but they do so explicitly, through separate directories, registrations, or config entries; Buildkite supports multiple workers via `--spawn`. None of them treats “many runners on one laptop” as the novice default. Stigmer should do the same: assume **single-runner-per-developer-machine** by default, and expose multi-runner only under advanced controls for CI-style loads, specialist hardware, or isolation requirements. citeturn8search15turn10view0turn10view5turn14view3

A concrete target state for the desktop app would look like this in operation. On launch, the app reads a small local health endpoint or socket exposed by the runner service, checks the last successful cloud heartbeat, and shows one of four states: **Connected**, **Reconnecting**, **Paused**, or **Needs attention**. If the service is absent, it auto-starts it. If the service is present and healthy, it simply adopts it. If the service is stale, it restarts it. The user only sees an interruptive dialog when credentials are missing, the service installation is blocked by platform permissions, or the machine has never been connected. This is the operational stance implied by the mature service-based and cloud-based examples above. citeturn3view1turn10view2turn16view2turn16view4turn22view0turn33view1

## Platform implementation guidance

On **macOS**, the strongest fit for a developer-machine runner is a **per-user background helper**, not an ad hoc child process of the desktop UI. Apple’s Service Management framework exists specifically to install and control **LoginItems, LaunchAgents, and LaunchDaemons** from within an app. Apple’s documentation says LoginItems start at login and continue until logout or manual quit; LaunchAgents run on behalf of the currently logged-in user; LaunchDaemons run as root and may start before any user logs in. For a Stigmer runner that may need user-session resources, local toolchains, browser automation, or editor integration, a **LoginItem or LaunchAgent** is the safer default than a root daemon. citeturn36search1turn36search2turn35search8turn35search12

That means the macOS production recommendation is: package the runner helper with the desktop app, register it with **`SMAppService`** on macOS 13+, and use a **user-context helper** unless you have a hard requirement to run before login or as root. If you need the runner to survive app quits but still live in the user session, this is exactly what LoginItems and LaunchAgents are for. GitHub’s own runner service model on macOS also uses `launchd`, and Buildkite’s macOS guidance recommends starting agents on login with LaunchAgents. citeturn36search2turn36search13turn41view0turn16view4

On **Linux**, the default should be a **systemd user service**, not a background child process or hand-rolled daemon manager. GitLab Runner natively supports user services and system services; GitHub’s runner service uses `systemd`; Buildkite packages enable and start a `buildkite-agent` service through `systemctl`. Systemd is the right place to get restart supervision, log capture, status inspection, and boot-time enablement. citeturn10view1turn10view2turn3view1turn16view2

If Stigmer wants the runner to stay alive even after the user logs out, the Linux-specific production pattern is to use a **systemd user service plus linger**, because `loginctl enable-linger` keeps the user manager spawned at boot and around after logout. That is the cleanest way to get “developer-owned but not session-tied” behaviour on Linux. If logout persistence is not required, a normal user service is enough. citeturn39search0turn39search1turn35search5turn35search1

A useful Linux nuance is that **`After=network.target` does not mean “the network is fully up”**. Freedesktop’s documentation is explicit that `network.target` is primarily about shutdown ordering and does not guarantee configured interfaces at startup. For a runner that can reconnect on its own, the right design is usually **start early, then retry with backoff**, rather than blocking boot on “network online” unless you have a hard dependency that truly requires it. citeturn37search1turn37search14turn37search0

Across both platforms, the runner service should expose at least three local control-plane affordances: a simple **health/readiness endpoint**, a **structured log location**, and an **idempotent start/restart contract**. GitHub, GitLab, and Buildkite all document service status and log inspection as normal operator flows. Stigmer should make those flows accessible inside the desktop app, but the app should consume them too, so it can decide whether to adopt, restart, or ask the user for help. citeturn41view0turn10view1turn13view5

## Open questions and limitations

The public documentation for **Cursor My Machines** and Windsurf’s **local-agent internals** is much thinner than the documentation for GitHub, GitLab, Buildkite, or Devin. The high-level UX direction is clear — both products increasingly present a unified task/agent surface instead of raw runner management — but their exact local process supervision details are not documented publicly to the same depth. For that reason, the most robust implementation references for Stigmer’s local-runner architecture are still **GitLab Runner, Buildkite Agent, GitHub self-hosted runners, and the platform-native service managers on macOS and Linux**. citeturn23search0turn29search0turn29search1turn3view1turn10view1turn16view2turn16view4turn36search2turn39search0

Extended Thinking:

# Runner Management UX & Architecture Research Report

Fair criticism. The right answer here is not “copy GitHub Actions.” GitHub Actions, GitLab Runner, and Buildkite are mostly **operator/admin tools**. Stigmer Desktop is a **developer product**, so the UX contract should be closer to Cursor, Windsurf, Devin, Gitpod Desktop, or Coord: the user starts work, not infrastructure.

My strongest recommendation:

> **Stigmer Desktop should treat a local runner as a reconciled desired state, not as a user-created process. “Start Runner” should become an idempotent “ensure local execution is available” operation. If a runner is already running, that is success, not an error.**

The current bug is not really that the process is still alive. That is actually good. The bug is that the desktop UX exposes a CI-runner mental model to someone who simply wants to use Stigmer.

---

## 1. Executive conclusion

The best-in-class pattern across modern developer-agent products is:

1. **Hide runner lifecycle in the happy path.**
   The user should see “Local execution: Ready / Working / Reconnecting / Needs attention,” not “Start Runner” plus a modal full of name, endpoint, and token fields.

2. **Make runner startup idempotent.**
   Clicking the control when a runner is already running should return success: “This computer is already connected.” The CLI should also exit `0` for the already-running same-runner case.

3. **Separate machine identity from display name.**
   Hostname-derived slugs are useful display defaults, but they should not be the primary uniqueness key. Use a stable generated `machine_id` plus `org_id` plus `endpoint`.

4. **Desktop should auto-adopt existing local runners.**
   If a CLI-started runner is already active, Desktop should detect it, classify it, and show it as available. The user should not have to know whether it was launched by Desktop, CLI, launchd, or systemd.

5. **Default to one runner per machine per org.**
   Multiple runners on one machine are valid for CI/fleet scenarios, but for a developer desktop, the default should be “this computer is available for Stigmer runs.” Additional runners should be an advanced/admin path.

6. **Move name, endpoint, and token out of the happy path.**
   Those belong in Settings or Advanced. A blank optional form is misleading because it creates the impression that the user is creating something new, when the desired operation is usually “use my existing local capability.”

7. **Auto-start on app launch; optionally run at login.**
   Desktop should silently ensure the runner after authentication. A separate “Run at login” toggle can install a login item / LaunchAgent / systemd user service.

8. **Treat duplicate-active as a state transition, not an exception.**
   “Runner already running” should become:

   * Desktop: adopt and show Ready/Busy.
   * CLI interactive: print “Runner already active; using existing runner PID 9730” and exit `0`.
   * CLI advanced: use explicit `--new`, `--replace`, or `--name` for power users.

---

## 2. The key product mismatch

Your current desktop flow says:

> “Start Runner”

But the user intent is:

> “Use this computer for Stigmer.”

That distinction matters. “Start Runner” implies there is no existing runner and that a new resource is being created. So when the system replies “runner already running,” it sounds like a failure. In reality, it means the user’s intent is already satisfied.

This is the same distinction Gitpod Desktop appears to make: it automatically starts a macOS runner for the current organization, has only one active runner at a time in that local-desktop mode, stops/starts it when switching organizations, and starts it again when Gitpod Desktop restarts. The user does not manually fill out a runner-name form in the normal path. ([Gitpod][1])

Coord, which is especially close to Stigmer’s model, also treats the runner as part of the desktop product: a lightweight desktop app connects to a workspace, watches for jobs, runs local agent sessions, streams progress, and gives the user Status, Logs, and History. Coord explicitly says each machine runs one runner instance, while the workspace can have many runners across machines. ([Coord Docs][2])

That is the product category Stigmer Desktop should copy more than GitHub Actions.

---

# 3. Comparative platform findings

## 3.1 GitHub Actions self-hosted runners

GitHub Actions is a good reference for **admin-runner lifecycle**, not necessarily for Desktop UX.

### Registration and service model

GitHub’s self-hosted runner flow is explicit: add/configure a runner, then optionally configure it as a service. On Linux, GitHub documents `svc.sh install`, `start`, `status`, `stop`, and `uninstall` after the runner has already been added. It also documents service customization through systemd. On macOS, GitHub points to launchd plist customization. ([GitHub Docs][3])

GitHub’s UI shows runners in repository/organization/enterprise settings with statuses such as **Idle**, **Active**, and **Offline**. “Offline” means the runner application is not connected, the machine is off, or it cannot communicate with GitHub. ([GitHub Docs][4])

### Auto-start and persistence

GitHub explicitly supports running the runner app as a service so it can start automatically when the machine starts. That is the right pattern for headless or infrastructure-like deployments. ([GitHub Docs][3])

GitHub also auto-updates self-hosted runners by default when a new version is available, and documents disabling that with `--disableupdate`, with the caveat that runners must be updated within a set window or they stop receiving jobs. ([GitHub Docs][5])

### Ephemeral and just-in-time runners

GitHub’s more modern pattern for autoscaling is **ephemeral** runners: one runner gets one job and then is automatically de-registered. GitHub recommends ephemeral runners for autoscaling because persistent self-hosted runners can receive jobs while being shut down or recycled. ([GitHub Docs][5])

GitHub also has just-in-time runner registration APIs for single-use runners without time-limited registration tokens, again aimed at autoscaling infrastructure. ([The GitHub Blog][6])

### Duplicate/active runner implication

GitHub’s model is fundamentally admin-facing: duplicate registration is handled through runner names, replacement flags, or separate runner instances. Active-session duplication is treated as a runner/session conflict, not a friendly “already ready” desktop state. Community reports show that trying to run a second session for the same configured runner can result in an active-session conflict and retry loop. ([Stack Overflow][7])

### Lesson for Stigmer

Copy GitHub’s **status vocabulary**, **service install path**, and **auto-update discipline**. Do not copy its user-facing “configure a runner” posture for the desktop happy path.

For Stigmer Desktop, the equivalent of GitHub’s “runner is Idle” should be a success state: **Local execution ready**.

---

## 3.2 GitLab Runner

GitLab Runner has a cleaner conceptual separation than many platforms:

* `gitlab-runner register` adds runner configuration.
* `gitlab-runner run` runs the multi-runner service.
* Service commands install/start/stop/restart/status the service.
* Multiple runner configurations can live in one `config.toml`.

GitLab’s docs say registration stores configuration in `config.toml`, and registering multiple runners on the same host means repeating `gitlab-runner register`. The command list includes `register`, `run`, `install`, `start`, `stop`, `restart`, `status`, `unregister`, and `verify`. ([GitLab Documentation][8])

A particularly important identity lesson: in GitLab Runner’s advanced config, the runner `name` is described as informational. The real runner identity is the runner configuration/token, not the display name. GitLab also supports multiple `[[runners]]` sections in a single config. ([GitLab Documentation][9])

On macOS, GitLab Runner is documented as a user-mode LaunchAgent, not a system LaunchDaemon. It runs when the user logs in and stops when the user logs out. GitLab explicitly notes this mode gives access to user-mode resources such as UI elements and keychain material, which is highly relevant for developer tools that need Git credentials, SSH keys, editors, and local dev environments. ([GitLab Documentation][10])

GitLab also documents service support across Windows, Linux systemd/Upstart/System V, and macOS launchd. ([GitLab Documentation][11])

### Lesson for Stigmer

GitLab’s most important lesson is: **registration and running are different operations**.

Stigmer’s Desktop happy path should not “register a runner” every time. It should call an idempotent operation like:

```bash
stigmer runner ensure --managed-by desktop --json
```

That operation may register, resume, reconnect, adopt, or start. The user does not care which one happened.

Also, Stigmer should copy GitLab’s identity separation: human-readable runner name should be informational and mutable; uniqueness should come from an internal ID.

---

## 3.3 Buildkite Agent

Buildkite is a strong reference for **agent session design**, naming templates, heartbeat/lost detection, and dashboard status.

Buildkite’s agent is a small cross-platform build runner that polls Buildkite, runs jobs, reports status/output, and uploads artifacts. ([Buildkite][12])

### Naming

Buildkite’s config supports optional agent names and name templates such as hostname, spawn number, random value, and process ID. This is important because it avoids pretending that a user-chosen name is necessarily the true identity. Buildkite also supports tags and queue routing. ([Buildkite][13])

### Lifecycle

`buildkite-agent start` manually starts and registers an agent connection. Buildkite supports lifecycle options such as disconnecting after a job, disconnecting after idle timeout, disconnecting after uptime, and acquiring a specific job. ([Buildkite][14])

Buildkite’s agent lifecycle docs describe graceful disconnect behavior: the agent can finish a current job before disconnecting. The docs also describe heartbeat/lost behavior: after registration, the agent sends regular heartbeats; if Buildkite misses heartbeats for consecutive minutes, the agent is marked lost and no more jobs are assigned. ([Buildkite][12])

Buildkite’s GraphQL Agent object exposes fields such as connection state, heartbeat time, hostname, IP address, whether the agent is running a job, whether it is paused, and lost/disconnected timestamps. ([Buildkite][15])

Buildkite also has queue metrics and an Agents panel that shows connected agents and busy-vs-idle utilization. ([Buildkite][16])

### Lesson for Stigmer

Buildkite’s best ideas for Stigmer are:

* Use **templateable display names**, not display names as identity.
* Treat the runner connection as a **session** with heartbeat, lost time, connected time, and current job state.
* Support graceful drain/stop.
* Expose enough status for debugging, but do not make the normal user configure all of it.

Stigmer should model:

```text
machine identity   stable across restarts
runner record      stable per org/machine/endpoint
runner session     ephemeral per process start
task assignment    bound to current live session
display name       user-facing, mutable, non-unique
```

---

## 3.4 Cursor Background Agents

Cursor is the opposite end of the spectrum from GitHub Actions. It does not ask users to manage local runners for Background Agents. Cursor’s Background Agents are remote agents that edit and run code in isolated Ubuntu-based machines. Users start agents from the UI, view status, send follow-ups, and take over the machine. ([Cursor Documentation][17])

Cursor exposes agent management through an Agents sidebar: users can view all agents associated with their account, inspect background agents, and jump into remote machines. Cursor’s changelog also describes a unified sidebar for foreground and background agents. ([Cursor][18])

Cursor’s setup surface is repository/environment-centric: environment files, install commands, startup commands, secrets, terminals, and repo cloning. The user configures the environment, not a runner process. ([Cursor Documentation][19])

### Lesson for Stigmer

Cursor’s key lesson is task-centric UX:

> The primary object is the agent/session/task, not the runner.

For Stigmer Desktop, runner status should be supporting infrastructure. The main UX should answer:

* Can Stigmer use this computer?
* What is currently running?
* Does it need my attention?
* Can I view logs or take over?

Not:

* What runner name do I want?
* What endpoint should this runner use?
* What token should I paste?
* Why is a PID from five hours ago blocking me?

---

## 3.5 Windsurf / Codeium

Windsurf’s Cascade is presented as an editor-integrated agent, not a user-managed local runner. Users interact through Cascade, which can use terminal/tool execution, detect packages, maintain plans/todos, continue work with context, and support auto-execution modes. ([Windsurf Docs][20])

Windsurf 2.0’s Agent Command Center is particularly relevant: it manages local and cloud agents from a Kanban-style view, with statuses like in-flight, blocked, and ready for review. It unifies local Cascade sessions and cloud/Devin sessions as work items. ([Windsurf Docs][21])

### Lesson for Stigmer

The UX object should be **work state**, not process state.

A good Stigmer equivalent would be:

```text
Local execution
Ready on Swarup’s MacBook Pro

Current work
- Agent run #123: Running tests
- Agent run #124: Waiting for approval

Controls
Pause new work | View logs | Restart local runner | Advanced
```

The runner remains observable, but it is not the center of the product.

---

## 3.6 Devin

Devin largely avoids local runner UX by using cloud dev environments. Devin’s environment is a Linux-based VM workspace, configured once and saved as a snapshot. Each session boots from a fresh copy of the snapshot. ([Cognition AI][22])

Devin’s declarative environment model uses concepts similar to Blueprint, Build, and Snapshot; builds run when saving the blueprint and periodically. Every session starts from the snapshot rather than from a user-managed local daemon. ([Cognition AI][23])

Repository setup is framed as project/environment setup, not runner setup. Devin can boot a session from the latest snapshot, run `git pull`, run maintenance commands, and then execute work. ([Devin Docs][24])

### Lesson for Stigmer

Devin’s lesson is not “use cloud VMs.” Stigmer explicitly wants local execution. The lesson is:

> The agent product should make execution substrate feel like part of the environment, not a thing the user starts repeatedly.

For Stigmer, the analog is: configure local environment once, then keep it available.

---

## 3.7 Coord

Coord is one of the closest public analogs to Stigmer’s local-runner model.

Coord’s desktop runner executes agentic jobs on the user’s local machine. It connects to a workspace, watches for ready jobs, picks them up, clones code, starts AI agents like Claude Code/Codex/Gemini CLI, streams progress, and packages results. Its interface includes Status, Logs, and History. ([Coord Docs][2])

Coord also says credentials stay on the user’s machine, each runner becomes part of the team execution network, users can run runners on as many machines as they want, and each machine runs one runner instance. ([Coord][25])

Coord supports automatic retries for crashes, timeouts, and network hiccups, and a graceful stop that lets the agent finish the current operation. ([Coord Docs][2])

### Lesson for Stigmer

Coord validates the likely best product stance for Stigmer:

* Runner is a desktop subsystem.
* One runner per machine by default.
* Status/logs/history are exposed.
* Start/stop exist, but not as “fill out a name/token/endpoint form.”
* Recovery is automatic.

---

## 3.8 Gitpod Desktop

Gitpod Desktop is another highly relevant example because it has a local runner but makes it app-managed.

Gitpod Desktop’s docs describe local environments as being backed by a runner that Gitpod Desktop automatically starts for the current organization. The older docs explicitly say only one runner is active at a time, stopping the runner stops local environments, quitting Gitpod Desktop stops the runner, switching orgs stops the current org runner and starts the new org runner, and restarting Gitpod Desktop starts the runner again. ([Gitpod][1])

### Lesson for Stigmer

This is probably the closest UX pattern for your immediate desktop problem:

> Desktop owns the local runner for the current org. It auto-starts, auto-stops, and auto-switches. The user does not create duplicate runners by clicking “Start.”

---

# 4. Comparison table

| Platform                          | Primary UX contract                        | Registration / identity                                          | Naming                                                      | Duplicate / already-active behavior                                                     | Persistence / auto-start                                                                     | Status display                                                             | Stop / recovery pattern                                                  | Lesson for Stigmer                                                                      |
| --------------------------------- | ------------------------------------------ | ---------------------------------------------------------------- | ----------------------------------------------------------- | --------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------- | ------------------------------------------------------------------------ | --------------------------------------------------------------------------------------- |
| GitHub Actions self-hosted runner | Admin configures CI capacity               | Runner is added/configured, then optionally installed as service | Runner name often machine-derived; labels/groups route jobs | Same configured runner can hit active-session conflicts; replacement is an admin action | `svc.sh` service install/start/status/stop; launchd/systemd patterns; auto-update by default | Idle / Active / Offline in GitHub settings                                 | Service stop/uninstall; ephemeral runners auto-de-register after one job | Use status/service ideas, not the desktop UX ([GitHub Docs][3])                         |
| GitLab Runner                     | Admin registers one or more runner configs | `register` writes config; `run` runs configured runners          | `name` is informational                                     | Multiple runner configs can coexist in `config.toml`                                    | Service commands; macOS LaunchAgent user mode                                                | CLI/service status; GitLab runner lists                                    | `verify --delete`, service management, graceful shutdown patterns        | Separate registration from running; make name non-identity ([GitLab Documentation][26]) |
| Buildkite Agent                   | Admin/operator starts agent sessions       | Agent starts with token, registers connection/session            | Supports templates: hostname, spawn, random, PID            | Multiple agents are normal when intentionally named/spawned                             | Long-running agent, disconnect-after-job/idle/uptime options                                 | Agent fields include heartbeat, connection state, running job, paused/lost | Graceful disconnect, heartbeat lost detection                            | Model runner sessions separately from stable machine identity ([Buildkite][13])         |
| Cursor Background Agents          | User starts agent work, not runners        | Remote VM environment per background agent                       | Agent/task identity, not local runner name                  | No local duplicate-runner problem exposed                                               | Cloud-managed remote environment                                                             | Sidebar for all agents; status and takeover                                | User can inspect/follow up/take over                                     | Make the task/session primary, runner secondary ([Cursor Documentation][17])            |
| Windsurf                          | Editor-integrated local/cloud agent work   | Local Cascade sessions and cloud sessions                        | Session/work item identity                                  | Runner process not exposed as a normal user concern                                     | App/editor manages sessions                                                                  | Agent Command Center Kanban for local/cloud agents                         | Status columns: in-flight, blocked, ready for review                     | Show work state, not process startup ([Windsurf Docs][21])                              |
| Devin                             | Cloud devbox abstraction                   | Environment snapshot configured once                             | Session/environment identity                                | No local runner exposed                                                                 | Cloud-managed VM/snapshot lifecycle                                                          | Session UI, repo/environment setup                                         | Fresh session boots from snapshot                                        | Hide execution substrate behind environment readiness ([Cognition AI][22])              |
| Coord                             | Local desktop runner executes agent jobs   | Desktop runner connects to workspace                             | One runner per machine instance                             | Each machine runs one runner; multiple machines allowed                                 | Desktop app runner; automatic retries                                                        | Status / Logs / History                                                    | Graceful stop; retry crashes/network hiccups                             | Closest pattern: desktop subsystem, not a modal form ([Coord Docs][2])                  |
| Gitpod Desktop                    | App-managed local environments             | Desktop automatically starts current-org runner                  | Organization-scoped local runner                            | Only one active runner in local desktop mode                                            | Starts on desktop launch/current org; switches org runner                                    | Local environments UI                                                      | Quitting/stopping runner stops envs                                      | Strong precedent for auto-start/adopt by current org ([Gitpod][1])                      |

---

# 5. Best-in-class UX patterns to adopt

## Pattern 1: Replace “Start Runner” with “Local execution availability”

The primary control should be something like:

```text
Local execution
Ready on Swarup’s MacBook Pro

[Pause new work] [View logs] [Restart] [Advanced]
```

Not:

```text
Start Runner
Name: _______
Endpoint: _______
Token: _______
```

The modal should disappear from the happy path. Endpoint and token should be derived from logged-in Desktop auth. Name should default automatically and be editable later.

Recommended UI states:

| User-facing state | Meaning                                                |
| ----------------- | ------------------------------------------------------ |
| Ready             | Runner connected and idle                              |
| Working           | Runner connected and running a task                    |
| Reconnecting      | Runner process exists but cloud stream is reconnecting |
| Needs attention   | Auth, permission, version, or environment issue        |
| Paused            | Runner exists but will not accept new work             |
| Disabled          | User has turned off local execution                    |
| Updating          | Desktop/runner is updating                             |
| Offline           | No local runner is reachable                           |

The key is that **already running maps to Ready or Working**, never to Error.

---

## Pattern 2: Make “start” an idempotent ensure operation

Implement a new semantic operation:

```bash
stigmer runner ensure --managed-by desktop --json
```

Expected results:

```json
{
  "ok": true,
  "status": "ready",
  "runner_id": "runr_...",
  "machine_id": "mach_...",
  "session_id": "sess_...",
  "adopted_existing": true,
  "pid": 9730,
  "started_at": "2026-05-09T06:12:00Z"
}
```

The operation should be allowed to do any of these:

* Detect existing Desktop-managed runner and return success.
* Detect existing CLI-managed runner and adopt/display it.
* Detect stale state and clean it up.
* Reconnect a disconnected runner.
* Register/resume a server-side runner record.
* Start a new process only when no compatible runner exists.

The user-facing verb should be **Connect**, **Enable local execution**, or simply an automatic background action. Internally, the verb is **ensure**.

---

## Pattern 3: Treat “already running” as success

Current behavior:

```text
runner "swarups-macbook-pro-local" is already running
```

Recommended Desktop behavior:

```text
This computer is already connected.
Using runner started 5 hours ago.
```

Recommended CLI behavior:

```text
Runner already active: swarups-macbook-pro-local
PID: 9730
Status: ready
Use `stigmer runner status` to inspect, or `stigmer runner restart` to restart.
```

Exit code should be `0` for the same org/endpoint/machine case.

Only true conflicts should be errors, for example:

* Existing runner belongs to a different org and cannot be adopted.
* Existing runner uses a different endpoint.
* Existing process is unresponsive and cannot be stopped.
* Credentials are invalid.
* Local state is corrupt and automatic repair failed.

Even then, the CTA should be “Reconnect,” “Restart runner,” or “Run diagnostics,” not “try another `--name`.”

---

## Pattern 4: Separate stable identity from display name

Your current key is effectively:

```text
runner name = sanitized hostname = uniqueness key = local state filename
```

That is fragile.

Problems:

* Hostnames can change.
* Hostnames collide.
* `.local` suffixes leak into names.
* Two orgs on the same machine need distinct runner records.
* A user changing display name should not create a new runner identity.
* “Use `--name`” tells the user to create a duplicate when they actually want to reuse the current machine.

Recommended identity model:

```text
machine_id:
  Stable generated UUID for this local Stigmer installation.
  Stored in ~/.stigmer/machine.json or OS keychain.
  Not user-facing.

runner_id:
  Stable server-side runner record.
  Scoped to org_id + endpoint + machine_id.
  Reused across process restarts.

session_id:
  Ephemeral process connection/session.
  New on every runner process start.

display_name:
  Human-readable default from hostname.
  Mutable.
  Not unique.
  Not used as the lock key.

task_queue:
  Stable per runner_id, e.g. runner:{runner_id}.
```

Recommended local path:

```text
~/.stigmer/
  machine.json
  runners/
    <endpoint_hash>/
      <org_id>/
        <machine_id>.json
        <machine_id>.lock
        <machine_id>.sock
```

The server should enforce uniqueness on:

```text
(org_id, endpoint, machine_id)
```

not on display name.

---

## Pattern 5: Introduce a local runner control plane

Relying only on a PID file is not enough. `signal 0` answers “does a process with this PID exist?” It does not prove:

* It is the Stigmer runner.
* It is the same runner instance.
* It belongs to the same org/endpoint.
* It is healthy.
* It is accepting or rejecting work.
* The PID has not been reused.

Add a local control socket:

```text
~/.stigmer/runners/<endpoint>/<org>/<machine_id>.sock
```

Supported RPCs:

```text
status
stop
restart
pause
resume
logs
health
adopt
```

The Desktop app should use this socket to adopt CLI-started runners. The CLI should use it to avoid starting duplicates.

State file should include:

```json
{
  "runner_id": "runr_...",
  "machine_id": "mach_...",
  "session_id": "sess_...",
  "org_id": "org_...",
  "endpoint": "api.stigmer.ai:443",
  "pid": 9730,
  "process_start_time": "...",
  "executable_path": "...",
  "executable_hash": "...",
  "control_socket": "...",
  "started_at": "...",
  "managed_by": "cli|desktop|service",
  "version": "...",
  "capabilities": [...]
}
```

Duplicate detection should check, in order:

1. File lock.
2. State file.
3. PID exists.
4. PID start time matches.
5. Executable/cmdline matches.
6. Control socket responds.
7. Server says current session is alive.

Only then classify it as active. If socket fails but PID exists, classify as **Unhealthy** and offer Restart/Repair.

---

# 6. Recommended Stigmer architecture

## 6.1 New command model

Keep `stigmer up runner` for compatibility, but make the underlying model more explicit.

Recommended commands:

```bash
stigmer runner ensure --json
stigmer runner status --json
stigmer runner stop
stigmer runner restart
stigmer runner pause
stigmer runner resume
stigmer runner logs
stigmer runner doctor
stigmer runner service install --user
stigmer runner service uninstall
```

Compatibility:

```bash
stigmer up runner
```

should become equivalent to:

```bash
stigmer runner ensure
```

unless the user explicitly requests foreground mode or a new named runner.

Power-user flags:

```bash
--new                 # intentionally create another runner
--replace             # gracefully stop/preempt existing runner
--name <display-name> # set display name, not identity
--foreground          # run in foreground
--service             # run as service/supervisor mode
--org <org-id>
--endpoint <endpoint>
```

Important default:

```text
same org + same endpoint + same machine_id + already active = success
```

---

## 6.2 Desktop app lifecycle

On Desktop launch:

1. Authenticate user.
2. Resolve active org/workspace.
3. Call `runner ensure --managed-by desktop --json`.
4. If existing local runner is found, adopt it.
5. If no runner exists, start one silently.
6. Display compact status.

Desktop should not show a runner form unless:

* The user is unauthenticated.
* There is no token/config available.
* Endpoint is non-default and not configured.
* The user opens Advanced settings.
* The org requires a special runner policy.

Recommended Desktop card:

```text
Local execution

Ready
This computer can run Stigmer agents.

Machine: Swarup’s MacBook Pro
Current task: None
Last connected: just now

[Pause new work] [View logs] [Restart] [Advanced]
```

If already running from CLI:

```text
Local execution

Ready
Connected to an existing local runner started from Terminal 5 hours ago.

[Manage here] [View logs] [Restart] [Advanced]
```

If busy:

```text
Local execution

Working
Running Agent Execution #8421

[View task] [View logs] [Pause new work after current task]
```

If reconnecting:

```text
Local execution

Reconnecting
Your runner is still running locally. We are reconnecting to Stigmer Cloud.

[View logs] [Restart]
```

---

## 6.3 Server-side runner/session model

Current:

```text
runner.apply()
runner_id
task_queue runner:{runner_id}
connect stream
heartbeats every 30s
phases PENDING -> STARTING -> READY -> BUSY -> STOPPED
```

Recommended extension:

```text
Runner:
  runner_id
  org_id
  endpoint
  machine_id
  display_name
  created_at
  last_seen_at
  desired_state: enabled|paused|disabled
  observed_state: offline|starting|ready|busy|reconnecting|stopping|stopped|error
  capabilities
  version
  managed_by_last: desktop|cli|service
  current_session_id

RunnerSession:
  session_id
  runner_id
  process_id_optional
  process_start_time_optional
  started_at
  connected_at
  last_heartbeat_at
  disconnected_at
  status
  version
  management_mode
```

`runner.apply()` should behave like Kubernetes-style apply/reconcile:

```text
Given org_id + endpoint + machine_id:
  if runner exists:
    update mutable fields and return existing runner_id
  else:
    create runner and return new runner_id
```

The connect stream should include:

```text
runner_id
session_id
machine_id
version
capabilities
resume_token / nonce
```

If a second session connects for the same `runner_id`, the server should not return a generic failure. It should return structured information:

```json
{
  "code": "ALREADY_CONNECTED",
  "runner_id": "runr_...",
  "active_session_id": "sess_...",
  "last_heartbeat_at": "...",
  "policy": "reject_without_replace"
}
```

The client then maps this to:

* Desktop: adopt existing if local; otherwise show “Runner active elsewhere.”
* CLI: exit `0` if it is the same local process; otherwise print clear conflict.
* Explicit `--replace`: request graceful preemption.

---

## 6.4 Task queue implications

Your current Temporal queue pattern, `runner:{runner_id}`, is directionally right because it gives a stable routing address. Temporal workers poll task queues, and Temporal task queues can have multiple workers; Temporal worker poller metadata includes worker identity and last access time. ([Temporal API Docs][27])

For Stigmer, keep:

```text
task_queue = runner:{runner_id}
```

But avoid binding logical runner identity to process lifetime. The process session should be replaceable.

Recommended policy:

```text
runner_id stable
session_id ephemeral
task_queue stable
worker identity includes session_id + version
```

This lets you show:

```text
Runner: Swarup’s MacBook Pro
Current session: started 5 hours ago
Current task queue: runner:runr_123
```

without forcing the user to understand any of it.

---

# 7. Handling the exact current failure

Current scenario:

1. Hostname: `Swarups-MacBook-Pro.local`
2. Slug: `swarups-macbook-pro-local`
3. Existing runner PID 9730 is still alive.
4. User clicks Start Runner with empty fields.
5. CLI errors: runner already running.
6. User is confused.

Recommended behavior:

### Desktop-side immediate fix

Before changing architecture, map this known error to success.

If sidecar returns:

```text
runner "<name>" is already running
PID: 9730
```

Desktop should:

1. Load `~/.stigmer/runners/<name>.json`.
2. Verify org/endpoint match.
3. Check whether process is alive.
4. Show status as Ready/Busy.
5. Emit `runner:started` or `runner:adopted`, not `runner:error`.

User sees:

```text
Connected to existing runner
Swarup’s MacBook Pro is already available for Stigmer runs.
```

No red error.

### CLI-side immediate fix

Change error to success for the same runner:

```text
Runner already active: swarups-macbook-pro-local
PID: 9730
Backend: api.stigmer.ai:443
Started: 5 hours ago
```

Exit code: `0`.

Only suggest `--name` under an “advanced” paragraph:

```text
To intentionally run an additional runner on this machine, use:
  stigmer up runner --new --name <name>
```

Do not lead with `--name`.

---

# 8. Recommended decision on your four options

You listed:

* Option A: Auto-adopt the existing runner.
* Option B: Show “Runner is already active” as success.
* Option C: Transparent reconnect / stop old and start new.
* Option D: Remove the “Start Runner” button when one is already running.

Recommended answer:

```text
Primary: A + B + D
Avoid as default: C
```

### Option A: Auto-adopt existing runner

Yes. This should be the core behavior.

Adoption should work across:

* Desktop-started runner.
* CLI-started runner.
* Service-started runner.
* Login-item-started runner.

### Option B: Already active as success

Yes. This is the minimum fix.

Already running means the user’s intent is satisfied.

### Option C: Stop old, start new

Only for explicit **Restart** or **Replace**.

Transparent replacement can kill in-flight work, lose logs, interrupt local tool state, or produce surprising behavior after laptop sleep/wake. Buildkite’s lifecycle distinction between graceful disconnect and forced disconnect is a useful reminder: stopping a runner is operationally meaningful and should not be hidden when work may be active. ([Buildkite][12])

### Option D: Remove Start button when active

Yes, but it is not enough alone.

The deeper fix is that the button should not represent “create process.” It should represent desired availability.

---

# 9. Background process management recommendations

## 9.1 macOS

For developer machines, prefer:

```text
Desktop app / LaunchAgent / login item
```

not a root LaunchDaemon.

Reason: developer runners often need user-session resources: SSH agent, Git credentials, keychain, editor integrations, filesystem permissions, and sometimes UI-adjacent tools. GitLab Runner’s macOS docs make this same point by supporting only user-mode LaunchAgent on macOS and noting access to user resources like keychain and UI elements. ([GitLab Documentation][10])

Apple’s launchd model uses property lists with keys such as `Label`, `ProgramArguments`, and `KeepAlive`; Apple also recommends designing daemons to run on demand where possible. ([Apple Developer][28])

For Tauri, the autostart plugin supports launching the app at system startup across macOS, Linux, and Windows, and supports macOS LaunchAgent mode. ([Tauri][29])

Recommended macOS modes:

| Mode               | Use case                                | Mechanism                                                       |
| ------------------ | --------------------------------------- | --------------------------------------------------------------- |
| Desktop default    | User opens Stigmer                      | Tauri app spawns/supervises runner                              |
| Run at login       | User wants always-ready local execution | LaunchAgent/login item starts Stigmer or lightweight supervisor |
| Headless machine   | Shared build/dev machine                | Explicit service install with service account                   |
| Enterprise managed | Admin-controlled fleet                  | MDM-installed LaunchAgent/Daemon depending on credential model  |

Do not make LaunchDaemon the default for ordinary developers.

---

## 9.2 Linux

For Linux developer desktops:

```text
systemd --user service
```

For headless servers:

```text
systemd system service
```

systemd’s `Restart=` policy can restart services on failure, and `Restart=on-failure` is the recommended pattern for long-running services. `RestartSec` controls delay before restart, and watchdog settings can mark a service failed if it stops pinging. ([freedesktop.org][30])

Recommended user service:

```ini
[Unit]
Description=Stigmer Runner
After=network-online.target

[Service]
ExecStart=/usr/local/bin/stigmer runner supervise --managed-by service
Restart=on-failure
RestartSec=5s
KillSignal=SIGTERM
TimeoutStopSec=300
WatchdogSec=90

[Install]
WantedBy=default.target
```

Recommended behavior on SIGTERM:

1. Stop accepting new tasks.
2. If idle, exit immediately.
3. If busy, mark draining and finish current safe checkpoint.
4. After grace period, terminate child process if necessary.
5. Report final status.

---

## 9.3 Desktop app versus service

Recommended default:

```text
Desktop app supervises the runner while the user is logged in.
```

Recommended optional setting:

```text
[ ] Run Stigmer at login
```

Recommended advanced setting:

```text
Install as background service...
```

Avoid forcing users into service setup during onboarding. That is CI-runner UX, not local-agent UX.

---

# 10. Sleep, wake, and network reconnection

Laptops sleep. Networks change. VPNs disconnect. The runner UX should assume this.

Recommended state handling:

```text
Heartbeat every 30s
Missed 1-2 heartbeats: reconnecting / degraded
Missed longer grace window: offline, no new tasks
Reconnect with same runner_id and new or resumed session_id
Do not create a new runner record
Do not show an error unless user action is required
```

Buildkite’s heartbeat/lost model is a useful precedent: agents send regular heartbeats, and the platform marks agents lost after missed heartbeat windows so no more jobs are assigned. ([Buildkite][31])

For Stigmer:

* On sleep: local runner may pause; cloud should show **Reconnecting** or **Offline**, not **Stopped**.
* On wake: runner reconnects using the same `runner_id`.
* If a task was running: use task heartbeat/checkpoint semantics and mark the execution **Recovering**, **Retrying**, or **Needs attention** depending on tool state.
* Do not ask the user to start a new runner after wake.

Suggested statuses:

```text
READY          connected and idle
BUSY           connected and working
RECONNECTING   process exists, stream reconnecting
OFFLINE        no recent heartbeat, not eligible for new work
DRAINING       finishing current task, no new tasks
PAUSED         user/admin paused new work
UNHEALTHY      local process exists but health check failed
STOPPED        intentionally stopped
```

Avoid using `STOPPED` for sleep/network loss unless the process actually exited intentionally.

---

# 11. Multi-runner versus single-runner

## CI platforms

GitHub, GitLab, and Buildkite all support multiple runners/agents per machine or per fleet because their primary use case includes CI capacity management, parallelism, queues, labels, executors, and autoscaling. GitLab explicitly supports multiple registered runner configs in one `config.toml`, and Buildkite supports spawn/name templates and multiple agent sessions. ([GitLab Documentation][26])

## Developer-agent platforms

For local AI coding agents, the default should be different:

```text
One machine = one local execution capability per org/endpoint.
```

Coord says each machine runs one runner instance, while users can run runners on many machines. ([Coord][25])

Gitpod Desktop’s local runner model similarly centers on one active local runner for the current organization in the desktop context. ([Gitpod][32])

## Recommendation for Stigmer

Default:

```text
one runner per machine per org per endpoint
```

Advanced:

```text
multiple named runners on same machine
```

Use cases for advanced multi-runner:

* CI/testing.
* Benchmarking.
* Multiple sandboxes.
* Different capability profiles.
* Different credentials.
* Different concurrency limits.
* One runner per repository/worktree.
* Enterprise fleet machines.

UX distinction:

```text
Happy path:
  “Use this computer for Stigmer.”

Advanced path:
  “Add another runner on this computer.”
```

These should not be the same button.

---

# 12. Anti-patterns to avoid

## Anti-pattern 1: Optional blank fields in the happy path

A modal with blank optional fields creates cognitive load. The user thinks:

* Do I need a name?
* What endpoint should I use?
* Which token?
* What happens if I leave these blank?

Then leaving them blank triggers a duplicate-name error. This feels like the user did something wrong.

Move these fields to Advanced.

---

## Anti-pattern 2: Hostname slug as identity

Hostname is a good display default, not a stable identity.

Bad:

```text
identity = sanitize(os.Hostname())
```

Better:

```text
display_name = pretty_hostname()
identity = generated_machine_id
```

Even better:

```text
runner uniqueness = org_id + endpoint + machine_id
```

---

## Anti-pattern 3: “Already running” as a red error

This is the precise problem. Already running is normally success.

Only error if:

* It is running for the wrong org.
* It is running against the wrong backend.
* It is unresponsive.
* The user explicitly requested a second runner and the name conflicts.

---

## Anti-pattern 4: Suggesting `--name` to normal desktop users

The current suggestion tells the user to create another runner. But their intent is to use the existing one.

Better:

```text
This computer is already connected.
```

Then advanced help:

```text
To intentionally create another runner, use --new --name <name>.
```

---

## Anti-pattern 5: PID-only liveness

`signal 0` is insufficient.

Use:

* PID.
* Process start time.
* Executable path/hash.
* State nonce.
* Lock file.
* Control socket health.
* Server heartbeat/session state.

---

## Anti-pattern 6: Mixing “stop runner” with “stop current work”

Stopping a runner may interrupt agent execution. The UI should distinguish:

```text
Pause new work
Stop after current task
Restart now
Force stop
```

Buildkite’s lifecycle distinction between graceful and forceful disconnect is a good model here. ([Buildkite][12])

---

# 13. Concrete Stigmer target design

## 13.1 Happy-path Desktop UX

### First launch after login

```text
Welcome to Stigmer

Local execution is ready on this computer.
```

No runner modal.

### Normal app header / status area

```text
Local runner: Ready
```

or

```text
Local runner: Working on Agent Run #1248
```

### Menu bar / tray

```text
Stigmer
✓ Local execution ready
Current task: none

Open Stigmer
Pause new work
View logs
Restart local runner
Quit
```

### Settings

```text
Local Execution

[x] Use this computer for Stigmer runs
[x] Start Stigmer at login

Runner name
Swarup’s MacBook Pro

Advanced
Endpoint: api.stigmer.ai:443
Machine ID: mach_...
Runner ID: runr_...
Management mode: Desktop
```

### Advanced danger zone

```text
Reset local runner identity
Create additional runner
Install as background service
Uninstall service
Clear stale local state
```

---

## 13.2 Desktop behavior when existing runner is active

### Existing Desktop-managed runner

```text
Do nothing except refresh status.
```

### Existing CLI-managed runner

```text
Adopt for display.
Use local control socket if available.
Show “Managed by Terminal” only if Desktop cannot control it.
```

### Existing service-managed runner

```text
Show “Managed by service.”
Controls may call service manager or ask for permission.
```

### Existing runner for different org

```text
Local runner active for another workspace.
[Switch workspace] [Start runner for this workspace] [Advanced]
```

But this should be rare if identity is scoped by org.

### Existing runner for different endpoint

```text
A local runner is connected to another Stigmer backend.
Current: staging.api.stigmer.ai
This app: api.stigmer.ai

[Use existing backend] [Start production runner] [Advanced]
```

Do not silently adopt a different endpoint.

---

## 13.3 Server API behavior

Recommended `runner.apply` response:

```json
{
  "runner": {
    "runner_id": "runr_123",
    "machine_id": "mach_456",
    "display_name": "Swarup’s MacBook Pro",
    "org_id": "org_789",
    "task_queue": "runner:runr_123",
    "desired_state": "enabled",
    "observed_state": "ready",
    "current_session_id": "sess_abc",
    "last_seen_at": "2026-05-09T11:24:00Z"
  },
  "created": false,
  "resumed": true
}
```

Recommended connect-stream duplicate response:

```json
{
  "error": "ALREADY_CONNECTED",
  "runner_id": "runr_123",
  "active_session_id": "sess_abc",
  "last_heartbeat_at": "2026-05-09T11:24:00Z",
  "replace_allowed": true
}
```

Client mapping:

| Context         |                       Same local process? | Action                                                         |
| --------------- | ----------------------------------------: | -------------------------------------------------------------- |
| Desktop         |                                       Yes | Adopt, show Ready                                              |
| Desktop         | No, but same machine and socket reachable | Adopt/control via socket                                       |
| Desktop         |       No, unreachable but heartbeat fresh | Show “active elsewhere on this machine,” offer restart/replace |
| CLI default     |                           Yes/same runner | Exit `0`                                                       |
| CLI `--replace` |                                       Any | Gracefully preempt                                             |
| CLI `--new`     |                  Name/capability explicit | Create additional runner                                       |

---

# 14. Stale state and crash recovery

Current stale cleanup is directionally right but too weak.

Recommended stale cleanup decision tree:

```text
Read state file.
Acquire lock.
If lock unavailable:
  query control socket.
  if healthy -> active, return success.
  if unhealthy -> mark needs attention.

If lock available:
  check PID.
  if PID absent -> stale, clean.
  if PID present:
    compare process start time.
    compare executable path/cmdline.
    query control socket.
    if healthy -> active.
    if not healthy -> unhealthy, offer restart.
```

Server-side:

```text
If no heartbeat within grace window:
  observed_state = offline
  eligible_for_new_tasks = false

If runner reconnects with same machine_id:
  same runner_id
  new session_id or resumed session
```

State files should not be removed only on clean exit. They should be repaired continuously.

---

# 15. Auto-update pattern

GitHub auto-updates self-hosted runners by default and documents a manual opt-out with operational consequences. ([GitHub Docs][5])

For Stigmer Desktop:

* Desktop app update should update bundled CLI/runner.
* Runner should support graceful drain before update.
* If idle: restart immediately.
* If busy: mark update pending; update after current task.
* If security-critical: pause new work and force update after a clear grace period.

Recommended states:

```text
READY
UPDATE_AVAILABLE
UPDATING
UPDATE_PENDING_AFTER_TASK
UPDATE_FAILED
```

Do not make users manually update a sidecar unless they are in service/headless mode.

---

# 16. Production implementation plan

## Phase 0: Immediate UX fix

Can be done before major architecture changes.

1. Desktop detects the exact “runner already running” error.
2. Loads local state.
3. Verifies org/endpoint/PID.
4. Converts error to success.
5. Shows “Connected to existing runner.”
6. Removes `--name` suggestion from Desktop error presentation.

CLI change:

```text
same runner already active -> exit 0
```

This alone fixes the screenshot scenario.

---

## Phase 1: Idempotent runner commands

Add:

```bash
stigmer runner ensure --json
stigmer runner status --json
stigmer runner stop
stigmer runner restart
stigmer runner doctor
```

Make Desktop call `ensure`, not `up runner`.

Return structured JSON for all cases.

Example:

```json
{
  "ok": true,
  "action": "adopted_existing",
  "status": "ready",
  "runner_id": "runr_...",
  "pid": 9730
}
```

---

## Phase 2: Stable machine identity

Create:

```text
~/.stigmer/machine.json
```

Example:

```json
{
  "machine_id": "mach_01H...",
  "created_at": "...",
  "default_display_name": "Swarup’s MacBook Pro"
}
```

Server uniqueness:

```text
unique(org_id, endpoint, machine_id)
```

Migrate existing hostname-slug runners:

```text
hostname slug -> display_name
existing runner_id -> preserved if possible
machine_id -> backfilled
```

---

## Phase 3: Local control socket

Runner opens:

```text
~/.stigmer/runners/<endpoint_hash>/<org_id>/<machine_id>.sock
```

Desktop uses it for:

* status
* logs
* stop
* restart
* pause/resume
* adoption

CLI uses it to avoid duplicates.

---

## Phase 4: Desktop UI redesign

Replace Start Runner modal with Local Execution card.

Move name/endpoint/token to Settings → Advanced.

Add tray/menu bar status.

Add clear management labels:

```text
Managed by Desktop
Managed by Terminal
Managed by Service
Remote runner
Stopped local runner
```

But do not make users choose this during normal startup.

---

## Phase 5: Service/login integration

Add:

```bash
stigmer runner service install --user
stigmer runner service uninstall
```

macOS:

```text
LaunchAgent/login item by default
LaunchDaemon only explicit/headless
```

Linux:

```text
systemd --user for desktop
system service for server
```

Use restart-on-failure and graceful stop semantics. systemd’s `Restart=on-failure`, `RestartSec`, and watchdog mechanisms are appropriate here. ([freedesktop.org][30])

---

## Phase 6: Server-side session model

Add `RunnerSession`.

Track:

* current session
* previous session
* heartbeat timestamp
* connect/disconnect timestamps
* version
* process metadata when available
* management mode

Change duplicate active connect from generic error to structured conflict.

---

# 17. Recommended product copy

## Button labels

Avoid:

```text
Start Runner
```

Prefer:

```text
Enable local execution
Use this computer
Connect this computer
Resume local execution
Restart local runner
Pause new work
```

## Success copy

```text
This computer is ready to run Stigmer agents.
```

## Already-running copy

```text
This computer is already connected.
We’ll use the existing runner started 5 hours ago.
```

## Busy copy

```text
This computer is running an agent task.
New work will wait until it is available.
```

## Reconnecting copy

```text
Runner is reconnecting.
Your local process is still running; Stigmer will resume automatically.
```

## Wrong-org copy

```text
A local runner is active for another workspace.
Switch workspace or start a separate runner from Advanced.
```

## Advanced duplicate copy

```text
To intentionally run more than one runner on this machine, create an additional runner in Advanced settings.
```

---

# 18. Direct answers to your research questions

## 1. Comparable platform runner UX patterns

CI platforms expose runner lifecycle because their users are admins/operators. GitHub, GitLab, and Buildkite expect explicit registration, service installation, status inspection, and sometimes multiple runner instances. GitHub and GitLab have service modes; Buildkite has strong heartbeat/session/status concepts. ([GitHub Docs][3])

AI coding tools mostly hide runner lifecycle. Cursor, Windsurf, and Devin expose agents, sessions, environments, and work status, not “start a runner process.” ([Cursor Documentation][17])

Closest local-runner analogs are Coord and Gitpod Desktop. Coord exposes a desktop runner with Status/Logs/History and one runner per machine. Gitpod Desktop automatically manages a current-org local runner. ([Coord Docs][2])

---

## 2. “Start and forget” patterns

The strongest patterns are:

* idempotent ensure
* auto-adopt existing local process
* auto-start on app launch
* run-at-login option
* graceful reconnect after sleep/wake
* status-first UI
* move configuration to Advanced

Already-running should be success.

---

## 3. Identity and naming patterns

GitLab explicitly treats runner `name` as informational; Buildkite supports generated/template names; GitHub often defaults toward machine names but also has replacement/ephemeral infrastructure patterns. ([GitLab Documentation][33])

For Stigmer:

```text
machine_id + org_id + endpoint = identity
hostname = display name
session_id = process instance
```

---

## 4. Desktop / GUI patterns

Cursor and Windsurf show agent/session status in sidebars and command centers. Coord shows Status/Logs/History in a desktop runner. Gitpod Desktop automatically starts/stops the current local runner. ([Cursor][18])

For Stigmer:

* remove the normal Start Runner dialog
* show Local Execution card
* show tray/menu-bar status
* add Advanced settings for runner internals

---

## 5. Error prevention and recovery

Best answer:

```text
Auto-adopt existing runner.
Treat active runner as success.
Use Restart only when explicit.
Hide Start when already active.
```

Stale state should be repaired with lock + PID + process birth time + executable check + control socket + server heartbeat.

Sleep/wake should be reconnect, not re-register.

---

## 6. Background process management

macOS developer mode should use Desktop-managed process or LaunchAgent/login item, not default LaunchDaemon. GitLab’s macOS runner design supports this user-mode approach because of user session/keychain/UI needs. ([GitLab Documentation][10])

Linux desktop should use systemd user service. Headless/server should use system-level systemd service with restart-on-failure and watchdog options. ([freedesktop.org][30])

---

## 7. Multi-runner versus single-runner

CI/admin tools support multiple runners per machine. Developer desktop tools should default to one local runner per machine per org. Coord explicitly says each machine runs one runner instance, while many machines can join the workspace. ([Coord][25])

For Stigmer:

```text
Default: one runner per machine/org/endpoint
Advanced: additional runners
```

---

# 19. Final recommendation

Stigmer should not ask desktop users to “start a runner” as though they are administering CI infrastructure.

The target product contract should be:

> **When Stigmer Desktop is open and the user is signed in, this computer is available for Stigmer runs unless the user disables it.**

The target technical contract should be:

> **`ensureRunner(org, endpoint, machine_id)` reconciles local and server state until a compatible runner is active, adopted, reconnected, or clearly needs attention.**

The target behavior for your screenshot scenario should be:

```text
User clicks local execution control.
Existing runner PID 9730 is alive.
Desktop adopts it.
UI shows: “Ready — this computer is already connected.”
No error.
No modal.
No `--name` suggestion.
```

The most important architectural shift is separating these concepts:

```text
display name ≠ identity
runner record ≠ process session
start command ≠ create new runner
already running ≠ error
desktop user ≠ CI administrator
```

Once those are separated, the UX becomes straightforward: Stigmer simply keeps local execution available, and runner management fades into the background where it belongs.

[1]: https://www.gitpod.io/docs/flex/gitpod-desktop?utm_source=chatgpt.com "Gitpod Desktop"
[2]: https://docs.coord.io/docs/runner/overview?utm_source=chatgpt.com "Overview | Coord Docs"
[3]: https://docs.github.com/en/actions/hosting-your-own-runners/managing-self-hosted-runners/configuring-the-self-hosted-runner-application-as-a-service?platform=linux&utm_source=chatgpt.com "Configuring the self-hosted runner application as a service - GitHub Docs"
[4]: https://docs.github.com/en/actions/how-tos/managing-self-hosted-runners/monitoring-and-troubleshooting-self-hosted-runners?learn=hosting_your_own_runners&learnProduct=actions&utm_source=chatgpt.com "Monitoring and troubleshooting self-hosted runners - GitHub Docs"
[5]: https://docs.github.com/en/actions/reference/supported-architectures-and-operating-systems-for-self-hosted-runners?utm_source=chatgpt.com "Self-hosted runners reference - GitHub Docs"
[6]: https://github.blog/changelog/2023-06-02-github-actions-just-in-time-self-hosted-runners/?utm_source=chatgpt.com "GitHub Actions - Just-in-time self-hosted runners - GitHub Changelog"
[7]: https://stackoverflow.com/questions/74460753/check-already-running-github-action-self-hosted-runner?utm_source=chatgpt.com "Check already running github action self-hosted runner - Stack Overflow"
[8]: https://docs.gitlab.com/runner/register/?utm_source=chatgpt.com "Registering runners | GitLab Docs"
[9]: https://docs.gitlab.com/runner/configuration/advanced-configuration.html?utm_source=chatgpt.com "Advanced configuration | GitLab Docs"
[10]: https://docs.gitlab.com/runner/install/osx/?utm_source=chatgpt.com "Install GitLab Runner on macOS | GitLab Docs"
[11]: https://docs.gitlab.com/runner/configuration/init.html?utm_source=chatgpt.com "The system services of GitLab Runner | GitLab Docs"
[12]: https://buildkite.com/docs/agent/v3?utm_source=chatgpt.com "The Buildkite Agent v3 | Buildkite Documentation"
[13]: https://buildkite.com/docs/agent/v3/configuration?utm_source=chatgpt.com "Buildkite Agent configuration v3 | Buildkite Documentation"
[14]: https://buildkite.com/docs/agent/v3/cli-start?utm_source=chatgpt.com "buildkite-agent start v3 | Buildkite Documentation"
[15]: https://buildkite.com/docs/apis/graphql/schemas/object/agent?utm_source=chatgpt.com "Agent – Objects – GraphQL API | Buildkite Documentation"
[16]: https://buildkite.com/docs/pipelines/insights/queue-metrics?utm_source=chatgpt.com "Queue metrics in clusters | Buildkite Documentation"
[17]: https://docs.cursor.com/en/background-agents?utm_source=chatgpt.com "Cursor – Background Agents"
[18]: https://cursor.com/changelog/1-4?utm_source=chatgpt.com "Improved Agent tools, steerability, and usage visibility · Cursor"
[19]: https://docs.cursor.com/background-agent?utm_source=chatgpt.com "Cursor – Background Agents"
[20]: https://docs.windsurf.com/windsurf/cascade/cascade?utm_source=chatgpt.com "Windsurf - Cascade"
[21]: https://docs.windsurf.com/windsurf/agent-command-center?utm_source=chatgpt.com "Agent Command Center - Windsurf Docs"
[22]: https://cognitionai.mintlify.app/onboard-devin/environment?utm_source=chatgpt.com "Environment configuration - Devin Docs"
[23]: https://cognitionai.mintlify.app/onboard-devin/environment/quickstart?utm_source=chatgpt.com "Declarative configuration - Devin Docs"
[24]: https://docs.devin.ai/onboard-devin/new-repo-setup?utm_source=chatgpt.com "Repository Setup - Devin Docs"
[25]: https://coord.io/?utm_source=chatgpt.com "Coord — Where Teams and AI Agents Turn Ideas Into Results"
[26]: https://docs.gitlab.com/runner/commands/?utm_source=chatgpt.com "GitLab Runner commands | GitLab Docs"
[27]: https://api-docs.temporal.io/?utm_source=chatgpt.com "Protocol Documentation"
[28]: https://developer.apple.com/library/archive/documentation/MacOSX/Conceptual/BPSystemStartup/Chapters/CreatingLaunchdJobs.html?utm_source=chatgpt.com "Creating Launch Daemons and Agents"
[29]: https://v2.tauri.app/plugin/autostart/?utm_source=chatgpt.com "Autostart | Tauri"
[30]: https://www.freedesktop.org/software/systemd/man/249/systemd.service.html?utm_source=chatgpt.com "systemd.service"
[31]: https://buildkite.com/docs/agent/lifecycle?utm_source=chatgpt.com "Agent lifecycle | Buildkite Documentation"
[32]: https://preview.gitpod.io/docs/flex/gitpod-desktop?utm_source=chatgpt.com "Gitpod Desktop - Docs"
[33]: https://docs.gitlab.com/runner/configuration/advanced-configuration/?utm_source=chatgpt.com "Advanced configuration | GitLab Docs"
