# Session Notes: 2026-05-09, Session 7

## Accomplishments

- Fixed runner startup latency: added urgent 2s polling during ensure (was 10s)
- Fixed Unix socket communication: removed half-close race in Rust client
- Added progressive bootstrap feedback (elapsed time + contextual messages)
- Added 30s grace period before showing socket health warning

## Decisions Made

- Use `setUrgent()` callback pattern (not parameter) to avoid circular hook dependencies between `useLocalRunnerStatus` and `useAutoEnsure`
- Remove `stream.into_split()` + `writer.shutdown()` entirely; use single-stream sequential write/read instead — simpler and avoids macOS Unix socket half-close issues
- Health warning grace at 30s (not longer) to still surface genuine socket failures within a reasonable time

## Key Code Changes

- `useLocalRunnerStatus.ts`: Added URGENT_POLL_MS, setUrgent/urgentRef, urgent in effect deps
- `RunnersPage.tsx`: Added useEffect to sync autoEnsureState → setUrgent
- `sidecar.rs`: Rewrote unix_http_request to avoid split/shutdown pattern
- `ThisMachineCard.tsx`: EnsuringCard with elapsed timer; ActiveCard with pastGrace state

## Next Session Plan

- T07: End-to-end testing of the full auto-ensure lifecycle
- Verify socket communication works on fresh install
- Test first-run prompt → active → disable → re-enable flow
