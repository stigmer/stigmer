# Session Notes: 2026-05-09

## Accomplishments

- Completed T01 Phase 0: "Already running = success" (runner adoption)
- All three layers implemented: CLI, Tauri sidecar, Desktop UI
- 8 unit tests written and passing
- Committed as `4db26fe36` on `feat/resource-views-ux-overhaul`

## Decisions Made

- **Org/endpoint comparison is defensive, not strict** — empty values in state files default to adoption rather than conflict. Avoids false positives with legacy state files.
- **No structured JSON output in Phase 0** — Tauri sidecar doesn't need to parse CLI stdout for success cases. Phase 1 (`runner ensure --json`) will add this.
- **No new CLI flags** (`--adopt`, `--new`) — behavior is unconditional for Phase 0. Phase 1 introduces `stigmer runner ensure` as the explicit idempotent command.
- **ProcessManager only tracks owned processes** — adopted runners remain tracked via filesystem state files only.
- **Toast over banner for adoption feedback** — sonner toast is less intrusive than a banner and appropriate for a "nothing really changed" success.

## Key Code Changes

- `client-apps/cli/internal/cli/runner/start.go`: Replaced `checkNameConflict` with `checkOrAdopt(name, opts)` returning three outcomes (adopt/continue/error)
- `client-apps/desktop/src-tauri/src/sidecar.rs`: Two fixes — early `Ok` for already-managed, and `return Ok` for exit-0-during-grace (prevents ghost entries)
- `client-apps/desktop/src/pages/runners/RunnersPage.tsx`: Sonner toast when `localRunners.has(runnerName)` after successful start
- `client-apps/cli/internal/cli/runner/adopt_test.go`: New test file with 8 scenarios

## Next Session Plan

- Begin T02: Idempotent `stigmer runner ensure --json` command
- Or move to T05 (Desktop UI redesign) if product direction prioritizes the status card view
