# Session Notes: 2026-05-09 (Session 17)

## Accomplishments

- **Desktop Parity Review & Implementation** — Identified and closed the systemic desktop app parity gap across all Phases 0-4
- Migrated all 3 desktop list pages (Agent, Skill, MCP Server) from deprecated `ResourceListView` to `ResourceWorkbench` with table/cards view modes, action menus, scope toggle, and search
- Updated all 3 desktop library detail pages (Agent, Skill, MCP Server) with full action bars: Copy ID, Copy slug, Export YAML/JSON, Download YAML, Delete with confirmation dialog
- Created 3 desktop creation pages (`AgentNewPage`, `SkillNewPage`, `McpServerNewPage`) with `CreationPicker` → wizard flow, replacing the old `/?draft=*` routes
- Added 3 creation routes + runner detail route to desktop `routes.tsx`
- Created `RunnerDetailPage` for desktop with stop/delete actions and `ConfirmDialog`
- Added fleet row click-through in `OrgFleetSection` (`onViewDetail` callback) with `stopPropagation` on action buttons
- Added `FetchCacheProvider` to desktop `App.tsx` for cross-mount data persistence
- Wired `ImportResourceDialog` on desktop Agent and MCP Server list pages
- Fixed web `AgentListPage` and `SkillListPage` TODO stubs — wired delete with `useConfirmAction` + `ConfirmDialog` + `key`-based workbench refresh
- Clarified DD-004 in `sdk-console-architecture.mdc` — `"use client"` is a React standard directive, not a framework import
- Deleted `ResourceListView` component + all barrel exports (no consumers remain, SDK not published)

## Decisions Made

- **Creation routes replace draft routes** — `/?draft=agent` etc. replaced with `/library/agents/new` using SDK creation wizards
- **Runner page: keep custom, add detail route** — Desktop `RunnersPage` stays custom (manages Tauri sidecar). `/runners/:id` drill-down added using SDK `RunnerDetailView`
- **Keep `"use client"` directives** — React standard directive, zero impact on non-RSC environments
- **Delete ResourceListView outright** — No deprecation needed since SDK is not yet published

## Key Code Changes

- `client-apps/desktop/src/App.tsx`: Added `FetchCacheProvider` import and wrapper
- `client-apps/desktop/src/pages/library/*.tsx`: All 6 list+detail pages rewritten
- `client-apps/desktop/src/pages/library/*NewPage.tsx`: 3 new creation pages
- `client-apps/desktop/src/pages/runners/RunnerDetailPage.tsx`: New runner detail page
- `client-apps/desktop/src/pages/runners/OrgFleetSection.tsx`: Added `onViewDetail` prop
- `client-apps/desktop/src/pages/runners/RunnersPage.tsx`: Wired fleet click-through
- `client-apps/desktop/src/routes.tsx`: 4 new routes (3 creation + 1 runner detail)
- `client-apps/web/src/domain/library/agents/AgentListPage.tsx`: Delete flow wired
- `client-apps/web/src/domain/library/skills/SkillListPage.tsx`: Delete flow wired
- `sdk/react/src/library/ResourceListView.tsx`: Deleted
- `sdk/react/src/library/index.ts`: Removed ResourceListView exports
- `sdk/react/src/index.ts`: Removed ResourceListView exports
- `.cursor/rules/client-apps/sdk-console-architecture.mdc`: DD-004 clarification

## Learnings

- Desktop app was entirely untouched across 16 prior sessions — DD-016 (Client App Parity) was not being checked
- `SearchResult` proto uses `id` field, not `resourceId` — caught by linter
- `FetchCacheProvider` was exported from SDK but unused by either app — pure opt-in improvement
- `ResourceListView` already had `@deprecated` JSDoc but since SDK isn't published, deletion is cleaner

## Next Session Plan

- All desktop parity gaps closed
- Consider adding a CI parity check (per Gap 9 recommendation) that flags web page changes without corresponding desktop updates
