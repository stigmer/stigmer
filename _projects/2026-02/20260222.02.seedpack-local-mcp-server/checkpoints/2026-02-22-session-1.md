# Session Notes: 2026-02-22

## Accomplishments

- Reviewed and approved T01 implementation plan
- Completed Phase 1: Seedpack — Add MCP Server Resource
  - Created `mcp-servers/stigmer-mcp-server.yaml` (proto-compliant McpServer YAML)
  - Updated `manifest.json` (schema v3, version 1.2.0, mcp_servers array)
  - Extended `embed.go` with `//go:embed mcp-servers/*` directive
  - Added `McpServerEntry`, `LoadMcpServerYAML()`, `parseMcpServerYAML()`, `GetMcpServerByName()` to `seedpack.go`
  - Updated `BUILD.bazel` with mcp-servers glob and mcpserver proto dep
  - Added 4 test cases, updated existing version assertions
  - Updated `bootstrap_test.go` version assertion to match new seedpack version

## Decisions Made

- **Schema version "3"**: Bumped to signal the new `mcp_servers` field in the manifest
- **No generics for YAML parsing**: Wrote `parseMcpServerYAML` mirroring `parseAgentYAML` — generics premature for two resource types
- **Simple `McpServerEntry`**: Same shape as `AgentEntry` (Name + Path only) — MCP servers are pure YAML definitions, no artifacts

## Key Code Changes

- `backend/services/stigmer-server/pkg/seedpack/seedpack.go`: New types and loaders for MCP server
- `backend/services/stigmer-server/pkg/seedpack/embed.go`: Added mcp-servers embed directive
- `backend/services/stigmer-server/pkg/seedpack/manifest.json`: Schema v3, added mcp_servers
- `backend/services/stigmer-server/pkg/seedpack/mcp-servers/stigmer-mcp-server.yaml`: New file
- `backend/services/stigmer-server/pkg/seedpack/BUILD.bazel`: New glob and dep
- `backend/services/stigmer-server/pkg/seedpack/seedpack_test.go`: 4 new tests
- `backend/services/stigmer-server/pkg/bootstrap/bootstrap_test.go`: Version bump

## Learnings

- Pre-existing `TestVerifyContentDigest` failure exists (package_skill.py digest mismatch in provenance.json) — not caused by this work
- The seedpack was relocated from `backend/libs/go/seedpack/` to `backend/services/stigmer-server/pkg/seedpack/` in a prior session — this relocation is still uncommitted

## Open Questions

- Phase 4: Should the MCP server process be managed by the daemon or the server's component supervisor? Plan recommends daemon for STDIO isolation.
- Phase 4: Should ALL bootstrapped MCP servers be auto-started, or only the built-in one?

## Next Session Plan

- Phase 2: Extend `bootstrap.go` with `McpServerClient` interface and `bootstrapMcpServer()` method
- Phase 3: Wire the MCP server controller into bootstrap in `server.go`
- Phase 4: Auto-start MCP server in daemon
- Phase 5: Tests and validation
