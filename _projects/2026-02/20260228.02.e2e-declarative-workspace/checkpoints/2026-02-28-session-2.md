# Session Notes: 2026-02-28 (Session 2)

## Accomplishments

- **T03 Complete**: Full seedpack migration — moved to repo root, gutted to thin wrapper, CLI-driven bootstrap, ~1,100 lines deleted
- Seedpack moved from `backend/services/stigmer-server/pkg/seedpack/` to `seedpack/` at repo root
- `seedpack.go` replaced: 660 lines → 80 lines (ExtractToDir + ContentHash only)
- `seedpack_test.go` replaced: 800 lines → ~100 lines (4 focused tests)
- `bootstrap.go` + `bootstrap_test.go` + `BUILD.bazel` deleted entirely (428+ lines)
- Bootstrap migrated to CLI subprocess in `daemon.EnsureRunning()`
- Scanner enhanced for nested skill layouts (`skills/{name}/SKILL.md`)
- New Go module created: `github.com/stigmer/stigmer/seedpack`
- 2 new tool scripts: `06_draft-agent-creator-agent.sh`, `regenerate_all.sh`

## Decisions Made

- **DD-01 overturned**: Seedpack moved to repo root (previously decided to keep nested due to Go embed constraints — resolved by making seedpack its own Go module)
- **CLI-only bootstrap**: Server's internal bootstrap completely removed. CLI subprocess is the sole path for applying system resources — eliminates ~1,100 lines of duplicated logic
- **Search index rebuild preserved**: Kept at server startup, decoupled from bootstrap. FTS5 requires explicit rebuild since SaveResource doesn't update it
- **Recursion guard via env var**: `STIGMER_SKIP_SEEDPACK_BOOTSTRAP=1` prevents infinite loop when apply subprocess calls EnsureRunning
- **Flag file for idempotency**: `$dataDir/.seedpack-bootstrapped` stores content hash, replaces SQLite bootstrap_state table

## Key Code Changes

- `seedpack/seedpack.go`: Complete rewrite — thin embed wrapper
- `seedpack/embed.go`: Added `//go:embed stigmer.yaml`
- `seedpack/stigmer.yaml`: New project manifest
- `seedpack/BUILD.bazel`: Simplified, new importpath
- `seedpack/go.mod`: New Go module
- `client-apps/cli/internal/cli/daemon/daemon.go`: Added `ensureSeedpackBootstrapped()` with subprocess approach
- `client-apps/cli/cmd/stigmer/root/apply_declarative.go`: Enhanced `scanSkillDirectories()` for nested layouts, added `containsSkillDirectories()`
- `client-apps/cli/cmd/stigmer/root/apply_declarative_test.go`: 4 new tests for nested skill scanning
- `backend/services/stigmer-server/pkg/server/server.go`: Removed bootstrap call, kept search index rebuild
- `backend/services/stigmer-server/pkg/bootstrap/`: Deleted entirely

## Open Questions

- User raised concern about Go files (seedpack.go, embed.go, go.mod) being misleading alongside content files in seedpack/ — could separate into seedpack/content/ subdirectory but Go embed constraint requires files to be in or under the package directory
- Search index timing: bootstrap now happens after server starts, so there's a brief window where system resources aren't indexed. Acceptable for local dev

## Next Session Plan

- Address seedpack Go files concern if user wants restructuring
- T04: Add `--workspace` flag to CLI
- T05: End-to-end testing
- T06: Customer documentation
