---
title: Sessions
description:
  A Session is a conversation context that preserves memory across interactions.
  Learn how Sessions work, how they connect to Agents, and how to customize them
  per conversation.
---

When a user talks to an Agent, the conversation needs memory. The second message
should know about the first. The Agent should remember an order number mentioned
earlier without the user repeating it. That context lives in a Session.

A Session is a conversation context that holds message history and connects a
user to an [Agent](/docs/concepts/agents). When a user sends a message, the
Agent sees the full conversation history from that Session — it knows what was
asked before, what tools were called, and what the outcomes were.

## Sessions in action

Watch how the Agent remembers context across messages. The user mentions an
order in the first message, and the Agent recalls the product and order number
in the second — without the user repeating any details:

<DemoSessionMemoryPlayback />

This works because both messages exist in the same Session. The Agent sees the
complete thread when processing each new message.

## How Sessions fit in the lifecycle

A Session connects an Agent (via an Agent Instance) to a conversation:

{/* prettier-ignore */}
<Mermaid
  chart={`
graph LR
  AI["Agent Instance"] --> S["Session"]
  S --> E1["Execution 1"]
  S --> E2["Execution 2"]
  S --> E3["Execution 3"]
`}
/>

Each request-response cycle within a Session creates an
[Agent Execution](/docs/concepts/agents#blueprint-vs-runtime). The execution
sees all previous messages from the Session — this is how the Agent remembers
context.

## Creating a Session

In the SDK, you create a Session by specifying which Agent Instance to use:

<Tabs items={["TypeScript", "Go"]}>
  <Tab value="TypeScript">
```typescript
const session = await client.session.create({
  spec: {
    subject: "Order inquiry",
    agentInstanceId: "ain-abc123",
  },
});
```
  </Tab>
  <Tab value="Go">

```go
session, err := client.Session.Create(ctx, &sessionv1.CreateInput{
    Spec: &sessionv1.SessionSpec{
        Subject:         "Order inquiry",
        AgentInstanceId: "ain-abc123",
    },
})
```

  </Tab>
</Tabs>

If you don't specify an `agentInstanceId`, Stigmer uses the default Agent — the
same `assistant` from the [Quickstart](/docs/getting-started/quickstart).

## Harness

Every Session runs on a [Harness](/docs/concepts/harnesses) — the execution
engine that processes the Agent's turns. Stigmer supports two: **Native** (the
default) and **Cursor** (for Cursor SDK-powered execution with access to
Cursor-specific models and coding tools).

You set the Harness when creating a Session. Once the first execution runs, the
Harness is locked for that Session.

```yaml
spec:
  harness: HARNESS_CURSOR
```

See [Harnesses](/docs/concepts/harnesses) for a full comparison.

## Session-level overrides

Sessions can customize an Agent's behavior for a specific conversation without
changing the Agent definition. Two key overrides:

### Skills

Add domain knowledge for this conversation only:

```yaml
spec:
  skill_refs:
    - kind: skill
      slug: seasonal-promotions
```

Session-level Skills merge with the Agent's Skills. If the same Skill appears in
both, the Session-level reference takes precedence.

### Tools

Add or restrict tool access per conversation:

```yaml
spec:
  mcp_server_usages:
    - mcp_server_ref:
        kind: mcp_server
        slug: inventory-api
      enabled_tools:
        - check_stock
```

This lets you give a specific conversation access to tools that the Agent
doesn't normally have, or restrict tools for a sensitive interaction.

## Session lifecycle

Sessions persist until explicitly deleted. You can:

- **Resume** a Session by sending new messages to the same Session ID
- **List** past Sessions and their execution history
- **Delete** a Session when the conversation is no longer needed

Each Session maintains its own thread of messages. The Agent sees the complete
history when processing a new message, so context accumulates over the
conversation.

## What's next

Sessions capture conversations. But every conversation needs somewhere to run —
a process that picks up the work, calls the LLM, and executes tools. That's what
[Runners](/docs/concepts/runners) are. The next page explains how runners work,
the difference between local and cloud runners, and how Sessions find the right
runner for each execution.
