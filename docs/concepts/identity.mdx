---
title: Identity
description:
  How Stigmer identifies users and services. Learn the three account types, how
  Identity Providers establish trust, and how federated authentication lets your
  platform's users access Stigmer directly.
---

Every API call to Stigmer is made by an **Identity Account**---a principal that
Stigmer can authenticate and authorize. Identity Accounts come in three flavors,
each suited to a different integration pattern.

## Account types

| Type          | How it's created                                           | Use case                                                    |
| ------------- | ---------------------------------------------------------- | ----------------------------------------------------------- |
| **Direct**    | User signs up on Stigmer or through a Stigmer-hosted login | Self-managed Organizations using Stigmer's own auth         |
| **Federated** | Platform backend calls `createFederatedAccount`            | Platform-managed Organizations bringing their own user base |
| **Machine**   | Created via API with client credentials                    | Service-to-service communication (backend-to-backend)       |

Direct accounts use Stigmer's own authentication tenant. Federated accounts
belong to an external Identity Provider and authenticate with tokens issued by
that provider. Machine accounts use client credentials and have no interactive
login.

## Identity Providers

An Identity Provider represents a **trust relationship** between an external
platform and Stigmer. It is not a user database---it tells Stigmer how to
validate tokens that a platform's users present.

```yaml
apiVersion: iam.stigmer.ai/v1
kind: IdentityProvider
metadata:
  name: Acme Cloud
  slug: acme-cloud
  org: acme
spec:
  display_name: "Acme Cloud"
  jwks_uri: "https://acme.us.auth0.com/.well-known/jwks.json"
  allowed_issuers: ["https://acme.us.auth0.com/"]
  expected_audience: "https://api.stigmer.ai/"
```

When a user presents a JWT issued by `acme.us.auth0.com`, Stigmer fetches the
public keys from `jwks_uri`, verifies the signature, checks the issuer and
audience, and resolves the token's `sub` claim to a federated Identity Account.

Key fields:

| Field               | Purpose                                                                |
| ------------------- | ---------------------------------------------------------------------- |
| `jwks_uri`          | JWKS endpoint where Stigmer fetches public keys for token verification |
| `allowed_issuers`   | JWT `iss` values Stigmer accepts from this provider                    |
| `expected_audience` | JWT `aud` value Stigmer expects                                        |
| `is_sso_provider`   | Marks this IdP as the SSO login provider for its Organization          |

## How federated authentication works

Federated authentication is **explicit, not automatic**. Stigmer does not create
accounts on the fly when it sees a new token. Instead, the platform backend
provisions accounts ahead of time:

<!-- vale off -->

{/* prettier-ignore */}
<Mermaid
  chart={`
sequenceDiagram
  participant Platform as Platform Backend
  participant Stigmer as Stigmer API
  participant User as Platform User
  Platform->>Stigmer: 1. Register Identity Provider
  Platform->>Stigmer: 2. createFederatedAccount(sub, email, name)
  Platform->>Stigmer: 3. Grant org role via IAM Policy
  User->>Platform: 4. Sign in on platform
  Platform-->>User: 5. Issue JWT
  User->>Stigmer: 6. Call API with JWT
  Stigmer->>Stigmer: 7. Validate token, resolve account
  Stigmer-->>User: 8. Authorized response
`}
/>

<!-- vale on -->

Steps 1--3 happen once per user during onboarding. Steps 4--8 happen on every
API call. If a token's `sub` doesn't match any federated account, Stigmer
returns `401 Unauthorized`---it never provisions automatically.

## Authorization

Having an Identity Account is necessary but not sufficient. The account also
needs **IAM Policies** that grant it access to specific resources. An IAM Policy
binds three things:

- **WHO** --- the Identity Account (principal)
- **WHAT** --- the resource (Organization, Environment, Agent, etc.)
- **HOW** --- the relation (`admin`, `viewer`, `owner`)

```yaml
apiVersion: iam.stigmer.ai/v1
kind: IamPolicy
spec:
  principal:
    kind: identity_account
    id: ida_01abc123
  resource:
    kind: organization
    id: org_acme456
  relation: admin
```

Without a matching IAM Policy, an authenticated user gets `403 Forbidden`.

## What's next

If you're building a platform that manages Organizations on behalf of your
users, the [Identity federation guide](/docs/guides/federation/overview) walks
you through the complete setup: registering an Identity Provider, provisioning
federated accounts, granting access, and how the authentication flow works at
runtime.

For API reference details, see
[IdentityProvider](/docs/sdk/resources/identity-provider),
[IdentityAccount](/docs/sdk/resources/identity-account), and
[IamPolicy](/docs/sdk/resources/iam-policy).
