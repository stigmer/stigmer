---
title: Execution Context
description: "SDK reference for Execution Context resources."
---

{/* Auto-generated by stigmer-codegen. DO NOT EDIT. */}

ExecutionContextSpec defines ephemeral runtime configuration and secrets. This
is created during execution and deleted when execution completes.

## Client Access

<SDKTabs>
  <Tab value="Go">
    ```go executionContext, err := client.ExecutionContext.Get(ctx,
    "executionContext-id") ```
  </Tab>
  <Tab value="TypeScript">
    ```typescript const executionContext = await
    stigmer.executionContext.get("executionContext-id"); ```
  </Tab>
  <Tab value="Python">
    ```python executionContext =
    client.execution_context.get("executionContext-id") ```
  </Tab>
  <Tab value="Java">
    ```java ExecutionContext executionContext =
    client.executionContext().get("executionContext-id"); ```
  </Tab>
</SDKTabs>

## Methods

### apply

Create or update an ExecutionContext. The authorization and state-operation are
determined depending on whether the execution context is going to be created or
updated which is determined as part of the request execution.

<SDKTabs>
<Tab value="Go">
```go
executionContext, err := client.ExecutionContext.Apply(ctx, &stigmer.ExecutionContextInput{
  Name: "my-executionContext",
  Org:  "acme",
})
```
</Tab>
<Tab value="TypeScript">
```typescript
const executionContext = await stigmer.executionContext.apply({
  name: "my-executionContext",
  org: "acme",
});
```
</Tab>
<Tab value="Python">
```python
executionContext = client.execution_context.apply(ExecutionContextInput(
    name="my-executionContext",
    org="acme",
))
```
</Tab>
<Tab value="Java">
```java
ExecutionContext executionContext = client.executionContext().apply(ExecutionContextInput.builder()
    .name("my-executionContext")
    .org("acme")
    .build());
```
</Tab>
</SDKTabs>

**Parameters**: [`ExecutionContextInput`](#executioncontextinput)

**Returns**: `ExecutionContext`

### create

Create a new ExecutionContext (called by execution pipeline on behalf of the
user). Handler-level derived auth: checks can_edit on parent agent_execution or
workflow_execution.

<SDKTabs>
<Tab value="Go">
```go
executionContext, err := client.ExecutionContext.Create(ctx, &stigmer.ExecutionContextInput{
  Name: "my-executionContext",
  Org:  "acme",
})
```
</Tab>
<Tab value="TypeScript">
```typescript
const executionContext = await stigmer.executionContext.create({
  name: "my-executionContext",
  org: "acme",
});
```
</Tab>
<Tab value="Python">
```python
executionContext = client.execution_context.create(ExecutionContextInput(
    name="my-executionContext",
    org="acme",
))
```
</Tab>
<Tab value="Java">
```java
ExecutionContext executionContext = client.executionContext().create(ExecutionContextInput.builder()
    .name("my-executionContext")
    .org("acme")
    .build());
```
</Tab>
</SDKTabs>

**Parameters**: [`ExecutionContextInput`](#executioncontextinput)

**Returns**: `ExecutionContext`

### delete

Delete an ExecutionContext (called when execution completes). Handler-level
derived auth: checks can_edit on parent agent_execution or workflow_execution.

<SDKTabs>
<Tab value="Go">
```go
executionContext, err := client.ExecutionContext.Delete(ctx, &stigmer.DeleteResourceInput{
  ResourceID: id,
})
```
</Tab>
<Tab value="TypeScript">
```typescript
const executionContext = await stigmer.executionContext.delete({ resourceId: id });
```
</Tab>
<Tab value="Python">
```python
executionContext = client.execution_context.delete(DeleteResourceInput(resource_id=id))
```
</Tab>
<Tab value="Java">
```java
ExecutionContext executionContext = client.executionContext().delete(DeleteResourceInput.builder()
    .resourceId(id)
    .build());
```
</Tab>
</SDKTabs>

**Parameters**

| Parameter        | Type      | Description                              |
| ---------------- | --------- | ---------------------------------------- |
| `resourceId`     | `string`  | ID of the resource to delete.            |
| `versionMessage` | `string`  | Optional deletion reason.                |
| `force`          | `boolean` | Force delete even if resource is in use. |

**Returns**: `ExecutionContext`

### get

Get an ExecutionContext by ID. Handler-level derived auth: checks can_view on
parent agent_execution or workflow_execution.

<SDKTabs>
  <Tab value="Go">
    ```go executionContext, err := client.ExecutionContext.Get(ctx, id) ```
  </Tab>
  <Tab value="TypeScript">
    ```typescript const executionContext = await
    stigmer.executionContext.get(id); ```
  </Tab>
  <Tab value="Python">
    ```python executionContext = client.execution_context.get(id) ```
  </Tab>
  <Tab value="Java">
    ```java ExecutionContext executionContext =
    client.executionContext().get(id); ```
  </Tab>
</SDKTabs>

**Parameters**

| Parameter | Type     | Description                   |
| --------- | -------- | ----------------------------- |
| `id`      | `string` | Execution Context identifier. |

**Returns**: `ExecutionContext`

### getByReference

Get an ExecutionContext by reference (slug-based lookup). Handler-level derived
auth: checks can_view on parent agent_execution or workflow_execution.

<SDKTabs>
  <Tab value="Go">
    ```go executionContext, err := client.ExecutionContext.GetByReference(ctx,
    input) ```
  </Tab>
  <Tab value="TypeScript">
    ```typescript const executionContext = await
    stigmer.executionContext.getByReference(input); ```
  </Tab>
  <Tab value="Python">
    ```python executionContext =
    client.execution_context.get_by_reference(input) ```
  </Tab>
  <Tab value="Java">
    ```java ExecutionContext executionContext =
    client.executionContext().getByReference(input); ```
  </Tab>
</SDKTabs>

**Parameters**: `ApiResourceReference`

**Returns**: `ExecutionContext`

### getByExecutionId

Get an ExecutionContext by the execution ID it belongs to. This is the primary
lookup method used by runners to retrieve the merged environment variables
during workflow/agent execution. The returned context contains decrypted secrets
for runner consumption.

Handler-level derived auth: checks can_view on parent agent_execution or
workflow_execution.

<SDKTabs>
  <Tab value="Go">
    ```go executionContext, err := client.ExecutionContext.GetByExecutionId(ctx,
    input) ```
  </Tab>
  <Tab value="TypeScript">
    ```typescript const executionContext = await
    stigmer.executionContext.getByExecutionId(input); ```
  </Tab>
  <Tab value="Python">
    ```python executionContext =
    client.execution_context.get_by_execution_id(input) ```
  </Tab>
  <Tab value="Java">
    ```java ExecutionContext executionContext =
    client.executionContext().getByExecutionId(input); ```
  </Tab>
</SDKTabs>

**Parameters**: `ExecutionContextExecutionIdInput`

**Returns**: `ExecutionContext`

## Types

### ExecutionContextInput

Input for creating or updating a Execution Context.

<TypeTable
  type={{
    name: { type: "string", description: "Resource name.", required: true },
    slug: { type: "string", description: "URL-friendly identifier." },
    org: { type: "string", description: "Organization slug.", required: true },
    labels: {
      type: "Record<string, string>",
      description: "Key-value labels.",
    },
    executionId: {
      type: "string",
      description: "The execution ID this context belongs to.",
    },
    data: {
      type: "Record<string, EnvVarInput>",
      description: "Key-value pairs containing both configuration and secrets.",
    },
  }}
/>
