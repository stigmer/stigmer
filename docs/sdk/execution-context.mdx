---
title: Execution Context
description: "SDK reference for Execution Context resources."
---

{/* Auto-generated by stigmer-codegen. DO NOT EDIT. */}

This is created during execution and deleted when execution completes.

## Client Access

<SDKTabs>
<Tab value="Go">
```go
executionContext, err := client.ExecutionContext.Get(ctx, "ectx_01j5q3k7m8r2s4tnz2hfp0q0c3")
```
</Tab>
<Tab value="TypeScript">
```typescript
const executionContext = await stigmer.executionContext.get("ectx_01j5q3k7m8r2s4tnz2hfp0q0c3");
```
</Tab>
<Tab value="Python">
```python
executionContext = client.execution_context.get("ectx_01j5q3k7m8r2s4tnz2hfp0q0c3")
```
</Tab>
<Tab value="Java">
```java
ExecutionContext executionContext = client.executionContext().get("ectx_01j5q3k7m8r2s4tnz2hfp0q0c3");
```
</Tab>
</SDKTabs>

## Methods

| Method | Description |
|--------|-------------|
| [`get`](#get) | Get an ExecutionContext by ID. |
| [`getByExecutionId`](#getbyexecutionid) | Get an ExecutionContext by the execution ID it belongs to. |
| [`getByReference`](#getbyreference) | Get an ExecutionContext by reference (slug-based lookup). |
| [`apply`](#apply) | Create or update an ExecutionContext. |
| [`create`](#create) | Create a new ExecutionContext for an execution. |
| [`delete`](#delete) | Delete an ExecutionContext. |

### get

Get an ExecutionContext by ID.

<SDKTabs>
<Tab value="Go">
```go
executionContext, err := client.ExecutionContext.Get(ctx, id)
```
</Tab>
<Tab value="TypeScript">
```typescript
const executionContext = await stigmer.executionContext.get(id);
```
</Tab>
<Tab value="Python">
```python
executionContext = client.execution_context.get(id)
```
</Tab>
<Tab value="Java">
```java
ExecutionContext executionContext = client.executionContext().get(id);
```
</Tab>
</SDKTabs>

**Parameters**

| Parameter | Type | Description |
|-----------|------|-------------|
| `id` | `string` | Execution Context identifier. |

**Returns**: `ExecutionContext`

### getByExecutionId

Get an ExecutionContext by the execution ID it belongs to.

<SDKTabs>
<Tab value="Go">
```go
executionContext, err := client.ExecutionContext.GetByExecutionId(ctx, &stigmer.ExecutionContextExecutionIdInput{
  ExecutionId: "...",
})
```
</Tab>
<Tab value="TypeScript">
```typescript
const executionContext = await stigmer.executionContext.getByExecutionId({
  executionId: "...",
});
```
</Tab>
<Tab value="Python">
```python
executionContext = client.execution_context.get_by_execution_id(ExecutionContextExecutionIdInput(
    execution_id="...",
))
```
</Tab>
<Tab value="Java">
```java
ExecutionContext executionContext = client.executionContext().getByExecutionId(ExecutionContextExecutionIdInput.builder()
    .executionId("...")
    .build());
```
</Tab>
</SDKTabs>

**Parameters**: [`ExecutionContextExecutionIdInput`](#executioncontextexecutionidinput)

**Returns**: `ExecutionContext`

### getByReference

Get an ExecutionContext by reference (slug-based lookup).

<SDKTabs>
<Tab value="Go">
```go
executionContext, err := client.ExecutionContext.GetByReference(ctx, &stigmer.ApiResourceReference{
  Org:     "...",
  Kind:    "...",
  Slug:    "...",
  Version: "...",
})
```
</Tab>
<Tab value="TypeScript">
```typescript
const executionContext = await stigmer.executionContext.getByReference({
  org: "...",
  kind: "...",
  slug: "...",
  version: "...",
});
```
</Tab>
<Tab value="Python">
```python
executionContext = client.execution_context.get_by_reference(ApiResourceReference(
    org="...",
    kind="...",
    slug="...",
    version="...",
))
```
</Tab>
<Tab value="Java">
```java
ExecutionContext executionContext = client.executionContext().getByReference(ApiResourceReference.builder()
    .org("...")
    .kind("...")
    .slug("...")
    .version("...")
    .build());
```
</Tab>
</SDKTabs>

**Parameters**: [`ApiResourceReference`](#apiresourcereference)

**Returns**: `ExecutionContext`

### apply

Create or update an ExecutionContext. The authorization and state-operation are determined depending on whether the execution context is going to be created or updated which is determined as part of the request execution.

<SDKTabs>
<Tab value="Go">
```go
executionContext, err := client.ExecutionContext.Apply(ctx, &stigmer.ExecutionContextInput{
  Name:        "my-execution-context",
  Org:         "acme",
  Slug:        "...",
  Labels:      map[string]string{},
  ExecutionId: "...",
  Data:        map[string]string{},
})
```
</Tab>
<Tab value="TypeScript">
```typescript
const executionContext = await stigmer.executionContext.apply({
  name: "my-execution-context",
  org: "acme",
  slug: "...",
  labels: {},
  executionId: "...",
  data: {},
});
```
</Tab>
<Tab value="Python">
```python
executionContext = client.execution_context.apply(ExecutionContextInput(
    name="my-execution-context",
    org="acme",
    slug="...",
    labels={},
    execution_id="...",
    data={},
))
```
</Tab>
<Tab value="Java">
```java
ExecutionContext executionContext = client.executionContext().apply(ExecutionContextInput.builder()
    .name("my-execution-context")
    .org("acme")
    .slug("...")
    .labels(Map.of())
    .executionId("...")
    .data(Map.of())
    .build());
```
</Tab>
</SDKTabs>

**Parameters**: [`ExecutionContextInput`](#executioncontextinput)

**Returns**: `ExecutionContext`

### create

Create a new ExecutionContext for an execution.

<SDKTabs>
<Tab value="Go">
```go
executionContext, err := client.ExecutionContext.Create(ctx, &stigmer.ExecutionContextInput{
  Name:        "my-execution-context",
  Org:         "acme",
  Slug:        "...",
  Labels:      map[string]string{},
  ExecutionId: "...",
  Data:        map[string]string{},
})
```
</Tab>
<Tab value="TypeScript">
```typescript
const executionContext = await stigmer.executionContext.create({
  name: "my-execution-context",
  org: "acme",
  slug: "...",
  labels: {},
  executionId: "...",
  data: {},
});
```
</Tab>
<Tab value="Python">
```python
executionContext = client.execution_context.create(ExecutionContextInput(
    name="my-execution-context",
    org="acme",
    slug="...",
    labels={},
    execution_id="...",
    data={},
))
```
</Tab>
<Tab value="Java">
```java
ExecutionContext executionContext = client.executionContext().create(ExecutionContextInput.builder()
    .name("my-execution-context")
    .org("acme")
    .slug("...")
    .labels(Map.of())
    .executionId("...")
    .data(Map.of())
    .build());
```
</Tab>
</SDKTabs>

**Parameters**: [`ExecutionContextInput`](#executioncontextinput)

**Returns**: `ExecutionContext`

### delete

Delete an ExecutionContext.

<SDKTabs>
<Tab value="Go">
```go
executionContext, err := client.ExecutionContext.Delete(ctx, &stigmer.DeleteResourceInput{
  ResourceID: id,
})
```
</Tab>
<Tab value="TypeScript">
```typescript
const executionContext = await stigmer.executionContext.delete({ resourceId: id });
```
</Tab>
<Tab value="Python">
```python
executionContext = client.execution_context.delete(DeleteResourceInput(resource_id=id))
```
</Tab>
<Tab value="Java">
```java
ExecutionContext executionContext = client.executionContext().delete(DeleteResourceInput.builder()
    .resourceId(id)
    .build());
```
</Tab>
</SDKTabs>

**Parameters**

| Parameter | Type | Description |
|-----------|------|-------------|
| `resourceId` | `string` | ID of the resource to delete. |
| `versionMessage` | `string` | Optional deletion reason. |
| `force` | `boolean` | Force delete even if resource is in use. |

**Returns**: `ExecutionContext`

## Types

### ExecutionContextInput

Input for creating or updating an Execution Context.

<TypeTable
  type={{
    name: { type: "string", description: "Resource name.", required: true },
    slug: { type: "string", description: "URL-friendly identifier." },
    org: { type: "string", description: "Organization slug.", required: true },
    labels: { type: "Record<string, string>", description: "Key-value labels." },
    executionId: { type: "string", description: "The execution ID this context belongs to." },
    data: { type: "Record<string, EnvVarInput>", description: "Key-value pairs containing both configuration and secrets." },
  }}
/>

### ApiResourceReference

Generic reference to any API resource by org and slug.

<TypeTable
  type={{
    org: { type: "string", description: "Organization that owns the referenced resource." },
    kind: { type: "ApiResourceKind", description: "Kind of the referenced resource (e.g., SKILL, AGENT, MCP_SERVER)." },
    slug: { type: "string", description: "Resource slug (user-friendly identifier, unique within org).", required: true },
    version: { type: "string", description: "Version of the resource (optional, only applicable to versioned resources like Skills)." },
  }}
/>

### ExecutionContextExecutionIdInput

ExecutionContextExecutionIdInput is used to look up an ExecutionContext by the execution ID it belongs to (e.g., AgentExecution ID or WorkflowExecution ID).

<TypeTable
  type={{
    executionId: { type: "string", description: "The workflow or agent execution ID to look up." },
  }}
/>

