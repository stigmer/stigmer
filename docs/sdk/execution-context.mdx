---
title: Execution Context
description: "SDK reference for Execution Context resources."
---

{/* Auto-generated by stigmer-codegen. DO NOT EDIT. */}

This is created during execution and deleted when execution completes.

## Client Access

<SDKTabs>
<Tab value="Go">
```go
executionContext, err := client.ExecutionContext.Get(ctx, "execution-context-abc123")
```
</Tab>
<Tab value="TypeScript">
```typescript
const executionContext = await stigmer.executionContext.get("execution-context-abc123");
```
</Tab>
<Tab value="Python">
```python
executionContext = client.execution_context.get("execution-context-abc123")
```
</Tab>
<Tab value="Java">
```java
ExecutionContext executionContext = client.executionContext().get("execution-context-abc123");
```
</Tab>
</SDKTabs>

## Methods

| Method | Description |
|--------|-------------|
| [`get`](#get) | Get an ExecutionContext by ID. |
| [`getByExecutionId`](#getbyexecutionid) | Get an ExecutionContext by the execution ID it belongs to. |
| [`getByReference`](#getbyreference) | Get an ExecutionContext by reference (slug-based lookup). |
| [`apply`](#apply) | Create or update an ExecutionContext. |
| [`create`](#create) | Create a new ExecutionContext for an execution. |
| [`delete`](#delete) | Delete an ExecutionContext. |

### get

Get an ExecutionContext by ID.

<SDKTabs>
<Tab value="Go">
```go
executionContext, err := client.ExecutionContext.Get(ctx, id)
```
</Tab>
<Tab value="TypeScript">
```typescript
const executionContext = await stigmer.executionContext.get(id);
```
</Tab>
<Tab value="Python">
```python
executionContext = client.execution_context.get(id)
```
</Tab>
<Tab value="Java">
```java
ExecutionContext executionContext = client.executionContext().get(id);
```
</Tab>
</SDKTabs>

**Parameters**

| Parameter | Type | Description |
|-----------|------|-------------|
| `id` | `string` | Execution Context identifier. |

**Returns**: `ExecutionContext`

### getByExecutionId

Get an ExecutionContext by the execution ID it belongs to.

<SDKTabs>
<Tab value="Go">
```go
executionContext, err := client.ExecutionContext.GetByExecutionId(ctx, input)
```
</Tab>
<Tab value="TypeScript">
```typescript
const executionContext = await stigmer.executionContext.getByExecutionId(input);
```
</Tab>
<Tab value="Python">
```python
executionContext = client.execution_context.get_by_execution_id(input)
```
</Tab>
<Tab value="Java">
```java
ExecutionContext executionContext = client.executionContext().getByExecutionId(input);
```
</Tab>
</SDKTabs>

**Parameters**: `ExecutionContextExecutionIdInput`

**Returns**: `ExecutionContext`

### getByReference

Get an ExecutionContext by reference (slug-based lookup).

<SDKTabs>
<Tab value="Go">
```go
executionContext, err := client.ExecutionContext.GetByReference(ctx, input)
```
</Tab>
<Tab value="TypeScript">
```typescript
const executionContext = await stigmer.executionContext.getByReference(input);
```
</Tab>
<Tab value="Python">
```python
executionContext = client.execution_context.get_by_reference(input)
```
</Tab>
<Tab value="Java">
```java
ExecutionContext executionContext = client.executionContext().getByReference(input);
```
</Tab>
</SDKTabs>

**Parameters**: `ApiResourceReference`

**Returns**: `ExecutionContext`

### apply

Create or update an ExecutionContext. The authorization and state-operation are determined depending on whether the execution context is going to be created or updated which is determined as part of the request execution.

<SDKTabs>
<Tab value="Go">
```go
executionContext, err := client.ExecutionContext.Apply(ctx, &stigmer.ExecutionContextInput{
  Name: "my-execution-context",
  Org:  "acme",
})
```
</Tab>
<Tab value="TypeScript">
```typescript
const executionContext = await stigmer.executionContext.apply({
  name: "my-execution-context",
  org: "acme",
});
```
</Tab>
<Tab value="Python">
```python
executionContext = client.execution_context.apply(ExecutionContextInput(
    name="my-execution-context",
    org="acme",
))
```
</Tab>
<Tab value="Java">
```java
ExecutionContext executionContext = client.executionContext().apply(ExecutionContextInput.builder()
    .name("my-execution-context")
    .org("acme")
    .build());
```
</Tab>
</SDKTabs>

**Parameters**: [`ExecutionContextInput`](#executioncontextinput)

**Returns**: `ExecutionContext`

### create

Create a new ExecutionContext for an execution.

<SDKTabs>
<Tab value="Go">
```go
executionContext, err := client.ExecutionContext.Create(ctx, &stigmer.ExecutionContextInput{
  Name: "my-execution-context",
  Org:  "acme",
})
```
</Tab>
<Tab value="TypeScript">
```typescript
const executionContext = await stigmer.executionContext.create({
  name: "my-execution-context",
  org: "acme",
});
```
</Tab>
<Tab value="Python">
```python
executionContext = client.execution_context.create(ExecutionContextInput(
    name="my-execution-context",
    org="acme",
))
```
</Tab>
<Tab value="Java">
```java
ExecutionContext executionContext = client.executionContext().create(ExecutionContextInput.builder()
    .name("my-execution-context")
    .org("acme")
    .build());
```
</Tab>
</SDKTabs>

**Parameters**: [`ExecutionContextInput`](#executioncontextinput)

**Returns**: `ExecutionContext`

### delete

Delete an ExecutionContext.

<SDKTabs>
<Tab value="Go">
```go
executionContext, err := client.ExecutionContext.Delete(ctx, &stigmer.DeleteResourceInput{
  ResourceID: id,
})
```
</Tab>
<Tab value="TypeScript">
```typescript
const executionContext = await stigmer.executionContext.delete({ resourceId: id });
```
</Tab>
<Tab value="Python">
```python
executionContext = client.execution_context.delete(DeleteResourceInput(resource_id=id))
```
</Tab>
<Tab value="Java">
```java
ExecutionContext executionContext = client.executionContext().delete(DeleteResourceInput.builder()
    .resourceId(id)
    .build());
```
</Tab>
</SDKTabs>

**Parameters**

| Parameter | Type | Description |
|-----------|------|-------------|
| `resourceId` | `string` | ID of the resource to delete. |
| `versionMessage` | `string` | Optional deletion reason. |
| `force` | `boolean` | Force delete even if resource is in use. |

**Returns**: `ExecutionContext`

## Types

### ExecutionContextInput

Input for creating or updating an Execution Context.

<TypeTable
  type={{
    name: { type: "string", description: "Resource name.", required: true },
    slug: { type: "string", description: "URL-friendly identifier." },
    org: { type: "string", description: "Organization slug.", required: true },
    labels: { type: "Record<string, string>", description: "Key-value labels." },
    executionId: { type: "string", description: "The execution ID this context belongs to." },
    data: { type: "Record<string, EnvVarInput>", description: "Key-value pairs containing both configuration and secrets." },
  }}
/>

