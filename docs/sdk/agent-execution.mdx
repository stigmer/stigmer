---
title: Agent Execution
description: "SDK reference for Agent Execution resources."
---

{/* Auto-generated by stigmer-codegen. DO NOT EDIT. */}

All execution results and state live in AgentExecutionStatus (in api.proto).

## Client Access

<SDKTabs>
<Tab value="Go">
```go
agentExecution, err := client.AgentExecution.Get(ctx, "agent-execution-abc123")
```
</Tab>
<Tab value="TypeScript">
```typescript
const agentExecution = await stigmer.agentExecution.get("agent-execution-abc123");
```
</Tab>
<Tab value="Python">
```python
agentExecution = client.agent_execution.get("agent-execution-abc123")
```
</Tab>
<Tab value="Java">
```java
AgentExecution agentExecution = client.agentExecution().get("agent-execution-abc123");
```
</Tab>
</SDKTabs>

## Methods

| Method | Description | Streaming |
|--------|-------------|-----------|
| [`get`](#get) | Get a single agent execution by ID. | |
| [`getAgentUsageReport`](#getagentusagereport) | Get usage report for an agent (all sessions for an agent in a time range). | |
| [`getArtifactContent`](#getartifactcontent) | Read the raw content of an execution artifact. | |
| [`getArtifactDownloadUrl`](#getartifactdownloadurl) | Get a presigned download URL for an execution artifact. | |
| [`getOrgUsageReport`](#getorgusagereport) | Get usage report for an organization (all agents in a time range). | |
| [`getSessionUsageReport`](#getsessionusagereport) | Get usage report for a session (all executions in a session). | |
| [`list`](#list) | List all agent executions with pagination and optional filtering. | |
| [`listBySession`](#listbysession) | List all executions in a specific session. | |
| [`subscribe`](#subscribe) | Subscribe to real-time execution updates (streaming). | Server |
| [`create`](#create) | Create and trigger a new agent execution. | |
| [`update`](#update) | Update an execution with full state. | |
| [`updateStatus`](#updatestatus) | Update execution status during agent execution. | |
| [`delete`](#delete) | Delete an execution. | |
| [`pause`](#pause) | Pause a running agent execution. | |
| [`resume`](#resume) | Resume a paused agent execution. | |
| [`cancel`](#cancel) | Cancel a running agent execution gracefully. | |
| [`terminate`](#terminate) | Terminate an agent execution immediately. | |
| [`recover`](#recover) | Recover a failed agent execution from the last checkpoint. | |
| [`submitApproval`](#submitapproval) | Submit approval decision for a pending tool call (HITL Phase 1). | |
| [`uploadAttachment`](#uploadattachment) | Upload a file attachment for use in an agent execution. | |

### get

Get a single agent execution by ID.

<SDKTabs>
<Tab value="Go">
```go
agentExecution, err := client.AgentExecution.Get(ctx, id)
```
</Tab>
<Tab value="TypeScript">
```typescript
const agentExecution = await stigmer.agentExecution.get(id);
```
</Tab>
<Tab value="Python">
```python
agentExecution = client.agent_execution.get(id)
```
</Tab>
<Tab value="Java">
```java
AgentExecution agentExecution = client.agentExecution().get(id);
```
</Tab>
</SDKTabs>

**Parameters**

| Parameter | Type | Description |
|-----------|------|-------------|
| `id` | `string` | Agent Execution identifier. |

**Returns**: `AgentExecution`

### getAgentUsageReport

Get usage report for an agent (all sessions for an agent in a time range). Returns aggregated tokens, cost, and per-session breakdown with pagination.

<SDKTabs>
<Tab value="Go">
```go
getAgentUsageReportOutput, err := client.AgentExecution.GetAgentUsageReport(ctx, input)
```
</Tab>
<Tab value="TypeScript">
```typescript
const getAgentUsageReportOutput = await stigmer.agentExecution.getAgentUsageReport(input);
```
</Tab>
<Tab value="Python">
```python
getAgentUsageReportOutput = client.agent_execution.get_agent_usage_report(input)
```
</Tab>
<Tab value="Java">
```java
GetAgentUsageReportOutput getAgentUsageReportOutput = client.agentExecution().getAgentUsageReport(input);
```
</Tab>
</SDKTabs>

**Parameters**: `GetAgentUsageReportInput`

**Returns**: `GetAgentUsageReportOutput`

### getArtifactContent

Read the raw content of an execution artifact.

Returns artifact bytes through the Stigmer API, eliminating CORS concerns for SDK consumers who need to read content programmatically (e.g., YAML parsing for resource detection, in-app preview rendering).

For direct file downloads, use getArtifactDownloadUrl instead — it returns a presigned R2 URL that avoids proxying bytes through the server.

<SDKTabs>
<Tab value="Go">
```go
getArtifactContentResponse, err := client.AgentExecution.GetArtifactContent(ctx, input)
```
</Tab>
<Tab value="TypeScript">
```typescript
const getArtifactContentResponse = await stigmer.agentExecution.getArtifactContent(input);
```
</Tab>
<Tab value="Python">
```python
getArtifactContentResponse = client.agent_execution.get_artifact_content(input)
```
</Tab>
<Tab value="Java">
```java
GetArtifactContentResponse getArtifactContentResponse = client.agentExecution().getArtifactContent(input);
```
</Tab>
</SDKTabs>

**Parameters**: `GetArtifactContentRequest`

**Returns**: `GetArtifactContentResponse`

### getArtifactDownloadUrl

Get a presigned download URL for an execution artifact.

Returns a time-limited URL for downloading an artifact published by an agent during execution. The URL can be used with a simple HTTP GET request without authentication.

<SDKTabs>
<Tab value="Go">
```go
getArtifactDownloadUrlResponse, err := client.AgentExecution.GetArtifactDownloadUrl(ctx, input)
```
</Tab>
<Tab value="TypeScript">
```typescript
const getArtifactDownloadUrlResponse = await stigmer.agentExecution.getArtifactDownloadUrl(input);
```
</Tab>
<Tab value="Python">
```python
getArtifactDownloadUrlResponse = client.agent_execution.get_artifact_download_url(input)
```
</Tab>
<Tab value="Java">
```java
GetArtifactDownloadUrlResponse getArtifactDownloadUrlResponse = client.agentExecution().getArtifactDownloadUrl(input);
```
</Tab>
</SDKTabs>

**Parameters**: `GetArtifactDownloadUrlRequest`

**Returns**: `GetArtifactDownloadUrlResponse`

### getOrgUsageReport

Get usage report for an organization (all agents in a time range). Returns org-wide totals, top agents by cost, model breakdown, and daily trend.

<SDKTabs>
<Tab value="Go">
```go
getOrgUsageReportOutput, err := client.AgentExecution.GetOrgUsageReport(ctx, input)
```
</Tab>
<Tab value="TypeScript">
```typescript
const getOrgUsageReportOutput = await stigmer.agentExecution.getOrgUsageReport(input);
```
</Tab>
<Tab value="Python">
```python
getOrgUsageReportOutput = client.agent_execution.get_org_usage_report(input)
```
</Tab>
<Tab value="Java">
```java
GetOrgUsageReportOutput getOrgUsageReportOutput = client.agentExecution().getOrgUsageReport(input);
```
</Tab>
</SDKTabs>

**Parameters**: `GetOrgUsageReportInput`

**Returns**: `GetOrgUsageReportOutput`

### getSessionUsageReport

Get usage report for a session (all executions in a session). Returns aggregated tokens, cost, cache hit rate, and per-execution breakdown.

<SDKTabs>
<Tab value="Go">
```go
getSessionUsageReportOutput, err := client.AgentExecution.GetSessionUsageReport(ctx, input)
```
</Tab>
<Tab value="TypeScript">
```typescript
const getSessionUsageReportOutput = await stigmer.agentExecution.getSessionUsageReport(input);
```
</Tab>
<Tab value="Python">
```python
getSessionUsageReportOutput = client.agent_execution.get_session_usage_report(input)
```
</Tab>
<Tab value="Java">
```java
GetSessionUsageReportOutput getSessionUsageReportOutput = client.agentExecution().getSessionUsageReport(input);
```
</Tab>
</SDKTabs>

**Parameters**: `GetSessionUsageReportInput`

**Returns**: `GetSessionUsageReportOutput`

### list

List all agent executions with pagination and optional filtering.

<SDKTabs>
<Tab value="Go">
```go
result, err := client.AgentExecution.List(ctx, input)
```
</Tab>
<Tab value="TypeScript">
```typescript
const result = await stigmer.agentExecution.list(input);
```
</Tab>
<Tab value="Python">
```python
result = client.agent_execution.list(input)
```
</Tab>
<Tab value="Java">
```java
AgentExecutionList result = client.agentExecution().list(input);
```
</Tab>
</SDKTabs>

**Parameters**: `ListAgentExecutionsRequest`

**Returns**: `AgentExecutionList`

### listBySession

List all executions in a specific session.

<SDKTabs>
<Tab value="Go">
```go
result, err := client.AgentExecution.ListBySession(ctx, input)
```
</Tab>
<Tab value="TypeScript">
```typescript
const result = await stigmer.agentExecution.listBySession(input);
```
</Tab>
<Tab value="Python">
```python
result = client.agent_execution.list_by_session(input)
```
</Tab>
<Tab value="Java">
```java
AgentExecutionList result = client.agentExecution().listBySession(input);
```
</Tab>
</SDKTabs>

**Parameters**: `ListAgentExecutionsBySessionRequest`

**Returns**: `AgentExecutionList`

### subscribe

> **Server Streaming** — This method returns a stream of updates, not a single response.

Subscribe to real-time execution updates (streaming).

<SDKTabs>
<Tab value="Go">
```go
stream, err := client.AgentExecution.Subscribe(ctx, id)
for {
    agentExecution, err := stream.Recv()
    if err != nil {
        break
    }
    // process agentExecution
}
```
</Tab>
<Tab value="TypeScript">
```typescript
const stream = stigmer.agentExecution.subscribe(id);
for await (const agentExecution of stream) {
  // process agentExecution
}
```
</Tab>
<Tab value="Python">
```python
for agentExecution in client.agent_execution.subscribe(id):
    # process agentExecution
```
</Tab>
<Tab value="Java">
```java
client.agentExecution().subscribe(id, agentExecution -> {
    // process agentExecution
});
```
</Tab>
</SDKTabs>

**Parameters**

| Parameter | Type | Description |
|-----------|------|-------------|
| `id` | `string` | Agent Execution identifier. |

**Returns**: `Stream<AgentExecution>`

### create

Create and trigger a new agent execution. Session is optional — can be provided or auto-created from agent_id.

<SDKTabs>
<Tab value="Go">
```go
agentExecution, err := client.AgentExecution.Create(ctx, &stigmer.AgentExecutionInput{
  Name: "my-agent-execution",
  Org:  "acme",
})
```
</Tab>
<Tab value="TypeScript">
```typescript
const agentExecution = await stigmer.agentExecution.create({
  name: "my-agent-execution",
  org: "acme",
});
```
</Tab>
<Tab value="Python">
```python
agentExecution = client.agent_execution.create(AgentExecutionInput(
    name="my-agent-execution",
    org="acme",
))
```
</Tab>
<Tab value="Java">
```java
AgentExecution agentExecution = client.agentExecution().create(AgentExecutionInput.builder()
    .name("my-agent-execution")
    .org("acme")
    .build());
```
</Tab>
</SDKTabs>

**Parameters**: [`AgentExecutionInput`](#agentexecutioninput)

**Returns**: `AgentExecution`

### update

Update an execution with full state. Used by users to update execution configuration (spec fields). No individual field updates - always provide complete state.

<SDKTabs>
<Tab value="Go">
```go
agentExecution, err := client.AgentExecution.Update(ctx, &stigmer.AgentExecutionInput{
  Name: "my-agent-execution",
  Org:  "acme",
})
```
</Tab>
<Tab value="TypeScript">
```typescript
const agentExecution = await stigmer.agentExecution.update({
  name: "my-agent-execution",
  org: "acme",
});
```
</Tab>
<Tab value="Python">
```python
agentExecution = client.agent_execution.update(AgentExecutionInput(
    name="my-agent-execution",
    org="acme",
))
```
</Tab>
<Tab value="Java">
```java
AgentExecution agentExecution = client.agentExecution().update(AgentExecutionInput.builder()
    .name("my-agent-execution")
    .org("acme")
    .build());
```
</Tab>
</SDKTabs>

**Parameters**: [`AgentExecutionInput`](#agentexecutioninput)

**Returns**: `AgentExecution`

### updateStatus

Update execution status during agent execution.

<SDKTabs>
<Tab value="Go">
```go
agentExecution, err := client.AgentExecution.UpdateStatus(ctx, input)
```
</Tab>
<Tab value="TypeScript">
```typescript
const agentExecution = await stigmer.agentExecution.updateStatus(input);
```
</Tab>
<Tab value="Python">
```python
agentExecution = client.agent_execution.update_status(input)
```
</Tab>
<Tab value="Java">
```java
AgentExecution agentExecution = client.agentExecution().updateStatus(input);
```
</Tab>
</SDKTabs>

**Parameters**: `AgentExecutionUpdateStatusInput`

**Returns**: `AgentExecution`

### delete

Delete an execution.

<SDKTabs>
<Tab value="Go">
```go
agentExecution, err := client.AgentExecution.Delete(ctx, id)
```
</Tab>
<Tab value="TypeScript">
```typescript
const agentExecution = await stigmer.agentExecution.delete(id);
```
</Tab>
<Tab value="Python">
```python
agentExecution = client.agent_execution.delete(id)
```
</Tab>
<Tab value="Java">
```java
AgentExecution agentExecution = client.agentExecution().delete(id);
```
</Tab>
</SDKTabs>

**Parameters**

| Parameter | Type | Description |
|-----------|------|-------------|
| `id` | `string` | Agent Execution identifier. |

**Returns**: `AgentExecution`

### pause

Pause a running agent execution.

Temporarily stops the agent at its current checkpoint. Unlike cancel, the execution is NOT terminal and can be resumed later from where it left off.

<SDKTabs>
<Tab value="Go">
```go
agentExecution, err := client.AgentExecution.Pause(ctx, input)
```
</Tab>
<Tab value="TypeScript">
```typescript
const agentExecution = await stigmer.agentExecution.pause(input);
```
</Tab>
<Tab value="Python">
```python
agentExecution = client.agent_execution.pause(input)
```
</Tab>
<Tab value="Java">
```java
AgentExecution agentExecution = client.agentExecution().pause(input);
```
</Tab>
</SDKTabs>

**Parameters**: `PauseAgentExecutionInput`

**Returns**: `AgentExecution`

### resume

Resume a paused agent execution.

Continues execution from the checkpoint where it was paused. The agent re-invokes with the same thread_id, loading from LangGraph checkpoint and continuing from where it left off.

<SDKTabs>
<Tab value="Go">
```go
agentExecution, err := client.AgentExecution.Resume(ctx, input)
```
</Tab>
<Tab value="TypeScript">
```typescript
const agentExecution = await stigmer.agentExecution.resume(input);
```
</Tab>
<Tab value="Python">
```python
agentExecution = client.agent_execution.resume(input)
```
</Tab>
<Tab value="Java">
```java
AgentExecution agentExecution = client.agentExecution().resume(input);
```
</Tab>
</SDKTabs>

**Parameters**: `ResumeAgentExecutionInput`

**Returns**: `AgentExecution`

### cancel

Cancel a running agent execution gracefully.

Sends a cancellation signal to the agent execution. The agent can handle the cancellation signal to save checkpoint and clean up before transitioning to the CANCELLED phase.

<SDKTabs>
<Tab value="Go">
```go
agentExecution, err := client.AgentExecution.Cancel(ctx, input)
```
</Tab>
<Tab value="TypeScript">
```typescript
const agentExecution = await stigmer.agentExecution.cancel(input);
```
</Tab>
<Tab value="Python">
```python
agentExecution = client.agent_execution.cancel(input)
```
</Tab>
<Tab value="Java">
```java
AgentExecution agentExecution = client.agentExecution().cancel(input);
```
</Tab>
</SDKTabs>

**Parameters**: `CancelAgentExecutionInput`

**Returns**: `AgentExecution`

### terminate

Terminate an agent execution immediately.

Force-stops the agent execution without allowing cleanup. Unlike cancel, the agent cannot respond to termination - it is stopped immediately. Use this for stuck or unresponsive agents.

<SDKTabs>
<Tab value="Go">
```go
agentExecution, err := client.AgentExecution.Terminate(ctx, input)
```
</Tab>
<Tab value="TypeScript">
```typescript
const agentExecution = await stigmer.agentExecution.terminate(input);
```
</Tab>
<Tab value="Python">
```python
agentExecution = client.agent_execution.terminate(input)
```
</Tab>
<Tab value="Java">
```java
AgentExecution agentExecution = client.agentExecution().terminate(input);
```
</Tab>
</SDKTabs>

**Parameters**: `TerminateAgentExecutionInput`

**Returns**: `AgentExecution`

### recover

Recover a failed agent execution from the last checkpoint.

Resumes execution from the last checkpoint. Completed work is preserved - successful tool calls are NOT re-executed.

<SDKTabs>
<Tab value="Go">
```go
agentExecution, err := client.AgentExecution.Recover(ctx, input)
```
</Tab>
<Tab value="TypeScript">
```typescript
const agentExecution = await stigmer.agentExecution.recover(input);
```
</Tab>
<Tab value="Python">
```python
agentExecution = client.agent_execution.recover(input)
```
</Tab>
<Tab value="Java">
```java
AgentExecution agentExecution = client.agentExecution().recover(input);
```
</Tab>
</SDKTabs>

**Parameters**: `RecoverAgentExecutionInput`

**Returns**: `AgentExecution`

### submitApproval

Submit approval decision for a pending tool call (HITL Phase 1).

<SDKTabs>
<Tab value="Go">
```go
agentExecution, err := client.AgentExecution.SubmitApproval(ctx, input)
```
</Tab>
<Tab value="TypeScript">
```typescript
const agentExecution = await stigmer.agentExecution.submitApproval(input);
```
</Tab>
<Tab value="Python">
```python
agentExecution = client.agent_execution.submit_approval(input)
```
</Tab>
<Tab value="Java">
```java
AgentExecution agentExecution = client.agentExecution().submitApproval(input);
```
</Tab>
</SDKTabs>

**Parameters**: `SubmitApprovalInput`

**Returns**: `AgentExecution`

### uploadAttachment

Upload a file attachment for use in an agent execution.

Pre-uploads files to artifact storage before creating an execution. The returned storage_key can be used in Attachment.storage_key when creating the execution.

<SDKTabs>
<Tab value="Go">
```go
uploadAttachmentResponse, err := client.AgentExecution.UploadAttachment(ctx, input)
```
</Tab>
<Tab value="TypeScript">
```typescript
const uploadAttachmentResponse = await stigmer.agentExecution.uploadAttachment(input);
```
</Tab>
<Tab value="Python">
```python
uploadAttachmentResponse = client.agent_execution.upload_attachment(input)
```
</Tab>
<Tab value="Java">
```java
UploadAttachmentResponse uploadAttachmentResponse = client.agentExecution().uploadAttachment(input);
```
</Tab>
</SDKTabs>

**Parameters**: `UploadAttachmentRequest`

**Returns**: `UploadAttachmentResponse`

## Types

### AgentExecutionInput

Input for creating or updating an Agent Execution.

<TypeTable
  type={{
    name: { type: "string", description: "Resource name.", required: true },
    slug: { type: "string", description: "URL-friendly identifier." },
    org: { type: "string", description: "Organization slug.", required: true },
    labels: { type: "Record<string, string>", description: "Key-value labels." },
    sessionId: { type: "string", description: "Session ID this execution belongs to (optional)." },
    agentId: { type: "string", description: "Agent ID (optional)." },
    message: { type: "string", description: "User input message that triggers this execution." },
    executionConfig: { type: "ExecutionConfigInput", description: "Optional execution-time configuration overrides." },
    runtimeEnv: { type: "Record<string, EnvVarInput>", description: "Runtime environment variables and secrets (execution-scoped)." },
    callbackToken: { type: "Uint8Array", description: "Callback token for async activity completion (optional)." },
    autoApproveAll: { type: "boolean", description: "Auto-approve all tool executions for this execution." },
    parentWorkflowId: { type: "string", description: "Parent workflow context for events-based approval notification (optional)." },
    attachments: { type: "AttachmentInput[]", description: "Files attached to this execution." },
    workspaceFileRefs: { type: "string[]", description: "Workspace-relative file paths the user wants the agent to focus on." },
  }}
/>

### ExecutionConfigInput

Configuration that can be applied at execution time.

<TypeTable
  type={{
    modelName: { type: "string", description: "The model to use for this execution." },
    contextManagement: { type: "ContextManagementConfigInput", description: "Context management configuration for this execution." },
    maxToolRounds: { type: "number", description: "Maximum number of model-to-tools reasoning cycles per message." },
    maxToolResultChars: { type: "number", description: "Maximum number of characters for a single tool result before truncation." },
    maxCostUsd: { type: "number", description: "Maximum estimated cost in USD for this execution." },
  }}
/>

### ContextManagementConfigInput

ContextManagementConfig controls automatic context summarization behavior.

<TypeTable
  type={{
    disableSummarization: { type: "boolean", description: "Disable automatic context summarization for this execution." },
    customTriggerThreshold: { type: "number", description: "Custom token threshold to trigger summarization." },
    customTargetTokens: { type: "number", description: "Custom target token count after summarization." },
  }}
/>

### AttachmentInput

Attachment represents a file attached to an agent execution.

<TypeTable
  type={{
    filename: { type: "string", description: "Original filename for display and default mount path derivation." },
    storageKey: { type: "string", description: "Reference to pre-uploaded file in artifact store (required)." },
    mountPath: { type: "string", description: "Path in sandbox where file will be placed." },
    contentType: { type: "string", description: "MIME type for content negotiation (optional)." },
    extract: { type: "boolean", description: "When true, the attachment is a zip archive that should be extracted at mount_path rather than written as a single file." },
    localPath: { type: "string", description: "Absolute filesystem path to the original file on the CLI host (optional)." },
  }}
/>

