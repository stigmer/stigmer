---
title: Workflow
description: "SDK reference for Workflow resources."
---

{/* Auto-generated by stigmer-codegen. DO NOT EDIT. */}

WorkflowSpec defines the complete specification of a workflow. Follows the
"kind + Struct" pattern from CloudResource (Planton Cloud).

This replaces the old `synthesized_yaml` field with structured proto
definitions. Each workflow task uses WorkflowTaskKind enum +
google.protobuf.Struct for configuration, providing maximum flexibility and
extensibility.

## Client Access

<SDKTabs>
  <Tab value="Go">
    ```go workflow, err := client.Workflow.Get(ctx, "workflow-id") ```
  </Tab>
  <Tab value="TypeScript">
    ```typescript const workflow = await stigmer.workflow.get("workflow-id");
    ```
  </Tab>
  <Tab value="Python">
    ```python workflow = client.workflow.get("workflow-id") ```
  </Tab>
  <Tab value="Java">
    ```java Workflow workflow = client.workflow().get("workflow-id"); ```
  </Tab>
</SDKTabs>

## Methods

### apply

Create or update a workflow. The authorization and state-operation are
determined depending on whether the workflow is going to be created or updated
which is determined as part of the request execution.

<SDKTabs>
<Tab value="Go">
```go
workflow, err := client.Workflow.Apply(ctx, &stigmer.WorkflowInput{
  Name: "my-workflow",
  Org:  "acme",
})
```
</Tab>
<Tab value="TypeScript">
```typescript
const workflow = await stigmer.workflow.apply({
  name: "my-workflow",
  org: "acme",
});
```
</Tab>
<Tab value="Python">
```python
workflow = client.workflow.apply(WorkflowInput(
    name="my-workflow",
    org="acme",
))
```
</Tab>
<Tab value="Java">
```java
Workflow workflow = client.workflow().apply(WorkflowInput.builder()
    .name("my-workflow")
    .org("acme")
    .build());
```
</Tab>
</SDKTabs>

**Parameters**: [`WorkflowInput`](#workflowinput)

**Returns**: `Workflow`

### create

Create a new workflow.

Authorization: - Organization-scoped workflows: Caller must have
can_create_workflow permission in the organization - Platform-scoped workflows:
Caller must be a platform operator (handled automatically by common auth step)

<SDKTabs>
<Tab value="Go">
```go
workflow, err := client.Workflow.Create(ctx, &stigmer.WorkflowInput{
  Name: "my-workflow",
  Org:  "acme",
})
```
</Tab>
<Tab value="TypeScript">
```typescript
const workflow = await stigmer.workflow.create({
  name: "my-workflow",
  org: "acme",
});
```
</Tab>
<Tab value="Python">
```python
workflow = client.workflow.create(WorkflowInput(
    name="my-workflow",
    org="acme",
))
```
</Tab>
<Tab value="Java">
```java
Workflow workflow = client.workflow().create(WorkflowInput.builder()
    .name("my-workflow")
    .org("acme")
    .build());
```
</Tab>
</SDKTabs>

**Parameters**: [`WorkflowInput`](#workflowinput)

**Returns**: `Workflow`

### update

Update an existing workflow.

<SDKTabs>
<Tab value="Go">
```go
workflow, err := client.Workflow.Update(ctx, &stigmer.WorkflowInput{
  Name: "my-workflow",
  Org:  "acme",
})
```
</Tab>
<Tab value="TypeScript">
```typescript
const workflow = await stigmer.workflow.update({
  name: "my-workflow",
  org: "acme",
});
```
</Tab>
<Tab value="Python">
```python
workflow = client.workflow.update(WorkflowInput(
    name="my-workflow",
    org="acme",
))
```
</Tab>
<Tab value="Java">
```java
Workflow workflow = client.workflow().update(WorkflowInput.builder()
    .name("my-workflow")
    .org("acme")
    .build());
```
</Tab>
</SDKTabs>

**Parameters**: [`WorkflowInput`](#workflowinput)

**Returns**: `Workflow`

### delete

Delete a workflow.

<SDKTabs>
  <Tab value="Go">
    ```go workflow, err := client.Workflow.Delete(ctx, id) ```
  </Tab>
  <Tab value="TypeScript">
    ```typescript const workflow = await stigmer.workflow.delete(id); ```
  </Tab>
  <Tab value="Python">```python workflow = client.workflow.delete(id) ```</Tab>
  <Tab value="Java">
    ```java Workflow workflow = client.workflow().delete(id); ```
  </Tab>
</SDKTabs>

**Parameters**

| Parameter | Type     | Description          |
| --------- | -------- | -------------------- |
| `id`      | `string` | Workflow identifier. |

**Returns**: `Workflow`

### get

Get a single workflow by ID.

<SDKTabs>
  <Tab value="Go">```go workflow, err := client.Workflow.Get(ctx, id) ```</Tab>
  <Tab value="TypeScript">
    ```typescript const workflow = await stigmer.workflow.get(id); ```
  </Tab>
  <Tab value="Python">```python workflow = client.workflow.get(id) ```</Tab>
  <Tab value="Java">
    ```java Workflow workflow = client.workflow().get(id); ```
  </Tab>
</SDKTabs>

**Parameters**

| Parameter | Type     | Description          |
| --------- | -------- | -------------------- |
| `id`      | `string` | Workflow identifier. |

**Returns**: `Workflow`

### getByReference

Custom authorization in handler

<SDKTabs>
  <Tab value="Go">
    ```go workflow, err := client.Workflow.GetByReference(ctx, input) ```
  </Tab>
  <Tab value="TypeScript">
    ```typescript const workflow = await stigmer.workflow.getByReference(input);
    ```
  </Tab>
  <Tab value="Python">
    ```python workflow = client.workflow.get_by_reference(input) ```
  </Tab>
  <Tab value="Java">
    ```java Workflow workflow = client.workflow().getByReference(input); ```
  </Tab>
</SDKTabs>

**Parameters**: `ApiResourceReference`

**Returns**: `Workflow`

## Types

### WorkflowInput

Input for creating or updating a Workflow.

<TypeTable
  type={{
    name: { type: "string", description: "Resource name.", required: true },
    slug: { type: "string", description: "URL-friendly identifier." },
    org: { type: "string", description: "Organization slug.", required: true },
    labels: {
      type: "Record<string, string>",
      description: "Key-value labels.",
    },
    description: {
      type: "string",
      description: "Human-readable description for UI and marketplace display.",
    },
    document: {
      type: "WorkflowDocumentInput",
      description:
        "Workflow document metadata (DSL version, namespace, name, version).",
      required: true,
    },
    tasks: {
      type: "WorkflowTaskInput[]",
      description: "Ordered list of tasks that make up this workflow.",
    },
    envSpec: {
      type: "EnvSpecInput",
      description: "Environment variables required by the workflow.",
    },
  }}
/>

### WorkflowDocumentInput

WorkflowDocument contains workflow metadata.

<TypeTable
  type={{
    dsl: { type: "string", description: "DSL version (semver)." },
    namespace: {
      type: "string",
      description: "Workflow namespace (organization/categorization).",
      required: true,
    },
    name: {
      type: "string",
      description: "Workflow name (unique identifier within namespace).",
      required: true,
    },
    version: {
      type: "string",
      description: "Workflow version (semver).",
      required: true,
    },
    description: { type: "string", description: "Human-readable description." },
  }}
/>

### WorkflowTaskInput

WorkflowTask represents a single task in the workflow.

<TypeTable
  type={{
    name: {
      type: "string",
      description: "Task name/identifier (must be unique within workflow).",
      required: true,
    },
    kind: {
      type: "WorkflowTaskKind",
      description: "Task type (determines how to interpret task_config).",
      required: true,
    },
    taskConfig: {
      type: "object",
      description: "Task-specific configuration (dynamic typed).",
      required: true,
    },
    export: {
      type: "ExportInput",
      description: "Export configuration (how to save task output to context).",
    },
    flow: {
      type: "FlowControlInput",
      description: "Flow control (which task executes next).",
    },
  }}
/>

### ExportInput

Export defines how to save task output to context.

<TypeTable
  type={{
    as: {
      type: "string",
      description: "Expression defining how to export output.",
    },
  }}
/>

### FlowControlInput

FlowControl defines which task executes next.

<TypeTable
  type={{
    then: {
      type: "string",
      description: 'Target task name or "end" to terminate workflow.',
    },
  }}
/>
