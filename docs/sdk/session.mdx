---
title: Session
description: "SDK reference for Session resources."
---

{/* Auto-generated by stigmer-codegen. DO NOT EDIT. */}

This is the "Execution" layer - ephemeral runtime against an AgentInstance.

## Client Access

<SDKTabs>
<Tab value="TypeScript">
```typescript
const session = await stigmer.session.get("ses_01j5q3k7m8r2s4tnz2hfp0q0c3");
```
</Tab>
<Tab value="Go">
```go
session, err := client.Session.Get(ctx, "ses_01j5q3k7m8r2s4tnz2hfp0q0c3")
```
</Tab>
<Tab value="Python">
```python
session = client.session.get("ses_01j5q3k7m8r2s4tnz2hfp0q0c3")
```
</Tab>
<Tab value="Java">
```java
Session session = client.session().get("ses_01j5q3k7m8r2s4tnz2hfp0q0c3");
```
</Tab>
</SDKTabs>

## Methods

| Method | Description |
|--------|-------------|
| [`get`](#get) | Get a single session by ID. |
| [`list`](#list) | List all sessions with pagination and optional filtering. |
| [`listByAgent`](#listbyagent) | List all sessions for a specific agent. |
| [`apply`](#apply) | Create or update a session. |
| [`create`](#create) | Create a new session for an agent. |
| [`update`](#update) | Update an existing session (e.g., subject, thread_id, sandbox_id). |
| [`updateSandboxId`](#updatesandboxid) | Set the session sandbox ID. |
| [`updateSubject`](#updatesubject) | Set the session subject. |
| [`delete`](#delete) | Delete a session (also cleans up thread and sandbox). |

### get

Get a single session by ID.

<SDKTabs>
<Tab value="TypeScript">
```typescript
const session = await stigmer.session.get(id);
```
</Tab>
<Tab value="Go">
```go
session, err := client.Session.Get(ctx, id)
```
</Tab>
<Tab value="Python">
```python
session = client.session.get(id)
```
</Tab>
<Tab value="Java">
```java
Session session = client.session().get(id);
```
</Tab>
</SDKTabs>

**Parameters**

| Parameter | Type | Description |
|-----------|------|-------------|
| `id` | `string` | Session identifier. |

**Returns**: [`Session`](#session)

### list

List all sessions with pagination and optional filtering.

<SDKTabs>
<Tab value="TypeScript">
```typescript
const result = await stigmer.session.list({
  pageSize: 0,
  pageToken: "...",
  tags: [],
});
```
</Tab>
<Tab value="Go">
```go
result, err := client.Session.List(ctx, &stigmer.ListSessionsRequest{
  PageSize:  0,
  PageToken: "...",
  Tags:      []string{},
})
```
</Tab>
<Tab value="Python">
```python
result = client.session.list(ListSessionsRequest(
    page_size=0,
    page_token="...",
    tags=[],
))
```
</Tab>
<Tab value="Java">
```java
SessionList result = client.session().list(ListSessionsRequest.builder()
    .pageSize(0)
    .pageToken("...")
    .tags(List.of())
    .build());
```
</Tab>
</SDKTabs>

**Parameters**: [`ListSessionsRequest`](#listsessionsrequest)

**Returns**: [`SessionList`](#sessionlist)

### listByAgent

List all sessions for a specific agent.

<SDKTabs>
<Tab value="TypeScript">
```typescript
const result = await stigmer.session.listByAgent({
  agentId: "...",
  pageSize: 0,
  pageToken: "...",
});
```
</Tab>
<Tab value="Go">
```go
result, err := client.Session.ListByAgent(ctx, &stigmer.ListSessionsByAgentRequest{
  AgentId:   "...",
  PageSize:  0,
  PageToken: "...",
})
```
</Tab>
<Tab value="Python">
```python
result = client.session.list_by_agent(ListSessionsByAgentRequest(
    agent_id="...",
    page_size=0,
    page_token="...",
))
```
</Tab>
<Tab value="Java">
```java
SessionList result = client.session().listByAgent(ListSessionsByAgentRequest.builder()
    .agentId("...")
    .pageSize(0)
    .pageToken("...")
    .build());
```
</Tab>
</SDKTabs>

**Parameters**: [`ListSessionsByAgentRequest`](#listsessionsbyagentrequest)

**Returns**: [`SessionList`](#sessionlist)

### apply

Create or update a session. The authorization and state-operation are determined depending on whether the session is going to be created or updated which is determined as part of the request execution.

<SDKTabs>
<Tab value="TypeScript">
```typescript
const session = await stigmer.session.apply({
  name: "my-session",
  org: "acme",
  slug: "...",
  labels: {},
  agentInstanceId: "...",
  subject: "...",
  threadId: "...",
  sandboxId: "...",
  metadata: {},
  workspaceEntries: [],
  mcpServerUsages: [],
  skillRefs: [],
});
```
</Tab>
<Tab value="Go">
```go
session, err := client.Session.Apply(ctx, &stigmer.SessionInput{
  Name:             "my-session",
  Org:              "acme",
  Slug:             "...",
  Labels:           map[string]string{},
  AgentInstanceId:  "...",
  Subject:          "...",
  ThreadId:         "...",
  SandboxId:        "...",
  Metadata:         map[string]string{},
  WorkspaceEntries: []stigmer.WorkspaceEntryInput{},
  McpServerUsages:  []stigmer.McpServerUsageInput{},
  SkillRefs:        []stigmer.ResourceRef{},
})
```
</Tab>
<Tab value="Python">
```python
session = client.session.apply(SessionInput(
    name="my-session",
    org="acme",
    slug="...",
    labels={},
    agent_instance_id="...",
    subject="...",
    thread_id="...",
    sandbox_id="...",
    metadata={},
    workspace_entries=[],
    mcp_server_usages=[],
    skill_refs=[],
))
```
</Tab>
<Tab value="Java">
```java
Session session = client.session().apply(SessionInput.builder()
    .name("my-session")
    .org("acme")
    .slug("...")
    .labels(Map.of())
    .agentInstanceId("...")
    .subject("...")
    .threadId("...")
    .sandboxId("...")
    .metadata(Map.of())
    .workspaceEntries(List.of())
    .mcpServerUsages(List.of())
    .skillRefs(List.of())
    .build());
```
</Tab>
</SDKTabs>

**Parameters**: [`SessionInput`](#sessioninput)

**Returns**: [`Session`](#session)

### create

Create a new session for an agent. Requires can_create_session permission in the organization.

<SDKTabs>
<Tab value="TypeScript">
```typescript
const session = await stigmer.session.create({
  name: "my-session",
  org: "acme",
  slug: "...",
  labels: {},
  agentInstanceId: "...",
  subject: "...",
  threadId: "...",
  sandboxId: "...",
  metadata: {},
  workspaceEntries: [],
  mcpServerUsages: [],
  skillRefs: [],
});
```
</Tab>
<Tab value="Go">
```go
session, err := client.Session.Create(ctx, &stigmer.SessionInput{
  Name:             "my-session",
  Org:              "acme",
  Slug:             "...",
  Labels:           map[string]string{},
  AgentInstanceId:  "...",
  Subject:          "...",
  ThreadId:         "...",
  SandboxId:        "...",
  Metadata:         map[string]string{},
  WorkspaceEntries: []stigmer.WorkspaceEntryInput{},
  McpServerUsages:  []stigmer.McpServerUsageInput{},
  SkillRefs:        []stigmer.ResourceRef{},
})
```
</Tab>
<Tab value="Python">
```python
session = client.session.create(SessionInput(
    name="my-session",
    org="acme",
    slug="...",
    labels={},
    agent_instance_id="...",
    subject="...",
    thread_id="...",
    sandbox_id="...",
    metadata={},
    workspace_entries=[],
    mcp_server_usages=[],
    skill_refs=[],
))
```
</Tab>
<Tab value="Java">
```java
Session session = client.session().create(SessionInput.builder()
    .name("my-session")
    .org("acme")
    .slug("...")
    .labels(Map.of())
    .agentInstanceId("...")
    .subject("...")
    .threadId("...")
    .sandboxId("...")
    .metadata(Map.of())
    .workspaceEntries(List.of())
    .mcpServerUsages(List.of())
    .skillRefs(List.of())
    .build());
```
</Tab>
</SDKTabs>

**Parameters**: [`SessionInput`](#sessioninput)

**Returns**: [`Session`](#session)

### update

Update an existing session (e.g., subject, thread_id, sandbox_id).

<SDKTabs>
<Tab value="TypeScript">
```typescript
const session = await stigmer.session.update({
  name: "my-session",
  org: "acme",
  slug: "...",
  labels: {},
  agentInstanceId: "...",
  subject: "...",
  threadId: "...",
  sandboxId: "...",
  metadata: {},
  workspaceEntries: [],
  mcpServerUsages: [],
  skillRefs: [],
});
```
</Tab>
<Tab value="Go">
```go
session, err := client.Session.Update(ctx, &stigmer.SessionInput{
  Name:             "my-session",
  Org:              "acme",
  Slug:             "...",
  Labels:           map[string]string{},
  AgentInstanceId:  "...",
  Subject:          "...",
  ThreadId:         "...",
  SandboxId:        "...",
  Metadata:         map[string]string{},
  WorkspaceEntries: []stigmer.WorkspaceEntryInput{},
  McpServerUsages:  []stigmer.McpServerUsageInput{},
  SkillRefs:        []stigmer.ResourceRef{},
})
```
</Tab>
<Tab value="Python">
```python
session = client.session.update(SessionInput(
    name="my-session",
    org="acme",
    slug="...",
    labels={},
    agent_instance_id="...",
    subject="...",
    thread_id="...",
    sandbox_id="...",
    metadata={},
    workspace_entries=[],
    mcp_server_usages=[],
    skill_refs=[],
))
```
</Tab>
<Tab value="Java">
```java
Session session = client.session().update(SessionInput.builder()
    .name("my-session")
    .org("acme")
    .slug("...")
    .labels(Map.of())
    .agentInstanceId("...")
    .subject("...")
    .threadId("...")
    .sandboxId("...")
    .metadata(Map.of())
    .workspaceEntries(List.of())
    .mcpServerUsages(List.of())
    .skillRefs(List.of())
    .build());
```
</Tab>
</SDKTabs>

**Parameters**: [`SessionInput`](#sessioninput)

**Returns**: [`Session`](#session)

### updateSandboxId

Set the session sandbox ID.

This is a targeted update that modifies only the sandbox ID field, leaving other session fields untouched. Use this instead of the full update RPC when you only need to change the sandbox assignment.

<SDKTabs>
<Tab value="TypeScript">
```typescript
const session = await stigmer.session.updateSandboxId({
  id: "...",
  sandboxId: "...",
});
```
</Tab>
<Tab value="Go">
```go
session, err := client.Session.UpdateSandboxId(ctx, &stigmer.UpdateSessionSandboxIdRequest{
  Id:        "...",
  SandboxId: "...",
})
```
</Tab>
<Tab value="Python">
```python
session = client.session.update_sandbox_id(UpdateSessionSandboxIdRequest(
    id="...",
    sandbox_id="...",
))
```
</Tab>
<Tab value="Java">
```java
Session session = client.session().updateSandboxId(UpdateSessionSandboxIdRequest.builder()
    .id("...")
    .sandboxId("...")
    .build());
```
</Tab>
</SDKTabs>

**Parameters**: [`UpdateSessionSandboxIdRequest`](#updatesessionsandboxidrequest)

**Returns**: [`Session`](#session)

### updateSubject

Set the session subject.

This is a targeted update that modifies only the subject field, leaving other session fields untouched. Use this instead of the full update RPC when you only need to change the session subject.

<SDKTabs>
<Tab value="TypeScript">
```typescript
const session = await stigmer.session.updateSubject({
  id: "...",
  subject: "...",
});
```
</Tab>
<Tab value="Go">
```go
session, err := client.Session.UpdateSubject(ctx, &stigmer.UpdateSessionSubjectRequest{
  Id:      "...",
  Subject: "...",
})
```
</Tab>
<Tab value="Python">
```python
session = client.session.update_subject(UpdateSessionSubjectRequest(
    id="...",
    subject="...",
))
```
</Tab>
<Tab value="Java">
```java
Session session = client.session().updateSubject(UpdateSessionSubjectRequest.builder()
    .id("...")
    .subject("...")
    .build());
```
</Tab>
</SDKTabs>

**Parameters**: [`UpdateSessionSubjectRequest`](#updatesessionsubjectrequest)

**Returns**: [`Session`](#session)

### delete

Delete a session (also cleans up thread and sandbox).

<SDKTabs>
<Tab value="TypeScript">
```typescript
const session = await stigmer.session.delete(id);
```
</Tab>
<Tab value="Go">
```go
session, err := client.Session.Delete(ctx, id)
```
</Tab>
<Tab value="Python">
```python
session = client.session.delete(id)
```
</Tab>
<Tab value="Java">
```java
Session session = client.session().delete(id);
```
</Tab>
</SDKTabs>

**Parameters**

| Parameter | Type | Description |
|-----------|------|-------------|
| `id` | `string` | Session identifier. |

**Returns**: [`Session`](#session)

## Types

### SessionInput

Input for creating or updating a Session.

<TypeTable
  type={{
    name: { type: "string", description: "Resource name.", required: true },
    slug: { type: "string", description: "URL-friendly identifier." },
    org: { type: "string", description: "Organization slug.", required: true },
    labels: { type: "Record<string, string>", description: "Key-value labels." },
    agentInstanceId: { type: "string", description: "ID of the AgentInstance this session runs against." },
    subject: { type: "string", description: "Conversation title/subject for UI display (optional)." },
    threadId: { type: "string", description: "thread ID (generated on first execution, persists across all executions)." },
    sandboxId: { type: "string", description: "Daytona sandbox ID (created on first execution, reused for file persistence)." },
    metadata: { type: "Record<string, string>", description: "Session metadata (e.g., client info, tags)." },
    workspaceEntries: { type: "WorkspaceEntryInput[]", description: "Workspace entries for this session (optional, may be empty).", typeDescriptionLink: "#workspaceentryinput" },
    mcpServerUsages: { type: "McpServerUsageInput[]", description: "MCP servers to make available in this session (merged with agent's at execution time).", typeDescriptionLink: "#mcpserverusageinput" },
    skillRefs: { type: "ResourceRef[]", description: "Skills to inject into this session's context (merged with agent's at execution time).", typeDescriptionLink: "#resourceref" },
  }}
/>

### WorkspaceEntryInput

WorkspaceEntry pairs a WorkspaceSource with a human-readable name, forming an addressable unit within a session's workspace.

<TypeTable
  type={{
    name: { type: "string", description: "Short identifier for this workspace entry (required)." },
    source: { type: "WorkspaceSourceInput", description: "The source that provides this entry's content (required).", typeDescriptionLink: "#workspacesourceinput", required: true },
  }}
/>

### WorkspaceSourceInput

WorkspaceSource defines where the agent's workspace content comes from.

<TypeTable
  type={{
    gitRepo: { type: "GitRepoSourceInput", description: "", typeDescriptionLink: "#gitreposourceinput" },
    localPath: { type: "LocalPathSourceInput", description: "", typeDescriptionLink: "#localpathsourceinput" },
  }}
/>

### GitRepoSourceInput

GitRepoSource provisions a workspace by cloning a git repository.

<TypeTable
  type={{
    url: { type: "string", description: "HTTPS clone URL (required).", required: true },
    branch: { type: "string", description: "Branch to clone (optional)." },
    commit: { type: "string", description: "Commit SHA to checkout after cloning (optional)." },
    depth: { type: "number", description: "Clone depth (optional)." },
    writeBackMode: { type: "GitWriteBackMode", description: "Controls whether the platform automatically creates a branch and pull request from the agent's file changes during execution.", typeDescriptionLink: "#gitwritebackmode" },
  }}
/>

### LocalPathSourceInput

LocalPathSource uses an existing directory on the host filesystem as the workspace.

<TypeTable
  type={{
    path: { type: "string", description: "Absolute path to an existing directory on the host filesystem (required)." },
  }}
/>

### McpServerUsageInput

McpServerUsage declares that this agent uses a McpServer resource.

<TypeTable
  type={{
    mcpServerRef: { type: "ResourceRef", description: "Reference to the McpServer resource.", typeDescriptionLink: "#resourceref", required: true },
    enabledTools: { type: "string[]", description: "Tools to enable from this MCP server for this agent." },
    toolApprovalOverrides: { type: "ToolApprovalOverrideInput[]", description: "Override approval requirements for specific tools.", typeDescriptionLink: "#toolapprovaloverrideinput" },
  }}
/>

### ResourceRef

Generic reference to any API resource by org and slug.

<TypeTable
  type={{
    org: { type: "string", description: "Organization that owns the referenced resource." },
    kind: { type: "ApiResourceKind", description: "Kind of the referenced resource (e.g., SKILL, AGENT, MCP_SERVER).", typeDescriptionLink: "#apiresourcekind" },
    slug: { type: "string", description: "Resource slug (user-friendly identifier, unique within org).", required: true },
    version: { type: "string", description: "Version of the resource (optional, only applicable to versioned resources like Skills)." },
  }}
/>

### ToolApprovalOverrideInput

ToolApprovalOverride allows per-agent customization of approval requirements.

<TypeTable
  type={{
    toolName: { type: "string", description: "Name of the tool to override." },
    requiresApproval: { type: "boolean", description: "Whether this tool requires approval for this agent." },
    message: { type: "string", description: "Custom approval message shown to the reviewer." },
  }}
/>

### UpdateSessionSubjectRequest

UpdateSessionSubjectRequest sets the conversation title for a session.

<TypeTable
  type={{
    id: { type: "string", description: "Session ID to update.", required: true },
    subject: { type: "string", description: "New subject value." },
  }}
/>

### UpdateSessionSandboxIdRequest

UpdateSessionSandboxIdRequest sets the Daytona sandbox ID for a session.

<TypeTable
  type={{
    id: { type: "string", description: "Session ID to update.", required: true },
    sandboxId: { type: "string", description: "New sandbox ID." },
  }}
/>

### ListSessionsRequest

ListSessionsRequest specifies parameters for listing sessions.

<TypeTable
  type={{
    pageSize: { type: "number", description: "Maximum number of sessions to return per page." },
    pageToken: { type: "string", description: "Token for pagination, obtained from previous response." },
    tags: { type: "string[]", description: "Filter by tags (optional)." },
  }}
/>

### SessionList

SessionList contains a paginated list of sessions.

<TypeTable
  type={{
    totalPages: { type: "number", description: "Total number of pages available." },
    entries: { type: "SessionInput[]", description: "Sessions in the current page.", typeDescriptionLink: "#sessioninput" },
  }}
/>

### ListSessionsByAgentRequest

ListSessionsByAgentRequest lists all sessions for a specific agent.

<TypeTable
  type={{
    agentId: { type: "string", description: "Agent ID to filter by.", required: true },
    pageSize: { type: "number", description: "Maximum number of sessions to return per page." },
    pageToken: { type: "string", description: "Token for pagination, obtained from previous response." },
  }}
/>

### Session

Session represents a multi-turn conversation thread with an agent.

<TypeTable
  type={{
    apiVersion: { type: "string", description: "API version for this resource type." },
    kind: { type: "string", description: "Resource kind identifier." },
    metadata: { type: "ApiResourceMetadata", description: "Resource metadata including id, name, org, slug, labels, visibility, and timestamps." },
    spec: { type: "SessionInput", description: "Resource specification. See [`SessionInput`](#sessioninput).", typeDescriptionLink: "#sessioninput" },
    status: { type: "SessionStatus", description: "System-managed state. See `SessionStatus`." },
  }}
/>

### ApiResourceVisibility

Visibility controls who can access an API resource.

| Value | Description |
|-------|-------------|
| `visibility_private` | Only members of the owning organization can access. |
| `visibility_public` | Anyone can access (read) this resource. |

### GitWriteBackMode

GitWriteBackMode controls the platform's git workflow for a git-backed workspace entry.

| Value | Description |
|-------|-------------|
| `GIT_WRITE_BACK_BRANCH_AND_PR` | After execution completes, the platform automatically: 1. |

### ApiResourceKind

API resource kinds for Stigmer.

| Value | Description |
|-------|-------------|
| `api_resource_version` | Agentic - System-level audit trails and versioning. |
| `iam_policy` | IAM - Identity and Access Management. |
| `identity_account` |  |
| `api_key` |  |
| `identity_provider` | IAM - External identity provider configuration. |
| `organization` | Tenancy - Platform and organizational resources. |
| `platform` |  |
| `agent` | Agentic - AI agent management. |
| `agent_execution` |  |
| `session` |  |
| `skill` |  |
| `mcp_server` |  |
| `agent_instance` |  |
| `workflow` | Agentic - Workflow orchestration. |
| `workflow_instance` |  |
| `workflow_execution` |  |
| `environment` |  |
| `execution_context` |  |
| `project` | Tenancy - Project management (aggregate root for resource lifecycle). |

