---
title: Skill
description: "SDK reference for Skill resources."
---

{/* Auto-generated by stigmer-codegen. DO NOT EDIT. */}

SkillSpec defines the stored desired state of a skill. All fields are extracted
from the skill artifact by the backend. This follows the Kubernetes spec
pattern - contains user's desired state.

Note: Source/provenance information is NOT stored here - it's in SkillStatus as
GitProvenance (observed state). See synth.proto for SDK input structure.

## Client Access

<SDKTabs>
  <Tab value="Go">
    ```go skill, err := client.Skill.Get(ctx, "skill-id") ```
  </Tab>
  <Tab value="TypeScript">
    ```typescript const skill = await stigmer.skill.get("skill-id"); ```
  </Tab>
  <Tab value="Python">```python skill = client.skill.get("skill-id") ```</Tab>
  <Tab value="Java">
    ```java Skill skill = client.skill().get("skill-id"); ```
  </Tab>
</SDKTabs>

## Methods

### push

Push (upload) a skill artifact. This operation creates a new skill if it doesn't
exist, or creates a new version of an existing skill. The skill artifact must
contain SKILL.md.

Authorization: - Organization-scoped skills: Caller must have can_create_skill
permission in the organization - Platform-scoped skills: Caller must be a
platform operator (handled automatically by common auth step)

The backend will: 1. Normalize the name to a slug 2. Find or create the skill
resource 3. Extract SKILL.md from the artifact 4. Calculate SHA256 hash (version
identifier) 5. Store the artifact (deduplicated by hash) 6. Update skill spec
and status 7. Archive the previous version (if updating)

Returns: The created or updated Skill resource (consistent with other CRUD
operations)

<SDKTabs>
  <Tab value="Go">```go skill, err := client.Skill.Push(ctx, input) ```</Tab>
  <Tab value="TypeScript">
    ```typescript const skill = await stigmer.skill.push(input); ```
  </Tab>
  <Tab value="Python">```python skill = client.skill.push(input) ```</Tab>
  <Tab value="Java">```java Skill skill = client.skill().push(input); ```</Tab>
</SDKTabs>

**Parameters**: `PushSkillRequest`

**Returns**: `Skill`

### pushFromExecutionArtifact

Push a skill directly from an execution artifact already in storage.

This is the server-side equivalent of push() — instead of receiving ZIP bytes
from the client, it reads an existing directory artifact (produced by an agent
execution) from artifact storage and pushes it as a skill. This avoids
downloading the ZIP to the browser and re-uploading it, and eliminates CORS
concerns for SDK consumers.

The caller must have can_view on the referenced execution AND can_create_skill
in the target organization.

Returns: The created or updated Skill resource

<SDKTabs>
  <Tab value="Go">
    ```go skill, err := client.Skill.PushFromExecutionArtifact(ctx, input) ```
  </Tab>
  <Tab value="TypeScript">
    ```typescript const skill = await
    stigmer.skill.pushFromExecutionArtifact(input); ```
  </Tab>
  <Tab value="Python">
    ```python skill = client.skill.push_from_execution_artifact(input) ```
  </Tab>
  <Tab value="Java">
    ```java Skill skill = client.skill().pushFromExecutionArtifact(input); ```
  </Tab>
</SDKTabs>

**Parameters**: `PushSkillFromExecutionArtifactRequest`

**Returns**: `Skill`

### updateVisibility

Update the visibility of an existing skill.

This is a targeted metadata update — it only modifies metadata.visibility,
leaving spec, status, and other metadata fields untouched. Skills default to
visibility_private on push; use this RPC to make a skill publicly accessible
(marketplace-style sharing) or to revoke public access.

Authorization: Requires can_edit permission on the skill resource.

<SDKTabs>
  <Tab value="Go">
    ```go skill, err := client.Skill.UpdateVisibility(ctx, input) ```
  </Tab>
  <Tab value="TypeScript">
    ```typescript const skill = await stigmer.skill.updateVisibility(input); ```
  </Tab>
  <Tab value="Python">
    ```python skill = client.skill.update_visibility(input) ```
  </Tab>
  <Tab value="Java">
    ```java Skill skill = client.skill().updateVisibility(input); ```
  </Tab>
</SDKTabs>

**Parameters**: `UpdateVisibilityInput`

**Returns**: `Skill`

### delete

Delete a skill and all its versions. This removes the skill from the main
collection but preserves audit history.

<SDKTabs>
  <Tab value="Go">```go skill, err := client.Skill.Delete(ctx, id) ```</Tab>
  <Tab value="TypeScript">
    ```typescript const skill = await stigmer.skill.delete(id); ```
  </Tab>
  <Tab value="Python">```python skill = client.skill.delete(id) ```</Tab>
  <Tab value="Java">```java Skill skill = client.skill().delete(id); ```</Tab>
</SDKTabs>

**Parameters**

| Parameter | Type     | Description       |
| --------- | -------- | ----------------- |
| `id`      | `string` | Skill identifier. |

**Returns**: `Skill`

### get

Get a single skill by ID.

<SDKTabs>
  <Tab value="Go">```go skill, err := client.Skill.Get(ctx, id) ```</Tab>
  <Tab value="TypeScript">
    ```typescript const skill = await stigmer.skill.get(id); ```
  </Tab>
  <Tab value="Python">```python skill = client.skill.get(id) ```</Tab>
  <Tab value="Java">```java Skill skill = client.skill().get(id); ```</Tab>
</SDKTabs>

**Parameters**

| Parameter | Type     | Description       |
| --------- | -------- | ----------------- |
| `id`      | `string` | Skill identifier. |

**Returns**: `Skill`

### getByReference

Get a skill by API resource reference with version support.

Version resolution (via ApiResourceReference.version field): - Empty/"latest" →
Returns current version from main collection - Tag name (e.g., "stable", "v1.0")
→ Resolves to version with this tag - SHA256 hash (64 hex chars) → Returns exact
immutable version

Authorization is handled in the handler after resolving the reference to a skill
ID. (Input doesn't contain skill ID, so proto-level auth cannot work)

<SDKTabs>
  <Tab value="Go">
    ```go skill, err := client.Skill.GetByReference(ctx, input) ```
  </Tab>
  <Tab value="TypeScript">
    ```typescript const skill = await stigmer.skill.getByReference(input); ```
  </Tab>
  <Tab value="Python">
    ```python skill = client.skill.get_by_reference(input) ```
  </Tab>
  <Tab value="Java">
    ```java Skill skill = client.skill().getByReference(input); ```
  </Tab>
</SDKTabs>

**Parameters**: `ApiResourceReference`

**Returns**: `Skill`

### getArtifact

Download skill artifact from storage by its storage key. Returns the ZIP file
containing SKILL.md and implementation files.

This endpoint is used by the agent-runner to download and extract skill
artifacts into the sandbox at /bin/skills/\{version_hash\}/.

Authorization is skipped as the storage key itself acts as a capability token.

<SDKTabs>
  <Tab value="Go">
    ```go getArtifactResponse, err := client.Skill.GetArtifact(ctx, input) ```
  </Tab>
  <Tab value="TypeScript">
    ```typescript const getArtifactResponse = await
    stigmer.skill.getArtifact(input); ```
  </Tab>
  <Tab value="Python">
    ```python getArtifactResponse = client.skill.get_artifact(input) ```
  </Tab>
  <Tab value="Java">
    ```java GetArtifactResponse getArtifactResponse =
    client.skill().getArtifact(input); ```
  </Tab>
</SDKTabs>

**Parameters**: `GetArtifactRequest`

**Returns**: `GetArtifactResponse`

## Types

### SkillInput

Input for creating or updating a Skill.

<TypeTable
  type={{
    name: { type: "string", description: "Resource name.", required: true },
    slug: { type: "string", description: "URL-friendly identifier." },
    org: { type: "string", description: "Organization slug.", required: true },
    labels: {
      type: "Record<string, string>",
      description: "Key-value labels.",
    },
    skillMd: {
      type: "string",
      description: "SKILL.md content extracted from the artifact.",
    },
    tag: {
      type: "string",
      description: "Optional user-provided tag for this skill version.",
    },
    description: {
      type: "string",
      description:
        "Human-readable description extracted from SKILL.md YAML frontmatter.",
    },
  }}
/>
