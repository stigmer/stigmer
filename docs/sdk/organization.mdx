---
title: Organization
description: "SDK reference for Organization resources."
---

{/* Auto-generated by stigmer-codegen. DO NOT EDIT. */}

Organizations are the top-level container for all Stigmer resources. Similar to GitHub organizations, all agents, workflows, and other resources are scoped under an organization.

## Client Access

<SDKTabs>
<Tab value="Go">
```go
organization, err := client.Organization.Get(ctx, "organization-abc123")
```
</Tab>
<Tab value="TypeScript">
```typescript
const organization = await stigmer.organization.get("organization-abc123");
```
</Tab>
<Tab value="Python">
```python
organization = client.organization.get("organization-abc123")
```
</Tab>
<Tab value="Java">
```java
Organization organization = client.organization().get("organization-abc123");
```
</Tab>
</SDKTabs>

## Methods

| Method | Description |
|--------|-------------|
| [`get`](#get) | Get an organization by ID Requires: User must be a member of the organization (can_view). |
| [`getByExternalOrgId`](#getbyexternalorgid) | Look up a platform-managed organization by its external platform coordinates. |
| [`find`](#find) | Find organizations with pagination and filtering Requires: Platform admin permission This is for administrative purposes only. |
| [`findMyOrganizations`](#findmyorganizations) | Find organizations the authenticated user is a member of Authorization handled in handler via IAM Policy listAuthorizedResourceIds Returns only organizations the caller has access to. |
| [`apply`](#apply) | Create or update an organization. |
| [`create`](#create) | Create a new organization No authorization required - any authenticated user can create an organization The creator automatically becomes the owner of the organization. |
| [`update`](#update) | Update an existing organization Requires: Organization admin permission (can_edit). |
| [`delete`](#delete) | Delete an organization Requires: Organization owner permission (can_delete) Note: This will cascade delete all resources under the organization. |

### get

Get an organization by ID Requires: User must be a member of the organization (can_view)

<SDKTabs>
<Tab value="Go">
```go
organization, err := client.Organization.Get(ctx, id)
```
</Tab>
<Tab value="TypeScript">
```typescript
const organization = await stigmer.organization.get(id);
```
</Tab>
<Tab value="Python">
```python
organization = client.organization.get(id)
```
</Tab>
<Tab value="Java">
```java
Organization organization = client.organization().get(id);
```
</Tab>
</SDKTabs>

**Parameters**

| Parameter | Type | Description |
|-----------|------|-------------|
| `id` | `string` | Organization identifier. |

**Returns**: `Organization`

### getByExternalOrgId

Look up a platform-managed organization by its external platform coordinates. Returns the Stigmer organization mapped to the given IdentityProvider + external org ID. Authorization: custom — checks can_view on the referenced IdentityProvider.

<SDKTabs>
<Tab value="Go">
```go
organization, err := client.Organization.GetByExternalOrgId(ctx, input)
```
</Tab>
<Tab value="TypeScript">
```typescript
const organization = await stigmer.organization.getByExternalOrgId(input);
```
</Tab>
<Tab value="Python">
```python
organization = client.organization.get_by_external_org_id(input)
```
</Tab>
<Tab value="Java">
```java
Organization organization = client.organization().getByExternalOrgId(input);
```
</Tab>
</SDKTabs>

**Parameters**: `OrganizationExternalLookup`

**Returns**: `Organization`

### find

Find organizations with pagination and filtering Requires: Platform admin permission This is for administrative purposes only

<SDKTabs>
<Tab value="Go">
```go
result, err := client.Organization.Find(ctx, input)
```
</Tab>
<Tab value="TypeScript">
```typescript
const result = await stigmer.organization.find(input);
```
</Tab>
<Tab value="Python">
```python
result = client.organization.find(input)
```
</Tab>
<Tab value="Java">
```java
OrganizationList result = client.organization().find(input);
```
</Tab>
</SDKTabs>

**Parameters**: `FindApiResourcesRequest`

**Returns**: `OrganizationList`

### findMyOrganizations

Find organizations the authenticated user is a member of Authorization handled in handler via IAM Policy listAuthorizedResourceIds Returns only organizations the caller has access to

<SDKTabs>
<Tab value="Go">
```go
organizations, err := client.Organization.FindMyOrganizations(ctx)
```
</Tab>
<Tab value="TypeScript">
```typescript
const organizations = await stigmer.organization.findMyOrganizations();
```
</Tab>
<Tab value="Python">
```python
organizations = client.organization.find_my_organizations()
```
</Tab>
<Tab value="Java">
```java
Organizations organizations = client.organization().findMyOrganizations();
```
</Tab>
</SDKTabs>

**Returns**: `Organizations`

### apply

Create or update an organization. The authorization and state-operation are determined depending on whether the organization is going to be created or updated which is determined as part of the request execution.

<SDKTabs>
<Tab value="Go">
```go
organization, err := client.Organization.Apply(ctx, &stigmer.OrganizationInput{
  Name: "my-organization",
  Org:  "acme",
})
```
</Tab>
<Tab value="TypeScript">
```typescript
const organization = await stigmer.organization.apply({
  name: "my-organization",
  org: "acme",
});
```
</Tab>
<Tab value="Python">
```python
organization = client.organization.apply(OrganizationInput(
    name="my-organization",
    org="acme",
))
```
</Tab>
<Tab value="Java">
```java
Organization organization = client.organization().apply(OrganizationInput.builder()
    .name("my-organization")
    .org("acme")
    .build());
```
</Tab>
</SDKTabs>

**Parameters**: [`OrganizationInput`](#organizationinput)

**Returns**: `Organization`

### create

Create a new organization No authorization required - any authenticated user can create an organization The creator automatically becomes the owner of the organization

<SDKTabs>
<Tab value="Go">
```go
organization, err := client.Organization.Create(ctx, &stigmer.OrganizationInput{
  Name: "my-organization",
  Org:  "acme",
})
```
</Tab>
<Tab value="TypeScript">
```typescript
const organization = await stigmer.organization.create({
  name: "my-organization",
  org: "acme",
});
```
</Tab>
<Tab value="Python">
```python
organization = client.organization.create(OrganizationInput(
    name="my-organization",
    org="acme",
))
```
</Tab>
<Tab value="Java">
```java
Organization organization = client.organization().create(OrganizationInput.builder()
    .name("my-organization")
    .org("acme")
    .build());
```
</Tab>
</SDKTabs>

**Parameters**: [`OrganizationInput`](#organizationinput)

**Returns**: `Organization`

### update

Update an existing organization Requires: Organization admin permission (can_edit)

<SDKTabs>
<Tab value="Go">
```go
organization, err := client.Organization.Update(ctx, &stigmer.OrganizationInput{
  Name: "my-organization",
  Org:  "acme",
})
```
</Tab>
<Tab value="TypeScript">
```typescript
const organization = await stigmer.organization.update({
  name: "my-organization",
  org: "acme",
});
```
</Tab>
<Tab value="Python">
```python
organization = client.organization.update(OrganizationInput(
    name="my-organization",
    org="acme",
))
```
</Tab>
<Tab value="Java">
```java
Organization organization = client.organization().update(OrganizationInput.builder()
    .name("my-organization")
    .org("acme")
    .build());
```
</Tab>
</SDKTabs>

**Parameters**: [`OrganizationInput`](#organizationinput)

**Returns**: `Organization`

### delete

Delete an organization Requires: Organization owner permission (can_delete) Note: This will cascade delete all resources under the organization

<SDKTabs>
<Tab value="Go">
```go
organization, err := client.Organization.Delete(ctx, id)
```
</Tab>
<Tab value="TypeScript">
```typescript
const organization = await stigmer.organization.delete(id);
```
</Tab>
<Tab value="Python">
```python
organization = client.organization.delete(id)
```
</Tab>
<Tab value="Java">
```java
Organization organization = client.organization().delete(id);
```
</Tab>
</SDKTabs>

**Parameters**

| Parameter | Type | Description |
|-----------|------|-------------|
| `id` | `string` | Organization identifier. |

**Returns**: `Organization`

## Types

### OrganizationInput

Input for creating or updating an Organization.

<TypeTable
  type={{
    name: { type: "string", description: "Resource name.", required: true },
    slug: { type: "string", description: "URL-friendly identifier." },
    org: { type: "string", description: "Organization slug.", required: true },
    labels: { type: "Record<string, string>", description: "Key-value labels." },
    description: { type: "string", description: "description for the organization." },
    logoUrl: { type: "string", description: "public url for the organization logo (optional)." },
    managementMode: { type: "ManagementMode", description: "How this organization is operated." },
    identityProviderRef: { type: "ResourceRef", description: "Reference to the IdentityProvider that authenticates requests for this organization." },
    externalOrgId: { type: "string", description: "External platform's organization identifier for reverse mapping." },
    isPersonal: { type: "boolean", description: "Whether this is a personal organization, auto-created during identity provisioning." },
  }}
/>

