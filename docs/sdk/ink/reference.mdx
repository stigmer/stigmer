---
title: Ink SDK Reference
description: API reference for @stigmer/ink terminal UI components.
---

{/* Auto-generated by generate-ink-sdk-docs. DO NOT EDIT. */}

## Provider and Transport

### InkStigmerProvider

Stigmer provider for Ink (terminal) environments.

Provides the same React contexts as `StigmerProvider` from
`@stigmer/react`, but without the DOM `<div>` wrapper used for
CSS scoping (which is not applicable in a terminal).

All hooks from `@stigmer/react` (`useStigmer`, `useSessionConversation`,
`useExecutionStream`, etc.) work identically under this provider.

**Props** — `InkStigmerProviderProps`

<TypeTable
  type={{
    children: { type: "ReactNode", description: "Child components to render.", required: true },
    client: { type: "Stigmer", description: "Pre-configured Stigmer client (use createNodeClient to create one).", required: true },
    deploymentMode: { type: "DeploymentMode", description: "Deployment mode for feature gating. - `\"cloud\"`: All features available (default). - `\"local\"`: OSS feature subset only." },
  }}
/>

```tsx
import { render } from "ink";
import { InkStigmerProvider, createNodeClient } from "@stigmer/ink";

const client = createNodeClient({
  baseUrl: "https://api.stigmer.ai",
  apiKey: process.env.STIGMER_API_KEY,
});

render(
  <InkStigmerProvider client={client}>
    <MyTerminalApp />
  </InkStigmerProvider>
);
```


### createNodeClient

Re-exported from `@stigmer/sdk/node`. See [SDK Overview](/docs/sdk) for full documentation.

Create a Stigmer client configured for Node.js environments.

Uses `@connectrpc/connect-node` for native HTTP/2 transport with the
full SDK interceptor chain (auth, RPC metadata, error stripping).
Suitable for CLI tools, server-side scripts, and Ink terminal apps.

**Config** — `NodeClientConfig`

<TypeTable
  type={{
    apiKey: { type: "string", description: "Static API key for authentication." },
    baseUrl: { type: "string", description: "Stigmer API server URL (e.g., \"https://api.stigmer.ai\").", required: true },
    getAccessToken: { type: "TokenProvider", description: "Dynamic token provider for authentication." },
  }}
/>

```tsx
import { createNodeClient } from "@stigmer/sdk/node";

const stigmer = createNodeClient({
  baseUrl: "https://api.stigmer.ai",
  apiKey: "sk_live_abc123",
});

const agent = await stigmer.agent.get("my-agent");
```


### createNodeTransport

Re-exported from `@stigmer/sdk/node`. See [SDK Overview](/docs/sdk) for full documentation.

Create a gRPC-web transport for Node.js using native HTTP/2.

The interceptor chain matches the browser transport for consistent
behavior across runtimes:
1. Auth — attaches `Authorization: Bearer <token>`
2. RPC metadata — annotates errors with method name and service path
3. Error strip — removes gRPC status-code prefixes from messages

The browser-specific auth-redirect interceptor (`onUnauthenticated`)
is intentionally omitted — Node.js consumers handle auth failures
through error handling, not UI redirects.


## Components

### AgentPicker

Interactive picker for choosing an agent by name.

Composes the `useAgentSearch` data hook with `ResourcePicker`:
the user types, results stream in (debounced by the hook), and the selected
agent's `SearchResult` is returned via `onSelect`. Use the `id` field of
that result to resolve and run the agent.

**Props** — `AgentPickerProps`

<TypeTable
  type={{
    initialQuery: { type: "string", description: "Seeds the search box (e.g. an unresolved slug the user already typed)." },
    onCancel: { type: "() => void", description: "Called when the user cancels (Esc / Ctrl+C).", required: true },
    onSelect: { type: "(agent: SearchResult) => void", description: "Called with the chosen agent's search result on selection.", required: true },
    org: { type: "string", description: "Organization to search within.", required: true },
    scope: { type: "\"org\" | \"all\"", description: "`\"org\"` (default) searches the org; `\"all\"` includes public agents." },
  }}
/>

```tsx
render(
  <InkStigmerProvider client={client}>
    <AgentPicker org="acme" onSelect={(a) => run(a.id)} onCancel={() => process.exit(0)} />
  </InkStigmerProvider>,
);
```


### ApprovalPrompt

HITL approval prompt for tool call authorization.

Displays the tool name and args preview, then presents
Approve/Approve-all/Reject/Skip options navigable via arrow keys
or shortcut keys (y/a/n/s). Press Enter to confirm the highlighted
selection. The approve-all option (a) maps to APPROVE_ALL, which grants a
run-lifetime lease scoped to the clicked tool's CLASS (its MCP server, or its
file edit / delete / shell category) — other classes keep prompting. The
option label names that class so the scope is never a surprise.

**Props** — `ApprovalPromptProps`

<TypeTable
  type={{
    isSubmitting: { type: "boolean", description: "Disables input while an approval submission is in flight." },
    onSubmit: { type: "(action: ApprovalAction) => void", description: "Called when the user selects an action.", required: true },
    pendingApproval: { type: "PendingApproval", description: "The pending approval request to render.", required: true, typeDescriptionLink: "/docs/sdk/resources/agent-execution" },
  }}
/>


### blockReasonNote

The honest, one-line reason a change cannot be kept as-is, or `null` when it
is fully reviewable. Mirrors the web card's per-file block-reason copy so the
terminal reviewer sees the same explanation.


### changeDisplayPath

The path to show for a reviewable change: the post-edit path, or the pre-edit
path for a deletion. A rename shows `before → after` so both sides are visible.


### ContextGauge

Terminal-native context window utilization gauge.

Renders an ASCII progress bar with health-based coloring, token counts,
and summarization event details. Uses the headless
`useContextWindow` hook from `@stigmer/react`.

Returns `null` when `ContextInfo` is absent (e.g., Cursor harness
where context is managed externally).

**Props** — `ContextGaugeProps`

<TypeTable
  type={{
    execution: { type: "AgentExecution | null", description: "The execution snapshot from the active stream, or `null`. The gauge extracts `context_info` from the execution status.", required: true },
  }}
/>

```tsx
<ContextGauge execution={activeStreamExecution} />
```


### ExecutionProgress

Displays the current execution phase as a compact terminal badge.

Shows a spinner for active phases (pending, in-progress) and
static indicators for terminal phases.

Renders nothing for unspecified phases.

**Props** — `ExecutionProgressProps`

<TypeTable
  type={{
    phase: { type: "ExecutionPhase", description: "Current execution phase.", required: true, typeDescriptionLink: "/docs/sdk/resources/agent-execution" },
  }}
/>


### FileChangeProgressBar

The terminal analogue of the web's non-interactive "N files changing…" strip
(`@stigmer/react` `FileChangeProgressBar`), for a turn that is still running.

WHY IT EXISTS. The transcript streams per-edit tool rows, but a change made by
a shell command (a `sed`, a build step) has no tool row, and there is no
running net-of-many-edits rollup until the turn ends. This strip fills that
gap: a compact, non-interactive count of the workspace delta accumulating
during the turn, sitting where the `FileReviewPrompt` will later appear. The
two are mutually exclusive per turn (progress shows while CAPTURING; the prompt
once AWAITING_REVIEW), so the strip hands off cleanly to the prompt.

NEVER A DECISION SURFACE. Progress is non-authoritative and carries no file
bodies — it is never reviewable or decidable, and it owns no keyboard input.
The reviewed diff is the turn-boundary candidate (`FileReviewPrompt`). A
secret-like path is surfaced by name with its counts withheld (zeroed), so the
`+N −M` stat renders nothing for it. The quiet/dim register (no border)
distinguishes it from the loud, bordered decision prompt.

**Props** — `FileChangeProgressBarProps`

<TypeTable
  type={{
    expanded: { type: "boolean", description: "Whether to reveal the per-file list. The compact one-line summary always shows; the list is progressive disclosure. `SessionView` binds this to its existing Ctrl+O \"expand\" toggle so no new keybinding is introduced. Defaults to `false`." },
    progress: { type: "FileChangeProgress | undefined", description: "The transient mid-run progress snapshot for the active turn (DD-32). Feed this from `useSessionConversation().fileChangeProgress`. Renders nothing when undefined or when no files have changed yet — the server clears it once the turn's change set leaves CAPTURING, so the strip disappears exactly when the decision surface (`FileReviewPrompt`) takes over.", required: true },
  }}
/>

```tsx
const conv = useSessionConversation(sessionId, org);
<FileChangeProgressBar progress={conv.fileChangeProgress} expanded={expandTools} />
```


### FileReviewPrompt

The terminal file-review decision surface — the linear-log analogue of the
web's composer-docked `FileReviewDock`/`FileReviewCard`.

Renders `N files awaiting review` and, gated by `changeSetReviewability`:
bulk Approve/Keep all + Reject all (a complete set is approvable as-is; a
binary-only set is kept with an acknowledgement; a blocked set can only be
rejected in bulk), plus a per-file mode (`f`) for keeping/discarding each file
— the only way to resolve a blocked or mixed set, since the server refuses a
whole-set approve unless every file is complete. Decisions carry the same
digests (`aggregate_digest` / `file_digest`) and `acknowledge_unreviewable`
semantics as the web; no decision-model change.

Keyboard: `a` approve/keep all · `r` reject all · `f` per-file mode; in
per-file mode ↑/↓ move, `k` keep, `d` discard, `esc` back. Input is gated by
`isActive` so only one decision surface owns the keyboard at a time.

**Props** — `FileReviewPromptProps`

<TypeTable
  type={{
    changeSet: { type: "FileChangeSet", description: "The AWAITING_REVIEW change set this prompt decides.", required: true, typeDescriptionLink: "/docs/sdk/resources/agent-execution" },
    decisionErrors: { type: "ReadonlyMap<string, Error>", description: "Per-decision failures, keyed via `fileDecisionKey`." },
    isActive: { type: "boolean", description: "Whether this prompt owns the keyboard. False renders a read-only view (no `useInput`) so exactly one decision surface is interactive at a time. Defaults to `true`." },
    onSubmit: { type: "(changeSetId: string, action: FileDecisionAction, options?: FileDecisionOptions) => void", description: "Submit a decision for this set (or one file within it). Bound to the active execution by the caller; the terminal analogue of the web dock's `submitFileDecision`.", required: true },
    submittingDecisionKeys: { type: "ReadonlySet<string>", description: "Keys (via `fileDecisionKey`) currently being submitted — drives busy state." },
  }}
/>


### FileReviewRecord

A read-only, append-only record of a SETTLED file-review change set — the
terminal analogue of the web's in-thread `FileReviewRecordRow` (DD-27 D2).

Its primary job is the no-stamped-row case: a set changed only via shell
commands stamps no edit rows, so the per-row badges leave no trace and this
record is the only evidence of what was reviewed and decided. Sets with
stamped rows already carry their outcome on the (frozen) row badges; this
record complements them with a one-line summary and file list.

**Props** — `FileReviewRecordProps`

<TypeTable
  type={{
    fileChangeSet: { type: "FileChangeSet", description: "A settled (decided / reconciled / failed) change set to record.", required: true, typeDescriptionLink: "/docs/sdk/resources/agent-execution" },
  }}
/>


### FollowUpInput

Terminal text input for sending follow-up messages in a session.

Renders a single-line text input with a submit hint. Press Enter
to submit, Ctrl+C to exit the application.

**Props** — `FollowUpInputProps`

<TypeTable
  type={{
    disabled: { type: "boolean", description: "Disables the input entirely." },
    isSubmitting: { type: "boolean", description: "Shows a \"sending\" indicator and disables input." },
    mode: { type: "\"agent\" | \"plan\"", description: "Current interaction mode. Shown in the shortcut hint line." },
    onSubmit: { type: "(message: string) => void", description: "Called when the user submits a message (Enter key).", required: true },
    placeholder: { type: "string", description: "Placeholder text. Default: \"Reply...\"" },
  }}
/>


### kindLetter

Single-letter badge for a file change's kind, matching the web review card:
`A` added, `M` modified, `D` deleted, `R` renamed. A binary change reads as
`M` — "binary" is a review-completeness property (from `FileContent.is_binary`
via `fileReviewability`), not a distinct edit kind.


### MessageEntry

Renders a single message in the terminal conversation thread.

- `MESSAGE_HUMAN` — plain text prefixed with a "You" indicator
- `MESSAGE_AI` — markdown rendered to ANSI-styled terminal output,
  with a cursor indicator while streaming
- `MESSAGE_SYSTEM` — dimmed text
- `MESSAGE_TOOL` / `UNSPECIFIED` — renders nothing (tool results
  are handled by `ToolCallGroup`)

**Props** — `MessageEntryProps`

<TypeTable
  type={{
    message: { type: "AgentMessage", description: "The agent message to render.", required: true, typeDescriptionLink: "/docs/sdk/resources/agent-execution" },
  }}
/>


### MessageThread

Renders a continuous conversation thread from one or more
`AgentExecution` snapshots in the terminal.

Composes `MessageEntry`, `ToolCallGroup`, `SubAgentBlock`,
`ExecutionProgress`, `ApprovalPrompt`, and (for settled change
sets) `FileReviewRecord` into a scrolling terminal log.

Completed executions are rendered via Ink's `<Static>` component so they are
written once and don't re-render, keeping terminal output efficient for long
conversations. The active execution renders live so its file-review row
badges transition (Pending review → Kept/Discarded) in place, then freeze
into `<Static>` when the execution reaches a terminal phase. The thread is
wrapped in a `FileReviewContext` provider so every `ToolCallItem`
can badge its stamped edit rows.

**Props** — `MessageThreadProps`

<TypeTable
  type={{
    activeStreamExecution: { type: "AgentExecution | null", description: "The currently streaming execution (appended after `executions`)." },
    executions: { type: "readonly AgentExecution[]", description: "Completed executions in chronological order.", required: true },
    expandToolCalls: { type: "boolean", description: "Whether tool call groups should be rendered in expanded mode. Toggled via Ctrl+O." },
    onApprovalSubmit: { type: "(toolCallId: string, action: ApprovalAction) => void", description: "Callback for approval actions. Shows approval UI when provided." },
    pendingUserMessage: { type: "string | null", description: "Optimistic user message shown before the stream delivers it." },
    showFileReviewRecords: { type: "boolean", description: "Whether to render a read-only settled record for each decided/reconciled/ failed change set (the terminal analogue of the web's in-thread record, DD-27 D2). Off by default so bare consumers stay minimal; `SessionView` opts in. Pending (AWAITING_REVIEW) sets are never records here — their decision surface is the docked `FileReviewPrompt`." },
    submittingApprovalIds: { type: "ReadonlySet<string>", description: "Set of tool call IDs currently being submitted for approval." },
  }}
/>


### progressEntryDisplayPath

The path to show for a mid-run progress entry (DD-32), using the same
rename-aware rule as `changeDisplayPath`. The slim entry carries no
bodies or digests, only the same before/after/kind triple, so the terminal
renders its path identically to the reviewable surfaces.


### ResourcePicker

Generic, interactive search-and-select list for the terminal.

Renders a text input above a scrollable result list. The user types to
filter (the caller performs the actual search via `onQueryChange`), navigates
with the arrow keys, selects with Enter, and cancels with Esc. It holds no
data of its own — pass `items`/`query`/`isLoading` from a data hook such as
`useAgentSearch` or `useSessionList`.

This is the reusable organism behind `AgentPicker` and
`SessionPicker`; platform builders can compose it with any search
function to pick any resource.

**Props** — `ResourcePickerProps`

<TypeTable
  type={{
    emptyLabel: { type: "string", description: "Label shown when a completed search returns no rows." },
    error: { type: "Error | null", description: "Error from the last failed search, shown in place of the list." },
    isLoading: { type: "boolean", description: "`true` while a search request is in flight." },
    items: { type: "readonly PickerItem[]", description: "The current result rows to choose from (owned by the caller).", required: true },
    onCancel: { type: "() => void", description: "Called when the user cancels (Esc or Ctrl+C).", required: true },
    onQueryChange: { type: "(query: string) => void", description: "Called when the query text changes; the caller re-runs its search.", required: true },
    onSelect: { type: "(item: PickerItem) => void", description: "Called with the chosen item when the user presses Enter.", required: true },
    placeholder: { type: "string", description: "Placeholder shown in the empty input." },
    prompt: { type: "string", description: "Label shown before the input (e.g. \"Select an agent\")." },
    query: { type: "string", description: "Current query text (controlled — the caller owns search state).", required: true },
  }}
/>

```tsx
const { results, isLoading, query, setQuery } = useAgentSearch("acme");
const items = results.map((r) => ({ id: r.id, title: r.qualifiedSlug }));
render(
  <ResourcePicker
    prompt="Select an agent"
    items={items}
    query={query}
    onQueryChange={setQuery}
    isLoading={isLoading}
    onSelect={(item) => console.log(item.id)}
    onCancel={() => process.exit(0)}
  />,
);
```


### SessionPicker

Interactive picker for re-opening an existing session.

Unlike agents, sessions are not search-indexed, so this composes
`useSessionList` with client-side text filtering over the recent
session list. The selected `Session` is returned via `onSelect`; use
`session.metadata.id` to resume it.

**Props** — `SessionPickerProps`

<TypeTable
  type={{
    initialQuery: { type: "string", description: "Seeds the filter box (e.g. text the user already typed)." },
    onCancel: { type: "() => void", description: "Called when the user cancels (Esc / Ctrl+C).", required: true },
    onSelect: { type: "(session: Session) => void", description: "Called with the chosen session on selection.", required: true },
    pageSize: { type: "number", description: "Maximum sessions to fetch. Defaults to 50." },
  }}
/>

```tsx
render(
  <InkStigmerProvider client={client}>
    <SessionPicker onSelect={(s) => resume(s.metadata?.id)} onCancel={() => process.exit(0)} />
  </InkStigmerProvider>,
);
```


### settledCounts

Folds a settled change set's decisions into kept / discarded / not-reviewed
counts, using the same `deriveEffectiveVerdicts` fold the web uses so a
"Keep all" set correctly reads every file as kept.


### settledSummary

A one-line summary of a settled change set's outcome, e.g. `2 kept · 1
discarded`, or `3 files changed` when nothing was individually decided.


### SubAgentBlock

Renders a sub-agent execution as a collapsible block in the terminal.

Shows a summary line with status glyph, name/subject, and duration.
When expanded, renders the sub-agent's internal message thread using
the same MessageEntry and ToolCallGroup components as the main thread.

**Props** — `SubAgentBlockProps`

<TypeTable
  type={{
    defaultExpanded: { type: "boolean", description: "Whether this block starts expanded." },
    isFocused: { type: "boolean", description: "Whether this component can receive keyboard focus for toggling." },
    subAgent: { type: "SubAgentExecution", description: "The sub-agent execution data.", required: true, typeDescriptionLink: "/docs/sdk/resources/agent-execution" },
  }}
/>


### TodoList

Renders a sorted checklist of todo items in the terminal.

Items are sorted by activity: in-progress first, then pending,
completed, and cancelled. Terminal equivalent of the web SDK's
TodoList component.

**Props** — `TodoListProps`

<TypeTable
  type={{
    todos: { type: "object", description: "Map of todo item IDs to their current state, as provided by the execution status.", required: true },
  }}
/>


### ToolCallGroup

Renders a collapsible group of tool calls with an aggregate status.

When collapsed, shows a summary line with tool count and status.
When expanded, renders each tool call via `ToolCallItem`.

Press Enter or Space to toggle expansion when `isFocused` is true.

**Props** — `ToolCallGroupProps`

<TypeTable
  type={{
    defaultExpanded: { type: "boolean", description: "Whether to start in expanded mode (driven by Ctrl+O global toggle)." },
    isFocused: { type: "boolean", description: "Whether this group is currently focused for keyboard interaction." },
    toolCalls: { type: "readonly ToolCall[]", description: "Tool calls in this group, ordered by invocation time.", required: true },
  }}
/>


### ToolCallItem

Renders a single tool call with a status indicator, label, and an optional
expanded result. Labels and results come from the shared `@stigmer/sdk` view
model, so a terminal session shows the same semantics (diffs, exit codes,
match counts) as the web console.

**Props** — `ToolCallItemProps`

<TypeTable
  type={{
    expanded: { type: "boolean", description: "Whether to show the full args/result (expanded view)." },
    toolCall: { type: "ToolCall", description: "The tool call to render.", required: true, typeDescriptionLink: "/docs/sdk/resources/agent-execution" },
  }}
/>


### UsageWidget

Compact terminal widget showing session-level token usage and cost.

Aggregates usage data from `usageSummary` across all executions
in the session. Renders nothing when no usage data is available.

Uses the headless `useSessionUsage` hook from `@stigmer/react`.

**Props** — `UsageWidgetProps`

<TypeTable
  type={{
    executions: { type: "readonly AgentExecution[]", description: "All executions for the current session (completed + active).", required: true },
  }}
/>


## Composed Views

### SessionApp

Self-contained top-level Ink application for viewing and
interacting with a Stigmer agent session in the terminal.

Creates a Node.js-compatible Stigmer client, wraps in
`InkStigmerProvider`, and renders a `SessionView`.

This is the highest-level component — platform builders who
need more control should compose `InkStigmerProvider`,
`SessionView`, or the individual components directly.

**Props** — `SessionAppProps`

<TypeTable
  type={{
    apiKey: { type: "string", description: "Static API key for authentication." },
    baseUrl: { type: "string", description: "Stigmer API server URL.", required: true },
    getAccessToken: { type: "TokenProvider", description: "Dynamic token provider for authentication." },
    mode: { type: "\"agent\" | \"plan\"", description: "Default interaction mode for follow-up executions.  - `\"agent\"` (default): full tool access. - `\"plan\"`: read-only analysis, no file mutations.  When set, all follow-up executions in this session use this mode unless overridden by the user." },
    org: { type: "string", description: "Organization slug for creating follow-up executions.", required: true },
    sessionId: { type: "string", description: "Session ID to display and converse in.", required: true },
  }}
/>

```tsx
import { render } from "ink";
import { SessionApp } from "@stigmer/ink";

render(
  <SessionApp
    sessionId="ses-abc123"
    org="my-org"
    baseUrl="https://api.stigmer.ai"
    apiKey={process.env.STIGMER_API_KEY}
  />
);
```


### SessionView

Full-featured session conversation view for the terminal.

Uses the headless `useSessionConversation` hook from
`@stigmer/react` to manage the complete conversation lifecycle,
then renders the thread and input using Ink terminal components.

This is the main composition component that platform builders
drop into their Ink apps for a complete agent conversation UI.

**Props** — `SessionViewProps`

<TypeTable
  type={{
    mode: { type: "InteractionMode", description: "Initial interaction mode for follow-up executions.  - `\"agent\"` (default): full tool access. - `\"plan\"`: read-only analysis, no file mutations.  The user can toggle between modes with Ctrl+T during the session. This prop sets the initial value; subsequent toggles are managed as local state." },
    org: { type: "string", description: "Organization slug for creating follow-up executions.", required: true },
    sessionId: { type: "string", description: "Session ID to display and converse in.", required: true },
  }}
/>

```tsx
import { InkStigmerProvider, createNodeClient, SessionView } from "@stigmer/ink";

const client = createNodeClient({ baseUrl: "...", apiKey: "..." });

render(
  <InkStigmerProvider client={client}>
    <SessionView sessionId="ses-xxx" org="my-org" />
  </InkStigmerProvider>
);
```


## Utilities

### renderMarkdown

Render a markdown string to ANSI-styled terminal output.

Uses `marked` with `marked-terminal` to produce styled text suitable
for display in a terminal. Supports headings, code blocks (with syntax
highlighting), lists, bold/italic, links, tables, and blockquotes.

```tsx
import { renderMarkdown } from "@stigmer/ink";

const ansi = renderMarkdown("**Hello** from the agent!");
process.stdout.write(ansi);
```

