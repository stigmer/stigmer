---
title: Agent
description: "SDK reference for Agent resources."
---

{/* Auto-generated by stigmer-codegen. DO NOT EDIT. */}

AgentSpec defines the configurable properties of an AI agent. This is the
"Template" layer - declares capabilities and requirements.

Example YAML: apiVersion: agentic.stigmer.ai/v1 kind: Agent metadata: name:
engineering-assistant slug: eng-assistant spec: description: "Helps engineering
teams with code review" instructions: "You are an engineering assistant..."
mcp_server_usages: - mcp_server_ref: kind: mcp_server slug: github
enabled_tools: [search_code, create_pr] skill_refs: - kind: skill slug:
code-review-best-practices

## Client Access

<SDKTabs>
  <Tab value="Go">
    ```go agent, err := client.Agent.Get(ctx, "agent-id") ```
  </Tab>
  <Tab value="TypeScript">
    ```typescript const agent = await stigmer.agent.get("agent-id"); ```
  </Tab>
  <Tab value="Python">```python agent = client.agent.get("agent-id") ```</Tab>
  <Tab value="Java">
    ```java Agent agent = client.agent().get("agent-id"); ```
  </Tab>
</SDKTabs>

## Methods

### apply

Create or update an agent. The authorization and state-operation are determined
depending on whether the agent is going to be created or updated which is
determined as part of the request execution.

<SDKTabs>
<Tab value="Go">
```go
agent, err := client.Agent.Apply(ctx, &stigmer.AgentInput{
  Name: "my-agent",
  Org:  "acme",
})
```
</Tab>
<Tab value="TypeScript">
```typescript
const agent = await stigmer.agent.apply({
  name: "my-agent",
  org: "acme",
});
```
</Tab>
<Tab value="Python">
```python
agent = client.agent.apply(AgentInput(
    name="my-agent",
    org="acme",
))
```
</Tab>
<Tab value="Java">
```java
Agent agent = client.agent().apply(AgentInput.builder()
    .name("my-agent")
    .org("acme")
    .build());
```
</Tab>
</SDKTabs>

**Parameters**: [`AgentInput`](#agentinput)

**Returns**: `Agent`

### create

Create a new agent.

Authorization: - Organization-scoped agents: Caller must have can_create_agent
permission in the organization - Platform-scoped agents: Caller must be a
platform operator (handled automatically by common auth step)

<SDKTabs>
<Tab value="Go">
```go
agent, err := client.Agent.Create(ctx, &stigmer.AgentInput{
  Name: "my-agent",
  Org:  "acme",
})
```
</Tab>
<Tab value="TypeScript">
```typescript
const agent = await stigmer.agent.create({
  name: "my-agent",
  org: "acme",
});
```
</Tab>
<Tab value="Python">
```python
agent = client.agent.create(AgentInput(
    name="my-agent",
    org="acme",
))
```
</Tab>
<Tab value="Java">
```java
Agent agent = client.agent().create(AgentInput.builder()
    .name("my-agent")
    .org("acme")
    .build());
```
</Tab>
</SDKTabs>

**Parameters**: [`AgentInput`](#agentinput)

**Returns**: `Agent`

### update

Update an existing agent.

<SDKTabs>
<Tab value="Go">
```go
agent, err := client.Agent.Update(ctx, &stigmer.AgentInput{
  Name: "my-agent",
  Org:  "acme",
})
```
</Tab>
<Tab value="TypeScript">
```typescript
const agent = await stigmer.agent.update({
  name: "my-agent",
  org: "acme",
});
```
</Tab>
<Tab value="Python">
```python
agent = client.agent.update(AgentInput(
    name="my-agent",
    org="acme",
))
```
</Tab>
<Tab value="Java">
```java
Agent agent = client.agent().update(AgentInput.builder()
    .name("my-agent")
    .org("acme")
    .build());
```
</Tab>
</SDKTabs>

**Parameters**: [`AgentInput`](#agentinput)

**Returns**: `Agent`

### updateVisibility

Update the visibility of an existing agent.

This is a targeted metadata update — it only modifies metadata.visibility,
leaving spec, status, and other metadata fields untouched. Use this to make an
agent publicly accessible or to revoke public access without sending the entire
agent resource (avoiding read-modify-write races).

Authorization: Requires can_edit permission on the agent resource.

<SDKTabs>
  <Tab value="Go">
    ```go agent, err := client.Agent.UpdateVisibility(ctx, input) ```
  </Tab>
  <Tab value="TypeScript">
    ```typescript const agent = await stigmer.agent.updateVisibility(input); ```
  </Tab>
  <Tab value="Python">
    ```python agent = client.agent.update_visibility(input) ```
  </Tab>
  <Tab value="Java">
    ```java Agent agent = client.agent().updateVisibility(input); ```
  </Tab>
</SDKTabs>

**Parameters**: `UpdateVisibilityInput`

**Returns**: `Agent`

### delete

Delete an agent.

<SDKTabs>
  <Tab value="Go">```go agent, err := client.Agent.Delete(ctx, id) ```</Tab>
  <Tab value="TypeScript">
    ```typescript const agent = await stigmer.agent.delete(id); ```
  </Tab>
  <Tab value="Python">```python agent = client.agent.delete(id) ```</Tab>
  <Tab value="Java">```java Agent agent = client.agent().delete(id); ```</Tab>
</SDKTabs>

**Parameters**

| Parameter | Type     | Description       |
| --------- | -------- | ----------------- |
| `id`      | `string` | Agent identifier. |

**Returns**: `Agent`

### get

Get a single agent by ID.

<SDKTabs>
  <Tab value="Go">```go agent, err := client.Agent.Get(ctx, id) ```</Tab>
  <Tab value="TypeScript">
    ```typescript const agent = await stigmer.agent.get(id); ```
  </Tab>
  <Tab value="Python">```python agent = client.agent.get(id) ```</Tab>
  <Tab value="Java">```java Agent agent = client.agent().get(id); ```</Tab>
</SDKTabs>

**Parameters**

| Parameter | Type     | Description       |
| --------- | -------- | ----------------- |
| `id`      | `string` | Agent identifier. |

**Returns**: `Agent`

### getByReference

Custom authorization in handler

<SDKTabs>
  <Tab value="Go">
    ```go agent, err := client.Agent.GetByReference(ctx, input) ```
  </Tab>
  <Tab value="TypeScript">
    ```typescript const agent = await stigmer.agent.getByReference(input); ```
  </Tab>
  <Tab value="Python">
    ```python agent = client.agent.get_by_reference(input) ```
  </Tab>
  <Tab value="Java">
    ```java Agent agent = client.agent().getByReference(input); ```
  </Tab>
</SDKTabs>

**Parameters**: `ApiResourceReference`

**Returns**: `Agent`

### getDefault

Get the platform default agent.

Resolves the agent labeled stigmer.ai/default-agent: "true" with
visibility_public. Returns the full Agent resource including
status.default_instance_id for session creation.

Used by frontends to enable session-first UX where users start a conversation
without explicitly selecting an agent.

Error: NOT_FOUND if no default agent is configured. Custom authorization in
handler.

<SDKTabs>
  <Tab value="Go">
    ```go agent, err := client.Agent.GetDefault(ctx, input) ```
  </Tab>
  <Tab value="TypeScript">
    ```typescript const agent = await stigmer.agent.getDefault(input); ```
  </Tab>
  <Tab value="Python">
    ```python agent = client.agent.get_default(input) ```
  </Tab>
  <Tab value="Java">
    ```java Agent agent = client.agent().getDefault(input); ```
  </Tab>
</SDKTabs>

**Parameters**: `GetDefaultAgentRequest`

**Returns**: `Agent`

## Types

### AgentInput

Input for creating or updating a Agent.

<TypeTable
  type={{
    name: { type: "string", description: "Resource name.", required: true },
    slug: { type: "string", description: "URL-friendly identifier." },
    org: { type: "string", description: "Organization slug.", required: true },
    labels: {
      type: "Record<string, string>",
      description: "Key-value labels.",
    },
    description: {
      type: "string",
      description: "Human-readable description for UI and marketplace display.",
    },
    iconUrl: {
      type: "string",
      description: "Icon URL for marketplace and UI display.",
    },
    instructions: {
      type: "string",
      description:
        "Instructions defining the agent's behavior and personality.",
    },
    mcpServerUsages: {
      type: "McpServerUsageInput[]",
      description: "MCP servers this Agent can use.",
    },
    skillRefs: {
      type: "ResourceRef[]",
      description: "Skill resources providing agent knowledge.",
    },
    subAgents: {
      type: "SubAgentInput[]",
      description: "Sub-agents that can be delegated to.",
    },
    envSpec: {
      type: "EnvSpecInput",
      description: "Environment variables required by the agent.",
    },
  }}
/>

### McpServerUsageInput

McpServerUsage declares that this Agent uses a McpServer resource.

<TypeTable
  type={{
    mcpServerRef: {
      type: "ResourceRef",
      description: "Reference to the McpServer resource.",
      required: true,
    },
    enabledTools: {
      type: "string[]",
      description: "Tools to enable from this MCP server for this Agent.",
    },
    toolApprovalOverrides: {
      type: "ToolApprovalOverrideInput[]",
      description: "Override approval requirements for specific tools.",
    },
  }}
/>

### ToolApprovalOverrideInput

ToolApprovalOverride allows per-agent customization of approval requirements.

<TypeTable
  type={{
    toolName: { type: "string", description: "Name of the tool to override." },
    requiresApproval: {
      type: "boolean",
      description: "Whether this tool requires approval for this agent.",
    },
    message: {
      type: "string",
      description: "Optional: Custom approval message for this agent.",
    },
  }}
/>

### SubAgentInput

SubAgent defines a specialized agent that the parent can delegate to.

<TypeTable
  type={{
    name: {
      type: "string",
      description: "Unique name of the sub-agent within the parent agent.",
      required: true,
    },
    description: {
      type: "string",
      description: "Description of what this sub-agent specializes in.",
    },
    instructions: {
      type: "string",
      description: "Behavior instructions for this sub-agent.",
    },
    mcpAccess: {
      type: "McpAccessInput[]",
      description: "MCP server access grants for this sub-agent.",
    },
    skillRefs: {
      type: "ResourceRef[]",
      description: "Skill resources for this sub-agent's knowledge.",
    },
    modelOverride: {
      type: "string",
      description: "Model override for this sub-agent.",
    },
  }}
/>

### McpAccessInput

McpAccess grants a SubAgent access to one of the parent Agent's MCP servers.

<TypeTable
  type={{
    mcpServer: {
      type: "string",
      description: "Slug of the McpServer to grant access to.",
      required: true,
    },
    enabledTools: {
      type: "string[]",
      description: "Tools this SubAgent can use from this MCP server.",
    },
  }}
/>
