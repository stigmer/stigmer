---
title: MCP Server
description: "SDK reference for MCP Server resources."
---

{/* Auto-generated by stigmer-codegen. DO NOT EDIT. */}

This is a reusable definition that can be referenced by multiple agents. Actual secrets/credentials are provided at runtime via AgentInstance's environment.

## Client Access

<SDKTabs>
<Tab value="Go">
```go
mcpServer, err := client.McpServer.Get(ctx, "mcp-server-abc123")
```
</Tab>
<Tab value="TypeScript">
```typescript
const mcpServer = await stigmer.mcpServer.get("mcp-server-abc123");
```
</Tab>
<Tab value="Python">
```python
mcpServer = client.mcp_server.get("mcp-server-abc123")
```
</Tab>
<Tab value="Java">
```java
McpServer mcpServer = client.mcpServer().get("mcp-server-abc123");
```
</Tab>
</SDKTabs>

## Methods

| Method | Description |
|--------|-------------|
| [`get`](#get) | Get an MCP server by its unique identifier. |
| [`getByReference`](#getbyreference) | Get an MCP server by reference (scope + org + slug). |
| [`apply`](#apply) | Create or update an MCP server resource (Kubernetes-style apply). |
| [`create`](#create) | Create a new MCP server resource. |
| [`update`](#update) | Update an existing MCP server resource. |
| [`updateDiscoveredCapabilities`](#updatediscoveredcapabilities) | Update the discovered capabilities (tools and resource templates) for an MCP server. |
| [`updateVisibility`](#updatevisibility) | Update the visibility of an existing MCP server. |
| [`delete`](#delete) | Delete an MCP server resource. |
| [`discoverCapabilities`](#discovercapabilities) | Discover the capabilities of an MCP server by connecting to it. |

### get

Get an MCP server by its unique identifier.

<SDKTabs>
<Tab value="Go">
```go
mcpServer, err := client.McpServer.Get(ctx, id)
```
</Tab>
<Tab value="TypeScript">
```typescript
const mcpServer = await stigmer.mcpServer.get(id);
```
</Tab>
<Tab value="Python">
```python
mcpServer = client.mcp_server.get(id)
```
</Tab>
<Tab value="Java">
```java
McpServer mcpServer = client.mcpServer().get(id);
```
</Tab>
</SDKTabs>

**Parameters**

| Parameter | Type | Description |
|-----------|------|-------------|
| `id` | `string` | MCP Server identifier. |

**Returns**: `McpServer`

### getByReference

Get an MCP server by reference (scope + org + slug).

<SDKTabs>
<Tab value="Go">
```go
mcpServer, err := client.McpServer.GetByReference(ctx, input)
```
</Tab>
<Tab value="TypeScript">
```typescript
const mcpServer = await stigmer.mcpServer.getByReference(input);
```
</Tab>
<Tab value="Python">
```python
mcpServer = client.mcp_server.get_by_reference(input)
```
</Tab>
<Tab value="Java">
```java
McpServer mcpServer = client.mcpServer().getByReference(input);
```
</Tab>
</SDKTabs>

**Parameters**: `ApiResourceReference`

**Returns**: `McpServer`

### apply

Create or update an MCP server resource (Kubernetes-style apply).

<SDKTabs>
<Tab value="Go">
```go
mcpServer, err := client.McpServer.Apply(ctx, &stigmer.McpServerInput{
  Name: "my-mcp-server",
  Org:  "acme",
})
```
</Tab>
<Tab value="TypeScript">
```typescript
const mcpServer = await stigmer.mcpServer.apply({
  name: "my-mcp-server",
  org: "acme",
});
```
</Tab>
<Tab value="Python">
```python
mcpServer = client.mcp_server.apply(McpServerInput(
    name="my-mcp-server",
    org="acme",
))
```
</Tab>
<Tab value="Java">
```java
McpServer mcpServer = client.mcpServer().apply(McpServerInput.builder()
    .name("my-mcp-server")
    .org("acme")
    .build());
```
</Tab>
</SDKTabs>

**Parameters**: [`McpServerInput`](#mcpserverinput)

**Returns**: `McpServer`

### create

Create a new MCP server resource.

<SDKTabs>
<Tab value="Go">
```go
mcpServer, err := client.McpServer.Create(ctx, &stigmer.McpServerInput{
  Name: "my-mcp-server",
  Org:  "acme",
})
```
</Tab>
<Tab value="TypeScript">
```typescript
const mcpServer = await stigmer.mcpServer.create({
  name: "my-mcp-server",
  org: "acme",
});
```
</Tab>
<Tab value="Python">
```python
mcpServer = client.mcp_server.create(McpServerInput(
    name="my-mcp-server",
    org="acme",
))
```
</Tab>
<Tab value="Java">
```java
McpServer mcpServer = client.mcpServer().create(McpServerInput.builder()
    .name("my-mcp-server")
    .org("acme")
    .build());
```
</Tab>
</SDKTabs>

**Parameters**: [`McpServerInput`](#mcpserverinput)

**Returns**: `McpServer`

### update

Update an existing MCP server resource.

<SDKTabs>
<Tab value="Go">
```go
mcpServer, err := client.McpServer.Update(ctx, &stigmer.McpServerInput{
  Name: "my-mcp-server",
  Org:  "acme",
})
```
</Tab>
<Tab value="TypeScript">
```typescript
const mcpServer = await stigmer.mcpServer.update({
  name: "my-mcp-server",
  org: "acme",
});
```
</Tab>
<Tab value="Python">
```python
mcpServer = client.mcp_server.update(McpServerInput(
    name="my-mcp-server",
    org="acme",
))
```
</Tab>
<Tab value="Java">
```java
McpServer mcpServer = client.mcpServer().update(McpServerInput.builder()
    .name("my-mcp-server")
    .org("acme")
    .build());
```
</Tab>
</SDKTabs>

**Parameters**: [`McpServerInput`](#mcpserverinput)

**Returns**: `McpServer`

### updateDiscoveredCapabilities

Update the discovered capabilities (tools and resource templates) for an MCP server.

<SDKTabs>
<Tab value="Go">
```go
mcpServer, err := client.McpServer.UpdateDiscoveredCapabilities(ctx, input)
```
</Tab>
<Tab value="TypeScript">
```typescript
const mcpServer = await stigmer.mcpServer.updateDiscoveredCapabilities(input);
```
</Tab>
<Tab value="Python">
```python
mcpServer = client.mcp_server.update_discovered_capabilities(input)
```
</Tab>
<Tab value="Java">
```java
McpServer mcpServer = client.mcpServer().updateDiscoveredCapabilities(input);
```
</Tab>
</SDKTabs>

**Parameters**: `UpdateDiscoveredCapabilitiesInput`

**Returns**: `McpServer`

### updateVisibility

Update the visibility of an existing MCP server.

<SDKTabs>
<Tab value="Go">
```go
mcpServer, err := client.McpServer.UpdateVisibility(ctx, input)
```
</Tab>
<Tab value="TypeScript">
```typescript
const mcpServer = await stigmer.mcpServer.updateVisibility(input);
```
</Tab>
<Tab value="Python">
```python
mcpServer = client.mcp_server.update_visibility(input)
```
</Tab>
<Tab value="Java">
```java
McpServer mcpServer = client.mcpServer().updateVisibility(input);
```
</Tab>
</SDKTabs>

**Parameters**: `UpdateVisibilityInput`

**Returns**: `McpServer`

### delete

Delete an MCP server resource.

<SDKTabs>
<Tab value="Go">
```go
mcpServer, err := client.McpServer.Delete(ctx, &stigmer.DeleteResourceInput{
  ResourceID: id,
})
```
</Tab>
<Tab value="TypeScript">
```typescript
const mcpServer = await stigmer.mcpServer.delete({ resourceId: id });
```
</Tab>
<Tab value="Python">
```python
mcpServer = client.mcp_server.delete(DeleteResourceInput(resource_id=id))
```
</Tab>
<Tab value="Java">
```java
McpServer mcpServer = client.mcpServer().delete(DeleteResourceInput.builder()
    .resourceId(id)
    .build());
```
</Tab>
</SDKTabs>

**Parameters**

| Parameter | Type | Description |
|-----------|------|-------------|
| `resourceId` | `string` | ID of the resource to delete. |
| `versionMessage` | `string` | Optional deletion reason. |
| `force` | `boolean` | Force delete even if resource is in use. |

**Returns**: `McpServer`

### discoverCapabilities

Discover the capabilities of an MCP server by connecting to it.

<SDKTabs>
<Tab value="Go">
```go
mcpServer, err := client.McpServer.DiscoverCapabilities(ctx, input)
```
</Tab>
<Tab value="TypeScript">
```typescript
const mcpServer = await stigmer.mcpServer.discoverCapabilities(input);
```
</Tab>
<Tab value="Python">
```python
mcpServer = client.mcp_server.discover_capabilities(input)
```
</Tab>
<Tab value="Java">
```java
McpServer mcpServer = client.mcpServer().discoverCapabilities(input);
```
</Tab>
</SDKTabs>

**Parameters**: `DiscoverCapabilitiesInput`

**Returns**: `McpServer`

## Types

### McpServerInput

Input for creating or updating a MCP Server.

<TypeTable
  type={{
    name: { type: "string", description: "Resource name.", required: true },
    slug: { type: "string", description: "URL-friendly identifier." },
    org: { type: "string", description: "Organization slug.", required: true },
    labels: { type: "Record<string, string>", description: "Key-value labels." },
    description: { type: "string", description: "Human-readable description for marketplace display and documentation." },
    iconUrl: { type: "string", description: "Icon URL for UI display in marketplace and agent configuration screens." },
    stdio: { type: "StdioServerConfigInput", description: "stdio-based server (subprocess with stdin/stdout communication)." },
    http: { type: "HttpServerConfigInput", description: "HTTP-based server (HTTP + Server-Sent Events communication)." },
    defaultEnabledTools: { type: "string[]", description: "Default tools to enable from this MCP server." },
    envSpec: { type: "EnvSpecInput", description: "Environment specification declaring required environment variables." },
    defaultToolApprovals: { type: "ToolApprovalPolicyInput[]", description: "Default tool approval policies for this MCP server." },
  }}
/>

### StdioServerConfigInput

StdioServerConfig defines an MCP server that runs as a subprocess.

<TypeTable
  type={{
    command: { type: "string", description: "Command to execute the MCP server.", required: true },
    args: { type: "string[]", description: "Arguments to pass to the command." },
    workingDir: { type: "string", description: "Working directory for the process." },
  }}
/>

### HttpServerConfigInput

HttpServerConfig defines an MCP server accessible via HTTP + Server-Sent Events.

<TypeTable
  type={{
    url: { type: "string", description: "Base URL of the MCP server endpoint.", required: true },
    headers: { type: "Record<string, string>", description: "HTTP headers to include with every request." },
    queryParams: { type: "Record<string, string>", description: "Query parameters to append to the URL." },
    timeoutSeconds: { type: "number", description: "Timeout for HTTP requests in seconds." },
  }}
/>

### ToolApprovalPolicyInput

ToolApprovalPolicy defines approval requirements for a specific tool.

<TypeTable
  type={{
    toolName: { type: "string", description: "Name of the tool (must match tools/list from MCP server exactly)." },
    message: { type: "string", description: "Human-readable message shown to user when approval is requested." },
  }}
/>

