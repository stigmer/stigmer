---
title: MCP Server
description: "SDK reference for MCP Server resources."
---

{/* Auto-generated by stigmer-codegen. DO NOT EDIT. */}

This is a reusable definition that can be referenced by multiple agents. Actual secrets/credentials are provided at runtime via AgentInstance's environment.

MCP (Model Context Protocol) servers provide tools and capabilities to AI agents. This spec declares the server type, connection details, and required environment variables.

## Client Access

<SDKTabs>
<Tab value="TypeScript">
```typescript
const mcpServer = await stigmer.mcpServer.get("mcp_01j5q3k7m8r2s4tnz2hfp0q0c3");
```
</Tab>
<Tab value="Go">
```go
mcpServer, err := client.McpServer.Get(ctx, "mcp_01j5q3k7m8r2s4tnz2hfp0q0c3")
```
</Tab>
<Tab value="Python">
```python
mcpServer = client.mcp_server.get("mcp_01j5q3k7m8r2s4tnz2hfp0q0c3")
```
</Tab>
<Tab value="Java">
```java
McpServer mcpServer = client.mcpServer().get("mcp_01j5q3k7m8r2s4tnz2hfp0q0c3");
```
</Tab>
</SDKTabs>

## Methods

| Method | Description |
|--------|-------------|
| [`get`](#get) | Get an MCP server by its unique identifier. |
| [`getByReference`](#getbyreference) | Get an MCP server by reference (scope + org + slug). |
| [`apply`](#apply) | Create or update an MCP server resource (Kubernetes-style apply). |
| [`create`](#create) | Create a new MCP server resource. |
| [`update`](#update) | Update an existing MCP server resource. |
| [`updateDiscoveredCapabilities`](#updatediscoveredcapabilities) | Update the discovered capabilities (tools and resource templates) for an MCP server. |
| [`updateVisibility`](#updatevisibility) | Update the visibility of an existing MCP server. |
| [`delete`](#delete) | Delete an MCP server resource. |
| [`discoverCapabilities`](#discovercapabilities) | Discover the capabilities of an MCP server by connecting to it. |

### get

Get an MCP server by its unique identifier.

Input: ApiResourceId containing the MCP server's system-generated ID. Returns: The full McpServer resource including metadata, spec, and status.

Authorization: Requires can_view permission on the mcp_server resource. The caller must have access based on the resource's scope: - Platform: All authenticated users - Organization: Organization members - Identity Account: Only the owner

<SDKTabs>
<Tab value="TypeScript">
```typescript
const mcpServer = await stigmer.mcpServer.get(id);
```
</Tab>
<Tab value="Go">
```go
mcpServer, err := client.McpServer.Get(ctx, id)
```
</Tab>
<Tab value="Python">
```python
mcpServer = client.mcp_server.get(id)
```
</Tab>
<Tab value="Java">
```java
McpServer mcpServer = client.mcpServer().get(id);
```
</Tab>
</SDKTabs>

**Parameters**

| Parameter | Type | Description |
|-----------|------|-------------|
| `id` | `string` | MCP Server identifier. |

**Returns**: [`McpServer`](#mcpserver)

### getByReference

Get an MCP server by reference (scope + org + slug).

Input: ApiResourceReference with scope, org (if applicable), kind, and slug. Returns: The full McpServer resource.

This is the preferred method for looking up MCP servers by name/slug rather than system-generated ID. Useful for: - Resolving references from Agent.mcp_server_usages - CLI lookups by name - Cross-scope resolution (platform vs org vs personal)

Authorization: Custom authorization in handler. The handler performs scope-aware authorization based on the reference.

<SDKTabs>
<Tab value="TypeScript">
```typescript
const mcpServer = await stigmer.mcpServer.getByReference({
  org: "...",
  kind: "...",
  slug: "...",
  version: "...",
});
```
</Tab>
<Tab value="Go">
```go
mcpServer, err := client.McpServer.GetByReference(ctx, &stigmer.ApiResourceReference{
  Org:     "...",
  Kind:    "...",
  Slug:    "...",
  Version: "...",
})
```
</Tab>
<Tab value="Python">
```python
mcpServer = client.mcp_server.get_by_reference(ApiResourceReference(
    org="...",
    kind="...",
    slug="...",
    version="...",
))
```
</Tab>
<Tab value="Java">
```java
McpServer mcpServer = client.mcpServer().getByReference(ApiResourceReference.builder()
    .org("...")
    .kind("...")
    .slug("...")
    .version("...")
    .build());
```
</Tab>
</SDKTabs>

**Parameters**: [`ApiResourceReference`](#apiresourcereference)

**Returns**: [`McpServer`](#mcpserver)

### apply

Create or update an MCP server resource (Kubernetes-style apply).

This is the primary interface for MCP server management. Behavior: - If the resource doesn't exist: Creates a new MCP server - If the resource exists: Updates the existing MCP server

The resource is identified by (scope, org, slug) combination.

Input: Full McpServer resource with metadata and spec. Returns: The created/updated McpServer with system-populated fields.

Authorization: Custom authorization in handler. The handler determines whether this is a create or update operation and performs appropriate scope-aware authorization: - Create: Requires permission to create in the target scope - Update: Requires can_edit on the existing resource

<SDKTabs>
<Tab value="TypeScript">
```typescript
const mcpServer = await stigmer.mcpServer.apply({
  name: "my-mcp-server",
  org: "acme",
  slug: "...",
  labels: {},
  description: "...",
  iconUrl: "...",
  stdio: {},
  http: {},
  defaultEnabledTools: [],
  envSpec: {},
  defaultToolApprovals: [],
});
```
</Tab>
<Tab value="Go">
```go
mcpServer, err := client.McpServer.Apply(ctx, &stigmer.McpServerInput{
  Name:                 "my-mcp-server",
  Org:                  "acme",
  Slug:                 "...",
  Labels:               map[string]string{},
  Description:          "...",
  IconUrl:              "...",
  Stdio:                &stigmer.StdioServerConfigInput{},
  Http:                 &stigmer.HttpServerConfigInput{},
  DefaultEnabledTools:  []string{},
  EnvSpec:              &stigmer.EnvSpecInput{},
  DefaultToolApprovals: []stigmer.ToolApprovalPolicyInput{},
})
```
</Tab>
<Tab value="Python">
```python
mcpServer = client.mcp_server.apply(McpServerInput(
    name="my-mcp-server",
    org="acme",
    slug="...",
    labels={},
    description="...",
    icon_url="...",
    stdio=None,
    http=None,
    default_enabled_tools=[],
    env_spec=None,
    default_tool_approvals=[],
))
```
</Tab>
<Tab value="Java">
```java
McpServer mcpServer = client.mcpServer().apply(McpServerInput.builder()
    .name("my-mcp-server")
    .org("acme")
    .slug("...")
    .labels(Map.of())
    .description("...")
    .iconUrl("...")
    .stdio(null)
    .http(null)
    .defaultEnabledTools(List.of())
    .envSpec(null)
    .defaultToolApprovals(List.of())
    .build());
```
</Tab>
</SDKTabs>

**Parameters**: [`McpServerInput`](#mcpserverinput)

**Returns**: [`McpServer`](#mcpserver)

### create

Create a new MCP server resource.

Use this when you explicitly want to create a new resource and want an error if it already exists.

Input: McpServer with metadata (scope, org, name/slug) and spec. Returns: The created McpServer with system-generated ID and status.

Authorization: Custom authorization in handler. Requires permission to create MCP servers in the specified scope: - Platform: Requires platform operator role - Organization: Requires org admin role or can_create_mcp_server permission - Identity Account: Automatically allowed for the authenticated user

<SDKTabs>
<Tab value="TypeScript">
```typescript
const mcpServer = await stigmer.mcpServer.create({
  name: "my-mcp-server",
  org: "acme",
  slug: "...",
  labels: {},
  description: "...",
  iconUrl: "...",
  stdio: {},
  http: {},
  defaultEnabledTools: [],
  envSpec: {},
  defaultToolApprovals: [],
});
```
</Tab>
<Tab value="Go">
```go
mcpServer, err := client.McpServer.Create(ctx, &stigmer.McpServerInput{
  Name:                 "my-mcp-server",
  Org:                  "acme",
  Slug:                 "...",
  Labels:               map[string]string{},
  Description:          "...",
  IconUrl:              "...",
  Stdio:                &stigmer.StdioServerConfigInput{},
  Http:                 &stigmer.HttpServerConfigInput{},
  DefaultEnabledTools:  []string{},
  EnvSpec:              &stigmer.EnvSpecInput{},
  DefaultToolApprovals: []stigmer.ToolApprovalPolicyInput{},
})
```
</Tab>
<Tab value="Python">
```python
mcpServer = client.mcp_server.create(McpServerInput(
    name="my-mcp-server",
    org="acme",
    slug="...",
    labels={},
    description="...",
    icon_url="...",
    stdio=None,
    http=None,
    default_enabled_tools=[],
    env_spec=None,
    default_tool_approvals=[],
))
```
</Tab>
<Tab value="Java">
```java
McpServer mcpServer = client.mcpServer().create(McpServerInput.builder()
    .name("my-mcp-server")
    .org("acme")
    .slug("...")
    .labels(Map.of())
    .description("...")
    .iconUrl("...")
    .stdio(null)
    .http(null)
    .defaultEnabledTools(List.of())
    .envSpec(null)
    .defaultToolApprovals(List.of())
    .build());
```
</Tab>
</SDKTabs>

**Parameters**: [`McpServerInput`](#mcpserverinput)

**Returns**: [`McpServer`](#mcpserver)

### update

Update an existing MCP server resource.

Input: McpServer with metadata.id populated and updated spec. Returns: The updated McpServer.

Authorization: Requires can_edit permission on the mcp_server resource. Only the owner (based on scope) can update: - Platform: Platform operators - Organization: Org admins or resource owner - Identity Account: The owner

<SDKTabs>
<Tab value="TypeScript">
```typescript
const mcpServer = await stigmer.mcpServer.update({
  name: "my-mcp-server",
  org: "acme",
  slug: "...",
  labels: {},
  description: "...",
  iconUrl: "...",
  stdio: {},
  http: {},
  defaultEnabledTools: [],
  envSpec: {},
  defaultToolApprovals: [],
});
```
</Tab>
<Tab value="Go">
```go
mcpServer, err := client.McpServer.Update(ctx, &stigmer.McpServerInput{
  Name:                 "my-mcp-server",
  Org:                  "acme",
  Slug:                 "...",
  Labels:               map[string]string{},
  Description:          "...",
  IconUrl:              "...",
  Stdio:                &stigmer.StdioServerConfigInput{},
  Http:                 &stigmer.HttpServerConfigInput{},
  DefaultEnabledTools:  []string{},
  EnvSpec:              &stigmer.EnvSpecInput{},
  DefaultToolApprovals: []stigmer.ToolApprovalPolicyInput{},
})
```
</Tab>
<Tab value="Python">
```python
mcpServer = client.mcp_server.update(McpServerInput(
    name="my-mcp-server",
    org="acme",
    slug="...",
    labels={},
    description="...",
    icon_url="...",
    stdio=None,
    http=None,
    default_enabled_tools=[],
    env_spec=None,
    default_tool_approvals=[],
))
```
</Tab>
<Tab value="Java">
```java
McpServer mcpServer = client.mcpServer().update(McpServerInput.builder()
    .name("my-mcp-server")
    .org("acme")
    .slug("...")
    .labels(Map.of())
    .description("...")
    .iconUrl("...")
    .stdio(null)
    .http(null)
    .defaultEnabledTools(List.of())
    .envSpec(null)
    .defaultToolApprovals(List.of())
    .build());
```
</Tab>
</SDKTabs>

**Parameters**: [`McpServerInput`](#mcpserverinput)

**Returns**: [`McpServer`](#mcpserver)

### updateDiscoveredCapabilities

Update the discovered capabilities (tools and resource templates) for an MCP server.

This is a targeted status update — it only modifies status.discovered_capabilities, leaving spec, validation state, and other status fields untouched.

Typical flow: 1. CLI calls getByReference(org/slug) to fetch the McpServer and its ID 2. CLI connects to the MCP server locally and queries tools/resources 3. CLI calls this RPC with the ID and discovered capabilities

Input: UpdateDiscoveredCapabilitiesInput with mcp_server_id and discovered_capabilities. Returns: The updated McpServer with the new discovered capabilities.

Authorization: Requires can_edit permission on the mcp_server resource.

<SDKTabs>
<Tab value="TypeScript">
```typescript
const mcpServer = await stigmer.mcpServer.updateDiscoveredCapabilities({
  mcpServerId: "...",
  discoveredCapabilities: {},
});
```
</Tab>
<Tab value="Go">
```go
mcpServer, err := client.McpServer.UpdateDiscoveredCapabilities(ctx, &stigmer.UpdateDiscoveredCapabilitiesInput{
  McpServerId:            "...",
  DiscoveredCapabilities: &stigmer.DiscoveredCapabilitiesInput{},
})
```
</Tab>
<Tab value="Python">
```python
mcpServer = client.mcp_server.update_discovered_capabilities(UpdateDiscoveredCapabilitiesInput(
    mcp_server_id="...",
    discovered_capabilities=None,
))
```
</Tab>
<Tab value="Java">
```java
McpServer mcpServer = client.mcpServer().updateDiscoveredCapabilities(UpdateDiscoveredCapabilitiesInput.builder()
    .mcpServerId("...")
    .discoveredCapabilities(null)
    .build());
```
</Tab>
</SDKTabs>

**Parameters**: [`UpdateDiscoveredCapabilitiesInput`](#updatediscoveredcapabilitiesinput)

**Returns**: [`McpServer`](#mcpserver)

### updateVisibility

Update the visibility of an existing MCP server.

This is a targeted metadata update — it only modifies metadata.visibility, leaving spec, status, and other metadata fields untouched. Use this to make an MCP server publicly accessible (marketplace-style sharing) or to revoke public access without sending the entire resource.

Authorization: Requires can_edit permission on the mcp_server resource.

<SDKTabs>
<Tab value="TypeScript">
```typescript
const mcpServer = await stigmer.mcpServer.updateVisibility({
  resourceId: "...",
  visibility: "...",
});
```
</Tab>
<Tab value="Go">
```go
mcpServer, err := client.McpServer.UpdateVisibility(ctx, &stigmer.UpdateVisibilityInput{
  ResourceId: "...",
  Visibility: "...",
})
```
</Tab>
<Tab value="Python">
```python
mcpServer = client.mcp_server.update_visibility(UpdateVisibilityInput(
    resource_id="...",
    visibility="...",
))
```
</Tab>
<Tab value="Java">
```java
McpServer mcpServer = client.mcpServer().updateVisibility(UpdateVisibilityInput.builder()
    .resourceId("...")
    .visibility("...")
    .build());
```
</Tab>
</SDKTabs>

**Parameters**: [`UpdateVisibilityInput`](#updatevisibilityinput)

**Returns**: [`McpServer`](#mcpserver)

### delete

Delete an MCP server resource.

Permanently removes the MCP server definition. Agents referencing this MCP server will need to be updated.

Input: ApiResourceDeleteInput with resource_id and optional version_message. Returns: The deleted McpServer (for confirmation/audit).

Authorization: Requires can_delete permission on the mcp_server resource. Only the owner can delete: - Platform: Platform operators - Organization: Org admins or resource owner - Identity Account: The owner

<SDKTabs>
<Tab value="TypeScript">
```typescript
const mcpServer = await stigmer.mcpServer.delete({ resourceId: id });
```
</Tab>
<Tab value="Go">
```go
mcpServer, err := client.McpServer.Delete(ctx, &stigmer.DeleteResourceInput{
  ResourceID: id,
})
```
</Tab>
<Tab value="Python">
```python
mcpServer = client.mcp_server.delete(DeleteResourceInput(resource_id=id))
```
</Tab>
<Tab value="Java">
```java
McpServer mcpServer = client.mcpServer().delete(DeleteResourceInput.builder()
    .resourceId(id)
    .build());
```
</Tab>
</SDKTabs>

**Parameters**

| Parameter | Type | Description |
|-----------|------|-------------|
| `resourceId` | `string` | ID of the resource to delete. |
| `versionMessage` | `string` | Optional deletion reason. |
| `force` | `boolean` | Force delete even if resource is in use. |

**Returns**: [`McpServer`](#mcpserver)

### discoverCapabilities

Discover the capabilities of an MCP server by connecting to it.

This triggers server-side discovery: the backend resolves credentials, connects to the MCP server, enumerates tools and resource templates, and stores the result.

The RPC blocks until discovery completes (up to ~30 seconds) and returns the updated McpServer with populated status.discovered_capabilities.

<SDKTabs>
<Tab value="TypeScript">
```typescript
const mcpServer = await stigmer.mcpServer.discoverCapabilities({
  mcpServerId: "...",
  runtimeEnv: {},
});
```
</Tab>
<Tab value="Go">
```go
mcpServer, err := client.McpServer.DiscoverCapabilities(ctx, &stigmer.DiscoverCapabilitiesInput{
  McpServerId: "...",
  RuntimeEnv:  map[string]string{},
})
```
</Tab>
<Tab value="Python">
```python
mcpServer = client.mcp_server.discover_capabilities(DiscoverCapabilitiesInput(
    mcp_server_id="...",
    runtime_env={},
))
```
</Tab>
<Tab value="Java">
```java
McpServer mcpServer = client.mcpServer().discoverCapabilities(DiscoverCapabilitiesInput.builder()
    .mcpServerId("...")
    .runtimeEnv(Map.of())
    .build());
```
</Tab>
</SDKTabs>

**Parameters**: [`DiscoverCapabilitiesInput`](#discovercapabilitiesinput)

**Returns**: [`McpServer`](#mcpserver)

## Types

### McpServerInput

Input for creating or updating a MCP Server.

<TypeTable
  type={{
    name: { type: "string", description: "Resource name.", required: true },
    slug: { type: "string", description: "URL-friendly identifier." },
    org: { type: "string", description: "Organization slug.", required: true },
    labels: { type: "Record<string, string>", description: "Key-value labels." },
    description: { type: "string", description: "Human-readable description for marketplace display and documentation." },
    iconUrl: { type: "string", description: "Icon URL for UI display in marketplace and agent configuration screens." },
    stdio: { type: "StdioServerConfigInput", description: "stdio-based server (subprocess with stdin/stdout communication)." },
    http: { type: "HttpServerConfigInput", description: "HTTP-based server (HTTP + Server-Sent Events communication)." },
    defaultEnabledTools: { type: "string[]", description: "Default tools to enable from this MCP server." },
    envSpec: { type: "EnvSpecInput", description: "Environment specification declaring required environment variables." },
    defaultToolApprovals: { type: "ToolApprovalPolicyInput[]", description: "Default tool approval policies for this MCP server." },
  }}
/>

### StdioServerConfigInput

StdioServerConfig defines an MCP server that runs as a subprocess.

<TypeTable
  type={{
    command: { type: "string", description: "Command to execute the MCP server.", required: true },
    args: { type: "string[]", description: "Arguments to pass to the command." },
    workingDir: { type: "string", description: "Working directory for the process." },
  }}
/>

### HttpServerConfigInput

HttpServerConfig defines an MCP server accessible via HTTP + Server-Sent Events.

<TypeTable
  type={{
    url: { type: "string", description: "Base URL of the MCP server endpoint.", required: true },
    headers: { type: "Record<string, string>", description: "HTTP headers to include with every request." },
    queryParams: { type: "Record<string, string>", description: "Query parameters to append to the URL." },
    timeoutSeconds: { type: "number", description: "Timeout for HTTP requests in seconds." },
  }}
/>

### ToolApprovalPolicyInput

ToolApprovalPolicy defines approval requirements for a specific tool.

<TypeTable
  type={{
    toolName: { type: "string", description: "Name of the tool (must match tools/list from MCP server exactly)." },
    message: { type: "string", description: "Human-readable message shown to user when approval is requested." },
  }}
/>

### UpdateVisibilityInput

Input for updating the visibility of any API resource.

<TypeTable
  type={{
    resourceId: { type: "string", description: "ID of the resource whose visibility is being updated.", required: true },
    visibility: { type: "ApiResourceVisibility", description: "The new visibility setting for the resource." },
  }}
/>

### UpdateDiscoveredCapabilitiesInput

UpdateDiscoveredCapabilitiesInput is the request for the updateDiscoveredCapabilities RPC.

<TypeTable
  type={{
    mcpServerId: { type: "string", description: "System-generated ID of the MCP server to update.", required: true },
    discoveredCapabilities: { type: "DiscoveredCapabilitiesInput", description: "Discovered tools and resource templates to store.", required: true },
  }}
/>

### DiscoverCapabilitiesInput

DiscoverCapabilitiesInput is the request for the discoverCapabilities RPC.

<TypeTable
  type={{
    mcpServerId: { type: "string", description: "System-generated ID of the MCP server to discover.", required: true },
    runtimeEnv: { type: "Record<string, EnvVarInput>", description: "Optional environment variable values for one-time discovery." },
  }}
/>

### ApiResourceReference

Generic reference to any API resource by org and slug.

<TypeTable
  type={{
    org: { type: "string", description: "Organization that owns the referenced resource." },
    kind: { type: "ApiResourceKind", description: "Kind of the referenced resource (e.g., SKILL, AGENT, MCP_SERVER)." },
    slug: { type: "string", description: "Resource slug (user-friendly identifier, unique within org).", required: true },
    version: { type: "string", description: "Version of the resource (optional, only applicable to versioned resources like Skills)." },
  }}
/>

### McpServer

McpServer represents a reusable MCP (Model Context Protocol) server configuration.

<TypeTable
  type={{
    apiVersion: { type: "string", description: "API version for this resource type." },
    kind: { type: "string", description: "Resource kind identifier." },
    metadata: { type: "ApiResourceMetadata", description: "Resource metadata including id, name, org, slug, labels, visibility, and timestamps." },
    spec: { type: "McpServerInput", description: "Resource specification. See [`McpServerInput`](#mcpserverinput)." },
    status: { type: "McpServerStatus", description: "System-managed state. See [`McpServerStatus`](#mcpserverstatus)." },
  }}
/>

### McpServerStatus

McpServerStatus represents the system-managed state of an MCP server definition.

<TypeTable
  type={{
    validationState: { type: "ValidationState", description: "Validation state of the MCP server definition." },
    validationMessage: { type: "string", description: "Human-readable validation message." },
    discoveredCapabilities: { type: "DiscoveredCapabilities", description: "Tools and resource templates discovered from the MCP server." },
    audit: { type: "ApiResourceAudit", description: "Standard audit information tracking creation and modification." },
  }}
/>

