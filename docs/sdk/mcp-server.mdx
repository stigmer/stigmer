---
title: MCP Server
description: "SDK reference for MCP Server resources."
---

{/* Auto-generated by stigmer-codegen. DO NOT EDIT. */}

McpServerSpec defines the configuration template for an MCP server. This is a
reusable definition that can be referenced by multiple agents. Actual
secrets/credentials are provided at runtime via AgentInstance's environment.

MCP (Model Context Protocol) servers provide tools and capabilities to AI
agents. This spec declares the server type, connection details, and required
environment variables.

## Client Access

<SDKTabs>
  <Tab value="Go">
    ```go mcpServer, err := client.McpServer.Get(ctx, "mcpServer-id") ```
  </Tab>
  <Tab value="TypeScript">
    ```typescript const mcpServer = await stigmer.mcpServer.get("mcpServer-id");
    ```
  </Tab>
  <Tab value="Python">
    ```python mcpServer = client.mcp_server.get("mcpServer-id") ```
  </Tab>
  <Tab value="Java">
    ```java McpServer mcpServer = client.mcpServer().get("mcpServer-id"); ```
  </Tab>
</SDKTabs>

## Methods

### apply

Create or update an MCP server resource (Kubernetes-style apply).

This is the primary interface for MCP server management. Behavior: - If the
resource doesn't exist: Creates a new MCP server - If the resource exists:
Updates the existing MCP server

The resource is identified by (scope, org, slug) combination.

Input: Full McpServer resource with metadata and spec. Returns: The
created/updated McpServer with system-populated fields.

Authorization: Custom authorization in handler. The handler determines whether
this is a create or update operation and performs appropriate scope-aware
authorization: - Create: Requires permission to create in the target scope -
Update: Requires can_edit on the existing resource

<SDKTabs>
<Tab value="Go">
```go
mcpServer, err := client.McpServer.Apply(ctx, &stigmer.McpServerInput{
  Name: "my-mcpServer",
  Org:  "acme",
})
```
</Tab>
<Tab value="TypeScript">
```typescript
const mcpServer = await stigmer.mcpServer.apply({
  name: "my-mcpServer",
  org: "acme",
});
```
</Tab>
<Tab value="Python">
```python
mcpServer = client.mcp_server.apply(McpServerInput(
    name="my-mcpServer",
    org="acme",
))
```
</Tab>
<Tab value="Java">
```java
McpServer mcpServer = client.mcpServer().apply(McpServerInput.builder()
    .name("my-mcpServer")
    .org("acme")
    .build());
```
</Tab>
</SDKTabs>

**Parameters**: [`McpServerInput`](#mcpserverinput)

**Returns**: `McpServer`

### create

Create a new MCP server resource.

Use this when you explicitly want to create a new resource and want an error if
it already exists.

Input: McpServer with metadata (scope, org, name/slug) and spec. Returns: The
created McpServer with system-generated ID and status.

Authorization: Custom authorization in handler. Requires permission to create
MCP servers in the specified scope: - Platform: Requires platform operator
role - Organization: Requires org admin role or can_create_mcp_server
permission - Identity Account: Automatically allowed for the authenticated user

<SDKTabs>
<Tab value="Go">
```go
mcpServer, err := client.McpServer.Create(ctx, &stigmer.McpServerInput{
  Name: "my-mcpServer",
  Org:  "acme",
})
```
</Tab>
<Tab value="TypeScript">
```typescript
const mcpServer = await stigmer.mcpServer.create({
  name: "my-mcpServer",
  org: "acme",
});
```
</Tab>
<Tab value="Python">
```python
mcpServer = client.mcp_server.create(McpServerInput(
    name="my-mcpServer",
    org="acme",
))
```
</Tab>
<Tab value="Java">
```java
McpServer mcpServer = client.mcpServer().create(McpServerInput.builder()
    .name("my-mcpServer")
    .org("acme")
    .build());
```
</Tab>
</SDKTabs>

**Parameters**: [`McpServerInput`](#mcpserverinput)

**Returns**: `McpServer`

### update

Update an existing MCP server resource.

Input: McpServer with metadata.id populated and updated spec. Returns: The
updated McpServer.

Authorization: Requires can_edit permission on the mcp_server resource. Only the
owner (based on scope) can update: - Platform: Platform operators -
Organization: Org admins or resource owner - Identity Account: The owner

<SDKTabs>
<Tab value="Go">
```go
mcpServer, err := client.McpServer.Update(ctx, &stigmer.McpServerInput{
  Name: "my-mcpServer",
  Org:  "acme",
})
```
</Tab>
<Tab value="TypeScript">
```typescript
const mcpServer = await stigmer.mcpServer.update({
  name: "my-mcpServer",
  org: "acme",
});
```
</Tab>
<Tab value="Python">
```python
mcpServer = client.mcp_server.update(McpServerInput(
    name="my-mcpServer",
    org="acme",
))
```
</Tab>
<Tab value="Java">
```java
McpServer mcpServer = client.mcpServer().update(McpServerInput.builder()
    .name("my-mcpServer")
    .org("acme")
    .build());
```
</Tab>
</SDKTabs>

**Parameters**: [`McpServerInput`](#mcpserverinput)

**Returns**: `McpServer`

### delete

Delete an MCP server resource.

Permanently removes the MCP server definition. Agents referencing this MCP
server will need to be updated.

Input: ApiResourceDeleteInput with resource_id and optional version_message.
Returns: The deleted McpServer (for confirmation/audit).

Authorization: Requires can_delete permission on the mcp_server resource. Only
the owner can delete: - Platform: Platform operators - Organization: Org admins
or resource owner - Identity Account: The owner

<SDKTabs>
<Tab value="Go">
```go
mcpServer, err := client.McpServer.Delete(ctx, &stigmer.DeleteResourceInput{
  ResourceID: id,
})
```
</Tab>
<Tab value="TypeScript">
```typescript
const mcpServer = await stigmer.mcpServer.delete({ resourceId: id });
```
</Tab>
<Tab value="Python">
```python
mcpServer = client.mcp_server.delete(DeleteResourceInput(resource_id=id))
```
</Tab>
<Tab value="Java">
```java
McpServer mcpServer = client.mcpServer().delete(DeleteResourceInput.builder()
    .resourceId(id)
    .build());
```
</Tab>
</SDKTabs>

**Parameters**

| Parameter        | Type      | Description                              |
| ---------------- | --------- | ---------------------------------------- |
| `resourceId`     | `string`  | ID of the resource to delete.            |
| `versionMessage` | `string`  | Optional deletion reason.                |
| `force`          | `boolean` | Force delete even if resource is in use. |

**Returns**: `McpServer`

### updateVisibility

Update the visibility of an existing MCP server.

This is a targeted metadata update — it only modifies metadata.visibility,
leaving spec, status, and other metadata fields untouched. Use this to make an
MCP server publicly accessible (marketplace-style sharing) or to revoke public
access without sending the entire resource.

Authorization: Requires can_edit permission on the mcp_server resource.

<SDKTabs>
  <Tab value="Go">
    ```go mcpServer, err := client.McpServer.UpdateVisibility(ctx, input) ```
  </Tab>
  <Tab value="TypeScript">
    ```typescript const mcpServer = await
    stigmer.mcpServer.updateVisibility(input); ```
  </Tab>
  <Tab value="Python">
    ```python mcpServer = client.mcp_server.update_visibility(input) ```
  </Tab>
  <Tab value="Java">
    ```java McpServer mcpServer = client.mcpServer().updateVisibility(input);
    ```
  </Tab>
</SDKTabs>

**Parameters**: `UpdateVisibilityInput`

**Returns**: `McpServer`

### updateDiscoveredCapabilities

Update the discovered capabilities (tools and resource templates) for an MCP
server.

This is a targeted status update — it only modifies
status.discovered_capabilities, leaving spec, validation state, and other status
fields untouched.

Typical flow: 1. CLI calls getByReference(org/slug) to fetch the McpServer and
its ID 2. CLI connects to the MCP server locally and queries tools/resources 3.
CLI calls this RPC with the ID and discovered capabilities

Input: UpdateDiscoveredCapabilitiesInput with mcp_server_id and
discovered_capabilities. Returns: The updated McpServer with the new discovered
capabilities.

Authorization: Requires can_edit permission on the mcp_server resource.

<SDKTabs>
  <Tab value="Go">
    ```go mcpServer, err := client.McpServer.UpdateDiscoveredCapabilities(ctx,
    input) ```
  </Tab>
  <Tab value="TypeScript">
    ```typescript const mcpServer = await
    stigmer.mcpServer.updateDiscoveredCapabilities(input); ```
  </Tab>
  <Tab value="Python">
    ```python mcpServer =
    client.mcp_server.update_discovered_capabilities(input) ```
  </Tab>
  <Tab value="Java">
    ```java McpServer mcpServer =
    client.mcpServer().updateDiscoveredCapabilities(input); ```
  </Tab>
</SDKTabs>

**Parameters**: `UpdateDiscoveredCapabilitiesInput`

**Returns**: `McpServer`

### discoverCapabilities

Discover the capabilities of an MCP server by connecting to it.

This triggers server-side discovery: the backend resolves credentials from the
authenticated user's personal environment, delegates the actual MCP connection
to the agent-runner via a Temporal workflow, and stores the result.

The RPC blocks until discovery completes (up to ~30 seconds) and returns the
updated McpServer with populated status.discovered_capabilities.

Typical flow: 1. Web console ensures required credentials are saved in the
user's personal environment 2. Web console calls discoverCapabilities with the
MCP server ID 3. Backend resolves env vars from the user's personal
environment 4. Backend starts a Temporal workflow; agent-runner connects to the
MCP server 5. Discovered tools and resource templates are stored and returned

Input: DiscoverCapabilitiesInput with mcp_server_id. Returns: The updated
McpServer with discovered capabilities.

Errors: - FAILED_PRECONDITION: Required credentials missing from personal
environment - DEADLINE_EXCEEDED: Discovery did not complete within the timeout -
NOT_FOUND: MCP server does not exist

Authorization: Requires can_edit permission on the mcp_server resource.

<SDKTabs>
  <Tab value="Go">
    ```go mcpServer, err := client.McpServer.DiscoverCapabilities(ctx, input)
    ```
  </Tab>
  <Tab value="TypeScript">
    ```typescript const mcpServer = await
    stigmer.mcpServer.discoverCapabilities(input); ```
  </Tab>
  <Tab value="Python">
    ```python mcpServer = client.mcp_server.discover_capabilities(input) ```
  </Tab>
  <Tab value="Java">
    ```java McpServer mcpServer =
    client.mcpServer().discoverCapabilities(input); ```
  </Tab>
</SDKTabs>

**Parameters**: `DiscoverCapabilitiesInput`

**Returns**: `McpServer`

### get

Get an MCP server by its unique identifier.

Input: ApiResourceId containing the MCP server's system-generated ID. Returns:
The full McpServer resource including metadata, spec, and status.

Authorization: Requires can_view permission on the mcp_server resource. The
caller must have access based on the resource's scope: - Platform: All
authenticated users - Organization: Organization members - Identity Account:
Only the owner

<SDKTabs>
  <Tab value="Go">
    ```go mcpServer, err := client.McpServer.Get(ctx, id) ```
  </Tab>
  <Tab value="TypeScript">
    ```typescript const mcpServer = await stigmer.mcpServer.get(id); ```
  </Tab>
  <Tab value="Python">```python mcpServer = client.mcp_server.get(id) ```</Tab>
  <Tab value="Java">
    ```java McpServer mcpServer = client.mcpServer().get(id); ```
  </Tab>
</SDKTabs>

**Parameters**

| Parameter | Type     | Description            |
| --------- | -------- | ---------------------- |
| `id`      | `string` | MCP Server identifier. |

**Returns**: `McpServer`

### getByReference

Get an MCP server by reference (scope + org + slug).

Input: ApiResourceReference with scope, org (if applicable), kind, and slug.
Returns: The full McpServer resource.

This is the preferred method for looking up MCP servers by name/slug rather than
system-generated ID. Useful for: - Resolving references from
Agent.mcp_server_usages - CLI lookups by name - Cross-scope resolution (platform
vs org vs personal)

Authorization: Custom authorization in handler. The handler performs scope-aware
authorization based on the reference.

<SDKTabs>
  <Tab value="Go">
    ```go mcpServer, err := client.McpServer.GetByReference(ctx, input) ```
  </Tab>
  <Tab value="TypeScript">
    ```typescript const mcpServer = await
    stigmer.mcpServer.getByReference(input); ```
  </Tab>
  <Tab value="Python">
    ```python mcpServer = client.mcp_server.get_by_reference(input) ```
  </Tab>
  <Tab value="Java">
    ```java McpServer mcpServer = client.mcpServer().getByReference(input); ```
  </Tab>
</SDKTabs>

**Parameters**: `ApiResourceReference`

**Returns**: `McpServer`

## Types

### McpServerInput

Input for creating or updating a MCP Server.

<TypeTable
  type={{
    name: { type: "string", description: "Resource name.", required: true },
    slug: { type: "string", description: "URL-friendly identifier." },
    org: { type: "string", description: "Organization slug.", required: true },
    labels: {
      type: "Record<string, string>",
      description: "Key-value labels.",
    },
    description: {
      type: "string",
      description:
        "Human-readable description for marketplace display and documentation.",
    },
    iconUrl: {
      type: "string",
      description:
        "Icon URL for UI display in marketplace and agent configuration screens.",
    },
    stdio: {
      type: "StdioServerConfigInput",
      description:
        "stdio-based server (subprocess with stdin/stdout communication).",
    },
    http: {
      type: "HttpServerConfigInput",
      description:
        "HTTP-based server (HTTP + Server-Sent Events communication).",
    },
    defaultEnabledTools: {
      type: "string[]",
      description: "Default tools to enable from this MCP server.",
    },
    envSpec: {
      type: "EnvSpecInput",
      description:
        "Environment specification declaring required environment variables.",
    },
    defaultToolApprovals: {
      type: "ToolApprovalPolicyInput[]",
      description: "Default tool approval policies for this MCP server.",
    },
  }}
/>

### StdioServerConfigInput

StdioServerConfig defines an MCP server that runs as a subprocess.

<TypeTable
  type={{
    command: {
      type: "string",
      description: "Command to execute the MCP server.",
      required: true,
    },
    args: {
      type: "string[]",
      description: "Arguments to pass to the command.",
    },
    workingDir: {
      type: "string",
      description: "Working directory for the process.",
    },
  }}
/>

### HttpServerConfigInput

HttpServerConfig defines an MCP server accessible via HTTP + Server-Sent Events.

<TypeTable
  type={{
    url: {
      type: "string",
      description: "Base URL of the MCP server endpoint.",
      required: true,
    },
    headers: {
      type: "Record<string, string>",
      description: "HTTP headers to include with every request.",
    },
    queryParams: {
      type: "Record<string, string>",
      description: "Query parameters to append to the URL.",
    },
    timeoutSeconds: {
      type: "number",
      description: "Timeout for HTTP requests in seconds.",
    },
  }}
/>

### ToolApprovalPolicyInput

ToolApprovalPolicy defines approval requirements for a specific tool.

<TypeTable
  type={{
    toolName: {
      type: "string",
      description:
        "Name of the tool (must match tools/list from MCP server exactly).",
    },
    message: {
      type: "string",
      description:
        "Human-readable message shown to user when approval is requested.",
    },
  }}
/>
