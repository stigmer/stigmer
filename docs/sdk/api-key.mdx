---
title: API Key
description: "SDK reference for API Key resources."
---

{/* Auto-generated by stigmer-codegen. DO NOT EDIT. */}

api-key spec

## Client Access

<SDKTabs>
  <Tab value="Go">
    ```go apiKey, err := client.ApiKey.Get(ctx, "apiKey-id") ```
  </Tab>
  <Tab value="TypeScript">
    ```typescript const apiKey = await stigmer.apiKey.get("apiKey-id"); ```
  </Tab>
  <Tab value="Python">
    ```python apiKey = client.api_key.get("apiKey-id") ```
  </Tab>
  <Tab value="Java">
    ```java ApiKey apiKey = client.apiKey().get("apiKey-id"); ```
  </Tab>
</SDKTabs>

## Methods

### create

create an api-key

<SDKTabs>
<Tab value="Go">
```go
apiKey, err := client.ApiKey.Create(ctx, &stigmer.ApiKeyInput{
  Name: "my-apiKey",
  Org:  "acme",
})
```
</Tab>
<Tab value="TypeScript">
```typescript
const apiKey = await stigmer.apiKey.create({
  name: "my-apiKey",
  org: "acme",
});
```
</Tab>
<Tab value="Python">
```python
apiKey = client.api_key.create(ApiKeyInput(
    name="my-apiKey",
    org="acme",
))
```
</Tab>
<Tab value="Java">
```java
ApiKey apiKey = client.apiKey().create(ApiKeyInput.builder()
    .name("my-apiKey")
    .org("acme")
    .build());
```
</Tab>
</SDKTabs>

**Parameters**: [`ApiKeyInput`](#apikeyinput)

**Returns**: `ApiKey`

### update

update an existing api-key

<SDKTabs>
<Tab value="Go">
```go
apiKey, err := client.ApiKey.Update(ctx, &stigmer.ApiKeyInput{
  Name: "my-apiKey",
  Org:  "acme",
})
```
</Tab>
<Tab value="TypeScript">
```typescript
const apiKey = await stigmer.apiKey.update({
  name: "my-apiKey",
  org: "acme",
});
```
</Tab>
<Tab value="Python">
```python
apiKey = client.api_key.update(ApiKeyInput(
    name="my-apiKey",
    org="acme",
))
```
</Tab>
<Tab value="Java">
```java
ApiKey apiKey = client.apiKey().update(ApiKeyInput.builder()
    .name("my-apiKey")
    .org("acme")
    .build());
```
</Tab>
</SDKTabs>

**Parameters**: [`ApiKeyInput`](#apikeyinput)

**Returns**: `ApiKey`

### delete

delete an existing api-key

<SDKTabs>
  <Tab value="Go">```go apiKey, err := client.ApiKey.Delete(ctx, id) ```</Tab>
  <Tab value="TypeScript">
    ```typescript const apiKey = await stigmer.apiKey.delete(id); ```
  </Tab>
  <Tab value="Python">```python apiKey = client.api_key.delete(id) ```</Tab>
  <Tab value="Java">
    ```java ApiKey apiKey = client.apiKey().delete(id); ```
  </Tab>
</SDKTabs>

**Parameters**

| Parameter | Type     | Description         |
| --------- | -------- | ------------------- |
| `id`      | `string` | API Key identifier. |

**Returns**: `ApiKey`

### get

lookup api-key.

<SDKTabs>
  <Tab value="Go">```go apiKey, err := client.ApiKey.Get(ctx, id) ```</Tab>
  <Tab value="TypeScript">
    ```typescript const apiKey = await stigmer.apiKey.get(id); ```
  </Tab>
  <Tab value="Python">```python apiKey = client.api_key.get(id) ```</Tab>
  <Tab value="Java">```java ApiKey apiKey = client.apiKey().get(id); ```</Tab>
</SDKTabs>

**Parameters**

| Parameter | Type     | Description         |
| --------- | -------- | ------------------- |
| `id`      | `string` | API Key identifier. |

**Returns**: `ApiKey`

### getByKeyHash

lookup api-key by hashed key

<SDKTabs>
  <Tab value="Go">
    ```go apiKey, err := client.ApiKey.GetByKeyHash(ctx, input) ```
  </Tab>
  <Tab value="TypeScript">
    ```typescript const apiKey = await stigmer.apiKey.getByKeyHash(input); ```
  </Tab>
  <Tab value="Python">
    ```python apiKey = client.api_key.get_by_key_hash(input) ```
  </Tab>
  <Tab value="Java">
    ```java ApiKey apiKey = client.apiKey().getByKeyHash(input); ```
  </Tab>
</SDKTabs>

**Parameters**: `ApiKeyHash`

**Returns**: `ApiKey`

### findAll

lookup all api-keys for the identity-account in the auth header.

<SDKTabs>
  <Tab value="Go">```go apiKeys, err := client.ApiKey.FindAll(ctx) ```</Tab>
  <Tab value="TypeScript">
    ```typescript const apiKeys = await stigmer.apiKey.findAll(); ```
  </Tab>
  <Tab value="Python">```python apiKeys = client.api_key.find_all() ```</Tab>
  <Tab value="Java">
    ```java ApiKeys apiKeys = client.apiKey().findAll(); ```
  </Tab>
</SDKTabs>

**Returns**: `ApiKeys`

## Types

### ApiKeyInput

Input for creating or updating a API Key.

<TypeTable
  type={{
    name: { type: "string", description: "Resource name.", required: true },
    slug: { type: "string", description: "URL-friendly identifier." },
    org: { type: "string", description: "Organization slug.", required: true },
    labels: {
      type: "Record<string, string>",
      description: "Key-value labels.",
    },
    keyHash: {
      type: "string",
      description:
        "SHA-256/Bcrypt hash of the raw key generated by the system during.",
    },
    fingerprint: {
      type: "string",
      description: "UI-visible fingerprint (e.g.",
    },
    expiresAt: {
      type: "string",
      description: "absolute expiry time (null if neverExpires = true).",
    },
    neverExpires: {
      type: "boolean",
      description: "never expire flag (forces expires_at to be ignored).",
    },
  }}
/>
