---
title: API Key
description: "SDK reference for API Key resources."
---

{/* Auto-generated by stigmer-codegen. DO NOT EDIT. */}

API keys provide programmatic access to the Stigmer API. Each key is scoped to an identity account and can have an optional expiration time.

## Client Access

<SDKTabs>
<Tab value="TypeScript">
```typescript
const apiKey = await stigmer.apiKey.get("key_01j5q3k7m8r2s4tnz2hfp0q0c3");
```
</Tab>
<Tab value="Go">
```go
apiKey, err := client.ApiKey.Get(ctx, "key_01j5q3k7m8r2s4tnz2hfp0q0c3")
```
</Tab>
<Tab value="Python">
```python
apiKey = client.api_key.get("key_01j5q3k7m8r2s4tnz2hfp0q0c3")
```
</Tab>
<Tab value="Java">
```java
ApiKey apiKey = client.apiKey().get("key_01j5q3k7m8r2s4tnz2hfp0q0c3");
```
</Tab>
</SDKTabs>

## Methods

| Method | Description |
|--------|-------------|
| [`get`](#get) | Get an API key by its unique identifier. |
| [`getByKeyHash`](#getbykeyhash) | Get an API key by its hashed key value. |
| [`findAll`](#findall) | List all API keys belonging to the authenticated user. |
| [`create`](#create) | Create a new API key for the authenticated user. |
| [`update`](#update) | Update an existing API key. |
| [`delete`](#delete) | Delete an API key. |

### get

Get an API key by its unique identifier.

<SDKTabs>
<Tab value="TypeScript">
```typescript
const apiKey = await stigmer.apiKey.get(id);
```
</Tab>
<Tab value="Go">
```go
apiKey, err := client.ApiKey.Get(ctx, id)
```
</Tab>
<Tab value="Python">
```python
apiKey = client.api_key.get(id)
```
</Tab>
<Tab value="Java">
```java
ApiKey apiKey = client.apiKey().get(id);
```
</Tab>
</SDKTabs>

**Parameters**

| Parameter | Type | Description |
|-----------|------|-------------|
| `id` | `string` | API Key identifier. |

**Returns**: [`ApiKey`](#apikey)

### getByKeyHash

Get an API key by its hashed key value.

<SDKTabs>
<Tab value="TypeScript">
```typescript
const apiKey = await stigmer.apiKey.getByKeyHash({
  value: "...",
});
```
</Tab>
<Tab value="Go">
```go
apiKey, err := client.ApiKey.GetByKeyHash(ctx, &stigmer.ApiKeyHash{
  Value: "...",
})
```
</Tab>
<Tab value="Python">
```python
apiKey = client.api_key.get_by_key_hash(ApiKeyHash(
    value="...",
))
```
</Tab>
<Tab value="Java">
```java
ApiKey apiKey = client.apiKey().getByKeyHash(ApiKeyHash.builder()
    .value("...")
    .build());
```
</Tab>
</SDKTabs>

**Parameters**: [`ApiKeyHash`](#apikeyhash)

**Returns**: [`ApiKey`](#apikey)

### findAll

List all API keys belonging to the authenticated user.

<SDKTabs>
<Tab value="TypeScript">
```typescript
const apiKeys = await stigmer.apiKey.findAll();
```
</Tab>
<Tab value="Go">
```go
apiKeys, err := client.ApiKey.FindAll(ctx)
```
</Tab>
<Tab value="Python">
```python
apiKeys = client.api_key.find_all()
```
</Tab>
<Tab value="Java">
```java
ApiKeys apiKeys = client.apiKey().findAll();
```
</Tab>
</SDKTabs>

**Returns**: [`ApiKeys`](#apikeys)

### create

Create a new API key for the authenticated user. Any authenticated user can create API keys.

<SDKTabs>
<Tab value="TypeScript">
```typescript
const apiKey = await stigmer.apiKey.create({
  name: "my-api-key",
  org: "acme",
  slug: "...",
  labels: {},
  keyHash: "...",
  fingerprint: "...",
  expiresAt: "...",
  neverExpires: false,
});
```
</Tab>
<Tab value="Go">
```go
apiKey, err := client.ApiKey.Create(ctx, &stigmer.ApiKeyInput{
  Name:         "my-api-key",
  Org:          "acme",
  Slug:         "...",
  Labels:       map[string]string{},
  KeyHash:      "...",
  Fingerprint:  "...",
  ExpiresAt:    "...",
  NeverExpires: false,
})
```
</Tab>
<Tab value="Python">
```python
apiKey = client.api_key.create(ApiKeyInput(
    name="my-api-key",
    org="acme",
    slug="...",
    labels={},
    key_hash="...",
    fingerprint="...",
    expires_at="...",
    never_expires=False,
))
```
</Tab>
<Tab value="Java">
```java
ApiKey apiKey = client.apiKey().create(ApiKeyInput.builder()
    .name("my-api-key")
    .org("acme")
    .slug("...")
    .labels(Map.of())
    .keyHash("...")
    .fingerprint("...")
    .expiresAt("...")
    .neverExpires(false)
    .build());
```
</Tab>
</SDKTabs>

**Parameters**: [`ApiKeyInput`](#apikeyinput)

**Returns**: [`ApiKey`](#apikey)

### update

Update an existing API key.

<SDKTabs>
<Tab value="TypeScript">
```typescript
const apiKey = await stigmer.apiKey.update({
  name: "my-api-key",
  org: "acme",
  slug: "...",
  labels: {},
  keyHash: "...",
  fingerprint: "...",
  expiresAt: "...",
  neverExpires: false,
});
```
</Tab>
<Tab value="Go">
```go
apiKey, err := client.ApiKey.Update(ctx, &stigmer.ApiKeyInput{
  Name:         "my-api-key",
  Org:          "acme",
  Slug:         "...",
  Labels:       map[string]string{},
  KeyHash:      "...",
  Fingerprint:  "...",
  ExpiresAt:    "...",
  NeverExpires: false,
})
```
</Tab>
<Tab value="Python">
```python
apiKey = client.api_key.update(ApiKeyInput(
    name="my-api-key",
    org="acme",
    slug="...",
    labels={},
    key_hash="...",
    fingerprint="...",
    expires_at="...",
    never_expires=False,
))
```
</Tab>
<Tab value="Java">
```java
ApiKey apiKey = client.apiKey().update(ApiKeyInput.builder()
    .name("my-api-key")
    .org("acme")
    .slug("...")
    .labels(Map.of())
    .keyHash("...")
    .fingerprint("...")
    .expiresAt("...")
    .neverExpires(false)
    .build());
```
</Tab>
</SDKTabs>

**Parameters**: [`ApiKeyInput`](#apikeyinput)

**Returns**: [`ApiKey`](#apikey)

### delete

Delete an API key.

<SDKTabs>
<Tab value="TypeScript">
```typescript
const apiKey = await stigmer.apiKey.delete(id);
```
</Tab>
<Tab value="Go">
```go
apiKey, err := client.ApiKey.Delete(ctx, id)
```
</Tab>
<Tab value="Python">
```python
apiKey = client.api_key.delete(id)
```
</Tab>
<Tab value="Java">
```java
ApiKey apiKey = client.apiKey().delete(id);
```
</Tab>
</SDKTabs>

**Parameters**

| Parameter | Type | Description |
|-----------|------|-------------|
| `id` | `string` | API Key identifier. |

**Returns**: [`ApiKey`](#apikey)

## Types

### ApiKeyInput

Input for creating or updating an API Key.

<TypeTable
  type={{
    name: { type: "string", description: "Resource name.", required: true },
    slug: { type: "string", description: "URL-friendly identifier." },
    org: { type: "string", description: "Organization slug.", required: true },
    labels: { type: "Record<string, string>", description: "Key-value labels." },
    keyHash: { type: "string", description: "Hash of the raw API key." },
    fingerprint: { type: "string", description: "Short fingerprint of the key for display purposes (last 6 characters)." },
    expiresAt: { type: "string", description: "Expiration time for the API key." },
    neverExpires: { type: "boolean", description: "When true, the API key never expires regardless of expires_at." },
  }}
/>

### ApiKeyHash

wrapper for hashed api key.

<TypeTable
  type={{
    value: { type: "string", description: "", required: true },
  }}
/>

### ApiKeys

wrapper to get list of api keys.

<TypeTable
  type={{
    entries: { type: "ApiKeyInput[]", description: "", typeDescriptionLink: "#apikeyinput" },
  }}
/>

### ApiKey

api-key for user-account or machine-account.

<TypeTable
  type={{
    apiVersion: { type: "string", description: "API version for this resource type." },
    kind: { type: "string", description: "Resource kind identifier." },
    metadata: { type: "ApiResourceMetadata", description: "Resource metadata including id, name, org, slug, labels, visibility, and timestamps." },
    spec: { type: "ApiKeyInput", description: "Resource specification. See [`ApiKeyInput`](#apikeyinput).", typeDescriptionLink: "#apikeyinput" },
    status: { type: "ApiKeyStatus", description: "System-managed state. See [`ApiKeyStatus`](#apikeystatus).", typeDescriptionLink: "#apikeystatus" },
  }}
/>

### ApiKeyStatus

<TypeTable
  type={{
    audit: { type: "ApiResourceAudit", description: "audit.", typeDescriptionLink: "#apiresourceaudit" },
    lastUsedAt: { type: "string", description: "last used timestamp for the api-key." },
  }}
/>

### ApiResourceAudit

ApiResourceAudit contains both spec-level and status-level audit details.

<TypeTable
  type={{
    specAudit: { type: "ApiResourceAuditInfo", description: "Audit info for the resource's specification (desired state).", typeDescriptionLink: "#apiresourceauditinfo" },
    statusAudit: { type: "ApiResourceAuditInfo", description: "Audit info for the resource's status (actual state).", typeDescriptionLink: "#apiresourceauditinfo" },
  }}
/>

### ApiResourceAuditInfo

ApiResourceAuditInfo contains detailed audit information.

<TypeTable
  type={{
    createdBy: { type: "ApiResourceAuditActor", description: "Actor who created this resource.", typeDescriptionLink: "#apiresourceauditactor" },
    createdAt: { type: "string", description: "Timestamp when the resource was created." },
    updatedBy: { type: "ApiResourceAuditActor", description: "Actor who last updated this resource.", typeDescriptionLink: "#apiresourceauditactor" },
    updatedAt: { type: "string", description: "Timestamp when the resource was last updated." },
    event: { type: "string", description: "Type of the last event (e.g., \"created\", \"updated\")." },
  }}
/>

### ApiResourceAuditActor

ApiResourceAuditActor represents the entity that performed an action.

<TypeTable
  type={{
    id: { type: "string", description: "Unique identifier of the actor." },
    avatar: { type: "string", description: "URL to the actor's avatar image." },
  }}
/>

### ApiResourceVisibility

Visibility controls who can access an API resource.

| Value | Description |
|-------|-------------|
| `visibility_private` | Only members of the owning organization can access. |
| `visibility_public` | Anyone can access (read) this resource. |

