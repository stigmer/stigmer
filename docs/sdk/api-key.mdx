---
title: API Key
description: "SDK reference for API Key resources."
---

{/* Auto-generated by stigmer-codegen. DO NOT EDIT. */}

## Client Access

<SDKTabs>
<Tab value="Go">
```go
apiKey, err := client.ApiKey.Get(ctx, "api-key-abc123")
```
</Tab>
<Tab value="TypeScript">
```typescript
const apiKey = await stigmer.apiKey.get("api-key-abc123");
```
</Tab>
<Tab value="Python">
```python
apiKey = client.api_key.get("api-key-abc123")
```
</Tab>
<Tab value="Java">
```java
ApiKey apiKey = client.apiKey().get("api-key-abc123");
```
</Tab>
</SDKTabs>

## Methods

| Method | Description |
|--------|-------------|
| [`get`](#get) | lookup api-key. |
| [`getByKeyHash`](#getbykeyhash) | lookup api-key by hashed key. |
| [`findAll`](#findall) | lookup all api-keys for the identity-account in the auth header. |
| [`create`](#create) | create an api-key. |
| [`update`](#update) | update an existing api-key. |
| [`delete`](#delete) | delete an existing api-key. |

### get

lookup api-key.

<SDKTabs>
<Tab value="Go">
```go
apiKey, err := client.ApiKey.Get(ctx, id)
```
</Tab>
<Tab value="TypeScript">
```typescript
const apiKey = await stigmer.apiKey.get(id);
```
</Tab>
<Tab value="Python">
```python
apiKey = client.api_key.get(id)
```
</Tab>
<Tab value="Java">
```java
ApiKey apiKey = client.apiKey().get(id);
```
</Tab>
</SDKTabs>

**Parameters**

| Parameter | Type | Description |
|-----------|------|-------------|
| `id` | `string` | API Key identifier. |

**Returns**: `ApiKey`

### getByKeyHash

lookup api-key by hashed key

<SDKTabs>
<Tab value="Go">
```go
apiKey, err := client.ApiKey.GetByKeyHash(ctx, input)
```
</Tab>
<Tab value="TypeScript">
```typescript
const apiKey = await stigmer.apiKey.getByKeyHash(input);
```
</Tab>
<Tab value="Python">
```python
apiKey = client.api_key.get_by_key_hash(input)
```
</Tab>
<Tab value="Java">
```java
ApiKey apiKey = client.apiKey().getByKeyHash(input);
```
</Tab>
</SDKTabs>

**Parameters**: `ApiKeyHash`

**Returns**: `ApiKey`

### findAll

lookup all api-keys for the identity-account in the auth header.

<SDKTabs>
<Tab value="Go">
```go
apiKeys, err := client.ApiKey.FindAll(ctx)
```
</Tab>
<Tab value="TypeScript">
```typescript
const apiKeys = await stigmer.apiKey.findAll();
```
</Tab>
<Tab value="Python">
```python
apiKeys = client.api_key.find_all()
```
</Tab>
<Tab value="Java">
```java
ApiKeys apiKeys = client.apiKey().findAll();
```
</Tab>
</SDKTabs>

**Returns**: `ApiKeys`

### create

create an api-key

<SDKTabs>
<Tab value="Go">
```go
apiKey, err := client.ApiKey.Create(ctx, &stigmer.ApiKeyInput{
  Name: "my-api-key",
  Org:  "acme",
})
```
</Tab>
<Tab value="TypeScript">
```typescript
const apiKey = await stigmer.apiKey.create({
  name: "my-api-key",
  org: "acme",
});
```
</Tab>
<Tab value="Python">
```python
apiKey = client.api_key.create(ApiKeyInput(
    name="my-api-key",
    org="acme",
))
```
</Tab>
<Tab value="Java">
```java
ApiKey apiKey = client.apiKey().create(ApiKeyInput.builder()
    .name("my-api-key")
    .org("acme")
    .build());
```
</Tab>
</SDKTabs>

**Parameters**: [`ApiKeyInput`](#apikeyinput)

**Returns**: `ApiKey`

### update

update an existing api-key

<SDKTabs>
<Tab value="Go">
```go
apiKey, err := client.ApiKey.Update(ctx, &stigmer.ApiKeyInput{
  Name: "my-api-key",
  Org:  "acme",
})
```
</Tab>
<Tab value="TypeScript">
```typescript
const apiKey = await stigmer.apiKey.update({
  name: "my-api-key",
  org: "acme",
});
```
</Tab>
<Tab value="Python">
```python
apiKey = client.api_key.update(ApiKeyInput(
    name="my-api-key",
    org="acme",
))
```
</Tab>
<Tab value="Java">
```java
ApiKey apiKey = client.apiKey().update(ApiKeyInput.builder()
    .name("my-api-key")
    .org("acme")
    .build());
```
</Tab>
</SDKTabs>

**Parameters**: [`ApiKeyInput`](#apikeyinput)

**Returns**: `ApiKey`

### delete

delete an existing api-key

<SDKTabs>
<Tab value="Go">
```go
apiKey, err := client.ApiKey.Delete(ctx, id)
```
</Tab>
<Tab value="TypeScript">
```typescript
const apiKey = await stigmer.apiKey.delete(id);
```
</Tab>
<Tab value="Python">
```python
apiKey = client.api_key.delete(id)
```
</Tab>
<Tab value="Java">
```java
ApiKey apiKey = client.apiKey().delete(id);
```
</Tab>
</SDKTabs>

**Parameters**

| Parameter | Type | Description |
|-----------|------|-------------|
| `id` | `string` | API Key identifier. |

**Returns**: `ApiKey`

## Types

### ApiKeyInput

Input for creating or updating an API Key.

<TypeTable
  type={{
    name: { type: "string", description: "Resource name.", required: true },
    slug: { type: "string", description: "URL-friendly identifier." },
    org: { type: "string", description: "Organization slug.", required: true },
    labels: { type: "Record<string, string>", description: "Key-value labels." },
    keyHash: { type: "string", description: "SHA-256/Bcrypt hash of the raw key generated by the system during." },
    fingerprint: { type: "string", description: "UI-visible fingerprint (e.g. last 6 chars)." },
    expiresAt: { type: "string", description: "absolute expiry time (null if neverExpires = true)." },
    neverExpires: { type: "boolean", description: "never expire flag (forces expires_at to be ignored)." },
  }}
/>

