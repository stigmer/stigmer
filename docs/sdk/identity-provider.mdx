---
title: Identity Provider
description: "SDK reference for Identity Provider resources."
---

{/* Auto-generated by stigmer-codegen. DO NOT EDIT. */}

IdentityProviderSpec defines the configuration for an external identity
provider.

An IdentityProvider represents an external platform's trust relationship with
Stigmer. It is owned by an organization (e.g., "planton") and configures how
Stigmer validates tokens from that platform during token exchange. The platform
forwards its OIDC provider's access tokens to Stigmer's token exchange endpoint,
which: - Validates the token signature against the configured JWKS - Fetches
user profile data from the OIDC UserInfo endpoint - JIT-provisions a federated
identity account with email, name, and picture - Issues a Stigmer-native token
for subsequent API access

The spec contains only public validation configuration — no secrets are stored.
For OIDC-based integrators (e.g., Auth0), the jwks_uri and userinfo_endpoint
point to the OIDC provider's standard endpoints.

Example YAML: apiVersion: iam.stigmer.ai/v1 kind: IdentityProvider metadata:
name: Planton Cloud slug: planton-cloud org: planton spec: display_name:
"Planton Cloud" jwks_uri:
"https://planton-prod.us.auth0.com/.well-known/jwks.json" allowed_issuers:
["https://planton-prod.us.auth0.com/"] expected_audience:
"https://api.planton.ai/" userinfo_endpoint:
"https://planton-prod.us.auth0.com/userinfo"

## Client Access

<SDKTabs>
  <Tab value="Go">
    ```go identityProvider, err := client.IdentityProvider.Get(ctx,
    "identityProvider-id") ```
  </Tab>
  <Tab value="TypeScript">
    ```typescript const identityProvider = await
    stigmer.identityProvider.get("identityProvider-id"); ```
  </Tab>
  <Tab value="Python">
    ```python identityProvider =
    client.identity_provider.get("identityProvider-id") ```
  </Tab>
  <Tab value="Java">
    ```java IdentityProvider identityProvider =
    client.identityProvider().get("identityProvider-id"); ```
  </Tab>
</SDKTabs>

## Methods

### apply

Create or update an identity provider (Kubernetes-style apply). If the resource
doesn't exist: creates a new identity provider. If the resource exists: updates
the existing identity provider.

<SDKTabs>
<Tab value="Go">
```go
identityProvider, err := client.IdentityProvider.Apply(ctx, &stigmer.IdentityProviderInput{
  Name: "my-identityProvider",
  Org:  "acme",
})
```
</Tab>
<Tab value="TypeScript">
```typescript
const identityProvider = await stigmer.identityProvider.apply({
  name: "my-identityProvider",
  org: "acme",
});
```
</Tab>
<Tab value="Python">
```python
identityProvider = client.identity_provider.apply(IdentityProviderInput(
    name="my-identityProvider",
    org="acme",
))
```
</Tab>
<Tab value="Java">
```java
IdentityProvider identityProvider = client.identityProvider().apply(IdentityProviderInput.builder()
    .name("my-identityProvider")
    .org("acme")
    .build());
```
</Tab>
</SDKTabs>

**Parameters**: [`IdentityProviderInput`](#identityproviderinput)

**Returns**: `IdentityProvider`

### create

Create a new identity provider. The creator's organization owns the identity
provider.

<SDKTabs>
<Tab value="Go">
```go
identityProvider, err := client.IdentityProvider.Create(ctx, &stigmer.IdentityProviderInput{
  Name: "my-identityProvider",
  Org:  "acme",
})
```
</Tab>
<Tab value="TypeScript">
```typescript
const identityProvider = await stigmer.identityProvider.create({
  name: "my-identityProvider",
  org: "acme",
});
```
</Tab>
<Tab value="Python">
```python
identityProvider = client.identity_provider.create(IdentityProviderInput(
    name="my-identityProvider",
    org="acme",
))
```
</Tab>
<Tab value="Java">
```java
IdentityProvider identityProvider = client.identityProvider().create(IdentityProviderInput.builder()
    .name("my-identityProvider")
    .org("acme")
    .build());
```
</Tab>
</SDKTabs>

**Parameters**: [`IdentityProviderInput`](#identityproviderinput)

**Returns**: `IdentityProvider`

### update

Update an existing identity provider. Requires can_edit permission on the
identity provider.

<SDKTabs>
<Tab value="Go">
```go
identityProvider, err := client.IdentityProvider.Update(ctx, &stigmer.IdentityProviderInput{
  Name: "my-identityProvider",
  Org:  "acme",
})
```
</Tab>
<Tab value="TypeScript">
```typescript
const identityProvider = await stigmer.identityProvider.update({
  name: "my-identityProvider",
  org: "acme",
});
```
</Tab>
<Tab value="Python">
```python
identityProvider = client.identity_provider.update(IdentityProviderInput(
    name="my-identityProvider",
    org="acme",
))
```
</Tab>
<Tab value="Java">
```java
IdentityProvider identityProvider = client.identityProvider().update(IdentityProviderInput.builder()
    .name("my-identityProvider")
    .org("acme")
    .build());
```
</Tab>
</SDKTabs>

**Parameters**: [`IdentityProviderInput`](#identityproviderinput)

**Returns**: `IdentityProvider`

### delete

Delete an identity provider. Deletion is blocked if any platform-managed
organizations reference this identity provider.

<SDKTabs>
<Tab value="Go">
```go
identityProvider, err := client.IdentityProvider.Delete(ctx, &stigmer.DeleteResourceInput{
  ResourceID: id,
})
```
</Tab>
<Tab value="TypeScript">
```typescript
const identityProvider = await stigmer.identityProvider.delete({ resourceId: id });
```
</Tab>
<Tab value="Python">
```python
identityProvider = client.identity_provider.delete(DeleteResourceInput(resource_id=id))
```
</Tab>
<Tab value="Java">
```java
IdentityProvider identityProvider = client.identityProvider().delete(DeleteResourceInput.builder()
    .resourceId(id)
    .build());
```
</Tab>
</SDKTabs>

**Parameters**

| Parameter        | Type      | Description                              |
| ---------------- | --------- | ---------------------------------------- |
| `resourceId`     | `string`  | ID of the resource to delete.            |
| `versionMessage` | `string`  | Optional deletion reason.                |
| `force`          | `boolean` | Force delete even if resource is in use. |

**Returns**: `IdentityProvider`

### get

Get an identity provider by its unique identifier.

<SDKTabs>
  <Tab value="Go">
    ```go identityProvider, err := client.IdentityProvider.Get(ctx, id) ```
  </Tab>
  <Tab value="TypeScript">
    ```typescript const identityProvider = await
    stigmer.identityProvider.get(id); ```
  </Tab>
  <Tab value="Python">
    ```python identityProvider = client.identity_provider.get(id) ```
  </Tab>
  <Tab value="Java">
    ```java IdentityProvider identityProvider =
    client.identityProvider().get(id); ```
  </Tab>
</SDKTabs>

**Parameters**

| Parameter | Type     | Description                   |
| --------- | -------- | ----------------------------- |
| `id`      | `string` | Identity Provider identifier. |

**Returns**: `IdentityProvider`

### getByReference

Get an identity provider by reference (org + slug). Custom authorization in
handler.

<SDKTabs>
  <Tab value="Go">
    ```go identityProvider, err := client.IdentityProvider.GetByReference(ctx,
    input) ```
  </Tab>
  <Tab value="TypeScript">
    ```typescript const identityProvider = await
    stigmer.identityProvider.getByReference(input); ```
  </Tab>
  <Tab value="Python">
    ```python identityProvider =
    client.identity_provider.get_by_reference(input) ```
  </Tab>
  <Tab value="Java">
    ```java IdentityProvider identityProvider =
    client.identityProvider().getByReference(input); ```
  </Tab>
</SDKTabs>

**Parameters**: `ApiResourceReference`

**Returns**: `IdentityProvider`

## Types

### IdentityProviderInput

Input for creating or updating a Identity Provider.

<TypeTable
  type={{
    name: { type: "string", description: "Resource name.", required: true },
    slug: { type: "string", description: "URL-friendly identifier." },
    org: { type: "string", description: "Organization slug.", required: true },
    labels: {
      type: "Record<string, string>",
      description: "Key-value labels.",
    },
    displayName: {
      type: "string",
      description: "Human-readable display name for this identity provider.",
    },
    jwksUri: {
      type: "string",
      description:
        "JWKS endpoint URL for validating tokens presented during token exchange.",
    },
    allowedIssuers: {
      type: "string[]",
      description:
        "JWT issuer values that Stigmer will accept from this platform.",
    },
    expectedAudience: {
      type: "string",
      description: "Expected JWT audience value.",
    },
    rateLimitBudget: {
      type: "number",
      description:
        "Shared rate limit budget across all organizations managed via this identity provider.",
    },
    userinfoEndpoint: {
      type: "string",
      description:
        "OIDC UserInfo endpoint URL for fetching user profile data during token exchange.",
    },
  }}
/>
