---
title: Identity Provider
description: "SDK reference for Identity Provider resources."
---

{/* Auto-generated by stigmer-codegen. DO NOT EDIT. */}

An IdentityProvider represents an external platform's trust relationship with Stigmer. It is owned by an organization (e.g., "planton") and configures how Stigmer validates tokens from that platform during token exchange. The platform forwards its OIDC provider's access tokens to Stigmer's token exchange endpoint, which: - Validates the token signature against the configured JWKS - Fetches user profile data from the OIDC UserInfo endpoint - JIT-provisions a federated identity account with email, name, and picture - Issues a Stigmer-native token for subsequent API access

The spec contains only public validation configuration — no secrets are stored. For OIDC-based integrators (e.g., Auth0), the jwks_uri and userinfo_endpoint point to the OIDC provider's standard endpoints.

Example YAML: apiVersion: iam.stigmer.ai/v1 kind: IdentityProvider metadata: name: Planton Cloud slug: planton-cloud org: planton spec: display_name: "Planton Cloud" jwks_uri: "https://planton-prod.us.auth0.com/.well-known/jwks.json" allowed_issuers: ["https://planton-prod.us.auth0.com/"] expected_audience: "https://api.planton.ai/" userinfo_endpoint: "https://planton-prod.us.auth0.com/userinfo"

## Client Access

<SDKTabs>
<Tab value="Go">
```go
identityProvider, err := client.IdentityProvider.Get(ctx, "idp_01j5q3k7m8r2s4tnz2hfp0q0c3")
```
</Tab>
<Tab value="TypeScript">
```typescript
const identityProvider = await stigmer.identityProvider.get("idp_01j5q3k7m8r2s4tnz2hfp0q0c3");
```
</Tab>
<Tab value="Python">
```python
identityProvider = client.identity_provider.get("idp_01j5q3k7m8r2s4tnz2hfp0q0c3")
```
</Tab>
<Tab value="Java">
```java
IdentityProvider identityProvider = client.identityProvider().get("idp_01j5q3k7m8r2s4tnz2hfp0q0c3");
```
</Tab>
</SDKTabs>

## Methods

| Method | Description |
|--------|-------------|
| [`get`](#get) | Get an identity provider by its unique identifier. |
| [`getByReference`](#getbyreference) | Get an identity provider by reference (org + slug). |
| [`apply`](#apply) | Create or update an identity provider (Kubernetes-style apply). |
| [`create`](#create) | Create a new identity provider. |
| [`update`](#update) | Update an existing identity provider. |
| [`delete`](#delete) | Delete an identity provider. |

### get

Get an identity provider by its unique identifier.

<SDKTabs>
<Tab value="Go">
```go
identityProvider, err := client.IdentityProvider.Get(ctx, id)
```
</Tab>
<Tab value="TypeScript">
```typescript
const identityProvider = await stigmer.identityProvider.get(id);
```
</Tab>
<Tab value="Python">
```python
identityProvider = client.identity_provider.get(id)
```
</Tab>
<Tab value="Java">
```java
IdentityProvider identityProvider = client.identityProvider().get(id);
```
</Tab>
</SDKTabs>

**Parameters**

| Parameter | Type | Description |
|-----------|------|-------------|
| `id` | `string` | Identity Provider identifier. |

**Returns**: `IdentityProvider`

### getByReference

Get an identity provider by reference (org + slug).

<SDKTabs>
<Tab value="Go">
```go
identityProvider, err := client.IdentityProvider.GetByReference(ctx, &stigmer.ApiResourceReference{
  Org:     "...",
  Kind:    "...",
  Slug:    "...",
  Version: "...",
})
```
</Tab>
<Tab value="TypeScript">
```typescript
const identityProvider = await stigmer.identityProvider.getByReference({
  org: "...",
  kind: "...",
  slug: "...",
  version: "...",
});
```
</Tab>
<Tab value="Python">
```python
identityProvider = client.identity_provider.get_by_reference(ApiResourceReference(
    org="...",
    kind="...",
    slug="...",
    version="...",
))
```
</Tab>
<Tab value="Java">
```java
IdentityProvider identityProvider = client.identityProvider().getByReference(ApiResourceReference.builder()
    .org("...")
    .kind("...")
    .slug("...")
    .version("...")
    .build());
```
</Tab>
</SDKTabs>

**Parameters**: [`ApiResourceReference`](#apiresourcereference)

**Returns**: `IdentityProvider`

### apply

Create or update an identity provider (Kubernetes-style apply). If the resource doesn't exist: creates a new identity provider. If the resource exists: updates the existing identity provider.

<SDKTabs>
<Tab value="Go">
```go
identityProvider, err := client.IdentityProvider.Apply(ctx, &stigmer.IdentityProviderInput{
  Name:             "my-identity-provider",
  Org:              "acme",
  Slug:             "...",
  Labels:           map[string]string{},
  DisplayName:      "...",
  JwksUri:          "...",
  AllowedIssuers:   []string{},
  ExpectedAudience: "...",
  RateLimitBudget:  0,
  UserinfoEndpoint: "...",
})
```
</Tab>
<Tab value="TypeScript">
```typescript
const identityProvider = await stigmer.identityProvider.apply({
  name: "my-identity-provider",
  org: "acme",
  slug: "...",
  labels: {},
  displayName: "...",
  jwksUri: "...",
  allowedIssuers: [],
  expectedAudience: "...",
  rateLimitBudget: 0,
  userinfoEndpoint: "...",
});
```
</Tab>
<Tab value="Python">
```python
identityProvider = client.identity_provider.apply(IdentityProviderInput(
    name="my-identity-provider",
    org="acme",
    slug="...",
    labels={},
    display_name="...",
    jwks_uri="...",
    allowed_issuers=[],
    expected_audience="...",
    rate_limit_budget=0,
    userinfo_endpoint="...",
))
```
</Tab>
<Tab value="Java">
```java
IdentityProvider identityProvider = client.identityProvider().apply(IdentityProviderInput.builder()
    .name("my-identity-provider")
    .org("acme")
    .slug("...")
    .labels(Map.of())
    .displayName("...")
    .jwksUri("...")
    .allowedIssuers(List.of())
    .expectedAudience("...")
    .rateLimitBudget(0)
    .userinfoEndpoint("...")
    .build());
```
</Tab>
</SDKTabs>

**Parameters**: [`IdentityProviderInput`](#identityproviderinput)

**Returns**: `IdentityProvider`

### create

Create a new identity provider. The creator's organization owns the identity provider.

<SDKTabs>
<Tab value="Go">
```go
identityProvider, err := client.IdentityProvider.Create(ctx, &stigmer.IdentityProviderInput{
  Name:             "my-identity-provider",
  Org:              "acme",
  Slug:             "...",
  Labels:           map[string]string{},
  DisplayName:      "...",
  JwksUri:          "...",
  AllowedIssuers:   []string{},
  ExpectedAudience: "...",
  RateLimitBudget:  0,
  UserinfoEndpoint: "...",
})
```
</Tab>
<Tab value="TypeScript">
```typescript
const identityProvider = await stigmer.identityProvider.create({
  name: "my-identity-provider",
  org: "acme",
  slug: "...",
  labels: {},
  displayName: "...",
  jwksUri: "...",
  allowedIssuers: [],
  expectedAudience: "...",
  rateLimitBudget: 0,
  userinfoEndpoint: "...",
});
```
</Tab>
<Tab value="Python">
```python
identityProvider = client.identity_provider.create(IdentityProviderInput(
    name="my-identity-provider",
    org="acme",
    slug="...",
    labels={},
    display_name="...",
    jwks_uri="...",
    allowed_issuers=[],
    expected_audience="...",
    rate_limit_budget=0,
    userinfo_endpoint="...",
))
```
</Tab>
<Tab value="Java">
```java
IdentityProvider identityProvider = client.identityProvider().create(IdentityProviderInput.builder()
    .name("my-identity-provider")
    .org("acme")
    .slug("...")
    .labels(Map.of())
    .displayName("...")
    .jwksUri("...")
    .allowedIssuers(List.of())
    .expectedAudience("...")
    .rateLimitBudget(0)
    .userinfoEndpoint("...")
    .build());
```
</Tab>
</SDKTabs>

**Parameters**: [`IdentityProviderInput`](#identityproviderinput)

**Returns**: `IdentityProvider`

### update

Update an existing identity provider. Requires can_edit permission on the identity provider.

<SDKTabs>
<Tab value="Go">
```go
identityProvider, err := client.IdentityProvider.Update(ctx, &stigmer.IdentityProviderInput{
  Name:             "my-identity-provider",
  Org:              "acme",
  Slug:             "...",
  Labels:           map[string]string{},
  DisplayName:      "...",
  JwksUri:          "...",
  AllowedIssuers:   []string{},
  ExpectedAudience: "...",
  RateLimitBudget:  0,
  UserinfoEndpoint: "...",
})
```
</Tab>
<Tab value="TypeScript">
```typescript
const identityProvider = await stigmer.identityProvider.update({
  name: "my-identity-provider",
  org: "acme",
  slug: "...",
  labels: {},
  displayName: "...",
  jwksUri: "...",
  allowedIssuers: [],
  expectedAudience: "...",
  rateLimitBudget: 0,
  userinfoEndpoint: "...",
});
```
</Tab>
<Tab value="Python">
```python
identityProvider = client.identity_provider.update(IdentityProviderInput(
    name="my-identity-provider",
    org="acme",
    slug="...",
    labels={},
    display_name="...",
    jwks_uri="...",
    allowed_issuers=[],
    expected_audience="...",
    rate_limit_budget=0,
    userinfo_endpoint="...",
))
```
</Tab>
<Tab value="Java">
```java
IdentityProvider identityProvider = client.identityProvider().update(IdentityProviderInput.builder()
    .name("my-identity-provider")
    .org("acme")
    .slug("...")
    .labels(Map.of())
    .displayName("...")
    .jwksUri("...")
    .allowedIssuers(List.of())
    .expectedAudience("...")
    .rateLimitBudget(0)
    .userinfoEndpoint("...")
    .build());
```
</Tab>
</SDKTabs>

**Parameters**: [`IdentityProviderInput`](#identityproviderinput)

**Returns**: `IdentityProvider`

### delete

Delete an identity provider. Deletion is blocked if any platform-managed organizations reference this identity provider.

<SDKTabs>
<Tab value="Go">
```go
identityProvider, err := client.IdentityProvider.Delete(ctx, &stigmer.DeleteResourceInput{
  ResourceID: id,
})
```
</Tab>
<Tab value="TypeScript">
```typescript
const identityProvider = await stigmer.identityProvider.delete({ resourceId: id });
```
</Tab>
<Tab value="Python">
```python
identityProvider = client.identity_provider.delete(DeleteResourceInput(resource_id=id))
```
</Tab>
<Tab value="Java">
```java
IdentityProvider identityProvider = client.identityProvider().delete(DeleteResourceInput.builder()
    .resourceId(id)
    .build());
```
</Tab>
</SDKTabs>

**Parameters**

| Parameter | Type | Description |
|-----------|------|-------------|
| `resourceId` | `string` | ID of the resource to delete. |
| `versionMessage` | `string` | Optional deletion reason. |
| `force` | `boolean` | Force delete even if resource is in use. |

**Returns**: `IdentityProvider`

## Types

### IdentityProviderInput

Input for creating or updating an Identity Provider.

<TypeTable
  type={{
    name: { type: "string", description: "Resource name.", required: true },
    slug: { type: "string", description: "URL-friendly identifier." },
    org: { type: "string", description: "Organization slug.", required: true },
    labels: { type: "Record<string, string>", description: "Key-value labels." },
    displayName: { type: "string", description: "Human-readable display name for this identity provider." },
    jwksUri: { type: "string", description: "JWKS endpoint URL for validating tokens presented during token exchange." },
    allowedIssuers: { type: "string[]", description: "JWT issuer values that Stigmer will accept from this platform." },
    expectedAudience: { type: "string", description: "Expected JWT audience value." },
    rateLimitBudget: { type: "number", description: "Shared rate limit budget across all organizations managed via this identity provider." },
    userinfoEndpoint: { type: "string", description: "OIDC UserInfo endpoint URL for fetching user profile data during token exchange." },
  }}
/>

### ApiResourceReference

Generic reference to any API resource by org and slug.

<TypeTable
  type={{
    org: { type: "string", description: "Organization that owns the referenced resource." },
    kind: { type: "ApiResourceKind", description: "Kind of the referenced resource (e.g., SKILL, AGENT, MCP_SERVER)." },
    slug: { type: "string", description: "Resource slug (user-friendly identifier, unique within org).", required: true },
    version: { type: "string", description: "Version of the resource (optional, only applicable to versioned resources like Skills)." },
  }}
/>

