---
title: IAM Policy
description: "SDK reference for IAM Policy resources."
---

{/* Auto-generated by stigmer-codegen. DO NOT EDIT. */}

It specifies WHO (principal) gets WHAT permission (relation) on WHICH resource.

## Client Access

<SDKTabs>
<Tab value="Go">
```go
iamPolicy, err := client.IamPolicy.Get(ctx, "iamp_01j5q3k7m8r2s4tnz2hfp0q0c3")
```
</Tab>
<Tab value="TypeScript">
```typescript
const iamPolicy = await stigmer.iamPolicy.get("iamp_01j5q3k7m8r2s4tnz2hfp0q0c3");
```
</Tab>
<Tab value="Python">
```python
iamPolicy = client.iam_policy.get("iamp_01j5q3k7m8r2s4tnz2hfp0q0c3")
```
</Tab>
<Tab value="Java">
```java
IamPolicy iamPolicy = client.iamPolicy().get("iamp_01j5q3k7m8r2s4tnz2hfp0q0c3");
```
</Tab>
</SDKTabs>

## Methods

| Method | Description |
|--------|-------------|
| [`get`](#get) | Get an IAM policy by its unique identifier. |
| [`listAuthorizedPrincipalIds`](#listauthorizedprincipalids) | List all principal IDs of a specific kind that are authorized to access a resource. |
| [`listAuthorizedResourceIds`](#listauthorizedresourceids) | List all resource IDs of a specific kind that a principal is authorized to access. |
| [`create`](#create) | Create a new IAM policy. |
| [`delete`](#delete) | Delete a single IAM policy by spec. |
| [`bootstrapPolicy`](#bootstrappolicy) | Bootstrap IAM policy during resource creation. |
| [`checkAuthorization`](#checkauthorization) | Check if a principal is authorized to perform a relation on a resource. |
| [`cleanupResourcePolicies`](#cleanupresourcepolicies) | Cleanup all IAM policies for a deleted resource. |

### get

Get an IAM policy by its unique identifier.

<SDKTabs>
<Tab value="Go">
```go
iamPolicy, err := client.IamPolicy.Get(ctx, id)
```
</Tab>
<Tab value="TypeScript">
```typescript
const iamPolicy = await stigmer.iamPolicy.get(id);
```
</Tab>
<Tab value="Python">
```python
iamPolicy = client.iam_policy.get(id)
```
</Tab>
<Tab value="Java">
```java
IamPolicy iamPolicy = client.iamPolicy().get(id);
```
</Tab>
</SDKTabs>

**Parameters**

| Parameter | Type | Description |
|-----------|------|-------------|
| `id` | `string` | IAM Policy identifier. |

**Returns**: `IamPolicy`

### listAuthorizedPrincipalIds

List all principal IDs of a specific kind that are authorized to access a resource

This RPC answers: "What are all the [principal-kind] that have [relation] access on [resource]?" Inverse of listAuthorizedResourceIds.

Use Cases: - Resource access audit (who can access this?) - Team discovery for resources - Compliance and security audits

Input: ListAuthorizedPrincipalIdsInput with resource, principal_kind, and relation Output: AuthorizedPrincipalIdsList containing all authorized principal IDs

<SDKTabs>
<Tab value="Go">
```go
result, err := client.IamPolicy.ListAuthorizedPrincipalIds(ctx, &stigmer.ListAuthorizedPrincipalIdsInput{
  Resource:           &stigmer.ApiResourceRefInput{},
  PrincipalKind:      "...",
  Relation:           "...",
  ContextualPolicies: []stigmer.IamPolicySpecInput{},
})
```
</Tab>
<Tab value="TypeScript">
```typescript
const result = await stigmer.iamPolicy.listAuthorizedPrincipalIds({
  resource: {},
  principalKind: "...",
  relation: "...",
  contextualPolicies: [],
});
```
</Tab>
<Tab value="Python">
```python
result = client.iam_policy.list_authorized_principal_ids(ListAuthorizedPrincipalIdsInput(
    resource=None,
    principal_kind="...",
    relation="...",
    contextual_policies=[],
))
```
</Tab>
<Tab value="Java">
```java
AuthorizedPrincipalIdsList result = client.iamPolicy().listAuthorizedPrincipalIds(ListAuthorizedPrincipalIdsInput.builder()
    .resource(null)
    .principalKind("...")
    .relation("...")
    .contextualPolicies(List.of())
    .build());
```
</Tab>
</SDKTabs>

**Parameters**: [`ListAuthorizedPrincipalIdsInput`](#listauthorizedprincipalidsinput)

**Returns**: [`AuthorizedPrincipalIdsList`](#authorizedprincipalidslist)

### listAuthorizedResourceIds

List all resource IDs of a specific kind that a principal is authorized to access

This RPC answers: "What are all the [resource-kind] that [principal] can [relation] on?" Essential for building permission-aware UIs and filtering resource lists.

Use Cases: - Resource list filtering in dropdowns - Permission-based navigation - Bulk authorization checks

Input: ListAuthorizedResourceIdsInput with principal, resource_kind, and relation Output: AuthorizedResourceIdsList containing all accessible resource IDs

<SDKTabs>
<Tab value="Go">
```go
result, err := client.IamPolicy.ListAuthorizedResourceIds(ctx, &stigmer.ListAuthorizedResourceIdsInput{
  Principal:          &stigmer.ApiResourceRefInput{},
  ResourceKind:       "...",
  Relation:           "...",
  ContextualPolicies: []stigmer.IamPolicySpecInput{},
})
```
</Tab>
<Tab value="TypeScript">
```typescript
const result = await stigmer.iamPolicy.listAuthorizedResourceIds({
  principal: {},
  resourceKind: "...",
  relation: "...",
  contextualPolicies: [],
});
```
</Tab>
<Tab value="Python">
```python
result = client.iam_policy.list_authorized_resource_ids(ListAuthorizedResourceIdsInput(
    principal=None,
    resource_kind="...",
    relation="...",
    contextual_policies=[],
))
```
</Tab>
<Tab value="Java">
```java
AuthorizedResourceIdsList result = client.iamPolicy().listAuthorizedResourceIds(ListAuthorizedResourceIdsInput.builder()
    .principal(null)
    .resourceKind("...")
    .relation("...")
    .contextualPolicies(List.of())
    .build());
```
</Tab>
</SDKTabs>

**Parameters**: [`ListAuthorizedResourceIdsInput`](#listauthorizedresourceidsinput)

**Returns**: [`AuthorizedResourceIdsList`](#authorizedresourceidslist)

### create

Create a new IAM policy

Creates a single IAM policy that grants a principal access to a resource with a specific relation. This is the fundamental operation for establishing permissions.

<SDKTabs>
<Tab value="Go">
```go
iamPolicy, err := client.IamPolicy.Create(ctx, &stigmer.IamPolicySpec{
  Principal: &stigmer.ApiResourceRefInput{},
  Resource:  &stigmer.ApiResourceRefInput{},
  Relation:  "...",
})
```
</Tab>
<Tab value="TypeScript">
```typescript
const iamPolicy = await stigmer.iamPolicy.create({
  principal: {},
  resource: {},
  relation: "...",
});
```
</Tab>
<Tab value="Python">
```python
iamPolicy = client.iam_policy.create(IamPolicySpec(
    principal=None,
    resource=None,
    relation="...",
))
```
</Tab>
<Tab value="Java">
```java
IamPolicy iamPolicy = client.iamPolicy().create(IamPolicySpec.builder()
    .principal(null)
    .resource(null)
    .relation("...")
    .build());
```
</Tab>
</SDKTabs>

**Parameters**: [`IamPolicySpec`](#iampolicyspec)

**Returns**: `IamPolicy`

### delete

Delete a single IAM policy by spec

Removes an existing IAM policy by matching the principal, resource, and relation. This is a surgical operation — it removes one specific policy without affecting others.

<SDKTabs>
<Tab value="Go">
```go
iamPolicy, err := client.IamPolicy.Delete(ctx, &stigmer.IamPolicySpec{
  Principal: &stigmer.ApiResourceRefInput{},
  Resource:  &stigmer.ApiResourceRefInput{},
  Relation:  "...",
})
```
</Tab>
<Tab value="TypeScript">
```typescript
const iamPolicy = await stigmer.iamPolicy.delete({
  principal: {},
  resource: {},
  relation: "...",
});
```
</Tab>
<Tab value="Python">
```python
iamPolicy = client.iam_policy.delete(IamPolicySpec(
    principal=None,
    resource=None,
    relation="...",
))
```
</Tab>
<Tab value="Java">
```java
IamPolicy iamPolicy = client.iamPolicy().delete(IamPolicySpec.builder()
    .principal(null)
    .resource(null)
    .relation("...")
    .build());
```
</Tab>
</SDKTabs>

**Parameters**: [`IamPolicySpec`](#iampolicyspec)

**Returns**: `IamPolicy`

### bootstrapPolicy

Bootstrap IAM policy during resource creation

Creates IAM policies during resource creation when standard authorization cannot work yet because no tuples exist.

<SDKTabs>
<Tab value="Go">
```go
iamPolicy, err := client.IamPolicy.BootstrapPolicy(ctx, &stigmer.IamPolicySpec{
  Principal: &stigmer.ApiResourceRefInput{},
  Resource:  &stigmer.ApiResourceRefInput{},
  Relation:  "...",
})
```
</Tab>
<Tab value="TypeScript">
```typescript
const iamPolicy = await stigmer.iamPolicy.bootstrapPolicy({
  principal: {},
  resource: {},
  relation: "...",
});
```
</Tab>
<Tab value="Python">
```python
iamPolicy = client.iam_policy.bootstrap_policy(IamPolicySpec(
    principal=None,
    resource=None,
    relation="...",
))
```
</Tab>
<Tab value="Java">
```java
IamPolicy iamPolicy = client.iamPolicy().bootstrapPolicy(IamPolicySpec.builder()
    .principal(null)
    .resource(null)
    .relation("...")
    .build());
```
</Tab>
</SDKTabs>

**Parameters**: [`IamPolicySpec`](#iampolicyspec)

**Returns**: `IamPolicy`

### checkAuthorization

Check if a principal is authorized to perform a relation on a resource

This is the fundamental authorization check RPC that answers the question: "Does principal X have permission Y on resource Z?"

It provides a simple boolean answer based on the complete authorization state, including existing IAM policies, inherited permissions, and group memberships.

Use Cases: - Pre-flight UI checks before showing buttons/actions - API request authorization before processing operations - Service-to-service authorization - Team-based access checks

Input: CheckAuthorizationInput with policy spec and optional contextual policies Output: CheckAuthorizationResult with is_authorized boolean

<SDKTabs>
<Tab value="Go">
```go
checkAuthorizationResult, err := client.IamPolicy.CheckAuthorization(ctx, &stigmer.CheckAuthorizationInput{
  Policy:             &stigmer.IamPolicySpecInput{},
  ContextualPolicies: []stigmer.IamPolicySpecInput{},
})
```
</Tab>
<Tab value="TypeScript">
```typescript
const checkAuthorizationResult = await stigmer.iamPolicy.checkAuthorization({
  policy: {},
  contextualPolicies: [],
});
```
</Tab>
<Tab value="Python">
```python
checkAuthorizationResult = client.iam_policy.check_authorization(CheckAuthorizationInput(
    policy=None,
    contextual_policies=[],
))
```
</Tab>
<Tab value="Java">
```java
CheckAuthorizationResult checkAuthorizationResult = client.iamPolicy().checkAuthorization(CheckAuthorizationInput.builder()
    .policy(null)
    .contextualPolicies(List.of())
    .build());
```
</Tab>
</SDKTabs>

**Parameters**: [`CheckAuthorizationInput`](#checkauthorizationinput)

**Returns**: [`CheckAuthorizationResult`](#checkauthorizationresult)

### cleanupResourcePolicies

Cleanup all IAM policies for a deleted resource.

Removes all IAM policies associated with a deleted resource.

<SDKTabs>
<Tab value="Go">
```go
empty, err := client.IamPolicy.CleanupResourcePolicies(ctx, &stigmer.ApiResourceRef{
  Kind:     "...",
  Id:       "...",
  Relation: "...",
})
```
</Tab>
<Tab value="TypeScript">
```typescript
const empty = await stigmer.iamPolicy.cleanupResourcePolicies({
  kind: "...",
  id: "...",
  relation: "...",
});
```
</Tab>
<Tab value="Python">
```python
empty = client.iam_policy.cleanup_resource_policies(ApiResourceRef(
    kind="...",
    id="...",
    relation="...",
))
```
</Tab>
<Tab value="Java">
```java
Empty empty = client.iamPolicy().cleanupResourcePolicies(ApiResourceRef.builder()
    .kind("...")
    .id("...")
    .relation("...")
    .build());
```
</Tab>
</SDKTabs>

**Parameters**: [`ApiResourceRef`](#apiresourceref)

**Returns**: `void`

## Types

### IamPolicyInput

Input for creating or updating an IAM Policy.

<TypeTable
  type={{
    name: { type: "string", description: "Resource name.", required: true },
    slug: { type: "string", description: "URL-friendly identifier." },
    org: { type: "string", description: "Organization slug.", required: true },
    labels: { type: "Record<string, string>", description: "Key-value labels." },
    principal: { type: "ApiResourceRefInput", description: "Principal: WHO is being granted access This can be any API resource that acts as an identity: - identity_account (individual user) - team (group of users) - organization (for cross-org grants) - Any other resource that can be a subject in authorization.", required: true },
    resource: { type: "ApiResourceRefInput", description: "Resource: WHAT is being accessed This can be any API resource that is being protected: - organization - environment - cloud_resource (VPC, S3 bucket, etc.) - service - Any other resource that requires access control.", required: true },
    relation: { type: "string", description: "The permission being granted (e.g., \"admin\", \"viewer\", \"owner\").", required: true },
  }}
/>

### ApiResourceRefInput

ApiResourceRef represents a reference to ANY API resource in the system.

<TypeTable
  type={{
    kind: { type: "string", description: "Type of the API resource being referenced This should be the resource kind as defined in ApiResourceKind enum.", required: true },
    id: { type: "string", description: "Unique identifier of the resource This is the resource's ID field (e.g., ia-01HQUSER123, tm-01HQTEAM456).", required: true },
    relation: { type: "string", description: "Optional relation qualifier for the resource reference." },
  }}
/>

### IamPolicySpec

IamPolicySpec defines the desired state of an IAM policy binding.

<TypeTable
  type={{
    principal: { type: "ApiResourceRefInput", description: "Principal: WHO is being granted access This can be any API resource that acts as an identity: - identity_account (individual user) - team (group of users) - organization (for cross-org grants) - Any other resource that can be a subject in authorization.", required: true },
    resource: { type: "ApiResourceRefInput", description: "Resource: WHAT is being accessed This can be any API resource that is being protected: - organization - environment - cloud_resource (VPC, S3 bucket, etc.) - service - Any other resource that requires access control.", required: true },
    relation: { type: "string", description: "The permission being granted (e.g., \"admin\", \"viewer\", \"owner\").", required: true },
  }}
/>

### ApiResourceRef

ApiResourceRef represents a reference to ANY API resource in the system.

<TypeTable
  type={{
    kind: { type: "string", description: "Type of the API resource being referenced This should be the resource kind as defined in ApiResourceKind enum.", required: true },
    id: { type: "string", description: "Unique identifier of the resource This is the resource's ID field (e.g., ia-01HQUSER123, tm-01HQTEAM456).", required: true },
    relation: { type: "string", description: "Optional relation qualifier for the resource reference." },
  }}
/>

### CheckAuthorizationInput

CheckAuthorizationInput defines input for checking if a principal is authorized.

<TypeTable
  type={{
    policy: { type: "IamPolicySpecInput", description: "The policy to check (principal, resource, relation).", required: true },
    contextualPolicies: { type: "IamPolicySpecInput[]", description: "Optional contextual policies for \"what-if\" scenarios." },
  }}
/>

### CheckAuthorizationResult

CheckAuthorizationResult contains the result of an authorization check.

<TypeTable
  type={{
    isAuthorized: { type: "boolean", description: "Whether the principal is authorized." },
  }}
/>

### ListAuthorizedResourceIdsInput

ListAuthorizedResourceIdsInput defines input for listing resources a principal can access.

<TypeTable
  type={{
    principal: { type: "ApiResourceRefInput", description: "The principal whose access is being queried.", required: true },
    resourceKind: { type: "string", description: "The kind of resources to check.", required: true },
    relation: { type: "string", description: "The relation/permission to check for.", required: true },
    contextualPolicies: { type: "IamPolicySpecInput[]", description: "Optional contextual policies for \"what-if\" scenarios." },
  }}
/>

### AuthorizedResourceIdsList

AuthorizedResourceIdsList contains all resource IDs a principal can access.

<TypeTable
  type={{
    resourceIds: { type: "string[]", description: "" },
  }}
/>

### ListAuthorizedPrincipalIdsInput

ListAuthorizedPrincipalIdsInput defines input for listing principals that can access a resource.

<TypeTable
  type={{
    resource: { type: "ApiResourceRefInput", description: "The resource whose access is being queried.", required: true },
    principalKind: { type: "string", description: "The kind of principals to check for.", required: true },
    relation: { type: "string", description: "The relation/permission to check for.", required: true },
    contextualPolicies: { type: "IamPolicySpecInput[]", description: "Optional contextual policies for \"what-if\" scenarios." },
  }}
/>

### AuthorizedPrincipalIdsList

AuthorizedPrincipalIdsList contains all principal IDs that can access a resource.

<TypeTable
  type={{
    principalIds: { type: "string[]", description: "" },
  }}
/>

