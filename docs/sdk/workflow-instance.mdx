---
title: Workflow Instance
description: "SDK reference for Workflow Instance resources."
---

{/* Auto-generated by stigmer-codegen. DO NOT EDIT. */}

This is the "Instance" layer in the Template→Instance→Execution pattern. It provides stateful configuration with environment bindings and secrets.

The spec contains: - A reference to the Workflow template (the orchestration blueprint) - Descriptive metadata for humans - Environment references (layered configuration with secrets)

Design Philosophy: WorkflowInstanceSpec separates "what to run" (Workflow) from "how to run it" (Environments). This allows the same Workflow template to be instantiated multiple times with different configurations (dev vs prod, different cloud accounts, different teams).

## Client Access

<SDKTabs>
<Tab value="Go">
```go
workflowInstance, err := client.WorkflowInstance.Get(ctx, "win_01j5q3k7m8r2s4tnz2hfp0q0c3")
```
</Tab>
<Tab value="TypeScript">
```typescript
const workflowInstance = await stigmer.workflowInstance.get("win_01j5q3k7m8r2s4tnz2hfp0q0c3");
```
</Tab>
<Tab value="Python">
```python
workflowInstance = client.workflow_instance.get("win_01j5q3k7m8r2s4tnz2hfp0q0c3")
```
</Tab>
<Tab value="Java">
```java
WorkflowInstance workflowInstance = client.workflowInstance().get("win_01j5q3k7m8r2s4tnz2hfp0q0c3");
```
</Tab>
</SDKTabs>

## Methods

| Method | Description |
|--------|-------------|
| [`get`](#get) | Get a single workflow instance by ID. |
| [`getByReference`](#getbyreference) | Get a workflow instance by flexible reference (ID or slug). |
| [`getByWorkflow`](#getbyworkflow) | Get all instances of a specific workflow template. |
| [`apply`](#apply) | Create or update a workflow instance. |
| [`create`](#create) | Create a new workflow instance. |
| [`update`](#update) | Update an existing workflow instance. |
| [`delete`](#delete) | Delete a workflow instance. |

### get

Get a single workflow instance by ID.

Retrieves a specific WorkflowInstance using its unique resource identifier.

Input: WorkflowInstanceId with the instance ID (e.g., "wfi-abc123")

Returns: Complete WorkflowInstance resource with: - api_version, kind, metadata - spec (workflow_id, description, environment_refs) - status (audit information: created_at, updated_at, version)

Authorization: Requires "get" permission on the specific WorkflowInstance. Field path "value" extracts the resource ID from WorkflowInstanceId wrapper. Verifies user has access based on: - Instance owner scope (organization or identity_account) - User's IAM policies

Use Cases: - Retrieve instance configuration before executing - View instance details in UI - Fetch instance for editing/updating

Error: PERMISSION_DENIED if user lacks get permission Error: NOT_FOUND if instance ID doesn't exist

<SDKTabs>
<Tab value="Go">
```go
workflowInstance, err := client.WorkflowInstance.Get(ctx, id)
```
</Tab>
<Tab value="TypeScript">
```typescript
const workflowInstance = await stigmer.workflowInstance.get(id);
```
</Tab>
<Tab value="Python">
```python
workflowInstance = client.workflow_instance.get(id)
```
</Tab>
<Tab value="Java">
```java
WorkflowInstance workflowInstance = client.workflowInstance().get(id);
```
</Tab>
</SDKTabs>

**Parameters**

| Parameter | Type | Description |
|-----------|------|-------------|
| `id` | `string` | Workflow Instance identifier. |

**Returns**: `WorkflowInstance`

### getByReference

Get a workflow instance by flexible reference (ID or slug).

Retrieves a WorkflowInstance using ApiResourceReference which supports multiple lookup methods: - By ID: \{id: "wfi-abc123"\} - By slug: \{slug: "prod-deploy"\} - By name: \{name: "Production Deploy"\}

Input: ApiResourceReference with one of: id, slug, or name

Returns: Complete WorkflowInstance resource matching the reference.

Authorization: Uses custom authorization logic in the handler. Allows for flexible authorization based on reference type and context. Typical checks: - User has get permission on the resolved instance - Instance owner scope matches user's organization/identity

Use Cases: - User-friendly lookups by slug instead of opaque IDs - CLI commands using human-readable names - API integrations using stable slugs

Example: Input: ApiResourceReference\{slug: "prod-deploy"\} Output: WorkflowInstance with metadata.slug = "prod-deploy"

Error: PERMISSION_DENIED if user lacks access Error: NOT_FOUND if reference doesn't resolve to an instance

<SDKTabs>
<Tab value="Go">
```go
workflowInstance, err := client.WorkflowInstance.GetByReference(ctx, input)
```
</Tab>
<Tab value="TypeScript">
```typescript
const workflowInstance = await stigmer.workflowInstance.getByReference(input);
```
</Tab>
<Tab value="Python">
```python
workflowInstance = client.workflow_instance.get_by_reference(input)
```
</Tab>
<Tab value="Java">
```java
WorkflowInstance workflowInstance = client.workflowInstance().getByReference(input);
```
</Tab>
</SDKTabs>

**Parameters**: `ApiResourceReference`

**Returns**: `WorkflowInstance`

### getByWorkflow

Get all instances of a specific workflow template.

Retrieves all WorkflowInstance resources that reference a specific Workflow template. Useful for discovering all configured deployments of a workflow.

Example Use Case: Workflow "deploy-to-cloud" (wfl-123) has instances: - "prod-deploy" (wfi-abc) - Production deployment with aws-prod-env - "staging-deploy" (wfi-def) - Staging deployment with aws-staging-env - "dev-deploy" (wfi-ghi) - Development deployment with aws-dev-env

<SDKTabs>
<Tab value="Go">
```go
result, err := client.WorkflowInstance.GetByWorkflow(ctx, input)
```
</Tab>
<Tab value="TypeScript">
```typescript
const result = await stigmer.workflowInstance.getByWorkflow(input);
```
</Tab>
<Tab value="Python">
```python
result = client.workflow_instance.get_by_workflow(input)
```
</Tab>
<Tab value="Java">
```java
WorkflowInstanceList result = client.workflowInstance().getByWorkflow(input);
```
</Tab>
</SDKTabs>

**Parameters**: `GetWorkflowInstancesByWorkflowRequest`

**Returns**: `WorkflowInstanceList`

### apply

Create or update a workflow instance. The authorization and state-operation are determined depending on whether the workflow instance is going to be created or updated which is determined as part of the request execution.

<SDKTabs>
<Tab value="Go">
```go
workflowInstance, err := client.WorkflowInstance.Apply(ctx, &stigmer.WorkflowInstanceInput{
  Name: "my-workflow-instance",
  Org:  "acme",
})
```
</Tab>
<Tab value="TypeScript">
```typescript
const workflowInstance = await stigmer.workflowInstance.apply({
  name: "my-workflow-instance",
  org: "acme",
});
```
</Tab>
<Tab value="Python">
```python
workflowInstance = client.workflow_instance.apply(WorkflowInstanceInput(
    name="my-workflow-instance",
    org="acme",
))
```
</Tab>
<Tab value="Java">
```java
WorkflowInstance workflowInstance = client.workflowInstance().apply(WorkflowInstanceInput.builder()
    .name("my-workflow-instance")
    .org("acme")
    .build());
```
</Tab>
</SDKTabs>

**Parameters**: [`WorkflowInstanceInput`](#workflowinstanceinput)

**Returns**: `WorkflowInstance`

### create

Create a new workflow instance.

Creates a configured deployment of a Workflow template with environment bindings.

Input validation: - metadata.org must be specified - spec.workflow_id must be a valid Workflow resource ID - spec.environment_refs must reference valid Environment resources

Authorization: Uses custom authorization logic to verify: 1. User has permission to access the referenced Workflow template 2. User has permission to access all referenced Environment resources 3. Owner scope is valid for the user's organization/identity

Returns: The created WorkflowInstance with: - Assigned resource ID (metadata.id) - Created timestamp (status.audit.created_at) - Initial version (status.audit.version = 1)

Example: Input: WorkflowInstance with workflow_id="wfl-123", environment_refs=["env-prod"] Output: WorkflowInstance with id="wfi-abc456", created_at="2025-01-11T10:00:00Z"

<SDKTabs>
<Tab value="Go">
```go
workflowInstance, err := client.WorkflowInstance.Create(ctx, &stigmer.WorkflowInstanceInput{
  Name: "my-workflow-instance",
  Org:  "acme",
})
```
</Tab>
<Tab value="TypeScript">
```typescript
const workflowInstance = await stigmer.workflowInstance.create({
  name: "my-workflow-instance",
  org: "acme",
});
```
</Tab>
<Tab value="Python">
```python
workflowInstance = client.workflow_instance.create(WorkflowInstanceInput(
    name="my-workflow-instance",
    org="acme",
))
```
</Tab>
<Tab value="Java">
```java
WorkflowInstance workflowInstance = client.workflowInstance().create(WorkflowInstanceInput.builder()
    .name("my-workflow-instance")
    .org("acme")
    .build());
```
</Tab>
</SDKTabs>

**Parameters**: [`WorkflowInstanceInput`](#workflowinstanceinput)

**Returns**: `WorkflowInstance`

### update

Update an existing workflow instance.

Modifies the configuration of an existing WorkflowInstance. You can update: - spec.description (change descriptive text) - spec.environment_refs (add/remove/reorder environment bindings) - metadata.labels, metadata.tags, metadata.annotations

You cannot update: - spec.workflow_id (must delete and recreate to change template) - metadata.id (immutable resource identifier) - metadata.org (immutable after creation)

Authorization: Requires "update" permission on the specific WorkflowInstance resource. Field path "metadata.id" identifies which resource to authorize.

Versioning: Each update increments status.audit.version and updates status.audit.updated_at.

Returns: The updated WorkflowInstance with: - Incremented version number - Updated timestamp - Modified spec fields

Error: PERMISSION_DENIED if user lacks update permission

<SDKTabs>
<Tab value="Go">
```go
workflowInstance, err := client.WorkflowInstance.Update(ctx, &stigmer.WorkflowInstanceInput{
  Name: "my-workflow-instance",
  Org:  "acme",
})
```
</Tab>
<Tab value="TypeScript">
```typescript
const workflowInstance = await stigmer.workflowInstance.update({
  name: "my-workflow-instance",
  org: "acme",
});
```
</Tab>
<Tab value="Python">
```python
workflowInstance = client.workflow_instance.update(WorkflowInstanceInput(
    name="my-workflow-instance",
    org="acme",
))
```
</Tab>
<Tab value="Java">
```java
WorkflowInstance workflowInstance = client.workflowInstance().update(WorkflowInstanceInput.builder()
    .name("my-workflow-instance")
    .org("acme")
    .build());
```
</Tab>
</SDKTabs>

**Parameters**: [`WorkflowInstanceInput`](#workflowinstanceinput)

**Returns**: `WorkflowInstance`

### delete

Delete a workflow instance.

Permanently removes a WorkflowInstance resource.

Important: - Deletion is permanent and cannot be undone - Does NOT delete the referenced Workflow template (templates are reusable) - Does NOT delete the referenced Environment resources (environments are reusable) - DOES cascade delete any dependent WorkflowExecution resources (executions belong to instance)

Authorization: Requires "delete" permission on the specific WorkflowInstance resource. Field path "value" extracts the resource ID from WorkflowInstanceId wrapper.

Use Cases: - Remove deprecated instances - Clean up test/dev instances - Decommission old deployment configurations

Returns: The deleted WorkflowInstance (final state before deletion). Useful for audit logs and confirming what was deleted.

Error: PERMISSION_DENIED if user lacks delete permission Error: NOT_FOUND if instance ID doesn't exist

<SDKTabs>
<Tab value="Go">
```go
workflowInstance, err := client.WorkflowInstance.Delete(ctx, id)
```
</Tab>
<Tab value="TypeScript">
```typescript
const workflowInstance = await stigmer.workflowInstance.delete(id);
```
</Tab>
<Tab value="Python">
```python
workflowInstance = client.workflow_instance.delete(id)
```
</Tab>
<Tab value="Java">
```java
WorkflowInstance workflowInstance = client.workflowInstance().delete(id);
```
</Tab>
</SDKTabs>

**Parameters**

| Parameter | Type | Description |
|-----------|------|-------------|
| `id` | `string` | Workflow Instance identifier. |

**Returns**: `WorkflowInstance`

## Types

### WorkflowInstanceInput

Input for creating or updating a Workflow Instance.

<TypeTable
  type={{
    name: { type: "string", description: "Resource name.", required: true },
    slug: { type: "string", description: "URL-friendly identifier." },
    org: { type: "string", description: "Organization slug.", required: true },
    labels: { type: "Record<string, string>", description: "Key-value labels." },
    workflowId: { type: "string", description: "Reference to the Workflow template this instance deploys." },
    description: { type: "string", description: "Human-readable description explaining what this instance is for." },
    environmentRefs: { type: "ResourceRef[]", description: "References to Environment resources providing configuration and secrets." },
  }}
/>

