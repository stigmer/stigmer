---
title: Identity Account
description: "SDK reference for Identity Account resources."
---

{/* Auto-generated by stigmer-codegen. DO NOT EDIT. */}

An identity account represents a user or machine principal in Stigmer. Accounts can be direct (signed up via Stigmer), federated (provisioned through an external identity provider), or machine (service-to-service credentials).

## Client Access

<SDKTabs>
<Tab value="Go">
```go
identityAccount, err := client.IdentityAccount.Get(ctx, "identity-account-abc123")
```
</Tab>
<Tab value="TypeScript">
```typescript
const identityAccount = await stigmer.identityAccount.get("identity-account-abc123");
```
</Tab>
<Tab value="Python">
```python
identityAccount = client.identity_account.get("identity-account-abc123")
```
</Tab>
<Tab value="Java">
```java
IdentityAccount identityAccount = client.identityAccount().get("identity-account-abc123");
```
</Tab>
</SDKTabs>

## Methods

| Method | Description |
|--------|-------------|
| [`get`](#get) | Get an identity account by its unique identifier. |
| [`getActorInfo`](#getactorinfo) | Get lightweight actor information for an identity account. |
| [`getByEmail`](#getbyemail) | Get an identity account by email address. |
| [`getByIdpId`](#getbyidpid) | Get an identity account by identity provider ID. |
| [`create`](#create) | Create a new identity account. |
| [`update`](#update) | Update an existing identity account. |
| [`delete`](#delete) | Delete an identity account. |
| [`simulateSignupWebhook`](#simulatesignupwebhook) | Simulate a signup webhook for a user who exists in Auth0 but not in Stigmer. |
| [`whoAmI`](#whoami) | Get the identity account of the currently authenticated user. |

### get

Get an identity account by its unique identifier.

<SDKTabs>
<Tab value="Go">
```go
identityAccount, err := client.IdentityAccount.Get(ctx, id)
```
</Tab>
<Tab value="TypeScript">
```typescript
const identityAccount = await stigmer.identityAccount.get(id);
```
</Tab>
<Tab value="Python">
```python
identityAccount = client.identity_account.get(id)
```
</Tab>
<Tab value="Java">
```java
IdentityAccount identityAccount = client.identityAccount().get(id);
```
</Tab>
</SDKTabs>

**Parameters**

| Parameter | Type | Description |
|-----------|------|-------------|
| `id` | `string` | Identity Account identifier. |

**Returns**: `IdentityAccount`

### getActorInfo

Get lightweight actor information for an identity account.

<SDKTabs>
<Tab value="Go">
```go
apiResourceAuditActor, err := client.IdentityAccount.GetActorInfo(ctx, id)
```
</Tab>
<Tab value="TypeScript">
```typescript
const apiResourceAuditActor = await stigmer.identityAccount.getActorInfo(id);
```
</Tab>
<Tab value="Python">
```python
apiResourceAuditActor = client.identity_account.get_actor_info(id)
```
</Tab>
<Tab value="Java">
```java
ApiResourceAuditActor apiResourceAuditActor = client.identityAccount().getActorInfo(id);
```
</Tab>
</SDKTabs>

**Parameters**

| Parameter | Type | Description |
|-----------|------|-------------|
| `id` | `string` | Identity Account identifier. |

**Returns**: `ApiResourceAuditActor`

### getByEmail

Get an identity account by email address.

<SDKTabs>
<Tab value="Go">
```go
identityAccount, err := client.IdentityAccount.GetByEmail(ctx, input)
```
</Tab>
<Tab value="TypeScript">
```typescript
const identityAccount = await stigmer.identityAccount.getByEmail(input);
```
</Tab>
<Tab value="Python">
```python
identityAccount = client.identity_account.get_by_email(input)
```
</Tab>
<Tab value="Java">
```java
IdentityAccount identityAccount = client.identityAccount().getByEmail(input);
```
</Tab>
</SDKTabs>

**Parameters**: `IdentityAccountEmail`

**Returns**: `IdentityAccount`

### getByIdpId

Get an identity account by identity provider ID.

<SDKTabs>
<Tab value="Go">
```go
identityAccount, err := client.IdentityAccount.GetByIdpId(ctx, id)
```
</Tab>
<Tab value="TypeScript">
```typescript
const identityAccount = await stigmer.identityAccount.getByIdpId(id);
```
</Tab>
<Tab value="Python">
```python
identityAccount = client.identity_account.get_by_idp_id(id)
```
</Tab>
<Tab value="Java">
```java
IdentityAccount identityAccount = client.identityAccount().getByIdpId(id);
```
</Tab>
</SDKTabs>

**Parameters**

| Parameter | Type | Description |
|-----------|------|-------------|
| `id` | `string` | Identity Account identifier. |

**Returns**: `IdentityAccount`

### create

Create a new identity account.

<SDKTabs>
<Tab value="Go">
```go
identityAccount, err := client.IdentityAccount.Create(ctx, &stigmer.IdentityAccountInput{
  Name: "my-identity-account",
  Org:  "acme",
})
```
</Tab>
<Tab value="TypeScript">
```typescript
const identityAccount = await stigmer.identityAccount.create({
  name: "my-identity-account",
  org: "acme",
});
```
</Tab>
<Tab value="Python">
```python
identityAccount = client.identity_account.create(IdentityAccountInput(
    name="my-identity-account",
    org="acme",
))
```
</Tab>
<Tab value="Java">
```java
IdentityAccount identityAccount = client.identityAccount().create(IdentityAccountInput.builder()
    .name("my-identity-account")
    .org("acme")
    .build());
```
</Tab>
</SDKTabs>

**Parameters**: [`IdentityAccountInput`](#identityaccountinput)

**Returns**: `IdentityAccount`

### update

Update an existing identity account.

<SDKTabs>
<Tab value="Go">
```go
identityAccount, err := client.IdentityAccount.Update(ctx, &stigmer.IdentityAccountInput{
  Name: "my-identity-account",
  Org:  "acme",
})
```
</Tab>
<Tab value="TypeScript">
```typescript
const identityAccount = await stigmer.identityAccount.update({
  name: "my-identity-account",
  org: "acme",
});
```
</Tab>
<Tab value="Python">
```python
identityAccount = client.identity_account.update(IdentityAccountInput(
    name="my-identity-account",
    org="acme",
))
```
</Tab>
<Tab value="Java">
```java
IdentityAccount identityAccount = client.identityAccount().update(IdentityAccountInput.builder()
    .name("my-identity-account")
    .org("acme")
    .build());
```
</Tab>
</SDKTabs>

**Parameters**: [`IdentityAccountInput`](#identityaccountinput)

**Returns**: `IdentityAccount`

### delete

Delete an identity account.

<SDKTabs>
<Tab value="Go">
```go
identityAccount, err := client.IdentityAccount.Delete(ctx, id)
```
</Tab>
<Tab value="TypeScript">
```typescript
const identityAccount = await stigmer.identityAccount.delete(id);
```
</Tab>
<Tab value="Python">
```python
identityAccount = client.identity_account.delete(id)
```
</Tab>
<Tab value="Java">
```java
IdentityAccount identityAccount = client.identityAccount().delete(id);
```
</Tab>
</SDKTabs>

**Parameters**

| Parameter | Type | Description |
|-----------|------|-------------|
| `id` | `string` | Identity Account identifier. |

**Returns**: `IdentityAccount`

### simulateSignupWebhook

Simulate a signup webhook for a user who exists in Auth0 but not in Stigmer.

<SDKTabs>
<Tab value="Go">
```go
empty, err := client.IdentityAccount.SimulateSignupWebhook(ctx, input)
```
</Tab>
<Tab value="TypeScript">
```typescript
const empty = await stigmer.identityAccount.simulateSignupWebhook(input);
```
</Tab>
<Tab value="Python">
```python
empty = client.identity_account.simulate_signup_webhook(input)
```
</Tab>
<Tab value="Java">
```java
Empty empty = client.identityAccount().simulateSignupWebhook(input);
```
</Tab>
</SDKTabs>

**Parameters**: `IdentityAccountEmail`

**Returns**: `void`

### whoAmI

Get the identity account of the currently authenticated user.

<SDKTabs>
<Tab value="Go">
```go
identityAccount, err := client.IdentityAccount.WhoAmI(ctx)
```
</Tab>
<Tab value="TypeScript">
```typescript
const identityAccount = await stigmer.identityAccount.whoAmI();
```
</Tab>
<Tab value="Python">
```python
identityAccount = client.identity_account.who_am_i()
```
</Tab>
<Tab value="Java">
```java
IdentityAccount identityAccount = client.identityAccount().whoAmI();
```
</Tab>
</SDKTabs>

**Returns**: `IdentityAccount`

## Types

### IdentityAccountInput

Input for creating or updating an Identity Account.

<TypeTable
  type={{
    name: { type: "string", description: "Resource name.", required: true },
    slug: { type: "string", description: "URL-friendly identifier." },
    org: { type: "string", description: "Organization slug.", required: true },
    labels: { type: "Record<string, string>", description: "Key-value labels." },
    idpId: { type: "string", description: "IDP ID of the identity account.", required: true },
    email: { type: "string", description: "Email of the identity account." },
    firstName: { type: "string", description: "First name of the identity account." },
    lastName: { type: "string", description: "Last name of the identity account." },
    pictureUrl: { type: "string", description: "URL of the profile picture." },
    isMachineAccount: { type: "boolean", description: "Indicates if this is a machine account used for inter-service communication." },
    provisioningMode: { type: "IdentityAccountProvisioningMode", description: "How this identity account was provisioned." },
    identityProviderRef: { type: "ResourceRef", description: "Reference to the IdentityProvider that provisioned this account." },
  }}
/>

