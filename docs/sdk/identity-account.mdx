---
title: Identity Account
description: "SDK reference for Identity Account resources."
---

{/* Auto-generated by stigmer-codegen. DO NOT EDIT. */}

IdentityAccountSpec defines the configuration for an identity account.

An identity account represents a user or machine principal in Stigmer's
authorization model. All FGA tuples use identity_account as the principal type.

Accounts are provisioned in one of three ways: - direct: User signed up via
Stigmer's Auth0 (idp_id is the Auth0 subject ID) - federated: JIT-provisioned
via an external IdentityProvider during federated auth (idp_id is a compound
key: "federated:\{provider_id\}:\{external_sub\}") - machine: M2M client
credentials (idp_id ends with "@clients")

## Client Access

<SDKTabs>
  <Tab value="Go">
    ```go identityAccount, err := client.IdentityAccount.Get(ctx,
    "identityAccount-id") ```
  </Tab>
  <Tab value="TypeScript">
    ```typescript const identityAccount = await
    stigmer.identityAccount.get("identityAccount-id"); ```
  </Tab>
  <Tab value="Python">
    ```python identityAccount =
    client.identity_account.get("identityAccount-id") ```
  </Tab>
  <Tab value="Java">
    ```java IdentityAccount identityAccount =
    client.identityAccount().get("identityAccount-id"); ```
  </Tab>
</SDKTabs>

## Methods

### create

create a new identity-account. system-level RPC used by federated JIT
provisioning and Auth0 webhook flow. no FGA authorization — called via
inProcessChannelAsSystem (machine account). the handler's
createAuthorizationTuples step writes the self-ownership tuple after creation.

<SDKTabs>
<Tab value="Go">
```go
identityAccount, err := client.IdentityAccount.Create(ctx, &stigmer.IdentityAccountInput{
  Name: "my-identityAccount",
  Org:  "acme",
})
```
</Tab>
<Tab value="TypeScript">
```typescript
const identityAccount = await stigmer.identityAccount.create({
  name: "my-identityAccount",
  org: "acme",
});
```
</Tab>
<Tab value="Python">
```python
identityAccount = client.identity_account.create(IdentityAccountInput(
    name="my-identityAccount",
    org="acme",
))
```
</Tab>
<Tab value="Java">
```java
IdentityAccount identityAccount = client.identityAccount().create(IdentityAccountInput.builder()
    .name("my-identityAccount")
    .org("acme")
    .build());
```
</Tab>
</SDKTabs>

**Parameters**: [`IdentityAccountInput`](#identityaccountinput)

**Returns**: `IdentityAccount`

### update

update an existing identity-account

<SDKTabs>
<Tab value="Go">
```go
identityAccount, err := client.IdentityAccount.Update(ctx, &stigmer.IdentityAccountInput{
  Name: "my-identityAccount",
  Org:  "acme",
})
```
</Tab>
<Tab value="TypeScript">
```typescript
const identityAccount = await stigmer.identityAccount.update({
  name: "my-identityAccount",
  org: "acme",
});
```
</Tab>
<Tab value="Python">
```python
identityAccount = client.identity_account.update(IdentityAccountInput(
    name="my-identityAccount",
    org="acme",
))
```
</Tab>
<Tab value="Java">
```java
IdentityAccount identityAccount = client.identityAccount().update(IdentityAccountInput.builder()
    .name("my-identityAccount")
    .org("acme")
    .build());
```
</Tab>
</SDKTabs>

**Parameters**: [`IdentityAccountInput`](#identityaccountinput)

**Returns**: `IdentityAccount`

### delete

delete an existing identity-account

<SDKTabs>
  <Tab value="Go">
    ```go identityAccount, err := client.IdentityAccount.Delete(ctx, id) ```
  </Tab>
  <Tab value="TypeScript">
    ```typescript const identityAccount = await
    stigmer.identityAccount.delete(id); ```
  </Tab>
  <Tab value="Python">
    ```python identityAccount = client.identity_account.delete(id) ```
  </Tab>
  <Tab value="Java">
    ```java IdentityAccount identityAccount =
    client.identityAccount().delete(id); ```
  </Tab>
</SDKTabs>

**Parameters**

| Parameter | Type     | Description                  |
| --------- | -------- | ---------------------------- |
| `id`      | `string` | Identity Account identifier. |

**Returns**: `IdentityAccount`

### simulateSignupWebhook

simulate signup webhook to add a user who created account on auth0 but is not
created on stigmer. this rpc will take the email, look it up on auth0, if there
is a user with this email on auth0 and post a webhook to stigmer with auth0
payload format.

<SDKTabs>
  <Tab value="Go">
    ```go empty, err := client.IdentityAccount.SimulateSignupWebhook(ctx, input)
    ```
  </Tab>
  <Tab value="TypeScript">
    ```typescript const empty = await
    stigmer.identityAccount.simulateSignupWebhook(input); ```
  </Tab>
  <Tab value="Python">
    ```python empty = client.identity_account.simulate_signup_webhook(input) ```
  </Tab>
  <Tab value="Java">
    ```java Empty empty = client.identityAccount().simulateSignupWebhook(input);
    ```
  </Tab>
</SDKTabs>

**Parameters**: `IdentityAccountEmail`

**Returns**: `void`

### get

lookup identity-account.

<SDKTabs>
  <Tab value="Go">
    ```go identityAccount, err := client.IdentityAccount.Get(ctx, id) ```
  </Tab>
  <Tab value="TypeScript">
    ```typescript const identityAccount = await stigmer.identityAccount.get(id);
    ```
  </Tab>
  <Tab value="Python">
    ```python identityAccount = client.identity_account.get(id) ```
  </Tab>
  <Tab value="Java">
    ```java IdentityAccount identityAccount = client.identityAccount().get(id);
    ```
  </Tab>
</SDKTabs>

**Parameters**

| Parameter | Type     | Description                  |
| --------- | -------- | ---------------------------- |
| `id`      | `string` | Identity Account identifier. |

**Returns**: `IdentityAccount`

### whoAmI

look up identity-account by authentication token.

<SDKTabs>
  <Tab value="Go">
    ```go identityAccount, err := client.IdentityAccount.WhoAmI(ctx) ```
  </Tab>
  <Tab value="TypeScript">
    ```typescript const identityAccount = await
    stigmer.identityAccount.whoAmI(); ```
  </Tab>
  <Tab value="Python">
    ```python identityAccount = client.identity_account.who_am_i() ```
  </Tab>
  <Tab value="Java">
    ```java IdentityAccount identityAccount = client.identityAccount().whoAmI();
    ```
  </Tab>
</SDKTabs>

**Returns**: `IdentityAccount`

### getByEmail

lookup user-account by identity account email.

<SDKTabs>
  <Tab value="Go">
    ```go identityAccount, err := client.IdentityAccount.GetByEmail(ctx, input)
    ```
  </Tab>
  <Tab value="TypeScript">
    ```typescript const identityAccount = await
    stigmer.identityAccount.getByEmail(input); ```
  </Tab>
  <Tab value="Python">
    ```python identityAccount = client.identity_account.get_by_email(input) ```
  </Tab>
  <Tab value="Java">
    ```java IdentityAccount identityAccount =
    client.identityAccount().getByEmail(input); ```
  </Tab>
</SDKTabs>

**Parameters**: `IdentityAccountEmail`

**Returns**: `IdentityAccount`

### getByIdpId

lookup user-account by idp id.

<SDKTabs>
  <Tab value="Go">
    ```go identityAccount, err := client.IdentityAccount.GetByIdpId(ctx, id) ```
  </Tab>
  <Tab value="TypeScript">
    ```typescript const identityAccount = await
    stigmer.identityAccount.getByIdpId(id); ```
  </Tab>
  <Tab value="Python">
    ```python identityAccount = client.identity_account.get_by_idp_id(id) ```
  </Tab>
  <Tab value="Java">
    ```java IdentityAccount identityAccount =
    client.identityAccount().getByIdpId(id); ```
  </Tab>
</SDKTabs>

**Parameters**

| Parameter | Type     | Description                  |
| --------- | -------- | ---------------------------- |
| `id`      | `string` | Identity Account identifier. |

**Returns**: `IdentityAccount`

### getActorInfo

lookup identity-account actor-info (lightweight actor data for audit trail
caching)

This RPC is specifically designed to break circular dependency loops in audit
actor resolution. When converting IdentityAccount entities to proto responses,
the audit info (created_by, updated_by) needs actor details. If we use the
standard get() RPC, it triggers a full entity-to-proto conversion including
audit actors, which can create infinite recursion if audit actors reference
IdentityAccounts.

This dedicated endpoint: - Returns ONLY the lightweight ApiResourceAuditActor
(id + avatar) - Does NOT include full audit trail in the response - Accesses
entity data directly without recursive proto conversion - Is used by
ApiResourceAuditActorCacheProxy to safely populate Redis cache - Prevents
StackOverflowError when Redis cache is empty or cleared

Restricted to platform operators only as this is an internal cache-population
mechanism.

<SDKTabs>
  <Tab value="Go">
    ```go apiResourceAuditActor, err := client.IdentityAccount.GetActorInfo(ctx,
    id) ```
  </Tab>
  <Tab value="TypeScript">
    ```typescript const apiResourceAuditActor = await
    stigmer.identityAccount.getActorInfo(id); ```
  </Tab>
  <Tab value="Python">
    ```python apiResourceAuditActor = client.identity_account.get_actor_info(id)
    ```
  </Tab>
  <Tab value="Java">
    ```java ApiResourceAuditActor apiResourceAuditActor =
    client.identityAccount().getActorInfo(id); ```
  </Tab>
</SDKTabs>

**Parameters**

| Parameter | Type     | Description                  |
| --------- | -------- | ---------------------------- |
| `id`      | `string` | Identity Account identifier. |

**Returns**: `ApiResourceAuditActor`

## Types

### IdentityAccountInput

Input for creating or updating a Identity Account.

<TypeTable
  type={{
    name: { type: "string", description: "Resource name.", required: true },
    slug: { type: "string", description: "URL-friendly identifier." },
    org: { type: "string", description: "Organization slug.", required: true },
    labels: {
      type: "Record<string, string>",
      description: "Key-value labels.",
    },
    idpId: {
      type: "string",
      description: "IDP ID of the identity account.",
      required: true,
    },
    email: { type: "string", description: "Email of the identity account." },
    firstName: {
      type: "string",
      description: "First name of the identity account.",
    },
    lastName: {
      type: "string",
      description: "Last name of the identity account.",
    },
    pictureUrl: { type: "string", description: "URL of the profile picture." },
    isMachineAccount: {
      type: "boolean",
      description:
        "Indicates if this is a machine account used for inter-service communication.",
    },
    provisioningMode: {
      type: "IdentityAccountProvisioningMode",
      description: "How this identity account was provisioned.",
    },
    identityProviderRef: {
      type: "ResourceRef",
      description:
        "Reference to the IdentityProvider that provisioned this account.",
    },
  }}
/>
