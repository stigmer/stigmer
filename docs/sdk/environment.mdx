---
title: Environment
description: "SDK reference for Environment resources."
---

{/* Auto-generated by stigmer-codegen. DO NOT EDIT. */}

Created before AgentInstance or WorkflowInstance, referenced during instance creation.

## Client Access

<SDKTabs>
<Tab value="TypeScript">
```typescript
const environment = await stigmer.environment.get("env_01j5q3k7m8r2s4tnz2hfp0q0c3");
```
</Tab>
<Tab value="Go">
```go
environment, err := client.Environment.Get(ctx, "env_01j5q3k7m8r2s4tnz2hfp0q0c3")
```
</Tab>
<Tab value="Python">
```python
environment = client.environment.get("env_01j5q3k7m8r2s4tnz2hfp0q0c3")
```
</Tab>
<Tab value="Java">
```java
Environment environment = client.environment().get("env_01j5q3k7m8r2s4tnz2hfp0q0c3");
```
</Tab>
</SDKTabs>

## Methods

| Method | Description |
|--------|-------------|
| [`get`](#get) | Get an Environment by ID. |
| [`getByReference`](#getbyreference) | Get an environment by its organization-scoped reference (org/slug). |
| [`getSecretValue`](#getsecretvalue) | Get the unredacted value of a single secret key in an environment. |
| [`list`](#list) | List environments with optional label filtering. |
| [`apply`](#apply) | Create or update an Environment resource. |
| [`create`](#create) | Create a new Environment resource. |
| [`update`](#update) | Update an existing Environment resource. |
| [`updateVariables`](#updatevariables) | Add or update specific variables in an environment (server-side merge). |
| [`delete`](#delete) | Delete an Environment resource. |
| [`removeVariables`](#removevariables) | Remove specific variables from an environment by key. |

### get

Get an Environment by ID.

<SDKTabs>
<Tab value="TypeScript">
```typescript
const environment = await stigmer.environment.get(id);
```
</Tab>
<Tab value="Go">
```go
environment, err := client.Environment.Get(ctx, id)
```
</Tab>
<Tab value="Python">
```python
environment = client.environment.get(id)
```
</Tab>
<Tab value="Java">
```java
Environment environment = client.environment().get(id);
```
</Tab>
</SDKTabs>

**Parameters**

| Parameter | Type | Description |
|-----------|------|-------------|
| `id` | `string` | Environment identifier. |

**Returns**: [`Environment`](#environment)

### getByReference

Get an environment by its organization-scoped reference (org/slug).

<SDKTabs>
<Tab value="TypeScript">
```typescript
const environment = await stigmer.environment.getByReference({
  org: "...",
  kind: "...",
  slug: "...",
  version: "...",
});
```
</Tab>
<Tab value="Go">
```go
environment, err := client.Environment.GetByReference(ctx, &stigmer.ApiResourceReference{
  Org:     "...",
  Kind:    "...",
  Slug:    "...",
  Version: "...",
})
```
</Tab>
<Tab value="Python">
```python
environment = client.environment.get_by_reference(ApiResourceReference(
    org="...",
    kind="...",
    slug="...",
    version="...",
))
```
</Tab>
<Tab value="Java">
```java
Environment environment = client.environment().getByReference(ApiResourceReference.builder()
    .org("...")
    .kind("...")
    .slug("...")
    .version("...")
    .build());
```
</Tab>
</SDKTabs>

**Parameters**: [`ResourceRef`](#resourceref)

**Returns**: [`Environment`](#environment)

### getSecretValue

Get the unredacted value of a single secret key in an environment. Creator-only: requires can_read_secrets permission. Returns the EnvironmentValue with the decrypted value.

<SDKTabs>
<Tab value="TypeScript">
```typescript
const environmentValue = await stigmer.environment.getSecretValue({
  environmentId: "...",
  key: "...",
});
```
</Tab>
<Tab value="Go">
```go
environmentValue, err := client.Environment.GetSecretValue(ctx, &stigmer.EnvironmentSecretValueInput{
  EnvironmentId: "...",
  Key:           "...",
})
```
</Tab>
<Tab value="Python">
```python
environmentValue = client.environment.get_secret_value(EnvironmentSecretValueInput(
    environment_id="...",
    key="...",
))
```
</Tab>
<Tab value="Java">
```java
EnvironmentValue environmentValue = client.environment().getSecretValue(EnvironmentSecretValueInput.builder()
    .environmentId("...")
    .key("...")
    .build());
```
</Tab>
</SDKTabs>

**Parameters**: [`EnvironmentSecretValueInput`](#environmentsecretvalueinput)

**Returns**: [`EnvVarInput`](#envvarinput)

### list

List environments with optional label filtering. Secret values are redacted in the response.

<SDKTabs>
<Tab value="TypeScript">
```typescript
const result = await stigmer.environment.list({
  org: "...",
  labels: {},
  pageInfo: {},
});
```
</Tab>
<Tab value="Go">
```go
result, err := client.Environment.List(ctx, &stigmer.ListEnvironmentsRequest{
  Org:      "...",
  Labels:   map[string]string{},
  PageInfo: &stigmer.PageInfoInput{},
})
```
</Tab>
<Tab value="Python">
```python
result = client.environment.list(ListEnvironmentsRequest(
    org="...",
    labels={},
    page_info=None,
))
```
</Tab>
<Tab value="Java">
```java
EnvironmentList result = client.environment().list(ListEnvironmentsRequest.builder()
    .org("...")
    .labels(Map.of())
    .pageInfo(null)
    .build());
```
</Tab>
</SDKTabs>

**Parameters**: [`ListEnvironmentsRequest`](#listenvironmentsrequest)

**Returns**: [`EnvironmentList`](#environmentlist)

### apply

Create or update an Environment resource. The authorization and state-operation are determined depending on whether the environment is going to be created or updated which is determined as part of the request execution.

<SDKTabs>
<Tab value="TypeScript">
```typescript
const environment = await stigmer.environment.apply({
  name: "my-environment",
  org: "acme",
  slug: "...",
  labels: {},
  description: "...",
  data: {},
});
```
</Tab>
<Tab value="Go">
```go
environment, err := client.Environment.Apply(ctx, &stigmer.EnvironmentInput{
  Name:        "my-environment",
  Org:         "acme",
  Slug:        "...",
  Labels:      map[string]string{},
  Description: "...",
  Data:        map[string]string{},
})
```
</Tab>
<Tab value="Python">
```python
environment = client.environment.apply(EnvironmentInput(
    name="my-environment",
    org="acme",
    slug="...",
    labels={},
    description="...",
    data={},
))
```
</Tab>
<Tab value="Java">
```java
Environment environment = client.environment().apply(EnvironmentInput.builder()
    .name("my-environment")
    .org("acme")
    .slug("...")
    .labels(Map.of())
    .description("...")
    .data(Map.of())
    .build());
```
</Tab>
</SDKTabs>

**Parameters**: [`EnvironmentInput`](#environmentinput)

**Returns**: [`Environment`](#environment)

### create

Create a new Environment resource.

<SDKTabs>
<Tab value="TypeScript">
```typescript
const environment = await stigmer.environment.create({
  name: "my-environment",
  org: "acme",
  slug: "...",
  labels: {},
  description: "...",
  data: {},
});
```
</Tab>
<Tab value="Go">
```go
environment, err := client.Environment.Create(ctx, &stigmer.EnvironmentInput{
  Name:        "my-environment",
  Org:         "acme",
  Slug:        "...",
  Labels:      map[string]string{},
  Description: "...",
  Data:        map[string]string{},
})
```
</Tab>
<Tab value="Python">
```python
environment = client.environment.create(EnvironmentInput(
    name="my-environment",
    org="acme",
    slug="...",
    labels={},
    description="...",
    data={},
))
```
</Tab>
<Tab value="Java">
```java
Environment environment = client.environment().create(EnvironmentInput.builder()
    .name("my-environment")
    .org("acme")
    .slug("...")
    .labels(Map.of())
    .description("...")
    .data(Map.of())
    .build());
```
</Tab>
</SDKTabs>

**Parameters**: [`EnvironmentInput`](#environmentinput)

**Returns**: [`Environment`](#environment)

### update

Update an existing Environment resource.

<SDKTabs>
<Tab value="TypeScript">
```typescript
const environment = await stigmer.environment.update({
  name: "my-environment",
  org: "acme",
  slug: "...",
  labels: {},
  description: "...",
  data: {},
});
```
</Tab>
<Tab value="Go">
```go
environment, err := client.Environment.Update(ctx, &stigmer.EnvironmentInput{
  Name:        "my-environment",
  Org:         "acme",
  Slug:        "...",
  Labels:      map[string]string{},
  Description: "...",
  Data:        map[string]string{},
})
```
</Tab>
<Tab value="Python">
```python
environment = client.environment.update(EnvironmentInput(
    name="my-environment",
    org="acme",
    slug="...",
    labels={},
    description="...",
    data={},
))
```
</Tab>
<Tab value="Java">
```java
Environment environment = client.environment().update(EnvironmentInput.builder()
    .name("my-environment")
    .org("acme")
    .slug("...")
    .labels(Map.of())
    .description("...")
    .data(Map.of())
    .build());
```
</Tab>
</SDKTabs>

**Parameters**: [`EnvironmentInput`](#environmentinput)

**Returns**: [`Environment`](#environment)

### updateVariables

Add or update specific variables in an environment (server-side merge). Existing variables not included in the request are preserved unchanged.

<SDKTabs>
<Tab value="TypeScript">
```typescript
const environment = await stigmer.environment.updateVariables({
  environmentId: "...",
  variables: {},
});
```
</Tab>
<Tab value="Go">
```go
environment, err := client.Environment.UpdateVariables(ctx, &stigmer.UpdateEnvironmentVariablesRequest{
  EnvironmentId: "...",
  Variables:     map[string]string{},
})
```
</Tab>
<Tab value="Python">
```python
environment = client.environment.update_variables(UpdateEnvironmentVariablesRequest(
    environment_id="...",
    variables={},
))
```
</Tab>
<Tab value="Java">
```java
Environment environment = client.environment().updateVariables(UpdateEnvironmentVariablesRequest.builder()
    .environmentId("...")
    .variables(Map.of())
    .build());
```
</Tab>
</SDKTabs>

**Parameters**: [`UpdateEnvironmentVariablesRequest`](#updateenvironmentvariablesrequest)

**Returns**: [`Environment`](#environment)

### delete

Delete an Environment resource.

<SDKTabs>
<Tab value="TypeScript">
```typescript
const environment = await stigmer.environment.delete({ resourceId: id });
```
</Tab>
<Tab value="Go">
```go
environment, err := client.Environment.Delete(ctx, &stigmer.DeleteResourceInput{
  ResourceID: id,
})
```
</Tab>
<Tab value="Python">
```python
environment = client.environment.delete(DeleteResourceInput(resource_id=id))
```
</Tab>
<Tab value="Java">
```java
Environment environment = client.environment().delete(DeleteResourceInput.builder()
    .resourceId(id)
    .build());
```
</Tab>
</SDKTabs>

**Parameters**

| Parameter | Type | Description |
|-----------|------|-------------|
| `resourceId` | `string` | ID of the resource to delete. |
| `versionMessage` | `string` | Optional deletion reason. |
| `force` | `boolean` | Force delete even if resource is in use. |

**Returns**: [`Environment`](#environment)

### removeVariables

Remove specific variables from an environment by key. Keys that don't exist are silently ignored.

<SDKTabs>
<Tab value="TypeScript">
```typescript
const environment = await stigmer.environment.removeVariables({
  environmentId: "...",
  keys: [],
});
```
</Tab>
<Tab value="Go">
```go
environment, err := client.Environment.RemoveVariables(ctx, &stigmer.RemoveEnvironmentVariablesRequest{
  EnvironmentId: "...",
  Keys:          []string{},
})
```
</Tab>
<Tab value="Python">
```python
environment = client.environment.remove_variables(RemoveEnvironmentVariablesRequest(
    environment_id="...",
    keys=[],
))
```
</Tab>
<Tab value="Java">
```java
Environment environment = client.environment().removeVariables(RemoveEnvironmentVariablesRequest.builder()
    .environmentId("...")
    .keys(List.of())
    .build());
```
</Tab>
</SDKTabs>

**Parameters**: [`RemoveEnvironmentVariablesRequest`](#removeenvironmentvariablesrequest)

**Returns**: [`Environment`](#environment)

## Types

### EnvironmentInput

Input for creating or updating an Environment.

<TypeTable
  type={{
    name: { type: "string", description: "Resource name.", required: true },
    slug: { type: "string", description: "URL-friendly identifier." },
    org: { type: "string", description: "Organization slug.", required: true },
    labels: { type: "Record<string, string>", description: "Key-value labels." },
    description: { type: "string", description: "Human-readable description of this environment." },
    data: { type: "Record<string, EnvVarInput>", description: "Key-value pairs containing both configuration and secrets.", typeDescriptionLink: "#envvarinput" },
  }}
/>

### EnvVarInput

EnvironmentValue represents a single configuration or secret value.

<TypeTable
  type={{
    value: { type: "string", description: "The actual value." },
    isSecret: { type: "boolean", description: "Whether this value should be treated as a secret." },
    description: { type: "string", description: "Optional description for documentation." },
  }}
/>

### UpdateEnvironmentVariablesRequest

Request to add or update specific variables in an environment.

<TypeTable
  type={{
    environmentId: { type: "string", description: "The environment resource ID." },
    variables: { type: "Record<string, EnvVarInput>", description: "Variables to add or update.", typeDescriptionLink: "#envvarinput" },
  }}
/>

### RemoveEnvironmentVariablesRequest

Request to remove specific variables from an environment by key.

<TypeTable
  type={{
    environmentId: { type: "string", description: "The environment resource ID." },
    keys: { type: "string[]", description: "Keys to remove from EnvironmentSpec.data." },
  }}
/>

### ResourceRef

Generic reference to any API resource by org and slug.

<TypeTable
  type={{
    org: { type: "string", description: "Organization that owns the referenced resource." },
    kind: { type: "ApiResourceKind", description: "Kind of the referenced resource (e.g., SKILL, AGENT, MCP_SERVER).", typeDescriptionLink: "#apiresourcekind" },
    slug: { type: "string", description: "Resource slug (user-friendly identifier, unique within org).", required: true },
    version: { type: "string", description: "Version of the resource (optional, only applicable to versioned resources like Skills)." },
  }}
/>

### EnvironmentSecretValueInput

Input for retrieving a single unredacted secret value from an environment.

<TypeTable
  type={{
    environmentId: { type: "string", description: "The environment resource ID." },
    key: { type: "string", description: "The key within EnvironmentSpec.data to retrieve the unredacted value for." },
  }}
/>

### ListEnvironmentsRequest

ListEnvironmentsRequest specifies parameters for listing environments.

<TypeTable
  type={{
    org: { type: "string", description: "Organization to list environments for (required)." },
    labels: { type: "Record<string, string>", description: "Filter by metadata labels (optional)." },
    pageInfo: { type: "PageInfoInput", description: "Pagination options (optional)." },
  }}
/>

### EnvironmentList

EnvironmentList contains a paginated list of environments.

<TypeTable
  type={{
    totalCount: { type: "number", description: "Total count of matching environments." },
    items: { type: "EnvironmentInput[]", description: "Environments in the current page.", typeDescriptionLink: "#environmentinput" },
  }}
/>

### Environment

Environment represents a named collection of configuration and secrets.

<TypeTable
  type={{
    apiVersion: { type: "string", description: "API version for this resource type." },
    kind: { type: "string", description: "Resource kind identifier." },
    metadata: { type: "ApiResourceMetadata", description: "Resource metadata including id, name, org, slug, labels, visibility, and timestamps." },
    spec: { type: "EnvironmentInput", description: "Resource specification. See [`EnvironmentInput`](#environmentinput).", typeDescriptionLink: "#environmentinput" },
    status: { type: "EnvironmentStatus", description: "System-managed state. See `EnvironmentStatus`." },
  }}
/>

### ApiResourceKind

API resource kinds for Stigmer.

| Value | Description |
|-------|-------------|
| `api_resource_version` | Agentic - System-level audit trails and versioning. |
| `iam_policy` | IAM - Identity and Access Management. |
| `identity_account` |  |
| `api_key` |  |
| `identity_provider` | IAM - External identity provider configuration. |
| `organization` | Tenancy - Platform and organizational resources. |
| `platform` |  |
| `agent` | Agentic - AI agent management. |
| `agent_execution` |  |
| `session` |  |
| `skill` |  |
| `mcp_server` |  |
| `agent_instance` |  |
| `workflow` | Agentic - Workflow orchestration. |
| `workflow_instance` |  |
| `workflow_execution` |  |
| `environment` |  |
| `execution_context` |  |
| `project` | Tenancy - Project management (aggregate root for resource lifecycle). |

### ApiResourceVisibility

Visibility controls who can access an API resource.

| Value | Description |
|-------|-------------|
| `visibility_private` | Only members of the owning organization can access. |
| `visibility_public` | Anyone can access (read) this resource. |

