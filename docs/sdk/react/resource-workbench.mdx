---
title: Resource Workbench
description: Layout shell and collection hooks for building resource management consoles.
---

{/* Auto-generated by @stigmer/react docs generator. DO NOT EDIT. */}

## Hooks

### useResourceCollection

Headless hook that manages resource collection state: data fetching,
sorting, selection, and pagination.

Wraps `useFetch` for data and `@tanstack/react-table` for table state
(sorting, selection, column visibility). The hook itself produces no
DOM — it returns data, state, and a TanStack Table instance that view
components (`ResourceTable`, `ResourceCards`, `ResourceList`) consume.

Platform builders who want full rendering control use this hook
directly. The `ResourceWorkbench` shell composes it internally.

**Signature**

```tsx
function useResourceCollection(options: UseResourceCollectionOptions<TData>): UseResourceCollectionReturn<TData>
```

<TypeTable
  type={{
    options: { type: "UseResourceCollectionOptions<TData>", description: "", required: true },
  }}
/>

**Returns** — `UseResourceCollectionReturn`

<TypeTable
  type={{
    currentPage: { type: "number", description: "The current page number (mirrors input)." },
    error: { type: "Error | null", description: "Error from the last failed fetch, or `null`." },
    isLoading: { type: "boolean", description: "`true` during the initial data fetch (no data yet)." },
    isRefetching: { type: "boolean", description: "`true` during a background refetch while stale data is shown." },
    items: { type: "readonly TData[]", description: "The fetched data items for the current page." },
    refetch: { type: "() => void", description: "Imperatively trigger a refetch." },
    rowSelection: { type: "RowSelectionState", description: "Current row selection state (item ID -> selected)." },
    setRowSelection: { type: "Dispatch<SetStateAction<RowSelectionState>>", description: "Update row selection state directly." },
    table: { type: "Table<TData> | null", description: "The TanStack Table instance. Available for advanced consumers who need direct table API access (e.g. custom renderers). `null` when no columns are provided." },
    totalCount: { type: "number", description: "Total number of matching items across all pages." },
    totalPages: { type: "number", description: "Total number of pages." },
  }}
/>

```tsx
const collection = useResourceCollection({
  listFn: (params) => stigmer.agent.list(params),
  org: "acme",
  query: debouncedQuery,
  page: 1,
  columns: agentColumns,
});
```


### useResourceFilters

Headless hook that manages filter, sort, and search query state for
a resource collection.

State changes are communicated to the consumer via `onStateChange`
so they can be synced to URL search params. The hook itself has
**zero router dependency** (DD-004 compliance).

Platform builders use this alongside [`useResourceCollection`](#useresourcecollection)
for full control. The `ResourceWorkbench` shell composes both.

**Signature**

```tsx
function useResourceFilters(options: UseResourceFiltersOptions): UseResourceFiltersReturn
```

<TypeTable
  type={{
    options: { type: "UseResourceFiltersOptions", description: "", required: true },
  }}
/>

**Returns** — `UseResourceFiltersReturn`

<TypeTable
  type={{
    addFilter: { type: "(filter: FilterValue) => void", description: "Add a filter. If a filter with the same `filterId` + `operator` exists, it is replaced." },
    clearFilters: { type: "() => void", description: "Remove all active filters." },
    debouncedQuery: { type: "string", description: "The debounced search query (safe to pass to data-fetching hooks)." },
    filters: { type: "readonly FilterValue[]", description: "Current active filters." },
    hasActiveFilters: { type: "boolean", description: "`true` when any filters are active." },
    query: { type: "string", description: "The raw search query as typed by the user." },
    removeFilter: { type: "(filterId: string) => void", description: "Remove a specific filter by its `filterId`. Removes all operators for that filter." },
    setQuery: { type: "(query: string) => void", description: "Update the raw search query. Debouncing is handled internally." },
    setSort: { type: "(sort: SortValue | null) => void", description: "Set the active sort. Pass `null` to clear sorting." },
    sort: { type: "SortValue | null", description: "Current active sort, or `null` if unsorted." },
  }}
/>

```tsx
const filters = useResourceFilters({
  filterDefs: agentFilterDefs,
  sortDefs: agentSortDefs,
  onStateChange: (state) => syncToUrl(state),
});
```


### useResourceSelection

Headless hook that manages selection state for a resource collection.

Supports single toggle, shift-click range selection, select-all, and
automatic clearing when the items array identity changes (page or
filter change).

**Signature**

```tsx
function useResourceSelection(items: readonly TData[], getItemId: (item: TData) => string): UseResourceSelectionReturn<TData>
```

<TypeTable
  type={{
    items: { type: "readonly TData[]", description: "The current page of items. Used to resolve selected                   items and to implement range selection.", required: true },
    getItemId: { type: "(item: TData) => string", description: "Extracts a stable unique ID from an item.", required: true },
  }}
/>

**Returns** — `UseResourceSelectionReturn`

<TypeTable
  type={{
    allSelected: { type: "boolean", description: "`true` when all visible items are selected." },
    clearSelection: { type: "() => void", description: "Clear all selections." },
    hasSelection: { type: "boolean", description: "`true` when at least one item is selected." },
    rangeSelectTo: { type: "(id: string) => void", description: "Range-select from the last toggled item to the given ID (shift-click). Selects all items between them (inclusive)." },
    selectedCount: { type: "number", description: "Number of selected items." },
    selectedIds: { type: "ReadonlySet<string>", description: "Set of currently selected item IDs." },
    selectedItems: { type: "readonly TData[]", description: "The full selected items (resolved from the current items array)." },
    someSelected: { type: "boolean", description: "`true` when some (but not all) visible items are selected." },
    toggleAll: { type: "() => void", description: "Select or deselect all visible items." },
    toggleItem: { type: "(id: string) => void", description: "Toggle a single item's selection state." },
  }}
/>

```tsx
const selection = useResourceSelection(items, (item) => item.id);
```


### useViewPreference

Persists the user's preferred view mode (table / cards / list) in
localStorage, scoped by a caller-provided storage key.

Falls back to `defaultMode` when no persisted value exists or when
the stored value is invalid (forward-compatible with future modes).

Pass `undefined` for `storageKey` to use an ephemeral (non-persisted)
view mode that resets on unmount.

**Signature**

```tsx
function useViewPreference(storageKey: string | undefined, defaultMode: ViewMode): UseViewPreferenceReturn
```

<TypeTable
  type={{
    storageKey: { type: "string | undefined", description: "localStorage key (e.g. `\"stigmer:workbench:agents:viewMode\"`).                    Pass `undefined` to disable persistence.", required: true },
    defaultMode: { type: "ViewMode", description: "The view mode to use when no persisted value exists.", required: true },
  }}
/>

**Returns** — `UseViewPreferenceReturn`

<TypeTable
  type={{
    setViewMode: { type: "(mode: ViewMode) => void", description: "Update the view mode. Persists to localStorage when a storage key is provided." },
    viewMode: { type: "ViewMode", description: "The currently active view mode.", typeDescriptionLink: "#viewmode" },
  }}
/>

```tsx
const { viewMode, setViewMode } = useViewPreference(
  "stigmer:workbench:agents:viewMode",
  "table",
);
```


## Components

### BulkActionBar

Floating action bar that appears when items are selected in the
workbench. Shows selected count, available bulk actions, and a
cancel button.

Uses `aria-live="polite"` so screen readers announce the selection
count when it changes.

**Props** — `BulkActionBarProps`

<TypeTable
  type={{
    actions: { type: "readonly BulkAction<TData>[]", description: "Available bulk actions.", required: true },
    className: { type: "string", description: "Additional CSS classes for the bar." },
    onCancel: { type: "() => void", description: "Called when the user clicks \"Cancel\" (deselect all).", required: true },
    selectedCount: { type: "number", description: "Number of selected items.", required: true },
    selectedItems: { type: "readonly TData[]", description: "The selected items to pass to action handlers.", required: true },
  }}
/>

```tsx
{selection.hasSelection && (
  <BulkActionBar
    selectedCount={selection.selectedCount}
    selectedItems={selection.selectedItems}
    actions={agentBulkActions}
    onCancel={selection.clearSelection}
  />
)}
```


### ColumnHeader

Sortable table column header with accessible sort indicators.

Uses `aria-sort` to communicate sort state to screen readers.
The sort indicator uses both an icon and text direction so the
sort state is never conveyed by icon alone.

**Props** — `ColumnHeaderProps`

<TypeTable
  type={{
    className: { type: "string", description: "Additional CSS classes." },
    label: { type: "string", description: "Column header label.", required: true },
    onSort: { type: "() => void", description: "Called when the user clicks a sortable header." },
    sortable: { type: "boolean", description: "Whether clicking this header toggles sort.", required: true },
    sortDirection: { type: "SortDirection | null", description: "Current sort direction for this column, or `null` if not sorted.", required: true },
  }}
/>


### FilterBar

Displays active filters as removable chips with a "Clear all" action.

The filter bar is a lightweight presentation component — filter state
and mutation logic live in [`useResourceFilters`](#useresourcefilters). The parent
`ResourceWorkbench` wires them together.

**Props** — `FilterBarProps`

<TypeTable
  type={{
    className: { type: "string", description: "Additional CSS classes." },
    filterDefs: { type: "readonly FilterDef[]", description: "Available filter definitions (used to resolve labels).", required: true },
    filters: { type: "readonly FilterValue[]", description: "Currently active filter values.", required: true },
    onClearAll: { type: "() => void", description: "Called when the \"Clear all\" button is clicked.", required: true },
    onRemoveFilter: { type: "(filterId: string) => void", description: "Called when a filter chip's \"remove\" button is clicked.", required: true },
  }}
/>

```tsx
<FilterBar
  filters={filtersHook.filters}
  filterDefs={agentFilterDefs}
  onRemoveFilter={filtersHook.removeFilter}
  onClearAll={filtersHook.clearFilters}
/>
```


### ResourceAvatar

Compact resource avatar for cards, rows, and detail headers.

Renders one of three states:
1. **Image** — when `iconUrl` is a non-empty string
2. **Initial** — colored circle with the first letter of `name`,
   deterministic color derived from `slug`
3. **Hidden** — nothing (for resource types that don't support icons)

**Props** — `ResourceAvatarProps`

<TypeTable
  type={{
    className: { type: "string", description: "Additional CSS classes." },
    hidden: { type: "boolean", description: "When `true`, renders nothing — no image, no initial avatar. Used for resource types like skills that don't support icons." },
    iconUrl: { type: "string", description: "Icon URL from the resource's spec. Empty string means no icon." },
    name: { type: "string", description: "Display name used for the initial fallback.", required: true },
    size: { type: "\"md\" | \"sm\"", description: "Size variant." },
    slug: { type: "string", description: "Slug used to derive a deterministic background color.", required: true },
  }}
/>


### ResourceCards

Card grid view for the resource workbench.

Renders items as a responsive grid of cards. Each card supports an
optional selection checkbox, an action slot, and click handling.
Card content is fully customizable via the `renderCard` prop.

This component renders **only the card grid**. Search, filters, and
pagination are handled by the parent `ResourceWorkbench`.

**Props** — `ResourceCardsProps`

<TypeTable
  type={{
    "aria-label": { type: "string", description: "Accessible label for the card grid region." },
    className: { type: "string", description: "Additional CSS classes for the grid container." },
    enableSelection: { type: "boolean", description: "Whether selection checkboxes are shown on cards." },
    getItemId: { type: "(item: TData) => string", description: "Extracts a stable unique ID from an item." },
    items: { type: "readonly TData[]", description: "Items to render as cards.", required: true },
    onCardClick: { type: "(item: TData) => void", description: "Called when a card is clicked (not when a control inside is clicked)." },
    onToggleSelection: { type: "(id: string) => void", description: "Called when an item's selection state is toggled." },
    renderCard: { type: "(item: TData, index: number) => ReactNode", description: "Render function for each card's content.", required: true },
    renderCardAction: { type: "(item: TData) => ReactNode", description: "Render function for per-card actions (e.g. an `ActionMenu`). Shown in the top-right corner of the card." },
    selectedIds: { type: "ReadonlySet<string>", description: "Set of selected item IDs." },
  }}
/>


### ResourceInspector

Split-panel inspector that appears to the right of the workbench
content area, providing a preview of the focused resource without
navigating away from the list.

The inspector is a purely presentational shell — what renders inside
it is determined by the consumer via the `children` prop. The parent
`ResourceWorkbench` manages open/close state and passes the focused
item's detail view.

**Props** — `ResourceInspectorProps`

<TypeTable
  type={{
    "aria-label": { type: "string", description: "Accessible label for the panel." },
    children: { type: "ReactNode", description: "The content to render inside the inspector panel.", required: true },
    className: { type: "string", description: "Additional CSS classes for the panel." },
    onClose: { type: "() => void", description: "Called when the user closes the inspector (e.g. via the close button).", required: true },
    open: { type: "boolean", description: "Whether the inspector panel is currently visible.", required: true },
  }}
/>


### ResourceList

Compact list view for the resource workbench.

Renders items as a single-column vertical list of rows. Each row
supports selection, an action slot, and click handling. Row content
is customizable via the `renderRow` prop.

This component renders **only the list**. Search, filters, and
pagination are handled by the parent `ResourceWorkbench`.

**Props** — `ResourceListProps`

<TypeTable
  type={{
    "aria-label": { type: "string", description: "Accessible label for the list region." },
    className: { type: "string", description: "Additional CSS classes for the list container." },
    enableSelection: { type: "boolean", description: "Whether selection checkboxes are shown on rows." },
    getItemId: { type: "(item: TData) => string", description: "Extracts a stable unique ID from an item." },
    items: { type: "readonly TData[]", description: "Items to render as compact list rows.", required: true },
    onRowClick: { type: "(item: TData) => void", description: "Called when a row is clicked (not when a control inside is clicked)." },
    onToggleSelection: { type: "(id: string) => void", description: "Called when an item's selection state is toggled." },
    renderRow: { type: "(item: TData, index: number) => ReactNode", description: "Render function for each row's content.", required: true },
    renderRowAction: { type: "(item: TData) => ReactNode", description: "Render function for per-row actions (e.g. an `ActionMenu`). Shown at the end of the row." },
    selectedIds: { type: "ReadonlySet<string>", description: "Set of selected item IDs." },
  }}
/>


### ResourceTable

Table view for the resource workbench.

Renders a semantic `<table>` element using column definitions and
data from a TanStack Table instance. Supports sortable column
headers, row selection checkboxes, per-row action menus, and
keyboard-navigable rows.

This component renders **only the table**. Search, filters, view
switching, and pagination are handled by the parent `ResourceWorkbench`.

**Props** — `ResourceTableProps`

<TypeTable
  type={{
    "aria-label": { type: "string", description: "Accessible label for the table." },
    className: { type: "string", description: "Additional CSS classes for the root container." },
    enableSelection: { type: "boolean", description: "Whether row selection is enabled." },
    onRowClick: { type: "(item: TData) => void", description: "Called when a row is clicked (but not when a checkbox or action inside the row is clicked)." },
    renderRowAction: { type: "(item: TData) => ReactNode", description: "Render function for per-row actions (e.g. an `ActionMenu`). Shown in the last column. Omit to hide the actions column." },
    table: { type: "Table<TData>", description: "TanStack Table instance from `useResourceCollection`.", required: true },
  }}
/>

```tsx
const { table } = useResourceCollection({ ... });
<ResourceTable
  table={table}
  enableSelection
  renderRowAction={(item) => <AgentActionMenu item={item} />}
  onRowClick={(item) => navigate(item.slug)}
/>
```


### ResourceWorkbench

Full-featured resource collection workbench that composes headless
hooks and focused view components into a complete browsing experience.

Provides:
- Toolbar with search input, filter chips, and view mode switcher
- Three view modes: table, cards, and compact list
- Row/card selection with bulk action bar
- Optional split-panel inspector for item previews
- Empty, loading, and error states
- Pagination

The workbench is the "drop-in" tier of the resource collection
architecture. Platform builders who want more control use the
individual hooks and view components directly.

**Props** — `ResourceWorkbenchProps`

<TypeTable
  type={{
    "aria-label": { type: "string", description: "Accessible label for the workbench region." },
    bulkActions: { type: "readonly BulkAction<TData>[]", description: "Bulk actions available when items are selected." },
    className: { type: "string", description: "Additional CSS classes for the root container." },
    columns: { type: "readonly WorkbenchColumnDef<TData>[]", description: "Column definitions for the table view." },
    defaultViewMode: { type: "ViewMode", description: "Default view mode.", typeDescriptionLink: "#viewmode" },
    emptyAction: { type: "ReactNode", description: "Custom CTA rendered in the first-use empty state (no items, no active filters). Use this to provide a creation entry point that is visually co-located with the \"no resources yet\" message.  Accepts any ReactNode — typically a `<Link>` or `<button>`." },
    emptyDescription: { type: "string", description: "Description for the empty state." },
    emptyIcon: { type: "ReactNode", description: "Icon for the empty state." },
    emptyTitle: { type: "string", description: "Title for the empty state." },
    enableSelection: { type: "boolean", description: "Enable selection checkboxes and bulk actions." },
    filterDefs: { type: "readonly FilterDef[]", description: "Available filter definitions." },
    getItemId: { type: "(item: TData) => string", description: "Extracts a unique ID from an item (for selection tracking)." },
    headerAction: { type: "ReactNode", description: "Primary action rendered right-aligned in the toolbar, after the view mode switcher. Use this for the workbench's main creation entry point — e.g. a \"Create agent\" button or link.  Accepts any ReactNode so consumers control routing and styling." },
    initialFilters: { type: "readonly FilterValue[]", description: "Initial filter values (e.g. restored from URL on mount)." },
    initialQuery: { type: "string", description: "Initial search query (e.g. restored from URL on mount)." },
    initialSort: { type: "SortValue | null", description: "Initial sort (e.g. restored from URL on mount)." },
    listFn: { type: "(params: ListParams) => Promise<ListResult> | null", description: "Async function that fetches a page of resources. Receives `ListParams` and returns `ListResult`. Pass `null` to disable fetching.", required: true },
    onItemClick: { type: "(item: TData) => void", description: "Called when an item is clicked (navigates to detail, etc.)." },
    onRetry: { type: "() => void", description: "Called when the user clicks \"Retry\" after an error." },
    onScopeChange: { type: "(scope: \"org\" | \"all\") => void", description: "Called when scope changes (for Console to persist)." },
    onStateChange: { type: "(state: { filters: readonly FilterValue[]; query: string; sort: SortValue | null }) => void", description: "Called when any filter/sort/query state changes (for URL sync)." },
    org: { type: "string | null", description: "Organization slug. Pass `null` to disable fetching.", required: true },
    refetchToken: { type: "unknown", description: "Opaque token that forces a background refetch of the list whenever its value changes — the current view stays rendered with a refetch shimmer (no remount flash), and pagination and sort are preserved. Bump it after an out-of-band mutation (applying a manifest, deleting a row) so the change appears without a reload." },
    renderCard: { type: "(item: TData, index: number) => ReactNode", description: "Custom card renderer for card view. Receives the item and its index. When omitted, the workbench falls back to a default card layout." },
    renderInspector: { type: "(item: TData) => ReactNode", description: "Inspector content renderer. When provided, clicking an item opens the split-panel inspector with this content. When omitted, the inspector is disabled." },
    renderItemAction: { type: "(item: TData) => ReactNode", description: "Per-item action renderer (shown in table rows, card corners, list ends)." },
    renderRow: { type: "(item: TData, index: number) => ReactNode", description: "Custom row renderer for list view. Receives the item and its index. When omitted, falls back to a default row layout." },
    scope: { type: "\"org\" | \"all\"", description: "Scope for resource visibility." },
    searchPlaceholder: { type: "string", description: "Search input placeholder text." },
    viewModes: { type: "readonly ViewMode[]", description: "Available view modes." },
    viewModeStorageKey: { type: "string", description: "localStorage key for persisting view mode preference. Pass `undefined` to disable persistence." },
  }}
/>

```tsx
<ResourceWorkbench
  listFn={(params) => stigmer.agent.list(params)}
  org={activeOrg}
  columns={agentColumns}
  filterDefs={agentFilterDefs}
  renderItemAction={(item) => <AgentActionMenu item={item} />}
  onItemClick={(item) => navigate(`/agents/${item.slug}`)}
  emptyTitle="No agents yet"
  emptyDescription="Create an agent to get started."
/>
```


### SelectionCheckbox

Accessible checkbox for row/card selection and "select all" headers.

Uses a native `<input type="checkbox">` with the `indeterminate`
property set via ref for proper browser + screen reader support.

**Props** — `SelectionCheckboxProps`

<TypeTable
  type={{
    "aria-label": { type: "string", description: "Accessible label for the checkbox.", required: true },
    checked: { type: "boolean", description: "Whether the checkbox is checked.", required: true },
    className: { type: "string", description: "Additional CSS classes." },
    indeterminate: { type: "boolean", description: "Indeterminate state (e.g. header checkbox when some but not all rows are selected). Overrides `checked` visually." },
    onChange: { type: "(checked: boolean) => void", description: "Called when the user toggles the checkbox.", required: true },
  }}
/>


### StatusBadge

Accessible status indicator that combines a color dot with a text label.

Never communicates status through color alone — the text label is always
present (WCAG 1.4.1). Colors come from `--stgm-status-*` design tokens
defined in `@stigmer/theme`, ensuring correct appearance in both light
and dark modes.

**Props** — `StatusBadgeProps`

<TypeTable
  type={{
    className: { type: "string", description: "Additional CSS classes for the root element." },
    label: { type: "string", description: "Optional human-readable label override. When omitted, the phase name is title-cased (e.g. \"ready\" becomes \"Ready\")." },
    phase: { type: "StatusPhase", description: "The status phase to display. Maps to `--stgm-status-*` tokens.", required: true, typeDescriptionLink: "#statusphase" },
    tooltip: { type: "string", description: "Tooltip text shown on hover. When omitted, no tooltip renders." },
  }}
/>

```tsx
<StatusBadge phase="ready" />
<StatusBadge phase="failed" label="Unreachable" tooltip="Last checked 2m ago" />
```


### ViewSwitcher

Toggle button group for switching between table, cards, and list views.

Uses `role="radiogroup"` with `role="radio"` children for accessible
view switching. Each button shows an icon and is labeled for screen
readers.

**Props** — `ViewSwitcherProps`

<TypeTable
  type={{
    className: { type: "string", description: "Additional CSS classes." },
    modes: { type: "readonly ViewMode[]", description: "Which view modes to show." },
    onChange: { type: "(mode: ViewMode) => void", description: "Called when the user selects a different view mode.", required: true },
    value: { type: "ViewMode", description: "Currently active view mode.", required: true, typeDescriptionLink: "#viewmode" },
  }}
/>


## Types

### BulkAction

A bulk action available when multiple items are selected.

<TypeTable
  type={{
    disabled: { type: "boolean", description: "When `true`, the action is shown but non-interactive." },
    icon: { type: "ReactNode", description: "Icon rendered before the label." },
    id: { type: "string", description: "Stable action identifier.", required: true },
    label: { type: "string", description: "Display label (e.g. \"Delete\", \"Change visibility\").", required: true },
    onAction: { type: "(items: readonly TData[]) => void", description: "Called with the full set of selected items.", required: true },
    variant: { type: "\"default\" | \"destructive\"", description: "Visual variant." },
  }}
/>


### FilterDef

Defines a filterable property for the [`FilterBar`](#filterbar).

Platform builders declare which properties are filterable and how
they appear in the filter UI.

<TypeTable
  type={{
    id: { type: "string", description: "Stable filter identifier (used in URL state serialization).", required: true },
    label: { type: "string", description: "Human-readable label shown in the filter picker and chips.", required: true },
    operators: { type: "readonly FilterOperator[]", description: "Allowed operators. Defaults to `[\"eq\"]` for selects, `[\"contains\"]` for text, and `[\"gte\", \"lte\"]` for dates." },
    options: { type: "readonly FilterOption[]", description: "Available options for `select` and `multi-select` types. Ignored for `text` and `date`." },
    type: { type: "\"text\" | \"select\" | \"date\" | \"multi-select\"", description: "Filter input type.", required: true },
  }}
/>


### FilterOperator

Operator for a single filter expression.


### FilterOption

A selectable option within a `select` or `multi-select` filter.

<TypeTable
  type={{
    icon: { type: "ReactNode", description: "Optional icon rendered before the label." },
    label: { type: "string", description: "Display label.", required: true },
    value: { type: "string", description: "The value sent in the filter expression.", required: true },
  }}
/>


### FilterSortState

Serializable snapshot of filter + sort + query state.

<TypeTable
  type={{
    filters: { type: "readonly FilterValue[]", description: "", required: true },
    query: { type: "string", description: "", required: true },
    sort: { type: "SortValue | null", description: "", required: true },
  }}
/>


### FilterValue

A single active filter value applied to the collection.

<TypeTable
  type={{
    filterId: { type: "string", description: "The filter definition ID this value applies to.", required: true },
    operator: { type: "FilterOperator", description: "Comparison operator.", required: true, typeDescriptionLink: "#filteroperator" },
    value: { type: "string | readonly string[]", description: "Filter value(s). A string for scalar operators, an array for `in`.", required: true },
  }}
/>


### ResourceAction

A single action available for a resource item (shown in context menus).

<TypeTable
  type={{
    disabled: { type: "boolean", description: "When `true`, the action is shown but non-interactive." },
    icon: { type: "ReactNode", description: "Icon rendered before the label." },
    id: { type: "string", description: "Stable action identifier.", required: true },
    label: { type: "string", description: "Display label.", required: true },
    onAction: { type: "(item: TData) => void", description: "Called when the action is selected. Receives the target item.", required: true },
    shortcut: { type: "string", description: "Keyboard shortcut hint displayed in the menu." },
    variant: { type: "\"default\" | \"destructive\"", description: "Visual variant." },
  }}
/>


### SortDef

Defines a sortable property. Used by hooks to validate incoming sort
state and by the UI to render sort options in column headers and
sort dropdowns.

<TypeTable
  type={{
    defaultDirection: { type: "SortDirection", description: "Default direction when this sort is first activated.", typeDescriptionLink: "#sortdirection" },
    id: { type: "string", description: "Stable identifier matching a column ID or virtual sort key.", required: true },
    label: { type: "string", description: "Human-readable label (e.g. \"Name\", \"Last updated\").", required: true },
  }}
/>


### SortDirection

Sort direction.


### SortValue

A single active sort applied to the collection.

<TypeTable
  type={{
    direction: { type: "SortDirection", description: "Sort direction.", required: true, typeDescriptionLink: "#sortdirection" },
    id: { type: "string", description: "Column or property ID to sort by.", required: true },
  }}
/>


### StatusPhase

Resource status phases that map to `--stgm-status-*` design tokens.

Each phase corresponds to a token group defined in `@stigmer/theme`
(e.g. `--stgm-status-ready`, `--stgm-status-failed-subtle`).


### UseResourceCollectionOptions

Options for [`useResourceCollection`](#useresourcecollection).

<TypeTable
  type={{
    columns: { type: "readonly WorkbenchColumnDef<TData>[]", description: "Column definitions for the table view. These are converted to TanStack Table column defs internally." },
    enableSelection: { type: "boolean", description: "Enable row selection state tracking." },
    listFn: { type: "(params: ListParams) => Promise<ListResult> | null", description: "Async function that fetches a page of resources. The hook passes `ListParams` (org, query, page, scope) and expects `ListResult` back. Pass `null` to disable fetching (idle state).", required: true },
    onSortChange: { type: "(sort: SortValue | null) => void", description: "Called when the user changes the sort via a column header click." },
    org: { type: "string | null", description: "Organization slug to scope the query. Pass `null` to disable fetching.", required: true },
    page: { type: "number", description: "Current page number (1-indexed)." },
    pageSize: { type: "number", description: "Page size." },
    query: { type: "string", description: "Text search query. No debouncing is applied — the consumer controls timing." },
    refetchToken: { type: "unknown", description: "Opaque token that forces a background refetch whenever its value changes. Use it to re-read the list after an out-of-band mutation (e.g. applying a manifest or deleting a row) without remounting: bump a counter and pass it here. The current page stays rendered with an `isRefetching` shimmer — no full-list flash. A change to this token alone never resets pagination or sort." },
    scope: { type: "\"org\" | \"all\"", description: "Scope for resource visibility. - `\"org\"` — resources owned by the active organization. - `\"all\"` — includes public resources from other organizations." },
    sort: { type: "SortValue | null", description: "Active sort. When provided, the column header shows the sort indicator." },
  }}
/>


### UseResourceFiltersOptions

Options for [`useResourceFilters`](#useresourcefilters).

<TypeTable
  type={{
    filterDefs: { type: "readonly FilterDef[]", description: "Available filter definitions. Determines which properties can be filtered." },
    initialFilters: { type: "readonly FilterValue[]", description: "Initial active filters (e.g. restored from URL params on mount)." },
    initialQuery: { type: "string", description: "Initial search query (e.g. restored from URL params on mount)." },
    initialSort: { type: "SortValue | null", description: "Initial active sort (e.g. restored from URL params on mount)." },
    onStateChange: { type: "(state: FilterSortState) => void", description: "Called whenever filter, sort, or query state changes. The consumer uses this to sync state to URL search params or any other persistence layer. The hook itself has zero router dependency." },
    queryDebounceMs: { type: "number", description: "Debounce delay in milliseconds for the search query." },
    sortDefs: { type: "readonly SortDef[]", description: "Available sort definitions. Determines which properties can be sorted." },
  }}
/>


### ViewMode

Available layout modes for the resource workbench.


### WorkbenchColumnDef

Defines a single column in the [`ResourceTable`](#resourcetable).

Generic over `TData` so platform builders can use richer item types
beyond `SearchResult` while the Console uses `SearchResult` directly.

<TypeTable
  type={{
    cell: { type: "(item: TData) => ReactNode", description: "Accessor function that extracts the cell value from a row item. Return a `ReactNode` for custom cell rendering.", required: true },
    defaultVisible: { type: "boolean", description: "Whether this column is visible by default. Users can toggle visibility via column preferences." },
    flex: { type: "number", description: "Relative flex weight for distributing remaining horizontal space. Columns with higher weight receive more space." },
    header: { type: "string", description: "Column header label.", required: true },
    id: { type: "string", description: "Stable column identifier (used for visibility persistence and URL state).", required: true },
    minWidth: { type: "number", description: "Minimum column width in pixels. The table uses this as a CSS `min-width` to prevent content collapse." },
    sortable: { type: "boolean", description: "Whether this column is sortable. When `true`, clicking the header toggles sort direction." },
  }}
/>


### WorkbenchState

Serializable snapshot of the workbench's filter, sort, pagination,
and view state. Passed to `onStateChange` so consumers can sync
this state to URL search params or other persistence layers.

<TypeTable
  type={{
    filters: { type: "readonly FilterValue[]", description: "", required: true },
    page: { type: "number", description: "", required: true },
    pageSize: { type: "number", description: "", required: true },
    query: { type: "string", description: "", required: true },
    sort: { type: "SortValue | null", description: "", required: true },
    viewMode: { type: "ViewMode", description: "", required: true, typeDescriptionLink: "#viewmode" },
  }}
/>

