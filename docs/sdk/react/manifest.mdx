---
title: manifest
---

{/* Auto-generated by @stigmer/react docs generator. DO NOT EDIT. */}

## Hooks

### useApplyManifest

Headless "apply a YAML manifest" flow for any registry-supported kind —
the console counterpart of `stigmer apply -f`.

Orchestrates: paste or upload → strict parse against generated proto
schemas (`parseManifest`) → per-document create-vs-update preview
(server existence check) → sequential apply in dependency order with
live per-document status.

Accepts multi-document manifests (`---` separators) and JSON content
(YAML is a superset). The target `org` is injected into documents that
omit `metadata.org`; documents naming a *different* org are honored and
apply there (matching `stigmer apply` semantics — the org-mismatch
warning surfaces on the parsed document).

**Signature**

```tsx
function useApplyManifest(org: string): UseApplyManifestReturn
```

<TypeTable
  type={{
    org: { type: "string", description: "Target organization slug for `metadata.org` injection.", required: true },
  }}
/>

**Returns** — `UseApplyManifestReturn`

<TypeTable
  type={{
    applyAll: { type: "() => Promise<boolean>", description: "Apply all documents sequentially in dependency order. Entries update live. Stops at the first failure (later entries become `\"skipped\"`). Resolves `true` when every document applied." },
    content: { type: "string", description: "Current manifest text (paste target)." },
    entries: { type: "readonly ManifestPreviewEntry[] | null", description: "Validated per-document preview in dependency apply order, or `null` while the content is empty or invalid." },
    hasRedactedSecrets: { type: "boolean", description: "`true` when the content contains `***REDACTED***` secret markers." },
    isApplying: { type: "boolean", description: "`true` while applies are in flight." },
    isValidating: { type: "boolean", description: "`true` while the (debounced) validation + existence checks run." },
    readFile: { type: "(file: File) => Promise<void>", description: "Read a `.yaml`/`.yml`/`.json` file into `content`." },
    reset: { type: "() => void", description: "Clear all state (content, preview, errors)." },
    setContent: { type: "(value: string) => void", description: "Update the manifest text. Re-validates (debounced)." },
    validationError: { type: "string | null", description: "Validation error for the current content, or `null` when valid." },
  }}
/>

```tsx
const manifest = useApplyManifest(activeOrg);

<textarea value={manifest.content}
          onChange={(e) => manifest.setContent(e.target.value)} />
{manifest.entries?.map((e) => (
  <PreviewRow key={e.document.slug} entry={e} />
))}
<button onClick={manifest.applyAll} disabled={!manifest.entries}>Apply</button>
```


### useEditResourceYaml

Behavior hook for editing an existing resource as YAML and applying the
result — the console's kind-agnostic "Edit YAML" flow.

Seeds the editor from `serializeManifest` (full fidelity minus
system-managed state), validates every edit against the kind's generated
proto schema via `parseManifest` (strict — unknown fields fail
loudly), previews create-vs-update by resolving the edited document's
reference against the server, and applies through
`stigmer.manifest.apply()`.

Guardrails beyond the raw engine:
- the edited document must stay the same kind as the source resource
  (pasting an Environment into an Agent editor is an error, not an apply)
- the edited manifest must contain exactly one document

**Signature**

```tsx
function useEditResourceYaml(options: UseEditResourceYamlOptions): UseEditResourceYamlReturn
```

<TypeTable
  type={{
    options: { type: "UseEditResourceYamlOptions", description: "", required: true },
  }}
/>

**Returns** — `UseEditResourceYamlReturn`

<TypeTable
  type={{
    apply: { type: "() => Promise<AppliedManifest | null>", description: "Apply the edited YAML. Resolves with the applied resource, or `null` when the content is not currently valid (check `validation`)." },
    clearError: { type: "() => void", description: "Clear the apply error (e.g. when the user edits again)." },
    error: { type: "Error | null", description: "Error from the last failed apply, or `null` when healthy." },
    hasRedactedSecrets: { type: "boolean", description: "`true` when the content contains `***REDACTED***` secret markers." },
    isApplying: { type: "boolean", description: "`true` while the apply RPC is in flight." },
    isDirty: { type: "boolean", description: "`true` when the content differs from the seeded serialization." },
    reset: { type: "() => void", description: "Discard edits and reseed from the resource." },
    setYaml: { type: "(value: string) => void", description: "Update the editor content (wire to the YAML editor's `onChange`)." },
    target: { type: "EditYamlTarget | null", description: "Create-vs-update preview for the current content, or `null` while validation fails or the existence check is in flight." },
    validation: { type: "EditYamlValidation", description: "Live validation of the current content.", typeDescriptionLink: "#edityamlvalidation" },
    yaml: { type: "string", description: "Current editor content." },
  }}
/>

```tsx
const edit = useEditResourceYaml({ resource: agent });

<YamlEditor value={edit.yaml} onChange={edit.setYaml} />
{edit.validation.status === "invalid" && <p>{edit.validation.message}</p>}
<button disabled={edit.validation.status !== "valid"} onClick={edit.apply}>
  {edit.target?.action === "create" ? "Create" : "Apply changes"}
</button>
```


## Components

### ApplyManifestDialog

Modal dialog for applying a Stigmer resource manifest — paste YAML or
upload a file, review what will change, and apply. The console
counterpart of `stigmer apply -f`, for every supported resource kind.

Workflow:
1. Paste manifest YAML (or JSON) into the editor, or pick a file
2. Each document is validated against its kind's generated proto schema
   and previewed with a create-or-update badge resolved from server state
3. Apply runs the documents sequentially in dependency order with live
   per-document status; a failure stops the run and renders inline

Multi-document manifests (`---` separators) are supported. Documents
that omit `metadata.org` are applied to the active org; documents naming
a different org are honored with a warning (matching `stigmer apply`).

Uses the native `<dialog>` element for built-in focus trapping, Escape
key handling, and backdrop — consistent with [`ConfirmDialog`](/docs/sdk/react/resource-detail#confirmdialog).

Zero Console dependencies — safe for platform builder embedding.

**Props** — `ApplyManifestDialogProps`

<TypeTable
  type={{
    onOpenChange: { type: "(open: boolean) => void", description: "Called when the dialog should open or close.", required: true },
    onSuccess: { type: "(entries: readonly ManifestPreviewEntry[]) => void", description: "Called after every document applied successfully, with the per-document results in apply order. Use for navigation or list refresh." },
    open: { type: "boolean", description: "Whether the dialog is open.", required: true },
    org: { type: "string", description: "Target organization slug for documents that omit `metadata.org`.", required: true },
  }}
/>

```tsx
<ApplyManifestDialog
  open={open}
  onOpenChange={setOpen}
  org={activeOrg}
  onSuccess={() => refetchList()}
/>
```


### EditResourceYamlDialog

Modal dialog for editing a resource as YAML and applying the changes —
the kind-agnostic "Edit YAML" experience for detail pages and panels.

Workflow:
1. The resource is serialized to canonical YAML (system-managed state
   stripped) and seeded into a CodeMirror editor
2. Every edit is validated against the kind's generated proto schema;
   errors render inline with the exact schema complaint
3. A live summary states what applying will do ("update X" / "create Y"),
   resolved against server state — so a rename reads as a create
4. Apply calls the kind's `apply` RPC (create-or-update by slug)

Redacted secret values (`***REDACTED***`) are explained inline: applying
them preserves the stored secrets (the server's marker contract).

Uses the native `<dialog>` element for built-in focus trapping, Escape
key handling, and backdrop — consistent with [`ConfirmDialog`](/docs/sdk/react/resource-detail#confirmdialog).

Zero Console dependencies — safe for platform builder embedding.

**Props** — `EditResourceYamlDialogProps`

<TypeTable
  type={{
    onApplied: { type: "(result: AppliedManifest) => void", description: "Called after a successful apply. Use for refetching or navigation." },
    onOpenChange: { type: "(open: boolean) => void", description: "Called when the dialog should open or close.", required: true },
    open: { type: "boolean", description: "Whether the dialog is open.", required: true },
    resource: { type: "Message | null", description: "The resource to edit, as returned by the server. `null` while loading — the dialog shows the editor as soon as the resource arrives.", required: true },
  }}
/>

```tsx
const { agent, refetch } = useAgent(org, slug);

<EditResourceYamlDialog
  open={editOpen}
  onOpenChange={setEditOpen}
  resource={agent}
  onApplied={() => refetch()}
/>
```


### RedactedSecretsNotice

Inline explanation shown when manifest content contains `***REDACTED***`
secret markers, so the marker reads as a contract rather than a bug:
applying it preserves the stored secret; replacing it sets a new value.

Shared by [`EditResourceYamlDialog`](#editresourceyamldialog) and [`ApplyManifestDialog`](#applymanifestdialog).

**Props** — `RedactedSecretsNoticeProps`

<TypeTable
  type={{
    className: { type: "string", description: "Additional CSS classes for the root container." },
  }}
/>


## Types

### EditYamlTarget

What applying the current content will do — resolved against live
server state so renames surface as "create" before the user commits.


### EditYamlValidation

Result of validating the edited YAML against the source resource.

Discriminated on `status` so consumers can render exactly one of:
a disabled state (`empty`), an inline error (`invalid`), or the
apply affordance (`valid`).


### ManifestEntryStatus

Per-document apply lifecycle within one manifest.


### ManifestPreviewEntry

One document of the manifest, with its preview and apply state.

<TypeTable
  type={{
    action: { type: "\"unknown\" | \"create\" | \"update\"", description: "What applying will do, resolved against live server state.", required: true },
    document: { type: "ManifestDocument", description: "The parsed document (kind handler, message, name/slug/org).", required: true },
    errorMessage: { type: "string", description: "User-facing failure message, present when `status` is `\"failed\"`." },
    result: { type: "AppliedManifest", description: "The applied resource, present when `status` is `\"applied\"`." },
    status: { type: "ManifestEntryStatus", description: "Apply lifecycle status.", required: true, typeDescriptionLink: "#manifestentrystatus" },
  }}
/>


### UseEditResourceYamlOptions

Options for [`useEditResourceYaml`](#useeditresourceyaml).

<TypeTable
  type={{
    resource: { type: "Message | null", description: "The resource to edit, as returned by the server (e.g. from `useAgent` or a list panel row). `null` while the resource is loading — the hook seeds the editor when it arrives.", required: true },
  }}
/>


### YamlEditor

YAML editor built on CodeMirror 6, shared by every YAML surface in the
SDK (workflow editor tab, resource manifest dialogs).

Provides syntax highlighting, line numbers, bracket matching,
code folding, undo/redo, and inline diagnostic markers driven by
external validation (via the `diagnostics` prop).

Themed via `--stgm-*` CSS custom properties to respect the host
application's design tokens.

This is an optional SDK component — it requires CodeMirror peer
dependencies. Consumers that render it conditionally should load it
via `React.lazy` so the CodeMirror bundle stays out of paths that
never open an editor (DD-013).

```tsx
<YamlEditor
  value={yaml}
  onChange={setYaml}
  diagnostics={diagnostics}
/>
```


### YamlEditorProps

Props for [`YamlEditor`](#yamleditor).

<TypeTable
  type={{
    className: { type: "string", description: "Additional CSS class names for the root container." },
    diagnostics: { type: "readonly Diagnostic[]", description: "Validation diagnostics to display as inline markers." },
    onChange: { type: "(value: string) => void", description: "Called when the editor content changes." },
    readOnly: { type: "boolean", description: "Whether the editor is read-only." },
    style: { type: "CSSProperties", description: "Additional inline styles for the root container." },
    value: { type: "string", description: "Current YAML content.", required: true },
  }}
/>

