---
title: datastore
---

{/* Auto-generated by @stigmer/react docs generator. DO NOT EDIT. */}

## Hooks

### useDatastore

Data hook that fetches a single Datastore by organization and slug.

Wraps `stigmer.datastore.getByReference()` with loading, error, and
not-found state management. The loaded resource carries the spec
(collections, fields, constraints, authorization) and status (sync
report, per-collection record counts) — the authoritative source for
every structural view (DD-008 SD-5: spec for structure, status for
health; `describeDatastore` is consulted only for caller-effective
verbs — see [`useDatastoreDescription`](#usedatastoredescription)).

Pass `null` for either `org` or `slug` to skip fetching (stable
no-op).

**Not-found handling:** a NOT_FOUND response sets `datastore` to
`null` without raising an error; `datastore === null && !isLoading
&& !error` means the resource does not exist.

**Signature**

```tsx
function useDatastore(org: string | null, slug: string | null): UseDatastoreReturn
```

<TypeTable
  type={{
    org: { type: "string | null", description: "", required: true },
    slug: { type: "string | null", description: "", required: true },
  }}
/>

**Returns** — `UseDatastoreReturn`

<TypeTable
  type={{
    datastore: { type: "Datastore | null", description: "The resolved datastore, or `null` while loading, on error, or when not found." },
    error: { type: "Error | null", description: "Error from the last failed request, or `null` when healthy." },
    isLoading: { type: "boolean", description: "`true` while the initial fetch is in flight." },
    isRefetching: { type: "boolean", description: "`true` while a background refetch is in flight." },
    refetch: { type: "() => void", description: "Discard cached data and re-fetch the datastore from the server." },
  }}
/>

```tsx
const { datastore, isLoading, error } = useDatastore("acme", "clinic");
```


### useDatastoreCount

Data hook that fetches the total count of datastores.

Issues a minimal `stigmer.datastore.list()` call to retrieve only the
total count — no datastore entries are returned or stored. Useful for
the Library landing card and dashboard widgets.

For the full paginated datastore list, use [`useDatastoreList`](#usedatastorelist)
instead.

Pass `null` as `org` to skip fetching (stable no-op).

**Signature**

```tsx
function useDatastoreCount(org: string | null, options?: UseDatastoreCountOptions): UseDatastoreCountReturn
```

<TypeTable
  type={{
    org: { type: "string | null", description: "", required: true },
    options: { type: "UseDatastoreCountOptions", description: "" },
  }}
/>

**Returns** — `UseDatastoreCountReturn`

<TypeTable
  type={{
    count: { type: "number | undefined", description: "Total number of datastores matching the current filters. `undefined` until the first successful fetch completes." },
    error: { type: "Error | null", description: "Error from the last failed request, or `null` when healthy." },
    isLoading: { type: "boolean", description: "`true` while the count fetch is in flight." },
    refetch: { type: "() => void", description: "Discard cached data and re-fetch the count from the server." },
  }}
/>

```tsx
const { count, isLoading } = useDatastoreCount("acme");
```


### useDatastoreDescription

Data hook that fetches the caller-effective view of a datastore via
`describeDatastore`.

This is the console's **only** source of caller-effective record
verbs (DD-008 SD-2): record grants are datastore domain logic, not
FGA, so resource-layer permission checks structurally cannot answer
"may I insert into bookings?". Every record write affordance gates on
the per-collection `access` lists returned here — and those lists are
projections, never authority: the server's record layer remains the
enforcer, so a stale projection degrades to a clean denial.

**Denied-state semantics** (DD-008 SD-3): a caller with resource
visibility but no record grants gets a successful response with
empty `access` lists — deny-by-default renders as empty access,
never an error. A reach-level `PERMISSION_DENIED` (no
`can_use_records` in cloud) surfaces as `error`; consumers render
both as the Records-tab denied panel, not a page-level failure.

Pass `null` for `org` or `datastore` to skip fetching (stable no-op).

**Signature**

```tsx
function useDatastoreDescription(org: string | null, datastore: string | null): UseDatastoreDescriptionReturn
```

<TypeTable
  type={{
    org: { type: "string | null", description: "", required: true },
    datastore: { type: "string | null", description: "", required: true },
  }}
/>

**Returns** — `UseDatastoreDescriptionReturn`

<TypeTable
  type={{
    description: { type: "DatastoreDescription | null", description: "The caller-effective description, or `null` while loading or on error." },
    error: { type: "Error | null", description: "Error from the last failed request, or `null` when healthy." },
    isLoading: { type: "boolean", description: "`true` while the initial fetch is in flight." },
    isRefetching: { type: "boolean", description: "`true` while a background refetch is in flight." },
    partitions: { type: "readonly string[]", description: "Selectable data partitions: the datastore's partition catalog with `\"default\"` always present. The catalog materializes with the first record write, so on an empty datastore it reports nothing — but the default partition is always addressable (DD-010: unset partition means `\"default\"`), so the picker must always offer it." },
    refetch: { type: "() => void", description: "Discard cached data and re-fetch the description from the server." },
  }}
/>

```tsx
const { description, partitions } = useDatastoreDescription("acme", "clinic");
const bookings = description?.collections.find((c) => c.name === "bookings");
const canInsert = bookings?.access.some((a) => a.verb === DatastoreVerb.insert);
```


### useDatastoreList

Data hook that fetches a paginated list of datastores for the Library.

Wraps `stigmer.datastore.list()` (SearchService-backed) with
pagination, scope filtering, and text search. All parameters are
externally controlled — the consumer manages page state, query
debouncing, and scope toggling.

Pass `null` as `org` to skip fetching (stable no-op).

**Signature**

```tsx
function useDatastoreList(org: string | null, options?: UseDatastoreListOptions): UseDatastoreListReturn
```

<TypeTable
  type={{
    org: { type: "string | null", description: "", required: true },
    options: { type: "UseDatastoreListOptions", description: "" },
  }}
/>

**Returns** — `UseDatastoreListReturn`

<TypeTable
  type={{
    currentPage: { type: "number", description: "The current page number (mirrors the `page` option)." },
    datastores: { type: "readonly SearchResult[]", description: "Paginated datastore entries for the current page." },
    error: { type: "Error | null", description: "Error from the last failed request, or `null` when healthy." },
    isLoading: { type: "boolean", description: "`true` while the initial fetch or a refetch is in flight." },
    refetch: { type: "() => void", description: "Discard cached data and re-fetch the current page from the server." },
    totalCount: { type: "number", description: "Total number of datastores matching the current filters." },
    totalPages: { type: "number", description: "Total pages available at the current page size." },
  }}
/>

```tsx
const { datastores, totalCount, isLoading } = useDatastoreList("acme", {
  page: 1,
  pageSize: 20,
});
```


### useDatastoreSearch

Data hook that searches datastores available in the given organization.

Wraps `stigmer.datastore.list()` with debounced search, loading/error
tracking, and cancellation-safe fetching. Platform builders use this
when they want full control over rendering.

**Signature**

```tsx
function useDatastoreSearch(org: string, options?: UseResourceSearchOptions): UseResourceSearchReturn
```

<TypeTable
  type={{
    org: { type: "string", description: "", required: true },
    options: { type: "UseResourceSearchOptions", description: "" },
  }}
/>

```tsx
const { results, isLoading, query, setQuery } = useDatastoreSearch("acme");
```


### useDeleteDatastore

Behavior hook for the platform's only record-destroying resource
delete.

Deliberately domain-local rather than a new arm in the generic
`useDeleteResource` kind-switch: datastore deletion carries an
acknowledgment protocol (`force`) and guard-error rendering that the
generic one-shot delete contract must not absorb (DD-003 — extending
it would leak record-destruction semantics platform-wide). No
success/failure toasts here; `DeleteDatastoreDialog` owns the UX.

**Signature**

```tsx
function useDeleteDatastore(): UseDeleteDatastoreReturn
```

**Returns** — `UseDeleteDatastoreReturn`

<TypeTable
  type={{
    clearError: { type: "() => void", description: "Reset `error` to `null`." },
    deleteDatastore: { type: "(args: DeleteDatastoreArgs) => Promise<Datastore>", description: "Delete a datastore through its two server guards (DD-003), in order:  1. **Agent-reference block** — a datastore referenced by any    agent's `datastore_usages` cannot be deleted, force or not; the    `FAILED_PRECONDITION` names the referencing agents (detach    first). 2. **Non-empty acknowledgment** — held records require `force`;    without it the error reports current record/collection counts.  Rejects — and sets `error` — when a guard refuses; the message is the server's authoritative text with fresh counts, so on drift the dialog re-renders the guard rather than pre-empting it (DD-008 invariant 6)." },
    error: { type: "Error | null", description: "Error from the last refused or failed delete, or `null` when healthy." },
    isDeleting: { type: "boolean", description: "`true` while the delete is in flight." },
  }}
/>


### useDeleteRecord

Behavior hook that wraps `datastore.deleteRecord()` with loading and
error state.

Record deletes are id-addressed with a blast radius of one — the
standard destructive confirm suffices (DD-008 SD-4). Deleting the
datastore itself is a different, guarded operation: see
[`useDeleteDatastore`](#usedeletedatastore).

**Signature**

```tsx
function useDeleteRecord(): UseDeleteRecordReturn
```

**Returns** — `UseDeleteRecordReturn`

<TypeTable
  type={{
    clearError: { type: "() => void", description: "Reset `error` to `null`." },
    deleteRecord: { type: "(args: DeleteRecordArgs) => Promise<RecordEnvelope>", description: "Delete one record by id. Resolves with the deleted record's final envelope (usable for an undo-by-reinsert growth path). Rejects — and sets `error` — on denial; the error's message is the server's relayable text, byte-for-byte." },
    error: { type: "Error | null", description: "Error from the last failed delete, or `null` when healthy." },
    isDeleting: { type: "boolean", description: "`true` while the delete is in flight." },
  }}
/>


### useInsertRecord

Behavior hook that wraps `datastore.insertRecord()` with loading and
error state.

Constraint violations (`ALREADY_EXISTS` for uniques,
`FAILED_PRECONDITION` for checks) carry the constraint's declared
message plus a `google.rpc.ErrorInfo` naming the constraint — use
`getRecordConstraint` from `@stigmer/sdk` to place the message next
to the fields it covers.

**Signature**

```tsx
function useInsertRecord(): UseInsertRecordReturn
```

**Returns** — `UseInsertRecordReturn`

<TypeTable
  type={{
    clearError: { type: "() => void", description: "Reset `error` to `null`." },
    error: { type: "Error | null", description: "Error from the last failed insert, or `null` when healthy." },
    insertRecord: { type: "(args: InsertRecordArgs) => Promise<RecordEnvelope>", description: "Insert one record. Resolves with the server-stamped envelope (id, timestamps, attribution). Rejects — and sets `error` — on denial or constraint violation; the error's message is the server's relayable text, byte-for-byte (render it verbatim, DD-008 invariant 4)." },
    isInserting: { type: "boolean", description: "`true` while the insert is in flight." },
  }}
/>

```tsx
const { insertRecord, isInserting, error } = useInsertRecord();
await insertRecord({
  org: "acme",
  datastore: "clinic",
  collection: "bookings",
  record: { slot_date: "2026-07-22", slot_time: "09:30:00" },
});
```


### useRecordCollection

Headless hook that manages records-grid state: `findRecords` fetching,
server-side sort, and RPC limit/offset pagination.

The deliberate sibling of `useResourceCollection` (DD-008 SD-2):
same headless shape — `useFetch` underneath, TanStack table with
`manualPagination`/`manualSorting`, controlled sort via
`sort`/`onSortChange` — but the record data contract, which the
resource collection structurally cannot carry (its fetch path is
hardcoded to `ListParams`/`SearchResult`). Mirrored, not retrofitted,
so the SDK stays learnable; the produced table feeds the same
presentational `ResourceTable`.

Everything is server-side: filtering (the typed proto filter),
sorting (single-field `order_by`), and pagination (limit/offset,
default 25, max 100). The hook performs no client-side filtering or
sorting — one query surface (DD-008 invariant 1).

**Signature**

```tsx
function useRecordCollection(options: UseRecordCollectionOptions): UseRecordCollectionReturn
```

<TypeTable
  type={{
    options: { type: "UseRecordCollectionOptions", description: "", required: true },
  }}
/>

**Returns** — `UseRecordCollectionReturn`

<TypeTable
  type={{
    currentPage: { type: "number", description: "The current page number (mirrors input)." },
    error: { type: "Error | null", description: "Error from the last failed fetch, or `null`. A record-layer `PERMISSION_DENIED` lands here carrying the server's relayable message verbatim — render it as the denied panel, never a silently empty grid (DD-008 SD-3)." },
    isLoading: { type: "boolean", description: "`true` during the initial fetch (no data yet)." },
    isRefetching: { type: "boolean", description: "`true` during a background refetch while stale data is shown." },
    records: { type: "readonly RecordEnvelope[]", description: "Records in the current page." },
    refetch: { type: "() => void", description: "Imperatively trigger a refetch (e.g. after a record write)." },
    table: { type: "Table<RecordEnvelope> | null", description: "The TanStack Table instance for `ResourceTable` (or custom renderers). `null` when no columns are provided." },
    total: { type: "number", description: "Total records matching the filter (across all pages)." },
    totalPages: { type: "number", description: "Total number of pages at the current page size." },
  }}
/>

```tsx
const collection = useRecordCollection({
  org: "acme",
  datastore: "clinic",
  collection: "bookings",
  partition,
  filter,
  page,
  sort,
  onSortChange: setSort,
  columns: recordColumns,
});
return <ResourceTable table={collection.table} />;
```


### useRecordList

Data hook that fetches a page of records via `findRecords` — the one
query surface (DD-008 invariant 1): server-side filtering with the
DD-005 grammar, RPC limit/offset pagination, no client-side
filtering.

A record-layer `PERMISSION_DENIED` (caller holds no read grant on the
collection) surfaces as `error` carrying the server's relayable
message verbatim; consumers render it as the denied panel, never a
silently empty grid.

Pass `null` to skip fetching (stable no-op) — e.g. while the
collection is not yet selected.

For the full grid experience (TanStack table, sort bridge, page
state), use [`useRecordCollection`](#userecordcollection), which builds on the same
RPC.

**Signature**

```tsx
function useRecordList(params: UseRecordListParams | null): UseRecordListReturn
```

<TypeTable
  type={{
    params: { type: "UseRecordListParams | null", description: "", required: true },
  }}
/>

**Returns** — `UseRecordListReturn`

<TypeTable
  type={{
    error: { type: "Error | null", description: "Error from the last failed request, or `null` when healthy." },
    isLoading: { type: "boolean", description: "`true` while the initial fetch is in flight." },
    isRefetching: { type: "boolean", description: "`true` while a background refetch is in flight." },
    records: { type: "readonly RecordEnvelope[]", description: "Records in the current page." },
    refetch: { type: "() => void", description: "Discard cached data and re-fetch the current page from the server." },
    total: { type: "number", description: "Total records matching the filter (across all pages)." },
  }}
/>


### useUpdateRecord

Behavior hook that wraps `datastore.updateRecord()` with loading and
error state.

The RPC is an honest tri-state partial merge — the hook adds no
merge semantics of its own. A record in another partition is
NOT_FOUND (DD-010: partition scoping is structural).

**Signature**

```tsx
function useUpdateRecord(): UseUpdateRecordReturn
```

**Returns** — `UseUpdateRecordReturn`

<TypeTable
  type={{
    clearError: { type: "() => void", description: "Reset `error` to `null`." },
    error: { type: "Error | null", description: "Error from the last failed update, or `null` when healthy." },
    isUpdating: { type: "boolean", description: "`true` while the update is in flight." },
    updateRecord: { type: "(args: UpdateRecordArgs) => Promise<RecordEnvelope>", description: "Update one record by id. Resolves with the merged envelope. Rejects — and sets `error` — on denial or constraint violation; the error's message is the server's relayable text, byte-for-byte." },
  }}
/>

```tsx
const { updateRecord } = useUpdateRecord();
await updateRecord({
  org: "acme",
  datastore: "clinic",
  collection: "bookings",
  id: "dsr_01j9...",
  fields: { patient_phone: "+15550100", notes: null }, // notes cleared
});
```


## Components

### CollectionRecordsBrowser

The records browser (DD-008 SD-3): collection selector, partition
picker, schema-aware filter builder, typed-cell grid, RPC-paged
navigation, and verb-gated write affordances — the console surface
that absorbs the Supabase-dashboard capability the datastore
primitive retires.

Authorization is rendered as the two layers it is:
- Write affordances gate on `describeDatastore`'s per-collection
  effective verbs (projections, never authority — the server's
  record layer stays the enforcer).
- **The denied state is a first-class render**, driven primarily by
  empty access lists (deny-by-default renders as empty access, never
  an error) with a caught reach-level `PERMISSION_DENIED` as the
  defensive branch. Both show the relayable message plus operator
  guidance — never a silently empty grid.

All visual properties flow through `--stgm-*` design tokens.
Zero Console dependencies — safe for platform builder embedding.

**Props** — `CollectionRecordsBrowserProps`

<TypeTable
  type={{
    className: { type: "string", description: "Additional CSS classes for the root container." },
    datastoreSlug: { type: "string", description: "Datastore slug.", required: true },
    org: { type: "string", description: "Organization slug.", required: true },
    spec: { type: "DatastoreSpec", description: "The datastore spec — collections and fields drive the grid columns.", required: true },
  }}
/>


### CollectionSchemaView

Structural view of a datastore's declared schema, rendered from the
loaded resource spec: per-collection typed field tables, the declared
constraints with their authored messages (the operator's own copy —
the same bytes agents relay), and the authorization block summary
(roles, bindings, `default_role` — operator-facing state the
describe projection deliberately excludes).

Read-only by design: schema changes go through YAML apply, the one
write path for structure.

All visual properties flow through `--stgm-*` design tokens.
Zero Console dependencies — safe for platform builder embedding.

**Props** — `CollectionSchemaViewProps`

<TypeTable
  type={{
    className: { type: "string", description: "Additional CSS classes for the root container." },
    spec: { type: "DatastoreSpec", description: "The datastore spec — the authoritative source for structure (DD-008 SD-5).", required: true },
  }}
/>


### DatastoreDetailView

Self-contained detail view for a Datastore — the Library-resource
home (DD-008 SD-1): an **Overview** tab (schema, authorization
summary, and the sync-report health strip — all from the loaded
resource, the authoritative source for structure and health) and a
**Records** tab (the browser, whose caller-effective verbs come from
`describeDatastore`). No version-history affordance —
`is_versioned: false` (DD-003).

Handles loading, error, and not-found states automatically.
Zero Console dependencies — safe for platform builder embedding.
All visual properties flow through `--stgm-*` design tokens.

**Props** — `DatastoreDetailViewProps`

<TypeTable
  type={{
    actions: { type: "readonly DetailAction[]", description: "Overflow actions (Edit YAML / Export / Delete — assembled at the page level)." },
    activeTab: { type: "DatastoreDetailTab", description: "Controlled active tab. Uncontrolled (internal state) when omitted.", typeDescriptionLink: "#datastoredetailtab" },
    className: { type: "string", description: "Additional CSS classes for the root container." },
    defaultTab: { type: "DatastoreDetailTab", description: "Initial tab for uncontrolled usage.", typeDescriptionLink: "#datastoredetailtab" },
    onResourceLoad: { type: "(datastore: Datastore) => void", description: "Called when the datastore loads or reloads (e.g. breadcrumb label sync)." },
    onTabChange: { type: "(tab: DatastoreDetailTab) => void", description: "Tab-change callback for controlled usage." },
    org: { type: "string", description: "Organization slug.", required: true },
    primaryAction: { type: "DetailAction", description: "Primary action rendered as a visible button.", typeDescriptionLink: "/docs/sdk/react/resource-detail#detailaction" },
    slug: { type: "string", description: "Datastore slug.", required: true },
  }}
/>


### DatastoreIcon

Database-cylinder icon shared by the detail view's states.


### DatastoreSyncReport

The Overview health strip: projects `DatastoreStatus` — last apply
outcome, per-collection materialization state, and record counts —
so the operator can confirm an apply landed without leaving the
console (the cutover choreography's verification surface, DD-009).

All visual properties flow through `--stgm-*` design tokens.
Zero Console dependencies — safe for platform builder embedding.

**Props** — `DatastoreSyncReportProps`

<TypeTable
  type={{
    className: { type: "string", description: "Additional CSS classes for the root container." },
    status: { type: "DatastoreStatus", description: "The datastore status — the authoritative source for health (DD-008 SD-5).", required: true },
  }}
/>


### DeleteDatastoreDialog

The guarded delete flow for the platform's only record-destroying
resource action (DD-008 SD-6) — a status-informed acknowledgment
replacing the generic one-shot confirm:

- Counts are pre-filled from the already-loaded `status` ("holds N
  records across M collections").
- A **non-empty** datastore requires **typing the datastore slug** to
  arm the delete — the strongest-friction confirm. Arming sends the
  `force` acknowledgment.
- An **empty** datastore keeps the standard destructive confirm.
- The server's guards stay authoritative: on refusal (agent
  references, count drift) the `FAILED_PRECONDITION` message renders
  **verbatim** inside the dialog with fresh server counts — the
  console renders the guard, never pre-empts it. The agent-reference
  block names the referencing agents; detaching them is the operator's
  next step, and the block is deliberately not acknowledgeable-through.

All visual properties flow through `--stgm-*` design tokens.
Zero Console dependencies — safe for platform builder embedding.

**Props** — `DeleteDatastoreDialogProps`

<TypeTable
  type={{
    datastore: { type: "Datastore | null", description: "The loaded datastore (metadata + status feed the counts).", required: true },
    onDeleted: { type: "() => void", description: "Called after a successful delete (navigate away)." },
    onOpenChange: { type: "(open: boolean) => void", description: "Called when the dialog requests to close.", required: true },
    open: { type: "boolean", description: "Whether the dialog is open.", required: true },
  }}
/>


### FieldValueControl

The typed value control for a declared datastore field — one
controlled input per `FieldType`, emitting canonical encodings
(DD-004). Shared by `RecordFormPanel` (field editing) and
`RecordFilterBuilder` (condition values), so a field always offers
the same input affordance everywhere.

Type dispatch:
- `string`             — text input; enum-select when `enum_values` declared
- `integer` / `number` — numeric input (step 1 / any)
- `bool`               — select with true/false (a checkbox conflates
                         "false" with "unset", which the tri-state
                         write model must not do)
- `date`               — native date picker (emits `YYYY-MM-DD`)
- `time`               — native time picker (emits zero-padded `HH:MM[:SS]`)
- `timestamp`          — native datetime-local picker in the operator's
                         zone, converted to RFC 3339 UTC
- `json`               — mono textarea with local parse state; only
                         valid JSON is emitted

**Props** — `FieldValueControlProps`

<TypeTable
  type={{
    "aria-label": { type: "string", description: "Accessible name when no external label is wired." },
    disabled: { type: "boolean", description: "Disables the input." },
    field: { type: "FieldDeclaration", description: "The field declaration driving the control type.", required: true },
    id: { type: "string", description: "DOM id for external `<label htmlFor>` wiring." },
    onChange: { type: "(value: JsonValue | undefined) => void", description: "Called with the canonical value on every valid edit, or `undefined` when the input empties. Invalid intermediate states (half-typed JSON, unparseable numbers) stay local to the control and are never emitted — the precedent set by the workflow config form.", required: true },
    value: { type: "JsonValue | undefined", description: "Current value in its canonical encoding (DD-004), or `undefined` when empty/unset. The control never receives explicit `null` — the clear affordance is the owner's (a form row renders it; the control only edits values).", required: true },
  }}
/>


### RecordFilterBuilder

Schema-aware filter builder for the records browser (DD-008 SD-3):
active conditions render as removable chips; the "Add filter" popover
is draft-then-apply — a condition takes effect only on Apply, so
half-built drafts never fire queries.

The builder can express nothing `findRecords` cannot serve (DD-008
invariant 2): fields come from the declarations plus the filterable
system fields, operators from the per-type matrix (an unservable
operator is structurally unofferable), and values from the same typed
controls the record form uses.

All visual properties flow through `--stgm-*` design tokens.
Zero Console dependencies — safe for platform builder embedding.

**Props** — `RecordFilterBuilderProps`

<TypeTable
  type={{
    className: { type: "string", description: "Additional CSS classes for the root container." },
    collection: { type: "CollectionDeclaration", description: "The collection whose declared schema drives fields and operators.", required: true },
    conditions: { type: "readonly RecordConditionDraft[]", description: "Active conditions (AND-combined, per the DD-005 grammar).", required: true },
    onChange: { type: "(conditions: readonly RecordConditionDraft[]) => void", description: "Called with the full condition set on add/remove/clear.", required: true },
    readableFields: { type: "readonly string[]", description: "The caller's readable fields from `describeDatastore` (the read verb's `readable_fields`). Empty or omitted means every field. When restricted, unreadable fields are not offered — the server refuses conditions on them." },
  }}
/>


### RecordFormPanel

The record write surface (DD-008 SD-4): one schema-generated panel
form serving insert and edit, with typed controls dispatched from
`FieldDeclaration`.

- **Insert** submits every field the operator set; required fields
  gate the submit (error prevention — the server remains the
  enforcer).
- **Edit** submits a **partial merge**: only dirty fields travel, and
  the per-field clear affordance sends an explicit `null` — the form
  is honest about the RPC's tri-state. System fields render read-only
  and never travel.
- **Errors replay the DD-002/DD-005 contract for humans**: a
  constraint violation renders the declared message **verbatim** —
  field-adjacent when the `ErrorInfo` constraint name maps through
  the spec to declared fields (unique constraints), as a form-level
  banner otherwise. The console operator and the WhatsApp patient
  read the same bytes.

All visual properties flow through `--stgm-*` design tokens.
Zero Console dependencies — safe for platform builder embedding.

**Props** — `RecordFormPanelProps`

<TypeTable
  type={{
    collection: { type: "CollectionDeclaration", description: "The collection declaration driving the form's typed controls.", required: true },
    onOpenChange: { type: "(open: boolean) => void", description: "Called when the panel requests to close (Escape, Cancel, or save).", required: true },
    onSaved: { type: "(envelope: RecordEnvelope) => void", description: "Called with the server-stamped envelope after a successful save." },
    open: { type: "boolean", description: "Whether the panel is open.", required: true },
    record: { type: "RecordEnvelope | null", description: "The record to edit, or `null`/`undefined` to insert a new one. Edit submits a partial merge of dirty fields only." },
    scope: { type: "RecordScope", description: "Record addressing: org, datastore, collection, partition.", required: true, typeDescriptionLink: "#recordscope" },
  }}
/>


## Types

### buildRecordFilter

Build the proto `RecordFilter` from complete drafts. Returns
`undefined` for an empty set (an absent filter, not an empty one, is
the RPC's "no filter").

```tsx
function buildRecordFilter(conditions: readonly RecordConditionDraft[]): RecordFilter | undefined
```


### buildUpdateFields

Build an update payload from per-field edits (DD-005 partial merge).
The RPC's tri-state: a key present with a value replaces the field, a
key present with `null` explicitly clears it, an absent key leaves
the stored value untouched. Callers pass only the dirty fields —
cleared ones as `null`.

```tsx
function buildUpdateFields(edits: ReadonlyMap<string, JsonValue>): JsonObject
```


### coerceFieldValue

Validate a caller-supplied field value against its declaration and
return the canonical JSON encoding (see module doc). `null` means
explicit null — valid here for any field; required-ness is write-path
policy (updates only see the supplied subset), exactly as in the
server's CanonicalizeValue.

Error messages match the server's shape (`field "x" must be …`) so a
client-side rejection reads the same as the server rejection it
pre-empts.

```tsx
function coerceFieldValue(field: FieldDeclaration, value: JsonValue): CoerceResult
```


### CoerceResult

Result of [`coerceFieldValue`](#coercefieldvalue): a canonical value or a field-level message.


### DatastoreDetailTab

The detail view's tabs.


### DEFAULT_PARTITION

The partition every datastore starts with (DD-010).


### DeleteDatastoreArgs

Arguments for `UseDeleteDatastoreReturn.deleteDatastore`.

<TypeTable
  type={{
    force: { type: "boolean", description: "Acknowledgment that the datastore's records will be destroyed. Required for non-empty datastores (the server's GuardNonEmpty rejects without it, reporting record and collection counts); meaningless for empty ones. `DeleteDatastoreDialog` arms it only after the operator types the datastore slug." },
    resourceId: { type: "string", description: "Datastore resource id (`dst_…`).", required: true },
  }}
/>


### DeleteRecordArgs

Arguments for a single record delete.

<TypeTable
  type={{
    collection: { type: "string", description: "Collection name.", required: true },
    datastore: { type: "string", description: "Datastore slug.", required: true },
    id: { type: "string", description: "Record id (`dsr_…`).", required: true },
    org: { type: "string", description: "Organization slug. Required for cloud direct callers; OSS defaults it.", required: true },
    partition: { type: "string", description: "Data partition. Empty or omitted means `\"default\"`." },
  }}
/>


### FIELD_INPUT_CLASSES

Standard input styling for datastore value controls — the
token-compliant idiom (semantic Tailwind classes over `--stgm-*`
tokens; no pixel font sizes, no hex fallbacks).


### FilterableField

A field the builder can offer: a declared filterable field or a
filterable system field, with the exact operator set the server
admits for it.

<TypeTable
  type={{
    declaration: { type: "FieldDeclaration", description: "The declaration, or `undefined` for system fields." },
    name: { type: "string", description: "", required: true },
    operators: { type: "readonly RecordConditionOp[]", description: "", required: true },
    type: { type: "FieldType", description: "The field type driving the value control (system fields use fixed types).", required: true },
  }}
/>


### filterableFields

The fields a collection's filter builder offers: declared filterable
fields (json is not filterable) followed by the filterable system
fields. `id` conditions take string values; the audit timestamps take
RFC 3339 timestamps — both ride the `timestamp`/`string` value
controls.

`readableFields` is the caller's column-level read access from
`describeDatastore` (the read verb's `readable_fields`; empty means
every field). When restricted, unreadable declared fields are not
offered — the server refuses conditions on them (the existence-oracle
guard), and the builder must stay structurally unable to express an
unservable filter (DD-008 invariant 2). System fields are always
readable and always offered.

```tsx
function filterableFields(collection: CollectionDeclaration, readableFields?: readonly string[]): FilterableField[]
```


### formatConditionChip

Chip text for an active condition, e.g. `status = confirmed` or `notes is empty`.

```tsx
function formatConditionChip(draft: RecordConditionDraft): string
```


### formatFieldValue

Format a canonical field value for grid-cell display. Values render
in their DD-004 canonical encodings (honest, timezone-free); the one
concession to readability is trimming a timestamp's zero fractional
seconds (storage pads to nine digits for lexicographic ordering —
"…T04:30:00.000000000Z" and "…T04:30:00Z" are the same RFC 3339 UTC
instant).

```tsx
function formatFieldValue(type: FieldType, value: JsonValue | undefined): string
```


### formatSubject

Plain kind+value rendering of a datastore subject (DD-008 SD-3: the
grant system is the privacy boundary; display is honest).

```tsx
function formatSubject(subject: DatastoreSubject | undefined): string
```


### formatSystemTimestamp

Also used for the envelope's created_at/updated_at system columns.

```tsx
function formatSystemTimestamp(value: string): string
```


### InsertRecordArgs

Arguments for a single record insert.

<TypeTable
  type={{
    collection: { type: "string", description: "Collection name.", required: true },
    datastore: { type: "string", description: "Datastore slug.", required: true },
    org: { type: "string", description: "Organization slug. Required for cloud direct callers; OSS defaults it.", required: true },
    partition: { type: "string", description: "Data partition. Empty or omitted means `\"default\"`." },
    record: { type: "JsonObject", description: "Declared field values in their canonical encodings (DD-004). System field names (`id`, `created_at`, …) are rejected by the server.", required: true },
  }}
/>


### isConditionComplete

Whether a draft is complete enough to serve: value-less operators
need nothing, list operators need at least one value, scalar
operators need a value.

```tsx
function isConditionComplete(draft: RecordConditionDraft): boolean
```


### isListOperator

Operators that take a value list (`values`) rather than a scalar.

```tsx
function isListOperator(op: RecordConditionOp): boolean
```


### isSortableField

Sortable fields: any declared non-json field, plus the system id and
audit timestamps (mirrors the server's BuildOrderBy).

```tsx
function isSortableField(field: FieldDeclaration): boolean
```


### isValuelessOperator

Operators that take no value (null tests).

```tsx
function isValuelessOperator(op: RecordConditionOp): boolean
```


### OPERATOR_LABELS

Human labels for the filter builder and chips. `is_in` is displayed
as "in" — the proto spelling exists only because `in` is reserved in
Java/Python, a substrate detail operators never see.


### operatorsForField

The per-type operator matrix (record_io.proto @internal, DD-005 SD-2)
— a projection of the server's opsForFieldType. The filter builder
offers exactly these, making an unservable operator structurally
unofferable (DD-008 invariant 2):

  string (incl. enum):   eq, neq, is_in, not_in
  bool:                  eq, neq
  integer, number,
  timestamp, date, time: eq, neq, gt, gte, lt, lte
  json:                  not filterable
  + is_null / not_null on any optional (non-required) field

```tsx
function operatorsForField(field: FieldDeclaration): RecordConditionOp[]
```


### RecordColumnDef

Column definition for the records grid — a row is a `RecordEnvelope`.


### RecordConditionDraft

One draft condition in the filter builder — the client-side twin of
the proto `RecordCondition`, with plain JSON values (converted to
`google.protobuf.Value` only at request-build time).

<TypeTable
  type={{
    field: { type: "string", description: "Declared field name, or a filterable system field (`id`, `created_at`, `updated_at`).", required: true },
    op: { type: "RecordConditionOp", description: "Comparison operator — must be admitted by the field's operator set.", required: true },
    value: { type: "JsonValue", description: "Scalar comparison value. Absent for `is_null`/`not_null` and list operators." },
    values: { type: "readonly JsonValue[]", description: "Comparison values for `is_in`/`not_in`." },
  }}
/>


### RecordScope

Addressing shared by every record read/write: which datastore, which
collection, and which data partition (DD-010 — every record operation
is partition-scoped; empty means the `"default"` partition).

<TypeTable
  type={{
    collection: { type: "string", description: "Collection name.", required: true },
    datastore: { type: "string", description: "Datastore slug.", required: true },
    org: { type: "string", description: "Organization slug. Required for cloud direct callers; OSS defaults it.", required: true },
    partition: { type: "string", description: "Data partition. Empty or omitted means `\"default\"`." },
  }}
/>


### RESERVED_FIELD_NAMES

Record-envelope system field names (plus the cloud tenancy column and
the DD-010 partition label). Never declarable, never caller-writable.


### SYSTEM_FIELD_OPERATORS

The filterable system fields and their operator sets (mirrors the
server's buildSystemCondition): `id` by identity, the audit
timestamps by range. `created_by`, `org`, and `partition` are not
filterable — attribution is the grant system's privacy boundary and
partition is ambient scope, never data.


### UpdateRecordArgs

Arguments for a single record update.

<TypeTable
  type={{
    collection: { type: "string", description: "Collection name.", required: true },
    datastore: { type: "string", description: "Datastore slug.", required: true },
    fields: { type: "JsonObject", description: "Partial merge (DD-005): only the supplied fields change; an explicit `null` clears a field; absent fields keep their stored values. Constraints evaluate on the merged result. Build this from per-field edits with `buildUpdateFields`.", required: true },
    id: { type: "string", description: "Record id (`dsr_…`).", required: true },
    org: { type: "string", description: "Organization slug. Required for cloud direct callers; OSS defaults it.", required: true },
    partition: { type: "string", description: "Data partition. Empty or omitted means `\"default\"`." },
  }}
/>


### UseDatastoreCountOptions

Options for [`useDatastoreCount`](#usedatastorecount).

<TypeTable
  type={{
    query: { type: "string", description: "Text query to filter datastores before counting." },
    scope: { type: "ResourceListScope", description: "Controls which datastores are counted.  - `\"org\"` — only datastores owned by the given organization. - `\"all\"` — includes public/platform datastores." },
  }}
/>


### UseDatastoreListOptions

Options for [`useDatastoreList`](#usedatastorelist).

<TypeTable
  type={{
    page: { type: "number", description: "Page number (1-indexed)." },
    pageSize: { type: "number", description: "Maximum datastores per page." },
    query: { type: "string", description: "Text query to filter datastores by name, description, or tags." },
    scope: { type: "ResourceListScope", description: "Controls which datastores are visible.  - `\"org\"` — only datastores owned by the given organization. - `\"all\"` — includes public/platform datastores." },
  }}
/>


### UseDatastoreSearchOptions

Options for [`useDatastoreSearch`](#usedatastoresearch). Delegates to the shared resource search options.


### UseDatastoreSearchReturn

Return value of [`useDatastoreSearch`](#usedatastoresearch). Delegates to the shared resource search return.


### UseRecordCollectionOptions

Options for [`useRecordCollection`](#userecordcollection).

<TypeTable
  type={{
    collection: { type: "string | null", description: "Collection name. Pass `null` to disable fetching.", required: true },
    columns: { type: "readonly RecordColumnDef[]", description: "Column definitions for the grid, converted to TanStack columns internally." },
    datastore: { type: "string | null", description: "Datastore slug. Pass `null` to disable fetching.", required: true },
    filter: { type: "RecordFilter", description: "Typed AND-only filter in the proto shape (DD-005 grammar). The hook adds no grammar — anything expressible here is exactly what `findRecords` serves (DD-008 invariant 1)." },
    onSortChange: { type: "(sort: SortValue | null) => void", description: "Called when the user changes the sort via a column header click." },
    org: { type: "string | null", description: "Organization slug. Pass `null` to disable fetching.", required: true },
    page: { type: "number", description: "Current page number (1-indexed)." },
    pageSize: { type: "number", description: "Page size, 1–100 (the RPC max — one surface, one limit)." },
    partition: { type: "string", description: "Data partition. Empty or omitted means `\"default\"` (DD-010)." },
    sort: { type: "SortValue | null", description: "Active sort: `id` is a sortable field name (declared non-json field, or system `id`/`created_at`/`updated_at`). `null` keeps the server default (`created_at` desc, `id` tiebreak)." },
  }}
/>


### UseRecordListParams

Query parameters for [`useRecordList`](#userecordlist).

<TypeTable
  type={{
    collection: { type: "string", description: "Collection name.", required: true },
    datastore: { type: "string", description: "Datastore slug.", required: true },
    filter: { type: "RecordFilter", description: "Typed AND-only filter in the proto shape (DD-005 grammar). The hook adds no grammar of its own — anything expressible here is exactly what `findRecords` serves." },
    limit: { type: "number", description: "Page size, 1–100. Zero or omitted applies the RPC default of 25." },
    offset: { type: "number", description: "Row offset for pagination." },
    orderBy: { type: "RecordOrderBy", description: "Sort directive. Omitted keeps the default: `created_at` desc, `id` tiebreak." },
    org: { type: "string", description: "Organization slug. Required for cloud direct callers; OSS defaults it.", required: true },
    partition: { type: "string", description: "Data partition. Empty or omitted means `\"default\"`." },
  }}
/>

