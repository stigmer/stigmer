---
title: Dependency Graph
description: Hooks and components for visualizing agent specification dependencies.
---

{/* Auto-generated by @stigmer/react docs generator. DO NOT EDIT. */}

## Hooks

### useDependencyGraph

Pure transformation hook that converts an `AgentSpec` into a
[`DependencyTree`](#dependencytree) for rendering.

No data fetching, no side effects — the tree is computed entirely
from the spec fields already available via [`useAgent`](/docs/sdk/react/agent#useagent). The
result is memoized and only recomputes when the spec reference
changes.

Returns `null` for `tree` when `spec` is `undefined` (loading state).

**Signature**

```tsx
function useDependencyGraph(options: UseDependencyGraphOptions): UseDependencyGraphReturn
```

<TypeTable
  type={{
    options: { type: "UseDependencyGraphOptions", description: "", required: true },
  }}
/>

**Returns** — `UseDependencyGraphReturn`

<TypeTable
  type={{
    isEmpty: { type: "boolean", description: "`true` when the agent has zero dependencies (or spec is undefined)." },
    tree: { type: "DependencyTree | null", description: "The computed tree, or `null` when spec is undefined." },
  }}
/>

```tsx
const { tree, isEmpty } = useDependencyGraph({
  agentName: agent.metadata.name,
  agentOrg: agent.metadata.org,
  spec: agent.spec,
});

if (!isEmpty && tree) {
  return <DependencyGraph tree={tree} onNodeClick={handleClick} />;
}
```


## Components

### DependencyGraph

Accessible CSS-based tree visualization of an agent's dependencies.

Renders `MCP Servers`, `Skills`, and `Sub-Agents` (with recursive
sub-agent children) as a nested tree with visual connector lines.
The root node represents the agent itself.

Implements the WAI-ARIA TreeView pattern:
- `role="tree"` on the root list
- `role="treeitem"` on each node (via `DependencyTreeNode`)
- Arrow-key navigation between visible nodes (roving tabindex)
- `Home`/`End` to jump to first/last visible node

All visual properties flow through `--stgm-*` design tokens.
Zero Console dependencies — safe for platform builder embedding.

**Props** — `DependencyGraphProps`

<TypeTable
  type={{
    className: { type: "string", description: "Additional CSS classes for the root container." },
    defaultExpanded: { type: "boolean", description: "Whether sub-agent subtrees start expanded." },
    onNodeClick: { type: "(node: DependencyNode) => void", description: "Called when a navigable node is clicked. The `node.ref` field contains the `org` and `slug` for routing. Only fired for nodes where `ref` is defined (MCP servers, skills)." },
    tree: { type: "DependencyTree", description: "The dependency tree to render.", required: true, typeDescriptionLink: "#dependencytree" },
  }}
/>

```tsx
const { tree } = useDependencyGraph({
  agentName: "pr-review-agent",
  agentOrg: "acme",
  spec: agent.spec,
});

{tree && (
  <DependencyGraph
    tree={tree}
    onNodeClick={(node) => {
      if (node.kind === "mcp-server") navigateTo(`/mcp-servers/${node.ref!.slug}`);
    }}
  />
)}
```


## Types

### DependencyNode

A single node in the agent dependency tree.

The tree is recursive: sub-agent nodes contain their own children
(MCP servers and skills they access). Edges are implicit in the
parent-child relationship — no separate edge type is needed.

Nodes are navigable when `ref` is defined (MCP servers, skills).
Sub-agent nodes have no `ref` because they are inline definitions
within the agent spec, not standalone resources.

<TypeTable
  type={{
    children: { type: "readonly DependencyNode[]", description: "Child nodes. Empty array for leaf nodes (MCP servers, skills).", required: true },
    description: { type: "string", description: "Short description (sub-agent description, if available)." },
    id: { type: "string", description: "Deterministic identifier. Format: `{kind}:{slug}` for referenced resources, `sub-agent:{name}` for sub-agents, or `agent:{slug}` for the root.", required: true },
    kind: { type: "NodeKind", description: "What kind of resource this node represents.", required: true, typeDescriptionLink: "#nodekind" },
    label: { type: "string", description: "Primary display label (slug for resources, name for sub-agents).", required: true },
    metadata: { type: "Readonly<Record<string, string>>", description: "Key-value metadata displayed as secondary info. Examples: `{ tools: \"3 tools\" }`, `{ model: \"gpt-4\" }`." },
    qualifiedLabel: { type: "string", description: "Qualified label shown when the resource belongs to a different org than the agent (e.g., `\"other-org/shared-server\"`). Undefined when the resource is in the same org as the parent agent." },
    ref: { type: "{ org: string; slug: string }", description: "Navigation reference for clickable nodes. Defined for MCP servers and skills (standalone resources). Undefined for sub-agents (inline definitions) and the root agent." },
  }}
/>


### DependencyTree

Complete dependency tree for an agent, rooted at the agent itself.

<TypeTable
  type={{
    nodeCount: { type: "number", description: "Total number of nodes in the tree (including root).", required: true },
    root: { type: "DependencyNode", description: "The agent node with all dependencies as descendants.", required: true, typeDescriptionLink: "#dependencynode" },
  }}
/>


### NodeKind

Resource kind in a dependency tree.

- `"agent"` — the root agent (always the tree root)
- `"mcp-server"` — an MCP server referenced via `mcpServerUsages` or `mcpAccess`
- `"skill"` — a skill referenced via `skillRefs`
- `"sub-agent"` — an inline sub-agent definition with its own dependencies


### UseDependencyGraphOptions

Options for [`useDependencyGraph`](#usedependencygraph).

<TypeTable
  type={{
    agentName: { type: "string", description: "Display name of the agent (used as the root node label).", required: true },
    agentOrg: { type: "string", description: "Organization slug of the agent (used for cross-org label detection).", required: true },
    spec: { type: "{ mcpServerUsages: readonly { enabledTools: readonly string[]; mcpServerRef?: { org: string; slug: string }; toolApprovalOverrides: readonly unknown[] }[]; skillRefs: readonly { org: string; slug: string }[]; subAgents: readonly { description: string; mcpAccess: readonly { enabledTools: readonly string[]; mcpServer: string }[]; modelOverride: string; name: string; skillRefs: readonly { org: string; slug: string }[] }[] } | undefined", description: "The agent's spec containing dependency references. Pass `undefined` while the agent is loading — the hook returns `null` for `tree` and `true` for `isEmpty`.", required: true },
  }}
/>

