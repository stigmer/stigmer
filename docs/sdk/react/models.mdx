---
title: Models
description: Hooks and components for model registry data and selector.
---

{/* Auto-generated by @stigmer/react docs generator. DO NOT EDIT. */}

## Hooks

### useModelRegistry

Data hook that exposes the platform model registry with grouping
and lookup helpers.

Pure data layer — no rendering, no side effects. Platform builders
who want full control over rendering import this hook and build
their own UI.

**Signature**

```tsx
function useModelRegistry(): UseModelRegistryReturn
```

**Returns** — `UseModelRegistryReturn`

<TypeTable
  type={{
    byProvider: { type: "ReadonlyMap<Provider, readonly ModelInfo[]>", description: "" },
    defaultModel: { type: "ModelInfo", description: "", typeDescriptionLink: "#modelinfo" },
    getModel: { type: "(modelId: string) => ModelInfo | undefined", description: "" },
    models: { type: "readonly ModelInfo[]", description: "" },
    providers: { type: "readonly Provider[]", description: "" },
  }}
/>


## Components

### ModelSelector

Theme-able model picker built on `@base-ui/react` Select for
accessible keyboard navigation and ARIA.

Consumes [`useModelRegistry`](#usemodelregistry) internally. Groups models by
provider and shows a subtle cost-tier indicator.

All visual properties flow through `--stgm-*` tokens — no
hardcoded colors or sizes.

Platform builders who need different rendering use
`useModelRegistry()` directly.

<ComponentPreview component="ModelSelector" />

**Props** — `ModelSelectorProps`

<TypeTable
  type={{
    className: { type: "string", description: "" },
    disabled: { type: "boolean", description: "When true, disables the selector." },
    onValueChange: { type: "(modelId: string) => void", description: "", required: true },
    value: { type: "string", description: "" },
  }}
/>


## Types

### CostTier

Model registry — UI-relevant metadata for all platform-supported LLM models.

Ported from the Python source of truth at
backend/libs/python/graphton/src/graphton/core/model_registry.py.

This is a static, hardcoded list. A future backend RPC will replace it
with a dynamic query; when that happens consumers will only need to
swap from the static constant to a fetched result — the shape stays
the same.


### DEFAULT_MODEL_ID


### MODEL_REGISTRY


### ModelInfo

<TypeTable
  type={{
    costTier: { type: "CostTier", description: "", required: true, typeDescriptionLink: "#costtier" },
    displayName: { type: "string", description: "", required: true },
    modelId: { type: "string", description: "", required: true },
    provider: { type: "Provider", description: "", required: true, typeDescriptionLink: "#provider" },
  }}
/>

### Provider

