---
title: Models
description: Hooks and components for model registry data and selector.
---

{/* Auto-generated by @stigmer/react docs generator. DO NOT EDIT. */}

## Hooks

### useModelRegistry

Data hook that exposes the platform model registry with grouping
and lookup helpers.

Pure data layer — no rendering, no side effects. Platform builders
who want full control over rendering import this hook and build
their own UI.

The model data is fetched from the public model registry API by
[`StigmerProvider`](/docs/sdk/react/core#stigmerprovider) and cached in context. During loading,
`isLoading` is `true` and `models` is empty.

**Modes:**
- `options.harness === "cursor"` — Cursor-harness models only
- `options.harness === "native"` — native models, excluding disabled providers
- `options.harness` omitted — unified: both harnesses, excluding disabled providers

**Signature**

```tsx
function useModelRegistry(options?: UseModelRegistryOptions): UseModelRegistryReturn
```

<TypeTable
  type={{
    options: { type: "UseModelRegistryOptions", description: "" },
  }}
/>

**Returns** — `UseModelRegistryReturn`

<TypeTable
  type={{
    byProvider: { type: "ReadonlyMap<Provider, readonly ModelInfo[]>", description: "Models grouped by provider for sectioned rendering." },
    defaultModel: { type: "ModelInfo | undefined", description: "The platform default model for the current mode. `undefined` while the registry is loading." },
    error: { type: "Error | null", description: "Non-null if the API fetch failed. Models will be empty in this case." },
    featured: { type: "readonly ModelInfo[]", description: "Curated subset of models marked as `featured` in the registry." },
    getByKey: { type: "(key: string) => ModelInfo | undefined", description: "Look up a model by its compound key (`\"native/claude-sonnet-4.6\"`). Always unambiguous, even in unified mode." },
    getModel: { type: "(modelId: string) => ModelInfo | undefined", description: "Look up a single model by its `modelId`.  In unified mode the same modelId can exist under both harnesses (e.g. `claude-sonnet-4.5` on native and `claude-4.5-sonnet` on cursor). This returns the first match. Use getByKey with a compound key for an unambiguous lookup." },
    isLoading: { type: "boolean", description: "`true` while the model registry is being fetched from the API." },
    models: { type: "readonly ModelInfo[]", description: "All enabled models for the selected mode." },
    providers: { type: "readonly Provider[]", description: "Ordered list of enabled provider identifiers." },
    refetch: { type: "() => void", description: "Retry fetching the model registry. No-op while a fetch is in flight." },
    visionLimits: { type: "VisionLimits | undefined", description: "Document-level vision byte budget advertised by the registry (stigmer/stigmer#365). `undefined` while loading or when the server predates the `limits` block — treat as unassessed and stay silent." },
  }}
/>

```tsx
// Unified mode — flat picker with all models
const { featured, models, getByKey, isLoading } = useModelRegistry();

// Legacy single-harness mode
const { models, defaultModel } = useModelRegistry({ harness: "native" });
```


## Components

### HarnessSelector

Compact segmented control for choosing the session execution engine.

Renders two mutually exclusive options — "Stigmer" (native) and
"Cursor" (premium) — as adjacent pill segments. The Cursor segment
carries a subtle premium tier indicator.

Built as a `radiogroup` with full arrow-key navigation and ARIA
semantics. All visual properties flow through `--stgm-*` tokens.

Platform builders who need different rendering use
[`HarnessOption`](#harnessoption) and [`HARNESS_LABELS`](#harness_labels) directly.

**Props** — `HarnessSelectorProps`

<TypeTable
  type={{
    className: { type: "string", description: "Additional CSS class names for the root container." },
    disabled: { type: "boolean", description: "When true, disables the selector." },
    onValueChange: { type: "(harness: HarnessOption) => void", description: "Called when the user picks a different harness.", required: true },
    value: { type: "HarnessOption", description: "Currently selected harness.", required: true, typeDescriptionLink: "#harnessoption" },
  }}
/>

```tsx
function LauncherToolbar() {
  const [harness, setHarness] = useState<HarnessOption>("native");

  return <HarnessSelector value={harness} onValueChange={setHarness} />;
}
```


### ModelSelector

Combined harness + model picker with a compact trigger button.

Shows a harness dropdown at the top of the popover (when not locked
to a single harness), followed by a curated model list scoped to
the selected harness. Supports search and progressive disclosure
via "Show All Models."

The trigger button displays the current selection in compact format:
`Harness · Model Name ▾` (or just `Model Name ▾` when harness is locked).

**Service tier (#357).** When the consumer opts in via
`ModelSelectorProps.onServiceTierChange` and the selected model
prices a fast variant, a "Fast tier" switch renders in the options area
at the TOP of the popover — the first thing visible on open, mirroring
Cursor's own options panel (Jakob's Law: our users already know that
layout). Deliberately not a per-row affordance: row-level chips crowd
the rows and collide with the "Fast/Fastest" speed badge, and an
interactive element inside `role="option"` is an ARIA authoring error
(option children are presentational). An active fast tier persists
across switches between fast-capable models — the visible badge and
switch keep the state honest — and resets only for models with no fast
variant, where it would be refused at create time.

**Props** — `ModelSelectorProps`

<TypeTable
  type={{
    availableHarnesses: { type: "readonly HarnessOption[]", description: "Restrict which harnesses appear in the dropdown. When omitted, shows all registered harnesses that have models in the registry." },
    className: { type: "string", description: "Additional CSS class names for the trigger button." },
    compact: { type: "boolean", description: "Compact mode: smaller trigger, no descriptions. Default: false." },
    curatedModels: { type: "readonly string[]", description: "Override the curated (featured) list for the current harness." },
    disabled: { type: "boolean", description: "When true, disables the selector." },
    groupBy: { type: "\"provider\" | \"none\" | \"tier\"", description: "Grouping in the \"Show All\" expanded view. Default: \"provider\"." },
    harness: { type: "HarnessOption", description: "Current harness. When provided as a single value, locks the selector to that harness (dropdown hidden). When omitted, shows the harness dropdown.", typeDescriptionLink: "#harnessoption" },
    initialHarness: { type: "HarnessOption", description: "Initial harness value for the internal state when `harness` prop is undefined (unlocked mode). Prevents desync when the parent knows the active harness but delegates the dropdown to this component.  When `harness` is provided (locked mode), this prop is ignored.", typeDescriptionLink: "#harnessoption" },
    onHarnessChange: { type: "(harness: HarnessOption) => void", description: "Called when user changes harness in the dropdown." },
    onHarnessResolved: { type: "(harness: HarnessOption) => void", description: "" },
    onServiceTierChange: { type: "(tier: ServiceTierOption) => void", description: "Called when the user toggles the fast tier. Providing this enables the \"Fast tier\" switch in the popover's options area (top of the popover, beside the harness row — the Cursor options-panel convention), which renders ONLY while the selected model prices a fast variant (ModelInfo.serviceTiers) — no dead controls.  An active fast tier persists across switches between fast-capable models (the tier is always visible: trigger badge + options switch), so \"fast, but on that other model\" is one action, not a re-toggle. Selecting a model without a fast tier resets to \"standard\" through this callback — an unsupported combination can never be submitted (#357, refused at create time)." },
    onValueChange: { type: "(modelId: string) => void", description: "Called when the user picks a different model. Receives the `modelId`.", required: true },
    placeholderLabel: { type: "string", description: "Trigger label shown while `value` is empty, instead of falling back to the registry's default model. Lets a form distinguish \"nothing pinned — the platform default applies\" from an actual selection (the schedule form's contract: an unset model inherits the surface's platform default). The harness prefix is hidden while the placeholder shows, since no engine is pinned either." },
    serviceTier: { type: "ServiceTierOption", description: "Current service tier for the selected model (stigmer/stigmer#357). Defaults to \"standard\". Only meaningful together with onServiceTierChange." },
    showDescriptions: { type: "boolean", description: "Show short descriptions in curated view. Default: true." },
    showSpeedBadge: { type: "boolean", description: "Show speed tier badge. Default: true." },
    value: { type: "string", description: "Currently selected model ID." },
  }}
/>

```tsx
<ModelSelector
  value={modelId}
  onValueChange={setModelId}
  harness={harness}
  onHarnessChange={setHarness}
/>
```


## Types

### CostTier

Pricing bracket for a model.

- `economy` — lowest-cost models suitable for high-volume or latency-sensitive tasks
- `standard` — balanced cost-to-capability, the default choice for most workloads
- `premium` — highest-capability models for complex reasoning or critical tasks


### DEFAULT_CURSOR_MODEL_ID

Model ID used when the Cursor harness is selected and no user preference is set.


### DEFAULT_HARNESS

Platform default — resolves to the native engine.


### DEFAULT_MODEL_ID

Model ID used when no user preference is set (native harness).


### DISABLED_PROVIDERS

Providers whose models should be hidden from the UI.

The model entries themselves stay in the registry so backend
compatibility is preserved. The useModelRegistry hook filters
them out before anything reaches a component.

To re-enable a provider, simply remove it from this set.


### fetchModelRegistry

Fetch the model registry from the authenticated API endpoint.

Thin compatibility wrapper over [`fetchModelRegistryDocument`](#fetchmodelregistrydocument) —
prefer the document fetch when the caller also needs document-level
metadata such as [`VisionLimits`](#visionlimits).

```tsx
function fetchModelRegistry(apiUrl: string, token: string | null, customFetch?: (input: URL | RequestInfo, init?: RequestInit) => Promise<Response>): Promise<ModelInfo[]>
```


### fetchModelRegistryDocument

Fetch the model-registry document from the authenticated API endpoint.

```tsx
function fetchModelRegistryDocument(apiUrl: string, token: string | null, customFetch?: (input: URL | RequestInfo, init?: RequestInit) => Promise<Response>): Promise<ModelRegistryDocument>
```


### fromProtoHarness

Convert a proto `Harness` enum to a [`HarnessOption`](#harnessoption) string.

`UNSPECIFIED` and any unknown values map to `"native"`.

```tsx
function fromProtoHarness(h: Harness): HarnessOption
```


### HARNESS_LABELS

User-facing labels for each harness option.


### HarnessOption

String literal alias for the proto `Harness` enum.

Used on component props and hook options so platform builders
do not need to import proto enums to use the SDK.

This union grows as new execution engines are integrated.
Each value must have a corresponding entry in `HARNESS_META`.


### ModelInfo

UI-relevant metadata for a single platform-supported LLM model.

<TypeTable
  type={{
    costTier: { type: "CostTier", description: "Pricing bracket used for cost-tier indicators in the UI.", required: true, typeDescriptionLink: "#costtier" },
    displayName: { type: "string", description: "Human-readable name shown in the model picker.", required: true },
    featured: { type: "boolean", description: "When `true`, appears in the curated default list (the short view before \"Show All Models\" is expanded or search is used).", required: true },
    harness: { type: "HarnessOption", description: "Which execution engine serves this model.", required: true, typeDescriptionLink: "#harnessoption" },
    modelId: { type: "string", description: "Unique model identifier sent to the backend (e.g. `\"claude-sonnet-4.5\"`).", required: true },
    provider: { type: "Provider", description: "LLM provider that serves this model.", required: true, typeDescriptionLink: "#provider" },
    serviceTiers: { type: "readonly string[]", description: "Pricing-variant keys the registry prices for this model (e.g. `[\"fast\"]`). A priced variant is a *selectable* service tier (stigmer/stigmer#357): the fast-tier switch in the model popover's options area renders only while the selected model's `serviceTiers` includes it. Empty for models with no variants.  Distinct from speedTier, which is a static latency badge, not a selectable option.", required: true },
    shortDescription: { type: "string", description: "3-6 word pitch explaining why to pick this model. Shown in the curated view.", required: true },
    speedTier: { type: "SpeedTier", description: "Latency characteristic shown as a badge (Fastest / Fast / Balanced / Slow).", required: true },
    visionCapability: { type: "boolean", description: "Tri-state vision capability from the registry's `capabilities` block (stigmer/stigmer#386). The registry serializes `capabilities` only for models whose capabilities were actually assessed, so:  - `true` — images sent with a turn reach the model - `false` — explicitly assessed as blind; warn at selection/send time - `undefined` — never assessed; **stay silent, never treat as false**  Mirrors the runner's `parseVisionCapability` convention (backend/services/runner/src/shared/model-registry.ts) — both sides parse the same document and must agree on what absence means." },
  }}
/>

```tsx
const { getModel, defaultModel } = useModelRegistry();
const model = getModel(selectedId) ?? defaultModel;

<span>{model.displayName} ({model.costTier})</span>
```


### modelKey

Build a compound key that uniquely identifies a model across harnesses.

The same underlying model name (e.g. `claude-4.6-sonnet`) can exist in
both native and cursor harnesses. The compound key disambiguates.

```tsx
function modelKey(harness: HarnessOption, modelId: string): string
```


### ModelRegistryContext

React context that holds the model registry fetched from the public API.

Populated by [`StigmerProvider`](/docs/sdk/react/core#stigmerprovider) on mount. Consumer hooks read from
this context instead of a static JSON import, enabling always-fresh
model data without npm package updates.


### ModelRegistryDocument

Parsed model-registry document: the per-model list plus document-level
platform metadata. `visionLimits` is `undefined` when the served document
predates the `limits` block (older servers) — consumers must stay silent
rather than assume a budget.

<TypeTable
  type={{
    models: { type: "readonly ModelInfo[]", description: "", required: true },
    visionLimits: { type: "VisionLimits", description: "", typeDescriptionLink: "#visionlimits" },
  }}
/>


### ModelRegistryState

Internal state held by the model registry context provider.

<TypeTable
  type={{
    error: { type: "Error | null", description: "", required: true },
    isLoading: { type: "boolean", description: "", required: true },
    models: { type: "readonly ModelInfo[]", description: "", required: true },
    refetch: { type: "() => void", description: "Retry fetching the model registry. No-op while a fetch is in flight.", required: true },
    visionLimits: { type: "VisionLimits", description: "Document-level vision byte budget (stigmer/stigmer#365). `undefined` while loading or when the served document predates the `limits` block — consumers stay silent rather than assume a budget.", typeDescriptionLink: "#visionlimits" },
  }}
/>


### ParsedModelKey

Parsed result of a compound `harness/modelId` key.

<TypeTable
  type={{
    harness: { type: "HarnessOption", description: "Harness portion of the compound key.", required: true, typeDescriptionLink: "#harnessoption" },
    modelId: { type: "string", description: "Model ID portion of the compound key.", required: true },
  }}
/>


### parseModelKey

Parse a compound key back into its `(harness, modelId)` parts.
Returns `undefined` for malformed keys.

```tsx
function parseModelKey(key: string): ParsedModelKey | undefined
```


### parseRegistryDocument

Parse a raw registry document (from the API or a static file) into the
per-model list plus document-level metadata.

Expects the shape `{ models: RegistryJsonEntry[], limits?: {...} }`.
Filters out comment entries and invalid rows. Unknown top-level fields
are ignored (the document contract is additive).

```tsx
function parseRegistryDocument(data: unknown): ModelRegistryDocument
```


### parseRegistryJson

Parse raw registry JSON into `ModelInfo[]`.

Thin compatibility wrapper over [`parseRegistryDocument`](#parseregistrydocument) — prefer
the document parser when the caller also needs document-level metadata
such as [`VisionLimits`](#visionlimits).

```tsx
function parseRegistryJson(data: unknown): ModelInfo[]
```


### Provider

LLM provider identifier. Each provider maps to a distinct inference
backend (or intermediary, in the case of Cursor-served third-party
models). The model registry uses this for grouping in the
"Show All" expanded view.


### toProtoHarness

Convert a [`HarnessOption`](#harnessoption) string to the proto `Harness` enum.

```tsx
function toProtoHarness(h: HarnessOption): Harness
```


### UseModelRegistryOptions

Options for [`useModelRegistry`](#usemodelregistry).

<TypeTable
  type={{
    harness: { type: "HarnessOption", description: "Restrict to a single harness.  - `\"cursor\"` — only Cursor-harness models, default → DEFAULT_CURSOR_MODEL_ID - `\"native\"` — only native-harness models, filtered by DISABLED_PROVIDERS - omitted — **unified mode**: all enabled models from both harnesses", typeDescriptionLink: "#harnessoption" },
  }}
/>


### VisionLimits

Document-level vision byte budget advertised by the registry
(stigmer/stigmer#365) — the runner's inline-image delivery caps, exposed
so clients can warn *before* an upload the runner would degrade to
"not viewable inline". Field names match the runner's `VisionBudget`
options verbatim (backend/services/runner/src/shared/attachment-vision.ts).

ADVERTISED == ENFORCED: the serving side (cloud
`ModelRegistryDocumentCodec`) and the runner pin the same literal values
in their test suites. Treat an absent block as unassessed — no warnings —
exactly like the per-model `capabilities` tri-state.

<TypeTable
  type={{
    maxImageBytes: { type: "number", description: "Per-image raw byte cap for inline vision delivery.", required: true },
    maxImages: { type: "number", description: "Per-turn inline image count cap.", required: true },
    maxTotalBytes: { type: "number", description: "Per-turn total byte cap across all inline images.", required: true },
  }}
/>

