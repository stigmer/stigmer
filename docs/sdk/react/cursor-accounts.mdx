---
title: cursor-accounts
---

{/* Auto-generated by @stigmer/react docs generator. DO NOT EDIT. */}

## Hooks

### useCursorAccounts

Data hook that lists every managed Cursor account with its routing and
sync summary (enabled member key count, last-synced timestamp). Key
material is always redacted server-side.

Platform-operator surface: the caller needs
`can_manage_cursor_accounts` on `platform:stigmer`.

Pass `enabled: false` to skip fetching (stable no-op).

**Signature**

```tsx
function useCursorAccounts(options?: { enabled?: boolean }): UseCursorAccountsReturn
```

<TypeTable
  type={{
    options: { type: "{ enabled?: boolean }", description: "" },
  }}
/>

**Returns** — `UseCursorAccountsReturn`

<TypeTable
  type={{
    accounts: { type: "CursorAccountsResponse | null", description: "The account summaries, or `null` before the first successful fetch." },
    error: { type: "Error | null", description: "Error from the last failed request, or `null` when healthy." },
    isLoading: { type: "boolean", description: "`true` while the initial fetch is in flight." },
    isRefetching: { type: "boolean", description: "`true` while a background refetch is in flight and stale data is shown." },
    refetch: { type: "() => void", description: "Discard cached data and re-fetch from the server." },
  }}
/>

```tsx
const { accounts, isLoading, refetch } = useCursorAccounts();
```


### useCursorAccountView

Data hook for one Cursor account's detail view: the redacted account,
the latest roster/spend snapshot, and the server-computed coverage
join (per-key owner state and cycle spend, active members without
keys).

Pass `null` as `accountId` to skip fetching (e.g. while the console is
on the list phase).

**Signature**

```tsx
function useCursorAccountView(accountId: string | null): UseCursorAccountViewReturn
```

<TypeTable
  type={{
    accountId: { type: "string | null", description: "", required: true },
  }}
/>

**Returns** — `UseCursorAccountViewReturn`

<TypeTable
  type={{
    error: { type: "Error | null", description: "Error from the last failed request, or `null` when healthy." },
    isLoading: { type: "boolean", description: "`true` while the initial fetch is in flight." },
    isRefetching: { type: "boolean", description: "`true` while a background refetch is in flight and stale data is shown." },
    refetch: { type: "() => void", description: "Discard cached data and re-fetch from the server." },
    view: { type: "CursorAccountView | null", description: "The detail view, or `null` before the first successful fetch." },
  }}
/>

```tsx
const { view, isLoading, refetch } = useCursorAccountView(accountId);
```


### useCursorMemberKeyActions

Behavior hook bundling the member-key mutations of one Cursor account —
add, remove, enable/disable. One hook because the console's key panel
performs all three and they share submit/error surfaces.

Rotation is deliberately remove + add (no in-place key swap), so the
server-side `/v1/me` identity binding can never go stale.

**Signature**

```tsx
function useCursorMemberKeyActions(): UseCursorMemberKeyActionsReturn
```

**Returns** — `UseCursorMemberKeyActionsReturn`

<TypeTable
  type={{
    addKey: { type: "(input: AddCursorMemberKeyInput) => Promise<CursorAccount>", description: "Add one execution-capable member key. Returns the redacted account." },
    clearError: { type: "() => void", description: "Reset `error` to `null`." },
    error: { type: "Error | null", description: "Error from the last failed attempt, or `null` when healthy." },
    isSubmitting: { type: "boolean", description: "`true` while any key mutation is in flight." },
    removeKey: { type: "(input: RemoveCursorMemberKeyInput) => Promise<CursorAccount>", description: "Remove one member key (live-pin guarded)." },
    setKeyEnabled: { type: "(accountId: string, keyId: string, enabled: boolean) => Promise<CursorAccount>", description: "Enable/disable one key for NEW-session selection (pins unaffected)." },
  }}
/>


### useDeleteCursorAccount

Behavior hook for deleting a managed Cursor account. The live-pin
guard's FAILED_PRECONDITION message carries the pinned session count —
surface it verbatim so the operator can choose disable-instead.

**Signature**

```tsx
function useDeleteCursorAccount(): UseDeleteCursorAccountReturn
```

**Returns** — `UseDeleteCursorAccountReturn`

<TypeTable
  type={{
    clearError: { type: "() => void", description: "Reset `error` to `null`." },
    error: { type: "Error | null", description: "Error from the last failed attempt, or `null` when healthy." },
    isSubmitting: { type: "boolean", description: "`true` while a delete is in flight." },
    remove: { type: "(input: DeleteCursorAccountInput) => Promise<CursorAccount>", description: "Delete the account (plus its snapshots and session pins)." },
  }}
/>


### useSyncCursorAccount

Behavior hook for the console's "Sync now" — the same server-side sync
the hourly schedule runs, on demand.

**Signature**

```tsx
function useSyncCursorAccount(): UseSyncCursorAccountReturn
```

**Returns** — `UseSyncCursorAccountReturn`

<TypeTable
  type={{
    clearError: { type: "() => void", description: "Reset `error` to `null`." },
    error: { type: "Error | null", description: "Error from the last failed attempt, or `null` when healthy." },
    isSyncing: { type: "boolean", description: "`true` while a sync is in flight." },
    sync: { type: "(accountId: string) => Promise<CursorAccountView>", description: "Run an on-demand roster + spend sync and return the refreshed view. The Cursor Admin API is rate-limited (20 req/min, poll-hourly guidance) — `isSyncing` should disable the trigger while in flight, and callers should not poll this." },
  }}
/>


### useUpsertCursorAccount

Behavior hook for creating or updating a managed Cursor account.

The parent should refetch its list/detail view after a successful
save — org assignments affect key selection within the proxy's cache
TTL (~5 minutes).

**Signature**

```tsx
function useUpsertCursorAccount(): UseUpsertCursorAccountReturn
```

**Returns** — `UseUpsertCursorAccountReturn`

<TypeTable
  type={{
    clearError: { type: "() => void", description: "Reset `error` to `null`." },
    error: { type: "Error | null", description: "Error from the last failed attempt, or `null` when healthy." },
    isSubmitting: { type: "boolean", description: "`true` while a save is in flight." },
    upsert: { type: "(account: CursorAccount) => Promise<CursorAccount>", description: "Create (empty `accountId`) or update a Cursor account. The admin key is plaintext to set/rotate or the `***REDACTED***` marker to keep the stored value; the server validates new keys live against Cursor's `/teams/members` before persisting. Returns the redacted account." },
  }}
/>


## Components

### CursorAccountEditor

Create/edit form for one Cursor account: identity, team Admin API key,
enablement, platform-default flag, and org assignments.

Admin-key semantics mirror the server contract: on edit the field
shows the redaction marker and submitting it unchanged keeps the
stored key; typing anything else rotates it (validated live against
Cursor before persisting). Member keys are NOT edited here — they have
their own panel with per-key add/remove/enable.

**Props** — `CursorAccountEditorProps`

<TypeTable
  type={{
    className: { type: "string", description: "Additional CSS class names." },
    initial: { type: "CursorAccount | null", description: "The account to edit, or `null` to create a new one.", required: true },
    isSubmitting: { type: "boolean", description: "`true` while the save is in flight.", required: true },
    onCancel: { type: "() => void", description: "Called when the operator cancels.", required: true },
    onSubmit: { type: "(account: CursorAccount) => void", description: "Called with the assembled account on submit.", required: true },
    submitError: { type: "Error | null", description: "Error from the last failed save, or `null`.", required: true },
  }}
/>


### CursorAccountsAccessNotice

The access-denied state for the Cursor-accounts operator surface.
Server-side authorization (`can_manage_cursor_accounts` on
`platform:stigmer`) is the real gate — this is the honest, designed
face of that denial for anyone who navigates to the operator route
without the permission, instead of a raw RPC error string.


### CursorAccountsConsole

The platform-operator console for managed Cursor accounts: the Cursor
teams (admin keys, member execution keys, org assignments) that back
the cursor harness, with roster coverage and per-member cycle spend
from the hourly sync.

- **List** — every account with routability at a glance (an account
  with zero enabled member keys cannot serve executions).
- **Detail** — org assignments, the member-key panel (add / disable /
  remove, each key joined with its owner's roster state and spend),
  coverage gaps (active members without keys), and "Sync now".

Requires `can_manage_cursor_accounts` on `platform:stigmer` —
non-operators see the designed access notice. Key material never
reaches this component; the server redacts it on every read.

**Props** — `CursorAccountsConsoleProps`

<TypeTable
  type={{
    className: { type: "string", description: "Additional CSS class names." },
  }}
/>

```tsx
<CursorAccountsConsole />
```


## Types

### AddCursorMemberKeyInput

Input for adding one member key.

<TypeTable
  type={{
    accountId: { type: "string", description: "", required: true },
    apiKey: { type: "string", description: "Plaintext user-scoped Cursor API key. The server identifies it via Cursor's `/v1/me` and binds it to its owning team member; admin and service-account keys are rejected with Cursor's own explanation — surface the error verbatim.", required: true },
    label: { type: "string", description: "Optional operator label." },
  }}
/>


### DeleteCursorAccountInput

Input for [`useDeleteCursorAccount`](#usedeletecursoraccount).

<TypeTable
  type={{
    accountId: { type: "string", description: "", required: true },
    force: { type: "boolean", description: "Deletion is refused (FAILED_PRECONDITION) while live sessions are pinned to the account's keys. `force: true` overrides — their Cursor agent handles will be orphaned at disposal. Prefer disabling the account, which is always safe." },
  }}
/>


### RemoveCursorMemberKeyInput

Input for removing one member key.

<TypeTable
  type={{
    accountId: { type: "string", description: "", required: true },
    force: { type: "boolean", description: "Removal is refused while live sessions are pinned to this key. `force: true` overrides; prefer disabling (always safe)." },
    keyId: { type: "string", description: "", required: true },
  }}
/>

