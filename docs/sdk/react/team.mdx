---
title: team
---

{/* Auto-generated by @stigmer/react docs generator. DO NOT EDIT. */}

## Hooks

### useCreateTeam

Mutation hook that creates a team.

**Signature**

```tsx
function useCreateTeam(): UseCreateTeamReturn
```

**Returns** — `UseCreateTeamReturn`

<TypeTable
  type={{
    clearError: { type: "() => void", description: "Reset `error` to `null`." },
    create: { type: "(input: TeamInput) => Promise<Team>", description: "Create a team. Resolves with the server-created resource." },
    error: { type: "Error | null", description: "Error from the last failed call, or `null` when healthy." },
    isCreating: { type: "boolean", description: "`true` while the create request is in flight." },
  }}
/>

```tsx
const { create, isCreating, error } = useCreateTeam();
await create({ name: "Site Reliability", org: orgSlug, description: "On call" });
```


### useDeleteTeam

Mutation hook that deletes a team.

**Signature**

```tsx
function useDeleteTeam(): UseDeleteTeamReturn
```

**Returns** — `UseDeleteTeamReturn`

<TypeTable
  type={{
    clearError: { type: "() => void", description: "Reset `error` to `null`." },
    deleteTeam: { type: "(input: DeleteResourceInput) => Promise<Team>", description: "Delete a team, its memberships and every grant made to it." },
    error: { type: "Error | null", description: "Error from the last failed call, or `null` when healthy." },
    isDeleting: { type: "boolean", description: "`true` while the delete request is in flight." },
  }}
/>

```tsx
const { deleteTeam } = useDeleteTeam();
await deleteTeam({ resourceId: team.metadata?.id ?? "" });
```


### useTeam

Data hook that fetches a single team by id.

Pass `null` to skip fetching (stable no-op).

**Signature**

```tsx
function useTeam(id: string | null): UseTeamReturn
```

<TypeTable
  type={{
    id: { type: "string | null", description: "", required: true },
  }}
/>

**Returns** — `UseTeamReturn`

<TypeTable
  type={{
    error: { type: "Error | null", description: "Error from the last failed request, or `null` when healthy." },
    isLoading: { type: "boolean", description: "`true` while the initial fetch is in flight." },
    isRefetching: { type: "boolean", description: "`true` while a background refetch is in flight and stale data is shown." },
    refetch: { type: "() => void", description: "Discard cached data and re-fetch from the server." },
    team: { type: "Team | null", description: "The fetched team, or `null` while loading or on error." },
  }}
/>

```tsx
const { team, isLoading, error } = useTeam(teamId);
```


### useTeamList

Data hook that lists an organization's teams.

Pass `null` to skip fetching (stable no-op). Call `refetch()` after a
create, update or delete.

**Signature**

```tsx
function useTeamList(org: string | null): UseTeamListReturn
```

<TypeTable
  type={{
    org: { type: "string | null", description: "", required: true },
  }}
/>

**Returns** — `UseTeamListReturn`

<TypeTable
  type={{
    error: { type: "Error | null", description: "Error from the last failed request, or `null` when healthy." },
    isLoading: { type: "boolean", description: "`true` while the initial fetch is in flight." },
    isRefetching: { type: "boolean", description: "`true` while a background refetch is in flight and stale data is shown." },
    refetch: { type: "() => void", description: "Discard cached data and re-fetch from the server." },
    teams: { type: "readonly Team[]", description: "Every team in the organization. Empty while loading or on error." },
  }}
/>

```tsx
const teamsServed = useResourceAvailable(ApiResourceKind.team);
const { teams, isLoading } = useTeamList(teamsServed ? orgSlug : null);
```


### useUpdateTeam

Mutation hook that updates a team. Spread `toTeamUpdateInput(team)` so
the fields you do not edit are kept.

**Signature**

```tsx
function useUpdateTeam(): UseUpdateTeamReturn
```

**Returns** — `UseUpdateTeamReturn`

<TypeTable
  type={{
    clearError: { type: "() => void", description: "Reset `error` to `null`." },
    error: { type: "Error | null", description: "Error from the last failed call, or `null` when healthy." },
    isUpdating: { type: "boolean", description: "`true` while the update request is in flight." },
    update: { type: "(input: TeamInput) => Promise<Team>", description: "Replace a team's name and description." },
  }}
/>

```tsx
const { update } = useUpdateTeam();
await update({ ...toTeamUpdateInput(team), description: next });
```


## Components

### CreateTeamForm

Form that creates a team in an organization.

All visual properties flow through `--stgm-*` design tokens.

**Props** — `CreateTeamFormProps`

<TypeTable
  type={{
    className: { type: "string", description: "" },
    onCancel: { type: "() => void", description: "" },
    onCreated: { type: "(team: Team) => void", description: "Fired with the created team." },
    org: { type: "string", description: "Organization slug the team is created in.", required: true },
  }}
/>


### TeamDetailPanel

Shows and manages one team.

All visual properties flow through `--stgm-*` design tokens.

**Props** — `TeamDetailPanelProps`

<TypeTable
  type={{
    className: { type: "string", description: "" },
    onBack: { type: "() => void", description: "Return to the list." },
    onDeleted: { type: "() => void", description: "Fired after the team is deleted." },
    onUpdated: { type: "(team: Team) => void", description: "Fired with the saved team after an edit." },
    orgId: { type: "string", description: "The organization's id (`metadata.id`), whose members can be added.", required: true },
    team: { type: "Team", description: "", required: true },
  }}
/>


### TeamListPanel

Lists an organization's teams.

All visual properties flow through `--stgm-*` design tokens.

**Props** — `TeamListPanelProps`

<TypeTable
  type={{
    className: { type: "string", description: "Additional CSS class names for the root container." },
    error: { type: "Error | null", description: "", required: true },
    isLoading: { type: "boolean", description: "", required: true },
    onOpen: { type: "(team: Team) => void", description: "Fired when a person opens a team.", required: true },
    teams: { type: "readonly Team[]", description: "", required: true },
  }}
/>

```tsx
const list = useTeamList(orgSlug);
<TeamListPanel {...list} onOpen={(team) => setOpenTeam(team)} />
```


### TeamMembersPanel

Lists a team's members and, for callers who may manage them, adds and
removes people.

All visual properties flow through `--stgm-*` design tokens.

**Props** — `TeamMembersPanelProps`

<TypeTable
  type={{
    className: { type: "string", description: "" },
    orgId: { type: "string", description: "The organization's id (`metadata.id`), whose members can be added.", required: true },
    teamId: { type: "string", description: "The team's id (`metadata.id`).", required: true },
  }}
/>


## Types

### TEAM_DESCRIPTION_MAX_LENGTH

The longest description the contract accepts (`TeamSpec.description`).

