---
title: Billing
description: Hooks and components for subscription plans, payment methods, and invoices.
---

{/* Auto-generated by @stigmer/react docs generator. DO NOT EDIT. */}

## Hooks

### useBillingAccount

Data hook that fetches the billing account for an organization.

Calls `billing.getOrCreateBillingAccount` which is idempotent —
creates the account on first call, returns the existing account
on subsequent calls. The returned `BillingAccount` includes
the embedded `CreditBalance` with available, reserved,
promotional, and purchased breakdowns.

Pass `null` as `orgId` to skip fetching (stable no-op).

**Signature**

```tsx
function useBillingAccount(orgId: string | null): UseBillingAccountReturn
```

<TypeTable
  type={{
    orgId: { type: "string | null", description: "Organization ID, or `null` to skip.", required: true },
  }}
/>

**Returns** — `UseBillingAccountReturn`

<TypeTable
  type={{
    account: { type: "BillingAccount | null", description: "The billing account, or `null` before the first successful fetch." },
    error: { type: "Error | null", description: "Error from the last failed request, or `null` when healthy." },
    isLoading: { type: "boolean", description: "`true` while the initial fetch is in flight." },
    isRefetching: { type: "boolean", description: "`true` while a background refetch is in flight and stale data is shown." },
    refetch: { type: "() => void", description: "Discard cached data and re-fetch from the server." },
  }}
/>

```tsx
const { account, isLoading, error } = useBillingAccount(orgId);

if (isLoading) return <Skeleton />;
if (error) return <ErrorMessage error={error} />;
if (!account) return null;

return <div>Balance: {formatCreditBalance(account.balance?.availableMicros)}</div>;
```


### useBillingUsageReport

Data hook that fetches an aggregated billing usage report for a date range.

Returns total provider cost, total billable amount, execution and LLM
call counts, and a per-model breakdown with cost tier attribution.

Pass `null` as `orgId` to skip fetching (stable no-op).

**Signature**

```tsx
function useBillingUsageReport(orgId: string | null, startTime: Date, endTime: Date): UseBillingUsageReportReturn
```

<TypeTable
  type={{
    orgId: { type: "string | null", description: "Organization ID, or `null` to skip.", required: true },
    startTime: { type: "Date", description: "Start of the reporting period.", required: true },
    endTime: { type: "Date", description: "End of the reporting period.", required: true },
  }}
/>

**Returns** — `UseBillingUsageReportReturn`

<TypeTable
  type={{
    error: { type: "Error | null", description: "Error from the last failed request, or `null` when healthy." },
    isLoading: { type: "boolean", description: "`true` while the initial fetch is in flight." },
    isRefetching: { type: "boolean", description: "`true` while a background refetch is in flight and stale data is shown." },
    refetch: { type: "() => void", description: "Discard cached data and re-fetch from the server." },
    report: { type: "BillingUsageReportResponse | null", description: "The usage report, or `null` before the first successful fetch." },
  }}
/>

```tsx
const { report, isLoading } = useBillingUsageReport(
  orgId,
  new Date("2026-05-01"),
  new Date("2026-05-31"),
);

if (isLoading) return <Skeleton />;
if (!report) return null;

return <div>Total: {formatCreditBalance(report.totalBillableAmountMicros)}</div>;
```


### useCreateBillingPortalSession

Behavior hook that opens the Stripe Customer Portal for payment
method management.

Wraps `billing.createBillingPortalSession` with loading and error
state management. On success, redirects the user to the Stripe-hosted
portal. Payment method changes are synced back via webhooks.

**Signature**

```tsx
function useCreateBillingPortalSession(): UseCreateBillingPortalSessionReturn
```

**Returns** — `UseCreateBillingPortalSessionReturn`

<TypeTable
  type={{
    clearError: { type: "() => void", description: "Reset `error` to `null`." },
    error: { type: "Error | null", description: "Error from the last failed attempt, or `null` when healthy." },
    isLoading: { type: "boolean", description: "`true` while the portal session is being created." },
    openPortal: { type: "(orgId: string) => Promise<void>", description: "Open the Stripe Customer Portal for payment method management.  On success, redirects the user to the Stripe-hosted portal page. The promise resolves before the redirect occurs." },
  }}
/>

```tsx
const { openPortal, isLoading } = useCreateBillingPortalSession();

<button onClick={() => openPortal(orgId)} disabled={isLoading}>
  Manage payment methods
</button>
```


### useCreateCheckoutSession

Behavior hook that creates a Stripe Checkout Session for credit
pack purchases.

Wraps `billing.createCreditCheckoutSession` with loading and error
state management. On success, redirects the user to the Stripe-hosted
checkout page. Credits are provisioned asynchronously via webhook
after payment succeeds.

**Signature**

```tsx
function useCreateCheckoutSession(): UseCreateCheckoutSessionReturn
```

**Returns** — `UseCreateCheckoutSessionReturn`

<TypeTable
  type={{
    clearError: { type: "() => void", description: "Reset `error` to `null`." },
    createSession: { type: "(input: CreateCheckoutSessionInput) => Promise<CreateCreditCheckoutSessionResponse>", description: "Create a Stripe Checkout Session and redirect the user.  On success, sets `window.location.href` to the Stripe-hosted checkout page URL. The promise resolves with the response before the redirect occurs, allowing callers to perform cleanup if needed." },
    error: { type: "Error | null", description: "Error from the last failed attempt, or `null` when healthy." },
    isSubmitting: { type: "boolean", description: "`true` while the checkout session is being created." },
  }}
/>

```tsx
const { createSession, isSubmitting, error } = useCreateCheckoutSession();

const handleBuy = () => {
  createSession({
    orgId,
    packId: "growth",
    successUrl: `${window.location.origin}/settings/billing?checkout=success`,
    cancelUrl: `${window.location.origin}/settings/billing`,
  });
};
```


### useCreditLedger

Data hook that fetches paginated credit ledger entries for an organization.

Calls `billing.getCreditLedger` with optional pagination and type
filtering. Returns the entries and total page count for building
pagination controls.

Pass `null` as `orgId` to skip fetching (stable no-op).

**Signature**

```tsx
function useCreditLedger(orgId: string | null, options: UseCreditLedgerOptions): UseCreditLedgerReturn
```

<TypeTable
  type={{
    orgId: { type: "string | null", description: "Organization ID, or `null` to skip.", required: true },
    options: { type: "UseCreditLedgerOptions", description: "Pagination and filter options.", required: true },
  }}
/>

**Returns** — `UseCreditLedgerReturn`

<TypeTable
  type={{
    error: { type: "Error | null", description: "Error from the last failed request, or `null` when healthy." },
    isLoading: { type: "boolean", description: "`true` while the initial fetch is in flight." },
    isRefetching: { type: "boolean", description: "`true` while a background refetch is in flight and stale data is shown." },
    ledger: { type: "CreditLedgerResponse | null", description: "The ledger response, or `null` before the first successful fetch." },
    refetch: { type: "() => void", description: "Discard cached data and re-fetch from the server." },
  }}
/>

```tsx
const { ledger, isLoading } = useCreditLedger(orgId, { pageNum: 1 });

if (isLoading) return <Skeleton />;
if (!ledger) return null;

return ledger.entries.map(entry => <LedgerRow key={entry.entryId} entry={entry} />);
```


### useCustomerModelPricing

Data hook that fetches the customer-facing model price list.

Returns per-million-token prices for all billable models with
billing policy markup already applied, organized by harness
and cost tier.

Pass `null` as `orgId` to skip fetching (stable no-op).
Pass `undefined` to fetch default pricing (no org override).

**Signature**

```tsx
function useCustomerModelPricing(orgId: string | null | undefined): UseCustomerModelPricingReturn
```

<TypeTable
  type={{
    orgId: { type: "string | null | undefined", description: "Organization ID for org-specific overrides,   `undefined` for default pricing, or `null` to skip.", required: true },
  }}
/>

**Returns** — `UseCustomerModelPricingReturn`

<TypeTable
  type={{
    error: { type: "Error | null", description: "Error from the last failed request, or `null` when healthy." },
    isLoading: { type: "boolean", description: "`true` while the initial fetch is in flight." },
    isRefetching: { type: "boolean", description: "`true` while a background refetch is in flight and stale data is shown." },
    pricing: { type: "CustomerModelPricingResponse | null", description: "The pricing response, or `null` before the first successful fetch." },
    refetch: { type: "() => void", description: "Discard cached data and re-fetch from the server." },
  }}
/>

```tsx
const { pricing, isLoading } = useCustomerModelPricing(orgId);

if (isLoading) return <Skeleton />;
if (!pricing) return null;

return pricing.entries.map(entry => (
  <PricingRow key={entry.modelId} entry={entry} />
));
```


### useDecidePricingOverride

Behavior hook for deciding PENDING_SIGNOFF pricing overrides from the
pricing feedback loop.

Wraps `billing.decideModelPricingOverride` with loading and error
state. On approval the platform recomposes the effective registry
immediately, so the parent should refetch its governance view after a
successful decision.

**Signature**

```tsx
function useDecidePricingOverride(): UseDecidePricingOverrideReturn
```

**Returns** — `UseDecidePricingOverrideReturn`

<TypeTable
  type={{
    clearError: { type: "() => void", description: "Reset `error` to `null`." },
    decide: { type: "(input: DecidePricingOverrideInput) => Promise<ModelPricingOverride>", description: "Decide a pending pricing override. Returns the decided override with the operator's identity stamped." },
    error: { type: "Error | null", description: "Error from the last failed attempt, or `null` when healthy." },
    isSubmitting: { type: "boolean", description: "`true` while a decision is being recorded." },
  }}
/>

```tsx
const { decide, isSubmitting } = useDecidePricingOverride();

await decide({ overrideId, approve: true, decisionNote: "Verified vs ledger" });
refetchGovernance();
```


### useModelGovernanceView

Behavior hook backing the pricing-governance console: joins the model
registry baseline catalog ([`useModelPricingBaselines`](#usemodelpricingbaselines), with
history) and the governance view ([`usePricingGovernance`](#usepricinggovernance)) into
one row per catalog key, and owns the console's search and
list/create/detail flow state.

The two RPCs describe the same models from different angles — the
catalog is what operators author, governance is what the pricing
feedback loop composed on top — so the console renders them as one
surface instead of two parallel tables. Per-variant governance
entries fold into their model's row rather than appearing as
top-level rows.

Platform-operator surface: the caller needs `can_manage_model_pricing`
on `platform:stigmer`; rates are raw provider prices (pre-markup).

**Signature**

```tsx
function useModelGovernanceView(): UseModelGovernanceViewReturn
```

**Returns** — `UseModelGovernanceViewReturn`

<TypeTable
  type={{
    backToDetail: { type: "() => void", description: "Leave edit/retire back to the read-only detail." },
    backToList: { type: "() => void", description: "Return to the model list." },
    error: { type: "Error | null", description: "First error from the underlying fetches, or `null` when healthy." },
    flow: { type: "GovernanceFlow", description: "Current console flow state.", typeDescriptionLink: "#governanceflow" },
    isLoading: { type: "boolean", description: "`true` while either underlying fetch is in its initial load." },
    modelQuery: { type: "string", description: "Current search text over the model list." },
    models: { type: "readonly ModelGovernanceRow[]", description: "Unified model rows, filtered by modelQuery, sorted by display name." },
    openCreate: { type: "() => void", description: "Open the add-model editor." },
    openDetail: { type: "(modelKey: string) => void", description: "Focus one model (read-only detail)." },
    openEdit: { type: "() => void", description: "Switch the focused model into the edit form." },
    openRetire: { type: "() => void", description: "Switch the focused model into the retire confirmation." },
    pendingCount: { type: "number", description: "Unfiltered pending-override count (for the Sign-Offs tab badge)." },
    pendingOverrides: { type: "readonly ModelPricingOverride[]", description: "PENDING_SIGNOFF overrides, filtered by signOffQuery, oldest first as served." },
    refetch: { type: "() => void", description: "Re-fetch both the catalog and the governance view." },
    selected: { type: "ModelGovernanceRow | null", description: "The focused model when flow is in the detail phase, else `null`." },
    setModelQuery: { type: "(query: string) => void", description: "Set the model-list search text." },
    setSignOffQuery: { type: "(query: string) => void", description: "Set the sign-off search text." },
    signOffQuery: { type: "string", description: "Current search text over the sign-off queue." },
  }}
/>

```tsx
const view = useModelGovernanceView();
return view.models.map((row) => <ModelRow key={row.key} row={row} />);
```


### useModelPricingBaselines

Data hook that fetches the model registry baseline catalog: the
operator-governed catalog entries and list prices that (composed with
any ledger-derived overrides) form the effective registry.

Platform-operator surface: the caller needs `can_manage_model_pricing`
on `platform:stigmer`, and the rates are raw provider prices
(pre-markup) — never render them as customer prices.

**Signature**

```tsx
function useModelPricingBaselines(options?: UseModelPricingBaselinesOptions): UseModelPricingBaselinesReturn
```

<TypeTable
  type={{
    options: { type: "UseModelPricingBaselinesOptions", description: "" },
  }}
/>

**Returns** — `UseModelPricingBaselinesReturn`

<TypeTable
  type={{
    baselines: { type: "readonly ModelPricingBaseline[] | null", description: "The catalog entries, or `null` before the first successful fetch." },
    error: { type: "Error | null", description: "Error from the last failed request, or `null` when healthy." },
    isLoading: { type: "boolean", description: "`true` while the initial fetch is in flight." },
    isRefetching: { type: "boolean", description: "`true` while a background refetch is in flight and stale data is shown." },
    refetch: { type: "() => void", description: "Discard cached data and re-fetch from the server." },
  }}
/>

```tsx
const { baselines, isLoading, refetch } = useModelPricingBaselines();
```


### usePricingGovernance

Data hook that fetches the platform pricing governance view: every
model's baseline vs effective rates, the ACTIVE pricing overrides
composed into them (with full ledger provenance), and the
PENDING_SIGNOFF proposals awaiting an operator decision.

Platform-operator surface: the caller needs `can_manage_model_pricing`
on `platform:stigmer`, and the rates are raw provider prices
(pre-markup) — never render them as customer prices.

Pass `enabled: false` to skip fetching (stable no-op) — e.g. while the
caller's operator capability is still resolving.

**Signature**

```tsx
function usePricingGovernance(options?: { enabled?: boolean }): UsePricingGovernanceReturn
```

<TypeTable
  type={{
    options: { type: "{ enabled?: boolean }", description: "" },
  }}
/>

**Returns** — `UsePricingGovernanceReturn`

<TypeTable
  type={{
    error: { type: "Error | null", description: "Error from the last failed request, or `null` when healthy." },
    governance: { type: "ModelPricingGovernanceResponse | null", description: "The governance view, or `null` before the first successful fetch." },
    isLoading: { type: "boolean", description: "`true` while the initial fetch is in flight." },
    isRefetching: { type: "boolean", description: "`true` while a background refetch is in flight and stale data is shown." },
    refetch: { type: "() => void", description: "Discard cached data and re-fetch from the server." },
  }}
/>

```tsx
const { governance, isLoading, refetch } = usePricingGovernance();

if (isLoading) return <Skeleton />;
return <PricingGovernancePanel />;
```


### useRetireModelPricingBaseline

Behavior hook for retiring models from the registry catalog.

Wraps `billing.retireModelPricingBaseline` with loading and error
state. Retirement removes the model from every price surface on the
next composition pass; the document is kept for audit and the key can
be revived by a subsequent upsert. The parent should refetch its
catalog view after a successful retirement.

**Signature**

```tsx
function useRetireModelPricingBaseline(): UseRetireModelPricingBaselineReturn
```

**Returns** — `UseRetireModelPricingBaselineReturn`

<TypeTable
  type={{
    clearError: { type: "() => void", description: "Reset `error` to `null`." },
    error: { type: "Error | null", description: "Error from the last failed attempt, or `null` when healthy." },
    isSubmitting: { type: "boolean", description: "`true` while a retirement is being recorded." },
    retire: { type: "(input: RetireModelPricingBaselineInput) => Promise<ModelPricingBaseline>", description: "Retire a model from the catalog. Returns the RETIRED document." },
  }}
/>


### useSetAutoRechargeConfig

Behavior hook for configuring auto-recharge settings.

Wraps `billing.setAutoRechargeConfig` with loading and error state
management. Returns the updated `BillingAccount` on success so the
parent can refresh its cached data.

**Signature**

```tsx
function useSetAutoRechargeConfig(): UseSetAutoRechargeConfigReturn
```

**Returns** — `UseSetAutoRechargeConfigReturn`

<TypeTable
  type={{
    clearError: { type: "() => void", description: "Reset `error` to `null`." },
    error: { type: "Error | null", description: "Error from the last failed attempt, or `null` when healthy." },
    isSubmitting: { type: "boolean", description: "`true` while the configuration is being saved." },
    setConfig: { type: "(input: SetAutoRechargeConfigInput) => Promise<BillingAccount>", description: "Save auto-recharge configuration. Returns the updated BillingAccount on success." },
  }}
/>

```tsx
const { setConfig, isSubmitting, error } = useSetAutoRechargeConfig();

await setConfig({
  orgId,
  enabled: true,
  thresholdMicros: BigInt(5_000_000),
  rechargeAmountMicros: BigInt(50_000_000),
  monthlyCapMicros: BigInt(200_000_000),
});
```


### useUpsertModelPricingBaseline

Behavior hook for creating or revising model registry baseline entries.

Wraps `billing.upsertModelPricingBaseline` with loading and error
state. The platform recomposes the effective registry immediately, so
the parent should refetch its catalog view after a successful upsert.

A concurrent-edit conflict surfaces as an ABORTED error telling the
operator to reload and re-apply — render it, don't retry silently.

**Signature**

```tsx
function useUpsertModelPricingBaseline(): UseUpsertModelPricingBaselineReturn
```

**Returns** — `UseUpsertModelPricingBaselineReturn`

<TypeTable
  type={{
    clearError: { type: "() => void", description: "Reset `error` to `null`." },
    error: { type: "Error | null", description: "Error from the last failed attempt, or `null` when healthy." },
    isSubmitting: { type: "boolean", description: "`true` while a revision is being written." },
    upsert: { type: "(input: UpsertModelPricingBaselineInput) => Promise<ModelPricingBaseline>", description: "Create or revise a baseline entry. Returns the new revision with server-stamped lifecycle fields." },
  }}
/>


## Components

### AutoRechargeCard

Auto-recharge configuration card for the billing settings page.

Shows a toggle to enable/disable auto-recharge, with input fields
for threshold, recharge amount, and monthly cap. Displays a
disabled state with an explanatory message when no payment method
is on file. Validates inputs client-side before submitting.

**Props** — `AutoRechargeCardProps`

<TypeTable
  type={{
    accountStatus: { type: "BillingAccountStatus", description: "Account status — config disabled when suspended or closed.", required: true },
    autoRecharge: { type: "AutoRechargeConfig", description: "Current auto-recharge configuration from the billing account." },
    className: { type: "string", description: "Additional CSS class names." },
    hasPaymentMethod: { type: "boolean", description: "Whether the account has a saved payment method.", required: true },
    onSaved: { type: "() => void", description: "Called after a successful save so the parent can refresh account data." },
    orgId: { type: "string", description: "Organization ID.", required: true },
  }}
/>

```tsx
<AutoRechargeCard
  orgId={orgId}
  autoRecharge={account.autoRecharge}
  hasPaymentMethod={!!account.defaultPaymentMethod?.paymentMethodId}
  accountStatus={account.status}
  onSaved={refetch}
/>
```


### BillingSection

Top-level billing settings section.

Composes the billing sub-components into a cohesive settings page:
low-balance warning, checkout success banner, credit balance display,
credit pack purchase grid, and transaction history. Handles the
deployment mode gate (billing unavailable in local mode) and the
org-not-selected state.

**Props** — `BillingSectionProps`

<TypeTable
  type={{
    checkoutSuccess: { type: "boolean", description: "Whether a checkout just completed (e.g., `?checkout=success`).  When `true`, an optimistic banner is shown indicating that credits will appear shortly. This prop is typically driven by the host application's URL query parameters." },
    className: { type: "string", description: "Additional CSS class names." },
    onDismissCheckoutSuccess: { type: "() => void", description: "Callback to dismiss the checkout success banner." },
  }}
/>

```tsx
// In a settings page:
<BillingSection checkoutSuccess={searchParams.checkout === "success"} />
```


### CreditBalanceCard

Prominent display of the organization's available credit balance.

Shows the total available balance as the primary figure, with
a secondary breakdown of promotional vs. purchased credits and
any reserved amount. Uses semantic colors to indicate balance
health: green for healthy, amber for low, red for zero/negative.

**Props** — `CreditBalanceCardProps`

<TypeTable
  type={{
    balance: { type: "CreditBalance", description: "The credit balance breakdown from the billing account.", required: true },
    className: { type: "string", description: "Additional CSS class names." },
    isLowBalance: { type: "boolean", description: "Whether the balance is below the low-balance threshold." },
  }}
/>

```tsx
<CreditBalanceCard balance={account.balance} isLowBalance={isLow} />
```


### CreditLedgerTable

Paginated transaction history table showing credit ledger entries.

Displays entries with type badges, signed amounts, and running
balance. Supports pagination with simple prev/next controls.
Uses semantic colors: green for credits, red for debits, gray
for holds and releases.

**Props** — `CreditLedgerTableProps`

<TypeTable
  type={{
    className: { type: "string", description: "Additional CSS class names." },
    orgId: { type: "string", description: "Organization ID to fetch ledger entries for.", required: true },
  }}
/>

```tsx
<CreditLedgerTable orgId={activeOrg.metadata.id} />
```


### CreditPackGrid

Grid of credit pack cards with purchase buttons.

Displays the 3 self-serve credit packs (Starter, Growth, Team)
in a responsive grid. Each card shows the pack name, price,
credit count, and a buy button. Buttons are disabled when the
account is suspended/closed or a purchase is in progress.

**Props** — `CreditPackGridProps`

<TypeTable
  type={{
    accountStatus: { type: "BillingAccountStatus", description: "Account status — purchases disabled when suspended or closed.", required: true },
    className: { type: "string", description: "Additional CSS class names." },
    onPurchase: { type: "(packId: string) => void", description: "Called when the user clicks a pack's buy button.", required: true },
    purchasingPackId: { type: "string | null", description: "ID of the pack currently being purchased (shows loading state)." },
  }}
/>

```tsx
<CreditPackGrid
  accountStatus={account.status}
  purchasingPackId={isSubmitting ? activePackId : null}
  onPurchase={(packId) => createSession({ orgId, packId, ... })}
/>
```


### LowBalanceBanner

Conditional banner that warns when the available credit balance
is below the configured threshold.

Renders nothing when the balance is healthy. Not dismissible —
it reflects a real account state that the user should address.

**Props** — `LowBalanceBannerProps`

<TypeTable
  type={{
    availableMicros: { type: "bigint", description: "Available balance in micro-USD.", required: true },
    className: { type: "string", description: "Additional CSS class names." },
    thresholdMicros: { type: "bigint", description: "Threshold in micro-USD below which the warning is shown.", required: true },
  }}
/>

```tsx
<LowBalanceBanner
  availableMicros={account.balance.availableMicros}
  thresholdMicros={account.lowBalanceThresholdMicros}
/>
```


### ModelCatalogPanel

Platform-operator panel for authoring the model registry baseline:
the catalog list with revision history, an add/edit form with an
explicit old-to-new rate confirmation, and typed-confirmation
retirement (DD-004 — this replaces hand edits of
`model-registry.json`).

Prefer [`PricingGovernanceConsole`](#pricinggovernanceconsole) for new surfaces — it
composes this catalog with the governance view into one tabbed
console. This standalone panel remains for consumers embedding just
the catalog editor.

Requires `can_manage_model_pricing` on `platform:stigmer` — render it
only in operator-scoped surfaces. Rates are raw provider prices
(pre-markup), not customer prices.

**Props** — `ModelCatalogPanelProps`

<TypeTable
  type={{
    className: { type: "string", description: "Additional CSS class names." },
  }}
/>

```tsx
<ModelCatalogPanel />
```


### PaymentMethodCard

Displays the saved payment method with a button to open the
Stripe Customer Portal for management.

Shows card brand, last 4 digits, and expiry when a payment method
is on file. Shows an empty state prompt when none exists. The
"Manage" button opens the Stripe Customer Portal where users can
add, update, or remove payment methods.

**Props** — `PaymentMethodCardProps`

<TypeTable
  type={{
    accountStatus: { type: "BillingAccountStatus", description: "Account status — portal button disabled when suspended or closed.", required: true },
    className: { type: "string", description: "Additional CSS class names." },
    isPortalLoading: { type: "boolean", description: "`true` while the portal session is being created." },
    onManage: { type: "() => void", description: "Called when the user clicks \"Manage payment methods\".", required: true },
    paymentMethod: { type: "PaymentMethodSummary", description: "The saved payment method, or undefined if none is on file." },
  }}
/>

```tsx
<PaymentMethodCard
  paymentMethod={account.defaultPaymentMethod}
  accountStatus={account.status}
  onManage={() => openPortal(orgId)}
/>
```


### PricingGovernanceConsole

The platform-operator pricing console: a tabbed surface over the
model registry baseline and the pricing feedback loop.

- **Models** — every catalog entry joined with its governance state
  (effective vs baseline rates, ledger verification), searchable.
  Selecting a row opens a read-only detail record; editing (with an
  explicit old→new rate confirmation) and typed-confirmation
  retirement are deliberate steps from there.
- **Sign-Offs** — pricing overrides proposed by reconciliation and
  awaiting a human decision, searchable; the tab badge shows the
  pending count from anywhere in the console.

Requires `can_manage_model_pricing` on `platform:stigmer` — render it
only in operator-scoped surfaces (non-operators see the designed
access notice). Rates shown are raw provider prices (pre-markup),
not customer prices.

**Props** — `PricingGovernanceConsoleProps`

<TypeTable
  type={{
    className: { type: "string", description: "Additional CSS class names." },
    defaultTab: { type: "PricingGovernanceTab", description: "The tab shown on first render. Defaults to `\"models\"`.  Read once at mount, like a form field's `defaultValue` — changing it afterwards has no effect, and the user's tab choice stays local (the console never writes it back anywhere). Wire it from routing to make the tab URL-addressable:  ```tsx // e.g. honoring a ?tab=sign-offs deep link const tab = new URLSearchParams(window.location.search).get(\"tab\"); <PricingGovernanceConsole   defaultTab={tab === \"sign-offs\" ? \"sign-offs\" : undefined} /> ```", typeDescriptionLink: "#pricinggovernancetab" },
  }}
/>

```tsx
<PricingGovernanceConsole />
```


### PricingGovernancePanel

Platform-operator panel for the pricing feedback loop: pending
override sign-offs (approve/reject) and the effective-vs-baseline
pricing state of every model.

Prefer [`PricingGovernanceConsole`](#pricinggovernanceconsole) for new surfaces — it
composes this panel's content with the model catalog into one tabbed
console. This standalone panel remains for consumers embedding just
the governance view.

Requires `can_manage_model_pricing` on `platform:stigmer` — render it
only in operator-scoped surfaces. Rates shown are raw provider prices
(pre-markup), not customer prices.

**Props** — `PricingGovernancePanelProps`

<TypeTable
  type={{
    className: { type: "string", description: "Additional CSS class names." },
  }}
/>

```tsx
<PricingGovernancePanel />
```


## Types

### CreateCheckoutSessionInput

Parameters for [`useCreateCheckoutSession`](#usecreatecheckoutsession)'s `createSession` callback.

<TypeTable
  type={{
    cancelUrl: { type: "string", description: "URL to redirect to if the user cancels checkout.", required: true },
    orgId: { type: "string", description: "Organization purchasing credits.", required: true },
    packId: { type: "string", description: "Credit pack to purchase (e.g., \"starter\", \"growth\", \"team\").", required: true },
    successUrl: { type: "string", description: "URL to redirect to after successful payment.", required: true },
  }}
/>


### CREDIT_PACKS

Static credit pack catalog matching the backend `CreditPackCatalog`.

Packs are static product entries, not database-backed resources.
At launch, credits equal the dollar value (no volume bonus).


### CreditPackInfo

Metadata for a self-serve credit pack.

<TypeTable
  type={{
    credits: { type: "number", description: "Number of credits granted (1 credit = $0.01 USD).", required: true },
    description: { type: "string", description: "Brief description of the pack's positioning.", required: true },
    displayName: { type: "string", description: "Display name shown in the UI.", required: true },
    packId: { type: "string", description: "Stable identifier matching the backend catalog (e.g., \"starter\").", required: true },
    priceCents: { type: "number", description: "Price in USD cents (e.g., 1000 = $10.00).", required: true },
  }}
/>


### DecidePricingOverrideInput

Input for [`useDecidePricingOverride`](#usedecidepricingoverride).

<TypeTable
  type={{
    approve: { type: "boolean", description: "`true` approves (the override becomes ACTIVE and supersedes any current ACTIVE override on the same pricing key); `false` rejects.", required: true },
    decisionNote: { type: "string", description: "Optional note recorded on the decision for the audit trail." },
    overrideId: { type: "string", description: "The PENDING_SIGNOFF override to decide.", required: true },
  }}
/>


### formatCreditBalance

Convert micro-USD (bigint) to a display string like "$12.50".

Micro-USD uses 6 decimal places (1 USD = 1,000,000 micros).
The output always shows exactly 2 decimal places.

```tsx
function formatCreditBalance(micros: bigint | undefined): string
```


### formatCreditCount

Format a credit count with commas (e.g., 5000 -> "5,000").

```tsx
function formatCreditCount(credits: number): string
```


### formatLedgerAmount

Convert micro-USD (bigint) to a signed display string.

Positive amounts show "+$5.00", negative show "-$0.23",
zero shows "$0.00" (no sign).

```tsx
function formatLedgerAmount(micros: bigint): string
```


### formatLedgerDate

Format a protobuf Timestamp for ledger display.

Returns a locale-appropriate date and time string
(e.g., "May 3, 2026, 4:30 PM").

```tsx
function formatLedgerDate(seconds: bigint): string
```


### formatPackPrice

Format a cent amount as a dollar string (e.g., 1000 -> "$10").

```tsx
function formatPackPrice(cents: number): string
```


### GovernanceFlow

Where the operator is in the console: browsing the model list,
creating a new catalog entry, or focused on one model (viewing,
editing, or retiring it).


### isCredit

Whether a ledger entry type represents a credit (positive amount).

Used by UI components to choose semantic colors: green for credits,
red for debits, gray for holds/releases.

```tsx
function isCredit(type: LedgerEntryType): boolean
```


### isHold

Whether a ledger entry type represents a hold operation.

Holds are neither credits nor debits in the user's mental model —
they represent temporary balance changes (reservations, disputes).

```tsx
function isHold(type: LedgerEntryType): boolean
```


### ledgerEntryLabel

Human-readable label for a ledger entry type.

```tsx
function ledgerEntryLabel(type: LedgerEntryType): string
```


### ModelGovernanceRow

One model in the unified governance view: the ACTIVE catalog baseline
joined with its effective-registry governance state and its
append-only revision history.

<TypeTable
  type={{
    baseline: { type: "ModelPricingBaseline", description: "The ACTIVE baseline revision (catalog fields + list rates).", required: true },
    governance: { type: "ModelPricingGovernanceEntry | null", description: "The base-variant governance entry (baseline vs effective rates, active overrides, ledger reconcilability), or `null` when the governance view has no entry for this key — the row then renders baseline rates with no governance state.", required: true },
    history: { type: "readonly ModelPricingBaseline[]", description: "SUPERSEDED / RETIRED revisions, newest first as served.", required: true },
    key: { type: "string", description: "Stable identity: `modelId|provider|harness` (the catalog key).", required: true },
    variantGovernance: { type: "readonly ModelPricingGovernanceEntry[]", description: "Governance entries for this model's named pricing variants.", required: true },
  }}
/>


### PricingGovernanceTab

Tab ids of [`PricingGovernanceConsole`](#pricinggovernanceconsole), accepted by
`PricingGovernanceConsoleProps.defaultTab`.

These ids are deep-link targets: consumers wire them from their own
routing (e.g. a `?tab=sign-offs` query param), and external surfaces
compose URLs against them. Renaming a member is a breaking change to
every link in the wild, not just to this component's props.


### RetireModelPricingBaselineInput

Input for [`useRetireModelPricingBaseline`](#useretiremodelpricingbaseline).

<TypeTable
  type={{
    harness: { type: "string", description: "", required: true },
    modelId: { type: "string", description: "", required: true },
    provider: { type: "string", description: "", required: true },
    revisionNote: { type: "string", description: "Optional operator note recorded on the retirement." },
  }}
/>


### SetAutoRechargeConfigInput

Input for [`useSetAutoRechargeConfig`](#usesetautorechargeconfig).

<TypeTable
  type={{
    enabled: { type: "boolean", description: "Whether auto-recharge is enabled.", required: true },
    monthlyCapMicros: { type: "bigint", description: "Maximum monthly auto-recharge spend in micro-USD.", required: true },
    orgId: { type: "string", description: "Organization ID to configure auto-recharge for.", required: true },
    rechargeAmountMicros: { type: "bigint", description: "Fixed charge per recharge event in micro-USD.", required: true },
    thresholdMicros: { type: "bigint", description: "Trigger threshold in micro-USD.", required: true },
  }}
/>


### UpsertModelPricingBaselineInput

Input for [`useUpsertModelPricingBaseline`](#useupsertmodelpricingbaseline).

<TypeTable
  type={{
    baseline: { type: "ModelPricingBaseline", description: "The baseline entry to create or revise, keyed by (modelId, provider, harness). Lifecycle fields (baselineId, status, decision stamps, pricing effectiveAt) are server-owned and ignored.", required: true },
    revisionNote: { type: "string", description: "Optional operator note recorded on the revision for the audit trail." },
  }}
/>


### UseCreditLedgerOptions

Options for [`useCreditLedger`](#usecreditledger).

<TypeTable
  type={{
    pageNum: { type: "number", description: "1-based page number. Defaults to 1." },
    pageSize: { type: "number", description: "Page size. Defaults to 20." },
    typeFilter: { type: "LedgerEntryType[]", description: "Filter to specific ledger entry types. Empty means all types." },
    view: { type: "LedgerView", description: "Server-resolved ledger slice. When set to `LedgerView.statement`, the server returns only customer-facing money-movement entry types and excludes internal mechanics (per-call usage debits, reservation holds/releases). Defaults to the full ledger." },
  }}
/>


### UseModelPricingBaselinesOptions

Options for [`useModelPricingBaselines`](#usemodelpricingbaselines).

<TypeTable
  type={{
    enabled: { type: "boolean", description: "Pass `false` to skip fetching (stable no-op)." },
    includeHistory: { type: "boolean", description: "When `true`, includes SUPERSEDED and RETIRED revisions (the full append-only audit history). Default: ACTIVE entries only." },
  }}
/>

