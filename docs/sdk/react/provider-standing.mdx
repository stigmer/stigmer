---
title: provider-standing
---

{/* Auto-generated by @stigmer/react docs generator. DO NOT EDIT. */}

## Hooks

### useProviderStanding

Data hook for platform provider standing: the latest canary-probe
verdict per platform LLM provider account (status, latency, bounded
error summary, probe time), recorded hourly by the standing probe.

Platform-operator surface: the caller needs
`can_view_provider_standing` on `platform:stigmer`. Cloud-only — the
OSS Go server does not implement the controller.

Pass `enabled: false` to skip fetching (stable no-op).

**Signature**

```tsx
function useProviderStanding(options?: { enabled?: boolean }): UseProviderStandingReturn
```

<TypeTable
  type={{
    options: { type: "{ enabled?: boolean }", description: "" },
  }}
/>

**Returns** — `UseProviderStandingReturn`

<TypeTable
  type={{
    error: { type: "Error | null", description: "Error from the last failed request, or `null` when healthy." },
    isLoading: { type: "boolean", description: "`true` while the initial fetch is in flight." },
    isRefetching: { type: "boolean", description: "`true` while a background refetch is in flight and stale data is shown." },
    refetch: { type: "() => void", description: "Discard cached data and re-fetch from the server." },
    standing: { type: "ProviderStandingView | null", description: "The standing view, or `null` before the first successful fetch." },
  }}
/>

```tsx
const { standing, isLoading, refetch } = useProviderStanding();
```


## Components

### ProviderStandingAccessNotice

The access-denied state for the provider-standing operator surface.
Server-side authorization (`can_view_provider_standing` on
`platform:stigmer`) is the real gate — this is the honest, designed
face of that denial for anyone who navigates to the operator route
without the permission, instead of a raw RPC error string.


### ProviderStandingConsole

The platform-operator console page for provider standing: one card per
platform LLM provider account with the latest canary-probe verdict —
status, round-trip latency, upstream HTTP status, a bounded error
summary, and when the probe last ran (flagged when stale).

Read-only by design: the detection core (hourly probe, standing store,
degraded/silence alerts) lives server-side; this surface only makes it
visible where operators already look. Requires
`can_view_provider_standing` on `platform:stigmer` — non-operators see
the designed access notice.

**Props** — `ProviderStandingConsoleProps`

<TypeTable
  type={{
    className: { type: "string", description: "Additional CSS class names." },
  }}
/>

```tsx
<ProviderStandingConsole />
```

