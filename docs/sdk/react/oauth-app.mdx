---
title: OAuth App
description: Hooks and components for OAuth app creation, listing, editing, and deletion.
---

{/* Auto-generated by @stigmer/react docs generator. DO NOT EDIT. */}

## Hooks

### useCreateOAuthApp

Mutation hook that wraps `oauthapp.create()` with loading and error
state.

Creates an OAuth app resource within an organization. The caller
provides an `OAuthAppInput` with the required metadata (name,
org) and spec fields (provider, client ID, client secret, OAuth
endpoint URLs).

**Signature**

```tsx
function useCreateOAuthApp(): UseCreateOAuthAppReturn
```

**Returns** — `UseCreateOAuthAppReturn`

<TypeTable
  type={{
    clearError: { type: "() => void", description: "Reset `error` to `null`." },
    create: { type: "(input: OAuthAppInput) => Promise<OAuthApp>", description: "Submit an OAuthAppInput to create a new OAuth app. Resolves with the server-created resource." },
    error: { type: "Error | null", description: "Error from the last failed create, or `null` when healthy." },
    isCreating: { type: "boolean", description: "`true` while the create request is in flight." },
  }}
/>

```tsx
const { create, isCreating, error } = useCreateOAuthApp();

const app = await create({
  name: "My Slack App",
  org: "acme",
  provider: "Slack",
  clientId: "123456.789012",
  clientSecret: "secret",
  authorizationUrl: "https://slack.com/oauth/v2/authorize",
  tokenUrl: "https://slack.com/api/oauth.v2.access",
});
```


### useDeleteOAuthApp

Mutation hook that wraps `oauthapp.delete()` with loading and error
state.

Deletes an OAuth app by its resource ID. Returns the deleted
`OAuthApp` on success so callers can confirm which app was
removed. The deletion is permanent — any MCP server overrides
referencing this app will lose their binding.

**Signature**

```tsx
function useDeleteOAuthApp(): UseDeleteOAuthAppReturn
```

**Returns** — `UseDeleteOAuthAppReturn`

<TypeTable
  type={{
    clearError: { type: "() => void", description: "Reset `error` to `null`." },
    deleteApp: { type: "(id: string) => Promise<OAuthApp>", description: "Delete an OAuth app by its resource ID. Resolves with the deleted resource for confirmation display." },
    error: { type: "Error | null", description: "Error from the last failed delete, or `null` when healthy." },
    isDeleting: { type: "boolean", description: "`true` while the delete request is in flight." },
  }}
/>

```tsx
const { deleteApp, isDeleting, error } = useDeleteOAuthApp();

await deleteApp("oauth-app-id-abc123");
refetch(); // refresh the list after deletion
```


### useOAuthAppList

Data hook that fetches all `OAuthApp` entries for an organization.

Returns every OAuthApp whose `metadata.org` matches the input. In
practice these are the BYOA OAuth apps created through the "Bring
your own app" flow on MCP server detail pages.

Pass `null` for `org` to skip fetching (stable no-op). Useful when
the active organization has not been resolved yet.

**Signature**

```tsx
function useOAuthAppList(org: string | null): UseOAuthAppListReturn
```

<TypeTable
  type={{
    org: { type: "string | null", description: "", required: true },
  }}
/>

**Returns** — `UseOAuthAppListReturn`

<TypeTable
  type={{
    error: { type: "Error | null", description: "Error from the last failed request, or `null` when healthy." },
    isLoading: { type: "boolean", description: "`true` while the initial fetch or a refetch is in flight." },
    isRefetching: { type: "boolean", description: "`true` while a background refetch is in flight and stale data is shown." },
    oauthApps: { type: "readonly OAuthApp[]", description: "OAuth apps owned by the organization. Empty while loading or on error." },
    refetch: { type: "() => void", description: "Discard cached data and re-fetch the list from the server." },
  }}
/>

```tsx
const { oauthApps, isLoading, error } = useOAuthAppList(org);

if (isLoading) return <Spinner />;
oauthApps.map((app) => app.spec?.provider);
```


### useUpdateOAuthApp

Mutation hook that wraps `oauthapp.update()` with loading and error
state.

Updates an existing OAuth app. The input must include the `name` and
`org` fields to identify the target resource, along with the updated
spec fields. Omit `clientSecret` to leave the existing secret
unchanged.

**Signature**

```tsx
function useUpdateOAuthApp(): UseUpdateOAuthAppReturn
```

**Returns** — `UseUpdateOAuthAppReturn`

<TypeTable
  type={{
    clearError: { type: "() => void", description: "Reset `error` to `null`." },
    error: { type: "Error | null", description: "Error from the last failed update, or `null` when healthy." },
    isUpdating: { type: "boolean", description: "`true` while the update request is in flight." },
    update: { type: "(input: OAuthAppInput) => Promise<OAuthApp>", description: "Submit an OAuthAppInput to update an existing OAuth app. Resolves with the updated resource." },
  }}
/>

```tsx
const { update, isUpdating, error } = useUpdateOAuthApp();

await update({
  name: "My Slack App",
  org: "acme",
  provider: "Slack",
  clientId: "123456.789012",
  authorizationUrl: "https://slack.com/oauth/v2/authorize",
  tokenUrl: "https://slack.com/api/oauth.v2.access",
});
```


## Components

### CreateOAuthAppForm

Form for creating a new OAuth app within an organization.

Collects the required OAuth configuration: **name**, **provider**,
**client ID**, **client secret**, **authorization URL**, and
**token URL**. An expandable "Advanced" section provides optional
fields for scopes, userinfo URL, scope parameter name, and vendor
approval settings.

This is a pure presentational component with no dialog wrapper
(headless-first). The parent is responsible for rendering it inside
a card, dialog, or inline context as needed.

All visual properties flow through `--stgm-*` design tokens.

**Props** — `CreateOAuthAppFormProps`

<TypeTable
  type={{
    className: { type: "string", description: "Additional CSS class names for the root container." },
    onCancel: { type: "() => void", description: "Fired when the user cancels creation." },
    onCreated: { type: "(app: OAuthApp) => void", description: "Fired with the newly created OAuth app on success." },
    org: { type: "string", description: "Organization slug — the OAuth app will be created in this org.", required: true },
  }}
/>

```tsx
<CreateOAuthAppForm
  org="acme"
  onCreated={(app) => {
    refetch();
    setShowForm(false);
  }}
  onCancel={() => setShowForm(false)}
/>
```


### OAuthAppDetailPanel

View and edit panel for an existing OAuth app.

In **view mode**, displays all OAuth configuration fields in a
structured label/value layout with "Edit" and "Delete" buttons.

In **edit mode**, fields become editable inputs. The client secret
field shows a placeholder — leave it empty to keep the existing
secret, or enter a new value to replace it. "Save" submits the
update via [`useUpdateOAuthApp`](#useupdateoauthapp); "Cancel" discards changes
and returns to view mode.

Delete uses an inline confirmation pattern (no modal) to avoid
portal/z-index issues for SDK embedders.

All visual properties flow through `--stgm-*` design tokens.

**Props** — `OAuthAppDetailPanelProps`

<TypeTable
  type={{
    className: { type: "string", description: "Additional CSS class names for the root container." },
    oauthApp: { type: "OAuthApp", description: "The OAuth app resource to display and edit.", required: true },
    onBack: { type: "() => void", description: "Fired when the user clicks the back button." },
    onDeleted: { type: "() => void", description: "Fired after the resource is successfully deleted." },
    onUpdated: { type: "(app: OAuthApp) => void", description: "Fired with the updated resource after a successful save." },
  }}
/>

```tsx
<OAuthAppDetailPanel
  oauthApp={app}
  onUpdated={(updated) => refetch()}
  onDeleted={() => { refetch(); setFlow({ phase: "idle" }); }}
  onBack={() => setFlow({ phase: "idle" })}
/>
```


### OAuthAppListPanel

Displays a list of `OAuthApp` resources owned by an
organization.

Each row shows the provider name, client ID (non-secret), and
creation date. When `onEdit` is provided, rows include a pencil
icon that fires the callback with the selected app — enabling
navigation to a detail/edit view.

All visual properties flow through `--stgm-*` design tokens.

**Props** — `OAuthAppListPanelProps`

<TypeTable
  type={{
    className: { type: "string", description: "Additional CSS class names for the root container." },
    onEdit: { type: "(app: OAuthApp) => void", description: "Fired when the user wants to view/edit an OAuth app. When provided, rows become interactive with a pencil icon button. When absent, rows remain static (backward compatible)." },
    onRefetchRef: { type: "(refetch: () => void) => void", description: "Re-expose refetch so parents can trigger a list refresh." },
    org: { type: "string", description: "Organization slug to list OAuth apps for.", required: true },
  }}
/>

```tsx
<OAuthAppListPanel org="acme" />
```

```tsx
<OAuthAppListPanel
  org="acme"
  onEdit={(app) => setFlow({ phase: "editing", oauthApp: app })}
  onRefetchRef={(refetch) => { listRefetchRef.current = refetch; }}
/>
```

