---
title: Resource Creation
description: Hooks and components for template-driven resource creation flows.
---

{/* Auto-generated by @stigmer/react docs generator. DO NOT EDIT. */}

## Hooks

### useTemplateFilter

Headless hook for filtering and searching a set of resource templates.

Provides category tab state, text search across name/description/tags,
and the filtered result set. Fully memoized — only recalculates when
inputs change.

Platform builders can use this hook with their own rendering; the
styled `TemplateGallery` component uses it internally.

**Signature**

```tsx
function useTemplateFilter(options: UseTemplateFilterOptions<TData>): UseTemplateFilterReturn<TData>
```

<TypeTable
  type={{
    options: { type: "UseTemplateFilterOptions<TData>", description: "", required: true },
  }}
/>

**Returns** — `UseTemplateFilterReturn`

<TypeTable
  type={{
    activeCategory: { type: "TemplateCategory | null", description: "Currently active category filter, or `null` for \"All\"." },
    availableCategories: { type: "readonly TemplateCategory[]", description: "Deduplicated, sorted list of categories present in the template set." },
    filtered: { type: "readonly ResourceTemplate<TData>[]", description: "Templates matching the current search query and active category." },
    query: { type: "string", description: "Current search query string." },
    setActiveCategory: { type: "(category: TemplateCategory | null) => void", description: "Set the active category filter. Pass `null` to show all." },
    setQuery: { type: "(query: string) => void", description: "Update the search query." },
  }}
/>

```tsx
const { filtered, query, setQuery, activeCategory, setActiveCategory, availableCategories } =
  useTemplateFilter({ templates: AGENT_TEMPLATES });

// Render your own gallery UI using `filtered`
```


### useWizardState

Headless state machine for multi-step wizard flows.

Manages step navigation, per-step validation gating, and accumulated
form data. Generic over the data shape — each wizard defines its own
`TData` type with fields for all steps.

Navigation forward is gated by the current step's `validate` function.
Navigation backward is always allowed. Direct step jumps only go backward
(prevents skipping required steps).

Follows the `useReducer` + discriminated union pattern established
by `agentSetupReducer` — pure state transitions, imperative API on top.

**Signature**

```tsx
function useWizardState(options: UseWizardStateOptions<TData>): UseWizardStateReturn<TData>
```

<TypeTable
  type={{
    options: { type: "UseWizardStateOptions<TData>", description: "", required: true },
  }}
/>

**Returns** — `UseWizardStateReturn`

<TypeTable
  type={{
    canGoBack: { type: "boolean", description: "Whether navigation backward is possible (not on first step)." },
    canGoNext: { type: "boolean", description: "Whether navigation forward is allowed (current step is valid)." },
    currentStep: { type: "WizardStepDef<TData>", description: "Definition of the current step.", typeDescriptionLink: "#wizardstepdef" },
    currentStepIndex: { type: "number", description: "Zero-based index of the current step." },
    data: { type: "TData", description: "Accumulated form data across all steps." },
    goBack: { type: "() => void", description: "Return to the previous step." },
    goNext: { type: "() => void", description: "Advance to the next step (validates current step first)." },
    goToStep: { type: "(index: number) => void", description: "Jump directly to a specific step (only allows going backward)." },
    isFirstStep: { type: "boolean", description: "`true` when on the first step." },
    isLastStep: { type: "boolean", description: "`true` when on the last step." },
    updateData: { type: "(partial: Partial<TData>) => void", description: "Merges a partial update into the accumulated data." },
    validationError: { type: "string | null", description: "Validation error for the current step, or `null`." },
  }}
/>

```tsx
const wizard = useWizardState({
  steps: [
    { id: "basics", label: "Basics", validate: (d) => d.name ? null : "Name is required" },
    { id: "review", label: "Review" },
  ],
  initialData: { name: "", description: "" },
});

// wizard.data.name, wizard.updateData({ name: "My Agent" })
// wizard.goNext(), wizard.goBack()
```


## Components

### CreationPicker

"Step 0" landing screen for resource creation flows.

Presents three creation entry points as large option cards:
1. **Start from scratch** — blank wizard
2. **Browse templates** — opens the template gallery inline
3. **Import from file** — triggers the import dialog

When the user clicks "Browse templates", the picker transitions
to show the `TemplateGallery` inline with a back button. Selecting
a template emits `onSelect({ kind: "template", data })`.

This component is SDK-first with zero Console dependencies. The
consumer (e.g. `AgentNewPage`) handles routing, the import dialog,
and mounting the wizard.

**Props** — `CreationPickerProps`

<TypeTable
  type={{
    className: { type: "string", description: "Additional CSS classes for the root container." },
    onSelect: { type: "(path: CreationPath) => void", description: "Called when the user selects a creation path.", required: true },
    resourceLabel: { type: "string", description: "Label for the resource type (e.g. \"agent\", \"MCP server\"). Used in headings.", required: true },
    templates: { type: "readonly ResourceTemplate<TData>[]", description: "Available templates for the gallery view.", required: true },
  }}
/>

```tsx
<CreationPicker
  resourceLabel="agent"
  templates={AGENT_TEMPLATES}
  onSelect={(path) => {
    if (path.kind === "scratch") startWizard();
    if (path.kind === "template") startWizard(path.data);
    if (path.kind === "import") openImportDialog();
  }}
/>
```


### StepIndicator

Vertical step progress indicator for multi-step wizards.

Renders each step as a numbered circle with label. Steps are
color-coded by state: completed (filled), active (ring), pending
(muted). Completed steps are clickable for backward navigation.

Responsive: collapses to a compact horizontal layout on narrow
viewports via CSS container queries.

Uses `--stgm-*` tokens exclusively. Zero Console dependencies.

**Props** — `StepIndicatorProps`

<TypeTable
  type={{
    className: { type: "string", description: "Additional CSS classes for the root container." },
    currentStepIndex: { type: "number", description: "Zero-based index of the currently active step.", required: true },
    onStepClick: { type: "(index: number) => void", description: "Called when a completed step is clicked (backward navigation)." },
    steps: { type: "readonly { id: string; label: string }[]", description: "Step definitions to render (only `id` and `label` are used for rendering).", required: true },
  }}
/>


### TemplateCard

A clickable card that displays a resource template's metadata.

Renders the template's name, description, and category badge.
Displays a colored initial avatar derived from the template name.

Fully keyboard-accessible: focusable, activates on Enter/Space.
Uses `--stgm-*` tokens for all structural styling; category badge
colors use Tailwind's semantic palette with opacity for dark mode.

**Props** — `TemplateCardProps`

<TypeTable
  type={{
    className: { type: "string", description: "Additional CSS classes for the card container." },
    onSelect: { type: "(template: ResourceTemplate<TData>) => void", description: "Called when the card is selected (click or Enter).", required: true },
    template: { type: "ResourceTemplate<TData>", description: "The template to display.", required: true, typeDescriptionLink: "#resourcetemplate" },
  }}
/>


### TemplateGallery

A searchable, filterable gallery of resource templates.

Renders category tabs, a search input, and a responsive card grid.
Uses the headless `useTemplateFilter` hook for state — platform
builders who want custom rendering can use the hook directly.

Keyboard navigation:
- Tab moves focus between the search input, category tabs, and cards
- Arrow keys (Left/Right) navigate between category tabs
- Enter/Space activates the focused tab or card

All structural styling via `--stgm-*` tokens. Zero Console or
framework dependencies.

**Props** — `TemplateGalleryProps`

<TypeTable
  type={{
    className: { type: "string", description: "Additional CSS classes for the root container." },
    emptyContent: { type: "ReactNode", description: "Content to render when no templates match the search/filter. Falls back to a built-in empty message if not provided." },
    onSelect: { type: "(template: ResourceTemplate<TData>) => void", description: "Called when the user selects a template.", required: true },
    templates: { type: "readonly ResourceTemplate<TData>[]", description: "Templates to display in the gallery.", required: true },
  }}
/>

```tsx
<TemplateGallery
  templates={AGENT_TEMPLATES}
  onSelect={(template) => startWizardWithTemplate(template)}
/>
```


### WizardNav

Navigation footer for the wizard shell.

Renders Back / Next|Submit buttons in a standardized layout.
The primary (right-side) button adapts its label and loading
state based on whether it's the final step.

Keyboard: Enter triggers the primary action when focused within
the wizard content (not within text areas or other multi-line inputs).

**Props** — `WizardNavProps`

<TypeTable
  type={{
    className: { type: "string", description: "Additional CSS classes for the root container." },
    isSubmitting: { type: "boolean", description: "Whether the primary action is in a loading state." },
    nextDisabled: { type: "boolean", description: "Whether the primary action is disabled." },
    nextLabel: { type: "string", description: "Label for the primary action button.", required: true },
    onBack: { type: "() => void", description: "Called when Back is clicked.", required: true },
    onCancel: { type: "() => void", description: "Called when Cancel is clicked." },
    onNext: { type: "() => void", description: "Called when Next/Submit is clicked.", required: true },
    showBack: { type: "boolean", description: "Whether the Back button should be displayed.", required: true },
  }}
/>


### WizardShell

Reusable multi-step wizard layout for resource creation flows.

Renders three zones:
1. **Step indicator sidebar** — vertical progress showing all steps
2. **Content area** — the active step's form content (via `children`)
3. **Navigation footer** — Back / Next / Create buttons

This component is resource-agnostic. It accepts step definitions for
the indicator, delegates content rendering to the consumer, and
emits navigation events. The consumer (e.g., `AgentCreationWizard`)
manages form state via `useWizardState` and renders the appropriate
step component as `children`.

Layout:
- **Desktop** (>= 640px): Sidebar (200px) + content (flex-1) side by side
- **Mobile** (< 640px): Step indicator as compact top bar, content below

All visual properties via `--stgm-*` tokens. Zero Console or
framework dependencies — works in any React host.

**Props** — `WizardShellProps`

<TypeTable
  type={{
    canGoBack: { type: "boolean", description: "Whether the \"Back\" button should be shown." },
    canGoNext: { type: "boolean", description: "Whether the \"Next\" / submit button should be disabled." },
    children: { type: "ReactNode", description: "Content to render in the main area (the active step's UI).", required: true },
    className: { type: "string", description: "Additional CSS classes for the root container." },
    currentStepIndex: { type: "number", description: "Zero-based index of the currently active step.", required: true },
    isSubmitting: { type: "boolean", description: "Whether the submit action is currently in progress." },
    onBack: { type: "() => void", description: "Called when the user clicks \"Back\".", required: true },
    onCancel: { type: "() => void", description: "Called when the user cancels the wizard entirely." },
    onGoToStep: { type: "(index: number) => void", description: "Called when a completed step is clicked in the indicator (backward jump)." },
    onNext: { type: "() => void", description: "Called when the user clicks \"Next\" or the submit button.", required: true },
    steps: { type: "readonly { id: string; label: string }[]", description: "Step definitions for the progress indicator (only `id` and `label` are used for rendering).", required: true },
    submitLabel: { type: "string", description: "Label for the final step's action button (e.g. \"Create agent\").", required: true },
  }}
/>

```tsx
<WizardShell
  steps={steps}
  currentStepIndex={wizard.currentStepIndex}
  submitLabel="Create agent"
  isSubmitting={isCreating}
  canGoNext={wizard.canGoNext}
  canGoBack={wizard.canGoBack}
  onNext={wizard.goNext}
  onBack={wizard.goBack}
  onCancel={handleCancel}
>
  {renderCurrentStep()}
</WizardShell>
```


## Types

### AGENT_TEMPLATES

Built-in agent templates shipped with the SDK.

Each template pre-fills the agent creation wizard with a curated
set of instructions, name, and description. Users can customize
every field after selection — templates are starting points, not
constraints.

Platform builders can pass their own template arrays to the
gallery; these built-in templates are a convenience default.


### CreationPath

The creation path the user selected on the picker screen.

- `"scratch"` — start the wizard with empty fields
- `"template"` — start the wizard with template-provided data
- `"import"` — open the import dialog (handled by the consumer)


### EnvVarEntry

A single env var declaration entry in a wizard form.

<TypeTable
  type={{
    description: { type: "string", description: "Human-readable description.", required: true },
    isSecret: { type: "boolean", description: "Whether this variable holds a secret value.", required: true },
    key: { type: "string", description: "Environment variable name (e.g. GITHUB_TOKEN).", required: true },
    optional: { type: "boolean", description: "Whether the variable is optional.", required: true },
  }}
/>


### KeyValueEntry

A generic key-value pair entry for wizard forms (e.g. HTTP headers).

<TypeTable
  type={{
    key: { type: "string", description: "The key (e.g. header name).", required: true },
    value: { type: "string", description: "The value (e.g. header value).", required: true },
  }}
/>


### MCP_SERVER_TEMPLATES

Built-in MCP server templates shipped with the SDK.

Each template pre-fills the MCP server creation wizard with a
curated transport configuration, environment variable declarations,
and descriptive metadata. Users can customize every field after
selection.

Platform builders can pass their own template arrays to the
gallery; these built-in templates are a convenience default.


### ResourceTemplate

A pre-built resource configuration that seeds a creation wizard.

Generic over `TData` — the wizard's accumulated form state type.
For agents, `TData` is `AgentWizardData`; for MCP servers,
`TData` is `McpServerWizardData`.

`data` is `Partial<TData>`: only the fields the template wants to
pre-fill. The wizard merges this with its default empty state via
`{ ...createInitialWizardData(), ...template.data }`.

<TypeTable
  type={{
    category: { type: "TemplateCategory", description: "Category for gallery filtering.", required: true, typeDescriptionLink: "#templatecategory" },
    data: { type: "Partial<TData>", description: "Partial wizard data to merge with defaults when the user selects this template. Only fields that the template wants to pre-fill are included.", required: true },
    description: { type: "string", description: "Short description of what this template sets up.", required: true },
    id: { type: "string", description: "Stable template identifier (unique within a resource kind).", required: true },
    name: { type: "string", description: "Human-readable template name displayed in the gallery card.", required: true },
    tags: { type: "readonly string[]", description: "Searchable tags for the gallery's text search." },
  }}
/>


### TEMPLATE_CATEGORY_LABELS

Human-readable labels for template categories.

Used by the gallery UI for tab labels and screen readers.
Exported so platform builders can reuse or extend them.


### TemplateCategory

Template categories for grouping resource templates in the gallery.

Each category maps to a common use-case domain. The gallery renders
these as filterable tabs. Platform builders can use the same
categories or extend with custom values if they define their own
template arrays.


### UseTemplateFilterOptions

Options for [`useTemplateFilter`](#usetemplatefilter).

<TypeTable
  type={{
    templates: { type: "readonly ResourceTemplate<TData>[]", description: "The full set of templates to filter.", required: true },
  }}
/>


### UseWizardStateOptions

Options for [`useWizardState`](#usewizardstate).

<TypeTable
  type={{
    initialData: { type: "TData", description: "Initial form data (empty/default values for all fields).", required: true },
    steps: { type: "readonly WizardStepDef<TData>[]", description: "Ordered step definitions — determines wizard length and validation.", required: true },
  }}
/>


### WizardState

Internal state shape for the wizard reducer.

<TypeTable
  type={{
    currentStepIndex: { type: "number", description: "Zero-based index of the currently active step.", required: true },
    data: { type: "TData", description: "Accumulated form data across all steps.", required: true },
    validationError: { type: "string | null", description: "Validation error for the current step, or `null` when valid.", required: true },
  }}
/>


### WizardStepDef

Defines a wizard step's metadata and validation logic.

Generic over `TData` — the accumulated form state type that flows
through all steps. Each step's `validate` function receives the full
accumulated state and returns an error message or `null` if valid.

<TypeTable
  type={{
    id: { type: "string", description: "Stable step identifier (used as key and for navigation).", required: true },
    label: { type: "string", description: "Human-readable step label displayed in the step indicator.", required: true },
    validate: { type: "(data: TData) => string | null", description: "Validates the current data before allowing navigation to the next step. Returns a descriptive error string if invalid, or `null` if the step passes validation.  When omitted, the step is always considered valid." },
  }}
/>

