---
title: plugin
---

{/* Auto-generated by @stigmer/react docs generator. DO NOT EDIT. */}

## Hooks

### useAddToolsToAgent

Behaviour hook behind [`AddToolsToAgentDialog`](#addtoolstoagentdialog).

**Signature**

```tsx
function useAddToolsToAgent(servers: readonly AddableServer[]): UseAddToolsToAgentReturn
```

<TypeTable
  type={{
    servers: { type: "readonly AddableServer[]", description: "", required: true },
  }}
/>

**Returns** — `UseAddToolsToAgentReturn`

<TypeTable
  type={{
    add: { type: "() => Promise<number>", description: "Append the servers not already listed and save. Resolves to the number added." },
    clearError: { type: "() => void", description: "" },
    error: { type: "Error | null", description: "" },
    phase: { type: "AddToolsPhase", description: "", typeDescriptionLink: "#addtoolsphase" },
    pick: { type: "(ref: ResourceRef | null) => void", description: "The pick from the agent picker; `null` returns to picking." },
    reset: { type: "() => void", description: "" },
  }}
/>

```tsx
const flow = useAddToolsToAgent(servers);
<AgentPicker org={org} value={picked} onChange={flow.pick} />
```


### useInstallPlugin

Behaviour hook that installs (or upgrades) a plugin from a prepared
archive through `stigmer.plugin.push()`, the one RPC the CLI's `push
plugin` and `install` use too, size-routed by the SDK (inline under the
message cap, staged over HTTP above it). Pushing the archive the org
already holds changes nothing, by the server's contract.

**Signature**

```tsx
function useInstallPlugin(): UseInstallPluginReturn
```

**Returns** — `UseInstallPluginReturn`

<TypeTable
  type={{
    clearError: { type: "() => void", description: "Reset the error state." },
    error: { type: "Error | null", description: "The last error, or `null`." },
    install: { type: "(prepared: PreparedInstall, options: InstallPluginOptions) => Promise<InstallPluginOutcome>", description: "Push the prepared archive; resolves with the plugin and its members. Rejects with the server's refusal." },
    isInstalling: { type: "boolean", description: "`true` while the push is in flight." },
  }}
/>

```tsx
const { install, isInstalling } = useInstallPlugin();
const outcome = await install(prepared, { org, installedFrom: "acme-plugins" });
```


### useInstallRelation

Pass `null` for either argument to skip (stable no-op).

**Signature**

```tsx
function useInstallRelation(prepared: PreparedInstall | null, org: string | null): UseInstallRelationReturn
```

<TypeTable
  type={{
    prepared: { type: "PreparedInstall | null", description: "", required: true },
    org: { type: "string | null", description: "", required: true },
  }}
/>

**Returns** — `UseInstallRelationReturn`

<TypeTable
  type={{
    error: { type: "Error | null", description: "" },
    installed: { type: "Plugin | null", description: "The org's plugin of the same name, or `null` when none is installed (or not yet asked)." },
    isLoading: { type: "boolean", description: "" },
    refetch: { type: "() => void", description: "" },
    relation: { type: "InstallRelation | null", description: "" },
  }}
/>


### useManagingPlugin

Behaviour hook that says whether a resource is managed by an installed
plugin, from the resource's own labels.

The server refuses a client edit of a labelled resource only while the
labelled plugin row exists (a label alone never locks a resource), so
this hook answers the same way: a label whose plugin no longer exists
yields `null`, and the resource is the user's again. Detail views narrow
`editable` by this answer so an edit form is never offered for a write
the server would refuse.

**Signature**

```tsx
function useManagingPlugin(labels: Readonly<Record<string, string>> | undefined): UseManagingPluginReturn
```

<TypeTable
  type={{
    labels: { type: "Readonly<Record<string, string>> | undefined", description: "", required: true },
  }}
/>

**Returns** — `UseManagingPluginReturn`

<TypeTable
  type={{
    isLoading: { type: "boolean", description: "`true` while the label's plugin is being read." },
    plugin: { type: "Plugin | null", description: "The plugin that installed the resource, or `null` when none does (no label, or the plugin is gone)." },
  }}
/>


### useMarketplace

Data hook that opens a marketplace source and reads its catalogue.

A GitHub source is listed once through the Trees API and read at the
commit the listing resolved to; the official catalogue is read from the
npm CDN at the server's version. Pass `null` to skip (stable no-op).
The `error` carries a sentence the component renders as it is: a
repository that is not public, a development server with no published
catalogue, a tree the reader refuses.

**Signature**

```tsx
function useMarketplace(source: MarketplaceSource | null, options: UseMarketplaceOptions): UseMarketplaceReturn
```

<TypeTable
  type={{
    source: { type: "MarketplaceSource | null", description: "", required: true },
    options: { type: "UseMarketplaceOptions", description: "", required: true },
  }}
/>

**Returns** — `UseMarketplaceReturn`

<TypeTable
  type={{
    error: { type: "Error | null", description: "The source's or the reader's refusal, or `null` when healthy." },
    isLoading: { type: "boolean", description: "`true` while the tree is being listed and its marketplace file read." },
    marketplace: { type: "Marketplace | null", description: "The catalogue, or `null` while loading or on error." },
    opened: { type: "OpenedMarketplace | null", description: "The opened tree, kept for the install that follows; `null` until read." },
    refetch: { type: "() => void", description: "Re-list the tree (a conditional request, free when nothing changed)." },
    warnings: { type: "readonly MarketplaceFinding[]", description: "Entries the reader dropped, each with its own sentence, as `stigmer marketplace show` lists them." },
  }}
/>

```tsx
const { marketplace, warnings, isLoading, error } = useMarketplace({ type: "github", repo: "acme/plugins" });
```


### useMarketplaceCatalog

Read every source in `marketplaces` independently.

**Signature**

```tsx
function useMarketplaceCatalog(marketplaces: readonly KnownMarketplace[], options: UseMarketplaceCatalogOptions): UseMarketplaceCatalogReturn
```

<TypeTable
  type={{
    marketplaces: { type: "readonly KnownMarketplace[]", description: "", required: true },
    options: { type: "UseMarketplaceCatalogOptions", description: "", required: true },
  }}
/>

**Returns** — `UseMarketplaceCatalogReturn`

<TypeTable
  type={{
    reads: { type: "readonly SourceRead[]", description: "One read per source, in the order given; reference-stable while nothing changed." },
    refetch: { type: "(name: string) => void", description: "Read one source again (a conditional request for a GitHub tree, free when nothing changed)." },
  }}
/>

```tsx
const { marketplaces } = useMarketplaces();
const { reads, refetch } = useMarketplaceCatalog(marketplaces);
const entries = useMemo(() => catalogEntries(reads), [reads]);
```


### useMarketplaces

The sources this browser knows, with add and remove.

Reads through `useSyncExternalStore`, so every mounted consumer sees an
add or remove at once, and a change from another tab arrives through
the `storage` event.

**Signature**

```tsx
function useMarketplaces(options: UseMarketplacesOptions): UseMarketplacesReturn
```

<TypeTable
  type={{
    options: { type: "UseMarketplacesOptions", description: "", required: true },
  }}
/>

**Returns** — `UseMarketplacesReturn`

<TypeTable
  type={{
    add: { type: "(sourceText: string, name?: string) => Promise<AddSourceOutcome>", description: "Read a GitHub source and remember it. The name is the marketplace file's own unless `name` is given; a reserved or taken name, a text that is not a GitHub source, and a repository that is not a marketplace are refused with their sentences." },
    isBuiltIn: { type: "(name: string) => boolean", description: "Whether `name` is a source the user may neither add over nor remove." },
    marketplaces: { type: "readonly KnownMarketplace[]", description: "The built-in source first, then the remembered ones in the order they were added." },
    parseSource: { type: "(sourceText: string) => GitHubSourceOutcome", description: "Parse a source the user typed without adding it, for live validation in a form." },
    remove: { type: "(name: string) => string | null", description: "Forget the source called `name`; a built-in refuses. Returns the refusal or `null`." },
    unreadable: { type: "readonly UnreadableMarketplace[]", description: "" },
  }}
/>

```tsx
const { marketplaces, add, remove } = useMarketplaces();
const outcome = await add("acme/plugins");
if (!outcome.ok) toast.error(outcome.message);
```


### useMcpServerReadiness

Behaviour hook for one installed MCP server's readiness. Pass `null` for
either argument to hold the hook idle.

**Signature**

```tsx
function useMcpServerReadiness(org: string | null, slug: string | null, onSignedIn?: (mcpServerId: string) => void): UseMcpServerReadinessReturn
```

<TypeTable
  type={{
    org: { type: "string | null", description: "", required: true },
    slug: { type: "string | null", description: "", required: true },
    onSignedIn: { type: "(mcpServerId: string) => void", description: "" },
  }}
/>

**Returns** — `UseMcpServerReadinessReturn`

<TypeTable
  type={{
    connectionHealth: { type: "OAuthConnectionHealth", description: "The grant's health as the backend grades it; `UNSPECIFIED` until read." },
    declaredVariables: { type: "readonly string[]", description: "The variables an `api-key` server declares, in declaration order." },
    error: { type: "Error | null", description: "The read error, or the last sign-in error, or `null`." },
    isSigningIn: { type: "boolean", description: "`true` while a sign-in is in flight." },
    kind: { type: "McpServerReadinessKind", description: "", typeDescriptionLink: "#mcpserverreadinesskind" },
    mcpServer: { type: "McpServer | null", description: "The server, once read." },
    phase: { type: "OAuthConnectPhase", description: "The sign-in's phase; `idle` when none is running.", typeDescriptionLink: "/docs/sdk/react/mcp-server#oauthconnectphase" },
    refetch: { type: "() => void", description: "Read the server and its grant again." },
    signIn: { type: "() => void", description: "Start the OAuth sign-in. Call from a click handler so the popup is allowed. A no-op unless `kind` is `sign-in-needed`." },
  }}
/>

```tsx
const readiness = useMcpServerReadiness(org, member.slug);
if (readiness.kind === "sign-in-needed") return <button onClick={readiness.signIn}>Sign in</button>;
```


### usePlugin

Data hook that fetches one installed Plugin by organization and slug.

Wraps `stigmer.plugin.getByReference()` with loading, error and
not-found state. Pass `null` for `org` or `slug` to skip fetching
(stable no-op). A plugin that is not installed resolves to `plugin ===
null` with no error; `plugin === null && !isLoading && !error` is how a
consumer reads "not installed", the same three-field rule every detail
hook in this package follows.

**Signature**

```tsx
function usePlugin(org: string | null, slug: string | null): UsePluginReturn
```

<TypeTable
  type={{
    org: { type: "string | null", description: "", required: true },
    slug: { type: "string | null", description: "", required: true },
  }}
/>

**Returns** — `UsePluginReturn`

<TypeTable
  type={{
    error: { type: "Error | null", description: "Error from the last failed request, or `null` when healthy." },
    isLoading: { type: "boolean", description: "`true` while the initial fetch or a refetch is in flight." },
    isRefetching: { type: "boolean", description: "`true` while a background refetch is in flight." },
    plugin: { type: "Plugin | null", description: "The installed plugin, or `null` while loading, on error, or when not installed." },
    refetch: { type: "() => void", description: "Discard cached data and re-fetch the plugin from the server." },
  }}
/>

```tsx
const { plugin, isLoading } = usePlugin("acme", "thermos");
```


### usePluginCount

Data hook that fetches the total count of installed plugins, for summary
cards and badges. Issues a minimal `stigmer.plugin.list()` call for the
count alone. Pass `null` as `org` to skip fetching.

**Signature**

```tsx
function usePluginCount(org: string | null, options?: UsePluginCountOptions): UsePluginCountReturn
```

<TypeTable
  type={{
    org: { type: "string | null", description: "", required: true },
    options: { type: "UsePluginCountOptions", description: "" },
  }}
/>

**Returns** — `UsePluginCountReturn`

<TypeTable
  type={{
    count: { type: "number | undefined", description: "Total number of plugins matching the filters; `undefined` until the first fetch completes." },
    error: { type: "Error | null", description: "Error from the last failed request, or `null` when healthy." },
    isLoading: { type: "boolean", description: "`true` while the count fetch is in flight." },
    refetch: { type: "() => void", description: "Discard cached data and re-fetch the count from the server." },
  }}
/>


### usePluginList

Data hook that fetches a paginated list of installed plugins for the
Library. Wraps `stigmer.plugin.list()` (the search-backed listing every
indexed kind shares) with pagination and text search over the
organization's plugins; the consumer owns page state and query debouncing.

Pass `null` as `org` to skip fetching (stable no-op).

**Signature**

```tsx
function usePluginList(org: string | null, options?: UsePluginListOptions): UsePluginListReturn
```

<TypeTable
  type={{
    org: { type: "string | null", description: "", required: true },
    options: { type: "UsePluginListOptions", description: "" },
  }}
/>

**Returns** — `UsePluginListReturn`

<TypeTable
  type={{
    currentPage: { type: "number", description: "The current page number (mirrors the `page` option)." },
    error: { type: "Error | null", description: "Error from the last failed request, or `null` when healthy." },
    isLoading: { type: "boolean", description: "`true` while the initial fetch or a refetch is in flight." },
    plugins: { type: "readonly SearchResult[]", description: "Paginated plugin entries for the current page." },
    refetch: { type: "() => void", description: "Discard cached data and re-fetch the current page from the server." },
    totalCount: { type: "number", description: "Total number of plugins matching the current filters." },
    totalPages: { type: "number", description: "Total pages available at the current page size." },
  }}
/>

```tsx
const { plugins, totalCount } = usePluginList("acme", { page: 1, pageSize: 20 });
```


### usePluginMembers

Data hook that lists the resources an installed plugin owns.

Members are derived on the server from the plugin label on the four child
kinds; nothing is stored on the plugin, so this list is always the truth
about what the plugin currently holds. Pass `null` to skip fetching.

**Signature**

```tsx
function usePluginMembers(pluginId: string | null): UsePluginMembersReturn
```

<TypeTable
  type={{
    pluginId: { type: "string | null", description: "", required: true },
  }}
/>

**Returns** — `UsePluginMembersReturn`

<TypeTable
  type={{
    byKind: { type: "PluginMembersByKind", description: "", typeDescriptionLink: "#pluginmembersbykind" },
    error: { type: "Error | null", description: "Error from the last failed request, or `null` when healthy." },
    isLoading: { type: "boolean", description: "`true` while the initial fetch or a refetch is in flight." },
    members: { type: "readonly PluginMember[]", description: "Every member in materialisation order: skills, MCP servers, agents, workflows." },
    refetch: { type: "() => void", description: "Discard cached data and re-fetch from the server." },
  }}
/>

```tsx
const { byKind } = usePluginMembers(plugin.metadata.id);
byKind.agents[0] // the agent to start a session on, when the plugin carries one
```


### usePluginPresentation

The presentation of `entry` in `opened`; idle (and `{}`) when either is `null`.

**Signature**

```tsx
function usePluginPresentation(opened: OpenedMarketplace | null, entry: MarketplaceEntry | null): UsePluginPresentationReturn
```

<TypeTable
  type={{
    opened: { type: "OpenedMarketplace | null", description: "", required: true },
    entry: { type: "MarketplaceEntry | null", description: "", required: true },
  }}
/>

**Returns** — `UsePluginPresentationReturn`

<TypeTable
  type={{
    isLoading: { type: "boolean", description: "`true` until the manifest has been read once." },
    logoUrl: { type: "string | null", description: "The logo's URL at the tree's commit or version, or `null` when the plugin names none the tree can show." },
    presentation: { type: "PluginPresentation", description: "What the manifest says; every field optional, `{}` until read or when the manifest says nothing." },
  }}
/>

```tsx
const { presentation, logoUrl } = usePluginPresentation(opened, entry);
const title = presentation.displayName ?? entry.name;
```


### usePluginUpload

**Signature**

```tsx
function usePluginUpload(): UsePluginUploadReturn
```

**Returns** — `UsePluginUploadReturn`

<TypeTable
  type={{
    fromDrop: { type: "(items: DataTransferItemList, files: FileList) => Promise<void>", description: "The items of a drop: a directory is walked, a `.zip` is read, anything else is refused." },
    fromFolderInput: { type: "(files: ArrayLike<File>) => Promise<void>", description: "The files of an `<input type=\"file\" webkitdirectory multiple>` pick." },
    fromZip: { type: "(file: File) => Promise<void>", description: "One `.zip` file, from an input or a drop." },
    phase: { type: "PluginUploadPhase", description: "", typeDescriptionLink: "#pluginuploadphase" },
    reset: { type: "() => void", description: "Back to the drop zone." },
  }}
/>


### usePluginVersions

Data hook that fetches the version history of an installed plugin and
maps it to the generic [`VersionEntry`](/docs/sdk/react/version-history#versionentry) the `VersionTimeline` renders.
A version's id is its archive digest; its tag is the manifest version
when that fits the tag pattern. Pass `null` to skip fetching.

**Signature**

```tsx
function usePluginVersions(org: string | null, slug: string | null): UsePluginVersionsReturn
```

<TypeTable
  type={{
    org: { type: "string | null", description: "", required: true },
    slug: { type: "string | null", description: "", required: true },
  }}
/>

**Returns** — `UsePluginVersionsReturn`

<TypeTable
  type={{
    error: { type: "Error | null", description: "Error from the last failed request, or `null`." },
    isEmpty: { type: "boolean", description: "`true` when the plugin has no recorded versions." },
    isLoading: { type: "boolean", description: "`true` while the initial fetch is in flight." },
    refetch: { type: "() => void", description: "Discard cached data and re-fetch from the server." },
    versions: { type: "readonly VersionEntry[]", description: "Version entries mapped to the generic timeline format (newest first)." },
  }}
/>


### usePreparePluginInstall

Behaviour hook that turns a source's entry into the push the server
expects, and says whether the organization already holds it.

The entry's subtree is handed to the one preparation every client runs
(the ignore files, the selection, then only the selected files, read at
the tree's commit), so `prepared.digest` is the digest `stigmer install`
would compute for the same tree and the digest the server records after
the push. The relation to the org's plugin of the same name is
`useInstallRelation`'s, shared with the upload path.

Pass `null` for `opened`, `entryName` or `org` to skip (stable no-op).

**Signature**

```tsx
function usePreparePluginInstall(opened: OpenedMarketplace | null, entryName: string | null, org: string | null): UsePreparePluginInstallReturn
```

<TypeTable
  type={{
    opened: { type: "OpenedMarketplace | null", description: "", required: true },
    entryName: { type: "string | null", description: "", required: true },
    org: { type: "string | null", description: "", required: true },
  }}
/>

**Returns** — `UsePreparePluginInstallReturn`

<TypeTable
  type={{
    error: { type: "Error | null", description: "The reader's refusal (every sentence) or the source's, or `null`." },
    installed: { type: "Plugin | null", description: "The org's plugin of the same name, or `null` when none is installed." },
    isPreparing: { type: "boolean", description: "`true` while the entry's files are fetched and read, or the org is asked." },
    prepared: { type: "PreparedInstall | null", description: "The entry read, selected and archived; `null` while preparing or on refusal." },
    refetch: { type: "() => void", description: "Fetch and read the entry again." },
    relation: { type: "InstallRelation | null", description: "" },
  }}
/>


## Components

### AddToolsToAgentDialog

Adds a plugin's MCP servers to an existing agent, or hands the host the
servers for a new one.

**Props** — `AddToolsToAgentDialogProps`

<TypeTable
  type={{
    className: { type: "string", description: "" },
    onAgentClick: { type: "(ref: ResourceRef) => void", description: "Called with the updated agent's reference after a successful add, from \"Open agent\". The host owns the route." },
    onClose: { type: "() => void", description: "", required: true },
    onCreateAgent: { type: "(usages: readonly McpServerUsageInput[]) => void", description: "Called from \"Create a new agent with these tools\" with the usages a wizard preselects. The host owns the route; the link is hidden when omitted." },
    open: { type: "boolean", description: "", required: true },
    org: { type: "string", description: "The organization whose agents are offered.", required: true },
    servers: { type: "readonly AddableServer[]", description: "The servers to add, as the plugin installed them.", required: true },
  }}
/>

```tsx
<AddToolsToAgentDialog
  org={org}
  servers={servers}
  open={adding}
  onClose={() => setAdding(false)}
  onAgentClick={({ org, slug }) => navigateToDetail("agents", org, slug)}
  onCreateAgent={(usages) => router.push(`/library/agents/new?mcp=${usages.map((u) => u.mcpServerRef.slug).join(",")}`)}
/>
```


### InstallPreview

The `Plugin`, `Installs`, `Warnings` and `Not installed` blocks, then the relation sentence.

**Props** — `InstallPreviewProps`

<TypeTable
  type={{
    className: { type: "string", description: "" },
    prepared: { type: "PreparedInstall", description: "", required: true, typeDescriptionLink: "#preparedinstall" },
    relation: { type: "InstallRelation | null", description: "How the archive relates to the org's plugin of the same name; `null` while unknown.", required: true },
  }}
/>


### ManagedByPluginNotice

States that a resource was installed by a plugin and is changed by pushing the plugin again.

**Props** — `ManagedByPluginNoticeProps`

<TypeTable
  type={{
    className: { type: "string", description: "" },
    onPluginClick: { type: "(ref: { org: string; slug: string }) => void", description: "Called when the plugin's name is selected; the host owns the route to the plugin's page." },
    plugin: { type: "Plugin | null", description: "The plugin that installed the resource; `null` renders nothing.", required: true },
  }}
/>


### ManageSourcesDialog

**Props** — `ManageSourcesDialogProps`

<TypeTable
  type={{
    add: { type: "(sourceText: string, name?: string) => Promise<AddSourceOutcome>", description: "", required: true },
    isBuiltIn: { type: "(name: string) => boolean", description: "", required: true },
    marketplaces: { type: "readonly KnownMarketplace[]", description: "", required: true },
    onClose: { type: "() => void", description: "", required: true },
    open: { type: "boolean", description: "", required: true },
    reads: { type: "readonly SourceRead[]", description: "The catalogue reads, so each row can say what its source came to; a source without a read shows its identity alone." },
    remove: { type: "(name: string) => string | null", description: "", required: true },
    unreadable: { type: "readonly UnreadableMarketplace[]", description: "", required: true },
  }}
/>


### MarketplaceCatalog

Browse every known source and install a plugin from any of them, or upload one.

**Props** — `MarketplaceCatalogProps`

<TypeTable
  type={{
    className: { type: "string", description: "Additional CSS classes for the root container." },
    fetchImpl: { type: "(input: URL | RequestInfo, init?: RequestInit) => Promise<Response>", description: "The HTTP client the sources are read through; `globalThis.fetch` when absent. Tests inject a fake." },
    onInstalled: { type: "(outcome: InstallPluginOutcome) => void", description: "Called after a plugin is installed, from a card or from the Upload tile; the host navigates to its page." },
    org: { type: "string", description: "The organization plugins are installed into, and whose installed plugins mark the cards.", required: true },
  }}
/>

```tsx
<MarketplaceCatalog
  org={org}
  onInstalled={({ plugin }) => navigateToDetail("plugins", org, plugin.metadata.slug)}
/>
```


### McpServerReadiness

Renders [`useMcpServerReadiness`](#usemcpserverreadiness) for one server.

**Props** — `McpServerReadinessProps`

<TypeTable
  type={{
    className: { type: "string", description: "" },
    keysAskedAt: { type: "\"connect\" | \"agent\"", description: "Where an `api-key` server's variables are given. `\"agent\"` when the plugin installed an agent that declares them (the composer asks at the agent's first session); `\"connect\"` otherwise (they are set when the server is connected).", required: true },
    onSignedIn: { type: "(mcpServerId: string) => void", description: "Called with the server's id when a sign-in started from this cell lands; the composer records it against the agent." },
    org: { type: "string", description: "The organization the server lives in.", required: true },
    slug: { type: "string", description: "The server's slug.", required: true },
  }}
/>

```tsx
<McpServerReadiness org="acme" slug="linear" keysAskedAt="connect" />
```


### PluginCard

**Props** — `PluginCardProps`

<TypeTable
  type={{
    className: { type: "string", description: "" },
    entry: { type: "CatalogEntry", description: "", required: true, typeDescriptionLink: "#catalogentry" },
    installed: { type: "boolean", description: "Whether the organization already holds a plugin of this name.", required: true },
    onInstall: { type: "(entry: CatalogEntry) => void", description: "", required: true },
  }}
/>


### PluginDetailView

Operational detail hub for an installed Plugin: what it installed, what
its manifest declares, what the server warned about, and its versions.

**Props** — `PluginDetailViewProps`

<TypeTable
  type={{
    actions: { type: "readonly DetailAction[]", description: "Secondary actions rendered in the kebab overflow menu (remove lives here)." },
    activeTab: { type: "string", description: "" },
    additionalTabs: { type: "readonly AdditionalTab[]", description: "Additional tabs beside the built-in Overview and Versions." },
    className: { type: "string", description: "Additional CSS classes for the root container." },
    defaultTab: { type: "string", description: "" },
    onAgentClick: { type: "(ref: PluginMemberRef) => void", description: "" },
    onCreateAgent: { type: "(usages: readonly McpServerUsageInput[]) => void", description: "Called from \"Create a new agent with these tools\" with the usages the creation wizard preselects, for a plugin that installed MCP servers and no agent. The host owns the route to its wizard; the link is hidden when omitted, and \"Add to an agent\" still offers the organization's agents." },
    onMcpServerClick: { type: "(ref: PluginMemberRef) => void", description: "" },
    onResourceLoad: { type: "(meta: { id: string; name: string }) => void", description: "Called once when the plugin has been fetched, for breadcrumbs and titles. Not called on error or not-found." },
    onSkillClick: { type: "(ref: PluginMemberRef) => void", description: "Called when a member row is selected; the host owns the route to each kind's page." },
    onStartSession: { type: "(agent: PluginMemberRef) => void", description: "Called when the user asks to start a session on the plugin's agent. When provided and the plugin carries an agent, \"Start session\" is the header's primary action; the host owns the route." },
    onTabChange: { type: "(tabId: string) => void", description: "" },
    onWorkflowClick: { type: "(ref: PluginMemberRef) => void", description: "" },
    org: { type: "string", description: "Organization slug the plugin is installed in.", required: true },
    slug: { type: "string", description: "Plugin slug (the plugin's name).", required: true },
  }}
/>

```tsx
<PluginDetailView
  org="acme"
  slug="thermos"
  onStartSession={({ org, slug }) => router.push(getAgentSessionUrl(org, slug))}
  onSkillClick={({ org, slug }) => navigateToDetail("skills", org, slug)}
  onMcpServerClick={({ org, slug }) => navigateToDetail("mcp-servers", org, slug)}
  onAgentClick={({ org, slug }) => navigateToDetail("agents", org, slug)}
  onCreateAgent={(usages) => router.push(`/library/agents/new?mcp=${usages.map((u) => u.mcpServerRef.slug).join(",")}`)}
/>
```


### PluginFace

**Props** — `PluginFaceProps`

<TypeTable
  type={{
    className: { type: "string", description: "" },
    displayName: { type: "string", description: "The name for people; its first letter is the monogram.", required: true },
    logoUrl: { type: "string | null", description: "The logo's URL, or `null` for the monogram.", required: true },
    name: { type: "string", description: "The install name; the hash key for the monogram's colour, so a display-name edit does not recolour it.", required: true },
  }}
/>


### PluginIcon

The plugin glyph: a puzzle piece, the icon Cursor, Claude Code and Codex
all use for the same package, drawn inline like `SkillDetailView`'s bolt
so the SDK adds no icon dependency for one shape.


### PluginInstallDialog

Previews a source's entry as the CLI describes it and installs it on
confirmation.

**Props** — `PluginInstallDialogProps`

<TypeTable
  type={{
    className: { type: "string", description: "" },
    entryName: { type: "string | null", description: "The name of the entry to install; `null` closes the dialog.", required: true },
    onClose: { type: "() => void", description: "", required: true },
    onInstalled: { type: "(outcome: InstallPluginOutcome) => void", description: "Called after a successful install with the plugin and its members." },
    open: { type: "boolean", description: "", required: true },
    opened: { type: "OpenedMarketplace | null", description: "The opened source the entry belongs to; `null` closes the dialog.", required: true },
    org: { type: "string", description: "The organization to install into.", required: true },
    sourceName: { type: "string", description: "The source's name as the user knows it (`acme-plugins`), kept in the version message.", required: true },
  }}
/>

```tsx
<PluginInstallDialog
  opened={opened}
  entryName={selected}
  sourceName="acme-plugins"
  org={org}
  open={selected !== null}
  onClose={() => setSelected(null)}
  onInstalled={({ plugin }) => navigateToDetail("plugins", org, plugin.metadata.slug)}
/>
```


### PluginUploader

Upload a plugin folder or zip and install it.

**Props** — `PluginUploaderProps`

<TypeTable
  type={{
    className: { type: "string", description: "" },
    onCancel: { type: "() => void", description: "" },
    onComplete: { type: "(outcome: InstallPluginOutcome) => void", description: "Called after a successful install; the host navigates to the plugin's page." },
    org: { type: "string", description: "The organization to install into.", required: true },
  }}
/>

```tsx
<PluginUploader org={org} onComplete={({ plugin }) => navigateToDetail("plugins", org, plugin.metadata.slug)} />
```


### PrepareRefusal

The reader's refusal with every sentence, the way `validate -f` prints it; any other error as it is.


### SourceChips

**Props** — `SourceChipsProps`

<TypeTable
  type={{
    className: { type: "string", description: "" },
    onManage: { type: "() => void", description: "", required: true },
    onSelect: { type: "(name: string | null) => void", description: "", required: true },
    reads: { type: "readonly SourceRead[]", description: "", required: true },
    selected: { type: "string | null", description: "The selected source's name, or `null` for every source.", required: true },
  }}
/>


### SourceMark

**Props** — `SourceMarkProps`

<TypeTable
  type={{
    className: { type: "string", description: "" },
    size: { type: "\"md\" | \"sm\"", description: "`sm` for a chip or a card footer, `md` for a list row." },
    source: { type: "MarketplaceSource", description: "", required: true, typeDescriptionLink: "#marketplacesource" },
  }}
/>


### UploadTile

**Props** — `UploadTileProps`

<TypeTable
  type={{
    className: { type: "string", description: "" },
    onUpload: { type: "() => void", description: "", required: true },
  }}
/>


## Types

### AddableServer

A server the dialog offers, by reference and by the name the user reads.

<TypeTable
  type={{
    name: { type: "string", description: "", required: true },
    ref: { type: "ResourceRef", description: "", required: true },
  }}
/>


### AddSourceOutcome

What `add` comes to: the source recorded under `name`, or the sentence refusing it.


### AddToolsPhase

Where the flow stands.


### BUILT_IN_SOURCES

The sources every console ships with, in listing order: the CLI's list, in the console's type.


### CATALOG_PAGE_SIZE

Cards per page: three columns by eight rows at the widest layout, so a page is one screen and a half.


### catalogEntries

Every entry the ready sources offer, in source order then catalogue order.

```tsx
function catalogEntries(reads: readonly SourceRead[]): readonly CatalogEntry[]
```


### CatalogEntry

One plugin a grid can show: the entry, and where it came from.

<TypeTable
  type={{
    entry: { type: "MarketplaceEntry", description: "", required: true },
    opened: { type: "OpenedMarketplace", description: "", required: true, typeDescriptionLink: "#openedmarketplace" },
    source: { type: "KnownMarketplace", description: "", required: true, typeDescriptionLink: "#knownmarketplace" },
  }}
/>


### describeOrigin

One line saying where the archive came from, for a preview's header.

```tsx
function describeOrigin(origin: InstallOrigin): string
```


### describeSource

How a source is named in a list row: "built in" for the official catalogue, the repository for a GitHub one.

```tsx
function describeSource(source: MarketplaceSource): string
```


### entryCandidates

The entry's subtree as a lazily-read tree: plugin-relative paths with
their declared sizes, each fetched from the host on demand. One shape for
the install (`prepareEntry`) and for the card's face (the presentation
read), so both see the same files at the same commit.

```tsx
function entryCandidates(opened: OpenedMarketplace, entry: MarketplaceEntry): readonly LazyCandidate[]
```


### FetchImpl

The HTTP client a source reads through; injectable so tests run with none.


### filterEntries

The entries whose name or description contains `query`, case-insensitively; every entry for an empty query.

```tsx
function filterEntries(entries: readonly MarketplaceEntry[], query: string): readonly MarketplaceEntry[]
```


### findEntry

The offered entry called `name`, or `undefined`.

```tsx
function findEntry(marketplace: Marketplace, name: string): MarketplaceEntry | undefined
```


### folderPick

A folder the component walked itself (a drop), already as root-relative pairs.

```tsx
function folderPick(name: string, files: readonly LocalFile[]): LocalPick
```


### folderPickFromInput

The `File`s of an `<input type="file" webkitdirectory>` pick. Every path
begins with the picked folder's own name, which is stripped so the tree
is rooted where the manifest is.

```tsx
function folderPickFromInput(files: ArrayLike<File>): LocalPick
```


### formatMib

`bytes` as the MiB figure every size sentence quotes, one decimal.

```tsx
function formatMib(bytes: number): string
```


### githubAvatarUrl

The public avatar of the GitHub account `owner`, at a size a chip or a
card mark renders. This is how a source is identified visually: the
account that publishes a catalogue shows its own picture, for the
built-in vendors and for any repository a user adds alike, so the SDK
ships no vendor artwork and a new source needs no new asset.

```tsx
function githubAvatarUrl(owner: string, size: number): string
```


### githubOwner

The account half of an `owner/repo` slug.

```tsx
function githubOwner(repo: string): string
```


### InstallOrigin

Where a prepared install came from: a source's entry, or something the
user handed the browser. The preview says it in one line; the push's
version message records the source's name or "uploaded from the console".


### InstallPluginOptions

What an install needs beyond the prepared archive.

<TypeTable
  type={{
    installedFrom: { type: "string", description: "The source the archive came from, kept in the version message as `stigmer install` keeps it; omitted for an upload, whose message says so, as `stigmer push plugin` says nothing about a source." },
    org: { type: "string", description: "The organization the plugin is installed into.", required: true },
    visibility: { type: "ApiResourceVisibility", description: "Visibility for the plugin and every resource it materialises; the kind's default when omitted." },
  }}
/>


### InstallPluginOutcome

The install as the server reports it: the plugin and the members it now holds.

<TypeTable
  type={{
    members: { type: "readonly PluginMember[]", description: "", required: true },
    plugin: { type: "Plugin", description: "", required: true },
  }}
/>


### InstallRelation

- `not-installed`: no plugin of this name in the org.
- `installed`: the org's plugin carries this exact digest and is ready.
- `upgrade`: the org's plugin exists with another digest (or is not
  ready); a push replaces its members.


### isPublishedVersion

Whether `version` names a release the catalogue is published for: the shared rule, under this module's older name.

```tsx
function isPublishedVersion(version: string): boolean
```


### KnownMarketplace

A marketplace the console can read, named as the user sees it.

<TypeTable
  type={{
    name: { type: "string", description: "", required: true },
    source: { type: "MarketplaceSource", description: "", required: true, typeDescriptionLink: "#marketplacesource" },
  }}
/>


### LocalFile

One file of a local pick, at the path it has inside the plugin's root.

<TypeTable
  type={{
    file: { type: "File", description: "", required: true },
    path: { type: "string", description: "", required: true },
  }}
/>


### LocalPick

What the user handed over, before preparation.

<TypeTable
  type={{
    candidates: { type: "readonly LazyCandidate[]", description: "", required: true },
    kind: { type: "\"folder\" | \"zip\"", description: "", required: true },
    name: { type: "string", description: "The folder's or the archive's name, for sentences.", required: true },
    rerooted: { type: "string", description: "The one directory a zip wrapped its contents in, stripped before reading; `undefined` when none." },
  }}
/>


### looksLikeDroppedDotfiles

Whether `error` is the reader's "no manifest" refusal of a folder pick: the shape a browser that drops dotfiles produces.

```tsx
function looksLikeDroppedDotfiles(phase: PluginUploadPhase): boolean
```


### MARKETPLACE_TREE_LIMITS

The caps a hosted tree must fit under; each refusal names the one it hit.


### MARKETPLACES_STORAGE_KEY

The storage key; the version suffix changes when the entry shape does.


### MarketplaceSource

Where a marketplace tree comes from.


### MarketplaceTree

A marketplace tree the console can read: every file the host lists, with
its size, a way to fetch one, and the URL a browser would fetch it from.
`fetchFile` returns exactly `size` bytes or rejects; the tree is read at
one commit, so the answer cannot change between the listing and the read.
`fileUrl` names the same immutable file, for an `<img>` the browser loads
itself (a card's logo) rather than bytes through JavaScript.

<TypeTable
  type={{
    describe: { type: "string", description: "One phrase naming the tree for sentences, e.g. `github.com/cursor/plugins@abc123`.", required: true },
    fetchFile: { type: "(path: string) => Promise<Uint8Array<ArrayBufferLike>>", description: "", required: true },
    files: { type: "readonly { path: string; size: number }[]", description: "Root-relative POSIX paths and declared sizes; files only.", required: true },
    fileUrl: { type: "(path: string) => string", description: "The URL of `path` at the commit or version this tree was read at; not checked against the listing.", required: true },
  }}
/>


### matchesQuery

Whether `entry`'s name or description contains `query`, case-insensitively; everything matches an empty query.

```tsx
function matchesQuery(entry: MarketplaceEntry, query: string): boolean
```


### McpServerReadinessKind

One of the four things a server can want before its first tool call.


### monogramRing

A stable small hash: the same name, the same colour, across visits and clients.

```tsx
function monogramRing(name: string): "stg:border-chart-1" | "stg:border-chart-2" | "stg:border-chart-3" | "stg:border-chart-4" | "stg:border-chart-5"
```


### narrowStoredEntry

The loose stored entry as a source, or the reason it is not one (the CLI's `narrowEntry`).

```tsx
function narrowStoredEntry(entry: unknown): Narrowed
```


### OFFICIAL_MARKETPLACE

The official catalogue, listed first everywhere.


### OFFICIAL_PUBLISHER

The GitHub account the official catalogue is published from
(`github.com/stigmer/stigmer`, `plugins/`), so its mark follows the one
rule every source's mark follows: the publisher's own avatar.


### officialFileUrl

The content URL of `path` in the catalogue at `version`.

```tsx
function officialFileUrl(version: string, path: string): string
```


### officialListingUrl

The listing URL for the catalogue at `version`.

```tsx
function officialListingUrl(version: string): string
```


### OpenedMarketplace

A marketplace read from a hosted tree, with the tree kept for the install that follows.

<TypeTable
  type={{
    marketplace: { type: "Marketplace", description: "", required: true },
    tree: { type: "MarketplaceTree", description: "", required: true, typeDescriptionLink: "#marketplacetree" },
    warnings: { type: "readonly MarketplaceFinding[]", description: "Entries the reader dropped, each with its own sentence (`entry-not-a-plugin` and the rest).", required: true },
  }}
/>


### openGitHubTree

Open the repository at its ref as a tree the reader can select from.

```tsx
function openGitHubTree(source: GitHubMarketplaceSource, fetchImpl: (input: URL | RequestInfo, init?: RequestInit) => Promise<Response>): Promise<MarketplaceTree>
```


### openMarketplace

Read the catalogue a tree carries.

```tsx
function openMarketplace(tree: MarketplaceTree): Promise<OpenedMarketplace>
```


### openMarketplaceSource

Open and read the marketplace `source` names.

```tsx
function openMarketplaceSource(source: MarketplaceSource, stigmer: Stigmer, options: OpenSourceOptions): Promise<OpenedMarketplace>
```


### openOfficialTree

Open the catalogue the server's release ships.

```tsx
function openOfficialTree(serverVersion: string, fetchImpl: (input: URL | RequestInfo, init?: RequestInit) => Promise<Response>): Promise<MarketplaceTree>
```


### OpenSource

Opens a source; the store is agnostic of how (the hook binds the client and the fetch).


### OpenSourceOptions

<TypeTable
  type={{
    fetchImpl: { type: "(input: URL | RequestInfo, init?: RequestInit) => Promise<Response>", description: "The HTTP client the sources read through; `globalThis.fetch` when absent." },
  }}
/>


### PLUGIN_LABEL

The label a plugin stamps on every resource it installs; its value is the
plugin's id. Written only by the server's plugin controller; a client
reads it to know a resource is a plugin's to redefine.


### pluginMemberKindLabel

The vocabulary's word for each member kind; the CLI's install summary uses the same.

```tsx
function pluginMemberKindLabel(kind: ApiResourceKind): string
```


### PluginMemberRef

A member's reference as the host navigates to it.

<TypeTable
  type={{
    org: { type: "string", description: "", required: true },
    slug: { type: "string", description: "", required: true },
  }}
/>


### PluginMembersByKind

The members of a plugin grouped by kind, in the order the server materialises them.

<TypeTable
  type={{
    agents: { type: "readonly PluginMember[]", description: "", required: true },
    mcpServers: { type: "readonly PluginMember[]", description: "", required: true },
    skills: { type: "readonly PluginMember[]", description: "", required: true },
    workflows: { type: "readonly PluginMember[]", description: "", required: true },
  }}
/>


### PluginUploadPhase


### PreparedInstall

A plugin read, selected and archived, but not yet pushed: what the install preview shows.

<TypeTable
  type={{
    archive: { type: "Uint8Array", description: "The exact bytes a push sends.", required: true },
    digest: { type: "string", description: "SHA-256 of `archive`: the identity the server records, so \"already installed\" is known before a push.", required: true },
    origin: { type: "InstallOrigin", description: "", required: true, typeDescriptionLink: "#installorigin" },
    plugin: { type: "PluginPackage", description: "", required: true },
    stats: { type: "SelectionStats", description: "", required: true },
    warnings: { type: "readonly PluginFinding[]", description: "", required: true },
  }}
/>


### prepareEntry

Prepare the entry through the one preparation every client runs. The
declared sizes are checked before a byte moves (a fast refusal for a
subtree no selection could bring under the cap); the shared preparation
then reads the ignore files, selects, checks the selected size, and
fetches only the files the archive carries, each length-checked against
its declaration by the tree itself.

```tsx
function prepareEntry(opened: OpenedMarketplace, entry: MarketplaceEntry): Promise<PreparedInstall>
```


### prepareLocalPlugin

Prepare the pick through the one preparation every client runs. The
console's archive cap applies to the selected files, so a folder whose
ignored contents are large is not refused for them.

```tsx
function prepareLocalPlugin(pick: LocalPick): Promise<PreparedInstall>
```


### publisherOf

The GitHub account that publishes `source`.

```tsx
function publisherOf(source: MarketplaceSource): string
```


### rawUrl

The raw content URL of `path` at `commit`.

```tsx
function rawUrl(repo: string, commit: string, path: string): string
```


### sourceKeyOf

One string per source identity, so a re-added name with another repository reads again.

```tsx
function sourceKeyOf(source: MarketplaceSource): string
```


### SourceRead

One source and its read.

<TypeTable
  type={{
    marketplace: { type: "KnownMarketplace", description: "", required: true, typeDescriptionLink: "#knownmarketplace" },
    state: { type: "SourceReadState", description: "", required: true, typeDescriptionLink: "#sourcereadstate" },
  }}
/>


### SourceReadState

Where one source's read stands.


### summariseInstall

"2 skills, 1 MCP server, 1 agent": what the push produced, in the CLI's
words. A kind the plugin did not install is not named: most of the
catalogue installs tools alone, and "0 skills, 1 MCP server, 0 agents"
reads as three facts where there is one. A push that produced nothing
says so.

```tsx
function summariseInstall(outcome: InstallPluginOutcome): string
```


### treesUrl

The Trees API URL for `source` (one call, recursive).

```tsx
function treesUrl(source: GitHubMarketplaceSource): string
```


### UnreadableMarketplace

A remembered entry this browser cannot read; listed with its reason so the user can remove it.

<TypeTable
  type={{
    name: { type: "string", description: "", required: true },
    reason: { type: "string", description: "", required: true },
  }}
/>


### UseMarketplaceCatalogOptions

Options for [`useMarketplaceCatalog`](#usemarketplacecatalog).

<TypeTable
  type={{
    fetchImpl: { type: "(input: URL | RequestInfo, init?: RequestInit) => Promise<Response>", description: "The HTTP client the sources are read through; `globalThis.fetch` when absent. Tests inject a fake." },
  }}
/>


### UseMarketplaceOptions

Options for [`useMarketplace`](#usemarketplace).

<TypeTable
  type={{
    fetchImpl: { type: "(input: URL | RequestInfo, init?: RequestInit) => Promise<Response>", description: "The HTTP client the marketplace hosts are read through; `globalThis.fetch` when absent. Tests inject a fake." },
  }}
/>


### UseMarketplacesOptions

<TypeTable
  type={{
    fetchImpl: { type: "(input: URL | RequestInfo, init?: RequestInit) => Promise<Response>", description: "The HTTP client `add` reads a source with; `globalThis.fetch` by default, a fake in tests." },
  }}
/>


### UsePluginCountOptions

Options for [`usePluginCount`](#useplugincount).

<TypeTable
  type={{
    query: { type: "string", description: "Text query to filter plugins before counting." },
    refetchToken: { type: "unknown", description: "Opaque token that forces a recount when its value changes." },
  }}
/>


### UsePluginListOptions

Options for [`usePluginList`](#usepluginlist).

<TypeTable
  type={{
    page: { type: "number", description: "Page number (1-indexed)." },
    pageSize: { type: "number", description: "Maximum plugins per page." },
    query: { type: "string", description: "Text query to filter plugins by name, description or keywords." },
  }}
/>


### zipPick

A `.zip` the user picked, inflated and re-rooted; its bytes are all in memory once read.

```tsx
function zipPick(file: File): Promise<LocalPick>
```

