---
title: Version History
description: Components for resource version diffs, timeline, and rollback controls.
---

{/* Auto-generated by @stigmer/react docs generator. DO NOT EDIT. */}

## Components

### DiffFileList

File list with change-type indicators for a multi-file diff.

Each file shows its path, change type badge (M/A/D), and a compact
`+N -N` additions/deletions count. Files are keyboard-navigable.

Zero Console dependencies — safe for platform builder embedding.

**Props** — `DiffFileListProps`

<TypeTable
  type={{
    className: { type: "string", description: "Additional CSS classes for the root container." },
    files: { type: "readonly FileDiffEntry[]", description: "Changed files to display.", required: true },
    onSelect: { type: "(path: string) => void", description: "Called when a file is clicked.", required: true },
    selectedPath: { type: "string | null", description: "Currently selected file path.", required: true },
  }}
/>


### DiffSummary

Summary bar showing aggregate diff statistics.

Renders "N files changed, +X additions, -Y deletions" in a compact
single-line format. Uses diff-specific design tokens for coloring.

Zero Console dependencies — safe for platform builder embedding.

**Props** — `DiffSummaryProps`

<TypeTable
  type={{
    additions: { type: "number", description: "Total added lines across all files.", required: true },
    className: { type: "string", description: "Additional CSS classes for the root container." },
    deletions: { type: "number", description: "Total removed lines across all files.", required: true },
    fileCount: { type: "number", description: "Number of files with changes.", required: true },
  }}
/>


### DiffViewer

Generic unified diff renderer for a single file.

Renders a unified diff with line numbers, `+`/`-` prefix indicators,
and hunk separator headers. Uses `--stgm-diff-*` design tokens for
all colors — respects light/dark mode and preset overrides.

Accessibility: uses a `<table>` with proper column headers, `+`/`-`
text indicators alongside color (color is never the sole channel),
and ARIA labels on change lines for screen readers.

Zero Console dependencies — safe for platform builder embedding.

**Props** — `DiffViewerProps`

<TypeTable
  type={{
    className: { type: "string", description: "Additional CSS classes for the root container." },
    filePath: { type: "string", description: "File path displayed in the header." },
    hunks: { type: "readonly DiffHunk[]", description: "Hunks to render — output of `computeDiff()`.", required: true },
  }}
/>

```tsx
const hunks = computeDiff(oldText, newText);
<DiffViewer hunks={hunks} filePath="SKILL.md" />
```


### MultiFileDiffView

Composed multi-file diff view with file list and per-file unified diff.

Layout: summary bar at top, file list as a horizontal scrollable bar,
and the full unified diff for the selected file below. Designed to
work within a dialog or a detail page panel.

Zero Console dependencies — safe for platform builder embedding.

**Props** — `MultiFileDiffViewProps`

<TypeTable
  type={{
    className: { type: "string", description: "Additional CSS classes for the root container." },
    diff: { type: "MultiFileDiffResult", description: "Multi-file diff result from `computeMultiFileDiff()`.", required: true, typeDescriptionLink: "#multifilediffresult" },
    initialSelectedPath: { type: "string", description: "Initially selected file path. When omitted, defaults to the first changed file (SKILL.md if present, per the sort order)." },
  }}
/>

```tsx
const diff = computeMultiFileDiff(oldFiles, newFiles);
<MultiFileDiffView diff={diff} />
```


### VersionTimeline

Accessible vertical timeline for version history.

Renders a chronologically-ordered list of version entries with a
connecting line, selection state, and optional compare mode. Designed
as a generic SDK primitive usable for any versioned resource (skills,
and in future, agents, MCP servers).

Supports two interaction modes:
- **Select mode** (default): single-click selects an entry, fires `onEntrySelect`
- **Compare mode** (when `onCompare` is provided): selecting two entries fires
  `onCompare(fromId, toId)` — designed for T05-D diff viewer integration

All visual properties flow through `--stgm-*` design tokens.
Zero Console dependencies — safe for platform builder embedding.

**Props** — `VersionTimelineProps`

<TypeTable
  type={{
    className: { type: "string", description: "Additional CSS classes for the root container." },
    emptyMessage: { type: "string", description: "Message shown when `entries` is empty and not loading." },
    entries: { type: "readonly VersionEntry[]", description: "Ordered list of version entries (newest first).", required: true },
    isLoading: { type: "boolean", description: "`true` while version data is being fetched." },
    onCompare: { type: "(fromId: string, toId: string) => void", description: "Called when two entries are selected for comparison. The component manages compare-mode selection internally when this callback is provided — enabling T05-D integration." },
    onEntrySelect: { type: "(id: string) => void", description: "Called when a single entry is selected (clicked)." },
    selectedId: { type: "string", description: "Currently selected entry ID (controlled selection)." },
  }}
/>

```tsx
const { versions } = useSkillVersions(org, slug);

<VersionTimeline
  entries={versions}
  onEntrySelect={(id) => console.log("Selected:", id)}
/>
```


### VersionTimelineEntry

A single row in a version timeline.

Renders a version entry with: truncated hash badge, relative timestamp,
actor info, tag badge, "current" indicator, and git provenance link.
Supports keyboard activation and focus management.

All visual properties flow through `--stgm-*` design tokens.
Zero Console dependencies — safe for platform builder embedding.

**Props** — `VersionTimelineEntryProps`

<TypeTable
  type={{
    entry: { type: "VersionEntry", description: "The version entry data to render.", required: true, typeDescriptionLink: "#versionentry" },
    isCompareSource: { type: "boolean", description: "Whether this entry is the first pick in compare mode (dashed border, \"A\" badge)." },
    isLast: { type: "boolean", description: "Whether this is the last entry in the timeline (no connecting line below)." },
    isSelected: { type: "boolean", description: "Whether this entry is currently selected." },
    onSelect: { type: "() => void", description: "Called when this entry is clicked." },
    trailing: { type: "ReactNode", description: "Optional trailing content (e.g., action buttons)." },
  }}
/>

```tsx
<VersionTimelineEntry
  entry={version}
  isSelected={selectedId === version.id}
  onSelect={() => setSelectedId(version.id)}
/>
```


## Types

### computeDiff

Compute a unified diff between two text strings.

Returns an array of [`DiffHunk`](#diffhunk) objects, each containing the
changed and context lines with line numbers. Uses the `diff` library's
Myers algorithm internally, then maps the structured hunks through the
shared `mapPatchHunks` (the same mapper `parseUnifiedDiff` uses), so a
computed diff and a parsed unified diff render line-for-line identically.

Pure function — no side effects, safe to call outside React.

```tsx
function computeDiff(oldText: string, newText: string, contextLines: number): readonly DiffHunk[]
```


### computeMultiFileDiff

Compute a multi-file diff between two snapshots of a file tree.

Each snapshot is a `Map<path, textContent>`. The function categorises
every path as `added`, `removed`, or `modified`, computes per-file
unified diffs for modified files, and returns an aggregate result.

Pure function — no side effects, safe to call outside React.

```tsx
function computeMultiFileDiff(oldFiles: ReadonlyMap<string, string>, newFiles: ReadonlyMap<string, string>): MultiFileDiffResult
```


### DiffHunk

A contiguous group of changed/context lines in a unified diff.

<TypeTable
  type={{
    lines: { type: "readonly DiffLine[]", description: "", required: true },
    newLines: { type: "number", description: "", required: true },
    newStart: { type: "number", description: "", required: true },
    oldLines: { type: "number", description: "", required: true },
    oldStart: { type: "number", description: "", required: true },
  }}
/>


### DiffLine

A single line within a diff hunk.

<TypeTable
  type={{
    content: { type: "string", description: "", required: true },
    newLineNumber: { type: "number", description: "" },
    oldLineNumber: { type: "number", description: "" },
    type: { type: "\"context\" | \"added\" | \"removed\"", description: "", required: true },
  }}
/>


### DiffViewMode

Display mode for the diff viewer.


### FileDiffEntry

Summary of changes to a single file between two versions.

<TypeTable
  type={{
    additions: { type: "number", description: "", required: true },
    changeType: { type: "\"added\" | \"removed\" | \"modified\"", description: "", required: true },
    deletions: { type: "number", description: "", required: true },
    path: { type: "string", description: "", required: true },
  }}
/>


### MultiFileDiffResult

Aggregated diff result across all files in two version snapshots.

<TypeTable
  type={{
    files: { type: "readonly FileDiffEntry[]", description: "", required: true },
    getDiff: { type: "(path: string) => readonly DiffHunk[]", description: "Retrieve the hunk-level diff for a specific file path.", required: true },
    totalAdditions: { type: "number", description: "", required: true },
    totalDeletions: { type: "number", description: "", required: true },
  }}
/>


### VersionEntry

A single entry in a version timeline.

This is a generic presentation type — not tied to any specific resource.
Resource-specific hooks (e.g., `useSkillVersions`) map from proto types
to this shape, enabling the `VersionTimeline` component to render version
history for any resource type.

<TypeTable
  type={{
    actor: { type: "{ avatar?: string; displayName?: string; id: string }", description: "Actor who created this version." },
    gitProvenance: { type: "{ commit: string; ref: string; remoteUrl: string }", description: "Git provenance for traceability." },
    id: { type: "string", description: "Unique identifier for this version (e.g., content hash).", required: true },
    isCurrent: { type: "boolean", description: "Whether this is the currently active version." },
    label: { type: "string", description: "Primary display text (e.g., truncated content hash).", required: true },
    metadata: { type: "Readonly<Record<string, string>>", description: "Extensible key-value metadata for display." },
    sublabel: { type: "string", description: "Secondary text (e.g., commit message or version description)." },
    tag: { type: "string", description: "Version tag (e.g., \"stable\", \"v1.0\")." },
    timestamp: { type: "Date", description: "When this version was created/pushed.", required: true },
  }}
/>

