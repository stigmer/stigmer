---
title: channel-app
---

{/* Auto-generated by @stigmer/react docs generator. DO NOT EDIT. */}

## Hooks

### useChannelAppList

Data hook that fetches all `ChannelApp` entries for an
organization.

These are the customer-owned messaging-platform apps (bring-your-own
Slack apps) that agent channels can install through instead of the
shared Stigmer app. Secret fields arrive redacted.

Pass `null` for `org` to skip fetching (stable no-op). Useful when
the active organization has not been resolved yet.

**Signature**

```tsx
function useChannelAppList(org: string | null): UseChannelAppListReturn
```

<TypeTable
  type={{
    org: { type: "string | null", description: "", required: true },
  }}
/>

**Returns** — `UseChannelAppListReturn`

<TypeTable
  type={{
    channelApps: { type: "readonly ChannelApp[]", description: "Channel apps owned by the organization. Empty while loading or on error." },
    error: { type: "Error | null", description: "Error from the last failed request, or `null` when healthy." },
    isLoading: { type: "boolean", description: "`true` while the initial fetch or a refetch is in flight." },
    isRefetching: { type: "boolean", description: "`true` while a background refetch is in flight and stale data is shown." },
    refetch: { type: "() => void", description: "Discard cached data and re-fetch the list from the server." },
  }}
/>

```tsx
const { channelApps, isLoading, error } = useChannelAppList(org);

if (isLoading) return <Spinner />;
channelApps.map((app) => app.metadata?.name);
```


### useCreateChannelApp

Mutation hook that wraps `channelapp.create()` with loading and error
state.

Creates a channel app — a customer-owned messaging-platform app whose
credentials agent channels can install through instead of the shared
Stigmer app. The secret fields (`clientSecret`, `signingSecret`) are
encrypted server-side and come back redacted in the response.

**Signature**

```tsx
function useCreateChannelApp(): UseCreateChannelAppReturn
```

**Returns** — `UseCreateChannelAppReturn`

<TypeTable
  type={{
    clearError: { type: "() => void", description: "Reset `error` to `null`." },
    create: { type: "(input: ChannelAppInput) => Promise<ChannelApp>", description: "Submit a ChannelAppInput to create a new channel app. Resolves with the server-created resource." },
    error: { type: "Error | null", description: "Error from the last failed create, or `null` when healthy." },
    isCreating: { type: "boolean", description: "`true` while the create request is in flight." },
  }}
/>

```tsx
const { create, isCreating, error } = useCreateChannelApp();

const app = await create({
  name: "Acme Support Bot",
  org: "acme",
  slack: {
    clientId: "123456.789012",
    clientSecret: "…",
    signingSecret: "…",
  },
});
```


### useDeleteChannelApp

Mutation hook that wraps `channelapp.delete()` with loading and error
state.

Deletes a channel app by its resource ID. The server refuses
(`FAILED_PRECONDITION`) while any agent channel still references the
app via `spec.app_ref` — the error message names the fix; surface it
to the user rather than retrying.

**Signature**

```tsx
function useDeleteChannelApp(): UseDeleteChannelAppReturn
```

**Returns** — `UseDeleteChannelAppReturn`

<TypeTable
  type={{
    clearError: { type: "() => void", description: "Reset `error` to `null`." },
    deleteApp: { type: "(id: string) => Promise<ChannelApp>", description: "Delete a channel app by its resource ID. Resolves with the deleted resource for confirmation display." },
    error: { type: "Error | null", description: "Error from the last failed delete, or `null` when healthy." },
    isDeleting: { type: "boolean", description: "`true` while the delete request is in flight." },
  }}
/>

```tsx
const { deleteApp, isDeleting, error } = useDeleteChannelApp();

await deleteApp("chapp_abc123");
refetch(); // refresh the list after deletion
```


### useUpdateChannelApp

Mutation hook that wraps `channelapp.update()` with loading and error
state.

Updates an existing channel app — typically to rotate its secrets.
Sending the redaction marker (`***REDACTED***`) for a secret field
preserves the stored value, so a form prefilled from a fetched
(redacted) app can rotate one secret without re-entering the other.

**Signature**

```tsx
function useUpdateChannelApp(): UseUpdateChannelAppReturn
```

**Returns** — `UseUpdateChannelAppReturn`

<TypeTable
  type={{
    clearError: { type: "() => void", description: "Reset `error` to `null`." },
    error: { type: "Error | null", description: "Error from the last failed update, or `null` when healthy." },
    isUpdating: { type: "boolean", description: "`true` while the update request is in flight." },
    update: { type: "(input: ChannelAppInput) => Promise<ChannelApp>", description: "Submit a ChannelAppInput to update an existing channel app. Resolves with the updated resource." },
  }}
/>

```tsx
const { update, isUpdating, error } = useUpdateChannelApp();

await update({
  name: "Acme Support Bot",
  org: "acme",
  slack: {
    clientId: "123456.789012",
    clientSecret: "***REDACTED***",   // keep the stored value
    signingSecret: "new-signing-secret", // rotate this one
  },
});
```


## Components

### ChannelAppDetailPanel

Detail view for one `ChannelApp`: the "finish setup at the
provider" values — phase two of the two-phase setup — plus credential
rotation and deletion.

Provider-shaped bodies on a shared frame: a Slack app shows its
events webhook URL, OAuth redirect URL, and completed manifest; a
Meta (WhatsApp) app shows its webhook URL and verify token (no
manifest — Meta has no equivalent, so the guidance is a checklist).

Secret fields are prefilled with the redaction marker; leaving them
untouched preserves the stored values (rotate one secret without
re-entering the others). Deletion is refused server-side while any
agent channel still installs through this app.

**Props** — `ChannelAppDetailPanelProps`

<TypeTable
  type={{
    channelApp: { type: "ChannelApp", description: "The channel app to display and edit (secret fields arrive redacted).", required: true },
    className: { type: "string", description: "Additional CSS class names for the root container." },
    consoleOrigin: { type: "string", description: "Console origin used to derive the OAuth redirect URL shown in the Slack setup guidance. Defaults to the current window's origin." },
    createHandoff: { type: "ChannelAppCreateHandoff", description: "Once-visible values threaded from the create flow (in memory only). The WhatsApp verify token is entered at registration but answers redacted from then on, and phase-two setup needs it *together with* the webhook URL that only exists now — this handoff lets the panel show both, exactly once. Absent on a later visit, the token renders redacted (rotate to get a fresh one)." },
    onBack: { type: "() => void", description: "Fired when the user navigates back to the list." },
    onDeleted: { type: "() => void", description: "Fired after the app is deleted." },
    onUpdated: { type: "(app: ChannelApp) => void", description: "Fired with the updated app after a successful save." },
  }}
/>


### ChannelAppListPanel

Displays the `ChannelApp` resources owned by an organization —
the customer's own provider apps (Slack, Meta/WhatsApp) that agent
channels can install through.

Each row shows the app name, provider, the provider's non-secret app
identifier, and creation date. Secret fields never appear (they
arrive redacted).

All visual properties flow through `--stgm-*` design tokens.

**Props** — `ChannelAppListPanelProps`

<TypeTable
  type={{
    className: { type: "string", description: "Additional CSS class names for the root container." },
    onEdit: { type: "(app: ChannelApp) => void", description: "Fired when the user wants to view/edit a channel app. When provided, rows become interactive with a pencil icon button." },
    onRefetchRef: { type: "(refetch: () => void) => void", description: "Re-expose refetch so parents can trigger a list refresh." },
    org: { type: "string", description: "Organization slug to list channel apps for.", required: true },
  }}
/>

```tsx
<ChannelAppListPanel
  org="acme"
  onEdit={(app) => setFlow({ phase: "editing", channelApp: app })}
/>
```


### CreateChannelAppForm

Form for registering a customer-owned provider app as a
`ChannelApp` (bring your own app — the bot carries your name
and identity, and each app is its own bot identity).

Provider-shaped: a Slack app brings OAuth credentials plus a
ready-to-paste manifest; a Meta (WhatsApp) app brings dashboard
credentials plus a generated verify token — Meta has no manifest
equivalent, so the guidance is a checklist.

Setup is two-phase by nature for both providers (the provider mints
the credentials, Stigmer mints the per-app webhook URL): this form
covers phase one — the credentials. The detail panel shown after
creation carries phase two (the events webhook URL, plus the verify
token for WhatsApp).

This is a pure presentational component with no dialog wrapper
(headless-first). All visual properties flow through `--stgm-*`
design tokens.

**Props** — `CreateChannelAppFormProps`

<TypeTable
  type={{
    className: { type: "string", description: "Additional CSS class names for the root container." },
    consoleOrigin: { type: "string", description: "Console origin used to derive the OAuth redirect URL shown in the Slack setup guidance. Defaults to the current window's origin." },
    onCancel: { type: "() => void", description: "Fired when the user cancels creation." },
    onCreated: { type: "(app: ChannelApp, handoff: ChannelAppCreateHandoff) => void", description: "Fired with the newly created channel app on success. The handoff carries the once-visible secrets the detail panel shows for phase-two setup." },
    org: { type: "string", description: "Organization slug — the channel app will be created in this org.", required: true },
  }}
/>


## Types

### buildSlackChannelAppManifest

Render the ready-to-paste Slack app manifest for a customer channel
app — the platform app's manifest with the customer's identity.

Deliberate mirrors of the platform manifest (do not "fix" them):
four bot scopes only, `org_deploy_enabled: false` (Enterprise Grid
org-wide installs are unsupported), no interactivity, no socket mode.

```tsx
function buildSlackChannelAppManifest(input: SlackChannelAppManifestInput): string
```


### SLACK_CHANNEL_APP_BOT_EVENTS

The bot events every Stigmer channel app subscribes to — the platform
app's exact set (mentions, DMs, assistant surface, and the uninstall/
revocation lifecycle events).


### SLACK_CHANNEL_APP_BOT_SCOPES

The bot scopes every Stigmer channel app requests — the platform app's
exact set (scope minimalism is deliberate: it is the first thing a
workspace admin reviews on the consent screen).


### SlackChannelAppManifestInput

Inputs for [`buildSlackChannelAppManifest`](#buildslackchannelappmanifest).

<TypeTable
  type={{
    description: { type: "string", description: "One-line app description shown on the consent screen." },
    name: { type: "string", description: "Bot display name — what the workspace @mentions.", required: true },
    redirectUrl: { type: "string", description: "The console's OAuth redirect URL (slackChannelAppRedirectUrl).", required: true },
    webhookUrl: { type: "string", description: "The app's events webhook URL (slackChannelAppWebhookUrl). Omit before the ChannelApp exists — the manifest then skips event subscriptions, which are completed from the detail panel after creation." },
  }}
/>


### slackChannelAppRedirectUrl

The OAuth redirect URL a customer registers on their Slack app — the
console's install-callback route, shared by every app (the platform
completes the code exchange server-side with the app's own
credentials).

```tsx
function slackChannelAppRedirectUrl(consoleOrigin?: string): string
```


### slackChannelAppWebhookUrl

The events webhook URL for one ChannelApp: the per-app route the
receiver resolves the app's signing secret by.

```tsx
function slackChannelAppWebhookUrl(apiBaseUrl: string, channelAppId: string): string
```

