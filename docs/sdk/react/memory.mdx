---
title: memory
---

{/* Auto-generated by @stigmer/react docs generator. DO NOT EDIT. */}

## Hooks

### useConfirmMemory

Behavior hook that wraps `memory.confirm()` — the consent act
(DD-005 D3). A confirmed memory is recalled into the caller's future
sessions as background context.

Confirming an already-confirmed memory succeeds and changes nothing.
Confirming a rejected memory fails with FAILED_PRECONDITION: the
rejection stands — delete the record and let the agent propose again.

**Signature**

```tsx
function useConfirmMemory(): UseConfirmMemoryReturn
```

**Returns** — `UseConfirmMemoryReturn`

<TypeTable
  type={{
    clearError: { type: "() => void", description: "Reset `error` to `null`." },
    confirmMemory: { type: "(id: string) => Promise<Memory>", description: "Confirm a proposed memory by its resource ID. Resolves with the confirmed record." },
    error: { type: "Error | null", description: "Error from the last failed confirm, or `null` when healthy." },
    isConfirming: { type: "boolean", description: "`true` while the confirm request is in flight." },
  }}
/>

```tsx
const { confirmMemory, isConfirming } = useConfirmMemory();

await confirmMemory(memory.metadata?.id ?? "");
refetch(); // refresh the list after the decision
```


### useDeleteMemory

Behavior hook that wraps `memory.delete()`.

Deletion works in any lifecycle state — it is never refused on
lifecycle grounds (DD-004) — and is the revocation mechanism for
confirmed facts: the fact stops reaching future sessions immediately.
Past executions keep their immutable recall snapshots.

**Signature**

```tsx
function useDeleteMemory(): UseDeleteMemoryReturn
```

**Returns** — `UseDeleteMemoryReturn`

<TypeTable
  type={{
    clearError: { type: "() => void", description: "Reset `error` to `null`." },
    deleteMemory: { type: "(id: string) => Promise<Memory>", description: "Delete a memory by its resource ID, in any lifecycle state. Resolves with the deleted record." },
    error: { type: "Error | null", description: "Error from the last failed delete, or `null` when healthy." },
    isDeleting: { type: "boolean", description: "`true` while the delete request is in flight." },
  }}
/>

```tsx
const { deleteMemory, isDeleting, error } = useDeleteMemory();

await deleteMemory(memory.metadata?.id ?? "");
refetch(); // refresh the list after deletion
```


### useMemories

Data hook that fetches the caller's memories in an organization.

Memory content is subject-only: the server returns exactly the records
that are about the caller (FGA-filtered in cloud; single-user in local
mode), so this list is always "what the platform remembers about ME
here". Ordering is newest first; group by lifecycle state for display
with [`groupMemoriesByLifecycle`](#groupmemoriesbylifecycle).

Pass `null` as `org` to skip fetching (stable no-op).

**Signature**

```tsx
function useMemories(org: string | null): UseMemoriesReturn
```

<TypeTable
  type={{
    org: { type: "string | null", description: "", required: true },
  }}
/>

**Returns** — `UseMemoriesReturn`

<TypeTable
  type={{
    error: { type: "Error | null", description: "Error from the last failed request, or `null` when healthy." },
    isLoading: { type: "boolean", description: "`true` while the initial fetch is in flight." },
    isRefetching: { type: "boolean", description: "`true` while a background refetch is in flight." },
    memories: { type: "readonly Memory[]", description: "Every memory the caller can view in the org, newest first." },
    refetch: { type: "() => void", description: "Discard cached data and re-fetch the list from the server." },
  }}
/>

```tsx
const { memories, isLoading, refetch } = useMemories(org);
const groups = groupMemoriesByLifecycle(memories);
```


### useMemory

Data hook that fetches one memory record by id.

Built for surfaces that hold a memory's identity but not its CURRENT
lifecycle state — the in-thread proposal chip is the primary consumer:
the tool result it renders from is frozen at capture time, while the
record may have been confirmed, rejected, or deleted since (from the
chip itself, the memory settings page, or another device). Content
visibility is subject-only, so the fetch answers only for the person
the memory is about.

Pass `null` as `id` to skip fetching (stable no-op).

**Signature**

```tsx
function useMemory(id: string | null): UseMemoryReturn
```

<TypeTable
  type={{
    id: { type: "string | null", description: "", required: true },
  }}
/>

**Returns** — `UseMemoryReturn`

<TypeTable
  type={{
    error: { type: "Error | null", description: "Error from the last failed request (never not-found), or `null`." },
    isLoading: { type: "boolean", description: "`true` while the initial fetch is in flight." },
    memory: { type: "Memory | null", description: "The memory record, or `null` while loading / not found / on error." },
    notFound: { type: "boolean", description: "`true` when the record no longer exists. A first-class STATE, not an error: deletion is the platform's consent-revocation mechanism, so a consumer (the proposal chip, a deep link) must render \"no longer stored\" honestly rather than a failure." },
    refetch: { type: "() => void", description: "Discard cached data and re-fetch the record from the server." },
  }}
/>

```tsx
const { memory, isLoading, notFound } = useMemory(memoryId);
```


### useRejectMemory

Behavior hook that wraps `memory.reject()`. A rejected memory is never
recalled; the record is kept as an audit trail until deleted.

Rejecting an already-rejected memory succeeds and changes nothing.
Rejecting a confirmed memory fails with FAILED_PRECONDITION —
deleting a confirmed memory is how it is revoked.

Rejection is deliberately one click on every surface, with no
confirmation dialog (DD-005 D4): expensive review teaches users to
ignore the proposal queue.

**Signature**

```tsx
function useRejectMemory(): UseRejectMemoryReturn
```

**Returns** — `UseRejectMemoryReturn`

<TypeTable
  type={{
    clearError: { type: "() => void", description: "Reset `error` to `null`." },
    error: { type: "Error | null", description: "Error from the last failed reject, or `null` when healthy." },
    isRejecting: { type: "boolean", description: "`true` while the reject request is in flight." },
    rejectMemory: { type: "(id: string) => Promise<Memory>", description: "Reject a proposed memory by its resource ID. Resolves with the rejected record." },
  }}
/>

```tsx
const { rejectMemory, isRejecting } = useRejectMemory();

await rejectMemory(memory.metadata?.id ?? "");
refetch(); // refresh the list after the decision
```


### useUpdateMemoryContent

Behavior hook that edits a memory's fact text — the one field the
subject owns after capture (DD-004).

The update RPC replaces the entire spec, so the loaded record is
mapped through the generated `toMemoryUpdateInput` and only `content`
is overridden — the wipe-safe editing rule. Subject and provenance
ride along verbatim (they are update-immutable server-side), and the
consent lifecycle is never touched by updates.

**Signature**

```tsx
function useUpdateMemoryContent(): UseUpdateMemoryContentReturn
```

**Returns** — `UseUpdateMemoryContentReturn`

<TypeTable
  type={{
    clearError: { type: "() => void", description: "Reset `error` to `null`." },
    error: { type: "Error | null", description: "Error from the last failed update, or `null` when healthy." },
    isUpdating: { type: "boolean", description: "`true` while the update request is in flight." },
    updateContent: { type: "(memory: Memory, content: string) => Promise<Memory>", description: "Replace a memory's fact text. Resolves with the updated record." },
  }}
/>

```tsx
const { updateContent, isUpdating } = useUpdateMemoryContent();

await updateContent(memory, editedText);
refetch(); // refresh the list after the edit
```


## Components

### MemoryListPanel

Lists everything the platform remembers about the caller in an
organization, grouped pending-proposals-first (DD-005 D4): proposals
awaiting a decision, then confirmed facts, then rejected proposals
kept for audit.

Every fact is shown VERBATIM — the exact stored text is what future
prompts inject, byte for byte, so the review surface never paraphrases
(DD-005 D6). Provenance renders beside each agent-proposed fact.
Actions per state: confirm/reject/delete on proposals (reject is
one-click — no confirmation dialog), edit/delete on confirmed facts,
delete on rejected ones. Delete asks inline; it works in any state.

All visual properties flow through `--stgm-*` design tokens.

**Props** — `MemoryListPanelProps`

<TypeTable
  type={{
    className: { type: "string", description: "Additional CSS class names for the root container." },
    now: { type: "Date", description: "Reference instant for relative timestamps. Defaults to the live clock. **Deterministic hosts (documentation embeds, video export) must pass a frozen instant.**" },
    org: { type: "string", description: "Organization whose memories to show.", required: true },
  }}
/>

```tsx
<MemoryListPanel org="acme" />
```


### MemoryProposalCardBody

The in-session consent surface for a memory proposal (DD-005 D4): the
body of a `remember` tool call's row in the message thread, rendering
the proposed fact verbatim with one-click Confirm / Reject actions.

Lifecycle honesty across surfaces and reloads: the tool result this
renders from is FROZEN at capture time, so the card fetches the
record's current state ([`useMemory`](#usememory)) — a proposal decided from
the memory settings page (or another device) shows its decided state
here instead of stale action buttons, and a deleted record reads
"no longer stored" rather than failing. Rejection is one click, no
confirmation dialog (the T04 Cursor lesson: expensive review teaches
users to ignore the queue); deletion-with-confirmation lives on the
memory page, the catch-up surface.

Borderless by design (the [`ApprovalCardBody`](/docs/sdk/react/execution#approvalcardbody) posture): the tool
row's card owns the chrome, this component owns only the body. All
visual properties flow through `--stgm-*` design tokens.

**Props** — `MemoryProposalCardBodyProps`

<TypeTable
  type={{
    className: { type: "string", description: "Additional CSS class names for the root container." },
    fact: { type: "string", description: "The proposed fact as frozen in the tool result — shown VERBATIM (DD-005 D6: what you confirm is what future prompts inject, byte for byte). The live record's text supersedes it once fetched, so an edit made on the memory page is reflected here too.", required: true },
    memoryId: { type: "string", description: "The proposed memory's resource id (from the remember tool's answer).", required: true },
  }}
/>


## Types

### formatMemoryProvenance

One-line provenance attribution for a memory, or `null` when the
record has none (created directly through the API, not by an agent).

Trust requires "where did this come from" beside every fact (DD-004);
agent/session display names are not resolvable from the record alone,
so ids are shown — they are what links back to the source surfaces.

```tsx
function formatMemoryProvenance(memory: Memory): string | null
```


### groupMemoriesByLifecycle

Buckets memories by lifecycle state for the pending-proposals-first
page layout (DD-005 D4: the memory page is the catch-up surface for
proposals scrolled past in session). Input order (newest first from
the server) is preserved inside each bucket.

A record in the zero `unspecified` state should not exist (create
stamps `proposed`); it is bucketed with proposals so a decision
surface is always offered rather than hiding the record.

```tsx
function groupMemoriesByLifecycle(memories: readonly Memory[]): MemoryGroups
```


### MemoryGroups

Memories bucketed by consent lifecycle, pending proposals first.

<TypeTable
  type={{
    confirmed: { type: "readonly Memory[]", description: "Confirmed facts, recalled into future sessions.", required: true },
    proposed: { type: "readonly Memory[]", description: "Awaiting the caller's decision — the catch-up queue.", required: true },
    rejected: { type: "readonly Memory[]", description: "Rejected proposals, kept for audit until deleted.", required: true },
  }}
/>

