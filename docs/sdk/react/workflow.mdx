---
title: Workflow
description: Hooks and components for workflow definitions, instances, executions, and the visual builder.
---

{/* Auto-generated by @stigmer/react docs generator. DO NOT EDIT. */}

## Hooks

### useCreateWorkflowInstance

Mutation hook that creates a new WorkflowInstance resource.

Wraps `stigmer.workflowInstance.create()` with loading/error state.
The caller is responsible for refreshing the instance list after
a successful create (e.g., via `refetch` from `useWorkflowInstances`).

**Signature**

```tsx
function useCreateWorkflowInstance(): UseCreateWorkflowInstanceReturn
```

**Returns** — `UseCreateWorkflowInstanceReturn`

<TypeTable
  type={{
    clearError: { type: "() => void", description: "Reset `error` to `null`." },
    create: { type: "(input: WorkflowInstanceInput) => Promise<WorkflowInstance>", description: "Submit a WorkflowInstanceInput to create a new WorkflowInstance. Resolves with the server-created resource." },
    error: { type: "Error | null", description: "Error from the last failed create, or `null` when healthy." },
    isCreating: { type: "boolean", description: "`true` while the create request is in flight." },
  }}
/>

```tsx
const { create, isCreating, error } = useCreateWorkflowInstance();

const handleSubmit = async () => {
  const instance = await create({
    name: "prod-deploy",
    org: "my-org",
    workflowId: workflow.metadata.id,
    environmentRefs: [{ org: "my-org", slug: "aws-prod" }],
  });
  // instance created — refetch list
};
```


### useDeleteWorkflowInstance

Mutation hook that deletes a WorkflowInstance resource.

Wraps `stigmer.workflowInstance.delete()` with loading/error state.
The caller is responsible for handling post-delete UI updates
(e.g., refreshing the instance list, closing a detail panel).

Deletion cascades to all WorkflowExecution resources that belong
to the instance. The confirmation UI should warn the user about this.

**Signature**

```tsx
function useDeleteWorkflowInstance(): UseDeleteWorkflowInstanceReturn
```

**Returns** — `UseDeleteWorkflowInstanceReturn`

<TypeTable
  type={{
    clearError: { type: "() => void", description: "Reset `error` to `null`." },
    deleteInstance: { type: "(id: string) => Promise<WorkflowInstance>", description: "Delete a workflow instance by ID. Returns the deleted resource.  WARNING: Deleting an instance cascades — all executions belonging to this instance are permanently removed." },
    error: { type: "Error | null", description: "Error from the last failed delete, or `null` when healthy." },
    isDeleting: { type: "boolean", description: "`true` while the delete request is in flight." },
  }}
/>

```tsx
const { deleteInstance, isDeleting } = useDeleteWorkflowInstance();

const handleDelete = async () => {
  await deleteInstance(instance.metadata.id);
  refetch(); // refresh list after deletion
};
```


### useDiagnoseExecutionFlow

Behavior hook that orchestrates agent-powered workflow execution
diagnosis using the built-in Workflow Architect system agent.

Manages a multi-turn conversation within a single Session:
- First turn creates a Session + AgentExecution with diagnosis context
- Subsequent turns reuse the Session (conversational context)
- Each turn streams agent messages via [`useExecutionStream`](/docs/sdk/react/execution#useexecutionstream)
- YAML is extracted from agent responses via [`extractWorkflowYaml`](#extractworkflowyaml)
  — presence of YAML indicates a definition fix; absence indicates a
  runtime error with explanation only

Auto-starts diagnosis on mount by default (AD-B5-002). The agent uses
`get_workflow_execution` and `get_workflow_execution_events` MCP tools
to inspect the failure autonomously.

Framework-agnostic (DD-004), referentially stable returns (DD-010).

**Signature**

```tsx
function useDiagnoseExecutionFlow(options: UseDiagnoseExecutionFlowOptions): UseDiagnoseExecutionFlowReturn
```

<TypeTable
  type={{
    options: { type: "UseDiagnoseExecutionFlowOptions", description: "", required: true },
  }}
/>

**Returns** — `UseDiagnoseExecutionFlowReturn`

<TypeTable
  type={{
    acceptFix: { type: "() => string | null", description: "Accept the extracted YAML fix. Returns the YAML string. Transitions to `ready`." },
    activeExecution: { type: "AgentExecution | null", description: "Currently streaming execution, or null when idle/between turns." },
    completedExecutions: { type: "readonly AgentExecution[]", description: "Completed execution snapshots for MessageThread (chronological)." },
    diagnose: { type: "() => Promise<void>", description: "Start or restart the diagnosis. Callable from `idle` or `error` phases." },
    discardFix: { type: "() => void", description: "Discard the extracted YAML without applying. Transitions to `ready`." },
    error: { type: "string | null", description: "Human-readable error message, or `null` when healthy." },
    explanation: { type: "string | null", description: "Agent's explanation prose from the latest turn." },
    extractedYaml: { type: "string | null", description: "Extracted YAML fix from the latest turn (null unless phase is `complete`)." },
    isStreaming: { type: "boolean", description: "`true` while the agent execution is actively streaming." },
    phase: { type: "DiagnosePhase", description: "Current lifecycle phase.", typeDescriptionLink: "#diagnosephase" },
    reset: { type: "() => void", description: "Reset all state to initial values. Does not delete the server-side session." },
    sendFollowUp: { type: "(message: string) => Promise<void>", description: "Send a follow-up question within the same session." },
  }}
/>

```tsx
const flow = useDiagnoseExecutionFlow({
  executionId: "wex_abc123",
  org: "acme",
  onError: (msg) => toast.error(msg),
});

<WorkflowRepairCard flow={flow} onApplyFix={...} onClose={...} />
```


### useElkLayoutEngine

Behavior hook (DD-003 layer 2) that asynchronously creates an ELK layout
engine and manages its lifecycle.

Returns the engine once ready, or `null` while loading / if `elkjs` is
not installed. The caller can pass the return value directly to
`WorkflowCanvasEditor`'s `layoutEngine` prop — `null` is safe and causes
the canvas to use the default dagre engine.

The engine (and its Web Worker, if any) is terminated on unmount.

**Signature**

```tsx
function useElkLayoutEngine(options?: UseElkLayoutEngineOptions): LayoutEngine | null
```

<TypeTable
  type={{
    options: { type: "UseElkLayoutEngineOptions", description: "" },
  }}
/>

```tsx
const workerFactory = () =>
  new Worker(new URL("elkjs/lib/elk-worker.min.js", import.meta.url));

function MyEditor({ yaml }: { yaml: string }) {
  const elkEngine = useElkLayoutEngine({ workerFactory });
  return <WorkflowCanvasEditor yaml={yaml} layoutEngine={elkEngine} />;
}
```


### useExecutionHistoryData

Behavior hook that composes [`useWorkflowExecutionList`](#useworkflowexecutionlist) with the
[`deriveExecutionRows`](#deriveexecutionrows) derivation pipeline and optional client-side
filters.

Returns pre-computed [`ExecutionRow`](#executionrow) objects ready for rendering
by [`ExecutionHistoryTable`](#executionhistorytable).

**Signature**

```tsx
function useExecutionHistoryData(options?: UseExecutionHistoryDataOptions): UseExecutionHistoryDataReturn
```

<TypeTable
  type={{
    options: { type: "UseExecutionHistoryDataOptions", description: "" },
  }}
/>

**Returns** — `UseExecutionHistoryDataReturn`

<TypeTable
  type={{
    error: { type: "Error | null", description: "Error from the last failed request, or `null` when healthy." },
    isLoading: { type: "boolean", description: "`true` while the initial fetch is in flight." },
    isRefetching: { type: "boolean", description: "`true` while a background refetch is in flight and stale data is shown." },
    refetch: { type: "() => void", description: "Discard cached data and re-fetch from the server." },
    rows: { type: "readonly ExecutionRow[]", description: "Derived, optionally filtered execution rows for the current page." },
    totalPages: { type: "number", description: "Total pages from the server (does not account for client-side filtering)." },
  }}
/>

```tsx
const { rows, isLoading, error, refetch } = useExecutionHistoryData({
  workflowId: "wf_onboarding",
  pageSize: 20,
  clientFilters: { phases: [ExecutionPhase.EXECUTION_FAILED] },
});
```


### usePendingApprovals

Data hook that fetches pending human_input approvals for an organization.

Returns workflow executions with active human_input tasks awaiting
reviewer decisions. Includes task name, requester, and timeout info.

Auto-refreshes every 30 seconds by default since pending approvals
are time-sensitive.

**Signature**

```tsx
function usePendingApprovals(options: UsePendingApprovalsOptions): UsePendingApprovalsReturn
```

<TypeTable
  type={{
    options: { type: "UsePendingApprovalsOptions", description: "", required: true },
  }}
/>

**Returns** — `UsePendingApprovalsReturn`

<TypeTable
  type={{
    approvals: { type: "readonly PendingApproval[]", description: "" },
    error: { type: "Error | null", description: "" },
    isLoading: { type: "boolean", description: "" },
    isRefetching: { type: "boolean", description: "" },
    refetch: { type: "() => void", description: "" },
    totalCount: { type: "number", description: "" },
  }}
/>

```tsx
const { approvals, totalCount, isLoading } = usePendingApprovals({
  org: "acme",
});
```


### useRefineWorkflowFlow

Behavior hook that orchestrates agent-powered workflow refinement
using the built-in Workflow Architect system agent.

Manages a multi-turn conversation within a single Session:
- First turn creates a Session + AgentExecution
- Subsequent turns reuse the Session (conversational context)
- Each turn streams agent messages via [`useExecutionStream`](/docs/sdk/react/execution#useexecutionstream)
- YAML is extracted from agent responses via [`extractWorkflowYaml`](#extractworkflowyaml)

The `currentYaml` prop is captured at send-time (ref-based, not reactive)
and included in the message only when it differs from the last-sent version.

Framework-agnostic (DD-004), referentially stable returns (DD-010).

**Signature**

```tsx
function useRefineWorkflowFlow(options: UseRefineWorkflowFlowOptions): UseRefineWorkflowFlowReturn
```

<TypeTable
  type={{
    options: { type: "UseRefineWorkflowFlowOptions", description: "", required: true },
  }}
/>

**Returns** — `UseRefineWorkflowFlowReturn`

<TypeTable
  type={{
    acceptResult: { type: "() => string | null", description: "Accept the extracted YAML. Returns the YAML string. Transitions to `ready`." },
    activeExecution: { type: "AgentExecution | null", description: "Currently streaming execution, or null when idle/between turns." },
    completedExecutions: { type: "readonly AgentExecution[]", description: "Completed execution snapshots for MessageThread (chronological)." },
    discardResult: { type: "() => void", description: "Discard the extracted YAML without applying. Transitions to `ready`." },
    error: { type: "string | null", description: "Human-readable error message, or `null` when healthy." },
    explanation: { type: "string | null", description: "Agent's explanation prose from the latest turn." },
    extractedYaml: { type: "string | null", description: "Extracted YAML from the latest turn (null unless phase is `complete`)." },
    isStreaming: { type: "boolean", description: "`true` while the agent execution is actively streaming." },
    phase: { type: "RefinePhase", description: "Current lifecycle phase.", typeDescriptionLink: "#refinephase" },
    reset: { type: "() => void", description: "Reset all state to initial values. Does not delete the server-side session." },
    sendInstruction: { type: "(instruction: string) => Promise<void>", description: "Send a refinement instruction. Creates session on first call." },
  }}
/>

```tsx
const flow = useRefineWorkflowFlow({
  org: "acme",
  currentYaml: editor.yaml,
  onError: (msg) => toast.error(msg),
});

<WorkflowRefinePanel flow={flow} onAccept={...} onClose={...} />
```


### useResolveAgentExecutionSession

Resolves an AgentExecution ID (`aex_*`) to its parent Session ID.

This hook fetches the AgentExecution resource and extracts
`spec.sessionId`. Use it when navigating from a workflow execution
context (which knows the child agent execution ID) to the session
page (which requires the session ID).

Pass `null` to skip fetching (stable no-op).

**Signature**

```tsx
function useResolveAgentExecutionSession(agentExecutionId: string | null): UseResolveAgentExecutionSessionReturn
```

<TypeTable
  type={{
    agentExecutionId: { type: "string | null", description: "", required: true },
  }}
/>

**Returns** — `UseResolveAgentExecutionSessionReturn`

<TypeTable
  type={{
    error: { type: "Error | null", description: "Error from the last failed request, or `null` when healthy." },
    isLoading: { type: "boolean", description: "`true` while the fetch is in flight." },
    sessionId: { type: "string | null", description: "The resolved session ID, or `null` while loading or on error." },
  }}
/>

```tsx
function DrillDown({ agentExecutionId }: { agentExecutionId: string }) {
  const { sessionId, isLoading } = useResolveAgentExecutionSession(agentExecutionId);
  useEffect(() => {
    if (sessionId) navigateToSession(sessionId);
  }, [sessionId]);
  if (isLoading) return <Spinner />;
  return null;
}
```


### useRunWorkflowFlow

Behavior hook that orchestrates the "run a workflow" flow.

Manages form state (trigger message, runtime env overrides, instance
selection), validates required fields, and calls
`WorkflowExecutionClient.create()` on submission. On success, the
consumer-provided `onSuccess` callback receives the execution ID for
navigation or further action.

This hook is framework-agnostic — it works identically in Next.js,
Vite, Tauri, or any React environment. Navigation and toast feedback
are the consumer's responsibility.

**Signature**

```tsx
function useRunWorkflowFlow(options: UseRunWorkflowFlowOptions): UseRunWorkflowFlowReturn
```

<TypeTable
  type={{
    options: { type: "UseRunWorkflowFlowOptions", description: "", required: true },
  }}
/>

**Returns** — `UseRunWorkflowFlowReturn`

<TypeTable
  type={{
    envDeclarations: { type: "Record<string, EnvVarDeclaration>", description: "Declared environment variables from the workflow spec." },
    error: { type: "string | null", description: "Error from the last failed submission, or `null`." },
    fieldErrors: { type: "RunWorkflowFieldErrors", description: "Field-level validation errors (empty when valid).", typeDescriptionLink: "#runworkflowfielderrors" },
    instanceEnvKeys: { type: "Set<string>", description: "Set of env var keys already provided by the selected instance's bound environments. Empty when no instance is selected or its environments haven't loaded yet." },
    isLoadingInstanceEnvKeys: { type: "boolean", description: "`true` while instance environment keys are being resolved." },
    isSubmitting: { type: "boolean", description: "`true` while the create execution RPC is in flight." },
    reset: { type: "() => void", description: "Reset all form state to initial values." },
    runtimeEnv: { type: "Record<string, string>", description: "Current runtime environment variable overrides (keyed by var name)." },
    selectedInstanceId: { type: "string | null", description: "Selected instance ID, or `null` for server-resolved default." },
    setEnvVar: { type: "(key: string, value: string) => void", description: "Update a single env var value." },
    setSelectedInstanceId: { type: "(id: string | null) => void", description: "Update the selected instance." },
    setShowTriggerMessage: { type: "(show: boolean) => void", description: "Toggle visibility of the trigger message field (escape hatch)." },
    setTriggerMessage: { type: "(value: string) => void", description: "Update the trigger message." },
    showTriggerMessage: { type: "boolean", description: "Whether the trigger message field should be visible in the form. Defaults to `usesTriggerInput`; can be toggled by the user via the escape-hatch \"Add trigger input\" link." },
    submit: { type: "() => Promise<void>", description: "Validate, then create the workflow execution." },
    triggerMessage: { type: "string", description: "Current trigger message value." },
    usesTriggerInput: { type: "boolean", description: "Whether the workflow references `$input` / trigger_message in its tasks. When `false`, the trigger message field is irrelevant for this workflow." },
    validate: { type: "() => boolean", description: "Validate form fields. Returns `true` if valid." },
  }}
/>

```tsx
const flow = useRunWorkflowFlow({
  org: "acme",
  workflow,
  instances,
  onSuccess: (id) => router.push(`/workflows/executions/${id}`),
  onError: (msg) => toast.error(msg),
});

<input value={flow.triggerMessage} onChange={e => flow.setTriggerMessage(e.target.value)} />
<button onClick={flow.submit} disabled={flow.isSubmitting}>Run</button>
```


### useTaskKindRegistry

Data hook that exposes the task kind registry with lookup and grouping helpers.

Pure data layer — no rendering, no side effects. Platform builders
who want full control over rendering import this hook and build
their own UI (task palettes, form generators, validation).

The registry data is fetched from the task kind registry API by
[`StigmerProvider`](/docs/sdk/react/core#stigmerprovider) and cached in context. During loading,
`isLoading` is `true` and `descriptors` is empty.

**Signature**

```tsx
function useTaskKindRegistry(): UseTaskKindRegistryReturn
```

**Returns** — `UseTaskKindRegistryReturn`

<TypeTable
  type={{
    categories: { type: "ReadonlyMap<TaskKindCategory, readonly TaskKindDescriptor[]>", description: "Descriptors grouped by their functional category.  Useful for rendering a categorized task palette where tasks are organized into sections like \"AI\", \"Control Flow\", \"Data\", etc." },
    descriptors: { type: "readonly TaskKindDescriptor[]", description: "All registered task kind descriptors, one per WorkflowTaskKind." },
    error: { type: "Error | null", description: "Non-null if the API fetch failed. Descriptors will be empty in this case." },
    getByKind: { type: "(kind: string) => TaskKindDescriptor | undefined", description: "Look up a single descriptor by its kind identifier." },
    getJsonSchema: { type: "(kind: string) => Record<string, unknown> | undefined", description: "Retrieve the JSON Schema for a task kind's configuration.  Returns the parsed JSON Schema object suitable for Monaco editor validation or React JSON Schema Form (RJSF) rendering." },
    isLoading: { type: "boolean", description: "`true` while the task kind registry is being fetched from the API." },
    refetch: { type: "() => void", description: "Retry fetching the registry. No-op while a fetch is in flight." },
  }}
/>

```tsx
function TaskPalette() {
  const { categories, isLoading, error } = useTaskKindRegistry();

  if (isLoading) return <Skeleton />;
  if (error) return <ErrorMessage error={error} />;

  return (
    <div>
      {Array.from(categories).map(([category, tasks]) => (
        <CategorySection key={category} label={category}>
          {tasks.map(task => (
            <TaskCard key={task.kind} descriptor={task} />
          ))}
        </CategorySection>
      ))}
    </div>
  );
}
```

```tsx
// YAML editor integration
function WorkflowEditor({ taskKind }: { taskKind: string }) {
  const { getJsonSchema } = useTaskKindRegistry();
  const schema = getJsonSchema(taskKind);
  // Pass schema to Monaco for autocomplete + validation
}
```


### useUpdateWorkflowInstance

Mutation hook that updates an existing WorkflowInstance resource.

Wraps `stigmer.workflowInstance.update()` with loading/error state.
The caller is responsible for refreshing the instance after a successful
update (e.g., via `refetch` from `useWorkflowInstances` or `useWorkflowInstance`).

**Signature**

```tsx
function useUpdateWorkflowInstance(): UseUpdateWorkflowInstanceReturn
```

**Returns** — `UseUpdateWorkflowInstanceReturn`

<TypeTable
  type={{
    clearError: { type: "() => void", description: "Clear the error state." },
    error: { type: "Error | null", description: "Error from the last failed update, or `null` when healthy." },
    isUpdating: { type: "boolean", description: "`true` while the update RPC is in flight." },
    update: { type: "(input: WorkflowInstanceInput) => Promise<WorkflowInstance>", description: "Update an existing workflow instance with a full input. Returns the updated resource." },
  }}
/>

```tsx
const { update, isUpdating } = useUpdateWorkflowInstance();

const handleSave = async () => {
  await update({
    ...instanceToInput(instance),
    environmentRefs: newRefs,
  });
  refetch();
};
```


### useUpdateWorkflowInstanceExecutionVisibility

Mutation hook that updates the execution (run) visibility of a
WorkflowInstance.

Wraps `stigmer.workflowInstance.updateExecutionVisibility()` with
loading/error state. The caller is responsible for refreshing the instance
after a successful update.

**Signature**

```tsx
function useUpdateWorkflowInstanceExecutionVisibility(): UseUpdateWorkflowInstanceExecutionVisibilityReturn
```

**Returns** — `UseUpdateWorkflowInstanceExecutionVisibilityReturn`

<TypeTable
  type={{
    clearError: { type: "() => void", description: "Clear the error state." },
    error: { type: "Error | null", description: "Error from the last failed update, or `null` when healthy." },
    isUpdating: { type: "boolean", description: "`true` while the update RPC is in flight." },
    updateExecutionVisibility: { type: "(resourceId: string, executionVisibility: WorkflowExecutionVisibility) => Promise<WorkflowInstance>", description: "Update who can observe the runs (executions) of a workflow instance.  This is distinct from the instance's own visibility: it controls run observability via the `workflow_instance#execution_viewer` relation, letting an owner keep the instance private while opting all org members into watching its executions." },
  }}
/>


### useWorkflow

Data hook that fetches a single Workflow blueprint by organization and slug.

Wraps `stigmer.workflow.getByReference()` with loading, error, and
not-found state management. When the `org` or `slug` parameters
change, the previous in-flight request is discarded and a fresh
fetch begins.

Pass `null` for either `org` or `slug` to skip fetching (stable
no-op). This is useful when the slug is not yet available — for
example, while a parent component is still resolving route params.

**Not-found handling:** If the API returns a 404 (NOT_FOUND), the
hook sets `workflow` to `null` without raising an error. Consumers
distinguish "not found" from "loading" by checking all three fields:
`workflow === null && !isLoading && !error` means the resource does
not exist.

**Signature**

```tsx
function useWorkflow(org: string | null, slug: string | null): UseWorkflowReturn
```

<TypeTable
  type={{
    org: { type: "string | null", description: "", required: true },
    slug: { type: "string | null", description: "", required: true },
  }}
/>

**Returns** — `UseWorkflowReturn`

<TypeTable
  type={{
    error: { type: "Error | null", description: "Error from the last failed request, or `null` when healthy." },
    isLoading: { type: "boolean", description: "`true` while the initial fetch or a refetch is in flight." },
    isRefetching: { type: "boolean", description: "`true` while a background refetch is in flight and stale data is shown." },
    refetch: { type: "() => void", description: "Discard cached data and re-fetch the workflow from the server." },
    workflow: { type: "Workflow | null", description: "The resolved Workflow, or `null` while loading, on error, or when not found." },
  }}
/>

```tsx
function WorkflowDetail({ org, slug }: { org: string; slug: string }) {
  const { workflow, isLoading, error } = useWorkflow(org, slug);

  if (isLoading) return <Skeleton />;
  if (error) return <ErrorMessage error={error} />;
  if (!workflow) return <NotFound />;

  return <h1>{workflow.metadata?.name}</h1>;
}
```


### useWorkflowArchitectFlow

Behavior hook that orchestrates the agent-powered "generate a workflow"
flow using the built-in Workflow Architect system agent.

Composes existing infrastructure:
- [`useCreateSession`](/docs/sdk/react/session#usecreatesession) to create a session with the agent
- [`useCreateAgentExecution`](/docs/sdk/react/execution#usecreateagentexecution) to start the generation execution
- [`useExecutionStream`](/docs/sdk/react/execution#useexecutionstream) + `ConversationStore` for real-time streaming
- [`extractWorkflowYaml`](#extractworkflowyaml) to parse YAML from agent messages
- `workflow.apply()` to persist the generated workflow

The hook is framework-agnostic (DD-004) and returns referentially
stable values (DD-010).

**Signature**

```tsx
function useWorkflowArchitectFlow(options: UseWorkflowArchitectFlowOptions): UseWorkflowArchitectFlowReturn
```

<TypeTable
  type={{
    options: { type: "UseWorkflowArchitectFlowOptions", description: "", required: true },
  }}
/>

**Returns** — `UseWorkflowArchitectFlowReturn`

<TypeTable
  type={{
    createWorkflow: { type: "() => Promise<void>", description: "Create a workflow from the extracted YAML via `workflow.apply()`.  Only callable when phase is `complete`. Calls `onSuccess` with the org and slug on completion." },
    error: { type: "string | null", description: "Human-readable error message, or `null` when healthy." },
    execution: { type: "AgentExecution | null", description: "Latest execution snapshot from the agent stream.  Non-null during `streaming`, `complete`, and `extraction-failed` phases. Pass to `MessageThread` for rendering agent messages." },
    explanation: { type: "string | null", description: "Agent's explanation prose when phase is `complete`, otherwise `null`." },
    extractedYaml: { type: "string | null", description: "Extracted YAML when phase is `complete`, otherwise `null`." },
    generate: { type: "() => Promise<void>", description: "Validate the prompt and launch the Workflow Architect agent.  Creates a session with the `workflow-architect` system agent, starts an execution with the user's prompt, and begins streaming. The stream phase transitions automatically when the agent finishes." },
    isStreaming: { type: "boolean", description: "`true` while the agent execution is actively streaming." },
    phase: { type: "ArchitectPhase", description: "Current lifecycle phase.", typeDescriptionLink: "#architectphase" },
    prompt: { type: "string", description: "Current prompt value." },
    reset: { type: "() => void", description: "Reset all state to initial values. Does not delete the session." },
    setPrompt: { type: "(value: string) => void", description: "Update the prompt." },
  }}
/>

```tsx
const flow = useWorkflowArchitectFlow({
  org: "acme",
  onSuccess: (org, slug) => router.push(`/library/workflows/${org}/${slug}`),
  onError: (msg) => toast.error(msg),
});

<WorkflowArchitectDialog {...flow} />
```


### useWorkflowCanvas

Orchestrator hook for the workflow canvas editor.

Manages the [`WorkflowGraphModel`](#workflowgraphmodel) through a command/history pipeline
(AD-T15-B2-001). Structural mutations (add/delete nodes and edges) go
through `GraphCommand`s dispatched to the history. React Flow state
is derived from the model after each mutation.

**Signature**

```tsx
function useWorkflowCanvas(yaml: string | null, containerRef: RefObject<HTMLDivElement | null>, options?: UseWorkflowCanvasOptions): UseWorkflowCanvasReturn
```

<TypeTable
  type={{
    yaml: { type: "string | null", description: "The workflow YAML to initialize from. Changes trigger re-parse.", required: true },
    containerRef: { type: "RefObject<HTMLDivElement | null>", description: "Ref to the canvas container for keyboard shortcut scoping.", required: true },
    options: { type: "UseWorkflowCanvasOptions", description: "Optional configuration including a custom layout engine." },
  }}
/>

**Returns** — `UseWorkflowCanvasReturn`

<TypeTable
  type={{
    addCatchHandler: { type: "(tryCatchNodeId: string, errorType: string) => void", description: "" },
    addForkBranch: { type: "(forkNodeId: string, branchName: string) => void", description: "" },
    addNodeAtPosition: { type: "(kindString: string, position: { x: number; y: number }) => void", description: "" },
    addSuccessorTask: { type: "(sourceNodeId: string, kindString: string) => void", description: "" },
    addSwitchCase: { type: "(switchNodeId: string, caseName: string, condition: string) => void", description: "" },
    autoLayout: { type: "() => void", description: "" },
    canRedo: { type: "boolean", description: "" },
    canUndo: { type: "boolean", description: "" },
    clearSelection: { type: "() => void", description: "" },
    copySelection: { type: "() => void", description: "" },
    cutSelection: { type: "() => void", description: "" },
    deleteSelection: { type: "() => void", description: "" },
    disableSelection: { type: "() => void", description: "" },
    duplicateNode: { type: "(nodeId: string) => void", description: "" },
    duplicateSelection: { type: "() => void", description: "" },
    edges: { type: "Edge[]", description: "" },
    error: { type: "string | null", description: "" },
    getGraphModel: { type: "() => WorkflowGraphModel", description: "" },
    getNodeDescriptor: { type: "(nodeId: string) => TaskKindDescriptor | undefined", description: "" },
    getSelectedNodeIds: { type: "() => ReadonlySet<string>", description: "" },
    graph: { type: "WorkflowGraphModel | null", description: "" },
    hasClipboard: { type: "boolean", description: "" },
    insertTaskOnEdge: { type: "(edgeId: string, kindString: string) => void", description: "" },
    isDirty: { type: "boolean", description: "" },
    isValidConnection: { type: "IsValidConnection", description: "" },
    migrateBranchHandle: { type: "(nodeId: string, oldHandleId: string, newHandleId: string) => void", description: "" },
    nodes: { type: "Node[]", description: "" },
    onConnect: { type: "(connection: Connection) => void", description: "" },
    onDragOver: { type: "(event: DragEvent) => void", description: "" },
    onDrop: { type: "(event: DragEvent) => void", description: "" },
    onEdgesChange: { type: "OnEdgesChange", description: "" },
    onEdgesDelete: { type: "(deleted: Edge[]) => void", description: "" },
    onNodesChange: { type: "OnNodesChange", description: "" },
    onNodesDelete: { type: "(deleted: Node[]) => void", description: "" },
    pasteAtCenter: { type: "() => void", description: "" },
    redo: { type: "() => void", description: "" },
    removeBranchEdges: { type: "(nodeId: string, handleId: string) => void", description: "" },
    removeCatchBlock: { type: "(tryCatchNodeId: string) => void", description: "" },
    removeForkBranch: { type: "(forkNodeId: string, branchName: string) => void", description: "" },
    removeSwitchCase: { type: "(switchNodeId: string, caseName: string) => void", description: "" },
    renameForkBranch: { type: "(forkNodeId: string, oldName: string, newName: string) => void", description: "" },
    renameNode: { type: "(nodeId: string, newName: string) => void", description: "" },
    reorderForkBranches: { type: "(forkNodeId: string, newOrder: readonly string[]) => void", description: "" },
    reorderSwitchCases: { type: "(switchNodeId: string, newOrder: readonly string[]) => void", description: "" },
    selectAll: { type: "() => void", description: "" },
    selectEdge: { type: "(id: string) => void", description: "" },
    selection: { type: "CanvasSelection | null", description: "" },
    selectNode: { type: "(id: string) => void", description: "" },
    serializeToYaml: { type: "() => string | null", description: "" },
    setForkCompete: { type: "(forkNodeId: string, compete: boolean) => void", description: "" },
    toggleNodeDisabled: { type: "(nodeId: string) => void", description: "" },
    undo: { type: "() => void", description: "" },
    updateBranchRouting: { type: "(nodeId: string, handleId: string, targetTask: string | undefined) => void", description: "" },
    updateCatchConfig: { type: "(tryCatchNodeId: string, updates: { as?: string; compensate?: boolean }) => void", description: "" },
    updateForEachConfig: { type: "(forEachNodeId: string, updates: Partial<{ batch_size: number; each: string; in: string; max_parallelism: number; on_error: string }>) => void", description: "" },
    updateNodeExport: { type: "(nodeId: string, exportAs: string | undefined) => void", description: "" },
    updateNodeField: { type: "(nodeId: string, fieldPath: string, value: unknown) => void", description: "" },
    updateNodeFlow: { type: "(nodeId: string, thenTarget: string | undefined) => void", description: "" },
    wrapInTryCatch: { type: "(nodeId: string) => void", description: "" },
  }}
/>


### useWorkflowCount

Data hook that fetches the total count of workflows.

Issues a minimal `stigmer.workflow.list()` call to retrieve only the
total count — no workflow entries are returned or stored. Useful for
summary cards, badges, and dashboard widgets.

For the full paginated workflow list, use [`useWorkflowList`](#useworkflowlist) instead.

Pass `null` as `org` to skip fetching (stable no-op).

**Signature**

```tsx
function useWorkflowCount(org: string | null, options?: UseWorkflowCountOptions): UseWorkflowCountReturn
```

<TypeTable
  type={{
    org: { type: "string | null", description: "", required: true },
    options: { type: "UseWorkflowCountOptions", description: "" },
  }}
/>

**Returns** — `UseWorkflowCountReturn`

<TypeTable
  type={{
    count: { type: "number | undefined", description: "Total number of workflows matching the current filters. `undefined` until the first successful fetch completes." },
    error: { type: "Error | null", description: "Error message from the last failed fetch, or `null` when healthy." },
    isLoading: { type: "boolean", description: "`true` while the count fetch is in flight." },
    refetch: { type: "() => void", description: "Re-fetch the count with the same parameters." },
  }}
/>

```tsx
const { count, isLoading } = useWorkflowCount("acme");
```


### useWorkflowDashboardSummary

Data hook that fetches aggregated execution statistics for an organization,
optionally scoped to a single workflow.

Returns phase counts, cost totals, average duration, top failing workflows,
and per-workflow cost breakdowns — scoped to a configurable time window.

**Signature**

```tsx
function useWorkflowDashboardSummary(options: UseWorkflowDashboardSummaryOptions): UseWorkflowDashboardSummaryReturn
```

<TypeTable
  type={{
    options: { type: "UseWorkflowDashboardSummaryOptions", description: "", required: true },
  }}
/>

**Returns** — `UseWorkflowDashboardSummaryReturn`

<TypeTable
  type={{
    error: { type: "Error | null", description: "" },
    isLoading: { type: "boolean", description: "" },
    isRefetching: { type: "boolean", description: "" },
    refetch: { type: "() => void", description: "" },
    summary: { type: "ExecutionSummary | null", description: "" },
  }}
/>

```tsx
// Org-level dashboard summary
const { summary } = useWorkflowDashboardSummary({ org: "acme" });

// Per-workflow overview summary
const { summary } = useWorkflowDashboardSummary({
  org: "acme",
  workflowId: "wf-abc123",
});
```


### useWorkflowEditor

Behavior hook that orchestrates the workflow YAML editor experience.

Composes the validation pipeline, topology computation, and save logic
into a single return value that the `WorkflowEditorView` renders.

**Signature**

```tsx
function useWorkflowEditor(initialYaml: string, options: UseWorkflowEditorOptions): UseWorkflowEditorReturn
```

<TypeTable
  type={{
    initialYaml: { type: "string", description: "The YAML string to initialize the editor with.", required: true },
    options: { type: "UseWorkflowEditorOptions", description: "Editor configuration (organization slug).", required: true },
  }}
/>

**Returns** — `UseWorkflowEditorReturn`

<TypeTable
  type={{
    diagnostics: { type: "readonly Diagnostic[]", description: "CodeMirror-compatible diagnostics from the validation pipeline." },
    errorCount: { type: "number", description: "Count of error-level diagnostics." },
    isDirty: { type: "boolean", description: "`true` when the editor content differs from the initial value." },
    isSaving: { type: "boolean", description: "`true` while a save request is in flight." },
    reset: { type: "() => void", description: "Reset the editor to the initial YAML (discards changes)." },
    save: { type: "() => Promise<boolean>", description: "Persist the current YAML to the server. Returns `true` on success." },
    saveError: { type: "Error | null", description: "Error from the last failed save, or `null` on success." },
    setVersionMessage: { type: "(value: string) => void", description: "Update the version message." },
    setYaml: { type: "(value: string) => void", description: "Update the YAML content (called by the editor on change)." },
    topology: { type: "UseWorkflowTopologyReturn", description: "Topology data for the graph preview.", typeDescriptionLink: "#useworkflowtopologyreturn" },
    versionMessage: { type: "string", description: "Current version message (optional description of what changed)." },
    warningCount: { type: "number", description: "Count of warning-level diagnostics." },
    yaml: { type: "string", description: "Current YAML content in the editor." },
  }}
/>


### useWorkflowExecution

Data hook that fetches a single WorkflowExecution by ID.

Pass `null` for `executionId` to skip fetching (stable no-op).
When the ID changes, the previous request is discarded and a
fresh fetch begins.

If the API returns NOT_FOUND, `execution` is set to `null`
without raising an error.

**Signature**

```tsx
function useWorkflowExecution(executionId: string | null): UseWorkflowExecutionReturn
```

<TypeTable
  type={{
    executionId: { type: "string | null", description: "", required: true },
  }}
/>

**Returns** — `UseWorkflowExecutionReturn`

<TypeTable
  type={{
    error: { type: "Error | null", description: "Error from the last failed request, or `null` when healthy." },
    execution: { type: "WorkflowExecution | null", description: "The resolved execution, or `null` while loading, on error, or when not found." },
    isLoading: { type: "boolean", description: "`true` while the initial fetch is in flight." },
    isRefetching: { type: "boolean", description: "`true` while a background refetch is in flight and stale data is shown." },
    refetch: { type: "() => void", description: "Discard cached data and re-fetch from the server." },
  }}
/>

```tsx
function ExecutionHeader({ id }: { id: string }) {
  const { execution, isLoading, error } = useWorkflowExecution(id);
  if (isLoading) return <Skeleton />;
  if (error) return <ErrorMessage error={error} />;
  if (!execution) return <NotFound />;
  return <h2>{execution.metadata?.name}</h2>;
}
```


### useWorkflowExecutionActions

Behavior hook that encapsulates workflow execution lifecycle actions.

Each action calls the corresponding RPC and returns the updated
execution, or `null` on failure (with `error` populated).

Pass `null` for `executionId` to disable all actions (they become
no-ops that return `null`).

**Signature**

```tsx
function useWorkflowExecutionActions(executionId: string | null, options?: UseWorkflowExecutionActionsOptions): UseWorkflowExecutionActionsReturn
```

<TypeTable
  type={{
    executionId: { type: "string | null", description: "", required: true },
    options: { type: "UseWorkflowExecutionActionsOptions", description: "" },
  }}
/>

**Returns** — `UseWorkflowExecutionActionsReturn`

<TypeTable
  type={{
    approvalErrorsByToolCallId: { type: "ReadonlyMap<string, Error>", description: "Per-gate agent-tool approval failures, keyed by `toolCallId`. Consumed by WorkflowExecutionApprovalCard to surface the failure in-card, beside the gate that failed. Cleared for a gate when it is retried." },
    approvalSubmittingToolCallIds: { type: "ReadonlySet<string>", description: "Tool-call ids whose agent-tool approval is currently being submitted, keyed exactly like approvalErrorsByToolCallId. A workflow can hold many concurrent gates, so deciding one must not spin or disable the others." },
    cancel: { type: "(reason?: string) => Promise<WorkflowExecution | null>", description: "Cancel a running execution gracefully." },
    clearError: { type: "() => void", description: "Reset the lifecycle error to `null`." },
    error: { type: "Error | null", description: "Error from the last failed **lifecycle** action, or `null`. Approval failures are per-gate and live in approvalErrorsByToolCallId / taskApprovalErrorsByTaskName so each surfaces beside the gate that failed — never in this shared scalar (which backs the header banner)." },
    fileDecisionErrorsByKey: { type: "ReadonlyMap<string, Error>", description: "Per-decision file-review failures, keyed like fileDecisionSubmittingKeys. Pass straight through to FileReviewCard's `decisionErrors`." },
    fileDecisionSubmittingKeys: { type: "ReadonlySet<string>", description: "Decision keys currently being submitted for file reviews, keyed exactly like FileReviewCard's `submittingDecisionKeys` (via fileDecisionKey — the change set id for a whole-set decision, `changeSetId:fileChangeId` for a per-file one). Pass straight through to each surfaced card." },
    isSubmitting: { type: "boolean", description: "`true` while a **lifecycle** action (cancel/terminate/pause/resume/recover) is in flight. Approvals are excluded — they are per-gate, so read their in-flight state from approvalSubmittingToolCallIds / taskApprovalSubmittingTaskNames instead." },
    pause: { type: "(reason?: string) => Promise<WorkflowExecution | null>", description: "Pause a running execution." },
    recover: { type: "(reason?: string) => Promise<WorkflowExecution | null>", description: "Recover a failed execution with task-level resume.  Terminates stale workflows, re-resolves environment variables (picks up any config fixes), and starts a fresh run in recovery mode. Completed tasks are skipped — their outputs are restored from the event log — and execution resumes from the first incomplete or failed task." },
    resume: { type: "() => Promise<WorkflowExecution | null>", description: "Resume a paused execution." },
    submitApproval: { type: "(toolCallId: string, action: ApprovalAction, comment?: string) => Promise<WorkflowExecution | null>", description: "Submit an approval decision for a child agent's tool execution." },
    submitFileDecision: { type: "(childAgentExecutionId: string, changeSetId: string, action: FileDecisionAction, options?: FileDecisionOptions) => Promise<WorkflowExecution | null>", description: "Submit a keep/discard decision for a child agent's file review, surfaced on this workflow via `status.pending_file_reviews`. The file-review sibling of submitApproval: the server forwards it to the child's `AgentExecution.submitFileDecision`.  `childAgentExecutionId` comes from the surfaced `WorkflowPendingFileReview` reference. `options` carries the same per-decision detail as FileReviewCard's `onSubmit` (scope, `fileChangeId`, `expectedDigest`, `reason`, `acknowledgeUnreviewable`)." },
    submitTaskApproval: { type: "(taskName: string, outcome: string, formData?: Record<string, unknown>, comment?: string) => Promise<WorkflowExecution | null>", description: "Submit a reviewer's decision for a workflow-level human_input task.  Unlike `submitApproval` (which forwards agent tool approvals), this sends a Temporal signal to the workflow runner's human_input orchestrator, unblocking the task with the selected outcome." },
    taskApprovalErrorsByTaskName: { type: "ReadonlyMap<string, Error>", description: "Per-gate human_input task approval failures, keyed by `taskName`. Consumed by `WorkflowTaskApprovalCard` to surface the failure in-card, beside the gate that failed. Cleared for a gate when it is retried." },
    taskApprovalSubmittingTaskNames: { type: "ReadonlySet<string>", description: "Task names whose human_input task approval is currently being submitted, keyed exactly like taskApprovalErrorsByTaskName." },
    terminate: { type: "(reason?: string) => Promise<WorkflowExecution | null>", description: "Terminate a running execution immediately." },
  }}
/>

```tsx
const actions = useWorkflowExecutionActions(executionId, {
  onSuccess: () => refetchExecution(),
});

<button onClick={() => actions.cancel("No longer needed")} disabled={actions.isSubmitting}>
  Cancel
</button>
```


### useWorkflowExecutionArtifacts

Data hook that fetches artifacts produced by a workflow execution.

Pass `null` for `executionId` to skip fetching.

**Signature**

```tsx
function useWorkflowExecutionArtifacts(executionId: string | null): UseWorkflowExecutionArtifactsReturn
```

<TypeTable
  type={{
    executionId: { type: "string | null", description: "", required: true },
  }}
/>

**Returns** — `UseWorkflowExecutionArtifactsReturn`

<TypeTable
  type={{
    artifacts: { type: "readonly Artifact[]", description: "Artifacts produced by this execution." },
    error: { type: "Error | null", description: "Error from the last failed request, or `null` when healthy." },
    isLoading: { type: "boolean", description: "`true` while the initial fetch is in flight." },
    isRefetching: { type: "boolean", description: "`true` while a background refetch is in flight and stale data is shown." },
    refetch: { type: "() => void", description: "Discard cached data and re-fetch from the server." },
  }}
/>

```tsx
const { artifacts, isLoading } = useWorkflowExecutionArtifacts(executionId);
```


### useWorkflowExecutionEventLog

Data hook that fetches the paginated event log for a workflow execution.

Pass `null` for `executionId` to skip fetching. Supports cursor-based
pagination via `afterSequence` and optional filters by event type or
task name.

**Signature**

```tsx
function useWorkflowExecutionEventLog(executionId: string | null, options?: UseWorkflowExecutionEventLogOptions): UseWorkflowExecutionEventLogReturn
```

<TypeTable
  type={{
    executionId: { type: "string | null", description: "", required: true },
    options: { type: "UseWorkflowExecutionEventLogOptions", description: "" },
  }}
/>

**Returns** — `UseWorkflowExecutionEventLogReturn`

<TypeTable
  type={{
    error: { type: "Error | null", description: "Error from the last failed request, or `null` when healthy." },
    events: { type: "readonly WorkflowExecutionEvent[]", description: "Events in the current page, ordered by sequence_number ascending." },
    hasMore: { type: "boolean", description: "Whether more events exist after the last event in this response." },
    isLoading: { type: "boolean", description: "`true` while the initial fetch is in flight." },
    isRefetching: { type: "boolean", description: "`true` while a background refetch is in flight and stale data is shown." },
    latestSequence: { type: "bigint", description: "Highest sequence_number returned, for cursor-based pagination." },
    refetch: { type: "() => void", description: "Discard cached data and re-fetch from the server." },
  }}
/>

```tsx
const { events, hasMore, latestSequence } = useWorkflowExecutionEventLog(
  executionId,
  { pageSize: 50 },
);
```


### useWorkflowExecutionEventStream

Behavior hook that manages a live event stream subscription for a
workflow execution, or batch-loads events for completed executions.

Internally maintains a `WorkflowExecutionEventStore` and
exposes its state via `useSyncExternalStore` for efficient React
integration.

For running executions: subscribes via `subscribeEvents` with
replay+live-tail. A transient drop auto-reconnects with exponential
backoff, resuming from the last received sequence number so no events are
lost; `error` is surfaced only once retries are exhausted. A clean stream
end is the server's completion signal and is never retried.

For terminal executions: loads the full event log via paginated
`getEventLog` calls.

On recovery (terminal → active phase transition for the same
execution), the store is reset so stale events from the failed run
are cleared before the new subscription begins.

When `executionId` changes to a different execution, the store is
reset before subscribing — mirroring `useFetch`'s identity-change
reset — so the previous run's append-only events neither render as
stale progress nor cause the live subscription to resume from the
wrong `afterSequence`.

Pass `null` for `executionId` to skip (stable no-op).

**Signature**

```tsx
function useWorkflowExecutionEventStream(executionId: string | null, options?: UseWorkflowExecutionEventStreamOptions): UseWorkflowExecutionEventStreamReturn
```

<TypeTable
  type={{
    executionId: { type: "string | null", description: "", required: true },
    options: { type: "UseWorkflowExecutionEventStreamOptions", description: "" },
  }}
/>

**Returns** — `UseWorkflowExecutionEventStreamReturn`

<TypeTable
  type={{
    costSummary: { type: "DerivedCostSummary", description: "Derived cost/budget summary from the latest checkpoint or aggregation." },
    error: { type: "Error | null", description: "Error from the last failed stream attempt, or `null`. Set only once auto-reconnect exhausts its attempts (or for a non-transient failure)." },
    events: { type: "readonly WorkflowExecutionEvent[]", description: "All accumulated events, ordered by sequence_number ascending." },
    isConnecting: { type: "boolean", description: "`true` while connecting to the event stream." },
    isReconnecting: { type: "boolean", description: "`true` while a transient drop is being retried automatically. Accumulated events stay visible and `error` remains `null`; on success the subscription resumes from the last sequence number with no events lost." },
    isStreaming: { type: "boolean", description: "`true` while receiving live events." },
    reconnect: { type: "() => void", description: "Re-establish the stream subscription (manual fallback)." },
    reconnectAttempt: { type: "number", description: "1-based count of the in-flight reconnect attempt; `0` when not reconnecting." },
    streamState: { type: "WorkflowEventStreamState", description: "Stream lifecycle state." },
    taskStates: { type: "ReadonlyMap<string, DerivedTaskState>", description: "Derived task states from the event log." },
    totalTasks: { type: "number", description: "Total tasks declared in the execution_started event." },
  }}
/>


### useWorkflowExecutionList

Data hook that fetches a paginated list of workflow executions.

When `workflowId` is provided, fetches executions for that specific
workflow via `listByWorkflow()`. When omitted, fetches all executions
across all workflows via `list()`.

**Signature**

```tsx
function useWorkflowExecutionList(options?: UseWorkflowExecutionListOptions): UseWorkflowExecutionListReturn
```

<TypeTable
  type={{
    options: { type: "UseWorkflowExecutionListOptions", description: "" },
  }}
/>

**Returns** — `UseWorkflowExecutionListReturn`

<TypeTable
  type={{
    error: { type: "Error | null", description: "Error from the last failed request, or `null` when healthy." },
    executions: { type: "readonly WorkflowExecution[]", description: "Execution entries for the current page." },
    isLoading: { type: "boolean", description: "`true` while the initial fetch or a refetch is in flight." },
    isRefetching: { type: "boolean", description: "`true` while a background refetch is in flight and stale data is shown." },
    refetch: { type: "() => void", description: "Discard cached data and re-fetch from the server." },
    totalPages: { type: "number", description: "Total pages available at the current page size." },
  }}
/>

```tsx
// All executions
const { executions, isLoading } = useWorkflowExecutionList();

// Executions for a specific workflow
const { executions } = useWorkflowExecutionList({
  workflowId: workflow.metadata?.id,
  pageSize: 10,
});
```


### useWorkflowInstance

Data hook that fetches a single WorkflowInstance by ID.

Wraps `stigmer.workflowInstance.get()` with loading and error state.
Pass `null` or `undefined` as `instanceId` to skip fetching (stable no-op).

**Signature**

```tsx
function useWorkflowInstance(instanceId: string | null | undefined): UseWorkflowInstanceReturn
```

<TypeTable
  type={{
    instanceId: { type: "string | null | undefined", description: "", required: true },
  }}
/>

**Returns** — `UseWorkflowInstanceReturn`

<TypeTable
  type={{
    error: { type: "Error | null", description: "Error from the last failed request, or `null` when healthy." },
    instance: { type: "WorkflowInstance | null", description: "The fetched WorkflowInstance, or `null` while loading or on error." },
    isLoading: { type: "boolean", description: "`true` while the initial fetch is in flight." },
    isRefetching: { type: "boolean", description: "`true` while a background refetch is in flight and stale data is shown." },
    refetch: { type: "() => void", description: "Discard cached data and re-fetch the instance from the server." },
  }}
/>

```tsx
const { instance, isLoading } = useWorkflowInstance(selectedInstanceId);
```


### useWorkflowInstances

Data hook that fetches all instances for a specific workflow.

Wraps `stigmer.workflowInstance.getByWorkflow()` with loading
and error state management. Used on the Workflow detail page
"Instances" tab to show environment-bound deployments.

Pass `null` as `workflowId` to skip fetching (stable no-op).

**Signature**

```tsx
function useWorkflowInstances(workflowId: string | null | undefined): UseWorkflowInstancesReturn
```

<TypeTable
  type={{
    workflowId: { type: "string | null | undefined", description: "", required: true },
  }}
/>

**Returns** — `UseWorkflowInstancesReturn`

<TypeTable
  type={{
    error: { type: "Error | null", description: "Error from the last failed request, or `null` when healthy." },
    instances: { type: "readonly WorkflowInstance[]", description: "Instances for the given workflow, or empty array while loading." },
    isLoading: { type: "boolean", description: "`true` while the initial fetch or a refetch is in flight." },
    isRefetching: { type: "boolean", description: "`true` while a background refetch is in flight and stale data is shown." },
    refetch: { type: "() => void", description: "Discard cached data and re-fetch instances from the server." },
  }}
/>

```tsx
const { instances, isLoading } = useWorkflowInstances(workflow.metadata?.id);
```


### useWorkflowList

Data hook that fetches a paginated list of workflows for the Workflows section.

Wraps `stigmer.workflow.list()` with pagination, scope filtering,
and text search. All parameters are externally controlled — the
consumer manages page state, query debouncing, and scope toggling.

Pass `null` as `org` to skip fetching (stable no-op).

**Signature**

```tsx
function useWorkflowList(org: string | null, options?: UseWorkflowListOptions): UseWorkflowListReturn
```

<TypeTable
  type={{
    org: { type: "string | null", description: "", required: true },
    options: { type: "UseWorkflowListOptions", description: "" },
  }}
/>

**Returns** — `UseWorkflowListReturn`

<TypeTable
  type={{
    currentPage: { type: "number", description: "The current page number (mirrors the `page` option)." },
    error: { type: "Error | null", description: "Error from the last failed request, or `null` when healthy." },
    isLoading: { type: "boolean", description: "`true` while the initial fetch or a refetch is in flight." },
    refetch: { type: "() => void", description: "Discard cached data and re-fetch the current page from the server." },
    totalCount: { type: "number", description: "Total number of workflows matching the current filters." },
    totalPages: { type: "number", description: "Total pages available at the current page size." },
    workflows: { type: "readonly SearchResult[]", description: "Paginated workflow entries for the current page." },
  }}
/>

```tsx
const { workflows, totalCount, isLoading } = useWorkflowList("acme", {
  page: 1,
  pageSize: 20,
  scope: "org",
});
```


### useWorkflowSave

Behavior hook that persists workflow YAML to the server via `apply()`.

Parses the YAML into a `WorkflowInput`, calls `stigmer.workflow.apply()`,
and returns success/failure state. Handles UNIMPLEMENTED gracefully
(backend not yet deployed) with a descriptive error message.

**Signature**

```tsx
function useWorkflowSave(org: string): UseWorkflowSaveReturn
```

<TypeTable
  type={{
    org: { type: "string", description: "Organization slug for the target workflow.", required: true },
  }}
/>

**Returns** — `UseWorkflowSaveReturn`

<TypeTable
  type={{
    error: { type: "Error | null", description: "Error from the last failed save, or `null` on success." },
    isSaving: { type: "boolean", description: "`true` while a save request is in flight." },
    save: { type: "(yaml: string, options?: WorkflowSaveOptions) => Promise<boolean>", description: "Parse the YAML and call `workflow.apply()` on the server. Returns `true` on success, `false` on failure (check `error`)." },
  }}
/>


### useWorkflowTopology

Behavior hook that computes a DAG topology from workflow YAML.

Parses the YAML into tasks and builds a node + edge graph suitable
for rendering. Handles sequential flow, explicit `flow.then` directives,
and `switch_case` branching edges.

Returns stable refs (DD-010) — the result is memoized and only
recomputed when the YAML content changes.

**Signature**

```tsx
function useWorkflowTopology(yaml: string | null): UseWorkflowTopologyReturn
```

<TypeTable
  type={{
    yaml: { type: "string | null", description: "The current workflow YAML string (or `null`/empty to skip).", required: true },
  }}
/>

**Returns** — `UseWorkflowTopologyReturn`

<TypeTable
  type={{
    edges: { type: "readonly TopologyEdge[]", description: "" },
    nodes: { type: "readonly TopologyNode[]", description: "" },
  }}
/>


### useWorkflowValidation

Behavior hook that validates workflow YAML and produces CodeMirror diagnostics.

The pipeline runs entirely client-side with 150ms debounce:
1. YAML syntax check (parse errors)
2. Structural validation (required fields: document, tasks)
3. Per-task kind validation (known kinds, required task fields)
4. Task config schema validation (against TaskKindRegistry JSON schemas)
5. Reference validation (flow.then targets, duplicate task names)

Each diagnostic is source-mapped to exact character positions using the
`yaml` library's CST range information.

**Signature**

```tsx
function useWorkflowValidation(yaml: string, registry: UseTaskKindRegistryReturn | null): UseWorkflowValidationReturn
```

<TypeTable
  type={{
    yaml: { type: "string", description: "The current YAML content string.", required: true },
    registry: { type: "UseTaskKindRegistryReturn | null", description: "The task kind registry for schema validation.", required: true },
  }}
/>

**Returns** — `UseWorkflowValidationReturn`

<TypeTable
  type={{
    diagnostics: { type: "readonly Diagnostic[]", description: "CodeMirror-compatible diagnostics mapped to source positions." },
  }}
/>


### useWorkflowYaml

Data hook that fetches a Workflow and returns its YAML representation.

Composes [`useWorkflow`](#useworkflow) with [`serializeWorkflowYaml`](#serializeworkflowyaml) to
provide a memoized YAML string ready for editor rendering.

**Signature**

```tsx
function useWorkflowYaml(org: string | null, slug: string | null): UseWorkflowYamlReturn
```

<TypeTable
  type={{
    org: { type: "string | null", description: "Organization slug, or `null` to skip fetching.", required: true },
    slug: { type: "string | null", description: "Workflow slug, or `null` to skip fetching.", required: true },
  }}
/>

**Returns** — `UseWorkflowYamlReturn`

<TypeTable
  type={{
    error: { type: "Error | null", description: "Error from the last failed request, or `null` when healthy." },
    isLoading: { type: "boolean", description: "`true` while the workflow is being fetched." },
    refetch: { type: "() => void", description: "Discard cached data and re-fetch the workflow from the server." },
    workflowId: { type: "string | null", description: "The resource ID (needed for the save/update path)." },
    yaml: { type: "string | null", description: "The workflow resource serialized as canonical YAML, or `null` while loading." },
  }}
/>


## Components

### CreateWorkflowInstanceDialog

Modal dialog for creating a new WorkflowInstance.

Collects: name (required), description, environment bindings (ordered),
and visibility. Submits via `useCreateWorkflowInstance`.

Uses native `<dialog>` element for proper focus trapping and escape handling.

**Props** — `CreateWorkflowInstanceDialogProps`

<TypeTable
  type={{
    onCreated: { type: "(instance: WorkflowInstance) => void", description: "Called after the instance is successfully created." },
    onOpenChange: { type: "(open: boolean) => void", description: "Called when the dialog should open or close.", required: true },
    open: { type: "boolean", description: "Whether the dialog is open.", required: true },
    org: { type: "string", description: "Organization slug.", required: true },
    workflowId: { type: "string", description: "Workflow ID to bind the new instance to.", required: true },
  }}
/>


### RunVisibilityControl

Segmented control for an instance's run (execution) visibility — a separate
axis from the instance's own visibility: an owner can keep the instance
private while letting the whole org observe its runs.

Writes the `execution_visibility` spec field via the dedicated RPC, which
reconciles the dormant `workflow_instance#execution_viewer` FGA relation in
Cloud mode. `unspecified` is treated as private (the default).

Only meaningful while the instance itself is private; an ORG/PUBLIC instance
already exposes its runs to org members by inheritance, so callers should
render this only in the private case.

**Props** — `RunVisibilityControlProps`

<TypeTable
  type={{
    executionVisibility: { type: "WorkflowExecutionVisibility", description: "Current `execution_visibility` from the instance spec.", required: true },
    instanceId: { type: "string", description: "Id of the workflow instance whose run visibility is edited.", required: true },
    onChanged: { type: "() => void", description: "Called after a successful change so the host can refresh the instance." },
  }}
/>


### WorkflowArchitectDialog

Three-phase dialog for generating a workflow via the Workflow Architect agent.

**Phase 1 (Input)**: The user describes the workflow they want.

**Phase 2 (Streaming)**: The agent works in real-time — querying task kinds,
validating YAML, iterating on the design. Messages and tool calls stream
into a `MessageThread` inside the dialog.

**Phase 3 (Result)**: The validated YAML is extracted from the agent's
response, shown in a read-only preview alongside the agent's explanation.
The user can create the workflow, try again, or close.

Uses the same `<dialog>` + `showModal()` pattern as `WorkflowRunDialog`.
Styled via `--stgm-*` design tokens (DD-005).

**Props** — `WorkflowArchitectDialogProps`

<TypeTable
  type={{
    onError: { type: "(message: string) => void", description: "Called when generation or creation fails. Receives a human-readable message. Use for toast notifications." },
    onOpenChange: { type: "(open: boolean) => void", description: "Called when the dialog should close (cancel, backdrop click, Escape).", required: true },
    onSuccess: { type: "(org: string, slug: string) => void", description: "Called after the workflow is created successfully. Receives the org slug and workflow slug for navigation.", required: true },
    open: { type: "boolean", description: "Whether the dialog is open.", required: true },
    org: { type: "string", description: "Organization slug for generation context and workflow creation.", required: true },
  }}
/>

```tsx
<WorkflowArchitectDialog
  open={showDialog}
  onOpenChange={setShowDialog}
  org="acme"
  onSuccess={(org, slug) => router.push(`/library/workflows/${org}/${slug}`)}
  onError={(msg) => toast.error(msg)}
/>
```


### WorkflowDetailView

Operational detail hub for a Workflow blueprint.

Fetches the workflow via [`useWorkflow`](#useworkflow) internally and renders
its full specification inside a [`ResourceDetailShell`](/docs/sdk/react/resource-detail#resourcedetailshell):

- **Overview**: Description, budget, env vars, task flow DAG, document metadata
- **Instances**: Environment-bound deployments (embedded, not standalone)
- **Executions**: Recent executions with phase badges and timing

Handles loading, error, and not-found states automatically.
Zero Console dependencies — safe for platform builder embedding.
All visual properties flow through `--stgm-*` design tokens.

**Props** — `WorkflowDetailViewProps`

<TypeTable
  type={{
    actions: { type: "readonly DetailAction[]", description: "Secondary actions rendered in the kebab overflow menu." },
    activeTab: { type: "string", description: "Controlled active tab ID." },
    additionalTabs: { type: "readonly AdditionalTab[]", description: "Additional tabs to render alongside the built-in tabs." },
    className: { type: "string", description: "Additional CSS classes for the root container." },
    defaultTab: { type: "string", description: "Default active tab ID when in uncontrolled mode." },
    editable: { type: "boolean", description: "When `true`, description and environment variables become click-to-edit. Each field saves independently via `stigmer.workflow.update()`." },
    instancesRefreshKey: { type: "number", description: "Increment this value to trigger a refetch of the instance list. Useful after externally creating or deleting an instance." },
    onCreateInstanceClick: { type: "() => void", description: "Called when the user clicks \"Create Instance\" in the Instances tab. Opens the create instance dialog." },
    onExecutionClick: { type: "(executionId: string) => void", description: "Called when a user clicks an execution row in the Executions tab. Receives the execution ID — use for navigation to the execution viewer." },
    onInstanceClick: { type: "(instance: WorkflowInstance) => void", description: "Called when the user clicks an instance row in the Instances tab." },
    onInstanceDeleteClick: { type: "(instance: WorkflowInstance) => void", description: "Called when the user clicks \"Delete\" on a specific instance." },
    onInstanceRunClick: { type: "(instance: WorkflowInstance) => void", description: "Called when the user clicks \"Run\" on a specific instance." },
    onOpenInEditor: { type: "(taskName: string) => void", description: "Called when the user clicks \"Open in editor\" from the overview graph node popover. Receives the task name. Wire this to switch to the editor tab and optionally select the node." },
    onResourceLoad: { type: "(meta: { id: string; name: string }) => void", description: "Called once when the workflow resource has been fetched successfully. Provides the resource display name for breadcrumbs, document titles, etc." },
    onResourceUpdated: { type: "(workflow: Workflow) => void", description: "Called after a successful inline field save with the updated workflow." },
    onTabChange: { type: "(tabId: string) => void", description: "Controlled tab change handler." },
    onViewLatestRun: { type: "(executionId: string) => void", description: "Called when the user clicks \"View latest run\" in the overview quick actions. Receives the execution ID of the most recent execution." },
    org: { type: "string", description: "Organization slug that owns the workflow.", required: true },
    primaryAction: { type: "DetailAction", description: "Primary action rendered as a visible button in the header area.", typeDescriptionLink: "/docs/sdk/react/resource-detail#detailaction" },
    slug: { type: "string", description: "Workflow slug (URL-friendly identifier unique within the org).", required: true },
  }}
/>

```tsx
<WorkflowDetailView org="acme" slug="onboard-user" />
```


### WorkflowFileReviewList

Renders the child agent file reviews surfaced on a parent WorkflowExecution.

Reference-only, derive-from-child: the parent status carries only
`WorkflowPendingFileReview` references (child id + change set ids). This
list mounts one `WorkflowChildFileReview` per referenced child, which
streams that child's execution and reuses the agent-level [`FileReviewCard`](/docs/sdk/react/execution#filereviewcard)
to render the actual diff — the heavy content stays single-sourced on the child.
A decision is forwarded to the child via `WorkflowExecution.submitFileDecision`.

This is the file-review sibling of the workflow tool-approval surface
(`WorkflowExecutionApprovalCard`). Empty `pendingFileReviews` renders nothing.

**Props** — `WorkflowFileReviewListProps`

<TypeTable
  type={{
    className: { type: "string", description: "Additional CSS class names for the root container." },
    decisionErrors: { type: "ReadonlyMap<string, Error>", description: "Per-decision failures (pass `fileDecisionErrorsByKey` straight through)." },
    onNavigateToAgentExecution: { type: "(agentExecutionId: string) => void", description: "Optional deep-link to open the child agent execution in its own view. When provided, each child's review renders a \"View agent execution\" affordance. Routing is the host's responsibility (DD-004)." },
    onSubmitFileDecision: { type: "WorkflowFileDecisionSubmit", description: "Forwards a decision to the child (see WorkflowFileDecisionSubmit).", required: true, typeDescriptionLink: "#workflowfiledecisionsubmit" },
    pendingFileReviews: { type: "readonly WorkflowPendingFileReview[]", description: "The parent workflow's surfaced file-review references (`status.pending_file_reviews`) — one entry per child agent execution.", required: true },
    submittingDecisionKeys: { type: "ReadonlySet<string>", description: "In-flight decision keys (pass `fileDecisionSubmittingKeys` straight through)." },
  }}
/>


### WorkflowInstanceDetailPanel

Expandable detail panel for a single WorkflowInstance.

Shows metadata, description (inline-editable), environment bindings (editable),
visibility control, share panel (FGA-gated), and a delete action with cascade warning.

**Props** — `WorkflowInstanceDetailPanelProps`

<TypeTable
  type={{
    instance: { type: "WorkflowInstance", description: "The instance to display.", required: true, typeDescriptionLink: "/docs/sdk/resources/workflow-instance" },
    onClose: { type: "() => void", description: "Called when the panel should close.", required: true },
    onDeleted: { type: "() => void", description: "Called after successful deletion." },
    onExecutionClick: { type: "(executionId: string) => void", description: "Called when the user clicks an execution in the recent list." },
    onRunClick: { type: "(instance: WorkflowInstance) => void", description: "Called when the user clicks \"Run\" for this instance." },
    onUpdated: { type: "() => void", description: "Called when instance data changes (for parent refetch)." },
    org: { type: "string", description: "Organization slug.", required: true },
  }}
/>


### WorkflowInstanceEmptyState

Empty state shown in the Instances tab when no user-created instances exist.

Communicates the value proposition of workflow instances without
mentioning the platform-managed default instance (implementation detail).

**Props** — `WorkflowInstanceEmptyStateProps`

<TypeTable
  type={{
    className: { type: "string", description: "Additional CSS class names." },
    onCreateClick: { type: "() => void", description: "Called when the user clicks \"Create Instance\"." },
  }}
/>


### WorkflowInstanceList

Enhanced workflow instance list with environment badges, visibility controls,
and action buttons. Filters out the platform-managed default instance.

This is an SDK component (DD-001) — embeddable by platform builders.

**Props** — `WorkflowInstanceListProps`

<TypeTable
  type={{
    className: { type: "string", description: "Additional CSS class names." },
    defaultInstanceId: { type: "string", description: "The default instance ID (from workflow.status.defaultInstanceId) — filtered out of the list." },
    onCreateClick: { type: "() => void", description: "Called when the user wants to create a new instance." },
    onDeleteClick: { type: "(instance: WorkflowInstance) => void", description: "Called when the user clicks \"Delete\" on an instance." },
    onInstanceClick: { type: "(instance: WorkflowInstance) => void", description: "Called when the user clicks an instance row." },
    onRunClick: { type: "(instance: WorkflowInstance) => void", description: "Called when the user clicks \"Run\" on an instance." },
    org: { type: "string", description: "Organization slug (needed for environment resolution and create flow).", required: true },
    refreshKey: { type: "number", description: "Increment this value to trigger a refetch of the instance list. Useful after creating or deleting an instance externally." },
    workflowId: { type: "string", description: "Workflow resource ID to list instances for.", required: true },
  }}
/>


### WorkflowRefinePanel

Side panel for agent-powered iterative workflow refinement.

Renders inside the editor's right pane (replacing the topology graph
in code mode, or as a sidebar in visual mode). Layout:

1. **Header** — title + close button
2. **MessageThread** — streaming agent conversation (all turns)
3. **Result strip** — diff preview + accept/discard (when YAML extracted)
4. **Composer** — textarea + send button (pinned to bottom)

Powered by the Workflow Architect system agent via
[`useRefineWorkflowFlow`](#userefineworkflowflow). Styled via `--stgm-*` design tokens.
Zero console dependencies (DD-004).

**Props** — `WorkflowRefinePanelProps`

<TypeTable
  type={{
    className: { type: "string", description: "Additional CSS class names." },
    currentYaml: { type: "string", description: "Current workflow YAML from the editor.", required: true },
    initialInstruction: { type: "string", description: "When provided, auto-sends this instruction on mount (e.g. from \"Fix with AI\"). Consumed once — subsequent renders with the same value are ignored." },
    onAccept: { type: "(yaml: string) => void", description: "Called when the user accepts a refinement. Receives the updated YAML.", required: true },
    onClose: { type: "() => void", description: "Called when the panel should close.", required: true },
    org: { type: "string", description: "Organization slug for refinement context.", required: true },
  }}
/>


### WorkflowRepairCard

Panel component that displays agent-powered diagnosis of a failed workflow
execution. Designed for the execution viewer's right panel (AD-B5-001).

Layout:
1. Header — "AI Diagnosis" title with sparkles icon + close button
2. Message area — `MessageThread` showing the agent conversation
3. Result strip (conditional) — fix explanation, diff preview, Apply Fix / Discard
4. Follow-up composer (pinned to bottom) — text input for additional questions

Auto-starts diagnosis on mount (AD-B5-002). Supports multi-turn
follow-up questions within the same session (AD-B5-003).

Styled via `--stgm-*` design tokens. Zero console dependencies (DD-004).

**Props** — `WorkflowRepairCardProps`

<TypeTable
  type={{
    className: { type: "string", description: "Additional CSS class names." },
    currentWorkflowYaml: { type: "string", description: "Current workflow YAML for diff computation (optional)." },
    executionId: { type: "string", description: "ID of the failed workflow execution to diagnose.", required: true },
    onApplyFix: { type: "(yaml: string) => void", description: "Called when the user clicks \"Apply Fix\". Receives the suggested YAML. The host application handles navigation to the workflow editor (DD-004)." },
    onClose: { type: "() => void", description: "Called when the panel should close." },
    org: { type: "string", description: "Organization slug for authorization and resource context.", required: true },
  }}
/>


### WorkflowRunDialog

Dialog for running a workflow execution.

Composes [`useRunWorkflowFlow`](#userunworkflowflow) with [`WorkflowRunForm`](#workflowrunform)
inside a native `<dialog>` element. Manages the full lifecycle:
form fields, validation, submission, error display, and close.

Uses the same `<dialog>` + `showModal()` pattern as
[`ConfirmDialog`](/docs/sdk/react/resource-detail#confirmdialog) — built-in focus trapping, Escape key
handling, and backdrop. Styled via `--stgm-*` design tokens.

**Props** — `WorkflowRunDialogProps`

<TypeTable
  type={{
    defaultInstanceId: { type: "string", description: "The platform-managed default instance ID (from workflow.status.defaultInstanceId). Passed through to the form's instance picker to control visibility threshold." },
    instances: { type: "readonly WorkflowInstance[]", description: "Available workflow instances for the instance selector.", required: true },
    onError: { type: "(message: string) => void", description: "Called when submission fails. Receives a human-readable message. Use for toast notifications." },
    onOpenChange: { type: "(open: boolean) => void", description: "Called when the dialog should close (cancel, backdrop click, Escape).", required: true },
    onSuccess: { type: "(executionId: string) => void", description: "Called after the execution is created successfully. Receives the execution ID — use for navigation.", required: true },
    open: { type: "boolean", description: "Whether the dialog is open.", required: true },
    org: { type: "string", description: "Organization slug that owns the workflow.", required: true },
    workflow: { type: "Workflow", description: "The workflow blueprint to run.", required: true, typeDescriptionLink: "/docs/sdk/resources/workflow" },
  }}
/>

```tsx
<WorkflowRunDialog
  open={showRunDialog}
  onOpenChange={setShowRunDialog}
  org="acme"
  workflow={workflow}
  instances={instances}
  onSuccess={(id) => router.push(`/workflows/executions/${id}`)}
  onError={(msg) => toast.error(msg)}
/>
```


### WorkflowRunForm

Form fields for running a workflow execution.

Renders auto-generated environment variable fields from the workflow's
`spec.env` declarations, an instance selector (hidden when 0-1 instances
exist), and a conditionally-visible trigger message textarea.

Field ordering prioritizes required inputs (env vars) over optional
contextual fields (trigger message), following progressive disclosure.

This component is presentational — it does not manage state or
submit. Pair with [`useRunWorkflowFlow`](#userunworkflowflow) for the full
behavior, or wire the props manually for custom integrations.

All visuals flow through `--stgm-*` design tokens. Zero Console
dependencies — safe for platform builder embedding.

**Props** — `WorkflowRunFormProps`

<TypeTable
  type={{
    className: { type: "string", description: "Additional CSS classes for the root container." },
    defaultInstanceId: { type: "string", description: "The platform-managed default instance ID (from workflow.status.defaultInstanceId). When provided, the picker shows whenever user-created instances exist (>= 1), and labels the default option clearly." },
    disabled: { type: "boolean", description: "When `true`, all fields are disabled (during submission)." },
    envDeclarations: { type: "Record<string, EnvVarDeclaration>", description: "Declared environment variables from the workflow spec.", required: true },
    errors: { type: "RunWorkflowFieldErrors", description: "Field-level validation errors keyed by field name.", required: true, typeDescriptionLink: "#runworkflowfielderrors" },
    instanceEnvKeys: { type: "Set<string>", description: "Set of env var keys already provided by the selected instance's bound environments. Fields for these keys are shown as optional overrides rather than required inputs." },
    instances: { type: "readonly WorkflowInstance[]", description: "Available workflow instances for the selector.", required: true },
    onEnvVarChange: { type: "(key: string, value: string) => void", description: "Called when a single env var value changes.", required: true },
    onInstanceChange: { type: "(id: string | null) => void", description: "Called when the selected instance changes.", required: true },
    onShowTriggerMessageChange: { type: "(show: boolean) => void", description: "Called when the user toggles trigger message visibility.", required: true },
    onTriggerMessageChange: { type: "(value: string) => void", description: "Called when the trigger message changes.", required: true },
    runtimeEnv: { type: "Record<string, string>", description: "Current runtime env var overrides (keyed by variable name).", required: true },
    selectedInstanceId: { type: "string | null", description: "Currently selected instance ID, or `null` for server-resolved default.", required: true },
    showTriggerMessage: { type: "boolean", description: "Whether to show the trigger message field. When `false`, a subtle \"Add trigger input\" toggle is rendered instead.", required: true },
    triggerMessage: { type: "string", description: "Current trigger message value.", required: true },
  }}
/>

```tsx
const flow = useRunWorkflowFlow({ ... });

<WorkflowRunForm
  triggerMessage={flow.triggerMessage}
  onTriggerMessageChange={flow.setTriggerMessage}
  envDeclarations={flow.envDeclarations}
  runtimeEnv={flow.runtimeEnv}
  onEnvVarChange={flow.setEnvVar}
  instances={instances}
  selectedInstanceId={flow.selectedInstanceId}
  onInstanceChange={flow.setSelectedInstanceId}
  showTriggerMessage={flow.showTriggerMessage}
  onShowTriggerMessageChange={flow.setShowTriggerMessage}
  errors={flow.fieldErrors}
  disabled={flow.isSubmitting}
/>
```


### WorkflowTemplateCard

Enhanced template card for workflow templates.

Shows the template name, description, category badge, pattern badges,
task count, and env var count. Click selects the template; an optional
preview button opens the graph preview dialog.

**Props** — `WorkflowTemplateCardProps`

<TypeTable
  type={{
    className: { type: "string", description: "" },
    onPreview: { type: "(template: WorkflowTemplate) => void", description: "" },
    onSelect: { type: "(template: WorkflowTemplate) => void", description: "", required: true },
    template: { type: "WorkflowTemplate", description: "", required: true, typeDescriptionLink: "#workflowtemplate" },
  }}
/>


### WorkflowTemplateGallery

Searchable, filterable gallery of workflow templates.

Composes the headless `useTemplateFilter` hook with workflow-specific
`WorkflowTemplateCard` components. Includes an integrated preview
dialog — clicking the eye icon on a card opens a graph preview
before committing to the template.

Keyboard navigation follows the same pattern as the generic
`TemplateGallery`: Tab between search/tabs/cards, Arrow keys
navigate tabs, Enter/Space activates.

**Props** — `WorkflowTemplateGalleryProps`

<TypeTable
  type={{
    className: { type: "string", description: "" },
    emptyContent: { type: "ReactNode", description: "" },
    onSelect: { type: "(template: WorkflowTemplate) => void", description: "", required: true },
    templates: { type: "readonly WorkflowTemplate[]", description: "", required: true },
  }}
/>


### WorkflowTemplatePreview

Preview dialog for a workflow template.

Shows the template's metadata, pattern badges, and a read-only
graph preview rendered via the YAML → graph pipeline. Uses a
native `<dialog>` element for modal semantics.

The graph preview is lazy — it only parses and lays out the YAML
when the dialog opens, not while browsing cards.

**Props** — `WorkflowTemplatePreviewProps`

<TypeTable
  type={{
    onClose: { type: "() => void", description: "", required: true },
    onSelect: { type: "(template: WorkflowTemplate) => void", description: "", required: true },
    open: { type: "boolean", description: "", required: true },
    template: { type: "WorkflowTemplate | null", description: "", required: true },
  }}
/>


## Types

### ApprovalFormBuilder

Specialized inspector editor for `human_input` task nodes.

Organized into collapsible sections: Prompt, Outcomes, Form Fields,
Timeout, Approvers, and Notification Channels. Outcome routing uses
name-based handle IDs (`outcome_{name}`) per AD-T15-B4.


### ApprovalFormBuilderProps

Props for [`ApprovalFormBuilder`](#approvalformbuilder).

<TypeTable
  type={{
    allTaskNames: { type: "readonly string[]", description: "", required: true },
    config: { type: "JsonObject", description: "", required: true },
    edges: { type: "readonly WorkflowGraphEdge[]", description: "", required: true },
    nodeId: { type: "string", description: "", required: true },
    onMigrateBranchHandle: { type: "(oldHandleId: string, newHandleId: string) => void", description: "", required: true },
    onRemoveBranchEdges: { type: "(handleId: string) => void", description: "", required: true },
    onUpdateBranchRouting: { type: "(handleId: string, targetTask: string | undefined) => void", description: "", required: true },
    onUpdateConfig: { type: "(fieldPath: string, value: unknown) => void", description: "", required: true },
  }}
/>


### ArchitectPhase

Lifecycle phases for the Workflow Architect generate flow.

- `idle` — waiting for user to submit a prompt
- `starting` — creating session + execution
- `streaming` — agent is working (tool calls, validation, generation)
- `complete` — agent finished and YAML was extracted successfully
- `extraction-failed` — agent finished but no YAML block was found
- `applying` — creating the workflow from extracted YAML
- `error` — a failure occurred (RPC, stream, or apply)


### BranchConditionBuilder

Specialized inspector editor for `switch_case` task nodes.

Renders an ordered list of conditional branches with name, condition
expression (`when`), and target task routing. Handles are name-based
(`case_{name}`) per AD-T15-B4, making reorder operations safe without
edge remapping.

Edges are the source of truth for routing — the builder reads target
tasks from graph edges and writes routing changes via
`onUpdateBranchRouting`.


### BranchConditionBuilderProps

Props for [`BranchConditionBuilder`](#branchconditionbuilder).

<TypeTable
  type={{
    allTaskNames: { type: "readonly string[]", description: "", required: true },
    config: { type: "JsonObject", description: "", required: true },
    edges: { type: "readonly WorkflowGraphEdge[]", description: "", required: true },
    nodeId: { type: "string", description: "", required: true },
    onMigrateBranchHandle: { type: "(oldHandleId: string, newHandleId: string) => void", description: "", required: true },
    onRemoveBranchEdges: { type: "(handleId: string) => void", description: "", required: true },
    onUpdateBranchRouting: { type: "(handleId: string, targetTask: string | undefined) => void", description: "", required: true },
    onUpdateConfig: { type: "(fieldPath: string, value: unknown) => void", description: "", required: true },
  }}
/>


### CanvasContextMenu

Right-click context menu for the workflow canvas editor.

Renders different menu items depending on the [`CanvasContextMenuTarget`](#canvascontextmenutarget):
- **Node**: Delete Task, Duplicate Task, Add Task After
- **Edge**: Delete Connection, Insert Task
- **Pane**: Add Task, Select All, Auto-layout

Uses `@base-ui/react/menu` in controlled mode with a virtual anchor
positioned at the right-click coordinates. Portaled via
`useStigmerPortalContainer()` to inherit `--stgm-*` tokens.


### CanvasContextMenuProps

Props for [`CanvasContextMenu`](#canvascontextmenu).

<TypeTable
  type={{
    className: { type: "string", description: "Additional CSS class names for the popup." },
    hasClipboard: { type: "boolean", description: "Whether the internal clipboard has content available to paste." },
    onAddTaskAfter: { type: "(nodeId: string) => void", description: "Called to trigger the \"add task after\" flow (opens TaskPicker)." },
    onAddTaskAtPosition: { type: "() => void", description: "Called to trigger the \"add task at position\" flow (opens TaskPicker)." },
    onAutoLayout: { type: "() => void", description: "Called to trigger auto-layout." },
    onCopyNode: { type: "(nodeId: string) => void", description: "Called to copy a single node to clipboard." },
    onCopySelection: { type: "() => void", description: "Called to copy the current selection (multi-select)." },
    onDeleteEdge: { type: "(edgeId: string) => void", description: "Called to delete an edge." },
    onDeleteNode: { type: "(nodeId: string) => void", description: "Called to delete a node." },
    onDeleteSelection: { type: "() => void", description: "Called to delete the current selection (multi-select)." },
    onDisableSelection: { type: "() => void", description: "Called to disable/enable the current selection (multi-select)." },
    onDuplicateNode: { type: "(nodeId: string) => void", description: "Called to duplicate a node." },
    onDuplicateSelection: { type: "() => void", description: "Called to duplicate the current selection (multi-select)." },
    onFitView: { type: "() => void", description: "Called to zoom/fit the canvas." },
    onInsertTaskOnEdge: { type: "(edgeId: string) => void", description: "Called to trigger the \"insert task on edge\" flow (opens TaskPicker)." },
    onOpenChange: { type: "(open: boolean) => void", description: "Called when the menu open state changes (e.g. clicking outside, pressing Escape).", required: true },
    onPaste: { type: "() => void", description: "Called to paste from clipboard at current position." },
    onRenameNode: { type: "(nodeId: string) => void", description: "Called to rename a node (triggers inline rename in inspector)." },
    onSelectAll: { type: "() => void", description: "Called to select all non-sentinel nodes." },
    onToggleDisabled: { type: "(nodeId: string) => void", description: "Called to toggle disabled state on a node." },
    onViewYaml: { type: "(nodeId: string) => void", description: "Called to view a node's YAML representation." },
    onWrapInTryCatch: { type: "(nodeId: string) => void", description: "Called to wrap a node in try/catch." },
    open: { type: "boolean", description: "Whether the context menu is open.", required: true },
    position: { type: "{ x: number; y: number } | null", description: "Screen-coordinate position where the menu should appear.", required: true },
    target: { type: "CanvasContextMenuTarget | null", description: "What was right-clicked — determines which menu items are shown.", required: true },
  }}
/>


### CanvasContextMenuTarget

Describes what was right-clicked on the canvas.


### CanvasSelection

Selection state for the canvas inspector.

<TypeTable
  type={{
    id: { type: "string", description: "", required: true },
    type: { type: "\"node\" | \"edge\"", description: "", required: true },
  }}
/>


### computeUnifiedDiff

Compute a unified diff between two strings, split by newlines.

Returns an array of [`DiffLine`](#workflowdiffline) objects representing each line's status:
- `"equal"` — line is unchanged
- `"added"` — line exists only in `after`
- `"removed"` — line exists only in `before`

```tsx
function computeUnifiedDiff(before: string, after: string): DiffLine[]
```


### CostByWorkflowChart

Horizontal bar chart showing cost by workflow.

Sorted by total cost (descending). Each bar shows the workflow name,
formatted dollar cost, and execution count as a secondary label.
Uses pure CSS for rendering -- no recharts dependency needed.


### CostByWorkflowChartProps

<TypeTable
  type={{
    breakdowns: { type: "readonly WorkflowCostBreakdown[]", description: "", required: true },
    className: { type: "string", description: "" },
    isLoading: { type: "boolean", description: "", required: true },
  }}
/>


### deriveExecutionRow

Derives a UI-ready [`ExecutionRow`](#executionrow) from a raw `WorkflowExecution`.

Pure function with zero side effects. Computes duration, identifies
the failed/current task, counts retries, and normalizes bigint fields.

```tsx
function deriveExecutionRow(exec: WorkflowExecution): ExecutionRow
```


### deriveExecutionRows

Batch-derives execution rows from a list of raw executions.
Preserves input order.

```tsx
function deriveExecutionRows(executions: readonly WorkflowExecution[]): ExecutionRow[]
```


### deriveFailureAnalysis

Groups failed executions by their failing task name.

Pure function. Only considers executions in FAILED phase. Within each
execution, finds the first FAILED task and groups by its name. Executions
with no identifiable failed task are grouped under `"(unknown)"`.

Returns groups sorted by failure count descending, with instances
within each group sorted by failure time descending (most recent first).

```tsx
function deriveFailureAnalysis(executions: readonly WorkflowExecution[]): FailureGroup[]
```


### deriveTemplateMeta

Derive display metadata from a workflow template's YAML string.

This is a pure function with no React or SDK dependencies — it parses
the YAML, walks the task list, and returns counts, kinds, and detected
structural patterns. Used by the gallery card and preview dialog.

If the YAML fails to parse, returns a safe zero-value result so the
gallery card still renders (graceful degradation).

```tsx
function deriveTemplateMeta(yaml: string): WorkflowTemplateMeta
```


### DiagnosePhase

Lifecycle phases for the workflow execution diagnosis flow.

- `idle` — mounted, waiting for auto-start or manual trigger
- `starting` — creating session + execution
- `streaming` — agent is analyzing (tool calls, reasoning visible)
- `complete` — agent finished, YAML fix extracted (diff available)
- `ready` — agent finished without YAML (runtime error explanation),
  or user accepted/discarded a fix; follow-up available
- `error` — a failure occurred (RPC, stream)


### END_NODE_ID

Sentinel node ID for explicit workflow termination.


### ExecutionClientFilters

<TypeTable
  type={{
    failedTaskName: { type: "string", description: "" },
    hasRetries: { type: "boolean", description: "" },
    maxCostMicros: { type: "bigint", description: "" },
    maxDurationMs: { type: "number", description: "" },
    minCostMicros: { type: "bigint", description: "" },
    minDurationMs: { type: "number", description: "" },
    phases: { type: "readonly ExecutionPhase[]", description: "" },
  }}
/>


### ExecutionFilterBar

Horizontal filter bar for the execution history table.

Renders phase chips, duration presets, cost presets, a "has retries"
toggle, and a clear-all button. Each filter maps to an
[`ExecutionClientFilters`](#executionclientfilters) field.

Fully controlled: the parent owns the filter state.

```tsx
const [filters, setFilters] = useState<ExecutionClientFilters>({});
<ExecutionFilterBar filters={filters} onFiltersChange={setFilters} />
```


### ExecutionFilterBarProps

Props for [`ExecutionFilterBar`](#executionfilterbar).

<TypeTable
  type={{
    className: { type: "string", description: "Additional CSS classes for the root container." },
    filters: { type: "ExecutionClientFilters", description: "Current active filters (controlled).", required: true, typeDescriptionLink: "#executionclientfilters" },
    onFiltersChange: { type: "(filters: ExecutionClientFilters) => void", description: "Called when any filter changes.", required: true },
  }}
/>


### ExecutionHistorySortDirection


### ExecutionHistorySortField


### ExecutionHistoryTable

Data-dense execution history table with sortable columns, keyboard
navigation, and responsive column visibility.

Columns are defined statically and use existing formatters from
`format-utils.ts` and `WorkflowExecutionPhaseBadge`. Sort state
is managed internally with configurable defaults.

This component renders only the table. Filtering, pagination, and
health metrics are composed by the parent `WorkflowExecutionHistory`.

```tsx
<ExecutionHistoryTable
  rows={derivedRows}
  onRowClick={(id) => navigate(`/executions/${id}`)}
/>
```


### ExecutionHistoryTableProps

Props for [`ExecutionHistoryTable`](#executionhistorytable).

<TypeTable
  type={{
    className: { type: "string", description: "Additional CSS classes for the root container." },
    defaultSortDirection: { type: "SortDirection", description: "Initial sort direction.", typeDescriptionLink: "/docs/sdk/react/resource-workbench#sortdirection" },
    defaultSortField: { type: "ExecutionSortField", description: "Initial sort field." },
    error: { type: "Error | null", description: "Error from the last failed fetch." },
    isLoading: { type: "boolean", description: "`true` while data is being fetched for the first time." },
    onRowClick: { type: "(executionId: string) => void", description: "Called when a row is clicked. Receives the execution ID." },
    rows: { type: "readonly ExecutionRow[]", description: "Derived execution rows to display.", required: true },
    visibleColumns: { type: "readonly string[]", description: "Column IDs to display. When omitted, uses each column's `defaultVisible` setting." },
  }}
/>


### ExecutionRow

A flattened, UI-ready row derived from a `WorkflowExecution`.

All computed fields (duration, failed task, retry count) are derived
from the execution's status data — no additional API calls needed.
This type is the contract between the derivation layer and the table
component; it intentionally avoids protobuf types so the table can
be tested and rendered without proto dependencies.

<TypeTable
  type={{
    _source: { type: "WorkflowExecution", description: "Reference to the original execution for downstream consumers that need the full object.", required: true, typeDescriptionLink: "/docs/sdk/resources/workflow-execution" },
    completedAt: { type: "Date | null", description: "", required: true },
    completedTaskCount: { type: "number", description: "", required: true },
    costMicros: { type: "bigint", description: "", required: true },
    currentTaskName: { type: "string | null", description: "Name of the first task in IN_PROGRESS status, or `null` if none.", required: true },
    durationMs: { type: "number | null", description: "Milliseconds between started_at and completed_at. `null` when not yet completed or timestamps missing.", required: true },
    error: { type: "string | null", description: "", required: true },
    failedTaskName: { type: "string | null", description: "Name of the first task in FAILED status, or `null` if none.", required: true },
    hasHumanWait: { type: "boolean", description: "`true` when any task is in WAITING_APPROVAL status.", required: true },
    id: { type: "string", description: "", required: true },
    inputTokens: { type: "bigint", description: "", required: true },
    name: { type: "string", description: "", required: true },
    outputTokens: { type: "bigint", description: "", required: true },
    phase: { type: "ExecutionPhase", description: "", required: true },
    retryCount: { type: "number", description: "Number of retry attempts across all tasks (derived from task metadata).", required: true },
    startedAt: { type: "Date | null", description: "", required: true },
    taskCount: { type: "number", description: "", required: true },
    totalTokens: { type: "bigint", description: "", required: true },
  }}
/>


### ExecutionSummaryWidget

Dashboard widget showing aggregate execution KPIs.

Displays stat cards for active, completed, failed counts and total cost.
Below the cards, shows average duration and a phase breakdown bar.

All visuals use `--stgm-*` tokens. No hardcoded colors.


### ExecutionSummaryWidgetProps

<TypeTable
  type={{
    className: { type: "string", description: "" },
    isLoading: { type: "boolean", description: "", required: true },
    summary: { type: "ExecutionSummary | null", description: "", required: true },
  }}
/>


### ExecutionTrendChart

Phase distribution chart for workflow executions.

Shows a stacked horizontal bar with a legend listing each phase
and its count. Renders from `ExecutionSummary.phaseCounts`.


### ExecutionTrendChartProps

<TypeTable
  type={{
    className: { type: "string", description: "" },
    isLoading: { type: "boolean", description: "", required: true },
    summary: { type: "ExecutionSummary | null", description: "", required: true },
  }}
/>


### ExtractedWorkflowYaml

Extracted workflow YAML and accompanying explanation from an agent response.

<TypeTable
  type={{
    explanation: { type: "string", description: "Prose explanation surrounding the YAML block (concatenated non-YAML text).", required: true },
    yaml: { type: "string", description: "The raw YAML content extracted from the fenced code block.", required: true },
  }}
/>


### extractWorkflowYaml

Extracts the last YAML fenced code block from an agent execution's
assistant messages.

The Workflow Architect agent is instructed to return validated workflow
YAML inside a markdown ````yaml` fence. This function scans all AI
messages in reverse order, finds the last such block, and separates
the YAML content from the surrounding explanation prose.

Returns `null` when no YAML block is found — callers should surface
this as an extraction error.

```tsx
function extractWorkflowYaml(execution: AgentExecution | null): ExtractedWorkflowYaml | null
```


### FailedRunsWidget

Dashboard widget showing recent failed workflow executions.

Consumes data from `useWorkflowExecutionList({ phase: EXECUTION_FAILED })`.
Displays a compact list with execution name, error summary, and time of failure.

All visuals use `--stgm-*` tokens. No hardcoded colors.


### FailedRunsWidgetProps

<TypeTable
  type={{
    className: { type: "string", description: "" },
    executions: { type: "readonly WorkflowExecution[]", description: "Recent failed executions to display.", required: true },
    isLoading: { type: "boolean", description: "", required: true },
    onViewClick: { type: "(executionId: string) => void", description: "Called when the user clicks \"View\" on a failed execution." },
  }}
/>


### FailureAnalysisPanel

Collapsible panel showing failures grouped by failing task name.

Derives failure groups from the provided execution list using
[`deriveFailureAnalysis`](#derivefailureanalysis). Each group shows the task name,
failure count, latest error, and expandable individual failure links.

Renders nothing when there are no failures (no empty state).

```tsx
<FailureAnalysisPanel
  executions={allExecutions}
  onExecutionClick={(id) => navigate(`/executions/${id}`)}
/>
```


### FailureAnalysisPanelProps

Props for [`FailureAnalysisPanel`](#failureanalysispanel).

<TypeTable
  type={{
    className: { type: "string", description: "Additional CSS classes for the root container." },
    executions: { type: "readonly WorkflowExecution[]", description: "Raw executions to analyze. Only FAILED executions are considered.", required: true },
    initialVisibleGroups: { type: "number", description: "Maximum number of failure groups to show initially." },
    onExecutionClick: { type: "(executionId: string) => void", description: "Called when the user clicks a failed execution link." },
  }}
/>


### FailureGroup

A group of failures sharing the same failing task name.

Sorted by `count` descending so the most frequent failures
appear first in the failure analysis panel.

<TypeTable
  type={{
    count: { type: "number", description: "Number of executions where this task failed.", required: true },
    instances: { type: "readonly FailureInstance[]", description: "Individual failed execution references (most recent first).", required: true },
    latestError: { type: "string", description: "Most recent error message from this task.", required: true },
    latestFailedAt: { type: "Date | null", description: "Timestamp of the most recent failure.", required: true },
    taskName: { type: "string", description: "The task name that failed across multiple executions.", required: true },
  }}
/>


### FailureInstance

A single failed execution reference within a [`FailureGroup`](#failuregroup).

<TypeTable
  type={{
    error: { type: "string", description: "", required: true },
    executionId: { type: "string", description: "", required: true },
    executionName: { type: "string", description: "", required: true },
    failedAt: { type: "Date | null", description: "", required: true },
  }}
/>


### filterExecutionRows

Applies client-side filters to execution rows.
Returns a new array containing only rows that match all criteria.

```tsx
function filterExecutionRows(rows: readonly ExecutionRow[], filters: ExecutionClientFilters): ExecutionRow[]
```


### graphToWorkflowInput

Converts a graph model into a `WorkflowInput` suitable for
`stigmer.workflow.apply()`.

This will be the primary save path in Batch 3 when `useWorkflowSave`
is extended to accept `WorkflowInput` directly.

```tsx
function graphToWorkflowInput(graph: WorkflowGraphModel, org: string): WorkflowInput
```


### graphToYaml

Serializes a graph model back to canonical Stigmer Workflow YAML.

Uses topological sort to determine task ordering. Sequential transitions
(task N → task N+1 in sorted order) are implicit and omitted from YAML.
Non-sequential transitions emit explicit `flow.then` directives.

```tsx
function graphToYaml(graph: WorkflowGraphModel): string
```


### HealthMetricsStrip

Compact horizontal strip of key operational health metrics.

Designed for the execution history page context where vertical space
is at a premium. Displays 6 metrics in a single row with dividers:
Total, Success Rate, Avg Duration, Total Cost, Active, Tokens.

Data comes from the existing `ExecutionSummary` proto — no new APIs
needed. Loading state shows a skeleton strip.

```tsx
const { summary, isLoading } = useWorkflowDashboardSummary({ org });
<HealthMetricsStrip summary={summary} isLoading={isLoading} />
```


### HealthMetricsStripProps

Props for [`HealthMetricsStrip`](#healthmetricsstrip).

<TypeTable
  type={{
    className: { type: "string", description: "Additional CSS classes for the root container." },
    isLoading: { type: "boolean", description: "Whether the summary is still loading." },
    summary: { type: "ExecutionSummary | null", description: "Aggregated summary from `useWorkflowDashboardSummary`. `null` while loading.", required: true },
  }}
/>


### parseWorkflowYaml

Parses a Stigmer Workflow YAML string into a `WorkflowInput` suitable
for `stigmer.workflow.apply()`.

Handles snake_case → camelCase conversion for all nested structures
including tasks, env declarations, budget, and flow control.

```tsx
function parseWorkflowYaml(content: string, org: string): WorkflowInput
```


### PATTERN_LABELS

Human-readable labels for pattern badges.


### PendingApprovalsWidget

Dashboard widget showing pending human_input approvals.

Displays a compact list of workflow tasks awaiting reviewer decisions,
with workflow name, task name, waiting duration, and a review action.

All visuals use `--stgm-*` tokens. No hardcoded colors.


### PendingApprovalsWidgetProps

<TypeTable
  type={{
    approvals: { type: "readonly PendingApproval[]", description: "", required: true },
    className: { type: "string", description: "" },
    isLoading: { type: "boolean", description: "", required: true },
    onReviewClick: { type: "(executionId: string) => void", description: "Called when the user clicks \"Review\" on an approval." },
    totalCount: { type: "number", description: "", required: true },
  }}
/>


### RefinePhase

Lifecycle phases for the workflow refinement flow.

- `idle` — panel open, no conversation yet
- `starting` — creating session (first turn) + execution
- `streaming` — agent is working (tool calls, validation, refinement)
- `complete` — agent finished, YAML was extracted (diff showing)
- `ready` — agent finished without YAML (clarifying question / explanation),
  or user accepted/discarded a previous result
- `error` — a failure occurred (RPC, stream)


### RunWorkflowFieldErrors

Field-level validation errors keyed by field name.


### serializeWorkflowYaml

Serializes a proto `Workflow` into the canonical Stigmer YAML format.

The output uses snake_case field names and is round-trip compatible
with [`parseWorkflowYaml`](#parseworkflowyaml). Only emits `apiVersion`, `kind`,
`metadata`, and `spec` — the `status` field is system-managed.

```tsx
function serializeWorkflowYaml(workflow: Workflow): string
```

```tsx
const yaml = serializeWorkflowYaml(workflow);
// Edit yaml...
const input = parseWorkflowYaml(yaml, "acme");
await stigmer.workflow.apply(input);
```


### sortExecutionRows

Sorts execution rows by a given field and direction.
Returns a new array (does not mutate the input).

```tsx
function sortExecutionRows(rows: readonly ExecutionRow[], field: ExecutionSortField, direction: SortDirection): ExecutionRow[]
```


### START_NODE_ID

Sentinel node ID for the workflow entry point.


### STARTER_WORKFLOW_YAML

Minimal valid workflow YAML used as the starting point when creating
a new workflow from the visual editor or code editor.

Uses the full envelope format (`apiVersion` / `kind` / `metadata` / `spec`)
so `yamlToGraph()` can parse it in visual mode without errors.

Includes a single `agent_call` task so the user sees a meaningful
canvas node immediately rather than an empty graph.


### TASK_KIND_DRAG_MIME

MIME type for the drag transfer carrying the task kind identifier.


### TaskConfigForm

Schema-driven form generated from `TaskKindDescriptor.fields` and `fieldGroups`.

Renders each field with the appropriate control based on its `TaskFieldType`:
text inputs, number inputs, toggles, selects, embedded YAML editors, and
list/map editors. Fields are organized into collapsible groups.

Pure presentational — does not own state. Receives the current config
and reports changes via `onChange(fieldPath, value)`.


### TaskConfigFormProps

Props for [`TaskConfigForm`](#taskconfigform).

<TypeTable
  type={{
    className: { type: "string", description: "Additional CSS class names for the root container." },
    config: { type: "JsonObject", description: "Current config object (source of truth from the graph node).", required: true },
    fieldGroups: { type: "readonly TaskFieldGroup[]", description: "Logical field groups for collapsible sections.", required: true },
    fields: { type: "readonly TaskFieldDescriptor[]", description: "Ordered field descriptors from `TaskKindDescriptor.fields`.", required: true },
    onChange: { type: "(fieldPath: string, value: unknown) => void", description: "Called when a field value changes.", required: true },
  }}
/>


### TaskFieldDescriptor

Describes a single field in a task configuration message.

<TypeTable
  type={{
    defaultValue: { type: "string", description: "Default value as a JSON-encoded string." },
    description: { type: "string", description: "Field description from proto comments.", required: true },
    displayName: { type: "string", description: "Human-readable label for form rendering.", required: true },
    elementType: { type: "string", description: "For repeated: the element type. For message: the nested type name." },
    enumValues: { type: "readonly string[]", description: "Valid enum values when type is \"enum\"." },
    fieldNumber: { type: "number", description: "Proto field number for stable identification.", required: true },
    groupId: { type: "string", description: "Group this field belongs to for form section rendering." },
    isExpression: { type: "boolean", description: "Whether the field accepts ${ } expression interpolation." },
    name: { type: "string", description: "Proto field name (snake_case).", required: true },
    required: { type: "boolean", description: "Whether the field is required.", required: true },
    type: { type: "TaskFieldType", description: "Data type for rendering the appropriate form control.", required: true, typeDescriptionLink: "#taskfieldtype" },
    validationHints: { type: "readonly string[]", description: "Human-readable validation constraint descriptions." },
  }}
/>


### TaskFieldGroup

Logical grouping of fields for form section rendering.

<TypeTable
  type={{
    description: { type: "string", description: "Optional description shown below the group header." },
    displayName: { type: "string", description: "Human-readable label for the group section header.", required: true },
    id: { type: "string", description: "Group identifier.", required: true },
  }}
/>


### TaskFieldType

Data type of a task configuration field.


### TaskKindCategory

Functional category for grouping task kinds in the UI.


### TaskKindDescriptor

TypeScript representation of a TaskKindDescriptor from the task kind registry.

Each descriptor provides complete metadata for a single workflow task kind,
enabling UI form generation, YAML editor autocomplete, task palette
rendering, and client-side pre-validation.

<TypeTable
  type={{
    category: { type: "TaskKindCategory", description: "Functional category for palette grouping and filtering.", required: true, typeDescriptionLink: "#taskkindcategory" },
    configJsonSchema: { type: "Record<string, unknown>", description: "JSON Schema (draft 2020-12) for the task_config payload.", required: true },
    configProtoType: { type: "string", description: "Fully-qualified proto message type for the task config.", required: true },
    description: { type: "string", description: "One-line description of what this task kind does.", required: true },
    displayName: { type: "string", description: "Human-readable display name (e.g., \"LLM Call\", \"Human Input\").", required: true },
    documentationUrl: { type: "string", description: "Documentation URL path (relative to docs root).", required: true },
    fieldGroups: { type: "readonly TaskFieldGroup[]", description: "Logical field groups for form section rendering.", required: true },
    fields: { type: "readonly TaskFieldDescriptor[]", description: "Ordered field descriptors for form rendering.", required: true },
    icon: { type: "string", description: "Lucide icon name for UI rendering.", required: true },
    isAiNative: { type: "boolean", description: "Whether this task kind invokes AI models.", required: true },
    kind: { type: "string", description: "Workflow task kind identifier (e.g., \"llm_call\", \"human_input\").", required: true },
    outputJsonSchema: { type: "Record<string, unknown>", description: "JSON Schema describing the task's output shape." },
    requiresExternalService: { type: "boolean", description: "Whether this task kind requires external service connectivity.", required: true },
    yamlExamples: { type: "readonly string[]", description: "YAML code examples demonstrating common usage patterns." },
  }}
/>


### TaskKindRegistryContext

React context that holds the task kind registry fetched from the API.

Populated by [`StigmerProvider`](/docs/sdk/react/core#stigmerprovider) on mount. Consumer hooks read from
this context instead of a static JSON import, enabling always-fresh
task metadata without npm package updates.


### TaskKindRegistryState

Internal state held by the task kind registry context provider.

<TypeTable
  type={{
    descriptors: { type: "readonly TaskKindDescriptor[]", description: "", required: true },
    error: { type: "Error | null", description: "", required: true },
    isLoading: { type: "boolean", description: "", required: true },
    refetch: { type: "() => void", description: "", required: true },
  }}
/>


### TaskPickerPopover

Searchable, categorized popover for selecting a workflow task kind.

Shared between the node "+" button (add successor), the edge "+"
button (insert on edge), and later the right-click context menu.
Callers provide their own trigger element and control open state
via the `open` / `onOpenChange` props.

Uses `@base-ui/react/popover` for positioning and focus management,
`useTaskKindRegistry()` for the available task kinds, and `--stgm-*`
tokens for all styling (DD-005).


### TaskPickerPopoverProps

Props for [`TaskPickerPopover`](#taskpickerpopover).

<TypeTable
  type={{
    align: { type: "\"center\" | \"end\" | \"start\"", description: "Alignment along the anchor edge." },
    anchorRef: { type: "RefObject<HTMLElement | null>", description: "Ref to the element the popover should anchor to.", required: true },
    className: { type: "string", description: "Additional CSS class names for the popup container." },
    graph: { type: "WorkflowGraphModel | null", description: "Current graph model used for compatibility filtering." },
    insertionContext: { type: "InsertionContext | null", description: "Context for contextual header/suggestions/compatibility checks." },
    onOpenChange: { type: "(open: boolean) => void", description: "Called when the popover requests open/close state changes.", required: true },
    onSelectKind: { type: "(kindString: string) => void", description: "Called when the user selects a task kind from the list.", required: true },
    open: { type: "boolean", description: "Whether the popover is currently visible.", required: true },
    side: { type: "\"left\" | \"right\" | \"top\" | \"bottom\"", description: "Which side of the anchor to position the popover." },
  }}
/>


### TopologyEdge

An edge in the workflow topology graph.

<TypeTable
  type={{
    label: { type: "string", description: "" },
    source: { type: "string", description: "", required: true },
    target: { type: "string", description: "", required: true },
  }}
/>


### TopologyNode

A node in the workflow topology graph.

<TypeTable
  type={{
    category: { type: "TopologyNodeCategory", description: "", required: true, typeDescriptionLink: "#topologynodecategory" },
    height: { type: "number", description: "", required: true },
    id: { type: "string", description: "", required: true },
    kind: { type: "string", description: "", required: true },
    label: { type: "string", description: "", required: true },
    width: { type: "number", description: "", required: true },
    x: { type: "number", description: "Layout position computed by dagre (set after layout).", required: true },
    y: { type: "number", description: "", required: true },
  }}
/>


### TopologyNodeCategory


### UseDiagnoseExecutionFlowOptions

Options for [`useDiagnoseExecutionFlow`](#usediagnoseexecutionflow).

<TypeTable
  type={{
    autoStart: { type: "boolean", description: "Whether to start diagnosis automatically on mount." },
    currentWorkflowYaml: { type: "string", description: "Current workflow YAML for diff computation (optional)." },
    executionId: { type: "string", description: "ID of the failed workflow execution to diagnose.", required: true },
    onError: { type: "(message: string) => void", description: "Called when any step fails. Also available via the `error` return." },
    org: { type: "string", description: "Organization slug for session and execution creation.", required: true },
  }}
/>


### UseElkLayoutEngineOptions

Options for [`useElkLayoutEngine`](#useelklayoutengine).

<TypeTable
  type={{
    enabled: { type: "boolean", description: "When `false`, skip engine creation entirely and return `null`. Useful for conditional activation (e.g., feature flags)." },
    layoutOptions: { type: "Record<string, string>", description: "Additional ELK layout options to merge with workflow defaults." },
    workerFactory: { type: "() => Worker", description: "Factory function that creates a Web Worker for off-main-thread layout.  When provided, ELK uses the lightweight ~5KB API stub on the main thread and delegates the ~1.5MB WASM computation to the worker.  When omitted, ELK loads the full bundled WASM on the main thread (async but blocking during computation)." },
  }}
/>


### UseExecutionHistoryDataOptions

Options for [`useExecutionHistoryData`](#useexecutionhistorydata).

<TypeTable
  type={{
    clientFilters: { type: "ExecutionClientFilters", description: "Client-side filters applied post-fetch to the loaded page. These are a stopgap until server-side filters are wired (Phase 2).", typeDescriptionLink: "#executionclientfilters" },
    pageSize: { type: "number", description: "Maximum executions per page." },
    pageToken: { type: "string", description: "Opaque cursor for paginated fetching." },
    workflowId: { type: "string | null", description: "When set, scopes the list to executions of this workflow. When omitted, lists all executions across all workflows." },
  }}
/>


### UsePendingApprovalsOptions

<TypeTable
  type={{
    org: { type: "string | null | undefined", description: "Organization slug. When empty, the hook does not fetch.", required: true },
    pageSize: { type: "number", description: "Maximum results per page." },
    refetchInterval: { type: "number | false", description: "Refetch interval in milliseconds. `0` or `false` disables." },
  }}
/>


### UseRefineWorkflowFlowOptions

Options for [`useRefineWorkflowFlow`](#userefineworkflowflow).

<TypeTable
  type={{
    currentYaml: { type: "string", description: "Live YAML from the editor. Captured via ref at send-time, not reactively.", required: true },
    onError: { type: "(message: string) => void", description: "Called when any step fails. Also available via the `error` return." },
    org: { type: "string", description: "Organization slug for session and execution creation.", required: true },
  }}
/>


### UseRunWorkflowFlowOptions

Options for [`useRunWorkflowFlow`](#userunworkflowflow).

<TypeTable
  type={{
    instances: { type: "readonly WorkflowInstance[]", description: "Available workflow instances (for instance selector).", required: true },
    onError: { type: "(message: string) => void", description: "Called when submission fails. Receives a human-readable message. Errors are also available via UseRunWorkflowFlowReturn.error." },
    onSuccess: { type: "(executionId: string) => void", description: "Called after the execution is created successfully. Receives the execution ID for navigation.", required: true },
    org: { type: "string", description: "Organization slug that owns the workflow.", required: true },
    workflow: { type: "Workflow", description: "Workflow resource (must include metadata and spec).", required: true, typeDescriptionLink: "/docs/sdk/resources/workflow" },
  }}
/>


### UseWorkflowArchitectFlowOptions

Options for [`useWorkflowArchitectFlow`](#useworkflowarchitectflow).

<TypeTable
  type={{
    onError: { type: "(message: string) => void", description: "Called when any step fails. Receives a human-readable message. Errors are also available via UseWorkflowArchitectFlowReturn.error." },
    onSuccess: { type: "(org: string, slug: string) => void", description: "Called after the workflow is created successfully. Receives the org slug and workflow slug for navigation.", required: true },
    org: { type: "string", description: "Organization slug — used for session, execution, and workflow creation.", required: true },
  }}
/>


### UseWorkflowCanvasOptions

Options for [`useWorkflowCanvas`](#useworkflowcanvas).

<TypeTable
  type={{
    layoutEngine: { type: "LayoutEngine | null", description: "Layout engine for the \"Auto Layout\" action. When provided, this engine is used instead of the default dagre engine. Pass the result of useElkLayoutEngine for ELK-powered layout. When `null` or `undefined`, dagre is used as the default." },
  }}
/>


### UseWorkflowCountOptions

Options for [`useWorkflowCount`](#useworkflowcount).

<TypeTable
  type={{
    query: { type: "string", description: "Text query to filter workflows before counting." },
    scope: { type: "ResourceListScope", description: "Controls which workflows are counted.  - `\"org\"` — only workflows owned by the given organization. - `\"all\"` — includes public/platform workflows." },
  }}
/>


### UseWorkflowDashboardSummaryOptions

<TypeTable
  type={{
    org: { type: "string | null | undefined", description: "Organization slug. When empty, the hook does not fetch.", required: true },
    refetchInterval: { type: "number | false", description: "Refetch interval in milliseconds. `0` or `false` disables." },
    timeWindow: { type: "SummaryTimeWindow", description: "Time window for aggregation." },
    workflowId: { type: "string", description: "When set, scopes the summary to executions of this workflow only. When omitted, aggregates across all workflows in the organization." },
  }}
/>


### UseWorkflowEditorOptions

Options for [`useWorkflowEditor`](#useworkfloweditor).

<TypeTable
  type={{
    org: { type: "string", description: "Organization slug for the save path.", required: true },
  }}
/>


### UseWorkflowExecutionEventLogOptions

Options for [`useWorkflowExecutionEventLog`](#useworkflowexecutioneventlog).

<TypeTable
  type={{
    afterSequence: { type: "bigint", description: "Cursor: return events with sequence_number > this value." },
    eventTypes: { type: "readonly WorkflowEventType[]", description: "Filter to specific event types. When empty, all types are returned." },
    pageSize: { type: "number", description: "Maximum events per page." },
    taskName: { type: "string", description: "Filter to events for a specific task name." },
  }}
/>


### UseWorkflowExecutionEventStreamOptions

Options for [`useWorkflowExecutionEventStream`](#useworkflowexecutioneventstream).

<TypeTable
  type={{
    autoReconnect: { type: "boolean", description: "Automatically re-establish the live subscription with exponential backoff when it drops with a transient transport error, resuming from the last received `sequence_number` (no events lost). Defaults to `true`." },
    eventTypes: { type: "readonly WorkflowEventType[]", description: "Filter to specific event types for the subscription." },
    executionPhase: { type: "ExecutionPhase", description: "The current execution phase. When provided, the hook uses this to decide between live streaming (non-terminal) and batch loading (terminal). When omitted, defaults to live streaming." },
    reconnectOptions: { type: "BackoffOptions & { maxAttempts?: number }", description: "Tune the auto-reconnect backoff schedule and attempt cap. Omitted fields fall back to SDK defaults (base 1s, ×2, max 30s, 10 attempts)." },
    store: { type: "WorkflowExecutionEventStore", description: "External store instance. When provided, the hook writes to this store instead of creating an internal one. Useful for sharing event state across multiple components." },
  }}
/>


### UseWorkflowExecutionListOptions

Options for [`useWorkflowExecutionList`](#useworkflowexecutionlist).

<TypeTable
  type={{
    filter: { type: "Partial<ExecutionFilterCriteria>", description: "Server-side filter criteria. When set, the backend filters before returning results, reducing transfer size." },
    pageSize: { type: "number", description: "Maximum executions per page." },
    pageToken: { type: "string", description: "Opaque page token for cursor-based pagination." },
    sortAscending: { type: "boolean", description: "When true, sorts ascending. Defaults to false (descending)." },
    sortField: { type: "ExecutionSortField", description: "Server-side sort field." },
    workflowId: { type: "string | null", description: "Workflow or WorkflowInstance ID to scope executions. When omitted, lists all executions across all workflows." },
  }}
/>


### UseWorkflowListOptions

Options for [`useWorkflowList`](#useworkflowlist).

<TypeTable
  type={{
    page: { type: "number", description: "Page number (1-indexed)." },
    pageSize: { type: "number", description: "Maximum workflows per page." },
    query: { type: "string", description: "Text query to filter workflows by name, description, or tags." },
    scope: { type: "ResourceListScope", description: "Controls which workflows are visible.  - `\"org\"` — only workflows owned by the given organization. - `\"all\"` — includes public/platform workflows." },
  }}
/>


### WORKFLOW_CATEGORY_LABELS

Human-readable labels for workflow template categories.


### WorkflowCanvasEditor

Outer wrapper that provides the React Flow zustand store context.

`useWorkflowCanvas` (called inside the inner component) uses `useReactFlow()`
which requires a `<ReactFlowProvider>` ancestor. The `<ReactFlow>` component
inside `WorkflowCanvasInner` creates its own store but that is too late — the
hook runs before the child mounts. This wrapper solves the provider ordering.


### WorkflowCanvasEditorProps

Props for [`WorkflowCanvasEditor`](#workflowcanvaseditor).

<TypeTable
  type={{
    className: { type: "string", description: "Additional CSS class names for the root container." },
    isSaving: { type: "boolean", description: "Whether a save is currently in progress." },
    layoutEngine: { type: "LayoutEngine | null", description: "Layout engine for the \"Auto Layout\" action. Pass the result of useElkLayoutEngine for ELK-powered layout. When omitted, dagre is used as the default." },
    loadingFallback: { type: "ReactNode", description: "Fallback to show while the canvas loads (DD-013 lazy loading)." },
    nodeErrors: { type: "ReadonlyMap<string, readonly string[]>", description: "Map of nodeId -> error messages for validation badge rendering." },
    onDirtyChange: { type: "(dirty: boolean) => void", description: "Called when the canvas dirty state changes." },
    onEdgeSelect: { type: "(edgeId: string) => void", description: "Called when an edge is selected." },
    onNodeSelect: { type: "(nodeId: string) => void", description: "Called when a task node is selected." },
    onSave: { type: "(yaml: string) => void", description: "Called when the save button is clicked. Receives the serialized YAML." },
    onSelectionClear: { type: "() => void", description: "Called when selection is cleared." },
    showInspector: { type: "boolean", description: "Whether to show the inspector panel. Defaults to true." },
    showPalette: { type: "boolean", description: "Whether to show the task palette sidebar. Defaults to true." },
    yaml: { type: "string | null", description: "The workflow YAML string to render on the canvas.", required: true },
  }}
/>


### WorkflowCodePreviewGraph

Read-only React Flow graph preview for the code-mode editor side panel.

Accepts YAML directly (unlike `WorkflowOverviewGraph` which requires
a `Workflow` proto). Uses the unified `WorkflowNode` / `CanvasTransitionEdge`
rendering system in `"overview"` mode for DD-001 consistency.


### WorkflowCodePreviewGraphProps

Props for [`WorkflowCodePreviewGraph`](#workflowcodepreviewgraph).

<TypeTable
  type={{
    className: { type: "string", description: "Additional CSS class names for the root container." },
    yaml: { type: "string | null", description: "Workflow YAML to visualize.", required: true },
  }}
/>


### WorkflowDashboard

Composed dashboard widget that aggregates execution KPIs,
pending approvals, and recent failures into a responsive layout.

Composes three sub-widgets:
- **ExecutionSummaryWidget** — phase counts, cost, duration
- **PendingApprovalsWidget** — human_input tasks awaiting decisions
- **FailedRunsWidget** — recent failed executions (from existing list API)

All data fetching is internal. The consumer provides org context and
navigation callbacks.

```tsx
<WorkflowDashboard
  org="acme"
  onApprovalClick={(id) => navigate(`/workflows/executions/${id}`)}
  onFailedRunClick={(id) => navigate(`/workflows/executions/${id}`)}
/>
```


### WorkflowDashboardProps

<TypeTable
  type={{
    className: { type: "string", description: "" },
    onApprovalClick: { type: "(executionId: string) => void", description: "Called when the user clicks \"Review\" on a pending approval." },
    onFailedRunClick: { type: "(executionId: string) => void", description: "Called when the user clicks \"View\" on a failed execution." },
    org: { type: "string | null | undefined", description: "Organization slug for scoping the dashboard data.", required: true },
    timeWindow: { type: "SummaryTimeWindow", description: "Time window for the summary statistics." },
  }}
/>


### WorkflowDiffLine

A single line in a unified diff.

<TypeTable
  type={{
    content: { type: "string", description: "", required: true },
    type: { type: "DiffLineType", description: "", required: true },
  }}
/>


### WorkflowDiffLineType

Type of a diff line.


### WorkflowEditorMode

Editing mode for the workflow editor.


### WorkflowEditorView


### WorkflowEditorViewProps

Props for [`WorkflowEditorView`](#workfloweditorview).

<TypeTable
  type={{
    className: { type: "string", description: "Additional CSS class names for the root container." },
    defaultMode: { type: "WorkflowEditorMode", description: "Initial editor mode. Defaults to `\"code\"`.", typeDescriptionLink: "#workfloweditormode" },
    initialYaml: { type: "string", description: "The initial YAML content to load into the editor.", required: true },
    layoutEngine: { type: "LayoutEngine | null", description: "Layout engine for the visual canvas \"Auto Layout\" action. Pass the result of useElkLayoutEngine for ELK-powered layout. When omitted, dagre is used as the default." },
    onSaveError: { type: "(error: Error) => void", description: "Called when a save fails." },
    onSaveSuccess: { type: "() => void", description: "Called after a successful save." },
    org: { type: "string", description: "Organization slug for the save path.", required: true },
  }}
/>


### WorkflowExecutionApprovalCard

Inline approval card rendered within the execution timeline when
a `human_input` task or agent tool approval gate is active.

Provides approve and reject buttons with an optional comment field.
Keyboard-navigable and accessible.


### WorkflowExecutionApprovalCardProps

Props for [`WorkflowExecutionApprovalCard`](#workflowexecutionapprovalcard).

<TypeTable
  type={{
    approvers: { type: "readonly string[]", description: "List of users/roles that can approve.", required: true },
    className: { type: "string", description: "Additional CSS class names." },
    error: { type: "Error | null", description: "This gate's last failed decision, or `null`. Surfaced in-card beside the actions (via the shared InCardDecisionError) — supply useWorkflowExecutionActions's `approvalErrorsByToolCallId` for this `toolCallId`." },
    isSubmitting: { type: "boolean", description: "`true` while this gate's submission is in flight.", required: true },
    onSubmitApproval: { type: "(toolCallId: string, action: ApprovalAction, comment?: string) => Promise<unknown>", description: "Callback to submit the approval decision.", required: true },
    prompt: { type: "string", description: "The prompt message explaining what needs approval.", required: true },
    timeoutSeconds: { type: "number", description: "Timeout in seconds (0 = no timeout).", required: true },
    toolCallId: { type: "string", description: "Tool call ID for the approval request.", required: true },
  }}
/>


### WorkflowExecutionArtifactPanel

Sidebar panel listing artifacts produced by the workflow execution.
Each artifact shows its display name, content type, size, and a
download button that fetches a pre-signed URL via `getDownloadUrl`.


### WorkflowExecutionArtifactPanelProps

Props for [`WorkflowExecutionArtifactPanel`](#workflowexecutionartifactpanel).

<TypeTable
  type={{
    artifacts: { type: "readonly Artifact[]", description: "Artifacts produced by the execution.", required: true },
    className: { type: "string", description: "Additional CSS class names." },
  }}
/>


### WorkflowExecutionCostPanel

Sidebar panel showing budget consumption with progress bars
for cost and tokens.


### WorkflowExecutionCostPanelProps

Props for [`WorkflowExecutionCostPanel`](#workflowexecutioncostpanel).

<TypeTable
  type={{
    className: { type: "string", description: "Additional CSS class names." },
    costSummary: { type: "DerivedCostSummary", description: "Derived cost summary from the event store.", required: true },
    maxCostMicros: { type: "bigint", description: "Maximum cost budget in micro-USD from the workflow spec. -1 if no limit." },
    maxTokens: { type: "bigint", description: "Maximum token budget from the workflow spec. -1 if no limit." },
  }}
/>


### WorkflowExecutionHeader


### WorkflowExecutionHeaderProps

Props for [`WorkflowExecutionHeader`](#workflowexecutionheader).

<TypeTable
  type={{
    actions: { type: "UseWorkflowExecutionActionsReturn", description: "", required: true, typeDescriptionLink: "#useworkflowexecutionactionsreturn" },
    className: { type: "string", description: "" },
    costSummary: { type: "DerivedCostSummary", description: "", required: true },
    execution: { type: "WorkflowExecution", description: "", required: true, typeDescriptionLink: "/docs/sdk/resources/workflow-execution" },
    headerActions: { type: "ReactNode", description: "Host-supplied action elements rendered at the trailing edge of the header (e.g. a Share control). Kept routing/auth-agnostic per DD-004 — the SDK renders the slot; the host owns its behavior." },
    isDiagnosing: { type: "boolean", description: "Whether the diagnosis panel is currently active." },
    onCompare: { type: "() => void", description: "Called when the user clicks \"Compare with...\" on a terminal execution." },
    onDiagnose: { type: "() => void", description: "Called when the user clicks \"Diagnose with AI\" on a failed execution." },
    streamState: { type: "WorkflowEventStreamState", description: "", required: true },
  }}
/>


### WorkflowExecutionHistory

Composed execution history view assembling health metrics, a filter
bar, a sortable data table, and a failure analysis panel.

Self-contained: fetches data internally via `useWorkflowExecutionList`
and `useWorkflowDashboardSummary`. Zero Console dependencies.

Layout:
```
HealthMetricsStrip
ExecutionFilterBar
ExecutionHistoryTable
FailureAnalysisPanel (collapsible)
```

```tsx
<WorkflowExecutionHistory
  org="acme"
  workflowId={workflow.metadata?.id}
  onExecutionClick={(id) => navigate(`/executions/${id}`)}
/>
```


### WorkflowExecutionHistoryProps

Props for [`WorkflowExecutionHistory`](#workflowexecutionhistory).

<TypeTable
  type={{
    className: { type: "string", description: "Additional CSS classes for the root container." },
    onExecutionClick: { type: "(executionId: string) => void", description: "Called when the user clicks an execution row." },
    org: { type: "string", description: "Organization slug for scoping dashboard summary data.", required: true },
    pageSize: { type: "number", description: "Maximum executions per page." },
    workflowId: { type: "string", description: "When set, scopes to a single workflow's executions. When omitted, shows all executions across all workflows." },
  }}
/>


### WorkflowExecutionPhaseBadge

Displays the lifecycle phase of a `WorkflowExecution` as an
inline badge with a status icon and label.

Renders nothing for `EXECUTION_PHASE_UNSPECIFIED`.

All visual properties flow through `--stgm-*` tokens.

```tsx
<WorkflowExecutionPhaseBadge phase={ExecutionPhase.EXECUTION_COMPLETED} />
```


### WorkflowExecutionPhaseBadgeProps

Props for [`WorkflowExecutionPhaseBadge`](#workflowexecutionphasebadge).

<TypeTable
  type={{
    className: { type: "string", description: "Additional CSS class names for the root container." },
    phase: { type: "ExecutionPhase", description: "The workflow execution phase to display.", required: true },
  }}
/>


### WorkflowExecutionTaskPanel

Sidebar panel showing the status of each task in the workflow
execution. Tasks are ordered by their first appearance in the
event stream.

Clicking a task row invokes `onSelectTask` which can be used
to scroll the timeline to events for that task.


### WorkflowExecutionTaskPanelProps

Props for [`WorkflowExecutionTaskPanel`](#workflowexecutiontaskpanel).

<TypeTable
  type={{
    className: { type: "string", description: "Additional CSS class names." },
    onSelectTask: { type: "(taskName: string) => void", description: "Callback when a task row is clicked." },
    selectedTaskName: { type: "string | null", description: "Currently selected task name (for highlighting)." },
    taskStates: { type: "ReadonlyMap<string, DerivedTaskState>", description: "Derived task states from the event store.", required: true },
    totalTasks: { type: "number", description: "Total number of tasks declared in the workflow.", required: true },
  }}
/>


### WorkflowExecutionTimeline

Scrollable event timeline for a workflow execution.

Renders each event through `WorkflowExecutionTimelineEvent` and
auto-scrolls to the bottom when new events arrive during live
streaming, using a bottom sentinel + IntersectionObserver pattern
consistent with DD-009.

Auto-scroll activates only when the sentinel is visible (user is
near the bottom). When the user scrolls up, auto-scroll pauses
to allow reading history.


### WorkflowExecutionTimelineProps

Props for [`WorkflowExecutionTimeline`](#workflowexecutiontimeline).

<TypeTable
  type={{
    className: { type: "string", description: "Additional CSS class names." },
    events: { type: "readonly WorkflowExecutionEvent[]", description: "Events ordered by sequence_number ascending.", required: true },
    onNavigateToAgentExecution: { type: "(executionId: string) => void", description: "Callback when user clicks an agent execution link." },
    onSubmitTaskApproval: { type: "(taskName: string, outcome: string, formData?: Record<string, unknown>, comment?: string) => Promise<unknown>", description: "Callback to submit a human_input task approval decision." },
    streamState: { type: "WorkflowEventStreamState", description: "Current stream lifecycle state.", required: true },
    taskApprovalErrorsByTaskName: { type: "ReadonlyMap<string, Error>", description: "Per-gate human_input approval failures, keyed by `taskName`. Supply useWorkflowExecutionActions's `taskApprovalErrorsByTaskName`." },
    taskApprovalSubmittingTaskNames: { type: "ReadonlySet<string>", description: "Task names whose human_input approval is currently in flight, keyed by `taskName` so deciding one gate never spins another. Supply useWorkflowExecutionActions's `taskApprovalSubmittingTaskNames`." },
    taskStates: { type: "ReadonlyMap<string, DerivedTaskState>", description: "Derived task states for rendering interactive approval cards." },
  }}
/>


### WorkflowExecutionViewer

Top-level composed viewer for a single workflow execution.

Wires together all execution hooks and sub-components into a
two-region layout: an event timeline (main area) and a context
sidebar (tasks, budget, artifacts).

This component is designed to work identically whether rendered
in the Stigmer Console or embedded in a third-party dashboard.
No dependencies on Console routing, auth, or layout context.

```tsx
<WorkflowExecutionViewer
  executionId="wfx_abc123"
  onNavigateToAgentExecution={(id) => router.push(`/sessions/${id}`)}
/>
```


### WorkflowExecutionViewerProps

Props for [`WorkflowExecutionViewer`](#workflowexecutionviewer).

<TypeTable
  type={{
    additionalActions: { type: "ReactNode", description: "Additional action elements to render in the sidebar inspector footer." },
    className: { type: "string", description: "Additional CSS class names for the root container." },
    executionId: { type: "string", description: "ID of the workflow execution to display.", required: true },
    headerActions: { type: "ReactNode", description: "Host-supplied action elements rendered in the header action group (e.g. a Share control). Routing/auth-agnostic per DD-004." },
    nodesDraggable: { type: "boolean", description: "Whether task nodes in the execution graph can be dragged to rearrange the layout for presentations. Drag positions are ephemeral and not persisted." },
    onNavigateToAgentExecution: { type: "(agentExecutionId: string) => void", description: "Callback when the user clicks a link to a child agent execution. Receives the AgentExecution ID. The host application is responsible for navigation — this keeps the component routing-agnostic (DD-004)." },
    onNavigateToWorkflowEditor: { type: "(yaml: string, workflowSlug: string) => void", description: "Callback when the user clicks \"Apply Fix\" in the repair card. Receives the suggested YAML and the workflow slug. The host application handles navigation to the workflow editor (DD-004)." },
    org: { type: "string", description: "Organization slug — needed for AI diagnosis authorization." },
  }}
/>


### WorkflowFileDecisionSubmit

Submit handler for a workflow-level file decision, matching
`useWorkflowExecutionActions().submitFileDecision`. The child id is bound per
card so the surface only needs the card-level `(action, options)`.


### WorkflowGraphBudget

<TypeTable
  type={{
    maxCostMicros: { type: "number", description: "" },
    maxDurationSeconds: { type: "number", description: "" },
    maxTotalTokens: { type: "number", description: "" },
    onExceeded: { type: "string", description: "" },
  }}
/>


### WorkflowGraphDocument

<TypeTable
  type={{
    description: { type: "string", description: "" },
    dsl: { type: "string", description: "", required: true },
    name: { type: "string", description: "", required: true },
    namespace: { type: "string", description: "", required: true },
    version: { type: "string", description: "", required: true },
  }}
/>


### WorkflowGraphEdge

A directed edge between two nodes in the workflow graph.

Edges are inferred from task ordering, explicit `flow.then` directives,
`switch_case` branches, and `human_input` outcome routing.

<TypeTable
  type={{
    id: { type: "string", description: "", required: true },
    label: { type: "string", description: "" },
    source: { type: "string", description: "", required: true },
    sourceHandle: { type: "string", description: "For multi-port nodes (switch_case, human_input): identifies the source handle." },
    target: { type: "string", description: "", required: true },
  }}
/>


### WorkflowGraphEnvVar

<TypeTable
  type={{
    defaultValue: { type: "string", description: "" },
    description: { type: "string", description: "" },
    isSecret: { type: "boolean", description: "" },
    optional: { type: "boolean", description: "" },
  }}
/>


### WorkflowGraphModel

Complete graph representation of a workflow, serving as the source
of truth while in canvas editing mode.

Carries all data needed to:
- Render as React Flow elements (`toReactFlowElements`)
- Serialize back to YAML (`graphToYaml`)
- Save via the SDK (`graphToWorkflowInput`)

<TypeTable
  type={{
    budget: { type: "WorkflowGraphBudget", description: "", typeDescriptionLink: "#workflowgraphbudget" },
    description: { type: "string", description: "" },
    document: { type: "WorkflowGraphDocument", description: "", required: true, typeDescriptionLink: "#workflowgraphdocument" },
    edges: { type: "readonly WorkflowGraphEdge[]", description: "", required: true },
    env: { type: "Readonly<Record<string, WorkflowGraphEnvVar>>", description: "" },
    nodes: { type: "readonly WorkflowGraphNode[]", description: "", required: true },
  }}
/>


### WorkflowGraphNode

A single task node in the workflow graph.

Sentinel nodes (`__start__`, `__end__`) use `WorkflowTaskKind` value 0
(unspecified) and special category values.

<TypeTable
  type={{
    category: { type: "TopologyNodeCategory", description: "", required: true, typeDescriptionLink: "#topologynodecategory" },
    config: { type: "JsonObject", description: "", required: true },
    export: { type: "{ as: string }", description: "" },
    flow: { type: "{ then?: string }", description: "" },
    id: { type: "string", description: "", required: true },
    kind: { type: "WorkflowTaskKind", description: "", required: true },
    position: { type: "{ x: number; y: number }", description: "", required: true },
    taskName: { type: "string", description: "", required: true },
  }}
/>


### WorkflowInspectorPanel

Inspector panel for the visual canvas editor.

Thin wrapper that bridges the callback-based prop API from
`WorkflowCanvasEditor` into the tabbed `InspectorShell` architecture.

The shell renders a tabbed inspector with a consistent header, actions
menu, and per-tab content (Configure, Data, Runtime, Advanced, Docs).


### WorkflowInspectorPanelProps

Props for [`WorkflowInspectorPanel`](#workflowinspectorpanel).

<TypeTable
  type={{
    className: { type: "string", description: "Additional CSS class names." },
    descriptor: { type: "TaskKindDescriptor | undefined", description: "Descriptor for the selected node's task kind.", required: true },
    emptyState: { type: "ReactNode", description: "Content to render when nothing is selected (workflow summary)." },
    graph: { type: "WorkflowGraphModel | null", description: "The current graph model for node/edge lookups.", required: true },
    onDeleteEdge: { type: "(edgeId: string) => void", description: "Called to delete an edge." },
    onDeleteNode: { type: "(nodeId: string) => void", description: "Called to delete a node (task)." },
    onDuplicateNode: { type: "(nodeId: string) => void", description: "Called to duplicate a node." },
    onMigrateBranchHandle: { type: "(nodeId: string, oldHandleId: string, newHandleId: string) => void", description: "Called when a branch handle is renamed (case/outcome rename)." },
    onRemoveBranchEdges: { type: "(nodeId: string, handleId: string) => void", description: "Called to remove all edges from a specific branch handle." },
    onRemoveCatchBlock: { type: "(tryCatchNodeId: string) => void", description: "Called to remove catch block." },
    onRemoveForkBranch: { type: "(forkNodeId: string, branchName: string) => void", description: "Called to remove a fork branch." },
    onRemoveSwitchCase: { type: "(switchNodeId: string, caseName: string) => void", description: "Called to remove a switch case." },
    onRenameForkBranch: { type: "(forkNodeId: string, oldName: string, newName: string) => void", description: "Called to rename a fork branch." },
    onRenameNode: { type: "(nodeId: string, newName: string) => void", description: "Called when a node is renamed.", required: true },
    onReorderForkBranches: { type: "(forkNodeId: string, newOrder: readonly string[]) => void", description: "Called to reorder fork branches." },
    onReorderSwitchCases: { type: "(switchNodeId: string, newOrder: readonly string[]) => void", description: "Called to reorder switch cases." },
    onSetForkCompete: { type: "(forkNodeId: string, compete: boolean) => void", description: "Called to toggle fork compete (race) mode." },
    onToggleDisabled: { type: "(nodeId: string) => void", description: "Called to toggle disabled state on a node." },
    onUpdateBranchRouting: { type: "(nodeId: string, handleId: string, targetTask: string | undefined) => void", description: "Called to create/update/remove an edge for a specific branch handle." },
    onUpdateCatchConfig: { type: "(tryCatchNodeId: string, updates: { as?: string; compensate?: boolean }) => void", description: "Called to update catch configuration." },
    onUpdateExport: { type: "(nodeId: string, exportAs: string | undefined) => void", description: "Called when a node's export.as changes.", required: true },
    onUpdateField: { type: "(nodeId: string, fieldPath: string, value: unknown) => void", description: "Called when a config field value changes.", required: true },
    onUpdateFlow: { type: "(nodeId: string, thenTarget: string | undefined) => void", description: "Called when a node's flow.then changes.", required: true },
    onUpdateForEachConfig: { type: "(forEachNodeId: string, updates: Partial<{ batch_size: number; each: string; in: string; max_parallelism: number; on_error: string }>) => void", description: "Called to update for_each configuration." },
    onViewYaml: { type: "(nodeId: string) => void", description: "Called to open the View YAML dialog for a node." },
    onWrapInTryCatch: { type: "(nodeId: string) => void", description: "Called to wrap a node in a try/catch container." },
    selection: { type: "CanvasSelection | null", description: "Currently selected element on the canvas.", required: true },
    validationErrors: { type: "ReadonlyMap<string, readonly string[]>", description: "Validation errors keyed by node ID." },
  }}
/>


### WorkflowPattern

A structural workflow pattern detected in a template's YAML.

Pattern badges help users visually identify what a template
demonstrates without reading the full YAML.


### WorkflowTaskList

Compact list of tasks in a workflow specification.

Renders each task as a row with a kind icon (from the TaskKindRegistry),
task name, kind label, and a sequential connector line indicating flow
order. When the registry is loading, kind icons are omitted gracefully.

Zero Console dependencies — safe for platform builder embedding.
All visual properties flow through `--stgm-*` tokens.

```tsx
<WorkflowTaskList tasks={workflow.spec?.tasks ?? []} />
```


### WorkflowTaskListProps

Props for [`WorkflowTaskList`](#workflowtasklist).

<TypeTable
  type={{
    className: { type: "string", description: "Additional CSS class names for the root container." },
    tasks: { type: "readonly WorkflowTask[]", description: "Tasks from the workflow spec.", required: true },
  }}
/>


### WorkflowTaskPalette

Categorized sidebar listing all available workflow task kinds.

Each task kind entry is draggable. The drop target (the canvas)
reads the task kind from the drag transfer data using
[`TASK_KIND_DRAG_MIME`](#task_kind_drag_mime) and creates a new node.

Self-contained SDK component (DD-001, AD-T15-B2-005): depends only
on `useTaskKindRegistry()` and `--stgm-*` theme tokens. No canvas
or React Flow dependency.


### WorkflowTaskPaletteProps

Props for [`WorkflowTaskPalette`](#workflowtaskpalette).

<TypeTable
  type={{
    className: { type: "string", description: "Additional CSS class names for the root container." },
  }}
/>


### WorkflowTemplate

Workflow template = generic ResourceTemplate with workflow-specific data.


### WorkflowTemplateCategory

Workflow-specific template categories for the gallery filter tabs.

These extend the shared `TemplateCategory` type with workflow-specific
use-case domains. The gallery renders them as filterable tabs alongside
the generic categories.


### WorkflowTemplateData

The data payload carried by a workflow template.

Unlike agent templates (which carry form fields for a wizard),
workflow templates carry the full YAML string. Selecting a template
opens the editor with this YAML as `initialYaml`.

<TypeTable
  type={{
    yaml: { type: "string", description: "Full workflow YAML including apiVersion/kind/metadata/spec envelope.", required: true },
  }}
/>


### WorkflowTemplateMeta

Metadata derived from a workflow template's YAML content.

Computed once at render time by `deriveTemplateMeta()` and displayed
on template cards and preview dialogs.

<TypeTable
  type={{
    envVarCount: { type: "number", description: "Number of environment variable declarations.", required: true },
    hasBudget: { type: "boolean", description: "Whether the template declares a budget.", required: true },
    patterns: { type: "readonly WorkflowPattern[]", description: "Structural patterns detected in the template.", required: true },
    taskCount: { type: "number", description: "Number of top-level tasks (excludes nested tasks inside fork/try_catch/for_each).", required: true },
    taskKinds: { type: "readonly string[]", description: "Unique task kind strings used in the template.", required: true },
  }}
/>


### WorkflowYamlEditor

Schema-aware YAML editor built on CodeMirror 6.

Provides syntax highlighting, line numbers, bracket matching,
code folding, undo/redo, and inline diagnostic markers driven by
external validation (via the `diagnostics` prop).

Themed via `--stgm-*` CSS custom properties to respect the host
application's design tokens.

This is an optional SDK component — it requires CodeMirror peer
dependencies which are tree-shaken when not imported (DD-013).

```tsx
<WorkflowYamlEditor
  value={yaml}
  onChange={setYaml}
  diagnostics={diagnostics}
/>
```


### WorkflowYamlEditorProps

Props for [`WorkflowYamlEditor`](#workflowyamleditor).

<TypeTable
  type={{
    className: { type: "string", description: "Additional CSS class names for the root container." },
    diagnostics: { type: "readonly Diagnostic[]", description: "Validation diagnostics to display as inline markers." },
    onChange: { type: "(value: string) => void", description: "Called when the editor content changes." },
    readOnly: { type: "boolean", description: "Whether the editor is read-only." },
    style: { type: "CSSProperties", description: "Additional inline styles for the root container." },
    value: { type: "string", description: "Current YAML content.", required: true },
  }}
/>


### yamlToGraph

Parses a Stigmer Workflow YAML string into an editable graph model.

Builds nodes from `spec.tasks` with full configuration data, and infers
edges from sequential ordering, explicit `flow.then`, `switch_case`
branches, `human_input` outcomes, and fallback task references.

```tsx
function yamlToGraph(yaml: string): WorkflowGraphModel
```

