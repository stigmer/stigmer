---
title: IAM Policy
description: Hooks and components for access control bindings, role assignment, and org membership.
---

{/* Auto-generated by @stigmer/react docs generator. DO NOT EDIT. */}

## Hooks

### useCheckPermission

Hook that checks whether the current user has a specific permission
on a resource.

Wraps `iamPolicy.checkMyPermission()` — the dedicated self-check RPC
where the server derives the principal from the authenticated token
(the client never names a principal) — with caching and configurable
degradation. The RPC is served in every edition and its answer has one
definition: a permission on a kind the edition does not serve is
`false` (so operator-only navigation never appears where the seat
does not exist), `can_grant_access` on a resource the edition does not
share per person is `false` (open source grants roles on organizations
only), and everything else is the edition's authorizer's answer. By
default the hook *fails open* while the check is in flight or when the
RPC fails, so a transient error never hides a control the server would
honor. Pass `{ fail: "closed" }` for surfaces that must stay hidden
until authorization is confirmed (see [`CheckPermissionOptions`](#checkpermissionoptions)).

Pass `null` as `resource` to skip the check — the result resolves
through the fail mode (`allowed: true` under the default fail-open).

Only genuine server verdicts are cached (per (kind, id, relation)
triple, for the lifetime of the component mount). A failed check is
never cached, so a transient error does not pin a wrong answer.

**Signature**

```tsx
function useCheckPermission(resource: PermissionCheckResource | null, relation: string, options?: CheckPermissionOptions): UseCheckPermissionReturn
```

<TypeTable
  type={{
    resource: { type: "PermissionCheckResource | null", description: "The resource to check, or `null` to skip.", required: true },
    relation: { type: "string", description: "The permission to check (e.g. \"can_edit\", \"can_grant_access\").", required: true },
    options: { type: "CheckPermissionOptions", description: "Fail-mode configuration; see CheckPermissionOptions." },
  }}
/>

**Returns** — `UseCheckPermissionReturn`

<TypeTable
  type={{
    allowed: { type: "boolean", description: "Whether the current user has the specified permission." },
    error: { type: "Error | null", description: "Error from the last check, or `null`." },
    isLoading: { type: "boolean", description: "`true` while the authorization check is in flight." },
  }}
/>

```tsx
const { allowed, isLoading } = useCheckPermission(
  { kind: "agent", id: agentId },
  "can_edit",
);

if (!allowed) return null; // hide edit button
```


### useCreateIamPolicy

Behavior hook that wraps `iamPolicy.create()` with loading/error
state.

Creates an IAM policy binding — granting a principal (identity
account, team, etc.) a specific role on a resource. The caller
provides the full `IamPolicySpec` with `principal`, `resource`,
and `relation` (the role string, e.g. "admin").

**Signature**

```tsx
function useCreateIamPolicy(): UseCreateIamPolicyReturn
```

**Returns** — `UseCreateIamPolicyReturn`

<TypeTable
  type={{
    clearError: { type: "() => void", description: "Reset `error` to `null`." },
    create: { type: "(spec: IamPolicySpec) => Promise<IamPolicy>", description: "Grant access by creating an IAM policy binding." },
    error: { type: "Error | null", description: "Error from the last failed create, or `null` when healthy." },
    isCreating: { type: "boolean", description: "`true` while the create request is in flight." },
  }}
/>

```tsx
const { create, isCreating, error } = useCreateIamPolicy();

await create({
  principal: { kind: "identity_account", id: accountId },
  resource: { kind: "organization", id: orgId },
  relation: "admin",
});
```


### useDeleteIamPolicy

Behavior hook that wraps `iamPolicy.delete()` with loading/error
state.

Removes an IAM policy binding — revoking a principal's role on a
resource. The caller provides the same `IamPolicySpec` that was
used when creating the binding.

**Signature**

```tsx
function useDeleteIamPolicy(): UseDeleteIamPolicyReturn
```

**Returns** — `UseDeleteIamPolicyReturn`

<TypeTable
  type={{
    clearError: { type: "() => void", description: "Reset `error` to `null`." },
    error: { type: "Error | null", description: "Error from the last failed delete, or `null` when healthy." },
    isDeleting: { type: "boolean", description: "`true` while the delete request is in flight." },
    remove: { type: "(spec: IamPolicySpec) => Promise<IamPolicy>", description: "Revoke access by deleting an IAM policy binding." },
  }}
/>

```tsx
const { remove, isDeleting, error } = useDeleteIamPolicy();

await remove({
  principal: { kind: "identity_account", id: accountId },
  resource: { kind: "organization", id: orgId },
  relation: "admin",
});
```


### useGrantableRoles

Returns the list of IAM roles that can be granted on the given
resource kind.

This is a local read — no network call. The data comes from the
`grantable_roles` field in each `ApiResourceKind`'s proto metadata,
generated at build time.

**Signature**

```tsx
function useGrantableRoles(kind: ApiResourceKind | null): UseGrantableRolesReturn
```

<TypeTable
  type={{
    kind: { type: "ApiResourceKind | null", description: "The resource kind to query, or `null` while loading.", required: true },
  }}
/>

**Returns** — `UseGrantableRolesReturn`

<TypeTable
  type={{
    hasRoles: { type: "boolean", description: "Whether the kind has at least one user-grantable role." },
    roles: { type: "readonly IamRole[]", description: "Roles that can be granted on the given resource kind. Empty if none." },
  }}
/>

```tsx
const { roles, hasRoles } = useGrantableRoles(ApiResourceKind.agent);
// roles = [IamRole.owner, IamRole.viewer]
// hasRoles = true
```


### useGranteeCandidates

Data hook for the share picker's candidates.

**Signature**

```tsx
function useGranteeCandidates(options: UseGranteeCandidatesOptions): UseGranteeCandidatesReturn
```

<TypeTable
  type={{
    options: { type: "UseGranteeCandidatesOptions", description: "", required: true },
  }}
/>

**Returns** — `UseGranteeCandidatesReturn`

<TypeTable
  type={{
    error: { type: "Error | null", description: "The first error either list reported, or `null`." },
    isLoading: { type: "boolean", description: "`true` while either list is loading for the first time." },
    people: { type: "readonly PersonCandidate[]", description: "" },
    teams: { type: "readonly TeamCandidate[]", description: "" },
  }}
/>

```tsx
const { people, teams } = useGranteeCandidates({
  orgId,
  includeTeams: share.canShareWithTeams,
});
```


### usePrincipalsCount

Data hook that fetches the count of principals with access to an
organization.

Wraps `iamPolicy.getPrincipalsCount()`. Useful for member count
badges in navigation and summary statistics.

Pass `null` as `orgId` to skip fetching (stable no-op).

**Signature**

```tsx
function usePrincipalsCount(orgId: string | null, principalKind: string): UsePrincipalsCountReturn
```

<TypeTable
  type={{
    orgId: { type: "string | null", description: "Organization ID, or `null` to skip.", required: true },
    principalKind: { type: "string", description: "Kind of principals to count. Defaults to `\"identity_account\"`.", required: true },
  }}
/>

**Returns** — `UsePrincipalsCountReturn`

<TypeTable
  type={{
    count: { type: "number", description: "Number of distinct principals with access. `0` while loading." },
    error: { type: "Error | null", description: "Error from the last failed request, or `null` when healthy." },
    isLoading: { type: "boolean", description: "`true` while the fetch is in flight." },
    isRefetching: { type: "boolean", description: "`true` while a background refetch is in flight and stale data is shown." },
    refetch: { type: "() => void", description: "Re-fetch the count from the server." },
  }}
/>

```tsx
const { count, isLoading } = usePrincipalsCount(orgId);
// count = 5
```


### useResourceAccess

Data hook that fetches the list of principals and their roles on a
resource.

Wraps `iamPolicy.listResourceAccessByPrincipal()`. Returns each
principal with full display information (`ApiResourceRefView`) and
all their role grants, optionally including roles inherited from
parent resources.

Pass `null` as `resource` to skip fetching (stable no-op).

**Generic** — works for organizations, agents, environments, or any
resource kind that has IAM policies.

**Signature**

```tsx
function useResourceAccess(resource: ResourceAccessRef | null, options?: UseResourceAccessOptions): UseResourceAccessReturn
```

<TypeTable
  type={{
    resource: { type: "ResourceAccessRef | null", description: "The resource to query access for, or `null` to skip.", required: true },
    options: { type: "UseResourceAccessOptions", description: "Optional configuration." },
  }}
/>

**Returns** — `UseResourceAccessReturn`

<TypeTable
  type={{
    error: { type: "Error | null", description: "Error from the last failed request, or `null` when healthy." },
    isLoading: { type: "boolean", description: "`true` while the fetch is in flight." },
    isRefetching: { type: "boolean", description: "`true` while a background refetch is in flight and stale data is shown." },
    members: { type: "readonly PrincipalAccess[]", description: "Principals with their role grants on the resource." },
    refetch: { type: "() => void", description: "Re-fetch the access list from the server." },
  }}
/>

```tsx
const { members, isLoading } = useResourceAccess(
  { kind: "organization", id: orgId },
  { includeInherited: true },
);
```


### useRevokeOrgAccess

Behavior hook that wraps `iamPolicy.revokeOrgAccess()` with
loading/error state.

Removes all IAM policy bindings for a specific identity account
within an organization — including bindings on child resources
(agents, environments, etc.). This is the "remove member from org"
operation.

**Signature**

```tsx
function useRevokeOrgAccess(): UseRevokeOrgAccessReturn
```

**Returns** — `UseRevokeOrgAccessReturn`

<TypeTable
  type={{
    clearError: { type: "() => void", description: "Reset `error` to `null`." },
    error: { type: "Error | null", description: "Error from the last failed revoke, or `null` when healthy." },
    isRevoking: { type: "boolean", description: "`true` while the revoke request is in flight." },
    revoke: { type: "(accountId: string, orgId: string) => Promise<void>", description: "Remove all of a user's access to an organization.  Deletes every IAM policy granting the identity account access to the organization and its child resources (agents, environments, etc.) in a single operation." },
  }}
/>

```tsx
const { revoke, isRevoking, error } = useRevokeOrgAccess();

await revoke("ia-alice-123", "org-demo-456");
refetchMembers();
```


### useRoleSelector

Headless hook for selecting an IAM role for a given resource kind.

Provides role options with display metadata (label, description,
FGA relation string) and selection state. Use this directly if you
need a custom role picker UI; for the styled version, use
[`RoleSelector`](#roleselector).

**Signature**

```tsx
function useRoleSelector(kind: ApiResourceKind | null, defaultRole?: IamRole, granteeKind: "identity_account" | "team"): UseRoleSelectorReturn
```

<TypeTable
  type={{
    kind: { type: "ApiResourceKind | null", description: "The resource kind to show grantable roles for, or `null`.", required: true },
    defaultRole: { type: "IamRole", description: "Optional initial selection." },
    granteeKind: { type: "\"identity_account\" | \"team\"", description: "Who the role is for: a person (the default) may   hold any grantable role, a team only the kind's team roles.", required: true },
  }}
/>

**Returns** — `UseRoleSelectorReturn`

<TypeTable
  type={{
    clear: { type: "() => void", description: "Clear the selection." },
    hasOptions: { type: "boolean", description: "Whether at least one role option exists." },
    options: { type: "readonly RoleOption[]", description: "Available role options for the given resource kind." },
    select: { type: "(role: IamRole) => void", description: "Select a role." },
    selected: { type: "IamRole | null", description: "Currently selected role, or `null` if nothing is selected." },
    selectedValue: { type: "string", description: "FGA relation string for the selected role, or `\"\"` if nothing is selected." },
  }}
/>

```tsx
const { options, selected, select } = useRoleSelector(ApiResourceKind.organization);

options.map(opt => (
  <button key={opt.value} onClick={() => select(opt.role)}>
    {opt.label} — {opt.description}
  </button>
));
```


### useShareFlow

Behavior hook that orchestrates the share flow for any resource.

Composes [`useResourceAccess`](#useresourceaccess), [`useCreateIamPolicy`](#usecreateiampolicy), and
[`useDeleteIamPolicy`](#usedeleteiampolicy) into a single cohesive API suitable for
powering a share dialog.

Pass `null` as `resource` to produce a stable no-op (useful while
the resource hasn't loaded yet).

**Signature**

```tsx
function useShareFlow(resource: ShareFlowResource | null): UseShareFlowReturn
```

<TypeTable
  type={{
    resource: { type: "ShareFlowResource | null", description: "The resource to share, or `null` to skip.", required: true },
  }}
/>

**Returns** — `UseShareFlowReturn`

<TypeTable
  type={{
    accessList: { type: "readonly PrincipalAccess[]", description: "Grantees with their role grants on the resource." },
    canShareWithTeams: { type: "boolean", description: "Whether this resource can be shared with a team here: the edition serves teams and the kind accepts a team grant." },
    clearErrors: { type: "() => void", description: "Clear all errors." },
    fetchError: { type: "Error | null", description: "Error from the last access list fetch, or `null`." },
    grant: { type: "(grantee: Grantee, role: IamRole) => Promise<void>", description: "Grant a person or a team the given role." },
    grantableRoles: { type: "readonly IamRole[]", description: "Roles a person can be granted on this resource kind." },
    grantError: { type: "Error | null", description: "Error from the last grant attempt, or `null`." },
    hasGrantableRoles: { type: "boolean", description: "Whether the resource kind supports role grants." },
    isGranting: { type: "boolean", description: "Whether a grant operation is in flight." },
    isLoading: { type: "boolean", description: "Whether the initial access list is loading." },
    isRefetching: { type: "boolean", description: "Whether a background refetch of the access list is in flight." },
    isRevoking: { type: "boolean", description: "Whether a revoke operation is in flight." },
    refetch: { type: "() => void", description: "Re-fetch the access list from the server." },
    revoke: { type: "(grantee: Grantee) => Promise<void>", description: "Revoke every role the grantee holds directly on the resource." },
    revokeError: { type: "Error | null", description: "Error from the last revoke attempt, or `null`." },
    teamGrantableRoles: { type: "readonly IamRole[]", description: "Roles a team can be granted on this resource kind." },
  }}
/>

```tsx
const share = useShareFlow({
  kind: "agent",
  id: agentId,
  resourceKind: ApiResourceKind.agent,
});

await share.grant(personGrantee(accountId), IamRole.viewer);
if (share.canShareWithTeams) {
  await share.grant(teamGrantee(teamId), IamRole.viewer);
}
await share.revoke(teamGrantee(teamId));
```


### useWhoAmI

Data hook that fetches the current authenticated user's
`IdentityAccount` via `identityAccount.whoAmI()`.

The result is cached for the lifetime of the `Stigmer` client
instance — subsequent mounts and re-renders reuse the cached
value without additional network calls.

Useful for self-protection in access management UIs (disabling
"remove" or "change role" on the current user) and displaying
a "You" indicator.

**Signature**

```tsx
function useWhoAmI(): UseWhoAmIReturn
```

**Returns** — `UseWhoAmIReturn`

<TypeTable
  type={{
    account: { type: "IdentityAccount | null", description: "The authenticated user's identity account, or `null` while loading / on error." },
    error: { type: "Error | null", description: "Error from the failed request, or `null` when healthy." },
    isLoading: { type: "boolean", description: "`true` while the initial fetch is in flight." },
  }}
/>

```tsx
const { account } = useWhoAmI();
const myId = account?.metadata?.id;
```


## Components

### GrantAccessForm

Form for granting a person or a team access to a resource.

All visual properties flow through `--stgm-*` design tokens.

**Props** — `GrantAccessFormProps`

<TypeTable
  type={{
    className: { type: "string", description: "Additional CSS class names for the root container." },
    excludeGrantees: { type: "readonly Grantee[]", description: "Grantees that already have access (shown disabled in the picker)." },
    includeTeams: { type: "boolean", description: "Offer the organization's teams beside its people. Set it where the resource can be shared with a team (`useShareFlow().canShareWithTeams`)." },
    onCancel: { type: "() => void", description: "Fired when the user cancels." },
    onGranted: { type: "(policy: IamPolicy) => void", description: "Fired after a policy is successfully created." },
    orgId: { type: "string", description: "Organization whose people (and teams) can be granted access. Drives the PrincipalPicker typeahead.", required: true },
    resourceId: { type: "string", description: "ID of the resource being granted access to.", required: true },
    resourceKind: { type: "ApiResourceKind", description: "Resource kind being granted access to. Determines which roles appear.", required: true },
    resourceKindString: { type: "string", description: "Resource kind string (e.g. \"organization\") for the API resource ref.", required: true },
  }}
/>

```tsx
<GrantAccessForm
  resourceKind={ApiResourceKind.agent}
  resourceKindString="agent"
  resourceId={agentId}
  orgId={orgId}
  includeTeams={share.canShareWithTeams}
  onGranted={() => share.refetch()}
  onCancel={() => setShowForm(false)}
/>
```


### OrgMembersPanel

Self-contained panel for managing organization members.

Displays all principals with access to the organization, their
role grants, and provides actions to change roles and remove
members. New members arrive through invitations where the edition
serves them (Enterprise and Cloud); on open source people join the
organization the first time they sign in and the server records their
role. The current user is identified via
`identityAccount.whoAmI()` for self-protection (disabling
destructive actions on yourself).

All visual properties flow through `--stgm-*` design tokens.

**Props** — `OrgMembersPanelProps`

<TypeTable
  type={{
    className: { type: "string", description: "Additional CSS class names for the root container." },
    onRefetchRef: { type: "(refetch: () => void) => void", description: "Exposed refetch for parent-triggered refresh." },
    orgId: { type: "string", description: "Organization ID (`metadata.id`) whose members to manage.", required: true },
  }}
/>

```tsx
<OrgMembersPanel orgId={activeOrg.metadata.id} />
```


### PeopleWithAccess

The list of grantees with their roles, plus the inline grant form.

Reading the access list requires `can_view_access`; this component assumes
the caller has already gated rendering on it (e.g. via `PermissionGate` or
a parent trigger). Mutations are gated here, in proportion to the action:
the grant form and per-row remove buttons render only behind
`can_grant_access`, so a viewer sees *who* has access without being offered
controls the server would reject. The backend remains the enforcer.

On the open-source edition (`"local"`) a resource that is not an
organization is never shared with individual people — the server's grant
scope admits organizations only, and `checkMyPermission` answers
`can_grant_access` false for everything else — so the body is one honest
sentence naming the editions that do share per resource, never "0 with
access" with no controls. This is a facility question answered by
deployment mode, the pattern `useDeploymentMode` documents; the tier
question ("is IAM served?") is yes in every edition.

All visual properties flow through `--stgm-*` design tokens.

**Props** — `PeopleWithAccessProps`

<TypeTable
  type={{
    className: { type: "string", description: "Additional CSS class names for the root container." },
    orgId: { type: "string", description: "Organization the resource belongs to (`metadata.org`). Drives the people-and-teams typeahead in the grant form.", required: true },
    resource: { type: "ShareFlowResource", description: "The resource whose access list is shown.", required: true, typeDescriptionLink: "#shareflowresource" },
    resourceKind: { type: "ApiResourceKind", description: "ApiResourceKind enum value for grantable-role lookup.", required: true },
    resourceKindString: { type: "string", description: "Resource kind string for the API ref (e.g. \"agent\", \"session\").", required: true },
  }}
/>


### PermissionGate

Conditionally renders children based on the current user's
permission on a resource.

Uses [`useCheckPermission`](#usecheckpermission) under the hood. In OSS mode (where
the IAM service is unavailable), the gate is always permissive —
children are rendered immediately without a network call.

This component is designed to hide UI elements (buttons, actions,
controls) that the user cannot perform. It does NOT enforce
security — the server rejects unauthorized requests regardless of
what the client renders.

**Props** — `PermissionGateProps`

<TypeTable
  type={{
    children: { type: "ReactNode", description: "Content rendered when the user has the required permission.", required: true },
    fallback: { type: "ReactNode", description: "Optional content rendered when the user lacks permission. Defaults to rendering nothing (null)." },
    loading: { type: "ReactNode", description: "Optional content rendered while the permission check is in flight. Defaults to rendering nothing (null) to avoid UI flicker." },
    relation: { type: "string", description: "The permission relation to check (e.g. \"can_edit\", \"can_delete\").", required: true },
    resource: { type: "PermissionCheckResource | null", description: "The resource to check permission on, or `null` to skip (permissive).", required: true },
  }}
/>

```tsx
<PermissionGate resource={{ kind: "agent", id }} relation="can_edit">
  <EditButton onClick={handleEdit} />
</PermissionGate>

<PermissionGate
  resource={{ kind: "session", id }}
  relation="can_grant_access"
  fallback={<Tooltip content="You don't have permission to share" />}
>
  <ShareButton onClick={openShareDialog} />
</PermissionGate>
```


### PrincipalPicker

Accessible combobox for picking a person or a team to share with.

All visual properties flow through `--stgm-*` design tokens.

**Props** — `PrincipalPickerProps`

<TypeTable
  type={{
    autoFocus: { type: "boolean", description: "Focus the search input on mount. Right where the picker opens on a deliberate click (the grant form); turn it off where the picker is always on screen, or it takes focus from the rest of the page." },
    className: { type: "string", description: "Additional CSS class names for the root container." },
    disabled: { type: "boolean", description: "Disable the control." },
    excludeGrantees: { type: "readonly Grantee[]", description: "Grantees shown disabled because they already have access." },
    includeTeams: { type: "boolean", description: "Offer the organization's teams beside its people. Set it where the resource can be shared with a team (`useShareFlow().canShareWithTeams`)." },
    onChange: { type: "(grantee: GranteeCandidate | null) => void", description: "Fired when the selection changes.", required: true },
    orgId: { type: "string", description: "Organization whose people (and teams) are selectable.", required: true },
    value: { type: "GranteeCandidate | null", description: "Currently selected grantee, or `null`. Controlled.", required: true },
  }}
/>


### ProviderBadge

A subtle pill naming the identity source an account belongs to.

Renders nothing when no provider label is available (e.g. legacy accounts
with unspecified provisioning mode), so it never adds empty chrome.

All visual properties flow through `--stgm-*` design tokens.

**Props** — `ProviderBadgeProps`

<TypeTable
  type={{
    className: { type: "string", description: "Additional CSS class names." },
    principal: { type: "ApiResourceRefView | undefined", description: "The principal whose identity source to display.", required: true },
  }}
/>


### RoleSelector

Styled radio-group for selecting an IAM role.

Displays all grantable roles for the given resource kind as
selectable pills, each showing the role name and description.

For a headless alternative, use [`useRoleSelector`](#useroleselector) directly.

All visual properties flow through `--stgm-*` design tokens.

**Props** — `RoleSelectorProps`

<TypeTable
  type={{
    className: { type: "string", description: "Additional CSS class names for the root container." },
    defaultRole: { type: "IamRole", description: "Initial role to select (uncontrolled mode)." },
    disabled: { type: "boolean", description: "Disable all role options." },
    granteeKind: { type: "\"identity_account\" | \"team\"", description: "Who the role is for. A team is offered only the kind's team roles. Defaults to a person." },
    kind: { type: "ApiResourceKind | null", description: "The resource kind whose grantable roles to display.", required: true },
    onSelect: { type: "(role: IamRole) => void", description: "Fired when the user selects a role." },
    selected: { type: "IamRole | null", description: "Externally controlled selected role — makes this a controlled component." },
  }}
/>

```tsx
<RoleSelector
  kind={ApiResourceKind.organization}
  onSelect={(role) => setRole(role)}
/>
```


### SharePanel

Self-contained sharing panel that displays who has access to a
resource and allows granting/revoking access.

A thin header-and-close wrapper around [`PeopleWithAccess`](#peoplewithaccess) — the same
"people with access" body the unified Manage access dialog renders — so the
two surfaces stay byte-for-byte consistent. Suitable for embedding in a
popover or sidebar; for the full visibility + people experience use the
Manage access dialog instead.

All visual properties flow through `--stgm-*` design tokens.

**Props** — `SharePanelProps`

<TypeTable
  type={{
    className: { type: "string", description: "Additional CSS class names for the root container." },
    onClose: { type: "() => void", description: "Fired when the user closes the panel." },
    orgId: { type: "string", description: "Organization the resource belongs to (`metadata.org`). Drives the org-member typeahead in the grant form.", required: true },
    resource: { type: "ShareFlowResource", description: "The resource to share.", required: true, typeDescriptionLink: "#shareflowresource" },
    resourceKind: { type: "ApiResourceKind", description: "ApiResourceKind enum value for grantable-role lookup.", required: true },
    resourceKindString: { type: "string", description: "Resource kind string for the API ref (e.g. \"agent\", \"session\").", required: true },
  }}
/>

```tsx
<SharePanel
  resource={{ kind: "session", id: sessionId, resourceKind: ApiResourceKind.session }}
  resourceKindString="session"
  resourceKind={ApiResourceKind.session}
  orgId={orgId}
  onClose={() => setOpen(false)}
/>
```


## Types

### CheckPermissionOptions

Options for [`useCheckPermission`](#usecheckpermission).

<TypeTable
  type={{
    fail: { type: "\"open\" | \"closed\"", description: "Behavior when authorization cannot be confirmed — the check is in flight, the RPC failed, or the resource is `null`.  - `\"open\"` (default): `allowed` is `true`. Right for gating   *capabilities* (buttons, actions) — the server re-checks every   request anyway, so a transient failure never hides a control the   server would honor. - `\"closed\"`: `allowed` is `false` until the server explicitly   authorizes. Right for *discoverability* surfaces (navigation to   operator-only areas) that must not appear before the server has   said yes." },
  }}
/>


### GranteeCandidate

A person or a team the share can be granted to, discriminated by `kind`.


### PermissionCheckResource

Resource reference for permission checks.

<TypeTable
  type={{
    id: { type: "string", description: "Resource ID.", required: true },
    kind: { type: "string", description: "Resource kind (e.g. \"agent\", \"session\").", required: true },
  }}
/>


### PersonCandidate

A person the share can be granted to.

<TypeTable
  type={{
    email: { type: "string", description: "Email address, when known.", required: true },
    grantee: { type: "Grantee", description: "", required: true },
    kind: { type: "\"identity_account\"", description: "", required: true },
    name: { type: "string", description: "Display name, falling back to email, then id.", required: true },
    view: { type: "ApiResourceRefView", description: "The full view, for the provider badge and avatar.", required: true },
  }}
/>


### providerLabel

The human-readable identity-source label for a principal, or `""` when none
is known.

Email is not an identity key in Stigmer — a person can have a direct account
and federated accounts that share one email. This label (e.g. "Stigmer" for
direct sign-ups, or an IdentityProvider's display name for federated ones)
is what lets the UI tell those accounts apart.

```tsx
function providerLabel(principal: ApiResourceRefView | undefined): string
```


### ResourceAccessRef

Resource reference for the access query.

<TypeTable
  type={{
    id: { type: "string", description: "Resource ID.", required: true },
    kind: { type: "string", description: "Resource kind (e.g. \"organization\", \"agent\").", required: true },
  }}
/>


### RoleOption

A single role option with display metadata.

<TypeTable
  type={{
    description: { type: "string", description: "Short description suitable for tooltips (e.g. \"Edit access and member management\").", required: true },
    label: { type: "string", description: "Human-readable display name (e.g. \"Admin\").", required: true },
    role: { type: "IamRole", description: "The IamRole enum value.", required: true },
    value: { type: "string", description: "The FGA relation string (e.g. \"admin\").", required: true },
  }}
/>


### SelectedGrantee

A grantee selected through [`PrincipalPicker`](#principalpicker).


### ShareFlowResource

Identifies a resource for the share flow.

<TypeTable
  type={{
    id: { type: "string", description: "Resource ID.", required: true },
    kind: { type: "string", description: "Resource kind string (e.g. \"agent\", \"session\").", required: true },
    resourceKind: { type: "ApiResourceKind", description: "ApiResourceKind enum value for grantable-role lookup." },
  }}
/>


### TeamCandidate

A team the share can be granted to.

<TypeTable
  type={{
    description: { type: "string", description: "The team's description, possibly empty.", required: true },
    grantee: { type: "Grantee", description: "", required: true },
    kind: { type: "\"team\"", description: "", required: true },
    name: { type: "string", description: "The team's name.", required: true },
    team: { type: "Team", description: "The full resource.", required: true },
  }}
/>


### UseGranteeCandidatesOptions

Options for [`useGranteeCandidates`](#usegranteecandidates).

<TypeTable
  type={{
    includeTeams: { type: "boolean", description: "Offer the organization's teams too. Pass `false` where teams cannot be granted.", required: true },
    orgId: { type: "string | null", description: "The organization whose people (and teams) are offered, or `null` to skip.", required: true },
  }}
/>


### UseResourceAccessOptions

Options for [`useResourceAccess`](#useresourceaccess).

<TypeTable
  type={{
    includeInherited: { type: "boolean", description: "Include roles inherited from parent resources. Defaults to `false`." },
  }}
/>

