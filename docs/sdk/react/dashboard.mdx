---
title: Dashboard
description: Hooks for dashboard metrics, activity feeds, and analytics data.
---

{/* Auto-generated by @stigmer/react docs generator. DO NOT EDIT. */}

## Hooks

### useAgentExecutionSummary

Data hook that fetches aggregated agent execution statistics for an
organization. Returns phase counts, active count, average duration,
and top failing agents.

Cost is intentionally excluded from this response — the dashboard
sources cost from `useOrgUsageReport` (billing source of truth) to
prevent double-counting. See AD-DASH-005.

**Signature**

```tsx
function useAgentExecutionSummary(options: UseAgentExecutionSummaryOptions): UseAgentExecutionSummaryReturn
```

<TypeTable
  type={{
    options: { type: "UseAgentExecutionSummaryOptions", description: "", required: true },
  }}
/>

**Returns** — `UseAgentExecutionSummaryReturn`

<TypeTable
  type={{
    error: { type: "Error | null", description: "" },
    isLoading: { type: "boolean", description: "" },
    isRefetching: { type: "boolean", description: "" },
    refetch: { type: "() => void", description: "" },
    summary: { type: "AgentExecutionSummary | null", description: "" },
  }}
/>


### useDashboardFailedRuns

Composition hook that fetches recent failed executions from both
agent and workflow domains, normalizes them into [`DashboardFailedRun`](#dashboardfailedrun)
entries, and interleaves them by timestamp (newest first).

Returns at most 5 total entries to keep the widget compact.

**Signature**

```tsx
function useDashboardFailedRuns(org: string | null | undefined): UseDashboardFailedRunsReturn
```

<TypeTable
  type={{
    org: { type: "string | null | undefined", description: "", required: true },
  }}
/>

**Returns** — `UseDashboardFailedRunsReturn`

<TypeTable
  type={{
    error: { type: "Error | null", description: "" },
    failedRuns: { type: "readonly DashboardFailedRun[]", description: "" },
    isLoading: { type: "boolean", description: "" },
  }}
/>


### useDashboardSummary

Composition hook that merges agent execution summary, workflow execution
summary, and org usage report into a single [`DashboardSummary`](#dashboardsummary).

- Execution counts (active, completed, failed) are added across both
  domains — safe because agent and workflow executions are distinct resources.
- Cost comes from `getOrgUsageReport` (billing source of truth). NOT from
  summing agent + workflow costs. See AD-DASH-005.

Follows the same client-side merge pattern as `useRecentActivity`.

**Signature**

```tsx
function useDashboardSummary(options: UseDashboardSummaryOptions): UseDashboardSummaryReturn
```

<TypeTable
  type={{
    options: { type: "UseDashboardSummaryOptions", description: "", required: true },
  }}
/>

**Returns** — `UseDashboardSummaryReturn`

<TypeTable
  type={{
    error: { type: "Error | null", description: "" },
    isLoading: { type: "boolean", description: "" },
    refetch: { type: "() => void", description: "" },
    summary: { type: "DashboardSummary | null", description: "" },
  }}
/>


## Types

### DashboardFailedRun

A normalized entry representing a failed execution from either domain.

<TypeTable
  type={{
    error: { type: "string", description: "Error message from the failed execution.", required: true },
    failedAt: { type: "Date", description: "When the execution failed.", required: true },
    id: { type: "string", description: "Execution ID (`aex_*` or `wex_*`).", required: true },
    name: { type: "string", description: "Execution name or subject.", required: true },
    resourceName: { type: "string", description: "The agent or workflow name associated with this execution.", required: true },
    type: { type: "\"workflow_execution\" | \"agent_execution\"", description: "Discriminator for routing navigation.", required: true },
  }}
/>


### DashboardFailedRuns

Widget showing recent failed executions from both agent and workflow
domains, interleaved by timestamp.

Each row includes a type badge (Agent / Workflow), the execution name,
a truncated error, and a relative timestamp.


### DashboardFailedRunsProps

<TypeTable
  type={{
    className: { type: "string", description: "" },
    failedRuns: { type: "readonly DashboardFailedRun[]", description: "", required: true },
    isLoading: { type: "boolean", description: "", required: true },
    onViewClick: { type: "(id: string, type: \"workflow_execution\" | \"agent_execution\") => void", description: "Called when the user clicks \"View\" on a failed run." },
  }}
/>


### DashboardKPICards

Unified stat cards showing combined agent + workflow execution KPIs.

Each card shows the combined count with a breakdown tooltip showing
per-source values (e.g., "2 agent, 1 workflow").

Cost comes from the billing source of truth (getOrgUsageReport),
not from summing per-domain costs. See AD-DASH-005.


### DashboardKPICardsProps

<TypeTable
  type={{
    className: { type: "string", description: "" },
    isLoading: { type: "boolean", description: "", required: true },
    summary: { type: "DashboardSummary | null", description: "", required: true },
  }}
/>


### DashboardSummary

Unified dashboard summary combining operational metrics from both
agent and workflow execution domains.

Execution counts (active, completed, failed) are safe to add because
agent executions and workflow executions are distinct resources.

Cost comes from `GetOrgUsageReportOutput` (billing source of truth),
NOT from summing agent + workflow costs. This prevents double-counting
when workflows delegate to agents. See AD-DASH-005.

<TypeTable
  type={{
    activeCount: { type: "number", description: "Combined agent + workflow active execution count.", required: true },
    agent: { type: "AgentExecutionSummary | null", description: "Agent-side execution summary for per-source breakdown in tooltips.", required: true },
    completedCount: { type: "number", description: "Combined agent + workflow completed count.", required: true },
    failedCount: { type: "number", description: "Combined agent + workflow failed count.", required: true },
    orgUsage: { type: "GetOrgUsageReportOutput | null", description: "Org-level usage report for cost details.", required: true },
    totalCostUsd: { type: "number", description: "Total platform cost in USD from the billing source of truth. Sourced from `getOrgUsageReport.total_billable_cost_micros`.", required: true },
    workflow: { type: "ExecutionSummary | null", description: "Workflow-side execution summary for per-source breakdown in tooltips.", required: true },
  }}
/>


### OperationalDashboard

Composed dashboard widget that aggregates operational metrics from both
agent and workflow domains into a unified platform view.

Layout:
- Row 1: KPI stat cards (Active | Completed | Failed | Total Cost)
- Row 2: Pending Approvals | Recent Failures (2-column grid)

Cost comes from the billing source of truth (`getOrgUsageReport`),
not from summing agent + workflow costs. See AD-DASH-005.

```tsx
<OperationalDashboard
  org="acme"
  orgId={activeOrg?.metadata?.id}
  onApprovalClick={(id) => navigate(`/executions/${id}`)}
  onFailedRunClick={(id, type) => navigate(`/executions/${id}`)}
/>
```


### OperationalDashboardProps

<TypeTable
  type={{
    className: { type: "string", description: "" },
    onApprovalClick: { type: "(executionId: string) => void", description: "Called when the user clicks \"Review\" on a pending approval." },
    onFailedRunClick: { type: "(id: string, type: \"workflow_execution\" | \"agent_execution\") => void", description: "Called when the user clicks \"View\" on a failed execution." },
    org: { type: "string | null | undefined", description: "Organization slug for execution summaries and pending approvals.", required: true },
    orgId: { type: "string | null | undefined", description: "Organization ID (metadata.id) for the usage report.", required: true },
  }}
/>


### UseAgentExecutionSummaryOptions

Options for [`useAgentExecutionSummary`](#useagentexecutionsummary).

<TypeTable
  type={{
    org: { type: "string | null | undefined", description: "Organization slug. When empty/null, the hook does not fetch.", required: true },
    refetchInterval: { type: "number | false", description: "Refetch interval in milliseconds. `0` or `false` disables." },
    timeWindow: { type: "AgentExecutionSummaryTimeWindow", description: "Time window for aggregation." },
  }}
/>


### UseDashboardSummaryOptions

Options for [`useDashboardSummary`](#usedashboardsummary).

<TypeTable
  type={{
    org: { type: "string | null | undefined", description: "Organization slug for execution summaries.", required: true },
    orgId: { type: "string | null | undefined", description: "Organization ID (metadata.id) for the usage report.", required: true },
    refetchInterval: { type: "number", description: "Refetch interval in milliseconds." },
  }}
/>

