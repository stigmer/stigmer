---
title: switch
---

{/* Auto-generated by @stigmer/react docs generator. DO NOT EDIT. */}

## Components

### Switch

Accessible on/off toggle following the WAI-ARIA Switch pattern.

Built on a native `<button role="switch">` so Space/Enter activation,
focus, and disabled semantics come from the platform. `aria-checked`
reflects the state for assistive technology.

Use a switch only for instant binary state changes that take effect
immediately (e.g. enabling sharing) — not for choices that need a
separate save step (use radios or a select for those).

The visible track is compact; an invisible expanded hit area keeps the
touch target at ~44px without disturbing layout. All visual properties
flow through `--stgm-*` design tokens, and the thumb transition is
suppressed under `prefers-reduced-motion`.

**Props** — `SwitchProps`

<TypeTable
  type={{
    "aria-describedby": { type: "string", description: "ID of the element that describes this switch (helper copy)." },
    "aria-label": { type: "string", description: "Accessible name. Required unless `aria-labelledby` points at a visible label element." },
    "aria-labelledby": { type: "string", description: "ID of the element that labels this switch." },
    checked: { type: "boolean", description: "Whether the switch is on.", required: true },
    className: { type: "string", description: "Additional CSS classes for the root button." },
    disabled: { type: "boolean", description: "When `true`, the switch is shown but non-interactive." },
    id: { type: "string", description: "Element ID, for association with an external `<label htmlFor>`." },
    onCheckedChange: { type: "(checked: boolean) => void", description: "Called with the next value when the user toggles the switch.", required: true },
  }}
/>

```tsx
<Switch
  checked={sharing.enabled}
  onCheckedChange={(next) => save({ ...sharing, enabled: next })}
  aria-label="Enable sharing"
/>
```

