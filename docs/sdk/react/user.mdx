---
title: user
---

{/* Auto-generated by @stigmer/react docs generator. DO NOT EDIT. */}

## Components

### UserMenu

User menu dropdown for sidebar navigation.

Displays the current user (avatar + name/email) or a "Local mode"
indicator when unauthenticated, with optional color scheme switching,
settings navigation, app-specific extra items, and sign out.

Designed for sidebar placement — the trigger uses `sidebar-*` design
tokens. The portaled dropdown uses standard `popover-*` / main-area
tokens per theme-token-guidelines (DD-005).

Framework-agnostic: all actions are expressed as callback props
(DD-004). The consumer bridges to their routing, auth, and theme
systems.

**Props** — `UserMenuProps`

<TypeTable
  type={{
    className: { type: "string", description: "Additional CSS class names merged onto the trigger." },
    colorMode: { type: "ColorMode", description: "Current color mode selection. Accepts the unresolved value (`\"light\"`, `\"dark\"`, or `\"system\"`) so the radio group reflects the user's explicit choice — not the OS-resolved result.  When omitted (together with onColorModeChange), the color scheme section is hidden entirely.", typeDescriptionLink: "/docs/sdk/react/core#colormode" },
    extraItems: { type: "ReactNode", description: "Extra menu items rendered between the color scheme section and sign out. Use this for app-specific actions like \"Get Desktop App\" in the web Console.  Items should be `@base-ui/react` `Menu.Item` elements (or the host app's wrapper over them) to participate in the menu's keyboard navigation and ARIA roles." },
    onColorModeChange: { type: "(mode: ColorMode) => void", description: "Called when the user selects a different color mode.  The consumer is responsible for persisting the choice and updating the host application's theme (e.g. calling `next-themes` `setTheme` in the web Console, or writing to localStorage in the desktop app)." },
    onSettingsClick: { type: "() => void", description: "Called when the user clicks the \"Settings\" menu item. When omitted, the Settings item is hidden." },
    onSignOut: { type: "() => void", description: "Called when the user clicks \"Sign out\". When omitted, the Sign out item is hidden." },
    user: { type: "{ email?: string; name?: string } | null", description: "The authenticated user. Pass `null` for unauthenticated / local mode, which renders a generic icon and \"Local mode\" label in the trigger.", required: true },
  }}
/>

```tsx
<UserMenu
  user={{ name: "Jane", email: "jane@acme.com" }}
  colorMode="dark"
  onColorModeChange={(mode) => setTheme(mode)}
  onSettingsClick={() => router.push("/settings")}
  onSignOut={() => logout()}
/>
```

