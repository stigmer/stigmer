---
title: schedule
---

{/* Auto-generated by @stigmer/react docs generator. DO NOT EDIT. */}

## Hooks

### useResumeSchedule

Behavior hook that resumes a platform-paused schedule.

Resume is the ONE path that clears `status.paused_reason` (and the
failure streak with it) — re-applying the manifest deliberately does
not, so a routine GitOps apply can never silently un-pause a failing
schedule. Resuming a schedule that is not paused is a server-side
no-op; resuming a disabled schedule clears the pause but the schedule
stays disabled (the owner's lever is separate — see
[`useSetScheduleEnabled`](#usesetscheduleenabled)).

Error toasts relay the server's message verbatim: the copy is
identical across editions and states the remedy.

**Signature**

```tsx
function useResumeSchedule(): UseResumeScheduleReturn
```

**Returns** — `UseResumeScheduleReturn`

<TypeTable
  type={{
    error: { type: "Error | null", description: "Error from the last failed resume, or `null` when healthy." },
    isResuming: { type: "boolean", description: "`true` while the resume request is in flight." },
    resumeSchedule: { type: "(scheduleId: string) => Promise<Schedule>", description: "Clear the platform's failure-streak pause and let the schedule fire again. Resolves with the post-resume schedule; the caller refetches or replaces its local copy from it." },
  }}
/>


### useSchedule

Data hook that fetches a single Schedule by organization and slug.

Wraps `stigmer.schedule.getByReference()` with loading, error, and
not-found state management. The loaded resource carries the spec
(cron, time zone, enabled, target) and the platform-written status
(`next_fire_at`, `last_fire_at`, `last_execution_id`,
`consecutive_failures`, `paused_reason`) — everything the detail view
renders, including the disabled-vs-paused distinction (see
[`deriveScheduleState`](#deriveschedulestate)).

Pass `null` for either `org` or `slug` to skip fetching (stable
no-op).

**Not-found handling:** a NOT_FOUND response sets `schedule` to
`null` without raising an error; `schedule === null && !isLoading
&& !error` means the resource does not exist.

**Signature**

```tsx
function useSchedule(org: string | null, slug: string | null): UseScheduleReturn
```

<TypeTable
  type={{
    org: { type: "string | null", description: "", required: true },
    slug: { type: "string | null", description: "", required: true },
  }}
/>

**Returns** — `UseScheduleReturn`

<TypeTable
  type={{
    error: { type: "Error | null", description: "Error from the last failed request, or `null` when healthy." },
    isLoading: { type: "boolean", description: "`true` while the initial fetch is in flight." },
    isRefetching: { type: "boolean", description: "`true` while a background refetch is in flight." },
    refetch: { type: "() => void", description: "Discard cached data and re-fetch the schedule from the server." },
    schedule: { type: "Schedule | null", description: "The resolved schedule, or `null` while loading, on error, or when not found." },
  }}
/>

```tsx
const { schedule, isLoading, error, refetch } = useSchedule("isc", "daily-fee-reminders");
```


### useScheduleCount

Data hook that fetches the total count of an organization's schedules.

Issues a minimal direct `listSchedules` call to retrieve only the
total count. Useful for the Library landing card and dashboard
widgets. There is no scope option: schedules are org-only.

For the full paginated schedule list, use [`useScheduleList`](#useschedulelist)
instead.

Pass `null` as `org` to skip fetching (stable no-op).

**Signature**

```tsx
function useScheduleCount(org: string | null, options?: UseScheduleCountOptions): UseScheduleCountReturn
```

<TypeTable
  type={{
    org: { type: "string | null", description: "", required: true },
    options: { type: "UseScheduleCountOptions", description: "" },
  }}
/>

**Returns** — `UseScheduleCountReturn`

<TypeTable
  type={{
    count: { type: "number | undefined", description: "Total number of schedules in the organization. `undefined` until the first successful fetch completes." },
    error: { type: "Error | null", description: "Error from the last failed request, or `null` when healthy." },
    isLoading: { type: "boolean", description: "`true` while the count fetch is in flight." },
    refetch: { type: "() => void", description: "Discard cached data and re-fetch the count from the server." },
  }}
/>

```tsx
const { count, isLoading } = useScheduleCount("isc");
```


### useScheduleList

Data hook that fetches a paginated list of an organization's schedules.

Backed by the direct `listSchedules` query — not the search service —
so each entry is a full `Schedule` proto with the operational status
fields a schedule list exists to show (`next_fire_at`, enabled/paused
state, `last_fire_at`). There is no text query or visibility scope:
schedules are org-only and have no server-side search.

Pass `null` as `org` to skip fetching (stable no-op).

**Signature**

```tsx
function useScheduleList(org: string | null, options?: UseScheduleListOptions): UseScheduleListReturn
```

<TypeTable
  type={{
    org: { type: "string | null", description: "", required: true },
    options: { type: "UseScheduleListOptions", description: "" },
  }}
/>

**Returns** — `UseScheduleListReturn`

<TypeTable
  type={{
    currentPage: { type: "number", description: "The current page number (mirrors the `page` option)." },
    error: { type: "Error | null", description: "Error from the last failed request, or `null` when healthy." },
    isLoading: { type: "boolean", description: "`true` while the initial fetch is in flight." },
    isRefetching: { type: "boolean", description: "`true` while a background refetch is in flight." },
    refetch: { type: "() => void", description: "Discard cached data and re-fetch the current page from the server." },
    schedules: { type: "readonly Schedule[]", description: "Full `Schedule` protos (spec + status) for the current page." },
    totalCount: { type: "number", description: "Total number of schedules in the organization." },
    totalPages: { type: "number", description: "Total pages available at the current page size." },
  }}
/>

```tsx
const { schedules, totalCount, isLoading } = useScheduleList("isc", {
  page: 1,
  pageSize: 20,
});
```


### useSetScheduleEnabled

Behavior hook that flips a schedule's owner-controlled `enabled` flag.

The write path is deliberate: clone the fetched proto, flip one
field, and re-apply the WHOLE resource through the manifest engine
(`stigmer.manifest.apply`) — never down-convert to the curated
`ScheduleInput` shape. The server replaces spec and mutable metadata
wholesale on update, and `ScheduleInput` cannot express every
metadata field (tags, description), so the input route would silently
wipe them. The full-proto route is lossless by construction; the
server ignores client-provided status, and re-applying never clears a
platform pause (Resume is the one clearing path — see
[`useResumeSchedule`](#useresumeschedule)).

**Signature**

```tsx
function useSetScheduleEnabled(): UseSetScheduleEnabledReturn
```

**Returns** — `UseSetScheduleEnabledReturn`

<TypeTable
  type={{
    error: { type: "Error | null", description: "Error from the last failed apply, or `null` when healthy." },
    isPending: { type: "boolean", description: "`true` while the apply is in flight." },
    setEnabled: { type: "(schedule: Schedule, enabled: boolean) => Promise<Schedule>", description: "Flip the owner's `spec.enabled` switch. Takes the full fetched schedule (not just an id) because the write re-applies the whole resource. Resolves with the post-apply schedule; the caller refetches or replaces its local copy from it." },
  }}
/>


### useTriggerSchedule

Behavior hook that fires a schedule once, immediately.

The server enforces a refusal matrix — a disabled schedule refuses
first, then a paused one — both as `FAILED_PRECONDITION` with copy
that names the remedy ("enable it before triggering" / "resume it
before triggering"). Error toasts relay that copy verbatim; the copy
is byte-identical across editions.

Triggering starts a real, billable execution — gate the call behind a
confirmation (the detail view uses `ConfirmDialog`).

**Signature**

```tsx
function useTriggerSchedule(): UseTriggerScheduleReturn
```

**Returns** — `UseTriggerScheduleReturn`

<TypeTable
  type={{
    error: { type: "Error | null", description: "Error from the last failed or refused trigger, or `null` when healthy." },
    isTriggering: { type: "boolean", description: "`true` while the trigger request is in flight." },
    triggerSchedule: { type: "(scheduleId: string) => Promise<Schedule>", description: "Fire the schedule once, now — a real agent execution, outside the cron cadence. Resolves with the post-trigger schedule; refetch to pick up the new `status.last_execution_id`." },
  }}
/>


## Components

### ScheduleDetailView

Self-contained detail view for a Schedule (stigmer/stigmer#352).

The view's one non-negotiable is rendering the two stop-levers
distinctly, each with its inline remedy:

- **Disabled** (owner's `spec.enabled` switch) — banner with an
  Enable action (a lossless full-proto re-apply).
- **Paused** (platform's failure-streak latch,
  `status.paused_reason`) — banner showing the reason with a Resume
  action (the one path that clears the pause).

Trigger ("Run now") starts a real, billable execution and is gated
behind a confirmation; it is disabled while the schedule cannot fire
(the banner names the remedy). Edit YAML, Export, and Delete round
out the action set.

Handles loading, error, and not-found states automatically.
Zero Console dependencies — safe for platform builder embedding.
All visual properties flow through `--stgm-*` design tokens.

**Props** — `ScheduleDetailViewProps`

<TypeTable
  type={{
    className: { type: "string", description: "Additional CSS classes for the root container." },
    now: { type: "Date", description: "The instant \"next fire\" countdowns are computed against. Injectable for deterministic tests and Scenar fixtures." },
    onDeleted: { type: "() => void", description: "Called after a successful delete (navigate back to the list)." },
    onNavigateToAgent: { type: "(org: string, slug: string) => void", description: "Called when the user activates the target-agent reference. Navigation is the consumer's concern (DD-004) — the Console pushes a Library route; an embedding host does whatever fits." },
    onNavigateToExecution: { type: "(executionId: string) => void", description: "Called when the user activates the last-execution reference (`aex_…`). When omitted, the id renders as plain text." },
    onResourceLoad: { type: "(schedule: Schedule) => void", description: "Called when the schedule loads or reloads (e.g. breadcrumb label sync)." },
    org: { type: "string", description: "Organization slug.", required: true },
    slug: { type: "string", description: "Schedule slug.", required: true },
  }}
/>


### ScheduleIcon

Clock-with-arrow icon shared by the schedule views.


### ScheduleRowActions

Compact per-row actions for a schedule list: **Run now** on an active
schedule (confirmation-gated — it starts a real, billable execution)
and **Resume** on a platform-paused one. Exactly one action renders
at a time — the one that is currently meaningful; an owner-disabled
schedule renders neither (its remedy lives on the detail page).

Shared by every console's schedule workbench via `renderItemAction`
so row behavior never diverges between apps (DD-016).

**Props** — `ScheduleRowActionsProps`

<TypeTable
  type={{
    onChanged: { type: "() => void", description: "Called after a successful action so the list can refetch." },
    schedule: { type: "Schedule", description: "The schedule this row represents.", required: true },
  }}
/>


## Types

### createScheduleColumns

Column set for a schedule workbench — shared by every console so the
list renders identically everywhere (DD-016).

The columns exist because schedules are listed via the direct query:
each row is a full `Schedule` proto, so the operational fields the
issue calls for (next fire, last run, enabled/paused state) render
straight from the row — no generic `SearchResult` limitations.

Columns are not sortable: the direct query has one fixed server-side
order (newest first), and a sort affordance that silently does
nothing would lie.

```tsx
function createScheduleColumns(options?: ScheduleColumnsOptions): WorkbenchColumnDef<Schedule>[]
```


### createScheduleListFn

Build a `ResourceWorkbench`-compatible `listFn` over the direct
schedule query.

The returned entries are full `Schedule` protos occupying the
`SearchResult` slot of `ListResult` — `ResourceWorkbench` is generic
over its row type (`useResourceCollection` casts entries through to
`TData`), and this is the one documented place where that cast
happens. Consumers type their workbench as
`ResourceWorkbench<Schedule>` and pass `getItemId={(s) => s.metadata?.id}`.

`params.query` and `params.crossOrgPublic` are ignored: schedules have
no server-side text search (render the workbench with
`searchable={false}`) and are never public/cross-org.

```tsx
function createScheduleListFn(client: ScheduleListClient): (params: ListParams) => Promise<ListResult>
```


### deriveScheduleState

Derive a schedule's display state from its spec and status.

A missing spec derives as "disabled" — the same fail-closed reading the
server applies to a manifest that omits `enabled`.

```tsx
function deriveScheduleState(spec: ScheduleSpec | undefined, status: ScheduleStatus | undefined): ScheduleStateInfo
```


### formatNextFire

Compact future-instant formatter for a schedule's next fire time:
`now`, `in 5m`, `in 3h`, `in 6d`, then a short date once "days from
now" stops being how people think about it.

The schedule-local sibling of the activity module's past-only
`formatRelativeTime` (same compact units, same date tail) — kept
separate because that util's contract is deliberately past-only.
A fire time slightly in the past (a tick mid-flight, clock skew)
renders as `now` rather than a nonsense negative countdown.

`now` is injected, never read from the live clock — required for
deterministic tests and Scenar fixtures.

```tsx
function formatNextFire(date: Date, now: Date): string
```


### listSchedulesPage

Fetch one page of an organization's schedules via the direct query.

Handles a verified split between the two server editions: cloud slices
pages server-side, while the OSS server documents that it ignores
`page_info` and returns every matching schedule. A server that
paginated can never return more rows than the requested size, so an
overflowing response is the tell that the server ignored pagination —
only then is the page sliced locally (absolute indices into the full
list). A cloud page passes through untouched.

```tsx
function listSchedulesPage(client: ScheduleListClient, org: string, page: { num?: number; size?: number }): Promise<SchedulePage>
```


### ScheduleColumnsOptions

Options for [`createScheduleColumns`](#createschedulecolumns).

<TypeTable
  type={{
    now: { type: "() => Date", description: "Clock provider for the \"next fire\" and \"last run\" columns. Injectable for deterministic tests and Scenar fixtures." },
  }}
/>


### ScheduleListClient

The slice of the Stigmer client this module needs (mockable in tests).

<TypeTable
  type={{
    schedule: { type: "{ list: unknown }", description: "", required: true },
  }}
/>


### SchedulePage

One page of schedules, with pagination totals.

<TypeTable
  type={{
    items: { type: "readonly Schedule[]", description: "Full `Schedule` protos (spec + status) for the requested page.", required: true },
    totalCount: { type: "number", description: "Total schedules in the org (across all pages).", required: true },
    totalPages: { type: "number", description: "Total pages at the requested page size.", required: true },
  }}
/>


### ScheduleState

The three mutually-exclusive display states of a schedule.


### ScheduleStateInfo

Derived display state: the tri-state plus its shared-badge rendering.

<TypeTable
  type={{
    isPaused: { type: "boolean", description: "`true` when the platform pause latch is set — independent of the display state, so a disabled-AND-paused schedule can still surface its `status.pausedReason`.", required: true },
    label: { type: "string", description: "Human-readable badge label.", required: true },
    phase: { type: "StatusPhase", description: "`StatusPhase` for the shared `StatusBadge` (`--stgm-status-*` tokens).", required: true, typeDescriptionLink: "/docs/sdk/react/resource-workbench#statusphase" },
    state: { type: "ScheduleState", description: "The display state (\"disabled\" wins when both levers are set).", required: true, typeDescriptionLink: "#schedulestate" },
  }}
/>


### UseScheduleCountOptions

Options for [`useScheduleCount`](#useschedulecount).

<TypeTable
  type={{
    refetchToken: { type: "unknown", description: "Opaque token that forces a recount when its value changes." },
  }}
/>


### UseScheduleListOptions

Options for [`useScheduleList`](#useschedulelist).

<TypeTable
  type={{
    page: { type: "number", description: "Page number (1-indexed)." },
    pageSize: { type: "number", description: "Maximum schedules per page." },
  }}
/>

