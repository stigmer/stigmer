---
title: Activity
description: Hooks for recent activity feeds and session history.
---

{/* Auto-generated by @stigmer/react docs generator. DO NOT EDIT. */}

## Hooks

### useRecentActivity

Fetches recent agent sessions and workflow executions, merges them
into a single list sorted by most-recent-first, and exposes a
unified [`RecentActivityEntry`](#recentactivityentry) array.

Implementation uses client-side merge of two existing data hooks
(`useSessionList` and `useWorkflowExecutionList`), keeping the
backend contract unchanged. A dedicated `listRecentActivity` RPC
can replace this approach later without changing consumers.

**Signature**

```tsx
function useRecentActivity(options?: UseRecentActivityOptions): UseRecentActivityReturn
```

<TypeTable
  type={{
    options: { type: "UseRecentActivityOptions", description: "" },
  }}
/>

**Returns** — `UseRecentActivityReturn`

<TypeTable
  type={{
    entries: { type: "readonly RecentActivityEntry[]", description: "Merged entries sorted by `updatedAt` descending." },
    error: { type: "Error | null", description: "First non-null error from either source." },
    isLoading: { type: "boolean", description: "`true` while either source is loading for the first time." },
    refetch: { type: "() => void", description: "Re-fetch both sources." },
  }}
/>


## Types

### groupRecentActivityByTime

Groups recent-activity entries by their `updatedAt` timestamp into
time-based buckets: "Today", "Yesterday", "Previous 7 Days",
"Previous 30 Days", "Older".

Empty groups are omitted. Input order is preserved within each group.

```tsx
function groupRecentActivityByTime(entries: readonly RecentActivityEntry[], now?: Date): readonly RecentActivityGroup[]
```


### RecentActivityEntry

A normalized entry representing either an agent session or a workflow
execution. Used by [`useRecentActivity`](#userecentactivity) and rendered in the sidebar
recents section.

<TypeTable
  type={{
    id: { type: "string", description: "Resource ID (session ID or workflow execution ID).", required: true },
    status: { type: "string", description: "Execution phase for workflow executions (e.g. \"COMPLETED\", \"FAILED\"). `undefined` for sessions." },
    subject: { type: "string", description: "Human-readable label: session subject or workflow execution name.", required: true },
    type: { type: "RecentActivityType", description: "Discriminator — determines which viewer to open on click.", required: true, typeDescriptionLink: "#recentactivitytype" },
    updatedAt: { type: "Date", description: "Last meaningful update timestamp, used for interleaved sort. For sessions: `status.audit.specAudit.createdAt`. For workflow executions: `status.audit.specAudit.createdAt`.", required: true },
  }}
/>


### RecentActivityGroup

A time-based group of recent activity entries.

<TypeTable
  type={{
    entries: { type: "readonly RecentActivityEntry[]", description: "Entries in this group, sorted by `updatedAt` descending.", required: true },
    label: { type: "string", description: "Display label (e.g. \"Today\", \"Yesterday\").", required: true },
  }}
/>


### RecentActivityType

Discriminator for items in the unified recents list.


### UseRecentActivityOptions

Options for [`useRecentActivity`](#userecentactivity).

<TypeTable
  type={{
    pageSize: { type: "number", description: "Maximum entries per source. The hook fetches up to `pageSize` sessions and `pageSize` workflow executions, then merges and trims to `pageSize` total entries." },
  }}
/>

