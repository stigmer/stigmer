---
title: Composer
description: The unified message input with model selection, workspace, and attachments.
---

{/* Auto-generated by @stigmer/react docs generator. DO NOT EDIT. */}

## Hooks

### useComposer

Headless behavior hook for a self-resizing, Enter-to-submit textarea.

Encapsulates the common behavior shared across all Stigmer composer
surfaces (session launcher, follow-up input, and future variants):

- Auto-resizing textarea up to a configurable max height
- Enter to submit, Shift+Enter for newline
- Disabled state management
- Auto-focus when transitioning from disabled to enabled

Platform builders who want custom composer UI use this hook directly.
The [`SessionComposer`](#sessioncomposer) styled component uses it internally.

**Signature**

```tsx
function useComposer(options: UseComposerOptions): UseComposerReturn
```

<TypeTable
  type={{
    options: { type: "UseComposerOptions", description: "", required: true },
  }}
/>

**Returns** — `UseComposerReturn`

<TypeTable
  type={{
    canSubmit: { type: "boolean", description: "Whether the current message is non-empty and the input is not disabled." },
    clear: { type: "() => void", description: "Clear the message and reset textarea height." },
    message: { type: "string", description: "Current message text." },
    setMessage: { type: "(value: string) => void", description: "Replace the current message text." },
    submit: { type: "() => void", description: "Programmatically submit the current message. No-op if `canSubmit` is false." },
    textareaProps: { type: "{ disabled: boolean; onChange: (e: ChangeEvent<HTMLTextAreaElement>) => void; onKeyDown: (e: KeyboardEvent<HTMLTextAreaElement>) => void; ref: RefObject<HTMLTextAreaElement | null>; value: string }", description: "Spread-ready props for the `<textarea>` element.  Includes `ref`, `value`, `onChange`, `onKeyDown`, and `disabled`. Consumers can override individual props by spreading first and then specifying overrides." },
    textareaRef: { type: "RefObject<HTMLTextAreaElement | null>", description: "Ref to attach to the `<textarea>` element." },
  }}
/>

```tsx
function CustomInput({ onSend }: { onSend: (msg: string) => void }) {
  const composer = useComposer({ onSubmit: onSend });

  return (
    <div>
      <textarea {...composer.textareaProps} rows={2} />
      <button onClick={composer.submit} disabled={!composer.canSubmit}>
        Send
      </button>
    </div>
  );
}
```


## Components

### InteractionModePicker

Compact dropdown for choosing the execution interaction mode.

Renders a trigger button showing the current mode label with a
chevron, and a popover with the available options. Each option
shows a label and a short description.

Designed to scale to additional modes (e.g. Ask) without layout
changes. Uses `@base-ui/react` Popover for positioning and portal
rendering, matching the pattern used by [`ModelSelector`](/docs/sdk/react/models#modelselector).

**Props** — `InteractionModePickerProps`

<TypeTable
  type={{
    className: { type: "string", description: "Additional CSS class names for the trigger button." },
    disabled: { type: "boolean", description: "When true, disables the picker." },
    onValueChange: { type: "(mode: InteractionModeOption) => void", description: "Called when the user picks a different mode.", required: true },
    value: { type: "InteractionModeOption", description: "Currently selected interaction mode.", required: true, typeDescriptionLink: "#interactionmodeoption" },
  }}
/>

```tsx
function ComposerToolbar() {
  const [mode, setMode] = useState<InteractionModeOption>("agent");
  return <InteractionModePicker value={mode} onValueChange={setMode} />;
}
```


## Types

### InteractionModeOption

Interaction mode options for agent executions.

Maps to `InteractionMode` proto enum values (excluding UNSPECIFIED):
- `"agent"` → `INTERACTION_MODE_AGENT` (full tool access)
- `"plan"` → `INTERACTION_MODE_PLAN` (read-only analysis)


### SessionComposer


### SessionComposerHandle

Imperative handle exposed by [`SessionComposer`](#sessioncomposer) via `ref`.

Allows parent components to programmatically set the composer's
message text and focus the textarea. Used for features like
"Build from Plan" where a CTA outside the composer needs to
pre-fill and focus the input.

```tsx
const composerRef = useRef<SessionComposerHandle>(null);

function handleBuildFromPlan() {
  composerRef.current?.setMessage("Implement the plan above");
  composerRef.current?.focus();
}

<SessionComposer ref={composerRef} onSubmit={handleSubmit} />
```


### SessionComposerProps

Props for [`SessionComposer`](#sessioncomposer).

<TypeTable
  type={{
    agentRef: { type: "ResourceRef | null", description: "Currently selected agent reference, or null if none. When `onAgentRefChange` is provided, an agent trigger appears in the toolbar (single-select)." },
    ariaLabel: { type: "string", description: "ARIA label for the composer region." },
    autoApproveAll: { type: "boolean", description: "Whether tool calls are auto-approved for the next message.  When `onAutoApproveAllChange` is provided, renders an auto-approve toggle in the toolbar. Defaults to `false` (approvals required) when omitted." },
    autoFocus: { type: "boolean", description: "Auto-focus the textarea on mount." },
    className: { type: "string", description: "Additional CSS class names for the root container." },
    defaultModelId: { type: "string", description: "Initial model ID for the model selector." },
    disabled: { type: "boolean", description: "Disables the entire composer (e.g., while an execution streams)." },
    enableAttachments: { type: "boolean", description: "Enable file attachment support in the composer.  When `true`, renders an attach button in the toolbar and enables drag-and-drop file upload on the textarea. Attachments are uploaded immediately via `agentExecution.uploadAttachment()` and included in `context.attachments` on submit." },
    enableFileReferences: { type: "boolean", description: "Enable workspace file-reference support via drag-to-reference.  When `true`, dragging a file from the workspace tree into the composer creates a file-reference chip (no upload). Referenced paths are included in `context.workspaceFileRefs` on submit." },
    enableGitHub: { type: "boolean", description: "Show the GitHub Repo source button." },
    enableLocal: { type: "boolean", description: "Show the Local Folder source button." },
    gitHubConnection: { type: "UseGitHubConnectionReturn", description: "GitHub connection state for the repo picker.", typeDescriptionLink: "/docs/sdk/react/github#usegithubconnectionreturn" },
    harness: { type: "HarnessOption", description: "Currently selected execution harness.  Controls which models appear in the model selector and flows through to session creation. When omitted, defaults to `\"native\"`.", typeDescriptionLink: "/docs/sdk/react/models#harnessoption" },
    initialAgentRef: { type: "ResourceRef", description: "Agent to auto-select when the composer mounts.  When provided, the composer runs the full agent resolution flow on mount — exactly as if the user had picked this agent in the AgentPicker. If the agent requires credentials, the environment form appears automatically.  One-time: consumed on mount; subsequent changes are ignored. To change the agent after mount, use the picker or `onAgentRefChange` externally.  Requires `org` and `onAgentRefChange` to be set (same prerequisites as the agent picker)." },
    initialAttachments: { type: "File[]", description: "Files to attach programmatically when the composer mounts.  When provided, the composer uploads these files via the same attachment pipeline as user-selected files. Attachment chips appear in the UI so the user sees what is attached.  One-time: consumed when the value first becomes truthy. Subsequent changes are ignored. This allows async resource fetching — the effect waits for the files to be ready.  Requires `enableAttachments` to be `true` (default)." },
    initialInstanceId: { type: "string", description: "Pre-bind the session to a specific, already-existing `AgentInstance` when the composer mounts.  Requires `initialAgentRef` (the agent the instance deploys). When both are provided, the composer resolves the agent directly to this instance — skipping the environment-collection flow entirely, since the chosen instance already binds its own environment(s). The resulting resolution is `{ mode: \"saved\", instanceId }`, so the session is created against this exact configured deployment.  This is the agent analog of running a specific WorkflowInstance: it powers the \"Start session\" action on an instance in the Agent detail page's Instances tab.  One-time: consumed on mount; subsequent changes are ignored." },
    initialRows: { type: "number", description: "Initial number of visible rows." },
    interactionMode: { type: "InteractionModeOption", description: "Currently selected interaction mode.  When `onInteractionModeChange` is provided, renders a mode picker in the toolbar. Defaults to `\"agent\"` when omitted.", typeDescriptionLink: "#interactionmodeoption" },
    isDefaultAgent: { type: "boolean", description: "When `true`, the agent chip renders without an X (remove) button.  Used on the session page to indicate the session's default agent, where removing it would just re-select the same agent (a confusing no-op). The user can still switch agents via the Configure menu." },
    isSubmitting: { type: "boolean", description: "Shows loading indicator on the send button." },
    mcpServerUsages: { type: "McpServerUsageInput[]", description: "Currently selected MCP server usages. When `onMcpServerUsagesChange` is provided, a MCP server trigger appears in the toolbar." },
    onAgentRefChange: { type: "(ref: ResourceRef | null) => void", description: "Called when the agent selection changes. Providing this enables the agent trigger." },
    onAgentResolutionChange: { type: "(resolution: AgentResolution | null) => void", description: "Called when the agent setup flow resolves how the agent should be used for session creation.  The AgentResolution discriminated union tells the caller which session creation path to use: - `\"saved\"` — use `agentInstanceId` - `\"oneTime\"` — use `agentRef` + pass `runtimeEnv` to execution - `\"direct\"` — use `agentRef` (no secrets needed)  Set to `null` when the agent is deselected." },
    onAttachmentValidationError: { type: "(message: string) => void", description: "Called when a file is rejected (e.g., exceeds the 10 MB limit). Consumers can use this for toast notifications." },
    onAutoApproveAllChange: { type: "(autoApproveAll: boolean) => void", description: "Called when the user toggles auto-approve.  Providing this callback enables the auto-approve toggle in the toolbar." },
    onBrowseLocalFolder: { type: "() => Promise<string | null>", description: "Native folder picker callback passed through to WorkspaceEditor. When provided, clicking \"Local Folder\" opens a native dialog instead of the inline text input. See WorkspaceEditorProps.onBrowseLocalFolder." },
    onHarnessChange: { type: "(harness: HarnessOption) => void", description: "Called when the user switches the harness.  Providing this callback enables the harness selector in the toolbar." },
    onInteractionModeChange: { type: "(mode: InteractionModeOption) => void", description: "Called when the user switches the interaction mode.  Providing this callback enables the mode picker in the toolbar." },
    onMcpServerUsagesChange: { type: "(usages: McpServerUsageInput[]) => void", description: "Called when the MCP server selection changes. Providing this enables the MCP trigger." },
    onModelChange: { type: "(modelId: string) => void", description: "Called when the user changes the selected model." },
    onSkillRefsChange: { type: "(refs: ResourceRef[]) => void", description: "Called when the skill selection changes. Providing this enables the skill trigger." },
    onSubmit: { type: "(message: string, modelName?: string, context?: SessionComposerSubmitContext) => void", description: "Called when the user submits a message.  The optional `context` parameter carries aggregated runtime data collected by the composer's setup flows. When present, `context.runtimeEnv` should be passed to execution creation.", required: true },
    org: { type: "string", description: "Organization slug for agent, MCP server, and skill searches. Required when agent, MCP, or skill pickers are enabled." },
    placeholder: { type: "string", description: "Placeholder text for the textarea." },
    sessionVariables: { type: "UseSessionVariablesReturn", description: "Session variables state managed by useSessionVariables. When provided, renders a \"Session Variables\" trigger in the toolbar that opens a key-value editor for environment variables.  Variables are ephemeral by default (single execution). Individual entries can be marked `saveForFuture: true` to persist them to the user's personal environment. The consumer should call `sessionVariables.clear()` after submission.", typeDescriptionLink: "/docs/sdk/react/execution#usesessionvariablesreturn" },
    showAutoApproveToggle: { type: "boolean", description: "Show the auto-approve toggle in the toolbar." },
    showHarnessSelector: { type: "boolean", description: "Show the harness selector in the toolbar." },
    showInteractionModePicker: { type: "boolean", description: "Show the interaction mode picker in the toolbar." },
    showModelSelector: { type: "boolean", description: "Show the model selector." },
    skillRefs: { type: "ResourceRef[]", description: "Currently selected skill references. When `onSkillRefsChange` is provided, a skill trigger appears in the toolbar." },
    workspace: { type: "UseWorkspaceEntriesReturn", description: "Workspace state managed by useWorkspaceEntries. When provided, renders a workspace trigger in the toolbar that opens a popover with the workspace editor.", typeDescriptionLink: "/docs/sdk/react/workspace#useworkspaceentriesreturn" },
  }}
/>


### SessionComposerSubmitContext

Context provided to `onSubmit` at the moment of submission.

Contains aggregated one-time environment variables from all setup
flows managed by the composer (agent, MCP servers, manual secrets).
The consumer passes `context.runtimeEnv` directly to execution
creation without needing to understand the individual sources.

<TypeTable
  type={{
    attachments: { type: "AttachmentInput[]", description: "Pre-uploaded file attachments for the execution.  Each entry contains a `storageKey` obtained from `agentExecution.uploadAttachment()`. Only successfully uploaded attachments are included. Pass directly to execution creation as `attachments`.  `undefined` when no files were attached." },
    autoApproveAll: { type: "boolean", description: "Whether the user opted to auto-approve all tool calls for this execution.  `undefined` when no auto-approve toggle is shown (defaults to gated). Pass to execution creation as `auto_approve_all`." },
    interactionMode: { type: "InteractionModeOption", description: "Interaction mode selected by the user for this execution.  - `\"agent\"` (default): full tool access — read, write, create, delete. - `\"plan\"`: read-only analysis — read, search, list only.  `undefined` when no mode picker is shown (defaults to `\"agent\"`). Pass to execution creation as `execution_config.interaction_mode`.", typeDescriptionLink: "#interactionmodeoption" },
    runtimeEnv: { type: "Record<string, EnvVarInput>", description: "Aggregated one-time environment variables from all setup flows.  Merged from (in precedence order, last-write-wins): 1. Agent one-time env vars (when agent resolution mode is `\"oneTime\"`) 2. MCP server one-time env vars (collected with `saveForFuture: false`) 3. Manual session variables (from SessionVariablesInput)  `undefined` when no runtime env vars were collected from any source. Pass directly to execution creation as `runtimeEnv`." },
    workspaceFileRefs: { type: "string[]", description: "Workspace-relative file paths the user referenced via drag-to-reference.  These are lightweight \"attention\" signals — the agent reads the files directly from the workspace filesystem. No upload, no injection.  `undefined` when no file references are present. Pass to execution creation as `workspaceFileRefs`." },
  }}
/>


### UseComposerOptions

Options for [`useComposer`](#usecomposer).

<TypeTable
  type={{
    autoFocusOnEnable: { type: "boolean", description: "Auto-focus the textarea when `disabled` transitions from true to false." },
    disabled: { type: "boolean", description: "Prevents submission and disables the textarea." },
    maxHeight: { type: "number", description: "Maximum height (px) before the textarea scrolls instead of growing." },
    onSubmit: { type: "(message: string) => void", description: "Called with the trimmed message when the user submits.", required: true },
  }}
/>

