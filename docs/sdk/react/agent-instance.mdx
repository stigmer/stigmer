---
title: Agent Instance
description: Hooks for deployed agent instances bound to an environment.
---

{/* Auto-generated by @stigmer/react docs generator. DO NOT EDIT. */}

## Hooks

### useAgentInstance

Data hook that fetches a single AgentInstance by resource reference.

Pass `null` to skip fetching (stable no-op). When the reference
fields change, the previous in-flight request is discarded and a
fresh fetch begins. Call `refetch()` to re-query after mutating the
agent instance through a separate hook or API call.

Returns the full proto `AgentInstance` resource so consumers
have access to metadata, spec (including `environment_refs` and
agent reference), and status without additional calls.

This is a Layer 1 building-block hook for the **Environment Flow**
— agent instances bind Environment resources to Agent blueprints.
For the managed "personal agent instance" convenience, see
[`usePersonalAgentInstance`](#usepersonalagentinstance).

**Signature**

```tsx
function useAgentInstance(ref: ResourceRef | null): UseAgentInstanceReturn
```

<TypeTable
  type={{
    ref: { type: "ResourceRef | null", description: "", required: true },
  }}
/>

**Returns** — `UseAgentInstanceReturn`

<TypeTable
  type={{
    agentInstance: { type: "AgentInstance | null", description: "The fetched AgentInstance, or `null` while loading or on error." },
    error: { type: "Error | null", description: "Error from the last failed request, or `null` when healthy." },
    isLoading: { type: "boolean", description: "`true` while the initial fetch or a refetch is in flight." },
    isRefetching: { type: "boolean", description: "`true` while a background refetch is in flight and stale data is shown." },
    refetch: { type: "() => void", description: "Discard cached data and re-fetch the agent instance from the server." },
  }}
/>

```tsx
const { agentInstance, isLoading, error } = useAgentInstance({
  org: "acme",
  slug: "my-agent-personal",
});
```


### useAgentInstanceList

Data hook that fetches a paginated list of `AgentInstance` entries
for a given organization, with optional label filtering.

Pass `null` as `org` to skip fetching (stable no-op). When the org
or labels change, the previous in-flight request is discarded and a
fresh fetch begins. Call `refetch()` to re-query after mutations.

This is a Layer 1 building-block hook for the **Environment Flow**.
For the managed "personal agent instance" convenience, see
[`usePersonalAgentInstance`](#usepersonalagentinstance).

**Signature**

```tsx
function useAgentInstanceList(org: string | null, labels?: Record<string, string>): UseAgentInstanceListReturn
```

<TypeTable
  type={{
    org: { type: "string | null", description: "", required: true },
    labels: { type: "Record<string, string>", description: "" },
  }}
/>

**Returns** — `UseAgentInstanceListReturn`

<TypeTable
  type={{
    agentInstances: { type: "readonly AgentInstance[]", description: "The fetched list of AgentInstance entries. Empty while loading or on error." },
    error: { type: "Error | null", description: "Error from the last failed request, or `null` when healthy." },
    isLoading: { type: "boolean", description: "`true` while the initial fetch or a refetch is in flight." },
    isRefetching: { type: "boolean", description: "`true` while a background refetch is in flight and stale data is shown." },
    refetch: { type: "() => void", description: "Discard cached data and re-fetch the list from the server." },
    totalCount: { type: "number", description: "Total number of agent instances matching the query, including unpaged items." },
  }}
/>

```tsx
const { agentInstances, isLoading } = useAgentInstanceList("acme", {
  "stigmer.ai/personal": "true",
});
```


### useAgentInstances

Data hook that fetches instances for a specific agent.

Wraps `stigmer.agentInstance.getByAgent()` with loading and error
state management. Used on the Agent detail page "Instances" tab to
show environment-bound deployments of the agent blueprint.

Pass `org` to scope the list to one organization's instances — the
org-context view a console tab needs. Permissions alone cannot provide
this scope: a member of several orgs can view all of their orgs'
instances of the agent, so the unscoped list merges them. The server
applies the scope (it only ever narrows the permission-bounded view);
omit `org` for the full permission-bounded list.

Pass `null` as `agentId` to skip fetching (stable no-op).

Note: the `getByAgent` RPC returns an `AgentInstanceList` whose
entries live under `items` (unlike WorkflowInstance, which uses
`entries`).

**Signature**

```tsx
function useAgentInstances(agentId: string | null | undefined, org?: string): UseAgentInstancesReturn
```

<TypeTable
  type={{
    agentId: { type: "string | null | undefined", description: "", required: true },
    org: { type: "string", description: "" },
  }}
/>

**Returns** — `UseAgentInstancesReturn`

<TypeTable
  type={{
    error: { type: "Error | null", description: "Error from the last failed request, or `null` when healthy." },
    instances: { type: "readonly AgentInstance[]", description: "Instances for the given agent, or empty array while loading." },
    isLoading: { type: "boolean", description: "`true` while the initial fetch or a refetch is in flight." },
    isRefetching: { type: "boolean", description: "`true` while a background refetch is in flight and stale data is shown." },
    refetch: { type: "() => void", description: "Discard cached data and re-fetch instances from the server." },
  }}
/>

```tsx
const { instances, isLoading } = useAgentInstances(agent.metadata?.id, viewerOrg);
```


### useCreateAgentInstance

Behavior hook that wraps `agentInstance.create()` with
loading/error state.

Creates an AgentInstance resource — a deployed configuration of an
Agent blueprint bound to one or more Environments. The caller
provides an `AgentInstanceInput` with `name`, `org`, and
optionally `agentId`, `description`, and `environmentRefs`.

Returns the full `AgentInstance` proto including
server-generated metadata (id, version, timestamps) so callers
can immediately reference the created resource.

This is a Layer 1 building-block hook for the **Environment Flow**
— agent instances bind Environment resources to Agent blueprints.
For the managed "personal agent instance" convenience, see
[`usePersonalAgentInstance`](#usepersonalagentinstance) which composes this hook with
deterministic naming and label conventions.

**Signature**

```tsx
function useCreateAgentInstance(): UseCreateAgentInstanceReturn
```

**Returns** — `UseCreateAgentInstanceReturn`

<TypeTable
  type={{
    clearError: { type: "() => void", description: "Reset `error` to `null`." },
    create: { type: "(input: AgentInstanceInput) => Promise<AgentInstance>", description: "Submit an AgentInstanceInput to create a new AgentInstance. Resolves with the server-created resource." },
    error: { type: "Error | null", description: "Error from the last failed create, or `null` when healthy." },
    isCreating: { type: "boolean", description: "`true` while the create request is in flight." },
  }}
/>

```tsx
const { create, isCreating, error } = useCreateAgentInstance();

const instance = await create({
  name: "github-bot-prod",
  org: "acme",
  agentId: "agent-abc123",
  description: "Production GitHub bot",
  environmentRefs: [
    { org: "acme", slug: "prod-credentials" },
  ],
});
```


### useDeleteAgentInstance

Mutation hook that deletes an AgentInstance resource.

Wraps `stigmer.agentInstance.delete()` with loading/error state.
The caller is responsible for handling post-delete UI updates
(e.g., refreshing the instance list, closing a detail panel).

Unlike WorkflowInstance, deletion does **not** cascade to runtime
resources: sessions previously started against the instance and
their execution history remain intact. The confirmation UI copy
should reflect this.

**Signature**

```tsx
function useDeleteAgentInstance(): UseDeleteAgentInstanceReturn
```

**Returns** — `UseDeleteAgentInstanceReturn`

<TypeTable
  type={{
    clearError: { type: "() => void", description: "Reset `error` to `null`." },
    deleteInstance: { type: "(id: string) => Promise<AgentInstance>", description: "Delete an agent instance by ID. Returns the deleted resource.  Deleting an instance removes its configuration and environment bindings. Sessions already started against it (and their execution history) are preserved — deletion does not cascade to sessions." },
    error: { type: "Error | null", description: "Error from the last failed delete, or `null` when healthy." },
    isDeleting: { type: "boolean", description: "`true` while the delete request is in flight." },
  }}
/>

```tsx
const { deleteInstance, isDeleting } = useDeleteAgentInstance();

const handleDelete = async () => {
  await deleteInstance(instance.metadata.id);
  refetch(); // refresh list after deletion
};
```


### usePersonalAgentInstance

Layer 2 orchestration hook that manages the caller's personal
`AgentInstance` for a given organization, optionally scoped
to a specific agent.

Encapsulates the "personal agent instance" convention: unique slug
per user, labels (`stigmer.ai/personal`, `stigmer.ai/for-agent`),
and the get-or-create lifecycle. Composes [`useAgentInstanceList`](#useagentinstancelist)
for declarative reading and the SDK client directly for mutations.

Pass `null` as `org` to skip all operations (stable no-op).
The `agentId` parameter is optional for read-only use (listing all
personal instances) but required for `getOrCreate`.

This is a Layer 2 **Environment Flow** hook. It provides the managed
"personal agent instance" experience used by the Stigmer Console.
Callers who pre-provision agent instances should use the Layer 1
building-block hook [`useCreateAgentInstance`](#usecreateagentinstance) directly.

**Signature**

```tsx
function usePersonalAgentInstance(org: string | null, agentId?: string): UsePersonalAgentInstanceReturn
```

<TypeTable
  type={{
    org: { type: "string | null", description: "", required: true },
    agentId: { type: "string", description: "" },
  }}
/>

**Returns** — `UsePersonalAgentInstanceReturn`

<TypeTable
  type={{
    agentInstance: { type: "AgentInstance | null", description: "The caller's personal agent instance, or `null` if not yet created or still loading." },
    error: { type: "Error | null", description: "Error from the most recent failed operation (fetch or mutation), or `null`." },
    getOrCreate: { type: "(input: GetOrCreatePersonalInstanceInput) => Promise<AgentInstance>", description: "Ensure a personal agent instance exists for this org and agent.  If one already exists (matched by `agentId`), returns it immediately without a network call. Otherwise, creates a new instance with: - Name: `\"{agentSlug} Personal\"` (display-friendly, may be duplicate) - Slug: `\"{agentSlug}-personal-{random}\"` (unique per org) - Labels: `stigmer.ai/personal: \"true\"`, `stigmer.ai/for-agent: \"{org}/{agentSlug}\"` - Agent binding: the `agentId` passed to the hook - Environment linkage: `[personalEnvironmentRef]`  Requires `agentId` to be set on the hook. Throws a descriptive error if called without it." },
    isLoading: { type: "boolean", description: "`true` while the initial list query is in-flight." },
    isMutating: { type: "boolean", description: "`true` while a `getOrCreate` mutation is in-flight." },
    refetch: { type: "() => void", description: "Re-query personal agent instances from the server." },
  }}
/>

```tsx
// Read-only: check if a personal instance exists for an agent
const { agentInstance, isLoading } = usePersonalAgentInstance(
  "acme",
  agent.metadata.id,
);

// Get-or-create: ensure a personal instance exists
const { getOrCreate, isMutating } = usePersonalAgentInstance(
  "acme",
  agent.metadata.id,
);

const instance = await getOrCreate({
  agentSlug: "my-github-bot",
  personalEnvironmentRef: { org: "acme", slug: "env-personal-a1b2c3d4" },
});
```


### useUpdateAgentInstance

Mutation hook that updates an existing AgentInstance resource.

Wraps `stigmer.agentInstance.update()` with loading/error state.
The caller is responsible for refreshing the instance after a
successful update (e.g., via `refetch` from `useAgentInstances` or
`useAgentInstance`).

**Signature**

```tsx
function useUpdateAgentInstance(): UseUpdateAgentInstanceReturn
```

**Returns** — `UseUpdateAgentInstanceReturn`

<TypeTable
  type={{
    clearError: { type: "() => void", description: "Clear the error state." },
    error: { type: "Error | null", description: "Error from the last failed update, or `null` when healthy." },
    isUpdating: { type: "boolean", description: "`true` while the update RPC is in flight." },
    update: { type: "(input: AgentInstanceInput) => Promise<AgentInstance>", description: "Update an existing agent instance with a full input. Returns the updated resource." },
  }}
/>

```tsx
const { update, isUpdating } = useUpdateAgentInstance();

const handleSave = async () => {
  await update({
    ...toAgentInstanceUpdateInput(instance),
    environmentRefs: newRefs,
  });
  refetch();
};
```


## Components

### AgentInstanceDetailPanel

Expandable detail panel for a single AgentInstance.

Shows metadata, description, environment bindings (editable), visibility
control, share panel (FGA-gated), and a delete action. The agent analog
of `WorkflowInstanceDetailPanel`; deletion does not cascade to sessions,
so the delete copy reflects that.

**Props** — `AgentInstanceDetailPanelProps`

<TypeTable
  type={{
    instance: { type: "AgentInstance", description: "The instance to display.", required: true, typeDescriptionLink: "/docs/sdk/resources/agent-instance" },
    onClose: { type: "() => void", description: "Called when the panel should close.", required: true },
    onDeleted: { type: "() => void", description: "Called after successful deletion." },
    onStartSessionClick: { type: "(instance: AgentInstance) => void", description: "Called when the user clicks \"Start session\" for this instance." },
    onUpdated: { type: "() => void", description: "Called when instance data changes (for parent refetch)." },
    org: { type: "string", description: "Organization slug.", required: true },
  }}
/>


### AgentInstanceEmptyState

Empty state shown in the Instances tab when no user-created instances exist.

Communicates the value proposition of agent instances without mentioning
the platform-managed default instance (an implementation detail). For
agents, an instance is a reusable, environment-bound deployment you can
start sessions against — and share with your team.

A thin composition over the shared `EmptyState` primitive: only the
icon and copy are specific to agent instances.

**Props** — `AgentInstanceEmptyStateProps`

<TypeTable
  type={{
    className: { type: "string", description: "Additional CSS class names." },
    onCreateClick: { type: "() => void", description: "Called when the user clicks \"Create instance\"." },
  }}
/>


### AgentInstanceList

Agent instance list with environment badges, visibility controls, and
action buttons. Filters out the platform-managed default instance and
marks auto-managed personal instances with a badge.

The agent analog of `WorkflowInstanceList`: because agents run through
Sessions, the primary row verb is **Start session** (bound to the chosen
instance) rather than "Run".

This is an SDK component (DD-001) — embeddable by platform builders.

**Props** — `AgentInstanceListProps`

<TypeTable
  type={{
    agentId: { type: "string", description: "Agent resource ID to list instances for.", required: true },
    className: { type: "string", description: "Additional CSS class names." },
    defaultInstanceId: { type: "string", description: "The default instance ID (from agent.status.defaultInstanceId) — filtered out of the list." },
    onCreateClick: { type: "() => void", description: "Called when the user wants to create a new instance." },
    onDeleteClick: { type: "(instance: AgentInstance) => void", description: "Called when the user clicks \"Delete\" on an instance." },
    onInstanceClick: { type: "(instance: AgentInstance) => void", description: "Called when the user clicks an instance row (opens detail)." },
    onStartSessionClick: { type: "(instance: AgentInstance) => void", description: "Called when the user clicks \"Start session\" on an instance." },
    org: { type: "string", description: "The AGENT's organization slug (needed for environment resolution and the create flow). Not the list scope — that is `viewerOrg`, which differs from `org` when viewing another org's agent.", required: true },
    refreshKey: { type: "number", description: "Increment this value to trigger a refetch of the instance list. Useful after creating or deleting an instance externally." },
    viewerOrg: { type: "string", description: "The viewer's active organization slug. Scopes the list to this org's instances of the agent, so a member of several orgs sees exactly the current org context's instances. Omit to default to the agent's own org." },
  }}
/>


### CreateAgentInstanceDialog

Modal dialog for creating a new AgentInstance.

Collects: name (required), description, environment bindings (ordered),
and visibility. Submits via `useCreateAgentInstance`.

Uses native `<dialog>` element for proper focus trapping and escape handling.

**Props** — `CreateAgentInstanceDialogProps`

<TypeTable
  type={{
    agentId: { type: "string", description: "Agent ID to bind the new instance to.", required: true },
    onCreated: { type: "(instance: AgentInstance) => void", description: "Called after the instance is successfully created." },
    onOpenChange: { type: "(open: boolean) => void", description: "Called when the dialog should open or close.", required: true },
    open: { type: "boolean", description: "Whether the dialog is open.", required: true },
    org: { type: "string", description: "Organization slug.", required: true },
  }}
/>


## Types

### GetOrCreatePersonalInstanceInput

Input for creating a personal agent instance via
`UsePersonalAgentInstanceReturn.getOrCreate`.

The hook already knows `org` (from its first parameter) and
`agentId` (from its second parameter). This input supplies the
additional context needed only at creation time.

<TypeTable
  type={{
    agentSlug: { type: "string", description: "The agent's slug, used to derive the instance display name and unique slug, and the `stigmer.ai/for-agent` label.", required: true },
    personalEnvironmentRef: { type: "ResourceRef", description: "Reference to the caller's personal environment. The created instance will link to this environment via `environmentRefs`.", required: true },
  }}
/>

