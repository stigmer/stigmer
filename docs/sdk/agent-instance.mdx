---
title: Agent Instance
description: "SDK reference for Agent Instance resources."
---

{/* Auto-generated by stigmer-codegen. DO NOT EDIT. */}

This is the "Instance" layer - stateful configuration with secrets.

## Client Access

<SDKTabs>
<Tab value="TypeScript">
```typescript
const agentInstance = await stigmer.agentInstance.get("ain_01j5q3k7m8r2s4tnz2hfp0q0c3");
```
</Tab>
<Tab value="Go">
```go
agentInstance, err := client.AgentInstance.Get(ctx, "ain_01j5q3k7m8r2s4tnz2hfp0q0c3")
```
</Tab>
<Tab value="Python">
```python
agentInstance = client.agent_instance.get("ain_01j5q3k7m8r2s4tnz2hfp0q0c3")
```
</Tab>
<Tab value="Java">
```java
AgentInstance agentInstance = client.agentInstance().get("ain_01j5q3k7m8r2s4tnz2hfp0q0c3");
```
</Tab>
</SDKTabs>

## Methods

| Method | Description |
|--------|-------------|
| [`get`](#get) | Get a single agent instance by ID. |
| [`getByAgent`](#getbyagent) | Get all instances of a specific agent template. |
| [`getByReference`](#getbyreference) | Get an agent instance by its organization-scoped reference (org/slug). |
| [`list`](#list) | List agent instances with optional label filtering. |
| [`apply`](#apply) | Create or update an agent instance. |
| [`create`](#create) | Create a new agent instance with full state. |
| [`update`](#update) | Update an instance with full state. |
| [`delete`](#delete) | Delete an agent instance. |

### get

Get a single agent instance by ID.

<SDKTabs>
<Tab value="TypeScript">
```typescript
const agentInstance = await stigmer.agentInstance.get(id);
```
</Tab>
<Tab value="Go">
```go
agentInstance, err := client.AgentInstance.Get(ctx, id)
```
</Tab>
<Tab value="Python">
```python
agentInstance = client.agent_instance.get(id)
```
</Tab>
<Tab value="Java">
```java
AgentInstance agentInstance = client.agentInstance().get(id);
```
</Tab>
</SDKTabs>

**Parameters**

| Parameter | Type | Description |
|-----------|------|-------------|
| `id` | `string` | Agent Instance identifier. |

**Returns**: [`AgentInstance`](#agentinstance)

### getByAgent

Get all instances of a specific agent template. Returns only instances the caller has access to.

<SDKTabs>
<Tab value="TypeScript">
```typescript
const result = await stigmer.agentInstance.getByAgent({
  agentId: "...",
  pageInfo: {},
});
```
</Tab>
<Tab value="Go">
```go
result, err := client.AgentInstance.GetByAgent(ctx, &stigmer.GetAgentInstancesByAgentRequest{
  AgentId:  "...",
  PageInfo: &stigmer.PageInfoInput{},
})
```
</Tab>
<Tab value="Python">
```python
result = client.agent_instance.get_by_agent(GetAgentInstancesByAgentRequest(
    agent_id="...",
    page_info=None,
))
```
</Tab>
<Tab value="Java">
```java
AgentInstanceList result = client.agentInstance().getByAgent(GetAgentInstancesByAgentRequest.builder()
    .agentId("...")
    .pageInfo(null)
    .build());
```
</Tab>
</SDKTabs>

**Parameters**: [`GetAgentInstancesByAgentRequest`](#getagentinstancesbyagentrequest)

**Returns**: [`AgentInstanceList`](#agentinstancelist)

### getByReference

Get an agent instance by its organization-scoped reference (org/slug).

<SDKTabs>
<Tab value="TypeScript">
```typescript
const agentInstance = await stigmer.agentInstance.getByReference({
  org: "...",
  kind: "...",
  slug: "...",
  version: "...",
});
```
</Tab>
<Tab value="Go">
```go
agentInstance, err := client.AgentInstance.GetByReference(ctx, &stigmer.ApiResourceReference{
  Org:     "...",
  Kind:    "...",
  Slug:    "...",
  Version: "...",
})
```
</Tab>
<Tab value="Python">
```python
agentInstance = client.agent_instance.get_by_reference(ApiResourceReference(
    org="...",
    kind="...",
    slug="...",
    version="...",
))
```
</Tab>
<Tab value="Java">
```java
AgentInstance agentInstance = client.agentInstance().getByReference(ApiResourceReference.builder()
    .org("...")
    .kind("...")
    .slug("...")
    .version("...")
    .build());
```
</Tab>
</SDKTabs>

**Parameters**: [`ResourceRef`](#resourceref)

**Returns**: [`AgentInstance`](#agentinstance)

### list

List agent instances with optional label filtering.

<SDKTabs>
<Tab value="TypeScript">
```typescript
const result = await stigmer.agentInstance.list({
  org: "...",
  labels: {},
  pageInfo: {},
});
```
</Tab>
<Tab value="Go">
```go
result, err := client.AgentInstance.List(ctx, &stigmer.ListAgentInstancesRequest{
  Org:      "...",
  Labels:   map[string]string{},
  PageInfo: &stigmer.PageInfoInput{},
})
```
</Tab>
<Tab value="Python">
```python
result = client.agent_instance.list(ListAgentInstancesRequest(
    org="...",
    labels={},
    page_info=None,
))
```
</Tab>
<Tab value="Java">
```java
AgentInstanceList result = client.agentInstance().list(ListAgentInstancesRequest.builder()
    .org("...")
    .labels(Map.of())
    .pageInfo(null)
    .build());
```
</Tab>
</SDKTabs>

**Parameters**: [`ListAgentInstancesRequest`](#listagentinstancesrequest)

**Returns**: [`AgentInstanceList`](#agentinstancelist)

### apply

Create or update an agent instance. The authorization and state-operation are determined depending on whether the agent instance is going to be created or updated which is determined as part of the request execution.

<SDKTabs>
<Tab value="TypeScript">
```typescript
const agentInstance = await stigmer.agentInstance.apply({
  name: "my-agent-instance",
  org: "acme",
  slug: "...",
  labels: {},
  agentId: "...",
  description: "...",
  environmentRefs: [],
});
```
</Tab>
<Tab value="Go">
```go
agentInstance, err := client.AgentInstance.Apply(ctx, &stigmer.AgentInstanceInput{
  Name:            "my-agent-instance",
  Org:             "acme",
  Slug:            "...",
  Labels:          map[string]string{},
  AgentId:         "...",
  Description:     "...",
  EnvironmentRefs: []stigmer.ResourceRef{},
})
```
</Tab>
<Tab value="Python">
```python
agentInstance = client.agent_instance.apply(AgentInstanceInput(
    name="my-agent-instance",
    org="acme",
    slug="...",
    labels={},
    agent_id="...",
    description="...",
    environment_refs=[],
))
```
</Tab>
<Tab value="Java">
```java
AgentInstance agentInstance = client.agentInstance().apply(AgentInstanceInput.builder()
    .name("my-agent-instance")
    .org("acme")
    .slug("...")
    .labels(Map.of())
    .agentId("...")
    .description("...")
    .environmentRefs(List.of())
    .build());
```
</Tab>
</SDKTabs>

**Parameters**: [`AgentInstanceInput`](#agentinstanceinput)

**Returns**: [`AgentInstance`](#agentinstance)

### create

Create a new agent instance with full state. Provide organization_id in metadata.org, and complete spec with configuration and secrets.

Public agents allow any authenticated user to create instances (cross-org allowed). Private agents restrict instance creation to org members and the agent owner.

<SDKTabs>
<Tab value="TypeScript">
```typescript
const agentInstance = await stigmer.agentInstance.create({
  name: "my-agent-instance",
  org: "acme",
  slug: "...",
  labels: {},
  agentId: "...",
  description: "...",
  environmentRefs: [],
});
```
</Tab>
<Tab value="Go">
```go
agentInstance, err := client.AgentInstance.Create(ctx, &stigmer.AgentInstanceInput{
  Name:            "my-agent-instance",
  Org:             "acme",
  Slug:            "...",
  Labels:          map[string]string{},
  AgentId:         "...",
  Description:     "...",
  EnvironmentRefs: []stigmer.ResourceRef{},
})
```
</Tab>
<Tab value="Python">
```python
agentInstance = client.agent_instance.create(AgentInstanceInput(
    name="my-agent-instance",
    org="acme",
    slug="...",
    labels={},
    agent_id="...",
    description="...",
    environment_refs=[],
))
```
</Tab>
<Tab value="Java">
```java
AgentInstance agentInstance = client.agentInstance().create(AgentInstanceInput.builder()
    .name("my-agent-instance")
    .org("acme")
    .slug("...")
    .labels(Map.of())
    .agentId("...")
    .description("...")
    .environmentRefs(List.of())
    .build());
```
</Tab>
</SDKTabs>

**Parameters**: [`AgentInstanceInput`](#agentinstanceinput)

**Returns**: [`AgentInstance`](#agentinstance)

### update

Update an instance with full state. Used to update entire instance configuration including metadata, spec, and secrets. No individual field updates - always provide complete state. Authorization: Only owner can update (can_edit permission)

<SDKTabs>
<Tab value="TypeScript">
```typescript
const agentInstance = await stigmer.agentInstance.update({
  name: "my-agent-instance",
  org: "acme",
  slug: "...",
  labels: {},
  agentId: "...",
  description: "...",
  environmentRefs: [],
});
```
</Tab>
<Tab value="Go">
```go
agentInstance, err := client.AgentInstance.Update(ctx, &stigmer.AgentInstanceInput{
  Name:            "my-agent-instance",
  Org:             "acme",
  Slug:            "...",
  Labels:          map[string]string{},
  AgentId:         "...",
  Description:     "...",
  EnvironmentRefs: []stigmer.ResourceRef{},
})
```
</Tab>
<Tab value="Python">
```python
agentInstance = client.agent_instance.update(AgentInstanceInput(
    name="my-agent-instance",
    org="acme",
    slug="...",
    labels={},
    agent_id="...",
    description="...",
    environment_refs=[],
))
```
</Tab>
<Tab value="Java">
```java
AgentInstance agentInstance = client.agentInstance().update(AgentInstanceInput.builder()
    .name("my-agent-instance")
    .org("acme")
    .slug("...")
    .labels(Map.of())
    .agentId("...")
    .description("...")
    .environmentRefs(List.of())
    .build());
```
</Tab>
</SDKTabs>

**Parameters**: [`AgentInstanceInput`](#agentinstanceinput)

**Returns**: [`AgentInstance`](#agentinstance)

### delete

Delete an agent instance. Authorization: Only owner can delete (can_delete permission)

<SDKTabs>
<Tab value="TypeScript">
```typescript
const agentInstance = await stigmer.agentInstance.delete(id);
```
</Tab>
<Tab value="Go">
```go
agentInstance, err := client.AgentInstance.Delete(ctx, id)
```
</Tab>
<Tab value="Python">
```python
agentInstance = client.agent_instance.delete(id)
```
</Tab>
<Tab value="Java">
```java
AgentInstance agentInstance = client.agentInstance().delete(id);
```
</Tab>
</SDKTabs>

**Parameters**

| Parameter | Type | Description |
|-----------|------|-------------|
| `id` | `string` | Agent Instance identifier. |

**Returns**: [`AgentInstance`](#agentinstance)

## Types

### AgentInstanceInput

Input for creating or updating an Agent Instance.

<TypeTable
  type={{
    name: { type: "string", description: "Resource name.", required: true },
    slug: { type: "string", description: "URL-friendly identifier." },
    org: { type: "string", description: "Organization slug.", required: true },
    labels: { type: "Record<string, string>", description: "Key-value labels." },
    agentId: { type: "string", description: "Reference to the Agent template this instance deploys." },
    description: { type: "string", description: "Human-readable description for this instance." },
    environmentRefs: { type: "ResourceRef[]", description: "References to Environment resources (can be multiple).", typeDescriptionLink: "#resourceref" },
  }}
/>

### ResourceRef

Generic reference to any API resource by org and slug.

<TypeTable
  type={{
    org: { type: "string", description: "Organization that owns the referenced resource." },
    kind: { type: "ApiResourceKind", description: "Kind of the referenced resource (e.g., SKILL, AGENT, MCP_SERVER).", typeDescriptionLink: "/docs/sdk/commons#apiresourcekind" },
    slug: { type: "string", description: "Resource slug (user-friendly identifier, unique within org).", required: true },
    version: { type: "string", description: "Version of the resource (optional, only applicable to versioned resources like Skills)." },
  }}
/>

### GetAgentInstancesByAgentRequest

GetAgentInstancesByAgentRequest gets all instances of a specific agent template.

<TypeTable
  type={{
    agentId: { type: "string", description: "Agent ID to filter by (required).", required: true },
    pageInfo: { type: "PageInfoInput", description: "Pagination options." },
  }}
/>

### AgentInstanceList

AgentInstanceList represents a list of agent instances.

<TypeTable
  type={{
    totalCount: { type: "number", description: "Total count of instances." },
    items: { type: "AgentInstanceInput[]", description: "List of instances.", typeDescriptionLink: "#agentinstanceinput" },
  }}
/>

### ListAgentInstancesRequest

ListAgentInstancesRequest specifies parameters for listing agent instances.

<TypeTable
  type={{
    org: { type: "string", description: "Organization to list agent instances for (required)." },
    labels: { type: "Record<string, string>", description: "Filter by metadata labels (optional)." },
    pageInfo: { type: "PageInfoInput", description: "Pagination options (optional)." },
  }}
/>

### AgentInstance

AgentInstance represents a configured deployment of an Agent template.

<TypeTable
  type={{
    apiVersion: { type: "string", description: "API version for this resource type." },
    kind: { type: "ApiResourceKind", description: "Resource kind identifier.", typeDescriptionLink: "/docs/sdk/commons#apiresourcekind" },
    metadata: { type: "ApiResourceMetadata", description: "Resource metadata including id, name, org, slug, labels, visibility, and timestamps.", typeDescriptionLink: "/docs/sdk/commons#apiresourcemetadata" },
    spec: { type: "AgentInstanceInput", description: "Resource specification. See [`AgentInstanceInput`](#agentinstanceinput).", typeDescriptionLink: "#agentinstanceinput" },
    status: { type: "AgentInstanceStatus", description: "System-managed state. See `AgentInstanceStatus`." },
  }}
/>

