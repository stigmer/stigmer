---
title: Project
description: "SDK reference for Project resources."
---

{/* Auto-generated by stigmer-codegen. DO NOT EDIT. */}

A project is a lightweight membership tracker that groups related resources (agents, workflows, MCP servers, skills) under a single unit of management. It supports two tracks:

Declarative Track (entry_point absent): The CLI scans the project directory for YAML resource files, applies each individually, and sends the resulting references as members. No SDK needed.

SDK Track (entry_point set): The CLI executes the entry_point file to synthesize resources, applies each individually, and sends the resulting references as members. The runtime is inferred from the entry_point file extension: .go  → Go SDK    (go run &lt;entry_point&gt;) .py  → Python SDK (python &lt;entry_point&gt;) .ts  → Node SDK   (npx ts-node &lt;entry_point&gt;) .js  → Node SDK   (node &lt;entry_point&gt;)

In both tracks, individual resources are applied first via their own RPCs (e.g., AgentCommandController.Apply). The project receives only references to the already-applied resources. Orphan pruning is a set-difference: resources in the previous members list but absent from the current list are candidates for deletion.

## Client Access

<SDKTabs>
<Tab value="TypeScript">
```typescript
const project = await stigmer.project.get("prj_01j5q3k7m8r2s4tnz2hfp0q0c3");
```
</Tab>
<Tab value="Go">
```go
project, err := client.Project.Get(ctx, "prj_01j5q3k7m8r2s4tnz2hfp0q0c3")
```
</Tab>
<Tab value="Python">
```python
project = client.project.get("prj_01j5q3k7m8r2s4tnz2hfp0q0c3")
```
</Tab>
<Tab value="Java">
```java
Project project = client.project().get("prj_01j5q3k7m8r2s4tnz2hfp0q0c3");
```
</Tab>
</SDKTabs>

## Methods

| Method | Description |
|--------|-------------|
| [`get`](#get) | Get a single project by ID. |
| [`getByReference`](#getbyreference) | Get a project by reference (org/name). |
| [`apply`](#apply) | Create or update a project. |
| [`create`](#create) | Create a new project. |
| [`update`](#update) | Update an existing project. |
| [`delete`](#delete) | Delete a project. |

### get

Get a single project by ID.

<SDKTabs>
<Tab value="TypeScript">
```typescript
const project = await stigmer.project.get(id);
```
</Tab>
<Tab value="Go">
```go
project, err := client.Project.Get(ctx, id)
```
</Tab>
<Tab value="Python">
```python
project = client.project.get(id)
```
</Tab>
<Tab value="Java">
```java
Project project = client.project().get(id);
```
</Tab>
</SDKTabs>

**Parameters**

| Parameter | Type | Description |
|-----------|------|-------------|
| `id` | `string` | Project identifier. |

**Returns**: [`Project`](#project)

### getByReference

Get a project by reference (org/name).

<SDKTabs>
<Tab value="TypeScript">
```typescript
const project = await stigmer.project.getByReference({
  org: "...",
  kind: "...",
  slug: "...",
  version: "...",
});
```
</Tab>
<Tab value="Go">
```go
project, err := client.Project.GetByReference(ctx, &stigmer.ApiResourceReference{
  Org:     "...",
  Kind:    "...",
  Slug:    "...",
  Version: "...",
})
```
</Tab>
<Tab value="Python">
```python
project = client.project.get_by_reference(ApiResourceReference(
    org="...",
    kind="...",
    slug="...",
    version="...",
))
```
</Tab>
<Tab value="Java">
```java
Project project = client.project().getByReference(ApiResourceReference.builder()
    .org("...")
    .kind("...")
    .slug("...")
    .version("...")
    .build());
```
</Tab>
</SDKTabs>

**Parameters**: [`ApiResourceReference`](#apiresourcereference)

**Returns**: [`Project`](#project)

### apply

Create or update a project. The authorization and state-operation are determined depending on whether the project is going to be created or updated which is determined as part of the request execution.

<SDKTabs>
<Tab value="TypeScript">
```typescript
const project = await stigmer.project.apply({
  name: "my-project",
  org: "acme",
  slug: "...",
  labels: {},
  entryPoint: "...",
  description: "...",
  members: [],
});
```
</Tab>
<Tab value="Go">
```go
project, err := client.Project.Apply(ctx, &stigmer.ProjectInput{
  Name:        "my-project",
  Org:         "acme",
  Slug:        "...",
  Labels:      map[string]string{},
  EntryPoint:  "...",
  Description: "...",
  Members:     []stigmer.ResourceRef{},
})
```
</Tab>
<Tab value="Python">
```python
project = client.project.apply(ProjectInput(
    name="my-project",
    org="acme",
    slug="...",
    labels={},
    entry_point="...",
    description="...",
    members=[],
))
```
</Tab>
<Tab value="Java">
```java
Project project = client.project().apply(ProjectInput.builder()
    .name("my-project")
    .org("acme")
    .slug("...")
    .labels(Map.of())
    .entryPoint("...")
    .description("...")
    .members(List.of())
    .build());
```
</Tab>
</SDKTabs>

**Parameters**: [`ProjectInput`](#projectinput)

**Returns**: [`Project`](#project)

### create

Create a new project.

Authorization: - Organization-scoped projects: Caller must have can_create_project permission in the organization - Platform-scoped projects: Caller must be a platform operator (handled automatically by common auth step)

<SDKTabs>
<Tab value="TypeScript">
```typescript
const project = await stigmer.project.create({
  name: "my-project",
  org: "acme",
  slug: "...",
  labels: {},
  entryPoint: "...",
  description: "...",
  members: [],
});
```
</Tab>
<Tab value="Go">
```go
project, err := client.Project.Create(ctx, &stigmer.ProjectInput{
  Name:        "my-project",
  Org:         "acme",
  Slug:        "...",
  Labels:      map[string]string{},
  EntryPoint:  "...",
  Description: "...",
  Members:     []stigmer.ResourceRef{},
})
```
</Tab>
<Tab value="Python">
```python
project = client.project.create(ProjectInput(
    name="my-project",
    org="acme",
    slug="...",
    labels={},
    entry_point="...",
    description="...",
    members=[],
))
```
</Tab>
<Tab value="Java">
```java
Project project = client.project().create(ProjectInput.builder()
    .name("my-project")
    .org("acme")
    .slug("...")
    .labels(Map.of())
    .entryPoint("...")
    .description("...")
    .members(List.of())
    .build());
```
</Tab>
</SDKTabs>

**Parameters**: [`ProjectInput`](#projectinput)

**Returns**: [`Project`](#project)

### update

Update an existing project.

<SDKTabs>
<Tab value="TypeScript">
```typescript
const project = await stigmer.project.update({
  name: "my-project",
  org: "acme",
  slug: "...",
  labels: {},
  entryPoint: "...",
  description: "...",
  members: [],
});
```
</Tab>
<Tab value="Go">
```go
project, err := client.Project.Update(ctx, &stigmer.ProjectInput{
  Name:        "my-project",
  Org:         "acme",
  Slug:        "...",
  Labels:      map[string]string{},
  EntryPoint:  "...",
  Description: "...",
  Members:     []stigmer.ResourceRef{},
})
```
</Tab>
<Tab value="Python">
```python
project = client.project.update(ProjectInput(
    name="my-project",
    org="acme",
    slug="...",
    labels={},
    entry_point="...",
    description="...",
    members=[],
))
```
</Tab>
<Tab value="Java">
```java
Project project = client.project().update(ProjectInput.builder()
    .name("my-project")
    .org("acme")
    .slug("...")
    .labels(Map.of())
    .entryPoint("...")
    .description("...")
    .members(List.of())
    .build());
```
</Tab>
</SDKTabs>

**Parameters**: [`ProjectInput`](#projectinput)

**Returns**: [`Project`](#project)

### delete

Delete a project.

<SDKTabs>
<Tab value="TypeScript">
```typescript
const project = await stigmer.project.delete(id);
```
</Tab>
<Tab value="Go">
```go
project, err := client.Project.Delete(ctx, id)
```
</Tab>
<Tab value="Python">
```python
project = client.project.delete(id)
```
</Tab>
<Tab value="Java">
```java
Project project = client.project().delete(id);
```
</Tab>
</SDKTabs>

**Parameters**

| Parameter | Type | Description |
|-----------|------|-------------|
| `id` | `string` | Project identifier. |

**Returns**: [`Project`](#project)

## Types

### ProjectInput

Input for creating or updating a Project.

<TypeTable
  type={{
    name: { type: "string", description: "Resource name.", required: true },
    slug: { type: "string", description: "URL-friendly identifier." },
    org: { type: "string", description: "Organization slug.", required: true },
    labels: { type: "Record<string, string>", description: "Key-value labels." },
    entryPoint: { type: "string", description: "Entry point file for SDK synthesis." },
    description: { type: "string", description: "Human-readable description of the project." },
    members: { type: "ResourceRef[]", description: "References to resources managed by this project.", typeDescriptionLink: "#resourceref" },
  }}
/>

### ResourceRef

Generic reference to any API resource by org and slug.

<TypeTable
  type={{
    org: { type: "string", description: "Organization that owns the referenced resource." },
    kind: { type: "ApiResourceKind", description: "Kind of the referenced resource (e.g., SKILL, AGENT, MCP_SERVER).", typeDescriptionLink: "/docs/sdk/commons#apiresourcekind" },
    slug: { type: "string", description: "Resource slug (user-friendly identifier, unique within org).", required: true },
    version: { type: "string", description: "Version of the resource (optional, only applicable to versioned resources like Skills)." },
  }}
/>

### Project

Project is a lightweight membership tracker that groups related resources under a single unit of management with automatic orphan pruning.

<TypeTable
  type={{
    apiVersion: { type: "string", description: "API version for this resource type." },
    kind: { type: "ApiResourceKind", description: "Resource kind identifier.", typeDescriptionLink: "/docs/sdk/commons#apiresourcekind" },
    metadata: { type: "ApiResourceMetadata", description: "Resource metadata including id, name, org, slug, labels, visibility, and timestamps.", typeDescriptionLink: "/docs/sdk/commons#apiresourcemetadata" },
    spec: { type: "ProjectInput", description: "Resource specification. See [`ProjectInput`](#projectinput).", typeDescriptionLink: "#projectinput" },
    status: { type: "ProjectStatus", description: "System-managed state. See [`ProjectStatus`](#projectstatus).", typeDescriptionLink: "#projectstatus" },
  }}
/>

### ProjectStatus

ProjectStatus contains system-managed state for a Project.

<TypeTable
  type={{
    lastReconciliation: { type: "ReconciliationSummary", description: "Reconciliation summary from the most recent Apply operation.", typeDescriptionLink: "#reconciliationsummary" },
    audit: { type: "ApiResourceAudit", description: "Standard audit information (created_at, updated_at, etc.) The updated_at field indicates when the project was last successfully applied.", typeDescriptionLink: "/docs/sdk/commons#apiresourceaudit" },
  }}
/>

### ReconciliationSummary

ReconciliationSummary contains the results of project reconciliation.

<TypeTable
  type={{
    created: { type: "ApiResourceReference[]", description: "Resources created on the server during this project apply.", typeDescriptionLink: "#apiresourcereference" },
    updated: { type: "ApiResourceReference[]", description: "Resources updated on the server during this project apply.", typeDescriptionLink: "#apiresourcereference" },
    deleted: { type: "ApiResourceReference[]", description: "Resources deleted via orphan pruning during this project apply.", typeDescriptionLink: "#apiresourcereference" },
  }}
/>

### ApiResourceReference

Generic reference to any API resource by org and slug.

<TypeTable
  type={{
    org: { type: "string", description: "Organization that owns the referenced resource." },
    kind: { type: "ApiResourceKind", description: "Kind of the referenced resource (e.g., SKILL, AGENT, MCP_SERVER).", typeDescriptionLink: "/docs/sdk/commons#apiresourcekind" },
    slug: { type: "string", description: "Resource slug (user-friendly identifier, unique within org).", required: true },
    version: { type: "string", description: "Version of the resource (optional, only applicable to versioned resources like Skills)." },
  }}
/>

