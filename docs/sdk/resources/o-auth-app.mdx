---
title: O Auth App
---

{/* Auto-generated by stigmer-codegen. DO NOT EDIT. */}

An OAuthApp registers your OAuth client credentials with an external vendor
(Slack, GitHub, Figma, etc.) so Stigmer can acquire access tokens on behalf of
users. MCP Servers reference an OAuthApp via `spec.auth.oauth_app_ref` to
enable automated credential acquisition through the OAuth authorization flow.

```yaml
apiVersion: iam.stigmer.ai/v1
kind: OAuthApp
metadata:
  name: Slack OAuth
  slug: slack-oauth
  org: acme
spec:
  provider: "Slack"
  client_id: "1234567890.abcdef"
  client_secret: "xoxs-..."
  authorization_url: "https://slack.com/oauth/v2/authorize"
  token_url: "https://slack.com/api/oauth.v2.access"
  scopes: ["channels:read", "chat:write"]
```

## Client Access

<SDKTabs>
<Tab value="TypeScript">
```typescript
const oAuthApp = await stigmer.oauthapp.get("oapp_01j5q3k7m8r2s4tnz2hfp0q0c3");
```
</Tab>
<Tab value="Go">
```go
oAuthApp, err := client.OAuthApp.Get(ctx, "oapp_01j5q3k7m8r2s4tnz2hfp0q0c3")
```
</Tab>
<Tab value="Python">
```python
o_auth_app = client.oauthapps.get("oapp_01j5q3k7m8r2s4tnz2hfp0q0c3")
```
</Tab>
<Tab value="Java">
```java
OAuthApp oAuthApp = client.oauthapps().get("oapp_01j5q3k7m8r2s4tnz2hfp0q0c3");
```
</Tab>
</SDKTabs>

## Methods

| Method | Description |
|--------|-------------|
| [`get`](#get) | Get an OAuth app by its unique identifier. |
| [`getByReference`](#getbyreference) | Get an OAuth app by its organization-scoped reference (org/slug). |
| [`listByOrg`](#listbyorg) | List all OAuth apps belonging to an organization. |
| [`apply`](#apply) | Create or update an OAuth app. |
| [`create`](#create) | Create an OAuth app. |
| [`update`](#update) | Update an existing OAuth app. |
| [`delete`](#delete) | Delete an OAuth app. |

### get

Get an OAuth app by its unique identifier.

<SDKTabs>
<Tab value="TypeScript">
```typescript
const oAuthApp = await stigmer.oauthapp.get(id);
```
</Tab>
<Tab value="Go">
```go
oAuthApp, err := client.OAuthApp.Get(ctx, id)
```
</Tab>
<Tab value="Python">
```python
o_auth_app = client.oauthapps.get(id)
```
</Tab>
<Tab value="Java">
```java
OAuthApp oAuthApp = client.oauthapps().get(id);
```
</Tab>
</SDKTabs>

**Parameters**

| Parameter | Type | Description |
|-----------|------|-------------|
| `id` | `string` | O Auth App identifier. |

**Returns**: [`OAuthApp`](#oauthapp)

### getByReference

Get an OAuth app by its organization-scoped reference (org/slug).

Resolves a human-readable reference like "acme/slack-oauth" to the full OAuthApp resource.

<SDKTabs>
<Tab value="TypeScript">
```typescript
const oAuthApp = await stigmer.oauthapp.getByReference({
  org: "...",
  kind: "...",
  slug: "...",
  version: "...",
});
```
</Tab>
<Tab value="Go">
```go
oAuthApp, err := client.OAuthApp.GetByReference(ctx, &stigmer.ApiResourceReference{
  Org:     "...",
  Kind:    "...",
  Slug:    "...",
  Version: "...",
})
```
</Tab>
<Tab value="Python">
```python
o_auth_app = client.oauthapps.get_by_reference(ApiResourceReference(
    org="...",
    kind="...",
    slug="...",
    version="...",
))
```
</Tab>
<Tab value="Java">
```java
OAuthApp oAuthApp = client.oauthapps().getByReference(ApiResourceReference.builder()
    .org("...")
    .kind("...")
    .slug("...")
    .version("...")
    .build());
```
</Tab>
</SDKTabs>

**Parameters**: [`ResourceRef`](#resourceref)

**Returns**: [`OAuthApp`](#oauthapp)

### listByOrg

List all OAuth apps belonging to an organization.

Returns every OAuthApp whose metadata.org matches the input org. Typically a small set (1-5 per org), so results are not paginated.

<SDKTabs>
<Tab value="TypeScript">
```typescript
const oAuthApps = await stigmer.oauthapp.listByOrg({
  org: "...",
});
```
</Tab>
<Tab value="Go">
```go
oAuthApps, err := client.OAuthApp.ListByOrg(ctx, &stigmer.ListOAuthAppsByOrgInput{
  Org: "...",
})
```
</Tab>
<Tab value="Python">
```python
o_auth_apps = client.oauthapps.list_by_org(ListOAuthAppsByOrgInput(
    org="...",
))
```
</Tab>
<Tab value="Java">
```java
OAuthApps oAuthApps = client.oauthapps().listByOrg(ListOAuthAppsByOrgInput.builder()
    .org("...")
    .build());
```
</Tab>
</SDKTabs>

**Parameters**: [`ListOAuthAppsByOrgInput`](#listoauthappsbyorginput)

**Returns**: [`OAuthApps`](#oauthapps)

### apply

Create or update an OAuth app.

If the resource does not exist, creates a new OAuth app. If the resource exists, updates the existing OAuth app.

<SDKTabs>
<Tab value="TypeScript">
```typescript
const oAuthApp = await stigmer.oauthapp.apply({
  name: "my-o-auth-app",
  org: "acme",
  slug: "...",
  labels: {},
  provider: "...",
  clientId: "...",
  clientSecret: "...",
  authorizationUrl: "...",
  tokenUrl: "...",
  scopes: [],
  userinfoUrl: "...",
  scopeParameterName: "...",
  vendorApprovalStatus: "...",
  vendorApprovalDocsUrl: "...",
});
```
</Tab>
<Tab value="Go">
```go
oAuthApp, err := client.OAuthApp.Apply(ctx, &stigmer.OAuthAppInput{
  Name:                  "my-o-auth-app",
  Org:                   "acme",
  Slug:                  "...",
  Labels:                map[string]string{},
  Provider:              "...",
  ClientId:              "...",
  ClientSecret:          "...",
  AuthorizationUrl:      "...",
  TokenUrl:              "...",
  Scopes:                []string{},
  UserinfoUrl:           "...",
  ScopeParameterName:    "...",
  VendorApprovalStatus:  "...",
  VendorApprovalDocsUrl: "...",
})
```
</Tab>
<Tab value="Python">
```python
o_auth_app = client.oauthapps.apply(OAuthAppInput(
    name="my-o-auth-app",
    org="acme",
    slug="...",
    labels={},
    provider="...",
    client_id="...",
    client_secret="...",
    authorization_url="...",
    token_url="...",
    scopes=[],
    userinfo_url="...",
    scope_parameter_name="...",
    vendor_approval_status="...",
    vendor_approval_docs_url="...",
))
```
</Tab>
<Tab value="Java">
```java
OAuthApp oAuthApp = client.oauthapps().apply(OAuthAppInput.builder()
    .name("my-o-auth-app")
    .org("acme")
    .slug("...")
    .labels(Map.of())
    .provider("...")
    .clientId("...")
    .clientSecret("...")
    .authorizationUrl("...")
    .tokenUrl("...")
    .scopes(List.of())
    .userinfoUrl("...")
    .scopeParameterName("...")
    .vendorApprovalStatus("...")
    .vendorApprovalDocsUrl("...")
    .build());
```
</Tab>
</SDKTabs>

**Parameters**: [`OAuthAppInput`](#oauthappinput)

**Returns**: [`OAuthApp`](#oauthapp)

### create

Create an OAuth app.

The creator's organization owns the OAuth app. The creator is granted the owner role automatically.

<SDKTabs>
<Tab value="TypeScript">
```typescript
const oAuthApp = await stigmer.oauthapp.create({
  name: "my-o-auth-app",
  org: "acme",
  slug: "...",
  labels: {},
  provider: "...",
  clientId: "...",
  clientSecret: "...",
  authorizationUrl: "...",
  tokenUrl: "...",
  scopes: [],
  userinfoUrl: "...",
  scopeParameterName: "...",
  vendorApprovalStatus: "...",
  vendorApprovalDocsUrl: "...",
});
```
</Tab>
<Tab value="Go">
```go
oAuthApp, err := client.OAuthApp.Create(ctx, &stigmer.OAuthAppInput{
  Name:                  "my-o-auth-app",
  Org:                   "acme",
  Slug:                  "...",
  Labels:                map[string]string{},
  Provider:              "...",
  ClientId:              "...",
  ClientSecret:          "...",
  AuthorizationUrl:      "...",
  TokenUrl:              "...",
  Scopes:                []string{},
  UserinfoUrl:           "...",
  ScopeParameterName:    "...",
  VendorApprovalStatus:  "...",
  VendorApprovalDocsUrl: "...",
})
```
</Tab>
<Tab value="Python">
```python
o_auth_app = client.oauthapps.create(OAuthAppInput(
    name="my-o-auth-app",
    org="acme",
    slug="...",
    labels={},
    provider="...",
    client_id="...",
    client_secret="...",
    authorization_url="...",
    token_url="...",
    scopes=[],
    userinfo_url="...",
    scope_parameter_name="...",
    vendor_approval_status="...",
    vendor_approval_docs_url="...",
))
```
</Tab>
<Tab value="Java">
```java
OAuthApp oAuthApp = client.oauthapps().create(OAuthAppInput.builder()
    .name("my-o-auth-app")
    .org("acme")
    .slug("...")
    .labels(Map.of())
    .provider("...")
    .clientId("...")
    .clientSecret("...")
    .authorizationUrl("...")
    .tokenUrl("...")
    .scopes(List.of())
    .userinfoUrl("...")
    .scopeParameterName("...")
    .vendorApprovalStatus("...")
    .vendorApprovalDocsUrl("...")
    .build());
```
</Tab>
</SDKTabs>

**Parameters**: [`OAuthAppInput`](#oauthappinput)

**Returns**: [`OAuthApp`](#oauthapp)

### update

Update an existing OAuth app.

<SDKTabs>
<Tab value="TypeScript">
```typescript
const oAuthApp = await stigmer.oauthapp.update({
  name: "my-o-auth-app",
  org: "acme",
  slug: "...",
  labels: {},
  provider: "...",
  clientId: "...",
  clientSecret: "...",
  authorizationUrl: "...",
  tokenUrl: "...",
  scopes: [],
  userinfoUrl: "...",
  scopeParameterName: "...",
  vendorApprovalStatus: "...",
  vendorApprovalDocsUrl: "...",
});
```
</Tab>
<Tab value="Go">
```go
oAuthApp, err := client.OAuthApp.Update(ctx, &stigmer.OAuthAppInput{
  Name:                  "my-o-auth-app",
  Org:                   "acme",
  Slug:                  "...",
  Labels:                map[string]string{},
  Provider:              "...",
  ClientId:              "...",
  ClientSecret:          "...",
  AuthorizationUrl:      "...",
  TokenUrl:              "...",
  Scopes:                []string{},
  UserinfoUrl:           "...",
  ScopeParameterName:    "...",
  VendorApprovalStatus:  "...",
  VendorApprovalDocsUrl: "...",
})
```
</Tab>
<Tab value="Python">
```python
o_auth_app = client.oauthapps.update(OAuthAppInput(
    name="my-o-auth-app",
    org="acme",
    slug="...",
    labels={},
    provider="...",
    client_id="...",
    client_secret="...",
    authorization_url="...",
    token_url="...",
    scopes=[],
    userinfo_url="...",
    scope_parameter_name="...",
    vendor_approval_status="...",
    vendor_approval_docs_url="...",
))
```
</Tab>
<Tab value="Java">
```java
OAuthApp oAuthApp = client.oauthapps().update(OAuthAppInput.builder()
    .name("my-o-auth-app")
    .org("acme")
    .slug("...")
    .labels(Map.of())
    .provider("...")
    .clientId("...")
    .clientSecret("...")
    .authorizationUrl("...")
    .tokenUrl("...")
    .scopes(List.of())
    .userinfoUrl("...")
    .scopeParameterName("...")
    .vendorApprovalStatus("...")
    .vendorApprovalDocsUrl("...")
    .build());
```
</Tab>
</SDKTabs>

**Parameters**: [`OAuthAppInput`](#oauthappinput)

**Returns**: [`OAuthApp`](#oauthapp)

### delete

Delete an OAuth app.

Deletion should be blocked if any McpServer resources reference this OAuth app via McpServerVendorOAuth.oauth_app_ref.

<SDKTabs>
<Tab value="TypeScript">
```typescript
const oAuthApp = await stigmer.oauthapp.delete({ resourceId: id });
```
</Tab>
<Tab value="Go">
```go
oAuthApp, err := client.OAuthApp.Delete(ctx, &stigmer.DeleteResourceInput{
  ResourceID: id,
})
```
</Tab>
<Tab value="Python">
```python
o_auth_app = client.oauthapps.delete(DeleteResourceInput(resource_id=id))
```
</Tab>
<Tab value="Java">
```java
OAuthApp oAuthApp = client.oauthapps().delete(DeleteResourceInput.builder()
    .resourceId(id)
    .build());
```
</Tab>
</SDKTabs>

**Parameters**

| Parameter | Type | Description |
|-----------|------|-------------|
| `resourceId` | `string` | ID of the resource to delete. |
| `versionMessage` | `string` | Optional deletion reason. |
| `force` | `boolean` | Force delete even if resource is in use. |

**Returns**: [`OAuthApp`](#oauthapp)

## Types

### OAuthAppInput

Input for creating or updating an O Auth App.

<TypeTable
  type={{
    name: { type: "string", description: "Resource name.", required: true },
    slug: { type: "string", description: "URL-friendly identifier." },
    org: { type: "string", description: "Organization slug.", required: true },
    labels: { type: "Record<string, string>", description: "Key-value labels." },
    provider: { type: "string", description: "Human-readable vendor name for UI display and logging." },
    clientId: { type: "string", description: "OAuth client identifier registered with the vendor." },
    clientSecret: { type: "string", description: "OAuth client secret registered with the vendor." },
    authorizationUrl: { type: "string", description: "Vendor's OAuth authorization endpoint." },
    tokenUrl: { type: "string", description: "Vendor's OAuth token endpoint." },
    scopes: { type: "string[]", description: "OAuth scopes to request during the authorization flow." },
    userinfoUrl: { type: "string", description: "Optional OIDC UserInfo endpoint for fetching user profile data." },
    scopeParameterName: { type: "string", description: "Name of the query parameter used to send scopes in the authorization URL." },
    vendorApprovalStatus: { type: "VendorApprovalStatus", description: "Vendor marketplace/app-review approval status for this OAuth app.", typeDescriptionLink: "#vendorapprovalstatus" },
    vendorApprovalDocsUrl: { type: "string", description: "Documentation URL explaining how users can bring their own OAuth app credentials or personal access tokens for this vendor while the platform's OAuth app is pending approval." },
  }}
/>

### ResourceRef

Generic reference to any API resource by org and slug.

<TypeTable
  type={{
    org: { type: "string", description: "Organization that owns the referenced resource." },
    kind: { type: "ApiResourceKind", description: "Kind of the referenced resource (e.g., SKILL, AGENT, MCP_SERVER).", typeDescriptionLink: "/docs/sdk/resources/commons#apiresourcekind" },
    slug: { type: "string", description: "Resource slug (user-friendly identifier, unique within org).", required: true },
    version: { type: "string", description: "Version of the resource (optional, only applicable to versioned resources like Skills)." },
  }}
/>

### ListOAuthAppsByOrgInput

ListOAuthAppsByOrgInput specifies the organization whose OAuth apps should be returned.

<TypeTable
  type={{
    org: { type: "string", description: "Organization slug to list OAuth apps for." },
  }}
/>

### OAuthApps

OAuthApps contains a list of OAuth app resources.

<TypeTable
  type={{
    entries: { type: "OAuthAppInput[]", description: "OAuth app entries.", typeDescriptionLink: "#oauthappinput" },
  }}
/>

### OAuthApp

OAuthApp represents a registered OAuth application with an external vendor.

<TypeTable
  type={{
    apiVersion: { type: "string", description: "API version for this resource type." },
    kind: { type: "string", description: "Resource kind identifier." },
    metadata: { type: "ApiResourceMetadata", description: "Resource metadata including id, name, org, slug, labels, visibility, and timestamps.", typeDescriptionLink: "/docs/sdk/resources/commons#apiresourcemetadata" },
    spec: { type: "OAuthAppInput", description: "Resource specification. See [`OAuthAppInput`](#oauthappinput).", typeDescriptionLink: "#oauthappinput" },
    status: { type: "OAuthAppStatus", description: "System-managed state. See `OAuthAppStatus`." },
  }}
/>

### VendorApprovalStatus

Lifecycle status of an OAuthApp's vendor marketplace approval.

| Value | Description |
|-------|-------------|
| `VENDOR_APPROVAL_STATUS_PENDING` | The OAuth app has been submitted to the vendor for review and is awaiting approval. |
| `VENDOR_APPROVAL_STATUS_APPROVED` | The vendor has approved the OAuth app. |
| `VENDOR_APPROVAL_STATUS_REJECTED` | The vendor rejected the OAuth app. |

