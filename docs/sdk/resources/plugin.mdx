---
title: Plugin
---

{/* Auto-generated by stigmer-codegen. DO NOT EDIT. */}

A Plugin is an installed Agent Plugins package: the unit of install, upgrade and
removal for a set of skills, MCP servers, an agent and workflows. A plugin is
what you install; an agent is what runs. Pushing a plugin folder in the Agent
Plugins, Cursor, Claude Code or Codex layout materialises ordinary Stigmer
resources in the organization, each labelled with the plugin's id, and the
plugin's status reports what the push produced.

A plugin is not authored as YAML. Its spec is read from the package manifest;
the folder below is what `stigmer push plugin ./thermos` sends.

```text
thermos/
  .cursor-plugin/plugin.json      # name, version, description, author
  skills/thermos/SKILL.md         # one Skill per skills/<name>/SKILL.md
  agents/reviewer.md              # one sub-agent of the plugin's Agent
  mcp.json                        # one McpServer per mcpServers entry
  ai.stigmer/agent.yaml           # optional: the Agent that replaces the composed default
```

The resulting resource, as `stigmer get plugin thermos -o yaml` renders it:

```yaml
apiVersion: agentic.stigmer.ai/v1
kind: Plugin
metadata:
  name: thermos
  slug: thermos
spec:
  name: thermos
  version: "1.2.0"
  description: "Code review with a thermonuclear standard"
  dialect: PLUGIN_DIALECT_CURSOR
status:
  digest: "3f8c1d2e9b7a4c5d6e7f8091a2b3c4d5e6f708192a3b4c5d6e7f8091a2b3c4d5"
  state: PLUGIN_STATE_READY
  materialized:
    skills: 3
    mcp_servers: 0
    agents: 1
    workflows: 0
```

## Client Access

<SDKTabs>
<Tab value="TypeScript">
```typescript
const plugin = await stigmer.plugin.get("plg_01j5q3k7m8r2s4tnz2hfp0q0c3");
```
</Tab>
<Tab value="Go">
```go
plugin, err := client.Plugin.Get(ctx, "plg_01j5q3k7m8r2s4tnz2hfp0q0c3")
```
</Tab>
<Tab value="Python">
```python
plugin = client.plugins.get("plg_01j5q3k7m8r2s4tnz2hfp0q0c3")
```
</Tab>
<Tab value="Java">
```java
Plugin plugin = client.plugins().get("plg_01j5q3k7m8r2s4tnz2hfp0q0c3");
```
</Tab>
</SDKTabs>

## Methods

| Method | Description |
|--------|-------------|
| [`get`](#get) | Get a single plugin by ID. |
| [`getByReference`](#getbyreference) | Get a plugin by API resource reference with version support. |
| [`listMembers`](#listmembers) | List the resources an installed plugin materialised. |
| [`listVersions`](#listversions) | List version history for a plugin. |
| [`updateVisibility`](#updatevisibility) | Update the visibility of a plugin and of every resource it materialised. |
| [`delete`](#delete) | Delete a plugin and every resource it materialised. |
| [`createArtifactUploadUrl`](#createartifactuploadurl) | Mint a short-lived, single-use upload URL for staging a plugin archive that exceeds the gRPC message-size cap (10MB). |
| [`push`](#push) | Push a plugin archive to install or upgrade it. |

### get

Get a single plugin by ID.

<SDKTabs>
<Tab value="TypeScript">
```typescript
const plugin = await stigmer.plugin.get(id);
```
</Tab>
<Tab value="Go">
```go
plugin, err := client.Plugin.Get(ctx, id)
```
</Tab>
<Tab value="Python">
```python
plugin = client.plugins.get(id)
```
</Tab>
<Tab value="Java">
```java
Plugin plugin = client.plugins().get(id);
```
</Tab>
</SDKTabs>

**Parameters**

| Parameter | Type | Description |
|-----------|------|-------------|
| `id` | `string` | Plugin identifier. |

**Returns**: [`Plugin`](#plugin)

### getByReference

Get a plugin by API resource reference with version support.

Version resolution (via ApiResourceReference.version field): - Empty/"latest" → Returns the installed version - Tag name (the manifest version, e.g. "1.2.0") → Resolves to the version holding this tag - SHA-256 digest (64 hex chars) → Returns the exact immutable version

<SDKTabs>
<Tab value="TypeScript">
```typescript
const plugin = await stigmer.plugin.getByReference({
  org: "...",
  kind: "...",
  slug: "...",
  version: "...",
});
```
</Tab>
<Tab value="Go">
```go
plugin, err := client.Plugin.GetByReference(ctx, &stigmer.ApiResourceReference{
  Org:     "...",
  Kind:    "...",
  Slug:    "...",
  Version: "...",
})
```
</Tab>
<Tab value="Python">
```python
plugin = client.plugins.get_by_reference(ApiResourceReference(
    org="...",
    kind="...",
    slug="...",
    version="...",
))
```
</Tab>
<Tab value="Java">
```java
Plugin plugin = client.plugins().getByReference(ApiResourceReference.builder()
    .org("...")
    .kind("...")
    .slug("...")
    .version("...")
    .build());
```
</Tab>
</SDKTabs>

**Parameters**: [`ResourceRef`](#resourceref)

**Returns**: [`Plugin`](#plugin)

### listMembers

List the resources an installed plugin materialised.

Returns every skill, MCP server, agent and workflow the plugin owns, in materialisation order.

<SDKTabs>
<Tab value="TypeScript">
```typescript
const listPluginMembersResponse = await stigmer.plugin.listMembers(id);
```
</Tab>
<Tab value="Go">
```go
listPluginMembersResponse, err := client.Plugin.ListMembers(ctx, id)
```
</Tab>
<Tab value="Python">
```python
list_plugin_members_response = client.plugins.list_members(id)
```
</Tab>
<Tab value="Java">
```java
ListPluginMembersResponse listPluginMembersResponse = client.plugins().listMembers(id);
```
</Tab>
</SDKTabs>

**Parameters**

| Parameter | Type | Description |
|-----------|------|-------------|
| `id` | `string` | Plugin identifier. |

**Returns**: [`ListPluginMembersResponse`](#listpluginmembersresponse)

### listVersions

List version history for a plugin.

Returns every installed version ordered by push time (newest first), with its digest, tag, actor and archive storage key.

<SDKTabs>
<Tab value="TypeScript">
```typescript
const listPluginVersionsResponse = await stigmer.plugin.listVersions({
  org: "...",
  slug: "...",
  pageToken: "...",
  pageSize: 0,
});
```
</Tab>
<Tab value="Go">
```go
listPluginVersionsResponse, err := client.Plugin.ListVersions(ctx, &stigmer.ListPluginVersionsInput{
  Org:       "...",
  Slug:      "...",
  PageToken: "...",
  PageSize:  0,
})
```
</Tab>
<Tab value="Python">
```python
list_plugin_versions_response = client.plugins.list_versions(ListPluginVersionsInput(
    org="...",
    slug="...",
    page_token="...",
    page_size=0,
))
```
</Tab>
<Tab value="Java">
```java
ListPluginVersionsResponse listPluginVersionsResponse = client.plugins().listVersions(ListPluginVersionsInput.builder()
    .org("...")
    .slug("...")
    .pageToken("...")
    .pageSize(0)
    .build());
```
</Tab>
</SDKTabs>

**Parameters**: [`ListPluginVersionsInput`](#listpluginversionsinput)

**Returns**: [`ListPluginVersionsResponse`](#listpluginversionsresponse)

### updateVisibility

Update the visibility of a plugin and of every resource it materialised. Only modifies metadata.visibility on the plugin and its members.

<SDKTabs>
<Tab value="TypeScript">
```typescript
const plugin = await stigmer.plugin.updateVisibility({
  resourceId: "...",
  visibility: "...",
});
```
</Tab>
<Tab value="Go">
```go
plugin, err := client.Plugin.UpdateVisibility(ctx, &stigmer.UpdateVisibilityInput{
  ResourceId: "...",
  Visibility: "...",
})
```
</Tab>
<Tab value="Python">
```python
plugin = client.plugins.update_visibility(UpdateVisibilityInput(
    resource_id="...",
    visibility="...",
))
```
</Tab>
<Tab value="Java">
```java
Plugin plugin = client.plugins().updateVisibility(UpdateVisibilityInput.builder()
    .resourceId("...")
    .visibility("...")
    .build());
```
</Tab>
</SDKTabs>

**Parameters**: [`UpdateVisibilityInput`](#updatevisibilityinput)

**Returns**: [`Plugin`](#plugin)

### delete

Delete a plugin and every resource it materialised. Refused when a resource outside the plugin still references a member; the error names the referencing resources.

<SDKTabs>
<Tab value="TypeScript">
```typescript
const plugin = await stigmer.plugin.delete(id);
```
</Tab>
<Tab value="Go">
```go
plugin, err := client.Plugin.Delete(ctx, id)
```
</Tab>
<Tab value="Python">
```python
plugin = client.plugins.delete(id)
```
</Tab>
<Tab value="Java">
```java
Plugin plugin = client.plugins().delete(id);
```
</Tab>
</SDKTabs>

**Parameters**

| Parameter | Type | Description |
|-----------|------|-------------|
| `id` | `string` | Plugin identifier. |

**Returns**: [`Plugin`](#plugin)

### createArtifactUploadUrl

Mint a short-lived, single-use upload URL for staging a plugin archive that exceeds the gRPC message-size cap (10MB). Flow:

1. createArtifactUploadUrl(org, size_bytes) → \{ url, artifact_upload_ref \} 2. HTTP PUT the ZIP bytes to url 3. push(PushPluginRequest\{ artifact_upload_ref \})

<SDKTabs>
<Tab value="TypeScript">
```typescript
const pluginArtifactUploadUrl = await stigmer.plugin.createArtifactUploadUrl({
  org: "...",
  sizeBytes: 0,
});
```
</Tab>
<Tab value="Go">
```go
pluginArtifactUploadUrl, err := client.Plugin.CreateArtifactUploadUrl(ctx, &stigmer.CreatePluginArtifactUploadUrlRequest{
  Org:       "...",
  SizeBytes: 0,
})
```
</Tab>
<Tab value="Python">
```python
plugin_artifact_upload_url = client.plugins.create_artifact_upload_url(CreatePluginArtifactUploadUrlRequest(
    org="...",
    size_bytes=0,
))
```
</Tab>
<Tab value="Java">
```java
PluginArtifactUploadUrl pluginArtifactUploadUrl = client.plugins().createArtifactUploadUrl(CreatePluginArtifactUploadUrlRequest.builder()
    .org("...")
    .sizeBytes(0)
    .build());
```
</Tab>
</SDKTabs>

**Parameters**: [`CreatePluginArtifactUploadUrlRequest`](#createpluginartifactuploadurlrequest)

**Returns**: [`PluginArtifactUploadUrl`](#pluginartifactuploadurl)

### push

Push a plugin archive to install or upgrade it. Creates the plugin if it does not exist, or installs a new version of an existing plugin; pushing the archive already installed changes nothing. The archive is a plugin folder in the Agent Plugins, Cursor, Claude Code or Codex layout; the response's status names what was materialised and what was skipped.

<SDKTabs>
<Tab value="TypeScript">
```typescript
const plugin = await stigmer.plugin.push({
  org: "...",
  artifact: "...",
  artifactUploadRef: "...",
  visibility: "...",
  message: "...",
});
```
</Tab>
<Tab value="Go">
```go
plugin, err := client.Plugin.Push(ctx, &stigmer.PushPluginRequest{
  Org:               "...",
  Artifact:          "...",
  ArtifactUploadRef: "...",
  Visibility:        "...",
  Message:           "...",
})
```
</Tab>
<Tab value="Python">
```python
plugin = client.plugins.push(PushPluginRequest(
    org="...",
    artifact="...",
    artifact_upload_ref="...",
    visibility="...",
    message="...",
))
```
</Tab>
<Tab value="Java">
```java
Plugin plugin = client.plugins().push(PushPluginRequest.builder()
    .org("...")
    .artifact("...")
    .artifactUploadRef("...")
    .visibility("...")
    .message("...")
    .build());
```
</Tab>
</SDKTabs>

**Parameters**: [`PushPluginRequest`](#pushpluginrequest)

**Returns**: [`Plugin`](#plugin)

## Types

### PluginInput

Input for creating or updating a Plugin.

<TypeTable
  type={{
    name: { type: "string", description: "Resource name.", required: true },
    slug: { type: "string", description: "URL-friendly identifier." },
    org: { type: "string", description: "Organization slug.", required: true },
    labels: { type: "Record<string, string>", description: "Key-value labels." },
    version: { type: "string", description: "Version string as written in the manifest, typically Semantic Versioning." },
    description: { type: "string", description: "Human-readable description from the manifest." },
    author: { type: "PluginAuthorInput", description: "Author information from the manifest.", typeDescriptionLink: "#pluginauthorinput" },
    homepage: { type: "string", description: "Homepage URL from the manifest." },
    repository: { type: "string", description: "Source repository URL from the manifest." },
    license: { type: "string", description: "License identifier from the manifest." },
    keywords: { type: "string[]", description: "Keywords from the manifest, for search and marketplace display." },
    dialect: { type: "PluginDialect", description: "The manifest dialect the package was written in.", typeDescriptionLink: "#plugindialect" },
  }}
/>

### PluginAuthorInput

PluginAuthor identifies who published the plugin.

<TypeTable
  type={{
    name: { type: "string", description: "Author name." },
    email: { type: "string", description: "Author email." },
    url: { type: "string", description: "Author URL." },
  }}
/>

### PushPluginRequest

PushPluginRequest installs or upgrades a plugin from its archive.

<TypeTable
  type={{
    org: { type: "string", description: "Organization the plugin is installed into.", required: true },
    artifact: { type: "Uint8Array", description: "The plugin package as a ZIP archive: the plugin folder with its manifest, skills/, mcp.json, agents/ and ai.stigmer/ at the archive root." },
    artifactUploadRef: { type: "string", description: "Reference to an archive staged via createArtifactUploadUrl()." },
    visibility: { type: "ApiResourceVisibility", description: "Visibility for the plugin and every resource it materialises.", typeDescriptionLink: "/docs/sdk/resources/commons#apiresourcevisibility" },
    message: { type: "string", description: "Optional human-readable message describing this version, kept in metadata.version.message for the version history." },
  }}
/>

### CreatePluginArtifactUploadUrlRequest

CreatePluginArtifactUploadUrlRequest asks the server to mint a staging slot for a plugin archive too large to travel inline.

<TypeTable
  type={{
    org: { type: "string", description: "Organization that will own the plugin (the authorization scope; the caller needs the same can_create_plugin permission push requires).", required: true },
    sizeBytes: { type: "number", description: "Declared size of the archive in bytes; an over-limit declaration is refused before any bytes move." },
  }}
/>

### PluginArtifactUploadUrl

PluginArtifactUploadUrl is a short-lived, single-use capability for staging a plugin archive over HTTP.

<TypeTable
  type={{
    url: { type: "string", description: "URL to PUT the archive bytes to." },
    artifactUploadRef: { type: "string", description: "Opaque reference to pass as PushPluginRequest.artifact_upload_ref after the PUT succeeds." },
    ttlSeconds: { type: "number", description: "Time-to-live for the upload URL in seconds." },
  }}
/>

### UpdateVisibilityInput

Input for updating the visibility of any API resource.

<TypeTable
  type={{
    resourceId: { type: "string", description: "ID of the resource whose visibility is being updated.", required: true },
    visibility: { type: "ApiResourceVisibility", description: "The new visibility setting for the resource.", typeDescriptionLink: "/docs/sdk/resources/commons#apiresourcevisibility" },
  }}
/>

### ResourceRef

Generic reference to any API resource by org and slug.

<TypeTable
  type={{
    org: { type: "string", description: "Organization that owns the referenced resource." },
    kind: { type: "ApiResourceKind", description: "Kind of the referenced resource (e.g., SKILL, AGENT, MCP_SERVER).", typeDescriptionLink: "/docs/sdk/resources/commons#apiresourcekind" },
    slug: { type: "string", description: "Resource slug (user-friendly identifier, unique within org).", required: true },
    version: { type: "string", description: "Version of the resource (optional, only applicable to versioned resources like Skills)." },
  }}
/>

### ListPluginMembersResponse

ListPluginMembersResponse lists the resources an installed plugin owns.

<TypeTable
  type={{
    members: { type: "PluginMemberInput[]", description: "Members in materialisation order: skills, MCP servers, agents, workflows." },
  }}
/>

### ListPluginVersionsInput

ListPluginVersionsInput requests the version history for a plugin.

<TypeTable
  type={{
    org: { type: "string", description: "Organization that owns the plugin.", required: true },
    slug: { type: "string", description: "Plugin slug (unique within the organization).", required: true },
    pageToken: { type: "string", description: "Opaque token for cursor-based pagination." },
    pageSize: { type: "number", description: "Maximum number of versions to return per page." },
  }}
/>

### ListPluginVersionsResponse

ListPluginVersionsResponse is a page of plugin version history.

<TypeTable
  type={{
    versions: { type: "PluginVersionEntryInput[]", description: "Versions, newest first." },
    nextPageToken: { type: "string", description: "Token for the next page." },
    totalCount: { type: "number", description: "Total number of versions across all pages." },
  }}
/>

### Plugin

Plugin is an installed Agent Plugins package: the unit of install, upgrade and removal for a set of skills, MCP servers, an agent and workflows.

<TypeTable
  type={{
    apiVersion: { type: "string", description: "API version for this resource type." },
    kind: { type: "string", description: "Resource kind identifier." },
    metadata: { type: "ApiResourceMetadata", description: "Resource metadata including id, name, org, slug, labels, visibility, and timestamps.", typeDescriptionLink: "/docs/sdk/resources/commons#apiresourcemetadata" },
    spec: { type: "PluginInput", description: "Resource specification. See [`PluginInput`](#plugininput).", typeDescriptionLink: "#plugininput" },
    status: { type: "PluginStatus", description: "System-managed state. See [`PluginStatus`](#pluginstatus).", typeDescriptionLink: "#pluginstatus" },
  }}
/>

### PluginStatus

PluginStatus is the system-managed state of an installed plugin: the archive identity and the receipt of the last push.

<TypeTable
  type={{
    audit: { type: "ApiResourceAudit", description: "Standard audit information tracking creation and modification.", typeDescriptionLink: "/docs/sdk/resources/commons#apiresourceaudit" },
    digest: { type: "string", description: "SHA-256 of the plugin archive, the immutable version identifier." },
    artifactStorageKey: { type: "string", description: "Storage key of the plugin archive." },
    state: { type: "PluginState", description: "Current lifecycle state of the install.", typeDescriptionLink: "#pluginstate" },
    error: { type: "string", description: "One sentence naming what failed when state is FAILED; empty otherwise." },
    materialized: { type: "PluginMaterialization", description: "What the last push materialised, by kind.", typeDescriptionLink: "#pluginmaterialization" },
    warnings: { type: "PluginWarning[]", description: "Warnings the last push recorded: components Stigmer does not carry, model hints it did not apply, names that shadow built-ins.", typeDescriptionLink: "#pluginwarning" },
  }}
/>

### PluginMaterialization

PluginMaterialization counts what a push produced, by kind.

<TypeTable
  type={{
    skills: { type: "number", description: "Skills materialised." },
    mcpServers: { type: "number", description: "MCP servers materialised." },
    agents: { type: "number", description: "Agents materialised: one when the plugin carries a skill, a sub-agent or an agent overlay; zero for an MCP-only plugin." },
    workflows: { type: "number", description: "Workflows materialised from the plugin's Stigmer overlay." },
  }}
/>

### PluginWarning

PluginWarning is one thing a push noticed but did not refuse.

<TypeTable
  type={{
    kind: { type: "string", description: "Stable warning kind, e.g. \"component-ignored\", \"member-adopted\", \"model-hint-unresolved\", \"sub-agent-name-builtin\", \"version-not-taggable\"." },
    message: { type: "string", description: "The warning as one user-facing sentence." },
    path: { type: "string", description: "The plugin-relative path the warning points at, when it has one." },
  }}
/>

### PluginState

PluginState is the lifecycle state of a plugin install.

| Value | Description |
|-------|-------------|
| `PLUGIN_STATE_INSTALLING` | The archive is stored and its members are being materialised. |
| `PLUGIN_STATE_READY` | Every member of the last push is materialised. |
| `PLUGIN_STATE_FAILED` | A member failed to materialise; `error` names it. |

### PluginDialect

PluginDialect names the manifest format a package was read from.

| Value | Description |
|-------|-------------|
| `PLUGIN_DIALECT_AGENT_PLUGINS` | The open Agent Plugins format: a root plugin.json. |
| `PLUGIN_DIALECT_CLAUDE` | Claude Code: .claude-plugin/plugin.json. |
| `PLUGIN_DIALECT_CURSOR` | Cursor: .cursor-plugin/plugin.json. |
| `PLUGIN_DIALECT_CODEX` | Codex: .codex-plugin/plugin.json. |

