---
title: Workflow
---

{/* Auto-generated by stigmer-codegen. DO NOT EDIT. */}

A Workflow defines a multi-step task orchestration. It declares the sequence of
tasks to execute, including HTTP calls, gRPC calls, conditional branching,
parallel execution, error handling, and AI agent invocations.

```yaml
apiVersion: agentic.stigmer.ai/v1
kind: Workflow
metadata:
  name: data-pipeline
  slug: data-pipeline
spec:
  description: "Fetches data from an API, processes it, and notifies on completion"
  document:
    dsl: "1.0.0"
    namespace: acme
    name: data-pipeline
    version: "1.0.0"
  tasks:
    - name: fetchData
      kind: http_call
      task_config:
        method: GET
        endpoint:
          uri: "https://api.example.com/data"
      export:
        as: "${.}"
    - name: processResults
      kind: agent_call
      task_config:
        agent: acme/data-analyst
        message: "Analyze this data: ${ $context.fetchData.body }"
```

## Client Access

<SDKTabs>
<Tab value="TypeScript">
```typescript
const workflow = await stigmer.workflow.get("wfl_01j5q3k7m8r2s4tnz2hfp0q0c3");
```
</Tab>
<Tab value="Go">
```go
workflow, err := client.Workflow.Get(ctx, "wfl_01j5q3k7m8r2s4tnz2hfp0q0c3")
```
</Tab>
<Tab value="Python">
```python
workflow = client.workflows.get("wfl_01j5q3k7m8r2s4tnz2hfp0q0c3")
```
</Tab>
<Tab value="Java">
```java
Workflow workflow = client.workflows().get("wfl_01j5q3k7m8r2s4tnz2hfp0q0c3");
```
</Tab>
</SDKTabs>

## Methods

| Method | Description |
|--------|-------------|
| [`get`](#get) | Get a single workflow by ID. |
| [`getByReference`](#getbyreference) | Get a workflow by its organization-scoped reference (org/slug). |
| [`getTaskKindRegistry`](#gettaskkindregistry) | Retrieve the complete task kind registry. |
| [`apply`](#apply) | Create or update a workflow. |
| [`create`](#create) | Create a workflow. |
| [`update`](#update) | Update an existing workflow. |
| [`updateVisibility`](#updatevisibility) | Update the visibility of an existing workflow. |
| [`delete`](#delete) | Delete a workflow. |
| [`validateSpec`](#validatespec) | Validate a workflow spec without persisting it. |

### get

Get a single workflow by ID.

<SDKTabs>
<Tab value="TypeScript">
```typescript
const workflow = await stigmer.workflow.get(id);
```
</Tab>
<Tab value="Go">
```go
workflow, err := client.Workflow.Get(ctx, id)
```
</Tab>
<Tab value="Python">
```python
workflow = client.workflows.get(id)
```
</Tab>
<Tab value="Java">
```java
Workflow workflow = client.workflows().get(id);
```
</Tab>
</SDKTabs>

**Parameters**

| Parameter | Type | Description |
|-----------|------|-------------|
| `id` | `string` | Workflow identifier. |

**Returns**: [`Workflow`](#workflow)

### getByReference

Get a workflow by its organization-scoped reference (org/slug). Resolves a human-readable reference like "stigmer/deploy" to the full Workflow resource.

<SDKTabs>
<Tab value="TypeScript">
```typescript
const workflow = await stigmer.workflow.getByReference({
  org: "...",
  kind: "...",
  slug: "...",
  version: "...",
});
```
</Tab>
<Tab value="Go">
```go
workflow, err := client.Workflow.GetByReference(ctx, &stigmer.ApiResourceReference{
  Org:     "...",
  Kind:    "...",
  Slug:    "...",
  Version: "...",
})
```
</Tab>
<Tab value="Python">
```python
workflow = client.workflows.get_by_reference(ApiResourceReference(
    org="...",
    kind="...",
    slug="...",
    version="...",
))
```
</Tab>
<Tab value="Java">
```java
Workflow workflow = client.workflows().getByReference(ApiResourceReference.builder()
    .org("...")
    .kind("...")
    .slug("...")
    .version("...")
    .build());
```
</Tab>
</SDKTabs>

**Parameters**: [`ResourceRef`](#resourceref)

**Returns**: [`Workflow`](#workflow)

### getTaskKindRegistry

Retrieve the complete task kind registry.

Returns descriptors for all 20 workflow task kinds with full metadata: field schemas, JSON Schemas, categories, icons, examples, and output shapes.

Caching: responses are immutable within a platform version. Clients should cache aggressively (recommended: 1 hour TTL with stale-while-revalidate).

<SDKTabs>
<Tab value="TypeScript">
```typescript
const getTaskKindRegistryResponse = await stigmer.workflow.getTaskKindRegistry(input);
```
</Tab>
<Tab value="Go">
```go
getTaskKindRegistryResponse, err := client.Workflow.GetTaskKindRegistry(ctx, input)
```
</Tab>
<Tab value="Python">
```python
get_task_kind_registry_response = client.workflows.get_task_kind_registry(input)
```
</Tab>
<Tab value="Java">
```java
GetTaskKindRegistryResponse getTaskKindRegistryResponse = client.workflows().getTaskKindRegistry(input);
```
</Tab>
</SDKTabs>

**Parameters**: [`GetTaskKindRegistryRequest`](#gettaskkindregistryrequest)

**Returns**: [`GetTaskKindRegistryResponse`](#gettaskkindregistryresponse)

### apply

Create or update a workflow.

<SDKTabs>
<Tab value="TypeScript">
```typescript
const workflow = await stigmer.workflow.apply({
  name: "my-workflow",
  org: "acme",
  slug: "...",
  labels: {},
  description: "...",
  document: {},
  tasks: [],
  env: {},
  budget: {},
});
```
</Tab>
<Tab value="Go">
```go
workflow, err := client.Workflow.Apply(ctx, &stigmer.WorkflowInput{
  Name:        "my-workflow",
  Org:         "acme",
  Slug:        "...",
  Labels:      map[string]string{},
  Description: "...",
  Document:    &stigmer.WorkflowDocumentInput{},
  Tasks:       []stigmer.WorkflowTaskInput{},
  Env:         map[string]string{},
  Budget:      &stigmer.WorkflowBudgetInput{},
})
```
</Tab>
<Tab value="Python">
```python
workflow = client.workflows.apply(WorkflowInput(
    name="my-workflow",
    org="acme",
    slug="...",
    labels={},
    description="...",
    document=None,
    tasks=[],
    env={},
    budget=None,
))
```
</Tab>
<Tab value="Java">
```java
Workflow workflow = client.workflows().apply(WorkflowInput.builder()
    .name("my-workflow")
    .org("acme")
    .slug("...")
    .labels(Map.of())
    .description("...")
    .document(null)
    .tasks(List.of())
    .env(Map.of())
    .budget(null)
    .build());
```
</Tab>
</SDKTabs>

**Parameters**: [`WorkflowInput`](#workflowinput)

**Returns**: [`Workflow`](#workflow)

### create

Create a workflow.

<SDKTabs>
<Tab value="TypeScript">
```typescript
const workflow = await stigmer.workflow.create({
  name: "my-workflow",
  org: "acme",
  slug: "...",
  labels: {},
  description: "...",
  document: {},
  tasks: [],
  env: {},
  budget: {},
});
```
</Tab>
<Tab value="Go">
```go
workflow, err := client.Workflow.Create(ctx, &stigmer.WorkflowInput{
  Name:        "my-workflow",
  Org:         "acme",
  Slug:        "...",
  Labels:      map[string]string{},
  Description: "...",
  Document:    &stigmer.WorkflowDocumentInput{},
  Tasks:       []stigmer.WorkflowTaskInput{},
  Env:         map[string]string{},
  Budget:      &stigmer.WorkflowBudgetInput{},
})
```
</Tab>
<Tab value="Python">
```python
workflow = client.workflows.create(WorkflowInput(
    name="my-workflow",
    org="acme",
    slug="...",
    labels={},
    description="...",
    document=None,
    tasks=[],
    env={},
    budget=None,
))
```
</Tab>
<Tab value="Java">
```java
Workflow workflow = client.workflows().create(WorkflowInput.builder()
    .name("my-workflow")
    .org("acme")
    .slug("...")
    .labels(Map.of())
    .description("...")
    .document(null)
    .tasks(List.of())
    .env(Map.of())
    .budget(null)
    .build());
```
</Tab>
</SDKTabs>

**Parameters**: [`WorkflowInput`](#workflowinput)

**Returns**: [`Workflow`](#workflow)

### update

Update an existing workflow.

<SDKTabs>
<Tab value="TypeScript">
```typescript
const workflow = await stigmer.workflow.update({
  name: "my-workflow",
  org: "acme",
  slug: "...",
  labels: {},
  description: "...",
  document: {},
  tasks: [],
  env: {},
  budget: {},
});
```
</Tab>
<Tab value="Go">
```go
workflow, err := client.Workflow.Update(ctx, &stigmer.WorkflowInput{
  Name:        "my-workflow",
  Org:         "acme",
  Slug:        "...",
  Labels:      map[string]string{},
  Description: "...",
  Document:    &stigmer.WorkflowDocumentInput{},
  Tasks:       []stigmer.WorkflowTaskInput{},
  Env:         map[string]string{},
  Budget:      &stigmer.WorkflowBudgetInput{},
})
```
</Tab>
<Tab value="Python">
```python
workflow = client.workflows.update(WorkflowInput(
    name="my-workflow",
    org="acme",
    slug="...",
    labels={},
    description="...",
    document=None,
    tasks=[],
    env={},
    budget=None,
))
```
</Tab>
<Tab value="Java">
```java
Workflow workflow = client.workflows().update(WorkflowInput.builder()
    .name("my-workflow")
    .org("acme")
    .slug("...")
    .labels(Map.of())
    .description("...")
    .document(null)
    .tasks(List.of())
    .env(Map.of())
    .budget(null)
    .build());
```
</Tab>
</SDKTabs>

**Parameters**: [`WorkflowInput`](#workflowinput)

**Returns**: [`Workflow`](#workflow)

### updateVisibility

Update the visibility of an existing workflow.

This is a targeted metadata update — it only modifies metadata.visibility, leaving spec, status, and other metadata fields untouched. Use this to make a workflow publicly accessible or to revoke public access without sending the entire workflow resource (avoiding read-modify-write races).

<SDKTabs>
<Tab value="TypeScript">
```typescript
const workflow = await stigmer.workflow.updateVisibility({
  resourceId: "...",
  visibility: "...",
});
```
</Tab>
<Tab value="Go">
```go
workflow, err := client.Workflow.UpdateVisibility(ctx, &stigmer.UpdateVisibilityInput{
  ResourceId: "...",
  Visibility: "...",
})
```
</Tab>
<Tab value="Python">
```python
workflow = client.workflows.update_visibility(UpdateVisibilityInput(
    resource_id="...",
    visibility="...",
))
```
</Tab>
<Tab value="Java">
```java
Workflow workflow = client.workflows().updateVisibility(UpdateVisibilityInput.builder()
    .resourceId("...")
    .visibility("...")
    .build());
```
</Tab>
</SDKTabs>

**Parameters**: [`UpdateVisibilityInput`](#updatevisibilityinput)

**Returns**: [`Workflow`](#workflow)

### delete

Delete a workflow.

<SDKTabs>
<Tab value="TypeScript">
```typescript
const workflow = await stigmer.workflow.delete(id);
```
</Tab>
<Tab value="Go">
```go
workflow, err := client.Workflow.Delete(ctx, id)
```
</Tab>
<Tab value="Python">
```python
workflow = client.workflows.delete(id)
```
</Tab>
<Tab value="Java">
```java
Workflow workflow = client.workflows().delete(id);
```
</Tab>
</SDKTabs>

**Parameters**

| Parameter | Type | Description |
|-----------|------|-------------|
| `id` | `string` | Workflow identifier. |

**Returns**: [`Workflow`](#workflow)

### validateSpec

Validate a workflow spec without persisting it.

Runs the same two-layer validation pipeline used by create/update: Layer 1: Proto field constraints (buf validate / protovalidate) Layer 2: Temporal-based structural validation (Go activity: proto → YAML → Zigflow)

Returns ServerlessWorkflowValidation with: - VALID: Workflow structure passed all checks - INVALID: User error (bad structure, missing fields, unknown task kinds) - FAILED: System error (Temporal unavailable, converter crash)

This RPC does NOT persist, authorize, or create instances. It is a pure validation endpoint suitable for iterative authoring workflows where the caller needs fast feedback before committing.

<SDKTabs>
<Tab value="TypeScript">
```typescript
const serverlessWorkflowValidation = await stigmer.workflow.validateSpec({
  name: "my-workflow",
  org: "acme",
  slug: "...",
  labels: {},
  description: "...",
  document: {},
  tasks: [],
  env: {},
  budget: {},
});
```
</Tab>
<Tab value="Go">
```go
serverlessWorkflowValidation, err := client.Workflow.ValidateSpec(ctx, &stigmer.WorkflowInput{
  Name:        "my-workflow",
  Org:         "acme",
  Slug:        "...",
  Labels:      map[string]string{},
  Description: "...",
  Document:    &stigmer.WorkflowDocumentInput{},
  Tasks:       []stigmer.WorkflowTaskInput{},
  Env:         map[string]string{},
  Budget:      &stigmer.WorkflowBudgetInput{},
})
```
</Tab>
<Tab value="Python">
```python
serverless_workflow_validation = client.workflows.validate_spec(WorkflowInput(
    name="my-workflow",
    org="acme",
    slug="...",
    labels={},
    description="...",
    document=None,
    tasks=[],
    env={},
    budget=None,
))
```
</Tab>
<Tab value="Java">
```java
Workflow serverlessWorkflowValidation = client.workflows().validateSpec(WorkflowInput.builder()
    .name("my-workflow")
    .org("acme")
    .slug("...")
    .labels(Map.of())
    .description("...")
    .document(null)
    .tasks(List.of())
    .env(Map.of())
    .budget(null)
    .build());
```
</Tab>
</SDKTabs>

**Parameters**: [`WorkflowInput`](#workflowinput)

**Returns**: [`ServerlessWorkflowValidation`](#serverlessworkflowvalidation)

## Types

### WorkflowInput

Input for creating or updating a Workflow.

<TypeTable
  type={{
    name: { type: "string", description: "Resource name.", required: true },
    slug: { type: "string", description: "URL-friendly identifier." },
    org: { type: "string", description: "Organization slug.", required: true },
    labels: { type: "Record<string, string>", description: "Key-value labels." },
    description: { type: "string", description: "Human-readable description for UI and marketplace display." },
    document: { type: "WorkflowDocumentInput", description: "Workflow document metadata including DSL version, namespace, name, and version.", typeDescriptionLink: "#workflowdocumentinput", required: true },
    tasks: { type: "WorkflowTaskInput[]", description: "Ordered list of tasks that make up this workflow.", typeDescriptionLink: "#workflowtaskinput" },
    env: { type: "Record<string, EnvVarDeclarationInput>", description: "Environment variable declarations for this workflow.", typeDescriptionLink: "#envvardeclarationinput" },
    budget: { type: "WorkflowBudgetInput", description: "Budget limits for this workflow execution.", typeDescriptionLink: "#workflowbudgetinput" },
  }}
/>

### WorkflowDocumentInput

WorkflowDocument contains workflow-level metadata for versioning and identification.

<TypeTable
  type={{
    dsl: { type: "string", description: "Workflow DSL version (semver)." },
    namespace: { type: "string", description: "Workflow namespace (organization/categorization).", required: true },
    name: { type: "string", description: "Workflow name (unique identifier within namespace).", required: true },
    version: { type: "string", description: "Workflow version (semver).", required: true },
    description: { type: "string", description: "Human-readable description." },
  }}
/>

### WorkflowTaskInput

WorkflowTask represents a single executable step in a workflow.

<TypeTable
  type={{
    name: { type: "string", description: "Task name/identifier (must be unique within workflow)." },
    kind: { type: "WorkflowTaskKind", description: "Task type (determines how to interpret task_config).", typeDescriptionLink: "#workflowtaskkind", required: true },
    taskConfig: { type: "object", description: "Task-specific configuration whose structure depends on the `kind` field.", required: true },
    export: { type: "ExportInput", description: "Export configuration (how to save task output to context).", typeDescriptionLink: "#exportinput" },
    flow: { type: "FlowControlInput", description: "Flow control (which task executes next).", typeDescriptionLink: "#flowcontrolinput" },
    compensate: { type: "WorkflowTaskInput[]", description: "Compensation tasks to execute if this task needs to be \"undone.\".", typeDescriptionLink: "#workflowtaskinput" },
  }}
/>

### ExportInput

Export defines how task output is saved to the workflow context.

<TypeTable
  type={{
    as: { type: "string", description: "Expression defining how to export output using ${...} syntax." },
  }}
/>

### FlowControlInput

FlowControl defines which task executes next after the current task completes.

<TypeTable
  type={{
    then: { type: "string", description: "Target task name or \"end\" to terminate workflow." },
  }}
/>

### EnvVarDeclarationInput

EnvVarDeclaration declares an environment variable required (or optionally accepted) by a blueprint resource (McpServer, Agent, Workflow).

<TypeTable
  type={{
    isSecret: { type: "boolean", description: "Whether the resolved value should be treated as a secret." },
    description: { type: "string", description: "Human-readable description shown in the UI credential form." },
    optional: { type: "boolean", description: "Whether this variable is optional." },
  }}
/>

### WorkflowBudgetInput

WorkflowBudget declares cost, token, and duration limits for a workflow execution.

<TypeTable
  type={{
    maxCostMicros: { type: "number", description: "Maximum total cost for this workflow execution in micro-USD." },
    maxTotalTokens: { type: "number", description: "Maximum total tokens (input + output) across all LLM/agent tasks." },
    maxDurationSeconds: { type: "number", description: "Maximum wall-clock duration for the entire workflow execution in seconds." },
    onExceeded: { type: "BudgetExceededPolicy", description: "Policy when any budget limit is exceeded.", typeDescriptionLink: "#budgetexceededpolicy" },
  }}
/>

### UpdateVisibilityInput

Input for updating the visibility of any API resource.

<TypeTable
  type={{
    resourceId: { type: "string", description: "ID of the resource whose visibility is being updated.", required: true },
    visibility: { type: "ApiResourceVisibility", description: "The new visibility setting for the resource.", typeDescriptionLink: "/docs/sdk/resources/commons#apiresourcevisibility" },
  }}
/>

### ServerlessWorkflowValidation

ServerlessWorkflowValidation contains the generated Serverless Workflow YAML and its validation state.

<TypeTable
  type={{
    state: { type: "ValidationState", description: "Current validation state.", typeDescriptionLink: "#validationstate" },
    yaml: { type: "string", description: "Generated CNCF Serverless Workflow DSL 1.0.0 YAML." },
    errors: { type: "string[]", description: "Validation errors, empty when valid." },
    warnings: { type: "string[]", description: "Non-fatal validation warnings." },
    validatedAt: { type: "string", description: "When the validation was performed." },
    validationWorkflowId: { type: "string", description: "Validation process ID for tracking validation progress." },
  }}
/>

### ResourceRef

Generic reference to any API resource by org and slug.

<TypeTable
  type={{
    org: { type: "string", description: "Organization that owns the referenced resource." },
    kind: { type: "ApiResourceKind", description: "Kind of the referenced resource (e.g., SKILL, AGENT, MCP_SERVER).", typeDescriptionLink: "/docs/sdk/resources/commons#apiresourcekind" },
    slug: { type: "string", description: "Resource slug (user-friendly identifier, unique within org).", required: true },
    version: { type: "string", description: "Version of the resource (optional, only applicable to versioned resources like Skills)." },
  }}
/>

### GetTaskKindRegistryRequest

GetTaskKindRegistryRequest is the request message for retrieving the complete task kind registry.

### GetTaskKindRegistryResponse

GetTaskKindRegistryResponse returns the full set of task kind descriptors.

<TypeTable
  type={{
    descriptors: { type: "TaskKindDescriptorInput[]", description: "All registered task kind descriptors, one per WorkflowTaskKind value." },
  }}
/>

### Workflow

Workflow defines a multi-step task orchestration with sequential, parallel, and conditional execution.

<TypeTable
  type={{
    apiVersion: { type: "string", description: "API version for this resource type." },
    kind: { type: "string", description: "Resource kind identifier." },
    metadata: { type: "ApiResourceMetadata", description: "Resource metadata including id, name, org, slug, labels, visibility, and timestamps.", typeDescriptionLink: "/docs/sdk/resources/commons#apiresourcemetadata" },
    spec: { type: "WorkflowInput", description: "Resource specification. See [`WorkflowInput`](#workflowinput).", typeDescriptionLink: "#workflowinput" },
    status: { type: "WorkflowStatus", description: "System-managed state. See [`WorkflowStatus`](#workflowstatus).", typeDescriptionLink: "#workflowstatus" },
  }}
/>

### WorkflowStatus

WorkflowStatus contains system-managed state for a Workflow resource.

<TypeTable
  type={{
    audit: { type: "ApiResourceAudit", description: "Standard audit information (created_at, updated_at, created_by, etc.).", typeDescriptionLink: "/docs/sdk/resources/commons#apiresourceaudit" },
    defaultInstanceId: { type: "string", description: "ID of the default workflow instance created automatically for this workflow." },
    serverlessWorkflowValidation: { type: "ServerlessWorkflowValidation", description: "Serverless Workflow YAML generation and validation state.", typeDescriptionLink: "#serverlessworkflowvalidation" },
  }}
/>

### ServerlessWorkflowValidation

ServerlessWorkflowValidation contains the generated Serverless Workflow YAML and its validation state.

<TypeTable
  type={{
    state: { type: "ValidationState", description: "Current validation state.", typeDescriptionLink: "#validationstate" },
    yaml: { type: "string", description: "Generated CNCF Serverless Workflow DSL 1.0.0 YAML." },
    errors: { type: "string[]", description: "Validation errors, empty when valid." },
    warnings: { type: "string[]", description: "Non-fatal validation warnings." },
    validatedAt: { type: "string", description: "When the validation was performed." },
    validationWorkflowId: { type: "string", description: "Validation process ID for tracking validation progress." },
  }}
/>

### ValidationState

ValidationState represents the current state of serverless workflow validation.

| Value | Description |
|-------|-------------|
| `PENDING` | Validation is pending and has not yet completed. |
| `VALID` | Validation completed successfully and the workflow structure is valid. |
| `INVALID` | Validation completed but found errors in the workflow structure. |
| `FAILED` | Validation process failed due to a system error (not a user error). |

### WorkflowTaskKind

WorkflowTaskKind defines the supported task types in a workflow.

| Value | Description |
|-------|-------------|
| `set_vars` | Set variables in workflow state. |
| `http_call` | Make an HTTP request (GET, POST, PUT, DELETE, PATCH). |
| `grpc_call` | Make a gRPC request to an external service. |
| `activity_call` | Execute an activity. |
| `switch_case` | Branch conditionally based on expressions. |
| `for_each` | Iterate over a collection, executing tasks for each item. |
| `fork` | Execute multiple branches in parallel. |
| `try_catch` | Handle errors with try/catch logic. |
| `listen` | Wait for external signals or events. |
| `wait` | Pause execution for a duration or until a timestamp. |
| `raise_error` | Raise an error to terminate or trigger error handling. |
| `run_workflow` | Execute a sub-workflow. |
| `agent_call` | Invoke an AI agent as a workflow task. |
| `llm_call` | Direct LLM call for classification, extraction, scoring, or routing. |
| `transform` | Deterministic data transformation using JQ, JSONata, or template engines. |
| `human_input` | Workflow-level approval gate for human input, review, or sign-off. |
| `validate` | Schema and business-rule validation checkpoint. |
| `emit_event` | Emit a CloudEvents-formatted event for external consumers or other workflows. |
| `notification` | Send a notification to humans through a channel (Slack, email, Discord, etc.). |
| `eval` | LLM-as-a-judge evaluation for semantic quality assessment. |

### BudgetExceededPolicy

BudgetExceededPolicy defines the runtime behavior when a workflow or per-task budget limit is exceeded.

| Value | Description |
|-------|-------------|
| `budget_exceeded_terminate` | Terminate the workflow immediately with EXECUTION_FAILED status. |
| `budget_exceeded_human_review` | Pause the workflow and request human review via a system-generated approval gate. |
| `budget_exceeded_warn` | Log a warning but allow the workflow to continue executing. |

### TaskKindCategory

TaskKindCategory classifies task kinds by their functional role in a workflow.

| Value | Description |
|-------|-------------|
| `TASK_KIND_CATEGORY_CONTROL_FLOW` | Control flow tasks that direct execution order. |
| `TASK_KIND_CATEGORY_INVOCATION` | Tasks that invoke external services or sub-workflows. |
| `TASK_KIND_CATEGORY_AI` | AI-native tasks that invoke language models or agents. |
| `TASK_KIND_CATEGORY_DATA` | Tasks that manipulate, validate, or transform data. |
| `TASK_KIND_CATEGORY_GOVERNANCE` | Tasks that require human interaction to proceed. |
| `TASK_KIND_CATEGORY_EVENT` | Tasks that emit, listen for, or route events and notifications. |

### TaskFieldType

TaskFieldType represents the data type of a task configuration field as understood by UI form renderers and schema validators.

| Value | Description |
|-------|-------------|
| `TASK_FIELD_TYPE_STRING` | Plain string value. |
| `TASK_FIELD_TYPE_INT32` | 32-bit integer. |
| `TASK_FIELD_TYPE_FLOAT` | 32-bit floating point. |
| `TASK_FIELD_TYPE_BOOL` | Boolean true/false. |
| `TASK_FIELD_TYPE_ENUM` | Enumeration — valid values listed in TaskFieldDescriptor.enum_values. |
| `TASK_FIELD_TYPE_STRUCT` | Structured JSON object (google.protobuf.Struct in proto). |
| `TASK_FIELD_TYPE_REPEATED` | Repeated (array) of a scalar or message type. |
| `TASK_FIELD_TYPE_MAP` | Map from string keys to string values. |
| `TASK_FIELD_TYPE_MESSAGE` | Nested message type — field contains a sub-object with its own fields. |

