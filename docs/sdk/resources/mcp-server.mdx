---
title: MCP Server
---

{/* Auto-generated by stigmer-codegen. DO NOT EDIT. */}

An MCP Server defines a reusable tool provider that agents can connect to via the
Model Context Protocol. It declares the server type (stdio or HTTP), connection
details, and required environment variables. Tool approval policies are
auto-classified when you connect, and manual overrides go in
`pinned_tool_approvals`.

```yaml
apiVersion: agentic.stigmer.ai/v1
kind: McpServer
metadata:
  name: github
  slug: github
spec:
  description: "GitHub MCP server for repository operations"
  stdio:
    command: npx
    args: ["-y", "@modelcontextprotocol/server-github"]
  env_spec:
    data:
      GITHUB_TOKEN:
        is_secret: true
        description: "GitHub personal access token"
  pinned_tool_approvals:
    - tool_name: delete_repository
      message: "Delete repository: {{args.repo}}"
```

## Client Access

<SDKTabs>
<Tab value="TypeScript">
```typescript
const mcpServer = await stigmer.mcpServer.get("mcp_01j5q3k7m8r2s4tnz2hfp0q0c3");
```
</Tab>
<Tab value="Go">
```go
mcpServer, err := client.McpServer.Get(ctx, "mcp_01j5q3k7m8r2s4tnz2hfp0q0c3")
```
</Tab>
<Tab value="Python">
```python
mcp_server = client.mcp_servers.get("mcp_01j5q3k7m8r2s4tnz2hfp0q0c3")
```
</Tab>
<Tab value="Java">
```java
McpServer mcpServer = client.mcpServers().get("mcp_01j5q3k7m8r2s4tnz2hfp0q0c3");
```
</Tab>
</SDKTabs>

## Methods

| Method | Description |
|--------|-------------|
| [`get`](#get) | Get an MCP server by its unique identifier. |
| [`getByReference`](#getbyreference) | Get an MCP server by reference (scope + org + slug). |
| [`getOAuthGrantStatus`](#getoauthgrantstatus) | Check whether the authenticated user has an active OAuth grant for an MCP server in the specified org. |
| [`getOrgOAuthApp`](#getorgoauthapp) | Query whether an org has a BYOA override for a resource. |
| [`apply`](#apply) | Create or update an MCP server resource. |
| [`create`](#create) | Create an MCP server resource. |
| [`update`](#update) | Update an existing MCP server resource. |
| [`updateVisibility`](#updatevisibility) | Update the visibility of an existing MCP server. |
| [`delete`](#delete) | Delete an MCP server resource. |
| [`completeOAuthConnect`](#completeoauthconnect) | Complete the OAuth authorization flow by exchanging the authorization code for tokens. |
| [`connect`](#connect) | Connect to an MCP server: discover its tools and classify approval policies. |
| [`deleteOrgOAuthApp`](#deleteorgoauthapp) | Remove an org-level BYOA override for a resource. |
| [`disconnectOAuth`](#disconnectoauth) | Disconnect the authenticated user's OAuth connection for a resource. |
| [`initiateOAuthConnect`](#initiateoauthconnect) | Start the OAuth authorization flow for an MCP server. |
| [`setOrgOAuthApp`](#setorgoauthapp) | Create or update an org-level BYOA OAuth app override for a resource. |

### get

Get an MCP server by its unique identifier.

<SDKTabs>
<Tab value="TypeScript">
```typescript
const mcpServer = await stigmer.mcpServer.get(id);
```
</Tab>
<Tab value="Go">
```go
mcpServer, err := client.McpServer.Get(ctx, id)
```
</Tab>
<Tab value="Python">
```python
mcp_server = client.mcp_servers.get(id)
```
</Tab>
<Tab value="Java">
```java
McpServer mcpServer = client.mcpServers().get(id);
```
</Tab>
</SDKTabs>

**Parameters**

| Parameter | Type | Description |
|-----------|------|-------------|
| `id` | `string` | MCP Server identifier. |

**Returns**: [`McpServer`](#mcpserver)

### getByReference

Get an MCP server by reference (scope + org + slug).

Preferred method for looking up MCP servers by name/slug rather than system-generated ID.

<SDKTabs>
<Tab value="TypeScript">
```typescript
const mcpServer = await stigmer.mcpServer.getByReference({
  org: "...",
  kind: "...",
  slug: "...",
  version: "...",
});
```
</Tab>
<Tab value="Go">
```go
mcpServer, err := client.McpServer.GetByReference(ctx, &stigmer.ApiResourceReference{
  Org:     "...",
  Kind:    "...",
  Slug:    "...",
  Version: "...",
})
```
</Tab>
<Tab value="Python">
```python
mcp_server = client.mcp_servers.get_by_reference(ApiResourceReference(
    org="...",
    kind="...",
    slug="...",
    version="...",
))
```
</Tab>
<Tab value="Java">
```java
McpServer mcpServer = client.mcpServers().getByReference(ApiResourceReference.builder()
    .org("...")
    .kind("...")
    .slug("...")
    .version("...")
    .build());
```
</Tab>
</SDKTabs>

**Parameters**: [`ResourceRef`](#resourceref)

**Returns**: [`McpServer`](#mcpserver)

### getOAuthGrantStatus

Check whether the authenticated user has an active OAuth grant for an MCP server in the specified org.

Returns grant metadata (connected status, token expiry, auth method) without exposing any secret token values. The frontend uses this to render the correct OAuth state in the MCP server detail page and session composer.

<SDKTabs>
<Tab value="TypeScript">
```typescript
const getOAuthGrantStatusOutput = await stigmer.mcpServer.getOAuthGrantStatus({
  resourceId: "...",
  org: "...",
});
```
</Tab>
<Tab value="Go">
```go
getOAuthGrantStatusOutput, err := client.McpServer.GetOAuthGrantStatus(ctx, &stigmer.GetOAuthGrantStatusInput{
  ResourceId: "...",
  Org:        "...",
})
```
</Tab>
<Tab value="Python">
```python
get_o_auth_grant_status_output = client.mcp_servers.get_o_auth_grant_status(GetOAuthGrantStatusInput(
    resource_id="...",
    org="...",
))
```
</Tab>
<Tab value="Java">
```java
GetOAuthGrantStatusOutput getOAuthGrantStatusOutput = client.mcpServers().getOAuthGrantStatus(GetOAuthGrantStatusInput.builder()
    .resourceId("...")
    .org("...")
    .build());
```
</Tab>
</SDKTabs>

**Parameters**: [`GetOAuthGrantStatusInput`](#getoauthgrantstatusinput)

**Returns**: [`GetOAuthGrantStatusOutput`](#getoauthgrantstatusoutput)

### getOrgOAuthApp

Query whether an org has a BYOA override for a resource.

Returns override metadata (existence, OAuthApp ID, client_id) without exposing secrets. The frontend uses this to show which credential source is active and to offer override management options to org admins.

<SDKTabs>
<Tab value="TypeScript">
```typescript
const getOrgOAuthAppOutput = await stigmer.mcpServer.getOrgOAuthApp({
  resourceId: "...",
  org: "...",
});
```
</Tab>
<Tab value="Go">
```go
getOrgOAuthAppOutput, err := client.McpServer.GetOrgOAuthApp(ctx, &stigmer.GetOrgOAuthAppInput{
  ResourceId: "...",
  Org:        "...",
})
```
</Tab>
<Tab value="Python">
```python
get_org_o_auth_app_output = client.mcp_servers.get_org_o_auth_app(GetOrgOAuthAppInput(
    resource_id="...",
    org="...",
))
```
</Tab>
<Tab value="Java">
```java
GetOrgOAuthAppOutput getOrgOAuthAppOutput = client.mcpServers().getOrgOAuthApp(GetOrgOAuthAppInput.builder()
    .resourceId("...")
    .org("...")
    .build());
```
</Tab>
</SDKTabs>

**Parameters**: [`GetOrgOAuthAppInput`](#getorgoauthappinput)

**Returns**: [`GetOrgOAuthAppOutput`](#getorgoauthappoutput)

### apply

Create or update an MCP server resource.

If the resource doesn't exist, creates it. If it exists, updates it. The resource is identified by its (scope, org, slug) combination.

<SDKTabs>
<Tab value="TypeScript">
```typescript
const mcpServer = await stigmer.mcpServer.apply({
  name: "my-mcp-server",
  org: "acme",
  slug: "...",
  labels: {},
  description: "...",
  iconUrl: "...",
  stdio: {},
  http: {},
  defaultEnabledTools: [],
  env: {},
  pinnedToolApprovals: [],
  repositoryUrl: "...",
  githubStars: 0,
  auth: {},
});
```
</Tab>
<Tab value="Go">
```go
mcpServer, err := client.McpServer.Apply(ctx, &stigmer.McpServerInput{
  Name:                "my-mcp-server",
  Org:                 "acme",
  Slug:                "...",
  Labels:              map[string]string{},
  Description:         "...",
  IconUrl:             "...",
  Stdio:               &stigmer.StdioServerConfigInput{},
  Http:                &stigmer.HttpServerConfigInput{},
  DefaultEnabledTools: []string{},
  Env:                 map[string]string{},
  PinnedToolApprovals: []stigmer.ToolApprovalPolicyInput{},
  RepositoryUrl:       "...",
  GithubStars:         0,
  Auth:                &stigmer.McpServerAuthInput{},
})
```
</Tab>
<Tab value="Python">
```python
mcp_server = client.mcp_servers.apply(McpServerInput(
    name="my-mcp-server",
    org="acme",
    slug="...",
    labels={},
    description="...",
    icon_url="...",
    stdio=None,
    http=None,
    default_enabled_tools=[],
    env={},
    pinned_tool_approvals=[],
    repository_url="...",
    github_stars=0,
    auth=None,
))
```
</Tab>
<Tab value="Java">
```java
McpServer mcpServer = client.mcpServers().apply(McpServerInput.builder()
    .name("my-mcp-server")
    .org("acme")
    .slug("...")
    .labels(Map.of())
    .description("...")
    .iconUrl("...")
    .stdio(null)
    .http(null)
    .defaultEnabledTools(List.of())
    .env(Map.of())
    .pinnedToolApprovals(List.of())
    .repositoryUrl("...")
    .githubStars(0)
    .auth(null)
    .build());
```
</Tab>
</SDKTabs>

**Parameters**: [`McpServerInput`](#mcpserverinput)

**Returns**: [`McpServer`](#mcpserver)

### create

Create an MCP server resource.

Returns an error if a resource with the same (scope, org, slug) already exists. Use `apply` for idempotent create-or-update semantics.

<SDKTabs>
<Tab value="TypeScript">
```typescript
const mcpServer = await stigmer.mcpServer.create({
  name: "my-mcp-server",
  org: "acme",
  slug: "...",
  labels: {},
  description: "...",
  iconUrl: "...",
  stdio: {},
  http: {},
  defaultEnabledTools: [],
  env: {},
  pinnedToolApprovals: [],
  repositoryUrl: "...",
  githubStars: 0,
  auth: {},
});
```
</Tab>
<Tab value="Go">
```go
mcpServer, err := client.McpServer.Create(ctx, &stigmer.McpServerInput{
  Name:                "my-mcp-server",
  Org:                 "acme",
  Slug:                "...",
  Labels:              map[string]string{},
  Description:         "...",
  IconUrl:             "...",
  Stdio:               &stigmer.StdioServerConfigInput{},
  Http:                &stigmer.HttpServerConfigInput{},
  DefaultEnabledTools: []string{},
  Env:                 map[string]string{},
  PinnedToolApprovals: []stigmer.ToolApprovalPolicyInput{},
  RepositoryUrl:       "...",
  GithubStars:         0,
  Auth:                &stigmer.McpServerAuthInput{},
})
```
</Tab>
<Tab value="Python">
```python
mcp_server = client.mcp_servers.create(McpServerInput(
    name="my-mcp-server",
    org="acme",
    slug="...",
    labels={},
    description="...",
    icon_url="...",
    stdio=None,
    http=None,
    default_enabled_tools=[],
    env={},
    pinned_tool_approvals=[],
    repository_url="...",
    github_stars=0,
    auth=None,
))
```
</Tab>
<Tab value="Java">
```java
McpServer mcpServer = client.mcpServers().create(McpServerInput.builder()
    .name("my-mcp-server")
    .org("acme")
    .slug("...")
    .labels(Map.of())
    .description("...")
    .iconUrl("...")
    .stdio(null)
    .http(null)
    .defaultEnabledTools(List.of())
    .env(Map.of())
    .pinnedToolApprovals(List.of())
    .repositoryUrl("...")
    .githubStars(0)
    .auth(null)
    .build());
```
</Tab>
</SDKTabs>

**Parameters**: [`McpServerInput`](#mcpserverinput)

**Returns**: [`McpServer`](#mcpserver)

### update

Update an existing MCP server resource.

<SDKTabs>
<Tab value="TypeScript">
```typescript
const mcpServer = await stigmer.mcpServer.update({
  name: "my-mcp-server",
  org: "acme",
  slug: "...",
  labels: {},
  description: "...",
  iconUrl: "...",
  stdio: {},
  http: {},
  defaultEnabledTools: [],
  env: {},
  pinnedToolApprovals: [],
  repositoryUrl: "...",
  githubStars: 0,
  auth: {},
});
```
</Tab>
<Tab value="Go">
```go
mcpServer, err := client.McpServer.Update(ctx, &stigmer.McpServerInput{
  Name:                "my-mcp-server",
  Org:                 "acme",
  Slug:                "...",
  Labels:              map[string]string{},
  Description:         "...",
  IconUrl:             "...",
  Stdio:               &stigmer.StdioServerConfigInput{},
  Http:                &stigmer.HttpServerConfigInput{},
  DefaultEnabledTools: []string{},
  Env:                 map[string]string{},
  PinnedToolApprovals: []stigmer.ToolApprovalPolicyInput{},
  RepositoryUrl:       "...",
  GithubStars:         0,
  Auth:                &stigmer.McpServerAuthInput{},
})
```
</Tab>
<Tab value="Python">
```python
mcp_server = client.mcp_servers.update(McpServerInput(
    name="my-mcp-server",
    org="acme",
    slug="...",
    labels={},
    description="...",
    icon_url="...",
    stdio=None,
    http=None,
    default_enabled_tools=[],
    env={},
    pinned_tool_approvals=[],
    repository_url="...",
    github_stars=0,
    auth=None,
))
```
</Tab>
<Tab value="Java">
```java
McpServer mcpServer = client.mcpServers().update(McpServerInput.builder()
    .name("my-mcp-server")
    .org("acme")
    .slug("...")
    .labels(Map.of())
    .description("...")
    .iconUrl("...")
    .stdio(null)
    .http(null)
    .defaultEnabledTools(List.of())
    .env(Map.of())
    .pinnedToolApprovals(List.of())
    .repositoryUrl("...")
    .githubStars(0)
    .auth(null)
    .build());
```
</Tab>
</SDKTabs>

**Parameters**: [`McpServerInput`](#mcpserverinput)

**Returns**: [`McpServer`](#mcpserver)

### updateVisibility

Update the visibility of an existing MCP server.

Only modifies metadata.visibility, leaving spec, status, and other metadata fields untouched.

<SDKTabs>
<Tab value="TypeScript">
```typescript
const mcpServer = await stigmer.mcpServer.updateVisibility({
  resourceId: "...",
  visibility: "...",
});
```
</Tab>
<Tab value="Go">
```go
mcpServer, err := client.McpServer.UpdateVisibility(ctx, &stigmer.UpdateVisibilityInput{
  ResourceId: "...",
  Visibility: "...",
})
```
</Tab>
<Tab value="Python">
```python
mcp_server = client.mcp_servers.update_visibility(UpdateVisibilityInput(
    resource_id="...",
    visibility="...",
))
```
</Tab>
<Tab value="Java">
```java
McpServer mcpServer = client.mcpServers().updateVisibility(UpdateVisibilityInput.builder()
    .resourceId("...")
    .visibility("...")
    .build());
```
</Tab>
</SDKTabs>

**Parameters**: [`UpdateVisibilityInput`](#updatevisibilityinput)

**Returns**: [`McpServer`](#mcpserver)

### delete

Delete an MCP server resource.

Permanently removes the MCP server definition. Agents referencing this server will need to be updated.

<SDKTabs>
<Tab value="TypeScript">
```typescript
const mcpServer = await stigmer.mcpServer.delete({ resourceId: id });
```
</Tab>
<Tab value="Go">
```go
mcpServer, err := client.McpServer.Delete(ctx, &stigmer.DeleteResourceInput{
  ResourceID: id,
})
```
</Tab>
<Tab value="Python">
```python
mcp_server = client.mcp_servers.delete(DeleteResourceInput(resource_id=id))
```
</Tab>
<Tab value="Java">
```java
McpServer mcpServer = client.mcpServers().delete(DeleteResourceInput.builder()
    .resourceId(id)
    .build());
```
</Tab>
</SDKTabs>

**Parameters**

| Parameter | Type | Description |
|-----------|------|-------------|
| `resourceId` | `string` | ID of the resource to delete. |
| `versionMessage` | `string` | Optional deletion reason. |
| `force` | `boolean` | Force delete even if resource is in use. |

**Returns**: [`McpServer`](#mcpserver)

### completeOAuthConnect

Complete the OAuth authorization flow by exchanging the authorization code for tokens.

Called by the frontend after the user is redirected back from the OAuth authorization server. Exchanges the code for tokens, stores them in the user's personal environment, and creates an OAuthGrant record for pre-flight expiry checks.

After success, the frontend should call connect() to trigger tool discovery using the freshly acquired token.

<SDKTabs>
<Tab value="TypeScript">
```typescript
const completeOAuthConnectOutput = await stigmer.mcpServer.completeOAuthConnect({
  mcpServerId: "...",
  authorizationCode: "...",
  state: "...",
});
```
</Tab>
<Tab value="Go">
```go
completeOAuthConnectOutput, err := client.McpServer.CompleteOAuthConnect(ctx, &stigmer.CompleteOAuthConnectInput{
  McpServerId:       "...",
  AuthorizationCode: "...",
  State:             "...",
})
```
</Tab>
<Tab value="Python">
```python
complete_o_auth_connect_output = client.mcp_servers.complete_o_auth_connect(CompleteOAuthConnectInput(
    mcp_server_id="...",
    authorization_code="...",
    state="...",
))
```
</Tab>
<Tab value="Java">
```java
CompleteOAuthConnectOutput completeOAuthConnectOutput = client.mcpServers().completeOAuthConnect(CompleteOAuthConnectInput.builder()
    .mcpServerId("...")
    .authorizationCode("...")
    .state("...")
    .build());
```
</Tab>
</SDKTabs>

**Parameters**: [`CompleteOAuthConnectInput`](#completeoauthconnectinput)

**Returns**: [`CompleteOAuthConnectOutput`](#completeoauthconnectoutput)

### connect

Connect to an MCP server: discover its tools and classify approval policies.

Connects to the MCP server, enumerates tools and resource templates, classifies tool approval policies via a lightweight LLM, and stores the results in status.discovered_capabilities and status.tool_approvals. Blocks until completion (up to ~30 seconds) and returns the updated McpServer.

<SDKTabs>
<Tab value="TypeScript">
```typescript
const mcpServer = await stigmer.mcpServer.connect({
  mcpServerId: "...",
  runtimeEnv: {},
  org: "...",
});
```
</Tab>
<Tab value="Go">
```go
mcpServer, err := client.McpServer.Connect(ctx, &stigmer.ConnectInput{
  McpServerId: "...",
  RuntimeEnv:  map[string]string{},
  Org:         "...",
})
```
</Tab>
<Tab value="Python">
```python
mcp_server = client.mcp_servers.connect(ConnectInput(
    mcp_server_id="...",
    runtime_env={},
    org="...",
))
```
</Tab>
<Tab value="Java">
```java
McpServer mcpServer = client.mcpServers().connect(ConnectInput.builder()
    .mcpServerId("...")
    .runtimeEnv(Map.of())
    .org("...")
    .build());
```
</Tab>
</SDKTabs>

**Parameters**: [`ConnectInput`](#connectinput)

**Returns**: [`McpServer`](#mcpserver)

### deleteOrgOAuthApp

Remove an org-level BYOA override for a resource.

Deletes the OAuthAppOverride binding and the OAuthApp resource that was created for it. After this, the resolution chain falls back to the platform default.

Existing user OAuthGrants that were issued using the org's OAuthApp will fail on next token refresh — those users will need to re-authenticate using the platform default or a new org override.

<SDKTabs>
<Tab value="TypeScript">
```typescript
const deleteOrgOAuthAppOutput = await stigmer.mcpServer.deleteOrgOAuthApp({
  resourceId: "...",
  org: "...",
});
```
</Tab>
<Tab value="Go">
```go
deleteOrgOAuthAppOutput, err := client.McpServer.DeleteOrgOAuthApp(ctx, &stigmer.DeleteOrgOAuthAppInput{
  ResourceId: "...",
  Org:        "...",
})
```
</Tab>
<Tab value="Python">
```python
delete_org_o_auth_app_output = client.mcp_servers.delete_org_o_auth_app(DeleteOrgOAuthAppInput(
    resource_id="...",
    org="...",
))
```
</Tab>
<Tab value="Java">
```java
DeleteOrgOAuthAppOutput deleteOrgOAuthAppOutput = client.mcpServers().deleteOrgOAuthApp(DeleteOrgOAuthAppInput.builder()
    .resourceId("...")
    .org("...")
    .build());
```
</Tab>
</SDKTabs>

**Parameters**: [`DeleteOrgOAuthAppInput`](#deleteorgoauthappinput)

**Returns**: [`DeleteOrgOAuthAppOutput`](#deleteorgoauthappoutput)

### disconnectOAuth

Disconnect the authenticated user's OAuth connection for a resource.

Tears down the user's personal OAuth connection by deleting the OAuthGrant and its associated managed environment (which holds the access and refresh tokens). The MCP server definition is unchanged — only the caller's credentials are removed.

Other users' connections to the same resource are unaffected.

Idempotent: returns disconnected=true when a grant was deleted, disconnected=false when no grant existed. Never returns an error for a missing grant.

<SDKTabs>
<Tab value="TypeScript">
```typescript
const disconnectOAuthOutput = await stigmer.mcpServer.disconnectOAuth({
  resourceId: "...",
  org: "...",
});
```
</Tab>
<Tab value="Go">
```go
disconnectOAuthOutput, err := client.McpServer.DisconnectOAuth(ctx, &stigmer.DisconnectOAuthInput{
  ResourceId: "...",
  Org:        "...",
})
```
</Tab>
<Tab value="Python">
```python
disconnect_o_auth_output = client.mcp_servers.disconnect_o_auth(DisconnectOAuthInput(
    resource_id="...",
    org="...",
))
```
</Tab>
<Tab value="Java">
```java
DisconnectOAuthOutput disconnectOAuthOutput = client.mcpServers().disconnectOAuth(DisconnectOAuthInput.builder()
    .resourceId("...")
    .org("...")
    .build());
```
</Tab>
</SDKTabs>

**Parameters**: [`DisconnectOAuthInput`](#disconnectoauthinput)

**Returns**: [`DisconnectOAuthOutput`](#disconnectoauthoutput)

### initiateOAuthConnect

Start the OAuth authorization flow for an MCP server.

Performs setup (DCR registration or OAuthApp credential lookup, PKCE generation) and returns an authorization URL for the frontend to redirect the user to. The frontend calls completeOAuthConnect after the user authorizes.

<SDKTabs>
<Tab value="TypeScript">
```typescript
const initiateOAuthConnectOutput = await stigmer.mcpServer.initiateOAuthConnect({
  mcpServerId: "...",
  org: "...",
});
```
</Tab>
<Tab value="Go">
```go
initiateOAuthConnectOutput, err := client.McpServer.InitiateOAuthConnect(ctx, &stigmer.InitiateOAuthConnectInput{
  McpServerId: "...",
  Org:         "...",
})
```
</Tab>
<Tab value="Python">
```python
initiate_o_auth_connect_output = client.mcp_servers.initiate_o_auth_connect(InitiateOAuthConnectInput(
    mcp_server_id="...",
    org="...",
))
```
</Tab>
<Tab value="Java">
```java
InitiateOAuthConnectOutput initiateOAuthConnectOutput = client.mcpServers().initiateOAuthConnect(InitiateOAuthConnectInput.builder()
    .mcpServerId("...")
    .org("...")
    .build());
```
</Tab>
</SDKTabs>

**Parameters**: [`InitiateOAuthConnectInput`](#initiateoauthconnectinput)

**Returns**: [`InitiateOAuthConnectOutput`](#initiateoauthconnectoutput)

### setOrgOAuthApp

Create or update an org-level BYOA OAuth app override for a resource.

Allows an organization to use its own OAuth app credentials instead of the platform default. The handler clones the platform OAuthApp template (endpoint URLs, scopes) and applies the org-provided client credentials.

Idempotent: if an override already exists for this resource + org, the existing OAuthApp is updated with the new credentials.

<SDKTabs>
<Tab value="TypeScript">
```typescript
const setOrgOAuthAppOutput = await stigmer.mcpServer.setOrgOAuthApp({
  resourceId: "...",
  org: "...",
  clientId: "...",
  clientSecret: "...",
});
```
</Tab>
<Tab value="Go">
```go
setOrgOAuthAppOutput, err := client.McpServer.SetOrgOAuthApp(ctx, &stigmer.SetOrgOAuthAppInput{
  ResourceId:   "...",
  Org:          "...",
  ClientId:     "...",
  ClientSecret: "...",
})
```
</Tab>
<Tab value="Python">
```python
set_org_o_auth_app_output = client.mcp_servers.set_org_o_auth_app(SetOrgOAuthAppInput(
    resource_id="...",
    org="...",
    client_id="...",
    client_secret="...",
))
```
</Tab>
<Tab value="Java">
```java
SetOrgOAuthAppOutput setOrgOAuthAppOutput = client.mcpServers().setOrgOAuthApp(SetOrgOAuthAppInput.builder()
    .resourceId("...")
    .org("...")
    .clientId("...")
    .clientSecret("...")
    .build());
```
</Tab>
</SDKTabs>

**Parameters**: [`SetOrgOAuthAppInput`](#setorgoauthappinput)

**Returns**: [`SetOrgOAuthAppOutput`](#setorgoauthappoutput)

## Types

### McpServerInput

Input for creating or updating a MCP Server.

<TypeTable
  type={{
    name: { type: "string", description: "Resource name.", required: true },
    slug: { type: "string", description: "URL-friendly identifier." },
    org: { type: "string", description: "Organization slug.", required: true },
    labels: { type: "Record<string, string>", description: "Key-value labels." },
    description: { type: "string", description: "Human-readable description for marketplace display and documentation." },
    iconUrl: { type: "string", description: "Icon URL for UI display in marketplace and agent configuration screens." },
    stdio: { type: "StdioServerConfigInput", description: "stdio-based server (subprocess with stdin/stdout communication).", typeDescriptionLink: "#stdioserverconfiginput" },
    http: { type: "HttpServerConfigInput", description: "HTTP-based server (HTTP + Server-Sent Events communication).", typeDescriptionLink: "#httpserverconfiginput" },
    defaultEnabledTools: { type: "string[]", description: "Default tools to enable from this MCP server." },
    env: { type: "Record<string, EnvVarDeclarationInput>", description: "Environment variable declarations for this MCP server.", typeDescriptionLink: "#envvardeclarationinput" },
    pinnedToolApprovals: { type: "ToolApprovalPolicyInput[]", description: "Manual tool approval overrides set by the MCP server owner.", typeDescriptionLink: "#toolapprovalpolicyinput" },
    repositoryUrl: { type: "string", description: "URL of the upstream source repository for this MCP server." },
    githubStars: { type: "number", description: "GitHub star count at the time of curation." },
    auth: { type: "McpServerAuthInput", description: "OAuth authentication configuration for automated credential acquisition.", typeDescriptionLink: "#mcpserverauthinput" },
  }}
/>

### StdioServerConfigInput

StdioServerConfig defines an MCP server that runs as a subprocess.

<TypeTable
  type={{
    command: { type: "string", description: "Command to execute the MCP server.", required: true },
    args: { type: "string[]", description: "Arguments to pass to the command." },
    workingDir: { type: "string", description: "Working directory for the process." },
  }}
/>

### HttpServerConfigInput

HttpServerConfig defines an MCP server accessible via HTTP + Server-Sent Events.

<TypeTable
  type={{
    url: { type: "string", description: "Base URL of the MCP server endpoint.", required: true },
    headers: { type: "Record<string, string>", description: "HTTP headers to include with every request." },
    queryParams: { type: "Record<string, string>", description: "Query parameters to append to the URL." },
    timeoutSeconds: { type: "number", description: "Timeout for HTTP requests in seconds." },
  }}
/>

### EnvVarDeclarationInput

EnvVarDeclaration declares an environment variable required (or optionally accepted) by a blueprint resource (McpServer, Agent, Workflow).

<TypeTable
  type={{
    isSecret: { type: "boolean", description: "Whether the resolved value should be treated as a secret." },
    description: { type: "string", description: "Human-readable description shown in the UI credential form." },
    optional: { type: "boolean", description: "Whether this variable is optional." },
  }}
/>

### ToolApprovalPolicyInput

ToolApprovalPolicy defines approval requirements for a specific tool.

<TypeTable
  type={{
    toolName: { type: "string", description: "Name of the tool (must match tools/list from MCP server exactly)." },
    message: { type: "string", description: "Human-readable message shown to user when approval is requested." },
  }}
/>

### McpServerAuthInput

McpServerAuth configures automated credential acquisition via OAuth.

<TypeTable
  type={{
    oauthAppRef: { type: "ResourceRef", description: "Reference to an OAuthApp for vendor-specific OAuth.", typeDescriptionLink: "#resourceref" },
    targetEnvVar: { type: "string", description: "The env var where the acquired access token is stored." },
    tokenLifetimeHint: { type: "string", description: "Informational hint about expected token lifetime for UI display." },
    scopeHints: { type: "string[]", description: "Optional scope hints for UI display before the OAuth flow starts." },
    discoveryUrl: { type: "string", description: "Optional URL for OAuth authorization server discovery on stdio servers." },
  }}
/>

### ResourceRef

Generic reference to any API resource by org and slug.

<TypeTable
  type={{
    org: { type: "string", description: "Organization that owns the referenced resource." },
    kind: { type: "ApiResourceKind", description: "Kind of the referenced resource (e.g., SKILL, AGENT, MCP_SERVER).", typeDescriptionLink: "/docs/sdk/resources/commons#apiresourcekind" },
    slug: { type: "string", description: "Resource slug (user-friendly identifier, unique within org).", required: true },
    version: { type: "string", description: "Version of the resource (optional, only applicable to versioned resources like Skills)." },
  }}
/>

### UpdateVisibilityInput

Input for updating the visibility of any API resource.

<TypeTable
  type={{
    resourceId: { type: "string", description: "ID of the resource whose visibility is being updated.", required: true },
    visibility: { type: "ApiResourceVisibility", description: "The new visibility setting for the resource.", typeDescriptionLink: "/docs/sdk/resources/commons#apiresourcevisibility" },
  }}
/>

### ConnectInput

ConnectInput is the request for the connect RPC.

<TypeTable
  type={{
    mcpServerId: { type: "string", description: "System-generated ID of the MCP server to connect to.", required: true },
    runtimeEnv: { type: "Record<string, EnvVarInput>", description: "Optional environment variable values for one-time use." },
    org: { type: "string", description: "Organization context for credential resolution." },
  }}
/>

### InitiateOAuthConnectInput

InitiateOAuthConnectInput starts the OAuth authorization flow for an MCP server that has an auth block in its spec.

<TypeTable
  type={{
    mcpServerId: { type: "string", description: "System-generated ID of the MCP server to initiate OAuth for.", required: true },
    org: { type: "string", description: "Organization context for token storage." },
  }}
/>

### InitiateOAuthConnectOutput

InitiateOAuthConnectOutput contains the authorization URL and metadata the frontend needs to redirect the user to the OAuth authorization server.

<TypeTable
  type={{
    authorizationUrl: { type: "string", description: "Full authorization URL to redirect the user to." },
    state: { type: "string", description: "Opaque state parameter for CSRF protection." },
    scopes: { type: "string[]", description: "OAuth scopes that will be requested." },
    providerName: { type: "string", description: "Human-readable provider name for UI display during the redirect." },
  }}
/>

### CompleteOAuthConnectInput

CompleteOAuthConnectInput finishes the OAuth flow by exchanging the authorization code for tokens.

<TypeTable
  type={{
    mcpServerId: { type: "string", description: "System-generated ID of the MCP server.", required: true },
    authorizationCode: { type: "string", description: "Authorization code from the OAuth callback redirect." },
    state: { type: "string", description: "State parameter from the OAuth callback redirect." },
  }}
/>

### CompleteOAuthConnectOutput

CompleteOAuthConnectOutput confirms that tokens were successfully acquired and stored.

<TypeTable
  type={{
    connected: { type: "boolean", description: "Whether the OAuth flow completed successfully and tokens are stored." },
    targetEnvVar: { type: "string", description: "The environment variable name where the access token was stored." },
    tokenLifetimeHint: { type: "string", description: "Informational hint about expected token lifetime." },
  }}
/>

### GetOAuthGrantStatusInput

GetOAuthGrantStatusInput queries the OAuth grant status for a resource.

<TypeTable
  type={{
    resourceId: { type: "string", description: "System-generated ID of the resource to check grant status for.", required: true },
    org: { type: "string", description: "Organization context." },
  }}
/>

### GetOAuthGrantStatusOutput

GetOAuthGrantStatusOutput returns the current OAuth grant status.

<TypeTable
  type={{
    connected: { type: "boolean", description: "Whether the user has an active OAuth grant for this resource + org." },
    accessTokenExpiresAt: { type: "number", description: "When the access token expires (Unix timestamp seconds)." },
    targetEnvVar: { type: "string", description: "The env var name where the access token is stored." },
    authMethod: { type: "string", description: "Which auth method was used (\"mcp_oauth\" or \"vendor_oauth\")." },
    connectionHealth: { type: "OAuthConnectionHealth", description: "Evaluated health of the OAuth connection.", typeDescriptionLink: "#oauthconnectionhealth" },
  }}
/>

### DisconnectOAuthInput

DisconnectOAuthInput tears down a user's OAuth connection for a resource.

<TypeTable
  type={{
    resourceId: { type: "string", description: "System-generated ID of the resource to disconnect OAuth for.", required: true },
    org: { type: "string", description: "Organization context." },
  }}
/>

### DisconnectOAuthOutput

DisconnectOAuthOutput reports the result of a disconnect request.

<TypeTable
  type={{
    disconnected: { type: "boolean", description: "Whether an active grant was found and deleted." },
  }}
/>

### SetOrgOAuthAppInput

SetOrgOAuthAppInput creates or updates an org-level BYOA OAuth app override for a resource.

<TypeTable
  type={{
    resourceId: { type: "string", description: "System-generated ID of the resource to set the BYOA override for.", required: true },
    org: { type: "string", description: "Organization that will own this override." },
    clientId: { type: "string", description: "OAuth client ID from the org's own app registration with the vendor." },
    clientSecret: { type: "string", description: "OAuth client secret from the org's own app registration with the vendor." },
  }}
/>

### SetOrgOAuthAppOutput

SetOrgOAuthAppOutput confirms the BYOA override was created or updated.

<TypeTable
  type={{
    oauthAppId: { type: "string", description: "System-generated ID of the OAuthApp resource created (or updated) for this override." },
  }}
/>

### GetOrgOAuthAppInput

GetOrgOAuthAppInput queries whether an org has a BYOA override for a resource.

<TypeTable
  type={{
    resourceId: { type: "string", description: "System-generated ID of the resource to check for an override.", required: true },
    org: { type: "string", description: "Organization context to check." },
  }}
/>

### GetOrgOAuthAppOutput

GetOrgOAuthAppOutput returns the org's BYOA override status.

<TypeTable
  type={{
    hasOverride: { type: "boolean", description: "Whether an OAuthAppOverride exists for this resource + org." },
    oauthAppId: { type: "string", description: "System-generated ID of the override's OAuthApp." },
    clientId: { type: "string", description: "Client ID from the override's OAuthApp (non-secret, safe to display)." },
  }}
/>

### DeleteOrgOAuthAppInput

DeleteOrgOAuthAppInput removes an org-level BYOA override for a resource.

<TypeTable
  type={{
    resourceId: { type: "string", description: "System-generated ID of the resource to remove the override for.", required: true },
    org: { type: "string", description: "Organization whose override should be removed." },
  }}
/>

### DeleteOrgOAuthAppOutput

DeleteOrgOAuthAppOutput confirms the BYOA override was removed.

<TypeTable
  type={{
    deleted: { type: "boolean", description: "Whether the delete completed successfully." },
  }}
/>

### McpServer

McpServer represents a reusable MCP (Model Context Protocol) server configuration.

<TypeTable
  type={{
    apiVersion: { type: "string", description: "API version for this resource type." },
    kind: { type: "string", description: "Resource kind identifier." },
    metadata: { type: "ApiResourceMetadata", description: "Resource metadata including id, name, org, slug, labels, visibility, and timestamps.", typeDescriptionLink: "/docs/sdk/resources/commons#apiresourcemetadata" },
    spec: { type: "McpServerInput", description: "Resource specification. See [`McpServerInput`](#mcpserverinput).", typeDescriptionLink: "#mcpserverinput" },
    status: { type: "McpServerStatus", description: "System-managed state. See [`McpServerStatus`](#mcpserverstatus).", typeDescriptionLink: "#mcpserverstatus" },
  }}
/>

### McpServerStatus

McpServerStatus represents the system-managed state of an MCP server definition.

<TypeTable
  type={{
    validationState: { type: "ValidationState", description: "Validation state of the MCP server definition.", typeDescriptionLink: "#validationstate" },
    validationMessage: { type: "string", description: "Human-readable validation message." },
    discoveredCapabilities: { type: "DiscoveredCapabilities", description: "Tools and resource templates discovered from the MCP server.", typeDescriptionLink: "#discoveredcapabilities" },
    toolApprovals: { type: "ToolApprovalPolicy[]", description: "System-generated tool approval policies.", typeDescriptionLink: "#toolapprovalpolicy" },
    oauthStatus: { type: "OAuthStatus", description: "OAuth-related enrichment state, populated at query time by the backend.", typeDescriptionLink: "#oauthstatus" },
    audit: { type: "ApiResourceAudit", description: "Standard audit information (created_at, updated_at, created_by, etc.).", typeDescriptionLink: "/docs/sdk/resources/commons#apiresourceaudit" },
  }}
/>

### DiscoveredCapabilities

DiscoveredCapabilities holds the tools and resource templates reported by an MCP server.

<TypeTable
  type={{
    tools: { type: "DiscoveredTool[]", description: "Callable tools reported by the MCP server.", typeDescriptionLink: "#discoveredtool" },
    resourceTemplates: { type: "DiscoveredResourceTemplate[]", description: "Read-only data endpoints reported by the MCP server.", typeDescriptionLink: "#discoveredresourcetemplate" },
    lastDiscoveredAt: { type: "string", description: "When this snapshot was captured." },
  }}
/>

### DiscoveredResourceTemplate

DiscoveredResourceTemplate describes a parameterized resource template reported by an MCP server.

<TypeTable
  type={{
    uriTemplate: { type: "string", description: "URI template with placeholders (RFC 6570)." },
    name: { type: "string", description: "Identifier for this resource template." },
    description: { type: "string", description: "Human-readable description of the resource." },
    mimeType: { type: "string", description: "MIME type of the resource content." },
  }}
/>

### DiscoveredTool

DiscoveredTool describes a single tool reported by an MCP server.

<TypeTable
  type={{
    name: { type: "string", description: "Unique tool name within the MCP server (case-sensitive)." },
    description: { type: "string", description: "Human-readable description of the tool's purpose." },
    inputSchema: { type: "object", description: "JSON Schema describing the tool's input parameters." },
  }}
/>

### OAuthStatus

OAuthStatus holds system-derived OAuth enrichment state for an MCP server.

<TypeTable
  type={{
    vendorApprovalStatus: { type: "VendorApprovalStatus", description: "Vendor marketplace/app-review approval status for this MCP server's OAuth app.", typeDescriptionLink: "#vendorapprovalstatus" },
    vendorApprovalDocsUrl: { type: "string", description: "Documentation URL for users who want to bring their own OAuth app credentials while the platform's OAuth app is pending vendor approval." },
    effectiveOauthSource: { type: "OAuthAppSource", description: "Where the effective OAuth app was resolved from for the caller's org.", typeDescriptionLink: "#oauthappsource" },
    effectiveOauthAppId: { type: "string", description: "System-generated ID (metadata.id) of the OAuthApp that the resolution chain selected." },
  }}
/>

### ToolApprovalPolicy

ToolApprovalPolicy defines approval requirements for a specific tool.

<TypeTable
  type={{
    toolName: { type: "string", description: "Name of the tool (must match tools/list from MCP server exactly)." },
    message: { type: "string", description: "Human-readable message shown to user when approval is requested." },
  }}
/>

### OAuthConnectionHealth

OAuthConnectionHealth evaluates the health of an OAuth connection by examining grant existence and token expiry metadata.

| Value | Description |
|-------|-------------|
| `OAUTH_CONNECTION_HEALTH_HEALTHY` | The access token exists and is not expired (or does not expire). |
| `OAUTH_CONNECTION_HEALTH_TOKEN_EXPIRED` | The access token is expired and no refresh token is available. |
| `OAUTH_CONNECTION_HEALTH_TOKEN_EXPIRED_REFRESHABLE` | The access token is expired but a refresh token is available. |
| `OAUTH_CONNECTION_HEALTH_NO_GRANT` | No OAuth grant exists for this resource + org + user combination. |

### ValidationState

ValidationState represents the structural validity of an MCP server definition.

| Value | Description |
|-------|-------------|
| `valid` | The MCP server definition is structurally valid. |
| `invalid` | The MCP server definition is invalid. |

### VendorApprovalStatus

Lifecycle status of an OAuthApp's vendor marketplace approval.

| Value | Description |
|-------|-------------|
| `VENDOR_APPROVAL_STATUS_PENDING` | The OAuth app has been submitted to the vendor for review and is awaiting approval. |
| `VENDOR_APPROVAL_STATUS_APPROVED` | The vendor has approved the OAuth app. |
| `VENDOR_APPROVAL_STATUS_REJECTED` | The vendor rejected the OAuth app. |

### OAuthAppSource

OAuthAppSource identifies where the effective OAuth app for an MCP server was resolved from.

| Value | Description |
|-------|-------------|
| `OAUTH_APP_SOURCE_PLATFORM` | The platform-default OAuthApp is in use (McpServerAuth.oauth_app_ref). |
| `OAUTH_APP_SOURCE_ORG_OVERRIDE` | An org-level BYOA override is in use (OAuthAppOverride binding). |
| `OAUTH_APP_SOURCE_NONE` | No OAuthApp is available. |

