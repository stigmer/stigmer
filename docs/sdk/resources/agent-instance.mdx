---
title: Agent Instance
---

{/* Auto-generated by stigmer-codegen. DO NOT EDIT. */}

An AgentInstance binds an Agent template to one or more Environment resources,
supplying the secrets and configuration the agent needs at runtime. Create
separate instances for different environments (staging, production) or teams
without duplicating the agent definition.

```yaml
apiVersion: agentic.stigmer.ai/v1
kind: AgentInstance
metadata:
  name: github-bot-prod
  org: acme-corp
spec:
  agent_id: agt_abc123
  description: "GitHub bot with production credentials"
  environment_refs:
    - kind: environment
      slug: base-config
    - kind: environment
      slug: github-prod-secrets
```

## Client Access

<SDKTabs>
<Tab value="TypeScript">
```typescript
const agentInstance = await stigmer.agentInstance.get("ain_01j5q3k7m8r2s4tnz2hfp0q0c3");
```
</Tab>
<Tab value="Go">
```go
agentInstance, err := client.AgentInstance.Get(ctx, "ain_01j5q3k7m8r2s4tnz2hfp0q0c3")
```
</Tab>
<Tab value="Python">
```python
agent_instance = client.agent_instances.get("ain_01j5q3k7m8r2s4tnz2hfp0q0c3")
```
</Tab>
<Tab value="Java">
```java
AgentInstance agentInstance = client.agentInstances().get("ain_01j5q3k7m8r2s4tnz2hfp0q0c3");
```
</Tab>
</SDKTabs>

## Methods

| Method | Description |
|--------|-------------|
| [`get`](#get) | Get a single agent instance by ID. |
| [`getByAgent`](#getbyagent) | Get all instances of a specific agent template. |
| [`getByReference`](#getbyreference) | Get an agent instance by its organization-scoped reference (org/slug). |
| [`list`](#list) | List agent instances with optional label filtering. |
| [`apply`](#apply) | Create or update an agent instance. |
| [`create`](#create) | Create an agent instance. |
| [`update`](#update) | Update an existing agent instance. |
| [`delete`](#delete) | Delete an agent instance. |

### get

Get a single agent instance by ID.

<SDKTabs>
<Tab value="TypeScript">
```typescript
const agentInstance = await stigmer.agentInstance.get(id);
```
</Tab>
<Tab value="Go">
```go
agentInstance, err := client.AgentInstance.Get(ctx, id)
```
</Tab>
<Tab value="Python">
```python
agent_instance = client.agent_instances.get(id)
```
</Tab>
<Tab value="Java">
```java
AgentInstance agentInstance = client.agentInstances().get(id);
```
</Tab>
</SDKTabs>

**Parameters**

| Parameter | Type | Description |
|-----------|------|-------------|
| `id` | `string` | Agent Instance identifier. |

**Returns**: [`AgentInstance`](#agentinstance)

### getByAgent

Get all instances of a specific agent template. Returns only instances the caller has access to.

<SDKTabs>
<Tab value="TypeScript">
```typescript
const result = await stigmer.agentInstance.getByAgent({
  agentId: "...",
  pageInfo: {},
});
```
</Tab>
<Tab value="Go">
```go
result, err := client.AgentInstance.GetByAgent(ctx, &stigmer.GetAgentInstancesByAgentRequest{
  AgentId:  "...",
  PageInfo: &stigmer.PageInfoInput{},
})
```
</Tab>
<Tab value="Python">
```python
result = client.agent_instances.get_by_agent(GetAgentInstancesByAgentRequest(
    agent_id="...",
    page_info=None,
))
```
</Tab>
<Tab value="Java">
```java
AgentInstanceList result = client.agentInstances().getByAgent(GetAgentInstancesByAgentRequest.builder()
    .agentId("...")
    .pageInfo(null)
    .build());
```
</Tab>
</SDKTabs>

**Parameters**: [`GetAgentInstancesByAgentRequest`](#getagentinstancesbyagentrequest)

**Returns**: [`AgentInstanceList`](#agentinstancelist)

### getByReference

Get an agent instance by its organization-scoped reference (org/slug).

<SDKTabs>
<Tab value="TypeScript">
```typescript
const agentInstance = await stigmer.agentInstance.getByReference({
  org: "...",
  kind: "...",
  slug: "...",
  version: "...",
});
```
</Tab>
<Tab value="Go">
```go
agentInstance, err := client.AgentInstance.GetByReference(ctx, &stigmer.ApiResourceReference{
  Org:     "...",
  Kind:    "...",
  Slug:    "...",
  Version: "...",
})
```
</Tab>
<Tab value="Python">
```python
agent_instance = client.agent_instances.get_by_reference(ApiResourceReference(
    org="...",
    kind="...",
    slug="...",
    version="...",
))
```
</Tab>
<Tab value="Java">
```java
AgentInstance agentInstance = client.agentInstances().getByReference(ApiResourceReference.builder()
    .org("...")
    .kind("...")
    .slug("...")
    .version("...")
    .build());
```
</Tab>
</SDKTabs>

**Parameters**: [`ResourceRef`](#resourceref)

**Returns**: [`AgentInstance`](#agentinstance)

### list

List agent instances with optional label filtering.

<SDKTabs>
<Tab value="TypeScript">
```typescript
const result = await stigmer.agentInstance.list({
  org: "...",
  labels: {},
  pageInfo: {},
});
```
</Tab>
<Tab value="Go">
```go
result, err := client.AgentInstance.List(ctx, &stigmer.ListAgentInstancesRequest{
  Org:      "...",
  Labels:   map[string]string{},
  PageInfo: &stigmer.PageInfoInput{},
})
```
</Tab>
<Tab value="Python">
```python
result = client.agent_instances.list(ListAgentInstancesRequest(
    org="...",
    labels={},
    page_info=None,
))
```
</Tab>
<Tab value="Java">
```java
AgentInstanceList result = client.agentInstances().list(ListAgentInstancesRequest.builder()
    .org("...")
    .labels(Map.of())
    .pageInfo(null)
    .build());
```
</Tab>
</SDKTabs>

**Parameters**: [`ListAgentInstancesRequest`](#listagentinstancesrequest)

**Returns**: [`AgentInstanceList`](#agentinstancelist)

### apply

Create or update an agent instance.

<SDKTabs>
<Tab value="TypeScript">
```typescript
const agentInstance = await stigmer.agentInstance.apply({
  name: "my-agent-instance",
  org: "acme",
  slug: "...",
  labels: {},
  agentId: "...",
  description: "...",
  environmentRefs: [],
});
```
</Tab>
<Tab value="Go">
```go
agentInstance, err := client.AgentInstance.Apply(ctx, &stigmer.AgentInstanceInput{
  Name:            "my-agent-instance",
  Org:             "acme",
  Slug:            "...",
  Labels:          map[string]string{},
  AgentId:         "...",
  Description:     "...",
  EnvironmentRefs: []stigmer.ResourceRef{},
})
```
</Tab>
<Tab value="Python">
```python
agent_instance = client.agent_instances.apply(AgentInstanceInput(
    name="my-agent-instance",
    org="acme",
    slug="...",
    labels={},
    agent_id="...",
    description="...",
    environment_refs=[],
))
```
</Tab>
<Tab value="Java">
```java
AgentInstance agentInstance = client.agentInstances().apply(AgentInstanceInput.builder()
    .name("my-agent-instance")
    .org("acme")
    .slug("...")
    .labels(Map.of())
    .agentId("...")
    .description("...")
    .environmentRefs(List.of())
    .build());
```
</Tab>
</SDKTabs>

**Parameters**: [`AgentInstanceInput`](#agentinstanceinput)

**Returns**: [`AgentInstance`](#agentinstance)

### create

Create an agent instance.

Public agents allow any authenticated user to create instances (cross-org allowed). Private agents restrict instance creation to org members and the agent owner.

<SDKTabs>
<Tab value="TypeScript">
```typescript
const agentInstance = await stigmer.agentInstance.create({
  name: "my-agent-instance",
  org: "acme",
  slug: "...",
  labels: {},
  agentId: "...",
  description: "...",
  environmentRefs: [],
});
```
</Tab>
<Tab value="Go">
```go
agentInstance, err := client.AgentInstance.Create(ctx, &stigmer.AgentInstanceInput{
  Name:            "my-agent-instance",
  Org:             "acme",
  Slug:            "...",
  Labels:          map[string]string{},
  AgentId:         "...",
  Description:     "...",
  EnvironmentRefs: []stigmer.ResourceRef{},
})
```
</Tab>
<Tab value="Python">
```python
agent_instance = client.agent_instances.create(AgentInstanceInput(
    name="my-agent-instance",
    org="acme",
    slug="...",
    labels={},
    agent_id="...",
    description="...",
    environment_refs=[],
))
```
</Tab>
<Tab value="Java">
```java
AgentInstance agentInstance = client.agentInstances().create(AgentInstanceInput.builder()
    .name("my-agent-instance")
    .org("acme")
    .slug("...")
    .labels(Map.of())
    .agentId("...")
    .description("...")
    .environmentRefs(List.of())
    .build());
```
</Tab>
</SDKTabs>

**Parameters**: [`AgentInstanceInput`](#agentinstanceinput)

**Returns**: [`AgentInstance`](#agentinstance)

### update

Update an existing agent instance.

<SDKTabs>
<Tab value="TypeScript">
```typescript
const agentInstance = await stigmer.agentInstance.update({
  name: "my-agent-instance",
  org: "acme",
  slug: "...",
  labels: {},
  agentId: "...",
  description: "...",
  environmentRefs: [],
});
```
</Tab>
<Tab value="Go">
```go
agentInstance, err := client.AgentInstance.Update(ctx, &stigmer.AgentInstanceInput{
  Name:            "my-agent-instance",
  Org:             "acme",
  Slug:            "...",
  Labels:          map[string]string{},
  AgentId:         "...",
  Description:     "...",
  EnvironmentRefs: []stigmer.ResourceRef{},
})
```
</Tab>
<Tab value="Python">
```python
agent_instance = client.agent_instances.update(AgentInstanceInput(
    name="my-agent-instance",
    org="acme",
    slug="...",
    labels={},
    agent_id="...",
    description="...",
    environment_refs=[],
))
```
</Tab>
<Tab value="Java">
```java
AgentInstance agentInstance = client.agentInstances().update(AgentInstanceInput.builder()
    .name("my-agent-instance")
    .org("acme")
    .slug("...")
    .labels(Map.of())
    .agentId("...")
    .description("...")
    .environmentRefs(List.of())
    .build());
```
</Tab>
</SDKTabs>

**Parameters**: [`AgentInstanceInput`](#agentinstanceinput)

**Returns**: [`AgentInstance`](#agentinstance)

### delete

Delete an agent instance.

<SDKTabs>
<Tab value="TypeScript">
```typescript
const agentInstance = await stigmer.agentInstance.delete(id);
```
</Tab>
<Tab value="Go">
```go
agentInstance, err := client.AgentInstance.Delete(ctx, id)
```
</Tab>
<Tab value="Python">
```python
agent_instance = client.agent_instances.delete(id)
```
</Tab>
<Tab value="Java">
```java
AgentInstance agentInstance = client.agentInstances().delete(id);
```
</Tab>
</SDKTabs>

**Parameters**

| Parameter | Type | Description |
|-----------|------|-------------|
| `id` | `string` | Agent Instance identifier. |

**Returns**: [`AgentInstance`](#agentinstance)

## Types

### AgentInstanceInput

Input for creating or updating an Agent Instance.

<TypeTable
  type={{
    name: { type: "string", description: "Resource name.", required: true },
    slug: { type: "string", description: "URL-friendly identifier." },
    org: { type: "string", description: "Organization slug.", required: true },
    labels: { type: "Record<string, string>", description: "Key-value labels." },
    agentId: { type: "string", description: "Identifier of the Agent template this instance deploys." },
    description: { type: "string", description: "Human-readable description for UI and API display." },
    environmentRefs: { type: "ResourceRef[]", description: "References to Environment resources providing secrets and configuration at runtime.", typeDescriptionLink: "#resourceref" },
  }}
/>

### ResourceRef

Generic reference to any API resource by org and slug.

<TypeTable
  type={{
    org: { type: "string", description: "Organization that owns the referenced resource." },
    kind: { type: "ApiResourceKind", description: "Kind of the referenced resource (e.g., SKILL, AGENT, MCP_SERVER).", typeDescriptionLink: "/docs/sdk/resources/commons#apiresourcekind" },
    slug: { type: "string", description: "Resource slug (user-friendly identifier, unique within org).", required: true },
    version: { type: "string", description: "Version of the resource (optional, only applicable to versioned resources like Skills)." },
  }}
/>

### GetAgentInstancesByAgentRequest

Input for retrieving all instances deployed from a specific agent.

<TypeTable
  type={{
    agentId: { type: "string", description: "Identifier of the agent whose instances to retrieve.", required: true },
    pageInfo: { type: "PageInfoInput", description: "Pagination options." },
  }}
/>

### AgentInstanceList

Response containing a paginated list of agent instances.

<TypeTable
  type={{
    totalCount: { type: "number", description: "Total number of agent instances matching the query." },
    items: { type: "AgentInstanceInput[]", description: "Agent instances in the current page.", typeDescriptionLink: "#agentinstanceinput" },
  }}
/>

### ListAgentInstancesRequest

Input for listing agent instances within an organization.

<TypeTable
  type={{
    org: { type: "string", description: "Organization slug to scope the listing." },
    labels: { type: "Record<string, string>", description: "Filter by metadata labels." },
    pageInfo: { type: "PageInfoInput", description: "Pagination options." },
  }}
/>

### AgentInstance

AgentInstance defines a configured deployment of an Agent template.

<TypeTable
  type={{
    apiVersion: { type: "string", description: "API version for this resource type." },
    kind: { type: "string", description: "Resource kind identifier." },
    metadata: { type: "ApiResourceMetadata", description: "Resource metadata including id, name, org, slug, labels, visibility, and timestamps.", typeDescriptionLink: "/docs/sdk/resources/commons#apiresourcemetadata" },
    spec: { type: "AgentInstanceInput", description: "Resource specification. See [`AgentInstanceInput`](#agentinstanceinput).", typeDescriptionLink: "#agentinstanceinput" },
    status: { type: "AgentInstanceStatus", description: "System-managed state. See `AgentInstanceStatus`." },
  }}
/>

