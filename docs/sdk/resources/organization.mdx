---
title: Organization
---

{/* Auto-generated by stigmer-codegen. DO NOT EDIT. */}

An Organization is the top-level container for all Stigmer resources. Every
agent, workflow, session, and project belongs to exactly one organization,
providing multi-tenant isolation and access control.

```yaml
apiVersion: tenancy.stigmer.ai/v1
kind: Organization
metadata:
  name: acme-corp
  slug: acme
spec:
  description: "Acme Corp engineering team"
  logo_url: "https://cdn.example.com/acme-logo.svg"
```

## Client Access

<SDKTabs>
<Tab value="TypeScript">
```typescript
const organization = await stigmer.organization.get("org_01j5q3k7m8r2s4tnz2hfp0q0c3");
```
</Tab>
<Tab value="Go">
```go
organization, err := client.Organization.Get(ctx, "org_01j5q3k7m8r2s4tnz2hfp0q0c3")
```
</Tab>
<Tab value="Python">
```python
organization = client.organizations.get("org_01j5q3k7m8r2s4tnz2hfp0q0c3")
```
</Tab>
<Tab value="Java">
```java
Organization organization = client.organizations().get("org_01j5q3k7m8r2s4tnz2hfp0q0c3");
```
</Tab>
</SDKTabs>

## Methods

| Method | Description |
|--------|-------------|
| [`get`](#get) | Get an organization by ID. |
| [`getByExternalOrgId`](#getbyexternalorgid) | Look up a platform-managed organization by its external platform coordinates. |
| [`find`](#find) | List organizations with pagination and filtering. |
| [`findMyOrganizations`](#findmyorganizations) | Find organizations the authenticated user is a member of. |
| [`apply`](#apply) | Create or update an organization. |
| [`create`](#create) | Create an organization. |
| [`update`](#update) | Update an existing organization. |
| [`delete`](#delete) | Delete an organization. |

### get

Get an organization by ID.

<SDKTabs>
<Tab value="TypeScript">
```typescript
const organization = await stigmer.organization.get(id);
```
</Tab>
<Tab value="Go">
```go
organization, err := client.Organization.Get(ctx, id)
```
</Tab>
<Tab value="Python">
```python
organization = client.organizations.get(id)
```
</Tab>
<Tab value="Java">
```java
Organization organization = client.organizations().get(id);
```
</Tab>
</SDKTabs>

**Parameters**

| Parameter | Type | Description |
|-----------|------|-------------|
| `id` | `string` | Organization identifier. |

**Returns**: [`Organization`](#organization)

### getByExternalOrgId

Look up a platform-managed organization by its external platform coordinates. Returns the Stigmer organization mapped to the given IdentityProvider + external org ID.

<SDKTabs>
<Tab value="TypeScript">
```typescript
const organization = await stigmer.organization.getByExternalOrgId({
  identityProviderRef: {},
  externalOrgId: "...",
});
```
</Tab>
<Tab value="Go">
```go
organization, err := client.Organization.GetByExternalOrgId(ctx, &stigmer.OrganizationExternalLookup{
  IdentityProviderRef: &stigmer.ResourceRef{},
  ExternalOrgId:       "...",
})
```
</Tab>
<Tab value="Python">
```python
organization = client.organizations.get_by_external_org_id(OrganizationExternalLookup(
    identity_provider_ref=None,
    external_org_id="...",
))
```
</Tab>
<Tab value="Java">
```java
Organization organization = client.organizations().getByExternalOrgId(OrganizationExternalLookup.builder()
    .identityProviderRef(null)
    .externalOrgId("...")
    .build());
```
</Tab>
</SDKTabs>

**Parameters**: [`OrganizationExternalLookup`](#organizationexternallookup)

**Returns**: [`Organization`](#organization)

### find

List organizations with pagination and filtering.

<SDKTabs>
<Tab value="TypeScript">
```typescript
const result = await stigmer.organization.find({
  org: "...",
  kind: "...",
  page: {},
  pageNumber: 0,
  pageSize: 0,
});
```
</Tab>
<Tab value="Go">
```go
result, err := client.Organization.Find(ctx, &stigmer.FindApiResourcesRequest{
  Org:        "...",
  Kind:       "...",
  Page:       &stigmer.PageInfoInput{},
  PageNumber: 0,
  PageSize:   0,
})
```
</Tab>
<Tab value="Python">
```python
result = client.organizations.find(FindApiResourcesRequest(
    org="...",
    kind="...",
    page=None,
    page_number=0,
    page_size=0,
))
```
</Tab>
<Tab value="Java">
```java
OrganizationList result = client.organizations().find(FindApiResourcesRequest.builder()
    .org("...")
    .kind("...")
    .page(null)
    .pageNumber(0)
    .pageSize(0)
    .build());
```
</Tab>
</SDKTabs>

**Parameters**: [`FindApiResourcesRequest`](#findapiresourcesrequest)

**Returns**: [`OrganizationList`](#organizationlist)

### findMyOrganizations

Find organizations the authenticated user is a member of. Returns only organizations the caller has access to.

<SDKTabs>
<Tab value="TypeScript">
```typescript
const organizations = await stigmer.organization.findMyOrganizations();
```
</Tab>
<Tab value="Go">
```go
organizations, err := client.Organization.FindMyOrganizations(ctx)
```
</Tab>
<Tab value="Python">
```python
organizations = client.organizations.find_my_organizations()
```
</Tab>
<Tab value="Java">
```java
Organizations organizations = client.organizations().findMyOrganizations();
```
</Tab>
</SDKTabs>

**Returns**: [`Organizations`](#organizations)

### apply

Create or update an organization.

<SDKTabs>
<Tab value="TypeScript">
```typescript
const organization = await stigmer.organization.apply({
  name: "my-organization",
  org: "acme",
  slug: "...",
  labels: {},
  description: "...",
  logoUrl: "...",
  managementMode: "...",
  identityProviderRef: {},
  externalOrgId: "...",
  isPersonal: false,
  preferences: {},
});
```
</Tab>
<Tab value="Go">
```go
organization, err := client.Organization.Apply(ctx, &stigmer.OrganizationInput{
  Name:                "my-organization",
  Org:                 "acme",
  Slug:                "...",
  Labels:              map[string]string{},
  Description:         "...",
  LogoUrl:             "...",
  ManagementMode:      "...",
  IdentityProviderRef: &stigmer.ResourceRef{},
  ExternalOrgId:       "...",
  IsPersonal:          false,
  Preferences:         &stigmer.OrganizationPreferencesInput{},
})
```
</Tab>
<Tab value="Python">
```python
organization = client.organizations.apply(OrganizationInput(
    name="my-organization",
    org="acme",
    slug="...",
    labels={},
    description="...",
    logo_url="...",
    management_mode="...",
    identity_provider_ref=None,
    external_org_id="...",
    is_personal=False,
    preferences=None,
))
```
</Tab>
<Tab value="Java">
```java
Organization organization = client.organizations().apply(OrganizationInput.builder()
    .name("my-organization")
    .org("acme")
    .slug("...")
    .labels(Map.of())
    .description("...")
    .logoUrl("...")
    .managementMode("...")
    .identityProviderRef(null)
    .externalOrgId("...")
    .isPersonal(false)
    .preferences(null)
    .build());
```
</Tab>
</SDKTabs>

**Parameters**: [`OrganizationInput`](#organizationinput)

**Returns**: [`Organization`](#organization)

### create

Create an organization.

<SDKTabs>
<Tab value="TypeScript">
```typescript
const organization = await stigmer.organization.create({
  name: "my-organization",
  org: "acme",
  slug: "...",
  labels: {},
  description: "...",
  logoUrl: "...",
  managementMode: "...",
  identityProviderRef: {},
  externalOrgId: "...",
  isPersonal: false,
  preferences: {},
});
```
</Tab>
<Tab value="Go">
```go
organization, err := client.Organization.Create(ctx, &stigmer.OrganizationInput{
  Name:                "my-organization",
  Org:                 "acme",
  Slug:                "...",
  Labels:              map[string]string{},
  Description:         "...",
  LogoUrl:             "...",
  ManagementMode:      "...",
  IdentityProviderRef: &stigmer.ResourceRef{},
  ExternalOrgId:       "...",
  IsPersonal:          false,
  Preferences:         &stigmer.OrganizationPreferencesInput{},
})
```
</Tab>
<Tab value="Python">
```python
organization = client.organizations.create(OrganizationInput(
    name="my-organization",
    org="acme",
    slug="...",
    labels={},
    description="...",
    logo_url="...",
    management_mode="...",
    identity_provider_ref=None,
    external_org_id="...",
    is_personal=False,
    preferences=None,
))
```
</Tab>
<Tab value="Java">
```java
Organization organization = client.organizations().create(OrganizationInput.builder()
    .name("my-organization")
    .org("acme")
    .slug("...")
    .labels(Map.of())
    .description("...")
    .logoUrl("...")
    .managementMode("...")
    .identityProviderRef(null)
    .externalOrgId("...")
    .isPersonal(false)
    .preferences(null)
    .build());
```
</Tab>
</SDKTabs>

**Parameters**: [`OrganizationInput`](#organizationinput)

**Returns**: [`Organization`](#organization)

### update

Update an existing organization.

<SDKTabs>
<Tab value="TypeScript">
```typescript
const organization = await stigmer.organization.update({
  name: "my-organization",
  org: "acme",
  slug: "...",
  labels: {},
  description: "...",
  logoUrl: "...",
  managementMode: "...",
  identityProviderRef: {},
  externalOrgId: "...",
  isPersonal: false,
  preferences: {},
});
```
</Tab>
<Tab value="Go">
```go
organization, err := client.Organization.Update(ctx, &stigmer.OrganizationInput{
  Name:                "my-organization",
  Org:                 "acme",
  Slug:                "...",
  Labels:              map[string]string{},
  Description:         "...",
  LogoUrl:             "...",
  ManagementMode:      "...",
  IdentityProviderRef: &stigmer.ResourceRef{},
  ExternalOrgId:       "...",
  IsPersonal:          false,
  Preferences:         &stigmer.OrganizationPreferencesInput{},
})
```
</Tab>
<Tab value="Python">
```python
organization = client.organizations.update(OrganizationInput(
    name="my-organization",
    org="acme",
    slug="...",
    labels={},
    description="...",
    logo_url="...",
    management_mode="...",
    identity_provider_ref=None,
    external_org_id="...",
    is_personal=False,
    preferences=None,
))
```
</Tab>
<Tab value="Java">
```java
Organization organization = client.organizations().update(OrganizationInput.builder()
    .name("my-organization")
    .org("acme")
    .slug("...")
    .labels(Map.of())
    .description("...")
    .logoUrl("...")
    .managementMode("...")
    .identityProviderRef(null)
    .externalOrgId("...")
    .isPersonal(false)
    .preferences(null)
    .build());
```
</Tab>
</SDKTabs>

**Parameters**: [`OrganizationInput`](#organizationinput)

**Returns**: [`Organization`](#organization)

### delete

Delete an organization.

<SDKTabs>
<Tab value="TypeScript">
```typescript
const organization = await stigmer.organization.delete(id);
```
</Tab>
<Tab value="Go">
```go
organization, err := client.Organization.Delete(ctx, id)
```
</Tab>
<Tab value="Python">
```python
organization = client.organizations.delete(id)
```
</Tab>
<Tab value="Java">
```java
Organization organization = client.organizations().delete(id);
```
</Tab>
</SDKTabs>

**Parameters**

| Parameter | Type | Description |
|-----------|------|-------------|
| `id` | `string` | Organization identifier. |

**Returns**: [`Organization`](#organization)

## Types

### OrganizationInput

Input for creating or updating an Organization.

<TypeTable
  type={{
    name: { type: "string", description: "Resource name.", required: true },
    slug: { type: "string", description: "URL-friendly identifier." },
    org: { type: "string", description: "Organization slug.", required: true },
    labels: { type: "Record<string, string>", description: "Key-value labels." },
    description: { type: "string", description: "Human-readable description of the organization." },
    logoUrl: { type: "string", description: "Public URL for the organization logo." },
    managementMode: { type: "ManagementMode", description: "How this organization is operated.", typeDescriptionLink: "#managementmode" },
    identityProviderRef: { type: "ResourceRef", description: "Reference to the IdentityProvider that authenticates requests for this organization.", typeDescriptionLink: "#resourceref" },
    externalOrgId: { type: "string", description: "External platform's organization identifier for reverse mapping." },
    isPersonal: { type: "boolean", description: "Whether this is a personal organization, auto-created during identity provisioning." },
    preferences: { type: "OrganizationPreferencesInput", description: "Standing preferences declared by the organization.", typeDescriptionLink: "#organizationpreferencesinput" },
  }}
/>

### ResourceRef

Generic reference to any API resource by org and slug.

<TypeTable
  type={{
    org: { type: "string", description: "Organization that owns the referenced resource." },
    kind: { type: "ApiResourceKind", description: "Kind of the referenced resource (e.g., SKILL, AGENT, MCP_SERVER).", typeDescriptionLink: "/docs/sdk/resources/commons#apiresourcekind" },
    slug: { type: "string", description: "Resource slug (user-friendly identifier, unique within org).", required: true },
    version: { type: "string", description: "Version of the resource (optional, only applicable to versioned resources like Skills)." },
  }}
/>

### OrganizationPreferencesInput

OrganizationPreferences holds organization-declared defaults that apply to every eligible agent execution in the organization.

<TypeTable
  type={{
    standingContext: { type: "string", description: "Free-text standing context injected into eligible agent executions in this organization." },
  }}
/>

### FindApiResourcesRequest

Input for find (list) operations with pagination.

<TypeTable
  type={{
    org: { type: "string", description: "Organization ID to filter by.", required: true },
    kind: { type: "string", description: "API resource kind (optional filter)." },
    page: { type: "PageInfoInput", description: "Pagination info." },
    pageNumber: { type: "number", description: "Page number (1-indexed) - deprecated, use page.num." },
    pageSize: { type: "number", description: "Number of items per page (default: 20, max: 100) - deprecated, use page.size." },
  }}
/>

### OrganizationList

Response for a paginated organization list query.

<TypeTable
  type={{
    totalPages: { type: "number", description: "Total number of pages." },
    entries: { type: "OrganizationInput[]", description: "Organizations in the current page.", typeDescriptionLink: "#organizationinput" },
  }}
/>

### Organizations

List of organizations.

<TypeTable
  type={{
    entries: { type: "OrganizationInput[]", description: "Organization resources in this list.", typeDescriptionLink: "#organizationinput" },
  }}
/>

### OrganizationExternalLookup

Input for looking up a platform-managed organization by its external platform coordinates.

<TypeTable
  type={{
    identityProviderRef: { type: "ResourceRef", description: "Reference to the IdentityProvider (org + slug) that manages this organization.", typeDescriptionLink: "#resourceref", required: true },
    externalOrgId: { type: "string", description: "The external platform's organization identifier." },
  }}
/>

### Organization

Organization is the top-level container for all Stigmer resources.

<TypeTable
  type={{
    apiVersion: { type: "string", description: "API version for this resource type." },
    kind: { type: "string", description: "Resource kind identifier." },
    metadata: { type: "ApiResourceMetadata", description: "Resource metadata including id, name, org, slug, labels, visibility, and timestamps.", typeDescriptionLink: "/docs/sdk/resources/commons#apiresourcemetadata" },
    spec: { type: "OrganizationInput", description: "Resource specification. See [`OrganizationInput`](#organizationinput).", typeDescriptionLink: "#organizationinput" },
    status: { type: "OrganizationStatus", description: "System-managed state. See `OrganizationStatus`." },
  }}
/>

### ManagementMode

ManagementMode defines how an organization is operated.

| Value | Description |
|-------|-------------|
| `self_managed` | Organization created and operated directly by users via Stigmer UI, CLI, or API. |
| `platform_managed` | Organization created programmatically by an external platform via an IdentityProvider. |

