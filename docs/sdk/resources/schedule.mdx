---
title: Schedule
---

{/* Auto-generated by stigmer-codegen. DO NOT EDIT. */}

A Schedule runs an agent on a recurring cron schedule. The spec declares
the target agent and the prompt each run starts from, a cron expression in
the classic 5-field form (or an `@daily`-style shorthand), the IANA time
zone the expression is evaluated in, and whether the schedule is enabled.
Each fire creates a fresh agent execution in a new session, with the fire
time appended to the configured message so the agent knows the current
date. Firing observations — next fire time, last execution, failure
streak, and any platform pause reason — live in status; applying a
manifest never touches them. Disabling a schedule pauses firing while
preserving the schedule and its history; deleting it stops firing
permanently without touching the referenced agent or past executions.

`trigger` fires a schedule once, immediately (`stigmer schedule trigger`).
The manual fire runs through the schedule's own clock, so it records on
status and its outcome feeds the failure streak exactly like a cron fire;
a disabled or platform-paused schedule refuses instead of firing.

In the open-source edition, schedules fire while `stigmer up` is running.
The daemon is user-session scoped: after a reboot, nothing fires until you
run `stigmer up` again. A fire missed while the daemon (or your laptop)
was down lands within the 60-minute catch-up window once it returns, and
is skipped entirely after that.

```yaml
apiVersion: agentic.stigmer.ai/v1
kind: Schedule
metadata:
  name: daily-fee-reminders
  org: workshop
spec:
  cron: "0 9 * * *"
  time_zone: Asia/Kolkata
  enabled: true
  agent:
    agent_ref:
      kind: agent
      org: workshop
      slug: support-agent
    message: Send fee reminders to members whose dues fall in the next 3 days.
```

## Client Access

<SDKTabs>
<Tab value="TypeScript">
```typescript
const schedule = await stigmer.schedule.get("sch_01j5q3k7m8r2s4tnz2hfp0q0c3");
```
</Tab>
<Tab value="Go">
```go
schedule, err := client.Schedule.Get(ctx, "sch_01j5q3k7m8r2s4tnz2hfp0q0c3")
```
</Tab>
<Tab value="Python">
```python
schedule = client.schedules.get("sch_01j5q3k7m8r2s4tnz2hfp0q0c3")
```
</Tab>
<Tab value="Java">
```java
Schedule schedule = client.schedules().get("sch_01j5q3k7m8r2s4tnz2hfp0q0c3");
```
</Tab>
</SDKTabs>

## Methods

| Method | Description |
|--------|-------------|
| [`get`](#get) | Get a single schedule by ID. |
| [`getByAgent`](#getbyagent) | Get all schedules of a specific agent. |
| [`getByReference`](#getbyreference) | Get a schedule by its organization-scoped reference (org/slug). |
| [`list`](#list) | List schedules with optional label filtering. |
| [`listRuns`](#listruns) | List a schedule's run history, newest first. |
| [`apply`](#apply) | Create or update a schedule. |
| [`create`](#create) | Create a schedule. |
| [`update`](#update) | Update an existing schedule. |
| [`delete`](#delete) | Delete a schedule. |
| [`resume`](#resume) | Resume a schedule the platform paused after repeated run failures. |
| [`trigger`](#trigger) | Trigger a schedule to fire once, immediately, and answer with the run's real outcome. |

### get

Get a single schedule by ID.

<SDKTabs>
<Tab value="TypeScript">
```typescript
const schedule = await stigmer.schedule.get(id);
```
</Tab>
<Tab value="Go">
```go
schedule, err := client.Schedule.Get(ctx, id)
```
</Tab>
<Tab value="Python">
```python
schedule = client.schedules.get(id)
```
</Tab>
<Tab value="Java">
```java
Schedule schedule = client.schedules().get(id);
```
</Tab>
</SDKTabs>

**Parameters**

| Parameter | Type | Description |
|-----------|------|-------------|
| `id` | `string` | Schedule identifier. |

**Returns**: [`Schedule`](#schedule)

### getByAgent

Get all schedules of a specific agent. Returns only schedules the caller has access to.

This is how the agent's operational surfaces and CLI resolve an agent's existing schedules regardless of slug.

<SDKTabs>
<Tab value="TypeScript">
```typescript
const result = await stigmer.schedule.getByAgent({
  agentId: "...",
  pageInfo: {},
  org: "...",
});
```
</Tab>
<Tab value="Go">
```go
result, err := client.Schedule.GetByAgent(ctx, &stigmer.GetSchedulesByAgentRequest{
  AgentId:  "...",
  PageInfo: &stigmer.PageInfoInput{},
  Org:      "...",
})
```
</Tab>
<Tab value="Python">
```python
result = client.schedules.get_by_agent(GetSchedulesByAgentRequest(
    agent_id="...",
    page_info=None,
    org="...",
))
```
</Tab>
<Tab value="Java">
```java
ScheduleList result = client.schedules().getByAgent(GetSchedulesByAgentRequest.builder()
    .agentId("...")
    .pageInfo(null)
    .org("...")
    .build());
```
</Tab>
</SDKTabs>

**Parameters**: [`GetSchedulesByAgentRequest`](#getschedulesbyagentrequest)

**Returns**: [`ScheduleList`](#schedulelist)

### getByReference

Get a schedule by its organization-scoped reference (org/slug).

<SDKTabs>
<Tab value="TypeScript">
```typescript
const schedule = await stigmer.schedule.getByReference({
  org: "...",
  kind: "...",
  slug: "...",
  version: "...",
});
```
</Tab>
<Tab value="Go">
```go
schedule, err := client.Schedule.GetByReference(ctx, &stigmer.ApiResourceReference{
  Org:     "...",
  Kind:    "...",
  Slug:    "...",
  Version: "...",
})
```
</Tab>
<Tab value="Python">
```python
schedule = client.schedules.get_by_reference(ApiResourceReference(
    org="...",
    kind="...",
    slug="...",
    version="...",
))
```
</Tab>
<Tab value="Java">
```java
Schedule schedule = client.schedules().getByReference(ApiResourceReference.builder()
    .org("...")
    .kind("...")
    .slug("...")
    .version("...")
    .build());
```
</Tab>
</SDKTabs>

**Parameters**: [`ResourceRef`](#resourceref)

**Returns**: [`Schedule`](#schedule)

### list

List schedules with optional label filtering.

<SDKTabs>
<Tab value="TypeScript">
```typescript
const result = await stigmer.schedule.list({
  org: "...",
  labels: {},
  pageInfo: {},
});
```
</Tab>
<Tab value="Go">
```go
result, err := client.Schedule.List(ctx, &stigmer.ListSchedulesRequest{
  Org:      "...",
  Labels:   map[string]string{},
  PageInfo: &stigmer.PageInfoInput{},
})
```
</Tab>
<Tab value="Python">
```python
result = client.schedules.list(ListSchedulesRequest(
    org="...",
    labels={},
    page_info=None,
))
```
</Tab>
<Tab value="Java">
```java
ScheduleList result = client.schedules().list(ListSchedulesRequest.builder()
    .org("...")
    .labels(Map.of())
    .pageInfo(null)
    .build());
```
</Tab>
</SDKTabs>

**Parameters**: [`ListSchedulesRequest`](#listschedulesrequest)

**Returns**: [`ScheduleList`](#schedulelist)

### listRuns

List a schedule's run history, newest first.

Every fire leaves a row — including fires that created no execution (a refused launch gate, a missing target agent) — with the refusing gate's copy verbatim. This is the surface that explains status.consecutive_failures.

<SDKTabs>
<Tab value="TypeScript">
```typescript
const result = await stigmer.schedule.listRuns({
  scheduleId: "...",
  pageInfo: {},
});
```
</Tab>
<Tab value="Go">
```go
result, err := client.Schedule.ListRuns(ctx, &stigmer.ListScheduleRunsRequest{
  ScheduleId: "...",
  PageInfo:   &stigmer.PageInfoInput{},
})
```
</Tab>
<Tab value="Python">
```python
result = client.schedules.list_runs(ListScheduleRunsRequest(
    schedule_id="...",
    page_info=None,
))
```
</Tab>
<Tab value="Java">
```java
ScheduleRunList result = client.schedules().listRuns(ListScheduleRunsRequest.builder()
    .scheduleId("...")
    .pageInfo(null)
    .build());
```
</Tab>
</SDKTabs>

**Parameters**: [`ListScheduleRunsRequest`](#listschedulerunsrequest)

**Returns**: [`ScheduleRunList`](#schedulerunlist)

### apply

Create or update a schedule.

<SDKTabs>
<Tab value="TypeScript">
```typescript
const schedule = await stigmer.schedule.apply({
  name: "my-schedule",
  org: "acme",
  slug: "...",
  labels: {},
  cron: "...",
  timeZone: "...",
  enabled: false,
  agent: {},
});
```
</Tab>
<Tab value="Go">
```go
schedule, err := client.Schedule.Apply(ctx, &stigmer.ScheduleInput{
  Name:     "my-schedule",
  Org:      "acme",
  Slug:     "...",
  Labels:   map[string]string{},
  Cron:     "...",
  TimeZone: "...",
  Enabled:  false,
  Agent:    &stigmer.AgentInvocationInput{},
})
```
</Tab>
<Tab value="Python">
```python
schedule = client.schedules.apply(ScheduleInput(
    name="my-schedule",
    org="acme",
    slug="...",
    labels={},
    cron="...",
    time_zone="...",
    enabled=False,
    agent=None,
))
```
</Tab>
<Tab value="Java">
```java
Schedule schedule = client.schedules().apply(ScheduleInput.builder()
    .name("my-schedule")
    .org("acme")
    .slug("...")
    .labels(Map.of())
    .cron("...")
    .timeZone("...")
    .enabled(false)
    .agent(null)
    .build());
```
</Tab>
</SDKTabs>

**Parameters**: [`ScheduleInput`](#scheduleinput)

**Returns**: [`Schedule`](#schedule)

### create

Create a schedule.

Scheduling an agent is a billing-affecting decision: every fire creates an execution that consumes the schedule-owning organization's credits, unattended.

<SDKTabs>
<Tab value="TypeScript">
```typescript
const schedule = await stigmer.schedule.create({
  name: "my-schedule",
  org: "acme",
  slug: "...",
  labels: {},
  cron: "...",
  timeZone: "...",
  enabled: false,
  agent: {},
});
```
</Tab>
<Tab value="Go">
```go
schedule, err := client.Schedule.Create(ctx, &stigmer.ScheduleInput{
  Name:     "my-schedule",
  Org:      "acme",
  Slug:     "...",
  Labels:   map[string]string{},
  Cron:     "...",
  TimeZone: "...",
  Enabled:  false,
  Agent:    &stigmer.AgentInvocationInput{},
})
```
</Tab>
<Tab value="Python">
```python
schedule = client.schedules.create(ScheduleInput(
    name="my-schedule",
    org="acme",
    slug="...",
    labels={},
    cron="...",
    time_zone="...",
    enabled=False,
    agent=None,
))
```
</Tab>
<Tab value="Java">
```java
Schedule schedule = client.schedules().create(ScheduleInput.builder()
    .name("my-schedule")
    .org("acme")
    .slug("...")
    .labels(Map.of())
    .cron("...")
    .timeZone("...")
    .enabled(false)
    .agent(null)
    .build());
```
</Tab>
</SDKTabs>

**Parameters**: [`ScheduleInput`](#scheduleinput)

**Returns**: [`Schedule`](#schedule)

### update

Update an existing schedule.

Replaces the spec wholesale. The slug, the referenced agent, and the target arm are immutable; cron, time zone, enablement, and the message may all change. Status (firing observations, the platform pause) is never touched by updates — use resume to clear a platform pause.

<SDKTabs>
<Tab value="TypeScript">
```typescript
const schedule = await stigmer.schedule.update({
  name: "my-schedule",
  org: "acme",
  slug: "...",
  labels: {},
  cron: "...",
  timeZone: "...",
  enabled: false,
  agent: {},
});
```
</Tab>
<Tab value="Go">
```go
schedule, err := client.Schedule.Update(ctx, &stigmer.ScheduleInput{
  Name:     "my-schedule",
  Org:      "acme",
  Slug:     "...",
  Labels:   map[string]string{},
  Cron:     "...",
  TimeZone: "...",
  Enabled:  false,
  Agent:    &stigmer.AgentInvocationInput{},
})
```
</Tab>
<Tab value="Python">
```python
schedule = client.schedules.update(ScheduleInput(
    name="my-schedule",
    org="acme",
    slug="...",
    labels={},
    cron="...",
    time_zone="...",
    enabled=False,
    agent=None,
))
```
</Tab>
<Tab value="Java">
```java
Schedule schedule = client.schedules().update(ScheduleInput.builder()
    .name("my-schedule")
    .org("acme")
    .slug("...")
    .labels(Map.of())
    .cron("...")
    .timeZone("...")
    .enabled(false)
    .agent(null)
    .build());
```
</Tab>
</SDKTabs>

**Parameters**: [`ScheduleInput`](#scheduleinput)

**Returns**: [`Schedule`](#schedule)

### delete

Delete a schedule.

Firing stops permanently. Executions created by past fires are untouched. To stop firing while keeping the schedule and its history, disable it (enabled=false) instead.

<SDKTabs>
<Tab value="TypeScript">
```typescript
const schedule = await stigmer.schedule.delete(id);
```
</Tab>
<Tab value="Go">
```go
schedule, err := client.Schedule.Delete(ctx, id)
```
</Tab>
<Tab value="Python">
```python
schedule = client.schedules.delete(id)
```
</Tab>
<Tab value="Java">
```java
Schedule schedule = client.schedules().delete(id);
```
</Tab>
</SDKTabs>

**Parameters**

| Parameter | Type | Description |
|-----------|------|-------------|
| `id` | `string` | Schedule identifier. |

**Returns**: [`Schedule`](#schedule)

### resume

Resume a schedule the platform paused after repeated run failures.

Clears status.paused_reason, resets status.consecutive_failures, and re-arms the clock. Resuming a schedule that is not paused succeeds and changes nothing. A disabled schedule stays disabled: resume clears the platform's pause, not the owner's switch (spec.enabled).

<SDKTabs>
<Tab value="TypeScript">
```typescript
const schedule = await stigmer.schedule.resume(id);
```
</Tab>
<Tab value="Go">
```go
schedule, err := client.Schedule.Resume(ctx, id)
```
</Tab>
<Tab value="Python">
```python
schedule = client.schedules.resume(id)
```
</Tab>
<Tab value="Java">
```java
Schedule schedule = client.schedules().resume(id);
```
</Tab>
</SDKTabs>

**Parameters**

| Parameter | Type | Description |
|-----------|------|-------------|
| `id` | `string` | Schedule identifier. |

**Returns**: [`Schedule`](#schedule)

### trigger

Trigger a schedule to fire once, immediately, and answer with the run's real outcome.

The manual fire runs synchronously through the standard execution create pipeline — every launch gate runs — and the result names what happened: the created execution's id, or the refusing gate's own copy verbatim. status.last_fire_at and status.last_execution_id record a started run. Manual fires do NOT feed the failure streak — the streak is the unattended (cron) health signal, and a test fire of a broken schedule must not race its owner to the pause threshold.

A disabled schedule refuses (enable it first). A platform-paused schedule MAY be triggered — a test fire is exactly how an owner verifies a fix before resuming; resume remains the one path that clears the pause.

<SDKTabs>
<Tab value="TypeScript">
```typescript
const scheduleTriggerResult = await stigmer.schedule.trigger(id);
```
</Tab>
<Tab value="Go">
```go
scheduleTriggerResult, err := client.Schedule.Trigger(ctx, id)
```
</Tab>
<Tab value="Python">
```python
schedule_trigger_result = client.schedules.trigger(id)
```
</Tab>
<Tab value="Java">
```java
ScheduleTriggerResult scheduleTriggerResult = client.schedules().trigger(id);
```
</Tab>
</SDKTabs>

**Parameters**

| Parameter | Type | Description |
|-----------|------|-------------|
| `id` | `string` | Schedule identifier. |

**Returns**: [`ScheduleTriggerResult`](#scheduletriggerresult)

## Types

### ScheduleInput

Input for creating or updating a Schedule.

<TypeTable
  type={{
    name: { type: "string", description: "Resource name.", required: true },
    slug: { type: "string", description: "URL-friendly identifier." },
    org: { type: "string", description: "Organization slug.", required: true },
    labels: { type: "Record<string, string>", description: "Key-value labels." },
    cron: { type: "string", description: "Cron expression in the classic 5-field form (minute, hour, day-of-month, month, day-of-week), evaluated in `time_zone` — for example \"0 9 * * *\" for daily at 09:00." },
    timeZone: { type: "string", description: "IANA time zone the cron is evaluated in, e.g. \"Asia/Kolkata\"." },
    enabled: { type: "boolean", description: "Whether the schedule fires." },
    agent: { type: "AgentInvocationInput", description: "Run an agent with a configured prompt at each fire.", typeDescriptionLink: "#agentinvocationinput" },
  }}
/>

### AgentInvocationInput

AgentInvocation is what a caller may ask an agent to do.

<TypeTable
  type={{
    agentRef: { type: "ResourceRef", description: "Reference to the agent to run.", typeDescriptionLink: "#resourceref", required: true },
    message: { type: "string", description: "Prompt the run starts from." },
    harness: { type: "Harness", description: "Execution engine for the run's session.", typeDescriptionLink: "#harness" },
    workspaceEntries: { type: "WorkspaceEntryInput[]", description: "Workspace the run's session operates on.", typeDescriptionLink: "#workspaceentryinput" },
    environmentRefs: { type: "ResourceRef[]", description: "References to Environment resources whose values are provided to the runs this invocation creates.", typeDescriptionLink: "#resourceref" },
    runConfig: { type: "RunConfigInput", description: "Per-invocation model choice and run bounds.", typeDescriptionLink: "#runconfiginput" },
  }}
/>

### ResourceRef

Generic reference to any API resource by org and slug.

<TypeTable
  type={{
    org: { type: "string", description: "Organization that owns the referenced resource." },
    kind: { type: "ApiResourceKind", description: "Kind of the referenced resource (e.g., SKILL, AGENT, MCP_SERVER).", typeDescriptionLink: "/docs/sdk/resources/commons#apiresourcekind" },
    slug: { type: "string", description: "Resource slug (user-friendly identifier, unique within org).", required: true },
    version: { type: "string", description: "Version of the resource (optional, only applicable to versioned resources like Skills)." },
  }}
/>

### WorkspaceEntryInput

WorkspaceEntry pairs a WorkspaceSource with a human-readable name, forming an addressable unit within a session's workspace.

<TypeTable
  type={{
    name: { type: "string", description: "Short identifier for this workspace entry." },
    source: { type: "WorkspaceSourceInput", description: "The source that provides this entry's content.", typeDescriptionLink: "#workspacesourceinput", required: true },
  }}
/>

### WorkspaceSourceInput

WorkspaceSource defines where the workspace content comes from.

<TypeTable
  type={{
    gitRepo: { type: "GitRepoSourceInput", description: "Clone a git repository as the workspace source.", typeDescriptionLink: "#gitreposourceinput" },
    localPath: { type: "LocalPathSourceInput", description: "Use an existing local directory as the workspace source.", typeDescriptionLink: "#localpathsourceinput" },
  }}
/>

### GitRepoSourceInput

GitRepoSource provisions a workspace by cloning a git repository.

<TypeTable
  type={{
    url: { type: "string", description: "HTTPS clone URL for the repository.", required: true },
    branch: { type: "string", description: "Branch to clone." },
    commit: { type: "string", description: "Commit SHA to checkout after cloning." },
    depth: { type: "number", description: "Number of commits to include in the clone history." },
    writeBackMode: { type: "GitWriteBackMode", description: "Controls whether the platform creates a branch and pull request from the agent's file changes.", typeDescriptionLink: "#gitwritebackmode" },
  }}
/>

### LocalPathSourceInput

LocalPathSource uses an existing directory on the host filesystem as the workspace.

<TypeTable
  type={{
    path: { type: "string", description: "Absolute path to an existing directory on the host filesystem." },
  }}
/>

### RunConfigInput

RunConfig is the owner-settable model choice and run bounds.

<TypeTable
  type={{
    modelName: { type: "string", description: "The model each run uses." },
    maxCostUsd: { type: "number", description: "Maximum estimated cost in USD per run." },
    maxToolRounds: { type: "number", description: "Maximum model-to-tools reasoning cycles per run." },
    serviceTier: { type: "ServiceTier", description: "Service tier for each run's model calls: standard (the default) or fast, where fast bills at the model's fast-tier rates and requires a model that offers one.", typeDescriptionLink: "#servicetier" },
  }}
/>

### ScheduleTriggerResult

Result of manually triggering a schedule.

<TypeTable
  type={{
    schedule: { type: "ScheduleInput", description: "The schedule after the fire, status freshly stamped.", typeDescriptionLink: "#scheduleinput" },
    outcome: { type: "ScheduleRunOutcome", description: "What the fire produced: STARTED, REFUSED, or TARGET_MISSING (the terminal outcomes belong to run history — a manual fire answers at run start).", typeDescriptionLink: "#schedulerunoutcome" },
    executionId: { type: "string", description: "ID of the created execution." },
    refusalReason: { type: "string", description: "The refusing launch gate's copy, verbatim." },
  }}
/>

### GetSchedulesByAgentRequest

Input for retrieving all schedules of a specific agent.

<TypeTable
  type={{
    agentId: { type: "string", description: "Identifier of the agent whose schedules to retrieve.", required: true },
    pageInfo: { type: "PageInfoInput", description: "Pagination options." },
    org: { type: "string", description: "Organization slug to scope the results to." },
  }}
/>

### ScheduleList

Response containing a paginated list of schedules.

<TypeTable
  type={{
    totalCount: { type: "number", description: "Total number of schedules matching the query." },
    items: { type: "ScheduleInput[]", description: "Schedules in the current page.", typeDescriptionLink: "#scheduleinput" },
  }}
/>

### ListSchedulesRequest

Input for listing schedules within an organization.

<TypeTable
  type={{
    org: { type: "string", description: "Organization slug to scope the listing." },
    labels: { type: "Record<string, string>", description: "Filter by metadata labels." },
    pageInfo: { type: "PageInfoInput", description: "Pagination options." },
  }}
/>

### ListScheduleRunsRequest

Input for listing a schedule's run history.

<TypeTable
  type={{
    scheduleId: { type: "string", description: "Schedule whose runs to list.", required: true },
    pageInfo: { type: "PageInfoInput", description: "Pagination options." },
  }}
/>

### ScheduleRunList

Response containing a paginated list of schedule runs, newest first.

<TypeTable
  type={{
    totalCount: { type: "number", description: "Total number of recorded runs for the schedule." },
    items: { type: "ScheduleRunInput[]", description: "Runs in the current page." },
  }}
/>

### Schedule

Schedule runs an agent on a recurring cron schedule.

<TypeTable
  type={{
    apiVersion: { type: "string", description: "API version for this resource type." },
    kind: { type: "string", description: "Resource kind identifier." },
    metadata: { type: "ApiResourceMetadata", description: "Resource metadata including id, name, org, slug, labels, visibility, and timestamps.", typeDescriptionLink: "/docs/sdk/resources/commons#apiresourcemetadata" },
    spec: { type: "ScheduleInput", description: "Resource specification. See [`ScheduleInput`](#scheduleinput).", typeDescriptionLink: "#scheduleinput" },
    status: { type: "ScheduleStatus", description: "System-managed state. See [`ScheduleStatus`](#schedulestatus).", typeDescriptionLink: "#schedulestatus" },
  }}
/>

### ScheduleStatus

ScheduleStatus contains system-managed state for a schedule.

<TypeTable
  type={{
    nextFireAt: { type: "string", description: "When the schedule next fires, in UTC." },
    lastFireAt: { type: "string", description: "When the schedule last fired, in UTC." },
    lastExecutionId: { type: "string", description: "ID of the agent execution created by the most recent fire." },
    consecutiveFailures: { type: "number", description: "Number of consecutive failed runs." },
    pausedReason: { type: "string", description: "Why the platform paused this schedule; empty when not paused." },
    audit: { type: "ApiResourceAudit", description: "Standard audit information (created_at, updated_at, created_by, etc.).", typeDescriptionLink: "/docs/sdk/resources/commons#apiresourceaudit" },
  }}
/>

### ScheduleRunOutcome

What one schedule fire produced.

| Value | Description |
|-------|-------------|
| `SCHEDULE_RUN_OUTCOME_STARTED` | An execution was created (or idempotently re-found) and is running. |
| `SCHEDULE_RUN_OUTCOME_REFUSED` | A launch gate refused the run deterministically; reason carries the gate's copy verbatim. |
| `SCHEDULE_RUN_OUTCOME_TARGET_MISSING` | The schedule's agent_ref no longer resolves. |
| `SCHEDULE_RUN_OUTCOME_SKIPPED` | The fire no-opped: the row was deleted, disabled, or paused between the fire being recorded and the run starting. |
| `SCHEDULE_RUN_OUTCOME_COMPLETED` | The tracked run reached EXECUTION_COMPLETED. |
| `SCHEDULE_RUN_OUTCOME_FAILED` | The tracked run ended terminal-but-not-completed (failed, cancelled, or terminated); reason names the terminal phase. |
| `SCHEDULE_RUN_OUTCOME_TIMED_OUT` | The tracked run outlived the fire's tracking budget. |

### Harness

Harness identifies the execution engine that processes agent activities for a session.

| Value | Description |
|-------|-------------|
| `HARNESS_NATIVE` | Stigmer's native execution engine. |
| `HARNESS_CURSOR` | Cursor SDK execution engine. |

### GitWriteBackMode

GitWriteBackMode controls the platform's git workflow for a git-backed workspace entry.

| Value | Description |
|-------|-------------|
| `GIT_WRITE_BACK_BRANCH_AND_PR` | Create a branch and pull request from the agent's file changes after execution completes. |

### ServiceTier

ServiceTier selects the provider-side speed/service tier for an execution's model calls.

| Value | Description |
|-------|-------------|
| `SERVICE_TIER_STANDARD` | Standard tier: the model's base-priced configuration, requested explicitly. |
| `SERVICE_TIER_FAST` | Fast tier: the model's fast variant, billed at the registry's fast variant rates. |

### ScheduleRunOrigin

How a schedule fire was initiated.

| Value | Description |
|-------|-------------|
| `SCHEDULE_RUN_ORIGIN_CRON` | The clock fired the schedule on its cron cadence. |
| `SCHEDULE_RUN_ORIGIN_MANUAL` | A caller fired the schedule through the trigger command. |

