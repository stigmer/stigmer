---
title: Workflow Execution
---

{/* Auto-generated by stigmer-codegen. DO NOT EDIT. */}

A WorkflowExecution represents a single runtime invocation of a WorkflowInstance.
It captures the full lifecycle of a workflow run — from trigger through task-by-task
execution to completion or failure. Create a WorkflowExecution to start a workflow,
then read its status to track progress.

```yaml
apiVersion: agentic.stigmer.ai/v1
kind: WorkflowExecution
metadata:
  name: onboarding-20250111-143022
spec:
  workflow_instance_id: wfi-customer-onboarding-prod
  trigger_message: "New signup: john.doe@example.com"
  trigger_metadata:
    source: api
    caller_id: usr-jane-admin
  runtime_env:
    CUSTOMER_EMAIL:
      value: "john.doe@example.com"
```

## Client Access

<SDKTabs>
<Tab value="TypeScript">
```typescript
const workflowExecution = await stigmer.workflowExecution.get("wex_01j5q3k7m8r2s4tnz2hfp0q0c3");
```
</Tab>
<Tab value="Go">
```go
workflowExecution, err := client.WorkflowExecution.Get(ctx, "wex_01j5q3k7m8r2s4tnz2hfp0q0c3")
```
</Tab>
<Tab value="Python">
```python
workflow_execution = client.workflow_executions.get("wex_01j5q3k7m8r2s4tnz2hfp0q0c3")
```
</Tab>
<Tab value="Java">
```java
WorkflowExecution workflowExecution = client.workflowExecutions().get("wex_01j5q3k7m8r2s4tnz2hfp0q0c3");
```
</Tab>
</SDKTabs>

## Methods

| Method | Description | Streaming |
|--------|-------------|-----------|
| [`get`](#get) | Get a single workflow execution by ID. | |
| [`list`](#list) | List workflow executions with pagination and optional filtering. | |
| [`listByWorkflow`](#listbyworkflow) | List all executions for a specific Workflow or WorkflowInstance. | |
| [`subscribe`](#subscribe) | Subscribe to real-time updates for a specific workflow execution (server streaming). | Server |
| [`create`](#create) | Create and trigger a new workflow execution. | |
| [`update`](#update) | Update an existing workflow execution with full state. | |
| [`updateStatus`](#updatestatus) | Update execution status during workflow execution. | |
| [`delete`](#delete) | Delete a workflow execution. | |
| [`pause`](#pause) | Pause a running workflow execution. | |
| [`resume`](#resume) | Resume a paused workflow execution. | |
| [`cancel`](#cancel) | Cancel a running workflow execution gracefully. | |
| [`terminate`](#terminate) | Terminate a workflow execution immediately. | |
| [`recover`](#recover) | Recover a failed workflow execution from the last checkpoint. | |
| [`submitApproval`](#submitapproval) | Submit an approval decision for a child agent's tool execution. | |
| [`sendSignal`](#sendsignal) | Send a signal to a running workflow execution. | |

### get

Get a single workflow execution by ID.

Retrieves the complete WorkflowExecution resource including: - spec: User inputs (workflow_instance_id, trigger_message, etc.) - status: Current execution state (phase, tasks, progress_events, output/error) - metadata: Resource identification (id, name, labels, tags)

<SDKTabs>
<Tab value="TypeScript">
```typescript
const workflowExecution = await stigmer.workflowExecution.get(id);
```
</Tab>
<Tab value="Go">
```go
workflowExecution, err := client.WorkflowExecution.Get(ctx, id)
```
</Tab>
<Tab value="Python">
```python
workflow_execution = client.workflow_executions.get(id)
```
</Tab>
<Tab value="Java">
```java
WorkflowExecution workflowExecution = client.workflowExecutions().get(id);
```
</Tab>
</SDKTabs>

**Parameters**

| Parameter | Type | Description |
|-----------|------|-------------|
| `id` | `string` | Workflow Execution identifier. |

**Returns**: [`WorkflowExecution`](#workflowexecution)

### list

List workflow executions with pagination and optional filtering.

Returns a paginated list of WorkflowExecution resources that the user has access to. Results are automatically filtered based on user's permissions and owner scope.

<SDKTabs>
<Tab value="TypeScript">
```typescript
const result = await stigmer.workflowExecution.list({
  pageSize: 0,
  pageToken: "...",
  phase: "...",
  tags: [],
});
```
</Tab>
<Tab value="Go">
```go
result, err := client.WorkflowExecution.List(ctx, &stigmer.ListWorkflowExecutionsRequest{
  PageSize:  0,
  PageToken: "...",
  Phase:     "...",
  Tags:      []string{},
})
```
</Tab>
<Tab value="Python">
```python
result = client.workflow_executions.list(ListWorkflowExecutionsRequest(
    page_size=0,
    page_token="...",
    phase="...",
    tags=[],
))
```
</Tab>
<Tab value="Java">
```java
WorkflowExecutionList result = client.workflowExecutions().list(ListWorkflowExecutionsRequest.builder()
    .pageSize(0)
    .pageToken("...")
    .phase("...")
    .tags(List.of())
    .build());
```
</Tab>
</SDKTabs>

**Parameters**: [`ListWorkflowExecutionsRequest`](#listworkflowexecutionsrequest)

**Returns**: [`WorkflowExecutionList`](#workflowexecutionlist)

### listByWorkflow

List all executions for a specific Workflow or WorkflowInstance.

Returns executions filtered by a specific Workflow ID. This is useful for viewing execution history of a particular workflow.

<SDKTabs>
<Tab value="TypeScript">
```typescript
const result = await stigmer.workflowExecution.listByWorkflow({
  workflowId: "...",
  pageSize: 0,
  pageToken: "...",
});
```
</Tab>
<Tab value="Go">
```go
result, err := client.WorkflowExecution.ListByWorkflow(ctx, &stigmer.ListWorkflowExecutionsByWorkflowRequest{
  WorkflowId: "...",
  PageSize:   0,
  PageToken:  "...",
})
```
</Tab>
<Tab value="Python">
```python
result = client.workflow_executions.list_by_workflow(ListWorkflowExecutionsByWorkflowRequest(
    workflow_id="...",
    page_size=0,
    page_token="...",
))
```
</Tab>
<Tab value="Java">
```java
WorkflowExecutionList result = client.workflowExecutions().listByWorkflow(ListWorkflowExecutionsByWorkflowRequest.builder()
    .workflowId("...")
    .pageSize(0)
    .pageToken("...")
    .build());
```
</Tab>
</SDKTabs>

**Parameters**: [`ListWorkflowExecutionsByWorkflowRequest`](#listworkflowexecutionsbyworkflowrequest)

**Returns**: [`WorkflowExecutionList`](#workflowexecutionlist)

### subscribe

> **Server Streaming** — This method returns a stream of updates, not a single response.

Subscribe to real-time updates for a specific workflow execution (server streaming).

Opens a bidirectional stream that pushes WorkflowExecution updates as they occur. Client receives updates when: - Execution phase changes (PENDING → IN_PROGRESS → COMPLETED) - Tasks start or complete - Progress events are appended - Output or error fields are set

<SDKTabs>
<Tab value="TypeScript">
```typescript
const stream = stigmer.workflowExecution.subscribe(input);
for await (const workflowExecution of stream) {
  // process workflowExecution
}
```
</Tab>
<Tab value="Go">
```go
stream, err := client.WorkflowExecution.Subscribe(ctx, input)
for {
    workflowExecution, err := stream.Recv()
    if err != nil {
        break
    }
    // process workflowExecution
}
```
</Tab>
<Tab value="Python">
```python
for workflow_execution in client.workflow_executions.subscribe(input):
    # process workflow_execution
```
</Tab>
<Tab value="Java">
```java
client.workflowExecutions().subscribe(input, workflowExecution -> {
    // process workflowExecution
});
```
</Tab>
</SDKTabs>

**Parameters**: [`SubscribeWorkflowExecutionRequest`](#subscribeworkflowexecutionrequest)

**Returns**: Stream&lt;[`WorkflowExecution`](#workflowexecution)&gt;

### create

Create and trigger a new workflow execution.

This RPC creates a WorkflowExecution resource and immediately triggers it for execution. The workflow execution engine picks up the execution and begins processing tasks.

<SDKTabs>
<Tab value="TypeScript">
```typescript
const workflowExecution = await stigmer.workflowExecution.create({
  name: "my-workflow-execution",
  org: "acme",
  slug: "...",
  labels: {},
  workflowInstanceId: "...",
  workflowId: "...",
  triggerMessage: "...",
  triggerMetadata: {},
  runtimeEnv: {},
  callbackToken: "...",
});
```
</Tab>
<Tab value="Go">
```go
workflowExecution, err := client.WorkflowExecution.Create(ctx, &stigmer.WorkflowExecutionInput{
  Name:               "my-workflow-execution",
  Org:                "acme",
  Slug:               "...",
  Labels:             map[string]string{},
  WorkflowInstanceId: "...",
  WorkflowId:         "...",
  TriggerMessage:     "...",
  TriggerMetadata:    map[string]string{},
  RuntimeEnv:         map[string]string{},
  CallbackToken:      "...",
})
```
</Tab>
<Tab value="Python">
```python
workflow_execution = client.workflow_executions.create(WorkflowExecutionInput(
    name="my-workflow-execution",
    org="acme",
    slug="...",
    labels={},
    workflow_instance_id="...",
    workflow_id="...",
    trigger_message="...",
    trigger_metadata={},
    runtime_env={},
    callback_token="...",
))
```
</Tab>
<Tab value="Java">
```java
WorkflowExecution workflowExecution = client.workflowExecutions().create(WorkflowExecutionInput.builder()
    .name("my-workflow-execution")
    .org("acme")
    .slug("...")
    .labels(Map.of())
    .workflowInstanceId("...")
    .workflowId("...")
    .triggerMessage("...")
    .triggerMetadata(Map.of())
    .runtimeEnv(Map.of())
    .callbackToken("...")
    .build());
```
</Tab>
</SDKTabs>

**Parameters**: [`WorkflowExecutionInput`](#workflowexecutioninput)

**Returns**: [`WorkflowExecution`](#workflowexecution)

### update

Update an existing workflow execution with full state.

<SDKTabs>
<Tab value="TypeScript">
```typescript
const workflowExecution = await stigmer.workflowExecution.update({
  name: "my-workflow-execution",
  org: "acme",
  slug: "...",
  labels: {},
  workflowInstanceId: "...",
  workflowId: "...",
  triggerMessage: "...",
  triggerMetadata: {},
  runtimeEnv: {},
  callbackToken: "...",
});
```
</Tab>
<Tab value="Go">
```go
workflowExecution, err := client.WorkflowExecution.Update(ctx, &stigmer.WorkflowExecutionInput{
  Name:               "my-workflow-execution",
  Org:                "acme",
  Slug:               "...",
  Labels:             map[string]string{},
  WorkflowInstanceId: "...",
  WorkflowId:         "...",
  TriggerMessage:     "...",
  TriggerMetadata:    map[string]string{},
  RuntimeEnv:         map[string]string{},
  CallbackToken:      "...",
})
```
</Tab>
<Tab value="Python">
```python
workflow_execution = client.workflow_executions.update(WorkflowExecutionInput(
    name="my-workflow-execution",
    org="acme",
    slug="...",
    labels={},
    workflow_instance_id="...",
    workflow_id="...",
    trigger_message="...",
    trigger_metadata={},
    runtime_env={},
    callback_token="...",
))
```
</Tab>
<Tab value="Java">
```java
WorkflowExecution workflowExecution = client.workflowExecutions().update(WorkflowExecutionInput.builder()
    .name("my-workflow-execution")
    .org("acme")
    .slug("...")
    .labels(Map.of())
    .workflowInstanceId("...")
    .workflowId("...")
    .triggerMessage("...")
    .triggerMetadata(Map.of())
    .runtimeEnv(Map.of())
    .callbackToken("...")
    .build());
```
</Tab>
</SDKTabs>

**Parameters**: [`WorkflowExecutionInput`](#workflowexecutioninput)

**Returns**: [`WorkflowExecution`](#workflowexecution)

### updateStatus

Update execution status during workflow execution.

<SDKTabs>
<Tab value="TypeScript">
```typescript
const workflowExecution = await stigmer.workflowExecution.updateStatus({
  executionId: "...",
  status: {},
});
```
</Tab>
<Tab value="Go">
```go
workflowExecution, err := client.WorkflowExecution.UpdateStatus(ctx, &stigmer.WorkflowExecutionUpdateStatusInput{
  ExecutionId: "...",
  Status:      &stigmer.WorkflowExecutionStatusInput{},
})
```
</Tab>
<Tab value="Python">
```python
workflow_execution = client.workflow_executions.update_status(WorkflowExecutionUpdateStatusInput(
    execution_id="...",
    status=None,
))
```
</Tab>
<Tab value="Java">
```java
WorkflowExecution workflowExecution = client.workflowExecutions().updateStatus(WorkflowExecutionUpdateStatusInput.builder()
    .executionId("...")
    .status(null)
    .build());
```
</Tab>
</SDKTabs>

**Parameters**: [`WorkflowExecutionUpdateStatusInput`](#workflowexecutionupdatestatusinput)

**Returns**: [`WorkflowExecution`](#workflowexecution)

### delete

Delete a workflow execution.

<SDKTabs>
<Tab value="TypeScript">
```typescript
const workflowExecution = await stigmer.workflowExecution.delete(id);
```
</Tab>
<Tab value="Go">
```go
workflowExecution, err := client.WorkflowExecution.Delete(ctx, id)
```
</Tab>
<Tab value="Python">
```python
workflow_execution = client.workflow_executions.delete(id)
```
</Tab>
<Tab value="Java">
```java
WorkflowExecution workflowExecution = client.workflowExecutions().delete(id);
```
</Tab>
</SDKTabs>

**Parameters**

| Parameter | Type | Description |
|-----------|------|-------------|
| `id` | `string` | Workflow Execution identifier. |

**Returns**: [`WorkflowExecution`](#workflowexecution)

### pause

Pause a running workflow execution.

Temporarily stops the workflow at its current checkpoint. Unlike cancel, the execution is NOT terminal and can be resumed later from where it left off. The workflow gracefully checkpoints and exits, preserving all progress.

<SDKTabs>
<Tab value="TypeScript">
```typescript
const workflowExecution = await stigmer.workflowExecution.pause({
  id: "...",
  reason: "...",
});
```
</Tab>
<Tab value="Go">
```go
workflowExecution, err := client.WorkflowExecution.Pause(ctx, &stigmer.PauseWorkflowExecutionInput{
  Id:     "...",
  Reason: "...",
})
```
</Tab>
<Tab value="Python">
```python
workflow_execution = client.workflow_executions.pause(PauseWorkflowExecutionInput(
    id="...",
    reason="...",
))
```
</Tab>
<Tab value="Java">
```java
WorkflowExecution workflowExecution = client.workflowExecutions().pause(PauseWorkflowExecutionInput.builder()
    .id("...")
    .reason("...")
    .build());
```
</Tab>
</SDKTabs>

**Parameters**: [`PauseWorkflowExecutionInput`](#pauseworkflowexecutioninput)

**Returns**: [`WorkflowExecution`](#workflowexecution)

### resume

Resume a paused workflow execution.

Continues execution from the checkpoint where it was paused. The workflow re-invokes activities with the same thread_id, which loads from checkpoint and continues from where it left off.

<SDKTabs>
<Tab value="TypeScript">
```typescript
const workflowExecution = await stigmer.workflowExecution.resume({
  id: "...",
});
```
</Tab>
<Tab value="Go">
```go
workflowExecution, err := client.WorkflowExecution.Resume(ctx, &stigmer.ResumeWorkflowExecutionInput{
  Id: "...",
})
```
</Tab>
<Tab value="Python">
```python
workflow_execution = client.workflow_executions.resume(ResumeWorkflowExecutionInput(
    id="...",
))
```
</Tab>
<Tab value="Java">
```java
WorkflowExecution workflowExecution = client.workflowExecutions().resume(ResumeWorkflowExecutionInput.builder()
    .id("...")
    .build());
```
</Tab>
</SDKTabs>

**Parameters**: [`ResumeWorkflowExecutionInput`](#resumeworkflowexecutioninput)

**Returns**: [`WorkflowExecution`](#workflowexecution)

### cancel

Cancel a running workflow execution gracefully.

Sends a cancellation signal to the workflow. The workflow code can handle the cancellation signal to perform cleanup (e.g., compensation logic, resource cleanup, notifications) before transitioning to the CANCELLED phase.

<SDKTabs>
<Tab value="TypeScript">
```typescript
const workflowExecution = await stigmer.workflowExecution.cancel({
  id: "...",
  reason: "...",
});
```
</Tab>
<Tab value="Go">
```go
workflowExecution, err := client.WorkflowExecution.Cancel(ctx, &stigmer.CancelWorkflowExecutionInput{
  Id:     "...",
  Reason: "...",
})
```
</Tab>
<Tab value="Python">
```python
workflow_execution = client.workflow_executions.cancel(CancelWorkflowExecutionInput(
    id="...",
    reason="...",
))
```
</Tab>
<Tab value="Java">
```java
WorkflowExecution workflowExecution = client.workflowExecutions().cancel(CancelWorkflowExecutionInput.builder()
    .id("...")
    .reason("...")
    .build());
```
</Tab>
</SDKTabs>

**Parameters**: [`CancelWorkflowExecutionInput`](#cancelworkflowexecutioninput)

**Returns**: [`WorkflowExecution`](#workflowexecution)

### terminate

Terminate a workflow execution immediately.

Force-stops the workflow without allowing cleanup. Unlike cancel, the workflow code cannot respond to termination - it is stopped immediately. Use this for stuck or unresponsive workflows that don't respond to cancellation.

<SDKTabs>
<Tab value="TypeScript">
```typescript
const workflowExecution = await stigmer.workflowExecution.terminate({
  id: "...",
  reason: "...",
});
```
</Tab>
<Tab value="Go">
```go
workflowExecution, err := client.WorkflowExecution.Terminate(ctx, &stigmer.TerminateWorkflowExecutionInput{
  Id:     "...",
  Reason: "...",
})
```
</Tab>
<Tab value="Python">
```python
workflow_execution = client.workflow_executions.terminate(TerminateWorkflowExecutionInput(
    id="...",
    reason="...",
))
```
</Tab>
<Tab value="Java">
```java
WorkflowExecution workflowExecution = client.workflowExecutions().terminate(TerminateWorkflowExecutionInput.builder()
    .id("...")
    .reason("...")
    .build());
```
</Tab>
</SDKTabs>

**Parameters**: [`TerminateWorkflowExecutionInput`](#terminateworkflowexecutioninput)

**Returns**: [`WorkflowExecution`](#workflowexecution)

### recover

Recover a failed workflow execution from the last checkpoint.

Resumes execution from the last successful point. Completed work is preserved - successful tasks are NOT re-executed. This enables "retry and resume" semantics without duplicating side effects.

<SDKTabs>
<Tab value="TypeScript">
```typescript
const workflowExecution = await stigmer.workflowExecution.recover({
  id: "...",
  reason: "...",
});
```
</Tab>
<Tab value="Go">
```go
workflowExecution, err := client.WorkflowExecution.Recover(ctx, &stigmer.RecoverWorkflowExecutionInput{
  Id:     "...",
  Reason: "...",
})
```
</Tab>
<Tab value="Python">
```python
workflow_execution = client.workflow_executions.recover(RecoverWorkflowExecutionInput(
    id="...",
    reason="...",
))
```
</Tab>
<Tab value="Java">
```java
WorkflowExecution workflowExecution = client.workflowExecutions().recover(RecoverWorkflowExecutionInput.builder()
    .id("...")
    .reason("...")
    .build());
```
</Tab>
</SDKTabs>

**Parameters**: [`RecoverWorkflowExecutionInput`](#recoverworkflowexecutioninput)

**Returns**: [`WorkflowExecution`](#workflowexecution)

### submitApproval

Submit an approval decision for a child agent's tool execution.

This RPC forwards the approval decision to the child AgentExecution that is waiting for approval. The child is identified by the child_agent_execution_id in status.pending_approval.

When a workflow invokes an agent that requires tool approval, the approval request surfaces at the workflow level via status.pending_approval. Users can submit their decision through this RPC, which forwards it to the child agent.

<SDKTabs>
<Tab value="TypeScript">
```typescript
const workflowExecution = await stigmer.workflowExecution.submitApproval({
  executionId: "...",
  toolCallId: "...",
  action: "...",
  comment: "...",
});
```
</Tab>
<Tab value="Go">
```go
workflowExecution, err := client.WorkflowExecution.SubmitApproval(ctx, &stigmer.SubmitWorkflowApprovalInput{
  ExecutionId: "...",
  ToolCallId:  "...",
  Action:      "...",
  Comment:     "...",
})
```
</Tab>
<Tab value="Python">
```python
workflow_execution = client.workflow_executions.submit_approval(SubmitWorkflowApprovalInput(
    execution_id="...",
    tool_call_id="...",
    action="...",
    comment="...",
))
```
</Tab>
<Tab value="Java">
```java
WorkflowExecution workflowExecution = client.workflowExecutions().submitApproval(SubmitWorkflowApprovalInput.builder()
    .executionId("...")
    .toolCallId("...")
    .action("...")
    .comment("...")
    .build());
```
</Tab>
</SDKTabs>

**Parameters**: [`SubmitWorkflowApprovalInput`](#submitworkflowapprovalinput)

**Returns**: [`WorkflowExecution`](#workflowexecution)

### sendSignal

Send a signal to a running workflow execution.

Delivers a signal to a workflow execution, typically to unblock a LISTEN task. Delivery is race-proof: the signal is guaranteed to arrive even if sent before the workflow is fully started.

<SDKTabs>
<Tab value="TypeScript">
```typescript
const workflowExecution = await stigmer.workflowExecution.sendSignal({
  executionId: "...",
  signalName: "...",
  payload: {},
  idempotencyKey: "...",
});
```
</Tab>
<Tab value="Go">
```go
workflowExecution, err := client.WorkflowExecution.SendSignal(ctx, &stigmer.SendSignalInput{
  ExecutionId:    "...",
  SignalName:     "...",
  Payload:        nil,
  IdempotencyKey: "...",
})
```
</Tab>
<Tab value="Python">
```python
workflow_execution = client.workflow_executions.send_signal(SendSignalInput(
    execution_id="...",
    signal_name="...",
    payload=None,
    idempotency_key="...",
))
```
</Tab>
<Tab value="Java">
```java
WorkflowExecution workflowExecution = client.workflowExecutions().sendSignal(SendSignalInput.builder()
    .executionId("...")
    .signalName("...")
    .payload(null)
    .idempotencyKey("...")
    .build());
```
</Tab>
</SDKTabs>

**Parameters**: [`SendSignalInput`](#sendsignalinput)

**Returns**: [`WorkflowExecution`](#workflowexecution)

## Types

### WorkflowExecutionInput

Input for creating or updating a Workflow Execution.

<TypeTable
  type={{
    name: { type: "string", description: "Resource name.", required: true },
    slug: { type: "string", description: "URL-friendly identifier." },
    org: { type: "string", description: "Organization slug.", required: true },
    labels: { type: "Record<string, string>", description: "Key-value labels." },
    workflowInstanceId: { type: "string", description: "ID of the WorkflowInstance to execute." },
    workflowId: { type: "string", description: "ID of the Workflow template to execute (alternative to workflow_instance_id)." },
    triggerMessage: { type: "string", description: "Input message or payload that triggers the workflow." },
    triggerMetadata: { type: "Record<string, string>", description: "Contextual metadata about what triggered this execution." },
    runtimeEnv: { type: "Record<string, EnvVarInput>", description: "Execution-scoped environment variables and secrets that override instance defaults.", typeDescriptionLink: "#envvarinput" },
    callbackToken: { type: "Uint8Array", description: "Opaque callback token for asynchronous completion by a parent orchestrator." },
  }}
/>

### EnvVarInput

A single runtime configuration or secret value.

<TypeTable
  type={{
    value: { type: "string", description: "String content of this entry." },
    isSecret: { type: "boolean", description: "Whether this value should be treated as a secret." },
  }}
/>

### WorkflowExecutionUpdateStatusInput

Input for the updateStatus RPC.

<TypeTable
  type={{
    executionId: { type: "string", description: "ID of the workflow execution to update." },
    status: { type: "WorkflowExecutionStatusInput", description: "Status fields to merge into the existing execution status.", required: true },
  }}
/>

### SubmitWorkflowApprovalInput

Input for the submitApproval RPC.

<TypeTable
  type={{
    executionId: { type: "string", description: "ID of the workflow execution." },
    toolCallId: { type: "string", description: "ID of the tool call to approve, skip, or reject." },
    action: { type: "ApprovalAction", description: "Approval decision: APPROVE, SKIP, or REJECT.", typeDescriptionLink: "#approvalaction" },
    comment: { type: "string", description: "Optional reason or comment for the decision, stored in the audit trail." },
  }}
/>

### SendSignalInput

SendSignalInput delivers a named signal to a workflow execution.

<TypeTable
  type={{
    executionId: { type: "string", description: "Workflow execution ID to send the signal to." },
    signalName: { type: "string", description: "Signal name matching the workflow's LISTEN task signal ID." },
    payload: { type: "object", description: "Optional JSON payload delivered to the LISTEN task." },
    idempotencyKey: { type: "string", description: "Optional idempotency key for deduplication of signal delivery." },
  }}
/>

### CancelWorkflowExecutionInput

CancelWorkflowExecutionInput requests graceful cancellation of a workflow execution.

<TypeTable
  type={{
    id: { type: "string", description: "Workflow execution ID to cancel." },
    reason: { type: "string", description: "Human-readable reason for cancellation, stored in the audit trail." },
  }}
/>

### TerminateWorkflowExecutionInput

TerminateWorkflowExecutionInput requests immediate termination of a workflow execution.

<TypeTable
  type={{
    id: { type: "string", description: "Workflow execution ID to terminate." },
    reason: { type: "string", description: "Human-readable reason for termination, stored in the audit trail." },
  }}
/>

### RecoverWorkflowExecutionInput

RecoverWorkflowExecutionInput requests recovery of a failed workflow execution.

<TypeTable
  type={{
    id: { type: "string", description: "Workflow execution ID to recover (must be in FAILED phase)." },
    reason: { type: "string", description: "Human-readable reason for recovery, stored in the audit trail." },
  }}
/>

### PauseWorkflowExecutionInput

PauseWorkflowExecutionInput requests temporarily pausing a workflow execution.

<TypeTable
  type={{
    id: { type: "string", description: "Workflow execution ID to pause (must be in PENDING or IN_PROGRESS phase)." },
    reason: { type: "string", description: "Human-readable reason for pausing, stored in the audit trail." },
  }}
/>

### ResumeWorkflowExecutionInput

ResumeWorkflowExecutionInput requests resuming a paused workflow execution.

<TypeTable
  type={{
    id: { type: "string", description: "Workflow execution ID to resume (must be in PAUSED phase)." },
  }}
/>

### ListWorkflowExecutionsRequest

ListWorkflowExecutionsRequest specifies parameters for listing workflow executions.

<TypeTable
  type={{
    pageSize: { type: "number", description: "Maximum number of executions to return per page." },
    pageToken: { type: "string", description: "Opaque pagination token from a previous response." },
    phase: { type: "ExecutionPhase", description: "Filter by execution phase.", typeDescriptionLink: "#executionphase" },
    tags: { type: "string[]", description: "Filter by resource tags (AND logic)." },
  }}
/>

### WorkflowExecutionList

WorkflowExecutionList contains a paginated list of workflow executions.

<TypeTable
  type={{
    totalPages: { type: "number", description: "Total number of pages available for this query." },
    entries: { type: "WorkflowExecutionInput[]", description: "Workflow executions in the current page, sorted by created_at descending.", typeDescriptionLink: "#workflowexecutioninput" },
  }}
/>

### ListWorkflowExecutionsByWorkflowRequest

ListWorkflowExecutionsByWorkflowRequest lists executions for a specific workflow.

<TypeTable
  type={{
    workflowId: { type: "string", description: "Workflow or WorkflowInstance ID to filter by.", required: true },
    pageSize: { type: "number", description: "Maximum number of executions to return per page." },
    pageToken: { type: "string", description: "Opaque pagination token from a previous response." },
  }}
/>

### SubscribeWorkflowExecutionRequest

SubscribeWorkflowExecutionRequest subscribes to real-time execution updates.

<TypeTable
  type={{
    executionId: { type: "string", description: "Execution ID to subscribe to.", required: true },
  }}
/>

### WorkflowExecution

WorkflowExecution represents a single runtime invocation of a WorkflowInstance.

<TypeTable
  type={{
    apiVersion: { type: "string", description: "API version for this resource type." },
    kind: { type: "string", description: "Resource kind identifier." },
    metadata: { type: "ApiResourceMetadata", description: "Resource metadata including id, name, org, slug, labels, visibility, and timestamps.", typeDescriptionLink: "/docs/sdk/resources/commons#apiresourcemetadata" },
    spec: { type: "WorkflowExecutionInput", description: "Resource specification. See [`WorkflowExecutionInput`](#workflowexecutioninput).", typeDescriptionLink: "#workflowexecutioninput" },
    status: { type: "WorkflowExecutionStatus", description: "System-managed state. See [`WorkflowExecutionStatus`](#workflowexecutionstatus).", typeDescriptionLink: "#workflowexecutionstatus" },
  }}
/>

### WorkflowExecutionStatus

WorkflowExecutionStatus contains all system-managed execution state and results.

<TypeTable
  type={{
    audit: { type: "ApiResourceAudit", description: "Standard audit information including timestamps and created-by identity.", typeDescriptionLink: "/docs/sdk/resources/commons#apiresourceaudit" },
    phase: { type: "ExecutionPhase", description: "Current execution lifecycle phase.", typeDescriptionLink: "#executionphase" },
    tasks: { type: "WorkflowTask[]", description: "Workflow tasks with their individual execution state.", typeDescriptionLink: "#workflowtask" },
    output: { type: "object", description: "Final workflow output, populated only when phase is EXECUTION_COMPLETED." },
    error: { type: "string", description: "Error message, populated only when phase is EXECUTION_FAILED." },
    startedAt: { type: "string", description: "ISO 8601 timestamp when execution started processing." },
    completedAt: { type: "string", description: "ISO 8601 timestamp when execution reached a terminal state." },
    temporalWorkflowId: { type: "string", description: "Correlation ID for the underlying workflow engine." },
    pendingApprovals: { type: "WorkflowPendingApproval[]", description: "Pending approvals from child agent tool executions.", typeDescriptionLink: "#workflowpendingapproval" },
  }}
/>

### WorkflowTask

WorkflowTask represents a single task within a workflow execution.

<TypeTable
  type={{
    taskId: { type: "string", description: "Unique task identifier within this workflow execution." },
    taskName: { type: "string", description: "Human-readable task name." },
    taskType: { type: "WorkflowTaskType", description: "Type of task (agent invocation, API call, approval, etc.).", typeDescriptionLink: "#workflowtasktype" },
    input: { type: "object", description: "Task input parameters, structured as JSON." },
    output: { type: "object", description: "Task output results, populated only when status is WORKFLOW_TASK_COMPLETED." },
    status: { type: "WorkflowTaskStatus", description: "Current task execution status.", typeDescriptionLink: "#workflowtaskstatus" },
    startedAt: { type: "string", description: "ISO 8601 timestamp when the task started executing." },
    completedAt: { type: "string", description: "ISO 8601 timestamp when the task reached a terminal state." },
    error: { type: "string", description: "Error message, populated only when status is WORKFLOW_TASK_FAILED." },
    metadata: { type: "object", description: "Task-specific metadata as arbitrary JSON." },
  }}
/>

### WorkflowPendingApproval

WorkflowPendingApproval pairs approval details with routing information for workflow-level forwarding.

<TypeTable
  type={{
    approval: { type: "PendingApproval", description: "Approval details from the child agent's tool call.", typeDescriptionLink: "#pendingapproval" },
    childAgentExecutionId: { type: "string", description: "ID of the child agent execution to forward the approval decision to." },
  }}
/>

### PendingApproval

A pending approval request for a tool call that requires user consent before execution.

<TypeTable
  type={{
    toolCallId: { type: "string", description: "ID of the tool call waiting for approval." },
    toolName: { type: "string", description: "Name of the tool requiring approval." },
    message: { type: "string", description: "Human-readable approval message for display." },
    argsPreview: { type: "string", description: "Sanitized preview of tool arguments for informed decision-making." },
    requestedAt: { type: "string", description: "ISO 8601 timestamp when the approval was requested." },
    fromSubAgent: { type: "boolean", description: "True if this approval originates from a sub-agent." },
    subAgentName: { type: "string", description: "Name of the sub-agent if from_sub_agent is true." },
    mcpServerSlug: { type: "string", description: "Slug of the MCP server that provides this tool." },
    subAgentSubject: { type: "string", description: "Concise subject/description of the sub-agent's task (3-10 words)." },
  }}
/>

### ApprovalAction

ApprovalAction represents the user's decision on an approval request.

| Value | Description |
|-------|-------------|
| `APPROVAL_ACTION_APPROVE` | Execute the tool normally. |
| `APPROVAL_ACTION_SKIP` | Skip tool execution, continue without it. |
| `APPROVAL_ACTION_REJECT` | Reject tool execution, fail the entire execution. |

### ExecutionPhase

ExecutionPhase defines the lifecycle phase of a workflow execution.

| Value | Description |
|-------|-------------|
| `EXECUTION_PENDING` | Execution created, waiting to start. |
| `EXECUTION_IN_PROGRESS` | Execution is actively running tasks. |
| `EXECUTION_COMPLETED` | Execution completed successfully. |
| `EXECUTION_FAILED` | Execution failed with an error. |
| `EXECUTION_CANCELLED` | Execution was cancelled by user or system. |
| `EXECUTION_TERMINATED` | Execution was force-stopped immediately without cleanup. |
| `EXECUTION_PAUSED` | Execution was paused by user and can be resumed. |

### WorkflowTaskType

WorkflowTaskType defines the type of workflow task.

| Value | Description |
|-------|-------------|
| `WORKFLOW_TASK_AGENT_INVOCATION` | Invoke an AI agent with a prompt. |
| `WORKFLOW_TASK_APPROVAL` | Wait for human approval before proceeding. |
| `WORKFLOW_TASK_API_CALL` | Call an external HTTP or gRPC API. |
| `WORKFLOW_TASK_CONDITIONAL` | Evaluate a condition and branch to different paths. |
| `WORKFLOW_TASK_PARALLEL` | Execute multiple sub-tasks concurrently. |
| `WORKFLOW_TASK_TRANSFORM` | Transform data between tasks. |
| `WORKFLOW_TASK_CUSTOM` | Execute custom task logic defined by plugins. |

### WorkflowTaskStatus

WorkflowTaskStatus defines the execution status of a workflow task.

| Value | Description |
|-------|-------------|
| `WORKFLOW_TASK_PENDING` | Task is waiting to execute. |
| `WORKFLOW_TASK_IN_PROGRESS` | Task is currently executing. |
| `WORKFLOW_TASK_COMPLETED` | Task finished successfully. |
| `WORKFLOW_TASK_FAILED` | Task failed during execution. |
| `WORKFLOW_TASK_SKIPPED` | Task was skipped due to conditional logic. |
| `WORKFLOW_TASK_WAITING_APPROVAL` | Task is waiting for human approval from a child agent execution. |

