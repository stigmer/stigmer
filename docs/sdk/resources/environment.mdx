---
title: Environment
---

{/* Auto-generated by stigmer-codegen. DO NOT EDIT. */}

An Environment stores configuration and secrets as key-value pairs. Agents and
workflow instances reference environments at runtime to access credentials, API
tokens, feature flags, and other values without hard-coding them.

```yaml
apiVersion: agentic.stigmer.ai/v1
kind: Environment
metadata:
  name: github-prod
  org: acme-corp
spec:
  description: "GitHub production credentials"
  data:
    GITHUB_TOKEN:
      value: "ghp_xxxxxxxxxxxxxxxxxxxx"
      is_secret: true
      description: "Personal access token with repo scope"
    LOG_LEVEL:
      value: "info"
      is_secret: false
      description: "Default log verbosity"
```

## Client Access

<SDKTabs>
<Tab value="TypeScript">
```typescript
const environment = await stigmer.environment.get("env_01j5q3k7m8r2s4tnz2hfp0q0c3");
```
</Tab>
<Tab value="Go">
```go
environment, err := client.Environment.Get(ctx, "env_01j5q3k7m8r2s4tnz2hfp0q0c3")
```
</Tab>
<Tab value="Python">
```python
environment = client.environments.get("env_01j5q3k7m8r2s4tnz2hfp0q0c3")
```
</Tab>
<Tab value="Java">
```java
Environment environment = client.environments().get("env_01j5q3k7m8r2s4tnz2hfp0q0c3");
```
</Tab>
</SDKTabs>

## Methods

| Method | Description |
|--------|-------------|
| [`get`](#get) | Get an environment by ID. |
| [`getByReference`](#getbyreference) | Get an environment by its organization-scoped reference (org/slug). |
| [`getSecretValue`](#getsecretvalue) | Get the unredacted value of a single secret key in an environment. |
| [`list`](#list) | List environments with optional label filtering. |
| [`apply`](#apply) | Create or update an environment. |
| [`create`](#create) | Create an environment. |
| [`update`](#update) | Update an existing environment. |
| [`updateVariables`](#updatevariables) | Add or update specific variables in an environment. |
| [`updateVisibility`](#updatevisibility) | Update the visibility of an existing environment. |
| [`delete`](#delete) | Delete an environment. |
| [`removeVariables`](#removevariables) | Remove specific variables from an environment by key. |

### get

Get an environment by ID.

<SDKTabs>
<Tab value="TypeScript">
```typescript
const environment = await stigmer.environment.get(id);
```
</Tab>
<Tab value="Go">
```go
environment, err := client.Environment.Get(ctx, id)
```
</Tab>
<Tab value="Python">
```python
environment = client.environments.get(id)
```
</Tab>
<Tab value="Java">
```java
Environment environment = client.environments().get(id);
```
</Tab>
</SDKTabs>

**Parameters**

| Parameter | Type | Description |
|-----------|------|-------------|
| `id` | `string` | Environment identifier. |

**Returns**: [`Environment`](#environment)

### getByReference

Get an environment by its organization-scoped reference (org/slug). Resolves a human-readable reference like "acme/aws-prod" to the full Environment resource.

<SDKTabs>
<Tab value="TypeScript">
```typescript
const environment = await stigmer.environment.getByReference({
  org: "...",
  kind: "...",
  slug: "...",
  version: "...",
});
```
</Tab>
<Tab value="Go">
```go
environment, err := client.Environment.GetByReference(ctx, &stigmer.ApiResourceReference{
  Org:     "...",
  Kind:    "...",
  Slug:    "...",
  Version: "...",
})
```
</Tab>
<Tab value="Python">
```python
environment = client.environments.get_by_reference(ApiResourceReference(
    org="...",
    kind="...",
    slug="...",
    version="...",
))
```
</Tab>
<Tab value="Java">
```java
Environment environment = client.environments().getByReference(ApiResourceReference.builder()
    .org("...")
    .kind("...")
    .slug("...")
    .version("...")
    .build());
```
</Tab>
</SDKTabs>

**Parameters**: [`ResourceRef`](#resourceref)

**Returns**: [`Environment`](#environment)

### getSecretValue

Get the unredacted value of a single secret key in an environment. Returns the EnvironmentValue with the decrypted value for exactly one key.

<SDKTabs>
<Tab value="TypeScript">
```typescript
const environmentValue = await stigmer.environment.getSecretValue({
  environmentId: "...",
  key: "...",
});
```
</Tab>
<Tab value="Go">
```go
environmentValue, err := client.Environment.GetSecretValue(ctx, &stigmer.EnvironmentSecretValueInput{
  EnvironmentId: "...",
  Key:           "...",
})
```
</Tab>
<Tab value="Python">
```python
environment_value = client.environments.get_secret_value(EnvironmentSecretValueInput(
    environment_id="...",
    key="...",
))
```
</Tab>
<Tab value="Java">
```java
EnvironmentValue environmentValue = client.environments().getSecretValue(EnvironmentSecretValueInput.builder()
    .environmentId("...")
    .key("...")
    .build());
```
</Tab>
</SDKTabs>

**Parameters**: [`EnvironmentSecretValueInput`](#environmentsecretvalueinput)

**Returns**: [`EnvVarInput`](#envvarinput)

### list

List environments with optional label filtering. Secret values are redacted in the response.

<SDKTabs>
<Tab value="TypeScript">
```typescript
const result = await stigmer.environment.list({
  org: "...",
  labels: {},
  pageInfo: {},
});
```
</Tab>
<Tab value="Go">
```go
result, err := client.Environment.List(ctx, &stigmer.ListEnvironmentsRequest{
  Org:      "...",
  Labels:   map[string]string{},
  PageInfo: &stigmer.PageInfoInput{},
})
```
</Tab>
<Tab value="Python">
```python
result = client.environments.list(ListEnvironmentsRequest(
    org="...",
    labels={},
    page_info=None,
))
```
</Tab>
<Tab value="Java">
```java
EnvironmentList result = client.environments().list(ListEnvironmentsRequest.builder()
    .org("...")
    .labels(Map.of())
    .pageInfo(null)
    .build());
```
</Tab>
</SDKTabs>

**Parameters**: [`ListEnvironmentsRequest`](#listenvironmentsrequest)

**Returns**: [`EnvironmentList`](#environmentlist)

### apply

Create or update an environment.

<SDKTabs>
<Tab value="TypeScript">
```typescript
const environment = await stigmer.environment.apply({
  name: "my-environment",
  org: "acme",
  slug: "...",
  labels: {},
  description: "...",
  data: {},
});
```
</Tab>
<Tab value="Go">
```go
environment, err := client.Environment.Apply(ctx, &stigmer.EnvironmentInput{
  Name:        "my-environment",
  Org:         "acme",
  Slug:        "...",
  Labels:      map[string]string{},
  Description: "...",
  Data:        map[string]string{},
})
```
</Tab>
<Tab value="Python">
```python
environment = client.environments.apply(EnvironmentInput(
    name="my-environment",
    org="acme",
    slug="...",
    labels={},
    description="...",
    data={},
))
```
</Tab>
<Tab value="Java">
```java
Environment environment = client.environments().apply(EnvironmentInput.builder()
    .name("my-environment")
    .org("acme")
    .slug("...")
    .labels(Map.of())
    .description("...")
    .data(Map.of())
    .build());
```
</Tab>
</SDKTabs>

**Parameters**: [`EnvironmentInput`](#environmentinput)

**Returns**: [`Environment`](#environment)

### create

Create an environment.

<SDKTabs>
<Tab value="TypeScript">
```typescript
const environment = await stigmer.environment.create({
  name: "my-environment",
  org: "acme",
  slug: "...",
  labels: {},
  description: "...",
  data: {},
});
```
</Tab>
<Tab value="Go">
```go
environment, err := client.Environment.Create(ctx, &stigmer.EnvironmentInput{
  Name:        "my-environment",
  Org:         "acme",
  Slug:        "...",
  Labels:      map[string]string{},
  Description: "...",
  Data:        map[string]string{},
})
```
</Tab>
<Tab value="Python">
```python
environment = client.environments.create(EnvironmentInput(
    name="my-environment",
    org="acme",
    slug="...",
    labels={},
    description="...",
    data={},
))
```
</Tab>
<Tab value="Java">
```java
Environment environment = client.environments().create(EnvironmentInput.builder()
    .name("my-environment")
    .org("acme")
    .slug("...")
    .labels(Map.of())
    .description("...")
    .data(Map.of())
    .build());
```
</Tab>
</SDKTabs>

**Parameters**: [`EnvironmentInput`](#environmentinput)

**Returns**: [`Environment`](#environment)

### update

Update an existing environment.

<SDKTabs>
<Tab value="TypeScript">
```typescript
const environment = await stigmer.environment.update({
  name: "my-environment",
  org: "acme",
  slug: "...",
  labels: {},
  description: "...",
  data: {},
});
```
</Tab>
<Tab value="Go">
```go
environment, err := client.Environment.Update(ctx, &stigmer.EnvironmentInput{
  Name:        "my-environment",
  Org:         "acme",
  Slug:        "...",
  Labels:      map[string]string{},
  Description: "...",
  Data:        map[string]string{},
})
```
</Tab>
<Tab value="Python">
```python
environment = client.environments.update(EnvironmentInput(
    name="my-environment",
    org="acme",
    slug="...",
    labels={},
    description="...",
    data={},
))
```
</Tab>
<Tab value="Java">
```java
Environment environment = client.environments().update(EnvironmentInput.builder()
    .name("my-environment")
    .org("acme")
    .slug("...")
    .labels(Map.of())
    .description("...")
    .data(Map.of())
    .build());
```
</Tab>
</SDKTabs>

**Parameters**: [`EnvironmentInput`](#environmentinput)

**Returns**: [`Environment`](#environment)

### updateVariables

Add or update specific variables in an environment. Existing variables not included in the request are preserved unchanged.

<SDKTabs>
<Tab value="TypeScript">
```typescript
const environment = await stigmer.environment.updateVariables({
  environmentId: "...",
  variables: {},
});
```
</Tab>
<Tab value="Go">
```go
environment, err := client.Environment.UpdateVariables(ctx, &stigmer.UpdateEnvironmentVariablesRequest{
  EnvironmentId: "...",
  Variables:     map[string]string{},
})
```
</Tab>
<Tab value="Python">
```python
environment = client.environments.update_variables(UpdateEnvironmentVariablesRequest(
    environment_id="...",
    variables={},
))
```
</Tab>
<Tab value="Java">
```java
Environment environment = client.environments().updateVariables(UpdateEnvironmentVariablesRequest.builder()
    .environmentId("...")
    .variables(Map.of())
    .build());
```
</Tab>
</SDKTabs>

**Parameters**: [`UpdateEnvironmentVariablesRequest`](#updateenvironmentvariablesrequest)

**Returns**: [`Environment`](#environment)

### updateVisibility

Update the visibility of an existing environment.

Only modifies metadata.visibility, leaving spec, status, and other metadata fields untouched. Environments support two levels: private (the default) and org. Setting org shares the environment with the owning organization: members can view it with secret values redacted, and any execution in the organization may use its values at runtime. Secret values are revealed only to the environment's creator, at every visibility level.

<SDKTabs>
<Tab value="TypeScript">
```typescript
const environment = await stigmer.environment.updateVisibility({
  resourceId: "...",
  visibility: "...",
});
```
</Tab>
<Tab value="Go">
```go
environment, err := client.Environment.UpdateVisibility(ctx, &stigmer.UpdateVisibilityInput{
  ResourceId: "...",
  Visibility: "...",
})
```
</Tab>
<Tab value="Python">
```python
environment = client.environments.update_visibility(UpdateVisibilityInput(
    resource_id="...",
    visibility="...",
))
```
</Tab>
<Tab value="Java">
```java
Environment environment = client.environments().updateVisibility(UpdateVisibilityInput.builder()
    .resourceId("...")
    .visibility("...")
    .build());
```
</Tab>
</SDKTabs>

**Parameters**: [`UpdateVisibilityInput`](#updatevisibilityinput)

**Returns**: [`Environment`](#environment)

### delete

Delete an environment.

<SDKTabs>
<Tab value="TypeScript">
```typescript
const environment = await stigmer.environment.delete({ resourceId: id });
```
</Tab>
<Tab value="Go">
```go
environment, err := client.Environment.Delete(ctx, &stigmer.DeleteResourceInput{
  ResourceID: id,
})
```
</Tab>
<Tab value="Python">
```python
environment = client.environments.delete(DeleteResourceInput(resource_id=id))
```
</Tab>
<Tab value="Java">
```java
Environment environment = client.environments().delete(DeleteResourceInput.builder()
    .resourceId(id)
    .build());
```
</Tab>
</SDKTabs>

**Parameters**

| Parameter | Type | Description |
|-----------|------|-------------|
| `resourceId` | `string` | ID of the resource to delete. |
| `versionMessage` | `string` | Optional deletion reason. |
| `force` | `boolean` | Force delete even if resource is in use. |

**Returns**: [`Environment`](#environment)

### removeVariables

Remove specific variables from an environment by key. Keys that do not exist are silently ignored.

<SDKTabs>
<Tab value="TypeScript">
```typescript
const environment = await stigmer.environment.removeVariables({
  environmentId: "...",
  keys: [],
});
```
</Tab>
<Tab value="Go">
```go
environment, err := client.Environment.RemoveVariables(ctx, &stigmer.RemoveEnvironmentVariablesRequest{
  EnvironmentId: "...",
  Keys:          []string{},
})
```
</Tab>
<Tab value="Python">
```python
environment = client.environments.remove_variables(RemoveEnvironmentVariablesRequest(
    environment_id="...",
    keys=[],
))
```
</Tab>
<Tab value="Java">
```java
Environment environment = client.environments().removeVariables(RemoveEnvironmentVariablesRequest.builder()
    .environmentId("...")
    .keys(List.of())
    .build());
```
</Tab>
</SDKTabs>

**Parameters**: [`RemoveEnvironmentVariablesRequest`](#removeenvironmentvariablesrequest)

**Returns**: [`Environment`](#environment)

## Types

### EnvironmentInput

Input for creating or updating an Environment.

<TypeTable
  type={{
    name: { type: "string", description: "Resource name.", required: true },
    slug: { type: "string", description: "URL-friendly identifier." },
    org: { type: "string", description: "Organization slug.", required: true },
    labels: { type: "Record<string, string>", description: "Key-value labels." },
    description: { type: "string", description: "Human-readable description for UI and listing display." },
    data: { type: "Record<string, EnvVarInput>", description: "Key-value pairs containing configuration and secrets.", typeDescriptionLink: "#envvarinput" },
  }}
/>

### EnvVarInput

EnvironmentValue represents a single configuration or secret entry.

<TypeTable
  type={{
    value: { type: "string", description: "The configuration or secret string." },
    isSecret: { type: "boolean", description: "Whether this value should be treated as a secret." },
    description: { type: "string", description: "Human-readable description of what this value is used for." },
  }}
/>

### UpdateVisibilityInput

Input for updating the visibility of any API resource.

<TypeTable
  type={{
    resourceId: { type: "string", description: "ID of the resource whose visibility is being updated.", required: true },
    visibility: { type: "ApiResourceVisibility", description: "The new visibility setting for the resource.", typeDescriptionLink: "/docs/sdk/resources/commons#apiresourcevisibility" },
  }}
/>

### UpdateEnvironmentVariablesRequest

Request to add or update specific variables in an environment.

<TypeTable
  type={{
    environmentId: { type: "string", description: "The environment resource ID." },
    variables: { type: "Record<string, EnvVarInput>", description: "Variables to add or update.", typeDescriptionLink: "#envvarinput" },
  }}
/>

### RemoveEnvironmentVariablesRequest

Request to remove specific variables from an environment by key.

<TypeTable
  type={{
    environmentId: { type: "string", description: "The environment resource ID." },
    keys: { type: "string[]", description: "Keys to remove from EnvironmentSpec.data." },
  }}
/>

### ResourceRef

Generic reference to any API resource by org and slug.

<TypeTable
  type={{
    org: { type: "string", description: "Organization that owns the referenced resource." },
    kind: { type: "ApiResourceKind", description: "Kind of the referenced resource (e.g., SKILL, AGENT, MCP_SERVER).", typeDescriptionLink: "/docs/sdk/resources/commons#apiresourcekind" },
    slug: { type: "string", description: "Resource slug (user-friendly identifier, unique within org).", required: true },
    version: { type: "string", description: "Version of the resource (optional, only applicable to versioned resources like Skills)." },
  }}
/>

### EnvironmentSecretValueInput

Input for retrieving a single unredacted secret value from an environment.

<TypeTable
  type={{
    environmentId: { type: "string", description: "The environment resource ID." },
    key: { type: "string", description: "The key within EnvironmentSpec.data to retrieve the unredacted value for." },
  }}
/>

### ListEnvironmentsRequest

ListEnvironmentsRequest specifies parameters for listing environments.

<TypeTable
  type={{
    org: { type: "string", description: "Organization to list environments for." },
    labels: { type: "Record<string, string>", description: "Filter by metadata labels." },
    pageInfo: { type: "PageInfoInput", description: "Pagination options." },
  }}
/>

### EnvironmentList

EnvironmentList contains a paginated list of environments.

<TypeTable
  type={{
    totalCount: { type: "number", description: "Total count of matching environments." },
    items: { type: "EnvironmentInput[]", description: "Environments in the current page.", typeDescriptionLink: "#environmentinput" },
  }}
/>

### Environment

Environment stores configuration and secrets as key-value pairs for runtime use by agents and workflow instances.

<TypeTable
  type={{
    apiVersion: { type: "string", description: "API version for this resource type." },
    kind: { type: "string", description: "Resource kind identifier." },
    metadata: { type: "ApiResourceMetadata", description: "Resource metadata including id, name, org, slug, labels, visibility, and timestamps.", typeDescriptionLink: "/docs/sdk/resources/commons#apiresourcemetadata" },
    spec: { type: "EnvironmentInput", description: "Resource specification. See [`EnvironmentInput`](#environmentinput).", typeDescriptionLink: "#environmentinput" },
    status: { type: "EnvironmentStatus", description: "System-managed state. See `EnvironmentStatus`." },
  }}
/>

