---
title: License
---

{/* Auto-generated by stigmer-codegen. DO NOT EDIT. */}

A License is a signed grant of entitlements to a self-hosting customer for a
term. Stigmer Cloud issues it; the customer's Stigmer Enterprise deployment
verifies the signed ticket offline and reports its state through
`getLicenseStatus`. The customer is a value inside the license and need not
hold a Stigmer Cloud organization. A license is immutable once issued; a
renewal is a new license.

Licenses are issued by platform operators and belong to the platform, not to
an organization. The spec is what the operator issues; the status carries the
signed ticket the customer installs.

```yaml
apiVersion: billing.stigmer.ai/v1
kind: License
metadata:
  name: Acme Corp 2026
  slug: acme-corp-2026
spec:
  customer:
    id: cus_01j5q3k7m8r2s4tnz2hfp0q0b2
    display_name: Acme Corp
    contact_email: platform@acme.example
  plan_id: pln_01j5q3k7m8r2s4tnz2hfp0q0c3
  entitlements:
    features:
      - sso_enforcement
      - platform_client
      - channels
  term: paid
  expires_at: "2027-09-01T00:00:00Z"
  grace_until: "2027-10-01T00:00:00Z"
```

## Client Access

<SDKTabs>
<Tab value="TypeScript">
```typescript
const license = await stigmer.license.get("lic_01j5q3k7m8r2s4tnz2hfp0q0c3");
```
</Tab>
<Tab value="Go">
```go
license, err := client.License.Get(ctx, "lic_01j5q3k7m8r2s4tnz2hfp0q0c3")
```
</Tab>
<Tab value="Python">
```python
license = client.licenses.get("lic_01j5q3k7m8r2s4tnz2hfp0q0c3")
```
</Tab>
<Tab value="Java">
```java
License license = client.licenses().get("lic_01j5q3k7m8r2s4tnz2hfp0q0c3");
```
</Tab>
</SDKTabs>

## Methods

| Method | Description |
|--------|-------------|
| [`get`](#get) | Get a license by its unique identifier. |
| [`list`](#list) | List issued licenses, optionally for one customer. |
| [`create`](#create) | Issue a license. |

### get

Get a license by its unique identifier.

<SDKTabs>
<Tab value="TypeScript">
```typescript
const license = await stigmer.license.get(id);
```
</Tab>
<Tab value="Go">
```go
license, err := client.License.Get(ctx, id)
```
</Tab>
<Tab value="Python">
```python
license = client.licenses.get(id)
```
</Tab>
<Tab value="Java">
```java
License license = client.licenses().get(id);
```
</Tab>
</SDKTabs>

**Parameters**

| Parameter | Type | Description |
|-----------|------|-------------|
| `id` | `string` | License identifier. |

**Returns**: [`License`](#license)

### list

List issued licenses, optionally for one customer.

<SDKTabs>
<Tab value="TypeScript">
```typescript
const licenses = await stigmer.license.list({
  customerId: "...",
});
```
</Tab>
<Tab value="Go">
```go
licenses, err := client.License.List(ctx, &stigmer.ListLicensesInput{
  CustomerId: "...",
})
```
</Tab>
<Tab value="Python">
```python
licenses = client.licenses.list(ListLicensesInput(
    customer_id="...",
))
```
</Tab>
<Tab value="Java">
```java
Licenses licenses = client.licenses().list(ListLicensesInput.builder()
    .customerId("...")
    .build());
```
</Tab>
</SDKTabs>

**Parameters**: [`ListLicensesInput`](#listlicensesinput)

**Returns**: [`Licenses`](#licenses)

### create

Issue a license.

Builds the signed claims from the spec, signs them, and returns the license with its ticket in the status. The spec is final: nothing about an issued license changes afterwards.

<SDKTabs>
<Tab value="TypeScript">
```typescript
const license = await stigmer.license.create({
  name: "my-license",
  org: "acme",
  slug: "...",
  labels: {},
  customer: {},
  planId: "...",
  entitlements: {},
  term: "...",
  expiresAt: "...",
  graceUntil: "...",
  notes: "...",
});
```
</Tab>
<Tab value="Go">
```go
license, err := client.License.Create(ctx, &stigmer.LicenseInput{
  Name:         "my-license",
  Org:          "acme",
  Slug:         "...",
  Labels:       map[string]string{},
  Customer:     &stigmer.LicenseCustomerInput{},
  PlanId:       "...",
  Entitlements: &stigmer.EntitlementsInput{},
  Term:         "...",
  ExpiresAt:    "...",
  GraceUntil:   "...",
  Notes:        "...",
})
```
</Tab>
<Tab value="Python">
```python
license = client.licenses.create(LicenseInput(
    name="my-license",
    org="acme",
    slug="...",
    labels={},
    customer=None,
    plan_id="...",
    entitlements=None,
    term="...",
    expires_at="...",
    grace_until="...",
    notes="...",
))
```
</Tab>
<Tab value="Java">
```java
License license = client.licenses().create(LicenseInput.builder()
    .name("my-license")
    .org("acme")
    .slug("...")
    .labels(Map.of())
    .customer(null)
    .planId("...")
    .entitlements(null)
    .term("...")
    .expiresAt("...")
    .graceUntil("...")
    .notes("...")
    .build());
```
</Tab>
</SDKTabs>

**Parameters**: [`LicenseInput`](#licenseinput)

**Returns**: [`License`](#license)

## Types

### LicenseInput

Input for creating or updating a License.

<TypeTable
  type={{
    name: { type: "string", description: "Resource name.", required: true },
    slug: { type: "string", description: "URL-friendly identifier." },
    org: { type: "string", description: "Organization slug.", required: true },
    labels: { type: "Record<string, string>", description: "Key-value labels." },
    customer: { type: "LicenseCustomerInput", description: "Who the license is issued to.", typeDescriptionLink: "#licensecustomerinput", required: true },
    planId: { type: "string", description: "The id of the Plan the entitlements were copied from." },
    entitlements: { type: "EntitlementsInput", description: "What the license permits.", typeDescriptionLink: "#entitlementsinput", required: true },
    term: { type: "LicenseTerm", description: "Whether the term is a trial or a paid license.", typeDescriptionLink: "#licenseterm", required: true },
    expiresAt: { type: "string", description: "When the term ends.", required: true },
    graceUntil: { type: "string", description: "The end of the grace period after expiry, during which the licensed server keeps serving and reports the license as expired.", required: true },
    notes: { type: "string", description: "Operator notes about this issuance (the order, the contact, the reason for a bespoke entitlement)." },
  }}
/>

### LicenseCustomerInput

The customer a license is issued to, carried as a value inside the license.

<TypeTable
  type={{
    id: { type: "string", description: "Identifier the issuer minted for this customer, stable across every license issued to them.", required: true },
    displayName: { type: "string", description: "The customer's name as the licensed server shows it (\"licensed to Acme\").", required: true },
    contactEmail: { type: "string", description: "The address renewal and expiry notices go to.", required: true },
    organization: { type: "string", description: "The cloud organization this customer also holds, as its slug (the same value a resource's metadata.org carries)." },
  }}
/>

### EntitlementsInput

What the platform permits a tenant: a set of limits and a set of features.

<TypeTable
  type={{
    limits: { type: "EntitlementLimitsInput", description: "The numeric limits in force.", typeDescriptionLink: "#entitlementlimitsinput" },
    features: { type: "Feature[]", description: "The features unlocked, each named once.", typeDescriptionLink: "#feature" },
  }}
/>

### EntitlementLimitsInput

The numeric limits an entitlement carries.

<TypeTable
  type={{
    maxOrganizations: { type: "number", description: "The most organizations the tenant may hold at once." },
    maxUsers: { type: "number", description: "The most identity accounts the tenant may hold at once." },
    includedManagedOrganizations: { type: "number", description: "The platform-managed organizations a subscription includes before the per-organization fee applies." },
  }}
/>

### ListLicensesInput

ListLicensesInput selects which licenses to list.

<TypeTable
  type={{
    customerId: { type: "string", description: "When set, only licenses issued to this customer (LicenseCustomer.id) are returned; every license otherwise." },
  }}
/>

### Licenses

Licenses contains a list of license resources.

<TypeTable
  type={{
    entries: { type: "LicenseInput[]", description: "License entries.", typeDescriptionLink: "#licenseinput" },
  }}
/>

### License

License is a signed grant of entitlements to a self-hosting customer for a term.

<TypeTable
  type={{
    apiVersion: { type: "string", description: "API version for this resource type." },
    kind: { type: "string", description: "Resource kind identifier." },
    metadata: { type: "ApiResourceMetadata", description: "Resource metadata including id, name, org, slug, labels, visibility, and timestamps.", typeDescriptionLink: "/docs/sdk/resources/commons#apiresourcemetadata" },
    spec: { type: "LicenseInput", description: "Resource specification. See [`LicenseInput`](#licenseinput).", typeDescriptionLink: "#licenseinput" },
    status: { type: "LicenseStatus", description: "System-managed state. See [`LicenseStatus`](#licensestatus).", typeDescriptionLink: "#licensestatus" },
  }}
/>

### LicenseStatus

LicenseStatus is the system-managed state of an issued license: its audit trail and the signed ticket the customer installs.

<TypeTable
  type={{
    audit: { type: "ApiResourceAudit", description: "Standard audit information tracking creation and modification.", typeDescriptionLink: "/docs/sdk/resources/commons#apiresourceaudit" },
    keyId: { type: "string", description: "The `kid` of the key that signed the ticket, so the issuer can tell which licenses a rotated key still covers." },
    issuedAt: { type: "string", description: "When the ticket was signed; the issued_at the claims carry." },
    ticket: { type: "string", description: "The signed ticket: a compact JWS whose payload is the proto JSON of the LicenseClaims built from the spec." },
  }}
/>

### LicenseTerm

The commercial nature of a license's term.

| Value | Description |
|-------|-------------|
| `trial` | A time-limited evaluation with fixed modest limits. |
| `paid` | A paid license for its full term. |

### Feature

A capability an entitlement can unlock.

| Value | Description |
|-------|-------------|
| `sso_enforcement` | Enforcing sign-in through a registered identity provider, so members cannot use another login. |
| `platform_client` | Creating PlatformClient credentials so the tenant's own product can mint Stigmer tokens for its users. |
| `byo_provider_keys` | Bringing the tenant's own LLM provider keys instead of the platform's metered proxy credentials. |
| `channels` | Delivering agents over messaging channels (Slack, WhatsApp) through a channel runtime. |
| `sharing` | Sharing an agent with individuals and guests through hosted share links. |

