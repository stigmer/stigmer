---
title: Commons
---

{/* Auto-generated by stigmer-codegen. DO NOT EDIT. */}

Shared types and enums used across all API resources. Fields on resource
pages that reference these types link here.

## Types

### ApiResourceMetadata

ApiResourceMetadata contains standard metadata for all API resources.

<TypeTable
  type={{
    name: { type: "string", description: "Human-readable name of the resource." },
    slug: { type: "string", description: "URL-friendly identifier, unique within the organization." },
    id: { type: "string", description: "System-generated unique identifier." },
    org: { type: "string", description: "Organization that owns this resource." },
    visibility: { type: "ApiResourceVisibility", description: "Visibility controls who can access this resource.", typeDescriptionLink: "#apiresourcevisibility" },
    labels: { type: "Record<string, string>", description: "Key-value labels for organization and filtering." },
    annotations: { type: "Record<string, string>", description: "Key-value annotations for additional metadata." },
    tags: { type: "string[]", description: "Tags for categorization." },
    version: { type: "ApiResourceMetadataVersion", description: "Version information for the resource.", typeDescriptionLink: "#apiresourcemetadataversion" },
  }}
/>

### ApiResourceMetadataVersion

ApiResourceMetadataVersion contains version tracking information.

<TypeTable
  type={{
    id: { type: "string", description: "Unique identifier for this version." },
    message: { type: "string", description: "Message describing what changed in this version." },
    previousVersionId: { type: "string", description: "Reference to the previous version ID." },
  }}
/>

### ApiResourceAudit

ApiResourceAudit contains both spec-level and status-level audit details.

<TypeTable
  type={{
    specAudit: { type: "ApiResourceAuditInfo", description: "Audit info for the resource's specification (desired state).", typeDescriptionLink: "#apiresourceauditinfo" },
    statusAudit: { type: "ApiResourceAuditInfo", description: "Audit info for the resource's status (actual state).", typeDescriptionLink: "#apiresourceauditinfo" },
  }}
/>

### ApiResourceAuditInfo

ApiResourceAuditInfo contains detailed audit information.

<TypeTable
  type={{
    createdBy: { type: "ApiResourceAuditActor", description: "Actor who created this resource.", typeDescriptionLink: "#apiresourceauditactor" },
    createdAt: { type: "string", description: "Timestamp when the resource was created." },
    updatedBy: { type: "ApiResourceAuditActor", description: "Actor who last updated this resource.", typeDescriptionLink: "#apiresourceauditactor" },
    updatedAt: { type: "string", description: "Timestamp when the resource was last updated." },
    event: { type: "string", description: "Type of the last event (e.g., \"created\", \"updated\")." },
  }}
/>

### ApiResourceAuditActor

ApiResourceAuditActor represents the entity that performed an action.

<TypeTable
  type={{
    id: { type: "string", description: "Unique identifier of the actor." },
    avatar: { type: "string", description: "URL to the actor's avatar image." },
  }}
/>

## Enums

### ApiResourceKind

API resource kinds for Stigmer.

| Value | Description |
|-------|-------------|
| `api_resource_version` | Immutable version record tracking changes to an API resource. |
| `iam_policy` | Role-based access control policy for granting permissions on resources. |
| `identity_account` | User account in the identity system. |
| `api_key` | Credential for programmatic API access. |
| `invitation` | Shareable link for joining an organization with a configurable role. |
| `identity_provider` | External identity provider for federated authentication. |
| `organization` | Top-level tenant that owns and manages resources. |
| `platform` | Singleton platform instance representing the Stigmer deployment. |
| `agent` | AI assistant with instructions, tools, skills, and a delegation model. |
| `agent_execution` | Single run of an agent within a session, tracking tool calls and responses. |
| `session` | Conversation thread between a user and an agent. |
| `skill` | Knowledge resource that provides domain-specific context to an agent. |
| `mcp_server` | External tool server connected via the Model Context Protocol. |
| `agent_instance` | Configured deployment of an agent with environment-specific overrides. |
| `workflow` | Multi-step orchestration defining how agents collaborate on a task. |
| `workflow_instance` | Configured deployment of a workflow with environment-specific bindings. |
| `workflow_execution` | Single run of a workflow instance, tracking step progress and outcomes. |
| `environment` | Named set of variables and secrets for agent and workflow execution. |
| `execution_context` | User-owned runtime context for managing execution state. |
| `project` | Aggregate root grouping related resources for lifecycle management. |

### ApiResourceVisibility

Visibility controls who can access an API resource.

| Value | Description |
|-------|-------------|
| `visibility_private` | Only members of the owning organization can access. |
| `visibility_public` | Anyone can access (read) this resource. |

