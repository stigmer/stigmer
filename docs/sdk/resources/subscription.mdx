---
title: Subscription
---

{/* Auto-generated by stigmer-codegen. DO NOT EDIT. */}

A Subscription is an organization's binding to a Plan on Stigmer Cloud. An
organization has at most one active subscription, and an organization with
none is on the Free plan. What the organization may do is derived from its
plan on every read through `getEntitlements`, never stored on the
subscription.

Subscriptions are system-created: the payment system opens and moves them,
and an organization's billing administrators change or cancel the plan through
the command RPCs. You never author one as a manifest; the shape below is what
`getForOrganization` returns.

```yaml
apiVersion: billing.stigmer.ai/v1
kind: Subscription
metadata:
  name: acme
  org: acme
spec:
  plan_id: pln_01j5q3k7m8r2s4tnz2hfp0q0a1
status:
  state: active
  current_period_start: "2026-09-01T00:00:00Z"
  current_period_end: "2026-10-01T00:00:00Z"
```

## Methods

| Method | Description |
|--------|-------------|
| [`getEntitlements`](#getentitlements) | Resolve what the organization is permitted right now. |
| [`getForOrganization`](#getfororganization) | Get the organization's subscription. |
| [`cancel`](#cancel) | Cancel the organization's subscription. |
| [`changePlan`](#changeplan) | Move the organization onto a plan. |

### getEntitlements

Resolve what the organization is permitted right now.

The answer is derived, never stored: the entitlements of the plan the organization's active subscription names, else the Free plan's with an empty plan_id. A platform-managed organization has no subscription of its own; its entitlements resolve through its identity provider to the integrator organization, whose plan counts the managed organization against the managed organizations it includes.

<SDKTabs>
<Tab value="TypeScript">
```typescript
const getEntitlementsOutput = await stigmer.subscription.getEntitlements({
  orgId: "...",
});
```
</Tab>
<Tab value="Go">
```go
getEntitlementsOutput, err := client.Subscription.GetEntitlements(ctx, &stigmer.GetEntitlementsInput{
  OrgId: "...",
})
```
</Tab>
<Tab value="Python">
```python
get_entitlements_output = client.subscriptions.get_entitlements(GetEntitlementsInput(
    org_id="...",
))
```
</Tab>
<Tab value="Java">
```java
GetEntitlementsOutput getEntitlementsOutput = client.subscriptions().getEntitlements(GetEntitlementsInput.builder()
    .orgId("...")
    .build());
```
</Tab>
</SDKTabs>

**Parameters**: [`GetEntitlementsInput`](#getentitlementsinput)

**Returns**: [`GetEntitlementsOutput`](#getentitlementsoutput)

### getForOrganization

Get the organization's subscription.

NOT_FOUND when the organization has none: it is on the Free plan, which is not a row. Callers that only need what the organization may do should call getEntitlements, which answers for every organization.

<SDKTabs>
<Tab value="TypeScript">
```typescript
const subscription = await stigmer.subscription.getForOrganization({
  orgId: "...",
});
```
</Tab>
<Tab value="Go">
```go
subscription, err := client.Subscription.GetForOrganization(ctx, &stigmer.GetSubscriptionForOrganizationInput{
  OrgId: "...",
})
```
</Tab>
<Tab value="Python">
```python
subscription = client.subscriptions.get_for_organization(GetSubscriptionForOrganizationInput(
    org_id="...",
))
```
</Tab>
<Tab value="Java">
```java
Subscription subscription = client.subscriptions().getForOrganization(GetSubscriptionForOrganizationInput.builder()
    .orgId("...")
    .build());
```
</Tab>
</SDKTabs>

**Parameters**: [`GetSubscriptionForOrganizationInput`](#getsubscriptionfororganizationinput)

**Returns**: [`Subscription`](#subscription)

### cancel

Cancel the organization's subscription.

The organization keeps its plan to the end of the current period and is on Free after it. NOT_FOUND when the organization has no subscription.

<SDKTabs>
<Tab value="TypeScript">
```typescript
const subscription = await stigmer.subscription.cancel({
  orgId: "...",
});
```
</Tab>
<Tab value="Go">
```go
subscription, err := client.Subscription.Cancel(ctx, &stigmer.CancelSubscriptionInput{
  OrgId: "...",
})
```
</Tab>
<Tab value="Python">
```python
subscription = client.subscriptions.cancel(CancelSubscriptionInput(
    org_id="...",
))
```
</Tab>
<Tab value="Java">
```java
Subscription subscription = client.subscriptions().cancel(CancelSubscriptionInput.builder()
    .orgId("...")
    .build());
```
</Tab>
</SDKTabs>

**Parameters**: [`CancelSubscriptionInput`](#cancelsubscriptioninput)

**Returns**: [`Subscription`](#subscription)

### changePlan

Move the organization onto a plan.

Subscribes an organization that has no subscription, or changes the plan of one that does. The plan must be active and bought through a subscription. Returns the subscription as it stands after the change.

<SDKTabs>
<Tab value="TypeScript">
```typescript
const subscription = await stigmer.subscription.changePlan({
  orgId: "...",
  planId: "...",
});
```
</Tab>
<Tab value="Go">
```go
subscription, err := client.Subscription.ChangePlan(ctx, &stigmer.ChangePlanInput{
  OrgId:  "...",
  PlanId: "...",
})
```
</Tab>
<Tab value="Python">
```python
subscription = client.subscriptions.change_plan(ChangePlanInput(
    org_id="...",
    plan_id="...",
))
```
</Tab>
<Tab value="Java">
```java
Subscription subscription = client.subscriptions().changePlan(ChangePlanInput.builder()
    .orgId("...")
    .planId("...")
    .build());
```
</Tab>
</SDKTabs>

**Parameters**: [`ChangePlanInput`](#changeplaninput)

**Returns**: [`Subscription`](#subscription)

## Types

### SubscriptionInput

Input for creating or updating a Subscription.

<TypeTable
  type={{
    name: { type: "string", description: "Resource name.", required: true },
    slug: { type: "string", description: "URL-friendly identifier." },
    org: { type: "string", description: "Organization slug.", required: true },
    labels: { type: "Record<string, string>", description: "Key-value labels." },
    planId: { type: "string", description: "The id of the Plan the organization is bound to.", required: true },
  }}
/>

### ChangePlanInput

ChangePlanInput moves an organization onto a plan.

<TypeTable
  type={{
    orgId: { type: "string", description: "The organization's id.", required: true },
    planId: { type: "string", description: "The id of the Plan to move onto.", required: true },
  }}
/>

### CancelSubscriptionInput

CancelSubscriptionInput names the organization whose subscription is canceled.

<TypeTable
  type={{
    orgId: { type: "string", description: "The organization's id.", required: true },
  }}
/>

### GetSubscriptionForOrganizationInput

GetSubscriptionForOrganizationInput names the organization whose subscription is read.

<TypeTable
  type={{
    orgId: { type: "string", description: "The organization's id.", required: true },
  }}
/>

### GetEntitlementsInput

GetEntitlementsInput names the organization whose entitlements are resolved.

<TypeTable
  type={{
    orgId: { type: "string", description: "The organization's id.", required: true },
  }}
/>

### GetEntitlementsOutput

GetEntitlementsOutput is what an organization is permitted right now and the plan that permits it.

<TypeTable
  type={{
    entitlements: { type: "EntitlementsInput", description: "The entitlements in force for the organization." },
    planId: { type: "string", description: "The id of the Plan the entitlements were resolved from." },
  }}
/>

### Subscription

Subscription is an organization's binding to a Plan.

<TypeTable
  type={{
    apiVersion: { type: "string", description: "API version for this resource type." },
    kind: { type: "string", description: "Resource kind identifier." },
    metadata: { type: "ApiResourceMetadata", description: "Resource metadata including id, name, org, slug, labels, visibility, and timestamps.", typeDescriptionLink: "/docs/sdk/resources/commons#apiresourcemetadata" },
    spec: { type: "SubscriptionInput", description: "Resource specification. See [`SubscriptionInput`](#subscriptioninput).", typeDescriptionLink: "#subscriptioninput" },
    status: { type: "SubscriptionStatus", description: "System-managed state. See [`SubscriptionStatus`](#subscriptionstatus).", typeDescriptionLink: "#subscriptionstatus" },
  }}
/>

### SubscriptionStatus

SubscriptionStatus is the system-managed state of a subscription: its audit trail, its lifecycle state and the billing period it is in.

<TypeTable
  type={{
    audit: { type: "ApiResourceAudit", description: "Standard audit information tracking creation and modification.", typeDescriptionLink: "/docs/sdk/resources/commons#apiresourceaudit" },
    state: { type: "SubscriptionState", description: "The lifecycle state the payment system last reported.", typeDescriptionLink: "#subscriptionstate" },
    currentPeriodStart: { type: "string", description: "Start of the current billing period." },
    currentPeriodEnd: { type: "string", description: "End of the current billing period." },
    canceledAt: { type: "string", description: "When the subscription was canceled." },
  }}
/>

### SubscriptionState

SubscriptionState is the lifecycle state a subscription is in.

| Value | Description |
|-------|-------------|
| `active` | In good standing; the plan's entitlements apply. |
| `past_due` | A payment is overdue; the plan's entitlements still apply while the payment system retries. |
| `canceled` | Ended by the organization or the payment system; the organization is on Free from the end of the period. |

### Feature

A capability an entitlement can unlock.

| Value | Description |
|-------|-------------|
| `sso_enforcement` | Enforcing sign-in through a registered identity provider, so members cannot use another login. |
| `platform_client` | Creating PlatformClient credentials so the tenant's own product can mint Stigmer tokens for its users. |
| `byo_provider_keys` | Bringing the tenant's own LLM provider keys instead of the platform's metered proxy credentials. |
| `channels` | Delivering agents over messaging channels (Slack, WhatsApp) through a channel runtime. |
| `sharing` | Sharing an agent with individuals and guests through hosted share links. |

