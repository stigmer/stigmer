---
title: Agent Share
---

{/* Auto-generated by stigmer-codegen. DO NOT EDIT. */}

An AgentShare turns an agent into a hosted chat link. It controls who can
chat with the agent at `/chat/<org>/<slug>` (anyone with the link, or org
members only), which sites may embed the chat widget, the messages visitors
see when a limit refuses them, and the environment credentials guest
conversations receive. The referenced agent is never modified by share
operations — applying an agent manifest cannot touch a share.

```yaml
apiVersion: agentic.stigmer.ai/v1
kind: AgentShare
metadata:
  name: org-knowledge-agent
  org: workshop
spec:
  agent_ref:
    kind: agent
    org: workshop
    slug: org-knowledge-agent
  enabled: true
  audience: agent_share_audience_public
  allowed_origins:
    - "https://docs.example.com"
  messages:
    rate_limited: "You're sending messages too quickly — give it a moment."
  environment_refs:
    - kind: environment
      org: workshop
      slug: github-org-shared
```

## Client Access

<SDKTabs>
<Tab value="TypeScript">
```typescript
const agentShare = await stigmer.agentShare.get("ash_01j5q3k7m8r2s4tnz2hfp0q0c3");
```
</Tab>
<Tab value="Go">
```go
agentShare, err := client.AgentShare.Get(ctx, "ash_01j5q3k7m8r2s4tnz2hfp0q0c3")
```
</Tab>
<Tab value="Python">
```python
agent_share = client.agent_shares.get("ash_01j5q3k7m8r2s4tnz2hfp0q0c3")
```
</Tab>
<Tab value="Java">
```java
AgentShare agentShare = client.agentShares().get("ash_01j5q3k7m8r2s4tnz2hfp0q0c3");
```
</Tab>
</SDKTabs>

## Methods

| Method | Description |
|--------|-------------|
| [`get`](#get) | Get a single agent share by ID. |
| [`getByAgent`](#getbyagent) | Get all shares of a specific agent. |
| [`getByReference`](#getbyreference) | Get an agent share by its organization-scoped reference (org/slug). |
| [`getSharedProfile`](#getsharedprofile) | Get the public profile of a shared agent by the share's org/slug. |
| [`getSharedProfileForMember`](#getsharedprofileformember) | Get the profile of a shared agent as a signed-in organization member. |
| [`list`](#list) | List agent shares with optional label filtering. |
| [`apply`](#apply) | Create or update an agent share. |
| [`create`](#create) | Create an agent share. |
| [`update`](#update) | Update an existing agent share. |
| [`delete`](#delete) | Delete an agent share. |
| [`rotateShareLink`](#rotatesharelink) | Rotate the share's link token. |

### get

Get a single agent share by ID.

<SDKTabs>
<Tab value="TypeScript">
```typescript
const agentShare = await stigmer.agentShare.get(id);
```
</Tab>
<Tab value="Go">
```go
agentShare, err := client.AgentShare.Get(ctx, id)
```
</Tab>
<Tab value="Python">
```python
agent_share = client.agent_shares.get(id)
```
</Tab>
<Tab value="Java">
```java
AgentShare agentShare = client.agentShares().get(id);
```
</Tab>
</SDKTabs>

**Parameters**

| Parameter | Type | Description |
|-----------|------|-------------|
| `id` | `string` | Agent Share identifier. |

**Returns**: [`AgentShare`](#agentshare)

### getByAgent

Get all shares of a specific agent. Returns only shares the caller has access to.

This is how the Share dialog and CLI resolve an agent's existing share regardless of its slug (a renamed share keeps working).

<SDKTabs>
<Tab value="TypeScript">
```typescript
const result = await stigmer.agentShare.getByAgent({
  agentId: "...",
  pageInfo: {},
});
```
</Tab>
<Tab value="Go">
```go
result, err := client.AgentShare.GetByAgent(ctx, &stigmer.GetAgentSharesByAgentRequest{
  AgentId:  "...",
  PageInfo: &stigmer.PageInfoInput{},
})
```
</Tab>
<Tab value="Python">
```python
result = client.agent_shares.get_by_agent(GetAgentSharesByAgentRequest(
    agent_id="...",
    page_info=None,
))
```
</Tab>
<Tab value="Java">
```java
AgentShareList result = client.agentShares().getByAgent(GetAgentSharesByAgentRequest.builder()
    .agentId("...")
    .pageInfo(null)
    .build());
```
</Tab>
</SDKTabs>

**Parameters**: [`GetAgentSharesByAgentRequest`](#getagentsharesbyagentrequest)

**Returns**: [`AgentShareList`](#agentsharelist)

### getByReference

Get an agent share by its organization-scoped reference (org/slug).

<SDKTabs>
<Tab value="TypeScript">
```typescript
const agentShare = await stigmer.agentShare.getByReference({
  org: "...",
  kind: "...",
  slug: "...",
  version: "...",
});
```
</Tab>
<Tab value="Go">
```go
agentShare, err := client.AgentShare.GetByReference(ctx, &stigmer.ApiResourceReference{
  Org:     "...",
  Kind:    "...",
  Slug:    "...",
  Version: "...",
})
```
</Tab>
<Tab value="Python">
```python
agent_share = client.agent_shares.get_by_reference(ApiResourceReference(
    org="...",
    kind="...",
    slug="...",
    version="...",
))
```
</Tab>
<Tab value="Java">
```java
AgentShare agentShare = client.agentShares().getByReference(ApiResourceReference.builder()
    .org("...")
    .kind("...")
    .slug("...")
    .version("...")
    .build());
```
</Tab>
</SDKTabs>

**Parameters**: [`ResourceRef`](#resourceref)

**Returns**: [`AgentShare`](#agentshare)

### getSharedProfile

Get the public profile of a shared agent by the share's org/slug.

This is the resolution path for the hosted chat page: anonymous visitors (no Stigmer account, no token) resolve a shared link to the trimmed SharedAgentProfile — never the full Agent, whose spec carries the system prompt, environment declarations, and MCP wiring.

Returns NOT_FOUND when the share does not exist, is disabled, has the org audience (anonymous callers must not be able to distinguish an org-internal share from a nonexistent one; use getSharedProfileForMember instead), or the share link is locked and link_token does not match the share's current status.share_link_token. The cases are deliberately indistinguishable so an unshared, revoked, or rotated URL leaks nothing. Returns INVALID_ARGUMENT when org is empty: org+slug is the shared URL's identity, and cross-org slug matching on a public endpoint would enable enumeration.

<SDKTabs>
<Tab value="TypeScript">
```typescript
const sharedAgentProfile = await stigmer.agentShare.getSharedProfile({
  org: "...",
  slug: "...",
  linkToken: "...",
});
```
</Tab>
<Tab value="Go">
```go
sharedAgentProfile, err := client.AgentShare.GetSharedProfile(ctx, &stigmer.GetSharedProfileRequest{
  Org:       "...",
  Slug:      "...",
  LinkToken: "...",
})
```
</Tab>
<Tab value="Python">
```python
shared_agent_profile = client.agent_shares.get_shared_profile(GetSharedProfileRequest(
    org="...",
    slug="...",
    link_token="...",
))
```
</Tab>
<Tab value="Java">
```java
SharedAgentProfile sharedAgentProfile = client.agentShares().getSharedProfile(GetSharedProfileRequest.builder()
    .org("...")
    .slug("...")
    .linkToken("...")
    .build());
```
</Tab>
</SDKTabs>

**Parameters**: [`GetSharedProfileRequest`](#getsharedprofilerequest)

**Returns**: [`SharedAgentProfile`](#sharedagentprofile)

### getSharedProfileForMember

Get the profile of a shared agent as a signed-in organization member.

This is the resolution path for the hosted chat page when a share has the org audience: the public getSharedProfile deliberately returns NOT_FOUND for such shares, so a signed-in member resolves the same trimmed SharedAgentProfile through this authenticated RPC instead. Also resolves public-audience shares, so an authenticated caller can use one resolution path for any share.

Returns NOT_FOUND when the share does not exist, is disabled, the caller is not a member of the sharing organization, or the share is a public-audience share locked with a link token (this tokenless path must not reveal a killed link's profile) — the cases are deliberately indistinguishable so a share URL leaks nothing to non-members. Returns INVALID_ARGUMENT when org is empty.

<SDKTabs>
<Tab value="TypeScript">
```typescript
const sharedAgentProfile = await stigmer.agentShare.getSharedProfileForMember({
  org: "...",
  kind: "...",
  slug: "...",
  version: "...",
});
```
</Tab>
<Tab value="Go">
```go
sharedAgentProfile, err := client.AgentShare.GetSharedProfileForMember(ctx, &stigmer.ApiResourceReference{
  Org:     "...",
  Kind:    "...",
  Slug:    "...",
  Version: "...",
})
```
</Tab>
<Tab value="Python">
```python
shared_agent_profile = client.agent_shares.get_shared_profile_for_member(ApiResourceReference(
    org="...",
    kind="...",
    slug="...",
    version="...",
))
```
</Tab>
<Tab value="Java">
```java
SharedAgentProfile sharedAgentProfile = client.agentShares().getSharedProfileForMember(ApiResourceReference.builder()
    .org("...")
    .kind("...")
    .slug("...")
    .version("...")
    .build());
```
</Tab>
</SDKTabs>

**Parameters**: [`ResourceRef`](#resourceref)

**Returns**: [`SharedAgentProfile`](#sharedagentprofile)

### list

List agent shares with optional label filtering.

<SDKTabs>
<Tab value="TypeScript">
```typescript
const result = await stigmer.agentShare.list({
  org: "...",
  labels: {},
  pageInfo: {},
});
```
</Tab>
<Tab value="Go">
```go
result, err := client.AgentShare.List(ctx, &stigmer.ListAgentSharesRequest{
  Org:      "...",
  Labels:   map[string]string{},
  PageInfo: &stigmer.PageInfoInput{},
})
```
</Tab>
<Tab value="Python">
```python
result = client.agent_shares.list(ListAgentSharesRequest(
    org="...",
    labels={},
    page_info=None,
))
```
</Tab>
<Tab value="Java">
```java
AgentShareList result = client.agentShares().list(ListAgentSharesRequest.builder()
    .org("...")
    .labels(Map.of())
    .pageInfo(null)
    .build());
```
</Tab>
</SDKTabs>

**Parameters**: [`ListAgentSharesRequest`](#listagentsharesrequest)

**Returns**: [`AgentShareList`](#agentsharelist)

### apply

Create or update an agent share.

<SDKTabs>
<Tab value="TypeScript">
```typescript
const agentShare = await stigmer.agentShare.apply({
  name: "my-agent-share",
  org: "acme",
  slug: "...",
  labels: {},
  agentRef: {},
  enabled: false,
  audience: "...",
  allowedOrigins: [],
  messages: {},
  environmentRefs: [],
});
```
</Tab>
<Tab value="Go">
```go
agentShare, err := client.AgentShare.Apply(ctx, &stigmer.AgentShareInput{
  Name:            "my-agent-share",
  Org:             "acme",
  Slug:            "...",
  Labels:          map[string]string{},
  AgentRef:        &stigmer.ResourceRef{},
  Enabled:         false,
  Audience:        "...",
  AllowedOrigins:  []string{},
  Messages:        &stigmer.AgentShareMessagesInput{},
  EnvironmentRefs: []stigmer.ResourceRef{},
})
```
</Tab>
<Tab value="Python">
```python
agent_share = client.agent_shares.apply(AgentShareInput(
    name="my-agent-share",
    org="acme",
    slug="...",
    labels={},
    agent_ref=None,
    enabled=False,
    audience="...",
    allowed_origins=[],
    messages=None,
    environment_refs=[],
))
```
</Tab>
<Tab value="Java">
```java
AgentShare agentShare = client.agentShares().apply(AgentShareInput.builder()
    .name("my-agent-share")
    .org("acme")
    .slug("...")
    .labels(Map.of())
    .agentRef(null)
    .enabled(false)
    .audience("...")
    .allowedOrigins(List.of())
    .messages(null)
    .environmentRefs(List.of())
    .build());
```
</Tab>
</SDKTabs>

**Parameters**: [`AgentShareInput`](#agentshareinput)

**Returns**: [`AgentShare`](#agentshare)

### create

Create an agent share.

Enabling a share is a billing-affecting decision: conversations over the hosted link consume the sharing organization's credits.

<SDKTabs>
<Tab value="TypeScript">
```typescript
const agentShare = await stigmer.agentShare.create({
  name: "my-agent-share",
  org: "acme",
  slug: "...",
  labels: {},
  agentRef: {},
  enabled: false,
  audience: "...",
  allowedOrigins: [],
  messages: {},
  environmentRefs: [],
});
```
</Tab>
<Tab value="Go">
```go
agentShare, err := client.AgentShare.Create(ctx, &stigmer.AgentShareInput{
  Name:            "my-agent-share",
  Org:             "acme",
  Slug:            "...",
  Labels:          map[string]string{},
  AgentRef:        &stigmer.ResourceRef{},
  Enabled:         false,
  Audience:        "...",
  AllowedOrigins:  []string{},
  Messages:        &stigmer.AgentShareMessagesInput{},
  EnvironmentRefs: []stigmer.ResourceRef{},
})
```
</Tab>
<Tab value="Python">
```python
agent_share = client.agent_shares.create(AgentShareInput(
    name="my-agent-share",
    org="acme",
    slug="...",
    labels={},
    agent_ref=None,
    enabled=False,
    audience="...",
    allowed_origins=[],
    messages=None,
    environment_refs=[],
))
```
</Tab>
<Tab value="Java">
```java
AgentShare agentShare = client.agentShares().create(AgentShareInput.builder()
    .name("my-agent-share")
    .org("acme")
    .slug("...")
    .labels(Map.of())
    .agentRef(null)
    .enabled(false)
    .audience("...")
    .allowedOrigins(List.of())
    .messages(null)
    .environmentRefs(List.of())
    .build());
```
</Tab>
</SDKTabs>

**Parameters**: [`AgentShareInput`](#agentshareinput)

**Returns**: [`AgentShare`](#agentshare)

### update

Update an existing agent share.

Replaces the spec wholesale: a manifest that omits audience resets the share to public, and one that omits environment_refs unbinds them (fails closed). The slug and referenced agent are immutable.

<SDKTabs>
<Tab value="TypeScript">
```typescript
const agentShare = await stigmer.agentShare.update({
  name: "my-agent-share",
  org: "acme",
  slug: "...",
  labels: {},
  agentRef: {},
  enabled: false,
  audience: "...",
  allowedOrigins: [],
  messages: {},
  environmentRefs: [],
});
```
</Tab>
<Tab value="Go">
```go
agentShare, err := client.AgentShare.Update(ctx, &stigmer.AgentShareInput{
  Name:            "my-agent-share",
  Org:             "acme",
  Slug:            "...",
  Labels:          map[string]string{},
  AgentRef:        &stigmer.ResourceRef{},
  Enabled:         false,
  Audience:        "...",
  AllowedOrigins:  []string{},
  Messages:        &stigmer.AgentShareMessagesInput{},
  EnvironmentRefs: []stigmer.ResourceRef{},
})
```
</Tab>
<Tab value="Python">
```python
agent_share = client.agent_shares.update(AgentShareInput(
    name="my-agent-share",
    org="acme",
    slug="...",
    labels={},
    agent_ref=None,
    enabled=False,
    audience="...",
    allowed_origins=[],
    messages=None,
    environment_refs=[],
))
```
</Tab>
<Tab value="Java">
```java
AgentShare agentShare = client.agentShares().update(AgentShareInput.builder()
    .name("my-agent-share")
    .org("acme")
    .slug("...")
    .labels(Map.of())
    .agentRef(null)
    .enabled(false)
    .audience("...")
    .allowedOrigins(List.of())
    .messages(null)
    .environmentRefs(List.of())
    .build());
```
</Tab>
</SDKTabs>

**Parameters**: [`AgentShareInput`](#agentshareinput)

**Returns**: [`AgentShare`](#agentshare)

### delete

Delete an agent share.

Full teardown of the channel: the hosted link stops resolving, live guest tokens die on their next message, and the share's configuration (origins, messages, credentials, link token) is gone. To pause serving while keeping configuration, update the share with enabled=false instead.

<SDKTabs>
<Tab value="TypeScript">
```typescript
const agentShare = await stigmer.agentShare.delete(id);
```
</Tab>
<Tab value="Go">
```go
agentShare, err := client.AgentShare.Delete(ctx, id)
```
</Tab>
<Tab value="Python">
```python
agent_share = client.agent_shares.delete(id)
```
</Tab>
<Tab value="Java">
```java
AgentShare agentShare = client.agentShares().delete(id);
```
</Tab>
</SDKTabs>

**Parameters**

| Parameter | Type | Description |
|-----------|------|-------------|
| `id` | `string` | Agent Share identifier. |

**Returns**: [`AgentShare`](#agentshare)

### rotateShareLink

Rotate the share's link token.

Generates a fresh server-side token for the share's hosted chat link. The share URL becomes `/chat/&lt;org&gt;/&lt;slug&gt;?k=&lt;token&gt;` and the previous link (tokened or plain) stops working immediately — including for visitors mid-conversation. Use this to kill a leaked or over-shared public link without disabling the share or changing its slug.

The token lives in status.share_link_token, so manifest applies never reset it. Rotation affects public-audience shares only; org-audience access is governed by live org membership instead.

<SDKTabs>
<Tab value="TypeScript">
```typescript
const agentShare = await stigmer.agentShare.rotateShareLink({
  resourceId: "...",
});
```
</Tab>
<Tab value="Go">
```go
agentShare, err := client.AgentShare.RotateShareLink(ctx, &stigmer.RotateShareLinkInput{
  ResourceId: "...",
})
```
</Tab>
<Tab value="Python">
```python
agent_share = client.agent_shares.rotate_share_link(RotateShareLinkInput(
    resource_id="...",
))
```
</Tab>
<Tab value="Java">
```java
AgentShare agentShare = client.agentShares().rotateShareLink(RotateShareLinkInput.builder()
    .resourceId("...")
    .build());
```
</Tab>
</SDKTabs>

**Parameters**: [`RotateShareLinkInput`](#rotatesharelinkinput)

**Returns**: [`AgentShare`](#agentshare)

## Types

### AgentShareInput

Input for creating or updating an Agent Share.

<TypeTable
  type={{
    name: { type: "string", description: "Resource name.", required: true },
    slug: { type: "string", description: "URL-friendly identifier." },
    org: { type: "string", description: "Organization slug.", required: true },
    labels: { type: "Record<string, string>", description: "Key-value labels." },
    agentRef: { type: "ResourceRef", description: "Reference to the agent this share distributes.", typeDescriptionLink: "#resourceref", required: true },
    enabled: { type: "boolean", description: "Whether hosted-chat access for the configured audience is enabled." },
    audience: { type: "AgentShareAudience", description: "Who can chat over this share.", typeDescriptionLink: "#agentshareaudience" },
    allowedOrigins: { type: "string[]", description: "Origins permitted to embed this share's chat widget." },
    messages: { type: "AgentShareMessagesInput", description: "Owner-customizable copy shown to visitors when a launch-gate limit refuses their message.", typeDescriptionLink: "#agentsharemessagesinput" },
    environmentRefs: { type: "ResourceRef[]", description: "References to Environment resources whose values are provided to guest conversations on this share.", typeDescriptionLink: "#resourceref" },
  }}
/>

### ResourceRef

Generic reference to any API resource by org and slug.

<TypeTable
  type={{
    org: { type: "string", description: "Organization that owns the referenced resource." },
    kind: { type: "ApiResourceKind", description: "Kind of the referenced resource (e.g., SKILL, AGENT, MCP_SERVER).", typeDescriptionLink: "/docs/sdk/resources/commons#apiresourcekind" },
    slug: { type: "string", description: "Resource slug (user-friendly identifier, unique within org).", required: true },
    version: { type: "string", description: "Version of the resource (optional, only applicable to versioned resources like Skills)." },
  }}
/>

### AgentShareMessagesInput

AgentShareMessages holds owner-customizable visitor-facing copy for shared-agent limit refusals.

<TypeTable
  type={{
    rateLimited: { type: "string", description: "Shown when a visitor or the org exceeds the message rate limit." },
    unavailable: { type: "string", description: "Shown when the share cannot serve the conversation — the org's credits are exhausted or the agent's tools are not ready (fails closed)." },
    conversationEnded: { type: "string", description: "Shown when a conversation hits its turn limit or inactivity timeout." },
  }}
/>

### RotateShareLinkInput

Input for rotating a share's link token.

<TypeTable
  type={{
    resourceId: { type: "string", description: "ID of the agent share whose link token is being rotated.", required: true },
  }}
/>

### GetAgentSharesByAgentRequest

Input for retrieving all shares of a specific agent.

<TypeTable
  type={{
    agentId: { type: "string", description: "Identifier of the agent whose shares to retrieve.", required: true },
    pageInfo: { type: "PageInfoInput", description: "Pagination options." },
  }}
/>

### AgentShareList

Response containing a paginated list of agent shares.

<TypeTable
  type={{
    totalCount: { type: "number", description: "Total number of agent shares matching the query." },
    items: { type: "AgentShareInput[]", description: "Agent shares in the current page.", typeDescriptionLink: "#agentshareinput" },
  }}
/>

### ListAgentSharesRequest

Input for listing agent shares within an organization.

<TypeTable
  type={{
    org: { type: "string", description: "Organization slug to scope the listing." },
    labels: { type: "Record<string, string>", description: "Filter by metadata labels." },
    pageInfo: { type: "PageInfoInput", description: "Pagination options." },
  }}
/>

### GetSharedProfileRequest

Input for resolving a share's public profile.

<TypeTable
  type={{
    org: { type: "string", description: "Organization slug from the share URL." },
    slug: { type: "string", description: "Share slug from the share URL." },
    linkToken: { type: "string", description: "Link token from the share URL's `?k=` parameter." },
  }}
/>

### SharedAgentProfile

SharedAgentProfile is the public projection of a shared agent.

<TypeTable
  type={{
    org: { type: "string", description: "Organization that owns the share." },
    slug: { type: "string", description: "Slug of the share (unique within the org)." },
    name: { type: "string", description: "Human-readable agent name for the chat page header." },
    description: { type: "string", description: "Human-readable description for the chat page header." },
    iconUrl: { type: "string", description: "Icon URL for the chat page header." },
    defaultInstanceId: { type: "string", description: "ID of the referenced agent's default instance, used by the hosted chat page to create sessions." },
  }}
/>

### AgentShare

AgentShare turns an agent into a hosted chat link with its own audience, embed origins, and tool credentials.

<TypeTable
  type={{
    apiVersion: { type: "string", description: "API version for this resource type." },
    kind: { type: "string", description: "Resource kind identifier." },
    metadata: { type: "ApiResourceMetadata", description: "Resource metadata including id, name, org, slug, labels, visibility, and timestamps.", typeDescriptionLink: "/docs/sdk/resources/commons#apiresourcemetadata" },
    spec: { type: "AgentShareInput", description: "Resource specification. See [`AgentShareInput`](#agentshareinput).", typeDescriptionLink: "#agentshareinput" },
    status: { type: "AgentShareStatus", description: "System-managed state. See [`AgentShareStatus`](#agentsharestatus).", typeDescriptionLink: "#agentsharestatus" },
  }}
/>

### AgentShareStatus

AgentShareStatus contains system-managed state for an agent share.

<TypeTable
  type={{
    audit: { type: "ApiResourceAudit", description: "Standard audit information (created_at, updated_at, created_by, etc.).", typeDescriptionLink: "/docs/sdk/resources/commons#apiresourceaudit" },
    shareLinkToken: { type: "string", description: "Rotatable token protecting the share's public link." },
    agentId: { type: "string", description: "ID of the agent this share was created against." },
  }}
/>

### AgentShareAudience

AgentShareAudience selects who can chat over a share's hosted link.

| Value | Description |
|-------|-------------|
| `agent_share_audience_public` | Anyone with the link can chat — no Stigmer account required. |
| `agent_share_audience_org` | Only signed-in members of the sharing organization can chat. |

