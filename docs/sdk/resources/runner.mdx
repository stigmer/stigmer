---
title: Runner
---

{/* Auto-generated by stigmer-codegen. DO NOT EDIT. */}

A Runner is a process that connects to Stigmer and executes your Agents. The
spec is intentionally thin — a runner registers itself and reports all
operational state (phase, machine info, capacity) via heartbeat. Sessions bind
to a runner through `runner_id`.

```yaml
apiVersion: agentic.stigmer.ai/v1
kind: Runner
metadata:
  name: my-macbook
  slug: my-macbook
spec:
  description: "Development runner on my MacBook"
```

## Client Access

<SDKTabs>
<Tab value="TypeScript">
```typescript
const runner = await stigmer.runner.get("rnr_01j5q3k7m8r2s4tnz2hfp0q0c3");
```
</Tab>
<Tab value="Go">
```go
runner, err := client.Runner.Get(ctx, "rnr_01j5q3k7m8r2s4tnz2hfp0q0c3")
```
</Tab>
<Tab value="Python">
```python
runner = client.runners.get("rnr_01j5q3k7m8r2s4tnz2hfp0q0c3")
```
</Tab>
<Tab value="Java">
```java
Runner runner = client.runners().get("rnr_01j5q3k7m8r2s4tnz2hfp0q0c3");
```
</Tab>
</SDKTabs>

## Methods

| Method | Description | Streaming |
|--------|-------------|-----------|
| [`get`](#get) | Get a single runner by ID. | |
| [`getByReference`](#getbyreference) | Get a runner by its organization-scoped reference (org/slug). | |
| [`list`](#list) | List runners within an organization. | |
| [`apply`](#apply) | Create or update a runner. | |
| [`create`](#create) | Create a new runner. | |
| [`update`](#update) | Update an existing runner. | |
| [`delete`](#delete) | Delete a runner. | |
| [`connect`](#connect) | Establish a bidirectional command stream between the runner and the server. | Server |
| [`createLaunchToken`](#createlaunchtoken) | Create a one-time launch token for the browser-to-CLI runner handshake. | |
| [`exchangeLaunchToken`](#exchangelaunchtoken) | Exchange a one-time launch token for long-lived runner credentials. | |
| [`sendCommand`](#sendcommand) | Send a command to a connected runner and return the response synchronously. | |
| [`stop`](#stop) | Stop a running runner gracefully. | |

### get

Get a single runner by ID.

<SDKTabs>
<Tab value="TypeScript">
```typescript
const runner = await stigmer.runner.get(id);
```
</Tab>
<Tab value="Go">
```go
runner, err := client.Runner.Get(ctx, id)
```
</Tab>
<Tab value="Python">
```python
runner = client.runners.get(id)
```
</Tab>
<Tab value="Java">
```java
Runner runner = client.runners().get(id);
```
</Tab>
</SDKTabs>

**Parameters**

| Parameter | Type | Description |
|-----------|------|-------------|
| `id` | `string` | Runner identifier. |

**Returns**: [`Runner`](#runner)

### getByReference

Get a runner by its organization-scoped reference (org/slug).

Used by the CLI to resolve a runner by name: stigmer run agent my-agent --runner my-macbook resolves "my-macbook" to the full Runner resource.

<SDKTabs>
<Tab value="TypeScript">
```typescript
const runner = await stigmer.runner.getByReference({
  org: "...",
  kind: "...",
  slug: "...",
  version: "...",
});
```
</Tab>
<Tab value="Go">
```go
runner, err := client.Runner.GetByReference(ctx, &stigmer.ApiResourceReference{
  Org:     "...",
  Kind:    "...",
  Slug:    "...",
  Version: "...",
})
```
</Tab>
<Tab value="Python">
```python
runner = client.runners.get_by_reference(ApiResourceReference(
    org="...",
    kind="...",
    slug="...",
    version="...",
))
```
</Tab>
<Tab value="Java">
```java
Runner runner = client.runners().getByReference(ApiResourceReference.builder()
    .org("...")
    .kind("...")
    .slug("...")
    .version("...")
    .build());
```
</Tab>
</SDKTabs>

**Parameters**: [`ResourceRef`](#resourceref)

**Returns**: [`Runner`](#runner)

### list

List runners within an organization.

Supports label-based filtering for common use cases: - Session composer: exclude system-managed runners, show only READY - Admin view: show all runners including system-managed - Debugging: filter by specific labels

<SDKTabs>
<Tab value="TypeScript">
```typescript
const result = await stigmer.runner.list({
  org: "...",
  labels: {},
  pageInfo: {},
});
```
</Tab>
<Tab value="Go">
```go
result, err := client.Runner.List(ctx, &stigmer.ListRunnersRequest{
  Org:      "...",
  Labels:   map[string]string{},
  PageInfo: &stigmer.PageInfoInput{},
})
```
</Tab>
<Tab value="Python">
```python
result = client.runners.list(ListRunnersRequest(
    org="...",
    labels={},
    page_info=None,
))
```
</Tab>
<Tab value="Java">
```java
RunnerList result = client.runners().list(ListRunnersRequest.builder()
    .org("...")
    .labels(Map.of())
    .pageInfo(null)
    .build());
```
</Tab>
</SDKTabs>

**Parameters**: [`ListRunnersRequest`](#listrunnersrequest)

**Returns**: [`RunnerList`](#runnerlist)

### apply

Create or update a runner.

<SDKTabs>
<Tab value="TypeScript">
```typescript
const runner = await stigmer.runner.apply({
  name: "my-runner",
  org: "acme",
  slug: "...",
  labels: {},
  description: "...",
});
```
</Tab>
<Tab value="Go">
```go
runner, err := client.Runner.Apply(ctx, &stigmer.RunnerInput{
  Name:        "my-runner",
  Org:         "acme",
  Slug:        "...",
  Labels:      map[string]string{},
  Description: "...",
})
```
</Tab>
<Tab value="Python">
```python
runner = client.runners.apply(RunnerInput(
    name="my-runner",
    org="acme",
    slug="...",
    labels={},
    description="...",
))
```
</Tab>
<Tab value="Java">
```java
Runner runner = client.runners().apply(RunnerInput.builder()
    .name("my-runner")
    .org("acme")
    .slug("...")
    .labels(Map.of())
    .description("...")
    .build());
```
</Tab>
</SDKTabs>

**Parameters**: [`RunnerInput`](#runnerinput)

**Returns**: [`Runner`](#runner)

### create

Create a new runner.

<SDKTabs>
<Tab value="TypeScript">
```typescript
const runner = await stigmer.runner.create({
  name: "my-runner",
  org: "acme",
  slug: "...",
  labels: {},
  description: "...",
});
```
</Tab>
<Tab value="Go">
```go
runner, err := client.Runner.Create(ctx, &stigmer.RunnerInput{
  Name:        "my-runner",
  Org:         "acme",
  Slug:        "...",
  Labels:      map[string]string{},
  Description: "...",
})
```
</Tab>
<Tab value="Python">
```python
runner = client.runners.create(RunnerInput(
    name="my-runner",
    org="acme",
    slug="...",
    labels={},
    description="...",
))
```
</Tab>
<Tab value="Java">
```java
Runner runner = client.runners().create(RunnerInput.builder()
    .name("my-runner")
    .org("acme")
    .slug("...")
    .labels(Map.of())
    .description("...")
    .build());
```
</Tab>
</SDKTabs>

**Parameters**: [`RunnerInput`](#runnerinput)

**Returns**: [`Runner`](#runner)

### update

Update an existing runner.

<SDKTabs>
<Tab value="TypeScript">
```typescript
const runner = await stigmer.runner.update({
  name: "my-runner",
  org: "acme",
  slug: "...",
  labels: {},
  description: "...",
});
```
</Tab>
<Tab value="Go">
```go
runner, err := client.Runner.Update(ctx, &stigmer.RunnerInput{
  Name:        "my-runner",
  Org:         "acme",
  Slug:        "...",
  Labels:      map[string]string{},
  Description: "...",
})
```
</Tab>
<Tab value="Python">
```python
runner = client.runners.update(RunnerInput(
    name="my-runner",
    org="acme",
    slug="...",
    labels={},
    description="...",
))
```
</Tab>
<Tab value="Java">
```java
Runner runner = client.runners().update(RunnerInput.builder()
    .name("my-runner")
    .org("acme")
    .slug("...")
    .labels(Map.of())
    .description("...")
    .build());
```
</Tab>
</SDKTabs>

**Parameters**: [`RunnerInput`](#runnerinput)

**Returns**: [`Runner`](#runner)

### delete

Delete a runner.

<SDKTabs>
<Tab value="TypeScript">
```typescript
const runner = await stigmer.runner.delete(id);
```
</Tab>
<Tab value="Go">
```go
runner, err := client.Runner.Delete(ctx, id)
```
</Tab>
<Tab value="Python">
```python
runner = client.runners.delete(id)
```
</Tab>
<Tab value="Java">
```java
Runner runner = client.runners().delete(id);
```
</Tab>
</SDKTabs>

**Parameters**

| Parameter | Type | Description |
|-----------|------|-------------|
| `id` | `string` | Runner identifier. |

**Returns**: [`Runner`](#runner)

### connect

> **Server Streaming** — This method returns a stream of updates, not a single response.

Establish a bidirectional command stream between the runner and the server.

This is the runner's primary ongoing communication channel. The runner pushes heartbeats (liveness + state); the server pushes commands (e.g., ListDirectory for workspace browsing). Both directions use the same open connection.

Stream lifecycle: 1. Runner calls apply to register/reactivate, then opens this stream. 2. First message MUST be a RunnerHeartbeat (authenticates via runner_id). 3. Runner sends heartbeats every 30s. 4. Server pushes RunnerCommandRequest when the UI triggers an operation. 5. Runner handles commands locally and sends RunnerCommandResponse. 6. On graceful shutdown: runner sends phase=STOPPED heartbeat, closes stream. 7. On disconnect: server starts heartbeat timeout (90s) -&gt; STOPPED.

<SDKTabs>
<Tab value="TypeScript">
```typescript
const stream = stigmer.runner.connect(input);
for await (const runnerStreamServerMessage of stream) {
  // process runnerStreamServerMessage
}
```
</Tab>
<Tab value="Go">
```go
stream, err := client.Runner.Connect(ctx, input)
for {
    runnerStreamServerMessage, err := stream.Recv()
    if err != nil {
        break
    }
    // process runnerStreamServerMessage
}
```
</Tab>
<Tab value="Python">
```python
for runner_stream_server_message in client.runners.connect(input):
    # process runner_stream_server_message
```
</Tab>
<Tab value="Java">
```java
client.runners().connect(input, runnerStreamServerMessage -> {
    // process runnerStreamServerMessage
});
```
</Tab>
</SDKTabs>

**Parameters**: [`RunnerStreamClientMessage`](#runnerstreamclientmessage)

**Returns**: Stream&lt;[`RunnerStreamServerMessage`](#runnerstreamservermessage)&gt;

### createLaunchToken

Create a one-time launch token for the browser-to-CLI runner handshake.

Called by the web console when the user clicks "Launch Local Runner." The server mints a Stigmer JWT for the caller, wraps it in an opaque token stored in Redis (60s TTL, single-use), and returns the token for the browser to embed in a stigmer:// URL.

The caller must have can_create_runner permission in the organization — if you can create a runner, you can create a launch token.

<SDKTabs>
<Tab value="TypeScript">
```typescript
const createLaunchTokenResponse = await stigmer.runner.createLaunchToken({
  org: "...",
});
```
</Tab>
<Tab value="Go">
```go
createLaunchTokenResponse, err := client.Runner.CreateLaunchToken(ctx, &stigmer.CreateLaunchTokenRequest{
  Org: "...",
})
```
</Tab>
<Tab value="Python">
```python
create_launch_token_response = client.runners.create_launch_token(CreateLaunchTokenRequest(
    org="...",
))
```
</Tab>
<Tab value="Java">
```java
CreateLaunchTokenResponse createLaunchTokenResponse = client.runners().createLaunchToken(CreateLaunchTokenRequest.builder()
    .org("...")
    .build());
```
</Tab>
</SDKTabs>

**Parameters**: [`CreateLaunchTokenRequest`](#createlaunchtokenrequest)

**Returns**: [`CreateLaunchTokenResponse`](#createlaunchtokenresponse)

### exchangeLaunchToken

Exchange a one-time launch token for long-lived runner credentials.

Called by the CLI (or Desktop app) after receiving a stigmer:// URL from the OS. The token is consumed atomically — a second exchange attempt returns NOT_FOUND.

<SDKTabs>
<Tab value="TypeScript">
```typescript
const exchangeLaunchTokenResponse = await stigmer.runner.exchangeLaunchToken({
  token: "...",
});
```
</Tab>
<Tab value="Go">
```go
exchangeLaunchTokenResponse, err := client.Runner.ExchangeLaunchToken(ctx, &stigmer.ExchangeLaunchTokenRequest{
  Token: "...",
})
```
</Tab>
<Tab value="Python">
```python
exchange_launch_token_response = client.runners.exchange_launch_token(ExchangeLaunchTokenRequest(
    token="...",
))
```
</Tab>
<Tab value="Java">
```java
ExchangeLaunchTokenResponse exchangeLaunchTokenResponse = client.runners().exchangeLaunchToken(ExchangeLaunchTokenRequest.builder()
    .token("...")
    .build());
```
</Tab>
</SDKTabs>

**Parameters**: [`ExchangeLaunchTokenRequest`](#exchangelaunchtokenrequest)

**Returns**: [`ExchangeLaunchTokenResponse`](#exchangelaunchtokenresponse)

### sendCommand

Send a command to a connected runner and return the response synchronously.

This is the API entry point for UI-triggered runner commands. The server looks up the runner's active bidi stream, pushes the command, and blocks until the runner responds or the timeout (10s) expires.

Requires an active connect stream — returns UNAVAILABLE if the runner is not connected. Returns FAILED_PRECONDITION if the runner's phase prevents command delivery (STOPPED, PENDING, FAILED).

<SDKTabs>
<Tab value="TypeScript">
```typescript
const runnerCommandResponse = await stigmer.runner.sendCommand({
  runnerId: "...",
  listDirectory: {},
});
```
</Tab>
<Tab value="Go">
```go
runnerCommandResponse, err := client.Runner.SendCommand(ctx, &stigmer.RunnerSendCommandInput{
  RunnerId:      "...",
  ListDirectory: &stigmer.ListDirectoryRequestInput{},
})
```
</Tab>
<Tab value="Python">
```python
runner_command_response = client.runners.send_command(RunnerSendCommandInput(
    runner_id="...",
    list_directory=None,
))
```
</Tab>
<Tab value="Java">
```java
RunnerCommandResponse runnerCommandResponse = client.runners().sendCommand(RunnerSendCommandInput.builder()
    .runnerId("...")
    .listDirectory(null)
    .build());
```
</Tab>
</SDKTabs>

**Parameters**: [`RunnerSendCommandInput`](#runnersendcommandinput)

**Returns**: [`RunnerCommandResponse`](#runnercommandresponse)

### stop

Stop a running runner gracefully.

If the runner is connected: sends a StopRunnerRequest via the bidi stream, waits for acknowledgment, then returns the updated Runner. The runner will send a STOPPED heartbeat and close its stream after acknowledging — the phase transition completes asynchronously.

If the runner is not connected (offline, already stopped): directly transitions the runner to STOPPED and returns the updated resource.

Idempotent: stopping an already-STOPPED or FAILED runner returns the resource as-is without error.

<SDKTabs>
<Tab value="TypeScript">
```typescript
const runner = await stigmer.runner.stop({
  runnerId: "...",
  reason: "...",
});
```
</Tab>
<Tab value="Go">
```go
runner, err := client.Runner.Stop(ctx, &stigmer.RunnerStopInput{
  RunnerId: "...",
  Reason:   "...",
})
```
</Tab>
<Tab value="Python">
```python
runner = client.runners.stop(RunnerStopInput(
    runner_id="...",
    reason="...",
))
```
</Tab>
<Tab value="Java">
```java
Runner runner = client.runners().stop(RunnerStopInput.builder()
    .runnerId("...")
    .reason("...")
    .build());
```
</Tab>
</SDKTabs>

**Parameters**: [`RunnerStopInput`](#runnerstopinput)

**Returns**: [`Runner`](#runner)

## Types

### RunnerInput

Input for creating or updating a Runner.

<TypeTable
  type={{
    name: { type: "string", description: "Resource name.", required: true },
    slug: { type: "string", description: "URL-friendly identifier." },
    org: { type: "string", description: "Organization slug.", required: true },
    labels: { type: "Record<string, string>", description: "Key-value labels." },
    description: { type: "string", description: "Human-readable purpose of this runner." },
  }}
/>

### RunnerSendCommandInput

RunnerSendCommandInput is the request for the sendCommand unary RPC.

<TypeTable
  type={{
    runnerId: { type: "string", description: "ID of the runner to send the command to.", required: true },
    listDirectory: { type: "ListDirectoryRequestInput", description: "" },
  }}
/>

### RunnerCommandResponse

RunnerCommandResponse is the runner's reply to a RunnerCommandRequest.

<TypeTable
  type={{
    requestId: { type: "string", description: "Echoed from RunnerCommandRequest.request_id." },
    listDirectory: { type: "ListDirectoryResponseInput", description: "" },
    error: { type: "RunnerCommandErrorInput", description: "" },
    stop: { type: "StopRunnerResponseInput", description: "" },
  }}
/>

### RunnerStopInput

RunnerStopInput is the request for the stop RPC.

<TypeTable
  type={{
    runnerId: { type: "string", description: "ID of the runner to stop.", required: true },
    reason: { type: "string", description: "Optional reason for the stop." },
  }}
/>

### RunnerStreamClientMessage

RunnerStreamClientMessage is the envelope for all runner-to-server messages on the connect bidi stream.

<TypeTable
  type={{
    heartbeat: { type: "RunnerHeartbeatInput", description: "" },
    commandResponse: { type: "RunnerCommandResponseInput", description: "" },
  }}
/>

### RunnerStreamServerMessage

RunnerStreamServerMessage is the envelope for all server-to-runner messages on the connect bidi stream.

<TypeTable
  type={{
    commandRequest: { type: "RunnerCommandRequestInput", description: "" },
  }}
/>

### CreateLaunchTokenRequest

CreateLaunchTokenRequest initiates a browser-to-CLI credential handshake.

<TypeTable
  type={{
    org: { type: "string", description: "Organization slug." },
  }}
/>

### CreateLaunchTokenResponse

CreateLaunchTokenResponse contains the opaque launch token for the browser to embed in the stigmer:// URL.

<TypeTable
  type={{
    token: { type: "string", description: "Opaque, single-use token." },
    expiresAt: { type: "string", description: "Absolute expiry time." },
  }}
/>

### ExchangeLaunchTokenRequest

ExchangeLaunchTokenRequest is the CLI's side of the handshake.

<TypeTable
  type={{
    token: { type: "string", description: "The opaque token received from the stigmer:// URL." },
  }}
/>

### ExchangeLaunchTokenResponse

ExchangeLaunchTokenResponse provides the CLI with everything it needs to operate as an authenticated runner: a Stigmer-signed JWT and the org scope.

<TypeTable
  type={{
    accessToken: { type: "string", description: "Stigmer-signed JWT for authenticating subsequent runner operations (apply, connect, heartbeat)." },
    tokenType: { type: "string", description: "Token type." },
    expiresIn: { type: "number", description: "Token lifetime in seconds from the time of issuance." },
    org: { type: "string", description: "Organization slug the credentials are scoped to." },
  }}
/>

### ResourceRef

Generic reference to any API resource by org and slug.

<TypeTable
  type={{
    org: { type: "string", description: "Organization that owns the referenced resource." },
    kind: { type: "ApiResourceKind", description: "Kind of the referenced resource (e.g., SKILL, AGENT, MCP_SERVER).", typeDescriptionLink: "/docs/sdk/resources/commons#apiresourcekind" },
    slug: { type: "string", description: "Resource slug (user-friendly identifier, unique within org).", required: true },
    version: { type: "string", description: "Version of the resource (optional, only applicable to versioned resources like Skills)." },
  }}
/>

### ListRunnersRequest

ListRunnersRequest is the input for listing runners within an organization.

<TypeTable
  type={{
    org: { type: "string", description: "Organization slug to scope the listing." },
    labels: { type: "Record<string, string>", description: "Filter by metadata labels." },
    pageInfo: { type: "PageInfoInput", description: "Pagination options." },
  }}
/>

### RunnerList

RunnerList is the response for listing runners.

<TypeTable
  type={{
    totalCount: { type: "number", description: "Total number of runners matching the query." },
    items: { type: "RunnerInput[]", description: "Runners in the current page.", typeDescriptionLink: "#runnerinput" },
  }}
/>

### Runner

Runner represents a process that executes work on the Stigmer platform.

<TypeTable
  type={{
    apiVersion: { type: "string", description: "API version for this resource type." },
    kind: { type: "string", description: "Resource kind identifier." },
    metadata: { type: "ApiResourceMetadata", description: "Resource metadata including id, name, org, slug, labels, visibility, and timestamps.", typeDescriptionLink: "/docs/sdk/resources/commons#apiresourcemetadata" },
    spec: { type: "RunnerInput", description: "Resource specification. See [`RunnerInput`](#runnerinput).", typeDescriptionLink: "#runnerinput" },
    status: { type: "RunnerStatus", description: "System-managed state. See [`RunnerStatus`](#runnerstatus).", typeDescriptionLink: "#runnerstatus" },
  }}
/>

### RunnerStatus

RunnerStatus contains all operational state for a runner.

<TypeTable
  type={{
    audit: { type: "ApiResourceAudit", description: "Standard audit information (created_at, updated_at, created_by, etc.).", typeDescriptionLink: "/docs/sdk/resources/commons#apiresourceaudit" },
    phase: { type: "RunnerPhase", description: "Current operational phase of the runner process.", typeDescriptionLink: "#runnerphase" },
    taskQueue: { type: "string", description: "Temporal task queue this runner polls for work." },
    lastHeartbeatAt: { type: "string", description: "Timestamp of the last successful heartbeat from the runner process." },
    startedAt: { type: "string", description: "Timestamp when this runner first transitioned to READY." },
    stoppedAt: { type: "string", description: "Timestamp when this runner last transitioned to STOPPED or FAILED." },
    currentExecutions: { type: "number", description: "Number of executions currently in progress on this runner." },
    connectionInfo: { type: "RunnerConnectionInfo", description: "Self-reported information about the machine running this runner.", typeDescriptionLink: "#runnerconnectioninfo" },
    processExecutions: { type: "Record<string, number>", description: "Per-process execution counts in a multi-process sandbox." },
    idleSince: { type: "string", description: "Timestamp when all processes in this runner last became idle (every value in process_executions is zero and no streams are active)." },
  }}
/>

### RunnerConnectionInfo

RunnerConnectionInfo contains self-reported details about the runner's host environment.

<TypeTable
  type={{
    hostname: { type: "string", description: "Hostname or user-friendly machine name." },
    os: { type: "string", description: "Operating system reported by the runner process." },
    arch: { type: "string", description: "CPU architecture reported by the runner process." },
    runnerVersion: { type: "string", description: "Version of the runner software." },
    machineId: { type: "string", description: "Stable machine identifier, generated once per Stigmer CLI installation and persisted at ~/.stigmer/machine.json." },
  }}
/>

### RunnerPhase

RunnerPhase tracks the operational state of a runner process.

| Value | Description |
|-------|-------------|
| `RUNNER_PHASE_PENDING` | Resource created but no heartbeat received yet. |
| `RUNNER_PHASE_READY` | Runner is heartbeating and accepting new executions. |
| `RUNNER_PHASE_BUSY` | Runner is heartbeating but at maximum concurrent execution capacity. |
| `RUNNER_PHASE_STOPPED` | Runner has stopped heartbeating (timeout) or was explicitly stopped. |
| `RUNNER_PHASE_FAILED` | Unrecoverable error. |
| `RUNNER_PHASE_STARTING` | Runner process has been launched and is bootstrapping its runtime (downloading dependencies, extracting source, creating venv). |

