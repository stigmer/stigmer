---
title: API Key
---

{/* Auto-generated by stigmer-codegen. DO NOT EDIT. */}

An ApiKey provides programmatic access to the Stigmer API on behalf of an
identity account. Each key is scoped to a single user or machine account and
can optionally be configured to expire.

```yaml
apiVersion: iam.stigmer.ai/v1
kind: ApiKey
metadata:
  name: my-ci-key
spec:
  never_expires: false
  expires_at: "2026-12-31T00:00:00Z"
```

## Client Access

<SDKTabs>
<Tab value="TypeScript">
```typescript
const apiKey = await stigmer.apiKey.get("key_01j5q3k7m8r2s4tnz2hfp0q0c3");
```
</Tab>
<Tab value="Go">
```go
apiKey, err := client.ApiKey.Get(ctx, "key_01j5q3k7m8r2s4tnz2hfp0q0c3")
```
</Tab>
<Tab value="Python">
```python
api_key = client.api_keys.get("key_01j5q3k7m8r2s4tnz2hfp0q0c3")
```
</Tab>
<Tab value="Java">
```java
ApiKey apiKey = client.apiKeys().get("key_01j5q3k7m8r2s4tnz2hfp0q0c3");
```
</Tab>
</SDKTabs>

## Methods

| Method | Description |
|--------|-------------|
| [`get`](#get) | Get an API key by its unique identifier. |
| [`getByKeyHash`](#getbykeyhash) | Get an API key by its hashed key value. |
| [`findAll`](#findall) | List all API keys belonging to the authenticated user. |
| [`create`](#create) | Create an API key for the authenticated user. |
| [`update`](#update) | Update an existing API key. |
| [`delete`](#delete) | Delete an API key. |

### get

Get an API key by its unique identifier.

<SDKTabs>
<Tab value="TypeScript">
```typescript
const apiKey = await stigmer.apiKey.get(id);
```
</Tab>
<Tab value="Go">
```go
apiKey, err := client.ApiKey.Get(ctx, id)
```
</Tab>
<Tab value="Python">
```python
api_key = client.api_keys.get(id)
```
</Tab>
<Tab value="Java">
```java
ApiKey apiKey = client.apiKeys().get(id);
```
</Tab>
</SDKTabs>

**Parameters**

| Parameter | Type | Description |
|-----------|------|-------------|
| `id` | `string` | API Key identifier. |

**Returns**: [`ApiKey`](#apikey)

### getByKeyHash

Get an API key by its hashed key value.

<SDKTabs>
<Tab value="TypeScript">
```typescript
const apiKey = await stigmer.apiKey.getByKeyHash({
  value: "...",
});
```
</Tab>
<Tab value="Go">
```go
apiKey, err := client.ApiKey.GetByKeyHash(ctx, &stigmer.ApiKeyHash{
  Value: "...",
})
```
</Tab>
<Tab value="Python">
```python
api_key = client.api_keys.get_by_key_hash(ApiKeyHash(
    value="...",
))
```
</Tab>
<Tab value="Java">
```java
ApiKey apiKey = client.apiKeys().getByKeyHash(ApiKeyHash.builder()
    .value("...")
    .build());
```
</Tab>
</SDKTabs>

**Parameters**: [`ApiKeyHash`](#apikeyhash)

**Returns**: [`ApiKey`](#apikey)

### findAll

List all API keys belonging to the authenticated user.

Returns only the keys owned by the identity account in the auth header.

<SDKTabs>
<Tab value="TypeScript">
```typescript
const apiKeys = await stigmer.apiKey.findAll();
```
</Tab>
<Tab value="Go">
```go
apiKeys, err := client.ApiKey.FindAll(ctx)
```
</Tab>
<Tab value="Python">
```python
api_keys = client.api_keys.find_all()
```
</Tab>
<Tab value="Java">
```java
ApiKeys apiKeys = client.apiKeys().findAll();
```
</Tab>
</SDKTabs>

**Returns**: [`ApiKeys`](#apikeys)

### create

Create an API key for the authenticated user.

The raw key value is included in the response and is never returned again. Store it securely before discarding the response.

<SDKTabs>
<Tab value="TypeScript">
```typescript
const apiKey = await stigmer.apiKey.create({
  name: "my-api-key",
  org: "acme",
  slug: "...",
  labels: {},
  keyHash: "...",
  fingerprint: "...",
  expiresAt: "...",
  neverExpires: false,
});
```
</Tab>
<Tab value="Go">
```go
apiKey, err := client.ApiKey.Create(ctx, &stigmer.ApiKeyInput{
  Name:         "my-api-key",
  Org:          "acme",
  Slug:         "...",
  Labels:       map[string]string{},
  KeyHash:      "...",
  Fingerprint:  "...",
  ExpiresAt:    "...",
  NeverExpires: false,
})
```
</Tab>
<Tab value="Python">
```python
api_key = client.api_keys.create(ApiKeyInput(
    name="my-api-key",
    org="acme",
    slug="...",
    labels={},
    key_hash="...",
    fingerprint="...",
    expires_at="...",
    never_expires=False,
))
```
</Tab>
<Tab value="Java">
```java
ApiKey apiKey = client.apiKeys().create(ApiKeyInput.builder()
    .name("my-api-key")
    .org("acme")
    .slug("...")
    .labels(Map.of())
    .keyHash("...")
    .fingerprint("...")
    .expiresAt("...")
    .neverExpires(false)
    .build());
```
</Tab>
</SDKTabs>

**Parameters**: [`ApiKeyInput`](#apikeyinput)

**Returns**: [`ApiKey`](#apikey)

### update

Update an existing API key.

<SDKTabs>
<Tab value="TypeScript">
```typescript
const apiKey = await stigmer.apiKey.update({
  name: "my-api-key",
  org: "acme",
  slug: "...",
  labels: {},
  keyHash: "...",
  fingerprint: "...",
  expiresAt: "...",
  neverExpires: false,
});
```
</Tab>
<Tab value="Go">
```go
apiKey, err := client.ApiKey.Update(ctx, &stigmer.ApiKeyInput{
  Name:         "my-api-key",
  Org:          "acme",
  Slug:         "...",
  Labels:       map[string]string{},
  KeyHash:      "...",
  Fingerprint:  "...",
  ExpiresAt:    "...",
  NeverExpires: false,
})
```
</Tab>
<Tab value="Python">
```python
api_key = client.api_keys.update(ApiKeyInput(
    name="my-api-key",
    org="acme",
    slug="...",
    labels={},
    key_hash="...",
    fingerprint="...",
    expires_at="...",
    never_expires=False,
))
```
</Tab>
<Tab value="Java">
```java
ApiKey apiKey = client.apiKeys().update(ApiKeyInput.builder()
    .name("my-api-key")
    .org("acme")
    .slug("...")
    .labels(Map.of())
    .keyHash("...")
    .fingerprint("...")
    .expiresAt("...")
    .neverExpires(false)
    .build());
```
</Tab>
</SDKTabs>

**Parameters**: [`ApiKeyInput`](#apikeyinput)

**Returns**: [`ApiKey`](#apikey)

### delete

Delete an API key.

<SDKTabs>
<Tab value="TypeScript">
```typescript
const apiKey = await stigmer.apiKey.delete(id);
```
</Tab>
<Tab value="Go">
```go
apiKey, err := client.ApiKey.Delete(ctx, id)
```
</Tab>
<Tab value="Python">
```python
api_key = client.api_keys.delete(id)
```
</Tab>
<Tab value="Java">
```java
ApiKey apiKey = client.apiKeys().delete(id);
```
</Tab>
</SDKTabs>

**Parameters**

| Parameter | Type | Description |
|-----------|------|-------------|
| `id` | `string` | API Key identifier. |

**Returns**: [`ApiKey`](#apikey)

## Types

### ApiKeyInput

Input for creating or updating an API Key.

<TypeTable
  type={{
    name: { type: "string", description: "Resource name.", required: true },
    slug: { type: "string", description: "URL-friendly identifier." },
    org: { type: "string", description: "Organization slug.", required: true },
    labels: { type: "Record<string, string>", description: "Key-value labels." },
    keyHash: { type: "string", description: "Hash of the raw API key." },
    fingerprint: { type: "string", description: "Short fingerprint of the key for display purposes (last 6 characters)." },
    expiresAt: { type: "string", description: "Expiration time for the API key." },
    neverExpires: { type: "boolean", description: "When true, the API key never expires regardless of expires_at." },
  }}
/>

### ApiKeyHash

ApiKeyHash identifies an API key by its hashed key value.

<TypeTable
  type={{
    value: { type: "string", description: "Hash of the raw API key.", required: true },
  }}
/>

### ApiKeys

ApiKeys contains a list of API key resources.

<TypeTable
  type={{
    entries: { type: "ApiKeyInput[]", description: "API key entries.", typeDescriptionLink: "#apikeyinput" },
  }}
/>

### ApiKey

ApiKey provides programmatic access to the Stigmer API on behalf of an identity account.

<TypeTable
  type={{
    apiVersion: { type: "string", description: "API version for this resource type." },
    kind: { type: "string", description: "Resource kind identifier." },
    metadata: { type: "ApiResourceMetadata", description: "Resource metadata including id, name, org, slug, labels, visibility, and timestamps.", typeDescriptionLink: "/docs/sdk/resources/commons#apiresourcemetadata" },
    spec: { type: "ApiKeyInput", description: "Resource specification. See [`ApiKeyInput`](#apikeyinput).", typeDescriptionLink: "#apikeyinput" },
    status: { type: "ApiKeyStatus", description: "System-managed state. See [`ApiKeyStatus`](#apikeystatus).", typeDescriptionLink: "#apikeystatus" },
  }}
/>

### ApiKeyStatus

ApiKeyStatus contains system-managed state for an API key.

<TypeTable
  type={{
    audit: { type: "ApiResourceAudit", description: "Standard audit information (created_at, updated_at, created_by, etc.).", typeDescriptionLink: "/docs/sdk/resources/commons#apiresourceaudit" },
    lastUsedAt: { type: "string", description: "Timestamp of the most recent API call made with this key." },
  }}
/>

