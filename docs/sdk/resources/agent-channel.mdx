---
title: Agent Channel
---

{/* Auto-generated by stigmer-codegen. DO NOT EDIT. */}

An AgentChannel connects an agent to an external messaging platform so
people can chat with it where they already work. The spec declares which
agent serves the channel, whether serving is enabled, the provider (Slack
or WhatsApp), optional environment references that supply the agent's tool
credentials for channel conversations, and an `app_ref` to a ChannelApp
when the channel installs through your own provider app — optional for
Slack (absent means the shared Stigmer app), required for WhatsApp.
Provider identity facts and credentials are produced by the install flow
and live in status — applying a manifest never touches them, and channel
operations never modify the referenced agent.

```yaml
apiVersion: agentic.stigmer.ai/v1
kind: AgentChannel
metadata:
  name: support-agent-slack
  org: workshop
spec:
  agent_ref:
    kind: agent
    org: workshop
    slug: support-agent
  enabled: true
  slack: {}
  environment_refs:
    - kind: environment
      org: workshop
      slug: support-tools-credentials
```

A WhatsApp channel names the Business phone number it serves and always
installs through your own Meta app. A channel is reply-only until its
owner sets `proactive_messaging_enabled`, which lets the serving agent
send business-initiated messages (reminders, notifications) on the
channel through `sendMessage`:

```yaml
apiVersion: agentic.stigmer.ai/v1
kind: AgentChannel
metadata:
  name: support-agent-whatsapp
  org: workshop
spec:
  agent_ref:
    kind: agent
    org: workshop
    slug: support-agent
  enabled: true
  proactive_messaging_enabled: true
  whatsapp:
    phone_number_id: "106540352242922"
  app_ref:
    kind: channel_app
    org: workshop
    slug: acme-whatsapp
```

## Client Access

<SDKTabs>
<Tab value="TypeScript">
```typescript
const agentChannel = await stigmer.agentChannel.get("ach_01j5q3k7m8r2s4tnz2hfp0q0c3");
```
</Tab>
<Tab value="Go">
```go
agentChannel, err := client.AgentChannel.Get(ctx, "ach_01j5q3k7m8r2s4tnz2hfp0q0c3")
```
</Tab>
<Tab value="Python">
```python
agent_channel = client.agent_channels.get("ach_01j5q3k7m8r2s4tnz2hfp0q0c3")
```
</Tab>
<Tab value="Java">
```java
AgentChannel agentChannel = client.agentChannels().get("ach_01j5q3k7m8r2s4tnz2hfp0q0c3");
```
</Tab>
</SDKTabs>

## Methods

| Method | Description |
|--------|-------------|
| [`get`](#get) | Get a single agent channel by ID. |
| [`getByAgent`](#getbyagent) | Get all channels of a specific agent. |
| [`getByReference`](#getbyreference) | Get an agent channel by its organization-scoped reference (org/slug). |
| [`getConversation`](#getconversation) | Get one conversation's identity and participation state. |
| [`getTimeline`](#gettimeline) | Get one conversation's timeline, newest first, cursor-paged. |
| [`list`](#list) | List agent channels with optional label filtering. |
| [`listConversations`](#listconversations) | List an organization's channel conversations, newest activity first. |
| [`listMessagingChannels`](#listmessagingchannels) | List the agent channels the caller can send business-initiated messages on. |
| [`listTemplates`](#listtemplates) | List the message templates available on an agent channel. |
| [`apply`](#apply) | Create or update an agent channel. |
| [`create`](#create) | Create an agent channel. |
| [`update`](#update) | Update an existing agent channel. |
| [`delete`](#delete) | Delete an agent channel. |
| [`clearAttention`](#clearattention) | Clear a conversation's needs-attention flag without taking it over. |
| [`completeInstall`](#completeinstall) | Complete the provider install flow for an agent channel. |
| [`escalate`](#escalate) | Flag this conversation for human attention. |
| [`handBack`](#handback) | Hand a conversation back to the agent, restoring automatic replies. |
| [`initiateInstall`](#initiateinstall) | Start the provider install flow for an agent channel. |
| [`reply`](#reply) | Reply to a conversation as the business, through the same number the agent serves. |
| [`sendMessage`](#sendmessage) | Send a business-initiated message to a recipient on an agent channel. |
| [`takeOver`](#takeover) | Take over a conversation: the agent goes quiet until handBack. |

### get

Get a single agent channel by ID.

<SDKTabs>
<Tab value="TypeScript">
```typescript
const agentChannel = await stigmer.agentChannel.get(id);
```
</Tab>
<Tab value="Go">
```go
agentChannel, err := client.AgentChannel.Get(ctx, id)
```
</Tab>
<Tab value="Python">
```python
agent_channel = client.agent_channels.get(id)
```
</Tab>
<Tab value="Java">
```java
AgentChannel agentChannel = client.agentChannels().get(id);
```
</Tab>
</SDKTabs>

**Parameters**

| Parameter | Type | Description |
|-----------|------|-------------|
| `id` | `string` | Agent Channel identifier. |

**Returns**: [`AgentChannel`](#agentchannel)

### getByAgent

Get all channels of a specific agent. Returns only channels the caller has access to.

This is how the agent's integrations surface and CLI resolve an agent's existing channels regardless of slug.

<SDKTabs>
<Tab value="TypeScript">
```typescript
const result = await stigmer.agentChannel.getByAgent({
  agentId: "...",
  pageInfo: {},
  org: "...",
});
```
</Tab>
<Tab value="Go">
```go
result, err := client.AgentChannel.GetByAgent(ctx, &stigmer.GetAgentChannelsByAgentRequest{
  AgentId:  "...",
  PageInfo: &stigmer.PageInfoInput{},
  Org:      "...",
})
```
</Tab>
<Tab value="Python">
```python
result = client.agent_channels.get_by_agent(GetAgentChannelsByAgentRequest(
    agent_id="...",
    page_info=None,
    org="...",
))
```
</Tab>
<Tab value="Java">
```java
AgentChannelList result = client.agentChannels().getByAgent(GetAgentChannelsByAgentRequest.builder()
    .agentId("...")
    .pageInfo(null)
    .org("...")
    .build());
```
</Tab>
</SDKTabs>

**Parameters**: [`GetAgentChannelsByAgentRequest`](#getagentchannelsbyagentrequest)

**Returns**: [`AgentChannelList`](#agentchannellist)

### getByReference

Get an agent channel by its organization-scoped reference (org/slug).

<SDKTabs>
<Tab value="TypeScript">
```typescript
const agentChannel = await stigmer.agentChannel.getByReference({
  org: "...",
  kind: "...",
  slug: "...",
  version: "...",
});
```
</Tab>
<Tab value="Go">
```go
agentChannel, err := client.AgentChannel.GetByReference(ctx, &stigmer.ApiResourceReference{
  Org:     "...",
  Kind:    "...",
  Slug:    "...",
  Version: "...",
})
```
</Tab>
<Tab value="Python">
```python
agent_channel = client.agent_channels.get_by_reference(ApiResourceReference(
    org="...",
    kind="...",
    slug="...",
    version="...",
))
```
</Tab>
<Tab value="Java">
```java
AgentChannel agentChannel = client.agentChannels().getByReference(ApiResourceReference.builder()
    .org("...")
    .kind("...")
    .slug("...")
    .version("...")
    .build());
```
</Tab>
</SDKTabs>

**Parameters**: [`ResourceRef`](#resourceref)

**Returns**: [`AgentChannel`](#agentchannel)

### getConversation

Get one conversation's identity and participation state.

The single-row read behind a conversation detail view: who holds control, whether the conversation needs attention and why, the customer's display name, and the activity clocks. Answers NOT_FOUND until the customer's first message creates the conversation.

<SDKTabs>
<Tab value="TypeScript">
```typescript
const channelConversation = await stigmer.agentChannel.getConversation({
  agentChannelId: "...",
  conversationKey: "...",
});
```
</Tab>
<Tab value="Go">
```go
channelConversation, err := client.AgentChannel.GetConversation(ctx, &stigmer.GetChannelConversationInput{
  AgentChannelId:  "...",
  ConversationKey: "...",
})
```
</Tab>
<Tab value="Python">
```python
channel_conversation = client.agent_channels.get_conversation(GetChannelConversationInput(
    agent_channel_id="...",
    conversation_key="...",
))
```
</Tab>
<Tab value="Java">
```java
ChannelConversation channelConversation = client.agentChannels().getConversation(GetChannelConversationInput.builder()
    .agentChannelId("...")
    .conversationKey("...")
    .build());
```
</Tab>
</SDKTabs>

**Parameters**: [`GetChannelConversationInput`](#getchannelconversationinput)

**Returns**: [`ChannelConversation`](#channelconversation)

### getTimeline

Get one conversation's timeline, newest first, cursor-paged.

The timeline contains customer-visible items only: inbound customer messages (including non-text kinds the platform cannot render), delivered agent replies, and operator or platform sends. Execution internals never appear.

<SDKTabs>
<Tab value="TypeScript">
```typescript
const conversationTimeline = await stigmer.agentChannel.getTimeline({
  agentChannelId: "...",
  conversationKey: "...",
  pageSize: 0,
  pageToken: "...",
});
```
</Tab>
<Tab value="Go">
```go
conversationTimeline, err := client.AgentChannel.GetTimeline(ctx, &stigmer.GetConversationTimelineInput{
  AgentChannelId:  "...",
  ConversationKey: "...",
  PageSize:        0,
  PageToken:       "...",
})
```
</Tab>
<Tab value="Python">
```python
conversation_timeline = client.agent_channels.get_timeline(GetConversationTimelineInput(
    agent_channel_id="...",
    conversation_key="...",
    page_size=0,
    page_token="...",
))
```
</Tab>
<Tab value="Java">
```java
ConversationTimeline conversationTimeline = client.agentChannels().getTimeline(GetConversationTimelineInput.builder()
    .agentChannelId("...")
    .conversationKey("...")
    .pageSize(0)
    .pageToken("...")
    .build());
```
</Tab>
</SDKTabs>

**Parameters**: [`GetConversationTimelineInput`](#getconversationtimelineinput)

**Returns**: [`ConversationTimeline`](#conversationtimeline)

### list

List agent channels with optional label filtering.

<SDKTabs>
<Tab value="TypeScript">
```typescript
const result = await stigmer.agentChannel.list({
  org: "...",
  labels: {},
  pageInfo: {},
});
```
</Tab>
<Tab value="Go">
```go
result, err := client.AgentChannel.List(ctx, &stigmer.ListAgentChannelsRequest{
  Org:      "...",
  Labels:   map[string]string{},
  PageInfo: &stigmer.PageInfoInput{},
})
```
</Tab>
<Tab value="Python">
```python
result = client.agent_channels.list(ListAgentChannelsRequest(
    org="...",
    labels={},
    page_info=None,
))
```
</Tab>
<Tab value="Java">
```java
AgentChannelList result = client.agentChannels().list(ListAgentChannelsRequest.builder()
    .org("...")
    .labels(Map.of())
    .pageInfo(null)
    .build());
```
</Tab>
</SDKTabs>

**Parameters**: [`ListAgentChannelsRequest`](#listagentchannelsrequest)

**Returns**: [`AgentChannelList`](#agentchannellist)

### listConversations

List an organization's channel conversations, newest activity first.

Returns conversations across all of the org's channels the caller can view, optionally filtered to one channel. Entries carry participation state and the customer's display name.

<SDKTabs>
<Tab value="TypeScript">
```typescript
const result = await stigmer.agentChannel.listConversations({
  org: "...",
  agentChannelId: "...",
  pageInfo: {},
  filter: "...",
});
```
</Tab>
<Tab value="Go">
```go
result, err := client.AgentChannel.ListConversations(ctx, &stigmer.ListChannelConversationsInput{
  Org:            "...",
  AgentChannelId: "...",
  PageInfo:       &stigmer.PageInfoInput{},
  Filter:         "...",
})
```
</Tab>
<Tab value="Python">
```python
result = client.agent_channels.list_conversations(ListChannelConversationsInput(
    org="...",
    agent_channel_id="...",
    page_info=None,
    filter="...",
))
```
</Tab>
<Tab value="Java">
```java
ChannelConversationList result = client.agentChannels().listConversations(ListChannelConversationsInput.builder()
    .org("...")
    .agentChannelId("...")
    .pageInfo(null)
    .filter("...")
    .build());
```
</Tab>
</SDKTabs>

**Parameters**: [`ListChannelConversationsInput`](#listchannelconversationsinput)

**Returns**: [`ChannelConversationList`](#channelconversationlist)

### listMessagingChannels

List the agent channels the caller can send business-initiated messages on.

Everything the read needs derives from the caller's credential; an agent with no proactive-messaging channel receives an empty list.

<SDKTabs>
<Tab value="TypeScript">
```typescript
const messagingChannels = await stigmer.agentChannel.listMessagingChannels(input);
```
</Tab>
<Tab value="Go">
```go
messagingChannels, err := client.AgentChannel.ListMessagingChannels(ctx, input)
```
</Tab>
<Tab value="Python">
```python
messaging_channels = client.agent_channels.list_messaging_channels(input)
```
</Tab>
<Tab value="Java">
```java
MessagingChannels messagingChannels = client.agentChannels().listMessagingChannels(input);
```
</Tab>
</SDKTabs>

**Parameters**: [`ListMessagingChannelsInput`](#listmessagingchannelsinput)

**Returns**: [`MessagingChannels`](#messagingchannels)

### listTemplates

List the message templates available on an agent channel.

Read live from the channel provider's registry (WhatsApp: the WABA's templates), lightly cached. The provider is the source of truth; entries carry its vocabulary verbatim.

<SDKTabs>
<Tab value="TypeScript">
```typescript
const channelTemplates = await stigmer.agentChannel.listTemplates({
  channel: "...",
  org: "...",
  approvedOnly: false,
});
```
</Tab>
<Tab value="Go">
```go
channelTemplates, err := client.AgentChannel.ListTemplates(ctx, &stigmer.ListChannelTemplatesInput{
  Channel:      "...",
  Org:          "...",
  ApprovedOnly: false,
})
```
</Tab>
<Tab value="Python">
```python
channel_templates = client.agent_channels.list_templates(ListChannelTemplatesInput(
    channel="...",
    org="...",
    approved_only=False,
))
```
</Tab>
<Tab value="Java">
```java
ChannelTemplates channelTemplates = client.agentChannels().listTemplates(ListChannelTemplatesInput.builder()
    .channel("...")
    .org("...")
    .approvedOnly(false)
    .build());
```
</Tab>
</SDKTabs>

**Parameters**: [`ListChannelTemplatesInput`](#listchanneltemplatesinput)

**Returns**: [`ChannelTemplates`](#channeltemplates)

### apply

Create or update an agent channel.

<SDKTabs>
<Tab value="TypeScript">
```typescript
const agentChannel = await stigmer.agentChannel.apply({
  name: "my-agent-channel",
  org: "acme",
  slug: "...",
  labels: {},
  agentRef: {},
  enabled: false,
  slack: {},
  whatsapp: {},
  environmentRefs: [],
  appRef: {},
  proactiveMessagingEnabled: false,
  runConfig: {},
});
```
</Tab>
<Tab value="Go">
```go
agentChannel, err := client.AgentChannel.Apply(ctx, &stigmer.AgentChannelInput{
  Name:                      "my-agent-channel",
  Org:                       "acme",
  Slug:                      "...",
  Labels:                    map[string]string{},
  AgentRef:                  &stigmer.ResourceRef{},
  Enabled:                   false,
  Slack:                     &stigmer.SlackChannelConfigInput{},
  Whatsapp:                  &stigmer.WhatsAppChannelConfigInput{},
  EnvironmentRefs:           []stigmer.ResourceRef{},
  AppRef:                    &stigmer.ResourceRef{},
  ProactiveMessagingEnabled: false,
  RunConfig:                 &stigmer.RunConfigInput{},
})
```
</Tab>
<Tab value="Python">
```python
agent_channel = client.agent_channels.apply(AgentChannelInput(
    name="my-agent-channel",
    org="acme",
    slug="...",
    labels={},
    agent_ref=None,
    enabled=False,
    slack=None,
    whatsapp=None,
    environment_refs=[],
    app_ref=None,
    proactive_messaging_enabled=False,
    run_config=None,
))
```
</Tab>
<Tab value="Java">
```java
AgentChannel agentChannel = client.agentChannels().apply(AgentChannelInput.builder()
    .name("my-agent-channel")
    .org("acme")
    .slug("...")
    .labels(Map.of())
    .agentRef(null)
    .enabled(false)
    .slack(null)
    .whatsapp(null)
    .environmentRefs(List.of())
    .appRef(null)
    .proactiveMessagingEnabled(false)
    .runConfig(null)
    .build());
```
</Tab>
</SDKTabs>

**Parameters**: [`AgentChannelInput`](#agentchannelinput)

**Returns**: [`AgentChannel`](#agentchannel)

### create

Create an agent channel.

Connecting an agent to a channel is a billing-affecting decision: conversations arriving over the channel consume the connection-owning organization's credits.

<SDKTabs>
<Tab value="TypeScript">
```typescript
const agentChannel = await stigmer.agentChannel.create({
  name: "my-agent-channel",
  org: "acme",
  slug: "...",
  labels: {},
  agentRef: {},
  enabled: false,
  slack: {},
  whatsapp: {},
  environmentRefs: [],
  appRef: {},
  proactiveMessagingEnabled: false,
  runConfig: {},
});
```
</Tab>
<Tab value="Go">
```go
agentChannel, err := client.AgentChannel.Create(ctx, &stigmer.AgentChannelInput{
  Name:                      "my-agent-channel",
  Org:                       "acme",
  Slug:                      "...",
  Labels:                    map[string]string{},
  AgentRef:                  &stigmer.ResourceRef{},
  Enabled:                   false,
  Slack:                     &stigmer.SlackChannelConfigInput{},
  Whatsapp:                  &stigmer.WhatsAppChannelConfigInput{},
  EnvironmentRefs:           []stigmer.ResourceRef{},
  AppRef:                    &stigmer.ResourceRef{},
  ProactiveMessagingEnabled: false,
  RunConfig:                 &stigmer.RunConfigInput{},
})
```
</Tab>
<Tab value="Python">
```python
agent_channel = client.agent_channels.create(AgentChannelInput(
    name="my-agent-channel",
    org="acme",
    slug="...",
    labels={},
    agent_ref=None,
    enabled=False,
    slack=None,
    whatsapp=None,
    environment_refs=[],
    app_ref=None,
    proactive_messaging_enabled=False,
    run_config=None,
))
```
</Tab>
<Tab value="Java">
```java
AgentChannel agentChannel = client.agentChannels().create(AgentChannelInput.builder()
    .name("my-agent-channel")
    .org("acme")
    .slug("...")
    .labels(Map.of())
    .agentRef(null)
    .enabled(false)
    .slack(null)
    .whatsapp(null)
    .environmentRefs(List.of())
    .appRef(null)
    .proactiveMessagingEnabled(false)
    .runConfig(null)
    .build());
```
</Tab>
</SDKTabs>

**Parameters**: [`AgentChannelInput`](#agentchannelinput)

**Returns**: [`AgentChannel`](#agentchannel)

### update

Update an existing agent channel.

Replaces the spec wholesale. The slug, referenced agent, and provider arm are immutable; status (install facts, credential reference) is never touched by updates.

<SDKTabs>
<Tab value="TypeScript">
```typescript
const agentChannel = await stigmer.agentChannel.update({
  name: "my-agent-channel",
  org: "acme",
  slug: "...",
  labels: {},
  agentRef: {},
  enabled: false,
  slack: {},
  whatsapp: {},
  environmentRefs: [],
  appRef: {},
  proactiveMessagingEnabled: false,
  runConfig: {},
});
```
</Tab>
<Tab value="Go">
```go
agentChannel, err := client.AgentChannel.Update(ctx, &stigmer.AgentChannelInput{
  Name:                      "my-agent-channel",
  Org:                       "acme",
  Slug:                      "...",
  Labels:                    map[string]string{},
  AgentRef:                  &stigmer.ResourceRef{},
  Enabled:                   false,
  Slack:                     &stigmer.SlackChannelConfigInput{},
  Whatsapp:                  &stigmer.WhatsAppChannelConfigInput{},
  EnvironmentRefs:           []stigmer.ResourceRef{},
  AppRef:                    &stigmer.ResourceRef{},
  ProactiveMessagingEnabled: false,
  RunConfig:                 &stigmer.RunConfigInput{},
})
```
</Tab>
<Tab value="Python">
```python
agent_channel = client.agent_channels.update(AgentChannelInput(
    name="my-agent-channel",
    org="acme",
    slug="...",
    labels={},
    agent_ref=None,
    enabled=False,
    slack=None,
    whatsapp=None,
    environment_refs=[],
    app_ref=None,
    proactive_messaging_enabled=False,
    run_config=None,
))
```
</Tab>
<Tab value="Java">
```java
AgentChannel agentChannel = client.agentChannels().update(AgentChannelInput.builder()
    .name("my-agent-channel")
    .org("acme")
    .slug("...")
    .labels(Map.of())
    .agentRef(null)
    .enabled(false)
    .slack(null)
    .whatsapp(null)
    .environmentRefs(List.of())
    .appRef(null)
    .proactiveMessagingEnabled(false)
    .runConfig(null)
    .build());
```
</Tab>
</SDKTabs>

**Parameters**: [`AgentChannelInput`](#agentchannelinput)

**Returns**: [`AgentChannel`](#agentchannel)

### delete

Delete an agent channel.

Full teardown of the connection: inbound events for the workspace stop resolving, pending deliveries are abandoned, and the credentials environment is deleted with the grant. To pause serving while keeping the install, update the channel with enabled=false instead.

<SDKTabs>
<Tab value="TypeScript">
```typescript
const agentChannel = await stigmer.agentChannel.delete(id);
```
</Tab>
<Tab value="Go">
```go
agentChannel, err := client.AgentChannel.Delete(ctx, id)
```
</Tab>
<Tab value="Python">
```python
agent_channel = client.agent_channels.delete(id)
```
</Tab>
<Tab value="Java">
```java
AgentChannel agentChannel = client.agentChannels().delete(id);
```
</Tab>
</SDKTabs>

**Parameters**

| Parameter | Type | Description |
|-----------|------|-------------|
| `id` | `string` | Agent Channel identifier. |

**Returns**: [`AgentChannel`](#agentchannel)

### clearAttention

Clear a conversation's needs-attention flag without taking it over.

The false-alarm dismissal: an escalation that needs no reply is cleared in place, never via a take-over-and-hand-back dance.

<SDKTabs>
<Tab value="TypeScript">
```typescript
const channelConversation = await stigmer.agentChannel.clearAttention({
  agentChannelId: "...",
  conversationKey: "...",
});
```
</Tab>
<Tab value="Go">
```go
channelConversation, err := client.AgentChannel.ClearAttention(ctx, &stigmer.ConversationControlInput{
  AgentChannelId:  "...",
  ConversationKey: "...",
})
```
</Tab>
<Tab value="Python">
```python
channel_conversation = client.agent_channels.clear_attention(ConversationControlInput(
    agent_channel_id="...",
    conversation_key="...",
))
```
</Tab>
<Tab value="Java">
```java
ChannelConversation channelConversation = client.agentChannels().clearAttention(ConversationControlInput.builder()
    .agentChannelId("...")
    .conversationKey("...")
    .build());
```
</Tab>
</SDKTabs>

**Parameters**: [`ConversationControlInput`](#conversationcontrolinput)

**Returns**: [`ChannelConversation`](#channelconversation)

### completeInstall

Complete the provider install flow for an agent channel.

Called by the console after the provider's consent redirect, with the state from initiateInstall and the provider's authorization code. On success the channel's status carries the install facts and the channel begins serving (subject to spec.enabled).

Refusals a client should branch on carry a google.rpc.ErrorInfo detail (domain "stigmer.ai") on the standard grpc-status-details-bin trailer, alongside the human-readable FAILED_PRECONDITION message:

- SLACK_WORKSPACE_ALREADY_CONNECTED — the workspace already hosts an agent through this serving app (one agent per workspace per app). Metadata: team_name (the occupied workspace), channel_app_id (the serving app; empty = platform app). - CHANNEL_INSTALL_STATE_INVALID — the state parameter is missing, expired, or replayed; restart the install. - SLACK_ENTERPRISE_INSTALL_UNSUPPORTED — an Enterprise Grid org-wide install was attempted; install into a single workspace.

Other refusals (unconfigured deployment, provider-refused code exchange) carry no reason — their message is the interface.

<SDKTabs>
<Tab value="TypeScript">
```typescript
const agentChannel = await stigmer.agentChannel.completeInstall({
  resourceId: "...",
  state: "...",
  code: "...",
});
```
</Tab>
<Tab value="Go">
```go
agentChannel, err := client.AgentChannel.CompleteInstall(ctx, &stigmer.CompleteChannelInstallInput{
  ResourceId: "...",
  State:      "...",
  Code:       "...",
})
```
</Tab>
<Tab value="Python">
```python
agent_channel = client.agent_channels.complete_install(CompleteChannelInstallInput(
    resource_id="...",
    state="...",
    code="...",
))
```
</Tab>
<Tab value="Java">
```java
AgentChannel agentChannel = client.agentChannels().completeInstall(CompleteChannelInstallInput.builder()
    .resourceId("...")
    .state("...")
    .code("...")
    .build());
```
</Tab>
</SDKTabs>

**Parameters**: [`CompleteChannelInstallInput`](#completechannelinstallinput)

**Returns**: [`AgentChannel`](#agentchannel)

### escalate

Flag this conversation for human attention.

Non-blocking: the agent keeps serving while the flag summons humans. Repeated escalation is harmless; the latest reason wins.

<SDKTabs>
<Tab value="TypeScript">
```typescript
const channelConversation = await stigmer.agentChannel.escalate({
  reason: "...",
});
```
</Tab>
<Tab value="Go">
```go
channelConversation, err := client.AgentChannel.Escalate(ctx, &stigmer.EscalateConversationInput{
  Reason: "...",
})
```
</Tab>
<Tab value="Python">
```python
channel_conversation = client.agent_channels.escalate(EscalateConversationInput(
    reason="...",
))
```
</Tab>
<Tab value="Java">
```java
ChannelConversation channelConversation = client.agentChannels().escalate(EscalateConversationInput.builder()
    .reason("...")
    .build());
```
</Tab>
</SDKTabs>

**Parameters**: [`EscalateConversationInput`](#escalateconversationinput)

**Returns**: [`ChannelConversation`](#channelconversation)

### handBack

Hand a conversation back to the agent, restoring automatic replies.

The agent re-enters with the conversation context it missed while the human held control.

<SDKTabs>
<Tab value="TypeScript">
```typescript
const channelConversation = await stigmer.agentChannel.handBack({
  agentChannelId: "...",
  conversationKey: "...",
});
```
</Tab>
<Tab value="Go">
```go
channelConversation, err := client.AgentChannel.HandBack(ctx, &stigmer.ConversationControlInput{
  AgentChannelId:  "...",
  ConversationKey: "...",
})
```
</Tab>
<Tab value="Python">
```python
channel_conversation = client.agent_channels.hand_back(ConversationControlInput(
    agent_channel_id="...",
    conversation_key="...",
))
```
</Tab>
<Tab value="Java">
```java
ChannelConversation channelConversation = client.agentChannels().handBack(ConversationControlInput.builder()
    .agentChannelId("...")
    .conversationKey("...")
    .build());
```
</Tab>
</SDKTabs>

**Parameters**: [`ConversationControlInput`](#conversationcontrolinput)

**Returns**: [`ChannelConversation`](#channelconversation)

### initiateInstall

Start the provider install flow for an agent channel.

For redirect-installed providers (Slack) the output carries the provider authorization URL to redirect the installing user to, plus the single-use state that completeInstall verifies. For direct-installed providers (WhatsApp) the install runs to completion inside this RPC and the output carries completed=true instead.

Refusals a client should branch on carry a google.rpc.ErrorInfo detail (domain "stigmer.ai") on the standard grpc-status-details-bin trailer, alongside the human-readable FAILED_PRECONDITION message:

- WHATSAPP_NUMBER_ALREADY_CONNECTED — the WhatsApp Business number already serves an agent through this channel app (one agent per number per app). Metadata: display_phone_number (the occupied number), channel_app_id (the serving app).

<SDKTabs>
<Tab value="TypeScript">
```typescript
const initiateChannelInstallOutput = await stigmer.agentChannel.initiateInstall({
  resourceId: "...",
});
```
</Tab>
<Tab value="Go">
```go
initiateChannelInstallOutput, err := client.AgentChannel.InitiateInstall(ctx, &stigmer.InitiateChannelInstallInput{
  ResourceId: "...",
})
```
</Tab>
<Tab value="Python">
```python
initiate_channel_install_output = client.agent_channels.initiate_install(InitiateChannelInstallInput(
    resource_id="...",
))
```
</Tab>
<Tab value="Java">
```java
InitiateChannelInstallOutput initiateChannelInstallOutput = client.agentChannels().initiateInstall(InitiateChannelInstallInput.builder()
    .resourceId("...")
    .build());
```
</Tab>
</SDKTabs>

**Parameters**: [`InitiateChannelInstallInput`](#initiatechannelinstallinput)

**Returns**: [`InitiateChannelInstallOutput`](#initiatechannelinstalloutput)

### reply

Reply to a conversation as the business, through the same number the agent serves.

The message is durably recorded and attempted inline; the outcome reports the truth of the attempt. A staff reply on an agent-held conversation is an implicit takeover: the agent goes quiet until handBack. Replying requires an existing conversation — the customer must have written first; an unknown conversation answers NOT_FOUND.

<SDKTabs>
<Tab value="TypeScript">
```typescript
const sendChannelMessageOutput = await stigmer.agentChannel.reply({
  agentChannelId: "...",
  conversationKey: "...",
  payload: {},
});
```
</Tab>
<Tab value="Go">
```go
sendChannelMessageOutput, err := client.AgentChannel.Reply(ctx, &stigmer.ReplyToConversationInput{
  AgentChannelId:  "...",
  ConversationKey: "...",
  Payload:         &stigmer.ChannelOutboundPayloadInput{},
})
```
</Tab>
<Tab value="Python">
```python
send_channel_message_output = client.agent_channels.reply(ReplyToConversationInput(
    agent_channel_id="...",
    conversation_key="...",
    payload=None,
))
```
</Tab>
<Tab value="Java">
```java
SendChannelMessageOutput sendChannelMessageOutput = client.agentChannels().reply(ReplyToConversationInput.builder()
    .agentChannelId("...")
    .conversationKey("...")
    .payload(null)
    .build());
```
</Tab>
</SDKTabs>

**Parameters**: [`ReplyToConversationInput`](#replytoconversationinput)

**Returns**: [`SendChannelMessageOutput`](#sendchannelmessageoutput)

### sendMessage

Send a business-initiated message to a recipient on an agent channel.

The message is durably recorded and attempted once inline; transient failures are retried in the background. The outcome reports the truth of the inline attempt.

<SDKTabs>
<Tab value="TypeScript">
```typescript
const sendChannelMessageOutput = await stigmer.agentChannel.sendMessage({
  channel: "...",
  org: "...",
  recipient: "...",
  payload: {},
});
```
</Tab>
<Tab value="Go">
```go
sendChannelMessageOutput, err := client.AgentChannel.SendMessage(ctx, &stigmer.SendChannelMessageInput{
  Channel:   "...",
  Org:       "...",
  Recipient: "...",
  Payload:   &stigmer.ChannelOutboundPayloadInput{},
})
```
</Tab>
<Tab value="Python">
```python
send_channel_message_output = client.agent_channels.send_message(SendChannelMessageInput(
    channel="...",
    org="...",
    recipient="...",
    payload=None,
))
```
</Tab>
<Tab value="Java">
```java
SendChannelMessageOutput sendChannelMessageOutput = client.agentChannels().sendMessage(SendChannelMessageInput.builder()
    .channel("...")
    .org("...")
    .recipient("...")
    .payload(null)
    .build());
```
</Tab>
</SDKTabs>

**Parameters**: [`SendChannelMessageInput`](#sendchannelmessageinput)

**Returns**: [`SendChannelMessageOutput`](#sendchannelmessageoutput)

### takeOver

Take over a conversation: the agent goes quiet until handBack.

Taking over also clears the needs-attention flag — the human arriving is the answer to an escalation.

<SDKTabs>
<Tab value="TypeScript">
```typescript
const channelConversation = await stigmer.agentChannel.takeOver({
  agentChannelId: "...",
  conversationKey: "...",
});
```
</Tab>
<Tab value="Go">
```go
channelConversation, err := client.AgentChannel.TakeOver(ctx, &stigmer.ConversationControlInput{
  AgentChannelId:  "...",
  ConversationKey: "...",
})
```
</Tab>
<Tab value="Python">
```python
channel_conversation = client.agent_channels.take_over(ConversationControlInput(
    agent_channel_id="...",
    conversation_key="...",
))
```
</Tab>
<Tab value="Java">
```java
ChannelConversation channelConversation = client.agentChannels().takeOver(ConversationControlInput.builder()
    .agentChannelId("...")
    .conversationKey("...")
    .build());
```
</Tab>
</SDKTabs>

**Parameters**: [`ConversationControlInput`](#conversationcontrolinput)

**Returns**: [`ChannelConversation`](#channelconversation)

## Types

### AgentChannelInput

Input for creating or updating an Agent Channel.

<TypeTable
  type={{
    name: { type: "string", description: "Resource name.", required: true },
    slug: { type: "string", description: "URL-friendly identifier." },
    org: { type: "string", description: "Organization slug.", required: true },
    labels: { type: "Record<string, string>", description: "Key-value labels." },
    agentRef: { type: "ResourceRef", description: "Reference to the agent this channel serves.", typeDescriptionLink: "#resourceref", required: true },
    enabled: { type: "boolean", description: "Whether serving traffic on this channel is enabled." },
    slack: { type: "SlackChannelConfigInput", description: "Slack workspace connection (multi-tenant Stigmer Slack app).", typeDescriptionLink: "#slackchannelconfiginput" },
    whatsapp: { type: "WhatsAppChannelConfigInput", description: "WhatsApp Business number connection (Meta Cloud API).", typeDescriptionLink: "#whatsappchannelconfiginput" },
    environmentRefs: { type: "ResourceRef[]", description: "References to Environment resources whose values are provided to conversations on this channel.", typeDescriptionLink: "#resourceref" },
    appRef: { type: "ResourceRef", description: "Reference to the ChannelApp this channel installs through.", typeDescriptionLink: "#resourceref" },
    proactiveMessagingEnabled: { type: "boolean", description: "Whether the serving agent may send business-initiated (proactive) messages on this channel." },
    runConfig: { type: "RunConfigInput", description: "Per-turn model choice and run bounds for conversations on this channel, overriding the platform's channel execution profile.", typeDescriptionLink: "#runconfiginput" },
  }}
/>

### ResourceRef

Generic reference to any API resource by org and slug.

<TypeTable
  type={{
    org: { type: "string", description: "Organization that owns the referenced resource." },
    kind: { type: "ApiResourceKind", description: "Kind of the referenced resource (e.g., SKILL, AGENT, MCP_SERVER).", typeDescriptionLink: "/docs/sdk/resources/commons#apiresourcekind" },
    slug: { type: "string", description: "Resource slug (user-friendly identifier, unique within org).", required: true },
    version: { type: "string", description: "Version of the resource (optional, only applicable to versioned resources like Skills)." },
  }}
/>

### SlackChannelConfigInput

SlackChannelConfig selects Slack as the channel provider.

### WhatsAppChannelConfigInput

WhatsAppChannelConfig selects WhatsApp as the channel provider and names the WhatsApp Business phone number the channel serves.

<TypeTable
  type={{
    phoneNumberId: { type: "string", description: "Phone number ID of the WhatsApp Business number this channel serves, from Meta's WhatsApp Manager (API Setup), e.g. \"106540352242922\"." },
  }}
/>

### RunConfigInput

RunConfig is the owner-settable model choice and run bounds.

<TypeTable
  type={{
    modelName: { type: "string", description: "The model each run uses." },
    maxCostUsd: { type: "number", description: "Maximum estimated cost in USD per run." },
    maxToolRounds: { type: "number", description: "Maximum model-to-tools reasoning cycles per run." },
    serviceTier: { type: "ServiceTier", description: "Service tier for each run's model calls: standard (the default) or fast, where fast bills at the model's fast-tier rates and requires a model that offers one.", typeDescriptionLink: "#servicetier" },
  }}
/>

### InitiateChannelInstallInput

Input for starting a provider install flow.

<TypeTable
  type={{
    resourceId: { type: "string", description: "ID of the agent channel to install.", required: true },
  }}
/>

### InitiateChannelInstallOutput

Output of the initiate-install RPC.

<TypeTable
  type={{
    authorizationUrl: { type: "string", description: "Provider authorization URL to redirect the installing user to (for Slack: the \"Add to Slack\" consent screen)." },
    state: { type: "string", description: "Single-use opaque state parameter bound to this install attempt." },
    completed: { type: "boolean", description: "True when the install completed synchronously inside this RPC (direct-installed providers)." },
  }}
/>

### CompleteChannelInstallInput

Input for completing a provider install flow.

<TypeTable
  type={{
    resourceId: { type: "string", description: "ID of the agent channel being installed.", required: true },
    state: { type: "string", description: "Single-use state parameter issued by initiateInstall.", required: true },
    code: { type: "string", description: "Authorization code returned by the provider's consent screen.", required: true },
  }}
/>

### ReplyToConversationInput

Input for replying to a conversation as the business.

<TypeTable
  type={{
    agentChannelId: { type: "string", description: "AgentChannel the conversation belongs to." },
    conversationKey: { type: "string", description: "Conversation key within the channel (WhatsApp: the customer's wa_id)." },
    payload: { type: "ChannelOutboundPayloadInput", description: "Typed message content (the outbound payload vocabulary, shared with sendMessage — one payload contract, no twin).", required: true },
  }}
/>

### SendChannelMessageOutput

Output of the send-message RPC: the truthful outcome of the inline delivery attempt.

<TypeTable
  type={{
    outcome: { type: "ChannelSendOutcome", description: "Outcome of the inline attempt.", typeDescriptionLink: "#channelsendoutcome" },
    outboundMessageId: { type: "string", description: "Platform id of the durable outbound record (audit handle)." },
    providerMessageId: { type: "string", description: "Provider message id when accepted (WhatsApp: wamid)." },
    detail: { type: "string", description: "Honest, agent-readable detail for queued/refused outcomes." },
  }}
/>

### ConversationControlInput

Input for the conversation control commands (takeOver, handBack, clearAttention).

<TypeTable
  type={{
    agentChannelId: { type: "string", description: "AgentChannel the conversation belongs to." },
    conversationKey: { type: "string", description: "Conversation key within the channel (WhatsApp: the customer's wa_id)." },
  }}
/>

### ChannelConversation

ChannelConversation is the durable identity and participation state of one channel conversation: one external customer on one agent channel, spanning sessions.

<TypeTable
  type={{
    agentChannelId: { type: "string", description: "AgentChannel connection this conversation belongs to." },
    conversationKey: { type: "string", description: "Provider-neutral conversation key (WhatsApp: the customer's wa_id)." },
    org: { type: "string", description: "Organization that owns the channel." },
    control: { type: "ConversationControl", description: "Who may speak to the customer right now.", typeDescriptionLink: "#conversationcontrol" },
    controlledBy: { type: "string", description: "Identity account holding control when control is control_human." },
    controlChangedAt: { type: "string", description: "When control last changed hands." },
    needsAttention: { type: "boolean", description: "True when the conversation is flagged for human attention." },
    attentionReason: { type: "string", description: "Why the conversation needs attention, in the escalating agent's words." },
    attentionChangedAt: { type: "string", description: "When the attention flag last changed." },
    displayName: { type: "string", description: "Customer display name as the provider reports it (WhatsApp: contacts[].profile.name)." },
    lastCustomerMessageAt: { type: "string", description: "When the customer last sent a message." },
    lastActivityAt: { type: "string", description: "When anything last happened on the conversation, across all lanes." },
    awaitingReply: { type: "boolean", description: "True when the customer's last message has not yet received a real answer — from the agent or from a teammate." },
  }}
/>

### EscalateConversationInput

Input for flagging a conversation for human attention.

<TypeTable
  type={{
    reason: { type: "string", description: "Why the agent is escalating, in the agent's own words." },
  }}
/>

### ListChannelConversationsInput

Input for listing an organization's channel conversations.

<TypeTable
  type={{
    org: { type: "string", description: "Organization whose conversations to list." },
    agentChannelId: { type: "string", description: "Optional filter: only conversations on this agent channel." },
    pageInfo: { type: "PageInfoInput", description: "Pagination options." },
    filter: { type: "ChannelConversationListFilter", description: "Optional filter: narrow the list to a server-evaluated predicate.", typeDescriptionLink: "#channelconversationlistfilter" },
  }}
/>

### ChannelConversationList

ChannelConversationList contains a page of channel conversations, newest activity first.

<TypeTable
  type={{
    totalCount: { type: "number", description: "Total number of conversations matching the filter." },
    items: { type: "ChannelConversationInput[]", description: "Conversations in the current page." },
  }}
/>

### GetChannelConversationInput

Input for reading one conversation's identity and participation state.

<TypeTable
  type={{
    agentChannelId: { type: "string", description: "AgentChannel the conversation belongs to." },
    conversationKey: { type: "string", description: "Conversation key within the channel (WhatsApp: the customer's wa_id)." },
  }}
/>

### GetConversationTimelineInput

Input for reading one conversation's timeline.

<TypeTable
  type={{
    agentChannelId: { type: "string", description: "AgentChannel the conversation belongs to." },
    conversationKey: { type: "string", description: "Conversation key within the channel (WhatsApp: the customer's wa_id)." },
    pageSize: { type: "number", description: "Maximum number of items to return per page." },
    pageToken: { type: "string", description: "Opaque cursor from a previous response." },
  }}
/>

### ConversationTimeline

ConversationTimeline contains one page of a conversation's timeline, newest first.

<TypeTable
  type={{
    items: { type: "ConversationTimelineItemInput[]", description: "Timeline items in the current page, newest first." },
    nextPageToken: { type: "string", description: "Cursor for the next (older) page." },
  }}
/>

### SendChannelMessageInput

Input for sending a business-initiated message on an agent channel.

<TypeTable
  type={{
    channel: { type: "string", description: "AgentChannel slug to send through, resolved within `org`." },
    org: { type: "string", description: "Organization the channel belongs to." },
    recipient: { type: "string", description: "External recipient key on the channel's provider (WhatsApp: the recipient's wa_id)." },
    payload: { type: "ChannelOutboundPayloadInput", description: "Typed message content.", required: true },
  }}
/>

### ListChannelTemplatesInput

Input for listing the message templates available on an agent channel.

<TypeTable
  type={{
    channel: { type: "string", description: "AgentChannel slug whose provider registry to read, resolved within `org`." },
    org: { type: "string", description: "Organization the channel belongs to." },
    approvedOnly: { type: "boolean", description: "When true, only templates the provider will accept for sending are returned (WhatsApp: status APPROVED, filtered server-side)." },
  }}
/>

### ChannelTemplates

ChannelTemplates contains the message templates available on an agent channel.

<TypeTable
  type={{
    entries: { type: "ChannelTemplateInput[]", description: "Template entries, one per (name, language) pair." },
  }}
/>

### ListMessagingChannelsInput

Input for listing the agent channels the caller can send business-initiated messages on.

### MessagingChannels

MessagingChannels contains the agent channels the caller can send business-initiated messages on.

<TypeTable
  type={{
    entries: { type: "MessagingChannelInput[]", description: "One entry per serving proactive-enabled channel." },
  }}
/>

### GetAgentChannelsByAgentRequest

Input for retrieving all channels of a specific agent.

<TypeTable
  type={{
    agentId: { type: "string", description: "Identifier of the agent whose channels to retrieve.", required: true },
    pageInfo: { type: "PageInfoInput", description: "Pagination options." },
    org: { type: "string", description: "Organization slug to scope the results to." },
  }}
/>

### AgentChannelList

Response containing a paginated list of agent channels.

<TypeTable
  type={{
    totalCount: { type: "number", description: "Total number of agent channels matching the query." },
    items: { type: "AgentChannelInput[]", description: "Agent channels in the current page.", typeDescriptionLink: "#agentchannelinput" },
  }}
/>

### ListAgentChannelsRequest

Input for listing agent channels within an organization.

<TypeTable
  type={{
    org: { type: "string", description: "Organization slug to scope the listing." },
    labels: { type: "Record<string, string>", description: "Filter by metadata labels." },
    pageInfo: { type: "PageInfoInput", description: "Pagination options." },
  }}
/>

### AgentChannel

AgentChannel connects an agent to an external messaging platform so people can chat with it where they already work.

<TypeTable
  type={{
    apiVersion: { type: "string", description: "API version for this resource type." },
    kind: { type: "string", description: "Resource kind identifier." },
    metadata: { type: "ApiResourceMetadata", description: "Resource metadata including id, name, org, slug, labels, visibility, and timestamps.", typeDescriptionLink: "/docs/sdk/resources/commons#apiresourcemetadata" },
    spec: { type: "AgentChannelInput", description: "Resource specification. See [`AgentChannelInput`](#agentchannelinput).", typeDescriptionLink: "#agentchannelinput" },
    status: { type: "AgentChannelStatus", description: "System-managed state. See [`AgentChannelStatus`](#agentchannelstatus).", typeDescriptionLink: "#agentchannelstatus" },
  }}
/>

### AgentChannelStatus

AgentChannelStatus contains system-managed state for an agent channel.

<TypeTable
  type={{
    installState: { type: "AgentChannelInstallState", description: "Install lifecycle of the provider connection.", typeDescriptionLink: "#agentchannelinstallstate" },
    slack: { type: "SlackInstallStatus", description: "Slack workspace install facts.", typeDescriptionLink: "#slackinstallstatus" },
    whatsapp: { type: "WhatsAppInstallStatus", description: "WhatsApp Business number install facts.", typeDescriptionLink: "#whatsappinstallstatus" },
    credentialsEnvironmentId: { type: "string", description: "ID of the system-managed Environment holding this connection's provider credentials (e.g. the Slack bot token)." },
    audit: { type: "ApiResourceAudit", description: "Standard audit information (created_at, updated_at, created_by, etc.).", typeDescriptionLink: "/docs/sdk/resources/commons#apiresourceaudit" },
  }}
/>

### SlackInstallStatus

SlackInstallStatus holds the facts observed when the Stigmer Slack app was installed into a workspace.

<TypeTable
  type={{
    teamId: { type: "string", description: "Slack workspace (team) ID, e.g. \"T0123ABCD\"." },
    teamName: { type: "string", description: "Human-readable workspace name at install time." },
    botUserId: { type: "string", description: "Bot user ID minted by the install, e.g. \"U0456EFGH\"." },
    grantedScopes: { type: "string[]", description: "OAuth scopes granted to the installation." },
    installerSlackUserId: { type: "string", description: "Slack user ID of the person who performed the install." },
    installedAt: { type: "string", description: "When the install completed." },
    channelAppId: { type: "string", description: "ID of the ChannelApp the install went through; empty for installs of the platform's shared Stigmer app." },
  }}
/>

### WhatsAppInstallStatus

WhatsAppInstallStatus holds the facts observed when a WhatsApp Business number was connected through the Meta Cloud API.

<TypeTable
  type={{
    phoneNumberId: { type: "string", description: "Phone number ID the install validated and bound, e.g. \"106540352242922\"." },
    displayPhoneNumber: { type: "string", description: "Human-readable phone number at install time, e.g. \"+1 555 025 3483\"." },
    verifiedName: { type: "string", description: "WhatsApp Business verified display name of the number at install time." },
    channelAppId: { type: "string", description: "ID of the ChannelApp the install went through." },
    installedAt: { type: "string", description: "When the install completed." },
  }}
/>

### ChannelSendOutcome

ChannelSendOutcome reports what happened to a business-initiated message on the inline delivery attempt.

| Value | Description |
|-------|-------------|
| `accepted` | Provider accepted the message. |
| `queued` | Transient failure; the platform retries in the background. |
| `refused` | Terminal refusal; detail says why (caps, recipient policy, provider policy such as an expired 24-hour window). |

### ConversationControl

ConversationControl names who may speak to the customer on a conversation's public lane.

| Value | Description |
|-------|-------------|
| `control_agent` | The agent serves the conversation (the default for every conversation). |
| `control_human` | A human holds the conversation; the agent is quiet until handback. |

### ChannelConversationListFilter

ChannelConversationListFilter names the server-evaluated predicates the conversation list can be narrowed to.

| Value | Description |
|-------|-------------|
| `filter_wants_human` | Conversations where a human action is wanted: flagged for attention, or human-held with the customer's last message still unanswered (needs_attention OR (awaiting_reply AND control_human)). |

### AgentChannelInstallState

AgentChannelInstallState tracks the provider install lifecycle.

| Value | Description |
|-------|-------------|
| `pending_install` | The channel exists but the provider install has not completed. |
| `installed` | The provider install completed; credentials are stored and the channel serves traffic while spec.enabled is true. |
| `revoked` | The provider revoked the installation (e.g. the Slack app was uninstalled from the workspace or tokens were revoked). |

### ServiceTier

ServiceTier selects the provider-side speed/service tier for an execution's model calls.

| Value | Description |
|-------|-------------|
| `SERVICE_TIER_STANDARD` | Standard tier: the model's base-priced configuration, requested explicitly. |
| `SERVICE_TIER_FAST` | Fast tier: the model's fast variant, billed at the registry's fast variant rates. |

### ConversationLane

ConversationLane distinguishes customer-visible items from internal ones.

| Value | Description |
|-------|-------------|
| `lane_public` | The customer sees this item (their message, a delivered reply, a send). |
| `lane_internal` | Org-internal item (escalations, notes); the customer never sees it. |

### ConversationItemAuthor

ConversationItemAuthor names who authored a timeline item.

| Value | Description |
|-------|-------------|
| `author_customer` | The external customer. |
| `author_agent` | The serving agent. |
| `author_teammate` | An org human replying as the business. |
| `author_platform` | Platform-authored conversation copy (e.g. the takeover acknowledgment), neither the agent's words nor a teammate's. |

### ChannelDeliveryStatus

ChannelDeliveryStatus is the delivery record lifecycle.

| Value | Description |
|-------|-------------|
| `pending` | Awaiting the execution's terminal phase, or awaiting (re)claim. |
| `delivering` | Claimed by a delivery worker; provider I/O in flight. |
| `delivered` | Reply delivered to the provider. |
| `failed` | Dead-lettered after exhausting attempts; last_error records why. |
| `suppressed` | Withheld because a human held the conversation when the reply came due; the customer never received it. |

### ChannelReceiptState

ChannelReceiptState is the provider-reported delivery progress of an accepted outbound message, as learned from delivery receipts.

| Value | Description |
|-------|-------------|
| `receipt_sent` | The provider accepted the message into its delivery queue. |
| `receipt_delivered` | The message reached the recipient's device. |
| `receipt_read` | The recipient viewed the message (or played it, for voice). |
| `receipt_failed` | The provider could not deliver the message; receipt_detail and receipt_error_code carry the provider's verdict. |

### ChannelOutboundOrigin

ChannelOutboundOrigin records which trust context authorized a business-initiated send.

| Value | Description |
|-------|-------------|
| `channel_conversation` | The send came from an agent run created by an inbound channel message; the recipient must be a known sender on the channel. |
| `operator` | The send was authored by an operator (console, CLI, SDK, scheduled run); any recipient, bounded by caps. |
| `platform` | Platform-authored conversation-context copy (e.g. the takeover acknowledgment), neither the agent's words nor a human's. |
| `participant` | A staff member's reply inside a live conversation, sent through the conversation-scoped reply command. |

