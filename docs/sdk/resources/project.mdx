---
title: Project
---

{/* Auto-generated by stigmer-codegen. DO NOT EDIT. */}

A Project groups related resources (agents, workflows, MCP servers, skills)
under a single unit of management with automatic orphan pruning. It supports
a declarative track using YAML files and an SDK track using a code entry point.

```yaml
apiVersion: tenancy.stigmer.ai/v1
kind: Project
metadata:
  name: my-agent-fleet
  org: acme-corp
spec:
  description: "Production agent fleet"
```

To use the SDK track, set an entry point:

```yaml
apiVersion: tenancy.stigmer.ai/v1
kind: Project
metadata:
  name: my-super-app
  org: acme-corp
spec:
  entry_point: main.go
  description: "Go SDK project"
```

## Client Access

<SDKTabs>
<Tab value="TypeScript">
```typescript
const project = await stigmer.project.get("prj_01j5q3k7m8r2s4tnz2hfp0q0c3");
```
</Tab>
<Tab value="Go">
```go
project, err := client.Project.Get(ctx, "prj_01j5q3k7m8r2s4tnz2hfp0q0c3")
```
</Tab>
<Tab value="Python">
```python
project = client.projects.get("prj_01j5q3k7m8r2s4tnz2hfp0q0c3")
```
</Tab>
<Tab value="Java">
```java
Project project = client.projects().get("prj_01j5q3k7m8r2s4tnz2hfp0q0c3");
```
</Tab>
</SDKTabs>

## Methods

| Method | Description |
|--------|-------------|
| [`get`](#get) | Get a single project by ID. |
| [`getByReference`](#getbyreference) | Get a project by reference (org/name). |
| [`apply`](#apply) | Create or update a project. |
| [`create`](#create) | Create a project. |
| [`update`](#update) | Update an existing project. |
| [`delete`](#delete) | Delete a project. |

### get

Get a single project by ID.

<SDKTabs>
<Tab value="TypeScript">
```typescript
const project = await stigmer.project.get(id);
```
</Tab>
<Tab value="Go">
```go
project, err := client.Project.Get(ctx, id)
```
</Tab>
<Tab value="Python">
```python
project = client.projects.get(id)
```
</Tab>
<Tab value="Java">
```java
Project project = client.projects().get(id);
```
</Tab>
</SDKTabs>

**Parameters**

| Parameter | Type | Description |
|-----------|------|-------------|
| `id` | `string` | Project identifier. |

**Returns**: [`Project`](#project)

### getByReference

Get a project by reference (org/name).

<SDKTabs>
<Tab value="TypeScript">
```typescript
const project = await stigmer.project.getByReference({
  org: "...",
  kind: "...",
  slug: "...",
  version: "...",
});
```
</Tab>
<Tab value="Go">
```go
project, err := client.Project.GetByReference(ctx, &stigmer.ApiResourceReference{
  Org:     "...",
  Kind:    "...",
  Slug:    "...",
  Version: "...",
})
```
</Tab>
<Tab value="Python">
```python
project = client.projects.get_by_reference(ApiResourceReference(
    org="...",
    kind="...",
    slug="...",
    version="...",
))
```
</Tab>
<Tab value="Java">
```java
Project project = client.projects().getByReference(ApiResourceReference.builder()
    .org("...")
    .kind("...")
    .slug("...")
    .version("...")
    .build());
```
</Tab>
</SDKTabs>

**Parameters**: [`ApiResourceReference`](#apiresourcereference)

**Returns**: [`Project`](#project)

### apply

Create or update a project.

<SDKTabs>
<Tab value="TypeScript">
```typescript
const project = await stigmer.project.apply({
  name: "my-project",
  org: "acme",
  slug: "...",
  labels: {},
  entryPoint: "...",
  description: "...",
  members: [],
});
```
</Tab>
<Tab value="Go">
```go
project, err := client.Project.Apply(ctx, &stigmer.ProjectInput{
  Name:        "my-project",
  Org:         "acme",
  Slug:        "...",
  Labels:      map[string]string{},
  EntryPoint:  "...",
  Description: "...",
  Members:     []stigmer.ResourceRef{},
})
```
</Tab>
<Tab value="Python">
```python
project = client.projects.apply(ProjectInput(
    name="my-project",
    org="acme",
    slug="...",
    labels={},
    entry_point="...",
    description="...",
    members=[],
))
```
</Tab>
<Tab value="Java">
```java
Project project = client.projects().apply(ProjectInput.builder()
    .name("my-project")
    .org("acme")
    .slug("...")
    .labels(Map.of())
    .entryPoint("...")
    .description("...")
    .members(List.of())
    .build());
```
</Tab>
</SDKTabs>

**Parameters**: [`ProjectInput`](#projectinput)

**Returns**: [`Project`](#project)

### create

Create a project.

<SDKTabs>
<Tab value="TypeScript">
```typescript
const project = await stigmer.project.create({
  name: "my-project",
  org: "acme",
  slug: "...",
  labels: {},
  entryPoint: "...",
  description: "...",
  members: [],
});
```
</Tab>
<Tab value="Go">
```go
project, err := client.Project.Create(ctx, &stigmer.ProjectInput{
  Name:        "my-project",
  Org:         "acme",
  Slug:        "...",
  Labels:      map[string]string{},
  EntryPoint:  "...",
  Description: "...",
  Members:     []stigmer.ResourceRef{},
})
```
</Tab>
<Tab value="Python">
```python
project = client.projects.create(ProjectInput(
    name="my-project",
    org="acme",
    slug="...",
    labels={},
    entry_point="...",
    description="...",
    members=[],
))
```
</Tab>
<Tab value="Java">
```java
Project project = client.projects().create(ProjectInput.builder()
    .name("my-project")
    .org("acme")
    .slug("...")
    .labels(Map.of())
    .entryPoint("...")
    .description("...")
    .members(List.of())
    .build());
```
</Tab>
</SDKTabs>

**Parameters**: [`ProjectInput`](#projectinput)

**Returns**: [`Project`](#project)

### update

Update an existing project.

<SDKTabs>
<Tab value="TypeScript">
```typescript
const project = await stigmer.project.update({
  name: "my-project",
  org: "acme",
  slug: "...",
  labels: {},
  entryPoint: "...",
  description: "...",
  members: [],
});
```
</Tab>
<Tab value="Go">
```go
project, err := client.Project.Update(ctx, &stigmer.ProjectInput{
  Name:        "my-project",
  Org:         "acme",
  Slug:        "...",
  Labels:      map[string]string{},
  EntryPoint:  "...",
  Description: "...",
  Members:     []stigmer.ResourceRef{},
})
```
</Tab>
<Tab value="Python">
```python
project = client.projects.update(ProjectInput(
    name="my-project",
    org="acme",
    slug="...",
    labels={},
    entry_point="...",
    description="...",
    members=[],
))
```
</Tab>
<Tab value="Java">
```java
Project project = client.projects().update(ProjectInput.builder()
    .name("my-project")
    .org("acme")
    .slug("...")
    .labels(Map.of())
    .entryPoint("...")
    .description("...")
    .members(List.of())
    .build());
```
</Tab>
</SDKTabs>

**Parameters**: [`ProjectInput`](#projectinput)

**Returns**: [`Project`](#project)

### delete

Delete a project.

<SDKTabs>
<Tab value="TypeScript">
```typescript
const project = await stigmer.project.delete(id);
```
</Tab>
<Tab value="Go">
```go
project, err := client.Project.Delete(ctx, id)
```
</Tab>
<Tab value="Python">
```python
project = client.projects.delete(id)
```
</Tab>
<Tab value="Java">
```java
Project project = client.projects().delete(id);
```
</Tab>
</SDKTabs>

**Parameters**

| Parameter | Type | Description |
|-----------|------|-------------|
| `id` | `string` | Project identifier. |

**Returns**: [`Project`](#project)

## Types

### ProjectInput

Input for creating or updating a Project.

<TypeTable
  type={{
    name: { type: "string", description: "Resource name.", required: true },
    slug: { type: "string", description: "URL-friendly identifier." },
    org: { type: "string", description: "Organization slug.", required: true },
    labels: { type: "Record<string, string>", description: "Key-value labels." },
    entryPoint: { type: "string", description: "Entry point file for SDK synthesis." },
    description: { type: "string", description: "Human-readable description of the project." },
    members: { type: "ResourceRef[]", description: "References to resources managed by this project.", typeDescriptionLink: "#resourceref" },
  }}
/>

### ResourceRef

Generic reference to any API resource by org and slug.

<TypeTable
  type={{
    org: { type: "string", description: "Organization that owns the referenced resource." },
    kind: { type: "ApiResourceKind", description: "Kind of the referenced resource (e.g., SKILL, AGENT, MCP_SERVER).", typeDescriptionLink: "/docs/sdk/resources/commons#apiresourcekind" },
    slug: { type: "string", description: "Resource slug (user-friendly identifier, unique within org).", required: true },
    version: { type: "string", description: "Version of the resource (optional, only applicable to versioned resources like Skills)." },
  }}
/>

### Project

Project groups related resources under a single unit of management with automatic orphan pruning.

<TypeTable
  type={{
    apiVersion: { type: "string", description: "API version for this resource type." },
    kind: { type: "string", description: "Resource kind identifier." },
    metadata: { type: "ApiResourceMetadata", description: "Resource metadata including id, name, org, slug, labels, visibility, and timestamps.", typeDescriptionLink: "/docs/sdk/resources/commons#apiresourcemetadata" },
    spec: { type: "ProjectInput", description: "Resource specification. See [`ProjectInput`](#projectinput).", typeDescriptionLink: "#projectinput" },
    status: { type: "ProjectStatus", description: "System-managed state. See [`ProjectStatus`](#projectstatus).", typeDescriptionLink: "#projectstatus" },
  }}
/>

### ProjectStatus

ProjectStatus contains system-managed state for a Project.

<TypeTable
  type={{
    lastReconciliation: { type: "ReconciliationSummary", description: "Reconciliation summary from the most recent Apply operation.", typeDescriptionLink: "#reconciliationsummary" },
    audit: { type: "ApiResourceAudit", description: "Standard audit information (created_at, updated_at, etc.) The updated_at field indicates when the project was last successfully applied.", typeDescriptionLink: "/docs/sdk/resources/commons#apiresourceaudit" },
  }}
/>

### ReconciliationSummary

ReconciliationSummary contains the results of project reconciliation.

<TypeTable
  type={{
    created: { type: "ApiResourceReference[]", description: "Resources created on the server during this project apply.", typeDescriptionLink: "#apiresourcereference" },
    updated: { type: "ApiResourceReference[]", description: "Resources updated on the server during this project apply.", typeDescriptionLink: "#apiresourcereference" },
    deleted: { type: "ApiResourceReference[]", description: "Resources deleted via orphan pruning during this project apply.", typeDescriptionLink: "#apiresourcereference" },
  }}
/>

### ApiResourceReference

Generic reference to any API resource by org and slug.

<TypeTable
  type={{
    org: { type: "string", description: "Organization that owns the referenced resource." },
    kind: { type: "ApiResourceKind", description: "Kind of the referenced resource (e.g., SKILL, AGENT, MCP_SERVER).", typeDescriptionLink: "/docs/sdk/resources/commons#apiresourcekind" },
    slug: { type: "string", description: "Resource slug (user-friendly identifier, unique within org).", required: true },
    version: { type: "string", description: "Version of the resource (optional, only applicable to versioned resources like Skills)." },
  }}
/>

