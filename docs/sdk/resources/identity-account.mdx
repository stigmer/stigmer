---
title: Identity Account
---

{/* Auto-generated by stigmer-codegen. DO NOT EDIT. */}

An IdentityAccount represents a user or machine principal in Stigmer. It is the
core identity primitive for authentication and authorization. Accounts can be
direct (signed up via Stigmer), federated (provisioned through an external
identity provider), or machine (service-to-service credentials).

```yaml
apiVersion: iam.stigmer.ai/v1
kind: IdentityAccount
metadata:
  name: alice
spec:
  idp_id: "auth0|abc123def456"
  email: alice@example.com
  first_name: Alice
  last_name: Smith
  provisioning_mode: direct
```

## Client Access

<SDKTabs>
<Tab value="TypeScript">
```typescript
const identityAccount = await stigmer.identityAccount.get("ida_01j5q3k7m8r2s4tnz2hfp0q0c3");
```
</Tab>
<Tab value="Go">
```go
identityAccount, err := client.IdentityAccount.Get(ctx, "ida_01j5q3k7m8r2s4tnz2hfp0q0c3")
```
</Tab>
<Tab value="Python">
```python
identity_account = client.identity_accounts.get("ida_01j5q3k7m8r2s4tnz2hfp0q0c3")
```
</Tab>
<Tab value="Java">
```java
IdentityAccount identityAccount = client.identityAccounts().get("ida_01j5q3k7m8r2s4tnz2hfp0q0c3");
```
</Tab>
</SDKTabs>

## Methods

| Method | Description |
|--------|-------------|
| [`get`](#get) | Get an identity account by its unique identifier. |
| [`getActorInfo`](#getactorinfo) | Get lightweight actor information for an identity account. |
| [`getByEmail`](#getbyemail) | Get a direct identity account by email address. |
| [`getByExternalSub`](#getbyexternalsub) | Get a federated identity account by identity provider reference and external subject. |
| [`getByIdpId`](#getbyidpid) | Get an identity account by identity provider ID (Auth0 subject). |
| [`create`](#create) | Create a new identity account. |
| [`update`](#update) | Update an existing identity account. |
| [`updateFederatedAccount`](#updatefederatedaccount) | Update profile fields on a federated identity account. |
| [`delete`](#delete) | Delete an identity account. |
| [`createFederatedAccount`](#createfederatedaccount) | Create a federated identity account for an external platform user. |
| [`deprovisionFederatedAccount`](#deprovisionfederatedaccount) | Deprovision a federated identity account by revoking access or deleting it. |
| [`provisionMyAccount`](#provisionmyaccount) | Provision the caller's own identity account. |
| [`whoAmI`](#whoami) | Get the identity account of the currently authenticated user. |

### get

Get an identity account by its unique identifier.

<SDKTabs>
<Tab value="TypeScript">
```typescript
const identityAccount = await stigmer.identityAccount.get(id);
```
</Tab>
<Tab value="Go">
```go
identityAccount, err := client.IdentityAccount.Get(ctx, id)
```
</Tab>
<Tab value="Python">
```python
identity_account = client.identity_accounts.get(id)
```
</Tab>
<Tab value="Java">
```java
IdentityAccount identityAccount = client.identityAccounts().get(id);
```
</Tab>
</SDKTabs>

**Parameters**

| Parameter | Type | Description |
|-----------|------|-------------|
| `id` | `string` | Identity Account identifier. |

**Returns**: [`IdentityAccount`](#identityaccount)

### getActorInfo

Get lightweight actor information for an identity account.

<SDKTabs>
<Tab value="TypeScript">
```typescript
const apiResourceAuditActor = await stigmer.identityAccount.getActorInfo(id);
```
</Tab>
<Tab value="Go">
```go
apiResourceAuditActor, err := client.IdentityAccount.GetActorInfo(ctx, id)
```
</Tab>
<Tab value="Python">
```python
api_resource_audit_actor = client.identity_accounts.get_actor_info(id)
```
</Tab>
<Tab value="Java">
```java
ApiResourceAuditActor apiResourceAuditActor = client.identityAccounts().getActorInfo(id);
```
</Tab>
</SDKTabs>

**Parameters**

| Parameter | Type | Description |
|-----------|------|-------------|
| `id` | `string` | Identity Account identifier. |

**Returns**: [`ApiResourceAuditActor`](/docs/sdk/resources/commons#apiresourceauditactor)

### getByEmail

Get a direct identity account by email address.

Only returns direct (non-federated) accounts. Federated accounts are not returned by this RPC — use getByExternalSub for IdP-scoped federated lookups.

<SDKTabs>
<Tab value="TypeScript">
```typescript
const identityAccount = await stigmer.identityAccount.getByEmail({
  value: "...",
});
```
</Tab>
<Tab value="Go">
```go
identityAccount, err := client.IdentityAccount.GetByEmail(ctx, &stigmer.IdentityAccountEmail{
  Value: "...",
})
```
</Tab>
<Tab value="Python">
```python
identity_account = client.identity_accounts.get_by_email(IdentityAccountEmail(
    value="...",
))
```
</Tab>
<Tab value="Java">
```java
IdentityAccount identityAccount = client.identityAccounts().getByEmail(IdentityAccountEmail.builder()
    .value("...")
    .build());
```
</Tab>
</SDKTabs>

**Parameters**: [`IdentityAccountEmail`](#identityaccountemail)

**Returns**: [`IdentityAccount`](#identityaccount)

### getByExternalSub

Get a federated identity account by identity provider reference and external subject.

Used by platform backends to check whether a federated account already exists for a given OIDC subject before calling createFederatedAccount.

Authorization: Requires can_create_identity_account on the organization that owns the identity provider.

<SDKTabs>
<Tab value="TypeScript">
```typescript
const identityAccount = await stigmer.identityAccount.getByExternalSub({
  org: "...",
  identityProviderRef: {},
  externalSub: "...",
});
```
</Tab>
<Tab value="Go">
```go
identityAccount, err := client.IdentityAccount.GetByExternalSub(ctx, &stigmer.ExternalSubLookup{
  Org:                 "...",
  IdentityProviderRef: &stigmer.ResourceRef{},
  ExternalSub:         "...",
})
```
</Tab>
<Tab value="Python">
```python
identity_account = client.identity_accounts.get_by_external_sub(ExternalSubLookup(
    org="...",
    identity_provider_ref=None,
    external_sub="...",
))
```
</Tab>
<Tab value="Java">
```java
IdentityAccount identityAccount = client.identityAccounts().getByExternalSub(ExternalSubLookup.builder()
    .org("...")
    .identityProviderRef(null)
    .externalSub("...")
    .build());
```
</Tab>
</SDKTabs>

**Parameters**: [`ExternalSubLookup`](#externalsublookup)

**Returns**: [`IdentityAccount`](#identityaccount)

### getByIdpId

Get an identity account by identity provider ID (Auth0 subject).

Primarily used for direct and machine accounts where the IDP ID is the Auth0 user_id or client_id. For federated account lookups, use getByExternalSub which is scoped to a specific identity provider.

<SDKTabs>
<Tab value="TypeScript">
```typescript
const identityAccount = await stigmer.identityAccount.getByIdpId(id);
```
</Tab>
<Tab value="Go">
```go
identityAccount, err := client.IdentityAccount.GetByIdpId(ctx, id)
```
</Tab>
<Tab value="Python">
```python
identity_account = client.identity_accounts.get_by_idp_id(id)
```
</Tab>
<Tab value="Java">
```java
IdentityAccount identityAccount = client.identityAccounts().getByIdpId(id);
```
</Tab>
</SDKTabs>

**Parameters**

| Parameter | Type | Description |
|-----------|------|-------------|
| `id` | `string` | Identity Account identifier. |

**Returns**: [`IdentityAccount`](#identityaccount)

### create

Create a new identity account.

<SDKTabs>
<Tab value="TypeScript">
```typescript
const identityAccount = await stigmer.identityAccount.create({
  name: "my-identity-account",
  org: "acme",
  slug: "...",
  labels: {},
  idpId: "...",
  email: "...",
  firstName: "...",
  lastName: "...",
  pictureUrl: "...",
  isMachineAccount: false,
  provisioningMode: "...",
  identityProviderRef: {},
  preferences: {},
});
```
</Tab>
<Tab value="Go">
```go
identityAccount, err := client.IdentityAccount.Create(ctx, &stigmer.IdentityAccountInput{
  Name:                "my-identity-account",
  Org:                 "acme",
  Slug:                "...",
  Labels:              map[string]string{},
  IdpId:               "...",
  Email:               "...",
  FirstName:           "...",
  LastName:            "...",
  PictureUrl:          "...",
  IsMachineAccount:    false,
  ProvisioningMode:    "...",
  IdentityProviderRef: &stigmer.ResourceRef{},
  Preferences:         &stigmer.IdentityAccountPreferencesInput{},
})
```
</Tab>
<Tab value="Python">
```python
identity_account = client.identity_accounts.create(IdentityAccountInput(
    name="my-identity-account",
    org="acme",
    slug="...",
    labels={},
    idp_id="...",
    email="...",
    first_name="...",
    last_name="...",
    picture_url="...",
    is_machine_account=False,
    provisioning_mode="...",
    identity_provider_ref=None,
    preferences=None,
))
```
</Tab>
<Tab value="Java">
```java
IdentityAccount identityAccount = client.identityAccounts().create(IdentityAccountInput.builder()
    .name("my-identity-account")
    .org("acme")
    .slug("...")
    .labels(Map.of())
    .idpId("...")
    .email("...")
    .firstName("...")
    .lastName("...")
    .pictureUrl("...")
    .isMachineAccount(false)
    .provisioningMode("...")
    .identityProviderRef(null)
    .preferences(null)
    .build());
```
</Tab>
</SDKTabs>

**Parameters**: [`IdentityAccountInput`](#identityaccountinput)

**Returns**: [`IdentityAccount`](#identityaccount)

### update

Update an existing identity account.

<SDKTabs>
<Tab value="TypeScript">
```typescript
const identityAccount = await stigmer.identityAccount.update({
  name: "my-identity-account",
  org: "acme",
  slug: "...",
  labels: {},
  idpId: "...",
  email: "...",
  firstName: "...",
  lastName: "...",
  pictureUrl: "...",
  isMachineAccount: false,
  provisioningMode: "...",
  identityProviderRef: {},
  preferences: {},
});
```
</Tab>
<Tab value="Go">
```go
identityAccount, err := client.IdentityAccount.Update(ctx, &stigmer.IdentityAccountInput{
  Name:                "my-identity-account",
  Org:                 "acme",
  Slug:                "...",
  Labels:              map[string]string{},
  IdpId:               "...",
  Email:               "...",
  FirstName:           "...",
  LastName:            "...",
  PictureUrl:          "...",
  IsMachineAccount:    false,
  ProvisioningMode:    "...",
  IdentityProviderRef: &stigmer.ResourceRef{},
  Preferences:         &stigmer.IdentityAccountPreferencesInput{},
})
```
</Tab>
<Tab value="Python">
```python
identity_account = client.identity_accounts.update(IdentityAccountInput(
    name="my-identity-account",
    org="acme",
    slug="...",
    labels={},
    idp_id="...",
    email="...",
    first_name="...",
    last_name="...",
    picture_url="...",
    is_machine_account=False,
    provisioning_mode="...",
    identity_provider_ref=None,
    preferences=None,
))
```
</Tab>
<Tab value="Java">
```java
IdentityAccount identityAccount = client.identityAccounts().update(IdentityAccountInput.builder()
    .name("my-identity-account")
    .org("acme")
    .slug("...")
    .labels(Map.of())
    .idpId("...")
    .email("...")
    .firstName("...")
    .lastName("...")
    .pictureUrl("...")
    .isMachineAccount(false)
    .provisioningMode("...")
    .identityProviderRef(null)
    .preferences(null)
    .build());
```
</Tab>
</SDKTabs>

**Parameters**: [`IdentityAccountInput`](#identityaccountinput)

**Returns**: [`IdentityAccount`](#identityaccount)

### updateFederatedAccount

Update profile fields on a federated identity account.

Looks up the account by natural key (identity_provider_ref + external_sub) and updates email, name, and picture. Identity keys are immutable.

Called by platform backends when a user's profile changes on their platform.

Authorization: Requires can_create_identity_account on the organization that owns the identity provider.

<SDKTabs>
<Tab value="TypeScript">
```typescript
const identityAccount = await stigmer.identityAccount.updateFederatedAccount({
  org: "...",
  identityProviderRef: {},
  externalSub: "...",
  email: "...",
  firstName: "...",
  lastName: "...",
  pictureUrl: "...",
});
```
</Tab>
<Tab value="Go">
```go
identityAccount, err := client.IdentityAccount.UpdateFederatedAccount(ctx, &stigmer.UpdateFederatedAccountInput{
  Org:                 "...",
  IdentityProviderRef: &stigmer.ResourceRef{},
  ExternalSub:         "...",
  Email:               "...",
  FirstName:           "...",
  LastName:            "...",
  PictureUrl:          "...",
})
```
</Tab>
<Tab value="Python">
```python
identity_account = client.identity_accounts.update_federated_account(UpdateFederatedAccountInput(
    org="...",
    identity_provider_ref=None,
    external_sub="...",
    email="...",
    first_name="...",
    last_name="...",
    picture_url="...",
))
```
</Tab>
<Tab value="Java">
```java
IdentityAccount identityAccount = client.identityAccounts().updateFederatedAccount(UpdateFederatedAccountInput.builder()
    .org("...")
    .identityProviderRef(null)
    .externalSub("...")
    .email("...")
    .firstName("...")
    .lastName("...")
    .pictureUrl("...")
    .build());
```
</Tab>
</SDKTabs>

**Parameters**: [`UpdateFederatedAccountInput`](#updatefederatedaccountinput)

**Returns**: [`IdentityAccount`](#identityaccount)

### delete

Delete an identity account.

<SDKTabs>
<Tab value="TypeScript">
```typescript
const identityAccount = await stigmer.identityAccount.delete(id);
```
</Tab>
<Tab value="Go">
```go
identityAccount, err := client.IdentityAccount.Delete(ctx, id)
```
</Tab>
<Tab value="Python">
```python
identity_account = client.identity_accounts.delete(id)
```
</Tab>
<Tab value="Java">
```java
IdentityAccount identityAccount = client.identityAccounts().delete(id);
```
</Tab>
</SDKTabs>

**Parameters**

| Parameter | Type | Description |
|-----------|------|-------------|
| `id` | `string` | Identity Account identifier. |

**Returns**: [`IdentityAccount`](#identityaccount)

### createFederatedAccount

Create a federated identity account for an external platform user.

Called by platform backends (via API key) when a new user signs up on their platform. The platform provides the user's OIDC subject identifier and profile data. The account must be created before the user can authenticate via the IdP.

Returns the full identity account including its ID, which the platform uses to grant roles via IAM policies.

Authorization: Requires can_create_identity_account on the organization that owns the identity provider.

<SDKTabs>
<Tab value="TypeScript">
```typescript
const identityAccount = await stigmer.identityAccount.createFederatedAccount({
  org: "...",
  identityProviderRef: {},
  externalSub: "...",
  email: "...",
  firstName: "...",
  lastName: "...",
  pictureUrl: "...",
});
```
</Tab>
<Tab value="Go">
```go
identityAccount, err := client.IdentityAccount.CreateFederatedAccount(ctx, &stigmer.CreateFederatedAccountInput{
  Org:                 "...",
  IdentityProviderRef: &stigmer.ResourceRef{},
  ExternalSub:         "...",
  Email:               "...",
  FirstName:           "...",
  LastName:            "...",
  PictureUrl:          "...",
})
```
</Tab>
<Tab value="Python">
```python
identity_account = client.identity_accounts.create_federated_account(CreateFederatedAccountInput(
    org="...",
    identity_provider_ref=None,
    external_sub="...",
    email="...",
    first_name="...",
    last_name="...",
    picture_url="...",
))
```
</Tab>
<Tab value="Java">
```java
IdentityAccount identityAccount = client.identityAccounts().createFederatedAccount(CreateFederatedAccountInput.builder()
    .org("...")
    .identityProviderRef(null)
    .externalSub("...")
    .email("...")
    .firstName("...")
    .lastName("...")
    .pictureUrl("...")
    .build());
```
</Tab>
</SDKTabs>

**Parameters**: [`CreateFederatedAccountInput`](#createfederatedaccountinput)

**Returns**: [`IdentityAccount`](#identityaccount)

### deprovisionFederatedAccount

Deprovision a federated identity account by revoking access or deleting it.

Looks up the account by natural key (identity_provider_ref + external_sub). When delete_account is false, revokes all IAM policies in the organization. When delete_account is true, revokes policies and deletes the account.

Called by platform backends during user offboarding.

Authorization: Requires can_create_identity_account on the organization that owns the identity provider.

<SDKTabs>
<Tab value="TypeScript">
```typescript
const identityAccount = await stigmer.identityAccount.deprovisionFederatedAccount({
  org: "...",
  identityProviderRef: {},
  externalSub: "...",
  deleteAccount: false,
});
```
</Tab>
<Tab value="Go">
```go
identityAccount, err := client.IdentityAccount.DeprovisionFederatedAccount(ctx, &stigmer.DeprovisionFederatedAccountInput{
  Org:                 "...",
  IdentityProviderRef: &stigmer.ResourceRef{},
  ExternalSub:         "...",
  DeleteAccount:       false,
})
```
</Tab>
<Tab value="Python">
```python
identity_account = client.identity_accounts.deprovision_federated_account(DeprovisionFederatedAccountInput(
    org="...",
    identity_provider_ref=None,
    external_sub="...",
    delete_account=False,
))
```
</Tab>
<Tab value="Java">
```java
IdentityAccount identityAccount = client.identityAccounts().deprovisionFederatedAccount(DeprovisionFederatedAccountInput.builder()
    .org("...")
    .identityProviderRef(null)
    .externalSub("...")
    .deleteAccount(false)
    .build());
```
</Tab>
</SDKTabs>

**Parameters**: [`DeprovisionFederatedAccountInput`](#deprovisionfederatedaccountinput)

**Returns**: [`IdentityAccount`](#identityaccount)

### provisionMyAccount

Provision the caller's own identity account.

Called by the console when whoAmI() returns NOT_FOUND (first login after signup). Derives all identity information from the caller's JWT and the OIDC /userinfo endpoint. Creates the IdentityAccount and a personal Organization owned by the caller. Idempotent: returns the existing account on retry.

<SDKTabs>
<Tab value="TypeScript">
```typescript
const identityAccount = await stigmer.identityAccount.provisionMyAccount();
```
</Tab>
<Tab value="Go">
```go
identityAccount, err := client.IdentityAccount.ProvisionMyAccount(ctx)
```
</Tab>
<Tab value="Python">
```python
identity_account = client.identity_accounts.provision_my_account()
```
</Tab>
<Tab value="Java">
```java
IdentityAccount identityAccount = client.identityAccounts().provisionMyAccount();
```
</Tab>
</SDKTabs>

**Returns**: [`IdentityAccount`](#identityaccount)

### whoAmI

Get the identity account of the currently authenticated user.

Returns the full identity account for the caller based on the auth header.

<SDKTabs>
<Tab value="TypeScript">
```typescript
const identityAccount = await stigmer.identityAccount.whoAmI();
```
</Tab>
<Tab value="Go">
```go
identityAccount, err := client.IdentityAccount.WhoAmI(ctx)
```
</Tab>
<Tab value="Python">
```python
identity_account = client.identity_accounts.who_am_i()
```
</Tab>
<Tab value="Java">
```java
IdentityAccount identityAccount = client.identityAccounts().whoAmI();
```
</Tab>
</SDKTabs>

**Returns**: [`IdentityAccount`](#identityaccount)

## Types

### IdentityAccountInput

Input for creating or updating an Identity Account.

<TypeTable
  type={{
    name: { type: "string", description: "Resource name.", required: true },
    slug: { type: "string", description: "URL-friendly identifier." },
    org: { type: "string", description: "Organization slug.", required: true },
    labels: { type: "Record<string, string>", description: "Key-value labels." },
    idpId: { type: "string", description: "IDP ID of the identity account.", required: true },
    email: { type: "string", description: "Email of the identity account." },
    firstName: { type: "string", description: "First name of the identity account." },
    lastName: { type: "string", description: "Last name of the identity account." },
    pictureUrl: { type: "string", description: "URL of the profile picture." },
    isMachineAccount: { type: "boolean", description: "Indicates if this is a machine account used for inter-service communication." },
    provisioningMode: { type: "IdentityAccountProvisioningMode", description: "How this identity account was provisioned.", typeDescriptionLink: "#identityaccountprovisioningmode" },
    identityProviderRef: { type: "ResourceRef", description: "Reference to the IdentityProvider that owns this federated account.", typeDescriptionLink: "#resourceref" },
    preferences: { type: "IdentityAccountPreferencesInput", description: "Standing preferences declared by this identity account.", typeDescriptionLink: "#identityaccountpreferencesinput" },
  }}
/>

### ResourceRef

Generic reference to any API resource by org and slug.

<TypeTable
  type={{
    org: { type: "string", description: "Organization that owns the referenced resource." },
    kind: { type: "ApiResourceKind", description: "Kind of the referenced resource (e.g., SKILL, AGENT, MCP_SERVER).", typeDescriptionLink: "/docs/sdk/resources/commons#apiresourcekind" },
    slug: { type: "string", description: "Resource slug (user-friendly identifier, unique within org).", required: true },
    version: { type: "string", description: "Version of the resource (optional, only applicable to versioned resources like Skills)." },
  }}
/>

### IdentityAccountPreferencesInput

IdentityAccountPreferences holds user-declared defaults that apply to the user's own agent executions.

<TypeTable
  type={{
    standingContext: { type: "string", description: "Free-text standing context injected into this user's eligible agent executions." },
    defaultHarness: { type: "string", description: "Default harness for new sessions: \"native\" or \"cursor\"." },
    defaultNativeModel: { type: "string", description: "Default model (registry model ID) for native-harness sessions." },
    defaultCursorModel: { type: "string", description: "Default model (registry model ID) for cursor-harness sessions." },
    memoryEnabled: { type: "boolean", description: "Whether the platform may remember confirmed facts about this account." },
  }}
/>

### CreateFederatedAccountInput

CreateFederatedAccountInput is the command for creating a federated identity account linked to an external platform's identity provider.

<TypeTable
  type={{
    org: { type: "string", description: "Organization that owns the identity provider.", required: true },
    identityProviderRef: { type: "ResourceRef", description: "Reference to the IdentityProvider that this federated account belongs to.", typeDescriptionLink: "#resourceref" },
    externalSub: { type: "string", description: "External subject identifier from the platform's OIDC provider.", required: true },
    email: { type: "string", description: "Email address of the user.", required: true },
    firstName: { type: "string", description: "First name of the user." },
    lastName: { type: "string", description: "Last name of the user." },
    pictureUrl: { type: "string", description: "URL of the user's profile picture." },
  }}
/>

### UpdateFederatedAccountInput

UpdateFederatedAccountInput is the command for updating profile fields on a federated identity account identified by its natural key (identity provider reference + external subject).

<TypeTable
  type={{
    org: { type: "string", description: "Organization that owns the identity provider.", required: true },
    identityProviderRef: { type: "ResourceRef", description: "Reference to the IdentityProvider that the federated account belongs to.", typeDescriptionLink: "#resourceref" },
    externalSub: { type: "string", description: "External subject identifier (OIDC sub claim) — lookup key, not updatable.", required: true },
    email: { type: "string", description: "Updated email address.", required: true },
    firstName: { type: "string", description: "Updated first name." },
    lastName: { type: "string", description: "Updated last name." },
    pictureUrl: { type: "string", description: "Updated profile picture URL." },
  }}
/>

### DeprovisionFederatedAccountInput

DeprovisionFederatedAccountInput is the command for revoking a federated identity account's access, with an option to delete the account entirely.

<TypeTable
  type={{
    org: { type: "string", description: "Organization that owns the identity provider.", required: true },
    identityProviderRef: { type: "ResourceRef", description: "Reference to the IdentityProvider that the federated account belongs to.", typeDescriptionLink: "#resourceref" },
    externalSub: { type: "string", description: "External subject identifier (OIDC sub claim) — lookup key.", required: true },
    deleteAccount: { type: "boolean", description: "When false (default): revoke the account's access in this organization only." },
  }}
/>

### IdentityAccountEmail

IdentityAccountEmail identifies an identity account by its email address.

<TypeTable
  type={{
    value: { type: "string", description: "Email address of the identity account.", required: true },
  }}
/>

### ExternalSubLookup

ExternalSubLookup identifies a federated identity account by its identity provider reference and external subject identifier (OIDC sub claim).

<TypeTable
  type={{
    org: { type: "string", description: "Organization that owns the identity provider.", required: true },
    identityProviderRef: { type: "ResourceRef", description: "Reference to the IdentityProvider that the federated account belongs to.", typeDescriptionLink: "#resourceref" },
    externalSub: { type: "string", description: "External subject identifier from the platform's OIDC provider.", required: true },
  }}
/>

### IdentityAccount

IdentityAccount represents a user or machine principal in Stigmer.

<TypeTable
  type={{
    apiVersion: { type: "string", description: "API version for this resource type." },
    kind: { type: "string", description: "Resource kind identifier." },
    metadata: { type: "ApiResourceMetadata", description: "Resource metadata including id, name, org, slug, labels, visibility, and timestamps.", typeDescriptionLink: "/docs/sdk/resources/commons#apiresourcemetadata" },
    spec: { type: "IdentityAccountInput", description: "Resource specification. See [`IdentityAccountInput`](#identityaccountinput).", typeDescriptionLink: "#identityaccountinput" },
    status: { type: "IdentityAccountStatus", description: "System-managed state. See `IdentityAccountStatus`." },
  }}
/>

### IdentityAccountProvisioningMode

IdentityAccountProvisioningMode defines how an identity account was created.

| Value | Description |
|-------|-------------|
| `direct` | User signed up directly via Stigmer's own authentication tenant. |
| `federated` | Account was created by the platform for federated authentication via an IdentityProvider. |
| `machine` | Machine-to-machine client credentials account for inter-service communication. |
| `platform_client` | Account provisioned via a PlatformClient's mintUserToken endpoint. |

