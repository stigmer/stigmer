---
title: Skill
---

{/* Auto-generated by stigmer-codegen. DO NOT EDIT. */}

A Skill is a versioned, reusable unit of knowledge and tools that an agent can
use. It contains a SKILL.md file defining the skill's interface and optional
tool executables. Each version is immutably identified by a content hash.

```yaml
apiVersion: agentic.stigmer.ai/v1
kind: Skill
metadata:
  name: pdf-extractor
  slug: pdf-extractor
spec:
  name: pdf-extractor
  description: "Extracts text and tables from PDF files using OCR when needed"
  tag: stable
  skill_md: |
    ---
    name: pdf-extractor
    description: Extracts text and tables from PDF files using OCR when needed
    ---
    # PDF Extractor
    ...
```

## Client Access

<SDKTabs>
<Tab value="TypeScript">
```typescript
const skill = await stigmer.skill.get("skl_01j5q3k7m8r2s4tnz2hfp0q0c3");
```
</Tab>
<Tab value="Go">
```go
skill, err := client.Skill.Get(ctx, "skl_01j5q3k7m8r2s4tnz2hfp0q0c3")
```
</Tab>
<Tab value="Python">
```python
skill = client.skills.get("skl_01j5q3k7m8r2s4tnz2hfp0q0c3")
```
</Tab>
<Tab value="Java">
```java
Skill skill = client.skills().get("skl_01j5q3k7m8r2s4tnz2hfp0q0c3");
```
</Tab>
</SDKTabs>

## Methods

| Method | Description |
|--------|-------------|
| [`get`](#get) | Get a single skill by ID. |
| [`getArtifact`](#getartifact) | Download skill artifact from storage by its storage key. |
| [`getArtifactDownloadUrl`](#getartifactdownloadurl) | Mint a URL for downloading a skill artifact over HTTP. |
| [`getByReference`](#getbyreference) | Get a skill by API resource reference with version support. |
| [`listVersions`](#listversions) | List version history for a skill. |
| [`updateVisibility`](#updatevisibility) | Update the visibility of an existing skill. |
| [`delete`](#delete) | Delete a skill and all its versions. |
| [`createArtifactUploadUrl`](#createartifactuploadurl) | Mint a short-lived, single-use upload URL for staging a skill artifact that exceeds the gRPC message-size cap (10MB). |
| [`push`](#push) | Push a skill artifact. |
| [`pushFromExecutionArtifact`](#pushfromexecutionartifact) | Push a skill from an execution artifact already in storage. |

### get

Get a single skill by ID.

<SDKTabs>
<Tab value="TypeScript">
```typescript
const skill = await stigmer.skill.get(id);
```
</Tab>
<Tab value="Go">
```go
skill, err := client.Skill.Get(ctx, id)
```
</Tab>
<Tab value="Python">
```python
skill = client.skills.get(id)
```
</Tab>
<Tab value="Java">
```java
Skill skill = client.skills().get(id);
```
</Tab>
</SDKTabs>

**Parameters**

| Parameter | Type | Description |
|-----------|------|-------------|
| `id` | `string` | Skill identifier. |

**Returns**: [`Skill`](#skill)

### getArtifact

Download skill artifact from storage by its storage key. Returns the ZIP file containing SKILL.md and implementation files.

<SDKTabs>
<Tab value="TypeScript">
```typescript
const getArtifactResponse = await stigmer.skill.getArtifact({
  artifactStorageKey: "...",
});
```
</Tab>
<Tab value="Go">
```go
getArtifactResponse, err := client.Skill.GetArtifact(ctx, &stigmer.GetArtifactRequest{
  ArtifactStorageKey: "...",
})
```
</Tab>
<Tab value="Python">
```python
get_artifact_response = client.skills.get_artifact(GetArtifactRequest(
    artifact_storage_key="...",
))
```
</Tab>
<Tab value="Java">
```java
GetArtifactResponse getArtifactResponse = client.skills().getArtifact(GetArtifactRequest.builder()
    .artifactStorageKey("...")
    .build());
```
</Tab>
</SDKTabs>

**Parameters**: [`GetArtifactRequest`](#getartifactrequest)

**Returns**: [`GetArtifactResponse`](#getartifactresponse)

### getArtifactDownloadUrl

Mint a URL for downloading a skill artifact over HTTP.

Preferred over getArtifact for anything that might exceed the gRPC message-size cap (10MB): the bytes ride HTTP, so the full 100MB skill limit is deliverable. Callers should try this first and fall back to getArtifact against servers that predate it (UNIMPLEMENTED).

<SDKTabs>
<Tab value="TypeScript">
```typescript
const skillArtifactDownloadUrl = await stigmer.skill.getArtifactDownloadUrl({
  artifactStorageKey: "...",
});
```
</Tab>
<Tab value="Go">
```go
skillArtifactDownloadUrl, err := client.Skill.GetArtifactDownloadUrl(ctx, &stigmer.GetArtifactRequest{
  ArtifactStorageKey: "...",
})
```
</Tab>
<Tab value="Python">
```python
skill_artifact_download_url = client.skills.get_artifact_download_url(GetArtifactRequest(
    artifact_storage_key="...",
))
```
</Tab>
<Tab value="Java">
```java
SkillArtifactDownloadUrl skillArtifactDownloadUrl = client.skills().getArtifactDownloadUrl(GetArtifactRequest.builder()
    .artifactStorageKey("...")
    .build());
```
</Tab>
</SDKTabs>

**Parameters**: [`GetArtifactRequest`](#getartifactrequest)

**Returns**: [`SkillArtifactDownloadUrl`](#skillartifactdownloadurl)

### getByReference

Get a skill by API resource reference with version support.

Version resolution (via ApiResourceReference.version field): - Empty/"latest" → Returns the current version - Tag name (e.g., "stable", "v1.0") → Resolves to the version with this tag - SHA256 hash (64 hex chars) → Returns the exact immutable version

<SDKTabs>
<Tab value="TypeScript">
```typescript
const skill = await stigmer.skill.getByReference({
  org: "...",
  kind: "...",
  slug: "...",
  version: "...",
});
```
</Tab>
<Tab value="Go">
```go
skill, err := client.Skill.GetByReference(ctx, &stigmer.ApiResourceReference{
  Org:     "...",
  Kind:    "...",
  Slug:    "...",
  Version: "...",
})
```
</Tab>
<Tab value="Python">
```python
skill = client.skills.get_by_reference(ApiResourceReference(
    org="...",
    kind="...",
    slug="...",
    version="...",
))
```
</Tab>
<Tab value="Java">
```java
Skill skill = client.skills().getByReference(ApiResourceReference.builder()
    .org("...")
    .kind("...")
    .slug("...")
    .version("...")
    .build());
```
</Tab>
</SDKTabs>

**Parameters**: [`ResourceRef`](#resourceref)

**Returns**: [`Skill`](#skill)

### listVersions

List version history for a skill.

Returns all historical versions ordered by push time (newest first). Each entry includes the version hash, push timestamp, actor, tag, git provenance, and artifact storage key for historical artifact access.

<SDKTabs>
<Tab value="TypeScript">
```typescript
const listSkillVersionsResponse = await stigmer.skill.listVersions({
  org: "...",
  slug: "...",
  pageToken: "...",
  pageSize: 0,
});
```
</Tab>
<Tab value="Go">
```go
listSkillVersionsResponse, err := client.Skill.ListVersions(ctx, &stigmer.ListSkillVersionsInput{
  Org:       "...",
  Slug:      "...",
  PageToken: "...",
  PageSize:  0,
})
```
</Tab>
<Tab value="Python">
```python
list_skill_versions_response = client.skills.list_versions(ListSkillVersionsInput(
    org="...",
    slug="...",
    page_token="...",
    page_size=0,
))
```
</Tab>
<Tab value="Java">
```java
ListSkillVersionsResponse listSkillVersionsResponse = client.skills().listVersions(ListSkillVersionsInput.builder()
    .org("...")
    .slug("...")
    .pageToken("...")
    .pageSize(0)
    .build());
```
</Tab>
</SDKTabs>

**Parameters**: [`ListSkillVersionsInput`](#listskillversionsinput)

**Returns**: [`ListSkillVersionsResponse`](#listskillversionsresponse)

### updateVisibility

Update the visibility of an existing skill. Only modifies metadata.visibility, leaving spec, status, and other metadata fields untouched. Use this to make a skill publicly accessible or to revoke public access.

In the cloud edition, PUBLIC is operator-gated: public listing crosses every org boundary, so it is granted by the platform team on request. Un-publishing and all other levels stay self-service.

<SDKTabs>
<Tab value="TypeScript">
```typescript
const skill = await stigmer.skill.updateVisibility({
  resourceId: "...",
  visibility: "...",
});
```
</Tab>
<Tab value="Go">
```go
skill, err := client.Skill.UpdateVisibility(ctx, &stigmer.UpdateVisibilityInput{
  ResourceId: "...",
  Visibility: "...",
})
```
</Tab>
<Tab value="Python">
```python
skill = client.skills.update_visibility(UpdateVisibilityInput(
    resource_id="...",
    visibility="...",
))
```
</Tab>
<Tab value="Java">
```java
Skill skill = client.skills().updateVisibility(UpdateVisibilityInput.builder()
    .resourceId("...")
    .visibility("...")
    .build());
```
</Tab>
</SDKTabs>

**Parameters**: [`UpdateVisibilityInput`](#updatevisibilityinput)

**Returns**: [`Skill`](#skill)

### delete

Delete a skill and all its versions.

<SDKTabs>
<Tab value="TypeScript">
```typescript
const skill = await stigmer.skill.delete(id);
```
</Tab>
<Tab value="Go">
```go
skill, err := client.Skill.Delete(ctx, id)
```
</Tab>
<Tab value="Python">
```python
skill = client.skills.delete(id)
```
</Tab>
<Tab value="Java">
```java
Skill skill = client.skills().delete(id);
```
</Tab>
</SDKTabs>

**Parameters**

| Parameter | Type | Description |
|-----------|------|-------------|
| `id` | `string` | Skill identifier. |

**Returns**: [`Skill`](#skill)

### createArtifactUploadUrl

Mint a short-lived, single-use upload URL for staging a skill artifact that exceeds the gRPC message-size cap (10MB). Flow:

1. createArtifactUploadUrl(org, size_bytes) → \{ url, artifact_upload_ref \} 2. HTTP PUT the ZIP bytes to url 3. push(PushSkillRequest\{ artifact_upload_ref \}) — same pipeline, validation, and versioning as an inline push

The server refuses over-limit size_bytes here, before any bytes move.

<SDKTabs>
<Tab value="TypeScript">
```typescript
const skillArtifactUploadUrl = await stigmer.skill.createArtifactUploadUrl({
  org: "...",
  sizeBytes: 0,
});
```
</Tab>
<Tab value="Go">
```go
skillArtifactUploadUrl, err := client.Skill.CreateArtifactUploadUrl(ctx, &stigmer.CreateSkillArtifactUploadUrlRequest{
  Org:       "...",
  SizeBytes: 0,
})
```
</Tab>
<Tab value="Python">
```python
skill_artifact_upload_url = client.skills.create_artifact_upload_url(CreateSkillArtifactUploadUrlRequest(
    org="...",
    size_bytes=0,
))
```
</Tab>
<Tab value="Java">
```java
SkillArtifactUploadUrl skillArtifactUploadUrl = client.skills().createArtifactUploadUrl(CreateSkillArtifactUploadUrlRequest.builder()
    .org("...")
    .sizeBytes(0)
    .build());
```
</Tab>
</SDKTabs>

**Parameters**: [`CreateSkillArtifactUploadUrlRequest`](#createskillartifactuploadurlrequest)

**Returns**: [`SkillArtifactUploadUrl`](#skillartifactuploadurl)

### push

Push a skill artifact. Creates a skill if it does not exist, or creates a new version of an existing skill. The artifact must contain a SKILL.md file.

<SDKTabs>
<Tab value="TypeScript">
```typescript
const skill = await stigmer.skill.push({
  org: "...",
  artifact: "...",
  tag: "...",
  gitProvenance: {},
  message: "...",
  artifactUploadRef: "...",
});
```
</Tab>
<Tab value="Go">
```go
skill, err := client.Skill.Push(ctx, &stigmer.PushSkillRequest{
  Org:               "...",
  Artifact:          "...",
  Tag:               "...",
  GitProvenance:     &stigmer.GitProvenanceInput{},
  Message:           "...",
  ArtifactUploadRef: "...",
})
```
</Tab>
<Tab value="Python">
```python
skill = client.skills.push(PushSkillRequest(
    org="...",
    artifact="...",
    tag="...",
    git_provenance=None,
    message="...",
    artifact_upload_ref="...",
))
```
</Tab>
<Tab value="Java">
```java
Skill skill = client.skills().push(PushSkillRequest.builder()
    .org("...")
    .artifact("...")
    .tag("...")
    .gitProvenance(null)
    .message("...")
    .artifactUploadRef("...")
    .build());
```
</Tab>
</SDKTabs>

**Parameters**: [`PushSkillRequest`](#pushskillrequest)

**Returns**: [`Skill`](#skill)

### pushFromExecutionArtifact

Push a skill from an execution artifact already in storage. Use this when an agent execution has already produced a skill artifact and you want to publish it without downloading and re-uploading the ZIP.

<SDKTabs>
<Tab value="TypeScript">
```typescript
const skill = await stigmer.skill.pushFromExecutionArtifact({
  org: "...",
  executionId: "...",
  storageKey: "...",
  tag: "...",
});
```
</Tab>
<Tab value="Go">
```go
skill, err := client.Skill.PushFromExecutionArtifact(ctx, &stigmer.PushSkillFromExecutionArtifactRequest{
  Org:         "...",
  ExecutionId: "...",
  StorageKey:  "...",
  Tag:         "...",
})
```
</Tab>
<Tab value="Python">
```python
skill = client.skills.push_from_execution_artifact(PushSkillFromExecutionArtifactRequest(
    org="...",
    execution_id="...",
    storage_key="...",
    tag="...",
))
```
</Tab>
<Tab value="Java">
```java
Skill skill = client.skills().pushFromExecutionArtifact(PushSkillFromExecutionArtifactRequest.builder()
    .org("...")
    .executionId("...")
    .storageKey("...")
    .tag("...")
    .build());
```
</Tab>
</SDKTabs>

**Parameters**: [`PushSkillFromExecutionArtifactRequest`](#pushskillfromexecutionartifactrequest)

**Returns**: [`Skill`](#skill)

## Types

### SkillInput

Input for creating or updating a Skill.

<TypeTable
  type={{
    name: { type: "string", description: "Resource name.", required: true },
    slug: { type: "string", description: "URL-friendly identifier." },
    org: { type: "string", description: "Organization slug.", required: true },
    labels: { type: "Record<string, string>", description: "Key-value labels." },
    skillMd: { type: "string", description: "SKILL.md content extracted from the artifact." },
    tag: { type: "string", description: "Optional user-provided tag for this skill version." },
    description: { type: "string", description: "Human-readable description extracted from SKILL.md YAML frontmatter." },
  }}
/>

### PushSkillRequest

PushSkillRequest contains the skill artifact and metadata for upload.

<TypeTable
  type={{
    org: { type: "string", description: "Organization that owns this skill.", required: true },
    artifact: { type: "Uint8Array", description: "Skill artifact as a ZIP file (binary content)." },
    tag: { type: "string", description: "Optional tag to associate with this version." },
    gitProvenance: { type: "GitProvenanceInput", description: "Git provenance for this skill version." },
    message: { type: "string", description: "Optional human-readable message describing what changed in this version." },
    artifactUploadRef: { type: "string", description: "Reference to an artifact staged via createArtifactUploadUrl()." },
  }}
/>

### CreateSkillArtifactUploadUrlRequest

CreateSkillArtifactUploadUrlRequest asks the server to mint a staging slot for a skill artifact that is too large to travel inline in PushSkillRequest.artifact (the gRPC message cap is 10MB; skills may be up to 100MB).

<TypeTable
  type={{
    org: { type: "string", description: "Organization that will own the skill (authorization scope — the caller needs the same can_create_skill permission that push requires).", required: true },
    sizeBytes: { type: "number", description: "Declared size of the artifact ZIP in bytes." },
  }}
/>

### SkillArtifactUploadUrl

SkillArtifactUploadUrl is a short-lived, single-use capability for staging a skill artifact over HTTP.

<TypeTable
  type={{
    url: { type: "string", description: "URL to PUT the artifact ZIP bytes to." },
    artifactUploadRef: { type: "string", description: "Opaque reference to pass as PushSkillRequest.artifact_upload_ref after the PUT succeeds." },
    ttlSeconds: { type: "number", description: "Time-to-live for the upload URL in seconds." },
  }}
/>

### PushSkillFromExecutionArtifactRequest

PushSkillFromExecutionArtifactRequest publishes a skill from an execution artifact already in storage, without downloading and re-uploading the ZIP.

<TypeTable
  type={{
    org: { type: "string", description: "Organization that will own the skill.", required: true },
    executionId: { type: "string", description: "ID of the agent execution that produced the artifact (e.g., \"aex_abc123xyz456\")." },
    storageKey: { type: "string", description: "Storage key of the directory artifact (ZIP) to push as a skill." },
    tag: { type: "string", description: "Optional version tag (same semantics as PushSkillRequest.tag)." },
  }}
/>

### UpdateVisibilityInput

Input for updating the visibility of any API resource.

<TypeTable
  type={{
    resourceId: { type: "string", description: "ID of the resource whose visibility is being updated.", required: true },
    visibility: { type: "ApiResourceVisibility", description: "The new visibility setting for the resource.", typeDescriptionLink: "/docs/sdk/resources/commons#apiresourcevisibility" },
  }}
/>

### ResourceRef

Generic reference to any API resource by org and slug.

<TypeTable
  type={{
    org: { type: "string", description: "Organization that owns the referenced resource." },
    kind: { type: "ApiResourceKind", description: "Kind of the referenced resource (e.g., SKILL, AGENT, MCP_SERVER).", typeDescriptionLink: "/docs/sdk/resources/commons#apiresourcekind" },
    slug: { type: "string", description: "Resource slug (user-friendly identifier, unique within org).", required: true },
    version: { type: "string", description: "Version of the resource (optional, only applicable to versioned resources like Skills)." },
  }}
/>

### GetArtifactRequest

GetArtifactRequest requests download of a skill artifact by storage key.

<TypeTable
  type={{
    artifactStorageKey: { type: "string", description: "The artifact storage key from skill.status.artifact_storage_key.", required: true },
  }}
/>

### GetArtifactResponse

GetArtifactResponse contains the skill artifact content.

<TypeTable
  type={{
    artifact: { type: "Uint8Array", description: "The skill artifact as a ZIP file (binary content)." },
  }}
/>

### SkillArtifactDownloadUrl

SkillArtifactDownloadUrl provides a URL for downloading a skill artifact over HTTP.

<TypeTable
  type={{
    url: { type: "string", description: "URL to download the artifact ZIP via HTTP GET." },
    ttlSeconds: { type: "number", description: "Time-to-live for the download URL in seconds." },
    sizeBytes: { type: "number", description: "Size of the artifact ZIP in bytes." },
  }}
/>

### ListSkillVersionsInput

ListSkillVersionsInput requests the version history for a skill.

<TypeTable
  type={{
    org: { type: "string", description: "Organization that owns the skill.", required: true },
    slug: { type: "string", description: "Skill slug (unique within the organization).", required: true },
    pageToken: { type: "string", description: "Opaque token for cursor-based pagination." },
    pageSize: { type: "number", description: "Maximum number of versions to return per page." },
  }}
/>

### ListSkillVersionsResponse

ListSkillVersionsResponse contains a page of skill version history.

<TypeTable
  type={{
    versions: { type: "SkillVersionEntryInput[]", description: "Ordered list of versions (newest first)." },
    nextPageToken: { type: "string", description: "Token for fetching the next page." },
    totalCount: { type: "number", description: "Total number of versions across all pages." },
  }}
/>

### Skill

Skill represents a versioned, reusable unit of agent capability.

<TypeTable
  type={{
    apiVersion: { type: "string", description: "API version for this resource type." },
    kind: { type: "string", description: "Resource kind identifier." },
    metadata: { type: "ApiResourceMetadata", description: "Resource metadata including id, name, org, slug, labels, visibility, and timestamps.", typeDescriptionLink: "/docs/sdk/resources/commons#apiresourcemetadata" },
    spec: { type: "SkillInput", description: "Resource specification. See [`SkillInput`](#skillinput).", typeDescriptionLink: "#skillinput" },
    status: { type: "SkillStatus", description: "System-managed state. See [`SkillStatus`](#skillstatus).", typeDescriptionLink: "#skillstatus" },
  }}
/>

### SkillStatus

SkillStatus represents the system-managed state of a skill.

<TypeTable
  type={{
    audit: { type: "ApiResourceAudit", description: "Standard audit information tracking creation and modification.", typeDescriptionLink: "/docs/sdk/resources/commons#apiresourceaudit" },
    versionHash: { type: "string", description: "SHA256 hash of the skill artifact, used as the immutable version identifier." },
    artifactStorageKey: { type: "string", description: "Storage key for the skill artifact." },
    state: { type: "SkillState", description: "Current lifecycle state of the skill.", typeDescriptionLink: "#skillstate" },
    gitProvenance: { type: "GitProvenance", description: "Git provenance tracking where the skill artifacts originated from.", typeDescriptionLink: "#gitprovenance" },
  }}
/>

### GitProvenance

GitProvenance tracks the git origin of skill artifacts.

<TypeTable
  type={{
    remoteUrl: { type: "string", description: "Git remote URL (e.g., \"https://github.com/stigmer/stigmer.git\")." },
    ref: { type: "string", description: "Original git reference such as a branch or tag name (e.g., \"main\", \"v1.0.0\")." },
    commit: { type: "string", description: "Resolved commit SHA for exact reproducibility." },
    subdir: { type: "string", description: "Subdirectory path relative to repo root." },
  }}
/>

### SkillState

SkillState represents the lifecycle state of a skill.

| Value | Description |
|-------|-------------|
| `SKILL_STATE_UPLOADING` | Skill artifact is being uploaded to storage. |
| `SKILL_STATE_READY` | Skill is ready to be used by agents. |
| `SKILL_STATE_FAILED` | Skill upload or processing failed. |

