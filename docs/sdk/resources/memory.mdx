---
title: Memory
---

{/* Auto-generated by stigmer-codegen. DO NOT EDIT. */}

A Memory is a single fact the platform remembers about a person. An agent
proposes it during a session with the `remember` tool; it becomes active only
after the person it is about confirms it. Confirmed memories are recalled
into that person's future agent executions as background context. Every
memory is individually listable, editable, and deletable.

Memories are system-generated — you never author one as a manifest. The
shape below is what `get` and `list` return.

```yaml
apiVersion: agentic.stigmer.ai/v1
kind: Memory
metadata:
  name: prefers-terse-answers
  org: acme
spec:
  content: "Prefers terse answers with code examples over long explanations."
  subject_identity_account_id: ida_01j5q3k7m8r2s4tnz2hfp0q0c3
  provenance:
    agent_id: agt_01j5q3k7m8r2s4tnz2hfp0q0d7
    session_id: ses_01j5q3k7m8r2s4tnz2hfp0q0e1
    agent_execution_id: aex_01j5q3k7m8r2s4tnz2hfp0q0f5
    tool_call_id: call_9f2c1a
status:
  lifecycle_state: lifecycle_state_confirmed
```

## Client Access

<SDKTabs>
<Tab value="TypeScript">
```typescript
const memory = await stigmer.memory.get("mem_01j5q3k7m8r2s4tnz2hfp0q0c3");
```
</Tab>
<Tab value="Go">
```go
memory, err := client.Memory.Get(ctx, "mem_01j5q3k7m8r2s4tnz2hfp0q0c3")
```
</Tab>
<Tab value="Python">
```python
memory = client.memorys.get("mem_01j5q3k7m8r2s4tnz2hfp0q0c3")
```
</Tab>
<Tab value="Java">
```java
Memory memory = client.memories().get("mem_01j5q3k7m8r2s4tnz2hfp0q0c3");
```
</Tab>
</SDKTabs>

## Methods

| Method | Description |
|--------|-------------|
| [`get`](#get) | Get a single memory by ID. |
| [`list`](#list) | List the caller's memories in an organization. |
| [`create`](#create) | Create a memory in the proposed state. |
| [`update`](#update) | Update a memory's fact text. |
| [`delete`](#delete) | Delete a memory permanently, in any lifecycle state. |
| [`confirm`](#confirm) | Confirm a proposed memory, making it recallable. |
| [`reject`](#reject) | Reject a proposed memory, keeping it as an audit record. |

### get

Get a single memory by ID.

<SDKTabs>
<Tab value="TypeScript">
```typescript
const memory = await stigmer.memory.get(id);
```
</Tab>
<Tab value="Go">
```go
memory, err := client.Memory.Get(ctx, id)
```
</Tab>
<Tab value="Python">
```python
memory = client.memorys.get(id)
```
</Tab>
<Tab value="Java">
```java
Memory memory = client.memories().get(id);
```
</Tab>
</SDKTabs>

**Parameters**

| Parameter | Type | Description |
|-----------|------|-------------|
| `id` | `string` | Memory identifier. |

**Returns**: [`Memory`](#memory)

### list

List the caller's memories in an organization.

Returns only memories the caller can view — for memories that is always exactly the ones about the caller.

<SDKTabs>
<Tab value="TypeScript">
```typescript
const result = await stigmer.memory.list({
  org: "...",
  pageInfo: {},
});
```
</Tab>
<Tab value="Go">
```go
result, err := client.Memory.List(ctx, &stigmer.ListMemoriesRequest{
  Org:      "...",
  PageInfo: &stigmer.PageInfoInput{},
})
```
</Tab>
<Tab value="Python">
```python
result = client.memorys.list(ListMemoriesRequest(
    org="...",
    page_info=None,
))
```
</Tab>
<Tab value="Java">
```java
MemoryList result = client.memories().list(ListMemoriesRequest.builder()
    .org("...")
    .pageInfo(null)
    .build());
```
</Tab>
</SDKTabs>

**Parameters**: [`ListMemoriesRequest`](#listmemoriesrequest)

**Returns**: [`MemoryList`](#memorylist)

### create

Create a memory in the proposed state.

The memory starts its consent lifecycle as proposed: it is not recalled into any execution until the person it is about confirms it. The subject and provenance are derived by the server from the calling credential and request context — values supplied on the request are overwritten.

<SDKTabs>
<Tab value="TypeScript">
```typescript
const memory = await stigmer.memory.create({
  name: "my-memory",
  org: "acme",
  slug: "...",
  labels: {},
  content: "...",
  subjectIdentityAccountId: "...",
  provenance: {},
});
```
</Tab>
<Tab value="Go">
```go
memory, err := client.Memory.Create(ctx, &stigmer.MemoryInput{
  Name:                     "my-memory",
  Org:                      "acme",
  Slug:                     "...",
  Labels:                   map[string]string{},
  Content:                  "...",
  SubjectIdentityAccountId: "...",
  Provenance:               &stigmer.MemoryProvenanceInput{},
})
```
</Tab>
<Tab value="Python">
```python
memory = client.memorys.create(MemoryInput(
    name="my-memory",
    org="acme",
    slug="...",
    labels={},
    content="...",
    subject_identity_account_id="...",
    provenance=None,
))
```
</Tab>
<Tab value="Java">
```java
Memory memory = client.memories().create(MemoryInput.builder()
    .name("my-memory")
    .org("acme")
    .slug("...")
    .labels(Map.of())
    .content("...")
    .subjectIdentityAccountId("...")
    .provenance(null)
    .build());
```
</Tab>
</SDKTabs>

**Parameters**: [`MemoryInput`](#memoryinput)

**Returns**: [`Memory`](#memory)

### update

Update a memory's fact text.

Replaces the spec wholesale, but only the content may actually change: the subject and provenance are immutable, and the consent lifecycle in status is never touched by updates — use confirm or reject to decide on a proposal.

<SDKTabs>
<Tab value="TypeScript">
```typescript
const memory = await stigmer.memory.update({
  name: "my-memory",
  org: "acme",
  slug: "...",
  labels: {},
  content: "...",
  subjectIdentityAccountId: "...",
  provenance: {},
});
```
</Tab>
<Tab value="Go">
```go
memory, err := client.Memory.Update(ctx, &stigmer.MemoryInput{
  Name:                     "my-memory",
  Org:                      "acme",
  Slug:                     "...",
  Labels:                   map[string]string{},
  Content:                  "...",
  SubjectIdentityAccountId: "...",
  Provenance:               &stigmer.MemoryProvenanceInput{},
})
```
</Tab>
<Tab value="Python">
```python
memory = client.memorys.update(MemoryInput(
    name="my-memory",
    org="acme",
    slug="...",
    labels={},
    content="...",
    subject_identity_account_id="...",
    provenance=None,
))
```
</Tab>
<Tab value="Java">
```java
Memory memory = client.memories().update(MemoryInput.builder()
    .name("my-memory")
    .org("acme")
    .slug("...")
    .labels(Map.of())
    .content("...")
    .subjectIdentityAccountId("...")
    .provenance(null)
    .build());
```
</Tab>
</SDKTabs>

**Parameters**: [`MemoryInput`](#memoryinput)

**Returns**: [`Memory`](#memory)

### delete

Delete a memory permanently, in any lifecycle state.

Deletion is the retention mechanism: deleting a confirmed memory is how consent is revoked, and the fact stops reaching future executions immediately. Past executions that already recalled it keep their immutable snapshots.

<SDKTabs>
<Tab value="TypeScript">
```typescript
const memory = await stigmer.memory.delete(id);
```
</Tab>
<Tab value="Go">
```go
memory, err := client.Memory.Delete(ctx, id)
```
</Tab>
<Tab value="Python">
```python
memory = client.memorys.delete(id)
```
</Tab>
<Tab value="Java">
```java
Memory memory = client.memories().delete(id);
```
</Tab>
</SDKTabs>

**Parameters**

| Parameter | Type | Description |
|-----------|------|-------------|
| `id` | `string` | Memory identifier. |

**Returns**: [`Memory`](#memory)

### confirm

Confirm a proposed memory, making it recallable.

Confirmation is the consent act: from the next eligible execution on, the fact is injected as background context. Confirming an already-confirmed memory succeeds and changes nothing. Confirming a rejected memory is refused — delete it instead and let the agent propose again.

<SDKTabs>
<Tab value="TypeScript">
```typescript
const memory = await stigmer.memory.confirm(id);
```
</Tab>
<Tab value="Go">
```go
memory, err := client.Memory.Confirm(ctx, id)
```
</Tab>
<Tab value="Python">
```python
memory = client.memorys.confirm(id)
```
</Tab>
<Tab value="Java">
```java
Memory memory = client.memories().confirm(id);
```
</Tab>
</SDKTabs>

**Parameters**

| Parameter | Type | Description |
|-----------|------|-------------|
| `id` | `string` | Memory identifier. |

**Returns**: [`Memory`](#memory)

### reject

Reject a proposed memory, keeping it as an audit record.

A rejected memory is never recalled. The record is kept rather than deleted so the decision is auditable; delete it to remove it entirely. Rejecting an already-rejected memory succeeds and changes nothing. Rejecting a confirmed memory is refused — deleting it is how a confirmed fact is revoked.

<SDKTabs>
<Tab value="TypeScript">
```typescript
const memory = await stigmer.memory.reject(id);
```
</Tab>
<Tab value="Go">
```go
memory, err := client.Memory.Reject(ctx, id)
```
</Tab>
<Tab value="Python">
```python
memory = client.memorys.reject(id)
```
</Tab>
<Tab value="Java">
```java
Memory memory = client.memories().reject(id);
```
</Tab>
</SDKTabs>

**Parameters**

| Parameter | Type | Description |
|-----------|------|-------------|
| `id` | `string` | Memory identifier. |

**Returns**: [`Memory`](#memory)

## Types

### MemoryInput

Input for creating or updating a Memory.

<TypeTable
  type={{
    name: { type: "string", description: "Resource name.", required: true },
    slug: { type: "string", description: "URL-friendly identifier." },
    org: { type: "string", description: "Organization slug.", required: true },
    labels: { type: "Record<string, string>", description: "Key-value labels." },
    content: { type: "string", description: "The remembered fact, verbatim." },
    subjectIdentityAccountId: { type: "string", description: "The identity account this memory is about." },
    provenance: { type: "MemoryProvenanceInput", description: "Where this fact came from — displayed beside the fact everywhere it appears.", typeDescriptionLink: "#memoryprovenanceinput" },
  }}
/>

### MemoryProvenanceInput

MemoryProvenance records which agent proposed a memory and in which conversation.

<TypeTable
  type={{
    agentId: { type: "string", description: "ID of the agent that proposed this memory." },
    sessionId: { type: "string", description: "ID of the session in which this memory was proposed." },
    agentExecutionId: { type: "string", description: "ID of the agent execution in which this memory was proposed." },
    toolCallId: { type: "string", description: "ID of the remember tool call that proposed this memory." },
  }}
/>

### ListMemoriesRequest

Input for listing memories within an organization.

<TypeTable
  type={{
    org: { type: "string", description: "Organization slug to scope the listing." },
    pageInfo: { type: "PageInfoInput", description: "Pagination options." },
  }}
/>

### MemoryList

Response containing a paginated list of memories.

<TypeTable
  type={{
    totalCount: { type: "number", description: "Total number of memories matching the query." },
    items: { type: "MemoryInput[]", description: "Memories in the current page.", typeDescriptionLink: "#memoryinput" },
  }}
/>

### Memory

Memory is a single fact the platform remembers about a person.

<TypeTable
  type={{
    apiVersion: { type: "string", description: "API version for this resource type." },
    kind: { type: "string", description: "Resource kind identifier." },
    metadata: { type: "ApiResourceMetadata", description: "Resource metadata including id, name, org, slug, labels, visibility, and timestamps.", typeDescriptionLink: "/docs/sdk/resources/commons#apiresourcemetadata" },
    spec: { type: "MemoryInput", description: "Resource specification. See [`MemoryInput`](#memoryinput).", typeDescriptionLink: "#memoryinput" },
    status: { type: "MemoryStatus", description: "System-managed state. See [`MemoryStatus`](#memorystatus).", typeDescriptionLink: "#memorystatus" },
  }}
/>

### MemoryStatus

MemoryStatus contains system-managed state for a memory.

<TypeTable
  type={{
    lifecycleState: { type: "MemoryLifecycleState", description: "Where this memory is in its consent lifecycle.", typeDescriptionLink: "#memorylifecyclestate" },
    stateChangedAt: { type: "string", description: "When the lifecycle state last changed." },
    audit: { type: "ApiResourceAudit", description: "Standard audit information (created_at, updated_at, created_by, etc.).", typeDescriptionLink: "/docs/sdk/resources/commons#apiresourceaudit" },
  }}
/>

### MemoryLifecycleState

MemoryLifecycleState tracks a memory's consent lifecycle.

| Value | Description |
|-------|-------------|
| `lifecycle_state_proposed` | Proposed by an agent; awaiting the subject's decision. |
| `lifecycle_state_confirmed` | Confirmed by the subject. |
| `lifecycle_state_rejected` | Rejected by the subject. |

