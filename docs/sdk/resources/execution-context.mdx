---
title: Execution Context
---

{/* Auto-generated by stigmer-codegen. DO NOT EDIT. */}

An ExecutionContext holds ephemeral runtime configuration and secrets for a
single AgentExecution or WorkflowExecution. The execution engine creates it at
start and deletes it when the execution completes.

```yaml
apiVersion: agentic.stigmer.ai/v1
kind: ExecutionContext
metadata:
  name: exec-ctx-aex-abc123
  org: acme-corp
spec:
  execution_id: "aex_abc123"
  data:
    AWS_REGION:
      value: "us-east-1"
      is_secret: false
    AWS_ACCESS_KEY_ID:
      value: "AKIAIOSFODNN7EXAMPLE"
      is_secret: true
```

## Client Access

<SDKTabs>
<Tab value="TypeScript">
```typescript
const executionContext = await stigmer.executionContext.get("ectx_01j5q3k7m8r2s4tnz2hfp0q0c3");
```
</Tab>
<Tab value="Go">
```go
executionContext, err := client.ExecutionContext.Get(ctx, "ectx_01j5q3k7m8r2s4tnz2hfp0q0c3")
```
</Tab>
<Tab value="Python">
```python
execution_context = client.execution_contexts.get("ectx_01j5q3k7m8r2s4tnz2hfp0q0c3")
```
</Tab>
<Tab value="Java">
```java
ExecutionContext executionContext = client.executionContexts().get("ectx_01j5q3k7m8r2s4tnz2hfp0q0c3");
```
</Tab>
</SDKTabs>

## Methods

| Method | Description |
|--------|-------------|
| [`get`](#get) | Get an ExecutionContext by ID. |
| [`getByExecutionId`](#getbyexecutionid) | Get the ExecutionContext for a given execution ID. |
| [`getByReference`](#getbyreference) | Get an ExecutionContext by reference (slug-based lookup). |
| [`apply`](#apply) | Create or update an ExecutionContext. |
| [`create`](#create) | Create a new ExecutionContext for an execution. |
| [`delete`](#delete) | Delete an ExecutionContext. |

### get

Get an ExecutionContext by ID.

<SDKTabs>
<Tab value="TypeScript">
```typescript
const executionContext = await stigmer.executionContext.get(id);
```
</Tab>
<Tab value="Go">
```go
executionContext, err := client.ExecutionContext.Get(ctx, id)
```
</Tab>
<Tab value="Python">
```python
execution_context = client.execution_contexts.get(id)
```
</Tab>
<Tab value="Java">
```java
ExecutionContext executionContext = client.executionContexts().get(id);
```
</Tab>
</SDKTabs>

**Parameters**

| Parameter | Type | Description |
|-----------|------|-------------|
| `id` | `string` | Execution Context identifier. |

**Returns**: [`ExecutionContext`](#executioncontext)

### getByExecutionId

Get the ExecutionContext for a given execution ID.

<SDKTabs>
<Tab value="TypeScript">
```typescript
const executionContext = await stigmer.executionContext.getByExecutionId({
  executionId: "...",
});
```
</Tab>
<Tab value="Go">
```go
executionContext, err := client.ExecutionContext.GetByExecutionId(ctx, &stigmer.ExecutionContextExecutionIdInput{
  ExecutionId: "...",
})
```
</Tab>
<Tab value="Python">
```python
execution_context = client.execution_contexts.get_by_execution_id(ExecutionContextExecutionIdInput(
    execution_id="...",
))
```
</Tab>
<Tab value="Java">
```java
ExecutionContext executionContext = client.executionContexts().getByExecutionId(ExecutionContextExecutionIdInput.builder()
    .executionId("...")
    .build());
```
</Tab>
</SDKTabs>

**Parameters**: [`ExecutionContextExecutionIdInput`](#executioncontextexecutionidinput)

**Returns**: [`ExecutionContext`](#executioncontext)

### getByReference

Get an ExecutionContext by reference (slug-based lookup).

<SDKTabs>
<Tab value="TypeScript">
```typescript
const executionContext = await stigmer.executionContext.getByReference({
  org: "...",
  kind: "...",
  slug: "...",
  version: "...",
});
```
</Tab>
<Tab value="Go">
```go
executionContext, err := client.ExecutionContext.GetByReference(ctx, &stigmer.ApiResourceReference{
  Org:     "...",
  Kind:    "...",
  Slug:    "...",
  Version: "...",
})
```
</Tab>
<Tab value="Python">
```python
execution_context = client.execution_contexts.get_by_reference(ApiResourceReference(
    org="...",
    kind="...",
    slug="...",
    version="...",
))
```
</Tab>
<Tab value="Java">
```java
ExecutionContext executionContext = client.executionContexts().getByReference(ApiResourceReference.builder()
    .org("...")
    .kind("...")
    .slug("...")
    .version("...")
    .build());
```
</Tab>
</SDKTabs>

**Parameters**: [`ResourceRef`](#resourceref)

**Returns**: [`ExecutionContext`](#executioncontext)

### apply

Create or update an ExecutionContext.

<SDKTabs>
<Tab value="TypeScript">
```typescript
const executionContext = await stigmer.executionContext.apply({
  name: "my-execution-context",
  org: "acme",
  slug: "...",
  labels: {},
  executionId: "...",
  data: {},
});
```
</Tab>
<Tab value="Go">
```go
executionContext, err := client.ExecutionContext.Apply(ctx, &stigmer.ExecutionContextInput{
  Name:        "my-execution-context",
  Org:         "acme",
  Slug:        "...",
  Labels:      map[string]string{},
  ExecutionId: "...",
  Data:        map[string]string{},
})
```
</Tab>
<Tab value="Python">
```python
execution_context = client.execution_contexts.apply(ExecutionContextInput(
    name="my-execution-context",
    org="acme",
    slug="...",
    labels={},
    execution_id="...",
    data={},
))
```
</Tab>
<Tab value="Java">
```java
ExecutionContext executionContext = client.executionContexts().apply(ExecutionContextInput.builder()
    .name("my-execution-context")
    .org("acme")
    .slug("...")
    .labels(Map.of())
    .executionId("...")
    .data(Map.of())
    .build());
```
</Tab>
</SDKTabs>

**Parameters**: [`ExecutionContextInput`](#executioncontextinput)

**Returns**: [`ExecutionContext`](#executioncontext)

### create

Create a new ExecutionContext for an execution.

<SDKTabs>
<Tab value="TypeScript">
```typescript
const executionContext = await stigmer.executionContext.create({
  name: "my-execution-context",
  org: "acme",
  slug: "...",
  labels: {},
  executionId: "...",
  data: {},
});
```
</Tab>
<Tab value="Go">
```go
executionContext, err := client.ExecutionContext.Create(ctx, &stigmer.ExecutionContextInput{
  Name:        "my-execution-context",
  Org:         "acme",
  Slug:        "...",
  Labels:      map[string]string{},
  ExecutionId: "...",
  Data:        map[string]string{},
})
```
</Tab>
<Tab value="Python">
```python
execution_context = client.execution_contexts.create(ExecutionContextInput(
    name="my-execution-context",
    org="acme",
    slug="...",
    labels={},
    execution_id="...",
    data={},
))
```
</Tab>
<Tab value="Java">
```java
ExecutionContext executionContext = client.executionContexts().create(ExecutionContextInput.builder()
    .name("my-execution-context")
    .org("acme")
    .slug("...")
    .labels(Map.of())
    .executionId("...")
    .data(Map.of())
    .build());
```
</Tab>
</SDKTabs>

**Parameters**: [`ExecutionContextInput`](#executioncontextinput)

**Returns**: [`ExecutionContext`](#executioncontext)

### delete

Delete an ExecutionContext.

<SDKTabs>
<Tab value="TypeScript">
```typescript
const executionContext = await stigmer.executionContext.delete({ resourceId: id });
```
</Tab>
<Tab value="Go">
```go
executionContext, err := client.ExecutionContext.Delete(ctx, &stigmer.DeleteResourceInput{
  ResourceID: id,
})
```
</Tab>
<Tab value="Python">
```python
execution_context = client.execution_contexts.delete(DeleteResourceInput(resource_id=id))
```
</Tab>
<Tab value="Java">
```java
ExecutionContext executionContext = client.executionContexts().delete(DeleteResourceInput.builder()
    .resourceId(id)
    .build());
```
</Tab>
</SDKTabs>

**Parameters**

| Parameter | Type | Description |
|-----------|------|-------------|
| `resourceId` | `string` | ID of the resource to delete. |
| `versionMessage` | `string` | Optional deletion reason. |
| `force` | `boolean` | Force delete even if resource is in use. |

**Returns**: [`ExecutionContext`](#executioncontext)

## Types

### ExecutionContextInput

Input for creating or updating an Execution Context.

<TypeTable
  type={{
    name: { type: "string", description: "Resource name.", required: true },
    slug: { type: "string", description: "URL-friendly identifier." },
    org: { type: "string", description: "Organization slug.", required: true },
    labels: { type: "Record<string, string>", description: "Key-value labels." },
    executionId: { type: "string", description: "ID of the parent AgentExecution or WorkflowExecution." },
    data: { type: "Record<string, EnvVarInput>", description: "Runtime key-value pairs, each marked as secret or plaintext.", typeDescriptionLink: "#envvarinput" },
  }}
/>

### EnvVarInput

A single runtime configuration or secret value.

<TypeTable
  type={{
    value: { type: "string", description: "String content of this entry." },
    isSecret: { type: "boolean", description: "Whether this value should be treated as a secret." },
  }}
/>

### ResourceRef

Generic reference to any API resource by org and slug.

<TypeTable
  type={{
    org: { type: "string", description: "Organization that owns the referenced resource." },
    kind: { type: "ApiResourceKind", description: "Kind of the referenced resource (e.g., SKILL, AGENT, MCP_SERVER).", typeDescriptionLink: "/docs/sdk/resources/commons#apiresourcekind" },
    slug: { type: "string", description: "Resource slug (user-friendly identifier, unique within org).", required: true },
    version: { type: "string", description: "Version of the resource (optional, only applicable to versioned resources like Skills)." },
  }}
/>

### ExecutionContextExecutionIdInput

Input for looking up an ExecutionContext by its parent execution ID.

<TypeTable
  type={{
    executionId: { type: "string", description: "AgentExecution or WorkflowExecution ID to look up." },
  }}
/>

### ExecutionContext

Ephemeral runtime configuration and secrets scoped to a single execution.

<TypeTable
  type={{
    apiVersion: { type: "string", description: "API version for this resource type." },
    kind: { type: "string", description: "Resource kind identifier." },
    metadata: { type: "ApiResourceMetadata", description: "Resource metadata including id, name, org, slug, labels, visibility, and timestamps.", typeDescriptionLink: "/docs/sdk/resources/commons#apiresourcemetadata" },
    spec: { type: "ExecutionContextInput", description: "Resource specification. See [`ExecutionContextInput`](#executioncontextinput).", typeDescriptionLink: "#executioncontextinput" },
    status: { type: "ExecutionContextStatus", description: "System-managed state. See `ExecutionContextStatus`." },
  }}
/>

