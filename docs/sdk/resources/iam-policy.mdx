---
title: IAM Policy
---

{/* Auto-generated by stigmer-codegen. DO NOT EDIT. */}

An IamPolicy grants a specific permission to a principal on a resource. It
binds three elements: WHO (principal), WHAT (resource), and HOW (relation).
Policies are the source of truth for authorization in Stigmer.

```yaml
apiVersion: iam.stigmer.ai/v1
kind: IamPolicy
metadata:
  name: alice-org-admin
  org: acme
spec:
  principal:
    kind: identity_account
    id: ia_alice-123
  resource:
    kind: organization
    id: org_acme-456
  relation: admin
```

## Client Access

<SDKTabs>
<Tab value="TypeScript">
```typescript
const iamPolicy = await stigmer.iamPolicy.get("iamp_01j5q3k7m8r2s4tnz2hfp0q0c3");
```
</Tab>
<Tab value="Go">
```go
iamPolicy, err := client.IamPolicy.Get(ctx, "iamp_01j5q3k7m8r2s4tnz2hfp0q0c3")
```
</Tab>
<Tab value="Python">
```python
iam_policy = client.iam_policies.get("iamp_01j5q3k7m8r2s4tnz2hfp0q0c3")
```
</Tab>
<Tab value="Java">
```java
IamPolicy iamPolicy = client.iamPolicies().get("iamp_01j5q3k7m8r2s4tnz2hfp0q0c3");
```
</Tab>
</SDKTabs>

## Methods

| Method | Description |
|--------|-------------|
| [`get`](#get) | Get an IAM policy by its unique identifier. |
| [`getPrincipalResourceRoles`](#getprincipalresourceroles) | Get all roles a specific principal has on a specific resource. |
| [`getPrincipalsCount`](#getprincipalscount) | Count distinct principals that have access to a resource. |
| [`listAuthorizedPrincipalIds`](#listauthorizedprincipalids) | List all principal IDs of a specific kind that are authorized to access a resource. |
| [`listAuthorizedResourceIds`](#listauthorizedresourceids) | List all resource IDs of a specific kind that a principal is authorized to access. |
| [`listResourceAccessByPrincipal`](#listresourceaccessbyprincipal) | List all principals and their roles on a resource, grouped by principal. |
| [`create`](#create) | Create a new IAM policy. |
| [`delete`](#delete) | Delete a single IAM policy by spec. |
| [`bootstrapPolicy`](#bootstrappolicy) | Bootstrap IAM policy during resource creation. |
| [`checkAuthorization`](#checkauthorization) | Check if a principal is authorized to perform a relation on a resource. |
| [`cleanupResourcePolicies`](#cleanupresourcepolicies) | Cleanup all IAM policies for a deleted resource. |
| [`revokeOrgAccess`](#revokeorgaccess) | Revoke all of a user's access to an organization. |

### get

Get an IAM policy by its unique identifier.

Returns the full IAM policy including its principal, resource, and relation binding.

<SDKTabs>
<Tab value="TypeScript">
```typescript
const iamPolicy = await stigmer.iamPolicy.get(id);
```
</Tab>
<Tab value="Go">
```go
iamPolicy, err := client.IamPolicy.Get(ctx, id)
```
</Tab>
<Tab value="Python">
```python
iam_policy = client.iam_policies.get(id)
```
</Tab>
<Tab value="Java">
```java
IamPolicy iamPolicy = client.iamPolicies().get(id);
```
</Tab>
</SDKTabs>

**Parameters**

| Parameter | Type | Description |
|-----------|------|-------------|
| `id` | `string` | IAM Policy identifier. |

**Returns**: [`IamPolicy`](#iampolicy)

### getPrincipalResourceRoles

Get all roles a specific principal has on a specific resource.

This RPC answers: "What roles does [principal] have on [resource]?" Returns role metadata (code, display name, description) for each assigned role.

Use Cases: - Displaying a user's current role in a resource detail view - Pre-populating role selectors when editing access - Permission summary for a specific user-resource pair

Input: PrincipalResourceInput with principal and resource refs Output: PrincipalResourceRoles with list of RoleInfo entries

<SDKTabs>
<Tab value="TypeScript">
```typescript
const principalResourceRoles = await stigmer.iamPolicy.getPrincipalResourceRoles({
  principal: {},
  resource: {},
});
```
</Tab>
<Tab value="Go">
```go
principalResourceRoles, err := client.IamPolicy.GetPrincipalResourceRoles(ctx, &stigmer.PrincipalResourceInput{
  Principal: &stigmer.ApiResourceRefInput{},
  Resource:  &stigmer.ApiResourceRefInput{},
})
```
</Tab>
<Tab value="Python">
```python
principal_resource_roles = client.iam_policies.get_principal_resource_roles(PrincipalResourceInput(
    principal=None,
    resource=None,
))
```
</Tab>
<Tab value="Java">
```java
PrincipalResourceRoles principalResourceRoles = client.iamPolicies().getPrincipalResourceRoles(PrincipalResourceInput.builder()
    .principal(null)
    .resource(null)
    .build());
```
</Tab>
</SDKTabs>

**Parameters**: [`PrincipalResourceInput`](#principalresourceinput)

**Returns**: [`PrincipalResourceRoles`](#principalresourceroles)

### getPrincipalsCount

Count distinct principals that have access to a resource.

This RPC answers: "How many [principal-kind] have access to this organization?" Used for member count badges and summary statistics.

Use Cases: - Organization members count badge in navigation - Settings page member summary

Input: GetPrincipalsCountInput with org_id and principal_kind Output: PrincipalsCount with integer count

<SDKTabs>
<Tab value="TypeScript">
```typescript
const principalsCount = await stigmer.iamPolicy.getPrincipalsCount({
  orgId: "...",
  principalKind: "...",
});
```
</Tab>
<Tab value="Go">
```go
principalsCount, err := client.IamPolicy.GetPrincipalsCount(ctx, &stigmer.GetPrincipalsCountInput{
  OrgId:         "...",
  PrincipalKind: "...",
})
```
</Tab>
<Tab value="Python">
```python
principals_count = client.iam_policies.get_principals_count(GetPrincipalsCountInput(
    org_id="...",
    principal_kind="...",
))
```
</Tab>
<Tab value="Java">
```java
PrincipalsCount principalsCount = client.iamPolicies().getPrincipalsCount(GetPrincipalsCountInput.builder()
    .orgId("...")
    .principalKind("...")
    .build());
```
</Tab>
</SDKTabs>

**Parameters**: [`GetPrincipalsCountInput`](#getprincipalscountinput)

**Returns**: [`PrincipalsCount`](#principalscount)

### listAuthorizedPrincipalIds

List all principal IDs of a specific kind that are authorized to access a resource

This RPC answers: "What are all the [principal-kind] that have [relation] access on [resource]?" Inverse of listAuthorizedResourceIds.

Use Cases: - Resource access audit (who can access this?) - Team discovery for resources - Compliance and security audits

Input: ListAuthorizedPrincipalIdsInput with resource, principal_kind, and relation Output: AuthorizedPrincipalIdsList containing all authorized principal IDs

<SDKTabs>
<Tab value="TypeScript">
```typescript
const result = await stigmer.iamPolicy.listAuthorizedPrincipalIds({
  resource: {},
  principalKind: "...",
  relation: "...",
  contextualPolicies: [],
});
```
</Tab>
<Tab value="Go">
```go
result, err := client.IamPolicy.ListAuthorizedPrincipalIds(ctx, &stigmer.ListAuthorizedPrincipalIdsInput{
  Resource:           &stigmer.ApiResourceRefInput{},
  PrincipalKind:      "...",
  Relation:           "...",
  ContextualPolicies: []stigmer.IamPolicySpecInput{},
})
```
</Tab>
<Tab value="Python">
```python
result = client.iam_policies.list_authorized_principal_ids(ListAuthorizedPrincipalIdsInput(
    resource=None,
    principal_kind="...",
    relation="...",
    contextual_policies=[],
))
```
</Tab>
<Tab value="Java">
```java
AuthorizedPrincipalIdsList result = client.iamPolicies().listAuthorizedPrincipalIds(ListAuthorizedPrincipalIdsInput.builder()
    .resource(null)
    .principalKind("...")
    .relation("...")
    .contextualPolicies(List.of())
    .build());
```
</Tab>
</SDKTabs>

**Parameters**: [`ListAuthorizedPrincipalIdsInput`](#listauthorizedprincipalidsinput)

**Returns**: [`AuthorizedPrincipalIdsList`](#authorizedprincipalidslist)

### listAuthorizedResourceIds

List all resource IDs of a specific kind that a principal is authorized to access

This RPC answers: "What are all the [resource-kind] that [principal] can [relation] on?" Essential for building permission-aware UIs and filtering resource lists.

Use Cases: - Resource list filtering in dropdowns - Permission-based navigation - Bulk authorization checks

Input: ListAuthorizedResourceIdsInput with principal, resource_kind, and relation Output: AuthorizedResourceIdsList containing all accessible resource IDs

<SDKTabs>
<Tab value="TypeScript">
```typescript
const result = await stigmer.iamPolicy.listAuthorizedResourceIds({
  principal: {},
  resourceKind: "...",
  relation: "...",
  contextualPolicies: [],
});
```
</Tab>
<Tab value="Go">
```go
result, err := client.IamPolicy.ListAuthorizedResourceIds(ctx, &stigmer.ListAuthorizedResourceIdsInput{
  Principal:          &stigmer.ApiResourceRefInput{},
  ResourceKind:       "...",
  Relation:           "...",
  ContextualPolicies: []stigmer.IamPolicySpecInput{},
})
```
</Tab>
<Tab value="Python">
```python
result = client.iam_policies.list_authorized_resource_ids(ListAuthorizedResourceIdsInput(
    principal=None,
    resource_kind="...",
    relation="...",
    contextual_policies=[],
))
```
</Tab>
<Tab value="Java">
```java
AuthorizedResourceIdsList result = client.iamPolicies().listAuthorizedResourceIds(ListAuthorizedResourceIdsInput.builder()
    .principal(null)
    .resourceKind("...")
    .relation("...")
    .contextualPolicies(List.of())
    .build());
```
</Tab>
</SDKTabs>

**Parameters**: [`ListAuthorizedResourceIdsInput`](#listauthorizedresourceidsinput)

**Returns**: [`AuthorizedResourceIdsList`](#authorizedresourceidslist)

### listResourceAccessByPrincipal

List all principals and their roles on a resource, grouped by principal.

This RPC answers: "Who has access to this resource, and what roles do they have?" Returns each principal with full display information and all their role grants, optionally including roles inherited from parent resources.

Use Cases: - Organization members page (show all users and their roles) - Resource "Share" dialog (show who already has access) - Access audit views

Input: ListResourceAccessInput with resource ref and include_inherited flag Output: ResourceAccessByPrincipalList with PrincipalAccess entries

<SDKTabs>
<Tab value="TypeScript">
```typescript
const result = await stigmer.iamPolicy.listResourceAccessByPrincipal({
  resource: {},
  includeInherited: false,
});
```
</Tab>
<Tab value="Go">
```go
result, err := client.IamPolicy.ListResourceAccessByPrincipal(ctx, &stigmer.ListResourceAccessInput{
  Resource:         &stigmer.ApiResourceRefInput{},
  IncludeInherited: false,
})
```
</Tab>
<Tab value="Python">
```python
result = client.iam_policies.list_resource_access_by_principal(ListResourceAccessInput(
    resource=None,
    include_inherited=False,
))
```
</Tab>
<Tab value="Java">
```java
ResourceAccessByPrincipalList result = client.iamPolicies().listResourceAccessByPrincipal(ListResourceAccessInput.builder()
    .resource(null)
    .includeInherited(false)
    .build());
```
</Tab>
</SDKTabs>

**Parameters**: [`ListResourceAccessInput`](#listresourceaccessinput)

**Returns**: [`ResourceAccessByPrincipalList`](#resourceaccessbyprincipallist)

### create

Create a new IAM policy

Creates a single IAM policy that grants a principal access to a resource with a specific relation. This is the fundamental operation for establishing permissions.

<SDKTabs>
<Tab value="TypeScript">
```typescript
const iamPolicy = await stigmer.iamPolicy.create({
  principal: {},
  resource: {},
  relation: "...",
});
```
</Tab>
<Tab value="Go">
```go
iamPolicy, err := client.IamPolicy.Create(ctx, &stigmer.IamPolicySpec{
  Principal: &stigmer.ApiResourceRefInput{},
  Resource:  &stigmer.ApiResourceRefInput{},
  Relation:  "...",
})
```
</Tab>
<Tab value="Python">
```python
iam_policy = client.iam_policies.create(IamPolicySpec(
    principal=None,
    resource=None,
    relation="...",
))
```
</Tab>
<Tab value="Java">
```java
IamPolicy iamPolicy = client.iamPolicies().create(IamPolicySpec.builder()
    .principal(null)
    .resource(null)
    .relation("...")
    .build());
```
</Tab>
</SDKTabs>

**Parameters**: [`IamPolicySpec`](#iampolicyspec)

**Returns**: [`IamPolicy`](#iampolicy)

### delete

Delete a single IAM policy by spec

Removes an existing IAM policy by matching the principal, resource, and relation. This is a surgical operation — it removes one specific policy without affecting others.

<SDKTabs>
<Tab value="TypeScript">
```typescript
const iamPolicy = await stigmer.iamPolicy.delete({
  principal: {},
  resource: {},
  relation: "...",
});
```
</Tab>
<Tab value="Go">
```go
iamPolicy, err := client.IamPolicy.Delete(ctx, &stigmer.IamPolicySpec{
  Principal: &stigmer.ApiResourceRefInput{},
  Resource:  &stigmer.ApiResourceRefInput{},
  Relation:  "...",
})
```
</Tab>
<Tab value="Python">
```python
iam_policy = client.iam_policies.delete(IamPolicySpec(
    principal=None,
    resource=None,
    relation="...",
))
```
</Tab>
<Tab value="Java">
```java
IamPolicy iamPolicy = client.iamPolicies().delete(IamPolicySpec.builder()
    .principal(null)
    .resource(null)
    .relation("...")
    .build());
```
</Tab>
</SDKTabs>

**Parameters**: [`IamPolicySpec`](#iampolicyspec)

**Returns**: [`IamPolicy`](#iampolicy)

### bootstrapPolicy

Bootstrap IAM policy during resource creation

Creates IAM policies during resource creation when standard authorization cannot work yet because no tuples exist.

<SDKTabs>
<Tab value="TypeScript">
```typescript
const iamPolicy = await stigmer.iamPolicy.bootstrapPolicy({
  principal: {},
  resource: {},
  relation: "...",
});
```
</Tab>
<Tab value="Go">
```go
iamPolicy, err := client.IamPolicy.BootstrapPolicy(ctx, &stigmer.IamPolicySpec{
  Principal: &stigmer.ApiResourceRefInput{},
  Resource:  &stigmer.ApiResourceRefInput{},
  Relation:  "...",
})
```
</Tab>
<Tab value="Python">
```python
iam_policy = client.iam_policies.bootstrap_policy(IamPolicySpec(
    principal=None,
    resource=None,
    relation="...",
))
```
</Tab>
<Tab value="Java">
```java
IamPolicy iamPolicy = client.iamPolicies().bootstrapPolicy(IamPolicySpec.builder()
    .principal(null)
    .resource(null)
    .relation("...")
    .build());
```
</Tab>
</SDKTabs>

**Parameters**: [`IamPolicySpec`](#iampolicyspec)

**Returns**: [`IamPolicy`](#iampolicy)

### checkAuthorization

Check if a principal is authorized to perform a relation on a resource

This is the fundamental authorization check RPC that answers the question: "Does principal X have permission Y on resource Z?"

It provides a simple boolean answer based on the complete authorization state, including existing IAM policies, inherited permissions, and group memberships.

Use Cases: - Pre-flight UI checks before showing buttons/actions - API request authorization before processing operations - Service-to-service authorization - Team-based access checks

Input: CheckAuthorizationInput with policy spec and optional contextual policies Output: CheckAuthorizationResult with is_authorized boolean

<SDKTabs>
<Tab value="TypeScript">
```typescript
const checkAuthorizationResult = await stigmer.iamPolicy.checkAuthorization({
  policy: {},
  contextualPolicies: [],
});
```
</Tab>
<Tab value="Go">
```go
checkAuthorizationResult, err := client.IamPolicy.CheckAuthorization(ctx, &stigmer.CheckAuthorizationInput{
  Policy:             &stigmer.IamPolicySpecInput{},
  ContextualPolicies: []stigmer.IamPolicySpecInput{},
})
```
</Tab>
<Tab value="Python">
```python
check_authorization_result = client.iam_policies.check_authorization(CheckAuthorizationInput(
    policy=None,
    contextual_policies=[],
))
```
</Tab>
<Tab value="Java">
```java
CheckAuthorizationResult checkAuthorizationResult = client.iamPolicies().checkAuthorization(CheckAuthorizationInput.builder()
    .policy(null)
    .contextualPolicies(List.of())
    .build());
```
</Tab>
</SDKTabs>

**Parameters**: [`CheckAuthorizationInput`](#checkauthorizationinput)

**Returns**: [`CheckAuthorizationResult`](#checkauthorizationresult)

### cleanupResourcePolicies

Cleanup all IAM policies for a deleted resource.

Removes all IAM policies associated with a deleted resource.

<SDKTabs>
<Tab value="TypeScript">
```typescript
const empty = await stigmer.iamPolicy.cleanupResourcePolicies({
  kind: "...",
  id: "...",
  relation: "...",
});
```
</Tab>
<Tab value="Go">
```go
empty, err := client.IamPolicy.CleanupResourcePolicies(ctx, &stigmer.ApiResourceRef{
  Kind:     "...",
  Id:       "...",
  Relation: "...",
})
```
</Tab>
<Tab value="Python">
```python
empty = client.iam_policies.cleanup_resource_policies(ApiResourceRef(
    kind="...",
    id="...",
    relation="...",
))
```
</Tab>
<Tab value="Java">
```java
Empty empty = client.iamPolicies().cleanupResourcePolicies(ApiResourceRef.builder()
    .kind("...")
    .id("...")
    .relation("...")
    .build());
```
</Tab>
</SDKTabs>

**Parameters**: [`ApiResourceRef`](#apiresourceref)

**Returns**: `void`

### revokeOrgAccess

Revoke all of a user's access to an organization.

Removes every IAM policy that grants the specified identity account access to resources within the given organization, including policies on the organization itself and on child resources (environments, agents, etc.).

<SDKTabs>
<Tab value="TypeScript">
```typescript
const empty = await stigmer.iamPolicy.revokeOrgAccess({
  identityAccountId: "...",
  organizationId: "...",
});
```
</Tab>
<Tab value="Go">
```go
empty, err := client.IamPolicy.RevokeOrgAccess(ctx, &stigmer.RevokeOrgAccessInput{
  IdentityAccountId: "...",
  OrganizationId:    "...",
})
```
</Tab>
<Tab value="Python">
```python
empty = client.iam_policies.revoke_org_access(RevokeOrgAccessInput(
    identity_account_id="...",
    organization_id="...",
))
```
</Tab>
<Tab value="Java">
```java
Empty empty = client.iamPolicies().revokeOrgAccess(RevokeOrgAccessInput.builder()
    .identityAccountId("...")
    .organizationId("...")
    .build());
```
</Tab>
</SDKTabs>

**Parameters**: [`RevokeOrgAccessInput`](#revokeorgaccessinput)

**Returns**: `void`

## Types

### IamPolicyInput

Input for creating or updating an IAM Policy.

<TypeTable
  type={{
    name: { type: "string", description: "Resource name.", required: true },
    slug: { type: "string", description: "URL-friendly identifier." },
    org: { type: "string", description: "Organization slug.", required: true },
    labels: { type: "Record<string, string>", description: "Key-value labels." },
    principal: { type: "ApiResourceRefInput", description: "Principal: WHO is being granted access This can be any API resource that acts as an identity: - identity_account (individual user) - team (group of users) - organization (for cross-org grants) - Any other resource that can be a subject in authorization.", typeDescriptionLink: "#apiresourcerefinput", required: true },
    resource: { type: "ApiResourceRefInput", description: "Resource: WHAT is being accessed This can be any API resource that is being protected: - organization - environment - cloud_resource (VPC, S3 bucket, etc.) - service - Any other resource that requires access control.", typeDescriptionLink: "#apiresourcerefinput", required: true },
    relation: { type: "string", description: "The permission being granted (e.g., \"admin\", \"viewer\", \"owner\").", required: true },
  }}
/>

### ApiResourceRefInput

ApiResourceRef represents a reference to ANY API resource in the system.

<TypeTable
  type={{
    kind: { type: "string", description: "Type of the API resource being referenced This should be the resource kind as defined in ApiResourceKind enum.", required: true },
    id: { type: "string", description: "Unique identifier of the resource This is the resource's ID field (e.g., ia-01HQUSER123, tm-01HQTEAM456).", required: true },
    relation: { type: "string", description: "Optional relation qualifier for the resource reference." },
  }}
/>

### IamPolicySpec

IamPolicySpec defines the desired state of an IAM policy binding.

<TypeTable
  type={{
    principal: { type: "ApiResourceRefInput", description: "Principal: WHO is being granted access This can be any API resource that acts as an identity: - identity_account (individual user) - team (group of users) - organization (for cross-org grants) - Any other resource that can be a subject in authorization.", typeDescriptionLink: "#apiresourcerefinput", required: true },
    resource: { type: "ApiResourceRefInput", description: "Resource: WHAT is being accessed This can be any API resource that is being protected: - organization - environment - cloud_resource (VPC, S3 bucket, etc.) - service - Any other resource that requires access control.", typeDescriptionLink: "#apiresourcerefinput", required: true },
    relation: { type: "string", description: "The permission being granted (e.g., \"admin\", \"viewer\", \"owner\").", required: true },
  }}
/>

### ApiResourceRef

ApiResourceRef represents a reference to ANY API resource in the system.

<TypeTable
  type={{
    kind: { type: "string", description: "Type of the API resource being referenced This should be the resource kind as defined in ApiResourceKind enum.", required: true },
    id: { type: "string", description: "Unique identifier of the resource This is the resource's ID field (e.g., ia-01HQUSER123, tm-01HQTEAM456).", required: true },
    relation: { type: "string", description: "Optional relation qualifier for the resource reference." },
  }}
/>

### RevokeOrgAccessInput

RevokeOrgAccessInput defines the input for revoking all of a user's access to an organization.

<TypeTable
  type={{
    identityAccountId: { type: "string", description: "The identity account whose access is being revoked.", required: true },
    organizationId: { type: "string", description: "The organization from which access is being revoked.", required: true },
  }}
/>

### CheckAuthorizationInput

CheckAuthorizationInput defines input for checking if a principal is authorized.

<TypeTable
  type={{
    policy: { type: "IamPolicySpecInput", description: "The policy to check (principal, resource, relation).", required: true },
    contextualPolicies: { type: "IamPolicySpecInput[]", description: "Optional contextual policies for \"what-if\" scenarios." },
  }}
/>

### CheckAuthorizationResult

CheckAuthorizationResult contains the result of an authorization check.

<TypeTable
  type={{
    isAuthorized: { type: "boolean", description: "Whether the principal is authorized." },
  }}
/>

### ListAuthorizedResourceIdsInput

ListAuthorizedResourceIdsInput defines input for listing resources a principal can access.

<TypeTable
  type={{
    principal: { type: "ApiResourceRefInput", description: "The principal whose access is being queried.", typeDescriptionLink: "#apiresourcerefinput", required: true },
    resourceKind: { type: "string", description: "The kind of resources to check.", required: true },
    relation: { type: "string", description: "The relation/permission to check for.", required: true },
    contextualPolicies: { type: "IamPolicySpecInput[]", description: "Optional contextual policies for \"what-if\" scenarios." },
  }}
/>

### AuthorizedResourceIdsList

AuthorizedResourceIdsList contains all resource IDs a principal can access.

<TypeTable
  type={{
    resourceIds: { type: "string[]", description: "Resource IDs the principal is authorized to access." },
  }}
/>

### ListAuthorizedPrincipalIdsInput

ListAuthorizedPrincipalIdsInput defines input for listing principals that can access a resource.

<TypeTable
  type={{
    resource: { type: "ApiResourceRefInput", description: "The resource whose access is being queried.", typeDescriptionLink: "#apiresourcerefinput", required: true },
    principalKind: { type: "string", description: "The kind of principals to check for.", required: true },
    relation: { type: "string", description: "The relation/permission to check for.", required: true },
    contextualPolicies: { type: "IamPolicySpecInput[]", description: "Optional contextual policies for \"what-if\" scenarios." },
  }}
/>

### AuthorizedPrincipalIdsList

AuthorizedPrincipalIdsList contains all principal IDs that can access a resource.

<TypeTable
  type={{
    principalIds: { type: "string[]", description: "Principal IDs authorized to access the resource." },
  }}
/>

### ListResourceAccessInput

ListResourceAccessInput defines input for querying access grants on a resource.

<TypeTable
  type={{
    resource: { type: "ApiResourceRefInput", description: "The resource to query access for.", typeDescriptionLink: "#apiresourcerefinput", required: true },
    includeInherited: { type: "boolean", description: "Whether to include inherited permissions from parent resources." },
  }}
/>

### ResourceAccessByPrincipalList

ResourceAccessByPrincipalList groups access grants by principal, showing each principal and all their assigned roles on the queried resource.

<TypeTable
  type={{
    entries: { type: "PrincipalAccessInput[]", description: "Principal access entries." },
  }}
/>

### PrincipalResourceInput

PrincipalResourceInput defines a generic input for operations involving a principal and a resource.

<TypeTable
  type={{
    principal: { type: "ApiResourceRefInput", description: "The principal in the relationship (WHO).", typeDescriptionLink: "#apiresourcerefinput", required: true },
    resource: { type: "ApiResourceRefInput", description: "The resource in the relationship (WHAT).", typeDescriptionLink: "#apiresourcerefinput", required: true },
  }}
/>

### PrincipalResourceRoles

PrincipalResourceRoles contains all roles a principal has on a specific resource.

<TypeTable
  type={{
    roles: { type: "RoleInfoInput[]", description: "Roles assigned to the principal on this resource." },
  }}
/>

### GetPrincipalsCountInput

GetPrincipalsCountInput defines input for counting principals in an organization.

<TypeTable
  type={{
    orgId: { type: "string", description: "Organization ID to count principals for.", required: true },
    principalKind: { type: "string", description: "The kind of principals to count (identity_account or team).", required: true },
  }}
/>

### PrincipalsCount

PrincipalsCount contains the count of principals.

<TypeTable
  type={{
    count: { type: "number", description: "Number of principals matching the query." },
  }}
/>

### IamPolicy

IamPolicy represents a binding between a principal (who), a resource (what), and a relation/permission (how).

<TypeTable
  type={{
    apiVersion: { type: "string", description: "API version for this resource type." },
    kind: { type: "string", description: "Resource kind identifier." },
    metadata: { type: "ApiResourceMetadata", description: "Resource metadata including id, name, org, slug, labels, visibility, and timestamps.", typeDescriptionLink: "/docs/sdk/resources/commons#apiresourcemetadata" },
    spec: { type: "IamPolicyInput", description: "Resource specification. See [`IamPolicyInput`](#iampolicyinput).", typeDescriptionLink: "#iampolicyinput" },
    status: { type: "IamPolicyStatus", description: "System-managed state. See `IamPolicyStatus`." },
  }}
/>

