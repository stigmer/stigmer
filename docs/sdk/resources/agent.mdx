---
title: Agent
---

{/* Auto-generated by stigmer-codegen. DO NOT EDIT. */}

An Agent defines what an AI assistant knows and can do. It declares the agent's
instructions (system prompt), which MCP servers it can use, which Skills it has,
and optional Sub-Agents for delegation.

```yaml
apiVersion: agentic.stigmer.ai/v1
kind: Agent
metadata:
  name: engineering-assistant
  slug: eng-assistant
spec:
  description: "Helps engineering teams with code review"
  instructions: "You are an engineering assistant..."
  mcp_server_usages:
    - mcp_server_ref:
        kind: mcp_server
        slug: github
      enabled_tools: [search_code, create_pr]
  skill_refs:
    - kind: skill
      slug: code-review-best-practices
```

## Client Access

<SDKTabs>
<Tab value="TypeScript">
```typescript
const agent = await stigmer.agent.get("agt_01j5q3k7m8r2s4tnz2hfp0q0c3");
```
</Tab>
<Tab value="Go">
```go
agent, err := client.Agent.Get(ctx, "agt_01j5q3k7m8r2s4tnz2hfp0q0c3")
```
</Tab>
<Tab value="Python">
```python
agent = client.agents.get("agt_01j5q3k7m8r2s4tnz2hfp0q0c3")
```
</Tab>
<Tab value="Java">
```java
Agent agent = client.agents().get("agt_01j5q3k7m8r2s4tnz2hfp0q0c3");
```
</Tab>
</SDKTabs>

## Methods

| Method | Description |
|--------|-------------|
| [`get`](#get) | Get a single agent by ID. |
| [`getByReference`](#getbyreference) | Get an agent by its organization-scoped reference (org/slug). |
| [`getDefault`](#getdefault) | Get the platform default agent. |
| [`apply`](#apply) | Create or update an agent. |
| [`create`](#create) | Create an agent. |
| [`update`](#update) | Update an existing agent. |
| [`updateVisibility`](#updatevisibility) | Update the visibility of an existing agent. |
| [`delete`](#delete) | Delete an agent. |

### get

Get a single agent by ID.

<SDKTabs>
<Tab value="TypeScript">
```typescript
const agent = await stigmer.agent.get(id);
```
</Tab>
<Tab value="Go">
```go
agent, err := client.Agent.Get(ctx, id)
```
</Tab>
<Tab value="Python">
```python
agent = client.agents.get(id)
```
</Tab>
<Tab value="Java">
```java
Agent agent = client.agents().get(id);
```
</Tab>
</SDKTabs>

**Parameters**

| Parameter | Type | Description |
|-----------|------|-------------|
| `id` | `string` | Agent identifier. |

**Returns**: [`Agent`](#agent)

### getByReference

Get an agent by its organization-scoped reference (org/slug). Resolves a human-readable reference like "acme/web-search" to the full Agent resource.

<SDKTabs>
<Tab value="TypeScript">
```typescript
const agent = await stigmer.agent.getByReference({
  org: "...",
  kind: "...",
  slug: "...",
  version: "...",
});
```
</Tab>
<Tab value="Go">
```go
agent, err := client.Agent.GetByReference(ctx, &stigmer.ApiResourceReference{
  Org:     "...",
  Kind:    "...",
  Slug:    "...",
  Version: "...",
})
```
</Tab>
<Tab value="Python">
```python
agent = client.agents.get_by_reference(ApiResourceReference(
    org="...",
    kind="...",
    slug="...",
    version="...",
))
```
</Tab>
<Tab value="Java">
```java
Agent agent = client.agents().getByReference(ApiResourceReference.builder()
    .org("...")
    .kind("...")
    .slug("...")
    .version("...")
    .build());
```
</Tab>
</SDKTabs>

**Parameters**: [`ResourceRef`](#resourceref)

**Returns**: [`Agent`](#agent)

### getDefault

Get the platform default agent.

Returns the default agent for the platform, including status.default_instance_id for creating a session. Use this to start a conversation without selecting an agent first.

Returns NOT_FOUND if no default agent is configured.

<SDKTabs>
<Tab value="TypeScript">
```typescript
const agent = await stigmer.agent.getDefault({
  org: "...",
});
```
</Tab>
<Tab value="Go">
```go
agent, err := client.Agent.GetDefault(ctx, &stigmer.GetDefaultAgentRequest{
  Org: "...",
})
```
</Tab>
<Tab value="Python">
```python
agent = client.agents.get_default(GetDefaultAgentRequest(
    org="...",
))
```
</Tab>
<Tab value="Java">
```java
Agent agent = client.agents().getDefault(GetDefaultAgentRequest.builder()
    .org("...")
    .build());
```
</Tab>
</SDKTabs>

**Parameters**: [`GetDefaultAgentRequest`](#getdefaultagentrequest)

**Returns**: [`Agent`](#agent)

### apply

Create or update an agent.

<SDKTabs>
<Tab value="TypeScript">
```typescript
const agent = await stigmer.agent.apply({
  name: "my-agent",
  org: "acme",
  slug: "...",
  labels: {},
  description: "...",
  iconUrl: "...",
  instructions: "...",
  mcpServerUsages: [],
  skillRefs: [],
  subAgents: [],
  env: {},
});
```
</Tab>
<Tab value="Go">
```go
agent, err := client.Agent.Apply(ctx, &stigmer.AgentInput{
  Name:            "my-agent",
  Org:             "acme",
  Slug:            "...",
  Labels:          map[string]string{},
  Description:     "...",
  IconUrl:         "...",
  Instructions:    "...",
  McpServerUsages: []stigmer.McpServerUsageInput{},
  SkillRefs:       []stigmer.ResourceRef{},
  SubAgents:       []stigmer.SubAgentInput{},
  Env:             map[string]string{},
})
```
</Tab>
<Tab value="Python">
```python
agent = client.agents.apply(AgentInput(
    name="my-agent",
    org="acme",
    slug="...",
    labels={},
    description="...",
    icon_url="...",
    instructions="...",
    mcp_server_usages=[],
    skill_refs=[],
    sub_agents=[],
    env={},
))
```
</Tab>
<Tab value="Java">
```java
Agent agent = client.agents().apply(AgentInput.builder()
    .name("my-agent")
    .org("acme")
    .slug("...")
    .labels(Map.of())
    .description("...")
    .iconUrl("...")
    .instructions("...")
    .mcpServerUsages(List.of())
    .skillRefs(List.of())
    .subAgents(List.of())
    .env(Map.of())
    .build());
```
</Tab>
</SDKTabs>

**Parameters**: [`AgentInput`](#agentinput)

**Returns**: [`Agent`](#agent)

### create

Create an agent.

<SDKTabs>
<Tab value="TypeScript">
```typescript
const agent = await stigmer.agent.create({
  name: "my-agent",
  org: "acme",
  slug: "...",
  labels: {},
  description: "...",
  iconUrl: "...",
  instructions: "...",
  mcpServerUsages: [],
  skillRefs: [],
  subAgents: [],
  env: {},
});
```
</Tab>
<Tab value="Go">
```go
agent, err := client.Agent.Create(ctx, &stigmer.AgentInput{
  Name:            "my-agent",
  Org:             "acme",
  Slug:            "...",
  Labels:          map[string]string{},
  Description:     "...",
  IconUrl:         "...",
  Instructions:    "...",
  McpServerUsages: []stigmer.McpServerUsageInput{},
  SkillRefs:       []stigmer.ResourceRef{},
  SubAgents:       []stigmer.SubAgentInput{},
  Env:             map[string]string{},
})
```
</Tab>
<Tab value="Python">
```python
agent = client.agents.create(AgentInput(
    name="my-agent",
    org="acme",
    slug="...",
    labels={},
    description="...",
    icon_url="...",
    instructions="...",
    mcp_server_usages=[],
    skill_refs=[],
    sub_agents=[],
    env={},
))
```
</Tab>
<Tab value="Java">
```java
Agent agent = client.agents().create(AgentInput.builder()
    .name("my-agent")
    .org("acme")
    .slug("...")
    .labels(Map.of())
    .description("...")
    .iconUrl("...")
    .instructions("...")
    .mcpServerUsages(List.of())
    .skillRefs(List.of())
    .subAgents(List.of())
    .env(Map.of())
    .build());
```
</Tab>
</SDKTabs>

**Parameters**: [`AgentInput`](#agentinput)

**Returns**: [`Agent`](#agent)

### update

Update an existing agent.

<SDKTabs>
<Tab value="TypeScript">
```typescript
const agent = await stigmer.agent.update({
  name: "my-agent",
  org: "acme",
  slug: "...",
  labels: {},
  description: "...",
  iconUrl: "...",
  instructions: "...",
  mcpServerUsages: [],
  skillRefs: [],
  subAgents: [],
  env: {},
});
```
</Tab>
<Tab value="Go">
```go
agent, err := client.Agent.Update(ctx, &stigmer.AgentInput{
  Name:            "my-agent",
  Org:             "acme",
  Slug:            "...",
  Labels:          map[string]string{},
  Description:     "...",
  IconUrl:         "...",
  Instructions:    "...",
  McpServerUsages: []stigmer.McpServerUsageInput{},
  SkillRefs:       []stigmer.ResourceRef{},
  SubAgents:       []stigmer.SubAgentInput{},
  Env:             map[string]string{},
})
```
</Tab>
<Tab value="Python">
```python
agent = client.agents.update(AgentInput(
    name="my-agent",
    org="acme",
    slug="...",
    labels={},
    description="...",
    icon_url="...",
    instructions="...",
    mcp_server_usages=[],
    skill_refs=[],
    sub_agents=[],
    env={},
))
```
</Tab>
<Tab value="Java">
```java
Agent agent = client.agents().update(AgentInput.builder()
    .name("my-agent")
    .org("acme")
    .slug("...")
    .labels(Map.of())
    .description("...")
    .iconUrl("...")
    .instructions("...")
    .mcpServerUsages(List.of())
    .skillRefs(List.of())
    .subAgents(List.of())
    .env(Map.of())
    .build());
```
</Tab>
</SDKTabs>

**Parameters**: [`AgentInput`](#agentinput)

**Returns**: [`Agent`](#agent)

### updateVisibility

Update the visibility of an existing agent.

This is a targeted metadata update — it only modifies metadata.visibility, leaving spec, status, and other metadata fields untouched. Use this to make an agent publicly accessible or to revoke public access without sending the entire agent resource (avoiding read-modify-write races).

<SDKTabs>
<Tab value="TypeScript">
```typescript
const agent = await stigmer.agent.updateVisibility({
  resourceId: "...",
  visibility: "...",
});
```
</Tab>
<Tab value="Go">
```go
agent, err := client.Agent.UpdateVisibility(ctx, &stigmer.UpdateVisibilityInput{
  ResourceId: "...",
  Visibility: "...",
})
```
</Tab>
<Tab value="Python">
```python
agent = client.agents.update_visibility(UpdateVisibilityInput(
    resource_id="...",
    visibility="...",
))
```
</Tab>
<Tab value="Java">
```java
Agent agent = client.agents().updateVisibility(UpdateVisibilityInput.builder()
    .resourceId("...")
    .visibility("...")
    .build());
```
</Tab>
</SDKTabs>

**Parameters**: [`UpdateVisibilityInput`](#updatevisibilityinput)

**Returns**: [`Agent`](#agent)

### delete

Delete an agent.

Deletion also removes the agent's system-managed default instance and every AgentShare referencing the agent, so a later agent created at the same org/slug starts clean. Personal instances and sessions are not deleted.

<SDKTabs>
<Tab value="TypeScript">
```typescript
const agent = await stigmer.agent.delete(id);
```
</Tab>
<Tab value="Go">
```go
agent, err := client.Agent.Delete(ctx, id)
```
</Tab>
<Tab value="Python">
```python
agent = client.agents.delete(id)
```
</Tab>
<Tab value="Java">
```java
Agent agent = client.agents().delete(id);
```
</Tab>
</SDKTabs>

**Parameters**

| Parameter | Type | Description |
|-----------|------|-------------|
| `id` | `string` | Agent identifier. |

**Returns**: [`Agent`](#agent)

## Types

### AgentInput

Input for creating or updating an Agent.

<TypeTable
  type={{
    name: { type: "string", description: "Resource name.", required: true },
    slug: { type: "string", description: "URL-friendly identifier." },
    org: { type: "string", description: "Organization slug.", required: true },
    labels: { type: "Record<string, string>", description: "Key-value labels." },
    description: { type: "string", description: "Human-readable description for UI and marketplace display." },
    iconUrl: { type: "string", description: "Icon URL for marketplace and UI display." },
    instructions: { type: "string", description: "System prompt defining the agent's behavior and personality." },
    mcpServerUsages: { type: "McpServerUsageInput[]", description: "MCP servers this agent can use.", typeDescriptionLink: "#mcpserverusageinput" },
    skillRefs: { type: "ResourceRef[]", description: "Skill resources providing additional knowledge to the agent.", typeDescriptionLink: "#resourceref" },
    subAgents: { type: "SubAgentInput[]", description: "Sub-agents that can be delegated to.", typeDescriptionLink: "#subagentinput" },
    env: { type: "Record<string, EnvVarDeclarationInput>", description: "Environment variable declarations for this agent.", typeDescriptionLink: "#envvardeclarationinput" },
  }}
/>

### McpServerUsageInput

McpServerUsage declares that this agent uses a McpServer resource.

<TypeTable
  type={{
    mcpServerRef: { type: "ResourceRef", description: "Reference to the McpServer resource.", typeDescriptionLink: "#resourceref", required: true },
    enabledTools: { type: "string[]", description: "Tools to enable from this MCP server for this agent." },
    toolApprovalOverrides: { type: "ToolApprovalOverrideInput[]", description: "Override approval requirements for specific tools.", typeDescriptionLink: "#toolapprovaloverrideinput" },
  }}
/>

### ResourceRef

Generic reference to any API resource by org and slug.

<TypeTable
  type={{
    org: { type: "string", description: "Organization that owns the referenced resource." },
    kind: { type: "ApiResourceKind", description: "Kind of the referenced resource (e.g., SKILL, AGENT, MCP_SERVER).", typeDescriptionLink: "/docs/sdk/resources/commons#apiresourcekind" },
    slug: { type: "string", description: "Resource slug (user-friendly identifier, unique within org).", required: true },
    version: { type: "string", description: "Version of the resource (optional, only applicable to versioned resources like Skills)." },
  }}
/>

### ToolApprovalOverrideInput

ToolApprovalOverride allows per-agent customization of approval requirements.

<TypeTable
  type={{
    toolName: { type: "string", description: "Name of the tool to override." },
    requiresApproval: { type: "boolean", description: "Whether this tool requires approval for this agent." },
    message: { type: "string", description: "Custom approval message shown to the reviewer." },
  }}
/>

### SubAgentInput

SubAgent defines a specialized agent that the parent can delegate to.

<TypeTable
  type={{
    name: { type: "string", description: "Unique name of the sub-agent within the parent agent.", required: true },
    description: { type: "string", description: "What this sub-agent specializes in." },
    instructions: { type: "string", description: "System prompt for this sub-agent." },
    mcpAccess: { type: "McpAccessInput[]", description: "MCP server access grants for this sub-agent.", typeDescriptionLink: "#mcpaccessinput" },
    skillRefs: { type: "ResourceRef[]", description: "Skill resources for this sub-agent.", typeDescriptionLink: "#resourceref" },
    modelOverride: { type: "string", description: "Model override for this sub-agent." },
  }}
/>

### McpAccessInput

McpAccess grants a sub-agent access to one of the parent's MCP servers.

<TypeTable
  type={{
    mcpServer: { type: "string", description: "Slug of the McpServer to grant access to.", required: true },
    enabledTools: { type: "string[]", description: "Tools this sub-agent can use from this MCP server." },
  }}
/>

### EnvVarDeclarationInput

EnvVarDeclaration declares an environment variable required (or optionally accepted) by a blueprint resource (McpServer, Agent, Workflow).

<TypeTable
  type={{
    isSecret: { type: "boolean", description: "Whether the resolved value should be treated as a secret." },
    description: { type: "string", description: "Human-readable description shown in the UI credential form." },
    optional: { type: "boolean", description: "Whether this variable is optional." },
  }}
/>

### UpdateVisibilityInput

Input for updating the visibility of any API resource.

<TypeTable
  type={{
    resourceId: { type: "string", description: "ID of the resource whose visibility is being updated.", required: true },
    visibility: { type: "ApiResourceVisibility", description: "The new visibility setting for the resource.", typeDescriptionLink: "/docs/sdk/resources/commons#apiresourcevisibility" },
  }}
/>

### GetDefaultAgentRequest

GetDefaultAgentRequest is the input for retrieving the platform default agent.

<TypeTable
  type={{
    org: { type: "string", description: "Organization slug for authorization scoping." },
  }}
/>

### Agent

Agent defines an AI assistant's identity, tools, skills, and delegation model.

<TypeTable
  type={{
    apiVersion: { type: "string", description: "API version for this resource type." },
    kind: { type: "string", description: "Resource kind identifier." },
    metadata: { type: "ApiResourceMetadata", description: "Resource metadata including id, name, org, slug, labels, visibility, and timestamps.", typeDescriptionLink: "/docs/sdk/resources/commons#apiresourcemetadata" },
    spec: { type: "AgentInput", description: "Resource specification. See [`AgentInput`](#agentinput).", typeDescriptionLink: "#agentinput" },
    status: { type: "AgentStatus", description: "System-managed state. See [`AgentStatus`](#agentstatus).", typeDescriptionLink: "#agentstatus" },
  }}
/>

### AgentStatus

AgentStatus contains system-managed state for an agent.

<TypeTable
  type={{
    audit: { type: "ApiResourceAudit", description: "Standard audit information (created_at, updated_at, created_by, etc.).", typeDescriptionLink: "/docs/sdk/resources/commons#apiresourceaudit" },
    defaultInstanceId: { type: "string", description: "ID of the default agent instance." },
  }}
/>

