---
title: Workflow Instance
---

{/* Auto-generated by stigmer-codegen. DO NOT EDIT. */}

A WorkflowInstance binds a reusable Workflow template to the Environment resources
that supply credentials, secrets, and configuration for execution. Create multiple
instances of the same Workflow to target different environments (dev, staging, prod).

```yaml
apiVersion: agentic.stigmer.ai/v1
kind: WorkflowInstance
metadata:
  name: deploy-service-prod
  slug: deploy-service-prod
spec:
  workflow_id: wfl_01abc123def456789
  description: "Production deployment with AWS and GitHub credentials"
  environment_refs:
    - kind: environment
      slug: base-config
    - kind: environment
      slug: aws-prod-env
```

## Client Access

<SDKTabs>
<Tab value="TypeScript">
```typescript
const workflowInstance = await stigmer.workflowInstance.get("win_01j5q3k7m8r2s4tnz2hfp0q0c3");
```
</Tab>
<Tab value="Go">
```go
workflowInstance, err := client.WorkflowInstance.Get(ctx, "win_01j5q3k7m8r2s4tnz2hfp0q0c3")
```
</Tab>
<Tab value="Python">
```python
workflow_instance = client.workflow_instances.get("win_01j5q3k7m8r2s4tnz2hfp0q0c3")
```
</Tab>
<Tab value="Java">
```java
WorkflowInstance workflowInstance = client.workflowInstances().get("win_01j5q3k7m8r2s4tnz2hfp0q0c3");
```
</Tab>
</SDKTabs>

## Methods

| Method | Description |
|--------|-------------|
| [`get`](#get) | Get a single workflow instance by ID. |
| [`getByReference`](#getbyreference) | Get a workflow instance by reference (ID or slug). |
| [`getByWorkflow`](#getbyworkflow) | Get all workflow instances that use a specific workflow template. |
| [`apply`](#apply) | Create or update a workflow instance. |
| [`create`](#create) | Create a workflow instance. |
| [`update`](#update) | Update an existing workflow instance. |
| [`delete`](#delete) | Delete a workflow instance. |

### get

Get a single workflow instance by ID.

<SDKTabs>
<Tab value="TypeScript">
```typescript
const workflowInstance = await stigmer.workflowInstance.get(id);
```
</Tab>
<Tab value="Go">
```go
workflowInstance, err := client.WorkflowInstance.Get(ctx, id)
```
</Tab>
<Tab value="Python">
```python
workflow_instance = client.workflow_instances.get(id)
```
</Tab>
<Tab value="Java">
```java
WorkflowInstance workflowInstance = client.workflowInstances().get(id);
```
</Tab>
</SDKTabs>

**Parameters**

| Parameter | Type | Description |
|-----------|------|-------------|
| `id` | `string` | Workflow Instance identifier. |

**Returns**: [`WorkflowInstance`](#workflowinstance)

### getByReference

Get a workflow instance by reference (ID or slug).

<SDKTabs>
<Tab value="TypeScript">
```typescript
const workflowInstance = await stigmer.workflowInstance.getByReference({
  org: "...",
  kind: "...",
  slug: "...",
  version: "...",
});
```
</Tab>
<Tab value="Go">
```go
workflowInstance, err := client.WorkflowInstance.GetByReference(ctx, &stigmer.ApiResourceReference{
  Org:     "...",
  Kind:    "...",
  Slug:    "...",
  Version: "...",
})
```
</Tab>
<Tab value="Python">
```python
workflow_instance = client.workflow_instances.get_by_reference(ApiResourceReference(
    org="...",
    kind="...",
    slug="...",
    version="...",
))
```
</Tab>
<Tab value="Java">
```java
WorkflowInstance workflowInstance = client.workflowInstances().getByReference(ApiResourceReference.builder()
    .org("...")
    .kind("...")
    .slug("...")
    .version("...")
    .build());
```
</Tab>
</SDKTabs>

**Parameters**: [`ResourceRef`](#resourceref)

**Returns**: [`WorkflowInstance`](#workflowinstance)

### getByWorkflow

Get all workflow instances that use a specific workflow template.

Returns a paginated list of instances that reference the given workflow ID.

<SDKTabs>
<Tab value="TypeScript">
```typescript
const result = await stigmer.workflowInstance.getByWorkflow({
  workflowId: "...",
  pageInfo: {},
});
```
</Tab>
<Tab value="Go">
```go
result, err := client.WorkflowInstance.GetByWorkflow(ctx, &stigmer.GetWorkflowInstancesByWorkflowRequest{
  WorkflowId: "...",
  PageInfo:   &stigmer.PageInfoInput{},
})
```
</Tab>
<Tab value="Python">
```python
result = client.workflow_instances.get_by_workflow(GetWorkflowInstancesByWorkflowRequest(
    workflow_id="...",
    page_info=None,
))
```
</Tab>
<Tab value="Java">
```java
WorkflowInstanceList result = client.workflowInstances().getByWorkflow(GetWorkflowInstancesByWorkflowRequest.builder()
    .workflowId("...")
    .pageInfo(null)
    .build());
```
</Tab>
</SDKTabs>

**Parameters**: [`GetWorkflowInstancesByWorkflowRequest`](#getworkflowinstancesbyworkflowrequest)

**Returns**: [`WorkflowInstanceList`](#workflowinstancelist)

### apply

Create or update a workflow instance.

<SDKTabs>
<Tab value="TypeScript">
```typescript
const workflowInstance = await stigmer.workflowInstance.apply({
  name: "my-workflow-instance",
  org: "acme",
  slug: "...",
  labels: {},
  workflowId: "...",
  description: "...",
  environmentRefs: [],
});
```
</Tab>
<Tab value="Go">
```go
workflowInstance, err := client.WorkflowInstance.Apply(ctx, &stigmer.WorkflowInstanceInput{
  Name:            "my-workflow-instance",
  Org:             "acme",
  Slug:            "...",
  Labels:          map[string]string{},
  WorkflowId:      "...",
  Description:     "...",
  EnvironmentRefs: []stigmer.ResourceRef{},
})
```
</Tab>
<Tab value="Python">
```python
workflow_instance = client.workflow_instances.apply(WorkflowInstanceInput(
    name="my-workflow-instance",
    org="acme",
    slug="...",
    labels={},
    workflow_id="...",
    description="...",
    environment_refs=[],
))
```
</Tab>
<Tab value="Java">
```java
WorkflowInstance workflowInstance = client.workflowInstances().apply(WorkflowInstanceInput.builder()
    .name("my-workflow-instance")
    .org("acme")
    .slug("...")
    .labels(Map.of())
    .workflowId("...")
    .description("...")
    .environmentRefs(List.of())
    .build());
```
</Tab>
</SDKTabs>

**Parameters**: [`WorkflowInstanceInput`](#workflowinstanceinput)

**Returns**: [`WorkflowInstance`](#workflowinstance)

### create

Create a workflow instance.

<SDKTabs>
<Tab value="TypeScript">
```typescript
const workflowInstance = await stigmer.workflowInstance.create({
  name: "my-workflow-instance",
  org: "acme",
  slug: "...",
  labels: {},
  workflowId: "...",
  description: "...",
  environmentRefs: [],
});
```
</Tab>
<Tab value="Go">
```go
workflowInstance, err := client.WorkflowInstance.Create(ctx, &stigmer.WorkflowInstanceInput{
  Name:            "my-workflow-instance",
  Org:             "acme",
  Slug:            "...",
  Labels:          map[string]string{},
  WorkflowId:      "...",
  Description:     "...",
  EnvironmentRefs: []stigmer.ResourceRef{},
})
```
</Tab>
<Tab value="Python">
```python
workflow_instance = client.workflow_instances.create(WorkflowInstanceInput(
    name="my-workflow-instance",
    org="acme",
    slug="...",
    labels={},
    workflow_id="...",
    description="...",
    environment_refs=[],
))
```
</Tab>
<Tab value="Java">
```java
WorkflowInstance workflowInstance = client.workflowInstances().create(WorkflowInstanceInput.builder()
    .name("my-workflow-instance")
    .org("acme")
    .slug("...")
    .labels(Map.of())
    .workflowId("...")
    .description("...")
    .environmentRefs(List.of())
    .build());
```
</Tab>
</SDKTabs>

**Parameters**: [`WorkflowInstanceInput`](#workflowinstanceinput)

**Returns**: [`WorkflowInstance`](#workflowinstance)

### update

Update an existing workflow instance.

<SDKTabs>
<Tab value="TypeScript">
```typescript
const workflowInstance = await stigmer.workflowInstance.update({
  name: "my-workflow-instance",
  org: "acme",
  slug: "...",
  labels: {},
  workflowId: "...",
  description: "...",
  environmentRefs: [],
});
```
</Tab>
<Tab value="Go">
```go
workflowInstance, err := client.WorkflowInstance.Update(ctx, &stigmer.WorkflowInstanceInput{
  Name:            "my-workflow-instance",
  Org:             "acme",
  Slug:            "...",
  Labels:          map[string]string{},
  WorkflowId:      "...",
  Description:     "...",
  EnvironmentRefs: []stigmer.ResourceRef{},
})
```
</Tab>
<Tab value="Python">
```python
workflow_instance = client.workflow_instances.update(WorkflowInstanceInput(
    name="my-workflow-instance",
    org="acme",
    slug="...",
    labels={},
    workflow_id="...",
    description="...",
    environment_refs=[],
))
```
</Tab>
<Tab value="Java">
```java
WorkflowInstance workflowInstance = client.workflowInstances().update(WorkflowInstanceInput.builder()
    .name("my-workflow-instance")
    .org("acme")
    .slug("...")
    .labels(Map.of())
    .workflowId("...")
    .description("...")
    .environmentRefs(List.of())
    .build());
```
</Tab>
</SDKTabs>

**Parameters**: [`WorkflowInstanceInput`](#workflowinstanceinput)

**Returns**: [`WorkflowInstance`](#workflowinstance)

### delete

Delete a workflow instance.

<SDKTabs>
<Tab value="TypeScript">
```typescript
const workflowInstance = await stigmer.workflowInstance.delete(id);
```
</Tab>
<Tab value="Go">
```go
workflowInstance, err := client.WorkflowInstance.Delete(ctx, id)
```
</Tab>
<Tab value="Python">
```python
workflow_instance = client.workflow_instances.delete(id)
```
</Tab>
<Tab value="Java">
```java
WorkflowInstance workflowInstance = client.workflowInstances().delete(id);
```
</Tab>
</SDKTabs>

**Parameters**

| Parameter | Type | Description |
|-----------|------|-------------|
| `id` | `string` | Workflow Instance identifier. |

**Returns**: [`WorkflowInstance`](#workflowinstance)

## Types

### WorkflowInstanceInput

Input for creating or updating a Workflow Instance.

<TypeTable
  type={{
    name: { type: "string", description: "Resource name.", required: true },
    slug: { type: "string", description: "URL-friendly identifier." },
    org: { type: "string", description: "Organization slug.", required: true },
    labels: { type: "Record<string, string>", description: "Key-value labels." },
    workflowId: { type: "string", description: "Reference to the Workflow template this instance deploys." },
    description: { type: "string", description: "Human-readable description of this workflow instance." },
    environmentRefs: { type: "ResourceRef[]", description: "Ordered list of Environment resources providing configuration and secrets.", typeDescriptionLink: "#resourceref" },
  }}
/>

### ResourceRef

Generic reference to any API resource by org and slug.

<TypeTable
  type={{
    org: { type: "string", description: "Organization that owns the referenced resource." },
    kind: { type: "ApiResourceKind", description: "Kind of the referenced resource (e.g., SKILL, AGENT, MCP_SERVER).", typeDescriptionLink: "/docs/sdk/resources/commons#apiresourcekind" },
    slug: { type: "string", description: "Resource slug (user-friendly identifier, unique within org).", required: true },
    version: { type: "string", description: "Version of the resource (optional, only applicable to versioned resources like Skills)." },
  }}
/>

### GetWorkflowInstancesByWorkflowRequest

GetWorkflowInstancesByWorkflowRequest retrieves all instances of a specific workflow template.

<TypeTable
  type={{
    workflowId: { type: "string", description: "Workflow template ID to filter by.", required: true },
    pageInfo: { type: "PageInfoInput", description: "Pagination options for result set control." },
  }}
/>

### WorkflowInstanceList

WorkflowInstanceList contains a paginated list of workflow instances.

<TypeTable
  type={{
    totalPages: { type: "number", description: "Total number of pages in the result set." },
    entries: { type: "WorkflowInstanceInput[]", description: "Workflow instances in the current page.", typeDescriptionLink: "#workflowinstanceinput" },
  }}
/>

### WorkflowInstance

WorkflowInstance represents a configured deployment of a Workflow template.

<TypeTable
  type={{
    apiVersion: { type: "string", description: "API version for this resource type." },
    kind: { type: "string", description: "Resource kind identifier." },
    metadata: { type: "ApiResourceMetadata", description: "Resource metadata including id, name, org, slug, labels, visibility, and timestamps.", typeDescriptionLink: "/docs/sdk/resources/commons#apiresourcemetadata" },
    spec: { type: "WorkflowInstanceInput", description: "Resource specification. See [`WorkflowInstanceInput`](#workflowinstanceinput).", typeDescriptionLink: "#workflowinstanceinput" },
    status: { type: "WorkflowInstanceStatus", description: "System-managed state. See `WorkflowInstanceStatus`." },
  }}
/>

