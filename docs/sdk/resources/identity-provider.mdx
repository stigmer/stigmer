---
title: Identity Provider
---

{/* Auto-generated by stigmer-codegen. DO NOT EDIT. */}

An IdentityProvider represents an external platform's trust relationship with
Stigmer. It configures how Stigmer validates tokens from that platform during
token exchange and enables federated identity provisioning.

```yaml
apiVersion: iam.stigmer.ai/v1
kind: IdentityProvider
metadata:
  name: Planton Cloud
  slug: planton-cloud
  org: planton
spec:
  display_name: "Planton Cloud"
  jwks_uri: "https://planton-prod.us.auth0.com/.well-known/jwks.json"
  allowed_issuers: ["https://planton-prod.us.auth0.com/"]
  expected_audience: "https://api.planton.ai/"
  userinfo_endpoint: "https://planton-prod.us.auth0.com/userinfo"
```

## Client Access

<SDKTabs>
<Tab value="TypeScript">
```typescript
const identityProvider = await stigmer.identityProvider.get("idp_01j5q3k7m8r2s4tnz2hfp0q0c3");
```
</Tab>
<Tab value="Go">
```go
identityProvider, err := client.IdentityProvider.Get(ctx, "idp_01j5q3k7m8r2s4tnz2hfp0q0c3")
```
</Tab>
<Tab value="Python">
```python
identity_provider = client.identity_providers.get("idp_01j5q3k7m8r2s4tnz2hfp0q0c3")
```
</Tab>
<Tab value="Java">
```java
IdentityProvider identityProvider = client.identityProviders().get("idp_01j5q3k7m8r2s4tnz2hfp0q0c3");
```
</Tab>
</SDKTabs>

## Methods

| Method | Description |
|--------|-------------|
| [`get`](#get) | Get an identity provider by its unique identifier. |
| [`getByReference`](#getbyreference) | Get an identity provider by its organization-scoped reference (org/slug). |
| [`getSsoProvider`](#getssoprovider) | Look up the SSO identity provider for an organization. |
| [`listByOrg`](#listbyorg) | List all identity providers belonging to an organization. |
| [`apply`](#apply) | Create or update an identity provider. |
| [`create`](#create) | Create an identity provider. |
| [`update`](#update) | Update an existing identity provider. |
| [`delete`](#delete) | Delete an identity provider. |

### get

Get an identity provider by its unique identifier.

<SDKTabs>
<Tab value="TypeScript">
```typescript
const identityProvider = await stigmer.identityProvider.get(id);
```
</Tab>
<Tab value="Go">
```go
identityProvider, err := client.IdentityProvider.Get(ctx, id)
```
</Tab>
<Tab value="Python">
```python
identity_provider = client.identity_providers.get(id)
```
</Tab>
<Tab value="Java">
```java
IdentityProvider identityProvider = client.identityProviders().get(id);
```
</Tab>
</SDKTabs>

**Parameters**

| Parameter | Type | Description |
|-----------|------|-------------|
| `id` | `string` | Identity Provider identifier. |

**Returns**: [`IdentityProvider`](#identityprovider)

### getByReference

Get an identity provider by its organization-scoped reference (org/slug).

Resolves a human-readable reference like "acme/planton-cloud" to the full IdentityProvider resource.

<SDKTabs>
<Tab value="TypeScript">
```typescript
const identityProvider = await stigmer.identityProvider.getByReference({
  org: "...",
  kind: "...",
  slug: "...",
  version: "...",
});
```
</Tab>
<Tab value="Go">
```go
identityProvider, err := client.IdentityProvider.GetByReference(ctx, &stigmer.ApiResourceReference{
  Org:     "...",
  Kind:    "...",
  Slug:    "...",
  Version: "...",
})
```
</Tab>
<Tab value="Python">
```python
identity_provider = client.identity_providers.get_by_reference(ApiResourceReference(
    org="...",
    kind="...",
    slug="...",
    version="...",
))
```
</Tab>
<Tab value="Java">
```java
IdentityProvider identityProvider = client.identityProviders().getByReference(ApiResourceReference.builder()
    .org("...")
    .kind("...")
    .slug("...")
    .version("...")
    .build());
```
</Tab>
</SDKTabs>

**Parameters**: [`ResourceRef`](#resourceref)

**Returns**: [`IdentityProvider`](#identityprovider)

### getSsoProvider

Look up the SSO identity provider for an organization.

Returns the SSO-relevant projection (display name, OIDC client ID, issuer) of the IdentityProvider where is_sso_provider is true for the given org. Returns NOT_FOUND if the organization has no SSO provider configured.

This endpoint is called by the web app's login page before the user has authenticated, so it requires no authorization. The response intentionally omits internal IdP configuration (JWKS URI, rate limits, userinfo endpoint).

<SDKTabs>
<Tab value="TypeScript">
```typescript
const ssoProviderInfo = await stigmer.identityProvider.getSsoProvider({
  org: "...",
});
```
</Tab>
<Tab value="Go">
```go
ssoProviderInfo, err := client.IdentityProvider.GetSsoProvider(ctx, &stigmer.OrganizationSsoLookup{
  Org: "...",
})
```
</Tab>
<Tab value="Python">
```python
sso_provider_info = client.identity_providers.get_sso_provider(OrganizationSsoLookup(
    org="...",
))
```
</Tab>
<Tab value="Java">
```java
SsoProviderInfo ssoProviderInfo = client.identityProviders().getSsoProvider(OrganizationSsoLookup.builder()
    .org("...")
    .build());
```
</Tab>
</SDKTabs>

**Parameters**: [`OrganizationSsoLookup`](#organizationssolookup)

**Returns**: [`SsoProviderInfo`](#ssoproviderinfo)

### listByOrg

List all identity providers belonging to an organization.

Returns every IdentityProvider whose metadata.org matches the input org. Typically a small set (1-3 per org), so results are not paginated.

<SDKTabs>
<Tab value="TypeScript">
```typescript
const identityProviders = await stigmer.identityProvider.listByOrg({
  org: "...",
});
```
</Tab>
<Tab value="Go">
```go
identityProviders, err := client.IdentityProvider.ListByOrg(ctx, &stigmer.ListIdentityProvidersByOrgInput{
  Org: "...",
})
```
</Tab>
<Tab value="Python">
```python
identity_providers = client.identity_providers.list_by_org(ListIdentityProvidersByOrgInput(
    org="...",
))
```
</Tab>
<Tab value="Java">
```java
IdentityProviders identityProviders = client.identityProviders().listByOrg(ListIdentityProvidersByOrgInput.builder()
    .org("...")
    .build());
```
</Tab>
</SDKTabs>

**Parameters**: [`ListIdentityProvidersByOrgInput`](#listidentityprovidersbyorginput)

**Returns**: [`IdentityProviders`](#identityproviders)

### apply

Create or update an identity provider.

If the resource does not exist, creates a new identity provider. If the resource exists, updates the existing identity provider.

<SDKTabs>
<Tab value="TypeScript">
```typescript
const identityProvider = await stigmer.identityProvider.apply({
  name: "my-identity-provider",
  org: "acme",
  slug: "...",
  labels: {},
  displayName: "...",
  jwksUri: "...",
  allowedIssuers: [],
  expectedAudience: "...",
  rateLimitBudget: 0,
  userinfoEndpoint: "...",
  isSsoProvider: false,
  oidcClientId: "...",
  autoProvisionAccounts: false,
  autoGrantOnOrg: false,
  autoGrantRole: "...",
  tenantOrgClaim: "...",
});
```
</Tab>
<Tab value="Go">
```go
identityProvider, err := client.IdentityProvider.Apply(ctx, &stigmer.IdentityProviderInput{
  Name:                  "my-identity-provider",
  Org:                   "acme",
  Slug:                  "...",
  Labels:                map[string]string{},
  DisplayName:           "...",
  JwksUri:               "...",
  AllowedIssuers:        []string{},
  ExpectedAudience:      "...",
  RateLimitBudget:       0,
  UserinfoEndpoint:      "...",
  IsSsoProvider:         false,
  OidcClientId:          "...",
  AutoProvisionAccounts: false,
  AutoGrantOnOrg:        false,
  AutoGrantRole:         "...",
  TenantOrgClaim:        "...",
})
```
</Tab>
<Tab value="Python">
```python
identity_provider = client.identity_providers.apply(IdentityProviderInput(
    name="my-identity-provider",
    org="acme",
    slug="...",
    labels={},
    display_name="...",
    jwks_uri="...",
    allowed_issuers=[],
    expected_audience="...",
    rate_limit_budget=0,
    userinfo_endpoint="...",
    is_sso_provider=False,
    oidc_client_id="...",
    auto_provision_accounts=False,
    auto_grant_on_org=False,
    auto_grant_role="...",
    tenant_org_claim="...",
))
```
</Tab>
<Tab value="Java">
```java
IdentityProvider identityProvider = client.identityProviders().apply(IdentityProviderInput.builder()
    .name("my-identity-provider")
    .org("acme")
    .slug("...")
    .labels(Map.of())
    .displayName("...")
    .jwksUri("...")
    .allowedIssuers(List.of())
    .expectedAudience("...")
    .rateLimitBudget(0)
    .userinfoEndpoint("...")
    .isSsoProvider(false)
    .oidcClientId("...")
    .autoProvisionAccounts(false)
    .autoGrantOnOrg(false)
    .autoGrantRole("...")
    .tenantOrgClaim("...")
    .build());
```
</Tab>
</SDKTabs>

**Parameters**: [`IdentityProviderInput`](#identityproviderinput)

**Returns**: [`IdentityProvider`](#identityprovider)

### create

Create an identity provider.

The creator's organization owns the identity provider.

<SDKTabs>
<Tab value="TypeScript">
```typescript
const identityProvider = await stigmer.identityProvider.create({
  name: "my-identity-provider",
  org: "acme",
  slug: "...",
  labels: {},
  displayName: "...",
  jwksUri: "...",
  allowedIssuers: [],
  expectedAudience: "...",
  rateLimitBudget: 0,
  userinfoEndpoint: "...",
  isSsoProvider: false,
  oidcClientId: "...",
  autoProvisionAccounts: false,
  autoGrantOnOrg: false,
  autoGrantRole: "...",
  tenantOrgClaim: "...",
});
```
</Tab>
<Tab value="Go">
```go
identityProvider, err := client.IdentityProvider.Create(ctx, &stigmer.IdentityProviderInput{
  Name:                  "my-identity-provider",
  Org:                   "acme",
  Slug:                  "...",
  Labels:                map[string]string{},
  DisplayName:           "...",
  JwksUri:               "...",
  AllowedIssuers:        []string{},
  ExpectedAudience:      "...",
  RateLimitBudget:       0,
  UserinfoEndpoint:      "...",
  IsSsoProvider:         false,
  OidcClientId:          "...",
  AutoProvisionAccounts: false,
  AutoGrantOnOrg:        false,
  AutoGrantRole:         "...",
  TenantOrgClaim:        "...",
})
```
</Tab>
<Tab value="Python">
```python
identity_provider = client.identity_providers.create(IdentityProviderInput(
    name="my-identity-provider",
    org="acme",
    slug="...",
    labels={},
    display_name="...",
    jwks_uri="...",
    allowed_issuers=[],
    expected_audience="...",
    rate_limit_budget=0,
    userinfo_endpoint="...",
    is_sso_provider=False,
    oidc_client_id="...",
    auto_provision_accounts=False,
    auto_grant_on_org=False,
    auto_grant_role="...",
    tenant_org_claim="...",
))
```
</Tab>
<Tab value="Java">
```java
IdentityProvider identityProvider = client.identityProviders().create(IdentityProviderInput.builder()
    .name("my-identity-provider")
    .org("acme")
    .slug("...")
    .labels(Map.of())
    .displayName("...")
    .jwksUri("...")
    .allowedIssuers(List.of())
    .expectedAudience("...")
    .rateLimitBudget(0)
    .userinfoEndpoint("...")
    .isSsoProvider(false)
    .oidcClientId("...")
    .autoProvisionAccounts(false)
    .autoGrantOnOrg(false)
    .autoGrantRole("...")
    .tenantOrgClaim("...")
    .build());
```
</Tab>
</SDKTabs>

**Parameters**: [`IdentityProviderInput`](#identityproviderinput)

**Returns**: [`IdentityProvider`](#identityprovider)

### update

Update an existing identity provider.

<SDKTabs>
<Tab value="TypeScript">
```typescript
const identityProvider = await stigmer.identityProvider.update({
  name: "my-identity-provider",
  org: "acme",
  slug: "...",
  labels: {},
  displayName: "...",
  jwksUri: "...",
  allowedIssuers: [],
  expectedAudience: "...",
  rateLimitBudget: 0,
  userinfoEndpoint: "...",
  isSsoProvider: false,
  oidcClientId: "...",
  autoProvisionAccounts: false,
  autoGrantOnOrg: false,
  autoGrantRole: "...",
  tenantOrgClaim: "...",
});
```
</Tab>
<Tab value="Go">
```go
identityProvider, err := client.IdentityProvider.Update(ctx, &stigmer.IdentityProviderInput{
  Name:                  "my-identity-provider",
  Org:                   "acme",
  Slug:                  "...",
  Labels:                map[string]string{},
  DisplayName:           "...",
  JwksUri:               "...",
  AllowedIssuers:        []string{},
  ExpectedAudience:      "...",
  RateLimitBudget:       0,
  UserinfoEndpoint:      "...",
  IsSsoProvider:         false,
  OidcClientId:          "...",
  AutoProvisionAccounts: false,
  AutoGrantOnOrg:        false,
  AutoGrantRole:         "...",
  TenantOrgClaim:        "...",
})
```
</Tab>
<Tab value="Python">
```python
identity_provider = client.identity_providers.update(IdentityProviderInput(
    name="my-identity-provider",
    org="acme",
    slug="...",
    labels={},
    display_name="...",
    jwks_uri="...",
    allowed_issuers=[],
    expected_audience="...",
    rate_limit_budget=0,
    userinfo_endpoint="...",
    is_sso_provider=False,
    oidc_client_id="...",
    auto_provision_accounts=False,
    auto_grant_on_org=False,
    auto_grant_role="...",
    tenant_org_claim="...",
))
```
</Tab>
<Tab value="Java">
```java
IdentityProvider identityProvider = client.identityProviders().update(IdentityProviderInput.builder()
    .name("my-identity-provider")
    .org("acme")
    .slug("...")
    .labels(Map.of())
    .displayName("...")
    .jwksUri("...")
    .allowedIssuers(List.of())
    .expectedAudience("...")
    .rateLimitBudget(0)
    .userinfoEndpoint("...")
    .isSsoProvider(false)
    .oidcClientId("...")
    .autoProvisionAccounts(false)
    .autoGrantOnOrg(false)
    .autoGrantRole("...")
    .tenantOrgClaim("...")
    .build());
```
</Tab>
</SDKTabs>

**Parameters**: [`IdentityProviderInput`](#identityproviderinput)

**Returns**: [`IdentityProvider`](#identityprovider)

### delete

Delete an identity provider.

Deletion is blocked if any platform-managed organizations reference this identity provider.

<SDKTabs>
<Tab value="TypeScript">
```typescript
const identityProvider = await stigmer.identityProvider.delete({ resourceId: id });
```
</Tab>
<Tab value="Go">
```go
identityProvider, err := client.IdentityProvider.Delete(ctx, &stigmer.DeleteResourceInput{
  ResourceID: id,
})
```
</Tab>
<Tab value="Python">
```python
identity_provider = client.identity_providers.delete(DeleteResourceInput(resource_id=id))
```
</Tab>
<Tab value="Java">
```java
IdentityProvider identityProvider = client.identityProviders().delete(DeleteResourceInput.builder()
    .resourceId(id)
    .build());
```
</Tab>
</SDKTabs>

**Parameters**

| Parameter | Type | Description |
|-----------|------|-------------|
| `resourceId` | `string` | ID of the resource to delete. |
| `versionMessage` | `string` | Optional deletion reason. |
| `force` | `boolean` | Force delete even if resource is in use. |

**Returns**: [`IdentityProvider`](#identityprovider)

## Types

### IdentityProviderInput

Input for creating or updating an Identity Provider.

<TypeTable
  type={{
    name: { type: "string", description: "Resource name.", required: true },
    slug: { type: "string", description: "URL-friendly identifier." },
    org: { type: "string", description: "Organization slug.", required: true },
    labels: { type: "Record<string, string>", description: "Key-value labels." },
    displayName: { type: "string", description: "Human-readable display name for this identity provider." },
    jwksUri: { type: "string", description: "JWKS endpoint URL for validating tokens presented during token exchange." },
    allowedIssuers: { type: "string[]", description: "JWT issuer values that Stigmer will accept from this platform." },
    expectedAudience: { type: "string", description: "Expected JWT audience value." },
    rateLimitBudget: { type: "number", description: "Shared rate limit budget across all organizations managed via this identity provider." },
    userinfoEndpoint: { type: "string", description: "OIDC UserInfo endpoint URL for fetching user profile data during token exchange." },
    isSsoProvider: { type: "boolean", description: "Whether this identity provider serves as the SSO login provider for its owning organization." },
    oidcClientId: { type: "string", description: "OIDC client identifier for browser-based SSO login." },
    autoProvisionAccounts: { type: "boolean", description: "Whether to automatically create a federated identity account when a valid JWT arrives but no account exists for the token's sub claim." },
    autoGrantOnOrg: { type: "boolean", description: "Whether to automatically grant a role on an organization when an account is auto-provisioned." },
    autoGrantRole: { type: "IamRole", description: "The role to grant when auto_grant_on_org is true.", typeDescriptionLink: "#iamrole" },
    tenantOrgClaim: { type: "string", description: "Name of the JWT claim that identifies the tenant organization for multi-tenant provisioning." },
  }}
/>

### ResourceRef

Generic reference to any API resource by org and slug.

<TypeTable
  type={{
    org: { type: "string", description: "Organization that owns the referenced resource." },
    kind: { type: "ApiResourceKind", description: "Kind of the referenced resource (e.g., SKILL, AGENT, MCP_SERVER).", typeDescriptionLink: "/docs/sdk/resources/commons#apiresourcekind" },
    slug: { type: "string", description: "Resource slug (user-friendly identifier, unique within org).", required: true },
    version: { type: "string", description: "Version of the resource (optional, only applicable to versioned resources like Skills)." },
  }}
/>

### ListIdentityProvidersByOrgInput

ListIdentityProvidersByOrgInput specifies the organization whose identity providers should be returned.

<TypeTable
  type={{
    org: { type: "string", description: "Organization slug to list identity providers for." },
  }}
/>

### IdentityProviders

IdentityProviders contains a list of identity provider resources.

<TypeTable
  type={{
    entries: { type: "IdentityProviderInput[]", description: "Identity provider entries.", typeDescriptionLink: "#identityproviderinput" },
  }}
/>

### OrganizationSsoLookup

OrganizationSsoLookup identifies an organization for SSO provider discovery.

<TypeTable
  type={{
    org: { type: "string", description: "Organization slug to look up SSO configuration for." },
  }}
/>

### SsoProviderInfo

SsoProviderInfo contains the minimal OIDC configuration the web app needs to render an SSO login button and initiate the Authorization Code flow.

<TypeTable
  type={{
    displayName: { type: "string", description: "Display name of the SSO provider (e.g., \"Acme Corp Okta\")." },
    oidcClientId: { type: "string", description: "OIDC client ID for initiating the Authorization Code flow with PKCE." },
    issuer: { type: "string", description: "OIDC issuer URL." },
    expectedAudience: { type: "string", description: "Expected JWT audience value for the OIDC token request." },
  }}
/>

### IdentityProvider

IdentityProvider represents an external platform's trust relationship with Stigmer.

<TypeTable
  type={{
    apiVersion: { type: "string", description: "API version for this resource type." },
    kind: { type: "string", description: "Resource kind identifier." },
    metadata: { type: "ApiResourceMetadata", description: "Resource metadata including id, name, org, slug, labels, visibility, and timestamps.", typeDescriptionLink: "/docs/sdk/resources/commons#apiresourcemetadata" },
    spec: { type: "IdentityProviderInput", description: "Resource specification. See [`IdentityProviderInput`](#identityproviderinput).", typeDescriptionLink: "#identityproviderinput" },
    status: { type: "IdentityProviderStatus", description: "System-managed state. See `IdentityProviderStatus`." },
  }}
/>

### IamRole

IamRole defines the roles that can be assigned to principals on resources via IAM policies.

| Value | Description |
|-------|-------------|
| `owner` | Full control of the resource. |
| `admin` | Administrative access. |
| `member` | Membership-level access. |
| `viewer` | Read-only access to the resource. |

