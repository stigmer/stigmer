---
title: Plan
---

{/* Auto-generated by stigmer-codegen. DO NOT EDIT. */}

A Plan is a catalog entry on Stigmer Cloud: the bundle of entitlements (limits
and features) a customer buys, and the terms that buy it. A plan is bought
through one of two instruments, a Subscription for an organization on Stigmer
Cloud or a License for a self-hosting customer. A plan is immutable once
created; to change its terms, create a new plan and retire the old one, which
stays readable so existing subscriptions can show what they bought.

Plans belong to the platform, not to an organization, and are created by
platform operators; the limits and prices of the plans on offer are set on
each plan row, never in this contract. Any signed-in caller may read the
catalog.

```yaml
apiVersion: billing.stigmer.ai/v1
kind: Plan
metadata:
  name: Team
  slug: team
spec:
  instrument: subscription
  entitlements:
    features:
      - channels
      - sharing
  description: "Channels and sharing for a growing team."
```

## Client Access

<SDKTabs>
<Tab value="TypeScript">
```typescript
const plan = await stigmer.plan.get("pln_01j5q3k7m8r2s4tnz2hfp0q0c3");
```
</Tab>
<Tab value="Go">
```go
plan, err := client.Plan.Get(ctx, "pln_01j5q3k7m8r2s4tnz2hfp0q0c3")
```
</Tab>
<Tab value="Python">
```python
plan = client.plans.get("pln_01j5q3k7m8r2s4tnz2hfp0q0c3")
```
</Tab>
<Tab value="Java">
```java
Plan plan = client.plans().get("pln_01j5q3k7m8r2s4tnz2hfp0q0c3");
```
</Tab>
</SDKTabs>

## Methods

| Method | Description |
|--------|-------------|
| [`get`](#get) | Get a plan by its unique identifier. |
| [`list`](#list) | List the plans in the catalog. |
| [`create`](#create) | Create a plan. |
| [`retire`](#retire) | Retire a plan so it can no longer be bought. |

### get

Get a plan by its unique identifier.

Any signed-in caller may read any plan, retired plans included.

<SDKTabs>
<Tab value="TypeScript">
```typescript
const plan = await stigmer.plan.get(id);
```
</Tab>
<Tab value="Go">
```go
plan, err := client.Plan.Get(ctx, id)
```
</Tab>
<Tab value="Python">
```python
plan = client.plans.get(id)
```
</Tab>
<Tab value="Java">
```java
Plan plan = client.plans().get(id);
```
</Tab>
</SDKTabs>

**Parameters**

| Parameter | Type | Description |
|-----------|------|-------------|
| `id` | `string` | Plan identifier. |

**Returns**: [`Plan`](#plan)

### list

List the plans in the catalog.

Returns the active plans; set include_retired to see retired ones too. Any signed-in caller may list.

<SDKTabs>
<Tab value="TypeScript">
```typescript
const plans = await stigmer.plan.list({
  includeRetired: false,
});
```
</Tab>
<Tab value="Go">
```go
plans, err := client.Plan.List(ctx, &stigmer.ListPlansInput{
  IncludeRetired: false,
})
```
</Tab>
<Tab value="Python">
```python
plans = client.plans.list(ListPlansInput(
    include_retired=False,
))
```
</Tab>
<Tab value="Java">
```java
Plans plans = client.plans().list(ListPlansInput.builder()
    .includeRetired(false)
    .build());
```
</Tab>
</SDKTabs>

**Parameters**: [`ListPlansInput`](#listplansinput)

**Returns**: [`Plans`](#plans)

### create

Create a plan.

The plan's slug is its stable handle and must be unique across the catalog. The spec is final at create: to change any term, create a new Plan and retire this one.

<SDKTabs>
<Tab value="TypeScript">
```typescript
const plan = await stigmer.plan.create({
  name: "my-plan",
  org: "acme",
  slug: "...",
  labels: {},
  instrument: "...",
  entitlements: {},
  terms: {},
  description: "...",
});
```
</Tab>
<Tab value="Go">
```go
plan, err := client.Plan.Create(ctx, &stigmer.PlanInput{
  Name:         "my-plan",
  Org:          "acme",
  Slug:         "...",
  Labels:       map[string]string{},
  Instrument:   "...",
  Entitlements: &stigmer.EntitlementsInput{},
  Terms:        &stigmer.PlanTermsInput{},
  Description:  "...",
})
```
</Tab>
<Tab value="Python">
```python
plan = client.plans.create(PlanInput(
    name="my-plan",
    org="acme",
    slug="...",
    labels={},
    instrument="...",
    entitlements=None,
    terms=None,
    description="...",
))
```
</Tab>
<Tab value="Java">
```java
Plan plan = client.plans().create(PlanInput.builder()
    .name("my-plan")
    .org("acme")
    .slug("...")
    .labels(Map.of())
    .instrument("...")
    .entitlements(null)
    .terms(null)
    .description("...")
    .build());
```
</Tab>
</SDKTabs>

**Parameters**: [`PlanInput`](#planinput)

**Returns**: [`Plan`](#plan)

### retire

Retire a plan so it can no longer be bought.

Existing subscriptions and licenses on the plan keep it; the plan stays readable so they can show their terms. Retiring an already retired plan changes nothing.

<SDKTabs>
<Tab value="TypeScript">
```typescript
const plan = await stigmer.plan.retire(id);
```
</Tab>
<Tab value="Go">
```go
plan, err := client.Plan.Retire(ctx, id)
```
</Tab>
<Tab value="Python">
```python
plan = client.plans.retire(id)
```
</Tab>
<Tab value="Java">
```java
Plan plan = client.plans().retire(id);
```
</Tab>
</SDKTabs>

**Parameters**

| Parameter | Type | Description |
|-----------|------|-------------|
| `id` | `string` | Plan identifier. |

**Returns**: [`Plan`](#plan)

## Types

### PlanInput

Input for creating or updating a Plan.

<TypeTable
  type={{
    name: { type: "string", description: "Resource name.", required: true },
    slug: { type: "string", description: "URL-friendly identifier." },
    org: { type: "string", description: "Organization slug.", required: true },
    labels: { type: "Record<string, string>", description: "Key-value labels." },
    instrument: { type: "PlanInstrument", description: "The instrument this plan is bought through.", typeDescriptionLink: "#planinstrument", required: true },
    entitlements: { type: "EntitlementsInput", description: "What the plan permits: its limits and features.", typeDescriptionLink: "#entitlementsinput", required: true },
    terms: { type: "PlanTermsInput", description: "The commercial terms.", typeDescriptionLink: "#plantermsinput" },
    description: { type: "string", description: "One or two sentences describing the plan as the pricing page shows it." },
  }}
/>

### EntitlementsInput

What the platform permits a tenant: a set of limits and a set of features.

<TypeTable
  type={{
    limits: { type: "EntitlementLimitsInput", description: "The numeric limits in force.", typeDescriptionLink: "#entitlementlimitsinput" },
    features: { type: "Feature[]", description: "The features unlocked, each named once.", typeDescriptionLink: "#feature" },
  }}
/>

### EntitlementLimitsInput

The numeric limits an entitlement carries.

<TypeTable
  type={{
    maxOrganizations: { type: "number", description: "The most organizations the tenant may hold at once." },
    maxUsers: { type: "number", description: "The most identity accounts the tenant may hold at once." },
    includedManagedOrganizations: { type: "number", description: "The platform-managed organizations a subscription includes before the per-organization fee applies." },
  }}
/>

### PlanTermsInput

The commercial terms of a plan.

<TypeTable
  type={{
    monthlyMinimumMicros: { type: "number", description: "The least a subscription plan bills per month, in micro-USD." },
    usageShareBasisPoints: { type: "number", description: "The share of monthly usage a subscription plan bills when it exceeds the minimum, in basis points." },
    perExtraOrganizationMicros: { type: "number", description: "What a subscription plan bills per month for each platform-managed organization beyond the ones its entitlements include, in micro-USD." },
    annualPriceMicros: { type: "number", description: "What a license plan invoices for a one-year term, in micro-USD." },
  }}
/>

### ListPlansInput

ListPlansInput selects which plans to list.

<TypeTable
  type={{
    includeRetired: { type: "boolean", description: "When true, retired plans are included beside the active ones; by default only plans that can still be bought are returned." },
  }}
/>

### Plans

Plans contains a list of plan resources.

<TypeTable
  type={{
    entries: { type: "PlanInput[]", description: "Plan entries.", typeDescriptionLink: "#planinput" },
  }}
/>

### Plan

Plan is a catalog entry naming a bundle of entitlements and the terms that buy it.

<TypeTable
  type={{
    apiVersion: { type: "string", description: "API version for this resource type." },
    kind: { type: "string", description: "Resource kind identifier." },
    metadata: { type: "ApiResourceMetadata", description: "Resource metadata including id, name, org, slug, labels, visibility, and timestamps.", typeDescriptionLink: "/docs/sdk/resources/commons#apiresourcemetadata" },
    spec: { type: "PlanInput", description: "Resource specification. See [`PlanInput`](#planinput).", typeDescriptionLink: "#planinput" },
    status: { type: "PlanStatus", description: "System-managed state. See [`PlanStatus`](#planstatus).", typeDescriptionLink: "#planstatus" },
  }}
/>

### PlanStatus

PlanStatus is the system-managed state of a plan: its audit trail and whether it is still offered.

<TypeTable
  type={{
    audit: { type: "ApiResourceAudit", description: "Standard audit information tracking creation and modification.", typeDescriptionLink: "/docs/sdk/resources/commons#apiresourceaudit" },
    lifecycle: { type: "PlanLifecycle", description: "Whether the plan is offered to new buyers or retired.", typeDescriptionLink: "#planlifecycle" },
    retiredAt: { type: "string", description: "When the plan was retired." },
  }}
/>

### PlanLifecycle

PlanLifecycle is whether a plan can still be bought.

| Value | Description |
|-------|-------------|
| `active` | Offered to new buyers. |
| `retired` | No longer offered; existing subscriptions and licenses keep it. |

### PlanInstrument

The instrument a plan is bought through.

| Value | Description |
|-------|-------------|
| `subscription` | Bought as a Subscription by an organization on Stigmer Cloud, billed monthly against usage. |
| `license` | Bought as a License by a self-hosting customer, invoiced for a term. |

### Feature

A capability an entitlement can unlock.

| Value | Description |
|-------|-------------|
| `sso_enforcement` | Enforcing sign-in through a registered identity provider, so members cannot use another login. |
| `platform_client` | Creating PlatformClient credentials so the tenant's own product can mint Stigmer tokens for its users. |
| `byo_provider_keys` | Bringing the tenant's own LLM provider keys instead of the platform's metered proxy credentials. |
| `channels` | Delivering agents over messaging channels (Slack, WhatsApp) through a channel runtime. |
| `sharing` | Sharing an agent with individuals and guests through hosted share links. |

