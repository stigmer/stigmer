---
title: Artifact
description: "SDK reference for Artifact resources."
---

{/* Auto-generated by stigmer-codegen. DO NOT EDIT. */}

## Client Access

<SDKTabs>
<Tab value="TypeScript">
```typescript
const artifact = await stigmer.artifact.get("art_01j5q3k7m8r2s4tnz2hfp0q0c3");
```
</Tab>
<Tab value="Go">
```go
artifact, err := client.Artifact.Get(ctx, "art_01j5q3k7m8r2s4tnz2hfp0q0c3")
```
</Tab>
<Tab value="Python">
```python
artifact = client.artifacts.get("art_01j5q3k7m8r2s4tnz2hfp0q0c3")
```
</Tab>
<Tab value="Java">
```java
Artifact artifact = client.artifacts().get("art_01j5q3k7m8r2s4tnz2hfp0q0c3");
```
</Tab>
</SDKTabs>

## Methods

| Method | Description |
|--------|-------------|
| [`get`](#get) | Get a single artifact by ID. |
| [`getDownloadUrl`](#getdownloadurl) | Get a download URL for artifact content. |
| [`listByExecution`](#listbyexecution) | List all artifacts produced by a specific execution. |
| [`create`](#create) | Create a new artifact with content. |
| [`delete`](#delete) | Delete an artifact. |

### get

Get a single artifact by ID.

Returns the complete Artifact resource including metadata, spec, and status (content hash, size, storage state, expiration).

Does NOT return the artifact content — use getDownloadUrl to retrieve a URL for downloading the content via HTTP GET.

Use Cases:

1. Artifact Detail View: - User clicks an artifact in the execution viewer - UI calls get() to fetch full metadata - UI displays content type, size, source task, expiration

2. Artifact Reference Resolution: - Task output contains _artifact_ref field - UI calls get() to resolve the reference to metadata - UI renders a download/preview widget instead of raw JSON

Error Cases:

- NOT_FOUND: No Artifact exists with the given ID - PERMISSION_DENIED: User doesn't have view access to the parent execution

<SDKTabs>
<Tab value="TypeScript">
```typescript
const artifact = await stigmer.artifact.get(id);
```
</Tab>
<Tab value="Go">
```go
artifact, err := client.Artifact.Get(ctx, id)
```
</Tab>
<Tab value="Python">
```python
artifact = client.artifacts.get(id)
```
</Tab>
<Tab value="Java">
```java
Artifact artifact = client.artifacts().get(id);
```
</Tab>
</SDKTabs>

**Parameters**

| Parameter | Type | Description |
|-----------|------|-------------|
| `id` | `string` | Artifact identifier. |

**Returns**: [`Artifact`](#artifact)

### getDownloadUrl

Get a download URL for artifact content.

Returns a URL that the client can use to download the artifact's content via HTTP GET. This avoids streaming large blobs through the gRPC control plane.

Cloud: returns a pre-signed S3 URL with a short TTL (15 minutes). OSS: returns a direct URL to the local artifact server endpoint.

Use Cases:

1. Download Artifact: - User clicks "Download" in the execution viewer - UI calls getDownloadUrl() to get a URL - Browser opens the URL in a new tab or triggers a download

2. Preview Artifact: - UI calls getDownloadUrl() for JSON/text artifacts - UI fetches content from the URL and renders inline - Large artifacts show a truncated preview with download option

3. CLI Download: - `stigmer workflow artifact download art_abc123 -o output.json` - CLI calls getDownloadUrl() then fetches via HTTP GET

Error Cases:

- NOT_FOUND: No Artifact exists with the given ID - PERMISSION_DENIED: User doesn't have view access to the parent execution - FAILED_PRECONDITION: Artifact blob has been deleted (storage_state_deleted)

<SDKTabs>
<Tab value="TypeScript">
```typescript
const artifactDownloadUrl = await stigmer.artifact.getDownloadUrl(id);
```
</Tab>
<Tab value="Go">
```go
artifactDownloadUrl, err := client.Artifact.GetDownloadUrl(ctx, id)
```
</Tab>
<Tab value="Python">
```python
artifact_download_url = client.artifacts.get_download_url(id)
```
</Tab>
<Tab value="Java">
```java
ArtifactDownloadUrl artifactDownloadUrl = client.artifacts().getDownloadUrl(id);
```
</Tab>
</SDKTabs>

**Parameters**

| Parameter | Type | Description |
|-----------|------|-------------|
| `id` | `string` | Artifact identifier. |

**Returns**: [`ArtifactDownloadUrl`](#artifactdownloadurl)

### listByExecution

List all artifacts produced by a specific execution.

Returns a paginated list of artifacts filtered by either workflow_execution_id or agent_execution_id.

Use Cases:

1. Execution Viewer Artifact Panel: - User views a workflow execution in the execution viewer - UI calls listByExecution() to populate the artifact sidebar - Each artifact shows display name, content type, size, source task

2. CLI Artifact Listing: - `stigmer workflow artifacts wex_abc123` - CLI calls listByExecution() and formats as a table

Error Cases:

- INVALID_ARGUMENT: Neither workflow_execution_id nor agent_execution_id provided - PERMISSION_DENIED: User doesn't have view access to the parent execution

<SDKTabs>
<Tab value="TypeScript">
```typescript
const result = await stigmer.artifact.listByExecution({
  workflowExecutionId: "...",
  agentExecutionId: "...",
  pageSize: 0,
  pageToken: "...",
});
```
</Tab>
<Tab value="Go">
```go
result, err := client.Artifact.ListByExecution(ctx, &stigmer.ListArtifactsByExecutionRequest{
  WorkflowExecutionId: "...",
  AgentExecutionId:    "...",
  PageSize:            0,
  PageToken:           "...",
})
```
</Tab>
<Tab value="Python">
```python
result = client.artifacts.list_by_execution(ListArtifactsByExecutionRequest(
    workflow_execution_id="...",
    agent_execution_id="...",
    page_size=0,
    page_token="...",
))
```
</Tab>
<Tab value="Java">
```java
ArtifactList result = client.artifacts().listByExecution(ListArtifactsByExecutionRequest.builder()
    .workflowExecutionId("...")
    .agentExecutionId("...")
    .pageSize(0)
    .pageToken("...")
    .build());
```
</Tab>
</SDKTabs>

**Parameters**: [`ListArtifactsByExecutionRequest`](#listartifactsbyexecutionrequest)

**Returns**: [`ArtifactList`](#artifactlist)

### create

Create a new artifact with content.

Persists the artifact's metadata and content blob. The backend: 1. Computes SHA-256 hash of the content bytes 2. Checks for existing blob with same hash (content-addressable dedup) 3. If new: writes blob to storage (filesystem in OSS, S3 in Cloud) 4. Creates Artifact metadata record with status populated 5. Returns the created Artifact

<SDKTabs>
<Tab value="TypeScript">
```typescript
const artifact = await stigmer.artifact.create({
  spec: {},
  content: "...",
});
```
</Tab>
<Tab value="Go">
```go
artifact, err := client.Artifact.Create(ctx, &stigmer.CreateArtifactInput{
  Spec:    &stigmer.ArtifactSpecInput{},
  Content: "...",
})
```
</Tab>
<Tab value="Python">
```python
artifact = client.artifacts.create(CreateArtifactInput(
    spec=None,
    content="...",
))
```
</Tab>
<Tab value="Java">
```java
Artifact artifact = client.artifacts().create(CreateArtifactInput.builder()
    .spec(null)
    .content("...")
    .build());
```
</Tab>
</SDKTabs>

**Parameters**: [`CreateArtifactInput`](#createartifactinput)

**Returns**: [`Artifact`](#artifact)

### delete

Delete an artifact.

Marks the artifact for deletion. The backend transitions the storage state to deleted and schedules the blob for garbage collection. If other artifacts reference the same content hash, the blob is retained until all references are deleted.

<SDKTabs>
<Tab value="TypeScript">
```typescript
const artifact = await stigmer.artifact.delete(id);
```
</Tab>
<Tab value="Go">
```go
artifact, err := client.Artifact.Delete(ctx, id)
```
</Tab>
<Tab value="Python">
```python
artifact = client.artifacts.delete(id)
```
</Tab>
<Tab value="Java">
```java
Artifact artifact = client.artifacts().delete(id);
```
</Tab>
</SDKTabs>

**Parameters**

| Parameter | Type | Description |
|-----------|------|-------------|
| `id` | `string` | Artifact identifier. |

**Returns**: [`Artifact`](#artifact)

## Types

### ArtifactInput

Input for creating or updating an Artifact.

<TypeTable
  type={{
    name: { type: "string", description: "Resource name.", required: true },
    slug: { type: "string", description: "URL-friendly identifier." },
    org: { type: "string", description: "Organization slug.", required: true },
    labels: { type: "Record<string, string>", description: "Key-value labels." },
    contentType: { type: "string", description: "MIME content type of the artifact payload.", required: true },
    displayName: { type: "string", description: "Human-readable display name for the artifact.", required: true },
    source: { type: "ArtifactSourceInput", description: "Provenance: which execution and task produced this artifact.", typeDescriptionLink: "#artifactsourceinput", required: true },
    retention: { type: "RetentionPolicyInput", description: "Retention policy for this artifact.", typeDescriptionLink: "#retentionpolicyinput" },
  }}
/>

### ArtifactSourceInput

ArtifactSource identifies the execution context that produced an artifact.

<TypeTable
  type={{
    workflowExecutionId: { type: "string", description: "WorkflowExecution that produced this artifact." },
    agentExecutionId: { type: "string", description: "AgentExecution that produced this artifact." },
    taskName: { type: "string", description: "Name of the task that produced this artifact." },
  }}
/>

### RetentionPolicyInput

RetentionPolicy controls how long an artifact's blob is retained in storage.

<TypeTable
  type={{
    ttlDays: { type: "number", description: "Time-to-live in days from creation." },
  }}
/>

### CreateArtifactInput

CreateArtifactInput provides the data needed to create an artifact.

<TypeTable
  type={{
    spec: { type: "ArtifactSpecInput", description: "Artifact properties (content type, display name, source, retention).", required: true },
    content: { type: "Uint8Array", description: "Raw artifact content bytes.", required: true },
  }}
/>

### ListArtifactsByExecutionRequest

ListArtifactsByExecutionRequest lists artifacts produced by a specific execution.

<TypeTable
  type={{
    workflowExecutionId: { type: "string", description: "WorkflowExecution ID to list artifacts for." },
    agentExecutionId: { type: "string", description: "AgentExecution ID to list artifacts for." },
    pageSize: { type: "number", description: "Maximum number of artifacts to return per page." },
    pageToken: { type: "string", description: "Opaque pagination token from a previous response." },
  }}
/>

### ArtifactList

ArtifactList contains a paginated list of artifacts.

<TypeTable
  type={{
    totalPages: { type: "number", description: "Total number of pages available for this query." },
    entries: { type: "ArtifactInput[]", description: "Artifacts in the current page, sorted by created_at descending.", typeDescriptionLink: "#artifactinput" },
  }}
/>

### ArtifactDownloadUrl

ArtifactDownloadUrl provides a URL for downloading artifact content.

<TypeTable
  type={{
    url: { type: "string", description: "URL to download the artifact content via HTTP GET." },
    ttlSeconds: { type: "number", description: "Time-to-live for the download URL in seconds." },
    sizeBytes: { type: "number", description: "Size of the artifact content in bytes (convenience, same as status.size_bytes)." },
    contentType: { type: "string", description: "MIME content type (convenience, same as spec.content_type)." },
  }}
/>

### Artifact

Artifact represents a persisted blob produced during execution.

<TypeTable
  type={{
    apiVersion: { type: "string", description: "API version for this resource type." },
    kind: { type: "string", description: "Resource kind identifier." },
    metadata: { type: "ApiResourceMetadata", description: "Resource metadata including id, name, org, slug, labels, visibility, and timestamps.", typeDescriptionLink: "/docs/sdk/resources/commons#apiresourcemetadata" },
    spec: { type: "ArtifactInput", description: "Resource specification. See [`ArtifactInput`](#artifactinput).", typeDescriptionLink: "#artifactinput" },
    status: { type: "ArtifactStatus", description: "System-managed state. See [`ArtifactStatus`](#artifactstatus).", typeDescriptionLink: "#artifactstatus" },
  }}
/>

### ArtifactStatus

ArtifactStatus contains system-managed state for an artifact.

<TypeTable
  type={{
    audit: { type: "ApiResourceAudit", description: "Standard audit information.", typeDescriptionLink: "/docs/sdk/resources/commons#apiresourceaudit" },
    contentHash: { type: "string", description: "SHA-256 hash of the artifact content, hex-encoded (64 characters)." },
    sizeBytes: { type: "number", description: "Size of the artifact content in bytes." },
    storageState: { type: "ArtifactStorageState", description: "Current storage state of the artifact's blob.", typeDescriptionLink: "#artifactstoragestate" },
    expiresAt: { type: "string", description: "ISO 8601 timestamp when this artifact expires and becomes eligible for garbage collection." },
  }}
/>

### ArtifactStorageState

ArtifactStorageState tracks the lifecycle of an artifact's blob in storage.

| Value | Description |
|-------|-------------|
| `storage_state_pending` | Metadata created, blob upload in progress or not yet confirmed. |
| `storage_state_stored` | Blob successfully stored and content hash verified. |
| `storage_state_deleted` | Blob deleted from storage (retention expired or manual deletion). |

