---
title: Session
---

{/* Auto-generated by stigmer-codegen. DO NOT EDIT. */}

A Session is a persistent conversation context that groups multiple executions
together. It preserves the message thread, workspace files, and sandbox state
across every turn in the conversation.

```yaml
apiVersion: agentic.stigmer.ai/v1
kind: Session
metadata:
  name: refactor-auth-module
  org: acme
spec:
  agent_instance_id: agi_01j5q3k7m8r2s4tnz2hfp0q0c3
  subject: "Refactor the auth module"
  workspace_entries:
    - name: backend
      source:
        git_repo:
          url: https://github.com/acme/backend.git
          branch: main
  mcp_server_usages:
    - mcp_server_ref:
        kind: mcp_server
        slug: github
      enabled_tools: [search_code, create_pr]
  skill_refs:
    - kind: skill
      slug: go-best-practices
```

## Client Access

<SDKTabs>
<Tab value="TypeScript">
```typescript
const session = await stigmer.session.get("ses_01j5q3k7m8r2s4tnz2hfp0q0c3");
```
</Tab>
<Tab value="Go">
```go
session, err := client.Session.Get(ctx, "ses_01j5q3k7m8r2s4tnz2hfp0q0c3")
```
</Tab>
<Tab value="Python">
```python
session = client.sessions.get("ses_01j5q3k7m8r2s4tnz2hfp0q0c3")
```
</Tab>
<Tab value="Java">
```java
Session session = client.sessions().get("ses_01j5q3k7m8r2s4tnz2hfp0q0c3");
```
</Tab>
</SDKTabs>

## Methods

| Method | Description |
|--------|-------------|
| [`get`](#get) | Get a single session by ID. |
| [`list`](#list) | List all sessions with pagination and optional filtering. |
| [`listByAgent`](#listbyagent) | List all sessions for a specific agent. |
| [`apply`](#apply) | Create or update a session. |
| [`create`](#create) | Create a session. |
| [`update`](#update) | Update an existing session (e.g., subject, thread_id). |
| [`updateSessionMemory`](#updatesessionmemory) | Persist session memory after execution. |
| [`updateSubject`](#updatesubject) | Set the session subject. |
| [`delete`](#delete) | Delete a session. |

### get

Get a single session by ID.

<SDKTabs>
<Tab value="TypeScript">
```typescript
const session = await stigmer.session.get(id);
```
</Tab>
<Tab value="Go">
```go
session, err := client.Session.Get(ctx, id)
```
</Tab>
<Tab value="Python">
```python
session = client.sessions.get(id)
```
</Tab>
<Tab value="Java">
```java
Session session = client.sessions().get(id);
```
</Tab>
</SDKTabs>

**Parameters**

| Parameter | Type | Description |
|-----------|------|-------------|
| `id` | `string` | Session identifier. |

**Returns**: [`Session`](#session)

### list

List all sessions with pagination and optional filtering.

<SDKTabs>
<Tab value="TypeScript">
```typescript
const result = await stigmer.session.list({
  pageSize: 0,
  pageToken: "...",
  tags: [],
});
```
</Tab>
<Tab value="Go">
```go
result, err := client.Session.List(ctx, &stigmer.ListSessionsRequest{
  PageSize:  0,
  PageToken: "...",
  Tags:      []string{},
})
```
</Tab>
<Tab value="Python">
```python
result = client.sessions.list(ListSessionsRequest(
    page_size=0,
    page_token="...",
    tags=[],
))
```
</Tab>
<Tab value="Java">
```java
SessionList result = client.sessions().list(ListSessionsRequest.builder()
    .pageSize(0)
    .pageToken("...")
    .tags(List.of())
    .build());
```
</Tab>
</SDKTabs>

**Parameters**: [`ListSessionsRequest`](#listsessionsrequest)

**Returns**: [`SessionList`](#sessionlist)

### listByAgent

List all sessions for a specific agent.

<SDKTabs>
<Tab value="TypeScript">
```typescript
const result = await stigmer.session.listByAgent({
  agentId: "...",
  pageSize: 0,
  pageToken: "...",
});
```
</Tab>
<Tab value="Go">
```go
result, err := client.Session.ListByAgent(ctx, &stigmer.ListSessionsByAgentRequest{
  AgentId:   "...",
  PageSize:  0,
  PageToken: "...",
})
```
</Tab>
<Tab value="Python">
```python
result = client.sessions.list_by_agent(ListSessionsByAgentRequest(
    agent_id="...",
    page_size=0,
    page_token="...",
))
```
</Tab>
<Tab value="Java">
```java
SessionList result = client.sessions().listByAgent(ListSessionsByAgentRequest.builder()
    .agentId("...")
    .pageSize(0)
    .pageToken("...")
    .build());
```
</Tab>
</SDKTabs>

**Parameters**: [`ListSessionsByAgentRequest`](#listsessionsbyagentrequest)

**Returns**: [`SessionList`](#sessionlist)

### apply

Create or update a session.

<SDKTabs>
<Tab value="TypeScript">
```typescript
const session = await stigmer.session.apply({
  name: "my-session",
  org: "acme",
  slug: "...",
  labels: {},
  agentInstanceId: "...",
  subject: "...",
  harnessStateId: "...",
  metadata: {},
  workspaceEntries: [],
  mcpServerUsages: [],
  skillRefs: [],
  harness: "...",
  cursorMode: "...",
  executionTarget: "...",
});
```
</Tab>
<Tab value="Go">
```go
session, err := client.Session.Apply(ctx, &stigmer.SessionInput{
  Name:             "my-session",
  Org:              "acme",
  Slug:             "...",
  Labels:           map[string]string{},
  AgentInstanceId:  "...",
  Subject:          "...",
  HarnessStateId:   "...",
  Metadata:         map[string]string{},
  WorkspaceEntries: []stigmer.WorkspaceEntryInput{},
  McpServerUsages:  []stigmer.McpServerUsageInput{},
  SkillRefs:        []stigmer.ResourceRef{},
  Harness:          "...",
  CursorMode:       "...",
  ExecutionTarget:  "...",
})
```
</Tab>
<Tab value="Python">
```python
session = client.sessions.apply(SessionInput(
    name="my-session",
    org="acme",
    slug="...",
    labels={},
    agent_instance_id="...",
    subject="...",
    harness_state_id="...",
    metadata={},
    workspace_entries=[],
    mcp_server_usages=[],
    skill_refs=[],
    harness="...",
    cursor_mode="...",
    execution_target="...",
))
```
</Tab>
<Tab value="Java">
```java
Session session = client.sessions().apply(SessionInput.builder()
    .name("my-session")
    .org("acme")
    .slug("...")
    .labels(Map.of())
    .agentInstanceId("...")
    .subject("...")
    .harnessStateId("...")
    .metadata(Map.of())
    .workspaceEntries(List.of())
    .mcpServerUsages(List.of())
    .skillRefs(List.of())
    .harness("...")
    .cursorMode("...")
    .executionTarget("...")
    .build());
```
</Tab>
</SDKTabs>

**Parameters**: [`SessionInput`](#sessioninput)

**Returns**: [`Session`](#session)

### create

Create a session.

<SDKTabs>
<Tab value="TypeScript">
```typescript
const session = await stigmer.session.create({
  name: "my-session",
  org: "acme",
  slug: "...",
  labels: {},
  agentInstanceId: "...",
  subject: "...",
  harnessStateId: "...",
  metadata: {},
  workspaceEntries: [],
  mcpServerUsages: [],
  skillRefs: [],
  harness: "...",
  cursorMode: "...",
  executionTarget: "...",
});
```
</Tab>
<Tab value="Go">
```go
session, err := client.Session.Create(ctx, &stigmer.SessionInput{
  Name:             "my-session",
  Org:              "acme",
  Slug:             "...",
  Labels:           map[string]string{},
  AgentInstanceId:  "...",
  Subject:          "...",
  HarnessStateId:   "...",
  Metadata:         map[string]string{},
  WorkspaceEntries: []stigmer.WorkspaceEntryInput{},
  McpServerUsages:  []stigmer.McpServerUsageInput{},
  SkillRefs:        []stigmer.ResourceRef{},
  Harness:          "...",
  CursorMode:       "...",
  ExecutionTarget:  "...",
})
```
</Tab>
<Tab value="Python">
```python
session = client.sessions.create(SessionInput(
    name="my-session",
    org="acme",
    slug="...",
    labels={},
    agent_instance_id="...",
    subject="...",
    harness_state_id="...",
    metadata={},
    workspace_entries=[],
    mcp_server_usages=[],
    skill_refs=[],
    harness="...",
    cursor_mode="...",
    execution_target="...",
))
```
</Tab>
<Tab value="Java">
```java
Session session = client.sessions().create(SessionInput.builder()
    .name("my-session")
    .org("acme")
    .slug("...")
    .labels(Map.of())
    .agentInstanceId("...")
    .subject("...")
    .harnessStateId("...")
    .metadata(Map.of())
    .workspaceEntries(List.of())
    .mcpServerUsages(List.of())
    .skillRefs(List.of())
    .harness("...")
    .cursorMode("...")
    .executionTarget("...")
    .build());
```
</Tab>
</SDKTabs>

**Parameters**: [`SessionInput`](#sessioninput)

**Returns**: [`Session`](#session)

### update

Update an existing session (e.g., subject, thread_id).

<SDKTabs>
<Tab value="TypeScript">
```typescript
const session = await stigmer.session.update({
  name: "my-session",
  org: "acme",
  slug: "...",
  labels: {},
  agentInstanceId: "...",
  subject: "...",
  harnessStateId: "...",
  metadata: {},
  workspaceEntries: [],
  mcpServerUsages: [],
  skillRefs: [],
  harness: "...",
  cursorMode: "...",
  executionTarget: "...",
});
```
</Tab>
<Tab value="Go">
```go
session, err := client.Session.Update(ctx, &stigmer.SessionInput{
  Name:             "my-session",
  Org:              "acme",
  Slug:             "...",
  Labels:           map[string]string{},
  AgentInstanceId:  "...",
  Subject:          "...",
  HarnessStateId:   "...",
  Metadata:         map[string]string{},
  WorkspaceEntries: []stigmer.WorkspaceEntryInput{},
  McpServerUsages:  []stigmer.McpServerUsageInput{},
  SkillRefs:        []stigmer.ResourceRef{},
  Harness:          "...",
  CursorMode:       "...",
  ExecutionTarget:  "...",
})
```
</Tab>
<Tab value="Python">
```python
session = client.sessions.update(SessionInput(
    name="my-session",
    org="acme",
    slug="...",
    labels={},
    agent_instance_id="...",
    subject="...",
    harness_state_id="...",
    metadata={},
    workspace_entries=[],
    mcp_server_usages=[],
    skill_refs=[],
    harness="...",
    cursor_mode="...",
    execution_target="...",
))
```
</Tab>
<Tab value="Java">
```java
Session session = client.sessions().update(SessionInput.builder()
    .name("my-session")
    .org("acme")
    .slug("...")
    .labels(Map.of())
    .agentInstanceId("...")
    .subject("...")
    .harnessStateId("...")
    .metadata(Map.of())
    .workspaceEntries(List.of())
    .mcpServerUsages(List.of())
    .skillRefs(List.of())
    .harness("...")
    .cursorMode("...")
    .executionTarget("...")
    .build());
```
</Tab>
</SDKTabs>

**Parameters**: [`SessionInput`](#sessioninput)

**Returns**: [`Session`](#session)

### updateSessionMemory

Persist session memory after execution.

Atomically updates only status.session_memory, leaving all other session fields untouched. Called by the cursor-runner after each completed execution turn.

<SDKTabs>
<Tab value="TypeScript">
```typescript
const session = await stigmer.session.updateSessionMemory({
  id: "...",
  sessionMemory: {},
});
```
</Tab>
<Tab value="Go">
```go
session, err := client.Session.UpdateSessionMemory(ctx, &stigmer.UpdateSessionMemoryRequest{
  Id:            "...",
  SessionMemory: &stigmer.SessionMemoryInput{},
})
```
</Tab>
<Tab value="Python">
```python
session = client.sessions.update_session_memory(UpdateSessionMemoryRequest(
    id="...",
    session_memory=None,
))
```
</Tab>
<Tab value="Java">
```java
Session session = client.sessions().updateSessionMemory(UpdateSessionMemoryRequest.builder()
    .id("...")
    .sessionMemory(null)
    .build());
```
</Tab>
</SDKTabs>

**Parameters**: [`UpdateSessionMemoryRequest`](#updatesessionmemoryrequest)

**Returns**: [`Session`](#session)

### updateSubject

Set the session subject.

This is a targeted update that modifies only the subject field, leaving other session fields untouched. Use this instead of the full update RPC when you only need to change the session subject.

<SDKTabs>
<Tab value="TypeScript">
```typescript
const session = await stigmer.session.updateSubject({
  id: "...",
  subject: "...",
});
```
</Tab>
<Tab value="Go">
```go
session, err := client.Session.UpdateSubject(ctx, &stigmer.UpdateSessionSubjectRequest{
  Id:      "...",
  Subject: "...",
})
```
</Tab>
<Tab value="Python">
```python
session = client.sessions.update_subject(UpdateSessionSubjectRequest(
    id="...",
    subject="...",
))
```
</Tab>
<Tab value="Java">
```java
Session session = client.sessions().updateSubject(UpdateSessionSubjectRequest.builder()
    .id("...")
    .subject("...")
    .build());
```
</Tab>
</SDKTabs>

**Parameters**: [`UpdateSessionSubjectRequest`](#updatesessionsubjectrequest)

**Returns**: [`Session`](#session)

### delete

Delete a session.

<SDKTabs>
<Tab value="TypeScript">
```typescript
const session = await stigmer.session.delete(id);
```
</Tab>
<Tab value="Go">
```go
session, err := client.Session.Delete(ctx, id)
```
</Tab>
<Tab value="Python">
```python
session = client.sessions.delete(id)
```
</Tab>
<Tab value="Java">
```java
Session session = client.sessions().delete(id);
```
</Tab>
</SDKTabs>

**Parameters**

| Parameter | Type | Description |
|-----------|------|-------------|
| `id` | `string` | Session identifier. |

**Returns**: [`Session`](#session)

## Types

### SessionInput

Input for creating or updating a Session.

<TypeTable
  type={{
    name: { type: "string", description: "Resource name.", required: true },
    slug: { type: "string", description: "URL-friendly identifier." },
    org: { type: "string", description: "Organization slug.", required: true },
    labels: { type: "Record<string, string>", description: "Key-value labels." },
    agentInstanceId: { type: "string", description: "Agent instance this session runs against." },
    subject: { type: "string", description: "Conversation title for UI display." },
    harnessStateId: { type: "string", description: "Harness-specific state identifier for conversation continuity." },
    metadata: { type: "Record<string, string>", description: "Custom key-value pairs for client-specific information." },
    workspaceEntries: { type: "WorkspaceEntryInput[]", description: "Workspace entries for this session.", typeDescriptionLink: "#workspaceentryinput" },
    mcpServerUsages: { type: "McpServerUsageInput[]", description: "MCP servers to make available in this session.", typeDescriptionLink: "#mcpserverusageinput" },
    skillRefs: { type: "ResourceRef[]", description: "Skills to inject into this session's context.", typeDescriptionLink: "#resourceref" },
    harness: { type: "Harness", description: "Execution harness for this session.", typeDescriptionLink: "#harness" },
    cursorMode: { type: "CursorMode", description: "Cursor SDK agent mode for this session.", typeDescriptionLink: "#cursormode" },
    executionTarget: { type: "ExecutionTarget", description: "Where session activities are executed — local client or cloud sandbox.", typeDescriptionLink: "#executiontarget" },
  }}
/>

### WorkspaceEntryInput

WorkspaceEntry pairs a WorkspaceSource with a human-readable name, forming an addressable unit within a session's workspace.

<TypeTable
  type={{
    name: { type: "string", description: "Short identifier for this workspace entry." },
    source: { type: "WorkspaceSourceInput", description: "The source that provides this entry's content.", typeDescriptionLink: "#workspacesourceinput", required: true },
  }}
/>

### WorkspaceSourceInput

WorkspaceSource defines where the workspace content comes from.

<TypeTable
  type={{
    gitRepo: { type: "GitRepoSourceInput", description: "Clone a git repository as the workspace source.", typeDescriptionLink: "#gitreposourceinput" },
    localPath: { type: "LocalPathSourceInput", description: "Use an existing local directory as the workspace source.", typeDescriptionLink: "#localpathsourceinput" },
  }}
/>

### GitRepoSourceInput

GitRepoSource provisions a workspace by cloning a git repository.

<TypeTable
  type={{
    url: { type: "string", description: "HTTPS clone URL for the repository.", required: true },
    branch: { type: "string", description: "Branch to clone." },
    commit: { type: "string", description: "Commit SHA to checkout after cloning." },
    depth: { type: "number", description: "Number of commits to include in the clone history." },
    writeBackMode: { type: "GitWriteBackMode", description: "Controls whether the platform creates a branch and pull request from the agent's file changes.", typeDescriptionLink: "#gitwritebackmode" },
  }}
/>

### LocalPathSourceInput

LocalPathSource uses an existing directory on the host filesystem as the workspace.

<TypeTable
  type={{
    path: { type: "string", description: "Absolute path to an existing directory on the host filesystem." },
  }}
/>

### McpServerUsageInput

McpServerUsage declares that this agent uses a McpServer resource.

<TypeTable
  type={{
    mcpServerRef: { type: "ResourceRef", description: "Reference to the McpServer resource.", typeDescriptionLink: "#resourceref", required: true },
    enabledTools: { type: "string[]", description: "Tools to enable from this MCP server for this agent." },
    toolApprovalOverrides: { type: "ToolApprovalOverrideInput[]", description: "Override approval requirements for specific tools.", typeDescriptionLink: "#toolapprovaloverrideinput" },
  }}
/>

### ResourceRef

Generic reference to any API resource by org and slug.

<TypeTable
  type={{
    org: { type: "string", description: "Organization that owns the referenced resource." },
    kind: { type: "ApiResourceKind", description: "Kind of the referenced resource (e.g., SKILL, AGENT, MCP_SERVER).", typeDescriptionLink: "/docs/sdk/resources/commons#apiresourcekind" },
    slug: { type: "string", description: "Resource slug (user-friendly identifier, unique within org).", required: true },
    version: { type: "string", description: "Version of the resource (optional, only applicable to versioned resources like Skills)." },
  }}
/>

### ToolApprovalOverrideInput

ToolApprovalOverride allows per-agent customization of approval requirements.

<TypeTable
  type={{
    toolName: { type: "string", description: "Name of the tool to override." },
    requiresApproval: { type: "boolean", description: "Whether this tool requires approval for this agent." },
    message: { type: "string", description: "Custom approval message shown to the reviewer." },
  }}
/>

### UpdateSessionSubjectRequest

UpdateSessionSubjectRequest sets the conversation title for a session.

<TypeTable
  type={{
    id: { type: "string", description: "Session ID to update.", required: true },
    subject: { type: "string", description: "New subject value." },
  }}
/>

### UpdateSessionMemoryRequest

UpdateSessionMemoryRequest persists durable session memory after each completed execution turn.

<TypeTable
  type={{
    id: { type: "string", description: "Session ID to update.", required: true },
    sessionMemory: { type: "SessionMemoryInput", description: "The session memory to persist." },
  }}
/>

### ListSessionsRequest

ListSessionsRequest specifies parameters for listing sessions.

<TypeTable
  type={{
    pageSize: { type: "number", description: "Maximum number of sessions to return per page." },
    pageToken: { type: "string", description: "Token for pagination, obtained from previous response." },
    tags: { type: "string[]", description: "Filter by tags (optional)." },
  }}
/>

### SessionList

SessionList contains a paginated list of sessions.

<TypeTable
  type={{
    totalPages: { type: "number", description: "Total number of pages available." },
    entries: { type: "SessionInput[]", description: "Sessions in the current page.", typeDescriptionLink: "#sessioninput" },
  }}
/>

### ListSessionsByAgentRequest

ListSessionsByAgentRequest lists all sessions for a specific agent.

<TypeTable
  type={{
    agentId: { type: "string", description: "Agent ID to filter by.", required: true },
    pageSize: { type: "number", description: "Maximum number of sessions to return per page." },
    pageToken: { type: "string", description: "Token for pagination, obtained from previous response." },
  }}
/>

### Session

Session represents a multi-turn conversation thread with an agent.

<TypeTable
  type={{
    apiVersion: { type: "string", description: "API version for this resource type." },
    kind: { type: "string", description: "Resource kind identifier." },
    metadata: { type: "ApiResourceMetadata", description: "Resource metadata including id, name, org, slug, labels, visibility, and timestamps.", typeDescriptionLink: "/docs/sdk/resources/commons#apiresourcemetadata" },
    spec: { type: "SessionInput", description: "Resource specification. See [`SessionInput`](#sessioninput).", typeDescriptionLink: "#sessioninput" },
    status: { type: "SessionStatus", description: "System-managed state. See [`SessionStatus`](#sessionstatus).", typeDescriptionLink: "#sessionstatus" },
  }}
/>

### SessionStatus

SessionStatus contains system-managed runtime state for a session.

<TypeTable
  type={{
    sessionMemory: { type: "SessionMemory", description: "Durable session memory for continuation across agent evictions.", typeDescriptionLink: "#sessionmemory" },
    audit: { type: "ApiResourceAudit", description: "Standard audit information (created_at, updated_at, created_by, etc.).", typeDescriptionLink: "/docs/sdk/resources/commons#apiresourceaudit" },
  }}
/>

### ConversationTurn

ConversationTurn captures a single message in the conversation for replay.

<TypeTable
  type={{
    role: { type: "string", description: "The role of the message author." },
    content: { type: "string", description: "The message content, potentially truncated." },
    timestamp: { type: "string", description: "ISO 8601 timestamp when the message was sent." },
  }}
/>

### SessionMemory

SessionMemory captures durable conversation state that survives agent eviction, process restarts, and Cursor SDK "agent not found" failures.

<TypeTable
  type={{
    durableSummary: { type: "string", description: "High-level narrative of what has been accomplished so far." },
    workspaceDigest: { type: "string", description: "Digest of the workspace state at the time of last persistence." },
    changedFiles: { type: "string[]", description: "Files created or modified during the session." },
    openTasks: { type: "string[]", description: "Outstanding tasks or questions the agent identified but has not completed." },
    toolObservations: { type: "ToolObservation[]", description: "Summaries of significant tool executions during the session.", typeDescriptionLink: "#toolobservation" },
    recentTurns: { type: "ConversationTurn[]", description: "Recent conversation turns for verbatim replay in the continuation prompt.", typeDescriptionLink: "#conversationturn" },
    decisions: { type: "string[]", description: "Explicit decisions the agent made during the session." },
    failedAttempts: { type: "string[]", description: "Approaches that were tried and failed during the session." },
  }}
/>

### ToolObservation

ToolObservation captures the outcome of a significant tool execution.

<TypeTable
  type={{
    command: { type: "string", description: "The command or tool invocation that was executed." },
    cwd: { type: "string", description: "Working directory where the command was executed." },
    exitCode: { type: "number", description: "Process exit code (0 = success)." },
    summary: { type: "string", description: "Short summary of the tool output." },
  }}
/>

### Harness

Harness identifies the execution engine that processes agent activities for a session.

| Value | Description |
|-------|-------------|
| `HARNESS_NATIVE` | Stigmer's native execution engine. |
| `HARNESS_CURSOR` | Cursor SDK execution engine. |

### CursorMode

CursorMode selects the Cursor SDK agent type for sessions using HARNESS_CURSOR.

| Value | Description |
|-------|-------------|
| `CURSOR_MODE_LOCAL` | Local Cursor agent created with a local cwd configuration. |
| `CURSOR_MODE_CLOUD` | Cloud Cursor agent created with a cloud repos configuration. |

### ExecutionTarget

ExecutionTarget specifies where session activities are executed.

| Value | Description |
|-------|-------------|
| `EXECUTION_TARGET_LOCAL` | Client's embedded runner handles activities. |
| `EXECUTION_TARGET_CLOUD` | Server provisions a cloud sandbox for execution. |

### GitWriteBackMode

GitWriteBackMode controls the platform's git workflow for a git-backed workspace entry.

| Value | Description |
|-------|-------------|
| `GIT_WRITE_BACK_BRANCH_AND_PR` | Create a branch and pull request from the agent's file changes after execution completes. |

