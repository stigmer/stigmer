---
title: Invitation
description: "SDK reference for Invitation resources."
---

{/* Auto-generated by stigmer-codegen. DO NOT EDIT. */}

An invitation is a shareable link that grants a specific role to anyone who redeems it. Invitations can be multi-use (persistent org invite link) or single-use (targeted invitation for a specific person).

Example YAML (persistent org invite link): apiVersion: iam.stigmer.ai/v1 kind: Invitation metadata: name: engineering-team-link org: acme spec: role: viewer max_redemptions: 0 expires_at: "2026-05-06T00:00:00Z" label: "Engineering team invite"

Example YAML (single-use targeted invite): apiVersion: iam.stigmer.ai/v1 kind: Invitation metadata: name: contractor-invite org: acme spec: role: member max_redemptions: 1 expires_at: "2026-04-13T00:00:00Z" label: "Contractor onboarding"

## Client Access

<SDKTabs>
<Tab value="TypeScript">
```typescript
const invitation = await stigmer.invitation.get("inv_01j5q3k7m8r2s4tnz2hfp0q0c3");
```
</Tab>
<Tab value="Go">
```go
invitation, err := client.Invitation.Get(ctx, "inv_01j5q3k7m8r2s4tnz2hfp0q0c3")
```
</Tab>
<Tab value="Python">
```python
invitation = client.invitations.get("inv_01j5q3k7m8r2s4tnz2hfp0q0c3")
```
</Tab>
<Tab value="Java">
```java
Invitation invitation = client.invitations().get("inv_01j5q3k7m8r2s4tnz2hfp0q0c3");
```
</Tab>
</SDKTabs>

## Methods

| Method | Description |
|--------|-------------|
| [`get`](#get) | Get an invitation by its unique identifier. |
| [`getByToken`](#getbytoken) | Get a preview of an invitation by its shareable token. |
| [`listByOrg`](#listbyorg) | List all invitations belonging to an organization. |
| [`create`](#create) | Create an invitation link for an organization. |
| [`redeem`](#redeem) | Redeem an invitation to join an organization. |
| [`revoke`](#revoke) | Revoke an active invitation, preventing further redemptions. |

### get

Get an invitation by its unique identifier.

<SDKTabs>
<Tab value="TypeScript">
```typescript
const invitation = await stigmer.invitation.get(id);
```
</Tab>
<Tab value="Go">
```go
invitation, err := client.Invitation.Get(ctx, id)
```
</Tab>
<Tab value="Python">
```python
invitation = client.invitations.get(id)
```
</Tab>
<Tab value="Java">
```java
Invitation invitation = client.invitations().get(id);
```
</Tab>
</SDKTabs>

**Parameters**

| Parameter | Type | Description |
|-----------|------|-------------|
| `id` | `string` | Invitation identifier. |

**Returns**: [`Invitation`](#invitation)

### getByToken

Get a preview of an invitation by its shareable token.

Returns a safe projection (InvitationPreview) containing only the information needed to render the invite acceptance page: organization name, logo, the role being offered, and whether the invitation is still valid.

This endpoint is called by the web app's invite page before the user has authenticated, so it requires no authorization. The response intentionally omits the token value, redemption history, and internal invitation metadata.

<SDKTabs>
<Tab value="TypeScript">
```typescript
const invitationPreview = await stigmer.invitation.getByToken({
  token: "...",
});
```
</Tab>
<Tab value="Go">
```go
invitationPreview, err := client.Invitation.GetByToken(ctx, &stigmer.InvitationTokenInput{
  Token: "...",
})
```
</Tab>
<Tab value="Python">
```python
invitation_preview = client.invitations.get_by_token(InvitationTokenInput(
    token="...",
))
```
</Tab>
<Tab value="Java">
```java
InvitationPreview invitationPreview = client.invitations().getByToken(InvitationTokenInput.builder()
    .token("...")
    .build());
```
</Tab>
</SDKTabs>

**Parameters**: [`InvitationTokenInput`](#invitationtokeninput)

**Returns**: [`InvitationPreview`](#invitationpreview)

### listByOrg

List all invitations belonging to an organization.

Returns invitations ordered by creation time (newest first).

<SDKTabs>
<Tab value="TypeScript">
```typescript
const invitations = await stigmer.invitation.listByOrg({
  org: "...",
});
```
</Tab>
<Tab value="Go">
```go
invitations, err := client.Invitation.ListByOrg(ctx, &stigmer.ListInvitationsByOrgInput{
  Org: "...",
})
```
</Tab>
<Tab value="Python">
```python
invitations = client.invitations.list_by_org(ListInvitationsByOrgInput(
    org="...",
))
```
</Tab>
<Tab value="Java">
```java
Invitations invitations = client.invitations().listByOrg(ListInvitationsByOrgInput.builder()
    .org("...")
    .build());
```
</Tab>
</SDKTabs>

**Parameters**: [`ListInvitationsByOrgInput`](#listinvitationsbyorginput)

**Returns**: [`Invitations`](#invitations)

### create

Create an invitation link for an organization.

Generates a cryptographically random token and returns the full invitation resource including the token. The invite URL is constructed as: https://&lt;host&gt;/invite/&lt;token&gt;

The specified role must be in the organization's grantable_roles. Platform-managed organizations cannot create invitations.

<SDKTabs>
<Tab value="TypeScript">
```typescript
const invitation = await stigmer.invitation.create({
  name: "my-invitation",
  org: "acme",
  slug: "...",
  labels: {},
  role: "...",
  maxRedemptions: 0,
  expiresAt: "...",
  label: "...",
});
```
</Tab>
<Tab value="Go">
```go
invitation, err := client.Invitation.Create(ctx, &stigmer.InvitationInput{
  Name:           "my-invitation",
  Org:            "acme",
  Slug:           "...",
  Labels:         map[string]string{},
  Role:           "...",
  MaxRedemptions: 0,
  ExpiresAt:      "...",
  Label:          "...",
})
```
</Tab>
<Tab value="Python">
```python
invitation = client.invitations.create(InvitationInput(
    name="my-invitation",
    org="acme",
    slug="...",
    labels={},
    role="...",
    max_redemptions=0,
    expires_at="...",
    label="...",
))
```
</Tab>
<Tab value="Java">
```java
Invitation invitation = client.invitations().create(InvitationInput.builder()
    .name("my-invitation")
    .org("acme")
    .slug("...")
    .labels(Map.of())
    .role("...")
    .maxRedemptions(0)
    .expiresAt("...")
    .label("...")
    .build());
```
</Tab>
</SDKTabs>

**Parameters**: [`InvitationInput`](#invitationinput)

**Returns**: [`Invitation`](#invitation)

### redeem

Redeem an invitation to join an organization.

Creates an IAM policy granting the invitation's configured role to the authenticated user on the invitation's organization. The redemption is atomic: the IAM policy is created and the redemption count is incremented in a single operation.

Validation: - Invitation must be in active state - Invitation must not be expired - Invitation must not have reached max_redemptions (if &gt; 0) - Redeemer must not already be a member of the organization

<SDKTabs>
<Tab value="TypeScript">
```typescript
const invitation = await stigmer.invitation.redeem({
  token: "...",
});
```
</Tab>
<Tab value="Go">
```go
invitation, err := client.Invitation.Redeem(ctx, &stigmer.RedeemInvitationInput{
  Token: "...",
})
```
</Tab>
<Tab value="Python">
```python
invitation = client.invitations.redeem(RedeemInvitationInput(
    token="...",
))
```
</Tab>
<Tab value="Java">
```java
Invitation invitation = client.invitations().redeem(RedeemInvitationInput.builder()
    .token("...")
    .build());
```
</Tab>
</SDKTabs>

**Parameters**: [`RedeemInvitationInput`](#redeeminvitationinput)

**Returns**: [`Invitation`](#invitation)

### revoke

Revoke an active invitation, preventing further redemptions.

Sets the invitation state to revoked. Idempotent — revoking an already-revoked invitation is a no-op.

<SDKTabs>
<Tab value="TypeScript">
```typescript
const invitation = await stigmer.invitation.revoke(id);
```
</Tab>
<Tab value="Go">
```go
invitation, err := client.Invitation.Revoke(ctx, id)
```
</Tab>
<Tab value="Python">
```python
invitation = client.invitations.revoke(id)
```
</Tab>
<Tab value="Java">
```java
Invitation invitation = client.invitations().revoke(id);
```
</Tab>
</SDKTabs>

**Parameters**

| Parameter | Type | Description |
|-----------|------|-------------|
| `id` | `string` | Invitation identifier. |

**Returns**: [`Invitation`](#invitation)

## Types

### InvitationInput

Input for creating or updating an Invitation.

<TypeTable
  type={{
    name: { type: "string", description: "Resource name.", required: true },
    slug: { type: "string", description: "URL-friendly identifier." },
    org: { type: "string", description: "Organization slug.", required: true },
    labels: { type: "Record<string, string>", description: "Key-value labels." },
    role: { type: "IamRole", description: "The role granted to the identity account upon redemption.", typeDescriptionLink: "#iamrole", required: true },
    maxRedemptions: { type: "number", description: "Maximum number of times this invitation can be redeemed." },
    expiresAt: { type: "string", description: "When this invitation expires.", required: true },
    label: { type: "string", description: "Human-readable label for organizational purposes." },
  }}
/>

### RedeemInvitationInput

RedeemInvitationInput is the request to redeem an invitation.

<TypeTable
  type={{
    token: { type: "string", description: "The cryptographically random token from the invite URL." },
  }}
/>

### ListInvitationsByOrgInput

ListInvitationsByOrgInput specifies the organization whose invitations should be returned.

<TypeTable
  type={{
    org: { type: "string", description: "Organization slug to list invitations for." },
  }}
/>

### Invitations

Invitations contains a list of invitation resources.

<TypeTable
  type={{
    entries: { type: "InvitationInput[]", description: "Invitation entries.", typeDescriptionLink: "#invitationinput" },
  }}
/>

### InvitationTokenInput

InvitationTokenInput identifies an invitation by its shareable token.

<TypeTable
  type={{
    token: { type: "string", description: "The cryptographically random token from the invite URL." },
  }}
/>

### InvitationPreview

InvitationPreview is a safe projection of an invitation for unauthenticated users viewing an invite link.

<TypeTable
  type={{
    organizationName: { type: "string", description: "Display name of the organization." },
    organizationSlug: { type: "string", description: "URL-friendly identifier of the organization." },
    organizationLogoUrl: { type: "string", description: "URL to the organization's logo image." },
    role: { type: "IamRole", description: "The role that will be granted upon redemption.", typeDescriptionLink: "#iamrole" },
    expiresAt: { type: "string", description: "When this invitation expires." },
    label: { type: "string", description: "Human-readable label describing the invitation's purpose." },
    isValid: { type: "boolean", description: "Whether this invitation can currently be redeemed." },
    invalidReason: { type: "string", description: "Human-readable reason when is_valid is false." },
  }}
/>

### Invitation

Invitation is a shareable link that grants org membership with a configurable role.

<TypeTable
  type={{
    apiVersion: { type: "string", description: "API version for this resource type." },
    kind: { type: "string", description: "Resource kind identifier." },
    metadata: { type: "ApiResourceMetadata", description: "Resource metadata including id, name, org, slug, labels, visibility, and timestamps.", typeDescriptionLink: "/docs/sdk/resources/commons#apiresourcemetadata" },
    spec: { type: "InvitationInput", description: "Resource specification. See [`InvitationInput`](#invitationinput).", typeDescriptionLink: "#invitationinput" },
    status: { type: "InvitationStatus", description: "System-managed state. See [`InvitationStatus`](#invitationstatus).", typeDescriptionLink: "#invitationstatus" },
  }}
/>

### InvitationStatus

InvitationStatus contains system-managed state for an invitation.

<TypeTable
  type={{
    token: { type: "string", description: "Server-generated cryptographically random token." },
    state: { type: "InvitationState", description: "Current lifecycle state of the invitation.", typeDescriptionLink: "#invitationstate" },
    redemptionCount: { type: "number", description: "Number of times this invitation has been successfully redeemed." },
    redemptions: { type: "InvitationRedemption[]", description: "Audit trail of each successful redemption.", typeDescriptionLink: "#invitationredemption" },
    audit: { type: "ApiResourceAudit", description: "Standard audit information (created_at, updated_at, created_by, etc.).", typeDescriptionLink: "/docs/sdk/resources/commons#apiresourceaudit" },
  }}
/>

### InvitationRedemption

InvitationRedemption records a single successful redemption event.

<TypeTable
  type={{
    identityAccountId: { type: "string", description: "The identity account that redeemed the invitation." },
    redeemedAt: { type: "string", description: "When the redemption occurred." },
  }}
/>

### IamRole

IamRole defines the roles that can be assigned to principals on resources via IAM policies.

| Value | Description |
|-------|-------------|
| `owner` | Full control of the resource. |
| `admin` | Administrative access. |
| `member` | Membership-level access. |
| `viewer` | Read-only access to the resource. |
| `participant` | Conversation participant on an agent channel: may read the channel's conversations and speak to its customers as the business (reply, take over, hand back, clear attention). |

### InvitationState

InvitationState tracks the lifecycle of an invitation link.

| Value | Description |
|-------|-------------|
| `active` | The invitation is valid and can be redeemed. |
| `expired` | The invitation has passed its expires_at timestamp. |
| `revoked` | An org admin explicitly revoked the invitation. |
| `fully_redeemed` | The invitation reached its max_redemptions limit. |

