---
title: Team
---

{/* Auto-generated by stigmer-codegen. DO NOT EDIT. */}

A Team is a named group of an organization's people that access is shared
with as one. Sharing an agent, a workflow, a skill or a channel with a team
gives every member the access the share names; someone who joins the team
gains it, and someone who leaves the team or the organization loses it at
once.

A team belongs to its organization, and only the organization's members can be
in it. People join and leave a team through the IAM policy service, by being
granted or revoked the `member` role on it. Teams are served by Stigmer
Enterprise and Stigmer Cloud.

```yaml
apiVersion: iam.stigmer.ai/v1
kind: Team
metadata:
  name: Site Reliability
  slug: sre
  org: acme
spec:
  description: On-call engineers who run the production agents.
```

## Client Access

<SDKTabs>
<Tab value="TypeScript">
```typescript
const team = await stigmer.team.get("tm_01j5q3k7m8r2s4tnz2hfp0q0c3");
```
</Tab>
<Tab value="Go">
```go
team, err := client.Team.Get(ctx, "tm_01j5q3k7m8r2s4tnz2hfp0q0c3")
```
</Tab>
<Tab value="Python">
```python
team = client.teams.get("tm_01j5q3k7m8r2s4tnz2hfp0q0c3")
```
</Tab>
<Tab value="Java">
```java
Team team = client.teams().get("tm_01j5q3k7m8r2s4tnz2hfp0q0c3");
```
</Tab>
</SDKTabs>

## Methods

| Method | Description |
|--------|-------------|
| [`get`](#get) | Get a team by its unique identifier. |
| [`getByReference`](#getbyreference) | Get a team by its organization-scoped reference (org/slug). |
| [`listByOrg`](#listbyorg) | List the teams of an organization. |
| [`create`](#create) | Create a team in an organization. |
| [`update`](#update) | Update a team's name or description. |
| [`delete`](#delete) | Delete a team. |

### get

Get a team by its unique identifier.

<SDKTabs>
<Tab value="TypeScript">
```typescript
const team = await stigmer.team.get(id);
```
</Tab>
<Tab value="Go">
```go
team, err := client.Team.Get(ctx, id)
```
</Tab>
<Tab value="Python">
```python
team = client.teams.get(id)
```
</Tab>
<Tab value="Java">
```java
Team team = client.teams().get(id);
```
</Tab>
</SDKTabs>

**Parameters**

| Parameter | Type | Description |
|-----------|------|-------------|
| `id` | `string` | Team identifier. |

**Returns**: [`Team`](#team)

### getByReference

Get a team by its organization-scoped reference (org/slug).

<SDKTabs>
<Tab value="TypeScript">
```typescript
const team = await stigmer.team.getByReference({
  org: "...",
  kind: "...",
  slug: "...",
  version: "...",
});
```
</Tab>
<Tab value="Go">
```go
team, err := client.Team.GetByReference(ctx, &stigmer.ApiResourceReference{
  Org:     "...",
  Kind:    "...",
  Slug:    "...",
  Version: "...",
})
```
</Tab>
<Tab value="Python">
```python
team = client.teams.get_by_reference(ApiResourceReference(
    org="...",
    kind="...",
    slug="...",
    version="...",
))
```
</Tab>
<Tab value="Java">
```java
Team team = client.teams().getByReference(ApiResourceReference.builder()
    .org("...")
    .kind("...")
    .slug("...")
    .version("...")
    .build());
```
</Tab>
</SDKTabs>

**Parameters**: [`ResourceRef`](#resourceref)

**Returns**: [`Team`](#team)

### listByOrg

List the teams of an organization.

<SDKTabs>
<Tab value="TypeScript">
```typescript
const teams = await stigmer.team.listByOrg({
  org: "...",
});
```
</Tab>
<Tab value="Go">
```go
teams, err := client.Team.ListByOrg(ctx, &stigmer.ListTeamsByOrgInput{
  Org: "...",
})
```
</Tab>
<Tab value="Python">
```python
teams = client.teams.list_by_org(ListTeamsByOrgInput(
    org="...",
))
```
</Tab>
<Tab value="Java">
```java
Teams teams = client.teams().listByOrg(ListTeamsByOrgInput.builder()
    .org("...")
    .build());
```
</Tab>
</SDKTabs>

**Parameters**: [`ListTeamsByOrgInput`](#listteamsbyorginput)

**Returns**: [`Teams`](#teams)

### create

Create a team in an organization.

<SDKTabs>
<Tab value="TypeScript">
```typescript
const team = await stigmer.team.create({
  name: "my-team",
  org: "acme",
  slug: "...",
  labels: {},
  description: "...",
});
```
</Tab>
<Tab value="Go">
```go
team, err := client.Team.Create(ctx, &stigmer.TeamInput{
  Name:        "my-team",
  Org:         "acme",
  Slug:        "...",
  Labels:      map[string]string{},
  Description: "...",
})
```
</Tab>
<Tab value="Python">
```python
team = client.teams.create(TeamInput(
    name="my-team",
    org="acme",
    slug="...",
    labels={},
    description="...",
))
```
</Tab>
<Tab value="Java">
```java
Team team = client.teams().create(TeamInput.builder()
    .name("my-team")
    .org("acme")
    .slug("...")
    .labels(Map.of())
    .description("...")
    .build());
```
</Tab>
</SDKTabs>

**Parameters**: [`TeamInput`](#teaminput)

**Returns**: [`Team`](#team)

### update

Update a team's name or description.

<SDKTabs>
<Tab value="TypeScript">
```typescript
const team = await stigmer.team.update({
  name: "my-team",
  org: "acme",
  slug: "...",
  labels: {},
  description: "...",
});
```
</Tab>
<Tab value="Go">
```go
team, err := client.Team.Update(ctx, &stigmer.TeamInput{
  Name:        "my-team",
  Org:         "acme",
  Slug:        "...",
  Labels:      map[string]string{},
  Description: "...",
})
```
</Tab>
<Tab value="Python">
```python
team = client.teams.update(TeamInput(
    name="my-team",
    org="acme",
    slug="...",
    labels={},
    description="...",
))
```
</Tab>
<Tab value="Java">
```java
Team team = client.teams().update(TeamInput.builder()
    .name("my-team")
    .org("acme")
    .slug("...")
    .labels(Map.of())
    .description("...")
    .build());
```
</Tab>
</SDKTabs>

**Parameters**: [`TeamInput`](#teaminput)

**Returns**: [`Team`](#team)

### delete

Delete a team.

Deleting a team removes its memberships and every share made with it.

<SDKTabs>
<Tab value="TypeScript">
```typescript
const team = await stigmer.team.delete({ resourceId: id });
```
</Tab>
<Tab value="Go">
```go
team, err := client.Team.Delete(ctx, &stigmer.DeleteResourceInput{
  ResourceID: id,
})
```
</Tab>
<Tab value="Python">
```python
team = client.teams.delete(DeleteResourceInput(resource_id=id))
```
</Tab>
<Tab value="Java">
```java
Team team = client.teams().delete(DeleteResourceInput.builder()
    .resourceId(id)
    .build());
```
</Tab>
</SDKTabs>

**Parameters**

| Parameter | Type | Description |
|-----------|------|-------------|
| `resourceId` | `string` | ID of the resource to delete. |
| `versionMessage` | `string` | Optional deletion reason. |
| `force` | `boolean` | Force delete even if resource is in use. |

**Returns**: [`Team`](#team)

## Types

### TeamInput

Input for creating or updating a Team.

<TypeTable
  type={{
    name: { type: "string", description: "Resource name.", required: true },
    slug: { type: "string", description: "URL-friendly identifier." },
    org: { type: "string", description: "Organization slug.", required: true },
    labels: { type: "Record<string, string>", description: "Key-value labels." },
    description: { type: "string", description: "What the team is for, shown where people pick a team to share with." },
  }}
/>

### ResourceRef

Generic reference to any API resource by org and slug.

<TypeTable
  type={{
    org: { type: "string", description: "Organization that owns the referenced resource." },
    kind: { type: "ApiResourceKind", description: "Kind of the referenced resource (e.g., SKILL, AGENT, MCP_SERVER).", typeDescriptionLink: "/docs/sdk/resources/commons#apiresourcekind" },
    slug: { type: "string", description: "Resource slug (user-friendly identifier, unique within org).", required: true },
    version: { type: "string", description: "Version of the resource (optional, only applicable to versioned resources like Skills)." },
  }}
/>

### ListTeamsByOrgInput

ListTeamsByOrgInput names the organization whose teams are listed.

<TypeTable
  type={{
    org: { type: "string", description: "Organization slug to list teams for." },
  }}
/>

### Teams

Teams contains a list of team resources.

<TypeTable
  type={{
    entries: { type: "TeamInput[]", description: "Team entries.", typeDescriptionLink: "#teaminput" },
  }}
/>

### Team

Team is a named group of an organization's people that access is shared with as one.

<TypeTable
  type={{
    apiVersion: { type: "string", description: "API version for this resource type." },
    kind: { type: "string", description: "Resource kind identifier." },
    metadata: { type: "ApiResourceMetadata", description: "Resource metadata including id, name, org, slug, labels, visibility, and timestamps.", typeDescriptionLink: "/docs/sdk/resources/commons#apiresourcemetadata" },
    spec: { type: "TeamInput", description: "Resource specification. See [`TeamInput`](#teaminput).", typeDescriptionLink: "#teaminput" },
    status: { type: "TeamStatus", description: "System-managed state. See `TeamStatus`." },
  }}
/>

