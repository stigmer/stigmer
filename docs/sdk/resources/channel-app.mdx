---
title: Channel App
---

{/* Auto-generated by stigmer-codegen. DO NOT EDIT. */}

A ChannelApp registers your own messaging-platform app (a Slack app or a
Meta app with WhatsApp Business access) for serving agent channels.
Channels that reference a ChannelApp via `spec.app_ref` install through
your app: for Slack this replaces the shared Stigmer app so the bot
carries your name and icon and multiple agents can serve the same
workspace; for WhatsApp a ChannelApp is the only install path. Secret
fields are encrypted at rest and redacted in responses.

```yaml
apiVersion: agentic.stigmer.ai/v1
kind: ChannelApp
metadata:
  name: Acme Support Bot
  slug: acme-support-bot
  org: acme
spec:
  slack:
    client_id: "1234567890.abcdef"
    client_secret: "8f742a..."
    signing_secret: "9c31b8..."
```

A WhatsApp ChannelApp carries the Meta app's credentials and the verify
token you configure on its webhook:

```yaml
apiVersion: agentic.stigmer.ai/v1
kind: ChannelApp
metadata:
  name: Acme WhatsApp
  slug: acme-whatsapp
  org: acme
spec:
  whatsapp:
    app_id: "735281906457812"
    app_secret: "e21c94..."
    access_token: "EAAKZB..."
    verify_token: "a-long-random-string"
```

## Client Access

<SDKTabs>
<Tab value="TypeScript">
```typescript
const channelApp = await stigmer.channelapp.get("chapp_01j5q3k7m8r2s4tnz2hfp0q0c3");
```
</Tab>
<Tab value="Go">
```go
channelApp, err := client.ChannelApp.Get(ctx, "chapp_01j5q3k7m8r2s4tnz2hfp0q0c3")
```
</Tab>
<Tab value="Python">
```python
channel_app = client.channelapps.get("chapp_01j5q3k7m8r2s4tnz2hfp0q0c3")
```
</Tab>
<Tab value="Java">
```java
ChannelApp channelApp = client.channelapps().get("chapp_01j5q3k7m8r2s4tnz2hfp0q0c3");
```
</Tab>
</SDKTabs>

## Methods

| Method | Description |
|--------|-------------|
| [`get`](#get) | Get a channel app by its unique identifier. |
| [`getByReference`](#getbyreference) | Get a channel app by its organization-scoped reference (org/slug). |
| [`listByOrg`](#listbyorg) | List all channel apps belonging to an organization. |
| [`apply`](#apply) | Create or update a channel app. |
| [`create`](#create) | Create a channel app. |
| [`update`](#update) | Update an existing channel app. |
| [`delete`](#delete) | Delete a channel app. |

### get

Get a channel app by its unique identifier.

Secret fields are redacted in the response.

<SDKTabs>
<Tab value="TypeScript">
```typescript
const channelApp = await stigmer.channelapp.get(id);
```
</Tab>
<Tab value="Go">
```go
channelApp, err := client.ChannelApp.Get(ctx, id)
```
</Tab>
<Tab value="Python">
```python
channel_app = client.channelapps.get(id)
```
</Tab>
<Tab value="Java">
```java
ChannelApp channelApp = client.channelapps().get(id);
```
</Tab>
</SDKTabs>

**Parameters**

| Parameter | Type | Description |
|-----------|------|-------------|
| `id` | `string` | Channel App identifier. |

**Returns**: [`ChannelApp`](#channelapp)

### getByReference

Get a channel app by its organization-scoped reference (org/slug).

Secret fields are redacted in the response.

<SDKTabs>
<Tab value="TypeScript">
```typescript
const channelApp = await stigmer.channelapp.getByReference({
  org: "...",
  kind: "...",
  slug: "...",
  version: "...",
});
```
</Tab>
<Tab value="Go">
```go
channelApp, err := client.ChannelApp.GetByReference(ctx, &stigmer.ApiResourceReference{
  Org:     "...",
  Kind:    "...",
  Slug:    "...",
  Version: "...",
})
```
</Tab>
<Tab value="Python">
```python
channel_app = client.channelapps.get_by_reference(ApiResourceReference(
    org="...",
    kind="...",
    slug="...",
    version="...",
))
```
</Tab>
<Tab value="Java">
```java
ChannelApp channelApp = client.channelapps().getByReference(ApiResourceReference.builder()
    .org("...")
    .kind("...")
    .slug("...")
    .version("...")
    .build());
```
</Tab>
</SDKTabs>

**Parameters**: [`ResourceRef`](#resourceref)

**Returns**: [`ChannelApp`](#channelapp)

### listByOrg

List all channel apps belonging to an organization.

Returns every ChannelApp whose metadata.org matches the input org, with secret fields redacted. Typically a small set, so results are not paginated.

<SDKTabs>
<Tab value="TypeScript">
```typescript
const channelApps = await stigmer.channelapp.listByOrg({
  org: "...",
});
```
</Tab>
<Tab value="Go">
```go
channelApps, err := client.ChannelApp.ListByOrg(ctx, &stigmer.ListChannelAppsByOrgInput{
  Org: "...",
})
```
</Tab>
<Tab value="Python">
```python
channel_apps = client.channelapps.list_by_org(ListChannelAppsByOrgInput(
    org="...",
))
```
</Tab>
<Tab value="Java">
```java
ChannelApps channelApps = client.channelapps().listByOrg(ListChannelAppsByOrgInput.builder()
    .org("...")
    .build());
```
</Tab>
</SDKTabs>

**Parameters**: [`ListChannelAppsByOrgInput`](#listchannelappsbyorginput)

**Returns**: [`ChannelApps`](#channelapps)

### apply

Create or update a channel app.

If the resource does not exist, creates a new channel app. If the resource exists, updates the existing channel app. Sending the redaction marker for a secret field preserves the stored value.

<SDKTabs>
<Tab value="TypeScript">
```typescript
const channelApp = await stigmer.channelapp.apply({
  name: "my-channel-app",
  org: "acme",
  slug: "...",
  labels: {},
  slack: {},
  whatsapp: {},
});
```
</Tab>
<Tab value="Go">
```go
channelApp, err := client.ChannelApp.Apply(ctx, &stigmer.ChannelAppInput{
  Name:     "my-channel-app",
  Org:      "acme",
  Slug:     "...",
  Labels:   map[string]string{},
  Slack:    &stigmer.SlackChannelAppConfigInput{},
  Whatsapp: &stigmer.WhatsAppChannelAppConfigInput{},
})
```
</Tab>
<Tab value="Python">
```python
channel_app = client.channelapps.apply(ChannelAppInput(
    name="my-channel-app",
    org="acme",
    slug="...",
    labels={},
    slack=None,
    whatsapp=None,
))
```
</Tab>
<Tab value="Java">
```java
ChannelApp channelApp = client.channelapps().apply(ChannelAppInput.builder()
    .name("my-channel-app")
    .org("acme")
    .slug("...")
    .labels(Map.of())
    .slack(null)
    .whatsapp(null)
    .build());
```
</Tab>
</SDKTabs>

**Parameters**: [`ChannelAppInput`](#channelappinput)

**Returns**: [`ChannelApp`](#channelapp)

### create

Create a channel app.

The creator's organization owns the channel app. The creator is granted the owner role automatically.

<SDKTabs>
<Tab value="TypeScript">
```typescript
const channelApp = await stigmer.channelapp.create({
  name: "my-channel-app",
  org: "acme",
  slug: "...",
  labels: {},
  slack: {},
  whatsapp: {},
});
```
</Tab>
<Tab value="Go">
```go
channelApp, err := client.ChannelApp.Create(ctx, &stigmer.ChannelAppInput{
  Name:     "my-channel-app",
  Org:      "acme",
  Slug:     "...",
  Labels:   map[string]string{},
  Slack:    &stigmer.SlackChannelAppConfigInput{},
  Whatsapp: &stigmer.WhatsAppChannelAppConfigInput{},
})
```
</Tab>
<Tab value="Python">
```python
channel_app = client.channelapps.create(ChannelAppInput(
    name="my-channel-app",
    org="acme",
    slug="...",
    labels={},
    slack=None,
    whatsapp=None,
))
```
</Tab>
<Tab value="Java">
```java
ChannelApp channelApp = client.channelapps().create(ChannelAppInput.builder()
    .name("my-channel-app")
    .org("acme")
    .slug("...")
    .labels(Map.of())
    .slack(null)
    .whatsapp(null)
    .build());
```
</Tab>
</SDKTabs>

**Parameters**: [`ChannelAppInput`](#channelappinput)

**Returns**: [`ChannelApp`](#channelapp)

### update

Update an existing channel app.

Sending the redaction marker for a secret field preserves the stored value; the provider arm is immutable.

<SDKTabs>
<Tab value="TypeScript">
```typescript
const channelApp = await stigmer.channelapp.update({
  name: "my-channel-app",
  org: "acme",
  slug: "...",
  labels: {},
  slack: {},
  whatsapp: {},
});
```
</Tab>
<Tab value="Go">
```go
channelApp, err := client.ChannelApp.Update(ctx, &stigmer.ChannelAppInput{
  Name:     "my-channel-app",
  Org:      "acme",
  Slug:     "...",
  Labels:   map[string]string{},
  Slack:    &stigmer.SlackChannelAppConfigInput{},
  Whatsapp: &stigmer.WhatsAppChannelAppConfigInput{},
})
```
</Tab>
<Tab value="Python">
```python
channel_app = client.channelapps.update(ChannelAppInput(
    name="my-channel-app",
    org="acme",
    slug="...",
    labels={},
    slack=None,
    whatsapp=None,
))
```
</Tab>
<Tab value="Java">
```java
ChannelApp channelApp = client.channelapps().update(ChannelAppInput.builder()
    .name("my-channel-app")
    .org("acme")
    .slug("...")
    .labels(Map.of())
    .slack(null)
    .whatsapp(null)
    .build());
```
</Tab>
</SDKTabs>

**Parameters**: [`ChannelAppInput`](#channelappinput)

**Returns**: [`ChannelApp`](#channelapp)

### delete

Delete a channel app.

Deletion is blocked while any AgentChannel references this app via spec.app_ref — disconnect or delete those channels first.

<SDKTabs>
<Tab value="TypeScript">
```typescript
const channelApp = await stigmer.channelapp.delete({ resourceId: id });
```
</Tab>
<Tab value="Go">
```go
channelApp, err := client.ChannelApp.Delete(ctx, &stigmer.DeleteResourceInput{
  ResourceID: id,
})
```
</Tab>
<Tab value="Python">
```python
channel_app = client.channelapps.delete(DeleteResourceInput(resource_id=id))
```
</Tab>
<Tab value="Java">
```java
ChannelApp channelApp = client.channelapps().delete(DeleteResourceInput.builder()
    .resourceId(id)
    .build());
```
</Tab>
</SDKTabs>

**Parameters**

| Parameter | Type | Description |
|-----------|------|-------------|
| `resourceId` | `string` | ID of the resource to delete. |
| `versionMessage` | `string` | Optional deletion reason. |
| `force` | `boolean` | Force delete even if resource is in use. |

**Returns**: [`ChannelApp`](#channelapp)

## Types

### ChannelAppInput

Input for creating or updating a Channel App.

<TypeTable
  type={{
    name: { type: "string", description: "Resource name.", required: true },
    slug: { type: "string", description: "URL-friendly identifier." },
    org: { type: "string", description: "Organization slug.", required: true },
    labels: { type: "Record<string, string>", description: "Key-value labels." },
    slack: { type: "SlackChannelAppConfigInput", description: "Slack app credentials.", typeDescriptionLink: "#slackchannelappconfiginput" },
    whatsapp: { type: "WhatsAppChannelAppConfigInput", description: "WhatsApp (Meta) app credentials.", typeDescriptionLink: "#whatsappchannelappconfiginput" },
  }}
/>

### SlackChannelAppConfigInput

SlackChannelAppConfig holds the credentials of a customer-created Slack app.

<TypeTable
  type={{
    clientId: { type: "string", description: "OAuth client ID of the Slack app." },
    clientSecret: { type: "string", description: "OAuth client secret of the Slack app." },
    signingSecret: { type: "string", description: "Signing secret used to verify Slack webhook requests from this app." },
  }}
/>

### WhatsAppChannelAppConfigInput

WhatsAppChannelAppConfig holds the credentials of a customer-created Meta app with WhatsApp Business access.

<TypeTable
  type={{
    appId: { type: "string", description: "Meta app ID, from the app dashboard's Basic Settings." },
    appSecret: { type: "string", description: "Meta app secret used to verify webhook signatures from this app." },
    accessToken: { type: "string", description: "Long-lived Cloud API access token (system-user token) used to send messages on behalf of the WhatsApp Business account." },
    verifyToken: { type: "string", description: "Verify token echoed during Meta's webhook verification handshake." },
  }}
/>

### ResourceRef

Generic reference to any API resource by org and slug.

<TypeTable
  type={{
    org: { type: "string", description: "Organization that owns the referenced resource." },
    kind: { type: "ApiResourceKind", description: "Kind of the referenced resource (e.g., SKILL, AGENT, MCP_SERVER).", typeDescriptionLink: "/docs/sdk/resources/commons#apiresourcekind" },
    slug: { type: "string", description: "Resource slug (user-friendly identifier, unique within org).", required: true },
    version: { type: "string", description: "Version of the resource (optional, only applicable to versioned resources like Skills)." },
  }}
/>

### ListChannelAppsByOrgInput

ListChannelAppsByOrgInput specifies the organization whose channel apps should be returned.

<TypeTable
  type={{
    org: { type: "string", description: "Organization slug to list channel apps for." },
  }}
/>

### ChannelApps

ChannelApps contains a list of channel app resources.

<TypeTable
  type={{
    entries: { type: "ChannelAppInput[]", description: "Channel app entries.", typeDescriptionLink: "#channelappinput" },
  }}
/>

### ChannelApp

ChannelApp registers your own messaging-platform app (e.g. a Slack app) for serving agent channels.

<TypeTable
  type={{
    apiVersion: { type: "string", description: "API version for this resource type." },
    kind: { type: "string", description: "Resource kind identifier." },
    metadata: { type: "ApiResourceMetadata", description: "Resource metadata including id, name, org, slug, labels, visibility, and timestamps.", typeDescriptionLink: "/docs/sdk/resources/commons#apiresourcemetadata" },
    spec: { type: "ChannelAppInput", description: "Resource specification. See [`ChannelAppInput`](#channelappinput).", typeDescriptionLink: "#channelappinput" },
    status: { type: "ChannelAppStatus", description: "System-managed state. See `ChannelAppStatus`." },
  }}
/>

