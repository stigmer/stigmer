---
title: Agent Runner
description: "SDK reference for Agent Runner resources."
---

{/* Auto-generated by stigmer-codegen. DO NOT EDIT. */}

AgentRunner follows the Kubernetes Node pattern: the spec is intentionally thin because the runner is infrastructure that registers itself. The user's intent when creating a runner is minimal — "I want a runner with this name." All operational state (phase, queue, machine info, capacity) is reported by the runner process via heartbeat and lives in AgentRunnerStatus.

For system-managed (ephemeral) runners created by the platform for cloud executions, the spec is typically empty — the platform fills in metadata labels (stigmer.ai/system-managed: "true") and the runner self-reports everything else.

## Client Access

<SDKTabs>
<Tab value="TypeScript">
```typescript
const agentRunner = await stigmer.agentrunner.get("arn_01j5q3k7m8r2s4tnz2hfp0q0c3");
```
</Tab>
<Tab value="Go">
```go
agentRunner, err := client.AgentRunner.Get(ctx, "arn_01j5q3k7m8r2s4tnz2hfp0q0c3")
```
</Tab>
<Tab value="Python">
```python
agent_runner = client.agentrunners.get("arn_01j5q3k7m8r2s4tnz2hfp0q0c3")
```
</Tab>
<Tab value="Java">
```java
AgentRunner agentRunner = client.agentrunners().get("arn_01j5q3k7m8r2s4tnz2hfp0q0c3");
```
</Tab>
</SDKTabs>

## Methods

| Method | Description |
|--------|-------------|
| [`get`](#get) | Get a single agent runner by ID. |
| [`getByReference`](#getbyreference) | Get an agent runner by its organization-scoped reference (org/slug). |
| [`list`](#list) | List agent runners within an organization. |
| [`apply`](#apply) | Create or update an agent runner. |
| [`create`](#create) | Create a new agent runner. |
| [`update`](#update) | Update an existing agent runner. |
| [`delete`](#delete) | Delete an agent runner. |
| [`heartbeat`](#heartbeat) | Report runner liveness and operational state. |

### get

Get a single agent runner by ID.

<SDKTabs>
<Tab value="TypeScript">
```typescript
const agentRunner = await stigmer.agentrunner.get(id);
```
</Tab>
<Tab value="Go">
```go
agentRunner, err := client.AgentRunner.Get(ctx, id)
```
</Tab>
<Tab value="Python">
```python
agent_runner = client.agentrunners.get(id)
```
</Tab>
<Tab value="Java">
```java
AgentRunner agentRunner = client.agentrunners().get(id);
```
</Tab>
</SDKTabs>

**Parameters**

| Parameter | Type | Description |
|-----------|------|-------------|
| `id` | `string` | Agent Runner identifier. |

**Returns**: [`AgentRunner`](#agentrunner)

### getByReference

Get an agent runner by its organization-scoped reference (org/slug).

Used by the CLI to resolve a runner by name: stigmer run agent my-agent --runner my-macbook resolves "my-macbook" to the full AgentRunner resource.

<SDKTabs>
<Tab value="TypeScript">
```typescript
const agentRunner = await stigmer.agentrunner.getByReference({
  org: "...",
  kind: "...",
  slug: "...",
  version: "...",
});
```
</Tab>
<Tab value="Go">
```go
agentRunner, err := client.AgentRunner.GetByReference(ctx, &stigmer.ApiResourceReference{
  Org:     "...",
  Kind:    "...",
  Slug:    "...",
  Version: "...",
})
```
</Tab>
<Tab value="Python">
```python
agent_runner = client.agentrunners.get_by_reference(ApiResourceReference(
    org="...",
    kind="...",
    slug="...",
    version="...",
))
```
</Tab>
<Tab value="Java">
```java
AgentRunner agentRunner = client.agentrunners().getByReference(ApiResourceReference.builder()
    .org("...")
    .kind("...")
    .slug("...")
    .version("...")
    .build());
```
</Tab>
</SDKTabs>

**Parameters**: [`ResourceRef`](#resourceref)

**Returns**: [`AgentRunner`](#agentrunner)

### list

List agent runners within an organization.

Supports label-based filtering for common use cases: - Session composer: exclude system-managed runners, show only READY/IDLE - Admin view: show all runners including system-managed - Debugging: filter by specific labels

<SDKTabs>
<Tab value="TypeScript">
```typescript
const result = await stigmer.agentrunner.list({
  org: "...",
  labels: {},
  pageInfo: {},
});
```
</Tab>
<Tab value="Go">
```go
result, err := client.AgentRunner.List(ctx, &stigmer.ListAgentRunnersRequest{
  Org:      "...",
  Labels:   map[string]string{},
  PageInfo: &stigmer.PageInfoInput{},
})
```
</Tab>
<Tab value="Python">
```python
result = client.agentrunners.list(ListAgentRunnersRequest(
    org="...",
    labels={},
    page_info=None,
))
```
</Tab>
<Tab value="Java">
```java
AgentRunnerList result = client.agentrunners().list(ListAgentRunnersRequest.builder()
    .org("...")
    .labels(Map.of())
    .pageInfo(null)
    .build());
```
</Tab>
</SDKTabs>

**Parameters**: [`ListAgentRunnersRequest`](#listagentrunnersrequest)

**Returns**: [`AgentRunnerList`](#agentrunnerlist)

### apply

Create or update an agent runner.

<SDKTabs>
<Tab value="TypeScript">
```typescript
const agentRunner = await stigmer.agentrunner.apply({
  name: "my-agent-runner",
  org: "acme",
  slug: "...",
  labels: {},
  description: "...",
});
```
</Tab>
<Tab value="Go">
```go
agentRunner, err := client.AgentRunner.Apply(ctx, &stigmer.AgentRunnerInput{
  Name:        "my-agent-runner",
  Org:         "acme",
  Slug:        "...",
  Labels:      map[string]string{},
  Description: "...",
})
```
</Tab>
<Tab value="Python">
```python
agent_runner = client.agentrunners.apply(AgentRunnerInput(
    name="my-agent-runner",
    org="acme",
    slug="...",
    labels={},
    description="...",
))
```
</Tab>
<Tab value="Java">
```java
AgentRunner agentRunner = client.agentrunners().apply(AgentRunnerInput.builder()
    .name("my-agent-runner")
    .org("acme")
    .slug("...")
    .labels(Map.of())
    .description("...")
    .build());
```
</Tab>
</SDKTabs>

**Parameters**: [`AgentRunnerInput`](#agentrunnerinput)

**Returns**: [`AgentRunner`](#agentrunner)

### create

Create a new agent runner.

<SDKTabs>
<Tab value="TypeScript">
```typescript
const agentRunner = await stigmer.agentrunner.create({
  name: "my-agent-runner",
  org: "acme",
  slug: "...",
  labels: {},
  description: "...",
});
```
</Tab>
<Tab value="Go">
```go
agentRunner, err := client.AgentRunner.Create(ctx, &stigmer.AgentRunnerInput{
  Name:        "my-agent-runner",
  Org:         "acme",
  Slug:        "...",
  Labels:      map[string]string{},
  Description: "...",
})
```
</Tab>
<Tab value="Python">
```python
agent_runner = client.agentrunners.create(AgentRunnerInput(
    name="my-agent-runner",
    org="acme",
    slug="...",
    labels={},
    description="...",
))
```
</Tab>
<Tab value="Java">
```java
AgentRunner agentRunner = client.agentrunners().create(AgentRunnerInput.builder()
    .name("my-agent-runner")
    .org("acme")
    .slug("...")
    .labels(Map.of())
    .description("...")
    .build());
```
</Tab>
</SDKTabs>

**Parameters**: [`AgentRunnerInput`](#agentrunnerinput)

**Returns**: [`AgentRunner`](#agentrunner)

### update

Update an existing agent runner.

<SDKTabs>
<Tab value="TypeScript">
```typescript
const agentRunner = await stigmer.agentrunner.update({
  name: "my-agent-runner",
  org: "acme",
  slug: "...",
  labels: {},
  description: "...",
});
```
</Tab>
<Tab value="Go">
```go
agentRunner, err := client.AgentRunner.Update(ctx, &stigmer.AgentRunnerInput{
  Name:        "my-agent-runner",
  Org:         "acme",
  Slug:        "...",
  Labels:      map[string]string{},
  Description: "...",
})
```
</Tab>
<Tab value="Python">
```python
agent_runner = client.agentrunners.update(AgentRunnerInput(
    name="my-agent-runner",
    org="acme",
    slug="...",
    labels={},
    description="...",
))
```
</Tab>
<Tab value="Java">
```java
AgentRunner agentRunner = client.agentrunners().update(AgentRunnerInput.builder()
    .name("my-agent-runner")
    .org("acme")
    .slug("...")
    .labels(Map.of())
    .description("...")
    .build());
```
</Tab>
</SDKTabs>

**Parameters**: [`AgentRunnerInput`](#agentrunnerinput)

**Returns**: [`AgentRunner`](#agentrunner)

### delete

Delete an agent runner.

<SDKTabs>
<Tab value="TypeScript">
```typescript
const agentRunner = await stigmer.agentrunner.delete(id);
```
</Tab>
<Tab value="Go">
```go
agentRunner, err := client.AgentRunner.Delete(ctx, id)
```
</Tab>
<Tab value="Python">
```python
agent_runner = client.agentrunners.delete(id)
```
</Tab>
<Tab value="Java">
```java
AgentRunner agentRunner = client.agentrunners().delete(id);
```
</Tab>
</SDKTabs>

**Parameters**

| Parameter | Type | Description |
|-----------|------|-------------|
| `id` | `string` | Agent Runner identifier. |

**Returns**: [`AgentRunner`](#agentrunner)

### heartbeat

Report runner liveness and operational state.

Called by the runner process every 30 seconds. Updates status fields (phase, last_heartbeat_at, current_executions, connection_info) without modifying spec or metadata.

If the runner is in PENDING or STOPPED phase, a heartbeat transitions it to the phase reported in the input (typically READY). This enables the "restart and reconnect" flow: a stopped runner resumes heartbeating and goes back to READY with the same identity and task queue.

<SDKTabs>
<Tab value="TypeScript">
```typescript
const agentRunner = await stigmer.agentrunner.heartbeat({
  agentRunnerId: "...",
  phase: "...",
  currentExecutions: 0,
  connectionInfo: {},
});
```
</Tab>
<Tab value="Go">
```go
agentRunner, err := client.AgentRunner.Heartbeat(ctx, &stigmer.AgentRunnerHeartbeatInput{
  AgentRunnerId:     "...",
  Phase:             "...",
  CurrentExecutions: 0,
  ConnectionInfo:    &stigmer.AgentRunnerConnectionInfoInput{},
})
```
</Tab>
<Tab value="Python">
```python
agent_runner = client.agentrunners.heartbeat(AgentRunnerHeartbeatInput(
    agent_runner_id="...",
    phase="...",
    current_executions=0,
    connection_info=None,
))
```
</Tab>
<Tab value="Java">
```java
AgentRunner agentRunner = client.agentrunners().heartbeat(AgentRunnerHeartbeatInput.builder()
    .agentRunnerId("...")
    .phase("...")
    .currentExecutions(0)
    .connectionInfo(null)
    .build());
```
</Tab>
</SDKTabs>

**Parameters**: [`AgentRunnerHeartbeatInput`](#agentrunnerheartbeatinput)

**Returns**: [`AgentRunner`](#agentrunner)

## Types

### AgentRunnerInput

Input for creating or updating an Agent Runner.

<TypeTable
  type={{
    name: { type: "string", description: "Resource name.", required: true },
    slug: { type: "string", description: "URL-friendly identifier." },
    org: { type: "string", description: "Organization slug.", required: true },
    labels: { type: "Record<string, string>", description: "Key-value labels." },
    description: { type: "string", description: "Human-readable purpose of this runner." },
  }}
/>

### AgentRunnerHeartbeatInput

AgentRunnerHeartbeatInput is sent by the runner process on a regular interval (default 30s) to report liveness and operational state.

<TypeTable
  type={{
    agentRunnerId: { type: "string", description: "ID of the agent runner sending the heartbeat.", required: true },
    phase: { type: "AgentRunnerPhase", description: "Current operational phase as reported by the runner.", typeDescriptionLink: "#agentrunnerphase" },
    currentExecutions: { type: "number", description: "Number of agent executions currently in progress on this runner." },
    connectionInfo: { type: "AgentRunnerConnectionInfoInput", description: "Self-reported machine information." },
  }}
/>

### ResourceRef

Generic reference to any API resource by org and slug.

<TypeTable
  type={{
    org: { type: "string", description: "Organization that owns the referenced resource." },
    kind: { type: "ApiResourceKind", description: "Kind of the referenced resource (e.g., SKILL, AGENT, MCP_SERVER).", typeDescriptionLink: "/docs/sdk/resources/commons#apiresourcekind" },
    slug: { type: "string", description: "Resource slug (user-friendly identifier, unique within org).", required: true },
    version: { type: "string", description: "Version of the resource (optional, only applicable to versioned resources like Skills)." },
  }}
/>

### ListAgentRunnersRequest

ListAgentRunnersRequest is the input for listing agent runners within an organization.

<TypeTable
  type={{
    org: { type: "string", description: "Organization slug to scope the listing." },
    labels: { type: "Record<string, string>", description: "Filter by metadata labels." },
    pageInfo: { type: "PageInfoInput", description: "Pagination options." },
  }}
/>

### AgentRunnerList

AgentRunnerList is the response for listing agent runners.

<TypeTable
  type={{
    totalCount: { type: "number", description: "Total number of agent runners matching the query." },
    items: { type: "AgentRunnerInput[]", description: "Agent runners in the current page.", typeDescriptionLink: "#agentrunnerinput" },
  }}
/>

### AgentRunner

AgentRunner represents a process that executes agent work.

<TypeTable
  type={{
    apiVersion: { type: "string", description: "API version for this resource type." },
    kind: { type: "string", description: "Resource kind identifier." },
    metadata: { type: "ApiResourceMetadata", description: "Resource metadata including id, name, org, slug, labels, visibility, and timestamps.", typeDescriptionLink: "/docs/sdk/resources/commons#apiresourcemetadata" },
    spec: { type: "AgentRunnerInput", description: "Resource specification. See [`AgentRunnerInput`](#agentrunnerinput).", typeDescriptionLink: "#agentrunnerinput" },
    status: { type: "AgentRunnerStatus", description: "System-managed state. See [`AgentRunnerStatus`](#agentrunnerstatus).", typeDescriptionLink: "#agentrunnerstatus" },
  }}
/>

### AgentRunnerStatus

AgentRunnerStatus contains all operational state for a runner.

<TypeTable
  type={{
    audit: { type: "ApiResourceAudit", description: "Standard audit information (created_at, updated_at, created_by, etc.).", typeDescriptionLink: "/docs/sdk/resources/commons#apiresourceaudit" },
    phase: { type: "AgentRunnerPhase", description: "Current operational phase of the runner process.", typeDescriptionLink: "#agentrunnerphase" },
    taskQueue: { type: "string", description: "Temporal task queue this runner polls for work." },
    lastHeartbeatAt: { type: "string", description: "Timestamp of the last successful heartbeat from the runner process." },
    startedAt: { type: "string", description: "Timestamp when this runner first transitioned to READY." },
    stoppedAt: { type: "string", description: "Timestamp when this runner last transitioned to STOPPED or FAILED." },
    currentExecutions: { type: "number", description: "Number of agent executions currently in progress on this runner." },
    sandboxId: { type: "string", description: "Sandbox ID for cloud runners that execute inside a Daytona sandbox." },
    connectionInfo: { type: "AgentRunnerConnectionInfo", description: "Self-reported information about the machine running this runner.", typeDescriptionLink: "#agentrunnerconnectioninfo" },
  }}
/>

### AgentRunnerConnectionInfo

AgentRunnerConnectionInfo contains self-reported details about the runner's host environment.

<TypeTable
  type={{
    hostname: { type: "string", description: "Hostname or user-friendly machine name." },
    os: { type: "string", description: "Operating system reported by the runner process." },
    arch: { type: "string", description: "CPU architecture reported by the runner process." },
    runnerVersion: { type: "string", description: "Version of the agent-runner software." },
  }}
/>

### AgentRunnerPhase

AgentRunnerPhase tracks the operational state of an agent runner process.

| Value | Description |
|-------|-------------|
| `AGENT_RUNNER_PHASE_PENDING` | Resource created but no heartbeat received yet. |
| `AGENT_RUNNER_PHASE_READY` | Runner is heartbeating and accepting new executions. |
| `AGENT_RUNNER_PHASE_BUSY` | Runner is heartbeating but at maximum concurrent execution capacity. |
| `AGENT_RUNNER_PHASE_STOPPED` | Runner has stopped heartbeating (timeout) or was explicitly stopped. |
| `AGENT_RUNNER_PHASE_FAILED` | Unrecoverable error. |

