---
title: "stigmer {command}"
description: "{One-sentence description of what the command does.}"
sidebar_label: "stigmer {command}"
sidebar_position: 1
tags: [cli, reference]
last_updated: 2026-01-01
---

{/*
  CLI REFERENCE TEMPLATE
  ======================
  Use this template for CLI command reference pages. These live in docs/cli/.

  Each page documents one command (or a command group like "stigmer server").
  The format is modeled after standard man pages: synopsis, description,
  options table, examples.

  Section order is mandatory. Do not reorder or skip sections.
*/}

# stigmer {command}

{/*
  One sentence describing what the command does. Match the description
  frontmatter field.
*/}

{One-sentence description.}

---

## Synopsis

{/*
  The usage pattern. Use standard CLI notation:
  - [optional] for optional arguments
  - <required> for required arguments
  - ... for repeatable arguments
  - | for alternatives
*/}

```bash
stigmer {command} [flags] <required-arg> [optional-arg]
```

---

## Description

{/*
  2-3 paragraphs explaining what the command does, when to use it,
  and how it fits into a typical workflow. Link to the relevant
  concept doc for the underlying resource.
*/}

The `stigmer {command}` command {detailed description}.

For background on {concept}, see [What is {Resource}?](../concepts/{slug}.mdx).

---

## Arguments

{/*
  Table of positional arguments. Omit this section if the command
  takes no positional arguments.
*/}

| Argument | Required | Description |
|---|---|---|
| `{arg}` | Yes | {Description} |

---

## Flags

{/*
  Table of all flags/options. Include the short form if one exists.
  Sort by importance (most-used first), not alphabetically.
*/}

| Flag | Short | Type | Default | Description |
|---|---|---|---|---|
| `--{flag}` | `-{f}` | `{type}` | `{default}` | {Description} |
| `--{flag}` | | `{type}` | `{default}` | {Description} |

### Global flags

These flags are available on all commands:

| Flag | Short | Type | Default | Description |
|---|---|---|---|---|
| `--org` | `-o` | `string` | `local` | Organization context |
| `--output` | | `string` | `text` | Output format: `text`, `json`, `yaml` |
| `--verbose` | `-v` | `bool` | `false` | Enable verbose output |

---

## Examples

{/*
  Numbered, runnable examples progressing from simple to advanced.
  Each example has a title, the command, and the expected output.
  Every example must work when pasted into a terminal.
*/}

### 1. {Simple use case}

```bash
stigmer {command} {args}
```

Output:

```text
{expected output}
```

### 2. {Common use case with flags}

```bash
stigmer {command} --{flag} {value} {args}
```

Output:

```text
{expected output}
```

### 3. {Advanced use case}

{Brief explanation of when you would use this.}

```bash
stigmer {command} --{flag} {value} --{flag2} {value2} {args}
```

---

## Exit codes

{/*
  Table of exit codes the command can return.
  Omit if the command only returns 0 (success) and 1 (error).
*/}

| Code | Meaning |
|---|---|
| `0` | Success |
| `1` | General error |
| `{N}` | {Specific error condition} |

---

## Related commands

{/*
  Commands that are commonly used before, after, or alongside this one.
*/}

- [`stigmer {related}`](../cli/{slug}.mdx) — {Brief description of relationship}
- [`stigmer {related}`](../cli/{slug}.mdx) — {Brief description of relationship}
