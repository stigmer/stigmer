---
title: What is {ResourceName}?
description: "{One-sentence summary of the resource — max 160 characters.}"
sidebar_label: "{Short sidebar label, e.g., 'Agents'}"
sidebar_position: 1
tags: [concepts]
last_updated: 2026-01-01
---

{/*
  CONCEPT TEMPLATE
  ================
  Use this template for "What is X?" documents that explain a Stigmer API
  resource or core concept. These live in docs/concepts/.

  This template formalizes the pattern established by the existing
  docs/product/what-is-*.md files.

  Section order is mandatory. Do not reorder or skip sections.
*/}

# What is {ResourceName}?

## One-sentence positioning

{/*
  A single bold sentence that positions the concept using a concrete analogy.
  Pattern: "{ResourceName} is a {what it is} — the same way {familiar analog}
  is a {what the analog is}."
*/}

**A {ResourceName} is a portable, versioned, declarative definition of {what it does} — the same way a {familiar analog} is a {what the analog does}.**

---

## Executive summary

{/*
  2-4 paragraphs that give a complete mental model of the resource.
  Cover: what it is, what it does, how it relates to other resources,
  and what separates the blueprint from the runtime (if applicable).

  Use canonical terminology from terminology.json.
  Cross-reference related concepts with relative links.
*/}

A {ResourceName} is a Stigmer API resource that...

---

## The problem {ResourceName} solves

{/*
  Start with a subsection heading that names the specific problem.
  Show a "bad code" or "bad workflow" example that illustrates the
  status quo pain. Then list the compounding problems this creates.
*/}

### {Problem statement as a heading}

Most teams handle this by...

**Typical approach:**

```python
# Bad example showing the status quo pain
```

**What goes wrong:**

- Problem 1
- Problem 2
- Problem 3

---

## What {ResourceName} provides

{/*
  Show the Stigmer solution. Include a concrete YAML example that
  a reader can paste into a file and stigmer apply.
  Use subsections for each major capability.
*/}

### {Capability 1}

```yaml
apiVersion: agentic.stigmer.ai/v1
kind: {ResourceName}
metadata:
  name: example
  org: acme-corp
spec:
  # Complete, runnable example
```

### {Capability 2}

{Description with code example.}

---

## How it works

{/*
  Architecture details. Include a diagram (Mermaid or ASCII) showing
  how this resource relates to others in the runtime.
  Use a comparison table if the resource has a layered model
  (like Agent → AgentInstance → Session → AgentExecution).
*/}

```
{Resource hierarchy or flow diagram}
```

| Layer | Analogy | What it does |
|---|---|---|
| {Layer 1} | {Analog} | {Description} |

---

## How it compares

{/*
  A two-column comparison table: "Without Stigmer {ResourceName}" vs
  "With Stigmer {ResourceName}". Focus on concrete operational
  differences, not marketing claims.
*/}

| Without Stigmer | With Stigmer |
|---|---|
| {Pain point} | {Solution} |

---

## Getting started

{/*
  A minimal, runnable example: 3-5 bash commands that create and
  use the resource. This is not the quickstart — it is a teaser
  that gives the reader immediate hands-on confidence.
*/}

```bash
# 1. Create the resource
stigmer apply -f example.yaml

# 2. Use it
stigmer run example "Hello"

# 3. Verify
stigmer list {resources}
```

---

## Further reading

{/*
  Relative links to related docs. Group by category:
  - Core concepts (other what-is-*.mdx)
  - Guides (how-to docs)
  - Reference (API/CLI docs)
  - Architecture (deep dives)
*/}

- [Related Concept](../concepts/related.mdx) — Brief description
- [Related Guide](../guides/related.mdx) — Brief description
- [Architecture Deep Dive](../architecture/related.mdx) — Brief description
