---
title: "{Task description}"
description: "How to {accomplish task} in Stigmer."
sidebar_label: "{Short label}"
sidebar_position: 1
tags: [guides]
last_updated: 2026-01-01
---

{/*
  HOW-TO GUIDE TEMPLATE
  =====================
  Use this template for task-oriented documents that show how to accomplish
  a specific goal. These live in docs/guides/.

  How-to guides differ from quickstarts: quickstarts assume no prior setup
  and target brand-new users. How-to guides assume the reader has a working
  Stigmer installation and wants to accomplish a specific task.

  How-to guides differ from concept docs: concept docs explain *what* and *why*.
  How-to guides explain *how*.

  Section order is mandatory. Do not reorder or skip sections.
*/}

# {Task description}

{/*
  One paragraph (2-3 sentences) stating what the reader will accomplish
  and why this task matters. Link to the relevant concept doc for background.
*/}

This guide shows you how to {task}. For background on {concept}, see [What is {Resource}?](../concepts/{slug}.mdx).

---

## Prerequisites

{/*
  List what the reader needs before starting. Be specific about versions
  and state. Unlike quickstarts, you can assume Stigmer is installed.
*/}

- A running Stigmer server (`stigmer server status` returns OK)
- {Prerequisite 1}
- {Prerequisite 2}

---

## Overview

{/*
  A brief (3-5 sentence) explanation of the approach. If the process
  has multiple stages, list them here so the reader knows what to expect.
  Include a Mermaid diagram for multi-step flows.
*/}

The process involves {number} steps:

1. {Step 1 summary}
2. {Step 2 summary}
3. {Step 3 summary}

```mermaid
flowchart LR
    A[Step 1] --> B[Step 2] --> C[Step 3] --> D[Done]
```

---

## {Step 1: Action verb} {thing}

{/*
  Each major step is an H2 with format "{Action verb} {object}".
  Include the command or code, a 1-2 sentence explanation, and
  the expected output.
*/}

{Explanation of what this step does and why.}

```bash
{command or code}
```

Expected output:

```text
{output}
```

---

## {Step 2: Action verb} {thing}

{Explanation.}

```yaml
{configuration or code example}
```

{Explanation of key fields or parameters.}

---

## {Step 3: Action verb} {thing}

{Explanation.}

```bash
{command}
```

---

## Verify the result

{/*
  Show the reader how to confirm the task completed successfully.
  This section is mandatory.
*/}

Verify that {thing} is working:

```bash
{verification command}
```

You should see:

```text
{expected output}
```

---

## Troubleshooting

{/*
  List 2-3 common issues the reader might encounter during this task.
  Use a consistent format: symptom → cause → fix.
  If there are no known issues, omit this section.
*/}

### {Symptom}

**Cause:** {Why this happens.}

**Fix:** {How to resolve it.}

```bash
{fix command if applicable}
```

---

## Related guides

- [{Related guide}](../guides/{slug}.mdx) — {Brief description}
- [{Related concept}](../concepts/{slug}.mdx) — {Brief description}
