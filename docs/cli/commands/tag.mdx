---
title: stigmer tag
description: "assign a tag to a resource version (supported types: workflow)"
---

{/* Auto-generated by gen-cli-docs. Do not edit manually. */}
{/* To enrich this page, edit the template in client-apps/cli-ts/docs/commands/ */}

Assign a tag to a resource version.

`stigmer tag` attaches a named tag to a specific version of a resource,
identified by its content hash. Tags give a stable, human-friendly handle to a
particular version (for example `stable` or `v1`). Workflows are the supported
resource type today.

## Usage

```bash
stigmer tag <type> <org/slug> <hash> <tag>
```

### Common patterns

```bash
# Tag a workflow version as stable
stigmer tag workflow acme-corp/deploy-pipeline a1b2c3d stable

# Tag a version as v1
stigmer tag workflow acme-corp/deploy-pipeline a1b2c3d v1
```

## Examples

### Promote a version to "stable"

```bash
stigmer tag workflow acme-corp/deploy-pipeline a1b2c3d stable
```

The tag now points at the version with hash `a1b2c3d`. Re-tagging moves the tag
to a new version.

## Global Flags

| Flag            | Type     | Default | Description                                            |
| --------------- | -------- | ------- | ------------------------------------------------------ |
| `--api-key`     | `string` |         | API key for authentication                             |
| `--debug`, `-d` | `bool`   |         | enable debug output                                    |
| `--org`         | `string` |         | organization slug override                             |
| `--standalone`  | `bool`   |         | ignore the config file; use flags and environment only |

## Related commands

| Command                    | Description                         |
| -------------------------- | ----------------------------------- |
| [`stigmer get`](./get)     | Inspect a resource and its versions |
| [`stigmer list`](./list)   | List resources of a type            |
| [`stigmer apply`](./apply) | Create or update resource versions  |

## See also

- [Command Reference](./) — All available commands
