---
title: stigmer tag
description: Assign a tag to a resource version
---

{/* Auto-generated by gen-cli-docs. Do not edit manually. */}
{/* To enrich this page, create an enrichment template in client-apps/cli/cmd/stigmer/root/docs/ */}

Assign or move a tag to a specific version of a versioned resource.

Tags are mutable pointers — assigning an existing tag to a new version moves it
from the previous version. A version can have at most one tag.

Supported resource types: workflow

## Usage

```bash
stigmer tag <type> <org/slug> <hash> <tag>
```

## Global Flags

| Flag            | Type     | Default | Description                                                     |
| --------------- | -------- | ------- | --------------------------------------------------------------- |
| `--api-key`     | `string` |         | API key for cloud authentication (overrides stored token)       |
| `--debug`, `-d` | `bool`   |         | enable debug mode with detailed logs                            |
| `--org`         | `string` |         | organization slug (overrides context)                           |
| `--standalone`  | `bool`   |         | ignore config file; all config must come from flags or env vars |

## Examples

```bash
# Tag a workflow version as stable
stigmer tag workflow my-org/my-workflow abc123def456 stable

# Tag a workflow version for release
stigmer tag workflow my-org/my-workflow abc123def456 v1.0
```

## See also

- [Command Reference](./) — all available commands
