---
title: stigmer reset
description: Reset local environment to a fresh state
---

{/* Auto-generated by gen-cli-docs. Do not edit manually. */}
{/* To enrich this page, create an enrichment template in client-apps/cli/cmd/stigmer/root/docs/ */}

Stop all services and remove all persistent state — the SQLite database, skill
artifacts, sessions, runtimes, Temporal state, logs, and downloaded binaries —
then automatically restart the server fresh.

Configuration (config.yaml) is preserved by default. Use --include-config to
also remove it; the setup wizard will run again on next start.

## Usage

```bash
stigmer reset [flags]
```

## Options

| Flag               | Type   | Default | Description                                               |
| ------------------ | ------ | ------- | --------------------------------------------------------- |
| `--force`          | `bool` |         | Skip confirmation prompt                                  |
| `--include-config` | `bool` |         | Also remove configuration (API keys, backend preferences) |

## Global Flags

| Flag            | Type     | Default | Description                                               |
| --------------- | -------- | ------- | --------------------------------------------------------- |
| `--api-key`     | `string` |         | API key for cloud authentication (overrides stored token) |
| `--debug`, `-d` | `bool`   |         | enable debug mode with detailed logs                      |
| `--org`         | `string` |         | organization slug (overrides context)                     |

## Examples

```bash
# Reset and restart
stigmer reset

# Reset without confirmation prompt
stigmer reset --force

# Full reset including configuration
stigmer reset --include-config
```

## See also

- [Command Reference](./) — all available commands
