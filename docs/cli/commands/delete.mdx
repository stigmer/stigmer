---
title: stigmer delete
description: Delete a resource by type and reference
---

{/* Auto-generated by gen-cli-docs. Do not edit manually. */}

Delete a resource by type and name or ID.

The type can be specified using any alias:

- agent, agt, agents
- workflow, wf, workflows
- mcpserver, mcp, mcp-server
- project, proj, projects
- skill, skills
- execution, exec (cancels running execution)

The reference can be:

- Resource ID (e.g., agt_abc123, aex_xyz789)
- Slug (e.g., my-agent) - not for executions
- Org/slug (e.g., stigmer/my-agent) - not for executions

WARNING: This operation is permanent and cannot be undone. For executions, this
gracefully cancels the running agent.

## Usage

```bash
stigmer delete <type> <name-or-id> [flags]
```

## Flags

| Flag            | Type   | Default | Description                             |
| --------------- | ------ | ------- | --------------------------------------- |
| `--force`, `-f` | `bool` |         | skip confirmation prompt                |
| `--json`        | `bool` |         | output result as JSON                   |
| `--quiet`, `-q` | `bool` |         | suppress output, print status line only |

## Global Flags

| Flag            | Type     | Default | Description                                               |
| --------------- | -------- | ------- | --------------------------------------------------------- |
| `--api-key`     | `string` |         | API key for cloud authentication (overrides stored token) |
| `--debug`, `-d` | `bool`   |         | enable debug mode with detailed logs                      |
| `--org`         | `string` |         | organization slug (overrides context)                     |

## Examples

```bash
# Delete agent by slug
stigmer delete agent my-agent

# Delete workflow by ID
stigmer delete workflow wfl_abc123

# Cancel a running execution
stigmer delete execution aex_01abc123

# Force delete (skip confirmation)
stigmer delete agent my-agent --force
```
