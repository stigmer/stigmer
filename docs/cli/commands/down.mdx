---
title: stigmer down
description: Stop Stigmer services
---

{/* Auto-generated by gen-cli-docs. Do not edit manually. */}
{/* To enrich this page, create an enrichment template in client-apps/cli/cmd/stigmer/root/docs/ */}

Stop all running Stigmer services.

Stops the daemon process and all managed components (Temporal, stigmer-server,
runner, web console).

Use 'stigmer down server' to stop only the control plane.

## Usage

```bash
stigmer down [flags]
```

## Options

| Flag            | Type   | Default | Description                             |
| --------------- | ------ | ------- | --------------------------------------- |
| `--json`        | `bool` |         | output result as JSON                   |
| `--quiet`, `-q` | `bool` |         | suppress output, print status line only |

## Global Flags

| Flag            | Type     | Default | Description                                                     |
| --------------- | -------- | ------- | --------------------------------------------------------------- |
| `--api-key`     | `string` |         | API key for cloud authentication (overrides stored token)       |
| `--debug`, `-d` | `bool`   |         | enable debug mode with detailed logs                            |
| `--org`         | `string` |         | organization slug (overrides context)                           |
| `--standalone`  | `bool`   |         | ignore config file; all config must come from flags or env vars |

## Examples

```bash
# Stop all services
stigmer down
```

## Subcommands

### stigmer down server

Stop the Stigmer control plane and all managed processes (Temporal,
stigmer-server).

```bash
stigmer down server [flags]
```

| Flag            | Type   | Default | Description                             |
| --------------- | ------ | ------- | --------------------------------------- |
| `--json`        | `bool` |         | output result as JSON                   |
| `--quiet`, `-q` | `bool` |         | suppress output, print status line only |

```bash
stigmer down server
```

## See also

- [Command Reference](./) — all available commands
