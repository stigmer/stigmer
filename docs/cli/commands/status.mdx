---
title: stigmer status
description: show the status of the local Stigmer stack
---

{/* Auto-generated by gen-cli-docs. Do not edit manually. */}
{/* To enrich this page, edit the template in client-apps/cli/docs/commands/ */}

Show the status of the local Stigmer stack.

`stigmer status` reports the health of each component the local daemon
supervises — the daemon itself, managed Temporal, `stigmer-server`, and the
runner — alongside the effective LLM configuration. Use it to confirm the stack
is up before running agents, or to diagnose a stack that will not start.

## Usage

```bash
stigmer status [flags]
```

### Common patterns

```bash
# Show component health and LLM configuration
stigmer status

# Machine-readable status for scripts
stigmer status --json
```

## Options

| Flag      | Type   | Default | Description                |
| --------- | ------ | ------- | -------------------------- |
| `--json`  | `bool` |         | emit the result as JSON    |
| `--quiet` | `bool` |         | print only the status line |

## Examples

### Check the stack before running an agent

```bash
stigmer status
```

Shows whether each component is running and which LLM provider is configured.

### Use status in a script

```bash
stigmer status --json | jq '.sections'
```

## Global Flags

| Flag            | Type     | Default | Description                                            |
| --------------- | -------- | ------- | ------------------------------------------------------ |
| `--api-key`     | `string` |         | API key for authentication                             |
| `--debug`, `-d` | `bool`   |         | enable debug output                                    |
| `--org`         | `string` |         | organization slug override                             |
| `--standalone`  | `bool`   |         | ignore the config file; use flags and environment only |

## Related commands

| Command                  | Description                    |
| ------------------------ | ------------------------------ |
| [`stigmer up`](./up)     | Start the local stack          |
| [`stigmer down`](./down) | Stop the local stack           |
| [`stigmer logs`](./logs) | View logs from the local stack |

## See also

- [Command Reference](./) — All available commands
