---
title: stigmer validate
description: Validate resource YAML files
---

{/* Auto-generated by gen-cli-docs. Do not edit manually. */}

Validate resource YAML files without applying them.

Checks that the file contains valid YAML with correct schema. Supports Agent,
Workflow, McpServer, and Project resources.

The resource type is auto-detected from the 'kind' field in the YAML. Supports
single files, directories, and multi-document YAML.

## Usage

```bash
stigmer validate [flags]
```

## Flags

| Flag           | Type     | Default | Description                               |
| -------------- | -------- | ------- | ----------------------------------------- |
| `--file`, `-f` | `string` |         | path to YAML file or directory (required) |

## Global Flags

| Flag            | Type     | Default | Description                                               |
| --------------- | -------- | ------- | --------------------------------------------------------- |
| `--api-key`     | `string` |         | API key for cloud authentication (overrides stored token) |
| `--debug`, `-d` | `bool`   |         | enable debug mode with detailed logs                      |
| `--org`         | `string` |         | organization slug (overrides context)                     |

## Examples

```bash
# Validate a single file
stigmer validate -f agent.yaml
stigmer validate -f workflow.yaml

# Validate all YAML files in a directory
stigmer validate -f ./manifests/

# Validate a project configuration
stigmer validate -f stigmer.yaml
```
