---
title: stigmer auth
description: Manage authentication with Stigmer Cloud
---

{/* Auto-generated by gen-cli-docs. Do not edit manually. */}

Authenticate with the Stigmer Cloud platform using browser-based PKCE OAuth.

The auth command provides subcommands to log in, log out, and check your current
authentication status.

## Usage

```bash
stigmer auth
```

## Global Flags

| Flag            | Type     | Default | Description                                               |
| --------------- | -------- | ------- | --------------------------------------------------------- |
| `--api-key`     | `string` |         | API key for cloud authentication (overrides stored token) |
| `--debug`, `-d` | `bool`   |         | enable debug mode with detailed logs                      |
| `--org`         | `string` |         | organization slug (overrides context)                     |

## Examples

```bash
# Login to Stigmer Cloud
stigmer auth login

# Check who you're logged in as
stigmer auth whoami

# Logout from Stigmer Cloud
stigmer auth logout
```

## Subcommands

### stigmer auth login

Authenticate with Stigmer Cloud using browser-based PKCE OAuth flow.

This command will:

1. Open your default web browser to the Stigmer login page
2. Wait for you to complete authentication
3. Save your access token to ~/.stigmer/config.yaml
4. Set the backend to cloud mode

```bash
stigmer auth login
```

```bash
stigmer auth login
```

### stigmer auth logout

Clear your authentication token and logout from Stigmer Cloud.

You will need to run 'stigmer auth login' again to use cloud commands.

```bash
stigmer auth logout [flags]
```

| Flag            | Type   | Default | Description                             |
| --------------- | ------ | ------- | --------------------------------------- |
| `--json`        | `bool` |         | output result as JSON                   |
| `--quiet`, `-q` | `bool` |         | suppress output, print status line only |

```bash
stigmer auth logout
```

### stigmer auth whoami

Show information about the currently authenticated user.

Fetches account details from the Stigmer Cloud backend using the stored
authentication token.

```bash
stigmer auth whoami [flags]
```

| Flag            | Type   | Default | Description                             |
| --------------- | ------ | ------- | --------------------------------------- |
| `--json`        | `bool` |         | output result as JSON                   |
| `--quiet`, `-q` | `bool` |         | suppress output, print status line only |

```bash
stigmer auth whoami
```
