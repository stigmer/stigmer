---
title: Set Variables
description: "Assign values to workflow state variables using expressions."
---

{/* Autogenerated by stigmer-codegen. DO NOT EDIT. */}

SetTaskConfig defines the configuration for set_vars tasks that assign variables
in workflow state.

## Kind

`set_vars`

## YAML Example

```yaml
- initialize:
    set:
      variables:
        status: "pending"
        timestamp: "${ now() }"
        request_id: "${ $context.input.id }"
```

## task_config Fields

<TypeTable
  type={{
    variables: {
      type: "Record<string, string>",
      description: "Variables to set in workflow state.",
      required: true,
    },
  }}
/>

## Output Shape

```json
{
  "description": "set_vars mutates workflow context directly; it does not produce task output.",
  "type": "null"
}
```

## See Also

- [Workflow Authoring Guide](/docs/guides/workflows)
- [Task Type Overview](/docs/guides/workflow-tasks)
- [Workflow Patterns](/docs/guides/workflow-patterns)
