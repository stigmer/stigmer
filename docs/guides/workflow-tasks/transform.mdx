---
title: Transform
description:
  "Deterministic data transformation using JQ, JSONata, or template engines."
---

{/* Autogenerated by stigmer-codegen. DO NOT EDIT. */}

TransformTaskConfig defines the configuration for transform tasks that perform
deterministic data transformation without LLM calls.

## Kind

`transform`

## YAML Examples

```yaml
- build_api_payload:
    transform:
      engine: jq
      expression:
        "{name: .customer.full_name, severity: .triage.severity, summary:
        .agent_analysis.structured.summary}"
      input: "${ $context }"
    export:
      as: "${ . }"
```

```yaml
- render_message:
    transform:
      engine: template
      expression: "Ticket {{ .ticket_id }} classified as {{ .severity }}"
      input: "${ $context.build_api_payload }"
    export:
      as: "${ . }"
```

## task_config Fields

<TypeTable
  type={{
    engine: {
      type: "TransformEngine",
      description: "Transformation engine to use.",
      typeDescriptionLink: "#transformengine",
      required: true,
    },
    expression: {
      type: "string",
      description: "Transformation expression in the chosen engine's syntax.",
      required: true,
    },
    input: {
      type: "string",
      description:
        "Expression selecting the input data for the transformation.",
    },
  }}
/>

## Enums

### TransformEngine

| Value                       | Description |
| --------------------------- | ----------- |
| `TRANSFORM_ENGINE_JQ`       |             |
| `TRANSFORM_ENGINE_JSONATA`  |             |
| `TRANSFORM_ENGINE_TEMPLATE` |             |

## Output Shape

```json
{
  "description": "Result of applying the transformation expression to the input data.",
  "type": "object"
}
```

## See Also

- [Workflow Authoring Guide](/docs/guides/workflows)
- [Task Type Overview](/docs/guides/workflow-tasks)
- [Workflow Patterns](/docs/guides/workflow-patterns)
