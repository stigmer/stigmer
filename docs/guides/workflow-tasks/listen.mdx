---
title: Listen
description: "Wait for external signals or events before proceeding."
---

{/* Autogenerated by stigmer-codegen. DO NOT EDIT. */}

ListenTaskConfig defines the configuration for listen tasks that wait for
external signals.

## Kind

`listen`

## YAML Example

```yaml
- wait_for_approval:
    listen:
      to:
        mode: "one"
        signals:
          - id: "approval_signal"
            type: "signal"
    export:
      as: "${ . }"
```

## task_config Fields

<TypeTable
  type={{
    to: {
      type: "ListenTo",
      description: "Signal listening configuration.",
      typeDescriptionLink: "#listento",
      required: true,
    },
  }}
/>

## Nested Types

### ListenTo

ListenTo defines what signals to listen for.

<TypeTable
  type={{
    mode: {
      type: "string",
      description:
        'Listening mode: - "one": Wait for any one signal - "all": Wait for all signals.',
      required: true,
    },
    signals: {
      type: "SignalSpec[]",
      description: "Signals to listen for.",
      typeDescriptionLink: "#signalspec",
    },
  }}
/>

### SignalSpec

SignalSpec defines a signal/event to listen for.

<TypeTable
  type={{
    id: { type: "string", description: "Signal identifier.", required: true },
    type: {
      type: "string",
      description: 'Signal type: "signal", "query", or "update".',
      required: true,
    },
  }}
/>

## Output Shape

```json
{
  "description": "Signal payload received from the external source.",
  "type": "object"
}
```

## See Also

- [Workflow Authoring Guide](/docs/guides/workflows)
- [Task Type Overview](/docs/guides/workflow-tasks)
- [Workflow Patterns](/docs/guides/workflow-patterns)
