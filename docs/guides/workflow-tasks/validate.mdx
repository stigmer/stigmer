---
title: Validate
description: "Schema and business-rule validation checkpoint for workflow data."
---

{/* Autogenerated by stigmer-codegen. DO NOT EDIT. */}

ValidateTaskConfig defines the configuration for validate tasks that perform
explicit schema and business-rule validation on workflow data.

## Kind

`validate`

## YAML Example

```yaml
- check_triage_quality:
    validate:
      input: "${ $context.triage }"
      schema:
        type: object
        required: [severity, category, customer_impact]
        properties:
          severity:
            type: string
            enum: [low, medium, high, critical]
          category:
            type: string
            minLength: 1
          customer_impact:
            type: boolean
      rules:
        - name: critical_needs_rationale
          expression:
            "${ .severity != 'critical' || (.rationale != null && .rationale !=
            '') }"
          message: "Critical severity requires a rationale"
      on_fail: VALIDATION_FAIL_BRANCH
      fallback_task: human_review
    export:
      as: "${ . }"
```

## task_config Fields

<TypeTable
  type={{
    input: {
      type: "string",
      description: "Expression selecting the data to validate.",
      required: true,
    },
    schema: {
      type: "object",
      description: "JSON Schema to validate the input data against.",
    },
    rules: {
      type: "ValidationRule[]",
      description: "Business rules to evaluate against the input data.",
      typeDescriptionLink: "#validationrule",
    },
    on_fail: {
      type: "ValidationFailPolicy",
      description: "Policy applied when validation fails.",
      typeDescriptionLink: "#validationfailpolicy",
    },
    fallback_task: {
      type: "string",
      description:
        "Target task to branch to when on_fail is VALIDATION_FAIL_BRANCH.",
    },
  }}
/>

## Nested Types

### ValidationRule

ValidationRule defines a business rule evaluated as a boolean expression.

<TypeTable
  type={{
    name: {
      type: "string",
      description: "Rule identifier for error reporting and debugging.",
      required: true,
    },
    expression: {
      type: "string",
      description:
        "Boolean expression that must evaluate to true for the rule to pass.",
      required: true,
    },
    message: {
      type: "string",
      description:
        "Error message included in validation output when the rule fails.",
    },
  }}
/>

## Enums

### ValidationFailPolicy

| Value                    | Description |
| ------------------------ | ----------- |
| `VALIDATION_FAIL_RAISE`  |             |
| `VALIDATION_FAIL_BRANCH` |             |
| `VALIDATION_FAIL_WARN`   |             |

## Output Shape

```json
{
  "description": "Validation result with pass/fail status and error details.",
  "properties": {
    "data": {
      "description": "The original input data that was validated.",
      "type": "object"
    },
    "errors": {
      "description": "List of validation errors (empty when valid is true).",
      "items": {
        "properties": {
          "message": {
            "type": "string"
          },
          "path": {
            "type": "string"
          },
          "rule": {
            "type": "string"
          }
        },
        "type": "object"
      },
      "type": "array"
    },
    "valid": {
      "description": "Whether all schema and rule validations passed.",
      "type": "boolean"
    }
  },
  "type": "object"
}
```

## See Also

- [Workflow Authoring Guide](/docs/guides/workflows)
- [Task Type Overview](/docs/guides/workflow-tasks)
- [Workflow Patterns](/docs/guides/workflow-patterns)
