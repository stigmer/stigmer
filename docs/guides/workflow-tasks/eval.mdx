---
title: Evaluate (LLM Judge)
description:
  "Assess semantic quality, correctness, safety, or completeness of content
  using an LLM judge."
---

{/* Autogenerated by stigmer-codegen. DO NOT EDIT. */}

EvalTaskConfig defines the configuration for eval tasks that use an LLM judge to
assess the semantic quality of workflow data.

## Kind

`eval`

## YAML Examples

```yaml
- check_summary_quality:
    call: eval
    with:
      model: "gpt-4o"
      subject: "${ $context.summarize.text }"
      rubric: |
        Evaluate whether this summary accurately captures the key points
        of the source document without hallucinations or omissions.
      scoring_mode: EVAL_PASS_FAIL
      on_fail: EVAL_FAIL_BRANCH
      fallback_task: regenerate_summary
    export:
      as: "${ . }"
```

```yaml
- score_translation:
    call: eval
    with:
      model: "gpt-4o"
      subject: "${ $context.translate.text }"
      rubric: |
        Rate the quality of this translation on accuracy, fluency,
        and preservation of meaning.
      scoring_mode: EVAL_NUMERIC_SCORE
      threshold: 0.7
      on_fail: EVAL_FAIL_WARN
    export:
      as: "${ . }"
```

```yaml
- evaluate_response:
    call: eval
    with:
      model: "gpt-4o"
      subject: "${ $context.agent_response }"
      rubric: "Evaluate this customer support response."
      scoring_mode: EVAL_MULTI_CRITERIA
      threshold: 0.75
      criteria:
        - name: accuracy
          description: "Is the information factually correct?"
          weight: 3.0
        - name: helpfulness
          description: "Does it address the customer's actual question?"
          weight: 2.0
        - name: tone
          description: "Is the tone professional and empathetic?"
          weight: 1.0
      on_fail: EVAL_FAIL_RAISE
    export:
      as: "${ . }"
```

## task_config Fields

<TypeTable
  type={{
    model: {
      type: "string",
      description: "LLM model used as the judge.",
      required: true,
    },
    subject: {
      type: "string",
      description: "Expression selecting the data to evaluate.",
      required: true,
    },
    rubric: {
      type: "string",
      description: "Evaluation criteria in natural language.",
      required: true,
    },
    scoring_mode: {
      type: "EvalScoringMode",
      description: "How the judge scores the subject.",
      typeDescriptionLink: "#evalscoringmode",
    },
    threshold: {
      type: "number",
      description: "Pass threshold for numeric_score and multi_criteria modes.",
    },
    on_fail: {
      type: "EvalFailPolicy",
      description: "Policy applied when the subject fails evaluation.",
      typeDescriptionLink: "#evalfailpolicy",
    },
    fallback_task: {
      type: "string",
      description: "Target task to branch to when on_fail is EVAL_FAIL_BRANCH.",
    },
    system_prompt: {
      type: "string",
      description: "Optional custom system prompt override for the judge LLM.",
    },
    criteria: {
      type: "EvalCriterion[]",
      description: "Evaluation criteria for multi_criteria mode.",
      typeDescriptionLink: "#evalcriterion",
    },
    max_cost_micros: {
      type: "number",
      description: "Per-task cost cap in micro-USD (1 USD = 1,000,000 micros).",
    },
  }}
/>

## Nested Types

### EvalCriterion

EvalCriterion defines a single evaluation dimension for multi-criteria mode.

<TypeTable
  type={{
    name: {
      type: "string",
      description: "Name of this evaluation dimension.",
      required: true,
    },
    description: {
      type: "string",
      description: "Description of what this criterion measures.",
      required: true,
    },
    weight: {
      type: "number",
      description:
        "Relative weight for computing the weighted average overall score.",
    },
  }}
/>

## Enums

### EvalScoringMode

| Value                 | Description |
| --------------------- | ----------- |
| `EVAL_PASS_FAIL`      |             |
| `EVAL_NUMERIC_SCORE`  |             |
| `EVAL_MULTI_CRITERIA` |             |

### EvalFailPolicy

| Value              | Description |
| ------------------ | ----------- |
| `EVAL_FAIL_RAISE`  |             |
| `EVAL_FAIL_BRANCH` |             |
| `EVAL_FAIL_WARN`   |             |

## Output Shape

```json
{
  "description": "Evaluation result with pass/fail, score, and judge reasoning.",
  "properties": {
    "criteria": {
      "description": "Per-criterion scores and reasoning. Present only for multi_criteria mode.",
      "items": {
        "properties": {
          "name": {
            "type": "string"
          },
          "reasoning": {
            "type": "string"
          },
          "score": {
            "type": "number"
          }
        },
        "type": "object"
      },
      "type": "array"
    },
    "model_used": {
      "description": "The LLM model that served as judge.",
      "type": "string"
    },
    "pass": {
      "description": "Whether the subject passed evaluation.",
      "type": "boolean"
    },
    "reasoning": {
      "description": "The judge's explanation of its assessment.",
      "type": "string"
    },
    "score": {
      "description": "Numeric score (0.0-1.0). Present for numeric_score and multi_criteria modes.",
      "type": "number"
    },
    "subject": {
      "description": "The original subject data that was evaluated."
    }
  },
  "type": "object"
}
```

## See Also

- [Workflow Authoring Guide](/docs/guides/workflows)
- [Task Type Overview](/docs/guides/workflow-tasks)
- [Workflow Patterns](/docs/guides/workflow-patterns)
