---
title: Emit Event
description:
  "Publish a CloudEvents-formatted event for external consumers or other
  workflows."
---

{/* Autogenerated by stigmer-codegen. DO NOT EDIT. */}

EmitEventTaskConfig defines the configuration for emit_event tasks that publish
CloudEvents to external consumers, other workflows, audit trails, and metrics
systems.

## Kind

`emit_event`

## YAML Examples

```yaml
- name: notify_classified
  kind: emit_event
  task_config:
    event:
      type: "stigmer.workflow.ticket.classified"
      subject: "${ $context.ticket.id }"
      data:
        ticket_id: "${ $context.ticket.id }"
        severity: "${ $context.triage.severity }"
        category: "${ $context.triage.category }"
  export:
    as: "${ . }"
```

```yaml
- name: notify_downstream
  kind: emit_event
  task_config:
    event:
      type: "acme.order.fulfilled"
      subject: "${ $context.order.number }"
      data:
        order_id: "${ $context.order.id }"
    delivery:
      - webhook:
          url: "https://hooks.acme.com/orders"
          headers:
            Authorization: "Bearer ${.secrets.ACME_HOOK_TOKEN}"
      - signal:
          execution_id: "${ .start_shipping.execution_id }"
          signal_name: "order-fulfilled"
```

## task_config Fields

<TypeTable
  type={{
    event: {
      type: "EmitEventSpec",
      description: "The event specification to emit.",
      typeDescriptionLink: "#emiteventspec",
      required: true,
    },
    delivery: {
      type: "EmitDeliveryTarget[]",
      description: "Delivery targets for the emitted event (optional).",
      typeDescriptionLink: "#emitdeliverytarget",
    },
  }}
/>

## Nested Types

### EmitEventSpec

EmitEventSpec defines the CloudEvents envelope for an event to be emitted.

<TypeTable
  type={{
    type: {
      type: "string",
      description: "CloudEvents type identifier.",
      required: true,
    },
    source: { type: "string", description: "CloudEvents source identifier." },
    data: { type: "object", description: "Event payload data." },
    subject: { type: "string", description: "CloudEvents subject identifier." },
  }}
/>

### EmitDeliveryTarget

EmitDeliveryTarget selects one destination for an emitted event.

<TypeTable
  type={{
    webhook: {
      type: "WebhookDelivery",
      description: "POST the CloudEvents envelope to an HTTP endpoint.",
      typeDescriptionLink: "#webhookdelivery",
    },
    signal: {
      type: "SignalDelivery",
      description: "Signal another workflow execution's listen task.",
      typeDescriptionLink: "#signaldelivery",
    },
  }}
/>

### WebhookDelivery

WebhookDelivery posts the CloudEvents envelope to an external HTTP endpoint with
Content-Type: application/cloudevents+json.

<TypeTable
  type={{
    url: {
      type: "string",
      description: "Endpoint URL to POST the event to.",
      required: true,
    },
    headers: {
      type: "Record<string, string>",
      description: "HTTP headers to send with the POST (optional).",
    },
  }}
/>

### SignalDelivery

SignalDelivery routes the CloudEvents envelope to another workflow execution as
a signal, completing the emit/listen pairing: the target execution receives the
envelope on the listen task whose signal id matches signal_name.

<TypeTable
  type={{
    execution_id: {
      type: "string",
      description:
        'Target workflow execution id ("wfx_..."), as returned by run/create.',
      required: true,
    },
    signal_name: {
      type: "string",
      description:
        "Signal name, matching the target's listen task event id (verbatim).",
      required: true,
    },
  }}
/>

## Output Shape

```json
{
  "description": "Fully-resolved CloudEvents envelope with runtime-generated fields.",
  "properties": {
    "data": {
      "type": "object"
    },
    "datacontenttype": {
      "description": "Always 'application/json'.",
      "type": "string"
    },
    "delivery_errors": {
      "description": "Present only when at least one delivery target failed. One entry per failed target; delivery is best-effort and never fails the task.",
      "items": {
        "type": "string"
      },
      "type": "array"
    },
    "id": {
      "description": "Generated UUID for this event.",
      "type": "string"
    },
    "source": {
      "type": "string"
    },
    "specversion": {
      "description": "Always '1.0'.",
      "type": "string"
    },
    "subject": {
      "type": "string"
    },
    "time": {
      "description": "ISO 8601 timestamp of emission.",
      "type": "string"
    },
    "type": {
      "type": "string"
    }
  },
  "type": "object"
}
```

## See Also

- [Workflow Authoring Guide](/docs/guides/workflows/authoring)
- [Task Type Overview](/docs/guides/workflows/task-types)
- [Workflow Patterns](/docs/guides/workflows/patterns)
