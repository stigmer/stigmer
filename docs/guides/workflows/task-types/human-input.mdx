---
title: Human Input
description:
  "Workflow-level approval gate that pauses execution for human review or input."
---

{/* Autogenerated by stigmer-codegen. DO NOT EDIT. */}

HumanInputTaskConfig defines the configuration for human_input tasks that pause
workflow execution to collect typed input or approval from a human reviewer,
then resume based on the reviewer's response.

## Kind

`human_input`

## YAML Example

```yaml
- manager_approval:
    human_input:
      prompt: "Approve escalation for ${ $context.triage.severity } incident?"
      form_schema:
        type: object
        properties:
          notes:
            type: string
            description: "Optional notes for the engineering team"
          priority_override:
            type: string
            enum: [P1, P2, P3]
      outcomes:
        - name: approve
          label: "Approve Escalation"
        - name: deny
          label: "Reject"
          then: re_classify
      approvers:
        - "team:engineering-leads"
      timeout: 86400
      on_timeout: HUMAN_INPUT_TIMEOUT_ESCALATE
      notification_channels:
        - "slack:#incident-approvals"
    export:
      as: "${ . }"
```

## task_config Fields

<TypeTable
  type={{
    prompt: {
      type: "string",
      description:
        "Message shown to the reviewer explaining what needs approval or input.",
      required: true,
    },
    form_schema: {
      type: "object",
      description:
        "JSON Schema defining the input form rendered to the reviewer.",
    },
    outcomes: {
      type: "HumanInputOutcome[]",
      description: "Named outcomes that the reviewer can select.",
      typeDescriptionLink: "#humaninputoutcome",
    },
    approvers: {
      type: "string[]",
      description:
        "Approver identifiers — who is allowed to respond to this task.",
    },
    timeout: {
      type: "number",
      description: "Timeout in seconds before the on_timeout policy applies.",
    },
    on_timeout: {
      type: "HumanInputTimeoutPolicy",
      description:
        "Policy applied when the timeout expires without a response.",
      typeDescriptionLink: "#humaninputtimeoutpolicy",
    },
    notification_channels: {
      type: "string[]",
      description:
        "Channel identifiers for sending approval request notifications.",
    },
  }}
/>

## Nested Types

### HumanInputOutcome

HumanInputOutcome defines a named outcome that a reviewer can select when
responding to a human_input task.

<TypeTable
  type={{
    name: {
      type: "string",
      description: "Outcome identifier used in task output and routing.",
      required: true,
    },
    label: {
      type: "string",
      description:
        "Human-readable label for the button/action in the approval UI.",
    },
    then: {
      type: "string",
      description: "Task to branch to when this outcome is selected.",
    },
  }}
/>

## Enums

### HumanInputTimeoutPolicy

| Value                          | Description |
| ------------------------------ | ----------- |
| `HUMAN_INPUT_TIMEOUT_FAIL`     |             |
| `HUMAN_INPUT_TIMEOUT_APPROVE`  |             |
| `HUMAN_INPUT_TIMEOUT_DENY`     |             |
| `HUMAN_INPUT_TIMEOUT_ESCALATE` |             |

## Output Shape

```json
{
  "description": "Reviewer response including selected outcome and form data.",
  "properties": {
    "form_data": {
      "description": "Validated form response (when form_schema is configured).",
      "type": "object"
    },
    "outcome": {
      "description": "Selected outcome name (e.g., 'approve', 'deny', 'needs_revision').",
      "type": "string"
    },
    "responded_at": {
      "description": "ISO 8601 timestamp of the response.",
      "type": "string"
    },
    "reviewer": {
      "description": "Identity of the user who responded.",
      "type": "string"
    }
  },
  "type": "object"
}
```

## See Also

- [Workflow Authoring Guide](/docs/guides/workflows/authoring)
- [Task Type Overview](/docs/guides/workflows/task-types)
- [Workflow Patterns](/docs/guides/workflows/patterns)
