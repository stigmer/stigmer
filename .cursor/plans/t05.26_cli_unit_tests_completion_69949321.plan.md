---
name: T05.26 CLI Unit Tests Completion
overview: Complete the remaining 3 test suites (Agent CRUD, Workflow CRUD, Display & Integration) plus final verification to achieve comprehensive CLI test coverage. This continues the world-class testing foundation established in Suites 1-2.
todos:
  - id: suite3-agent-applier-fix
    content: Fix agent/applier.go Conn type from *grpc.ClientConn to grpc.ClientConnInterface for testability
    status: completed
  - id: suite3-agent-applier
    content: Create agent/applier_test.go (~300 lines, ~15 tests) - validation, dry-run, metadata tests
    status: completed
  - id: suite3-agent-get
    content: Create agent/get_test.go (~200 lines, ~12 tests) - reference routing, validation tests
    status: completed
  - id: suite3-agent-delete
    content: Create agent/delete_test.go (~250 lines, ~12 tests) - delete validation, structure tests
    status: completed
  - id: suite3-agent-display
    content: Create agent/display_test.go (~350 lines, ~18 tests) - display functions, truncateString tests
    status: completed
  - id: suite3-agent-build
    content: Update agent/BUILD.bazel to register new test files
    status: completed
  - id: suite4-workflow-applier-fix
    content: Fix workflow/applier.go Conn type from *grpc.ClientConn to grpc.ClientConnInterface
    status: completed
  - id: suite4-workflow-tests
    content: Create workflow/ test files (applier_test.go, get_test.go, delete_test.go, display_test.go) mirroring agent patterns
    status: completed
  - id: suite4-workflow-build
    content: Update workflow/BUILD.bazel to register new test files
    status: completed
  - id: suite5-project-display
    content: Create project/display_test.go (~300 lines, ~15 tests) - helper functions, format routing
    status: completed
  - id: suite5-project-build
    content: Update project/BUILD.bazel to register display_test.go
    status: completed
  - id: verification
    content: Run bazel test //client-apps/cli/... and bazel build //client-apps/cli/... to verify all tests pass
    status: completed
isProject: false
---

# T05.26: CLI Unit Tests - Remaining Work Plan

## Current Status

**Completed (Suites 1-2):**

- Suite 1: MCP Server Package (40 tests, ~897 lines)
- Suite 2: Config Package (45 tests, ~843 lines)

**Remaining (Suites 3-5):**

- Suite 3: Agent CRUD Tests
- Suite 4: Workflow CRUD Tests
- Suite 5: Display & Integration Tests
- Final Verification

---

## Suite 3: Agent CRUD Tests

**Target Package:** `client-apps/cli/internal/cli/agent`

**Files to Create:**

### 3.1 - agent/applier_test.go (~300 lines, ~15 tests)

Following the pattern from [mcpserver/applier_test.go](client-apps/cli/internal/cli/mcpserver/applier_test.go) and [project/applier_test.go](client-apps/cli/internal/cli/project/applier_test.go):

**Test Categories:**

- ApplyOptions Validation (nil agent, nil conn, empty orgID)
- DryRun Mode (returns without RPC, preserves agent)
- Metadata Population (sets org when empty, preserves existing org)
- ApplyResult Structure (Created flag, Agent field)
- Validation Order (correct error priority)

**Key Implementation:**

- Use `mockConn` struct pattern from project tests
- Test constants: `testOrgID`, `testAgentID`, `testAgentName`
- Helper function: `createTestAgent()` returning valid agent proto

**Conn Type Fix Required:**

```go
// applier.go line 23 - currently uses *grpc.ClientConn
// Should be grpc.ClientConnInterface for testability
Conn *grpc.ClientConn  // Change to: grpc.ClientConnInterface
```

### 3.2 - agent/get_test.go (~200 lines, ~12 tests)

Following the pattern from [project/get_test.go](client-apps/cli/internal/cli/project/get_test.go):

**Test Categories:**

- Options Validation (nil opts, nil conn, empty reference)
- Reference Type Detection (ID format `agt_xxx`, org/slug, slug-only)
- GetFromBackend error wrapping
- GetOptions struct fields

**Table-Driven Tests:**

```go
func TestGetOptions_ValidatesAllFields(t *testing.T) { ... }
func TestGetFromBackend_ReferenceFormats(t *testing.T) { ... }
```

### 3.3 - agent/delete_test.go (~250 lines, ~12 tests)

Following the pattern from [project/delete_test.go](client-apps/cli/internal/cli/project/delete_test.go):

**Test Categories:**

- DeleteOptions Validation (nil opts, nil conn, empty agentID)
- DeleteFromBackend Validation
- DeleteResult Structure
- Validation Order Tests

### 3.4 - agent/display_test.go (~350 lines, ~18 tests)

**Functions Under Test:**

- `DisplayApplyResult(result *ApplyResult)`
- `DisplayDeleteResult(result *DeleteResult)`
- `DisplayDeleteConfirmation(agent *agentv1.Agent)`
- `DisplayAgentPreview(agent *agentv1.Agent)`
- `DisplayGetResult(agent *agentv1.Agent, format string)`
- `displayAgentSummary(agent *agentv1.Agent)` - internal helper
- `truncateString(s string, maxLen int)` - utility function

**Test Categories:**

- DisplayApplyResult (created vs updated messages)
- DisplayDeleteResult (success message)
- DisplayDeleteConfirmation (warning display)
- DisplayGetResult (table/yaml/json formats)
- truncateString (edge cases: short, exact, long, very short maxLen)

**Testing Approach:**

- Cannot easily capture stdout - test for no panics with valid/nil inputs
- Test truncateString directly (pure function)
- Test format routing logic

**BUILD.bazel Updates:**

```starlark
go_test(
    name = "agent_test",
    srcs = [
        "applier_test.go",  # NEW
        "delete_test.go",   # NEW
        "display_test.go",  # NEW
        "get_test.go",      # NEW
        "loader_test.go",
        "validator_test.go",
    ],
    ...
)
```

---

## Suite 4: Workflow CRUD Tests

**Target Package:** `client-apps/cli/internal/cli/workflow`

**Files to Create (mirror Agent structure):**

### 4.1 - workflow/applier_test.go (~300 lines, ~15 tests)

Same structure as agent/applier_test.go with workflow-specific types:

- `*workflowv1.Workflow` instead of `*agentv1.Agent`
- ID prefix `wfl_` instead of `agt_`
- WorkflowCommandController instead of AgentCommandController

**Conn Type Fix Required:**

```go
// applier.go line 23 - currently uses *grpc.ClientConn
// Should be grpc.ClientConnInterface for testability
Conn *grpc.ClientConn  // Change to: grpc.ClientConnInterface
```

### 4.2 - workflow/get_test.go (~200 lines, ~12 tests)

Same structure as agent/get_test.go with workflow-specific error messages.

### 4.3 - workflow/delete_test.go (~250 lines, ~12 tests)

Same structure as agent/delete_test.go with WorkflowID field.

### 4.4 - workflow/display_test.go (~350 lines, ~18 tests)

Same structure as agent/display_test.go with workflow-specific functions:

- `DisplayWorkflowPreview()` instead of `DisplayAgentPreview()`
- `displayWorkflowSummary()` shows tasks count instead of skills/mcp servers

**BUILD.bazel Updates:**

```starlark
go_test(
    name = "workflow_test",
    srcs = [
        "applier_test.go",  # NEW
        "delete_test.go",   # NEW
        "display_test.go",  # NEW
        "get_test.go",      # NEW
        "loader_test.go",
        "validator_test.go",
    ],
    ...
)
```

---

## Suite 5: Display & Integration Tests

### 5.1 - project/display_test.go (~300 lines, ~15 tests)

**Functions Under Test:**

- `DisplayProjectInfo(project, format)`
- `DisplayGetResult(project, format)`
- `DisplayDeleteResult(result)`
- `DisplayDeleteConfirmation(project)`
- `DisplayProjectPreview(project)`
- `DisplayValidationSuccess(project, sourcePath)`
- `runtimeToString(runtime)`
- `getDefaultEntryPoint(runtime)`
- `truncateString(s, maxLen)`

**Test Categories:**

- Format routing (table/yaml/json)
- Helper functions (runtimeToString, getDefaultEntryPoint, truncateString)
- Nil input handling
- Resource counts display

**BUILD.bazel Updates:**

```starlark
go_test(
    name = "project_test",
    srcs = [
        "applier_test.go",
        "delete_test.go",
        "detect_test.go",
        "display_test.go",  # NEW
        "get_test.go",
        "loader_test.go",
        "validator_test.go",
    ],
    ...
)
```

### 5.2 - Apply Package Enhancements (~150 lines, ~10 tests)

**File:** `client-apps/cli/internal/cli/apply/skill_verify_test.go` (enhance existing)

**Additional Tests:**

- `VerifyExternalSkills()` gRPC integration tests
- `checkSkillExists()` backend verification tests
- `DisplayMissingSkillsGuidance()` output tests

---

## Code Fixes Required

### Agent applier.go Conn Type Fix

```go
// client-apps/cli/internal/cli/agent/applier.go line 23
// Change from:
Conn *grpc.ClientConn
// Change to:
Conn grpc.ClientConnInterface
```

### Workflow applier.go Conn Type Fix

```go
// client-apps/cli/internal/cli/workflow/applier.go line 23
// Change from:
Conn *grpc.ClientConn
// Change to:
Conn grpc.ClientConnInterface
```

---

## Test Quality Standards

**Non-Negotiable:**

- Section headers with `// =====` separators
- Test constants at file top
- Helper functions for creating test protos
- Table-driven tests for validation scenarios
- No panics on nil inputs
- All tests pass on first run
- Files under 500 lines
- Functions under 50 lines

**Mock Pattern:**

```go
type mockConn struct {
    grpc.ClientConnInterface
}
```

---

## Implementation Order

1. **Agent applier.go fix** - Change Conn type to interface
2. **agent/applier_test.go** - Core apply tests
3. **agent/get_test.go** - Reference routing tests
4. **agent/delete_test.go** - Delete validation tests
5. **agent/display_test.go** - Display function tests
6. **agent/BUILD.bazel update** - Register new test files
7. **Workflow applier.go fix** - Change Conn type to interface
8. **workflow/applier_test.go** - Mirror agent tests
9. **workflow/get_test.go** - Mirror agent tests
10. **workflow/delete_test.go** - Mirror agent tests
11. **workflow/display_test.go** - Mirror agent tests
12. **workflow/BUILD.bazel update** - Register new test files
13. **project/display_test.go** - Helper function tests
14. **project/BUILD.bazel update** - Register new test file
15. **Final verification** - Run full test suite

---

## Build Verification Commands

```bash
# After each suite:
bazel test //client-apps/cli/internal/cli/agent:agent_test
bazel test //client-apps/cli/internal/cli/workflow:workflow_test
bazel test //client-apps/cli/internal/cli/project:project_test

# Final verification:
bazel test //client-apps/cli/...
bazel build //client-apps/cli/...
```

---

## Expected Deliverables

**Files Created:**

- `client-apps/cli/internal/cli/agent/applier_test.go`
- `client-apps/cli/internal/cli/agent/get_test.go`
- `client-apps/cli/internal/cli/agent/delete_test.go`
- `client-apps/cli/internal/cli/agent/display_test.go`
- `client-apps/cli/internal/cli/workflow/applier_test.go`
- `client-apps/cli/internal/cli/workflow/get_test.go`
- `client-apps/cli/internal/cli/workflow/delete_test.go`
- `client-apps/cli/internal/cli/workflow/display_test.go`
- `client-apps/cli/internal/cli/project/display_test.go`

**Files Modified:**

- `client-apps/cli/internal/cli/agent/applier.go` (Conn type fix)
- `client-apps/cli/internal/cli/agent/BUILD.bazel`
- `client-apps/cli/internal/cli/workflow/applier.go` (Conn type fix)
- `client-apps/cli/internal/cli/workflow/BUILD.bazel`
- `client-apps/cli/internal/cli/project/BUILD.bazel`

**Metrics:**

- ~153 new test methods
- ~2,250 lines of test code
- Agent: 30% to 90%+ coverage
- Workflow: 35% to 90%+ coverage
- Project: 70% to 95%+ coverage