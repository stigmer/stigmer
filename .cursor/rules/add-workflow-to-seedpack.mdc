---
description: Action rule to add a new workflow to the seedpack. Guides YAML creation, env declaration, validation with the CLI and the static tests, and an execution arm in the conformance suite when the workflow is offline-drivable.
alwaysApply: false
---

# Rule: Add Workflow to Seedpack

Purpose: When triggered, guide the complete process of adding a new workflow to `seedpack/workflows/`. This includes creating a structurally valid YAML definition, validating it with the CLI and the seedpack package's static suite, and adding a conformance execution arm when the workflow can be driven offline.

Usage: Invoke explicitly as `@add-workflow-to-seedpack`.

## Prerequisites

Before starting, gather from the user:
1. **Workflow name** (lowercase, hyphenated identifier — becomes filename and `metadata.name`)
2. **What it does** (high-level pipeline description)
3. **Task kinds used** (llm_call, human_input, transform, switch_case, fork, etc.)
4. **Environment variables** needed at runtime (and which are optional)
5. **Human approval gates** (which tasks pause for human input)
6. **LLM providers** required (Anthropic, OpenAI, or both — determines test skip logic)

## Step 1: Create the Workflow YAML

Create `seedpack/workflows/{name}.yaml`.

### Template

```yaml
apiVersion: agentic.stigmer.ai/v1
kind: Workflow
metadata:
  name: {name}
  visibility: visibility_public
  labels:
    stigmer.ai/system: "true"
spec:
  description: >
    {Multi-line description of what the workflow does and which task kinds
    it demonstrates.}
  document:
    dsl: "1.0.0"
    namespace: stigmer
    name: {name}
    version: "1.0.0"
  env:
    {ENV_VAR_NAME}:
      description: "{What this variable provides}"
    {OPTIONAL_VAR}:
      description: "{Description (mention it's optional)}"
      optional: true
  budget:
    max_cost_micros: {cost_limit}
    max_duration_seconds: {timeout}
  tasks:
    - name: {task_name}
      kind: {task_kind}
      task_config:
        {kind-specific config}
      export:
        as: "${ . }"
      flow:
        then: {next_task_name}
```

### CRITICAL: EnvVarDeclaration Schema

The `env` map values ONLY support these fields:
- `description` (string) — what the variable is for
- `optional` (bool) — true means not required; default false means required
- `is_secret` (bool) — true means encrypted at rest

**DO NOT USE**: `required`, `default`, `type`, or any other field. The CLI uses strict proto parsing (`DiscardUnknown: false`) and will reject unknown fields.

### Valid Task Kinds (19 total)

| Kind | Purpose |
|------|---------|
| `set_vars` | Set workflow variables |
| `http_call` | Make HTTP requests |
| `grpc_call` | Make gRPC calls |
| `activity_call` | Call Temporal activities |
| `switch_case` | Conditional branching |
| `for_each` | Iterate over collections |
| `fork` | Parallel execution branches |
| `try_catch` | Error handling |
| `listen` | Wait for external events |
| `wait` | Time-based delays |
| `raise_error` | Throw workflow errors |
| `run_workflow` | Invoke sub-workflows |
| `agent_call` | Delegate to agents |
| `llm_call` | Call LLM models |
| `transform` | JQ/expression transforms |
| `human_input` | Human approval gates |
| `validate` | Validate data shapes |
| `emit_event` | Publish events |
| `notification` | Send notifications |

### Validation Rules

- Every task needs `name`, `kind`, and `task_config`
- Task names must be unique within the workflow (C-style identifiers: `[a-z][a-z0-9_]*`)
- `flow.then` must reference a valid task name or be absent (implicit end)
- The task graph must be acyclic (no circular `flow.then` chains)
- `${ ... }` expressions reference exported values from upstream tasks

## Step 2: Validate the YAML

The Go parse/execution tests that once lived in `test/integration` retired with the Java service and the Go harness (stigmer#1024, #1031). Validation is the CLI and the seedpack package's own static suite (`seedpack/src/__tests__`, Vitest):

```bash
# Dry-run apply (the CLI's loader and validator parse the file exactly as a user's apply would)
stigmer apply -f seedpack/workflows/{name}.yaml --org stigmer --dry-run

# Offline validation of the resource YAML (the CLI's own validator, no server needed)
stigmer validate -f seedpack/workflows/{name}.yaml

# The seedpack package's static suite (every YAML has a kind; the MCP catalog policies)
make test-seedpack-static
```

## Step 3: Add an Execution Arm (when the workflow is offline-drivable)

An execution test for a seedpack workflow is a conformance execution arm: `test/conformance/src/suites-execution/` on the `local-execution` target, which boots Temporal, the OSS server and the runner from source and scripts every model turn on the mock LLM. Load the seedpack YAML in the arm, `workflowCommand.create` it, create a `WorkflowExecution`, and assert per-task status with `awaitTaskStatus` (`support/workflowexecutions.ts`) — see `workflowexecution-llm-call.conformance.test.ts` and `workflowexecution-eval.conformance.test.ts` for the shape. A workflow whose tasks need a live vendor (an `http_call` to a real API with a real credential) has no offline arm; its live check is the credentialed canary lane on the conformance harness (stigmer#1033).


## Step 4: Validate

Run these commands to verify:

```bash
# 1. Dry-run apply (confirms CLI parsing passes)
stigmer apply -f seedpack/workflows/{name}.yaml --org stigmer --dry-run

# 2. The seedpack package's static suite
make test-seedpack-static

# 3. If you added an execution arm
cd test/conformance && CONFORMANCE_TARGET=local-execution npx vitest run -c vitest.execution.config.ts src/suites-execution/<your-file>.test.ts
```


## Quality Checklist

Before considering the addition complete:

- [ ] `env` declarations use ONLY `description`, `optional`, `is_secret` (no `required`, no `default`)
- [ ] All task kinds are from the valid 19-kind set
- [ ] Task names are unique C-style identifiers
- [ ] `flow.then` targets reference valid task names
- [ ] No cycles in the task flow graph
- [ ] `${ ... }` expressions reference exported upstream values
- [ ] `stigmer apply -f --dry-run` passes; `make test-seedpack-static` green
- [ ] An execution arm in `test/conformance/src/suites-execution/` when the workflow is offline-drivable

## Reference Files

- Workflow proto: `apis/ai/stigmer/agentic/workflow/v1/spec.proto`
- EnvVarDeclaration proto: `apis/ai/stigmer/agentic/environment/v1/spec.proto`
- Existing workflows: `seedpack/workflows/*.yaml`
- Execution arm shape: `test/conformance/src/suites-execution/workflowexecution-llm-call.conformance.test.ts`, `support/workflows.ts`
- CLI apply/validate: `client-apps/cli/src/commands/apply.ts`, `client-apps/cli/src/commands/validate.ts`
